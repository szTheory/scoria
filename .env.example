# Copy to .env (gitignored) and fill in. Only needed for the LLM critique pass.
#
#   docker compose --profile shots run --rm critique
#   # or natively:  set -a; . ./.env; set +a; mix scoria.ui.shots --critique
#
# The screenshot-only pass needs NO key.
ANTHROPIC_API_KEY=sk-ant-...
