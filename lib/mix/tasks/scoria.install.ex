defmodule Mix.Tasks.Scoria.Install do
  use Mix.Task

  @shortdoc "Installs Scoria dashboard into a Phoenix application"

  def run(_args) do
    # Find router and tailwind config in cwd
    # For now, just a stub
  end

  def do_run(_router_path, _tailwind_path) do
    # Stub
  end
end
