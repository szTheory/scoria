---
phase: 61-proof-and-stability-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/install/contract.ex
  - lib/scoria/install/report.ex
  - lib/mix/tasks/scoria.install.ex
  - test/scoria/install/report_test.exs
autonomous: true
requirements: [INST-08]
must_haves:
  truths:
    - "Planner summary atoms remain canonical (create/update/no_op/manual_review) while operator vocabulary is projected separately (D-06, D-07)."
    - "Human and JSON outputs expose operator-ordered counts: would_change, already_present, skipped, manual_review (D-08)."
    - "JSON adds summary_operator additively with schema_version string 1.0; SCORIA_CHECK_RESULT trailer unchanged (D-09, D-10)."
    - "Install.Contract is SSOT for trailer prefix, default verify_command, and operator projection rules (D-25)."
  artifacts:
    - path: "lib/scoria/install/contract.ex"
      provides: "Installer contract SSOT parallel to VerificationLanes"
      contains: "defmodule Scoria.Install.Contract"
    - path: "lib/scoria/install/report.ex"
      provides: "Operator projection layer and summary_operator in JSON"
      contains: "summary_operator"
    - path: "lib/mix/tasks/scoria.install.ex"
      provides: "Ecto-style moduledoc for three install modes"
      contains: "@moduledoc"
    - path: "test/scoria/install/report_test.exs"
      provides: "Unit tests for operator projection permutations"
      contains: "optional_surface_absent"
  key_links:
    - from: "lib/scoria/install/contract.ex"
      to: "lib/scoria/install/report.ex"
      via: "Report imports Contract for projection keys and trailer constants"
      pattern: "Scoria.Install.Contract"
    - from: "lib/scoria/install/report.ex"
      to: "test/scoria/install/report_test.exs"
      via: "projection rules verified without subprocess"
      pattern: "summary_operator"
---

<objective>
Establish the installer contract SSOT and operator projection layer so preview/check/apply summaries speak operator truth without changing planner semantics from phases 59–60.

Purpose: Foundation for INST-08 proof tests and doc pins — operator vocabulary, additive JSON, stable automation trailer.
Output: `Scoria.Install.Contract`, updated `Report` with `summary_operator`, `@moduledoc` on `Mix.Tasks.Scoria.Install`.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/61-proof-and-stability-closeout/61-CONTEXT.md
@.planning/phases/61-proof-and-stability-closeout/61-RESEARCH.md
@.planning/phases/61-proof-and-stability-closeout/61-PATTERNS.md
@.planning/phases/61-proof-and-stability-closeout/61-VALIDATION.md
@lib/scoria/install/report.ex
@lib/scoria/verification_lanes.ex
@lib/mix/tasks/scoria.install.ex

Locked decisions D-06 through D-12 and D-25 are mandatory. Do not change planner classification logic or ApplyExecutor behavior in this plan.
</context>

<tasks>

<task type="auto">
  <name>Task 61-01-01: Create Scoria.Install.Contract SSOT with operator projection rules</name>
  <requirement_ids>INST-08</requirement_ids>
  <files>lib/scoria/install/contract.ex</files>
  <read_first>
    .planning/phases/61-proof-and-stability-closeout/61-CONTEXT.md
    .planning/phases/61-proof-and-stability-closeout/61-RESEARCH.md
    lib/scoria/verification_lanes.ex
    lib/scoria/install/report.ex
  </read_first>
  <action>Create `lib/scoria/install/contract.ex` with module `Scoria.Install.Contract` mirroring the VerificationLanes SSOT style. Export: `trailer_prefix/0` returning `"SCORIA_CHECK_RESULT"`, `default_verify_command/0` returning `"mix scoria.install --check"`, `planner_classifications/0` returning `[:create, :update, :no_op, :manual_review]`, `operator_summary_keys/0` returning `[:would_change, :already_present, :skipped, :manual_review]`, `schema_version/0` returning `"1.0"`, and `project_entry/3` accepting `{classification, drift_reason_code, mode}` where mode is `:dry_run | :check | :apply`. Implement projection rules exactly: `no_op` + `"optional_surface_absent"` → `:skipped`; `no_op` otherwise → `:already_present`; `create` or `update` in `:dry_run` or `:check` → `:would_change`; `create` or `update` in `:apply` → `:already_present` (post-apply semantics for summary only); `manual_review` → `:manual_review`. Add `project_summary/2` that folds a list of entries into counts keyed by operator buckets.</action>
  <acceptance_criteria>
    - `rg -n "defmodule Scoria\\.Install\\.Contract" lib/scoria/install/contract.ex` returns one match.
    - `rg -n "def trailer_prefix" lib/scoria/install/contract.ex` returns `"SCORIA_CHECK_RESULT"`.
    - `rg -n "def default_verify_command" lib/scoria/install/contract.ex` returns `"mix scoria.install --check"`.
    - `rg -n "optional_surface_absent" lib/scoria/install/contract.ex` returns skipped projection branch.
    - `rg -n "would_change|already_present|skipped|manual_review" lib/scoria/install/contract.ex` returns all four operator keys.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <done>Install.Contract exposes trailer prefix, verify command, and deterministic operator projection rules as code SSOT.</done>
</task>

<task type="auto">
  <name>Task 61-01-02: Wire operator projection into Report human/JSON renderers</name>
  <requirement_ids>INST-08</requirement_ids>
  <files>lib/scoria/install/report.ex, test/scoria/install/report_test.exs</files>
  <read_first>
    lib/scoria/install/contract.ex
    lib/scoria/install/report.ex
    lib/scoria/install/planner.ex
    .planning/phases/61-proof-and-stability-closeout/61-CONTEXT.md
  </read_first>
  <action>Update `Scoria.Install.Report` to alias `Scoria.Install.Contract`. Add public `project_operator_summary/2` delegating to Contract. Change `render_human/2` summary section to print operator-ordered lines: `would_change: N`, `already_present: N`, `skipped: N`, `manual_review: N` (keep entry-level `classification:` lines using existing planner labels). Update `normalize_plan_for_json/1` to set `schema_version` via `Contract.schema_version/0` (string `"1.0"`) and add `"summary_operator"` map with string keys matching operator buckets. Preserve existing `summary` map with planner atom keys unchanged. Keep `trailer_line/1` and `check_result/1` behavior identical. Create `test/scoria/install/report_test.exs` with unit tests: (1) entry with `classification: :no_op, drift: %{reason_code: "optional_surface_absent"}` projects to `skipped`; (2) entry with `classification: :create` in dry_run mode projects to `would_change`; (3) entry with `classification: :manual_review` projects to `manual_review`; (4) JSON payload includes `"summary_operator"` and `"schema_version": "1.0"` while `"summary"` still has planner keys.</action>
  <acceptance_criteria>
    - `rg -n "summary_operator" lib/scoria/install/report.ex` returns additive JSON field.
    - `rg -n "would_change:" lib/scoria/install/report.ex` returns operator-ordered human summary line.
    - `rg -n "def project_operator_summary" lib/scoria/install/report.ex` returns public API.
    - `rg -n "optional_surface_absent" test/scoria/install/report_test.exs` returns skipped projection test.
    - `MIX_ENV=test mix test test/scoria/install/report_test.exs` exits 0.
    - `MIX_ENV=test mix test test/mix/tasks/scoria.install_check_test.exs` exits 0 (trailer contract preserved).
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/install/report_test.exs test/mix/tasks/scoria.install_check_test.exs</automated>
  </verify>
  <done>Human and JSON reports expose operator vocabulary additively while planner summary and check trailer remain backward compatible.</done>
</task>

<task type="auto">
  <name>Task 61-01-03: Add Ecto-style moduledoc to Mix.Tasks.Scoria.Install</name>
  <requirement_ids>INST-08</requirement_ids>
  <files>lib/mix/tasks/scoria.install.ex</files>
  <read_first>
    lib/mix/tasks/scoria.install.ex
    lib/scoria/install/contract.ex
    docs/operator_verification.md
  </read_first>
  <action>Add `@moduledoc` to `Mix.Tasks.Scoria.Install` documenting three modes: default apply, `--dry-run` (no writes), `--check` (no writes, tri-state exit). Include exit/trailer table: compliant=0, drift/manual_review=1, error=2 with exact trailer format `SCORIA_CHECK_RESULT status=&lt;status&gt; exit_code=&lt;code&gt;`. State that `--check` never writes and `manual_review` never silent overwrite. Point to `docs/operator_verification.md` for upgrade-safe workflow. Use `Contract.default_verify_command/0` in doc example rather than hardcoding if referenced.</action>
  <acceptance_criteria>
    - `rg -n "@moduledoc" lib/mix/tasks/scoria.install.ex` returns moduledoc block.
    - `rg -n "dry-run|--check|SCORIA_CHECK_RESULT" lib/mix/tasks/scoria.install.ex` returns mode and trailer documentation.
    - `rg -n "never writes" lib/mix/tasks/scoria.install.ex` returns no-write guarantee for check mode.
    - `mix help scoria.install` output includes "dry-run" and "SCORIA_CHECK_RESULT".
  </acceptance_criteria>
  <verify>
    <automated>mix help scoria.install | rg -n "dry-run|SCORIA_CHECK_RESULT"</automated>
  </verify>
  <done>mix help scoria.install documents three modes, exit semantics, and operator doc pointer.</done>
</task>

</tasks>

<threat_model>
## Security Gate

- Enforcement: enabled
- ASVS level: 1
- Block threshold: high severity threats

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner artifact -> operator projection | Projection must never downgrade `manual_review` to a write-eligible bucket. |
| JSON machine output -> CI parsers | Additive `summary_operator` must not remove or rename existing planner summary keys. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-61-01 | Tampering | high | `Scoria.Install.Contract.project_entry/3` | block | Block if `manual_review` entries map to `would_change` or `already_present`. | `test/scoria/install/report_test.exs` manual_review projection case. |
| T-61-02 | Information Disclosure | medium | `Report.render_json/2` | mitigate | Keep planner `summary` intact; only add `summary_operator`; schema_version as string `"1.0"`. | JSON structure tests in report_test.exs. |
| T-61-03 | Repudiation | medium | trailer contract | block | Do not alter `trailer_line/1` or `check_result/1` semantics. | `mix test test/mix/tasks/scoria.install_check_test.exs` green. |

High-severity threat T-61-01 is a hard blocker for Plan 61-01 completion.
</threat_model>

<verification>
After all tasks:

```bash
MIX_ENV=test mix test test/scoria/install/report_test.exs
MIX_ENV=test mix test test/mix/tasks/scoria.install_check_test.exs
mix help scoria.install | rg "SCORIA_CHECK_RESULT"
MIX_ENV=test mix compile --warnings-as-errors
```
</verification>

## PLANNING COMPLETE
