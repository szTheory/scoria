---
phase: 61-proof-and-stability-closeout
plan: 02
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - test/support/scoria/host_install_fixtures.ex
  - test/mix/tasks/scoria.install_check_test.exs
  - test/mix/tasks/scoria.install_test.exs
  - test/scoria/install/mode_equivalence_test.exs
autonomous: true
requirements: [INST-08]
must_haves:
  truths:
    - "One shared HostInstallFixtures module serves all subprocess installer tests; no third parallel fixture builder (D-01)."
    - "Six fixture classes exist: compliant, drift, manual_review, error, root_list_form_browser, non_root_browser_only, plus owned_apply_host and optional_surface_absent (D-02, D-04)."
    - "Same fixture yields identical entries and summary between dry_run and check modes (D-11)."
    - "B-cycle on owned host proves no preview writes and apply→apply idempotency (D-17, D-18, D-19)."
  artifacts:
    - path: "test/support/scoria/host_install_fixtures.ex"
      provides: "Shared subprocess host fixture harness"
      contains: "defmodule Scoria.TestSupport.HostInstallFixtures"
    - path: "test/mix/tasks/scoria.install_test.exs"
      provides: "B-cycle idempotency integration proof"
      contains: "dry-run"
    - path: "test/scoria/install/mode_equivalence_test.exs"
      provides: "In-process dry_run vs check body equivalence"
      contains: "Planner.build"
  key_links:
    - from: "test/support/scoria/host_install_fixtures.ex"
      to: "test/mix/tasks/scoria.install_check_test.exs"
      via: "build!/1 replaces inline build_fixture!"
      pattern: "HostInstallFixtures.build!"
    - from: "test/mix/tasks/scoria.install_test.exs"
      to: "lib/scoria/install/report.ex"
      via: "subprocess output pins operator summary substrings"
      pattern: "already_present|would_change|skipped"
---

<objective>
Consolidate installer proof harness and deliver INST-08 integration proofs: fixture matrix, mode equivalence, B-cycle idempotency, and selective per-surface cases.

Purpose: Prove output truth and idempotency on representative host shapes without expanding adoption lane scope beyond the thin INST-08 slice.
Output: Shared fixtures, refactored install tests, mode equivalence test, B-cycle subprocess proof.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/61-proof-and-stability-closeout/61-CONTEXT.md
@.planning/phases/61-proof-and-stability-closeout/61-RESEARCH.md
@.planning/phases/61-proof-and-stability-closeout/61-PATTERNS.md
@.planning/phases/61-proof-and-stability-closeout/61-VALIDATION.md
@test/mix/tasks/scoria.install_check_test.exs
@test/mix/tasks/scoria.install_test.exs
@lib/scoria/install/planner.ex
@lib/scoria/install/contract.ex

Depends on Plan 61-01 for operator summary pins. Locked D-01 through D-05, D-11, D-13 through D-21.
</context>

<tasks>

<task type="auto">
  <name>Task 61-02-01: Extract Scoria.TestSupport.HostInstallFixtures shared harness</name>
  <requirement_ids>INST-08</requirement_ids>
  <files>test/support/scoria/host_install_fixtures.ex, test/mix/tasks/scoria.install_check_test.exs, test/mix/tasks/scoria.install_test.exs</files>
  <read_first>
    test/mix/tasks/scoria.install_check_test.exs
    test/mix/tasks/scoria.install_test.exs
    .planning/phases/61-proof-and-stability-closeout/61-CONTEXT.md
    .planning/phases/61-proof-and-stability-closeout/61-PATTERNS.md
  </read_first>
  <action>Create `test/support/scoria/host_install_fixtures.ex` with `Scoria.TestSupport.HostInstallFixtures`. Move shared logic from both test files: `build!/2` accepting kinds `:compliant`, `:drift`, `:manual_review`, `:error`, `:root_list_form_browser`, `:non_root_browser_only`, `:optional_surface_absent`, `:owned_apply_host`; `subprocess_mix_env/1`; `snapshot_host_files/1` excluding `.scoria/install/manifest.json` from byte snapshots (assert manifest separately if needed); `fixture_mix_exs/1`; migration copy helpers. Implement `:root_list_form_browser` with root scope using list form browser pipeline (distinct from compliant string router). Implement `:non_root_browser_only` mirroring owned router with browser pipeline only under non-root scope (manual_review expected). Implement `:optional_surface_absent` with no tailwind config files present. Implement `:owned_apply_host` with markers on router, runtime, tailwind for B-cycle. Refactor `install_check_test.exs` and `install_test.exs` to call `HostInstallFixtures.build!/1` — delete duplicate private `build_fixture!/1`, `write_host_mix_project!/1`, and overlapping router/tailwind helpers. Preserve unique temp dirs under `test/tmp/installer/` and `test/tmp/install_check/`.</action>
  <acceptance_criteria>
    - `rg -n "defmodule Scoria\\.TestSupport\\.HostInstallFixtures" test/support/scoria/host_install_fixtures.ex` returns one match.
    - `rg -n "def build!" test/support/scoria/host_install_fixtures.ex` returns build API.
    - `rg -n ":root_list_form_browser|:non_root_browser_only|:optional_surface_absent|:owned_apply_host" test/support/scoria/host_install_fixtures.ex` returns all required kinds.
    - `rg -n "defp build_fixture!" test/mix/tasks/scoria.install_check_test.exs` returns zero matches (removed).
    - `rg -n "HostInstallFixtures" test/mix/tasks/scoria.install_check_test.exs test/mix/tasks/scoria.install_test.exs` returns usage in both files.
    - `MIX_ENV=test mix test test/mix/tasks/scoria.install_check_test.exs test/mix/tasks/scoria.install_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/mix/tasks/scoria.install_check_test.exs test/mix/tasks/scoria.install_test.exs</automated>
  </verify>
  <done>Both install test modules consume one shared fixture harness with all required host topology classes.</done>
</task>

<task type="auto">
  <name>Task 61-02-02: Add mode equivalence and optional_surface_absent proof tests</name>
  <requirement_ids>INST-08</requirement_ids>
  <files>test/scoria/install/mode_equivalence_test.exs, test/mix/tasks/scoria.install_check_test.exs</files>
  <read_first>
    lib/scoria/install/planner.ex
    lib/scoria/install/report.ex
    test/support/scoria/host_install_fixtures.ex
    .planning/phases/61-proof-and-stability-closeout/61-CONTEXT.md
  </read_first>
  <action>Create `test/scoria/install/mode_equivalence_test.exs` (async: false). For fixture kinds `:compliant`, `:drift`, `:manual_review`, build host via `HostInstallFixtures.build!/1`, extract paths, then assert `Planner.build(..., mode: :dry_run)` and `Planner.build(..., mode: :check)` produce identical `entries` and `summary` (compare normalized maps). Add subprocess test in `install_check_test.exs`: run `--dry-run` and `--check` on same `:compliant` fixture, strip `SCORIA_CHECK_RESULT` trailer from both outputs, normalize whitespace, assert bodies equal. Add subprocess test for `:optional_surface_absent`: `--check` output includes operator `skipped:` count &gt; 0 in human summary; assert no `tailwind.config.js` or `assets/tailwind.config.js` created after check. Pin exact trailers per existing tri-state tests unchanged.</action>
  <acceptance_criteria>
    - `rg -n "mode: :dry_run.*mode: :check|mode: :check.*mode: :dry_run" test/scoria/install/mode_equivalence_test.exs` returns equivalence assertion.
    - `rg -n "optional_surface_absent" test/mix/tasks/scoria.install_check_test.exs` returns skipped surface test.
    - `rg -n "skipped:" test/mix/tasks/scoria.install_check_test.exs` returns operator summary pin for absent tailwind.
    - `MIX_ENV=test mix test test/scoria/install/mode_equivalence_test.exs test/mix/tasks/scoria.install_check_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/install/mode_equivalence_test.exs test/mix/tasks/scoria.install_check_test.exs</automated>
  </verify>
  <done>dry_run and check modes produce identical plan bodies; optional tailwind absence maps to skipped operator bucket with no file creation.</done>
</task>

<task type="auto">
  <name>Task 61-02-03: Implement B-cycle idempotency subprocess proof on owned host</name>
  <requirement_ids>INST-08</requirement_ids>
  <files>test/mix/tasks/scoria.install_test.exs</files>
  <read_first>
    test/support/scoria/host_install_fixtures.ex
    test/mix/tasks/scoria.install_test.exs
    lib/scoria/install/contract.ex
    .planning/phases/61-proof-and-stability-closeout/61-CONTEXT.md
  </read_first>
  <action>Add test `"mix scoria.install B-cycle: dry-run → check → apply → check → apply is idempotent"` in `install_test.exs` using `:owned_apply_host` fixture. Sequence via subprocess: (1) `--dry-run` exit 0, snapshot unchanged; (2) `--check` snapshot unchanged; (3) default apply exit 0, snapshot changed on expected surfaces only (router, migrations, runtime_config — not tailwind if already managed); (4) `--check` exit 0 with trailer `SCORIA_CHECK_RESULT status=compliant exit_code=0`; (5) second default apply exit 0, snapshot byte-identical to post-first-apply, human output shows `already_present:` counts for converged surfaces. Exclude `.scoria/install/manifest.json` from snapshot comparison; optionally assert manifest exists after first apply. Do not golden-file full CLI output — pin trailers, exit codes, and operator summary substrings only.</action>
  <acceptance_criteria>
    - `rg -n "B-cycle|dry-run.*check.*apply" test/mix/tasks/scoria.install_test.exs` returns B-cycle test.
    - `rg -n "status=compliant exit_code=0" test/mix/tasks/scoria.install_test.exs` returns post-apply check assertion.
    - `rg -n "already_present:" test/mix/tasks/scoria.install_test.exs` returns second-apply operator pin.
    - `MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs` exits 0.
    - `MIX_ENV=test mix test.adoption` exits 0 (thin INST-08 slice stays in adoption lane).
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs && MIX_ENV=test mix test.adoption</automated>
  </verify>
  <done>B-cycle proves preview modes write nothing and second apply converges to already_present with compliant check.</done>
</task>

</tasks>

<threat_model>
## Security Gate

- Enforcement: enabled
- ASVS level: 1
- Block threshold: high severity threats

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Subprocess fixture host -> apply mutations | B-cycle must prove preview modes never write; blocked hosts never mutate. |
| Fixture harness -> tri-state automation | Trailer lines must remain exact for CI parsers. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-61-04 | Tampering | high | B-cycle apply path | block | Assert snapshot unchanged on dry-run/check; second apply must not mutate converged host. | Task 61-02-03 snapshot assertions. |
| T-61-05 | Denial of Service | medium | fixture temp dirs | mitigate | Unique `test/tmp/installer/fixture-*` per test; on_exit cleanup preserved. | Existing ExUnit on_exit patterns in install tests. |
| T-61-06 | Elevation of Privilege | high | non_root_browser_only fixture | block | Assert apply exit 1 and zero writes for non-root-only browser topology. | Dedicated fixture kind test if not covered by existing manual_review case. |

High-severity threats T-61-04 and T-61-06 are hard blockers for Plan 61-02 completion.
</threat_model>

<verification>
After all tasks:

```bash
MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.install_check_test.exs
MIX_ENV=test mix test test/scoria/install/mode_equivalence_test.exs
MIX_ENV=test mix test.adoption
```
</verification>

## PLANNING COMPLETE
