---
phase: 49-support-truth-and-adoption-closeout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/scoria.install.ex
  - lib/mix/tasks/scoria.test.knowledge.ex
  - test/mix/tasks/scoria.install_test.exs
  - test/mix/tasks/scoria.test_knowledge_test.exs
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
must_haves:
  truths:
    - "Installer output keeps the default lane visually primary and lists later optional lanes without implying hidden prerequisites."
    - "The optional knowledge lane is publicly named as `mix test.knowledge` while `mix scoria.test.knowledge` remains a supported compatibility alias."
    - "The public Mix command family continues to separate verifier tasks under `mix test.*` from setup/bootstrap tasks under `mix scoria.*`."
  artifacts:
    - path: lib/mix/tasks/scoria.install.ex
      provides: "Installer summary text and optional-lane inventory"
    - path: lib/mix/tasks/scoria.test.knowledge.ex
      provides: "Knowledge verifier shortdocs and alias posture"
    - path: test/mix/tasks/scoria.install_test.exs
      provides: "Installer copy guardrails"
    - path: test/mix/tasks/scoria.test_knowledge_test.exs
      provides: "Knowledge alias compatibility guardrails"
  key_links:
    - from: lib/mix/tasks/scoria.install.ex
      to: test/mix/tasks/scoria.install_test.exs
      via: "captured shell output assertions"
      pattern: "Optional later lanes:|mix test\\.knowledge"
    - from: lib/mix/tasks/scoria.test.knowledge.ex
      to: test/mix/tasks/scoria.test_knowledge_test.exs
      via: "task discoverability assertions"
      pattern: "Mix\\.Task\\.get\\(\"test\\.knowledge\"\\)"
---

<objective>
Align Mix-task wording and compatibility aliases with the locked public verifier hierarchy without widening runtime behavior.

Purpose: Keep installer and task output truthful to the shipped adoption order per D-08 through D-18 while preserving the compatibility alias contract in code/tests.
Output: Updated Mix-task copy plus source-adjacent tests that promote `mix test.knowledge`, keep `mix scoria.test.knowledge` working, and reinforce the default-lane-first hierarchy.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/METHODOLOGY.md
@.planning/phases/49-support-truth-and-adoption-closeout/49-CONTEXT.md
@.planning/phases/49-support-truth-and-adoption-closeout/49-RESEARCH.md
@.planning/phases/49-support-truth-and-adoption-closeout/49-PATTERNS.md
@lib/mix/tasks/scoria.install.ex
@lib/mix/tasks/scoria.test.knowledge.ex
@mix.exs
@test/mix/tasks/scoria.install_test.exs
@test/mix/tasks/scoria.test_knowledge_test.exs

<interfaces>
From lib/mix/tasks/scoria.install.ex:
```elixir
@optional_later_lanes [
  "mix test.adoption",
  ~s(SCORIA_DB_PORT="${SCORIA_DB_PORT:-5432}" SCORIA_DB_PASSWORD="${SCORIA_DB_PASSWORD:-postgres}" MIX_ENV=test mix test.semantic_fast_path),
  "mix scoria.pgvector.bootstrap",
  "mix scoria.test.knowledge"
]
```

From mix.exs:
```elixir
preferred_envs: [
  "scoria.test.knowledge": :test,
  "test.knowledge": :test
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make installer and task copy match the public verifier hierarchy</name>
  <read_first>
    - .planning/phases/49-support-truth-and-adoption-closeout/49-CONTEXT.md
    - lib/mix/tasks/scoria.install.ex
    - lib/mix/tasks/scoria.test.knowledge.ex
    - mix.exs
  </read_first>
  <files>lib/mix/tasks/scoria.install.ex, lib/mix/tasks/scoria.test.knowledge.ex</files>
  <action>Update installer summary text per D-16 and D-17 so `mix test.adoption` remains the visually primary first-adoption verifier and the optional-later-lanes list promotes `mix test.knowledge` instead of `mix scoria.test.knowledge` per D-10. Keep `mix scoria.pgvector.bootstrap` as setup, not proof, and keep the closeout chain bounded to `mix scoria.release_preview` then `mix test.adoption` in any maintainer-facing copy per D-12 and D-13. In `lib/mix/tasks/scoria.test.knowledge.ex`, adjust shortdocs or module comments as needed so `mix test.knowledge` is the canonical public lane while `mix scoria.test.knowledge` remains a supported compatibility alias per D-08 and D-10; do not remove the alias or change runtime behavior.</action>
  <verify>
    <automated>rg -n "mix test\\.adoption|mix test\\.knowledge|mix scoria\\.test\\.knowledge|Optional later lanes|knowledge verification lane|compatibility" lib/mix/tasks/scoria.install.ex lib/mix/tasks/scoria.test.knowledge.ex</automated>
  </verify>
  <acceptance_criteria>
    - `lib/mix/tasks/scoria.install.ex` lists `mix test.knowledge` in the optional-later-lanes inventory.
    - Installer copy still keeps `mix scoria.pgvector.bootstrap` as setup and does not imply knowledge or semantic surfaces are required for first adoption.
    - `lib/mix/tasks/scoria.test.knowledge.ex` still defines both `Mix.Tasks.Scoria.Test.Knowledge` and `Mix.Tasks.Test.Knowledge`.
    - Any public-facing task copy makes `mix test.knowledge` canonical and leaves `mix scoria.test.knowledge` as compatibility-only.
  </acceptance_criteria>
  <done>Mix-task copy matches the public command-family hierarchy while preserving the compatibility alias in code.</done>
</task>

<task type="auto">
  <name>Task 2: Lock installer and knowledge alias wording with source-adjacent tests</name>
  <read_first>
    - test/mix/tasks/scoria.install_test.exs
    - test/mix/tasks/scoria.test_knowledge_test.exs
    - lib/mix/tasks/scoria.install.ex
    - lib/mix/tasks/scoria.test.knowledge.ex
  </read_first>
  <files>test/mix/tasks/scoria.install_test.exs, test/mix/tasks/scoria.test_knowledge_test.exs</files>
  <action>Update the installer output assertions so they guard the new optional-lane ordering and canonical knowledge verifier naming from Task 1. Extend the knowledge task test to prove both commands remain discoverable while the compatibility wrapper stays secondary per D-10 and D-11. Keep the tests bounded to copy/discoverability drift prevention; do not add a new verifier task or new runtime proof lane.</action>
  <verify>
    <automated>mix test test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.test_knowledge_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/mix/tasks/scoria.install_test.exs` asserts `mix test.knowledge` in captured installer output.
    - `test/mix/tasks/scoria.test_knowledge_test.exs` asserts both `Mix.Task.get("test.knowledge")` and `Mix.Task.get("scoria.test.knowledge")`.
    - No test in this plan introduces a new public handoff verifier or changes the bounded closeout chain.
  </acceptance_criteria>
  <done>Source-adjacent tests pin the installer and knowledge alias posture against future drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer->host maintainer | Installer output is a public support seam and must not imply hidden prerequisites |
| Mix task alias->public docs | Compatibility aliases can confuse adopters if surfaced as equal public commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-04 | T | lib/mix/tasks/scoria.install.ex | mitigate | Promote `mix test.knowledge` while keeping optional lanes explicitly secondary and non-required |
| T-49-05 | S | knowledge verifier naming | mitigate | Preserve both task registrations in code/tests but make the canonical public name singular |
| T-49-06 | R | compatibility alias support | accept | Alias remains for backward compatibility, with tests ensuring it stays supported but unpromoted |
</threat_model>

<verification>
Primary verification is `mix test test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.test_knowledge_test.exs`.
</verification>

<success_criteria>
- Installer output and knowledge-task copy reinforce the default-lane-first hierarchy.
- `mix test.knowledge` is the promoted public knowledge verifier.
- `mix scoria.test.knowledge` remains a working compatibility alias in code and tests.
</success_criteria>

<output>
After completion, create `.planning/phases/49-support-truth-and-adoption-closeout/49-02-SUMMARY.md`
</output>
