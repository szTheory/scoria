---
phase: 49-support-truth-and-adoption-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/adoption_lanes.md
  - docs/operator_verification.md
  - docs/bounded_handoffs.md
  - docs/semantic_fast_path.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
must_haves:
  truths:
    - "Adopters see one boring default-lane order: mix scoria.install -> mix ecto.migrate -> mix test.adoption -> inspect /scoria and /scoria/workflows/:run_id."
    - "Bounded handoffs are documented as an additive capability inside the default runtime story, not as a separate verifier lane."
    - "Semantic fast path and optional knowledge stay clearly secondary to first adoption and are labeled with one canonical verification command each."
    - "Maintainers can point support questions at the four-tier hierarchy from D-16 without treating mix test as canonical proof."
  artifacts:
    - path: README.md
      provides: "Top-level adoption order and command hierarchy"
    - path: docs/adoption_lanes.md
      provides: "Four-lane public field guide"
    - path: docs/operator_verification.md
      provides: "Canonical verifier hierarchy and closeout wording"
    - path: docs/bounded_handoffs.md
      provides: "Additive bounded-handoff positioning"
    - path: docs/semantic_fast_path.md
      provides: "Optional semantic troubleshooting positioning"
  key_links:
    - from: README.md
      to: docs/operator_verification.md
      via: "default-lane and closeout command references"
      pattern: "mix test\\.adoption|mix scoria\\.release_preview"
    - from: docs/adoption_lanes.md
      to: docs/bounded_handoffs.md
      via: "additive handoff framing"
      pattern: "Add this only after the default runtime lane"
    - from: docs/operator_verification.md
      to: docs/semantic_fast_path.md
      via: "lane-specific verifier naming"
      pattern: "mix test\\.semantic_fast_path"
---

<objective>
Align Scoria's adopter-facing docs around the locked four-tier support hierarchy and one canonical command per lane.

Purpose: Remove wording drift across README and guides so adopters and maintainers receive the same lane order, prerequisite boundaries, and closeout story per D-01 through D-18.
Output: Rewritten public docs that make the default lane visually primary, keep bounded handoffs additive, and demote broader repo-health guidance to context only.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/METHODOLOGY.md
@.planning/STATE.md
@.planning/phases/49-support-truth-and-adoption-closeout/49-CONTEXT.md
@.planning/phases/49-support-truth-and-adoption-closeout/49-RESEARCH.md
@.planning/phases/49-support-truth-and-adoption-closeout/49-PATTERNS.md
@.planning/phases/48-host-app-install-contract-and-consumer-proof/48-CONTEXT.md
@README.md
@docs/adoption_lanes.md
@docs/operator_verification.md
@docs/bounded_handoffs.md
@docs/semantic_fast_path.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite top-level adoption order and lane hierarchy</name>
  <read_first>
    - .planning/phases/49-support-truth-and-adoption-closeout/49-CONTEXT.md
    - README.md
    - docs/adoption_lanes.md
    - docs/operator_verification.md
  </read_first>
  <files>README.md, docs/adoption_lanes.md</files>
  <action>Rewrite the README verification and lane-order sections so the canonical default-lane flow reads exactly `mix scoria.install` -> `mix ecto.migrate` -> `mix test.adoption` -> inspect `/scoria` and `/scoria/workflows/:run_id` per D-01 and D-03. Remove any wording that presents `mix test` as the default proof per D-02. In `docs/adoption_lanes.md`, preserve the four-lane model but make the command family converge on `mix test.adoption`, `mix test.semantic_fast_path`, and `mix test.knowledge` per D-08 through D-11, while keeping optional prerequisites explicitly outside first adoption per D-04.</action>
  <verify>
    <automated>rg -n "mix test\\.adoption|mix test\\.knowledge|mix test\\.semantic_fast_path|mix scoria\\.release_preview|broader repo-health context" README.md docs/adoption_lanes.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `mix scoria.install`, `mix ecto.migrate`, and `mix test.adoption` in the default-lane verification guidance.
    - `README.md` no longer presents bare `mix test` as the canonical default-lane proof.
    - `docs/adoption_lanes.md` contains `mix test.knowledge` and does not promote `mix scoria.test.knowledge` as the primary knowledge verifier.
    - Both files state that pgvector, retrieval, grounding, semantic fast path, and knowledge verification are not prerequisites for first adoption.
  </acceptance_criteria>
  <done>Top-level docs present one default-lane proof order, one canonical command per lane, and explicit optional-lane boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Reframe operator, handoff, and semantic guides around the same hierarchy</name>
  <read_first>
    - .planning/phases/49-support-truth-and-adoption-closeout/49-CONTEXT.md
    - docs/operator_verification.md
    - docs/bounded_handoffs.md
    - docs/semantic_fast_path.md
  </read_first>
  <files>docs/operator_verification.md, docs/bounded_handoffs.md, docs/semantic_fast_path.md</files>
  <action>Update the operator guide so the four-tier hierarchy from D-16 is explicit: canonical closeout proof is exactly `mix scoria.release_preview` then `mix test.adoption` per D-12 and D-13; the default lane is `mix test.adoption`; semantic and knowledge verifiers are lane-specific only per D-14; `mix test` is advisory repo-health context per D-15. In `docs/bounded_handoffs.md`, clarify per D-05 through D-07 that adopters validate the base lane with `mix test.adoption` before intentionally expanding into `Scoria.start_handoff_run/3`, without inventing a separate handoff verifier. In `docs/semantic_fast_path.md`, keep semantic troubleshooting explicitly secondary to the default lane and use `mix test.knowledge` when referencing the optional knowledge lane.</action>
  <verify>
    <automated>rg -n "mix scoria\\.release_preview|mix test\\.adoption|mix test\\.knowledge|mix test\\.semantic_fast_path|broader repo-health context|start_handoff_run/3" docs/operator_verification.md docs/bounded_handoffs.md docs/semantic_fast_path.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/operator_verification.md` contains the exact closeout chain `mix scoria.release_preview` followed by `mix test.adoption`.
    - `docs/operator_verification.md` describes `mix test` as broader repo-health context rather than canonical proof.
    - `docs/bounded_handoffs.md` frames handoffs as additive after the default lane and does not introduce a separate verifier command.
    - `docs/semantic_fast_path.md` keeps `mix test.semantic_fast_path` as the semantic lane command and references `mix test.knowledge` for the optional knowledge lane.
  </acceptance_criteria>
  <done>All adopter-facing guides share the same closeout/default/optional/context hierarchy and bounded-handoff posture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer->docs | Public package and support docs define what adopters trust as canonical proof |
| docs->host-app operator | Misstated commands or prerequisites can send adopters down the wrong verification lane |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | T | README.md, docs/operator_verification.md | mitigate | Rewrite command hierarchy to match D-01 through D-18 and remove contradictory default-lane guidance |
| T-49-02 | I | docs/bounded_handoffs.md, docs/semantic_fast_path.md | mitigate | Keep optional lanes explicitly additive so adopters are not misled into enabling non-required surfaces |
| T-49-03 | R | public support guidance | accept | Repo-health `mix test` remains available as advisory context, but Phase 49 will label it non-canonical |
</threat_model>

<verification>
Run the grep verification above, then run the docs/source drift suite after dependent test plan execution lands:
`mix test test/scoria/adoption_surface_test.exs`
</verification>

<success_criteria>
- README and all lane guides reflect the same lane order and prerequisite boundaries.
- Bounded handoffs are additive to the default lane, not a separate public verifier lane.
- Closeout guidance names only `mix scoria.release_preview` and `mix test.adoption` as the bounded milestone proof chain.
</success_criteria>

<output>
After completion, create `.planning/phases/49-support-truth-and-adoption-closeout/49-01-SUMMARY.md`
</output>
