---
phase: 01-core-observability
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - lib/scoria/observe/buffer.ex
  - test/scoria/observe/buffer_test.exs
autonomous: true
requirements:
  - OBS-03
must_haves:
  truths:
    - Main processes are completely unaffected by tracing latency.
  artifacts:
    - path: lib/scoria/observe/buffer.ex
      provides: GenServer for asynchronous batching
  key_links:
    - from: lib/scoria/observe/buffer.ex
      to: Scoria.Repo
      via: Repo.insert_all
---

<objective>
Implement the asynchronous batching engine for OpenInference spans.
Purpose: Prevent synchronous database inserts from blocking LLM execution processes.
Output: GenServer buffer and background flush task.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/RESEARCH.md
@.planning/PATTERNS.md

<interfaces>
From lib/scoria/repo/span.ex:
```elixir
defmodule Scoria.Repo.Span do
  # Ecto schema providing `ai_spans`
end
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: GenServer Buffer</name>
  <files>lib/scoria/observe/buffer.ex, test/scoria/observe/buffer_test.exs</files>
  <behavior>
    - Buffers incoming spans via `cast/1`.
    - Periodically flushes buffered spans using `Repo.insert_all`.
    - Handles graceful shutdown to flush remaining spans.
  </behavior>
  <action>Create `Scoria.Observe.Buffer` (GenServer). Implement state holding a list of spans. Add `handle_cast` to append spans, and a periodic timer (`Process.send_after`) to flush to Postgres using `Repo.insert_all`. Trap exits to flush on shutdown.</action>
  <verify>
    <automated>mix test test/scoria/observe/buffer_test.exs</automated>
  </verify>
  <done>Buffer accumulates spans and inserts them in bulk without blocking the caller.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Memory | Holding unpersisted spans in GenServer state |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | DoS | Buffer | mitigate | Impose max buffer size; drop spans if exceeded (backpressure). |
</threat_model>

<success_criteria>
Buffer handles concurrent casts without blocking and flushes reliably.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-observability/01-03-SUMMARY.md`
</output>