---
phase: 01-core-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/repo/trace.ex
  - lib/scoria/repo/span.ex
  - lib/scoria/repo/span_event.ex
  - priv/repo/migrations/YYYYMMDDHHMMSS_create_ai_observability_tables.exs
  - test/scoria/repo/trace_test.exs
  - test/scoria/repo/span_test.exs
  - test/scoria/repo/span_event_test.exs
autonomous: true
requirements:
  - OBS-01
must_haves:
  truths:
    - AI traces and spans can be persisted to the database.
  artifacts:
    - path: lib/scoria/repo/span.ex
      provides: Ecto schema for spans
  key_links:
    - from: lib/scoria/repo/span.ex
      to: ai_spans
      via: Ecto schema mapping
---

<objective>
Implement the core database schemas for OpenInference traces, spans, and span events.
Purpose: Provides the foundation for storing observability data without blocking main processes.
Output: Ecto schemas and migrations.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/RESEARCH.md
@.planning/PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Generate Ecto Migrations and Schemas</name>
  <files>lib/scoria/repo/trace.ex, lib/scoria/repo/span.ex, lib/scoria/repo/span_event.ex, priv/repo/migrations/YYYYMMDDHHMMSS_create_ai_observability_tables.exs, test/scoria/repo/trace_test.exs, test/scoria/repo/span_test.exs, test/scoria/repo/span_event_test.exs</files>
  <behavior>
    - Trace, Span, and SpanEvent schemas must map correctly to their respective tables.
    - Uses JSONB `attributes` column for variable OpenInference fields.
  </behavior>
  <action>Create Ecto schemas mapping to `ai_traces`, `ai_spans`, and `ai_span_events`. Include highly indexed relational columns (id, trace_id, parent_id, name, span_kind, status_code, start_time, end_time) and an `attributes` map column. Create the corresponding migration.</action>
  <verify>
    <automated>mix test</automated>
  </verify>
  <done>Schemas are created, migrations pass, unit tests verify core fields and JSONB attribute insertion.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Database | Persistence of span data |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Ecto Schemas | accept | Data comes from internal telemetry; schema validates types. |
</threat_model>

<success_criteria>
Schemas and migrations exist, pass compilation, and basic Repo insert tests succeed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-observability/01-01-SUMMARY.md`
</output>
