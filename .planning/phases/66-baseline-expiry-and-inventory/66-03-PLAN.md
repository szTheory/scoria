---
phase: 66-baseline-expiry-and-inventory
plan: 03
type: execute
wave: 2
depends_on: [66-01]
files_modified:
  - lib/scoria/warning_inventory.ex
  - lib/scoria/warning_inventory/cluster.ex
  - lib/mix/tasks/scoria.warning_inventory.ex
  - test/scoria/warning_inventory/cluster_test.exs
  - test/fixtures/warning_inventory/
  - mix.exs
  - docs/operator_verification.md
  - .gitignore
autonomous: true
requirements: [WARN-04]
must_haves:
  truths:
    - "Maintainer runs `mix scoria.warning_inventory` to classify warnings without WAE (D-14, D-15)."
    - "Cluster registry uses stable atoms with hybrid regex/path match (D-21, D-24)."
    - "`--write` commits cluster-count JSON + WARNING-INVENTORY.md; full array gitignored (D-17)."
    - "Logger/HTTP/SQL noise excluded from compiler warning counts (D-26)."
  artifacts:
    - path: "lib/scoria/warning_inventory/cluster.ex"
      provides: "Cluster.match/1 registry"
      contains: ":knowledge_migration_redefine"
    - path: "lib/mix/tasks/scoria.warning_inventory.ex"
      provides: "WARN-04 documented path"
      contains: "defmodule Mix.Tasks.Scoria.WarningInventory"
    - path: "docs/operator_verification.md"
      provides: "Warning baseline and inventory subsection"
      contains: "warning_inventory"
  key_links:
    - from: "lib/scoria/warning_inventory.ex"
      to: "lib/scoria/warning_baseline.ex"
      via: "baseline_surface join on load"
      pattern: "WarningBaseline"
    - from: "lib/scoria/warning_inventory.ex"
      to: "lib/scoria/verification_lanes.ex"
      via: "lane_ids attribution"
      pattern: "VerificationLanes"
---

<objective>
Ship `mix scoria.warning_inventory` with hybrid cluster classification, maintainer DX, and reproducible committed artifacts for Phase 67 ratchet ordering.

Purpose: WARN-04 measurement infrastructure — capture mode, not CI gate.
Output: Inventory modules, Mix task, classifier unit tests, operator docs, gitignore for `tmp/warning-inventory/`.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/66-baseline-expiry-and-inventory/66-CONTEXT.md
@.planning/phases/66-baseline-expiry-and-inventory/66-RESEARCH.md
@lib/scoria/warning_baseline.ex
@lib/scoria/verification_lanes.ex
@lib/mix/tasks/test.adoption.ex
@mix.exs
@docs/operator_verification.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Command injection via `--since` git ref | low | Pass ref to `git` as single argv; reject refs containing `;` or `` ` `` |
| Writing inventory outside project root | low | `--write` paths fixed under `.planning/` and `tmp/warning-inventory/` |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 66-03-01: Implement cluster registry and stderr classifier</name>
  <requirement_ids>WARN-04</requirement_ids>
  <files>lib/scoria/warning_inventory/cluster.ex, lib/scoria/warning_inventory.ex, test/scoria/warning_inventory/cluster_test.exs, test/fixtures/warning_inventory/</files>
  <read_first>
    .planning/phases/66-baseline-expiry-and-inventory/66-CONTEXT.md
    lib/scoria/verification_lanes.ex
    lib/mix/tasks/test.adoption.ex
  </read_first>
  <action>
Create `lib/scoria/warning_inventory/cluster.ex` with `defmodule Scoria.WarningInventory.Cluster`.

Implement `match/1` accepting a map `%{file: binary, line: integer, message: binary, signal_kind: :compile_warning | :ex_unit | :runtime_log}` returning `cluster_id` atom.

Registry (D-24) — first match wins, document order in module:
1. `:knowledge_migration_redefine` — file matches `priv/repo/knowledge_migrations/` or message contains "redefining module"
2. `:test_unused_binding` — file under `test/` and message matches `variable ".*" is unused`
3. `:test_dead_default_args` — file under `test/` and message matches unused default args pattern
4. `:host_proof_generated_compile` — file matches `test/support/scoria/host_app_proof/`
5. `:host_overlay_test_path` — file matches `host_app_proof` and `test/`
6. `:liveview_async_teardown` — message matches async/teardown/sandbox noise patterns (signal_kind may be `:runtime_log`)
7. `:canonical_surface_clean` — sentinel: only when file under `lib/scoria/` AND zero compile warnings (used in summary guard, not match input)
8. `:unclassified_compile` — default for `:compile_warning` without match

Exclude from parsing pipeline entirely (do not emit rows): lines matching `\[warning\]` Logger style, `severity: "warning"` SQL, HTTP retry log patterns (D-26).

Create `lib/scoria/warning_inventory.ex`:
- `parse_output/1` — split capture string into warning maps
- `classify/1` — map rows with cluster_id, `path_area`, `ratchet_tier` per D-23 ordering helper
- `ratchet_tier/1` — function mapping cluster_id → `:p0_compile_lib` | `:p1_closeout_lane_contract` | `:p2_adoption_lane_files` | `:p3_high_signal_tests` | `:p4_baselined_deferred` | `:p5_out_of_scope`
- `lane_ids_for_file/1` — use `Mix.Tasks.Scoria.Test.Adoption.adoption_test_files/0` and `VerificationLanes` ids
- `join_baseline/2` — optional owner/expires from `Scoria.WarningBaseline.accepted_rows/1` matched by `surface` string
- `dedupe_key/1` — `{cluster_id, file, message_fingerprint}` where fingerprint is first 80 chars normalized whitespace

Fixture snippets in `test/fixtures/warning_inventory/*.txt` for each cluster; tests in `test/scoria/warning_inventory/cluster_test.exs` call `parse_output` + `classify` only — no subprocess (D-34).

If `:unclassified_compile` count > 0 in a classified list, test asserts module documents warn behavior for execute.
  </action>
  <acceptance_criteria>
    - `rg -n ":knowledge_migration_redefine|:test_unused_binding|:unclassified_compile" lib/scoria/warning_inventory/cluster.ex` returns all listed atoms.
    - `rg -n "def match\\(" lib/scoria/warning_inventory/cluster.ex` returns match/1.
    - `rg -n ":p0_compile_lib|:p4_baselined_deferred" lib/scoria/warning_inventory.ex` returns ratchet tiers.
    - `MIX_ENV=test mix test test/scoria/warning_inventory/cluster_test.exs` exits 0.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/warning_inventory/cluster_test.exs</automated>
  </verify>
  <done>Classifier assigns stable cluster_ids with ratchet tiers using fixture-only tests.</done>
</task>

<task type="auto">
  <name>Task 66-03-02: Add mix scoria.warning_inventory with capture, flags, and --write artifacts</name>
  <requirement_ids>WARN-04</requirement_ids>
  <files>lib/mix/tasks/scoria.warning_inventory.ex, mix.exs, .gitignore, docs/operator_verification.md</files>
  <read_first>
    lib/scoria/warning_inventory.ex
    lib/mix/tasks/scoria.release_preview.ex
    .planning/phases/66-baseline-expiry-and-inventory/66-CONTEXT.md
  </read_first>
  <action>
Create `lib/mix/tasks/scoria.warning_inventory.ex` as `Mix.Tasks.Scoria.WarningInventory`.

OptionParser (D-16):
- `--format json|md|table` default `table`
- `--write` boolean
- `--since git-ref` optional (for metadata / future diff; store in JSON metadata)
- `--scope full|high_signal` default `full`
- `--include-runtime` flag
- `--quiet`

Preflight (D-18):
- Assert `Mix.env() == :test` else `Mix.raise("warning inventory requires MIX_ENV=test")`
- If `File.exists?("test/tmp")` and not empty, `Mix.raise` with message to clean installer fixture pollution (list count of entries)
- Print note when pgvector not available: knowledge cluster counts may be incomplete (informational unless knowledge tests required)

Capture mode (D-15): unless `--quiet`, run:
`System.cmd("mix", ["do", "compile", "--force", "+", "test"], env: [{"MIX_ENV", "test"}], stderr_to_stdout: true)`
Parse output even on non-zero exit status.

`--scope high_signal`: filter rows to `lib/`, adoption test files, and paths matching `test/scoria_web/live/` — maintainer shortcut only.

`--write` produces:
- `.planning/warning-inventory.baseline.json` — `%{"schema_version" => "1.0", "git_sha" => ..., "generated_at" => ISO8601, "clusters" => %{cluster_id => count}}` only
- `.planning/WARNING-INVENTORY.md` — human summary sorted by ratchet_tier, Phase 67 queue table template
- `tmp/warning-inventory/latest.json` — full row array; ensure `.gitignore` contains `tmp/warning-inventory/`

Add to `mix.exs` `preferred_envs: ["scoria.warning_inventory": :test]`.

Add docs subsection to `docs/operator_verification.md` titled `## Warning baseline and inventory` with 2–3 sentences and commands:
`mix scoria.warning_baseline.check`, `mix scoria.warning_inventory`, `mix scoria.warning_inventory --write --scope full`.

Do NOT add inventory to CI workflow in this plan.
  </action>
  <acceptance_criteria>
    - `rg -n "defmodule Mix\\.Tasks\\.Scoria\\.WarningInventory" lib/mix/tasks/scoria.warning_inventory.ex` returns one match.
    - `rg -n "--format|--write|--scope" lib/mix/tasks/scoria.warning_inventory.ex` returns flags.
    - `rg -n "scoria.warning_inventory" mix.exs` returns preferred_envs entry.
    - `rg -n "tmp/warning-inventory" .gitignore` returns gitignore entry.
    - `rg -n "Warning baseline and inventory" docs/operator_verification.md` returns subsection.
    - `MIX_ENV=test mix scoria.warning_inventory --format table --scope high_signal` exits 0 (capture may warn but must not crash).
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/warning_inventory/cluster_test.exs && MIX_ENV=test mix help scoria.warning_inventory</automated>
  </verify>
  <done>WARN-04 maintainer path exists with documented commands and artifact layout for Phase 67.</done>
</task>

</tasks>

<verification>
- `MIX_ENV=test mix test test/scoria/warning_inventory/`
- Local (manual): `MIX_ENV=test mix scoria.warning_inventory --write --scope full` then review `.planning/warning-inventory.baseline.json` cluster counts
- Confirm inventory NOT added to `.github/workflows/ci.yml`
</verification>

<success_criteria>
- Reproducible inventory command documented in operator_verification.md
- Cluster taxonomy covers known debt from CONTEXT code_context
- Phase 67 can sort by ratchet_tier from committed baseline JSON
</success_criteria>
