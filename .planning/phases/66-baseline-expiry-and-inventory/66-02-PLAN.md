---
phase: 66-baseline-expiry-and-inventory
plan: 02
type: execute
wave: 2
depends_on: [66-01]
files_modified:
  - .github/workflows/ci.yml
  - test/scoria/ci_policy_contract_test.exs
  - .planning/ROADMAP.md
autonomous: true
requirements: [WARN-03]
must_haves:
  truths:
    - "CI `policy` job runs without Postgres and executes baseline check before compile WAE (D-09, D-10)."
    - "`test` job has `needs: policy` and preserves closeout command order (D-11)."
    - "Contract test fails if baseline check removed or reordered after compile WAE (D-13)."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "policy + test job split"
      contains: "mix scoria.warning_baseline.check"
    - path: "test/scoria/ci_policy_contract_test.exs"
      provides: "CI drift grep tests"
      contains: "warning_baseline"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "lib/mix/tasks/scoria.warning_baseline.check.ex"
      via: "policy job step"
      pattern: "mix scoria.warning_baseline.check"
---

<objective>
Wire WARN-03 into GitHub Actions via a Postgres-free `policy` job and add CI contract tests so workflow drift is caught like VerificationLanes.

Purpose: Executable CI truth; cheap policy before DB-heavy closeout.
Output: Updated `ci.yml`, `ci_policy_contract_test.exs`, ROADMAP phase header format fix for GSD SDK.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/66-baseline-expiry-and-inventory/66-CONTEXT.md
@.planning/phases/66-baseline-expiry-and-inventory/66-RESEARCH.md
@.github/workflows/ci.yml
@test/scoria/verification_lanes_test.exs
@lib/scoria/verification_lanes.ex
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Policy job skipped via workflow edit | medium | Contract test asserts job name and step presence |
| Baseline check moved after compile WAE | medium | Index ordering assertion in contract test |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 66-02-01: Split ci.yml into policy and test jobs</name>
  <requirement_ids>WARN-03</requirement_ids>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .github/workflows/ci.yml
    .planning/phases/66-baseline-expiry-and-inventory/66-CONTEXT.md
    .planning/phases/66-baseline-expiry-and-inventory/66-RESEARCH.md
  </read_first>
  <action>
Refactor `.github/workflows/ci.yml`:

**`policy` job** (`runs-on: ubuntu-latest`, NO `services.postgres`):
1. checkout@v4
2. setup-beam (otp 27, elixir 1.19)
3. cache deps (same key pattern as today)
4. `mix scoria.warning_baseline.check` — before deps.get is acceptable per D-10 but preferred order in CONTEXT is baseline first then setup-beam; implement D-10 exactly: (1) baseline check — note: baseline check needs Elixir; so order: checkout → setup-beam → **baseline check** → cache → deps.get → compile WAE → lane-contract WAE tests only:
   `mix test --warnings-as-errors test/scoria/verification_lanes_test.exs test/scoria/adoption_surface_test.exs`
5. Do NOT run release_preview, ecto, adoption, runtime_to_handoff, full mix test, or knowledge in policy job.

**`test` job**:
- Add `needs: policy`
- Keep existing `services.postgres` and env vars
- Remove steps moved to policy: `Compile with warnings as errors` and `Verify lane-contract tests` (only those two)
- Preserve remaining step order unchanged: deps.get (if not redundant — policy already fetched deps; test job still needs checkout+beam+cache+deps for isolation), then release_preview → ecto → test.adoption → test.runtime_to_handoff → mix test → test.knowledge

Hard-fail only — no `continue-on-error` on baseline step (D-12).

Do not add full-suite WAE or inventory steps (Phase 68/66-03).
  </action>
  <acceptance_criteria>
    - `rg -n "jobs:" .github/workflows/ci.yml` followed by `policy:` and `test:` job keys present.
    - `rg -n "mix scoria.warning_baseline.check" .github/workflows/ci.yml` returns policy job step.
    - `rg -n "needs: policy" .github/workflows/ci.yml` returns test job dependency.
    - `rg -n "services:" .github/workflows/ci.yml` — postgres appears under `test` job only, not under `policy`.
    - `rg -n "mix compile --warnings-as-errors" .github/workflows/ci.yml` appears only in `policy` job (removed from test).
  </acceptance_criteria>
  <verify>
    <automated>rg -n "policy:|test:|warning_baseline|needs: policy" .github/workflows/ci.yml</automated>
  </verify>
  <done>CI runs baseline expiry gate in isolated policy job before Postgres closeout.</done>
</task>

<task type="auto">
  <name>Task 66-02-02: Add CI policy contract tests and fix ROADMAP phase headers</name>
  <requirement_ids>WARN-03</requirement_ids>
  <files>test/scoria/ci_policy_contract_test.exs, .planning/ROADMAP.md</files>
  <read_first>
    test/scoria/verification_lanes_test.exs
    .github/workflows/ci.yml
    .planning/ROADMAP.md
  </read_first>
  <action>
Create `test/scoria/ci_policy_contract_test.exs` (or extend verification_lanes_test — prefer dedicated file per discretion).

Tests:
1. `ci.yml` contains `mix scoria.warning_baseline.check`
2. `index_of(baseline_check) < index_of("mix compile --warnings-as-errors")` within file
3. `index_of(compile WAE) < index_of("verification_lanes_test.exs")` within file
4. Closeout chain in **test** job: `release_preview` &lt; `test.adoption` &lt; `test.runtime_to_handoff` (reuse `VerificationLanes.ci_command/1` values like existing test)
5. `test` job includes `needs: policy`

Fix `.planning/ROADMAP.md` phase detail headers from em dash to colon for Phase 66–69:
- Change `### Phase 66 —` to `### Phase 66:` (and 67, 68, 69)
- Change checkbox line `Phase 66:` if needed for SDK (`Phase 66: Baseline...` already correct in list)

Use same `index_of/2` helper pattern as verification_lanes_test.
  </action>
  <acceptance_criteria>
    - `rg -n "CiPolicyContractTest|ci_policy_contract" test/scoria/ci_policy_contract_test.exs` returns module.
    - `rg -n "warning_baseline" test/scoria/ci_policy_contract_test.exs` returns assertion.
    - `rg -n "### Phase 66:" .planning/ROADMAP.md` returns match (no em dash for phase 66 header).
    - `MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs</automated>
  </verify>
  <done>CI policy ordering is contract-tested; ROADMAP parses for gsd-sdk phase metadata.</done>
</task>

</tasks>

<verification>
- `MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs`
- Manual: inspect generated workflow YAML for duplicate deps steps (acceptable if both jobs self-contained)
</verification>

<success_criteria>
- WARN-03 runs in CI on every PR before Postgres work
- Closeout chain order in test job unchanged from Phase 61 contract
</success_criteria>
