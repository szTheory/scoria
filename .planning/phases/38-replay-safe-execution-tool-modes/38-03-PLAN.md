---
phase: 38-replay-safe-execution-tool-modes
plan: 3
type: execute
wave: 3
depends_on:
  - 38-02
files_modified:
  - lib/scoria/runtime/run_summary.ex
  - lib/scoria/runtime/run_detail.ex
  - lib/scoria/workflows/remote_approval_projection.ex
  - test/scoria/runtime_view_test.exs
  - test/scoria/workflows/remote_approval_projection_test.exs
autonomous: true
requirements:
  - RPLY-02
must_haves:
  truths:
    - "Public runtime reads expose replay-safe provenance without pretending the whole replay run had one effect mode."
    - "Operator-facing run detail can distinguish safe replay posture, live allowlist posture, and actual seam outcomes."
    - "Replay evidence needed for later Phase 39 UI work is available through DTOs, not hidden in internal-only structs."
  artifacts:
    - path: "lib/scoria/runtime/run_summary.ex"
      provides: "Replay posture summary with run intent and approval wait status."
    - path: "lib/scoria/runtime/run_detail.ex"
      provides: "Curated seam-level replay evidence projection."
    - path: "lib/scoria/workflows/remote_approval_projection.ex"
      provides: "Replay-safe approval lineage projection for operator-facing inbox/evidence reads."
    - path: "test/scoria/runtime_view_test.exs"
      provides: "Projection regression coverage for replay-safe runtime reads."
    - path: "test/scoria/workflows/remote_approval_projection_test.exs"
      provides: "Replay approval projection regressions for seam-level evidence exposure."
  key_links:
    - from: "lib/scoria/runtime/run_detail.ex"
      to: "lib/scoria/workflows/checkpoint.ex"
      via: "checkpoint items expose seam-level replay disposition"
      pattern: "replay_disposition"
    - from: "lib/scoria/runtime/run_detail.ex"
      to: "lib/scoria/observe/approval.ex"
      via: "approval items expose replay scope and executed_live facts"
      pattern: "replay_scope|executed_live"
    - from: "lib/scoria/workflows/remote_approval_projection.ex"
      to: "lib/scoria/observe/approval.ex"
      via: "approval inbox and lineage reads surface replay-safe provenance fields directly from durable rows"
      pattern: "replay_disposition|replay_scope|executed_live|source_approval_id"
---

<objective>
Project the new replay-safe truth through the public runtime DTOs and approval evidence projections.

Purpose: Preserve explicit replay provenance per RPLY-02 and D-04/D-21 without adding UI-owned policy logic, so Phase 39 can render replay facts directly from runtime detail and approval lineage projections.
Output: Updated runtime summary/detail DTOs, replay-safe approval projection reads, and projection tests for replay-safe evidence.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-replay-safe-execution-tool-modes/38-CONTEXT.md
@.planning/phases/38-replay-safe-execution-tool-modes/38-02-PLAN.md
@.planning/phases/38-replay-safe-execution-tool-modes/38-02-SUMMARY.md
@lib/scoria/runtime/run_summary.ex
@lib/scoria/runtime/run_detail.ex
@lib/scoria/workflows/remote_approval_projection.ex
@test/scoria/runtime_view_test.exs
@test/scoria/workflows/remote_approval_projection_test.exs

<interfaces>
From lib/scoria/runtime/run_summary.ex:
```elixir
defstruct [
  :run_id,
  :session_id,
  :status,
  :source_run_id,
  :source_checkpoint_id,
  :execution_mode
]
```

From lib/scoria/runtime/run_detail.ex:
```elixir
@type replay_lineage :: %{
  source_run_id: Ecto.UUID.t() | nil,
  source_checkpoint_id: Ecto.UUID.t() | nil,
  execution_mode: String.t(),
  replay_overrides: map()
}
```

From lib/scoria/workflows/remote_approval_projection.ex:
```elixir
def list_pending_approvals(filters \\ %{}) do
def get_approval_lineage!(approval_id) do
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend runtime DTOs with replay posture and seam evidence</name>
  <files>lib/scoria/runtime/run_summary.ex, lib/scoria/runtime/run_detail.ex, test/scoria/runtime_view_test.exs</files>
  <read_first>.planning/phases/38-replay-safe-execution-tool-modes/38-CONTEXT.md, .planning/phases/38-replay-safe-execution-tool-modes/38-PATTERNS.md, lib/scoria/runtime/run_summary.ex, lib/scoria/runtime/run_detail.ex, lib/scoria/workflows/run.ex, lib/scoria/observe/approval.ex, lib/scoria/workflows/checkpoint.ex, lib/scoria/workflows/event.ex, test/scoria/runtime_view_test.exs</read_first>
  <behavior>
    - Test 1: run summary continues to show `execution_mode` as run intent only (`live|replay`).
    - Test 2: run detail exposes seam-level `replay_disposition` facts on checkpoints, events, and approvals.
    - Test 3: run detail exposes replay posture from `replay_overrides["live_tool_allowlist"]` and whether any seam actually executed live.
  </behavior>
  <action>Update `Scoria.Runtime.RunSummary` and `Scoria.Runtime.RunDetail` per D-04 and D-21 so the public runtime projections separate run intent from seam outcomes. Follow the exact analogs called out in `38-PATTERNS.md` for `lib/scoria/runtime/run_summary.ex`, `lib/scoria/runtime/run_detail.ex`, and `test/scoria/runtime_view_test.exs`: keep the DTO boundary curated, projection-first, and free of raw metadata inference. Keep `summary.execution_mode` as `live|replay` only. Extend `replay_lineage` with explicit replay posture fields derived from `replay_overrides`, including the persisted `live_tool_allowlist`. Add `replay_disposition`, `replay_reason_code`, source lineage IDs, `replay_scope`, and `executed_live` to the curated checkpoint/event/approval items surfaced through runtime detail. Do not add raw preloaded structs, and do not collapse seam outcomes back into a fake run-wide `historical_stubbed` mode.</action>
  <acceptance_criteria>
    - `rg -n "live_tool_allowlist|executed_live|replay_disposition" lib/scoria/runtime/run_summary.ex lib/scoria/runtime/run_detail.ex` returns matches.
    - `rg -n "historical_stubbed" lib/scoria/runtime/run_summary.ex lib/scoria/runtime/run_detail.ex` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/runtime_view_test.exs</automated>
  </verify>
  <done>Runtime DTOs expose replay-safe posture and seam evidence directly, while keeping run intent separate from effect outcomes.</done>
</task>

<task type="auto">
  <name>Task 2: Extend approval projections and add projection regressions for replay-safe evidence</name>
  <files>lib/scoria/workflows/remote_approval_projection.ex, test/scoria/workflows/remote_approval_projection_test.exs</files>
  <read_first>.planning/phases/38-replay-safe-execution-tool-modes/38-CONTEXT.md, .planning/phases/38-replay-safe-execution-tool-modes/38-PATTERNS.md, lib/scoria/workflows/remote_approval_projection.ex, test/scoria/workflows/remote_approval_projection_test.exs, lib/scoria/observe/approval.ex, lib/scoria/runtime/run_detail.ex</read_first>
  <action>Extend `Scoria.Workflows.RemoteApprovalProjection` and add `test/scoria/workflows/remote_approval_projection_test.exs` using the exact analog projection/test patterns called out in `38-PATTERNS.md`. Update `list_pending_approvals/1` and `get_approval_lineage!/1` so approval inbox and lineage reads expose `replay_disposition`, `replay_scope`, source lineage IDs, and `executed_live`, while treating `replay_allowed` as legacy compatibility only. Keep these assertions at the projection boundary so later LiveView work can rely on them unchanged without inspecting raw approval structs or metadata.</action>
  <acceptance_criteria>
    - `rg -n "replay_disposition|executed_live|replay_scope" lib/scoria/workflows/remote_approval_projection.ex test/scoria/workflows/remote_approval_projection_test.exs` returns matches.
    - `rg -n "replay_scope|source_approval_id|replay_allowed" test/scoria/workflows/remote_approval_projection_test.exs` returns matches proving the seam stays projection-driven instead of boolean-driven.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/workflows/remote_approval_projection_test.exs test/scoria/workflows/integration_test.exs</automated>
  </verify>
  <done>The approval projection read path proves explicit replay provenance and seam-level replay evidence without UI-side inference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable replay evidence -> public DTOs | projection mistakes could hide blocked/stubbed/live distinctions from operators |
| run intent -> operator interpretation | consumers must not mistake historical evidence for live authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-09 | I | `Scoria.Runtime.RunDetail` approval projection | mitigate | Expose replay scope and executed-live state so historical approvals cannot be misread as current live authority. |
| T-38-10 | T | `Scoria.Runtime.RunDetail` checkpoint/event projection | mitigate | Surface `replay_disposition` and `replay_reason_code` directly so missing historical evidence cannot be masked as normal live execution. |
| T-38-11 | R | runtime replay posture projection | mitigate | Include persisted allowlist posture and executed-live facts so duplicate replay writes and override consumption remain operator-visible. |
| T-38-12 | T | runtime summary/detail reads under concurrent updates | mitigate | Continue reading durable row state and rely on plan-02 optimistic-lock enforcement so projections reflect the latest committed authority row instead of overwritten in-memory state. |
</threat_model>

<verification>
- `mix test test/scoria/runtime_view_test.exs`
- `mix test test/scoria/runtime_view_test.exs test/scoria/workflows/integration_test.exs`
</verification>

<success_criteria>
- Public runtime detail exposes seam-level replay dispositions, not just run-level replay intent.
- Replay posture and allowlist state are queryable through DTOs for later operator UI work.
- Projection tests lock in the difference between historical stubs, blocked seams, and actual live replay execution.
</success_criteria>

<output>
After completion, create `.planning/phases/38-replay-safe-execution-tool-modes/38-03-SUMMARY.md`
</output>
