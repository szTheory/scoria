---
phase: 51-default-lane-verifier-hardening-and-support-truth-re-closeout
plan: 02
type: execute
wave: 2
depends_on:
  - "51-01"
files_modified:
  - README.md
  - docs/operator_verification.md
  - lib/mix/tasks/scoria.install.ex
  - test/scoria/adoption_surface_test.exs
  - test/mix/tasks/scoria.install_test.exs
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
must_haves:
  truths:
    - "README, installer output, and operator verification guide publish the same lane ordering and prerequisite boundaries per D-11 and DOCS-01."
    - "The default lane still names `mix test.adoption` as the only canonical verifier, while semantic fast path, optional knowledge, and broad `mix test` remain explicitly separate per D-01, D-10, and D-12."
    - "Published support text no longer implies the old accidental timeout/env assumptions and stays truthful about verifier scope and optional-lane boundaries per D-03, D-04, and D-13."
  artifacts:
    - path: README.md
      provides: "Adopter-facing lane ordering and verifier naming"
    - path: docs/operator_verification.md
      provides: "Maintainer closeout chain and lane-specific verification guidance"
    - path: lib/mix/tasks/scoria.install.ex
      provides: "Installer CLI summary that reinforces the same default-lane command contract"
    - path: test/scoria/adoption_surface_test.exs
      provides: "Source-truth assertions for docs lane ordering and command boundaries"
    - path: test/mix/tasks/scoria.install_test.exs
      provides: "Installer output drift guard for the default and optional lane inventory"
  key_links:
    - from: README.md
      to: docs/operator_verification.md
      via: "canonical command and lane-boundary wording"
      pattern: "mix test\\.adoption|mix test\\.semantic_fast_path|mix test\\.knowledge"
    - from: lib/mix/tasks/scoria.install.ex
      to: test/mix/tasks/scoria.install_test.exs
      via: "installer summary assertions"
      pattern: "Default lane verifier: mix test\\.adoption"
    - from: test/scoria/adoption_surface_test.exs
      to: README.md
      via: "public docs command hierarchy assertions"
      pattern: "mix test\\.adoption"
---

<objective>
Re-align the public support surfaces to the repaired default-lane verifier truth.

Purpose: Once the verifier is bounded and green again, the public docs and installer output have to state the same command hierarchy and prerequisite boundaries without hidden timeout assumptions or accidental closeout drift.
Output: Updated adopter docs and installer copy, plus drift-prevention tests that pin the repaired support story.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-default-lane-verifier-hardening-and-support-truth-re-closeout/51-CONTEXT.md
@.planning/phases/51-default-lane-verifier-hardening-and-support-truth-re-closeout/51-RESEARCH.md
@.planning/phases/51-default-lane-verifier-hardening-and-support-truth-re-closeout/51-PATTERNS.md
@README.md
@docs/adoption_lanes.md
@docs/operator_verification.md
@lib/mix/tasks/scoria.install.ex
@test/scoria/adoption_surface_test.exs
@test/mix/tasks/scoria.install_test.exs

<interfaces>
From `lib/mix/tasks/scoria.install.ex`:
```elixir
Mix.shell().info("Scoria installed for the default Phoenix lane.")
Mix.shell().info("Default lane verifier: mix test.adoption")
...
Mix.shell().info("Optional later lanes:")
```

From `test/scoria/adoption_surface_test.exs`:
```elixir
assert content =~ "mix test.adoption"
assert content =~ "mix test.semantic_fast_path"
assert content =~ "mix test.knowledge"
refute content =~ "mix scoria.test.knowledge"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish one repaired lane hierarchy across README and operator verification</name>
  <read_first>
    - README.md
    - docs/operator_verification.md
    - docs/adoption_lanes.md
    - .planning/phases/51-default-lane-verifier-hardening-and-support-truth-re-closeout/51-CONTEXT.md
    - .planning/phases/49-support-truth-and-adoption-closeout/49-01-SUMMARY.md
  </read_first>
  <files>README.md, docs/operator_verification.md, test/scoria/adoption_surface_test.exs</files>
  <action>Follow the `Canonical Lane Naming` and `Source-Truth Drift Guards` patterns in `51-PATTERNS.md` when updating `README.md`, `docs/operator_verification.md`, and `test/scoria/adoption_surface_test.exs`. Preserve the default-lane order `mix scoria.install`, `mix ecto.migrate`, `mix test.adoption` per D-01 and D-10, truthfully reflect the repaired verifier budget from Plan 51-01 without advertising `--trace` or suite-wide timeout changes per D-03 and D-04, keep the bounded semantic lane on `SCORIA_DB_PORT=55432 SCORIA_DB_PASSWORD=postgres MIX_ENV=test mix test.semantic_fast_path`, keep the optional knowledge lane on `mix scoria.pgvector.bootstrap` then `mix test.knowledge`, and keep broad `mix test` as repo-health context only per D-12. Update `test/scoria/adoption_surface_test.exs` so the assertions pin the repaired wording and explicitly exclude `mix test.semantic_fast_path`, `mix test.knowledge`, and `mix test` from the canonical Phase 49 closeout chain.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/adoption_surface_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `mix scoria.install`, `mix ecto.migrate`, and `mix test.adoption` in that order for the default lane.
    - `docs/operator_verification.md` contains the exact canonical closeout chain `mix scoria.release_preview` followed by `mix test.adoption`.
    - `docs/operator_verification.md` does not present `mix test.adoption --trace` as a support command.
    - `test/scoria/adoption_surface_test.exs` asserts the operator guide keeps `mix test.semantic_fast_path`, `mix test.knowledge`, and `mix test` outside the canonical closeout chain.
  </acceptance_criteria>
  <done>The adopter-facing docs now tell one consistent, repaired verifier story across default, semantic, knowledge, and repo-health lanes.</done>
</task>

<task type="auto">
  <name>Task 2: Align installer output with the repaired default-lane contract</name>
  <read_first>
    - lib/mix/tasks/scoria.install.ex
    - test/mix/tasks/scoria.install_test.exs
    - README.md
    - docs/operator_verification.md
    - .planning/phases/49-support-truth-and-adoption-closeout/49-02-SUMMARY.md
  </read_first>
  <files>lib/mix/tasks/scoria.install.ex, test/mix/tasks/scoria.install_test.exs</files>
  <action>Follow the `Canonical Lane Naming` and `Source-Truth Drift Guards` patterns in `51-PATTERNS.md` when adjusting `Mix.Tasks.Scoria.Install.print_summary/1` and `test/mix/tasks/scoria.install_test.exs`. Keep `Default lane verifier: mix test.adoption` as the only public default verifier per D-01; keep the semantic fast-path command on the explicit `SCORIA_DB_PORT=55432 SCORIA_DB_PASSWORD=postgres MIX_ENV=test mix test.semantic_fast_path` contract; keep `mix scoria.pgvector.bootstrap` plus `mix test.knowledge` as optional-lane commands; keep missing-Tailwind guidance aligned with the same lane hierarchy from Task 1; and do not add any second verifier or hidden env assumptions. Update `test/mix/tasks/scoria.install_test.exs` to assert the exact emitted strings the installer should print.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
    - `lib/mix/tasks/scoria.install.ex` prints `Default lane verifier: mix test.adoption`.
    - The installer output still prints the semantic fast-path command with `SCORIA_DB_PORT=55432` and `MIX_ENV=test`.
    - The installer output still prints `mix scoria.pgvector.bootstrap` and `mix test.knowledge` only under optional later lanes.
    - `test/mix/tasks/scoria.install_test.exs` asserts those exact strings.
  </acceptance_criteria>
  <done>The installer CLI now reinforces the same verifier and optional-lane boundaries as the docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/task copy -> adopter support claims | If README, operator docs, and installer output drift, Scoria can claim unsupported verifier behavior |
| verifier env guidance -> user execution | Hidden env or port assumptions can turn green local runs into misleading support guidance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-04 | I | `README.md`, `docs/operator_verification.md`, `lib/mix/tasks/scoria.install.ex` | mitigate | Update all three public surfaces together so they publish the same default-lane and optional-lane command hierarchy |
| T-51-05 | T | `test/scoria/adoption_surface_test.exs`, `test/mix/tasks/scoria.install_test.exs` | mitigate | Strengthen drift guards to reject false support claims and ensure the closeout chain excludes semantic, knowledge, and broad-suite commands |
| T-51-06 | I | env/port guidance | mitigate | Keep the semantic lane’s explicit `SCORIA_DB_PORT=55432` contract scoped to that lane and avoid leaking it into the default adoption verifier |
</threat_model>

<verification>
- `MIX_ENV=test mix test test/scoria/adoption_surface_test.exs --trace`
- `MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs --trace`
</verification>

<success_criteria>
- README, operator guide, and installer output name the same canonical commands.
- The default lane stays `mix test.adoption` only.
- Semantic, knowledge, and broad-suite commands remain explicitly outside the canonical default-lane and Phase 49 closeout chain.
</success_criteria>

<output>
After completion, create `.planning/phases/51-default-lane-verifier-hardening-and-support-truth-re-closeout/51-02-SUMMARY.md`
</output>
