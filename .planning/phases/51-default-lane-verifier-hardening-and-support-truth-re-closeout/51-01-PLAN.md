---
phase: 51-default-lane-verifier-hardening-and-support-truth-re-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/scoria/host_app_consumer_proof_test.exs
  - test/support/scoria/host_app_proof/runner.ex
  - test/mix/tasks/test.adoption_test.exs
autonomous: true
requirements:
  - DOCS-02
must_haves:
  truths:
    - "`mix test.adoption` remains the single canonical default-lane verifier per D-01."
    - "The fresh generated-host proof still runs inside `mix test.adoption` with an explicit scoped timeout instead of relying on the ExUnit default per D-02, D-03, and D-05."
    - "Host-proof runtime cost drops by collapsing duplicate host-side Mix boots without replacing the fresh host with a checked-in, synthetic, or warm post-install fixture per D-06 through D-09."
  artifacts:
    - path: test/scoria/host_app_consumer_proof_test.exs
      provides: "Scoped timeout contract for the generated-host proof"
    - path: test/support/scoria/host_app_proof/runner.ex
      provides: "Fresh-host proof runner that preserves the public proof steps while reducing duplicate harness work"
    - path: test/mix/tasks/test.adoption_test.exs
      provides: "Task-boundary assertions that keep the adoption lane canonical and bounded"
  key_links:
    - from: test/scoria/host_app_consumer_proof_test.exs
      to: test/support/scoria/host_app_proof/runner.ex
      via: "Runner.run_full_proof!/1"
      pattern: "run_full_proof!"
    - from: test/mix/tasks/test.adoption_test.exs
      to: lib/mix/tasks/test.adoption.ex
      via: "adoption task file list assertions"
      pattern: "adoption_test_files"
---

<objective>
Make the canonical adoption verifier bounded and honest without changing its public shape.

Purpose: Phase 51’s main failure is not docs copy but the default-lane verifier timing out on the generated-host proof. This plan repairs that proof seam while preserving `mix test.adoption` as the one public verifier and keeping the generated-host proof inside it.
Output: A scoped timeout for the host proof, a cheaper fresh-host runner that still executes the public install/migrate/runtime steps, and adoption-task assertions that keep the lane bounded.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-default-lane-verifier-hardening-and-support-truth-re-closeout/51-CONTEXT.md
@.planning/phases/51-default-lane-verifier-hardening-and-support-truth-re-closeout/51-RESEARCH.md
@.planning/phases/51-default-lane-verifier-hardening-and-support-truth-re-closeout/51-PATTERNS.md
@.planning/phases/51-default-lane-verifier-hardening-and-support-truth-re-closeout/51-VALIDATION.md
@lib/mix/tasks/test.adoption.ex
@test/mix/tasks/test.adoption_test.exs
@test/scoria/host_app_consumer_proof_test.exs
@test/support/scoria/host_app_proof/runner.ex

<interfaces>
From `test/scoria/host_app_consumer_proof_test.exs`:
```elixir
proof = Runner.run_full_proof!(host)

assert proof.steps == [
  :deps_get,
  :scoria_install,
  :ecto_create,
  :ecto_migrate,
  :route_smoke,
  :runtime_smoke
]
```

From `test/support/scoria/host_app_proof/runner.ex`:
```elixir
def deps_get!(host), do: run_mix!(host, :deps_get, ["deps.get"])
def scoria_install!(host), do: run_mix!(host, :scoria_install, ["scoria.install"])
def ecto_create!(host), do: run_mix!(host, :ecto_create, ["ecto.create"])
def ecto_migrate!(host), do: run_mix!(host, :ecto_migrate, ["ecto.migrate"])
def route_smoke!(host), do: run_mix!(host, :route_smoke, ["test", host.route_smoke_test, "--trace"])
def runtime_smoke!(host), do: run_mix!(host, :runtime_smoke, ["test", host.runtime_smoke_test, "--trace"])
def run_full_proof!(host), do: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Put an explicit verifier-scoped timeout on the generated-host proof</name>
  <read_first>
    - test/scoria/host_app_consumer_proof_test.exs
    - .planning/phases/51-default-lane-verifier-hardening-and-support-truth-re-closeout/51-CONTEXT.md
    - .planning/phases/51-default-lane-verifier-hardening-and-support-truth-re-closeout/51-RESEARCH.md
    - test/test_helper.exs
  </read_first>
  <files>test/scoria/host_app_consumer_proof_test.exs</files>
  <action>Add a scoped timeout directly on `Scoria.HostAppConsumerProofTest` or on its single `generated Phoenix host proves the bounded Scoria adoption path` test per D-02 and D-03. Use an explicit millisecond value in the 2-3 minute range recommended by research, such as `@moduletag timeout: 180_000`, unless the implementation proves a smaller honest budget with margin. Do not raise suite-wide ExUnit defaults, do not change `lib/mix/tasks/test.adoption.ex`, and do not introduce any second public verifier or `--trace` contract per D-01 and D-04.</action>
  <verify>
    <automated>rg -n "@(moduletag|tag) timeout: [0-9_]+" test/scoria/host_app_consumer_proof_test.exs && MIX_ENV=test mix test test/scoria/host_app_consumer_proof_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
    - `test/scoria/host_app_consumer_proof_test.exs` contains an explicit `timeout:` tag.
    - The timeout is local to the host proof file or test, not added in `test/test_helper.exs`.
    - `lib/mix/tasks/test.adoption.ex` remains the only public adoption verifier task entrypoint.
    - `MIX_ENV=test mix test test/scoria/host_app_consumer_proof_test.exs --trace` completes without an `ExUnit.TimeoutError`.
  </acceptance_criteria>
  <done>The slow host proof now has an honest published budget instead of inheriting the accidental 60-second default.</done>
</task>

<task type="auto">
  <name>Task 2: Collapse duplicate host-side Mix boots without weakening fresh-host proof</name>
  <read_first>
    - test/support/scoria/host_app_proof/runner.ex
    - test/scoria/host_app_consumer_proof_test.exs
    - .planning/phases/51-default-lane-verifier-hardening-and-support-truth-re-closeout/51-PATTERNS.md
    - .planning/phases/51-default-lane-verifier-hardening-and-support-truth-re-closeout/51-CONTEXT.md
    - test/mix/tasks/test.adoption_test.exs
  </read_first>
  <files>test/support/scoria/host_app_proof/runner.ex, test/mix/tasks/test.adoption_test.exs</files>
  <action>Refactor `Scoria.TestSupport.HostAppProof.Runner` so the fresh host still executes `deps.get`, `scoria.install`, `ecto.create`, `ecto.migrate`, and one combined smoke proof on the copied host per D-05 through D-08. The preferred optimization is to replace separate `route_smoke!` and `runtime_smoke!` host `mix test` invocations with one combined host-side test command that runs both `host.route_smoke_test` and `host.runtime_smoke_test` in the same `mix test ... --trace` process while preserving step visibility in the returned proof data. Keep failure output explicit, keep the host generated fresh for every run, and do not add cached post-install or post-migrate hosts per D-09. If the task boundary expectations in `test/mix/tasks/test.adoption_test.exs` need to mention the preserved host proof, update them there only.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/host_app_consumer_proof_test.exs test/mix/tasks/test.adoption_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
    - `test/support/scoria/host_app_proof/runner.ex` still runs `deps.get`, `scoria.install`, `ecto.create`, and `ecto.migrate` on the fresh host.
    - The runner uses a single host-side smoke `mix test` invocation for both `host.route_smoke_test` and `host.runtime_smoke_test`, or an equivalently bounded batching change that preserves both assertions.
    - No checked-in sample host, synthetic fixture, or warm post-install host path is introduced.
    - `MIX_ENV=test mix test test/scoria/host_app_consumer_proof_test.exs test/mix/tasks/test.adoption_test.exs --trace` passes.
  </acceptance_criteria>
  <done>The canonical verifier keeps the same fresh-host proof surface but pays less duplicate harness overhead.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier task -> generated host | The public verifier relies on an expensive fresh-host proof that can fail or pass for the wrong reason if its timeout or boot strategy is dishonest |
| host harness -> support contract | Internal runner optimizations can accidentally weaken the product claim if they stop proving install/create/migrate/runtime on a fresh host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | D | `test/scoria/host_app_consumer_proof_test.exs` | mitigate | Apply a host-proof-only timeout tag so the canonical verifier stops failing at the ExUnit default while the rest of the suite keeps normal budgets |
| T-51-02 | T | `test/support/scoria/host_app_proof/runner.ex` | mitigate | Batch duplicate host-side `mix test` work only after `deps.get`, `scoria.install`, `ecto.create`, and `ecto.migrate` still run on a fresh copied host |
| T-51-03 | S | host-proof caching/freshness | mitigate | Reject checked-in sample apps, synthetic fixtures, and warm post-install hosts; any caching must remain pristine-skeleton-only per D-09 |
</threat_model>

<verification>
- `rg -n "@(moduletag|tag) timeout: [0-9_]+" test/scoria/host_app_consumer_proof_test.exs`
- `MIX_ENV=test mix test test/scoria/host_app_consumer_proof_test.exs --trace`
- `MIX_ENV=test mix test test/scoria/host_app_consumer_proof_test.exs test/mix/tasks/test.adoption_test.exs --trace`
</verification>

<success_criteria>
- `mix test.adoption` remains the single public default-lane verifier.
- The generated-host proof has an explicit scoped timeout and no longer relies on the ExUnit default.
- The runner reduces duplicate harness work without weakening fresh-host adoption proof.
</success_criteria>

<output>
After completion, create `.planning/phases/51-default-lane-verifier-hardening-and-support-truth-re-closeout/51-01-SUMMARY.md`
</output>
