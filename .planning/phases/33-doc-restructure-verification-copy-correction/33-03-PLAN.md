---
phase: 33-doc-restructure-verification-copy-correction
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/scoria.ui.shots.ex
  - lib/mix/tasks/scoria.ui.e2e.ex
  - priv/repo/dev_seed.exs
  - priv/dev/shots.mjs
  - priv/dev/e2e/playwright.config.mjs
  - priv/dev/e2e/uat.spec.mjs
  - priv/dev/e2e/command_palette.spec.mjs
  - priv/dev/e2e/ia_orientation.spec.mjs
  - priv/dev/e2e/phase16_parity.spec.mjs
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "Host screenshot and e2e harness defaults use `http://localhost:4799/scoria` per D-09 and D-19."
    - "Harness docs tell operators to start Scoria with `make dev`, not a raw Phoenix command, while Docker profile URLs that use service-name `web:4000` stay untouched per D-20."
    - "Generated static assets, Compose ports, Traefik labels, CI ports, and `config/dev.exs` fallback remain unchanged per D-20."
  artifacts:
    - path: "lib/mix/tasks/scoria.ui.shots.ex"
      provides: "Screenshot task help/default URL"
      contains: "http://localhost:4799/scoria"
    - path: "lib/mix/tasks/scoria.ui.e2e.ex"
      provides: "E2E task help/default URL"
      contains: "http://localhost:4799/scoria"
    - path: "priv/dev/e2e/playwright.config.mjs"
      provides: "Playwright default base URL"
      contains: "http://localhost:4799/scoria"
  key_links:
    - from: "lib/mix/tasks/scoria.ui.shots.ex"
      to: "priv/dev/shots.mjs"
      via: "Mix task passes --base-url to the Node screenshot script"
      pattern: "--base-url"
    - from: "lib/mix/tasks/scoria.ui.e2e.ex"
      to: "priv/dev/e2e/playwright.config.mjs"
      via: "Mix task sets PLAYWRIGHT_BASE_URL for Playwright"
      pattern: "PLAYWRIGHT_BASE_URL"
    - from: "priv/repo/dev_seed.exs"
      to: "Makefile"
      via: "Seed completion output matches make dev native URL"
      pattern: "localhost:4799/scoria"
---

<objective>
Correct user-facing host harness docs and defaults that still point screenshot/e2e operators at stale Scoria dashboard URLs.

Purpose: DOCS-01 and D-19 include code-adjacent dev-harness copy/defaults because these surfaces print or default browser URLs for maintainers.
Output: Host screenshot/e2e Mix tasks, Playwright scripts/spec defaults, and seed completion copy all use native 4799 Scoria dashboard URLs.
</objective>

## Artifacts This Phase Produces

- `lib/mix/tasks/scoria.ui.shots.ex` changed: help text, prerequisites, screenshot default URL, and critique default URL use `make dev` / `http://localhost:4799/scoria`.
- `lib/mix/tasks/scoria.ui.e2e.ex` changed: help text, prerequisites, default base URL, and error recovery use `make dev` / `http://localhost:4799/scoria`.
- `priv/dev/shots.mjs` changed: usage comment and default `baseUrl` use `http://localhost:4799/scoria`.
- `priv/dev/e2e/*.spec.mjs` and `priv/dev/e2e/playwright.config.mjs` changed: default `BASE` / `baseURL` and prerequisite comments use `http://localhost:4799/scoria` and `make dev`.
- `priv/repo/dev_seed.exs` changed: completion banner prints `http://localhost:4799/scoria`.
- No generated `priv/static/scoria/app.js`, Compose service port, Traefik label, Dockerfile `EXPOSE`, CI `PORT`, or `config/dev.exs` runtime fallback changes.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-doc-restructure-verification-copy-correction/33-CONTEXT.md
@.planning/phases/33-doc-restructure-verification-copy-correction/33-RESEARCH.md
@.planning/phases/33-doc-restructure-verification-copy-correction/33-VALIDATION.md
@Makefile
@config/dev.exs
@compose.yml
@lib/mix/tasks/scoria.ui.shots.ex
@lib/mix/tasks/scoria.ui.e2e.ex
@priv/dev/shots.mjs
@priv/dev/e2e/playwright.config.mjs
@priv/dev/e2e/uat.spec.mjs
@priv/dev/e2e/command_palette.spec.mjs
@priv/dev/e2e/ia_orientation.spec.mjs
@priv/dev/e2e/phase16_parity.spec.mjs
@priv/repo/dev_seed.exs
</context>

<tasks>

<task id="33-03-01" type="auto">
  <name>Task 1: Correct Mix task docs/defaults and seed completion copy</name>
  <files>lib/mix/tasks/scoria.ui.shots.ex, lib/mix/tasks/scoria.ui.e2e.ex, priv/repo/dev_seed.exs</files>
  <read_first>
    - lib/mix/tasks/scoria.ui.shots.ex
    - lib/mix/tasks/scoria.ui.e2e.ex
    - priv/repo/dev_seed.exs
    - .planning/phases/33-doc-restructure-verification-copy-correction/33-CONTEXT.md
    - Makefile
    - config/dev.exs
  </read_first>
  <action>
    Update the host Mix task moduledocs, default URL assignments, and recovery copy per D-08, D-09, D-19, D-20, D-28, and D-29. In both Mix tasks, default the host browser URL to `http://localhost:4799/scoria`, instruct maintainers to start Scoria with `make dev`, and keep the tasks from booting a web server themselves. Update `priv/repo/dev_seed.exs` so its final printed dashboard URL matches the native host default. Do not change `config/dev.exs` fallback, Compose service URLs, Docker profile URLs, or any generated static asset.
  </action>
  <acceptance_criteria>
    - `lib/mix/tasks/scoria.ui.shots.ex` default URL and help text use `http://localhost:4799/scoria`.
    - `lib/mix/tasks/scoria.ui.e2e.ex` default URL and help text use `http://localhost:4799/scoria`.
    - Both Mix task prerequisite/recovery blocks instruct `make dev`.
    - `priv/repo/dev_seed.exs` prints `Done. Open http://localhost:4799/scoria`.
    - The scoped harness grep for stale Scoria dashboard URL or raw start-command copy returns zero hits across the files in this task.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail
rg -n "http://localhost:4799/scoria|make dev" lib/mix/tasks/scoria.ui.shots.ex lib/mix/tasks/scoria.ui.e2e.ex priv/repo/dev_seed.exs
rg -n "Done\\. Open http://localhost:4799/scoria" priv/repo/dev_seed.exs
! rg -n "localhost:4000/scoria|mix phx\\.server" lib/mix/tasks/scoria.ui.shots.ex lib/mix/tasks/scoria.ui.e2e.ex priv/repo/dev_seed.exs
make -n dev | rg -n "http://localhost:4799/scoria|PORT=4799"
SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/package_surface_test.exs test/scoria/support_journey_source_test.exs'</automated>
  </verify>
  <done>The Elixir-facing harness surfaces no longer print or default to the old Scoria dashboard browser URL.</done>
</task>

<task id="33-03-02" type="auto">
  <name>Task 2: Correct Playwright entrypoint defaults and UAT prerequisite comments</name>
  <files>priv/dev/shots.mjs, priv/dev/e2e/playwright.config.mjs, priv/dev/e2e/uat.spec.mjs</files>
  <read_first>
    - priv/dev/shots.mjs
    - priv/dev/e2e/playwright.config.mjs
    - priv/dev/e2e/uat.spec.mjs
    - .planning/phases/33-doc-restructure-verification-copy-correction/33-CONTEXT.md
    - lib/mix/tasks/scoria.ui.shots.ex
    - lib/mix/tasks/scoria.ui.e2e.ex
  </read_first>
  <action>
    Update the Node screenshot script usage/default URL, Playwright config fallback, and UAT spec prerequisite comments per D-09, D-19, and D-20. The fallback URL for host-run harnesses must be `http://localhost:4799/scoria`; prerequisite comments should name `make dev` as the Scoria repo dashboard start path. Keep `PLAYWRIGHT_BASE_URL` override support unchanged.
  </action>
  <acceptance_criteria>
    - `priv/dev/shots.mjs` usage comment and `baseUrl` default use `http://localhost:4799/scoria`.
    - `priv/dev/e2e/playwright.config.mjs` fallback `baseURL` uses `http://localhost:4799/scoria`.
    - `priv/dev/e2e/uat.spec.mjs` fallback `BASE` uses `http://localhost:4799/scoria`.
    - UAT prerequisite comments instruct `make dev`.
    - `PLAYWRIGHT_BASE_URL` remains the environment override path.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail
rg -n "http://localhost:4799/scoria|make dev|PLAYWRIGHT_BASE_URL" priv/dev/shots.mjs priv/dev/e2e/playwright.config.mjs priv/dev/e2e/uat.spec.mjs
! rg -n "localhost:4000/scoria|mix phx\\.server" priv/dev/shots.mjs priv/dev/e2e/playwright.config.mjs priv/dev/e2e/uat.spec.mjs
SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/package_surface_test.exs test/scoria/support_journey_source_test.exs'</automated>
  </verify>
  <done>Playwright entrypoint defaults and local-run comments align with the native 4799 host harness.</done>
</task>

<task id="33-03-03" type="auto">
  <name>Task 3: Correct remaining e2e spec base URL constants</name>
  <files>priv/dev/e2e/command_palette.spec.mjs, priv/dev/e2e/ia_orientation.spec.mjs, priv/dev/e2e/phase16_parity.spec.mjs</files>
  <read_first>
    - priv/dev/e2e/command_palette.spec.mjs
    - priv/dev/e2e/ia_orientation.spec.mjs
    - priv/dev/e2e/phase16_parity.spec.mjs
    - .planning/phases/33-doc-restructure-verification-copy-correction/33-CONTEXT.md
    - priv/dev/e2e/playwright.config.mjs
  </read_first>
  <action>
    Update remaining Playwright spec fallback `BASE` constants per D-19 so local direct spec execution lands on `http://localhost:4799/scoria`. Preserve the existing `PLAYWRIGHT_BASE_URL` override and do not alter test behavior, routes, assertions, retries, or fixtures.
  </action>
  <acceptance_criteria>
    - Each listed spec has a fallback `BASE` value of `http://localhost:4799/scoria`.
    - Each listed spec still reads `process.env.PLAYWRIGHT_BASE_URL` first.
    - No listed spec contains stale Scoria dashboard URL copy after the edit.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail
for f in priv/dev/e2e/command_palette.spec.mjs priv/dev/e2e/ia_orientation.spec.mjs priv/dev/e2e/phase16_parity.spec.mjs; do rg -n "process\\.env\\.PLAYWRIGHT_BASE_URL \\|\\| .http://localhost:4799/scoria." "$f" >/dev/null; done
! rg -n "localhost:4000/scoria|mix phx\\.server" priv/dev/e2e/command_palette.spec.mjs priv/dev/e2e/ia_orientation.spec.mjs priv/dev/e2e/phase16_parity.spec.mjs
rg -n "localhost:4000/scoria|mix phx\\.server" lib/mix/tasks/scoria.ui.*.ex priv/dev priv/repo/dev_seed.exs || true
SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/package_surface_test.exs test/scoria/support_journey_source_test.exs'</automated>
  </verify>
  <done>All host e2e spec fallbacks point at native 4799 while preserving environment override behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer -> harness CLI | A maintainer runs Mix tasks or Playwright specs against a local server URL. |
| docs/defaults -> generated/static assets | Source defaults must not drive edits to generated app bundle artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-07 | Spoofing | Host harness default URLs | mitigate | Default all host screenshot/e2e surfaces to native `localhost:4799` and keep `PLAYWRIGHT_BASE_URL` override explicit. |
| T-33-08 | Tampering | Protected runtime/dev infrastructure | mitigate | Do not edit generated static assets, Compose ports, Traefik labels, Dockerfile exposed ports, CI ports, or `config/dev.exs` fallback per D-20. |
| T-33-09 | Denial of Service | Harness execution guidance | accept | Tasks do not start servers or run browser automation; they update copy/defaults and rely on static/source-contract checks. |
| T-33-SC | Tampering | package installs | accept | No package-manager installs are planned; existing Node/Playwright dependencies are not modified. |
</threat_model>

<verification>
Run the D-28 scoped harness sweep:
`rg -n "localhost:4000/scoria|mix phx\\.server" lib/mix/tasks/scoria.ui.*.ex priv/dev priv/repo/dev_seed.exs`
Expected result after this plan: zero hits in the host harness surfaces.
</verification>

<success_criteria>
DOCS-01 is complete for code-adjacent harness surfaces when every user-facing host default/instruction uses `make dev` and `http://localhost:4799/scoria`, and protected Docker/internal wiring is untouched.
</success_criteria>

<output>
Create `.planning/phases/33-doc-restructure-verification-copy-correction/33-03-SUMMARY.md` when done.
</output>
