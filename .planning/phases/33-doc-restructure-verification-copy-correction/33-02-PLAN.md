---
phase: 33-doc-restructure-verification-copy-correction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/MAINTAINERS.md
  - docs/uat_automation.md
  - README.md
  - docs/operator_verification.md
  - docs/support_copilot_gallery.md
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "Maintainer docs use Docker-first Scoria dashboard wording for repo dashboard startup per D-06 and D-07."
    - "Native screenshot and browser UAT docs use `make dev` and `http://localhost:4799/scoria` per D-08 and D-09."
    - "Support-copilot gallery docs remain correct for the separate gallery app and explicitly name `http://localhost:4010/` and `http://localhost:4010/scoria` per D-18."
  artifacts:
    - path: "docs/MAINTAINERS.md"
      provides: "Maintainer screenshot/e2e copy corrected to native 4799"
      contains: "mix scoria.ui.shots --url http://localhost:4799/scoria"
    - path: "docs/uat_automation.md"
      provides: "Browser UAT local-run copy corrected to make dev"
      contains: "http://localhost:4799/scoria"
    - path: "docs/support_copilot_gallery.md"
      provides: "Qualified gallery-app startup and URLs"
      contains: "http://localhost:4010/scoria"
  key_links:
    - from: "docs/MAINTAINERS.md"
      to: "lib/mix/tasks/scoria.ui.shots.ex"
      via: "Maintainer command uses the same host harness URL default"
      pattern: "http://localhost:4799/scoria"
    - from: "docs/uat_automation.md"
      to: "lib/mix/tasks/scoria.ui.e2e.ex"
      via: "UAT command uses the same e2e base URL"
      pattern: "mix scoria.ui.e2e --base-url http://localhost:4799/scoria"
    - from: "docs/support_copilot_gallery.md"
      to: "examples/support_copilot/config/dev.exs"
      via: "Gallery docs state the separate app's 4010 URLs"
      pattern: "localhost:4010"
---

<objective>
Correct active docs that currently point maintainers or adopters at stale Scoria dev-start copy.

Purpose: DOCS-01 requires active docs to use `make up` / `make dev` and real browser URLs, while preserving the separate support-copilot gallery app path.
Output: Updated README/operator/maintainer/UAT/gallery docs with context-specific commands and allowed exceptions clearly labeled.
</objective>

## Artifacts This Phase Produces

- `docs/MAINTAINERS.md` changed: screenshot and critique harness startup copy uses `make dev` and `http://localhost:4799/scoria`.
- `docs/uat_automation.md` changed: real-browser UAT local-run copy uses `make dev` and `mix scoria.ui.e2e --base-url http://localhost:4799/scoria`.
- `README.md`, `docs/operator_verification.md`, and `docs/support_copilot_gallery.md` changed: support-copilot gallery startup is qualified as the separate `examples/support_copilot` Phoenix app on `http://localhost:4010/` and `http://localhost:4010/scoria`.
- No code defaults, routes, ports, Compose services, Traefik labels, or generated assets are changed by this plan.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-doc-restructure-verification-copy-correction/33-CONTEXT.md
@.planning/phases/33-doc-restructure-verification-copy-correction/33-RESEARCH.md
@.planning/phases/33-doc-restructure-verification-copy-correction/33-VALIDATION.md
@README.md
@docs/operator_verification.md
@docs/MAINTAINERS.md
@docs/uat_automation.md
@docs/support_copilot_gallery.md
@Makefile
@examples/support_copilot/config/dev.exs
@examples/support_copilot/README.md
@brandbook/README.md
@brandbook/brand-book.md
</context>

<tasks>

<task id="33-02-01" type="auto">
  <name>Task 1: Correct maintainer screenshot and UAT local-run docs</name>
  <files>docs/MAINTAINERS.md, docs/uat_automation.md</files>
  <read_first>
    - docs/MAINTAINERS.md
    - docs/uat_automation.md
    - .planning/phases/33-doc-restructure-verification-copy-correction/33-CONTEXT.md
    - Makefile
    - lib/mix/tasks/scoria.ui.shots.ex
    - lib/mix/tasks/scoria.ui.e2e.ex
    - priv/dev/e2e/playwright.config.mjs
  </read_first>
  <action>
    Update maintainer screenshot/e2e and UAT docs per D-06, D-08, D-09, D-16, D-17, D-21, D-22, D-23, D-24, D-25, and D-26. In `docs/MAINTAINERS.md`, replace the screenshot pass startup sequence with `make dev` in one shell and `mix scoria.ui.shots --url http://localhost:4799/scoria` in another shell; keep the seed-first instruction if it remains accurate, but make the browser URL match native 4799. In `docs/uat_automation.md`, replace the Scoria dashboard local-run sequence with `make dev` and `mix scoria.ui.e2e --base-url http://localhost:4799/scoria`. Keep the docs calm, exact, and useful; use operator-first nouns from D-23 and do not add new setup not backed by the Makefile or harness files.
  </action>
  <acceptance_criteria>
    - `docs/MAINTAINERS.md` contains `make dev` before the screenshot/e2e harness commands.
    - `docs/MAINTAINERS.md` contains `mix scoria.ui.shots --url http://localhost:4799/scoria`.
    - `docs/uat_automation.md` contains `mix scoria.ui.e2e --base-url http://localhost:4799/scoria`.
    - `docs/uat_automation.md` has zero `PORT=4010`, `localhost:4010`, and raw Phoenix start-command references for the Scoria dashboard.
    - `README.md`, `docs/operator_verification.md`, and `docs/MAINTAINERS.md` have zero `localhost:4000` hits.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail
rg -n "make dev" docs/MAINTAINERS.md docs/uat_automation.md
rg -n "mix scoria\\.ui\\.shots --url http://localhost:4799/scoria" docs/MAINTAINERS.md
rg -n "mix scoria\\.ui\\.e2e --base-url http://localhost:4799/scoria" docs/uat_automation.md
! rg -n "PORT=4010|localhost:4010|mix phx\\.server" docs/uat_automation.md
! rg -n "localhost:4000" docs/operator_verification.md docs/MAINTAINERS.md README.md
SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/ci_policy_contract_test.exs'</automated>
  </verify>
  <done>Maintainer and UAT docs no longer direct Scoria dashboard startup through stale native command/URL copy.</done>
</task>

<task id="33-02-02" type="auto">
  <name>Task 2: Qualify support-copilot gallery docs without changing its app command</name>
  <files>README.md, docs/operator_verification.md, docs/support_copilot_gallery.md</files>
  <read_first>
    - README.md
    - docs/operator_verification.md
    - docs/support_copilot_gallery.md
    - .planning/phases/33-doc-restructure-verification-copy-correction/33-CONTEXT.md
    - examples/support_copilot/config/dev.exs
    - examples/support_copilot/README.md
  </read_first>
  <action>
    Update README, operator verification, and gallery docs per D-16 and D-18. Preserve the support-copilot gallery's separate Phoenix app startup path, but make every occurrence explicitly say it runs from `examples/support_copilot` and serves the gallery host chat at `http://localhost:4010/` plus its gallery-local Scoria operator surface at `http://localhost:4010/scoria`. Do not replace the gallery app command with Scoria repo `make dev`; do not imply that `localhost:4010/scoria` is the Scoria repo dashboard. If README or operator verification keeps the gallery quickstart inline, the surrounding text must identify it as the gallery app. If they link to `docs/support_copilot_gallery.md` instead, the linked doc must carry the exact command and URL qualification.
  </action>
  <acceptance_criteria>
    - `docs/support_copilot_gallery.md` states that the gallery app lives under `examples/support_copilot`.
    - `docs/support_copilot_gallery.md` states `http://localhost:4010/` for host chat and `http://localhost:4010/scoria` for the gallery-local operator surface.
    - Any raw Phoenix server command left in README, operator verification, or gallery docs is only inside support-copilot gallery-app instructions and is adjacent to `examples/support_copilot` or `localhost:4010`.
    - `README.md`, `docs/operator_verification.md`, and `docs/MAINTAINERS.md` have zero `localhost:4000` hits.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail
rg -n "examples/support_copilot" README.md docs/operator_verification.md docs/support_copilot_gallery.md
rg -n "http://localhost:4010/|http://localhost:4010/scoria" docs/support_copilot_gallery.md
! rg -n "localhost:4000" docs/operator_verification.md docs/MAINTAINERS.md README.md
rg -n "PORT=4010|localhost:4010|localhost:4000|mix phx\\.server" docs/uat_automation.md docs/support_copilot_gallery.md || true
SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/package_surface_test.exs test/scoria/support_journey_source_test.exs'</automated>
  </verify>
  <done>Gallery docs remain accurate for the separate example app, while active Scoria repo docs no longer confuse that path with the repo dashboard startup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader -> local shell | A maintainer copies active docs commands into a shell. |
| Scoria repo docs -> gallery app docs | Docs describe two different Phoenix apps with different ports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04 | Spoofing | README/operator/gallery docs | mitigate | Qualify gallery URLs as the separate `examples/support_copilot` app and keep Scoria repo dashboard URLs on `make url` / native 4799. |
| T-33-05 | Tampering | UAT and maintainer copy | mitigate | Replace Scoria dashboard local-run docs with `make dev` and explicit 4799 harness commands, reducing wrong-app verification. |
| T-33-06 | Information Disclosure | Maintainer critique docs | mitigate | Do not reintroduce plaintext provider key examples; keep Phase 32 process-scoped secret pattern linked from Docker DX docs. |
| T-33-SC | Tampering | package installs | accept | No package-manager installs are planned; existing tests only. |
</threat_model>

<verification>
Run D-26 active-doc checks and the relevant source-contract tests:
`rg -n "localhost:4000|mix phx\\.server" README.md docs/operator_verification.md docs/MAINTAINERS.md docs/uat_automation.md docs/support_copilot_gallery.md`
Any remaining raw Phoenix server hit must be classified as support-copilot gallery-app startup in the SUMMARY.
</verification>

<success_criteria>
DOCS-01 is complete for active docs when no Scoria repo dashboard start/open/verify text points at the old fixed-port browser URL, and gallery app copy is explicitly separated from Scoria repo dashboard copy.
</success_criteria>

<output>
Create `.planning/phases/33-doc-restructure-verification-copy-correction/33-02-SUMMARY.md` when done.
</output>
