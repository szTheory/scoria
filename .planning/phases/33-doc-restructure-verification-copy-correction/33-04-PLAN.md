---
phase: 33-doc-restructure-verification-copy-correction
plan: 04
type: execute
wave: 2
depends_on:
  - 33-01
  - 33-02
  - 33-03
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/todos/pending/docker-dx-fleet-hardening.md
  - .planning/research/ARCHITECTURE.md
  - .planning/research/FEATURES.md
  - .planning/research/PITFALLS.md
  - .planning/research/STACK.md
  - .planning/research/SUMMARY.md
  - .planning/phases/33-doc-restructure-verification-copy-correction/33-PLANNING-SWEEP.md
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "Active living `.planning` guidance no longer tells verifiers to start or open Scoria through stale dev-start copy per D-10, D-12, and D-13."
    - "Historical milestone archives, debug history, memory, completed todos, and old shipped audits are not mutated per D-14."
    - "Remaining active planning hits are classified in the SUMMARY as implementation evidence, quoted rationale, current phase verification pattern, or a defect fixed by this plan per D-15 and D-27."
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Current milestone and state copy corrected or qualified"
      contains: "make up"
    - path: ".planning/research/FEATURES.md"
      provides: "Living research feature guidance corrected or marked as completed"
      contains: "make dev"
    - path: ".planning/todos/pending/docker-dx-fleet-hardening.md"
      provides: "Folded doc/plan drift item marked as handled by Phase 33 without closing fleet convergence"
      contains: "Phase 33"
    - path: ".planning/phases/33-doc-restructure-verification-copy-correction/33-PLANNING-SWEEP.md"
      provides: "Classification record for remaining active planning hits"
      contains: "File | Line or pattern | Classification | Action"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "DOCS-01/DOCS-02 Phase 33 requirement wording stays consistent"
      pattern: "DOCS-01|DOCS-02"
    - from: ".planning/research/*.md"
      to: "Makefile"
      via: "Current research recommendations cite Makefile commands instead of raw start commands"
      pattern: "make (up|url|dev)"
    - from: ".planning/todos/pending/docker-dx-fleet-hardening.md"
      to: ".planning/phases/33-doc-restructure-verification-copy-correction"
      via: "Folded doc/plan drift is Phase 33-owned while cross-repo fleet convergence remains outside this phase"
      pattern: "Phase 33"
---

<objective>
Sweep active `.planning` files for current stale verification/how-to-start guidance, with explicit archive exclusions and classification of allowed evidence.

Purpose: DOCS-01 covers GSD agent/plan prose as well as docs. D-12 through D-15 require an active/current sweep, not historical archive mutation.
Output: Living planning sources and active research/todo files corrected or qualified; remaining hits classified in the plan summary.

Scope note: this plan intentionally keeps the 11 active planning, research, todo, and sweep files together because they share one D-12 through D-15 archive boundary and one final classification gate. Keeping them in one wave-2 plan is safer than splitting the same active-scope inventory across multiple executors; do not expand the file list during execution.
</objective>

## Artifacts This Phase Produces

- `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/STATE.md` changed only where current/future-facing guidance still describes the old dev-start path.
- `.planning/research/ARCHITECTURE.md`, `.planning/research/FEATURES.md`, `.planning/research/PITFALLS.md`, `.planning/research/STACK.md`, and `.planning/research/SUMMARY.md` changed where living recommendations still present outdated Scoria start/open instructions.
- `.planning/todos/pending/docker-dx-fleet-hardening.md` changed only to mark the doc/plan drift and reader-empathy item as folded into Phase 33 while preserving cross-repo fleet convergence and fleet-wide cleanup exclusions.
- `.planning/milestones/**`, `.planning/debug/**`, `.planning/memory/**`, old top-level `v*-MILESTONE-AUDIT.md`, and `.planning/todos/completed/**` remain unchanged.
- `.planning/phases/33-doc-restructure-verification-copy-correction/33-PLANNING-SWEEP.md` changed: classification table for any remaining active-scope hits.
- The required `33-04-SUMMARY.md` must reference the sweep artifact and summarize whether any stale start instruction was fixed.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-doc-restructure-verification-copy-correction/33-CONTEXT.md
@.planning/phases/33-doc-restructure-verification-copy-correction/33-RESEARCH.md
@.planning/phases/33-doc-restructure-verification-copy-correction/33-VALIDATION.md
@.planning/todos/pending/docker-dx-fleet-hardening.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@.planning/research/SUMMARY.md
@Makefile
@docs/docker_dev_dx.md
</context>

<tasks>

<task id="33-04-01" type="auto">
  <name>Task 1: Correct living project state, roadmap, requirements, and folded todo copy</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/todos/pending/docker-dx-fleet-hardening.md</files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/todos/pending/docker-dx-fleet-hardening.md
    - .planning/phases/33-doc-restructure-verification-copy-correction/33-CONTEXT.md
    - Makefile
    - docs/docker_dev_dx.md
  </read_first>
  <action>
    Apply the active/current sweep boundary from D-12 through D-15 to living planning files. Replace current or future-facing Scoria verification/start instructions with D-10 wording: Docker verification uses `make up`, `make url`, and the printed `http://<instance>.localhost/scoria`; native verification uses `make dev` and `http://localhost:4799/scoria`. In requirement or roadmap text, keep DOCS-01/DOCS-02 meaning intact while avoiding phrasing that reads like an instruction to start the app through the old raw path. In the pending fleet-hardening todo, mark the doc/plan drift and docs-reader-empathy portions as folded into Phase 33 while leaving sibling-repo convergence and fleet-wide cleanup out of this phase. Do not edit archive paths named in D-14.
  </action>
  <acceptance_criteria>
    - Living files in this task contain `make up` or `make dev` when they describe current Scoria dev verification/start behavior.
    - The pending fleet-hardening todo explicitly says the doc/plan drift and reader-empathy items are folded into Phase 33.
    - The pending fleet-hardening todo still preserves cross-repo fleet convergence and fleet-wide cleanup as out of this phase.
    - The files in this task have no remaining unqualified stale browser-start or raw start-command instruction.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail
rg -n "make up|make url|make dev|http://<instance>\\.localhost/scoria|http://localhost:4799/scoria" .planning/PROJECT.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md .planning/todos/pending/docker-dx-fleet-hardening.md
rg -n "Phase 33|folded" .planning/todos/pending/docker-dx-fleet-hardening.md
! rg -n "localhost:4000/scoria|mix phx\\.server" .planning/PROJECT.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md .planning/todos/pending/docker-dx-fleet-hardening.md
test -z "$(git diff --name-only -- .planning/milestones .planning/debug .planning/memory .planning/todos/completed ".planning/v*-MILESTONE-AUDIT.md" | sed "/^$/d")"'</automated>
  </verify>
  <done>Living project-state planning files no longer carry stale current Scoria start instructions, and the folded todo is accurately scoped.</done>
</task>

<task id="33-04-02" type="auto">
  <name>Task 2: Correct active research guidance while preserving historical evidence boundaries</name>
  <files>.planning/research/ARCHITECTURE.md, .planning/research/FEATURES.md, .planning/research/PITFALLS.md, .planning/research/STACK.md, .planning/research/SUMMARY.md</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md
    - .planning/research/FEATURES.md
    - .planning/research/PITFALLS.md
    - .planning/research/STACK.md
    - .planning/research/SUMMARY.md
    - .planning/phases/33-doc-restructure-verification-copy-correction/33-CONTEXT.md
    - .planning/phases/29-makefile-hardening/29-01-SUMMARY.md
    - .planning/phases/30-launch-banner-native-dev-notice/30-01-SUMMARY.md
    - Makefile
    - docs/docker_dev_dx.md
  </read_first>
  <action>
    Update active `.planning/research/*.md` files where they still describe outdated Scoria dashboard startup as current, recommended, or still-to-do. Convert current recommendations to D-10 wording and mark already-completed Phase 29/30/33 facts as completed where needed. Preserve implementation evidence that explains why `make dev` wraps Phoenix internally, but label it as mechanism rather than user-facing start guidance per D-15. Do not mutate `.planning/research/.cache/**` or milestone archive copies.
  </action>
  <acceptance_criteria>
    - Active research guidance names `make up`, `make url`, `http://<instance>.localhost/scoria`, `make dev`, or `http://localhost:4799/scoria` for current Scoria dev verification.
    - Completed Phase 29/30 items are not described as open gaps in the active research docs.
    - Active research docs have no unqualified stale Scoria browser-start instructions after the edit.
    - `.planning/research/.cache/**` is not modified.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail
rg -n "make up|make url|make dev|http://<instance>\\.localhost/scoria|http://localhost:4799/scoria" .planning/research/ARCHITECTURE.md .planning/research/FEATURES.md .planning/research/PITFALLS.md .planning/research/STACK.md .planning/research/SUMMARY.md
! rg -n "localhost:4000/scoria|mix phx\\.server" .planning/research/ARCHITECTURE.md .planning/research/FEATURES.md .planning/research/PITFALLS.md .planning/research/STACK.md .planning/research/SUMMARY.md
test "$(git diff --name-only -- .planning/research/.cache | sed "/^$/d" | wc -l | tr -d " ")" -eq 0'</automated>
  </verify>
  <done>Active research docs no longer present stale Scoria dev-start instructions as current guidance.</done>
</task>

<task id="33-04-03" type="auto">
  <name>Task 3: Final active-scope classification sweep</name>
  <files>.planning/phases/33-doc-restructure-verification-copy-correction/33-PLANNING-SWEEP.md</files>
  <read_first>
    - .planning/phases/33-doc-restructure-verification-copy-correction/33-CONTEXT.md
    - .planning/phases/33-doc-restructure-verification-copy-correction/33-VALIDATION.md
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/research/ARCHITECTURE.md
    - .planning/research/FEATURES.md
    - .planning/research/PITFALLS.md
    - .planning/research/STACK.md
    - .planning/research/SUMMARY.md
  </read_first>
  <action>
    Run the corrected D-27 active planning sweep with `!**/milestones/**`, `!**/debug/**`, `!**/memory/**`, and `!**/todos/completed/**` archive exclusions. Save the actual `rg -n` output while implementing. For every remaining hit, create or update `.planning/phases/33-doc-restructure-verification-copy-correction/33-PLANNING-SWEEP.md` with a row containing the exact `path:line` token from the `rg -n` output and classify it as one of: correct implementation evidence, current phase decision quotation, current phase verification pattern, quoted historical rationale, or defect fixed in this plan. Fix any hit that is still a current how-to-start or verification instruction. Do not edit current Phase 33 context/research/validation files solely to remove locked decision quotes or required grep patterns; classify those as current phase decision/verification artifacts. In `33-04-SUMMARY.md`, reference this sweep artifact and state whether any stale start instruction was fixed.
  </action>
  <acceptance_criteria>
    - Final active-scope sweep is run with nested archive exclusions, not only top-level exclusions.
    - `33-PLANNING-SWEEP.md` includes a table with columns `File`, `Line or pattern`, `Classification`, and `Action`.
    - Every active `rg -n` hit has an exact matching `path:line` token in `33-PLANNING-SWEEP.md`.
    - Every non-fixed classification row in `33-PLANNING-SWEEP.md` still corresponds to the current active `rg -n` output.
    - No remaining active-scope hit uses the disallowed classification `CURRENT_STALE_START`.
    - No files under `.planning/milestones/**`, `.planning/debug/**`, `.planning/memory/**`, or `.planning/todos/completed/**` are modified.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail
sweep=".planning/phases/33-doc-restructure-verification-copy-correction/33-PLANNING-SWEEP.md"
active_hits="$(mktemp)"
active_keys="$(mktemp)"
sweep_keys="$(mktemp)"
stale_rows="$(mktemp)"
if rg -n "mix phx\\.server|localhost:4000/scoria" .planning -g "!**/milestones/**" -g "!v*-MILESTONE-AUDIT.md" -g "!**/debug/**" -g "!**/memory/**" -g "!**/todos/completed/**" > "$active_hits"; then :; else status=$?; test "$status" -eq 1; fi
test -f "$sweep"
rg -n "File \\| Line or pattern \\| Classification \\| Action" "$sweep"
awk -F: "{print \$1 \":\" \$2}" "$active_hits" | sort -u > "$active_keys"
while IFS= read -r key; do test -z "$key" && continue; rg -F "$key" "$sweep" >/dev/null || { echo "missing sweep row for $key" >&2; exit 1; }; done < "$active_keys"
awk -F"|" -v out="$sweep_keys" '"'"'function trim(s){gsub(/^[[:space:]]+|[[:space:]]+$/, "", s); return s} /^\|/ {file=trim($2); key=trim($3); class=trim($4); if (file == "File" || file ~ /^-+$/ || file == "") next; if (class !~ /^(correct implementation evidence|current phase decision quotation|current phase verification pattern|quoted historical rationale|defect fixed in this plan)$/) {print "stale or unknown classification: " class > "/dev/stderr"; bad=1} if (key ~ /^[^:]+:[0-9]+$/ && class != "defect fixed in this plan") print key > out} END{exit bad}'"'"' "$sweep"
sort -u -o "$sweep_keys" "$sweep_keys"
if test -s "$sweep_keys"; then comm -23 "$sweep_keys" "$active_keys" > "$stale_rows"; test ! -s "$stale_rows" || { sed "s/^/stale sweep row: /" "$stale_rows" >&2; exit 1; }; fi
! rg -n "\\| CURRENT_STALE_START \\||current stale instruction|current stale start" "$sweep"
test -z "$(git diff --name-only -- .planning/milestones .planning/debug .planning/memory .planning/todos/completed ".planning/v*-MILESTONE-AUDIT.md" | sed "/^$/d")"
SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/ci_policy_contract_test.exs'</automated>
  </verify>
  <done>The active planning sweep has no unclassified stale verification/start guidance, and archive boundaries are intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor -> planning history | The executor can edit living planning files but must preserve historical archives. |
| planner prose -> future agents | Future GSD agents may copy commands from active planning files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-10 | Repudiation | Planning sweep | mitigate | Record classification for every remaining active-scope hit in `33-PLANNING-SWEEP.md` and summarize the result in `33-04-SUMMARY.md`. |
| T-33-11 | Tampering | Historical archives | mitigate | Use D-14 archive exclusions and verify no diffs under milestone/debug/memory/completed paths. |
| T-33-12 | Spoofing | Future agent verification copy | mitigate | Living `.planning` guidance uses `make up`/`make url` or `make dev`/4799, not stale browser-start copy. |
| T-33-SC | Tampering | package installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run the full D-27 active planning sweep with nested archive exclusions and review the `33-PLANNING-SWEEP.md` classification table. Then run the policy lane command from 33-VALIDATION.
</verification>

<success_criteria>
DOCS-01 is complete for planning prose when living planning files and active research/todos no longer instruct stale Scoria dev-start behavior, and every remaining active-scope literal is classified as allowed evidence or current phase verification material.
</success_criteria>

<output>
Create `.planning/phases/33-doc-restructure-verification-copy-correction/33-04-SUMMARY.md` when done.
</output>
