---
phase: 33-doc-restructure-verification-copy-correction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/docker_dev_dx.md
autonomous: true
requirements:
  - DOCS-02
must_haves:
  truths:
    - "A new contributor can read docs/docker_dev_dx.md top-to-bottom and understand the full Scoria dev model per DOCS-02 and D-01 through D-05."
    - "Returning maintainers can scan the first screen and choose Docker daily loop, native dev server, cache, secrets, or stale-instance hygiene without hunting."
    - "Browser URLs are taught as Traefik *.localhost for Docker and localhost:4799 for native host work; Docker container port 4000 is explicitly qualified when it appears per D-11."
  artifacts:
    - path: "docs/docker_dev_dx.md"
      provides: "Reader-first Docker dev-DX reference standard"
      contains: "^## Docker daily loop"
    - path: "docs/docker_dev_dx.md"
      provides: "Native host dev reference"
      contains: "^## Native dev server"
    - path: "docs/docker_dev_dx.md"
      provides: "Cache/secrets/stale-instance standalone sections"
      contains: "^## (Caching guarantees|Secrets|Stale instance hygiene)"
  key_links:
    - from: "docs/docker_dev_dx.md"
      to: "Makefile"
      via: "Documents proxy/up-build/up/url/open/dev/fleet/doctor/down/nuke commands"
      pattern: "make (proxy|up-build|up|url|open|dev|fleet|doctor|down|nuke)"
    - from: "docs/docker_dev_dx.md"
      to: "compose.yml"
      via: "Documents Traefik route, unpublished DB, and ephemeral fallback without changing Compose"
      pattern: "Traefik|ephemeral|unpublished"
    - from: "docs/docker_dev_dx.md"
      to: ".env.op.example"
      via: "Documents process-scoped 1Password secret references"
      pattern: "op run --env-file"
---

<objective>
Rewrite `docs/docker_dev_dx.md` into the Phase 33 reference fleet standard for Scoria local development.

Purpose: DOCS-02 requires the doc to teach the full dev model in a reader-first order, using the completed Phase 29-32 facts without changing runtime behavior.
Output: A restructured `docs/docker_dev_dx.md` with persona/JTBD, TL;DR gameplan, mental model, standalone task sections, and appendices.
</objective>

## Artifacts This Phase Produces

- `docs/docker_dev_dx.md` changed: guided reference narrative per D-01 through D-05.
- `docs/docker_dev_dx.md` changed: exact standalone headings `Docker daily loop`, `Native dev server`, `Caching guarantees`, `Secrets`, and `Stale instance hygiene`.
- No runtime symbols, defaults, ports, Compose labels, Dockerfile instructions, or Phoenix config values are created or changed by this plan.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-doc-restructure-verification-copy-correction/33-CONTEXT.md
@.planning/phases/33-doc-restructure-verification-copy-correction/33-RESEARCH.md
@.planning/phases/33-doc-restructure-verification-copy-correction/33-VALIDATION.md
@docs/docker_dev_dx.md
@Makefile
@compose.yml
@config/dev.exs
@docker/dev-entrypoint.sh
@.env.example
@.envrc.example
@.env.op.example
@brandbook/README.md
@brandbook/brand-book.md
</context>

<tasks>

<task id="33-01-01" type="auto">
  <name>Task 1: Restructure the Docker DX guide around the reader journey</name>
  <files>docs/docker_dev_dx.md</files>
  <read_first>
    - docs/docker_dev_dx.md
    - .planning/phases/33-doc-restructure-verification-copy-correction/33-CONTEXT.md
    - .planning/phases/33-doc-restructure-verification-copy-correction/33-RESEARCH.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - Makefile
    - compose.yml
    - config/dev.exs
    - docker/dev-entrypoint.sh
    - brandbook/README.md
    - brandbook/brand-book.md
  </read_first>
  <action>
    Reorder `docs/docker_dev_dx.md` per D-01 into persona/JTBD, TL;DR gameplan, short mental model, task sections, then appendices. The opening must name the D-02 maintainer job: one solo maintainer running many Phoenix/Elixir demo repos on one Mac, needing hands-off, port-conflict-free local development. Create exact `## Docker daily loop`, `## Native dev server`, and `## Stale instance hygiene` sections. Use D-06 through D-08 command wording: Docker dashboard guidance starts with `make proxy`, `make up-build`, `make up`, then `make url` or `make open`; native host guidance starts with `make dev` and `http://localhost:4799/scoria`. Preserve the Makefile as the command source of truth and do not edit `Makefile`, `compose.yml`, `config/dev.exs`, `Dockerfile.dev`, Docker labels, or container port wiring per D-20. When mentioning port 4000, qualify it as Docker-internal, Traefik service target, CI self-test, or ephemeral fallback per D-11.
  </action>
  <acceptance_criteria>
    - The first screen contains a persona/JTBD paragraph naming the solo maintainer, many Phoenix/Elixir demo repos, and the port-conflict-free job.
    - The top TL;DR contains `make proxy`, `make up-build`, `make up`, `make url`, `make open`, `make fleet`, `make doctor`, and `make dev`.
    - Exact headings exist: `## Docker daily loop`, `## Native dev server`, and `## Stale instance hygiene`.
    - `http://localhost:4000/scoria` does not appear anywhere in `docs/docker_dev_dx.md`.
    - Destructive cleanup text names instance scope before `make nuke`; daemon-global prune commands and fleet-wide wipe commands are absent per D-24.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail
rg -n "solo maintainer|many Phoenix/Elixir|port-conflict-free" docs/docker_dev_dx.md
for h in "Docker daily loop" "Native dev server" "Stale instance hygiene"; do rg -n "^## ${h}$" docs/docker_dev_dx.md >/dev/null; done
for s in "make proxy" "make up-build" "make up" "make url" "make open" "make fleet" "make doctor" "make dev"; do rg -n "$s" docs/docker_dev_dx.md >/dev/null; done
! rg -n "http://localhost:4000/scoria" docs/docker_dev_dx.md
! rg -n "docker (system|volume) prune|make nuke-all" docs/docker_dev_dx.md
SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/ci_policy_contract_test.exs'</automated>
  </verify>
  <done>`docs/docker_dev_dx.md` opens with the locked reader/job framing and exposes the Docker, native, and stale-instance flows as named standalone sections.</done>
</task>

<task id="33-01-02" type="auto">
  <name>Task 2: Preserve cache, secrets, adoption, and recovery truth inside standalone sections</name>
  <files>docs/docker_dev_dx.md</files>
  <read_first>
    - docs/docker_dev_dx.md
    - .planning/phases/31-dockerfile-caching-audit-doc/31-01-SUMMARY.md
    - .planning/phases/32-secrets-pattern-key-rotation/32-01-SUMMARY.md
    - .planning/phases/32-secrets-pattern-key-rotation/32-02-SUMMARY.md
    - Dockerfile.dev
    - .env.example
    - .envrc.example
    - .env.op.example
    - Makefile
    - compose.yml
  </read_first>
  <action>
    Move and tighten the existing D-03 raw material without deleting it: multi-instance model, layer-cache table, process-scoped 1Password/direnv secrets pattern, Safari/dnsmasq note, adoption appendix, and file-role table. Create exact `## Caching guarantees` and `## Secrets` sections. For each required standalone section from D-04, include the same digestible pattern: when to use it, commands, expected URL/output, footguns, and recovery. Keep D-05's invariant visible: user-facing browser URLs are Traefik `*.localhost` for Docker or native `localhost:4799`; container port 4000 is not the browser start URL. Keep the Phase 32 SEC-02 closeout honest: document future key handling through committed examples and process-scoped commands without reading or claiming anything about local secret-bearing files.
  </action>
  <acceptance_criteria>
    - Exact headings exist: `## Caching guarantees` and `## Secrets`.
    - The cache section keeps the layer-invalidation table with rows for CSS/assets, HEEx or source, config, and `mix.exs`/`mix.lock`; it still names `mix deps.get`, `mix deps.compile`, app compile only, and full dep rebuild behavior.
    - The Secrets section keeps `direnv allow .`, `.env.op`, `op run --env-file`, `ANTHROPIC_API_KEY`, and the process-scoped critique command pattern from Phase 32.
    - The stale-instance section names `make fleet`, `make doctor`, `make down INSTANCE=<project>`, and `make nuke INSTANCE=<project>` with scope and recovery.
    - The adoption appendix remains present and clearly separates portable fleet standard guidance from Scoria-only implementation details.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail
for h in "Caching guarantees" "Secrets"; do rg -n "^## ${h}$" docs/docker_dev_dx.md >/dev/null; done
rg -n "mix deps\\.get|mix deps\\.compile|app compile only|full dep rebuild" docs/docker_dev_dx.md
rg -n "CSS|assets|HEEx|mix\\.exs|mix\\.lock" docs/docker_dev_dx.md
rg -n "direnv allow|\\.env\\.op|op run --env-file|ANTHROPIC_API_KEY" docs/docker_dev_dx.md
rg -n "make fleet|make doctor|make down INSTANCE=<project>|make nuke INSTANCE=<project>" docs/docker_dev_dx.md
rg -n "Adopting this in another repo|Converge the fleet" docs/docker_dev_dx.md
! rg -n "docker (system|volume) prune|make nuke-all" docs/docker_dev_dx.md
SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/ci_policy_contract_test.exs'</automated>
  </verify>
  <done>The rewritten guide keeps all Phase 29-32 factual material while making every required DOCS-02 section independently useful.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer -> docs commands | A human copies commands from `docs/docker_dev_dx.md` into a local shell. |
| docs -> secret examples | Secret-handling prose references committed examples without reading local secret-bearing files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Spoofing | Docker/native URL docs | mitigate | Use `make url` / `make open` for Docker and `make dev` / `localhost:4799` for native per D-07 and D-08; never present the old fixed-port browser URL as a start target. |
| T-33-02 | Tampering / Denial of Service | Cleanup guidance | mitigate | Show `make nuke` only with instance scope and recovery context per D-24; verify daemon-global prune and fleet-wide wipe commands are absent. |
| T-33-03 | Information Disclosure | Secrets section | mitigate | Document `.env.op` secret references and `op run --env-file`; do not read `.env`, shell history, logs, screenshots, or process environments. |
| T-33-SC | Tampering | package installs | accept | No npm/pip/cargo package install is planned; package legitimacy gate is not applicable. |
</threat_model>

<verification>
Run the task automated checks plus the Phase 33 doc heading gate:
`rg -n "^## (Docker daily loop|Native dev server|Caching guarantees|Secrets|Stale instance hygiene)$" docs/docker_dev_dx.md`
</verification>

<success_criteria>
DOCS-02 is complete for this plan when `docs/docker_dev_dx.md` reads as a guided reference narrative and the required standalone sections exist with commands, expected outputs, footguns, and recovery.
</success_criteria>

<output>
Create `.planning/phases/33-doc-restructure-verification-copy-correction/33-01-SUMMARY.md` when done.
</output>
