---
phase: 52-runtime-to-handoff-example-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md
autonomous: true
requirements:
  - EXMP-01
  - EXMP-02
must_haves:
  truths:
    - "The adopter-facing example shape starts with Scoria.start_run/2 before any bounded handoff call."
    - "The example shape uses Scoria.start_handoff_run/3 and Scoria.get_run_detail/1 instead of direct workflow internals."
    - "The example shape treats host session_id as continuity and Scoria run_id as the durable execution handle."
    - "The example shape presents projected_context as an explicit bounded payload with rejection behavior."
  artifacts:
    - path: ".planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md"
      provides: "Executor-facing decision record for the smallest Phase 52 example shape"
      contains: "No new public runtime API is required for Phase 52"
  key_links:
    - from: ".planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md"
      to: "docs/phoenix_runtime_example.md"
      via: "example artifact decision"
      pattern: "docs/phoenix_runtime_example\\.md"
    - from: ".planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md"
      to: "docs/bounded_handoffs.md"
      via: "projected context and host ownership decision"
      pattern: "docs/bounded_handoffs\\.md"
---

<objective>
Choose and record the smallest adopter-facing Phase 52 example shape before implementation.

Purpose: Prevent Phase 52 from growing into a new API, broad sample app, semantic lane, or docs-only assertion when existing public runtime and handoff APIs already provide the required path.
Output: A concise `.planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md` decision record that Plan 52-02 and Plan 52-03 execute against.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-runtime-to-handoff-example-contract/52-RESEARCH.md
@.planning/phases/52-runtime-to-handoff-example-contract/52-VALIDATION.md
@.planning/phases/52-runtime-to-handoff-example-contract/52-PATTERNS.md
@docs/phoenix_runtime_example.md
@docs/bounded_handoffs.md
@test/support/scoria/adoption_example.ex
@test/scoria/adoption_surface_test.exs
@test/scoria/handoff_example_source_test.exs
@test/scoria/phoenix_example_source_test.exs
@test/scoria/runtime_test.exs
@lib/scoria.ex
@lib/scoria/runtime.ex
@lib/scoria/runtime/params.ex
@lib/scoria/runtime/run_detail.ex

<interfaces>
From `lib/scoria.ex`, the public adopter-facing surface is:
```elixir
def identity(attrs \\ %{})
def start_run(identity, opts \\ [])
def start_handoff_run(identity, delegated_role_id, opts \\ [])
def resume_run(run_id, opts \\ [])
def get_run(run_id)
def get_run_detail(run_id)
def list_runs_for_session(session_id)
```

From `lib/scoria/runtime/params.ex`, `projected_context` must be a map and unsafe projected-context keys return `{:error, :unsafe_projected_context}` through `Scoria.start_handoff_run/3`. Unsafe names include `transcript`, `messages`, `history`, `provider_session`, `runtime_state`, `session`, `headers`, `cookies`, and `secrets`, plus matching aliases and suffix forms.
</interfaces>
</context>

<source_audit>
GOAL: Define and build the narrow adopter-facing path from `Scoria.start_run/2` into `Scoria.start_handoff_run/3` — COVERED by 52-01 shape decision, 52-02 implementation, and 52-03 documentation boundary.
REQ EXMP-01: One adopter-facing example starts a default run and escalates into `Scoria.start_handoff_run/3` — COVERED by 52-01 and 52-02.
REQ EXMP-02: Example shows bounded projected context and rejected/excluded inputs — COVERED by 52-01, 52-02, and 52-03.
RESEARCH: Use existing public `Scoria` facade; do not add new public API unless a blocking gap is proven — COVERED by this plan's decision record.
RESEARCH: Keep semantic fast path, knowledge, retrieval, pgvector, hosted onboarding, and broad catalogs out of Phase 52 — COVERED by this plan's exclusions.
CONTEXT: No `52-CONTEXT.md` exists; no locked D-XX decisions or deferred discuss-phase ideas were provided.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Record the Phase 52 example shape decision</name>
  <files>.planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md</files>
  <read_first>
    .planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/52-runtime-to-handoff-example-contract/52-RESEARCH.md
    .planning/phases/52-runtime-to-handoff-example-contract/52-VALIDATION.md
    .planning/phases/52-runtime-to-handoff-example-contract/52-PATTERNS.md
    docs/phoenix_runtime_example.md
    docs/bounded_handoffs.md
    lib/scoria.ex
    lib/scoria/runtime.ex
    lib/scoria/runtime/params.ex
    lib/scoria/runtime/run_detail.ex
    test/scoria/runtime_test.exs
  </read_first>
  <action>Create `.planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md` if it does not exist. Write exactly these top-level sections: `# Phase 52 Example Shape`, `## Decision`, `## In Scope`, `## Explicit Exclusions`, `## API Surface`, `## Example Flow`, `## Projected Context Contract`, `## Verification Contract`, and `## Source Audit`. Under `## Decision`, include the exact sentence `No new public runtime API is required for Phase 52.` Under `## API Surface`, list `Scoria.identity/1`, `Scoria.start_run/2`, `Scoria.start_handoff_run/3`, and `Scoria.get_run_detail/1`. Under `## Example Flow`, state that the host starts a default run, persists `started.run_id`, decides in host code whether a bounded review/classification handoff is needed, calls `Scoria.start_handoff_run/3`, persists `handoff_run.run_id`, and uses `Scoria.get_run_detail(handoff_run.run_id)` for curated delegated readback. Include the sentence `The host app owns escalation policy; Scoria does not infer or transfer policy from the default run.` Include the sentence `session_id groups related host turns; run_id names one exact Scoria execution.` Under `## Projected Context Contract`, state that accepted examples are `projected_context: %{}` and `%{"task" => "policy-and-accuracy review", "draft_answer" => draft_answer}` and rejected examples include `transcript`, `messages`, `history`, `provider_session`, `runtime_state`, `session`, `headers`, `cookies`, and `secrets`. Under `## Explicit Exclusions`, include `No sample Phoenix app`, `No semantic fast-path prerequisite`, `No knowledge or retrieval prerequisite`, `No pgvector prerequisite`, `No direct Scoria.Workflows calls`, and `No raw workflow table readback`.</action>
  <verify>
    <automated>test -f .planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md && grep -F "No new public runtime API is required for Phase 52." .planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md && grep -F "The host app owns escalation policy; Scoria does not infer or transfer policy from the default run." .planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md && grep -F "session_id groups related host turns; run_id names one exact Scoria execution." .planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "Scoria.start_run/2" .planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md` returns at least one match.
    - `grep -F "Scoria.start_handoff_run/3" .planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md` returns at least one match.
    - `grep -F "Scoria.get_run_detail(handoff_run.run_id)" .planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md` returns at least one match.
    - `grep -F "No direct Scoria.Workflows calls" .planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md` returns at least one match.
    - `grep -F "No raw workflow table readback" .planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md` returns at least one match.
  </acceptance_criteria>
  <done>The decision record exists and names the exact public API surface, host-owned escalation boundary, projected-context accept/reject examples, and exclusions that downstream plans must follow.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the existing source-alignment lane still runs before implementation</name>
  <files>.planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md</files>
  <read_first>
    .planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md
    .planning/phases/52-runtime-to-handoff-example-contract/52-VALIDATION.md
    test/scoria/adoption_surface_test.exs
    test/scoria/handoff_example_source_test.exs
  </read_first>
  <action>Run the docs/source quick command from `52-VALIDATION.md`: `MIX_ENV=test mix test test/scoria/adoption_surface_test.exs test/scoria/handoff_example_source_test.exs`. If it passes, append a `## Baseline Quick Verification` section to `52-EXAMPLE-SHAPE.md` with the exact command and the sentence `Baseline docs/source quick verification passed before Phase 52 implementation.` If the command fails, append the exact command, failing test module names, and the sentence `Baseline docs/source quick verification failed before Phase 52 implementation.` Do not edit production or docs files in this plan.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/adoption_surface_test.exs test/scoria/handoff_example_source_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "## Baseline Quick Verification" .planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md` returns one match.
    - `grep -F "MIX_ENV=test mix test test/scoria/adoption_surface_test.exs test/scoria/handoff_example_source_test.exs" .planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md` returns one match.
    - The executor summary records whether the command passed or failed.
  </acceptance_criteria>
  <done>The implementation plans have a concrete Phase 52 shape decision and a recorded quick-test baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phoenix host app -> Scoria public facade | Host-owned identity, escalation policy, handoff input, and projected context cross into Scoria. |
| Scoria runtime -> operator/readback surface | Durable workflow state is projected into curated DTOs and docs-visible operator evidence. |
| Planning artifact -> implementation plans | The decision record constrains downstream code/docs changes and prevents scope expansion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Information Disclosure | projected_context example contract | mitigate | Require accepted projected_context examples to be `%{}` or `%{"task" => "policy-and-accuracy review", "draft_answer" => draft_answer}` and require rejected keys `transcript`, `messages`, `history`, `provider_session`, `runtime_state`, `session`, `headers`, `cookies`, and `secrets` in `52-EXAMPLE-SHAPE.md`. |
| T-52-02 | Elevation of Privilege | host escalation policy boundary | mitigate | Require the exact sentence `The host app owns escalation policy; Scoria does not infer or transfer policy from the default run.` in the decision record. |
| T-52-03 | Spoofing / Repudiation | host `session_id` versus Scoria `run_id` | mitigate | Require the exact sentence `session_id groups related host turns; run_id names one exact Scoria execution.` in the decision record. |
| T-52-04 | Information Disclosure | delegated readback | mitigate | Require `Scoria.get_run_detail(handoff_run.run_id)` and `No raw workflow table readback`; prohibit direct `Scoria.Workflows` calls in the example shape. |
</threat_model>

<verification>
Run:
```bash
MIX_ENV=test mix test test/scoria/adoption_surface_test.exs test/scoria/handoff_example_source_test.exs
```
This command is the docs/source quick lane and does not require reachable PostgreSQL. DB-backed runtime rejection verification is intentionally left to Plan 52-02.
</verification>

<success_criteria>
- `.planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md` exists.
- The decision record contains `No new public runtime API is required for Phase 52.`
- The decision record names `Scoria.start_run/2`, `Scoria.start_handoff_run/3`, `Scoria.get_run_detail/1`, accepted projected context, rejected projected-context keys, and exclusions.
- The quick docs/source verification command is recorded with pass/fail status.
</success_criteria>

<output>
After completion, create `.planning/phases/52-runtime-to-handoff-example-contract/52-01-SUMMARY.md`
</output>
