---
phase: 26
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: ["lib/scoria/runtime/release_gate.ex", "test/scoria/runtime/release_gate_test.exs", "lib/scoria/runtime.ex"]
autonomous: true
requirements: ["GATE-01"]

must_haves:
  truths:
    - "System blocks draft prompts from serving production traffic."
    - "Production traffic only serves active prompts."
  artifacts:
    - path: "lib/scoria/runtime/release_gate.ex"
      provides: "Gating logic for draft prompts"
  key_links:
    - from: "lib/scoria/runtime.ex"
      to: "lib/scoria/runtime/release_gate.ex"
      via: "check before invocation"
---

<objective>
Implement the release gate in `Scoria.Runtime` to prevent "draft" prompts from serving production traffic.

Purpose: Enforce policies that new prompts cannot serve production traffic until explicitly approved.
Output: Working, tested release gate module wired into the runtime.
</objective>

<context>
@.planning/phases/26-release-gates-and-approvals/26-PATTERNS.md
@.planning/phases/26-release-gates-and-approvals/26-RESEARCH.md
</context>

<tasks>
<task type="tdd">
  <name>Task 1: Release Gate Middleware</name>
  <files>lib/scoria/runtime/release_gate.ex, test/scoria/runtime/release_gate_test.exs, lib/scoria/runtime.ex</files>
  <behavior>
    - Test 1: `Scoria.Runtime.ReleaseGate.check/1` returns `{:error, :unapproved_draft}` for draft prompts.
    - Test 2: `Scoria.Runtime.ReleaseGate.check/1` returns `:ok` for active prompts.
    - Test 3: `Scoria.Runtime` invocation fails with `:unapproved_draft` when attempting to invoke a draft prompt via production traffic entry points.
  </behavior>
  <action>
    Create `Scoria.Runtime.ReleaseGate` with a `check/1` function that inspects the prompt status. Wire `ReleaseGate.check/1` into the main invocation path of `lib/scoria/runtime.ex` to short-circuit if the prompt is in draft state. Reference `26-PATTERNS.md` for "Runtime Guarding" patterns, ensuring checks against "draft" status safely prevent illegal state mutations similar to existing prompt registry mechanisms.
  </action>
  <verify>
    <automated>mix test test/scoria/runtime/release_gate_test.exs</automated>
  </verify>
  <done>Draft prompts are safely rejected by the release gate during runtime invocation.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime -> ReleaseGate | Prevents unapproved data from reaching execution context in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Information Disclosure | `ReleaseGate` | mitigate | Ensure gating fails closed and does not leak unapproved prompt contents in error messages. |
</threat_model>