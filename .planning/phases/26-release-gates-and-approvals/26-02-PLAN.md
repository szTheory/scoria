---
phase: 26
plan: 02
type: tdd
wave: 2
depends_on: ["26-01"]
files_modified: ["lib/scoria/workflows/prompt_release.ex", "test/scoria/workflows/prompt_release_test.exs"]
autonomous: true
requirements: ["GATE-02"]

must_haves:
  truths:
    - "Workflow can request approval for a draft prompt release."
    - "Workflow can process an approval, promoting the draft to active."
    - "Audit events are durably recorded for approvals and rejections."
  artifacts:
    - path: "lib/scoria/workflows/prompt_release.ex"
      provides: "Event-driven approval workflow for prompts"
  key_links:
    - from: "lib/scoria/workflows/prompt_release.ex"
      to: "lib/scoria/repo.ex"
      via: "database transaction for approval state change"
---

<objective>
Implement the event-driven workflow for prompt release approvals using `Scoria.Workflows.PromptRelease`.

Purpose: Enable operators to explicitly approve drafts using the system's workflow framework and EvalRun evidence, making decisions durable.
Output: Working, tested workflow service for prompt approvals.
</objective>

<context>
@.planning/phases/26-release-gates-and-approvals/26-PATTERNS.md
@.planning/phases/26-release-gates-and-approvals/26-RESEARCH.md
</context>

<tasks>
<task type="tdd">
  <name>Task 1: Prompt Release Workflow</name>
  <files>lib/scoria/workflows/prompt_release.ex, test/scoria/workflows/prompt_release_test.exs</files>
  <behavior>
    - Test 1: `request_remote_approval/3` marks a run step as waiting for approval.
    - Test 2: `approve/3` with status "approved" promotes the draft and records an audit event.
    - Test 3: `approve/3` with status "rejected" leaves the draft but records a rejection audit event.
  </behavior>
  <action>
    Create `Scoria.Workflows.PromptRelease` following the "Workflow Approvals & Audit Trails" pattern in `26-PATTERNS.md`. Implement `request_remote_approval/3` and `approve/3`. Ensure `approve/3` uses a `Repo.transaction` wrapper to ensure the audit (`AuditOutboxEvent`) and state changes are strongly consistent, matching established patterns in `Scoria.Workflows`.
  </action>
  <verify>
    <automated>mix test test/scoria/workflows/prompt_release_test.exs</automated>
  </verify>
  <done>Operator can approve or reject drafts with durable audit events via workflow integration.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator -> Workflow | Workflow acts as the trusted authority over prompt status transitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02 | Elevation of Privilege | `PromptRelease` | mitigate | Ensure `approve/3` properly logs the operator ID in the SRE audit outbox event inside the same transaction. |
</threat_model>