---
phase: 26
plan: 03
type: execute
wave: 3
depends_on: ["26-01", "26-02"]
files_modified: ["lib/scoria_web/live/prompt_live/release_workbench_live.ex", "test/scoria_web/live/prompt_live/release_workbench_live_test.exs", "lib/scoria_web/router.ex"]
autonomous: true
requirements: ["GATE-03"]

must_haves:
  truths:
    - "Operator can view a comparison of draft vs active metrics in an embedded UI."
    - "Operator can approve or reject the draft directly from the UI."
  artifacts:
    - path: "lib/scoria_web/live/prompt_live/release_workbench_live.ex"
      provides: "Comparison workbench UI component"
  key_links:
    - from: "lib/scoria_web/live/prompt_live/release_workbench_live.ex"
      to: "lib/scoria/workflows/prompt_release.ex"
      via: "workflow approval CTA"
---

<objective>
Build the embedded LiveView workbench for prompt comparison, allowing operators to review EvalRun metrics and approve drafts.

Purpose: Provide a unified operator UI to compare draft metrics against the active baseline and execute release decisions.
Output: `ReleaseWorkbenchLive` component and route, complying with `26-UI-SPEC.md`.
</objective>

<context>
@.planning/phases/26-release-gates-and-approvals/26-UI-SPEC.md
@.planning/phases/26-release-gates-and-approvals/26-PATTERNS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Scaffold ReleaseWorkbenchLive Component</name>
  <files>lib/scoria_web/live/prompt_live/release_workbench_live.ex, test/scoria_web/live/prompt_live/release_workbench_live_test.exs, lib/scoria_web/router.ex</files>
  <behavior>
    - Test 1: Page loads and mounts successfully for a valid prompt ID.
    - Test 2: Renders a two-column comparison deck (draft vs active).
    - Test 3: Renders release gate status strip indicating 'Draft blocked'.
  </behavior>
  <action>
    Create the LiveView component `ScoriaWeb.PromptLive.ReleaseWorkbenchLive` and wire it into `lib/scoria_web/router.ex` under the operator prompts scope. Apply the "LiveView Async Evidence Loading" pattern from `26-PATTERNS.md`, using `assign_async` and `<.async_result>` to lazily fetch embedded UI data like EvalRuns. Follow the UI design contract from `26-UI-SPEC.md` for structure and spacing.
  </action>
  <verify>
    <automated>mix test test/scoria_web/live/prompt_live/release_workbench_live_test.exs</automated>
  </verify>
  <done>Component mounts and properly fetches/displays the comparison data asynchronously.</done>
</task>

<task type="tdd">
  <name>Task 2: Implement Approval Rail & CTA Interactions</name>
  <files>lib/scoria_web/live/prompt_live/release_workbench_live.ex, test/scoria_web/live/prompt_live/release_workbench_live_test.exs</files>
  <behavior>
    - Test 1: Approval CTA is disabled if prerequisites (e.g., both eval runs completed and aligned) are missing.
    - Test 2: Clicking "Approve Prompt Release" triggers the workflow approval and updates UI.
    - Test 3: Reject CTA records a rejection and remains on the page with a rejection notice.
  </behavior>
  <action>
    Add the approval logic connecting the LiveView buttons to `Scoria.Workflows.PromptRelease.approve/3`. Implement the state contract from `26-UI-SPEC.md` including enabling/disabling the CTA based on whether both sides have aligned, completed `EvalRun`s. Apply the "Workflow Approvals & Audit Trails" pattern from `26-PATTERNS.md` to interact cleanly with the workflows layer. Add modals/confirmations before executing the approval transaction.
  </action>
  <verify>
    <automated>mix test test/scoria_web/live/prompt_live/release_workbench_live_test.exs</automated>
  </verify>
  <done>Operator can approve or reject the draft directly via the LiveView UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator -> UI | Provides authorized users the ability to review evidence and submit decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Spoofing | `ReleaseWorkbenchLive` | mitigate | Ensure the LiveView mount function validates the operator's session identity before rendering the workbench or processing `handle_event` calls for approval. |
</threat_model>