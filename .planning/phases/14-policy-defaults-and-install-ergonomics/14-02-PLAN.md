---
phase: 14-policy-defaults-and-install-ergonomics
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - lib/scoria/runtime/defaults.ex
  - lib/scoria/runtime/params.ex
  - lib/scoria/runtime.ex
  - lib/scoria/workflows/runtime.ex
  - lib/scoria/mcp/executor.ex
  - lib/scoria/sre/telemetry_identity.ex
  - test/scoria/runtime/defaults_test.exs
  - test/scoria/runtime_test.exs
  - test/scoria/runtime_integration_test.exs
autonomous: true
nyquist_compliant: true
requirements:
  - POLY-01
  - POLY-02
must_haves:
  truths:
    - "Effective provider, model, and prompt-policy defaults resolve exactly once at the public runtime boundary."
    - "Identity-aware overlays use canonical actor and tenant identity without mutating root identity truth."
    - "Resolved policy decisions project into runtime metadata, MCP context, telemetry, and audit-sensitive seams."
  artifacts:
    - path: "lib/scoria/runtime/defaults.ex"
      provides: "Identity-aware overlay composition and governance validation"
    - path: "lib/scoria/runtime/params.ex"
      provides: "Single-pass runtime default resolution and metadata stamping"
    - path: "test/scoria/runtime_integration_test.exs"
      provides: "End-to-end proof that resolved defaults survive the public runtime path"
  key_links:
    - from: "lib/scoria/runtime/params.ex"
      to: "lib/scoria/runtime/defaults.ex"
      via: "public runtime start resolves effective defaults one time"
      pattern: "resolve|effective|defaults"
    - from: "lib/scoria/runtime/params.ex"
      to: "lib/scoria/workflows/runtime.ex"
      via: "resolved provider/model/policy metadata becomes downstream runtime context"
      pattern: "provider|model|policy_key|prompt_policy"
    - from: "lib/scoria/mcp/executor.ex"
      to: "lib/scoria/sre/telemetry_identity.ex"
      via: "resolved policy metadata projects into operator-visible telemetry and audit-sensitive execution seams"
      pattern: "policy_key|provider|model"
---

<objective>
Implement identity-aware default composition and projection so policy choices are stable, validated, and operator-visible across the public runtime path.

Purpose: satisfy `POLY-02` and complete the runtime side of Phase 14 by making defaults composition predictable, exact, and observable.
Output: single-pass runtime resolution, explicit overlay validation, and integration coverage proving the resolved metadata survives downstream execution seams.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-CONTEXT.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-RESEARCH.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-PATTERNS.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-VALIDATION.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-01-PLAN.md
@.planning/phases/13-public-runtime-api-and-session-lifecycle/13-CONTEXT.md
@.planning/phases/12-canonical-runtime-identity/12-CONTEXT.md

<interfaces>
From lib/scoria/runtime.ex:
```elixir
def start_run(identity, opts \\ [])
```

From lib/scoria/runtime/params.ex:
```elixir
def start(identity, opts \\ [])
```

From lib/scoria/workflows/runtime.ex:
```elixir
def execute_step(step_id, opts \\ [])
```

From lib/scoria/mcp/executor.ex:
```elixir
def execute(tool_module, args, context, timeout \\ 5000)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve identity-aware overlays once at the public runtime boundary</name>
  <read_first>lib/scoria/runtime/defaults.ex, lib/scoria/runtime/params.ex, lib/scoria/identity.ex, lib/scoria/runtime.ex, .planning/phases/14-policy-defaults-and-install-ergonomics/14-CONTEXT.md, .planning/phases/14-policy-defaults-and-install-ergonomics/14-RESEARCH.md</read_first>
  <files>lib/scoria/runtime/defaults.ex, lib/scoria/runtime/params.ex, lib/scoria/runtime.ex, test/scoria/runtime/defaults_test.exs, test/scoria/runtime_test.exs</files>
  <acceptance_criteria>The precedence chain from D-07 is implemented exactly as built-in defaults < app defaults < tenant defaults < actor defaults < per-run overrides; the optional resolver module uses canonical identity and remains explicit per D-03 and D-06; governance-sensitive fields do not widen silently through merge order per D-08; tests prove resolution happens once during `start_run/2` rather than being re-derived later.</acceptance_criteria>
  <action>Extend `Scoria.Runtime.Defaults` and `Scoria.Runtime.Params.start/2` so the public runtime boundary resolves effective defaults in one pass using canonical `Scoria.Identity`, optional resolver overlays, and explicit per-run overrides. Enforce D-05 through D-08 directly: root identity stays immutable, policy/config remains separate from identity truth, precedence is stable, and sensitive overrides are validated or narrowed instead of being widened accidentally. Keep the implementation intentionally boring and synchronous; do not introduce a config process, database-backed policy editor, or hidden callback chain.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/runtime/defaults_test.exs test/scoria/runtime_test.exs</automated>
  </verify>
  <done>The runtime boundary resolves effective defaults once, honors identity-aware precedence, and has automated contract coverage for overlay and override rules.</done>
</task>

<task type="auto">
  <name>Task 2: Project resolved policy decisions through workflow, MCP, and telemetry seams</name>
  <read_first>lib/scoria/runtime/params.ex, lib/scoria/workflows/runtime.ex, lib/scoria/mcp/executor.ex, lib/scoria/sre/telemetry_identity.ex, test/scoria/runtime_integration_test.exs, .planning/phases/14-policy-defaults-and-install-ergonomics/14-PATTERNS.md</read_first>
  <files>lib/scoria/runtime/params.ex, lib/scoria/workflows/runtime.ex, lib/scoria/mcp/executor.ex, lib/scoria/sre/telemetry_identity.ex, test/scoria/runtime_integration_test.exs</files>
  <acceptance_criteria>Resolved provider/model/prompt-policy data is stamped into runtime metadata before run creation per D-05; downstream workflow and MCP execution consume the canonical resolved fields instead of re-reading config; telemetry and audit-facing metadata include the effective provider/model/policy decisions per D-18; integration tests prove the host app can inspect the resolved decision path through the public runtime contract.</acceptance_criteria>
  <action>Thread the resolved default snapshot through the existing seams that already know how to carry identity and policy metadata: run metadata in `Params.start/2`, execution context in `Workflows.Runtime`, tool context in `MCP.Executor`, and operator-facing metadata shaping in `TelemetryIdentity`. Reuse the current repo’s projection patterns rather than adding a new persistence model. The key requirement is consistency: the same chosen provider/model/prompt-policy should be visible to workflow execution, MCP governance, telemetry, and later operator inspection without each subsystem recomputing it.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/runtime_test.exs test/scoria/runtime_integration_test.exs</automated>
  </verify>
  <done>Resolved defaults are now durable, execution-visible, and operator-visible throughout the public runtime path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical identity + config + per-run opts → runtime resolution | Public runtime start composes effective policy and governance decisions |
| resolved runtime metadata → workflow/MCP/telemetry consumers | Downstream seams must trust pre-resolved fields instead of recomputing them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | S | `lib/scoria/runtime/defaults.ex` | mitigate | Require canonical identity input for resolver overlays and keep identity separate from policy composition. |
| T-14-02-02 | T | `lib/scoria/runtime/params.ex` | mitigate | Validate governance-sensitive overrides before execution so merge order cannot silently widen tenant or actor boundaries. |
| T-14-02-03 | R | `test/scoria/runtime_integration_test.exs` | mitigate | Assert that the same resolved provider/model/policy snapshot is visible through the public runtime path and downstream execution effects. |
| T-14-02-04 | I | `lib/scoria/sre/telemetry_identity.ex` | mitigate | Project stable policy labels/refs without leaking arbitrary config maps or raw prompt internals into telemetry. |
| T-14-02-05 | D | `lib/scoria/workflows/runtime.ex` | mitigate | Consume pre-resolved fields from context rather than performing additional config lookups during execution. |
| T-14-02-06 | E | `lib/scoria/mcp/executor.ex` | mitigate | Use the resolved policy snapshot for governance-sensitive execution so callers cannot bypass boundaries with ad hoc downstream context. |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/runtime/defaults_test.exs test/scoria/runtime_test.exs`
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/runtime_test.exs test/scoria/runtime_integration_test.exs`
</verification>

<success_criteria>
- Effective defaults resolve exactly once at the public runtime boundary.
- Identity-aware overlays are explicit, precedence-stable, and governance-safe.
- Workflow, MCP, telemetry, and audit-sensitive seams consume the same resolved policy snapshot.
- The resolved provider/model/prompt-policy path is covered by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/14-policy-defaults-and-install-ergonomics/14-02-SUMMARY.md`
</output>
