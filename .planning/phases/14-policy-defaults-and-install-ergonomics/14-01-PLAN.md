---
phase: 14-policy-defaults-and-install-ergonomics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/prompt_policy.ex
  - lib/scoria/runtime/defaults.ex
  - lib/scoria/runtime/params.ex
  - test/scoria/prompt_policy_test.exs
  - test/scoria/runtime/defaults_test.exs
autonomous: true
nyquist_compliant: true
requirements:
  - POLY-01
must_haves:
  truths:
    - "Host apps configure boring runtime defaults through one documented Scoria surface instead of hidden processes or a DSL."
    - "Prompt policy has one canonical public noun that runtime code can trust after immediate normalization."
    - "Per-run runtime options remain explicit overrides on top of baseline defaults instead of replacing the app-facing config contract."
  artifacts:
    - path: "lib/scoria/prompt_policy.ex"
      provides: "Canonical prompt-policy struct and normalization boundary"
    - path: "lib/scoria/runtime/defaults.ex"
      provides: "Single public helper for reading and validating baseline runtime defaults"
    - path: "test/scoria/runtime/defaults_test.exs"
      provides: "Contract coverage for the app-facing defaults surface"
  key_links:
    - from: "lib/scoria/runtime/defaults.ex"
      to: "lib/scoria/runtime/params.ex"
      via: "normalized defaults flow into public runtime start contracts"
      pattern: "Defaults\\.|resolve|defaults"
    - from: "lib/scoria/prompt_policy.ex"
      to: "lib/scoria/runtime/defaults.ex"
      via: "prompt-policy sugar normalizes into the canonical struct before runtime resolution"
      pattern: "PromptPolicy\\.(new|normalize|to_map)"
    - from: "test/scoria/prompt_policy_test.exs"
      to: "lib/scoria/prompt_policy.ex"
      via: "public prompt-policy normalization rules"
      pattern: "normalize|policy_key|prompt_ref|prompt_version"
---

<objective>
Define the canonical public defaults surface for provider, model, and prompt policy before identity-aware overlays or installer work begins.

Purpose: satisfy `POLY-01` and lock the app-facing contract so later plans compose and project defaults without improvising their own shapes.
Output: a canonical prompt-policy struct, a runtime-defaults helper, and focused tests proving the boring application-config path.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-CONTEXT.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-RESEARCH.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-PATTERNS.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-VALIDATION.md
@.planning/phases/13-public-runtime-api-and-session-lifecycle/13-CONTEXT.md
@.planning/phases/13-public-runtime-api-and-session-lifecycle/13-RESEARCH.md

<interfaces>
From lib/scoria.ex:
```elixir
def start_run(identity, opts \\ [])
```

From lib/scoria/runtime/params.ex:
```elixir
def start(identity, opts \\ [])
```

From lib/scoria/identity.ex:
```elixir
def normalize(attrs)
def to_map(identity)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce the canonical prompt-policy noun and normalization tests</name>
  <read_first>lib/scoria/identity.ex, lib/scoria/runtime/params.ex, .planning/phases/14-policy-defaults-and-install-ergonomics/14-CONTEXT.md, .planning/phases/14-policy-defaults-and-install-ergonomics/14-RESEARCH.md, .planning/phases/14-policy-defaults-and-install-ergonomics/14-PATTERNS.md</read_first>
  <files>lib/scoria/prompt_policy.ex, test/scoria/prompt_policy_test.exs</files>
  <acceptance_criteria>`Scoria.PromptPolicy` exists as one explicit public struct per D-09; edge sugar such as atoms, strings, and maps normalizes immediately into the canonical struct per D-10; the struct carries stable identity plus resolved governance fields needed for runtime metadata and later projection per D-11; tests prove that runtime code can rely on one canonical shape instead of raw maps.</acceptance_criteria>
  <action>Create `lib/scoria/prompt_policy.ex` using the same posture as `Scoria.Identity`: permissive edge input, immediate normalization, and a `to_map/1` escape hatch for durable metadata snapshots. Keep the public fields scoped to what Phase 14 has actually locked: `policy_key`, prompt reference/version, and governance constraints that later plans must project into runtime metadata, telemetry, and audit evidence. Do not build a hosted prompt-management subsystem or behavior-first resolver API here; this plan is about stabilizing the noun that later plans consume.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/prompt_policy_test.exs</automated>
  </verify>
  <done>The repo has one canonical prompt-policy noun with direct automated coverage for normalization and stable public shape.</done>
</task>

<task type="auto">
  <name>Task 2: Add the public runtime-defaults helper and baseline config-surface tests</name>
  <read_first>lib/scoria/runtime/params.ex, lib/scoria/runtime.ex, lib/scoria/workflows/runtime.ex, test/scoria_test.exs, .planning/phases/14-policy-defaults-and-install-ergonomics/14-CONTEXT.md, .planning/phases/14-policy-defaults-and-install-ergonomics/14-VALIDATION.md</read_first>
  <files>lib/scoria/runtime/defaults.ex, lib/scoria/runtime/params.ex, test/scoria/runtime/defaults_test.exs</files>
  <acceptance_criteria>The repo exposes one obvious default surface through `config :scoria, Scoria.Runtime, defaults: ...` per D-01; the happy path accepts plain maps/keywords/struct-backed values rather than a DSL or hidden process per D-02; per-run inputs still remain explicit runtime options instead of being folded into config state per D-04; tests prove the helper reads, normalizes, and validates baseline provider/model/prompt-policy defaults without yet introducing identity-aware overlays.</acceptance_criteria>
  <action>Create `lib/scoria/runtime/defaults.ex` as the single helper for built-in defaults and app-config defaults, and wire `Scoria.Runtime.Params.start/2` to consume it as the baseline source for start contracts. Keep this plan deliberately narrow to the public config surface from D-01 through D-04: built-in defaults, app defaults, prompt-policy normalization, and explicit per-run override slots. Do not resolve tenant or actor overlays yet; that belongs to Plan 14-02. The implementation should stay pure and side-effect-free so later plans can safely layer policy validation and metadata projection on top of it.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/prompt_policy_test.exs test/scoria/runtime/defaults_test.exs</automated>
  </verify>
  <done>Phase 14 has a locked baseline defaults contract and automated proof that the boring app-config path works before overlay logic is added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app config / public runtime opts → defaults helper | Untrusted host-app configuration is normalized into canonical runtime defaults |
| prompt-policy edge sugar → canonical prompt-policy struct | Loose values must become stable runtime truth before execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | S | `lib/scoria/prompt_policy.ex` | mitigate | Normalize all prompt-policy edge inputs into one canonical struct before runtime code consumes them. |
| T-14-01-02 | T | `lib/scoria/runtime/defaults.ex` | mitigate | Keep defaults composition pure and explicit so later plans can validate overlays without hidden mutation or process state. |
| T-14-01-03 | R | `test/scoria/runtime/defaults_test.exs` | mitigate | Lock the public defaults surface in tests so future refactors cannot silently drift into DSLs or undocumented keys. |
| T-14-01-04 | I | `lib/scoria/prompt_policy.ex` | mitigate | Scope the canonical policy fields to stable identity and governance metadata only; do not leak arbitrary prompt internals into public runtime truth. |
| T-14-01-05 | D | `lib/scoria/runtime/defaults.ex` | mitigate | Resolve defaults from static config and pure normalization, not through runtime lookups that add boot-time or call-time complexity. |
| T-14-01-06 | E | `lib/scoria/runtime/params.ex` | mitigate | Preserve per-run overrides as explicit caller input instead of allowing hidden config expansion to bypass the public contract. |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/prompt_policy_test.exs`
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/prompt_policy_test.exs test/scoria/runtime/defaults_test.exs`
</verification>

<success_criteria>
- `Scoria.PromptPolicy` exists as the canonical public prompt-policy noun.
- Baseline runtime defaults come from one obvious app-facing config surface.
- The config contract stays plain and Phoenix-library-like rather than magical.
- Direct tests lock both the prompt-policy and defaults-surface contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/14-policy-defaults-and-install-ergonomics/14-01-SUMMARY.md`
</output>
