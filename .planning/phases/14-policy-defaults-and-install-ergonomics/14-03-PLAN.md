---
phase: 14-policy-defaults-and-install-ergonomics
plan: 03
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
files_modified:
  - lib/mix/tasks/scoria.install.ex
  - lib/mix/tasks/scoria.test.knowledge.ex
  - test/mix/tasks/scoria.install_test.exs
  - test/mix/tasks/scoria.install_route_smoke_test.exs
  - test/scoria/bootstrap/migration_lane_compatibility_test.exs
  - test/mix/tasks/scoria.test_knowledge_test.exs
autonomous: true
nyquist_compliant: true
requirements:
  - POLY-03
must_haves:
  truths:
    - "The default Scoria install lane works for a normal Phoenix app without requiring the optional knowledge subsystem."
    - "Core verification and knowledge verification remain explicit separate lanes with unambiguous commands."
    - "Installer behavior, migration expectations, and verification lane naming are locked by tests instead of README-only guidance."
  artifacts:
    - path: "lib/mix/tasks/scoria.install.ex"
      provides: "Default-lane install scaffolding for the boring Phoenix path"
    - path: "test/mix/tasks/scoria.install_route_smoke_test.exs"
      provides: "Route-level proof that the installed `/scoria` dashboard mount actually resolves"
    - path: "lib/mix/tasks/scoria.test.knowledge.ex"
      provides: "Explicit optional knowledge verification lane with corrected task contract"
    - path: "test/scoria/bootstrap/migration_lane_compatibility_test.exs"
      provides: "Core-versus-knowledge lane invariants"
  key_links:
    - from: "lib/mix/tasks/scoria.install.ex"
      to: "test/mix/tasks/scoria.install_test.exs"
      via: "installer contract and idempotent file mutation coverage"
      pattern: "router|tailwind|config|scoria_dashboard"
    - from: "test/mix/tasks/scoria.install_route_smoke_test.exs"
      to: "lib/mix/tasks/scoria.install.ex"
      via: "installed router mutation yields a live `/scoria` route instead of only patched text"
      pattern: "/scoria|route_info|Phoenix\\.LiveView\\.Plug"
    - from: "lib/mix/tasks/scoria.test.knowledge.ex"
      to: "test/scoria/bootstrap/migration_lane_compatibility_test.exs"
      via: "optional knowledge verification remains explicit and isolated from the core lane"
      pattern: "SCORIA_TEST_INCLUDE_KNOWLEDGE|migrate_knowledge|knowledge"
    - from: "test/mix/tasks/scoria.test_knowledge_test.exs"
      to: "lib/mix/tasks/scoria.test.knowledge.ex"
      via: "knowledge-lane command contract stays explicit and discoverable"
      pattern: "Mix\\.Tasks\\.(Scoria\\.)?Test\\.Knowledge"
---

<objective>
Harden the boring install and verification path so a normal Phoenix app can prove core Scoria integration without tripping into optional knowledge dependencies.

Purpose: satisfy `POLY-03` by making the default lane additive, conservative, and test-verified while preserving an explicit secondary knowledge lane.
Output: a hardened installer, explicit verification-lane task contracts, and tests that lock the core-vs-knowledge boundary.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-CONTEXT.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-RESEARCH.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-PATTERNS.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-VALIDATION.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-01-PLAN.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-02-PLAN.md

<interfaces>
From lib/mix/tasks/scoria.install.ex:
```elixir
def run(_args)
def do_run(router_path, tailwind_path)
```

From lib/mix/tasks/scoria.test.knowledge.ex:
```elixir
def run(args)
```

From test/scoria/bootstrap/migration_lane_compatibility_test.exs:
```elixir
test "core lane reaches the default schema without creating knowledge tables"
test "knowledge lane is idempotent even when the historical core version is already recorded"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden `mix scoria.install` for the boring Phoenix core lane</name>
  <read_first>lib/mix/tasks/scoria.install.ex, test/mix/tasks/scoria.install_test.exs, .planning/phases/14-policy-defaults-and-install-ergonomics/14-CONTEXT.md, .planning/phases/14-policy-defaults-and-install-ergonomics/14-RESEARCH.md</read_first>
  <files>lib/mix/tasks/scoria.install.ex, test/mix/tasks/scoria.install_test.exs, test/mix/tasks/scoria.install_route_smoke_test.exs</files>
  <acceptance_criteria>The installer wires only the default lane from D-13 and D-14: router/dashboard mount, asset/tailwind updates, baseline runtime config scaffolding, and migration guidance for the core lane; it does not require pgvector, Docker, or knowledge tables; tests prove file mutation stays additive, idempotent, and scoped to the boring Phoenix path; a route-level smoke test proves the installed `/scoria` dashboard route actually resolves, satisfying the D-15 core-lane success contract.</acceptance_criteria>
  <action>Extend `mix scoria.install` from its current minimal mutation helper into a clearer default-lane installer that teaches one obvious Phoenix integration path without broadening into full docs work. Keep the task conservative and additive: detect typical router/assets locations, inject only the core Scoria route and asset paths, scaffold the baseline runtime defaults config if appropriate, and print core migration/verification guidance that does not assume optional knowledge subsystems. Strengthen the installer tests accordingly, including idempotence and failure cases for missing expected files. Add a dedicated route smoke test that applies the installer to a fixture router, compiles or inspects the resulting router contract, and proves `/scoria` resolves as a live dashboard route rather than merely existing as patched text.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.install_route_smoke_test.exs</automated>
  </verify>
  <done>The installer now teaches and enforces one boring Phoenix core lane with direct automated coverage, including proof that the mounted `/scoria` route works.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the core-versus-knowledge verification lane boundary</name>
  <read_first>lib/mix/tasks/scoria.test.knowledge.ex, lib/mix/tasks/scoria.pgvector.bootstrap.ex, test/scoria/bootstrap/migration_lane_compatibility_test.exs, .planning/phases/14-policy-defaults-and-install-ergonomics/14-PATTERNS.md, .planning/phases/14-policy-defaults-and-install-ergonomics/14-VALIDATION.md</read_first>
  <files>lib/mix/tasks/scoria.test.knowledge.ex, test/scoria/bootstrap/migration_lane_compatibility_test.exs, test/mix/tasks/scoria.test_knowledge_test.exs</files>
  <acceptance_criteria>The default verification story from D-15 is satisfied by core migrations plus `mix test`; knowledge verification remains a separate explicit command per D-16; the current task naming mismatch is resolved or covered so the explicit optional lane is not ambiguous; migration-lane tests continue to prove that the core lane does not accidentally create knowledge tables while the knowledge lane remains idempotent.</acceptance_criteria>
  <action>Harden the optional verification lane around the repo’s existing split instead of inventing a new bootstrap system. Correct the knowledge-lane command contract so the task name, module name, and tests agree on an explicit Scoria-specific lane, and add targeted tests proving that core verification stays independent from knowledge setup. Use the existing migration-lane compatibility test as the durable invariant source: a normal Phoenix app should succeed on the core lane alone, while the knowledge lane remains explicit, opt-in, and repeatable.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.test_knowledge_test.exs test/scoria/bootstrap/migration_lane_compatibility_test.exs</automated>
  </verify>
  <done>The default verification lane is now unambiguous, the optional knowledge lane remains explicit, and the split is protected by automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer task → host app files | Scoria mutates Phoenix app code and config during onboarding |
| core verification lane → optional knowledge lane | Default integration proof must not silently depend on optional subsystems |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | S | `lib/mix/tasks/scoria.install.ex` | mitigate | Detect and mutate only expected Phoenix files so the installer does not pretend unsupported layouts are fully configured. |
| T-14-03-02 | T | `test/mix/tasks/scoria.install_test.exs` | mitigate | Lock additive and idempotent installer behavior so future changes do not over-write host app code or config. |
| T-14-03-03 | R | `test/scoria/bootstrap/migration_lane_compatibility_test.exs` | mitigate | Preserve explicit evidence that the core lane and knowledge lane remain distinct and attributable. |
| T-14-03-04 | I | `lib/mix/tasks/scoria.test.knowledge.ex` | mitigate | Keep optional knowledge commands explicit and avoid implying extra dependencies are part of the default lane. |
| T-14-03-05 | D | `lib/mix/tasks/scoria.install.ex` | mitigate | Prevent the default installer from pulling in pgvector or other optional steps that would block normal Phoenix adoption. |
| T-14-03-06 | E | `lib/mix/tasks/scoria.test.knowledge.ex` | mitigate | Require explicit invocation for the knowledge lane so callers cannot accidentally widen the default verification surface. |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.install_route_smoke_test.exs`
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.test_knowledge_test.exs test/scoria/bootstrap/migration_lane_compatibility_test.exs`
</verification>

<success_criteria>
- `mix scoria.install` supports one obvious boring Phoenix lane without optional knowledge dependencies.
- Core verification stays separate from the optional knowledge verification lane.
- Installer behavior, `/scoria` route viability, knowledge-lane command naming, and migration-lane boundaries are enforced by tests.
- A normal Phoenix app can prove the default Scoria lane works with baseline Postgres, `mix test`, and a working `/scoria` route.
</success_criteria>

<output>
After completion, create `.planning/phases/14-policy-defaults-and-install-ergonomics/14-03-SUMMARY.md`
</output>
