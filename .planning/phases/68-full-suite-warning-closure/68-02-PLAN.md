---
phase: 68-full-suite-warning-closure
plan: 02
type: execute
wave: 2
depends_on: [68-01]
files_modified:
  - test/support/scoria/host_app_proof/generator.ex
  - test/support/scoria/host_app_proof/runner.ex
  - priv/host_app_proof/overlay/test/host_route_smoke_test.exs
  - priv/host_app_proof/overlay/test/host_runtime_smoke_test.exs
  - test/scoria/host_app_consumer_proof_test.exs
  - test/mix/tasks/scoria.install_test.exs
  - test/mix/tasks/scoria.install_route_smoke_test.exs
  - .planning/WARNING-INVENTORY.md
autonomous: true
requirements: [WARN-07]
must_haves:
  truths:
    - "mix test.adoption --warnings-as-errors passes locally (p2 adoption lane WAE, D-07)."
    - "Full-scope inventory shows zero :host_proof_generated_compile and :host_overlay_test_path clusters after fixes (D-14)."
    - "Host-proof subprocess noise is fixed at source — not baselined (D-08, D-14)."
    - "Overlay templates remain under priv/host_app_proof/overlay/test/ per architecture test (D-15)."
  artifacts:
    - path: "test/support/scoria/host_app_proof/generator.ex"
      provides: "WAE-clean host proof generator"
      contains: "defmodule Scoria.HostAppProof.Generator"
    - path: "test/scoria/host_app_proof_architecture_test.exs"
      provides: "overlay path regression guard"
      contains: "host proof overlay lives under priv"
  key_links:
    - from: "test/scoria/host_app_consumer_proof_test.exs"
      to: "test/support/scoria/host_app_proof/generator.ex"
      via: "generated host under System.tmp_dir!"
      pattern: "HostAppProof"
---

<objective>
Close deferred p2 host-proof warning clusters at source so adoption lane and full-suite capture stay clean — without baselining subprocess phx.new noise or adding global suppressions.

Purpose: Remove `:host_proof_generated_compile` and `:host_overlay_test_path` from full-suite inventory before WARN-07 full WAE attempt in 68-03.
Output: WAE-clean host proof support code and overlay templates; green `mix test.adoption --warnings-as-errors`.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/68-full-suite-warning-closure/68-CONTEXT.md
@.planning/phases/68-full-suite-warning-closure/68-RESEARCH.md
@lib/scoria/warning_inventory/cluster.ex
@test/support/scoria/host_app_proof/generator.ex
@test/support/scoria/host_app_proof/runner.ex
@priv/host_app_proof/overlay/test/
@test/scoria/host_app_proof_architecture_test.exs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Baselining subprocess compile noise instead of fixing | high | Reject new Accepted rows for host-proof; inventory must show zero p2 clusters |
| Moving overlay back to test/support | high | host_app_proof_architecture_test.exs must keep passing |
| Global @compile suppressions on overlay templates | medium | Fix unused vars/bindings minimally; no blanket suppressions (D-08) |
| Subprocess warnings leak into parent capture | medium | Generated hosts use System.tmp_dir!(); parent only compiles support/generator paths in repo |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 68-02-01: Measure p2 clusters with clean-tmp full inventory</name>
  <requirement_ids>WARN-07</requirement_ids>
  <files>.planning/WARNING-INVENTORY.md</files>
  <read_first>
    lib/scoria/warning_inventory/cluster.ex
    lib/mix/tasks/scoria.warning_inventory.ex
    .planning/WARNING-INVENTORY.md
    test/fixtures/warning_inventory/host_proof_generated_compile.txt
    test/fixtures/warning_inventory/host_overlay_test_path.txt
  </read_first>
  <action>
1. Ensure clean tmp:

```bash
rm -rf test/tmp/*
```

2. Run maintainer measurement (not CI gate):

```bash
MIX_ENV=test mix scoria.warning_inventory --scope full --format table
```

3. Record cluster counts for `:host_proof_generated_compile` and `:host_overlay_test_path`. If both are zero, skip code fixes in 68-02-02/03 and proceed directly to 68-02-04 verification — document "p2 already clean" in commit message.

4. If clusters non-zero, capture offending `file:line message` rows grouped by cluster id for targeted fixes in next tasks.

Cluster matchers (SSOT `lib/scoria/warning_inventory/cluster.ex`):
- `:host_proof_generated_compile` — file contains `test/support/scoria/host_app_proof/` and NOT `host_app_proof/overlay/test/`
- `:host_overlay_test_path` — file contains `host_app_proof/overlay/test/`
  </action>
  <acceptance_criteria>
    - Inventory command completes without preflight error after `rm -rf test/tmp/*`.
    - Task output or commit notes list p2 cluster counts (zero or specific file paths to fix).
    - `mix scoria.warning_baseline.check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix scoria.warning_baseline.check && rm -rf test/tmp/* && MIX_ENV=test mix scoria.warning_inventory --scope full --format table --quiet</automated>
  </verify>
  <done>p2 debt measured with reproducible inventory command; fix targets identified or confirmed absent.</done>
</task>

<task type="auto">
  <name>Task 68-02-02: Fix host-proof support compile warnings (:host_proof_generated_compile)</name>
  <requirement_ids>WARN-07</requirement_ids>
  <files>test/support/scoria/host_app_proof/generator.ex, test/support/scoria/host_app_proof/runner.ex, test/scoria/host_app_consumer_proof_test.exs, test/mix/tasks/scoria.install_test.exs, test/mix/tasks/scoria.install_route_smoke_test.exs</files>
  <read_first>
    test/support/scoria/host_app_proof/generator.ex
    test/support/scoria/host_app_proof/runner.ex
    test/scoria/host_app_proof_architecture_test.exs
    lib/mix/tasks/test.adoption.ex
    test/fixtures/warning_inventory/host_proof_generated_compile.txt
  </read_first>
  <action>
Apply minimal fixes per inventory rows under `test/support/scoria/host_app_proof/` (NOT overlay paths):

Common patterns from Phase 67:
- Remove unused default args (do not prefix with `_` unless binding is required)
- Remove unused variable bindings
- Fix deprecated API warnings

Constraints:
- `generator.ex` line 11 area: generated hosts stay under `System.tmp_dir!()` — do not revert to repo `test/tmp` for host app root
- `copy_overlay!/1` copies from `priv/host_app_proof/overlay/test/` — do not move overlay back to test/support
- No `@compile {:no_warn_undefined, ...}` blanket on support modules

After each file change, verify scoped WAE:

```bash
MIX_ENV=test mix test --warnings-as-errors test/scoria/host_app_consumer_proof_test.exs
MIX_ENV=test mix test --warnings-as-errors test/mix/tasks/scoria.install_test.exs
MIX_ENV=test mix test --warnings-as-errors test/mix/tasks/scoria.install_route_smoke_test.exs
```

If 68-02-01 showed zero `:host_proof_generated_compile` rows, mark task done with no file changes.
  </action>
  <acceptance_criteria>
    - `MIX_ENV=test mix test --warnings-as-errors test/support/scoria/host_app_proof/` compiles clean (via adoption tests that load support).
    - `MIX_ENV=test mix test --warnings-as-errors test/scoria/host_app_consumer_proof_test.exs` exits 0.
    - `MIX_ENV=test mix test test/scoria/host_app_proof_architecture_test.exs` exits 0.
    - Full inventory `--scope full` shows `:host_proof_generated_compile` count 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/scoria/host_app_consumer_proof_test.exs && MIX_ENV=test mix test test/scoria/host_app_proof_architecture_test.exs</automated>
  </verify>
  <done>Host proof generator/runner support paths compile clean under WAE.</done>
</task>

<task type="auto">
  <name>Task 68-02-03: Fix overlay template warnings (:host_overlay_test_path)</name>
  <requirement_ids>WARN-07</requirement_ids>
  <files>priv/host_app_proof/overlay/test/host_route_smoke_test.exs, priv/host_app_proof/overlay/test/host_runtime_smoke_test.exs</files>
  <read_first>
    priv/host_app_proof/overlay/test/host_route_smoke_test.exs
    priv/host_app_proof/overlay/test/host_runtime_smoke_test.exs
    test/fixtures/warning_inventory/host_overlay_test_path.txt
    test/scoria/host_app_proof_architecture_test.exs
  </read_first>
  <action>
Fix compile warnings in overlay smoke templates copied into generated host apps:

Files:
- `priv/host_app_proof/overlay/test/host_route_smoke_test.exs`
- `priv/host_app_proof/overlay/test/host_runtime_smoke_test.exs`

Apply minimal diffs only (unused imports, dead defaults, unused aliases). Do **not** add `@compile` suppressions.

Note: These files are templates copied by `generator.ex` — they are not compiled in the Scoria app directly, but inventory attributes warnings to `host_app_proof/overlay/test/` paths when captured during full suite.

If 68-02-01 showed zero `:host_overlay_test_path` rows, mark task done with no file changes.

Re-run consumer proof after template edits:

```bash
MIX_ENV=test mix test --warnings-as-errors test/scoria/host_app_consumer_proof_test.exs
```
  </action>
  <acceptance_criteria>
    - `rg -n "@compile" priv/host_app_proof/overlay/test/` returns no suppression additions.
    - Full inventory `--scope full` shows `:host_overlay_test_path` count 0.
    - `MIX_ENV=test mix test --warnings-as-errors test/scoria/host_app_consumer_proof_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/scoria/host_app_consumer_proof_test.exs</automated>
  </verify>
  <done>Overlay template warnings cleared at source.</done>
</task>

<task type="auto">
  <name>Task 68-02-04: Adoption lane WAE closeout verification</name>
  <requirement_ids>WARN-07</requirement_ids>
  <files>.planning/WARNING-INVENTORY.md</files>
  <read_first>
    lib/mix/tasks/test.adoption.ex
    docs/operator_verification.md
    .planning/WARNING-BASELINE.md
  </read_first>
  <action>
Run p2 closeout stack:

```bash
mix scoria.warning_baseline.check
rm -rf test/tmp/*
MIX_ENV=test mix test.adoption --warnings-as-errors
MIX_ENV=test mix scoria.warning_ratchet.test --warnings-as-errors
MIX_ENV=test mix scoria.warning_inventory --scope full --format table
```

Assert:
- Adoption WAE: 12 files from `Mix.Tasks.Scoria.Test.Adoption.adoption_test_files/0`, 0 failures
- Inventory p2 clusters both zero

Do **not** run `--write` yet (reserved for 68-03 baseline ledger closeout).

Update `.planning/WARNING-INVENTORY.md` Phase 67 table row for `:host_proof_*` clusters to note "fixed in Phase 68 plan 68-02" if fixes were applied (manual edit or wait for 68-03 `--write`).
  </action>
  <acceptance_criteria>
    - `MIX_ENV=test mix test.adoption --warnings-as-errors` exits 0.
    - `MIX_ENV=test mix scoria.warning_ratchet.test --warnings-as-errors` exits 0.
    - `mix scoria.warning_baseline.check` exits 0.
    - Inventory full scope: `:host_proof_generated_compile` and `:host_overlay_test_path` counts are 0.
  </acceptance_criteria>
  <verify>
    <automated>mix scoria.warning_baseline.check && MIX_ENV=test mix test.adoption --warnings-as-errors && MIX_ENV=test mix scoria.warning_ratchet.test --warnings-as-errors</automated>
  </verify>
  <done>p2 adoption debt closed; staged ratchet WAE remains green; ready for 68-03 full-suite attempt.</done>
</task>

</tasks>

<verification>
mix scoria.warning_baseline.check
MIX_ENV=test mix test.adoption --warnings-as-errors
MIX_ENV=test mix scoria.warning_ratchet.test --warnings-as-errors
rm -rf test/tmp/*
MIX_ENV=test mix scoria.warning_inventory --scope full --format table
</verification>

<success_criteria>
- Adoption lane passes under WAE
- p2 inventory clusters at zero
- No new baseline rows for host-proof debt
</success_criteria>
