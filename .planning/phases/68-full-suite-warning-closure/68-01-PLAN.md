---
phase: 68-full-suite-warning-closure
plan: 01
type: execute
wave: 1
depends_on: [68-00]
files_modified:
  - .github/workflows/ci.yml
  - test/scoria/ci_policy_contract_test.exs
  - docs/operator_verification.md
autonomous: true
requirements: [WARN-07]
must_haves:
  truths:
    - "CI test job runs mix scoria.warning_ratchet.test --warnings-as-errors after runtime_to_handoff and before broad mix test (D-01, D-04)."
    - "Policy job still excludes warning_ratchet.test (D-17)."
    - "ci_policy_contract_test asserts ratchet placement and policy isolation (D-05)."
    - "operator_verification.md documents CI adoption-file WAE via ratchet bridge, not duplicate adoption WAE step (D-14)."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "staged WARN-06 CI gate"
      contains: "mix scoria.warning_ratchet.test --warnings-as-errors"
    - path: "test/scoria/ci_policy_contract_test.exs"
      provides: "ratchet gate-order contract"
      contains: "warning ratchet after runtime_to_handoff"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "lib/mix/tasks/scoria.warning_ratchet.test.ex"
      via: "Run high-signal warning ratchet step"
      pattern: "scoria.warning_ratchet.test"
---

<objective>
Wire the staged WARN-06 high-signal WAE gate into the CI test job, extend gate-order contracts, and document the CI↔ratchet mapping for adopters — without flipping to full-suite WAE until Plan 68-03.

Purpose: Enforce ~421 high-signal tests under WAE in CI while full-suite debt remains baselined until 2026-06-07 closeout.
Output: ci.yml ratchet step, ci_policy_contract_test extensions, operator doc CI subsection.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/68-full-suite-warning-closure/68-CONTEXT.md
@.planning/phases/68-full-suite-warning-closure/68-RESEARCH.md
@.github/workflows/ci.yml
@test/scoria/ci_policy_contract_test.exs
@lib/scoria/verification_lanes.ex
@docs/operator_verification.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Ratchet step placed before DB migrate breaks path tests | high | Insert only after `mix test.runtime_to_handoff` (line 110); adoption + handoff already ran ecto.create/migrate |
| Ratchet in policy job doubles CI time on every PR | medium | ci_policy_contract_test refutes ratchet in policy section; policy job unchanged |
| Contributors confuse ratchet with adoption lane rename | medium | operator doc states plain `mix test.adoption` remains behavior lane; ratchet is maintainer WAE bar |
| Gate-order regression undetected | medium | Automated contract test indexes step strings in ci.yml |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 68-01-01: Insert staged ratchet WAE step in ci.yml test job</name>
  <requirement_ids>WARN-07</requirement_ids>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .github/workflows/ci.yml
    .planning/phases/68-full-suite-warning-closure/68-CONTEXT.md
    lib/scoria/verification_lanes.ex
  </read_first>
  <action>
In `.github/workflows/ci.yml` test job, insert a new step **after** line 110 (`Run runtime-to-handoff proof lane` / `mix test.runtime_to_handoff`) and **before** line 112 (`Run tests` / `mix test`):

```yaml
      - name: Run high-signal warning ratchet
        run: mix scoria.warning_ratchet.test --warnings-as-errors
```

Do **not** change:
- Policy job steps (lines 27–47)
- `needs: policy` on test job (line 51)
- Plain `mix test` step (line 112–113) — remains without WAE until 68-03
- `mix test.knowledge` step (lines 115–116)
- Test job `env:` block (lines 68–74): `MIX_ENV: test`, `SCORIA_DB_PORT: 55432`

Do **not** add `mix test.adoption --warnings-as-errors` as a separate step (D-12).
  </action>
  <acceptance_criteria>
    - `rg -n "mix scoria.warning_ratchet.test --warnings-as-errors" .github/workflows/ci.yml` returns exactly one match in test job.
    - `rg -n "Run high-signal warning ratchet" .github/workflows/ci.yml` returns one match.
    - Step order: `index_of(ci.yml, "mix test.runtime_to_handoff")` &lt; `index_of(ci.yml, "scoria.warning_ratchet.test")` &lt; `index_of(ci.yml, "run: mix test\n")` (broad test step, not ratchet).
    - Policy job section (before `\n  test:`) does not contain `scoria.warning_ratchet`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "mix scoria.warning_ratchet.test --warnings-as-errors" .github/workflows/ci.yml && rg -n "mix test.runtime_to_handoff" .github/workflows/ci.yml</automated>
  </verify>
  <done>CI test job runs staged high-signal WAE between closeout lanes and broad test.</done>
</task>

<task type="auto">
  <name>Task 68-01-02: Extend ci_policy_contract_test for ratchet placement</name>
  <requirement_ids>WARN-07</requirement_ids>
  <files>test/scoria/ci_policy_contract_test.exs</files>
  <read_first>
    test/scoria/ci_policy_contract_test.exs
    .github/workflows/ci.yml
    lib/scoria/verification_lanes.ex
  </read_first>
  <action>
Add to `test/scoria/ci_policy_contract_test.exs`:

```elixir
@ratchet_wae "mix scoria.warning_ratchet.test --warnings-as-errors"
```

Add test:

```elixir
test "test job runs warning ratchet after runtime_to_handoff and before broad mix test" do
  ci_workflow = File.read!(".github/workflows/ci.yml")
  runtime_to_handoff = VerificationLanes.ci_command(:runtime_to_handoff)

  assert ci_workflow =~ @ratchet_wae
  assert index_of(ci_workflow, runtime_to_handoff) < index_of(ci_workflow, @ratchet_wae)
  assert index_of(ci_workflow, @ratchet_wae) < index_of(ci_workflow, "run: mix test\n")
end
```

Extend existing `"policy job does not run warning_ratchet.test"` test (lines 51–56) to also refute `@ratchet_wae` string explicitly in policy section.

Preserve all existing tests unchanged (baseline before compile WAE, closeout chain, postgres isolation, WARN-06 doc test).
  </action>
  <acceptance_criteria>
    - `rg -n "test job runs warning ratchet after runtime_to_handoff" test/scoria/ci_policy_contract_test.exs` returns one match.
    - `rg -n "@ratchet_wae" test/scoria/ci_policy_contract_test.exs` returns at least one match.
    - `MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs</automated>
  </verify>
  <done>Gate-order contracts enforce D-01/D-05 ratchet placement and policy isolation.</done>
</task>

<task type="auto">
  <name>Task 68-01-03: Document CI↔ratchet mapping and run staged WAE closeout</name>
  <requirement_ids>WARN-07</requirement_ids>
  <files>docs/operator_verification.md</files>
  <read_first>
    docs/operator_verification.md
    lib/mix/tasks/test.adoption.ex
    lib/scoria/warning_ratchet.ex
    .planning/phases/68-full-suite-warning-closure/68-CONTEXT.md
  </read_first>
  <action>
Add subsection `### WARN-07 CI warning gates (staged)` after the WARN-06 block (after line 233) in `docs/operator_verification.md`:

```markdown
### WARN-07 CI warning gates (staged)

CI preserves behavioral lane commands unchanged:

- `mix test.adoption` — default runtime lane (behavior)
- `mix test.runtime_to_handoff` — escalation lane (behavior)

CI enforces adoption-file compile warnings via the high-signal ratchet bridge (not a second `mix test.adoption --warnings-as-errors` invocation):

```bash
mix scoria.warning_ratchet.test --warnings-as-errors
```

Ratchet paths include `Mix.Tasks.Scoria.Test.Adoption.adoption_test_files/0` plus `test/scoria/**/*_test.exs` and `test/scoria_web/live/**/*_test.exs` (`Scoria.WarningRatchet.high_signal_wae_paths/0`).

Maintainer adopter-parity debug command (local only until full WARN-07 closeout):

```bash
MIX_ENV=test mix test.adoption --warnings-as-errors
```

If the ratchet step fails in CI, run the same command locally with pgvector Postgres on port 55432 (`SCORIA_DB_PORT=55432`).
```

Run closeout verification (CI-parity env when pgvector available):

```bash
mix scoria.warning_baseline.check
MIX_ENV=test mix scoria.warning_ratchet.test --warnings-as-errors
MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs
```
  </action>
  <acceptance_criteria>
    - `rg -n "WARN-07 CI warning gates \\(staged\\)" docs/operator_verification.md` returns one match.
    - `rg -n "not a second.*mix test.adoption --warnings-as-errors" docs/operator_verification.md` returns one match.
    - `rg -n "high_signal_wae_paths" docs/operator_verification.md` returns one match.
    - `mix scoria.warning_baseline.check` exits 0.
    - `MIX_ENV=test mix scoria.warning_ratchet.test --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix scoria.warning_baseline.check && MIX_ENV=test mix scoria.warning_ratchet.test --warnings-as-errors</automated>
  </verify>
  <done>Staged WARN-06 CI gate wired, contracted, documented; baseline check + ratchet WAE green.</done>
</task>

</tasks>

<verification>
mix scoria.warning_baseline.check
MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs
MIX_ENV=test mix scoria.warning_ratchet.test --warnings-as-errors
</verification>

<success_criteria>
- CI test job includes ratchet WAE step in correct order
- Policy job unchanged (no ratchet)
- Maintainer staged WAE command passes locally
- WARN-07 partial delivery: high-signal path WAE enforced in CI
</success_criteria>
