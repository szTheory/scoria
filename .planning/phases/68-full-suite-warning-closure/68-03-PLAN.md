---
phase: 68-full-suite-warning-closure
plan: 03
type: execute
wave: 3
depends_on: [68-02]
files_modified:
  - test/scoria_web/live/workflow_live_test.exs
  - test/scoria_web/live/review_queue_live_test.exs
  - test/scoria_web/live/prompt_live/release_workbench_live_test.exs
  - .github/workflows/ci.yml
  - test/scoria/ci_policy_contract_test.exs
  - .planning/WARNING-BASELINE.md
  - .planning/WARNING-INVENTORY.md
  - .planning/warning-inventory.baseline.json
  - docs/operator_verification.md
  - .planning/phases/68-full-suite-warning-closure/68-VERIFICATION.md
autonomous: true
requirements: [WARN-07]
must_haves:
  truths:
    - "MIX_ENV=test mix test --warnings-as-errors passes locally and matches CI test job (WARN-07)."
    - "CI test job uses full WAE — ratchet step removed after flip (D-01 full phase)."
    - "WARNING-BASELINE.md full-suite umbrella row resolved or honestly narrowed per D-10/D-11/D-16."
    - "Canonical lane warning-clean status preserved — compile WAE + lane-contract tests still pass (WARN-05 regression)."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "full-suite WAE gate"
      contains: "mix test --warnings-as-errors"
    - path: ".planning/WARNING-BASELINE.md"
      provides: "post-ratchet baseline truth"
      contains: "Resolved During v2.6"
    - path: ".planning/phases/68-full-suite-warning-closure/68-VERIFICATION.md"
      provides: "WARN-07 closeout evidence"
      contains: "mix test --warnings-as-errors"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "mix test --warnings-as-errors"
      via: "Run tests step replaces ratchet + plain test"
      pattern: "warnings-as-errors"
---

<objective>
Complete WARN-07: bounded LiveView async teardown fixes, green full-suite WAE, baseline ledger closeout before 2026-06-07 expiry, and CI flip from staged ratchet to `mix test --warnings-as-errors`.

Purpose: Deliver production regression gate for compiler warnings across the full test suite (or honest narrow re-baseline for runtime-only LiveView noise per D-10).
Output: Green full WAE, updated WARNING-BASELINE.md, committed inventory JSON, CI + contract updates, 68-VERIFICATION.md.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/68-full-suite-warning-closure/68-CONTEXT.md
@.planning/phases/68-full-suite-warning-closure/68-RESEARCH.md
@.planning/phases/68-full-suite-warning-closure/68-VALIDATION.md
@.planning/WARNING-BASELINE.md
@test/scoria_web/live/workflow_live_test.exs
@test/scoria_web/live/review_queue_live_test.exs
@test/scoria_web/live/orchestrator_live_test.exs
@.github/workflows/ci.yml
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| CI flip before local full WAE green | high | D-22: do not merge CI change until local `mix test --warnings-as-errors` passes |
| Immortal full-suite umbrella baseline | high | Delete full-suite row on success; never renew catch-all (D-11) |
| Expired baseline rows block merge | high | Update WARNING-BASELINE.md in same PR as CI flip (D-18) |
| LiveView sweep scope creep | medium | Only workflow/replay files from inventory priority list |
| Knowledge lane warnings excluded from default mix test | medium | Add `mix test.knowledge --warnings-as-errors` if pgvector lane emits warnings |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 68-03-01: Bounded LiveView render_async sweep (p4 workflow/replay)</name>
  <requirement_ids>WARN-07</requirement_ids>
  <files>test/scoria_web/live/workflow_live_test.exs, test/scoria_web/live/review_queue_live_test.exs, test/scoria_web/live/prompt_live/release_workbench_live_test.exs</files>
  <read_first>
    test/scoria_web/live/orchestrator_live_test.exs
    test/scoria_web/live/workflow_live_test.exs
    test/scoria_web/live/review_queue_live_test.exs
    test/scoria_web/live/prompt_live/release_workbench_live_test.exs
    test/fixtures/warning_inventory/liveview_async_teardown.txt
    lib/scoria/warning_inventory/cluster.ex
  </read_first>
  <action>
Add `render_async(view)` before test end in workflow/replay LiveView tests that mount async UI paths — follow pattern from `orchestrator_live_test.exs` line 166.

**Priority files (no render_async today):**

1. `test/scoria_web/live/workflow_live_test.exs` — 11 tests with `live/2` or `live/3`:
   - Tests binding `view`: add `render_async(view)` as final statement (e.g. lines 107, 165, 462, 570)
   - Tests binding `_view`: change to `view` and add `render_async(view)` OR use `try/after` if view must stay underscored
   - Key test: `"replay runs render provenance strip..."` (line 378) — high priority per fixture

2. `test/scoria_web/live/review_queue_live_test.exs` — 2 tests (lines 73, 102):
   - Add `render_async(view)` at end of each test after last `render_click`

3. `test/scoria_web/live/prompt_live/release_workbench_live_test.exs`:
   - Add `render_async(view)` to tests using `{:ok, view, ...} = live(...)`

Do **not** refactor orchestrator_live_test.exs (already has render_async at lines 166, 162, 227 in sre file).

After sweep, measure runtime cluster:

```bash
MIX_ENV=test mix scoria.warning_inventory --include-runtime --scope full --format table
```

Per-file WAE check:

```bash
MIX_ENV=test mix test --warnings-as-errors test/scoria_web/live/workflow_live_test.exs
MIX_ENV=test mix test --warnings-as-errors test/scoria_web/live/review_queue_live_test.exs
MIX_ENV=test mix test --warnings-as-errors test/scoria_web/live/prompt_live/release_workbench_live_test.exs
```
  </action>
  <acceptance_criteria>
    - `rg -n "render_async" test/scoria_web/live/workflow_live_test.exs` returns at least 3 matches.
    - `rg -n "render_async" test/scoria_web/live/review_queue_live_test.exs` returns at least 2 matches.
    - `MIX_ENV=test mix test --warnings-as-errors test/scoria_web/live/workflow_live_test.exs test/scoria_web/live/review_queue_live_test.exs test/scoria_web/live/prompt_live/release_workbench_live_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/scoria_web/live/workflow_live_test.exs test/scoria_web/live/review_queue_live_test.exs test/scoria_web/live/prompt_live/release_workbench_live_test.exs</automated>
  </verify>
  <done>Workflow/replay LiveView tests drain async assigns before teardown.</done>
</task>

<task type="auto">
  <name>Task 68-03-02: Full-suite WAE attempt and remaining compile warning fixes</name>
  <requirement_ids>WARN-07</requirement_ids>
  <files>test/**/*.exs, lib/**/*.ex</files>
  <read_first>
    .planning/WARNING-BASELINE.md
    .planning/STATE.md
    lib/scoria/warning_inventory/cluster.ex
  </read_first>
  <action>
1. CI-parity prep:

```bash
rm -rf test/tmp/*
export MIX_ENV=test SCORIA_DB_HOST=localhost SCORIA_DB_PORT=55432
mix ecto.create && mix ecto.migrate
```

2. Run full WAE:

```bash
MIX_ENV=test mix test --warnings-as-errors
```

3. If failures are **compile warnings** (not test assertions):
   - Run `MIX_ENV=test mix scoria.warning_inventory --scope full --format table` to classify
   - Fix `:unclassified_compile` and high-signal clusters outside ratchet scope using Phase 67 patterns (minimal diff)
   - Re-run full WAE until green

4. If failures are **test failures** (not warnings): fix tests — out of WARN-07 scope but blocks gate.

5. Optional knowledge lane (if warnings appear when running knowledge tests separately):

```bash
MIX_ENV=test mix test.knowledge --warnings-as-errors
```

Fix knowledge-tagged test warnings if present; document in 68-VERIFICATION.md.

Do **not** enable global `config/test.exs` `warnings_as_errors: true`.
  </action>
  <acceptance_criteria>
    - `MIX_ENV=test mix test --warnings-as-errors` exits 0.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0 (WARN-05 regression).
    - `MIX_ENV=test mix test --warnings-as-errors test/scoria/verification_lanes_test.exs test/scoria/adoption_surface_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors</automated>
  </verify>
  <done>Full test suite passes under warnings-as-errors locally.</done>
</task>

<task type="auto">
  <name>Task 68-03-03: Baseline ledger closeout and inventory --write</name>
  <requirement_ids>WARN-07</requirement_ids>
  <files>.planning/WARNING-BASELINE.md, .planning/WARNING-INVENTORY.md, .planning/warning-inventory.baseline.json</files>
  <read_first>
    .planning/WARNING-BASELINE.md
    .planning/phases/68-full-suite-warning-closure/68-CONTEXT.md
    lib/scoria/warning_baseline.ex
  </read_first>
  <action>
**Path A — full WARN-07 success (target):**

1. Edit `.planning/WARNING-BASELINE.md`:
   - Move both Accepted rows (lines 16–17) to new section `## Resolved During v2.6` after Accepted table:

```markdown
## Resolved During v2.6

| Surface | Resolved Debt | Resolution Date |
|--------|----------------|-----------------|
| full-suite (non-canonical) | Project-level warning audit; full mix test --warnings-as-errors green in CI | 2026-05-27 |
| workflow/replay LiveView tests | Async teardown noise cleared via render_async sweep | 2026-05-27 |
```

   - Leave `## Accepted Warning Debt` with header + empty table (headers only):

```markdown
## Accepted Warning Debt

| Surface | Warning Debt | Reason | Owner | Expires |
|--------|--------------|--------|-------|---------|
```

2. Run:

```bash
rm -rf test/tmp/*
MIX_ENV=test mix scoria.warning_inventory --write --scope full
mix scoria.warning_baseline.check
```

3. Commit `.planning/warning-inventory.baseline.json` with `"clusters": {}` (or only clusters that truly remain).

**Path B — partial success (runtime LiveView noise only, D-10):**

If `--include-runtime` inventory still shows `:liveview_async_teardown` but compile WAE and full `mix test --warnings-as-errors` pass:
- Remove **full-suite (non-canonical)** row only
- Keep **one** Accepted row for `workflow/replay LiveView tests` with expiry **2026-07-31**, owner `scoria-web-runtime`
- Inventory JSON non-empty only for remaining runtime cluster

**Never:** renew full-suite umbrella catch-all (D-11).

Update `.planning/WARNING-INVENTORY.md` Phase 68 fixed/deferred section via `--write` output.
  </action>
  <acceptance_criteria>
    - `mix scoria.warning_baseline.check` exits 0.
    - `test -f .planning/warning-inventory.baseline.json`
    - On Path A: `rg -n "Resolved During v2.6" .planning/WARNING-BASELINE.md` matches and full-suite row absent from Accepted section.
    - On Path A: `rg -n '"clusters": \\{\\}' .planning/warning-inventory.baseline.json` OR clusters object empty.
  </acceptance_criteria>
  <verify>
    <automated>mix scoria.warning_baseline.check && test -f .planning/warning-inventory.baseline.json</automated>
  </verify>
  <done>Baseline ledger reflects post-ratchet truth; expiry pressure cleared for 2026-06-07.</done>
</task>

<task type="auto">
  <name>Task 68-03-04: CI flip to full WAE and update gate contracts</name>
  <requirement_ids>WARN-07</requirement_ids>
  <files>.github/workflows/ci.yml, test/scoria/ci_policy_contract_test.exs, docs/operator_verification.md</files>
  <read_first>
    .github/workflows/ci.yml
    test/scoria/ci_policy_contract_test.exs
    .planning/phases/68-full-suite-warning-closure/68-CONTEXT.md
  </read_first>
  <action>
1. In `.github/workflows/ci.yml` test job:
   - **Remove** step `Run high-signal warning ratchet` (`mix scoria.warning_ratchet.test --warnings-as-errors`) added in 68-01
   - **Change** `Run tests` step (lines 112–113) from `mix test` to:

```yaml
      - name: Run tests
        run: mix test --warnings-as-errors
```

2. Optional: if 68-03-02 required knowledge WAE, change knowledge step:

```yaml
      - name: Run knowledge lane
        run: mix test.knowledge --warnings-as-errors
```

3. Update `test/scoria/ci_policy_contract_test.exs`:
   - Replace ratchet placement test with full WAE assertion:

```elixir
test "test job runs full suite WAE after runtime_to_handoff and before knowledge lane" do
  ci_workflow = File.read!(".github/workflows/ci.yml")
  runtime_to_handoff = VerificationLanes.ci_command(:runtime_to_handoff)

  assert ci_workflow =~ "mix test --warnings-as-errors"
  refute ci_workflow =~ "mix scoria.warning_ratchet.test --warnings-as-errors"
  assert index_of(ci_workflow, runtime_to_handoff) < index_of(ci_workflow, "mix test --warnings-as-errors")
  assert index_of(ci_workflow, "mix test --warnings-as-errors") < index_of(ci_workflow, "mix test.knowledge")
end
```

   - Keep policy job ratchet refute test

4. Update `docs/operator_verification.md` WARN-07 subsection: note CI now runs `mix test --warnings-as-errors` (full gate); ratchet bridge is maintainer-only pre-flip or remove staged wording.

5. Create `.planning/phases/68-full-suite-warning-closure/68-VERIFICATION.md` with timestamps and command outputs for:
   - `mix test --warnings-as-errors`
   - `mix scoria.warning_baseline.check`
   - `mix scoria.warning_inventory --write --scope full`
   - Policy job parity commands

6. Set `nyquist_compliant: true` in `68-VALIDATION.md` frontmatter when all automated rows green.
  </action>
  <acceptance_criteria>
    - `rg -n "mix test --warnings-as-errors" .github/workflows/ci.yml` returns at least one match in test job Run tests step.
    - `rg -n "scoria.warning_ratchet.test" .github/workflows/ci.yml` returns no matches.
    - `MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs` exits 0.
    - `test -f .planning/phases/68-full-suite-warning-closure/68-VERIFICATION.md`
    - `rg -n "WARN-07" .planning/phases/68-full-suite-warning-closure/68-VERIFICATION.md` returns a match.
    - `mix scoria.warning_baseline.check` exits 0.
    - `MIX_ENV=test mix test --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix scoria.warning_baseline.check && MIX_ENV=test mix test --warnings-as-errors && MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs</automated>
  </verify>
  <done>WARN-07 complete: CI enforces full-suite WAE; baseline ledger and verification artifacts committed.</done>
</task>

</tasks>

<verification>
mix scoria.warning_baseline.check
MIX_ENV=test mix compile --warnings-as-errors
MIX_ENV=test mix test --warnings-as-errors
MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs
rm -rf test/tmp/*
MIX_ENV=test mix scoria.warning_inventory --write --scope full
</verification>

<success_criteria>
- WARN-07: CI runs `mix test --warnings-as-errors` and passes
- WARNING-BASELINE.md reflects post-ratchet truth (resolved or honestly narrowed debt)
- Canonical lane warning-clean status preserved
- 68-VERIFICATION.md records closeout evidence
</success_criteria>
