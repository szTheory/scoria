---
phase: 37-dev-component-lab-and-stress-fixtures
plan: 06
type: execute
wave: 4
depends_on: ["37-05"]
files_modified:
  - priv/dev/e2e/lab.spec.mjs
  - docs/MAINTAINERS.md
autonomous: true
requirements: [LAB-01, LAB-02, FIXT-01]
user_setup: []

must_haves:
  truths:
    - "Browser proof (lab.spec.mjs) loads /scoria/_lab, toggles theme, emulates reduced motion, scans all 6 viewport widths, and probes overlay/focus + dense table + copy — every assertion true of what ships (satisfies ROADMAP SC1/SC2/SC3, PROOF groundwork)"
    - "docs/MAINTAINERS.md explains how to run the dev server, open the lab, inspect states, update fixtures, run focused proof, and which lab probes support Phases 38-41 (satisfies ROADMAP SC5, D-34)"
    - "Phase-37 proof establishes that the lab renders at /scoria/_lab, covers all 10 D-11 states and the required fixture domains, stays dev-only, and preserves the Hex/macro boundary — verified by the Plan 01 guard test + the active e2e probes (both already in required lanes) (D-30)"
  prohibitions:
    - "No screenshot-diff / visual-regression step is added to the required ci-gate this phase — no visual-diff job added to ci.yml or ci-verify.yml (D-30; VISUAL-CI-01 stays deferred). Recorded as a VERIFICATION note that no such job was added."
  artifacts:
    - priv/dev/e2e/lab.spec.mjs
    - docs/MAINTAINERS.md
  key_links:
    - "priv/dev/e2e/*.spec.mjs is auto-picked-up by the REQUIRED e2e CI gate — every assertion must reflect shipped reality; unproven coverage uses test.fixme('<reason> — <unlock>')"
    - "docs reference the exact commands: make dev, mix test test/scoria_web/dev_lab_boundary_test.exs, mix scoria.ui.e2e, and route /scoria/_lab"
---

<objective>
Close the phase with its two proof/documentation surfaces: the deterministic Playwright spec (`priv/dev/e2e/lab.spec.mjs`) that proves the lab loads and exercises theme/motion/viewport/overlay in a real browser, and the maintainer docs (`docs/MAINTAINERS.md`) that explain how to run and inspect the lab (D-34, ROADMAP SC5).

Purpose: LAB-02's theme/reduced-motion/viewport claims are client-observable truths — the e2e lane is their proof home. And the phase is not "done" until a maintainer can find and use the lab from the docs.

Output: new `priv/dev/e2e/lab.spec.mjs`, modified `docs/MAINTAINERS.md`.
</objective>

<artifacts_this_phase_produces>
This plan introduces:
- **Test/spec** `priv/dev/e2e/lab.spec.mjs` — route load, theme toggle, reduced-motion emulation, six-width viewport scan, overlay/focus probe, dense table, toast-over-dense-UI, copy control.
- **Docs** a new `Component Lab` maintainer section in `docs/MAINTAINERS.md` (D-34).

Consumes: the mounted route `/scoria/_lab` and all sections (Plan 05 + Waves 1–2), the reused `priv/dev/e2e/lib/ready.mjs` `waitForReady`, and the Plan 01 boundary-test command.
</artifacts_this_phase_produces>

<notes>
## Reviewed, not folded (out-of-scope; do NOT create tasks for these)
- **CI-correctness bug to file separately (NOT Phase 37 scope):** `ci-verify.yml`'s `connector` job step labeled "(advisory)" (`mix scoria.test.support_copilot`) has **no `continue-on-error: true`** yet feeds `verify-summary`'s `needs:`, so it actually blocks the required `verify` workflow — an "advisory-as-comment ≠ advisory-as-topology" mistake. File as a standalone CI follow-up; Phase 37's own docs must not repeat it when describing the lab's proof surfaces.
- **VISUAL-CI-01 promotion criteria (D-30):** adopt screenshot diffing only when lab IA is stable + a Docker-pinned deterministic renderer exists + a measured <2% flake in an advisory trial + a named baseline-review owner + explicit phase scope (~Phase 41). Recorded here for whoever picks it up.
- **Guard-#7 honesty caveat (from Plan 01):** the inventory-ID string-presence guard is a coverage FLOOR, not proof every primitive renders every state — the manual walkthrough in `37-VALIDATION.md` is its complement and must not be deleted by a future phase.
</notes>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-dev-component-lab-and-stress-fixtures/37-CONTEXT.md
@.planning/phases/37-dev-component-lab-and-stress-fixtures/37-RESEARCH.md
@.planning/phases/37-dev-component-lab-and-stress-fixtures/37-PATTERNS.md
@.planning/phases/37-dev-component-lab-and-stress-fixtures/37-VALIDATION.md
@priv/dev/e2e/phase16_parity.spec.mjs
@priv/dev/e2e/lib/ready.mjs
@docs/MAINTAINERS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the deterministic lab browser proof spec</name>
  <files>priv/dev/e2e/lab.spec.mjs</files>
  <read_first>
    - priv/dev/e2e/phase16_parity.spec.mjs (import shape, test.use viewport, emulateMedia, setViewportSize patterns)
    - priv/dev/e2e/lib/ready.mjs (waitForReady — reuse as-is)
    - priv/dev/e2e/playwright.config.mjs (testDir/testMatch — confirms auto-pickup)
    - docs/uat_automation.md (the test.fixme('<reason> — <unlock>') convention)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-RESEARCH.md (Pitfall 2 — e2e spec is immediately a required CI gate)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-CONTEXT.md (D-13, D-14, D-33)
  </read_first>
  <action>
    Create `priv/dev/e2e/lab.spec.mjs`. Import `{ test, expect }` from `@playwright/test` and `waitForReady` from `./lib/ready.mjs`. Resolve the base URL from `process.env.PLAYWRIGHT_BASE_URL` (dashboard base, e.g. `http://localhost:4799/scoria`) and target the lab at `/scoria/_lab`.

    Cover the D-33 focused, deterministic probes — each assertion must be true of what actually ships this phase: (1) route load at `/scoria/_lab` then `waitForReady(page)`; (2) the seven D-07 nav sections are present; (3) theme toggle changes `data-theme` on the root element; (4) `page.emulateMedia({ reducedMotion: 'reduce' })` and assert the reduced-motion affordance/label is observable; (5) a viewport scan calling `page.setViewportSize` for each of 320, 375, 768, 1024, 1440, and a wide width, asserting the lab shell has no trapped/clipped essential content at each; (6) an overlay/focus probe (drawer or modal opens, receives focus, dismisses); (7) a dense table/list case and the toast-over-dense-UI region render; (8) a copy control is present and clickable.

    CRITICAL (Pitfall 2): this file is auto-discovered by the REQUIRED `e2e` CI gate (`testDir`-driven, no task/CI change). Every assertion must reflect shipped reality. For any probe intentionally deferred or not yet provable, use `test.fixme('<reason/ID> — <named unlock>')` per `docs/uat_automation.md` — never leave a bare failing or flaky assertion. Reuse `waitForReady`; do not add a new readiness attribute or hook.
  </action>
  <verify>
    <automated>cd priv/dev && node -e "import('./e2e/lab.spec.mjs').then(()=>console.log('parse-ok')).catch(e=>{console.error(e);process.exit(1)})"</automated>
    <human-check>Run `make dev` then `mix scoria.ui.e2e --base-url http://localhost:4799/scoria` and confirm lab.spec.mjs passes (or its deferred probes are test.fixme, not failing).</human-check>
  </verify>
  <acceptance_criteria>
    - `priv/dev/e2e/lab.spec.mjs` parses as an ES module and imports `waitForReady` from `./lib/ready.mjs`.
    - Contains route-load, theme-toggle, `emulateMedia({reducedMotion:'reduce'})`, and `setViewportSize` calls for all six widths, plus overlay/focus, dense-table, and copy-control probes.
    - Any deferred coverage uses `test.fixme('<reason> — <unlock>')`; there is no bare assertion known to fail.
    - Adds no new readiness attribute/hook and no new npm dependency.
    - Local `mix scoria.ui.e2e` run is green (human-check), consistent with the required CI `e2e` gate.
  </acceptance_criteria>
  <done>The lab has a deterministic browser proof that the required e2e CI gate runs green.</done>
</task>

<task type="auto">
  <name>Task 2: Document the Component Lab for maintainers</name>
  <files>docs/MAINTAINERS.md</files>
  <read_first>
    - docs/MAINTAINERS.md (existing dev-command / CI-lane sections — match voice and structure; add, do not restructure)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-CONTEXT.md (D-34, D-05, D-22..D-24)
    - .planning/ROADMAP.md (Phases 38-41 goals — to explain which lab probes support them)
  </read_first>
  <action>
    Add a new `Component Lab` section to `docs/MAINTAINERS.md` (D-34) covering: how to start the dev server (`make dev`, dev endpoint on port 4799); how to open the lab (`http://localhost:4799/scoria/_lab`) and its D-07 IA sections; how to inspect component states (the 10 canonical D-11 state names) and the fixture domains; how to update fixtures (edit `dev/lab/fixtures.ex` — deterministic, reset-free, never referenced from `lib/`); how to run focused proof (`mix test test/scoria_web/dev_lab_boundary_test.exs` for the boundary/coverage guard, and `mix scoria.ui.e2e` for browser proof); and which lab probes support Phases 38–41 (e.g. the toast-over-dense-UI stress fixture feeds Phase 38 DS-04/RISK-TOAST-LEGIBILITY, the decided-approval fixtures feed Phase 39 approval-history reasoning, the viewport frames feed Phase 40 RESP-01, the state bands feed Phase 41 PROOF-01).

    State clearly (D-05) that the lab is a maintainer-only dev surface — not linked from the public dashboard nav, operator command palette, or adopter docs, and not shipped to Hex. Keep it factual and command-accurate; do not invent commands that do not exist.
  </action>
  <verify>
    <automated>grep -q "/scoria/_lab" docs/MAINTAINERS.md && grep -q "dev/lab/fixtures.ex" docs/MAINTAINERS.md && grep -q "mix scoria.ui.e2e" docs/MAINTAINERS.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/MAINTAINERS.md` has a `Component Lab` section naming the route `/scoria/_lab`, `make dev`, `dev/lab/fixtures.ex`, the boundary-test command, and `mix scoria.ui.e2e`.
    - It explains inspecting states/domains, updating fixtures, running focused proof, and which probes support Phases 38–41.
    - It states the lab is maintainer-only, unlinked from public surfaces, and excluded from Hex (D-05).
    - Every command referenced exists in the repo.
  </acceptance_criteria>
  <done>Maintainers can find, run, inspect, and extend the lab from the docs, and understand its role for Phases 38-41.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new e2e spec → required CI `e2e` gate | A new `priv/dev/e2e/*.spec.mjs` is auto-run by the required gate; a flaky/failing assertion breaks CI for all PRs. |
| docs → public exposure guidance | Docs must reinforce (not undermine) the maintainer-only, unlinked, un-shipped boundary (D-05). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-06 | Denial of Service (CI) | `lab.spec.mjs` flaky/failing in required `e2e` gate | medium | mitigate | Every assertion reflects shipped reality; deferred coverage uses `test.fixme('<reason> — <unlock>')`; reuse existing `waitForReady` for determinism. |
| T-37-01 | Info Disclosure | docs implying the lab is an adopter surface | low | mitigate | Docs explicitly state maintainer-only, unlinked from public nav/palette, excluded from Hex (D-05). |
| T-37-SC | Tampering | npm/hex installs | low | accept | Phase adds zero new packages; existing `@playwright/test` 1.60.0 reused. |
</threat_model>

<verification>
- `priv/dev/e2e/lab.spec.mjs` parses as an ES module.
- `docs/MAINTAINERS.md` grep checks pass.
- Local `make dev` + `mix scoria.ui.e2e --base-url http://localhost:4799/scoria` green before push (the CI `e2e` gate will also run it).
- Full ExUnit still green: `SCORIA_DB_PORT=55432 mix test --warnings-as-errors`.
- D-30 boundary: `git diff --stat .github/workflows/ci.yml .github/workflows/ci-verify.yml` shows no new visual-diff/screenshot-regression job; record this explicitly in the phase VERIFICATION/SUMMARY note.
</verification>

<success_criteria>
- The lab has deterministic browser proof (route/theme/motion/viewport/overlay/dense/copy) that the required e2e CI gate runs green, with deferred probes marked test.fixme.
- Maintainer docs explain how to run and inspect the lab and how its probes support Phases 38-41, reinforcing the maintainer-only boundary.
</success_criteria>

<output>
Create `.planning/phases/37-dev-component-lab-and-stress-fixtures/37-06-SUMMARY.md` when done.
</output>
