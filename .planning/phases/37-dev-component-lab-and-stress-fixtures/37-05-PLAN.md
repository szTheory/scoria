---
phase: 37-dev-component-lab-and-stress-fixtures
plan: 05
type: execute
wave: 3
depends_on: ["37-01", "37-02", "37-03", "37-04"]
files_modified:
  - dev/dev_router.ex
  - dev/lab/lab_live.ex
autonomous: true
requirements: [LAB-01, LAB-02]
user_setup: []

must_haves:
  truths:
    - "The lab is reachable in local dev at /scoria/_lab, mounted only by ScoriaWeb.DevRouter, entirely outside the public scoria_dashboard/2 macro (satisfies ROADMAP SC1)"
    - "DevLab.LabLive renders the D-07 IA (Foundations/Primitives/Groups/States/Viewports/Overlays/Fixtures) with the exact D-27 page copy, dispatching to the section modules from a fixed compile-time allowlist"
  artifacts:
    - dev/dev_router.ex
    - dev/lab/lab_live.ex
  key_links:
    - "dev/dev_router.ex must import Phoenix.LiveView.Router at module scope (live/live_session are currently only macro-local) or live/3 is undefined at compile"
    - "The new scope stays textually separate from scope \"/\" scoria_dashboard(\"/scoria\") — lib/scoria_web/router.ex is never touched"
    - ":section/:item params are matched against a fixed allowlist; never String.to_atom/1 on unvalidated params (V5)"
---

<objective>
Mount the lab surface: add the `live_session :scoria_lab` scope to `dev/dev_router.ex` at `/scoria/_lab`, and build `DevLab.LabLive` — the single param-driven LiveView that renders the lab shell chrome (D-07 IA nav, exact D-27 copy) and dispatches to the seven section modules built in Waves 1–2.

Purpose: This is where the lab becomes reachable. It composes every prior plan and is the last piece before browser proof. It must never touch the public macro, and must validate route params against a fixed allowlist.

Output: modified `dev/dev_router.ex`, new `dev/lab/lab_live.ex`.
</objective>

<artifacts_this_phase_produces>
This plan introduces:
- **Route path** `/scoria/_lab` (and `/scoria/_lab/:section`, `/scoria/_lab/:section/:item`).
- **live_session** `:scoria_lab` (`root_layout: {ScoriaWeb.Layouts, :root}`) in `dev/dev_router.ex`.
- **Module** `DevLab.LabLive` (`dev/lab/lab_live.ex`) — `mount/3`, `handle_params/3`, `render/1`, section dispatch.
- **Module-scope import** `Phoenix.LiveView.Router` added to `dev/dev_router.ex`.

Consumes: every section module from Plans 02–04 and `DevLab.Sections.States` from Plan 01.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-dev-component-lab-and-stress-fixtures/37-CONTEXT.md
@.planning/phases/37-dev-component-lab-and-stress-fixtures/37-RESEARCH.md
@.planning/phases/37-dev-component-lab-and-stress-fixtures/37-PATTERNS.md
@.planning/phases/37-dev-component-lab-and-stress-fixtures/37-UI-SPEC.md
@dev/dev_router.ex
@lib/scoria_web/components/layouts/root.html.heex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount the dev-only lab route in dev/dev_router.ex</name>
  <files>dev/dev_router.ex</files>
  <read_first>
    - dev/dev_router.ex (full 55-line file — the existing :browser pipeline and scope "/" scoria_dashboard("/scoria"))
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-RESEARCH.md (Pattern 1, Pitfall 3)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-PATTERNS.md ("dev/dev_router.ex" section — required addition)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <action>
    Modify `dev/dev_router.ex` (`ScoriaWeb.DevRouter`) only. At module scope, add `import Phoenix.LiveView.Router, only: [live: 3, live: 4, live_session: 3]` — this is required because `live`/`live_session` are currently available only inside the `scoria_dashboard/2` macro's own quote block, so calling `live(...)` in a new scope without this import fails to compile with `undefined function live/3` (Pitfall 3).

    Add a NEW `scope "/scoria/_lab" do` block, textually separate from the existing `scope "/" do ... scoria_dashboard("/scoria") end`. In the new scope: `pipe_through(:browser)` (reuse the existing pipeline verbatim, including `put_demo_tenant`), then `live_session :scoria_lab, root_layout: {ScoriaWeb.Layouts, :root} do` with three routes to `DevLab.LabLive` action `:index`: `live("/", ...)`, `live("/:section", ...)`, `live("/:section/:item", ...)`.

    Do NOT modify `lib/scoria_web/router.ex` or the `scoria_dashboard/2` macro. Do NOT add a public macro option (no `scoria_dashboard lab: true`, D-03). Keep the two scopes visually distinct so the Plan 01 boundary guard's separation intent holds.
  </action>
  <verify>
    <automated>MIX_ENV=dev mix compile --warnings-as-errors && mix test --no-start test/scoria_web/dev_lab_boundary_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `dev/dev_router.ex` compiles clean under `MIX_ENV=dev` with `--warnings-as-errors`.
    - Module-scope `import Phoenix.LiveView.Router` is present.
    - A `scope "/scoria/_lab"` block with `live_session :scoria_lab` and three `live` routes to `DevLab.LabLive` exists, separate from the `scoria_dashboard("/scoria")` scope.
    - `lib/scoria_web/router.ex` is unchanged (git shows no diff there); the Plan 01 boundary test still passes.
  </acceptance_criteria>
  <done>The lab route is mounted only in the dev router, outside the public macro, with the LiveView.Router import in place.</done>
</task>

<task type="auto">
  <name>Task 2: Build the DevLab.LabLive shell and section dispatch</name>
  <files>dev/lab/lab_live.ex</files>
  <read_first>
    - lib/scoria_web/components/layouts/root.html.heex (the self-contained root layout to reuse verbatim via layout: {ScoriaWeb.Layouts, :root})
    - lib/scoria_web/ui.ex (page_section/1, panel/1, eyebrow/1, badge/1, kbd/1, id/1 — the only chrome primitives permitted, per UI-SPEC)
    - dev/lab/sections/*.ex (the seven section function components to dispatch to) — from Plans 01–04
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-RESEARCH.md (Pattern 2, Security Domain V5 input validation)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-UI-SPEC.md (Copywriting Contract, IA section labels, Component Inventory For Lab Chrome)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-CONTEXT.md (D-07, D-25, D-27, D-28)
  </read_first>
  <action>
    Create `DevLab.LabLive` at `dev/lab/lab_live.ex` (`use Phoenix.LiveView, layout: {ScoriaWeb.Layouts, :root}`; import the specific `ScoriaWeb.UI` chrome primitives used).

    `mount/3` returns `{:ok, socket}`. `handle_params/3` reads `params["section"]` and `params["item"]` and resolves the current section against a FIXED compile-time allowlist of the seven D-07 sections (`foundations`, `primitives`, `groups`, `states`, `viewports`, `overlays`, `fixtures`), defaulting to `foundations` for unknown/nil; assign `page_title: "Component Lab"`. Match params via a `case`/allowlist or `Map` lookup — NEVER `String.to_atom/1` on an unvalidated param (V5). Unknown `item` falls back to the section overview.

    `render/1` builds the lab shell using ONLY the UI-SPEC-permitted primitives: a section nav rail listing the seven exact D-07 labels (`Foundations`, `Primitives`, `Groups`, `States`, `Viewports`, `Overlays`, `Fixtures`) with the active item styled via the accent token; a `page_section` header with the exact D-27 copy — title `Component Lab`, subtitle `Inspect Scoria primitives, groups, fixtures, themes, and stress states before changing shared UI.`, a primary command labeled `Run lab proof` and a secondary command labeled `Open fixture matrix` (linking to the Fixtures section). Dispatch the body to the matching section component (`DevLab.Sections.Foundations.foundations/1`, `.Primitives.primitives/1`, `.Groups.groups/1`, `.States.states_section/1`, `.Viewports.viewports/1`, `.Overlays.overlays/1`, `.FixturesView.fixtures_view/1`), passing the current `item`.

    Field-engineer-bench voice (D-25): compact section headers, state/viewport/fixture-source labels, inventory-ID chips as evidence; keep Ecto/PubSub/macro internals out of primary orientation (D-28). Two-weight rule (400/600), `--scoria-*` tokens only, no new primitives.
  </action>
  <verify>
    <automated>MIX_ENV=dev mix compile --warnings-as-errors && mix test --no-start test/scoria_web/dev_lab_boundary_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `dev/lab/lab_live.ex` compiles clean under `MIX_ENV=dev` with `--warnings-as-errors`.
    - `handle_params/3` resolves `:section` via a fixed allowlist; the file contains no `String.to_atom(` on a param.
    - `render/1` contains the exact D-27 title, subtitle, primary (`Run lab proof`) and secondary (`Open fixture matrix`) command strings and the seven exact D-07 nav labels.
    - Dispatches to all seven section components; uses only UI-SPEC-permitted chrome primitives and `--scoria-*` tokens (no raw hex, no new primitive).
    - The Plan 01 boundary/coverage test still passes.
  </acceptance_criteria>
  <done>DevLab.LabLive renders the lab shell with the D-07 IA and exact D-27 copy, dispatching safely to all seven sections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public `scoria_dashboard/2` macro → adopter app | The new scope must live only in `dev/dev_router.ex`, never in the shipped macro. |
| route params (`:section`/`:item`) → section dispatch | Untrusted path input must select only from a fixed compile-time allowlist. |
| dev router / LiveView → runtime `lib/` | The LiveView is dev-only; `lib/` never references it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Elevation of Privilege / Info Disclosure | lab route reachable via public macro / prod endpoint | high | mitigate | Route added only to `dev/dev_router.ex` (`:dev`-only compile), outside `scoria_dashboard/2`; Plan 01 boundary test proves `lib/` router/nav never reference `/_lab`. |
| T-37-04 | Tampering / Info Disclosure | `:section`/`:item` param → arbitrary atom or reflected content | medium | mitigate | Match params against a fixed allowlist / existing-atom lookup; never `String.to_atom/1` on unvalidated input (V5). |
| T-37-03 | Tampering | `DevLab.LabLive` referenced from `lib/` | high | mitigate | LiveView lives under `dev/`; Plan 01 `lib/` no-`DevLab.` scan (this plan's per-task verify) proves no leak. |
| T-37-SC | Tampering | npm/hex installs | low | accept | Phase adds zero new packages. |
</threat_model>

<verification>
- `MIX_ENV=dev mix compile --warnings-as-errors` clean.
- `mix test --no-start test/scoria_web/dev_lab_boundary_test.exs` passes.
- Manual smoke: `make dev`, open `http://localhost:4799/scoria/_lab`, confirm the seven sections render (formal browser proof is Plan 06).
- `git diff --stat lib/scoria_web/router.ex` shows no change.
</verification>

<success_criteria>
- /scoria/_lab is reachable in local dev, mounted only by the dev router, outside the public macro.
- DevLab.LabLive renders the D-07 IA with exact D-27 copy and dispatches to all seven sections with allowlist-validated params.
</success_criteria>

<output>
Create `.planning/phases/37-dev-component-lab-and-stress-fixtures/37-05-SUMMARY.md` when done.
</output>
