---
phase: 37-dev-component-lab-and-stress-fixtures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dev/lab/fixtures.ex
  - dev/lab/sections/states.ex
  - test/scoria_web/dev_lab_boundary_test.exs
autonomous: true
requirements: [LAB-01, FIXT-01]
user_setup: []

must_haves:
  truths:
    - "mix test test/scoria_web/dev_lab_boundary_test.exs passes and proves the public router/nav/package boundary and D-21 no-lib-reference rule (satisfies ROADMAP SC1)"
    - "DevLab.Fixtures returns deterministic HEEx-safe data covering all 13 D-20 domain scenarios (approvals, incidents, reviews, datasets, workflow, connectors, prompts, evals, empty, error) with realistic AND ugly values (satisfies ROADMAP SC4)"
    - "state_tone/1 is the single explicit lab-state to visual-tone mapping and never calls ScoriaWeb.UI.tone/1 (D-12)"
  artifacts:
    - dev/lab/fixtures.ex
    - dev/lab/sections/states.ex
    - test/scoria_web/dev_lab_boundary_test.exs
  key_links:
    - "DevLab.Fixtures scenario/1 clause names must be byte-identical to the D-20 strings the coverage test scans for"
    - "state_tone/1 is the only place lab-state atoms map to tones; all section views route through it, never inlining tone atoms per call site"
    - "the boundary test is pure File.read!/1 + Regex — it must never alias/import a dev/ module (test/ elixirc_paths excludes dev/, which would break mix test compilation)"
---

<objective>
Establish the dev-only Component Lab data spine and its structural safety guard: the deterministic fixture catalog (`DevLab.Fixtures`), the single lab-state to visual-tone mapping plus the reusable state-band renderer (`DevLab.Sections.States`), and the source-scan boundary/coverage test that proves the lab stays out of the public macro, out of the Hex tarball, and out of runtime `lib/` code paths.

Purpose: Everything the later section views render is keyed off this fixture catalog and state vocabulary, and every later build task verifies against this one guard test — so it lands first (Wave 1) as the Nyquist anchor for the whole phase.

Output: `dev/lab/fixtures.ex`, `dev/lab/sections/states.ex`, `test/scoria_web/dev_lab_boundary_test.exs`.
</objective>

<artifacts_this_phase_produces>
This plan introduces:
- **Module** `DevLab.Fixtures` (`dev/lab/fixtures.ex`) — `states_for/1`, `scenario/1`.
- **Module** `DevLab.Sections.States` (`dev/lab/sections/states.ex`) — `state_tone/1`, `states_band/1`, `states_section/1`.
- **Function** `state_tone/1` — the single lab-state → visual-tone table (D-12).
- **Fixture scenario names** (D-20): `approval_requested`, `approval_denied`, `incident_opened`, `incident_escalated`, `review_candidate_flagged`, `dataset_promoted`, `dataset_empty`, `workflow_waiting_for_approval`, `workflow_failed_step`, `connector_degraded`, `connector_scope_blocked`, `prompt_release_blocked`, `eval_regression_detected`.
- **Canonical lab state names** (D-11): `normal`, `long_text`, `empty`, `dense`, `disabled`, `selected`, `loading`, `warning`, `danger`, `error`.
- **Test file** `test/scoria_web/dev_lab_boundary_test.exs` (`ScoriaWeb.DevLabBoundaryTest`).
- **Module namespace** `DevLab.*` (the prefix the D-21 no-lib-reference guard scans for).

Downstream (later plans in this phase): route path `/scoria/_lab`, `live_session :scoria_lab`, `DevLab.LabLive`, the six section view modules, `priv/dev/e2e/lab.spec.mjs`, and the `docs/MAINTAINERS.md` Component Lab section.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-dev-component-lab-and-stress-fixtures/37-CONTEXT.md
@.planning/phases/37-dev-component-lab-and-stress-fixtures/37-RESEARCH.md
@.planning/phases/37-dev-component-lab-and-stress-fixtures/37-PATTERNS.md
@.planning/phases/37-dev-component-lab-and-stress-fixtures/37-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the deterministic dev-only fixture catalog DevLab.Fixtures</name>
  <files>dev/lab/fixtures.ex</files>
  <read_first>
    - dev/dev_router.ex (module-doc boundary comment: dev/ compiles only under MIX_ENV=dev, excluded from Hex)
    - lib/scoria/support_journey.ex and lib/scoria/support_journey/handlers.ex (existing Scoria domain vocabulary — approval/incident/tool events — match/extend these names, do not diverge)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-PATTERNS.md ("dev/lab/fixtures.ex" section)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-CONTEXT.md (D-16 through D-21, D-15 ugly-data list)
  </read_first>
  <action>
    Create `DevLab.Fixtures` at `dev/lab/fixtures.ex`. Moduledoc must state: dev-only (`:dev` env, never shipped to Hex per mix.exs elixirc_paths/1), deterministic and reset-free (no DB read, no PubSub), and that it must NEVER be referenced from any module under `lib/` (D-21 — enforced by dev_lab_boundary_test.exs). Namespace the module under the `DevLab` prefix so the D-21 guard regex matches.

    Provide `states_for/1` taking a primitive atom (e.g. `:badge`, `:table`, `:toast`, `:panel`, `:field`, `:overview_stats`, `:empty_state`, `:skeleton`, `:id`, `:timestamp`) and returning a keyword list keyed by ALL ten canonical D-11 state atoms in this order: `normal`, `long_text`, `empty`, `dense`, `disabled`, `selected`, `loading`, `warning`, `danger`, `error`. Each value is a HEEx-safe map of that primitive's attrs/slot data for that state.

    Provide `scenario/1` with one clause per D-20 domain-noun scenario name, returning a HEEx-safe map: `approval_requested`, `approval_denied`, `incident_opened`, `incident_escalated`, `review_candidate_flagged`, `dataset_promoted`, `dataset_empty`, `workflow_waiting_for_approval`, `workflow_failed_step`, `connector_degraded`, `connector_scope_blocked`, `prompt_release_blocked`, `eval_regression_detected`. Collectively these must cover the FIXT-01/D-19 domains: approvals, incidents, reviews, datasets, workflow detail, connectors, prompts, evals, empty paths, and error paths.

    Encode BOTH realistic and ugly values per D-15: at least one scenario carries a long unbroken ID and a long policy/version name, at least one carries dense multi-row data, and the empty/error scenarios carry the D-27 empty/error copy contract inputs. Include evidence-oriented fields per D-08/D-29 where natural: trace/run/approval/actor/session/tenant IDs, policy/version names, a fixture-source label, and inventory refs (e.g. string literals `PRIM-TABLE`, `GROUP-APPROVAL-INBOX-COMPONENT`) so the Fixtures IA section and coverage checks can anchor on them.

    Do NOT call `ScoriaWeb.UI.tone/1` on any lab-state atom (that function maps domain status strings — a different vocabulary, D-12). Do NOT read from `priv/repo/dev_seed.exs` or any DB. Keep every value a plain map/struct/binary/atom/number (HEEx-safe).
  </action>
  <verify>
    <automated>MIX_ENV=dev mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `dev/lab/fixtures.ex` compiles clean under `MIX_ENV=dev` with `--warnings-as-errors`.
    - Module name begins with the `DevLab` prefix.
    - `states_for/1` returns a keyword list containing all ten D-11 state atoms.
    - `scenario/1` has a clause for each of the 13 D-20 scenario names, byte-identical.
    - No `ScoriaWeb.UI.tone(` call anywhere in the file; no DB / `Repo` / `dev_seed` reference.
    - At least one scenario contains a long unbroken ID string and a long policy/version name (ugly-data coverage, D-15).
  </acceptance_criteria>
  <done>DevLab.Fixtures exists, compiles under :dev, and exposes all 10 states and all 13 domain scenarios as deterministic HEEx-safe data.</done>
</task>

<task type="auto">
  <name>Task 2: Add the lab-state to tone mapping and the reusable state-band renderer</name>
  <files>dev/lab/sections/states.ex</files>
  <read_first>
    - lib/scoria_web/ui.ex (badge/1 lines ~60-74 and panel/1 lines ~155-192 — the canonical attr/slot contract shape to mirror)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-PATTERNS.md ("dev/lab/sections/states.ex" section, Pattern 3)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-RESEARCH.md (Pattern 3, Pitfall 4)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-UI-SPEC.md (Color/tone table, Component Inventory For Lab Chrome)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-CONTEXT.md (D-11, D-12)
  </read_first>
  <action>
    Create `DevLab.Sections.States` at `dev/lab/sections/states.ex` (`use Phoenix.Component`, import only the specific `ScoriaWeb.UI` primitives used, e.g. `badge: 1, panel: 1, eyebrow: 1`).

    Define `state_tone/1` as the SINGLE explicit lab-state → visual-tone table (D-12), with a clause for every D-11 state: `warning` maps to `:warn`; `danger` and `error` map to `:fail`; `selected` maps to `:brand`; `loading` maps to `:info` (or `:neutral`); and `normal`, `long_text`, `empty`, `dense`, `disabled` map to `:neutral`. This function must NEVER call `ScoriaWeb.UI.tone/1` (Pitfall 4). It is the only place a lab-state atom becomes a tone.

    Define `states_band/1` function component: `attr :inventory_id, :string, required: true`; `attr :states, :list, required: true` (a keyword list of `{state_atom, fixture}` pairs from `DevLab.Fixtures.states_for/1`); `slot :render, required: true` with a nested `attr :fixture, :any`. It renders one labeled specimen per state: a container carrying `data-inventory-id={@inventory_id}`, and per entry a wrapper carrying `data-lab-state={state}`, a `badge` labeled with the state name and toned via `state_tone(state)`, and the specimen from `render_slot(@render, fixture)`. Use the exact D-11 state string as the label (never a synonym).

    Define `states_section/1` function component — the "States" IA nav section — that renders one representative primitive (e.g. badge) across all ten state bands using `states_band/1`, as a maintainer overview of the canonical state vocabulary.

    All lab chrome here uses ONLY existing `ScoriaWeb.UI` primitives and `--scoria-*` semantic tokens, the two-weight rule (400/600), and introduces no new colors/spacing/type/motion tokens (UI-SPEC binding).
  </action>
  <verify>
    <automated>MIX_ENV=dev mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `dev/lab/sections/states.ex` compiles clean under `MIX_ENV=dev` with `--warnings-as-errors`.
    - `state_tone/1` has clauses covering all 10 D-11 states and maps `warning`→`:warn`, `danger`→`:fail`, `error`→`:fail`, `selected`→`:brand`.
    - The file contains no `ScoriaWeb.UI.tone(` call.
    - `states_band/1` declares `attr :inventory_id`, `attr :states`, and `slot :render`, and its markup emits `data-lab-state` and `data-inventory-id` attributes.
    - No raw hex color, no new spacing/type/motion token literal in the file (semantic `--scoria-*` tokens only).
  </acceptance_criteria>
  <done>state_tone/1 and states_band/1 exist, compile under :dev, and give every downstream section a single tone mapping and one reusable per-state renderer.</done>
</task>

<task type="auto">
  <name>Task 3: Author the source-scan boundary and coverage guard test</name>
  <files>test/scoria_web/dev_lab_boundary_test.exs</files>
  <read_first>
    - test/scoria_web/ds06_drift_guard_test.exs (the proven File.read!/1 + Regex + violations-list + `assert violations == []` pattern)
    - mix.exs (locate the `package/0` `files:` list and `elixirc_paths/1` — note that `elixirc_paths(:dev)` itself contains the string "dev")
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-PATTERNS.md ("dev_lab_boundary_test.exs" section — concrete assertion sketch)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-RESEARCH.md (Pitfall 1, Pitfall 5, Code Examples, Security Domain)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-CONTEXT.md (D-21, D-31, D-32)
  </read_first>
  <action>
    Create `ScoriaWeb.DevLabBoundaryTest` at `test/scoria_web/dev_lab_boundary_test.exs` (`use ExUnit.Case, async: true`). This test is PURE `File.read!/1` + `Regex` reading source files as strings — it must NEVER `alias`/`import`/dot-call any module defined under `dev/`, because `test/` `elixirc_paths` does not include `dev/` and any such reference is a `mix test` compile failure (Pitfall 1). Mirror the structure of `ds06_drift_guard_test.exs` (wildcard + read + accumulate violations + single assert per test).

    Assertions:
    (a) Public-macro exclusion (T-37-01, D-01/D-03): read `lib/scoria_web/router.ex` as a string and assert it does not reference the private lab path segment. Use the literal `"/_lab"` (the route segment) as the search token so the assertion is unambiguous.
    (b) Nav / command-palette exclusion (D-05): assert `lib/scoria_web/dashboard_nav.ex` and `lib/scoria_web/components/layouts.ex` likewise contain no `"/_lab"` reference.
    (c) Package boundary (T-37-02, D-02): read `mix.exs`, capture ONLY the `package/0` `files:` list region (regex-capture the `files:` array between its brackets) and assert that captured region contains none of the tokens `"dev"`, `"priv/dev"`, `"priv/shots"`. Do NOT grep the whole `mix.exs` for `"dev"` — `elixirc_paths(:dev)` legitimately contains that string and would cause a false failure. Add a comment in the test explaining the region-scope rationale.
    (d) No runtime reference (T-37-03, D-21): for every path in `Path.wildcard("lib/**/*.{ex,heex}")`, assert the file body contains no `DevLab.` reference; collect offenders into a violations list and assert it is empty.
    (e) State coverage (D-32): join all `Path.wildcard("dev/lab/**/*.ex")` sources and assert each of the ten D-11 state names is present; report any missing name.
    (f) Domain coverage (FIXT-01, D-32): from the same joined `dev/lab` source assert each of the thirteen D-20 scenario names is present; report any missing name.

    Keep each assertion in its own `test` block with a descriptive message naming the decision ID it enforces.
  </action>
  <verify>
    <automated>mix test --no-start test/scoria_web/dev_lab_boundary_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test --no-start test/scoria_web/dev_lab_boundary_test.exs` passes with the Wave 1 source in place.
    - The test file contains no `alias`/`import` of any `dev/`-scoped module and no dot-call into `DevLab.*`.
    - The mix.exs assertion is region-scoped to the `files:` list (the test does not fail if the whole-file contains `"dev"` from `elixirc_paths`).
    - Distinct assertions exist for: public-router exclusion, nav/palette exclusion, package.files exclusion, `lib/` no-`DevLab.` guard, all 10 state names present, all 13 scenario names present.
  </acceptance_criteria>
  <done>The boundary/coverage guard passes and becomes the automated verify every later build task in this phase runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-only compile scope → Hex package | `dev/` and `priv/dev` must never cross into the shipped tarball (`mix.exs` `package.files` allowlist). |
| dev fixture module → runtime `lib/` | Fixture data is maintainer evidence, never a runtime dependency or hidden business rule (D-21). |
| public `scoria_dashboard/2` macro → adopter app | The lab must be unreachable through the macro any host app mounts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Elevation of Privilege / Info Disclosure | public router / `scoria_dashboard/2` macro | high | mitigate | Boundary test asserts `lib/scoria_web/router.ex`, `dashboard_nav.ex`, `layouts.ex` never reference the `/_lab` route segment; the lab scope lives only in `dev/dev_router.ex` (Plan 05). |
| T-37-02 | Info Disclosure | `mix.exs` `package.files` | high | mitigate | Region-scoped assertion proves the `files:` list excludes `dev`, `priv/dev`, `priv/shots`. |
| T-37-03 | Tampering | `DevLab.Fixtures` invoked from `lib/` | high | mitigate | Zero-reference scan asserts no `lib/**/*.{ex,heex}` file mentions `DevLab.` (D-21). |
| T-37-SC | Tampering | npm/hex installs | low | accept | Phase adds zero new packages; existing `@playwright/test` reused (RESEARCH Package Legitimacy Audit — not applicable). No supply-chain checkpoint required. |
</threat_model>

<verification>
- `MIX_ENV=dev mix compile --warnings-as-errors` is clean (catches Pitfall-3-class errors early).
- `mix test --no-start test/scoria_web/dev_lab_boundary_test.exs` passes.
- Full ExUnit still green: `SCORIA_DB_PORT=55432 mix test --warnings-as-errors`.
</verification>

<success_criteria>
- DevLab.Fixtures covers all 13 D-20 scenarios and all 10 D-11 states with deterministic, HEEx-safe, ugly-and-realistic data.
- state_tone/1 is the single lab-state→tone mapping and never calls ScoriaWeb.UI.tone/1.
- The boundary/coverage guard proves public-macro exclusion, Hex-package exclusion, the D-21 no-lib-reference rule, and full state/domain coverage — and passes.
</success_criteria>

<output>
Create `.planning/phases/37-dev-component-lab-and-stress-fixtures/37-01-SUMMARY.md` when done.
</output>
