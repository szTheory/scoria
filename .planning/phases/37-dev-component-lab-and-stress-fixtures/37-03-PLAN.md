---
phase: 37-dev-component-lab-and-stress-fixtures
plan: 03
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - dev/lab/sections/groups.ex
  - dev/lab/sections/fixtures_view.ex
autonomous: true
requirements: [LAB-02, FIXT-01]
user_setup: []

must_haves:
  truths:
    - "The Groups section renders recurring lib/scoria_web/components/*.ex component groups across stress states using domain-noun fixtures (satisfies ROADMAP SC2)"
    - "The Fixtures section lets a maintainer browse the fixture catalog itself and inspect each scenario's raw payload as evidence (satisfies ROADMAP SC4, D-27 Open fixture matrix)"
  artifacts:
    - dev/lab/sections/groups.ex
    - dev/lab/sections/fixtures_view.ex
  key_links:
    - "Groups renders the real dashboard component modules under lib/scoria_web/components/*.ex fed by DevLab.Fixtures.scenario/1"
    - "Each group band is anchored with its Phase-36 GROUP-* inventory ID (D-08)"
    - "The Fixtures section uses the exact D-27 empty/error copy and the Copy fixture payload / Open fixture matrix labels"
---

<objective>
Build the `Groups` section (recurring dashboard component groups under stress) and the `Fixtures` section (a browser/inspector for the fixture catalog itself). Together these deliver the group-level slice of LAB-02 and the catalog-discoverability slice of FIXT-01.

Purpose: A maintainer needs to see whole component groups (approval inbox, workflow tree/detail, connector drawer, incident evidence, evidence notebooks) under ugly data, and a contributor needs to find the canonical fixture example without reverse-engineering older pages (D-23).

Output: `dev/lab/sections/groups.ex`, `dev/lab/sections/fixtures_view.ex`.
</objective>

<artifacts_this_phase_produces>
This plan introduces:
- **Module** `DevLab.Sections.Groups` (`dev/lab/sections/groups.ex`) — renders recurring `lib/scoria_web/components/*.ex` groups × stress state, anchored by `GROUP-*` inventory IDs.
- **Module** `DevLab.Sections.FixturesView` (`dev/lab/sections/fixtures_view.ex`) — the `Fixtures` IA section: browse each D-20 scenario, inspect its raw payload as evidence, with the `Open fixture matrix` / `Copy fixture payload` labels and the D-27 empty/error copy.

Consumes (from Plan 01): `DevLab.Fixtures.scenario/1`, `DevLab.Fixtures.states_for/1`, `DevLab.Sections.States.states_band/1`.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-dev-component-lab-and-stress-fixtures/37-CONTEXT.md
@.planning/phases/37-dev-component-lab-and-stress-fixtures/37-PATTERNS.md
@.planning/phases/37-dev-component-lab-and-stress-fixtures/37-UI-SPEC.md
@dev/lab/sections/states.ex
@dev/lab/fixtures.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Groups section for recurring component groups under stress</name>
  <files>dev/lab/sections/groups.ex</files>
  <read_first>
    - lib/scoria_web/components/approval_inbox_component.ex, workflow_tree_component.ex, workflow_detail_panel_component.ex, connector_detail_drawer_component.ex, incident_evidence_component.ex, and the evidence notebook components (enumerate via Glob "lib/scoria_web/components/*.ex")
    - dev/lab/sections/states.ex (states_band/1) and dev/lab/fixtures.ex (scenario/1, states_for/1) — from Plan 01
    - .planning/phases/36-baseline-and-inventory/36-inventory.json (GROUP-* inventory IDs)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-CONTEXT.md (D-09 group list, D-10 curated-scope boundary, D-11, D-15)
  </read_first>
  <action>
    Create `DevLab.Sections.Groups` at `dev/lab/sections/groups.ex` (`use Phoenix.Component`, import the specific component-group modules under `ScoriaWeb.Components.*` plus `DevLab.Sections.States` `states_band/1`). Export a `groups/1` function component (accepting an optional `item` assign for `/scoria/_lab/groups/:item` deep-links).

    Render the recurring dashboard component groups named in D-09 — approval inbox, workflow tree, workflow detail, connector drawer, incident evidence, and the evidence notebook groups — each across the relevant stress states via `states_band/1`, fed by the matching `DevLab.Fixtures.scenario/1` domain-noun payloads (e.g. `approval_requested`/`approval_denied` for the approval inbox, `incident_opened`/`incident_escalated` for incident evidence, `connector_degraded`/`connector_scope_blocked` for the connector drawer, `workflow_waiting_for_approval`/`workflow_failed_step` for workflow detail). Anchor each band with the group's Phase-36 `GROUP-*` inventory ID.

    Respect D-10: catalog isolated groups under stress; do NOT recreate a full dashboard page clone here (dense-approvals-with-toast, drawer/modal focus, command palette, mobile nav belong to the Overlays section, Plan 04). Consume only existing `ScoriaWeb.UI` / component-group modules and `--scoria-*` tokens; introduce no new primitives; resolve any tone via `state_tone/1`, never a raw lab-state atom.
  </action>
  <verify>
    <automated>MIX_ENV=dev mix compile --warnings-as-errors && mix test --no-start test/scoria_web/dev_lab_boundary_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `dev/lab/sections/groups.ex` compiles clean under `MIX_ENV=dev` with `--warnings-as-errors`.
    - Exports a `groups/1` function component that renders at least the approval inbox, workflow tree/detail, connector drawer, and incident evidence groups.
    - Each band carries a `GROUP-*` inventory id and is fed by a `DevLab.Fixtures.scenario/1` payload.
    - No lab-state atom passed as a `tone` attr; no raw hex / new token.
    - The Plan 01 boundary/coverage test still passes.
  </acceptance_criteria>
  <done>Recurring component groups render under domain-noun stress fixtures, anchored to GROUP-* inventory IDs.</done>
</task>

<task type="auto">
  <name>Task 2: Build the Fixtures catalog browser section</name>
  <files>dev/lab/sections/fixtures_view.ex</files>
  <read_first>
    - lib/scoria_web/ui.ex (raw_evidence/1, notebook/1, evidence_section/1, evidence_rows/1, id/1, empty_state/1, panel/1 — the evidence-disclosure primitives to reuse)
    - dev/lab/fixtures.ex (scenario/1 names and shapes) — from Plan 01
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-UI-SPEC.md (Copywriting Contract — exact empty/error/label strings; Component Inventory For Lab Chrome)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-CONTEXT.md (D-18, D-19, D-27, D-29)
  </read_first>
  <action>
    Create `DevLab.Sections.FixturesView` at `dev/lab/sections/fixtures_view.ex` (`use Phoenix.Component`, import the specific `ScoriaWeb.UI` evidence primitives used). Export a `fixtures_view/1` function component for the `Fixtures` IA section (reachable via the `Open fixture matrix` secondary command).

    List every D-20 scenario grouped by domain (approvals, incidents, reviews, datasets, workflow detail, connectors, prompts, evals, empty, error). For each scenario, show its domain-noun name, its fixture-source label and inventory/risk refs as `id/1` chips (D-29), and a progressive-disclosure raw payload view via `raw_evidence/1` / `notebook/1` with a `Copy fixture payload` control (exact D-27 label). When a state has no fixture rows, render the exact empty-state body: `No fixture rows for this state. Add deterministic dev fixture data before using this state for proof.` For a fixture that fails to build, render the exact error copy: `Lab fixture failed to render. Check the fixture builder and component attrs before changing runtime UI.`

    Use only existing `ScoriaWeb.UI` primitives and `--scoria-*` tokens; keep backend guts (raw maps, schema names) inside evidence disclosures, not primary orientation (D-28). The catalog is read-only — no mutation, no DB.
  </action>
  <verify>
    <automated>MIX_ENV=dev mix compile --warnings-as-errors && mix test --no-start test/scoria_web/dev_lab_boundary_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `dev/lab/sections/fixtures_view.ex` compiles clean under `MIX_ENV=dev` with `--warnings-as-errors`.
    - Exports a `fixtures_view/1` function component listing all D-20 scenarios grouped by domain.
    - Contains the exact D-27 empty-state body and error strings verbatim, plus the `Copy fixture payload` and `Open fixture matrix` labels.
    - Raw payloads are shown via existing evidence primitives (`raw_evidence`/`notebook`), not a new component.
    - The Plan 01 boundary/coverage test still passes.
  </acceptance_criteria>
  <done>A maintainer can browse the fixture catalog by domain and inspect each scenario's raw payload as evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-only section modules → runtime `lib/` | Group/fixture-view modules live under `dev/`; they render existing `lib/` components but are never referenced back from `lib/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-03 | Tampering | `DevLab.Sections.*` referenced from `lib/` | high | mitigate | Modules live under `dev/`; Plan 01 `lib/` no-`DevLab.` scan (this plan's per-task verify) proves no runtime leak. |
| T-37-05 | Info Disclosure | new raw hex / off-token value in lab chrome | low | mitigate | UI-SPEC binding; acceptance criteria forbid raw hex / new tokens; evidence-only disclosure of backend guts (D-28). |
| T-37-SC | Tampering | npm/hex installs | low | accept | Phase adds zero new packages. |
</threat_model>

<verification>
- `MIX_ENV=dev mix compile --warnings-as-errors` clean.
- `mix test --no-start test/scoria_web/dev_lab_boundary_test.exs` still passes.
- Behavioral proof arrives in Plan 06 (`lab.spec.mjs`) after the route mounts (Plan 05).
</verification>

<success_criteria>
- Groups renders the recurring dashboard component groups under domain-noun stress fixtures, anchored to GROUP-* IDs.
- Fixtures section browses the catalog by domain with raw-payload evidence and the exact D-27 copy.
</success_criteria>

<output>
Create `.planning/phases/37-dev-component-lab-and-stress-fixtures/37-03-SUMMARY.md` when done.
</output>
