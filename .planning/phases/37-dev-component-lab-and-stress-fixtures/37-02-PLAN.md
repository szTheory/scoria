---
phase: 37-dev-component-lab-and-stress-fixtures
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - dev/lab/sections/foundations.ex
  - dev/lab/sections/primitives.ex
autonomous: true
requirements: [LAB-02]
user_setup: []

must_haves:
  truths:
    - "The Primitives section renders every canonical ScoriaWeb.UI primitive across all 10 D-11 state bands using DevLab.Fixtures + states_band/1 (satisfies ROADMAP SC2)"
    - "The Foundations section makes existing tokens, type scale, spacing scale, and motion durations inspectable as read-only specimens without inventing new values (satisfies ROADMAP SC3 groundwork)"
    - "Every Primitives lab entry is a component-owned page keyed to a stable 36-inventory.json PRIM-* ID (D-06 spine + D-08 anchor) — verified by the Plan 01 inventory-ID cross-reference guard (#7)"
    - "Lab-authored dev/ chrome in these sections emits zero raw hex and zero raw palette classes; all color/type/spacing/motion resolve through --scoria-* tokens and ScoriaWeb.UI primitives (D-26) — verified by the extended DS-06 drift guard"
  prohibitions:
    - "These sections use no phoenix-bird/flame imagery, no hype word-bank (revolutionary/magic/10x/seamless/powerful), and no marketing-gallery treatment (decorative illustration, shadows/gradients outside --scoria-shadow-panel/-raised) (D-26) — language half grep-checkable; gallery-vibe half is a manual gsd-ui-review line"
  artifacts:
    - dev/lab/sections/foundations.ex
    - dev/lab/sections/primitives.ex
  key_links:
    - "Primitives calls DevLab.Sections.States.states_band/1 (Plan 01) and DevLab.Fixtures.states_for/2 (Plan 01) — those signatures are fixed"
    - "Each primitive band is anchored with its Phase-36 PRIM-* inventory ID for coverage traceability (D-08)"
---

<objective>
Build the two component-specimen sections that carry the bulk of LAB-02 state coverage: `Foundations` (read-only inspection of the existing token/type/spacing/motion system) and `Primitives` (every canonical `ScoriaWeb.UI` primitive rendered across all ten D-11 states).

Purpose: These are the sections a maintainer opens first to see every primitive under stress before Phase 38 changes shared controls. They compose Plan 01's fixture catalog and state-band renderer; they invent nothing.

Output: `dev/lab/sections/foundations.ex`, `dev/lab/sections/primitives.ex`.
</objective>

<artifacts_this_phase_produces>
This plan introduces:
- **Module** `DevLab.Sections.Foundations` (`dev/lab/sections/foundations.ex`) — renders the existing `--scoria-*` token set, type scale, spacing scale (`space-1`–`space-6` in-lab), and motion durations as read-only specimens; includes the `Reduced motion` label affordance (D-27).
- **Module** `DevLab.Sections.Primitives` (`dev/lab/sections/primitives.ex`) — renders each canonical `ScoriaWeb.UI` primitive × 10 state bands, each anchored by its `PRIM-*` inventory ID.

Consumes (from Plan 01): `DevLab.Fixtures.states_for/2`, `DevLab.Sections.States.states_band/1`, `state_tone/1`.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-dev-component-lab-and-stress-fixtures/37-CONTEXT.md
@.planning/phases/37-dev-component-lab-and-stress-fixtures/37-PATTERNS.md
@.planning/phases/37-dev-component-lab-and-stress-fixtures/37-UI-SPEC.md
@dev/lab/sections/states.ex
@dev/lab/fixtures.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Foundations inspection section</name>
  <files>dev/lab/sections/foundations.ex</files>
  <read_first>
    - lib/scoria_web/ui.ex (page_section/1, panel/1, eyebrow/1, kbd/1, id/1 — the chrome primitives to reuse)
    - assets/css/02-tokens.css (the `--scoria-*` token SSOT the section inspects — do not fork values)
    - assets/css/05-motion.css (motion durations `--scoria-dur-fast/mid/slow` = 100/150/200ms, reduced-motion mechanism)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-UI-SPEC.md (Spacing Scale, Typography, Color, Motion & Theme Coverage)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-CONTEXT.md (D-07, D-14, D-27, D-28/D-29)
  </read_first>
  <action>
    Create `DevLab.Sections.Foundations` at `dev/lab/sections/foundations.ex` (`use Phoenix.Component`, import only the specific `ScoriaWeb.UI` chrome primitives used). Export a `foundations/1` function component rendered by `DevLab.LabLive` for the `Foundations` IA section.

    Render, as read-only specimens (no editing, no new tokens): the semantic color token swatches referencing `--scoria-surface-app` / `--scoria-surface-panel` / `--scoria-action` / `--scoria-text-muted` / `--scoria-border` / `--scoria-focus-ring` (by CSS custom-property reference, never a raw hex); the type scale rows (badge/eyebrow 11px, label 12px, body 14px, panel heading 18px, title 24px, display 30px) with their weights; the spacing scale steps used in-lab (`space-1`–`space-6`); and the motion durations with a visible `Reduced motion` label/indicator that reflects `prefers-reduced-motion` (D-14 — display current state only, do not invent a second motion mechanism or new durations).

    Surface evidence-oriented labels (fixture-source, token name, `--scoria-*` custom-property name) as `id/1`/`kbd/1` chips per D-29; keep Ecto/PubSub/macro internals out of primary orientation (D-28). Use the two-weight rule (400/600) and semantic tokens only. Do not introduce any raw hex, new spacing step, new type size, or new duration.
  </action>
  <verify>
    <automated>MIX_ENV=dev mix compile --warnings-as-errors && mix test --no-start test/scoria_web/dev_lab_boundary_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `dev/lab/sections/foundations.ex` compiles clean under `MIX_ENV=dev` with `--warnings-as-errors`.
    - Exports a `foundations/1` function component.
    - Contains no raw hex color literal and no new spacing/type/motion numeric token; all color references are `--scoria-*` custom properties.
    - Renders a `Reduced motion` label (exact D-27 string) tied to the existing reduced-motion signal.
    - The Plan 01 boundary/coverage test still passes.
  </acceptance_criteria>
  <done>The Foundations section inspects the existing token/type/spacing/motion system read-only, inventing nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Build the Primitives specimen section across all 10 states</name>
  <files>dev/lab/sections/primitives.ex</files>
  <read_first>
    - lib/scoria_web/ui.ex (the full primitive surface: button, icon button, badge, panel, page_section, overview_stats/metric, signal_strip, table, drawer, modal, toast, field/form, notebook, raw_evidence, empty_state, skeleton, id, timestamp)
    - dev/lab/sections/states.ex (states_band/1 signature + state_tone/1 — from Plan 01)
    - dev/lab/fixtures.ex (states_for/2 — from Plan 01)
    - .planning/phases/36-baseline-and-inventory/36-inventory.json (the PRIM-* inventory IDs to anchor each band)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-CONTEXT.md (D-09 primitive list, D-11 states, D-15 stress list)
  </read_first>
  <action>
    Create `DevLab.Sections.Primitives` at `dev/lab/sections/primitives.ex` (`use Phoenix.Component`, `import ScoriaWeb.UI`, and `import DevLab.Sections.States, only: [states_band: 1]`). Export a `primitives/1` function component (accepting an optional `item` assign so `DevLab.LabLive` can deep-link a single primitive via `/scoria/_lab/primitives/:item`).

    For each canonical D-09 primitive — at minimum buttons, icon buttons, badges, panels, page sections, overview stats, signal summaries, tables/lists, drawers, modals, toasts, fields/forms, notebooks, raw evidence/code, empty states, skeletons, IDs, timestamps — render a `states_band/1` whose `inventory_id` is that primitive's Phase-36 `PRIM-*` ID and whose `states` come from `DevLab.Fixtures.states_for/2`. The band's `:render` slot renders the actual `ScoriaWeb.UI` primitive with the per-state fixture, so every primitive appears once per D-11 state (normal, long_text, empty, dense, disabled, selected, loading, warning, danger, error). This surfaces the D-15 stress cases (long unbroken IDs, long policy names, dense rows, empty datasets, loading placeholders, disabled/selected states, warning/danger/error).

    Never pass a lab-state atom directly as a `tone` attr — the band routes tone through `state_tone/1` (Plan 01). For any `Technical evidence` disclosure (inventory/trace/actor refs, raw attrs), reuse the existing evidence-notebook group — `notebook/1`/`evidence_section/1`/`raw_evidence/1` with the default `open: false`, `id/1` for copyable refs, `eyebrow/1` for category labels — do NOT build a new disclosure widget (the `open:false` default is the structural enforcement of the D-28-hide / D-29-expose split). Do not invent new primitives or new chrome; consume only existing `ScoriaWeb.UI` components and `--scoria-*` tokens. Per D-26: no phoenix-bird/flame imagery, no hype word-bank, no marketing-gallery treatment.
  </action>
  <verify>
    <automated>MIX_ENV=dev mix compile --warnings-as-errors && mix test --no-start test/scoria_web/dev_lab_boundary_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `dev/lab/sections/primitives.ex` compiles clean under `MIX_ENV=dev` with `--warnings-as-errors`.
    - Exports a `primitives/1` function component that consumes `states_band/1` and `DevLab.Fixtures.states_for/2`.
    - Every rendered band carries a `PRIM-*` inventory id via the band's `inventory_id`.
    - No HEEx literal passes a D-11 state atom (`:warning`/`:danger`/`:error`/`:selected`/…) as a `tone` attr; tone is resolved via `state_tone/1`.
    - The Plan 01 boundary/coverage test still passes.
  </acceptance_criteria>
  <done>Every canonical ScoriaWeb.UI primitive is inspectable across all 10 D-11 states, anchored to its inventory ID.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-only section modules → runtime `lib/` | Section views live under `dev/`; they must never be referenced from `lib/` and must not import dev fixtures into runtime paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-03 | Tampering | `DevLab.Sections.*` referenced from `lib/` | high | mitigate | These modules live under `dev/`; the Plan 01 `lib/` no-`DevLab.` scan (run as this plan's per-task verify) proves no leak into runtime. |
| T-37-05 | Info Disclosure | new raw hex / off-token value in lab chrome | low | mitigate | UI-SPEC binding: chrome uses `--scoria-*` tokens only; acceptance criteria forbid raw hex and new numeric tokens. |
| T-37-SC | Tampering | npm/hex installs | low | accept | Phase adds zero new packages. |
</threat_model>

<verification>
- `MIX_ENV=dev mix compile --warnings-as-errors` clean.
- `mix test --no-start test/scoria_web/dev_lab_boundary_test.exs` still passes.
- Behavioral render proof arrives in Plan 06 (`lab.spec.mjs`) once the route is mounted (Plan 05).
</verification>

<success_criteria>
- Foundations renders the existing token/type/spacing/motion system read-only with no invented values.
- Primitives renders every canonical primitive across all 10 states, each anchored to a PRIM-* inventory ID, tone resolved only via state_tone/1.
</success_criteria>

<output>
Create `.planning/phases/37-dev-component-lab-and-stress-fixtures/37-02-SUMMARY.md` when done.
</output>
