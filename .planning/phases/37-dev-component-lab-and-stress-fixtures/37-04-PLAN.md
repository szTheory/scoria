---
phase: 37-dev-component-lab-and-stress-fixtures
plan: 04
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - dev/lab/sections/viewports.ex
  - dev/lab/sections/overlays.ex
autonomous: true
requirements: [LAB-02]
user_setup: []

must_haves:
  truths:
    - "The Viewports section frames specimens at 320/375/768/1024/1440/wide with maintainer proof-target labels (satisfies ROADMAP SC3)"
    - "The Overlays section provides curated flow probes — dense approvals+toast, drawer/modal focus & dismissal, command palette, mobile nav, raw-evidence copy, long evidence — reusing existing JS hooks (satisfies ROADMAP SC2/SC3, D-10)"
  artifacts:
    - dev/lab/sections/viewports.ex
    - dev/lab/sections/overlays.ex
  key_links:
    - "Viewport labels use the exact D-13 maintainer strings, never device marketing names"
    - "Overlay probes reuse the existing assets/js/scoria.js hooks (ThemeToggle, Dismissable, CommandPalette, MobileNav, CopyId) — no reimplemented motion or dismiss mechanism"
---

<objective>
Build the two cross-cutting probe sections: `Viewports` (the six proof-target widths made inspectable) and `Overlays` (the curated D-10 flow probes where isolated components are insufficient). These deliver the viewport and overlay/focus slices of LAB-02.

Purpose: Some risks — responsive scan, overlay/focus stacking, dense-approvals-with-toast legibility — only appear at specific widths or with real overlays. The lab must make those inspectable without recreating whole dashboard pages (D-10).

Output: `dev/lab/sections/viewports.ex`, `dev/lab/sections/overlays.ex`.
</objective>

<artifacts_this_phase_produces>
This plan introduces:
- **Module** `DevLab.Sections.Viewports` (`dev/lab/sections/viewports.ex`) — viewport-simulator frames at 320/375/768/1024/1440/wide with the exact D-13 maintainer labels.
- **Module** `DevLab.Sections.Overlays` (`dev/lab/sections/overlays.ex`) — curated probes: dense approvals with toast overlay, drawer/modal focus & dismissal, command palette, mobile nav, raw-evidence copy controls, long evidence payload — all reusing existing `assets/js/scoria.js` hooks.

Consumes (from Plan 01): `DevLab.Fixtures.scenario/1`, `DevLab.Sections.States.states_band/1`.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-dev-component-lab-and-stress-fixtures/37-CONTEXT.md
@.planning/phases/37-dev-component-lab-and-stress-fixtures/37-PATTERNS.md
@.planning/phases/37-dev-component-lab-and-stress-fixtures/37-UI-SPEC.md
@dev/lab/sections/states.ex
@dev/lab/fixtures.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Viewports simulator section</name>
  <files>dev/lab/sections/viewports.ex</files>
  <read_first>
    - lib/scoria_web/ui.ex (panel/1, page_section/1, kbd/1 — chrome for viewport frames and width indicators)
    - assets/css/04-components.css (existing responsive behavior the frames must not fork)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-UI-SPEC.md (Viewport labels — exact strings; Motion & Theme Coverage)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-CONTEXT.md (D-13, D-25)
  </read_first>
  <action>
    Create `DevLab.Sections.Viewports` at `dev/lab/sections/viewports.ex` (`use Phoenix.Component`, import the specific `ScoriaWeb.UI` chrome primitives used). Export a `viewports/1` function component for the `Viewports` IA section.

    Render a viewport-simulator frame for each of the six proof-target widths — 320, 375, 768, 1024, 1440, and wide desktop — each frame constraining a representative specimen (e.g. a dense table/list from `DevLab.Fixtures`) to that width via an inline width on a panel-based frame (no new breakpoint token, D-13). Label each frame with the exact D-13 maintainer strings: `320px — small mobile`, `375px — mobile`, `768px — tablet`, `1024px — small desktop`, `1440px — desktop`, `Wide desktop`. Never use device marketing names (e.g. "iPhone SE"). Use `kbd/1` for the width indicator chips.

    The frames are proof aids: they compose only existing `ScoriaWeb.UI` primitives + `--scoria-*` tokens + inline width style; they add no new spacing/type/motion token. This is the manual/visual complement to the Playwright `setViewportSize` proof in Plan 06.
  </action>
  <verify>
    <automated>MIX_ENV=dev mix compile --warnings-as-errors && mix test --no-start test/scoria_web/dev_lab_boundary_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `dev/lab/sections/viewports.ex` compiles clean under `MIX_ENV=dev` with `--warnings-as-errors`.
    - Exports a `viewports/1` function component with a frame for each of the six widths.
    - Contains the exact D-13 label strings verbatim; contains no device marketing name.
    - No new breakpoint/spacing/type token and no raw hex; width is applied via inline style only.
    - The Plan 01 boundary/coverage test still passes.
  </acceptance_criteria>
  <done>Six proof-target viewport frames are inspectable with exact maintainer labels.</done>
</task>

<task type="auto">
  <name>Task 2: Build the Overlays curated flow-probe section</name>
  <files>dev/lab/sections/overlays.ex</files>
  <read_first>
    - lib/scoria_web/ui.ex (drawer/1, modal/1, toast/1, raw_evidence/1, id/1)
    - assets/js/scoria.js (ThemeToggle, Dismissable, CommandPalette, MobileNav, CopyId hooks + data-scoria-ready sentinel — reuse, do not reimplement)
    - dev/lab/fixtures.ex (scenario/1: approval_requested/approval_denied for dense approvals; long-evidence payload) — from Plan 01
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-CONTEXT.md (D-10 curated-probe list, D-15, D-33)
    - .planning/phases/37-dev-component-lab-and-stress-fixtures/37-UI-SPEC.md (Component Inventory For Lab Chrome, Copywriting Contract labels)
  </read_first>
  <action>
    Create `DevLab.Sections.Overlays` at `dev/lab/sections/overlays.ex` (`use Phoenix.Component`, import the specific `ScoriaWeb.UI` overlay primitives used). Export an `overlays/1` function component for the `Overlays` IA section.

    Provide exactly the curated D-10 flow probes where isolated components are insufficient: (1) a dense approvals list with a toast overlay stacked over it (the RISK-TOAST-LEGIBILITY stress fixture — surface the problem, do NOT fix toast legibility here; that is Phase 38); (2) drawer/modal focus and dismissal probes; (3) command palette; (4) mobile nav; (5) raw-evidence copy controls near a payload (`Copy fixture payload` label); (6) a long unbroken evidence payload. Feed them from `DevLab.Fixtures.scenario/1`.

    Reuse the existing `assets/js/scoria.js` hooks for theme, dismissal, command palette, mobile nav, and copy — attach via the existing `phx-hook`/data-attribute conventions; do NOT reimplement dismissal, focus-trap, or motion mechanisms, and do NOT invent new motion durations (D-14). Do not build a full `/scoria/_lab/approvals` page clone (D-10). Use only existing primitives + `--scoria-*` tokens.
  </action>
  <verify>
    <automated>MIX_ENV=dev mix compile --warnings-as-errors && mix test --no-start test/scoria_web/dev_lab_boundary_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `dev/lab/sections/overlays.ex` compiles clean under `MIX_ENV=dev` with `--warnings-as-errors`.
    - Exports an `overlays/1` function component containing the dense-approvals-with-toast probe plus drawer/modal, command palette, mobile nav, copy-control, and long-evidence probes.
    - Reuses existing `scoria.js` hooks via `phx-hook`/data attributes; defines no new JS and no new motion duration.
    - No toast-legibility "fix" is attempted (stress fixture only); no raw hex / new token.
    - The Plan 01 boundary/coverage test still passes.
  </acceptance_criteria>
  <done>The curated overlay/focus/dense-data probes render, reusing existing hooks, surfacing (not fixing) the Phase-38/39 risks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-only section modules → runtime `lib/` | Viewport/overlay modules live under `dev/`; never referenced from `lib/`. |
| lab overlay probes → existing JS hooks | Probes must reuse the shipped `scoria.js` hooks, not introduce new client behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-03 | Tampering | `DevLab.Sections.*` referenced from `lib/` | high | mitigate | Modules live under `dev/`; Plan 01 `lib/` no-`DevLab.` scan (this plan's per-task verify) proves no runtime leak. |
| T-37-05 | Info Disclosure | new raw hex / new motion token in lab chrome | low | mitigate | UI-SPEC binding; acceptance criteria forbid new motion durations and raw hex; probes reuse existing hooks only. |
| T-37-SC | Tampering | npm/hex installs | low | accept | Phase adds zero new packages. |
</threat_model>

<verification>
- `MIX_ENV=dev mix compile --warnings-as-errors` clean.
- `mix test --no-start test/scoria_web/dev_lab_boundary_test.exs` still passes.
- Behavioral overlay/viewport proof arrives in Plan 06 (`lab.spec.mjs`) after the route mounts (Plan 05).
</verification>

<success_criteria>
- Viewports frames the six proof-target widths with exact D-13 labels.
- Overlays provides the curated D-10 probes (including the toast-over-dense-UI stress fixture) reusing existing hooks, without fixing Phase-38/39 issues.
</success_criteria>

<output>
Create `.planning/phases/37-dev-component-lab-and-stress-fixtures/37-04-SUMMARY.md` when done.
</output>
