---
phase: 77.2-address-tech-debt-v2-9-planning-hygiene-ci-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/scoria/ci_policy_contract_test.exs
autonomous: true
requirements: [CI-GALL-01]
must_haves:
  truths:
    - "ci_policy_contract_test.exs asserts support copilot gallery lane runs after knowledge WAE in ci-verify.yml test job."
    - "Gallery lane command string sourced from VerificationLanes.ci_command(:support_copilot_gallery) — not hardcoded duplicate."
    - "VerificationLanes.closeout_order/0 unchanged; gallery lane remains advisory outside closeout."
  artifacts:
    - path: "test/scoria/ci_policy_contract_test.exs"
      provides: "CI topology contract for gallery-after-knowledge ordering"
      contains: "support_copilot_gallery"
    - path: ".github/workflows/ci-verify.yml"
      provides: "Existing CI topology under test (read-only)"
      contains: "Run support copilot gallery lane"
  key_links:
    - from: "lib/scoria/verification_lanes.ex"
      to: "test/scoria/ci_policy_contract_test.exs"
      via: "VerificationLanes.ci_command(:support_copilot_gallery) in ordering assertion"
      pattern: "ci_command(:support_copilot_gallery)"
    - from: "test/scoria/ci_policy_contract_test.exs"
      to: ".github/workflows/ci-verify.yml"
      via: "index_of ordering check on test job section"
      pattern: "split_jobs"
---

<objective>
Close the v2.9 milestone audit integration gap: gallery lane ordering after knowledge WAE is present in ci-verify.yml but not contract-tested.

Purpose: Prevent CI topology drift for CI-GALL-01 without widening merge-blocking closeout.
Output: One new ExUnit test in `ci_policy_contract_test.exs` following existing `index_of` / `split_jobs` patterns.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/v2.9-MILESTONE-AUDIT.md
@test/scoria/ci_policy_contract_test.exs
@.github/workflows/ci-verify.yml
@lib/scoria/verification_lanes.ex
@test/scoria/verification_lanes_test.exs

**Gap (audit):** Integration 77→CI contract — gallery ordering asserted in YAML + docs only, not `ci_policy_contract_test.exs`.

**Existing pattern:** Test `"test job runs full suite WAE after closeout lanes and before knowledge WAE lane"` uses `split_jobs/1` and `index_of/2` on the test job section.

**CI topology (test job tail):**
- `mix test.knowledge --warnings-as-errors` (knowledge lane)
- `mix scoria.test.support_copilot` (gallery advisory lane)

**Non-goals:** Do not edit ci-verify.yml. Do not add gallery to `closeout_order/0`. Do not change advisory lane posture.
</context>

<tasks>

<task type="auto">
  <name>Task 77.2-02-01: Add gallery-after-knowledge CI ordering contract test</name>
  <requirement_ids>CI-GALL-01</requirement_ids>
  <files>test/scoria/ci_policy_contract_test.exs</files>
  <read_first>
    test/scoria/ci_policy_contract_test.exs
    .github/workflows/ci-verify.yml
    lib/scoria/verification_lanes.ex
    test/scoria/verification_lanes_test.exs
  </read_first>
  <action>
    Add a new test after the existing knowledge WAE ordering test (near line 190), named:
    `"test job runs support copilot gallery lane after knowledge WAE and outside closeout order"`

  Implementation:
    1. `ci_verify = File.read!(@ci_verify)`
    2. `[_policy_section, test_section] = split_jobs(ci_verify)`
    3. `gallery_cmd = VerificationLanes.ci_command(:support_copilot_gallery)` → `"mix scoria.test.support_copilot"`
    4. `knowledge_cmd = "mix test.knowledge --warnings-as-errors"`
    5. `assert test_section =~ gallery_cmd`
    6. `assert index_of(test_section, knowledge_cmd) < index_of(test_section, gallery_cmd)`
    7. `refute :support_copilot_gallery in VerificationLanes.closeout_order()` (belt-and-suspenders in same test or rely on verification_lanes_test — include refute for explicit CI-GALL-01 posture)

    Do not duplicate `@support_copilot_gallery` module attribute unless adding one improves consistency; inline `VerificationLanes.ci_command/1` call is preferred per lane SSOT.
  </action>
  <acceptance_criteria>
    - `test/scoria/ci_policy_contract_test.exs` contains test name `support copilot gallery lane after knowledge WAE`
    - Test uses `VerificationLanes.ci_command(:support_copilot_gallery)`
    - Test uses `index_of` to assert knowledge step precedes gallery step in test job section
    - `MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs` exits 0
    - `MIX_ENV=test mix test test/scoria/verification_lanes_test.exs` still exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 77.2-02-02: Update phase 77 verification gap note after contract lands</name>
  <requirement_ids>CI-GALL-01</requirement_ids>
  <files>.planning/phases/77-gallery-ci-docs-closeout/77-VERIFICATION.md</files>
  <read_first>
    .planning/phases/77-gallery-ci-docs-closeout/77-VERIFICATION.md
    test/scoria/ci_policy_contract_test.exs
  </read_first>
  <action>
    Only if Task 77.2-01-04 already wrote `77-VERIFICATION.md` with a Gaps entry deferring ordering contract to plan 77.2-02:
    - Edit the Gaps section to state ordering contract is now covered by `ci_policy_contract_test.exs`
    - Add automated check line: `MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs` with note about gallery-after-knowledge test name

    If 77-VERIFICATION.md does not exist yet (plans run out of order), skip this task — plan 01 task 04 will run after and should not list the gap.
  </action>
  <acceptance_criteria>
    - If `77-VERIFICATION.md` exists and mentioned ci_policy_contract gap: Gaps section no longer lists ordering as open OR documents it as closed
    - If file missing: task skipped without error
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs --trace` — all tests pass including new ordering test
2. `MIX_ENV=test mix test test/scoria/verification_lanes_test.exs` — 7 tests, 0 failures
3. `rg 'support copilot gallery lane after knowledge' test/scoria/ci_policy_contract_test.exs` — match found
</verification>

<success_criteria>
- New CI contract test asserts knowledge WAE precedes gallery lane in ci-verify.yml test job
- Gallery command sourced from VerificationLanes SSOT
- No change to closeout_order or advisory lane exclusions
- Full ci_policy_contract_test suite green
</success_criteria>
