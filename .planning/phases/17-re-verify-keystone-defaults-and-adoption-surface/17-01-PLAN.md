---
phase: 17-re-verify-keystone-defaults-and-adoption-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/14-policy-defaults-and-install-ergonomics/14-VERIFICATION.md
  - .planning/phases/14-policy-defaults-and-install-ergonomics/14-VALIDATION.md
  - .planning/phases/17-re-verify-keystone-defaults-and-adoption-surface/17-01-SUMMARY.md
autonomous: false
requirements:
  - POLY-01
  - POLY-02
  - POLY-03
must_haves:
  truths:
    - "Phase 14 has a canonical verification artifact in its own phase directory with explicit Phase 17 backfill chronology."
    - "POLY-01, POLY-02, and POLY-03 each map to targeted seam-specific proof commands instead of only a full green suite."
    - "One fresh `SCORIA_DB_PORT=55432 MIX_ENV=test mix test` pass is recorded as secondary regression hygiene rather than the primary evidence source."
    - "Phase 14 closes with one bounded install-to-run manual observation tying install, migrate, test, `run_id` readback, `/scoria`, and `/scoria/workflows/:run_id` into the same evidence chain."
  artifacts:
    - path: ".planning/phases/14-policy-defaults-and-install-ergonomics/14-VERIFICATION.md"
      provides: "Canonical Phase 14 verification report with requirement-to-command evidence and backfill chronology"
    - path: ".planning/phases/14-policy-defaults-and-install-ergonomics/14-VALIDATION.md"
      provides: "Terminal-truth validation ledger with green rows, bounded manual script, and final approval wording"
    - path: ".planning/phases/17-re-verify-keystone-defaults-and-adoption-surface/17-01-SUMMARY.md"
      provides: "Link-first Phase 17 breadcrumb pointing back to the canonical Phase 14 verification artifacts"
  key_links:
    - from: ".planning/phases/14-policy-defaults-and-install-ergonomics/14-VERIFICATION.md"
      to: ".planning/phases/14-policy-defaults-and-install-ergonomics/14-VALIDATION.md"
      via: "Requirement matrix and copied proof commands"
      pattern: "POLY-01|POLY-02|POLY-03|SCORIA_DB_PORT=55432"
    - from: ".planning/phases/14-policy-defaults-and-install-ergonomics/14-VERIFICATION.md"
      to: "docs/operator_verification.md"
      via: "Bounded manual install-to-run acceptance lane"
      pattern: "/scoria|/scoria/workflows/:run_id|run_id"
---

<objective>
Backfill the missing canonical verification chain for Phase 14 and bring its validation artifact to terminal truth.

Purpose: Prove the shipped defaults/install ergonomics behavior from Phase 14 still holds in the current repo, with requirement-mapped targeted lanes as primary proof and one bounded adopter-visible walkthrough as closure evidence.
Output: `.planning/phases/14-policy-defaults-and-install-ergonomics/14-VERIFICATION.md` plus a terminal-truth `14-VALIDATION.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.4-MILESTONE-AUDIT.md
@.planning/phases/17-re-verify-keystone-defaults-and-adoption-surface/17-CONTEXT.md
@.planning/phases/17-re-verify-keystone-defaults-and-adoption-surface/17-RESEARCH.md
@.planning/phases/17-re-verify-keystone-defaults-and-adoption-surface/17-PATTERNS.md
@.planning/phases/07-seismograph/07-VERIFICATION.md
@.planning/phases/13-public-runtime-api-and-session-lifecycle/13-VERIFICATION.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-CONTEXT.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-VALIDATION.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-01-SUMMARY.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-02-SUMMARY.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-03-SUMMARY.md
@docs/operator_verification.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical Phase 14 verification artifact and stage validation truth for `POLY-01` through `POLY-03`</name>
  <read_first>
    - .planning/phases/17-re-verify-keystone-defaults-and-adoption-surface/17-CONTEXT.md
    - .planning/phases/17-re-verify-keystone-defaults-and-adoption-surface/17-RESEARCH.md
    - .planning/phases/17-re-verify-keystone-defaults-and-adoption-surface/17-PATTERNS.md
    - .planning/phases/07-seismograph/07-VERIFICATION.md
    - .planning/phases/13-public-runtime-api-and-session-lifecycle/13-VERIFICATION.md
    - .planning/phases/14-policy-defaults-and-install-ergonomics/14-VALIDATION.md
    - .planning/phases/14-policy-defaults-and-install-ergonomics/14-01-SUMMARY.md
    - .planning/phases/14-policy-defaults-and-install-ergonomics/14-02-SUMMARY.md
    - .planning/phases/14-policy-defaults-and-install-ergonomics/14-03-SUMMARY.md
    - docs/operator_verification.md
  </read_first>
  <files>.planning/phases/14-policy-defaults-and-install-ergonomics/14-VERIFICATION.md, .planning/phases/14-policy-defaults-and-install-ergonomics/14-VALIDATION.md</files>
  <action>Create `.planning/phases/14-policy-defaults-and-install-ergonomics/14-VERIFICATION.md` using the same frontmatter and section order as `07-VERIFICATION.md` and `13-VERIFICATION.md`. Set `phase: 14-policy-defaults-and-install-ergonomics`, `status: passed`, `verified_on: 2026-05-16`, and `verified_by_phase: 17-re-verify-keystone-defaults-and-adoption-surface`. Map `POLY-01` to `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/prompt_policy_test.exs test/scoria/runtime/defaults_test.exs`; map `POLY-02` to `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/runtime/defaults_test.exs test/scoria/runtime_test.exs test/scoria/runtime_integration_test.exs`; map `POLY-03` to both `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.install_route_smoke_test.exs` and `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/bootstrap/migration_lane_compatibility_test.exs test/mix/tasks/scoria.test_knowledge_test.exs`. Run one fresh secondary full-suite confidence pass with `SCORIA_DB_PORT=55432 MIX_ENV=test mix test` and record its result explicitly in `14-VERIFICATION.md` as regression hygiene only. Stage the manual install-to-run closure in both Phase 14 artifacts: in `14-VALIDATION.md`, keep all existing rows `✅ green`, preserve the command matrix, add a numbered `## Manual-Only Verifications` acceptance script with explicit expected observations (`mix scoria.install`, `mix ecto.migrate`, `mix test`, one started run, same `run_id` visible via API readback and `/scoria/workflows/:run_id`), and replace ambiguous approval wording with a line that says targeted defaults/install seams plus the secondary full-suite pass completed on `2026-05-16` and the manual walkthrough is awaiting the final observation note; in `14-VERIFICATION.md`, add a matching placeholder observation block for the same manual lane so Task 3 has an explicit insertion point in both files.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/prompt_policy_test.exs test/scoria/runtime/defaults_test.exs test/scoria/runtime_test.exs test/scoria/runtime_integration_test.exs test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.install_route_smoke_test.exs test/scoria/bootstrap/migration_lane_compatibility_test.exs test/mix/tasks/scoria.test_knowledge_test.exs &amp;&amp; SCORIA_DB_PORT=55432 MIX_ENV=test mix test &amp;&amp; rg -n "phase: 14-policy-defaults-and-install-ergonomics|verified_by_phase: 17-re-verify-keystone-defaults-and-adoption-surface|POLY-01|POLY-02|POLY-03|SCORIA_DB_PORT=55432 MIX_ENV=test mix test|run_id|/scoria|/scoria/workflows/:run_id|awaiting observation|placeholder" .planning/phases/14-policy-defaults-and-install-ergonomics/14-VERIFICATION.md .planning/phases/14-policy-defaults-and-install-ergonomics/14-VALIDATION.md &amp;&amp; ! rg -n "⬜ pending|TBD manual note|awaiting proof command" .planning/phases/14-policy-defaults-and-install-ergonomics/14-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `14-VERIFICATION.md` exists and includes `status: passed`, `verified_on: 2026-05-16`, and `verified_by_phase: 17-re-verify-keystone-defaults-and-adoption-surface`.
    - The report contains separate requirement coverage for `POLY-01`, `POLY-02`, and `POLY-03` with the exact targeted command strings above.
    - Task-local execution runs one fresh `SCORIA_DB_PORT=55432 MIX_ENV=test mix test` pass and records it as secondary regression hygiene rather than only copying the command string into the report.
    - Both `14-VERIFICATION.md` and `14-VALIDATION.md` contain a staged placeholder/manual-observation block for the install-to-run lane before Task 2 runs.
    - `14-VALIDATION.md` contains a numbered manual acceptance script with explicit `run_id`, `/scoria`, and `/scoria/workflows/:run_id` observations instead of a vague reminder.
    - `14-VALIDATION.md` approval wording explicitly distinguishes targeted proof from the secondary full-suite pass.
  </acceptance_criteria>
  <done>Phase 14 gains its canonical verification artifact and a terminal-truth validation file that is ready for the final manual observation note.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture the bounded install-to-run walkthrough required for `POLY-03` closure</name>
  <read_first>
    - .planning/phases/14-policy-defaults-and-install-ergonomics/14-VALIDATION.md
    - .planning/phases/14-policy-defaults-and-install-ergonomics/14-VERIFICATION.md
    - docs/operator_verification.md
  </read_first>
  <action>Follow the exact numbered acceptance script added to `14-VALIDATION.md`, record one short observation note that names the run identifier used for the walkthrough, states whether the run could be started and read back successfully, and confirms whether `/scoria` plus `/scoria/workflows/:run_id` exposed the same durable run. Do not widen the check beyond the script, and do not return a generic “looked good” summary.</action>
  <what-built>Task 1 created the canonical Phase 14 verification report and prepared the exact install-to-run manual script plus placeholder observation block inside `14-VALIDATION.md`.</what-built>
  <how-to-verify>
    1. Run the install and baseline commands exactly as written in `14-VALIDATION.md`.
    2. Start one real run through the public runtime lane described by the script and capture its `run_id`.
    3. Read the run back through the public API path named by the script.
    4. Open `/scoria` and `/scoria/workflows/:run_id` and verify the same durable run is visible there.
    5. Return one short note stating the exact observed outcome for that `run_id`.
  </how-to-verify>
  <acceptance_criteria>
    - The returned note includes a concrete `run_id` or equivalent durable identifier.
    - The note explicitly says whether the API readback and `/scoria` surfaces pointed to the same run.
    - The note is short enough to paste directly into both Phase 14 artifacts without reinterpretation.
  </acceptance_criteria>
  <resume-signal>Type `approved` plus the exact observation note, or describe the precise mismatch.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Close Phase 14 validation and verification to terminal truth using the recorded walkthrough result</name>
  <read_first>
    - .planning/phases/14-policy-defaults-and-install-ergonomics/14-VERIFICATION.md
    - .planning/phases/14-policy-defaults-and-install-ergonomics/14-VALIDATION.md
  </read_first>
  <files>.planning/phases/14-policy-defaults-and-install-ergonomics/14-VERIFICATION.md, .planning/phases/14-policy-defaults-and-install-ergonomics/14-VALIDATION.md</files>
  <action>Replace the placeholder observation block in both Phase 14 artifacts with the exact note from Task 2. In `14-VALIDATION.md`, mark the manual walkthrough complete and update `**Approval:**` to a terminal-truth line stating that the targeted defaults/install seams, the secondary `SCORIA_DB_PORT=55432 MIX_ENV=test mix test` pass, and the bounded install-to-run walkthrough all passed on `2026-05-16`. In `14-VERIFICATION.md`, add the same observation in the requirement or evidence notes so the user-visible install path is part of the canonical `POLY-03` closure record.</action>
  <verify>
    <automated>rg -n "Approval:|approved|Observed|/scoria/workflows/:run_id|2026-05-16" .planning/phases/14-policy-defaults-and-install-ergonomics/14-VERIFICATION.md .planning/phases/14-policy-defaults-and-install-ergonomics/14-VALIDATION.md &amp;&amp; ! rg -n "awaiting observation|placeholder|TBD" .planning/phases/14-policy-defaults-and-install-ergonomics/14-VERIFICATION.md .planning/phases/14-policy-defaults-and-install-ergonomics/14-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Both Phase 14 artifacts contain the final manual observation text and no placeholder remains.
    - `14-VALIDATION.md` approval line explicitly names the targeted seams, the secondary full-suite pass, and the manual walkthrough.
    - `14-VERIFICATION.md` records that the backfill was performed by Phase 17 rather than implying original Phase 14 verification coverage.
  </acceptance_criteria>
  <done>Phase 14 closes with canonical verification plus a terminal validation file that includes the required user-visible install-path observation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validation matrix -> canonical verification report | The report must copy exact proof commands and requirement mappings rather than inventing summarized evidence |
| targeted proof lanes -> full-suite closeout | The full suite must remain secondary so `POLY-*` closure stays seam-specific |
| automated defaults/install proof -> manual install-to-run note | The human-visible adoption path can drift unless the walkthrough binds all surfaces to one durable run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | R | `.planning/phases/14-policy-defaults-and-install-ergonomics/14-VERIFICATION.md` | mitigate | Require a requirement-to-command matrix that maps `POLY-01` through `POLY-03` to exact targeted commands copied from `14-VALIDATION.md` |
| T-17-02 | T | `.planning/phases/14-policy-defaults-and-install-ergonomics/14-VALIDATION.md` manual section | mitigate | Convert the vague/manual lane into a numbered install-to-run script with explicit `run_id` and route observations |
| T-17-03 | I | operator-evidence wording | mitigate | Require the final note to name the durable run identifier and whether API readback plus `/scoria` surfaces matched exactly |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/prompt_policy_test.exs test/scoria/runtime/defaults_test.exs test/scoria/runtime_test.exs test/scoria/runtime_integration_test.exs test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.install_route_smoke_test.exs test/scoria/bootstrap/migration_lane_compatibility_test.exs test/mix/tasks/scoria.test_knowledge_test.exs`
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test`
- `rg -n "POLY-01|POLY-02|POLY-03|/scoria/workflows/:run_id|verified_by_phase: 17-re-verify-keystone-defaults-and-adoption-surface" .planning/phases/14-policy-defaults-and-install-ergonomics/14-VERIFICATION.md .planning/phases/14-policy-defaults-and-install-ergonomics/14-VALIDATION.md`
- Manual checkpoint result recorded verbatim in both files
</verification>

<success_criteria>
Phase 14 no longer relies on validation rows or summary prose alone. Its phase directory contains both `14-VALIDATION.md` and `14-VERIFICATION.md`, both reflect 2026-05-16 backfill chronology, all `POLY-*` requirements map to targeted proof seams, and the installer/user-visible lane is closed with an explicit manual observation.
</success_criteria>

<output>
After completion, create `.planning/phases/17-re-verify-keystone-defaults-and-adoption-surface/17-01-SUMMARY.md`
Summary contract:
- Keep the summary short and chronology-aware.
- Treat `14-VERIFICATION.md` and `14-VALIDATION.md` as canonical proof; do not duplicate their full requirement matrix or command list.
- Use the summary as a link-first Phase 17 breadcrumb that points back to the canonical Phase 14 artifacts and notes that the backfill was performed on `2026-05-16` by Phase 17.
</output>
