---
phase: 48-exdoc-and-guide-ladder-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/scoria/package_surface_test.exs
  - test/mix/tasks/scoria.release_preview_test.exs
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
must_haves:
  truths:
    - "DOCS-01: ExDoc config contract asserts grouped extras and grouped modules instead of a flat sidebar."
    - "DOCS-02: ExDoc config contract asserts source_url, dynamic source_ref, release docs URL metadata, logo, favicon, and html/markdown formatters."
    - "DOCS-03: Package/release-preview contracts assert the canonical guides ladder and old docs compatibility stubs ship together."
  artifacts:
    - path: "test/scoria/package_surface_test.exs"
      provides: "RED package and ExDoc surface contract for D-05 through D-15 and D-20 through D-21"
    - path: "test/mix/tasks/scoria.release_preview_test.exs"
      provides: "RED release-preview required path contract for guides, stubs, and brand assets"
  key_links:
    - from: "test/scoria/package_surface_test.exs"
      to: "mix.exs"
      via: "Mix.Project.config()[:docs] and Mix.Project.config()[:package]"
      pattern: "Mix\\.Project\\.config\\(\\)"
    - from: "test/mix/tasks/scoria.release_preview_test.exs"
      to: "lib/mix/tasks/scoria.release_preview.ex"
      via: "Mix.Tasks.Scoria.ReleasePreview.required_package_paths/0"
      pattern: "required_package_paths"
  prohibitions:
    - "Do not add package installs."
    - "Do not assert the old docs source paths as ExDoc extras."
---

<objective>
Create the Wave 0 RED contracts for the ExDoc, package, source-ref, redirect, grouping, and release-preview surfaces before implementation.

Purpose: Phase 48 changes the public docs/package surface; D-20 and D-21 require tests to describe the new contract before `mix.exs` and release-preview wiring are changed.
Output: Updated RED ExUnit contracts that fail only because the new guide ladder and docs config are not implemented yet.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/48-exdoc-and-guide-ladder-restructure/48-CONTEXT.md
@.planning/phases/48-exdoc-and-guide-ladder-restructure/48-RESEARCH.md
@.planning/phases/48-exdoc-and-guide-ladder-restructure/48-PATTERNS.md
@.planning/phases/48-exdoc-and-guide-ladder-restructure/48-VALIDATION.md
@mix.exs
@test/scoria/package_surface_test.exs
@test/mix/tasks/scoria.release_preview_test.exs
@lib/mix/tasks/scoria.release_preview.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED ExDoc config and package surface contract</name>
  <files>test/scoria/package_surface_test.exs</files>
  <read_first>
    test/scoria/package_surface_test.exs
    mix.exs
    .planning/phases/48-exdoc-and-guide-ladder-restructure/48-CONTEXT.md
    .planning/phases/48-exdoc-and-guide-ladder-restructure/48-RESEARCH.md
    .planning/phases/48-exdoc-and-guide-ladder-restructure/48-VALIDATION.md
  </read_first>
  <behavior>
    - Test asserts `project[:docs][:main] == "getting-started"` per D-05.
    - Test asserts `source_url: "https://github.com/szTheory/scoria"`, `source_ref: "main"` on non-tag builds, `homepage_url: "https://hexdocs.pm/scoria"`, `extra_section: "Guides"`, `formatters: ["html", "markdown"]`, `logo: "brandbook/logo-mark.svg"`, and `favicon: "brandbook/favicon.svg"` per D-06 through D-08.
    - Test asserts ExDoc extras include README, CHANGELOG, LICENSE, MAINTAINERS, all canonical `guides/` files from D-03, and no old `docs/*.md` guide source paths per D-01, D-02, D-10, and D-11.
    - Test asserts package files include canonical guides, old `docs/*.md` compatibility stubs, and the four brand assets from D-09.
  </behavior>
  <action>Add constants for `@canonical_guides`, `@compatibility_stub_paths`, and `@docs_brand_assets` inside `test/scoria/package_surface_test.exs`. Replace the old flat `@docs_extras` expectation with assertions for `main`, `source_url`, `source_ref`, `extra_section`, `formatters`, `logo`, `favicon`, `groups_for_extras`, `groups_for_modules`, `redirects`, canonical guide extras, package-file inclusion, and exclusion of old guide source paths from ExDoc extras. Include redirect assertions for `adoption_lanes`, `phoenix_runtime_example`, `semantic_fast_path`, `operator_verification`, `connector_adoption`, `support_copilot_gallery`, `scoria_vs_external_llm_ops`, `bounded_handoffs`, and `MAINTAINERS` per D-02 and D-20. Keep the existing install/fallback checks intact.</action>
  <acceptance_criteria>
    - Source assertion: `test/scoria/package_surface_test.exs` contains `guides/getting-started.md`, `guides/capabilities/semantic-cache.md`, `brandbook/favicon.svg`, `groups_for_extras`, `groups_for_modules`, and `redirects`.
    - Source assertion: `test/scoria/package_surface_test.exs` no longer asserts `project[:docs][:main] == "readme"` or `project[:docs][:source_ref] == "v#{project[:version]}"`.
    - Behavior assertion: running the focused package test exits nonzero only on missing Phase 48 implementation, not on compile errors.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/package_surface_test.exs -x</automated>
  </verify>
  <done>Package surface tests describe the new ExDoc and package inventory contract for D-05 through D-15 and D-20 through D-21, and the command fails RED only because implementation is pending.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RED release-preview required path contract</name>
  <files>test/mix/tasks/scoria.release_preview_test.exs</files>
  <read_first>
    test/mix/tasks/scoria.release_preview_test.exs
    lib/mix/tasks/scoria.release_preview.ex
    mix.exs
    .planning/phases/48-exdoc-and-guide-ladder-restructure/48-CONTEXT.md
    .planning/phases/48-exdoc-and-guide-ladder-restructure/48-VALIDATION.md
  </read_first>
  <behavior>
    - Test asserts release preview required paths include every D-03 canonical guide file.
    - Test asserts release preview required paths include old docs stubs for external source-link compatibility per D-02.
    - Test asserts release preview required paths include the D-09 brand assets.
    - Test asserts release preview still includes README, LICENSE, CHANGELOG, mix.exs, lib/scoria.ex, workflow migrations, and knowledge migrations.
  </behavior>
  <action>Replace the old release-preview expected path list with the Phase 48 inventory: README.md, LICENSE, mix.exs, CHANGELOG.md, lib/scoria.ex, the existing workflow and knowledge migration proof files, every canonical D-03 `guides/` path, every old `docs/*.md` compatibility stub that Phase 48 preserves, and `brandbook/logo-primary.svg`, `brandbook/logo-primary-light.svg`, `brandbook/logo-mark.svg`, and `brandbook/favicon.svg` per D-09 and D-21. Add assertions that dev-only docs such as `docs/design_system.md`, `docs/docker_dev_dx.md`, and `docs/uat_automation.md` are absent per D-11.</action>
  <acceptance_criteria>
    - Source assertion: `test/mix/tasks/scoria.release_preview_test.exs` contains all D-03 guide paths and all four D-09 brand asset paths.
    - Source assertion: `test/mix/tasks/scoria.release_preview_test.exs` refutes `docs/design_system.md`, `docs/docker_dev_dx.md`, and `docs/uat_automation.md`.
    - Behavior assertion: release-preview test exits nonzero only because `Mix.Tasks.Scoria.ReleasePreview.required_package_paths/0` has not yet moved to the Phase 48 list.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/mix/tasks/scoria.release_preview_test.exs -x</automated>
  </verify>
  <done>Release-preview RED contract covers canonical guides, compatibility stubs, brand assets, and dev-doc exclusion per D-02, D-03, D-09, D-11, D-20, and D-21.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer source -> generated HexDocs/package | Test expectations must prevent docs/package drift and accidental exposure of dev-only documentation. |
| Docs metadata -> external GitHub/HexDocs links | Source refs and release-doc links can mislead adopters if they point at missing tags or wrong versions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01-I | Information Disclosure | `test/scoria/package_surface_test.exs` | mitigate | Assert dev-only docs are excluded from ExDoc extras and release-preview inventory. |
| T-48-01-T | Tampering | ExDoc redirect and path contract | mitigate | Add exact redirect and canonical-guide assertions before implementation changes old paths. |
| T-48-01-R | Repudiation | `source_ref` and docs URL contract | mitigate | Assert dynamic non-tag source ref and stable HexDocs URLs so generated docs can be traced to the correct source. |
| T-48-01-SC | Tampering | package installs | accept | Phase 48 uses existing ExDoc/Hex tooling and installs no new packages. |
</threat_model>

<artifacts_this_phase_produces>
- RED assertions in `test/scoria/package_surface_test.exs` for `main`, `source_url`, `source_ref`, `homepage_url`, `extra_section`, `formatters`, `logo`, `favicon`, `groups_for_extras`, `groups_for_modules`, `redirects`, canonical `guides/` extras, package guide/stub files, and brand assets.
- RED assertions in `test/mix/tasks/scoria.release_preview_test.exs` for `Mix.Tasks.Scoria.ReleasePreview.required_package_paths/0`.
</artifacts_this_phase_produces>

<verification>
Run both RED contract commands:
- `MIX_ENV=test mix test test/scoria/package_surface_test.exs -x`
- `MIX_ENV=test mix test test/mix/tasks/scoria.release_preview_test.exs -x`

Both should compile and fail only on Phase 48 implementation gaps.
</verification>

<success_criteria>
Wave 0 package/docs contracts exist and describe the new docs surface with concrete Phase 48 paths, groups, redirects, source metadata, and package assets.
</success_criteria>

<output>
Create `.planning/phases/48-exdoc-and-guide-ladder-restructure/48-01-SUMMARY.md` when done.
</output>
