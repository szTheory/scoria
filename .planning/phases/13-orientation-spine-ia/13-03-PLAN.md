---
phase: 13-orientation-spine-ia
plan: "03"
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - lib/scoria_web/router.ex
  - lib/scoria_web/live/coming_soon_live.ex
  - test/scoria_web/router_test.exs
  - test/scoria_web/live/coming_soon_live_test.exs
autonomous: true
requirements: [IA-06]
must_haves:
  truths:
    - "Every reserved brand-name nav item routes to one shared coming-soon LiveView."
    - "Stub pages are honest: future-tense capability, what works today, tracking link, no fake rows or charts."
    - "Unknown stub slugs do not render arbitrary user-controlled labels."
  artifacts:
    - path: "lib/scoria_web/live/coming_soon_live.ex"
      provides: "allowlisted shared stub LiveView"
    - path: "lib/scoria_web/router.ex"
      provides: "`/coming/:screen` dashboard route"
  key_links:
    - from: "lib/scoria_web/live/coming_soon_live.ex"
      to: "lib/scoria_web/dashboard_nav.ex"
      via: "DashboardNav.stub_screen/1"
      pattern: "stub_screen"
---

<objective>
Add the shared coming-soon route and page for reserved capabilities.

Purpose: Reserved brand names must appear in the IA without pretending the backend capability exists.
Output: `/coming/:screen` LiveView backed by `DashboardNav` allowlisted metadata and verified route/live tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-orientation-spine-ia/13-CONTEXT.md
@.planning/phases/13-orientation-spine-ia/13-RESEARCH.md
@.planning/phases/13-orientation-spine-ia/13-VALIDATION.md
@.planning/phases/13-orientation-spine-ia/13-PATTERNS.md
@.planning/phases/13-orientation-spine-ia/13-UI-SPEC.md
@.planning/phases/13-orientation-spine-ia/13-DISCUSSION-LOG.md
@lib/scoria_web/router.ex
@lib/scoria_web/dashboard_nav.ex
@lib/scoria_web/ui.ex
@test/scoria_web/router_test.exs
</context>

<must_haves>
- D-13: Route is exactly `live("/coming/:screen", ScoriaWeb.ComingSoonLive, :show)` inside the existing dashboard `live_session`.
- D-14: Stub page order is screen name plus `Soon`, one will-do evidence sentence, `What works today`, and `Track progress`.
- D-15: Dataset Builder is not a stub.
- UI-SPEC: No mock charts, sample rows, skeleton screens, fake counts, fake screenshots, or present-tense capability claims.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add route and LiveView tests for allowlisted stubs</name>
  <files>test/scoria_web/router_test.exs, test/scoria_web/live/coming_soon_live_test.exs</files>
  <read_first>
    @test/scoria_web/router_test.exs
    @test/scoria_web/live/orchestrator_live_test.exs
    @lib/scoria_web/router.ex
    @lib/scoria_web/dashboard_nav.ex
    @.planning/phases/13-orientation-spine-ia/13-VALIDATION.md
    @.planning/phases/13-orientation-spine-ia/13-DISCUSSION-LOG.md
  </read_first>
  <behavior>
    - `/scoria/coming/replay-playground`, `/scoria/coming/cost-ledger`, `/scoria/coming/feedback-inbox`, `/scoria/coming/mcp-gateway`, and `/scoria/coming/tool-registry` mount a LiveView.
    - Cost Ledger page renders the approved sentence `Cost Ledger will reconcile model spend per run, tenant, and prompt version`.
    - Replay Playground page renders the approved sentence `Replay Playground will let you branch a run from any checkpoint`.
    - Unknown slug renders `Capability not found. Choose a screen from the dashboard navigation.`
  </behavior>
  <action>Extend `test/scoria_web/router_test.exs` to assert `Phoenix.Router.route_info/4` for `/scoria/coming/cost-ledger` returns `Phoenix.LiveView.Plug`. Create `test/scoria_web/live/coming_soon_live_test.exs` using the same tiny router and endpoint pattern as `orchestrator_live_test.exs`. Add LiveView assertions for all five allowlisted slugs, exact `Soon`, exact `What works today`, exact `Track progress`, and absence of `chart`, `sparkline`, `sample row`, `fake`, and `skeleton` in rendered stub body. Add an unknown-slug test for `/scoria/coming/not-real` with the exact error copy from UI-SPEC.</action>
  <acceptance_criteria>
    - Source assertion: `coming_soon_live_test.exs` exists and references all five final stub slugs.
    - Source assertion: no test references `dataset-builder` or `Dataset Builder`.
    - Behavior assertion: route test covers `/scoria/coming/cost-ledger`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/router_test.exs test/scoria_web/live/coming_soon_live_test.exs --max-failures 1</automated>
  </verify>
  <done>Failing tests describe the shared stub route, allowlist behavior, and honest stub copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared ComingSoonLive from DashboardNav metadata</name>
  <files>lib/scoria_web/router.ex, lib/scoria_web/live/coming_soon_live.ex, test/scoria_web/router_test.exs, test/scoria_web/live/coming_soon_live_test.exs</files>
  <read_first>
    @lib/scoria_web/router.ex
    @lib/scoria_web/dashboard_nav.ex
    @lib/scoria_web/ui.ex
    @test/scoria_web/router_test.exs
    @test/scoria_web/live/coming_soon_live_test.exs
    @.planning/phases/13-orientation-spine-ia/13-CONTEXT.md
    @.planning/phases/13-orientation-spine-ia/13-UI-SPEC.md
  </read_first>
  <behavior>
    - Stub metadata is read from `DashboardNav.stub_screen/1`; the LiveView does not build labels from raw params.
    - Stub pages link to existing dashboard paths using `@scoria_base`.
    - Tracking link uses an honest repository issue-search URL, not a fabricated issue number.
  </behavior>
  <action>Add `live("/coming/:screen", ScoriaWeb.ComingSoonLive, :show)` inside the existing `scoria_dashboard/2` live session. Create `ScoriaWeb.ComingSoonLive` with dashboard layout. In `mount/3`, read `params["screen"]`, call `DashboardNav.stub_screen/1`, assign `page_title` to the stub label when found, and assign `page_title` to `Capability not found` when absent. Render found stubs through `ScoriaWeb.UI.stub_page/1`. Use the exact Cost Ledger and Replay Playground copy from `13-DISCUSSION-LOG.md`. For Feedback Inbox, MCP Gateway, and Tool Registry use future-tense evidence verbs and present-day deep links to Reviews, Runs, Approvals, and Connectors as appropriate. Use tracking URLs under `https://github.com/szTheory/scoria/issues?q=is%3Aissue+<encoded-label>` rather than `scoria#<issue>`. Unknown slugs render only the exact error copy and a link back to Home.</action>
  <acceptance_criteria>
    - Source assertion: `lib/scoria_web/router.ex` contains `live("/coming/:screen", ScoriaWeb.ComingSoonLive, :show)`.
    - Source assertion: `lib/scoria_web/live/coming_soon_live.ex` calls `DashboardNav.stub_screen`.
    - Behavior assertion: `Dataset Builder` does not appear in `DashboardNav.stub_screens/0` output or stub tests.
    - CLI assertion: router, coming-soon, dashboard-nav, and DS-06 focused tests pass together.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/dashboard_nav_test.exs test/scoria_web/router_test.exs test/scoria_web/live/coming_soon_live_test.exs test/scoria_web/ds06_drift_guard_test.exs</automated>
  </verify>
  <done>All five reserved capabilities route to one honest shared stub page, unknown slugs are safe, and no fake capability data is rendered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL param to LiveView | `screen` is user-controlled and selects a stub page. |
| stub metadata to HTML | Stub labels, descriptions, and URLs render in the dashboard page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Tampering | `ComingSoonLive.mount/3` | mitigate | Lookup `params["screen"]` through `DashboardNav.stub_screen/1`; do not titleize or echo unknown params. |
| T-13-03-02 | Information disclosure | stub page body | mitigate | Use only fixed metadata; no backend queries, counts, charts, or fake rows. |
| T-13-03-03 | Spoofing | tracking links | mitigate | Use repository issue-search URLs without fabricated numeric issue IDs. |
| T-13-03-SC | Tampering | package supply chain | mitigate | No dependencies are installed; stubs use existing LiveView and UI components. |
</threat_model>

<verification>
Run `mix test test/scoria_web/dashboard_nav_test.exs test/scoria_web/router_test.exs test/scoria_web/live/coming_soon_live_test.exs test/scoria_web/ds06_drift_guard_test.exs`.
</verification>

<success_criteria>
- `/scoria/coming/:screen` mounts for five allowlisted stub slugs.
- Stub pages are future-tense, evidence-worded, and free of fabricated data.
- Unknown slug behavior is safe and deterministic.
</success_criteria>

<output>
Create `.planning/phases/13-orientation-spine-ia/13-03-SUMMARY.md` when done.
</output>
