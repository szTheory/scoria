---
phase: 13-orientation-spine-ia
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria_web/dashboard_nav.ex
  - lib/scoria_web/components/layouts/app.html.heex
  - test/scoria_web/dashboard_nav_test.exs
autonomous: true
requirements: [IA-01, IA-06]
must_haves:
  truths:
    - "Sidebar groups are exactly Operate, Improve, Configure."
    - "Home is the nav label for `/`; Connectors is in Configure; Runs index activates Runs."
    - "Five reserved capabilities are represented as clickable Soon nav items from the nav SSOT."
  artifacts:
    - path: "lib/scoria_web/dashboard_nav.ex"
      provides: "nav groups, aliases, active map, stub metadata"
    - path: "test/scoria_web/dashboard_nav_test.exs"
      provides: "unit coverage for IA-01 and stub metadata"
  key_links:
    - from: "lib/scoria_web/components/layouts/app.html.heex"
      to: "lib/scoria_web/dashboard_nav.ex"
      via: "nav_groups()"
      pattern: "nav_groups\\(\\)"
---

<objective>
Make `ScoriaWeb.DashboardNav` the complete source of truth for Phase 13 navigation.

Purpose: Sidebar, stubs, and the later command palette must read one IA model so active state and reserved names do not drift.
Output: Tested Operate / Improve / Configure groups, Home relabel, Connectors move, stub metadata, aliases, and active-key coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-orientation-spine-ia/13-CONTEXT.md
@.planning/phases/13-orientation-spine-ia/13-RESEARCH.md
@.planning/phases/13-orientation-spine-ia/13-VALIDATION.md
@.planning/phases/13-orientation-spine-ia/13-PATTERNS.md
@.planning/phases/13-orientation-spine-ia/13-UI-SPEC.md
@lib/scoria_web/dashboard_nav.ex
@lib/scoria_web/components/layouts/app.html.heex
</context>

<must_haves>
- D-01: `/` remains `ScoriaWeb.OrchestratorLive`; nav label changes from `Live Ops` to `Home`.
- D-11: final groups are Operate = Home, Approvals, Runs, Incidents; Improve = Review Queue, Eval Workbench, Prompt Registry, Replay Playground Soon, Cost Ledger Soon, Feedback Inbox Soon; Configure = Connectors, MCP Gateway Soon, Tool Registry Soon.
- D-12: A noun appears in exactly one nav group; Connectors appears only under Configure and any other connector-health surface is an in-page link.
- D-13: stub nav items are clickable links with visible `Soon` text badges.
- D-16: `ScoriaWeb.WorkflowLive.Index` maps to `:runs`.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create DashboardNav unit coverage for groups, stubs, aliases, and active keys</name>
  <files>test/scoria_web/dashboard_nav_test.exs</files>
  <read_first>
    @lib/scoria_web/dashboard_nav.ex
    @lib/scoria_web/router.ex
    @.planning/phases/13-orientation-spine-ia/13-CONTEXT.md
    @.planning/phases/13-orientation-spine-ia/13-VALIDATION.md
    @.planning/phases/13-orientation-spine-ia/13-PATTERNS.md
  </read_first>
  <behavior>
    - Group labels equal `["Operate", "Improve", "Configure"]`.
    - Operate item labels equal `["Home", "Approvals", "Runs", "Incidents"]`.
    - Configure includes `Connectors`, `MCP Gateway`, and `Tool Registry`.
    - Five stub labels are marked with `soon?: true` and have `/coming/:screen` paths.
    - `active_key(ScoriaWeb.WorkflowLive.Index, %{})` returns `:runs`.
  </behavior>
  <action>Create `test/scoria_web/dashboard_nav_test.exs` with direct assertions against `ScoriaWeb.DashboardNav.groups/0`, `stub_screens/0`, `stub_screen/1`, and `active_key/2`. Assert exact stub slugs: `replay-playground`, `cost-ledger`, `feedback-inbox`, `mcp-gateway`, and `tool-registry`. Assert aliases include `runs` for Runs, `review` for Review Queue, and `tools` for Tool Registry. Include a coming-soon active-key assertion: `active_key(ScoriaWeb.ComingSoonLive, %{"screen" => "cost-ledger"}) == :cost_ledger`.</action>
  <acceptance_criteria>
    - Source assertion: `test/scoria_web/dashboard_nav_test.exs` exists.
    - Source assertion: the test file contains the strings `Operate`, `Improve`, `Configure`, `WorkflowLive.Index`, and `cost-ledger`.
    - Behavior assertion: the tests prove the active-state bug for `WorkflowLive.Index` is covered before implementation.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/dashboard_nav_test.exs --max-failures 1</automated>
  </verify>
  <done>DashboardNav tests exist and fail only on missing Phase 13 nav metadata/active behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement grouped nav SSOT and sidebar Soon badges</name>
  <files>lib/scoria_web/dashboard_nav.ex, lib/scoria_web/components/layouts/app.html.heex, test/scoria_web/dashboard_nav_test.exs</files>
  <read_first>
    @lib/scoria_web/dashboard_nav.ex
    @lib/scoria_web/components/layouts/app.html.heex
    @test/scoria_web/dashboard_nav_test.exs
    @.planning/phases/13-orientation-spine-ia/13-UI-SPEC.md
    @.planning/phases/13-orientation-spine-ia/13-DISCUSSION-LOG.md
    @brandbook/brand-book.md
  </read_first>
  <behavior>
    - Sidebar renders `aria-current="page"` for the active nav item.
    - Soon items remain links and include visible text `Soon`.
    - `strip_known_prefixes/1` handles `/coming/:screen` and existing dashboard route suffixes.
  </behavior>
  <action>Update `ScoriaWeb.DashboardNav` so `@groups` uses the exact D-11 group order and labels. Keep Home path `/`; internal key may remain `:live_ops` for Home. Add `aliases` lists to every item for later palette filtering. Add `soon?: true` and `stub_slug` to the five reserved items only. Add public helpers `stub_screens/0`, `stub_screen/1`, `stub_key_for_slug/1`, and `active_key/2`; keep `active_key/1` as a compatibility wrapper. Update `on_mount/4` and `assign_base/3` to call `active_key(socket.view, params)`. Add `ScoriaWeb.WorkflowLive.Index => :runs` and coming-soon active lookup by `screen` param. Update `derive_base/2` and `strip_known_prefixes/1` so `/coming/replay-playground` resolves the same `@scoria_base` as other dashboard pages. Update `app.html.heex` to render a visible `Soon` text badge for `item[:soon?] == true`; do not make stubs disabled and do not add a duplicate nav group. Enforce D-12 by keeping Connectors only in Configure and by not adding any second connector noun under Operate or Improve.</action>
  <acceptance_criteria>
    - Source assertion: `dashboard_nav.ex` contains `label: "Configure"` and `ScoriaWeb.WorkflowLive.Index`.
    - Source assertion: the five stub slugs appear exactly once in `dashboard_nav.ex`.
    - Source assertion: `app.html.heex` contains the visible text `Soon` inside nav item rendering.
    - Behavior assertion: DashboardNav tests pass under `/coming/:screen` and existing route suffixes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/dashboard_nav_test.exs test/scoria_web/ds06_drift_guard_test.exs</automated>
  </verify>
  <done>The sidebar model is grouped, Home is relabeled, Connectors is Configure, stubs are represented honestly, and active keys cover every current routed view plus coming-soon metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL to nav active state | `params["screen"]` influences which coming-soon nav item is active. |
| host mount path to dashboard links | Current URI is parsed to derive `@scoria_base` for embedded apps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Spoofing | coming-soon active key | mitigate | Active key for `ScoriaWeb.ComingSoonLive` is derived only by allowlisted stub slugs from `stub_key_for_slug/1`; unknown slugs return nil. |
| T-13-02-02 | Tampering | mount-base derivation | mitigate | Extend the existing suffix stripping tests to include `/scoria/coming/:screen`; do not hardcode `/scoria` in nav paths. |
| T-13-02-03 | Information disclosure | nav labels | accept | Nav exposes only screen names already visible in the dashboard IA; no user data appears in nav metadata. |
| T-13-02-SC | Tampering | package supply chain | mitigate | No new dependencies or package installs; command palette dependencies are explicitly forbidden by D-05. |
</threat_model>

<verification>
Run `mix test test/scoria_web/dashboard_nav_test.exs test/scoria_web/ds06_drift_guard_test.exs`.
</verification>

<success_criteria>
- IA-01 group order and active-state coverage are proven by automated tests.
- IA-06 reserved items exist in the nav as clickable Soon links, but no routes or fake pages are introduced in this plan.
- No raw palette regression is introduced.
</success_criteria>

<output>
Create `.planning/phases/13-orientation-spine-ia/13-02-SUMMARY.md` when done.
</output>
