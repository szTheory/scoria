---
phase: 13-orientation-spine-ia
plan: "08"
type: execute
wave: 5
depends_on: ["13-05", "13-07"]
files_modified:
  - lib/scoria_web/live/workflow_live/show.ex
  - lib/scoria_web/live/prompt_live/release_workbench_live.ex
  - test/scoria_web/live/workflow_live_test.exs
  - test/scoria_web/live/prompt_live_test.exs
  - test/support/ds06_baseline.txt
autonomous: true
requirements: [IA-05]
must_haves:
  truths:
    - "Run show exposes next-step verbs for replay, dataset promotion, linked incidents, and prompt context."
    - "Prompt release workbench exposes eval and baseline-run links."
    - "Quality-loop threading remains a flat verb cluster, not a persistent stepper."
  artifacts:
    - path: "lib/scoria_web/live/workflow_live/show.ex"
      provides: "run egress links through the quality loop"
    - path: "lib/scoria_web/live/prompt_live/release_workbench_live.ex"
      provides: "release workbench egress links to evals and baseline runs"
  key_links:
    - from: "Workflow Run"
      to: "Prompt Release / dataset promotion / replay"
      via: "next-step verb cluster"
      pattern: "Replay run|Promote span to dataset|Open prompt"
---

<objective>
Thread run and prompt release object pages forward through the quality loop.

Purpose: Once an operator is on an object page, the next useful evidence action should be visible without a rigid workflow diagram.
Output: Tested next-step verb clusters on Run show and Prompt Release Workbench.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-orientation-spine-ia/13-CONTEXT.md
@.planning/phases/13-orientation-spine-ia/13-RESEARCH.md
@.planning/phases/13-orientation-spine-ia/13-VALIDATION.md
@.planning/phases/13-orientation-spine-ia/13-PATTERNS.md
@.planning/phases/13-orientation-spine-ia/13-UI-SPEC.md
@lib/scoria_web/live/workflow_live/show.ex
@lib/scoria_web/live/prompt_live/release_workbench_live.ex
@test/scoria_web/live/workflow_live_test.exs
@test/scoria_web/live/prompt_live_test.exs
@test/support/ds06_baseline.txt
</context>

<must_haves>
- D-19: Run show gets `Replay run`, `Promote span to dataset`, `Open incident` if linked, and `Open prompt`.
- D-19: Release Workbench gets `View eval results` and `View baseline runs`.
- D-18: Do not add a persistent loop rail, stepper, or wizard state.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add object egress threading tests</name>
  <files>test/scoria_web/live/workflow_live_test.exs, test/scoria_web/live/prompt_live_test.exs</files>
  <read_first>
    @test/scoria_web/live/workflow_live_test.exs
    @test/scoria_web/live/prompt_live_test.exs
    @lib/scoria_web/live/workflow_live/show.ex
    @lib/scoria_web/live/prompt_live/release_workbench_live.ex
    @.planning/phases/13-orientation-spine-ia/13-VALIDATION.md
    @.planning/phases/13-orientation-spine-ia/13-UI-SPEC.md
  </read_first>
  <behavior>
    - Run show renders `Replay run`, `Promote span to dataset`, and `Open prompt` when backing data exists.
    - Run show renders `Open incident` only when linked incident data exists.
    - Prompt release workbench renders `View eval results` and `View baseline runs`.
    - Tests assert absence of `stepper`, `wizard`, and `current step`.
  </behavior>
  <action>Add LiveView tests for the next-step verb clusters. In `workflow_live_test.exs`, assert visible links/buttons named exactly `Replay run`, `Promote span to dataset`, `Open prompt`, and conditionally `Open incident` for a run fixture with linked incident context. In `prompt_live_test.exs`, assert visible links named exactly `View eval results` and `View baseline runs`. Assert the rendered HTML does not contain `stepper`, `wizard`, or `current step`.</action>
  <acceptance_criteria>
    - Source assertion: workflow tests contain all four run verbs.
    - Source assertion: prompt tests contain both release workbench verbs.
    - Behavior assertion: tests reject stepper/wizard/current-step language.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/workflow_live_test.exs test/scoria_web/live/prompt_live_test.exs --max-failures 1</automated>
  </verify>
  <done>Object egress tests fail on missing next-step verb clusters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement run and release workbench next-step clusters</name>
  <files>lib/scoria_web/live/workflow_live/show.ex, lib/scoria_web/live/prompt_live/release_workbench_live.ex, test/scoria_web/live/workflow_live_test.exs, test/scoria_web/live/prompt_live_test.exs, test/support/ds06_baseline.txt</files>
  <read_first>
    @lib/scoria_web/live/workflow_live/show.ex
    @lib/scoria_web/live/prompt_live/release_workbench_live.ex
    @test/scoria_web/live/workflow_live_test.exs
    @test/scoria_web/live/prompt_live_test.exs
    @test/scoria_web/ds06_drift_guard_test.exs
    @test/support/ds06_baseline.txt
    @.planning/phases/13-orientation-spine-ia/13-PATTERNS.md
    @.planning/phases/13-orientation-spine-ia/13-CONTEXT.md
  </read_first>
  <behavior>
    - Verb clusters are flat lists of links/buttons and do not imply a current workflow stage.
    - Existing replay and promote behavior remains wired to current events or routes.
    - New links preserve `@scoria_base` and add `?from=run:<id>` or `?from=prompt:<id>` where useful.
  </behavior>
  <action>In `WorkflowLive.Show`, add a semantic next-step cluster near the object header or first evidence panel. Use exact labels `Replay run`, `Promote span to dataset`, `Open incident`, and `Open prompt`. Wire `Replay run` and `Promote span to dataset` to existing replay/promote behavior or existing links; do not create new backend capability. Render `Open incident` only when linked incident data is available from existing assigns/evidence. Render `Open prompt` only when prompt/template context exists. In `ReleaseWorkbenchLive`, add links named `View eval results` and `View baseline runs` to existing eval and workflow surfaces, preserving dashboard base and adding `from=prompt:<id>` context. Use semantic classes and existing UI components. If raw palette counts drop in either baselined file, regenerate `test/support/ds06_baseline.txt` in the same change.</action>
  <acceptance_criteria>
    - Source assertion: `workflow_live/show.ex` contains `Replay run`, `Promote span to dataset`, `Open prompt`, and conditional `Open incident`.
    - Source assertion: `release_workbench_live.ex` contains `View eval results` and `View baseline runs`.
    - Source assertion: no `stepper`, `wizard`, or `current step` string is added.
    - CLI assertion: workflow, prompt, and DS-06 tests pass.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/workflow_live_test.exs test/scoria_web/live/prompt_live_test.exs test/scoria_web/ds06_drift_guard_test.exs</automated>
  </verify>
  <done>Run and release object pages expose the required next-step verbs without adding fake capability or a loop stepper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow/prompt records to links | Stored IDs and optional incident/prompt context become navigation targets. |
| operator actions to existing events | Replay/promote controls may invoke existing LiveView events. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08-01 | Elevation of privilege | next-step actions | mitigate | Do not add new promote/release/replay backend actions; only surface existing controls and routes. |
| T-13-08-02 | Tampering | query context | mitigate | Add `from=run:` or `from=prompt:` from existing object IDs only; destination allowlist from Plan 13-05 handles display. |
| T-13-08-03 | Repudiation | action labels | accept | Labels expose existing actions; audit semantics remain owned by existing workflow/eval code paths. |
| T-13-08-SC | Tampering | package supply chain | mitigate | No dependencies or external integrations. |
</threat_model>

<verification>
Run `mix test test/scoria_web/live/workflow_live_test.exs test/scoria_web/live/prompt_live_test.exs test/scoria_web/ds06_drift_guard_test.exs`.
</verification>

<success_criteria>
- Run show exposes replay, promote, incident, and prompt next-step verbs where backed by existing data.
- Release Workbench links to eval results and baseline runs.
- No fake backend capability, stepper, or loop rail is introduced.
- DS-06 remains green with baseline reconciled if needed.
</success_criteria>

<output>
Create `.planning/phases/13-orientation-spine-ia/13-08-SUMMARY.md` when done.
</output>
