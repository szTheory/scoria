---
phase: 13-orientation-spine-ia
plan: "06"
type: execute
wave: 3
depends_on: ["13-01", "13-02", "13-04"]
files_modified:
  - lib/scoria_web/dashboard_nav.ex
  - lib/scoria_web/components/layouts/app.html.heex
  - lib/scoria_web/components/layouts.ex
  - assets/js/scoria.js
  - test/scoria_web/dashboard_nav_test.exs
  - test/scoria_web/live/orchestrator_live_test.exs
autonomous: false
requirements: [IA-04, IA-06]
must_haves:
  truths:
    - "Power users can open a dashboard-scoped command palette with Cmd+K or Ctrl+K."
    - "Palette Navigate rows are derived from DashboardNav groups and include stubs labeled Soon."
    - "Keyboard shortcuts are discoverable through visible topbar hint, palette row kbd chips, and `?` overlay."
  artifacts:
    - path: "assets/js/scoria.js"
      provides: "CommandPalette and RecordRecentObject hooks"
    - path: "lib/scoria_web/components/layouts/app.html.heex"
      provides: "palette dialog, topbar affordance, shortcut overlay"
  key_links:
    - from: "layouts/app.html.heex"
      to: "DashboardNav.groups/0"
      via: "command metadata helpers"
      pattern: "command"
---

<objective>
Add the dashboard-scoped command palette, recents, and keyboard shortcuts.

Purpose: Returning power users need fast navigation and actions without adding backend persistence or socket traffic while typing.
Output: Server-rendered command data and vanilla JS hooks for local filtering, recents, focus management, `Cmd/Ctrl+K`, `?`, and `g` chords.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-orientation-spine-ia/13-CONTEXT.md
@.planning/phases/13-orientation-spine-ia/13-RESEARCH.md
@.planning/phases/13-orientation-spine-ia/13-VALIDATION.md
@.planning/phases/13-orientation-spine-ia/13-PATTERNS.md
@.planning/phases/13-orientation-spine-ia/13-UI-SPEC.md
@lib/scoria_web/dashboard_nav.ex
@lib/scoria_web/components/layouts/app.html.heex
@lib/scoria_web/components/layouts.ex
@lib/scoria_web/ui.ex
@assets/js/scoria.js
@test/scoria_web/dashboard_nav_test.exs
@test/scoria_web/live/orchestrator_live_test.exs
</context>

<must_haves>
- D-05: Server renders full command list into DOM; one vanilla hook handles open/filter/keyboard-nav; no socket traffic while typing; no npm deps; no LiveComponent round trips.
- D-06: Empty query sections are Recent max 5, Navigate all screens from DashboardNav groups with stubs labeled Soon, and Actions `Toggle theme`, `Keyboard shortcuts`, `Copy current page URL`.
- D-07: Recents storage key is `scoria:recents:<mount-base>`, cap 8 stored and 5 shown.
- D-08 and D-09: Scope listeners to dashboard layout, ignore editable/IME events, implement chords `g h`, `g a`, `g r`, `g i`, `g c`, `g q`, `g e`, `g p`.
- D-10: Accessible dialog/listbox, focus trap, focus restoration, opacity-only motion, reduced-motion support, exact empty-state copy.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add command metadata and server-rendered palette tests</name>
  <files>test/scoria_web/dashboard_nav_test.exs, test/scoria_web/live/orchestrator_live_test.exs</files>
  <read_first>
    @test/scoria_web/dashboard_nav_test.exs
    @test/scoria_web/live/orchestrator_live_test.exs
    @lib/scoria_web/dashboard_nav.ex
    @lib/scoria_web/components/layouts/app.html.heex
    @.planning/phases/13-orientation-spine-ia/13-VALIDATION.md
    @.planning/phases/13-orientation-spine-ia/13-UI-SPEC.md
  </read_first>
  <behavior>
    - DashboardNav command metadata includes Navigate rows for all nav items and Actions rows for the three static actions.
    - `/scoria` server-rendered shell includes `Open command palette`, `Search screens, recent objects, and actions`, `Keyboard shortcuts`, `role="dialog"`, and `role="listbox"`.
    - Stub commands render visible `Soon`.
  </behavior>
  <action>Extend `dashboard_nav_test.exs` with assertions for `DashboardNav.command_sections/1` returning section labels `Navigate` and `Actions`, all nav item labels from `groups/0`, aliases, and the three static actions `Toggle theme`, `Keyboard shortcuts`, `Copy current page URL`. Extend `orchestrator_live_test.exs` to assert the layout renders a topbar button/title `Open command palette`, search input hint `Search screens, recent objects, and actions`, shortcut overlay title `Keyboard shortcuts`, the exact palette empty-state copy `No matches. The palette covers screens, recent objects, and actions — full object search lands in a later release.`, `role="dialog"`, `aria-modal="true"`, `role="listbox"`, and visible `Soon` for stub rows.</action>
  <acceptance_criteria>
    - Source assertion: dashboard nav tests contain `command_sections`, `Toggle theme`, `Copy current page URL`, and `Replay Playground`.
    - Source assertion: orchestrator tests contain `Open command palette`, `Keyboard shortcuts`, `No matches. The palette covers screens, recent objects, and actions — full object search lands in a later release.`, and `role="listbox"`.
    - Behavior assertion: tests prove stubs appear in palette metadata as `Soon`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/dashboard_nav_test.exs test/scoria_web/live/orchestrator_live_test.exs --max-failures 1</automated>
  </verify>
  <done>Tests pin command metadata and server-rendered palette/shortcut markup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement palette metadata, layout markup, and vanilla hooks</name>
  <files>lib/scoria_web/dashboard_nav.ex, lib/scoria_web/components/layouts/app.html.heex, lib/scoria_web/components/layouts.ex, assets/js/scoria.js, test/scoria_web/dashboard_nav_test.exs, test/scoria_web/live/orchestrator_live_test.exs</files>
  <read_first>
    @lib/scoria_web/dashboard_nav.ex
    @lib/scoria_web/components/layouts/app.html.heex
    @lib/scoria_web/components/layouts.ex
    @lib/scoria_web/ui.ex
    @assets/js/scoria.js
    @test/scoria_web/dashboard_nav_test.exs
    @test/scoria_web/live/orchestrator_live_test.exs
    @.planning/phases/13-orientation-spine-ia/13-PATTERNS.md
    @.planning/phases/13-orientation-spine-ia/13-UI-SPEC.md
  </read_first>
  <behavior>
    - `CommandPalette` hook has `mounted` and `destroyed` lifecycle cleanup.
    - Filtering is case-insensitive substring over label and aliases; no `pushEvent` is called while typing.
    - Shortcuts do not fire from `input`, `textarea`, `select`, `button`, `[contenteditable]`, or IME composition.
    - Recents are stored in `localStorage` under `scoria:recents:<mount-base>`.
  </behavior>
  <action>Add command metadata helpers to `DashboardNav` using the existing nav SSOT: Navigate rows for every item in `groups/0`, with `soon?: true` preserved; Actions rows for `Toggle theme`, `Keyboard shortcuts`, and `Copy current page URL`; chord metadata for `g h`, `g a`, `g r`, `g i`, `g c`, `g q`, `g e`, and `g p`. In `layouts.ex`, expose a helper that returns command sections for the current `@scoria_base`. In `app.html.heex`, replace the utility-only topbar cluster with a visible command button labeled/title `Open command palette`, visible kbd hint `Cmd K` or `Ctrl K`, the `<.command_palette>` component from Plan 13-01, and a shortcut overlay with title `Keyboard shortcuts`. In `assets/js/scoria.js`, add `Hooks.CommandPalette` and `Hooks.RecordRecentObject` following the existing IIFE and cleanup pattern. Implement local filtering, active row movement with ArrowUp/ArrowDown, Enter activation, Escape close, scrim close, focus trap, focus restoration, `?` overlay, 1.5 second g-chord timeout, and recents cap 8 stored/5 rendered. Do not add npm dependencies and do not call `pushEvent` from typing/filter handlers.</action>
  <acceptance_criteria>
    - Source assertion: `assets/js/scoria.js` contains `Hooks.CommandPalette`, `Hooks.RecordRecentObject`, `scoria:recents:`, `isComposing`, `contenteditable`, and `1500`.
    - Source assertion: `assets/js/scoria.js` does not contain `pushEvent` inside CommandPalette filtering code.
    - Source assertion: `package.json`, `priv/dev/package.json`, and lockfiles are unchanged by this plan.
    - CLI assertion: `node --check assets/js/scoria.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/dashboard_nav_test.exs test/scoria_web/live/orchestrator_live_test.exs test/scoria_web/ds06_drift_guard_test.exs && node --check assets/js/scoria.js</automated>
  </verify>
  <done>Palette and shortcuts are server-rendered, locally filtered, accessible in markup, dashboard-scoped in JS, and syntax/test clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser-check command palette keyboard behavior</name>
  <files>assets/js/scoria.js, lib/scoria_web/components/layouts/app.html.heex</files>
  <read_first>
    @assets/js/scoria.js
    @lib/scoria_web/components/layouts/app.html.heex
    @.planning/phases/13-orientation-spine-ia/13-VALIDATION.md
    @.planning/phases/13-orientation-spine-ia/13-UI-SPEC.md
  </read_first>
  <action>After Task 2 automated verification passes, run the manual browser lane below against the dashboard. This checkpoint validates browser-only focus trap, local filtering, shortcut timing, and event scoping that LiveViewTest cannot fully observe.</action>
  <what-built>Server-rendered command palette, shortcut overlay, local filtering, recents, and g-chords.</what-built>
  <how-to-verify>
    1. Start the dev dashboard and open `/scoria`.
    2. Press `Cmd+K` on macOS or `Ctrl+K` elsewhere; expect the palette to open and focus the search input.
    3. Type `runs`; expect Navigate results to include Runs and hide empty sections.
    4. Press Escape; expect the palette to close and focus to return to the opener.
    5. Press `?`; expect the `Keyboard shortcuts` overlay unless focus is inside a form input.
    6. Press `g h`, `g a`, `g r`, `g i`, `g c`, `g q`, `g e`, `g p`; expect navigation to Home, Approvals, Runs, Incidents, Connectors, Review Queue, Eval Workbench, Prompt Registry.
  </how-to-verify>
  <acceptance_criteria>
    - Manual behavior: Cmd+K or Ctrl+K opens the palette and focuses the search input.
    - Manual behavior: Escape closes palette/overlay and restores focus to the opener.
    - Manual behavior: `runs` filters Navigate results to Runs.
    - Manual behavior: g-chords navigate to the exact destinations listed above.
    - Manual behavior: shortcuts do not fire while focus is inside a form field.
  </acceptance_criteria>
  <verify>
    <automated>node --check assets/js/scoria.js</automated>
    <human-check>Complete the six browser steps in `how-to-verify`.</human-check>
  </verify>
  <done>Human verifier confirms the command palette keyboard lane or reports concrete mismatches for follow-up.</done>
  <resume-signal>Type `approved` or describe the mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser keyboard to dashboard navigation | Global keydown listeners can affect host app behavior if not scoped and cleaned up. |
| localStorage to palette UI | Recent objects are browser-local and user-controlled. |
| command rows to navigation | Command activation changes `window.location` to dashboard paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06-01 | Elevation of privilege | command actions | mitigate | Palette commands include navigation plus `Toggle theme`, `Keyboard shortcuts`, and `Copy current page URL` only; no approve, deny, release, promote, delete, or destructive command. |
| T-13-06-02 | Denial of service | global key listeners | mitigate | Bind listeners in `mounted`, remove in `destroyed`, ignore editable targets and IME composition, and preventDefault only for owned keys. |
| T-13-06-03 | Information disclosure | localStorage recents | mitigate | Store only object label, path, kind, and ID that are already visible on object pages; cap at 8; no server persistence. |
| T-13-06-04 | Tampering | command destinations | mitigate | Navigate rows are derived from allowlisted `DashboardNav.groups/0`; no arbitrary command path is read from user input. |
| T-13-06-SC | Tampering | package supply chain | mitigate | No npm package or registry component may be added; `node --check` validates existing plain JS only. |
</threat_model>

<verification>
Automated: `mix test test/scoria_web/dashboard_nav_test.exs test/scoria_web/live/orchestrator_live_test.exs test/scoria_web/ds06_drift_guard_test.exs && node --check assets/js/scoria.js`.
Human: Complete Task 3 keyboard lane.
</verification>

<success_criteria>
- Palette opens with Cmd/Ctrl+K and closes with Escape.
- Navigate rows come from the nav SSOT and include Soon stubs.
- `?` overlay and g-chords are discoverable and functional.
- Typing does not send socket events.
- No new JS dependency is added.
</success_criteria>

<output>
Create `.planning/phases/13-orientation-spine-ia/13-06-SUMMARY.md` when done.
</output>
