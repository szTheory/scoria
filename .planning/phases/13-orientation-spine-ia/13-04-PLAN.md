---
phase: 13-orientation-spine-ia
plan: "04"
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - lib/scoria_web/live/orchestrator_live.ex
  - lib/scoria_web/operator_surface.ex
  - test/scoria_web/live/orchestrator_live_test.exs
  - test/support/ds06_baseline.txt
autonomous: true
requirements: [IA-02]
must_haves:
  truths:
    - "D-01: A newcomer landing at `/scoria` sees Home on the existing `/` route; no `/home`, `/live_ops`, or base-derivation change is added."
    - "D-02: The page order is exact identity line, needs-attention strip, then the existing live run stream."
    - "D-03: Day-0 empty state uses the locked first-run copy for no traces."
    - "D-04: Only nonzero actionable attention cards render; all-clear collapses to one exact sentence and no vanity dashboard appears."
    - "D-12: Connector health appears as an in-page attention link, not a duplicate nav noun."
  artifacts:
    - path: "lib/scoria_web/live/orchestrator_live.ex"
      provides: "Status Home at existing `/` route"
    - path: "lib/scoria_web/operator_surface.ex"
      provides: "read-only status summary helpers"
  key_links:
    - from: "lib/scoria_web/live/orchestrator_live.ex"
      to: "lib/scoria_web/operator_surface.ex"
      via: "assign_async status summary"
      pattern: "assign_async"
---

<objective>
Turn `/` into a Status Home without splitting the Live Ops route.

Purpose: The first screen should state what Scoria does, surface what needs attention now, and preserve the existing live trace stream for power users.
Output: `OrchestratorLive` page title/copy/attention strip with read-only non-blocking summary data and tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-orientation-spine-ia/13-CONTEXT.md
@.planning/phases/13-orientation-spine-ia/13-RESEARCH.md
@.planning/phases/13-orientation-spine-ia/13-VALIDATION.md
@.planning/phases/13-orientation-spine-ia/13-PATTERNS.md
@.planning/phases/13-orientation-spine-ia/13-UI-SPEC.md
@lib/scoria_web/live/orchestrator_live.ex
@lib/scoria_web/operator_surface.ex
@lib/scoria_web/ui.ex
@test/scoria_web/live/orchestrator_live_test.exs
@test/support/ds06_baseline.txt
</context>

<must_haves>
- D-01: `ScoriaWeb.OrchestratorLive` stays mounted at `/`; no `/home`, no `/live_ops`, and no route/base-derivation change for Home.
- D-02: Page order is title `Home`, exact identity line, needs-attention strip, live run stream.
- D-03: Day-0 stream empty copy is exactly `No traces yet. Run your first chat response or MCP request to see the run tree.`
- D-04: Counts use non-blocking query semantics where they are not already cheap; attention strip is not a chart dashboard.
- D-12: Connector health appears as a Home attention-card link to Connectors when actionable, not as a duplicate nav noun.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Status Home LiveView tests</name>
  <files>test/scoria_web/live/orchestrator_live_test.exs</files>
  <read_first>
    @test/scoria_web/live/orchestrator_live_test.exs
    @lib/scoria_web/live/orchestrator_live.ex
    @lib/scoria_web/operator_surface.ex
    @.planning/phases/13-orientation-spine-ia/13-CONTEXT.md
    @.planning/phases/13-orientation-spine-ia/13-VALIDATION.md
    @.planning/phases/13-orientation-spine-ia/13-UI-SPEC.md
  </read_first>
  <behavior>
    - `/scoria` renders `Home` and the exact identity sentence.
    - Empty tenant renders all-clear copy and day-0 empty stream copy.
    - Pending approval, open incident, degraded connector, and review queue states render only when count is nonzero.
    - `traces-list` remains present and still accepts streamed traces.
  </behavior>
  <action>Extend `test/scoria_web/live/orchestrator_live_test.exs` with focused tests for Status Home. Assert exact strings: `Home`, `Every AI run in this app, traced. Approve tools, triage incidents, and gate prompt releases from here.`, `Nothing needs attention. 0 approvals pending, 0 open incidents.`, and `No traces yet. Run your first chat response or MCP request to see the run tree.` Add nonzero-state assertions for link text `Review approvals`, `Open incidents`, `View connector health`, and `Review flagged runs`. Keep the existing stream test and assert `id="traces-list"` still appears.</action>
  <acceptance_criteria>
    - Source assertion: orchestrator tests contain the exact identity line and all-clear copy.
    - Source assertion: orchestrator tests contain all four CTA labels: `Review approvals`, `Open incidents`, `View connector health`, `Review flagged runs`.
    - Behavior assertion: the existing streaming trace test remains in the file and still asserts streamed span content.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/orchestrator_live_test.exs --max-failures 1</automated>
  </verify>
  <done>Status Home tests describe the exact route, copy, conditional cards, empty state, and stream preservation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Status Home summary and attention strip</name>
  <files>lib/scoria_web/live/orchestrator_live.ex, lib/scoria_web/operator_surface.ex, test/scoria_web/live/orchestrator_live_test.exs, test/support/ds06_baseline.txt</files>
  <read_first>
    @lib/scoria_web/live/orchestrator_live.ex
    @lib/scoria_web/operator_surface.ex
    @lib/scoria_web/ui.ex
    @test/scoria_web/live/orchestrator_live_test.exs
    @test/scoria_web/ds06_drift_guard_test.exs
    @test/support/ds06_baseline.txt
    @.planning/phases/13-orientation-spine-ia/13-PATTERNS.md
    @.planning/phases/13-orientation-spine-ia/13-UI-SPEC.md
  </read_first>
  <behavior>
    - `assign(:page_title, "Home")` replaces the old `Live Ops` title.
    - Attention cards render only when their count is greater than zero.
    - Query failures hide that card instead of rendering error cards.
    - Existing PubSub subscription and trace stream remain unchanged.
  </behavior>
  <action>In `OperatorSurface`, add a read-only `status_home_summary/1` helper that returns maps for approvals, incidents, connectors, and reviews. Use existing helpers `pending_approval_count/1`, `fleet_summary/1`, and `incidents_summary/1`; add review queue count with `Scoria.Eval.list_review_queue(%{"review_status" => "pending"})`. Rescue failures to zero-valued maps. In `OrchestratorLive`, keep the route and PubSub behavior, set `page_title` to `Home`, and load the status summary with `assign_async(:status_home, fn -> {:ok, %{status_home: OperatorSurface.status_home_summary(tenant_id)}} end)`. Replace the old top task-card and `ops-summary` vanity cards with the exact identity line, nonzero `<.attention_card>` entries, all-clear copy, day-0 stream empty copy, and unchanged `traces-list`. If reducing raw palette counts in `orchestrator_live.ex`, regenerate `test/support/ds06_baseline.txt` using the DS-06 scanner pattern from 12-05 so the stale-baseline test stays green.</action>
  <acceptance_criteria>
    - Source assertion: no new route string `/home` or `/live_ops` appears in `router.ex` or `orchestrator_live.ex`.
    - Source assertion: `orchestrator_live.ex` contains `assign(:page_title, "Home")` or an equivalent literal Home assignment.
    - Source assertion: `orchestrator_live.ex` contains the exact identity line and day-0 empty string.
    - Behavior assertion: empty state renders all-clear and no attention card links.
    - CLI assertion: DS-06 passes with any baseline reduction committed.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/orchestrator_live_test.exs test/scoria_web/ds06_drift_guard_test.exs</automated>
  </verify>
  <done>`/scoria` is a Status Home with exact copy, conditional attention cards, non-blocking summary data, and the existing trace stream intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database to Home summary | Status counts come from read models and may fail or contain tenant-scoped data. |
| PubSub to LiveView stream | Existing trace events continue to update the page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | Information disclosure | `OperatorSurface.status_home_summary/1` | mitigate | Use existing tenant ID from session/params; do not query cross-tenant data; preserve existing helper scoping. |
| T-13-04-02 | Denial of service | Home summary queries | mitigate | Use assign_async-style loading and rescue-to-zero helpers; do not block trace stream mount on optional counts. |
| T-13-04-03 | Repudiation | attention cards | accept | Cards are navigation affordances only; no decisions or mutations occur in this plan. |
| T-13-04-SC | Tampering | package supply chain | mitigate | No new packages; implementation uses existing LiveView, OperatorSurface, and UI components. |
</threat_model>

<verification>
Run `mix test test/scoria_web/live/orchestrator_live_test.exs test/scoria_web/ds06_drift_guard_test.exs`.
</verification>

<success_criteria>
- `/scoria` remains the only Home route and `OrchestratorLive` stays mounted there.
- Exact Status Home copy renders.
- Attention strip is conditional and action-oriented, with no charts/sparklines.
- Live trace stream still works.
</success_criteria>

<output>
Create `.planning/phases/13-orientation-spine-ia/13-04-SUMMARY.md` when done.
</output>
