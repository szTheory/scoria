---
phase: 54.1-wire-reqllm-jido-adapters-at-boot-reconcile-changelog
plan: 02
subsystem: docs
tags: [changelog, exdoc, guides, mix.exs, release_preview, package_surface, reqllm, jido]

requires:
  - phase: 54.1-01 (parallel wave-1 plan)
    provides: safe_attach_observe_req_llm/0, safe_attach_observe_jido/0 boot-attach helpers this plan's CHANGELOG/guide text documents
provides:
  - Amended CHANGELOG [Unreleased] Added block distinguishing persist-vs-join for the ReqLLM/Jido adapters (COMPAT-01/D-03)
  - New guides/capabilities/llm-and-tool-adapters.md adopter capability guide (D-05, SPAN-01, EVENT-01)
  - New troubleshooting.md entry for standalone single-span traces
  - Guide registered across all six mirrored docs-source inventory sites (mix.exs x3, release_preview.ex, both surface tests)
affects: [54-docs-accuracy-conformance-check, future-changelog-work]

tech-stack:
  added: []
  patterns: [persist-vs-join documentation framing for telemetry-adapter spans, mirrored canonical-guides inventory registration, non-autolinking plain-prose references to filtered ExDoc modules]

key-files:
  created:
    - guides/capabilities/llm-and-tool-adapters.md
  modified:
    - CHANGELOG.md
    - guides/troubleshooting.md
    - mix.exs
    - lib/mix/tasks/scoria.release_preview.ex
    - test/scoria/package_surface_test.exs
    - test/mix/tasks/scoria.release_preview_test.exs
    - lib/scoria/knowledge.ex

key-decisions:
  - "Split the CHANGELOG's single adapter+default-pipeline paragraph into two paragraphs so the ReqLLM/Jido persist-vs-join claim could be amended without touching the still-true default-span-pipeline claim (D-03 scope guard)."
  - "Fixed pre-existing, baseline-broken `mix docs --warnings-as-errors` filtered-module autolink warnings across the whole CHANGELOG Unreleased section plus lib/scoria/knowledge.ex — required for the plan's own literal verification gate, documented as Rule 3 deviations."

requirements-completed: [SPAN-01, COMPAT-01, EVENT-01]

coverage:
  - id: D1
    description: "CHANGELOG [Unreleased] Added block distinguishes adapter-span PERSIST (zero host wiring) from trace JOIN (requires host-forwarded trace_id/parent_id/tenant_id), links the new guide, no ALL-CAPS ORPHAN, default-pipeline claim untouched"
    requirement: "COMPAT-01"
    verification:
      - kind: unit
        ref: "mix test test/scoria/application_test.exs"
        status: pass
      - kind: other
        ref: "grep -ic 'trace_id\\|join' CHANGELOG.md >=1; grep -c 'guides/capabilities/llm-and-tool-adapters.md' CHANGELOG.md >=1; grep -c ORPHAN CHANGELOG.md ==0"
        status: pass
    human_judgment: false
  - id: D2
    description: "New guides/capabilities/llm-and-tool-adapters.md covers boot-attach behavior, persist-vs-join, metadata forwarding, ownership-boundary tie-in, and the jido asymmetry, with no autolinking backtick references to filtered modules"
    requirement: "SPAN-01"
    verification:
      - kind: other
        ref: "test -f guides/capabilities/llm-and-tool-adapters.md; grep spot-checks (persist/trace_id/forward/jido/enabled: false) all >=1; filtered-module backtick grep ==0"
        status: pass
      - kind: other
        ref: "MIX_ENV=dev mix docs --warnings-as-errors"
        status: pass
    human_judgment: false
  - id: D3
    description: "guides/troubleshooting.md gains a standalone single-span-trace entry linking the new guide (edit only, no registration change)"
    requirement: "EVENT-01"
    verification:
      - kind: other
        ref: "grep -ic 'single-span trace\\|standalone' guides/troubleshooting.md >=1; grep -c llm-and-tool-adapters guides/troubleshooting.md >=1"
        status: pass
    human_judgment: false
  - id: D4
    description: "New guide registered across all six mirrored canonical-guides inventory sites; strict-equality surface tests and release_preview pass; docs build clean"
    verification:
      - kind: unit
        ref: "mix test test/scoria/package_surface_test.exs test/mix/tasks/scoria.release_preview_test.exs test/scoria/hex_consumer_contract_test.exs"
        status: pass
      - kind: other
        ref: "grep -rc 'guides/capabilities/llm-and-tool-adapters.md' mix.exs ==3; MIX_ENV=dev mix docs --warnings-as-errors"
        status: pass
    human_judgment: false

duration: ~15min
completed: 2026-07-18
status: complete
---

# Phase 54.1 Plan 02: Docs Reconciliation Summary

**Amended the CHANGELOG's ReqLLM/Jido adapter claim to distinguish span persistence from trace-join, added a new `guides/capabilities/llm-and-tool-adapters.md` adopter guide plus a troubleshooting entry, and registered the guide across all six mirrored docs-source inventory sites — while forward-fixing a pre-existing, baseline-broken `mix docs --warnings-as-errors` gate along the way.**

## Performance

- **Duration:** ~15 min
- **Completed:** 2026-07-18
- **Tasks:** 3
- **Files modified:** 7 (1 created, 6 modified)

## Accomplishments

- CHANGELOG `## [Unreleased]` "Added" block now has a dedicated adapter paragraph: ReqLLM/Jido spans **persist to Postgres with zero host wiring**, but only **join** the current workflow run's trace when the host forwards `trace_id`/`parent_id`/`tenant_id` — automatic inside `Scoria.Workflows.Runtime.execute_step/2`, manual otherwise. The separate, still-true default-span-pipeline claim (`emit_*_span/1` + custom `:telemetry.execute/3`) was left byte-consistent with `application_test.exs:6-7`.
- New one-page adopter guide `guides/capabilities/llm-and-tool-adapters.md` covers: what auto-attaches at boot (handler ids, config gate, `already_exists` tolerance), the persist-vs-join distinction, how to forward metadata (pointing at the adapter moduledocs as SSOT), the ownership-boundary tie-in (trace lineage is a host-owned noun), and the jido-is-not-a-dependency asymmetry.
- `guides/troubleshooting.md` gained a "Why is my LLM/tool call a standalone single-span trace?" entry linking the new guide.
- The new guide is registered in all six mirrored inventory sites: `mix.exs` `docs_extras`, `docs_extra_groups` Capabilities group, and `package` `files:` (3 sites); `lib/mix/tasks/scoria.release_preview.ex` `@required_package_paths`; and both `@canonical_guides` lists (plus the Capabilities `assert_group_contains`) in `test/scoria/package_surface_test.exs` and `test/mix/tasks/scoria.release_preview_test.exs`.
- `MIX_ENV=dev mix docs --warnings-as-errors` now exits 0 (was already broken at the plan's starting commit — see Deviations).

## Task Commits

1. **Task 1: Amend the scoped CHANGELOG adapter claim — persist vs. join (COMPAT-01, D-03)** - `5f1eec28` (docs)
2. **Task 2: Add the LLM/tool adapters capability guide + troubleshooting orphan-span entry (D-05, SPAN-01, EVENT-01)** - `0f714a74` (docs)
3. **Task 3: Register the new guide across the mirrored docs-source inventory (D-05)** - `ea5daac1` (docs)

_No TDD tasks in this plan (docs-only)._

## Files Created/Modified

- `CHANGELOG.md` - Split adapter persist-vs-join claim into its own paragraph; linked the new guide; house-fixed stale filtered-module autolink spans throughout the Unreleased section
- `guides/capabilities/llm-and-tool-adapters.md` - New adopter capability guide (D-05 five-point outline)
- `guides/troubleshooting.md` - New standalone-single-span-trace entry linking the new guide
- `mix.exs` - Registered the new guide in `docs_extras`, `docs_extra_groups` Capabilities, and `package` `files:`
- `lib/mix/tasks/scoria.release_preview.ex` - Registered the new guide in `@required_package_paths`
- `test/scoria/package_surface_test.exs` - Registered the new guide in `@canonical_guides` and the Capabilities `assert_group_contains` list
- `test/mix/tasks/scoria.release_preview_test.exs` - Registered the new guide in `@canonical_guides`
- `lib/scoria/knowledge.ex` - Stripped two stale filtered-module backtick autolink spans in the `retrieve/2` moduledoc (pre-existing, unrelated to this plan's file scope but blocking the docs-build gate)

## Decisions Made

- Kept the plan's D-03 scope guard literally: the default-span-pipeline claim (`emit_*_span/1` + custom `:telemetry.execute/3`) is untouched in substance; only the adapter-specific sentence was pulled into its own new paragraph.
- Manually re-flowed the new CHANGELOG paragraph's line wrap so the literal phrase "zero host wiring" stays on one line (the file's existing hard-wrap convention had split it across lines even in the original text, which would have caused the plan's own `grep -ic 'zero host wiring'` acceptance check to fail).
- Referenced the adapter moduledocs, not this guide, as the SSOT for the exact metadata-key set, per the plan's explicit doc-rot-resistance instruction.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed pre-existing baseline `mix docs --warnings-as-errors` breakage unrelated to this plan's intent**
- **Found during:** Task 1 (running the plan's own acceptance-criteria docs-build check)
- **Issue:** `MIX_ENV=dev mix docs --warnings-as-errors` was already failing at the plan's starting commit (`06c1a983`), before any edit in this plan — 16 pre-existing "reference to a filtered module" / "documentation references function ... but it is hidden" warnings scattered across the entire CHANGELOG `## [Unreleased]` section (`Scoria.Observe.Adapters.ReqLLM`, `Scoria.Observe.Buffer`, `Scoria.Application`/`.start/2`, `Scoria.Observe.Telemetry.attach/0`/`/1`, `Scoria.Observe.Bounds.enforce/2`, `Scoria.Observe.Semconv.attribute_registry/0`, `Scoria.Observe.emit_event/1`) plus two in `lib/scoria/knowledge.ex`'s `retrieve/2` moduledoc (`Scoria.Knowledge.Embedder`, `Scoria.Observe.Semconv`). This blocked the plan's own literal `<verification>` gate (`MIX_ENV=dev mix docs --warnings-as-errors` exits 0), which is not scoped to only my new content.
- **Fix:** Applied the established Phase-50 D-50-DEF-01 house technique — stripped the backticks around each filtered/hidden module or function reference, converting them to plain prose (no code-span autolink target). No wording, meaning, or assertions changed; purely a markup fix.
- **Files modified:** `CHANGELOG.md`, `lib/scoria/knowledge.ex`
- **Verification:** `MIX_ENV=dev mix docs --warnings-as-errors` exits 0 after the fix (confirmed 0 warnings on a clean run); `mix test test/scoria/application_test.exs` and the full package/release_preview/hex-consumer suite still pass.
- **Committed in:** `5f1eec28` (Task 1 commit)

**2. [Rule 3 - Blocking] Fixed two new filtered-module autolink spans introduced by my own Task 2 guide/troubleshooting prose**
- **Found during:** Task 3 (running the combined docs-build gate after the guide became a registered ExDoc extra)
- **Issue:** My own new guide and troubleshooting entry used backtick spans around `Scoria.Observe` (bare) and `Scoria.Workflows.Runtime.execute_step/2` — both filtered modules, both would have failed `mix docs --warnings-as-errors` once the guide was registered as an extra.
- **Fix:** Stripped backticks around those two references (same house technique).
- **Files modified:** `guides/capabilities/llm-and-tool-adapters.md`, `guides/troubleshooting.md`
- **Verification:** `MIX_ENV=dev mix docs --warnings-as-errors` exits 0 after the fix.
- **Committed in:** `ea5daac1` (Task 3 commit)

---

**Total deviations:** 2 auto-fixed (both Rule 3 - blocking, both docs-build-only markup fixes, no semantic/assertion changes)
**Impact on plan:** Both fixes were required to satisfy the plan's own literal `<verification>` block. No scope creep beyond what was necessary to get `MIX_ENV=dev mix docs --warnings-as-errors` green; deviation 1 is pre-existing debt this plan happened to be the first to actually run the full gate against.

## Issues Encountered

- The repo's CHANGELOG.md uses a hard-wrapped (~80-col) prose convention, which silently split the exact phrase `"zero host wiring"` across a line break in both the pre-existing text and my first draft — defeating a literal `grep` acceptance check. Resolved by manually adjusting the wrap point in my new paragraph only (no change to the untouched default-pipeline paragraph's wrapping).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Docs surface now agrees with the boot-attach behavior Plan 01 (parallel, wave 1) implements: `Scoria.Application.start/2`'s new `safe_attach_observe_req_llm/0` / `safe_attach_observe_jido/0` helpers referenced in this plan's CHANGELOG/guide text.
- `mix docs --warnings-as-errors` is now genuinely green at this plan's HEAD (not just for the files this plan touched) — worth confirming this stays true after Plan 01 merges, since Plan 01's own files_modified were not visible in this worktree.
- Phase 54 (Docs Accuracy + Conformance Check, separate phase) can build on a now-clean docs baseline rather than inheriting the pre-existing filtered-module warnings this plan forward-fixed.

---
*Phase: 54.1-wire-reqllm-jido-adapters-at-boot-reconcile-changelog*
*Completed: 2026-07-18*

## Self-Check: PASSED

All 8 created/modified files verified present on disk; all 4 commit hashes (`5f1eec28`, `0f714a74`, `ea5daac1`, `73138ec4`) verified present in `git log --oneline --all`.
