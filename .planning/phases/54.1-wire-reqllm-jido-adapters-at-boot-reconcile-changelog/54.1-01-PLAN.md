---
phase: 54.1-wire-reqllm-jido-adapters-at-boot-reconcile-changelog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/application.ex
  - test/scoria/observe/adapters/boot_attach_test.exs
  - test/scoria/workflows/runtime_span_test.exs
autonomous: true
requirements: [SPAN-01, SPAN-02, COMPAT-01, EVENT-01]

must_haves:
  truths:
    - "SC#1 (boot-attach fork, D-01): Scoria.Application.observe_children/0 boot-attaches BOTH the ReqLLM and Jido adapters, inside the existing `enabled != false` branch (D-04)."
    - "SC#2 (D-07, SPAN-01/EVENT-01): a `[:req_llm, :request, :stop]` telemetry event with NO manual attach/0 in the test body persists a span row to Postgres — the boot path did the wiring."
    - "T-53-08 (D-01): both new attach helpers match-and-ignore `{:error, :already_exists}` and never raise/halt in start/2."
    - "D-02: the Jido helper attaches UNCONDITIONALLY — no `Code.ensure_loaded?(Jido)` guard — and `jido` is NOT added to mix.exs."
    - "SC#3 (D-06): the runtime_span_test.exs moduledoc no longer claims ReqLLM is NOT boot-attached; the four affected test files still pass with zero functional edits."
  artifacts:
    - "lib/scoria/application.ex — safe_attach_observe_req_llm/0 + safe_attach_observe_jido/0, each called from observe_children/0"
    - "test/scoria/observe/adapters/boot_attach_test.exs — the additive SC#2 boot-path test (the only new test)"
    - "test/scoria/workflows/runtime_span_test.exs — corrected moduledoc (L13-15)"
  key_links:
    - "observe_children/0 → safe_attach_observe_req_llm/0 / safe_attach_observe_jido/0 (both inside the `enabled != false` branch, gated by the single D-04 switch)"
    - "[:req_llm, :request, :stop] → \"scoria-observe-reqllm\" handler → [:scoria, :observe, :span, :stop] → Scoria.Observe.Buffer → Postgres (ai_spans)"
    - "[:jido, :action, :stop] → \"scoria-observe-jido\" handler → same span pipeline (dormant in prod unless host runs Jido)"
---

<objective>
Close the runtime integration gap: boot-attach the ReqLLM and Jido telemetry adapters through the existing `Scoria.Application.observe_children/0` seam so their LLM/TOOL spans reach `Scoria.Observe.Buffer` in a real host app with zero hand-wiring (SC#1 boot-attach fork, D-01/D-02/D-04), prove it with an additive boot-path test (SC#2, D-07), and reconcile the one now-false test moduledoc (SC#3, D-06).

Purpose: Before this phase, `Adapters.ReqLLM.attach/0` and `Adapters.Jido.attach/0` have no `lib/` caller — only tests attach them — so in production those spans fire into a void while the CHANGELOG claims otherwise.
Output: Two new private attach helpers wired into the boot seam, one new SC#2 test, one corrected moduledoc.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54.1-wire-reqllm-jido-adapters-at-boot-reconcile-changelog/54.1-CONTEXT.md
</context>

<artifacts_this_phase_produces>
Symbols/files this plan creates (downstream plans and reviewers must know these names):
- `Scoria.Application.safe_attach_observe_req_llm/0` — private helper, mirrors `safe_attach_observe_mcp/0`.
- `Scoria.Application.safe_attach_observe_jido/0` — private helper, mirrors `safe_attach_observe_mcp/0`, attaches unconditionally.
- `test/scoria/observe/adapters/boot_attach_test.exs` (module `Scoria.Observe.Adapters.BootAttachTest`) — the SC#2 boot-path proof (the phase's only new test).
- (Plan 02 additionally produces the new guide `guides/capabilities/llm-and-tool-adapters.md`.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Boot-attach ReqLLM + Jido adapters through observe_children/0 (D-01, D-02, D-04, T-53-08)</name>
  <files>lib/scoria/application.ex</files>
  <read_first>
    - lib/scoria/application.ex — the whole file; specifically observe_children/0 (L42-50), safe_attach_observe_telemetry/0 (L56-61), and safe_attach_observe_mcp/0 (L68-73), which is the VERBATIM shape to mirror.
    - lib/scoria/observe/adapters/req_llm.ex — attach/0 (L26-33): handler id "scoria-observe-reqllm", already defined and correct (do NOT touch internals).
    - lib/scoria/observe/adapters/jido.ex — attach/0 (L25-32): handler id "scoria-observe-jido"; note the moduledoc/handler references ZERO Jido.* modules (basis for D-02 unconditional attach).
    - lib/scoria/observe/adapters/mcp.ex — attach/0 (L54-67): the first-party precedent whose `{:error, :already_exists}` contract the two new helpers copy.
  </read_first>
  <behavior>
    - Given config with no `Scoria.Observe, enabled:` override (default ON), `Scoria.Application.observe_children/0` returns `[Scoria.Observe.Buffer]` AND both new handlers are registered: `:telemetry.list_handlers([:req_llm, :request, :stop])` and `:telemetry.list_handlers([:jido, :action, :stop])` are each non-empty.
    - Given `config :scoria, Scoria.Observe, enabled: false`, `observe_children/0` returns `[]` and skips both new attaches (existing application_test.exs assertion at L109-113 stays green).
    - Calling either helper twice returns `{:error, :already_exists}` on the second call and does NOT raise (T-53-08).
    - Jido attaches with no presence guard: there is no `Code.ensure_loaded?` call anywhere in application.ex, and `jido` is absent from mix.exs deps.
  </behavior>
  <action>
    In lib/scoria/application.ex, extend `observe_children/0` (L42-50): inside the existing `if Application.get_env(:scoria, Scoria.Observe, [])[:enabled] != false do` branch, after the existing `safe_attach_observe_mcp()` call and before the `[Scoria.Observe.Buffer]` return, add two calls: `safe_attach_observe_req_llm()` and `safe_attach_observe_jido()`. Do NOT change the `enabled != false` gate, the else branch, or the Buffer return (D-04 — one switch, no per-adapter knob).

    Add two new private helpers mirroring `safe_attach_observe_mcp/0` (L68-73) VERBATIM in shape — a `case ...attach() do :ok -> :ok; {:error, :already_exists} -> :ok end` body: `safe_attach_observe_req_llm/0` calling `Scoria.Observe.Adapters.ReqLLM.attach()`, and `safe_attach_observe_jido/0` calling `Scoria.Observe.Adapters.Jido.attach()`. Give each a short doc-comment noting the distinct handler id ("scoria-observe-reqllm" / "scoria-observe-jido") and the T-53-08 match-and-ignore rationale, matching the comment style on the MCP helper.

    D-02 (decisive): the Jido helper attaches UNCONDITIONALLY — do NOT add a `Code.ensure_loaded?(Jido)` guard or any presence check; the adapter references zero Jido.* modules so a guard would guard nothing and mislead. Do NOT add `jido` to mix.exs deps (it stays telemetry-coupled, not an optional dep).
  </action>
  <acceptance_criteria>
    - `grep -c 'safe_attach_observe_req_llm\|safe_attach_observe_jido' lib/scoria/application.ex` returns 4 (two defs + two call sites).
    - `grep -c 'Code.ensure_loaded?' lib/scoria/application.ex` returns 0 (D-02 — no presence guard).
    - `grep -c ':jido\|jido:' mix.exs` returns 0 in the deps list (jido is not added as a dependency).
    - Both new helper bodies contain the literal `{:error, :already_exists} -> :ok` clause (T-53-08): `grep -c 'already_exists' lib/scoria/application.ex` returns 3 or more (telemetry + mcp + the two new helpers share the clause).
    - `mix test test/scoria/application_test.exs` exits 0 (the enabled:false → `observe_children() == []` assertion still holds).
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/scoria/application_test.exs</automated>
  </verify>
  <done>observe_children/0 calls both new helpers inside the enabled!=false branch; both helpers match-and-ignore {:error, :already_exists}; no Code.ensure_loaded? guard; jido not in mix.exs; application_test.exs green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Additive SC#2 boot-path test — boot-attached ReqLLM persists a span with no manual attach/0 (D-07)</name>
  <files>test/scoria/observe/adapters/boot_attach_test.exs</files>
  <read_first>
    - test/scoria/application_test.exs — the SC#2 template: setup L26-48 (shared-sandbox checkout + `Ecto.Adapters.SQL.Sandbox.mode(Repo, {:shared, self()})` + self-healing `Telemetry.attach()` tolerating `{:error, :already_exists}` at L34-37 + drain via `Buffer.flush_now()` at L45), and the default-pipeline persistence test L68-93 (`:telemetry.execute` → `Buffer.flush_now()` → `Repo.get_by!(Span, id: span_id)`).
    - test/scoria/observe/adapters/req_llm_test.exs — realistic_metadata/1 (L33-50) shows the production `[:req_llm, :request, :stop]` metadata shape (real `%LLMDB.Model{}` via `LLMDB.Model.new!/1`, not a bare string); this file's setup DETACHES "scoria-observe-reqllm" at L6 (why the SC#2 test must NOT live here).
    - test/scoria/observe/adapters/jido_test.exs — base_metadata/1 (L23+) shows the `[:jido, :action, :stop]` metadata shape for the hand-synthesized Jido event.
    - lib/scoria/observe/adapters/req_llm.ex + jido.ex — handle_event/4: the span `:id` is `metadata[:span_id] || Ecto.UUID.generate()` (set an explicit `span_id` in the event so `Repo.get_by!` has a key).
  </read_first>
  <behavior>
    - ReqLLM leg (the real SC#2 proof): with NO `Scoria.Observe.Adapters.ReqLLM.attach()` call in the test BODY, executing `:telemetry.execute([:req_llm, :request, :stop], %{}, metadata)` (metadata carrying an explicit `span_id`, a `trace_id`, a unique `tenant_id`, and a realistic `%LLMDB.Model{}`), then `Buffer.flush_now()`, persists a row `Repo.get_by!(Span, id: span_id)` whose name is "req_llm_request".
    - Boot-registration assertion: before executing, assert the boot handler exists — `assert [_] = :telemetry.list_handlers([:req_llm, :request, :stop])` OR the Test-9 idiom `assert {:error, :already_exists} = Scoria.Observe.Adapters.ReqLLM.attach()`.
    - Jido leg (SC#2-equivalent, explicitly weaker — jido is not a dep, no real in-suite producer): assert `[:jido, :action, :stop]` has a registered handler, then drive a HAND-SYNTHESIZED `[:jido, :action, :stop]` event with an explicit `span_id`, flush, and `Repo.get_by!` the span (name "jido_action"). A moduledoc/comment states plainly this proves handler registration + pipeline wiring, not an upstream jido producer.
  </behavior>
  <action>
    Create test/scoria/observe/adapters/boot_attach_test.exs, module `Scoria.Observe.Adapters.BootAttachTest`, `use ExUnit.Case, async: false`. Mirror application_test.exs's setup VERBATIM in structure: `Ecto.Adapters.SQL.Sandbox.checkout(Repo)`, `Sandbox.mode(Repo, {:shared, self()})`, then SELF-HEAL both boot handlers by calling `Scoria.Observe.Adapters.ReqLLM.attach()` and `Scoria.Observe.Adapters.Jido.attach()` each wrapped to tolerate `{:error, :already_exists}` (runtime_span_test.exs's on_exit at L165 can strip "scoria-observe-reqllm" earlier in the suite — D-07), then `Buffer.flush_now()` to drain parked spans before each test.

    CRITICAL: do NOT attach inside a test body as the mechanism under proof — the self-heal in setup only repairs suite-order damage; the assertion is that the handler was ALREADY registered by boot. Do NOT place this as a `describe` in req_llm_test.exs (its setup detaches "scoria-observe-reqllm" at L6, which would strip the very handler under test — D-07).

    Write the moduledoc to state: this is the additive SC#2 boot-path proof; the ReqLLM leg is the real proof (req_llm is a hard dep, a genuine upstream producer exists); the Jido leg asserts handler registration + drives a hand-synthesized event because jido is not a Scoria dependency (no real in-suite producer) — code/CHANGELOG/tests agree on what "proven" means for each (D-07).

    Reference the moduledocs of ReqLLM/Jido adapters as the SSOT for the metadata contract; do not re-document the gen_ai.* key set here (that lives in req_llm_test.exs / the Semconv module).
  </action>
  <acceptance_criteria>
    - `mix test test/scoria/observe/adapters/boot_attach_test.exs` exits 0.
    - `grep -c 'ReqLLM.attach()\|Adapters.ReqLLM.attach' test/scoria/observe/adapters/boot_attach_test.exs` shows attach calls appear ONLY in setup (self-heal), never inside a `test ... do` block that emits the proof event — verify by inspection that no `test` block calls `attach()` before its `:telemetry.execute([:req_llm...])`.
    - The file asserts boot registration via `list_handlers([:req_llm, :request, :stop])` or the `{:error, :already_exists}` idiom: `grep -c 'list_handlers(\[:req_llm\|already_exists' test/scoria/observe/adapters/boot_attach_test.exs` returns 1 or more.
    - The file drives a hand-synthesized `[:jido, :action, :stop]` event: `grep -c ':jido, :action, :stop' test/scoria/observe/adapters/boot_attach_test.exs` returns 1 or more.
    - `mix test test/scoria/application_test.exs test/scoria/observe/adapters/req_llm_test.exs test/scoria/observe/adapters/jido_test.exs` exits 0 (no regression from the new global-handler test).
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/observe/adapters/boot_attach_test.exs</automated>
  </verify>
  <done>A [:req_llm, :request, :stop] event with no attach/0 in the test body persists a span; boot registration asserted; Jido leg drives a hand-synthesized event; setup self-heals both handlers tolerating already_exists.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile runtime_span_test.exs moduledoc — ReqLLM IS boot-attached now (SC#3, D-06)</name>
  <files>test/scoria/workflows/runtime_span_test.exs</files>
  <read_first>
    - test/scoria/workflows/runtime_span_test.exs — moduledoc L1-16 (the now-false claim is at L13-15: "Scoria.Observe.Adapters.ReqLLM is attached/detached per test (it is NOT boot-attached)") and the per-test setup L157-166 (detach L160 → `:ok = ReqLLM.attach()` L161 → on_exit re-detach L165). NO functional edits to setup — the detach/re-attach already makes the file boot-attach tolerant (D-06).
    - lib/scoria/application.ex — the observe_children/0 change from Task 1 (the reason the old moduledoc line is now false).
  </read_first>
  <action>
    Rewrite ONLY the moduledoc sentence at L13-15 of runtime_span_test.exs. Replace the claim that `Scoria.Observe.Adapters.ReqLLM` "is NOT boot-attached" with a corrected statement: ReqLLM IS now boot-attached at `Scoria.Application` boot exactly like MCP (per Phase 54.1), and this file's per-test `:telemetry.detach("scoria-observe-reqllm")` + re-attach only keeps its span lifecycle independent of the boot-registered handler (so the scoped assertions here are not perturbed by the global handler). Keep the surrounding true statements (MCP stays live across the file; hand-synthesized events are NOT used for paths under test — D-ATTR01-6) intact.

    Make NO functional change to setup, on_exit, or any test body — the detach-before-attach pattern already makes this file boot-attach tolerant (D-06). Do NOT touch test/scoria_web/live/orchestrator_live_integration_test.exs (in the blast radius, no change needed — its detach at L174 / on_exit at L187 already self-heal).
  </action>
  <acceptance_criteria>
    - `grep -c 'NOT boot-attached' test/scoria/workflows/runtime_span_test.exs` returns 0 (the false claim is gone).
    - `grep -c 'boot-attached' test/scoria/workflows/runtime_span_test.exs` returns 1 or more (the corrected claim is present).
    - `git diff --stat test/scoria/workflows/runtime_span_test.exs` shows changes confined to the moduledoc region (roughly L1-16); no test/setup lines changed.
    - `mix test test/scoria/workflows/runtime_span_test.exs test/scoria_web/live/orchestrator_live_integration_test.exs` exits 0 (blast radius green, zero functional edits).
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/workflows/runtime_span_test.exs test/scoria_web/live/orchestrator_live_integration_test.exs</automated>
  </verify>
  <done>runtime_span_test.exs moduledoc no longer says "NOT boot-attached"; change confined to moduledoc; blast-radius tests green with zero functional edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host app ← Scoria.Application.start/2 | Scoria's OTP `start/2` runs inside the host application's supervision tree; anything that raises here takes the host boot down. This phase adds two `:telemetry.attach_many/4` calls at this boundary. |
| upstream telemetry → span pipeline | `[:req_llm, :request, :stop]` / `[:jido, :action, :stop]` events (host- or lib-produced) cross into Scoria's handlers, which emit `[:scoria, :observe, :span, :stop]` → Buffer → Postgres. No new user-input surface, no auth, no network endpoint, no secret handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-08 (T-54.1-01) | Denial of Service (availability) | safe_attach_observe_req_llm/0, safe_attach_observe_jido/0 in Scoria.Application.start/2 | high | mitigate | Both helpers mirror safe_attach_observe_mcp/0 VERBATIM: `case ...attach() do :ok -> :ok; {:error, :already_exists} -> :ok end`. A duplicate handler id (adopter/test attached first) returns `{:error, :already_exists}` and is match-and-ignored; the helper never raises, so `start/2` cannot crash the host app. Enforced by the Task-1 acceptance grep on the `already_exists` clause + the enabled:false / idempotency assertions in application_test.exs and the SC#2 test. |
| T-54.1-02 | Tampering (idle handler) | "scoria-observe-jido" registered with no upstream producer in prod | low | accept | Red-team T1/T3: with no `[:jido, :action, :stop]` producer, the handler is one idle ETS row that never runs — provably harmless, no boot/perf hazard. Unconditional attach (D-02) is strictly simpler than a guard that would guard nothing. Accepted. |
| T-54.1-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase. `jido` is deliberately NOT added to mix.exs/mix.lock (D-02 — telemetry-coupled, not a dependency). No new supply-chain surface. |
</threat_model>

<verification>
- `mix test test/scoria/application_test.exs test/scoria/observe/adapters/boot_attach_test.exs test/scoria/observe/adapters/req_llm_test.exs test/scoria/observe/adapters/jido_test.exs test/scoria/workflows/runtime_span_test.exs test/scoria_web/live/orchestrator_live_integration_test.exs` exits 0.
- `mix compile --warnings-as-errors` exits 0.
- `grep -c 'Code.ensure_loaded?' lib/scoria/application.ex` returns 0 (D-02).
</verification>

<success_criteria>
- SC#1 (boot-attach fork): observe_children/0 boot-attaches both adapters under the single `enabled != false` gate (D-01/D-04).
- SC#2: the boot-path test proves a `[:req_llm, :request, :stop]` event with no manual attach persists a span (D-07).
- SC#3: runtime_span_test.exs moduledoc no longer disagrees with the code (D-06).
- T-53-08 preserved: attaches never raise in start/2.
</success_criteria>

<output>
Create `.planning/phases/54.1-wire-reqllm-jido-adapters-at-boot-reconcile-changelog/54.1-01-SUMMARY.md` when done.
</output>
