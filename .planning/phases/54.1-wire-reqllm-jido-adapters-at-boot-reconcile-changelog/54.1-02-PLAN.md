---
phase: 54.1-wire-reqllm-jido-adapters-at-boot-reconcile-changelog
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - guides/capabilities/llm-and-tool-adapters.md
  - guides/troubleshooting.md
  - mix.exs
  - lib/mix/tasks/scoria.release_preview.ex
  - test/scoria/package_surface_test.exs
  - test/mix/tasks/scoria.release_preview_test.exs
autonomous: true
requirements: [SPAN-01, COMPAT-01, EVENT-01]

must_haves:
  truths:
    - "COMPAT-01 (D-03): the CHANGELOG [Unreleased] Added block distinguishes PERSIST (adapter spans reach Postgres with zero host wiring) from JOIN (they join the run trace only when the host forwards trace_id/parent_id/tenant_id), in honest tone, with a link to the new guide."
    - "D-03 scope guard: the separate, still-true default-span-pipeline zero-wiring persistence claim (emit_*_span/1 + custom telemetry.execute) is UNTOUCHED, so the CHANGELOG does not contradict application_test.exs:6-7."
    - "D-05 (SPAN-01/EVENT-01): one new guide guides/capabilities/llm-and-tool-adapters.md documents the auto-attached LLM/TOOL spans, the persist-vs-join distinction, metadata forwarding, ownership-boundary tie-in, and the jido asymmetry."
    - "D-05: the new guide is registered in all three mix.exs lists (docs_extras, docs_extra_groups Capabilities, package files) AND the mirrored canonical-guides inventory (release_preview.ex + two surface tests) so the docs-source inventory tests stay green."
    - "D-05: guides/troubleshooting.md (already registered) gains an orphan-span entry — an EDIT with no registration change."
  artifacts:
    - "guides/capabilities/llm-and-tool-adapters.md — the new adopter guide"
    - "CHANGELOG.md — amended [Unreleased] Added adapter sentence"
    - "guides/troubleshooting.md — new orphan-span entry"
    - "mix.exs + lib/mix/tasks/scoria.release_preview.ex + test/scoria/package_surface_test.exs + test/mix/tasks/scoria.release_preview_test.exs — guide registration mirrored across the inventory"
  key_links:
    - "guides/capabilities/llm-and-tool-adapters.md → mix.exs docs_extras (~L166) + docs_extra_groups Capabilities (~L191-196) + package files (~L370-380)"
    - "same path → lib/mix/tasks/scoria.release_preview.ex @required_package_paths + test @canonical_guides in package_surface_test.exs (L9-25 + Capabilities group L122-128) + release_preview_test.exs (L8-24) — strict-equality inventory kept consistent"
    - "guide prose → ReqLLM/Jido adapter moduledocs as the SSOT for metadata-forwarding keys (resists doc rot)"
---

<objective>
Reconcile the docs surface with the now-true boot-attach behavior: amend the scoped CHANGELOG adapter claim to distinguish persist-vs-join (COMPAT-01, D-03), add one adopter capability guide plus one troubleshooting entry (D-05), and register the new guide across the mirrored docs-source inventory so the release/package contract stays green.

Purpose: The CHANGELOG currently conflates the default-span-pipeline "zero wiring" persistence with adapter-span persistence, and there is no adopter guide explaining metadata forwarding (why an LLM/tool call may be a standalone single-span trace). This plan makes code/CHANGELOG/docs agree.
Output: One amended CHANGELOG block, one new guide, one troubleshooting entry, and consistent inventory registration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54.1-wire-reqllm-jido-adapters-at-boot-reconcile-changelog/54.1-CONTEXT.md
</context>

<artifacts_this_phase_produces>
- `guides/capabilities/llm-and-tool-adapters.md` — new adopter capability guide (registered across the docs inventory).
- (Plan 01 produces `Scoria.Application.safe_attach_observe_req_llm/0`, `safe_attach_observe_jido/0`, and `test/scoria/observe/adapters/boot_attach_test.exs`.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Amend the scoped CHANGELOG adapter claim — persist vs. join (COMPAT-01, D-03)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md — the `## [Unreleased]` "Added" block (L181-191). The sentence naming "the ReqLLM/Jido adapters" (L183-185) currently attributes their persistence to Buffer + Telemetry.attach booting; the "zero host wiring" persistence sentence about `Scoria.Observe.emit_*_span/1` + custom `:telemetry.execute/3` (the SAME L183-191 block) is the still-true default-pipeline claim that must NOT be weakened.
    - test/scoria/application_test.exs — moduledoc L1-18 (esp. L6-7): the still-true default-pipeline claim ("a span emitted through the DEFAULT pipeline ... persists to Postgres with zero host wiring") that a blanket CHANGELOG edit would contradict (D-03 scope guard).
    - lib/scoria/observe/adapters/req_llm.ex + jido.ex — moduledocs: the persist-vs-join / ORPHAN-single-span-trace vocabulary and the trace_id/parent_id/tenant_id forwarding contract (SSOT for the caveat wording).
  </read_first>
  <action>
    In the `## [Unreleased]` "Added" block, amend ONLY the adapter-specific claim so it distinguishes persist from join: state that the ReqLLM and Jido adapters now boot-attach (via the new `safe_attach_observe_req_llm/0` / `safe_attach_observe_jido/0` helpers this release adds), so their LLM/TOOL spans PERSIST to Postgres with zero host wiring — keep that value prop in bold — but add one calm caveat sentence that a span only JOINS the workflow run's trace as a child span when the host forwards `trace_id` / `parent_id` / `tenant_id` into the telemetry metadata (automatic in-workflow via `Scoria.Workflows.Runtime.execute_step/2`; a raw out-of-workflow call persists as a standalone single-span trace until the host forwards them). Add a link to the D-05 guide `guides/capabilities/llm-and-tool-adapters.md`. Note that `jido` is not a Scoria dependency — its adapter stays dormant unless the host runs Jido.

    Scope guard (D-03): do NOT touch the separate, still-accurate default-span-pipeline claim in the same block (the sentence about `Scoria.Observe.emit_*_span/1` and any custom `:telemetry.execute/3` on `[:scoria, :observe, :span, :stop]` persisting with zero wiring) — that claim is mirrored in application_test.exs:6-7 and must remain byte-consistent with it.

    Tone: honest, not scary. Do NOT use ALL-CAPS "ORPHAN" in this adopter-facing copy (that phrasing belongs only in the moduledocs). Exact wording is Claude's discretion provided the persist-vs-join distinction and the scope guard hold.
  </action>
  <acceptance_criteria>
    - The amended block mentions both persistence and trace-join: `grep -ic 'trace_id\|join' CHANGELOG.md` and `grep -ic 'zero host wiring' CHANGELOG.md` each return 1 or more.
    - The new guide is linked: `grep -c 'guides/capabilities/llm-and-tool-adapters.md' CHANGELOG.md` returns 1 or more.
    - No ALL-CAPS "ORPHAN" in the adopter copy: `grep -c 'ORPHAN' CHANGELOG.md` returns 0.
    - The default-pipeline claim survives and stays consistent with the test moduledoc: `mix test test/scoria/application_test.exs` exits 0.
    - `MIX_ENV=dev mix docs --warnings-as-errors` exits 0 (CHANGELOG is an ExDoc extra — the amended block must not introduce a broken autolink; if a filtered-module code span autolinks, apply the house fix — `$ ` prefix or plain non-backtick form — per the Phase-50 D-50-DEF-01 precedent).
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/application_test.exs</automated>
  </verify>
  <done>CHANGELOG adapter claim distinguishes persist vs. join, links the new guide, no ALL-CAPS ORPHAN; default-pipeline claim untouched and application_test.exs green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the LLM/tool adapters capability guide + troubleshooting orphan-span entry (D-05, SPAN-01, EVENT-01)</name>
  <files>guides/capabilities/llm-and-tool-adapters.md, guides/troubleshooting.md</files>
  <read_first>
    - guides/capabilities/connectors-and-mcp.md and guides/capabilities/default-runtime.md — the house voice, length (~4-7 KB, one page), and heading style the new guide must match.
    - guides/troubleshooting.md — the existing entry structure to append the orphan-span entry in the same shape (this file is already registered — EDIT only, no registration change).
    - guides/ownership-boundary.md — the "Scoria owns the mechanism; your app owns the noun" doctrine + the existing "Run records and traces" row, to frame metadata forwarding as host-owned trace lineage (D-05 item 4).
    - lib/scoria/observe/adapters/req_llm.ex + jido.ex moduledocs — the SSOT for the exact forwarding keys (`trace_id`, `parent_id`, `tenant_id`, `span_id`, `session_id`) and the LLM-vs-TOOL span kinds; the guide must POINT AT these moduledocs as SSOT rather than restating the full key set (resists doc rot).
    - mix.exs docs_public_modules/0 (L279-309): confirm `Scoria.Observe.Adapters.ReqLLM` / `.Jido` / `Scoria.Application` are NOT in the allowlist — so any `` `Module` ``/`` `Module.fun/arity` `` backtick reference to them in the guide WILL trip `mix docs --warnings-as-errors` (filtered-module autolink). Write those references in non-autolinking form (plain prose, or the `$ `/skip technique) — see the acceptance criterion.
  </read_first>
  <action>
    Create guides/capabilities/llm-and-tool-adapters.md as a single-page adopter guide joining the existing capabilities ladder (default-runtime → bounded-handoffs → semantic-cache → connectors-and-mcp). Cover, minimally (D-05 outline): (1) what auto-attaches at boot — the ReqLLM adapter (`[:req_llm, :request, :stop]` → LLM span) and the Jido adapter (`[:jido, :action, :stop]` → TOOL span), config-gated by `config :scoria, Scoria.Observe, enabled: false`, `{:error, :already_exists}`-tolerant, under handler ids `"scoria-observe-reqllm"` / `"scoria-observe-jido"`; (2) the persist-vs-join distinction (spans persist with zero wiring; they join the run trace only when the host forwards the lineage keys); (3) HOW to forward metadata — name the keys and give one `req_llm` example and one `jido` example, pointing at the adapter moduledocs as the SSOT for the key set; (4) the ownership-boundary tie-in — trace lineage/identity are host-owned nouns Scoria never infers (same class as tenant/actor); (5) the jido asymmetry — `req_llm` is a hard dependency, `jido` is not, so the Jido handler is dormant unless the host brings Jido.

    Steal OpenTelemetry's "context propagation" framing (honest but not scary): spans won't LINK without the forwarded keys, but they still PERSIST. Keep it to one page.

    EDIT guides/troubleshooting.md: append one entry titled to match the existing entry style, answering "Why is my LLM/tool call a standalone single-span trace?" — explain that the adapter persisted the span but the host did not forward `trace_id`/`parent_id`, so it did not join the run trace; link to the new capability guide. This is an edit only — do NOT change troubleshooting.md's registration (it is already registered at mix.exs docs_extras L172, docs_extra_groups L198, package files L376).

    Autolink footgun (Phase-50 precedent): because `Scoria.Observe.Adapters.ReqLLM`/`.Jido`/`Scoria.Application` are filtered from ExDoc's public module set, do NOT write them as autolinking backtick module spans. Reference them in plain prose (or use the `$ ` inline-span technique already used for filtered mix tasks); the guide MUST build clean under `mix docs --warnings-as-errors`.
  </action>
  <acceptance_criteria>
    - `test -f guides/capabilities/llm-and-tool-adapters.md` succeeds and the file is non-empty (`wc -c` > 1500).
    - The guide covers the five D-05 points — spot-check: `grep -ic 'persist' guides/capabilities/llm-and-tool-adapters.md` ≥ 1, `grep -ic 'trace_id\|forward' guides/capabilities/llm-and-tool-adapters.md` ≥ 1, `grep -ic 'jido' guides/capabilities/llm-and-tool-adapters.md` ≥ 1, `grep -ic 'enabled: false' guides/capabilities/llm-and-tool-adapters.md` ≥ 1.
    - Troubleshooting entry added: `grep -ic 'single-span trace\|standalone' guides/troubleshooting.md` ≥ 1 and it links the new guide: `grep -c 'llm-and-tool-adapters' guides/troubleshooting.md` ≥ 1.
    - No autolinking backtick reference to filtered modules: `grep -cE '`Scoria\.(Observe\.Adapters\.(ReqLLM|Jido)|Application)`' guides/capabilities/llm-and-tool-adapters.md` returns 0.
    - `MIX_ENV=dev mix docs --warnings-as-errors` exits 0 AFTER Task 3 registers the guide (the guide is not an ExDoc extra until then; run this as the combined gate at end of plan).
  </acceptance_criteria>
  <verify>
    <automated>test -f guides/capabilities/llm-and-tool-adapters.md && grep -c 'llm-and-tool-adapters' guides/troubleshooting.md</automated>
  </verify>
  <done>New capability guide exists covering the five D-05 points with no autolinking filtered-module spans; troubleshooting.md has an orphan-span entry linking the guide (edit only, no registration change).</done>
</task>

<task type="auto">
  <name>Task 3: Register the new guide across the mirrored docs-source inventory (D-05)</name>
  <files>mix.exs, lib/mix/tasks/scoria.release_preview.ex, test/scoria/package_surface_test.exs, test/mix/tasks/scoria.release_preview_test.exs</files>
  <read_first>
    - mix.exs — docs_extras (L157-178, add after the capabilities entries ~L170), docs_extra_groups Capabilities group (L190-196), and package files (L366-389, add near the capabilities entries ~L374). These are the THREE mix.exs lists D-05 names.
    - lib/mix/tasks/scoria.release_preview.ex — `@required_package_paths` (L5-48; capabilities guides at L19-23). `required_package_paths/0` (L48) returns this list; the release_preview test asserts it by STRICT equality, so the new guide MUST be added here too or that test fails.
    - test/scoria/package_surface_test.exs — `@canonical_guides` (L9-25) and the Capabilities `assert_group_contains` (L122-128); the "package files include guides" (L194-204) and "docs extras expose canonical guide ladder" (L98-108) tests iterate `@canonical_guides`.
    - test/mix/tasks/scoria.release_preview_test.exs — `@canonical_guides` (L8-24); `@expected_required_paths` (L57-63) is built from it and asserted `required_paths == @expected_required_paths` (STRICT equality) — both this list and release_preview.ex must include the new guide in lockstep.
  </read_first>
  <action>
    Add the path `guides/capabilities/llm-and-tool-adapters.md` to EVERY site the canonical-guides inventory is mirrored, keeping it grouped with the other `guides/capabilities/*` entries in each list:
    1. mix.exs `docs_extras` (after the capabilities block, ~L170).
    2. mix.exs `docs_extra_groups` → `Capabilities:` group (~L191-196).
    3. mix.exs `package` `files:` (with the other capabilities guides, ~L374).
    4. lib/mix/tasks/scoria.release_preview.ex `@required_package_paths` (with the capabilities guides, ~L23).
    5. test/scoria/package_surface_test.exs `@canonical_guides` (~L18) AND the `Capabilities` `assert_group_contains` list (~L128).
    6. test/mix/tasks/scoria.release_preview_test.exs `@canonical_guides` (~L18).

    Do NOT reorder or remove any existing entry. Do NOT add the guide to any "Start Here" / "Operate & Verify" / other group — it belongs only in Capabilities. Do NOT touch the compatibility-stub, dev-only, or brand-asset lists.
  </action>
  <acceptance_criteria>
    - The new path appears exactly in the expected sites: `grep -rc 'guides/capabilities/llm-and-tool-adapters.md' mix.exs` returns 3; `grep -c 'guides/capabilities/llm-and-tool-adapters.md' lib/mix/tasks/scoria.release_preview.ex` returns 1; `grep -c 'guides/capabilities/llm-and-tool-adapters.md' test/scoria/package_surface_test.exs` returns 2 (canonical list + Capabilities group); `grep -c 'guides/capabilities/llm-and-tool-adapters.md' test/mix/tasks/scoria.release_preview_test.exs` returns 1.
    - `mix test test/scoria/package_surface_test.exs test/mix/tasks/scoria.release_preview_test.exs` exits 0 (strict-equality inventory + group-membership assertions pass).
    - `mix test test/scoria/hex_consumer_contract_test.exs` exits 0 (no publish-surface drift).
    - `MIX_ENV=dev mix docs --warnings-as-errors` exits 0 (the now-registered guide builds clean — closes Task 2's deferred gate).
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/package_surface_test.exs test/mix/tasks/scoria.release_preview_test.exs test/scoria/hex_consumer_contract_test.exs && MIX_ENV=dev mix docs --warnings-as-errors</automated>
  </verify>
  <done>The new guide path appears in all six mirrored inventory sites (mix.exs x3, release_preview.ex, both surface tests); strict-equality surface tests pass; docs build clean under warnings-as-errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adopter ← CHANGELOG / guides | Adopter-facing documentation is the integrity surface for this plan. A false persistence claim (spans "persist with zero wiring" when the adapter path did not actually boot-attach) misleads hosts querying their own Postgres. No code, no runtime, no user-input, no auth, no secret surface. |
| release/package contract | mix.exs + release_preview inventory + surface tests gate what ships to Hex. An unregistered or inconsistently-registered guide breaks the publish surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54.1-03 | Repudiation / Information disclosure (docs integrity) | CHANGELOG [Unreleased] Added adapter claim | medium | mitigate | D-03 persist-vs-join reconciliation + scope guard: the amended sentence tells the truth (persist ≠ join), the still-true default-pipeline claim is left byte-consistent with application_test.exs:6-7, and `mix test test/scoria/application_test.exs` gates the consistency. Prevents an adopter from trusting a false "spans linked with zero wiring" claim. |
| T-54.1-04 | Tampering (release surface drift) | mirrored canonical-guides inventory across mix.exs / release_preview.ex / two surface tests | medium | mitigate | The new guide is added to all six mirrored sites in lockstep; the strict-equality `package_surface_test` + `release_preview_test` gates fail if any site drifts, and `mix docs --warnings-as-errors` fails on a broken/unregistered extra. |
| T-54.1-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase; no new dependency (`jido` deliberately not added — D-02). No supply-chain surface. |
</threat_model>

<verification>
- `mix test test/scoria/package_surface_test.exs test/mix/tasks/scoria.release_preview_test.exs test/scoria/hex_consumer_contract_test.exs test/scoria/application_test.exs` exits 0.
- `MIX_ENV=dev mix docs --warnings-as-errors` exits 0.
- `grep -rc 'guides/capabilities/llm-and-tool-adapters.md' mix.exs` returns 3.
</verification>

<success_criteria>
- COMPAT-01 (D-03): CHANGELOG distinguishes persist vs. join; default-pipeline claim untouched.
- D-05: one new registered capability guide + one troubleshooting entry; docs-source inventory tests green; docs build clean under warnings-as-errors.
</success_criteria>

<output>
Create `.planning/phases/54.1-wire-reqllm-jido-adapters-at-boot-reconcile-changelog/54.1-02-SUMMARY.md` when done.
</output>
