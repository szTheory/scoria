---
phase: 37-replay-lineage-branch-model
plan: 03
type: execute
wave: 3
depends_on:
  - 37-01
  - 37-02
files_modified:
  - lib/scoria/runtime/run_summary.ex
  - lib/scoria/runtime/run_detail.ex
  - lib/scoria_web/live/workflow_live/show.ex
  - lib/scoria_web/components/workflow_detail_panel_component.ex
  - lib/scoria_web/live/orchestrator_live.ex
  - test/scoria_web/live/workflow_live_test.exs
  - test/scoria_web/live/orchestrator_live_test.exs
  - test/scoria/runtime_view_test.exs
autonomous: true
requirements:
  - RPLY-01
must_haves:
  truths:
    - "Replay lineage is queryable from the public run detail surface."
    - "Workflow run UI shows replay provenance without reading raw metadata directly in templates."
    - "Trace-facing operator reads can connect a trace or run-linked evidence row back to replay lineage."
  artifacts:
    - path: "lib/scoria/runtime/run_summary.ex"
      provides: "Stable public summary fields for replay identification"
      contains: "source_run_id"
    - path: "lib/scoria/runtime/run_detail.ex"
      provides: "Detailed public replay lineage block"
      contains: "replay_lineage"
    - path: "lib/scoria_web/components/workflow_detail_panel_component.ex"
      provides: "Operator-visible replay provenance on the workflow page"
      contains: "Replay"
    - path: "test/scoria_web/live/workflow_live_test.exs"
      provides: "LiveView proof that workflow run detail surfaces replay lineage"
      contains: "source checkpoint"
  key_links:
    - from: "lib/scoria/runtime/run_detail.ex"
      to: "lib/scoria_web/live/workflow_live/show.ex"
      via: "workflow page renders replay lineage from a stable DTO/read model"
      pattern: "replay_lineage"
    - from: "lib/scoria_web/live/orchestrator_live.ex"
      to: "test/scoria_web/live/orchestrator_live_test.exs"
      via: "trace-facing read model exposes replay lineage or branch badges"
      pattern: "replay"
---

<objective>
Expose replay lineage on public run-detail and operator-facing workflow/trace surfaces.

Purpose: Phase 37 is not complete until operators can inspect branch provenance from the workflow page and query lineage from the trace-facing surface without reverse-engineering metadata.
Output: extended DTOs, workflow UI lineage rendering, trace-facing replay projection, and focused LiveView/runtime tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-replay-lineage-branch-model/37-RESEARCH.md
@.planning/phases/37-replay-lineage-branch-model/37-PATTERNS.md
@.planning/phases/37-replay-lineage-branch-model/37-VALIDATION.md
@lib/scoria/runtime.ex
@lib/scoria/runtime/run_summary.ex
@lib/scoria/runtime/run_detail.ex
@lib/scoria_web/live/workflow_live/show.ex
@lib/scoria_web/components/workflow_detail_panel_component.ex
@lib/scoria_web/live/orchestrator_live.ex
@test/scoria_web/live/workflow_live_test.exs
@test/scoria_web/live/orchestrator_live_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add public DTO and LiveView tests for replay lineage visibility</name>
  <read_first>
    - lib/scoria/runtime/run_summary.ex
    - lib/scoria/runtime/run_detail.ex
    - lib/scoria_web/live/workflow_live/show.ex
    - lib/scoria_web/components/workflow_detail_panel_component.ex
    - lib/scoria_web/live/orchestrator_live.ex
    - test/scoria_web/live/workflow_live_test.exs
    - test/scoria_web/live/orchestrator_live_test.exs
  </read_first>
  <files>test/scoria_web/live/workflow_live_test.exs, test/scoria_web/live/orchestrator_live_test.exs, test/scoria/runtime_view_test.exs</files>
  <action>
    Add or extend tests first so the public visibility contract is concrete.
    Cover these exact behaviors:
    - `Scoria.Runtime.get_run_detail!/1` exposes replay lineage fields or a `replay_lineage` block that contains source run id, source checkpoint id, execution mode, and override metadata.
    - the workflow run LiveView renders replay provenance text such as `Replay`, `Source run`, `Source checkpoint`, and `Execution mode` when the current run is a branch.
    - the trace-facing LiveView can surface replay lineage from run-linked evidence or branch badges without requiring a second canonical store.
    Keep assertions string-based and grep-verifiable in the rendered HTML.
  </action>
  <verify>
    <automated>mix test test/scoria/runtime_view_test.exs test/scoria_web/live/workflow_live_test.exs test/scoria_web/live/orchestrator_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The runtime/detail tests assert replay lineage fields explicitly.
    - The workflow LiveView test checks for rendered replay provenance strings.
    - The trace-facing test checks for replay lineage or replay-branch visibility in the operator surface.
  </acceptance_criteria>
  <done>Replay visibility expectations are executable before DTO and UI code changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Extend public run DTOs and workflow UI to render replay provenance</name>
  <read_first>
    - lib/scoria/runtime/run_summary.ex
    - lib/scoria/runtime/run_detail.ex
    - lib/scoria_web/live/workflow_live/show.ex
    - lib/scoria_web/components/workflow_detail_panel_component.ex
    - test/scoria_web/live/workflow_live_test.exs
  </read_first>
  <files>lib/scoria/runtime/run_summary.ex, lib/scoria/runtime/run_detail.ex, lib/scoria_web/live/workflow_live/show.ex, lib/scoria_web/components/workflow_detail_panel_component.ex</files>
  <action>
    Add replay lineage to the stable public runtime projections first, then render it in the workflow page.
    In `RunSummary`, add lightweight replay-identification fields needed for list/polling consumers, such as `source_run_id`, `source_checkpoint_id`, and `execution_mode`.
    In `RunDetail`, add a dedicated `replay_lineage` block or equivalent explicit fields derived from persisted run truth.
    Update `WorkflowLive.Show` and `WorkflowDetailPanelComponent` so replay lineage renders from those explicit fields. The page must show source run id, source checkpoint id, execution mode, and bounded override metadata for branch runs. Do not read nested raw metadata directly in HEEx.
  </action>
  <verify>
    <automated>mix test test/scoria/runtime_view_test.exs test/scoria_web/live/workflow_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `RunSummary` and `RunDetail` expose replay lineage explicitly.
    - The workflow page renders replay provenance strings from stable DTO fields.
    - Templates do not depend on raw nested replay metadata access.
    - The focused test command passes.
  </acceptance_criteria>
  <done>Replay provenance is part of the public run-detail contract and visible on the workflow operator page.</done>
</task>

<task type="auto">
  <name>Task 3: Surface replay lineage in the trace-facing operator read model</name>
  <read_first>
    - lib/scoria_web/live/orchestrator_live.ex
    - lib/scoria/repo/trace.ex
    - lib/scoria/runtime/run_detail.ex
    - test/scoria_web/live/orchestrator_live_test.exs
  </read_first>
  <files>lib/scoria_web/live/orchestrator_live.ex, test/scoria_web/live/orchestrator_live_test.exs</files>
  <action>
    Add the minimal trace-facing projection needed for success criterion 3. Use existing run-linked evidence or run-id joins already present in `orchestrator_live` so a trace-facing operator can tell whether a linked run is a replay branch and, if so, inspect basic lineage context. Keep this as a projection over workflow truth; do not create a second replay store or broaden Phase 37 into full replay-vs-original diff UX. A compact badge, notice, or lazy-loaded lineage block is sufficient as long as it is queryable from the operator surface and proven by test.
  </action>
  <verify>
    <automated>mix test test/scoria_web/live/orchestrator_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The trace-facing operator surface can expose replay lineage for run-linked traces.
    - The implementation reads workflow lineage as downstream truth rather than creating a second store.
    - The focused test command passes.
  </acceptance_criteria>
  <done>Replay lineage is queryable from both workflow detail and trace-facing operator surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow truth -> public DTOs | Replay lineage must be projected consistently to public APIs and UI |
| workflow truth -> trace-facing operator surface | Trace explorer reads must not invent a second canonical lineage model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-20 | Repudiation | public run detail | mitigate | Add explicit replay lineage fields to `RunSummary` / `RunDetail` so operator-visible provenance is stable and testable. |
| T-37-21 | Information Disclosure | workflow page overrides display | mitigate | Keep replay override metadata bounded and render only operator-safe fields already persisted as truth. |
| T-37-22 | Tampering | trace-facing read model | mitigate | Treat trace UI as a projection over workflow lineage and avoid a second replay store or template-side metadata scraping. |
</threat_model>

<verification>
- `mix test test/scoria/runtime_view_test.exs test/scoria_web/live/workflow_live_test.exs test/scoria_web/live/orchestrator_live_test.exs`
</verification>

<success_criteria>
Operators can inspect replay lineage from the public run-detail surface and from the trace-facing UI, using stable workflow-derived projections instead of ad hoc metadata inspection.
</success_criteria>

<output>
After completion, create `.planning/phases/37-replay-lineage-branch-model/37-03-SUMMARY.md`
</output>
