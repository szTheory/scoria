---
phase: 37-replay-lineage-branch-model
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - lib/scoria/workflows.ex
  - lib/scoria/runtime.ex
  - lib/scoria/workflows/reconciler.ex
  - lib/scoria/workflows/runtime.ex
  - test/scoria/workflows/replay_branch_test.exs
  - test/scoria/runtime_test.exs
autonomous: true
requirements:
  - RPLY-01
must_haves:
  truths:
    - "Replay branches dispatch through the existing workflow runtime path instead of a second execution engine."
    - "Branch runs begin from durable checkpoint truth and record a replay-start lifecycle transition before execution proceeds."
    - "Branch creation remains explicit and operator-reviewable even before Phase 38 replay-safety rules land."
  artifacts:
    - path: "lib/scoria/workflows.ex"
      provides: "Replay branch API that seeds branch state from a chosen checkpoint"
      contains: "replay_started"
    - path: "lib/scoria/runtime.ex"
      provides: "Public runtime entry point or helper for replay branch dispatch"
      contains: "replay"
    - path: "test/scoria/workflows/replay_branch_test.exs"
      provides: "Proof that replay branches use the existing runtime/reconciler path"
      contains: "dispatch"
  key_links:
    - from: "lib/scoria/runtime.ex"
      to: "lib/scoria/workflows/reconciler.ex"
      via: "branch runs reuse the same dispatch seam as ordinary runs"
      pattern: "dispatch_run"
---

<objective>
Implement replay branch execution on the existing workflow runtime seam.

Purpose: Phase 37 must prove replay branches are not a parallel engine; they are ordinary workflow runs with replay provenance and a replay-start seed state.
Output: replay branch dispatch path, runtime integration, and integration tests proving engine reuse.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-replay-lineage-branch-model/37-RESEARCH.md
@.planning/phases/37-replay-lineage-branch-model/37-PATTERNS.md
@.planning/phases/37-replay-lineage-branch-model/37-01-PLAN.md
@lib/scoria/runtime.ex
@lib/scoria/workflows.ex
@lib/scoria/workflows/reconciler.ex
@lib/scoria/workflows/runtime.ex
@test/scoria/workflows/replay_branch_test.exs
@test/scoria/runtime_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend replay-branch tests to prove runtime reuse and seeded replay-start state</name>
  <read_first>
    - .planning/phases/37-replay-lineage-branch-model/37-RESEARCH.md
    - lib/scoria/runtime.ex
    - lib/scoria/workflows/reconciler.ex
    - test/scoria/workflows/replay_branch_test.exs
    - test/scoria/runtime_test.exs
  </read_first>
  <files>test/scoria/workflows/replay_branch_test.exs, test/scoria/runtime_test.exs</files>
  <action>
    Extend the replay-branch coverage added in Plan 37-01 so it now proves runtime reuse explicitly.
    Add assertions for this concrete behavior:
    - creating a replay branch records a replay-start checkpoint/event transition such as `replay_started` or `run_started` with replay lineage in snapshot/metadata
    - dispatch of the branch run flows through the existing reconciler/runtime path rather than a replay-only module
    - the branch run remains queryable through `Scoria.Runtime.get_run!/1` and, if implemented, a replay-specific helper returns the same stable summary shape as ordinary runs
    Include one test that ensures replay branch identity inherits the source run's canonical `actor_id`, `tenant_id`, `session_id`, and `root_role_id` unless an explicitly allowed override is part of the Phase 37 contract.
  </action>
  <verify>
    <automated>mix test test/scoria/workflows/replay_branch_test.exs test/scoria/runtime_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The replay branch tests assert a replay-start transition exists on the branch run.
    - The tests prove branch runs are visible through the normal runtime summary APIs.
    - Identity inheritance from the source run is covered explicitly.
  </acceptance_criteria>
  <done>The runtime reuse contract is executable before integration code is widened.</done>
</task>

<task type="auto">
  <name>Task 2: Wire replay branch dispatch through the existing runtime and reconciler seams</name>
  <read_first>
    - lib/scoria/runtime.ex
    - lib/scoria/workflows.ex
    - lib/scoria/workflows/reconciler.ex
    - lib/scoria/workflows/runtime.ex
    - test/scoria/workflows/replay_branch_test.exs
  </read_first>
  <files>lib/scoria/workflows.ex, lib/scoria/runtime.ex, lib/scoria/workflows/reconciler.ex, lib/scoria/workflows/runtime.ex</files>
  <action>
    Add the smallest integration needed so replay branches execute through Scoria's current runtime engine.
    If a public helper is needed, add a narrow API such as `Scoria.Runtime.replay_run/2` or `Scoria.Workflows.start_replay_branch/2` that:
    - resolves the source run and checkpoint
    - creates the branch run via the Plan 37-01 persistence API
    - dispatches that branch run with the same reconciler/runtime flow ordinary runs use
    - records replay-start evidence before execution proceeds
    Keep the implementation additive. Do not create a separate replay scheduler, replay worker, or parallel lifecycle state machine.
    The implementation may default `execution_mode` to a Phase-37-safe replay marker, but leave detailed side-effect routing for Phase 38.
  </action>
  <verify>
    <automated>mix test test/scoria/workflows/replay_branch_test.exs test/scoria/runtime_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Replay branches can be started through a narrow workflow/runtime API.
    - The branch run is dispatched through existing runtime/reconciler seams.
    - No replay-only execution engine or second lifecycle state machine is introduced.
    - The focused test command passes.
  </acceptance_criteria>
  <done>Replay branches execute as ordinary workflow runs with replay provenance, not as a second engine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replay branch API -> runtime dispatch | Replay-specific entry points must not bypass normal workflow execution rules |
| source checkpoint truth -> seeded branch state | Branch runs must derive their starting state from persisted checkpoints, not mutable caller guesses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-10 | Tampering | replay start path | mitigate | Seed replay-start state from the persisted source checkpoint and typed lineage fields rather than arbitrary request payloads. |
| T-37-11 | Repudiation | runtime engine reuse | mitigate | Record replay-start checkpoint/event evidence and keep branch runs visible through normal runtime summary/detail APIs. |
| T-37-12 | Denial of Service | execution architecture | mitigate | Reuse existing reconciler/runtime seams instead of introducing a second engine or scheduler. |
</threat_model>

<verification>
- `mix test test/scoria/workflows/replay_branch_test.exs test/scoria/runtime_test.exs`
</verification>

<success_criteria>
Replay branches start from durable checkpoint truth and execute through the same workflow runtime Scoria already ships, with explicit replay-start evidence recorded first.
</success_criteria>

<output>
After completion, create `.planning/phases/37-replay-lineage-branch-model/37-02-SUMMARY.md`
</output>
