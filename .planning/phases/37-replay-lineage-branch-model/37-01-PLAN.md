---
phase: 37-replay-lineage-branch-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/workflows.ex
  - lib/scoria/workflows/run.ex
  - priv/repo/migrations/TIMESTAMP_add_replay_lineage_to_workflow_runs.exs
  - test/scoria/workflows/replay_branch_test.exs
  - test/scoria/workflows_test.exs
autonomous: true
requirements:
  - RPLY-01
must_haves:
  truths:
    - "A replay branch is persisted as a brand new workflow run, never as a mutation of the source run."
    - "The branch run stores typed `source_run_id`, `source_checkpoint_id`, bounded override metadata, and `execution_mode` as durable truth."
    - "Historical runs created before Phase 37 remain readable after the additive lineage migration."
    - "Replay lineage is strong enough to drive later runtime and UI reads without scraping generic metadata."
  artifacts:
    - path: "lib/scoria/workflows/run.ex"
      provides: "Typed replay lineage fields and validation on workflow runs"
      contains: "field(:source_run_id"
    - path: "lib/scoria/workflows.ex"
      provides: "Replay branch creation API that inserts a new run from a chosen source checkpoint"
      contains: "def create_replay_branch"
    - path: "priv/repo/migrations/TIMESTAMP_add_replay_lineage_to_workflow_runs.exs"
      provides: "Additive schema and indexes for replay lineage"
      contains: "alter table(:ai_workflow_runs)"
    - path: "test/scoria/workflows/replay_branch_test.exs"
      provides: "Persistence proof that source history stays immutable and branch lineage is durable"
      contains: "create_replay_branch"
  key_links:
    - from: "lib/scoria/workflows.ex"
      to: "lib/scoria/workflows/run.ex"
      via: "branch creation writes typed replay lineage into the run schema"
      pattern: "source_run_id|source_checkpoint_id|execution_mode"
    - from: "priv/repo/migrations/TIMESTAMP_add_replay_lineage_to_workflow_runs.exs"
      to: "test/scoria/workflows/replay_branch_test.exs"
      via: "new lineage columns are asserted through replay branch persistence tests"
      pattern: "source_run_id|source_checkpoint_id"
---

<objective>
Establish the durable replay lineage contract in the workflow persistence layer.

Purpose: Phase 37 cannot safely branch replays until replay provenance is first-class workflow truth instead of ad hoc metadata.
Output: additive lineage migration, typed run fields, replay branch creation API, and focused persistence coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-replay-lineage-branch-model/37-RESEARCH.md
@.planning/phases/37-replay-lineage-branch-model/37-PATTERNS.md
@.planning/phases/37-replay-lineage-branch-model/37-VALIDATION.md
@lib/scoria/workflows.ex
@lib/scoria/workflows/run.ex
@lib/scoria/workflows/checkpoint.ex
@priv/repo/migrations/20260511000100_create_workflow_tables.exs
@priv/repo/migrations/20260513000100_add_canonical_identity_to_workflow_runs.exs
@test/scoria/workflows_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focused replay-branch persistence tests before implementation</name>
  <read_first>
    - .planning/phases/37-replay-lineage-branch-model/37-RESEARCH.md
    - .planning/phases/37-replay-lineage-branch-model/37-PATTERNS.md
    - lib/scoria/workflows.ex
    - lib/scoria/workflows/run.ex
    - test/scoria/workflows_test.exs
  </read_first>
  <files>test/scoria/workflows/replay_branch_test.exs, test/scoria/workflows_test.exs</files>
  <action>
    Create `test/scoria/workflows/replay_branch_test.exs` first and extend `test/scoria/workflows_test.exs` only where a schema-level assertion already belongs.
    Lock the branch truth to this exact behavior:
    - `Scoria.Workflows.create_replay_branch/2` or equivalent creates a new run row with `source_run_id` equal to the original run id and `source_checkpoint_id` equal to the chosen checkpoint id.
    - the new run persists a bounded `execution_mode` value and replay override metadata exactly once on the branch row.
    - the source run's `latest_checkpoint_id`, checkpoint count, and event count remain unchanged after branch creation.
    - historical runs without replay lineage continue to load through `Workflows.get_run!/1` and existing tests still pass.
    Include one invalid-input test that rejects a replay request pointing at a checkpoint that does not belong to the given source run.
  </action>
  <verify>
    <automated>mix test test/scoria/workflows/replay_branch_test.exs test/scoria/workflows_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/scoria/workflows/replay_branch_test.exs` exists.
    - The new test file contains assertions for `source_run_id`, `source_checkpoint_id`, `execution_mode`, and source-run immutability.
    - The negative case proves mismatched source run / checkpoint pairs are rejected.
    - The focused test command fails before implementation or covers missing functionality directly.
  </acceptance_criteria>
  <done>Replay lineage expectations are executable and source-run immutability is defined before schema or API changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Add typed replay lineage fields and implement transactional branch creation</name>
  <read_first>
    - lib/scoria/workflows/run.ex
    - lib/scoria/workflows/checkpoint.ex
    - lib/scoria/workflows.ex
    - priv/repo/migrations/20260511000100_create_workflow_tables.exs
    - priv/repo/migrations/20260513000100_add_canonical_identity_to_workflow_runs.exs
  </read_first>
  <files>lib/scoria/workflows/run.ex, lib/scoria/workflows.ex, priv/repo/migrations/TIMESTAMP_add_replay_lineage_to_workflow_runs.exs</files>
  <action>
    Add additive replay lineage support to the workflow persistence layer.
    In `lib/scoria/workflows/run.ex`, extend the schema and changeset with typed fields for `source_run_id`, `source_checkpoint_id`, `execution_mode`, and replay override metadata. `execution_mode` must be validated against a bounded set that keeps Phase 37 narrow, for example `live`, `replay`, and `historical_stubbed` only if already needed by current code; do not solve full Phase 38 safety policy here.
    In the new migration, alter `ai_workflow_runs` to add nullable lineage columns plus indexes on `source_run_id`, `source_checkpoint_id`, and any combined lookup needed by operator reads. Keep historical compatibility: old rows must not require synthetic backfill.
    In `lib/scoria/workflows.ex`, add a transactional branch-creation API such as `create_replay_branch(source_run_id, source_checkpoint_id, attrs)` that:
    - loads the source run and checkpoint
    - verifies the checkpoint belongs to that run
    - inserts a brand new workflow run row carrying copied canonical identity (`actor_id`, `tenant_id`, `session_id`, `root_role_id`) plus typed replay lineage
    - seeds an initial replay-start checkpoint and event on the branch run
    - updates the branch run's `latest_checkpoint_id`
    The source run and source checkpoint must remain unchanged. Do not create a replay-only run table or a second engine.
  </action>
  <verify>
    <automated>mix test test/scoria/workflows/replay_branch_test.exs test/scoria/workflows_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/scoria/workflows/run.ex` contains typed replay lineage fields and changeset support.
    - `lib/scoria/workflows.ex` defines a replay branch creation function.
    - The branch creation flow inserts a new run and at least one replay-start checkpoint/event in one transaction.
    - Historical runs without replay lineage still load.
    - The focused test command passes.
  </acceptance_criteria>
  <done>Replay branch lineage becomes first-class workflow truth and can be created transactionally without mutating source history.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the lineage migration applies cleanly on a real database</name>
  <read_first>
    - priv/repo/migrations/TIMESTAMP_add_replay_lineage_to_workflow_runs.exs
    - .planning/phases/37-replay-lineage-branch-model/37-VALIDATION.md
  </read_first>
  <files>priv/repo/migrations/TIMESTAMP_add_replay_lineage_to_workflow_runs.exs</files>
  <action>
    Run the replay-lineage migration through one real local cycle after Task 2 lands: `mix ecto.migrate`, `mix ecto.rollback --step 1`, and `mix ecto.migrate` again. If a dedicated migration compatibility test file already exists for this repo, extend it to assert the new workflow-run lineage columns and indexes. Keep the migration additive and reversible; do not introduce manual backfill or destructive rewrites.
  </action>
  <verify>
    <automated>mix ecto.migrate && mix ecto.rollback --step 1 && mix ecto.migrate</automated>
  </verify>
  <acceptance_criteria>
    - The migrate/rollback/migrate cycle exits 0.
    - `ai_workflow_runs` includes replay lineage columns after migration.
    - The migration is additive and does not require synthetic backfill for old runs.
  </acceptance_criteria>
  <done>The replay-lineage schema is proven on a real database before runtime and UI work depend on it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replay request -> workflow persistence | Untrusted replay inputs become durable branch truth |
| existing workflow rows -> additive lineage migration | Historical workflow truth must remain readable after schema change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering | replay branch API | mitigate | Validate source-run/source-checkpoint pairing transactionally and persist typed lineage once on the branch row. |
| T-37-02 | Repudiation | workflow run truth | mitigate | Add first-class lineage columns plus replay-start checkpoint/event evidence so operators can prove branch origin later. |
| T-37-03 | Denial of Service | migration compatibility | mitigate | Keep the schema additive and require migrate/rollback/migrate proof before later plans depend on the new columns. |
</threat_model>

<verification>
- `mix test test/scoria/workflows/replay_branch_test.exs test/scoria/workflows_test.exs`
- `mix ecto.migrate && mix ecto.rollback --step 1 && mix ecto.migrate`
</verification>

<success_criteria>
Replay branches are durable new workflow runs with typed lineage truth, the source run stays immutable, and the additive schema change is proven against a real migration cycle.
</success_criteria>

<output>
After completion, create `.planning/phases/37-replay-lineage-branch-model/37-01-SUMMARY.md`
</output>
