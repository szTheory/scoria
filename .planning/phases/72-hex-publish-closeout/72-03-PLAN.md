---
phase: 72-hex-publish-closeout
plan: 03
type: execute
wave: 3
depends_on:
  - 72-02-PLAN.md
files_modified:
  - README.md
  - test/scoria/package_surface_test.exs
  - docs/operator_verification.md
  - release-please-config.json
  - test/scoria/ci_policy_contract_test.exs
autonomous: true
requirements:
  - HEX-02
must_haves:
  truths:
    - "Hex.pm lists 0.1.0 before any README change (gate from 72-02-SUMMARY or re-curl)."
    - "README primary install is {:scoria, ~> 0.1, hex: :scoria} with commented GitHub fallback."
    - "package_surface_test asserts Hex-primary install story."
    - "release-as pin removed; manifest 0.1.0; contract tests reflect post-ship config."
  artifacts:
    - path: README.md
      provides: "Hex-primary adopter install + badge"
    - path: test/scoria/package_surface_test.exs
      provides: "Executable install contract"
  key_links:
    - from: README.md
      to: https://hex.pm/packages/scoria
      via: Hex badge and primary dep
---

<objective>
Flip adopter-facing docs and tests only after live Hex `0.1.0`, add operator post-publish appendix, remove one-time `release-as`, and update post-ship contract assertions.

Purpose: HEX-02 closeout (D-72-11 through D-72-18, D-72-26–34).
Output: Hex-primary README; post-publish maintainer checks documented.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/72-hex-publish-closeout/72-CONTEXT.md
@.planning/phases/72-hex-publish-closeout/72-VERIFICATION.md
@lib/scoria/adopter_doc_contract.ex
@.planning/phases/70-docs-truth-foundation/70-CONTEXT.md
@README.md
</context>

<threat_model>
<trust_boundaries>
- README is first adopter touchpoint — must not advertise unavailable registry dep.
</trust_boundaries>
<stride_register>
- threat_id: T-72-05
  category: Tampering
  component: README.md / package_surface_test.exs
  disposition: mitigate
  mitigation_plan: First task re-verifies hex.pm 0.1.0; test enforces single uncommented Hex dep.
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 72-03-01: Registry gate — confirm Hex 0.1.0 before doc flip</name>
  <requirement_ids>HEX-02</requirement_ids>
  <files>.planning/phases/72-hex-publish-closeout/72-VERIFICATION.md</files>
  <read_first>
    .planning/phases/72-hex-publish-closeout/72-VERIFICATION.md
    .planning/phases/72-hex-publish-closeout/72-02-SUMMARY.md
  </read_first>
  <action>
Before editing README:

1. Run `curl -fsS https://hex.pm/api/packages/scoria/releases/0.1.0` — must succeed.
2. If 72-02-SUMMARY exists, confirm it cites publish evidence; else append gate row to `72-VERIFICATION.md`:

```markdown
## Adopter flip gate

| Check | Result | Date |
|-------|--------|------|
| hex.pm 0.1.0 API | 200 OK | YYYY-MM-DD |
```

**Abort README changes if curl fails** (D-72-11).
  </action>
  <acceptance_criteria>
- `curl -fsS https://hex.pm/api/packages/scoria/releases/0.1.0` exits 0
- `72-VERIFICATION.md` contains `Adopter flip gate` or publish evidence with 0.1.0 confirmed
  </acceptance_criteria>
  <verify>
    <automated>curl -fsS https://hex.pm/api/packages/scoria/releases/0.1.0</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 72-03-02: Flip README install block, badge, and status line</name>
  <requirement_ids>HEX-02</requirement_ids>
  <files>README.md</files>
  <read_first>
    README.md
    lib/scoria/adopter_doc_contract.ex
    .planning/phases/72-hex-publish-closeout/72-CONTEXT.md
    .planning/phases/70-docs-truth-foundation/70-CONTEXT.md
  </read_first>
  <action>
In `README.md` **Install** section (D-72-13, D-72-26–31):

1. Remove pre-publish disclaimer ("until the first Hex publish lands…").
2. Replace `## Status` Hex-metadata paragraph with: `Current release: \`0.1.0\` on [Hex](https://hex.pm/packages/scoria).`
3. Primary deps block (uncommented only one active line):
   ```elixir
   {:scoria, "~> 0.1", hex: :scoria},
   # Fork or pinned patch only: {:scoria, github: "szTheory/scoria", tag: "v0.1.0"}
   ```
4. One prose line after block: "Tagged GitHub installs are for forks and pinned patches; prefer Hex for normal adoption."
5. **Next steps (3 commands):** `mix deps.get` → `mix scoria.install` → `mix ecto.migrate` — link to `## Verification` for `mix test.adoption`.
6. Keep installer summary bullets (dashboard, migrations, runtime defaults, Tailwind optional).
7. Add Hex badge near CI badge: `[![Hex.pm](https://img.shields.io/hexpm/v/scoria.svg)](https://hex.pm/packages/scoria)`
8. **Do not** add `### Bleeding edge` subsection (R-72-07).
9. **Keep unchanged:** `## Quickstart`, `### Upgrading or re-running install`, capability banner, lane guide, maintainer tease to operator guide.

Do not duplicate Quickstart API samples in Install.
  </action>
  <acceptance_criteria>
- `rg '{:scoria, \"~> 0\\.1\", hex: :scoria}' README.md` matches
- `rg 'hexpm/v/scoria' README.md` matches
- `rg 'until the first Hex publish lands' README.md` returns no matches
- `rg '### Bleeding edge' README.md` returns no matches
- Exactly one uncommented `{:scoria,` dep line in Install deps block (github line commented)
  </acceptance_criteria>
  <verify>
    <automated>rg '{:scoria, "~> 0.1", hex: :scoria}' README.md && rg 'hexpm/v/scoria' README.md && ! rg 'until the first Hex publish lands' README.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 72-03-03: Flip package_surface_test to Hex-primary</name>
  <requirement_ids>HEX-02</requirement_ids>
  <files>test/scoria/package_surface_test.exs</files>
  <read_first>
    test/scoria/package_surface_test.exs
    README.md
    .planning/phases/72-hex-publish-closeout/72-CONTEXT.md
  </read_first>
  <action>
Replace test `"README keeps the pre-publish tagged GitHub install story"` with `"Hex-primary install with optional GitHub fallback"`:

Assertions:
- `readme =~ '{:scoria, "~> 0.1", hex: :scoria}'`
- `readme =~ 'github: "szTheory/scoria", tag: "v0.1.0"'` (commented fallback OK)
- `refute readme =~ "until the first Hex publish lands"`
- `refute` two uncommented `{:scoria,` lines (use regex or line scan)
- Guides from `@docs_extras` still linked in README

Keep other tests in file unchanged unless broken by README edits.
  </action>
  <acceptance_criteria>
- `MIX_ENV=test mix test test/scoria/package_surface_test.exs` exits 0
- Test name includes `Hex-primary install`
- `rg 'pre-publish tagged GitHub' test/scoria/package_surface_test.exs` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/package_surface_test.exs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 72-03-04: Add post-publish registry checks to operator guide</name>
  <requirement_ids>HEX-02</requirement_ids>
  <files>docs/operator_verification.md</files>
  <read_first>
    docs/operator_verification.md
    .planning/phases/72-hex-publish-closeout/72-CONTEXT.md
  </read_first>
  <action>
Under existing `## Hex release & recovery (maintainers)` section, add subsection:

`### Post-publish registry checks (maintainers)` (~15–25 lines, D-72-34):

- curl hex.pm package and release version
- hexdocs `source_ref` spot-check for v0.1.0
- clean-project `mix deps.get` + compile with `{:scoria, "~> 0.1", hex: :scoria}`
- optional `mix scoria.install --check`
- pointer: flip README only after release API returns 200
- record dated evidence in phase verification ledger (not in this doc)

No workflow run URLs in operator guide (D-72-20).
  </action>
  <acceptance_criteria>
- `rg '### Post-publish registry checks' docs/operator_verification.md` matches
- `rg 'phase verification ledger' docs/operator_verification.md` matches
- `rg 'github\\.com/actions/runs' docs/operator_verification.md` returns no matches in new subsection
  </acceptance_criteria>
  <verify>
    <automated>rg 'Post-publish registry checks' docs/operator_verification.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 72-03-05: Remove release-as pin and flip post-ship contract tests</name>
  <requirement_ids>HEX-01, HEX-02</requirement_ids>
  <files>release-please-config.json, test/scoria/ci_policy_contract_test.exs, .release-please-manifest.json</files>
  <read_first>
    release-please-config.json
    .release-please-manifest.json
    test/scoria/ci_policy_contract_test.exs
    .planning/phases/72-hex-publish-closeout/72-CONTEXT.md
  </read_first>
  <action>
1. In `release-please-config.json`, remove `"release-as": "0.1.0"` from packages `"."` object (D-72-18). Commit message intent: `chore: remove release-as pin (0.1.0 shipped)`.

2. Confirm `.release-please-manifest.json` is `{ ".": "0.1.0" }` (fix if not).

3. Update `ci_policy_contract_test.exs` bootstrap test (D-72-23):
   - Assert manifest contains `"0.1.0"` (not `0.0.0`)
   - `refute config =~ "release-as"`

4. Append **Post-flip smoke** to `72-VERIFICATION.md` with dated `mix deps.get` / optional 24h row placeholder.
  </action>
  <acceptance_criteria>
- `rg 'release-as' release-please-config.json` returns no matches
- `rg '\"0\\.1\\.0\"' .release-please-manifest.json` matches
- `MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs` exits 0
- `rg '0\\.0\\.0' test/scoria/ci_policy_contract_test.exs` returns no matches in manifest assertion
  </acceptance_criteria>
  <verify>
    <automated>! rg 'release-as' release-please-config.json && MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs</automated>
  </verify>
</task>

</tasks>

<verification>
- [ ] `MIX_ENV=test mix test test/scoria/package_surface_test.exs test/scoria/ci_policy_contract_test.exs` green
- [ ] `MIX_ENV=dev mix scoria.release_preview` green
- [ ] Hex badge renders in README preview
- [ ] Task order preserved: gate → README → tests → operator appendix → release-as
</verification>
