---
phase: 72-hex-publish-closeout
plan: 02
type: execute
wave: 2
depends_on:
  - 72-01-PLAN.md
files_modified:
  - .planning/phases/72-hex-publish-closeout/72-VERIFICATION.md
autonomous: false
requirements:
  - HEX-01
must_haves:
  truths:
    - "Release PR targeting 0.1.0 merged; git tag v0.1.0 and GitHub Release exist."
    - "publish-hex ran ci-verify, dry-run, and live publish; hex.pm lists 0.1.0."
    - "72-VERIFICATION.md records publish workflow URL, SHA, and hex.pm check."
  artifacts:
    - path: .planning/phases/72-hex-publish-closeout/72-VERIFICATION.md
      provides: "Publish evidence for 72-03 gate"
  key_links:
    - from: publish-hex job
      to: https://hex.pm/packages/scoria
      via: mix hex.publish after ci-verify
---

<objective>
Execute Release Please path: review Release PR for `0.1.0`, merge, verify tag + GitHub Release, confirm `publish-hex` publishes to Hex, and record ship proof in `72-VERIFICATION.md`.

Purpose: HEX-01 live publish (D-72-01, D-72-09, D-72-11 gate input).
Output: Registry lists `0.1.0` — unblocks 72-03 README flip. **README.md MUST NOT be modified.**
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/72-hex-publish-closeout/72-CONTEXT.md
@.planning/phases/72-hex-publish-closeout/72-VERIFICATION.md
@docs/operator_verification.md
@CHANGELOG.md
@release-please-config.json
@.release-please-manifest.json
</context>

<threat_model>
<trust_boundaries>
- HEX_API_KEY used only in GitHub Actions publish steps.
- Release PR body is public adopter narrative.
</trust_boundaries>
<stride_register>
- threat_id: T-72-04
  category: Tampering
  component: Hex registry
  disposition: mitigate
  mitigation_plan: Verify hex.pm API + publish workflow logs before any README flip (72-03).
</stride_register>
</threat_model>

<tasks>

<task type="checkpoint" gate="human">
  <name>Task 72-02-01: Review and merge Release PR for 0.1.0</name>
  <requirement_ids>HEX-01</requirement_ids>
  <read_first>
    .planning/phases/72-hex-publish-closeout/72-CONTEXT.md
    release-please-config.json
    .release-please-manifest.json
    docs/operator_verification.md
  </read_first>
  <action>
After 72-01 merged and human checklist complete:

1. Confirm Release Please opened/updated Release PR on `main` targeting version **0.1.0 only** (not 0.1.1, 0.2.0, 1.0.0).
2. Confirm CI green on `release-please--**` branch (policy + test via ci-verify).
3. Review CHANGELOG `[Unreleased]` → `0.1.0` body; no `### Summary` under version headings.
4. Optional D-72-25: `curl -fsS https://hex.pm/api/packages/scoria` → expect 404 before first publish.
5. Merge Release PR to `main`.

Reject: merge before 72-01 enablement; tag-only publish without Release PR (R-72-01).
  </action>
  <acceptance_criteria>
- GitHub Release exists for tag `v0.1.0`
- Release PR title/body references version 0.1.0
- `curl -fsS https://api.github.com/repos/szTheory/scoria/git/refs/tags/v0.1.0` succeeds (or equivalent `gh` check)
  </acceptance_criteria>
</task>

<task type="checkpoint" gate="human">
  <name>Task 72-02-02: Verify publish-hex and hex.pm 0.1.0</name>
  <requirement_ids>HEX-01</requirement_ids>
  <read_first>
    .github/workflows/release-please.yml
    .planning/phases/72-hex-publish-closeout/72-VERIFICATION.md
  </read_first>
  <action>
After Release PR merge:

1. Confirm `publish-hex` workflow ran on tag commit: `needs.verify` (ci-verify) → dry-run → `mix hex.publish --yes`.
2. Record in `72-VERIFICATION.md` **Publish evidence** section:
   - Date
   - Publish workflow run URL
   - Commit SHA published
   - `curl -fsS https://hex.pm/api/packages/scoria/releases/0.1.0` output summary (version field present)
3. Smoke in clean temp dir: `mix new smoke_hex --sup`; add `{:scoria, "~> 0.1", hex: :scoria}` to deps; `mix deps.get` exits 0.
4. Confirm `.release-please-manifest.json` reads `{ ".": "0.1.0" }` after merge (fix manually if recovery path used).

If publish failed but tag exists: use `hex-publish.yml` recovery **only** if `curl` release 0.1.0 returns 404 (D-72-02). Never re-publish existing version.
  </action>
  <acceptance_criteria>
- `curl -fsS https://hex.pm/api/packages/scoria/releases/0.1.0` exits 0
- `72-VERIFICATION.md` contains publish workflow run URL and commit SHA
- `.release-please-manifest.json` contains `"0.1.0"`
- Smoke `mix deps.get` with Hex dep succeeded (note path/date in ledger)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 72-02-03: Record remote CI on publish commit</name>
  <requirement_ids>HEX-01</requirement_ids>
  <files>.planning/phases/72-hex-publish-closeout/72-VERIFICATION.md</files>
  <read_first>
    .planning/phases/72-hex-publish-closeout/72-VERIFICATION.md
    .planning/phases/71-release-infrastructure/71-VERIFICATION.md
  </read_first>
  <action>
Append to `72-VERIFICATION.md`:

## Publish commit CI (non-waivable)

| Field | Value |
|-------|-------|
| Date | ... |
| Commit SHA | (tag v0.1.0 commit) |
| CI run URL | (ci-verify on publish commit or publish-hex verify job) |
| policy | success |
| test | success |

Closes Phase 71 gate-zero deferral (71-CONTEXT D-25).
  </action>
  <acceptance_criteria>
- `rg 'Publish commit CI' .planning/phases/72-hex-publish-closeout/72-VERIFICATION.md` matches
- `rg 'policy.*success' .planning/phases/72-hex-publish-closeout/72-VERIFICATION.md` matches
- `rg 'test.*success' .planning/phases/72-hex-publish-closeout/72-VERIFICATION.md` matches
  </acceptance_criteria>
  <verify>
    <automated>rg 'Publish commit CI' .planning/phases/72-hex-publish-closeout/72-VERIFICATION.md</automated>
  </verify>
</task>

</tasks>

<verification>
- [ ] `https://hex.pm/packages/scoria` shows version 0.1.0
- [ ] Git tag `v0.1.0` exists with CHANGELOG release body
- [ ] `MIX_ENV=dev mix scoria.release_preview` green on publish commit
- [ ] README still pre-publish (github install story) until 72-03
</verification>
