---
phase: 72-hex-publish-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - .github/workflows/release-please.yml
  - .github/workflows/hex-publish.yml
  - test/scoria/ci_policy_contract_test.exs
autonomous: false
requirements:
  - HEX-01
must_haves:
  truths:
    - "mix.exs uses @version module attribute matching publish workflow grep."
    - "publish-hex and hex-publish publish jobs are enabled with dry-run + live publish steps."
    - "ci_policy_contract_test asserts Phase 72 enabled publish shape (not if: false)."
    - "Integration PR records green remote ci-verify URL/SHA in 72-VERIFICATION.md."
  artifacts:
    - path: mix.exs
      provides: "@version shape for release and recovery workflows"
    - path: .planning/phases/72-hex-publish-closeout/72-VERIFICATION.md
      provides: "Integration PR + gate-zero attestation ledger"
  key_links:
    - from: .github/workflows/release-please.yml
      to: mix.exs
      via: grep '@version'
---

<objective>
Clear integration blockers before any Release PR merge: refactor `@version`, enable both publish jobs with contract co-flip, open integration PR with green remote `ci-verify`, and complete human secrets/permissions checklist.

Purpose: HEX-01 pre-publish gates (D-72-05 through D-72-10, D-72-22).
Output: Ready for Release PR review in 72-02. **README.md MUST NOT be modified in this plan.**
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/72-hex-publish-closeout/72-CONTEXT.md
@.planning/phases/72-hex-publish-closeout/72-RESEARCH.md
@.planning/phases/71-release-infrastructure/71-VERIFICATION.md
@.github/workflows/ci-verify.yml
@.github/workflows/release-please.yml
@.github/workflows/hex-publish.yml
</context>

<threat_model>
<trust_boundaries>
- HEX_API_KEY never committed; only documented in operator guide.
- Workflow YAML changes control production publish path.
</trust_boundaries>
<stride_register>
- threat_id: T-72-01
  category: Tampering
  component: mix.exs / workflows
  disposition: mitigate
  mitigation_plan: Contract tests assert enabled publish + @version grep; integration PR requires remote CI green.
- threat_id: T-72-02
  category: Elevation of Privilege
  component: publish jobs
  disposition: mitigate
  mitigation_plan: publish-hex needs release_created + verify success; hex-publish needs verify success; full ci-verify bar.
- threat_id: T-72-03
  category: Repudiation
  component: 72-VERIFICATION.md
  disposition: mitigate
  mitigation_plan: Record integration PR CI run URL + SHA (non-waivable gate-zero closeout).
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 72-01-01: Refactor mix.exs to @version module attribute</name>
  <requirement_ids>HEX-01</requirement_ids>
  <files>mix.exs</files>
  <read_first>
    mix.exs
    .github/workflows/release-please.yml
    .github/workflows/hex-publish.yml
    .planning/phases/72-hex-publish-closeout/72-CONTEXT.md
  </read_first>
  <action>
At top of `Scoria.MixProject` (after `use Mix.Project`), add:

```elixir
@version "0.1.0"
```

In `project/0`:
- Remove `version = "0.1.0"` local variable.
- Set `version: @version`.
- Change `docs: docs(version)` to `docs: docs()`.

Change `defp docs(version)` to `defp docs` with `source_ref: "v#{@version}"` (no parameter).

Do not change deps, package files list, or app name.
  </action>
  <acceptance_criteria>
- `rg '@version \"0\\.1\\.0\"' mix.exs` matches at least one line
- `rg 'version = \"0\\.1\\.0\"' mix.exs` returns no matches
- `rg 'defp docs\\(version\\)' mix.exs` returns no matches
- `MIX_ENV=test mix compile` exits 0
  </acceptance_criteria>
  <verify>
    <automated>rg '@version "0\.1\.0"' mix.exs && ! rg 'version = "0\.1\.0"' mix.exs && MIX_ENV=test mix compile</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 72-01-02: Enable publish jobs in both workflows</name>
  <requirement_ids>HEX-01</requirement_ids>
  <files>.github/workflows/release-please.yml, .github/workflows/hex-publish.yml</files>
  <read_first>
    .github/workflows/release-please.yml
    .github/workflows/hex-publish.yml
    .planning/phases/71-release-infrastructure/71-03-PLAN.md
    ~/projects/oarlock/.github/workflows/release-please.yml
  </read_first>
  <action>
In `release-please.yml` job `publish-hex`:
- Replace `if: false` with `if: ${{ needs.release-please.outputs.release_created == 'true' }}`
- Uncomment dry-run and publish steps using `HEX_API_KEY` secret and `mix hex.publish --dry-run --yes` / `mix hex.publish --yes`
- Keep `needs: [release-please, verify]` and `@version` grep step

In `hex-publish.yml` job `publish`:
- Replace `if: false` with `if: ${{ needs.verify.result == 'success' }}`
- Uncomment matching dry-run and publish steps
- Keep checkout `ref: ${{ inputs.tag }}` and `@version` grep

Do not add `sync_release_summary` job.
  </action>
  <acceptance_criteria>
- `rg 'if: false' .github/workflows/release-please.yml` returns no matches in publish-hex job
- `rg 'mix hex\\.publish --dry-run --yes' .github/workflows/release-please.yml` matches
- `rg 'mix hex\\.publish --yes' .github/workflows/release-please.yml` matches
- `rg 'if: false' .github/workflows/hex-publish.yml` returns no matches in publish job
- `rg 'HEX_API_KEY' .github/workflows/release-please.yml` matches
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs --only line:0 2>/dev/null; rg 'mix hex.publish --dry-run' .github/workflows/release-please.yml .github/workflows/hex-publish.yml</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 72-01-03: Co-flip ci_policy_contract_test for Phase 72 publish</name>
  <requirement_ids>HEX-01</requirement_ids>
  <files>test/scoria/ci_policy_contract_test.exs</files>
  <read_first>
    test/scoria/ci_policy_contract_test.exs
    .github/workflows/release-please.yml
    .github/workflows/hex-publish.yml
    .planning/phases/72-hex-publish-closeout/72-CONTEXT.md
  </read_first>
  <action>
Update tests that assert Phase 71 stubbed publish:

1. Rename or replace `"release-please.yml uses ci-verify and disables publish in phase 71"` test to assert:
   - `publish-hex` job `if` references `release_created` (not `if: false`)
   - `mix hex.publish --dry-run --yes` and `mix hex.publish --yes` present
   - `refute release_please =~ "if: false"` for publish-hex section

2. Update `"hex-publish.yml supports workflow_dispatch recovery with verify"` to assert:
   - publish job enabled (`needs.verify.result == 'success'` or equivalent)
   - `mix hex.publish --yes` present in publish job
   - Still `refute sync_release_summary`

Keep manifest `0.0.0` and `release-as` assertions until 72-03 (D-72-23).
  </action>
  <acceptance_criteria>
- `MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs` exits 0
- `rg 'disables publish in phase 71' test/scoria/ci_policy_contract_test.exs` returns no matches
- `rg 'if: false' test/scoria/ci_policy_contract_test.exs` returns no matches asserting publish disabled
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs</automated>
  </verify>
</task>

<task type="checkpoint" gate="human">
  <name>Task 72-01-04: Human secrets, permissions, and pre-merge smoke</name>
  <requirement_ids>HEX-01</requirement_ids>
  <read_first>
    docs/operator_verification.md
    .planning/phases/72-hex-publish-closeout/72-CONTEXT.md
  </read_first>
  <action>
**Human steps (do not automate secrets into repo):**

1. Generate Hex API key: `mix hex.user key generate` → `gh secret set HEX_API_KEY --repo szTheory/scoria`
2. Workflow permissions: `gh api -X PUT repos/szTheory/scoria/actions/permissions/workflow -f default_workflow_permissions=write -F can_approve_pull_request_reviews=true`
3. Optional: `RELEASE_PLEASE_TOKEN` only if GITHUB_TOKEN cannot open Release PRs
4. Local smoke: `mix hex.build`, `MIX_ENV=test mix test test/scoria/package_surface_test.exs test/scoria/ci_policy_contract_test.exs test/scoria/changelog_contract_test.exs`, `mix scoria.test.ci_trust`

**Do not merge Release PR until this checklist and 72-01-05 integration CI are complete (D-72-04).**
  </action>
  <acceptance_criteria>
- Maintainer confirms HEX_API_KEY exists in repo secrets (GitHub UI or `gh secret list`)
- Maintainer confirms workflow permissions applied
- Local smoke commands exited 0 (record date in checkpoint comment)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 72-01-05: Integration PR and remote ci-verify attestation</name>
  <requirement_ids>HEX-01</requirement_ids>
  <files>.planning/phases/72-hex-publish-closeout/72-VERIFICATION.md</files>
  <read_first>
    .planning/phases/71-release-infrastructure/71-VERIFICATION.md
    .planning/phases/72-hex-publish-closeout/72-CONTEXT.md
    .github/workflows/ci.yml
  </read_first>
  <action>
1. Open PR with tasks 72-01-01 through 72-01-03 changes (branch from current main).
2. Wait for remote `ci-verify` (policy + test jobs) green on PR head SHA.
3. Create `.planning/phases/72-hex-publish-closeout/72-VERIFICATION.md` with sections:

```markdown
# Phase 72 — Verification Ledger

## Integration PR (gate-zero closeout)

| Field | Value |
|-------|-------|
| Date | YYYY-MM-DD |
| PR URL | ... |
| Commit SHA | ... |
| CI run URL | ... |
| policy job | success |
| test job | success |

## Publish evidence

*(filled in plan 72-02)*

## Post-flip smoke

*(filled in plan 72-03)*
```

4. Merge integration PR to `main` after green CI.

Phase 71 gate-zero waiver does **not** waive this attestation on publish path (D-72-05).
  </action>
  <acceptance_criteria>
- File `.planning/phases/72-hex-publish-closeout/72-VERIFICATION.md` exists
- Ledger contains non-empty PR URL, Commit SHA, and CI run URL
- Ledger states `policy job | success` and `test job | success`
- Integration PR is merged to main
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/72-hex-publish-closeout/72-VERIFICATION.md && rg 'CI run URL' .planning/phases/72-hex-publish-closeout/72-VERIFICATION.md</automated>
  </verify>
</task>

</tasks>

<verification>
- [ ] `MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs` green
- [ ] `mix hex.build` succeeds locally
- [ ] No README.md changes in this plan's commits
- [ ] `.release-please-manifest.json` still shows `0.0.0` (do not pre-bump)
</verification>
