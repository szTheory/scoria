---
phase: 30-oban-infrastructure-and-queue-segregation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/config.exs
  - config/dev.exs
  - config/test.exs
  - config/runtime.exs
  - test/scoria/oban_config_test.exs
autonomous: true
requirements:
  - EVAL-01
must_haves:
  truths:
    - Oban limits concurrency separately for system, inference, and eval queues.
    - Production runtime environment variables can safely override these queue limits.
    - Test environments run Oban in manual testing mode to avoid race conditions.
  artifacts:
    - path: config/config.exs
      provides: Baseline Oban queue definitions
      contains: "system: 10"
    - path: config/runtime.exs
      provides: Dynamic queue configuration from environment
      contains: "OBAN_EVALS_CONCURRENCY"
    - path: test/scoria/oban_config_test.exs
      provides: Automated validation of Oban queue configuration
      contains: "defmodule Scoria.ObanConfigTest"
  key_links:
    - from: config/runtime.exs
      to: Oban application config
      via: Elixir Config native deep-merge
      pattern: "config :scoria, Oban"
---

<objective>
Configure Oban queues for system operations, inference, and scalable evaluation fan-out, supporting environment variable overrides.

Purpose: Establishing strict concurrency bounds is required before enabling massive evaluation runs, to prevent database pool exhaustion and Phoenix request starvation.
Output: Configured Oban queues in all environments, validated by automated tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Standard configuration mapping for Config in Elixir -->
# From config/config.exs pattern:
config :scoria, Oban,
  engine: Oban.Engines.Basic,
  queues: [connector_sync: 10, compaction: 10, ...], # keep existing, add new
  repo: Scoria.Repo

# From config/runtime.exs pattern:
config :scoria, Oban,
  queues: [
    system: String.to_integer(System.get_env("OBAN_SYSTEM_CONCURRENCY") || "10"),
    # ...
  ]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define baseline hardcoded queues and configuration validation test</name>
  <files>config/config.exs, config/dev.exs, config/test.exs, test/scoria/oban_config_test.exs</files>
  <behavior>
    - Assert `Application.get_env(:scoria, Oban)[:queues]` has system: 10, inference: 20, evals: 50
    - Assert test env is set to `testing: :manual` for Oban
  </behavior>
  <action>
    Create `test/scoria/oban_config_test.exs` to validate success criteria 1.
    Per D-01, D-03, D-04, D-05, update the Oban `queues:` list in `config/config.exs` (and `config/dev.exs` if overridden there), referencing analog `config/config.exs` from PATTERNS.md, to explicitly include:
    - `system: 10`
    - `inference: 20`
    - `evals: 50`
    Ensure any existing queues like `compaction` are retained.
    Per D-10, verify and ensure `config/test.exs` sets `testing: :manual` in the Oban config to avoid real execution during tests.
  </action>
  <verify>
    <automated>mix test test/scoria/oban_config_test.exs</automated>
  </verify>
  <done>Baseline configurations define all three required queues, testing is set to manual, and the validation test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Define runtime queue overrides</name>
  <files>config/runtime.exs, test/scoria/oban_config_test.exs</files>
  <action>
    Per D-02, inside the `if config_env() == :prod do` block in `config/runtime.exs`, add a new configuration for `config :scoria, Oban` specifically targeting `queues:`.
    Map `system:` to `OBAN_SYSTEM_CONCURRENCY` (fallback "10"), `inference:` to `OBAN_INFERENCE_CONCURRENCY` (fallback "20"), and `evals:` to `OBAN_EVALS_CONCURRENCY` (fallback "50").
    Convert the parsed env values to integers via `String.to_integer/1`.
    DO NOT use `Application.get_env/2` to merge existing config — rely entirely on native `Config` keyword deep-merging.
    Update `test/scoria/oban_config_test.exs` to add a test case that validates the production runtime config behavior, parsing `config/runtime.exs` dynamically or via an integration test.
  </action>
  <verify>
    <automated>mix test test/scoria/oban_config_test.exs</automated>
  </verify>
  <done>Production environment allows overriding queue sizes via ENV vars.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EnvVar→Config | System environment variables override concurrency settings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Denial of Service | config/runtime.exs | mitigate | Rely on integer parsing constraints; system administrators control ENV. |
</threat_model>

<verification>
Ensure the project still compiles and boots without configuration collisions.
</verification>

<success_criteria>
Configuration overrides can successfully parse environment variables, and `config.exs` safely defines the defaults.
</success_criteria>

<output>
After completion, create `.planning/phases/30-oban-infrastructure-and-queue-segregation/30-01-SUMMARY.md`
</output>