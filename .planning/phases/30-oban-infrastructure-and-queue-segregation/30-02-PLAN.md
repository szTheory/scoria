---
phase: 30-oban-infrastructure-and-queue-segregation
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - lib/scoria/workflows/batch_enqueue.ex
  - test/scoria/workflows/batch_enqueue_test.exs
autonomous: true
requirements:
  - EVAL-03
must_haves:
  truths:
    - Massive lists of Oban jobs can be inserted atomically without PostgreSQL parameter limit errors.
    - Batch insertion correctly wraps chunks inside an Ecto Multi transaction.
  artifacts:
    - path: lib/scoria/workflows/batch_enqueue.ex
      provides: Chunked Oban job insertion utility
      exports: ["enqueue_all"]
    - path: test/scoria/workflows/batch_enqueue_test.exs
      provides: Verification of chunked batching
      contains: "assert_enqueued"
  key_links:
    - from: lib/scoria/workflows/batch_enqueue.ex
      to: Scoria.Repo
      via: Ecto.Multi execution
      pattern: "Repo\\.transaction"
---

<objective>
Implement a batch queuing utility to insert thousands of jobs safely.

Purpose: Distributed evaluation fan-outs generate massive amounts of jobs simultaneously. Direct insertion triggers PostgreSQL parameter limit errors; chunked `Ecto.Multi` transactions solve this.
Output: `BatchEnqueue` utility module and its test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key concepts for the executor -->
From Oban docs for batch insertion within Ecto.Multi:
```elixir
Oban.insert_all(multi, multi_name, changesets_or_jobs)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement BatchEnqueue chunking</name>
  <files>lib/scoria/workflows/batch_enqueue.ex, test/scoria/workflows/batch_enqueue_test.exs</files>
  <behavior>
    - Test: Successfully chunks and enqueues a large list of jobs (e.g., > 1000) by the chunk limit (e.g., 500).
    - Test: Assert job enqueuing works using `assert_enqueued` after transaction commits.
  </behavior>
  <action>
    Per D-06, D-07, and D-08, implement `Scoria.Workflows.BatchEnqueue`.
    Reference analog `lib/scoria/observe/buffer.ex` (for Enum chunking/mapping) and `lib/scoria/eval.ex` (for Ecto.Multi usage) from PATTERNS.md.
    Expose `enqueue_all(jobs, opts \\ [])`. Get chunk size from opts with default `@default_chunk_size 500`.
    Use `Enum.chunk_every/2` to split `jobs`.
    Use `Enum.with_index()` and `Enum.reduce/3` over an initial `Ecto.Multi.new()`.
    Inside the reduce, call `Oban.insert_all(multi, :"batch_#{idx}", chunk)`.
    Finish by passing the multi to `Scoria.Repo.transaction()`.
    For testing (D-09, D-10), reference analog `test/scoria/workflows/event_compactor_test.exs` from PATTERNS.md to use `use Oban.Testing, repo: Scoria.Repo` and `assert_enqueued` to verify logic.
  </action>
  <verify>
    <automated>mix test test/scoria/workflows/batch_enqueue_test.exs</automated>
  </verify>
  <done>Batch enqueue primitive is implemented and fully tested with atomic chunk limits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB→Code | Massive job arrays can exhaust PostgreSQL connections |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02 | Denial of Service | BatchEnqueue | mitigate | Hardcode chunk limits (500) to keep parameter counts below ~65k Postgres max. |
</threat_model>

<verification>
Test suite executes rapidly using `Oban.Testing` asserting enqueued operations rather than polling a background runner.
</verification>

<success_criteria>
`BatchEnqueue.enqueue_all/2` operates cleanly over arbitrary list sizes and commits atomically.
</success_criteria>

<output>
After completion, create `.planning/phases/30-oban-infrastructure-and-queue-segregation/30-02-SUMMARY.md`
</output>
