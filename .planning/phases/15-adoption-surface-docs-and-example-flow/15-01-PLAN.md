---
phase: 15-adoption-surface-docs-and-example-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - lib/scoria.ex
  - lib/scoria/runtime.ex
  - lib/scoria/identity.ex
  - lib/scoria/prompt_policy.ex
autonomous: true
nyquist_compliant: true
requirements:
  - ADOP-01
  - ADOP-04
must_haves:
  truths:
    - "The public README teaches Scoria as the Phoenix-native runtime and operator surface for identity-aware runs instead of leading with a feature inventory."
    - "The first documented host-app path uses `Scoria`, preserves the `session_id` versus `run_id` distinction, and treats `/scoria` as evidence rather than the app integration boundary."
    - "The top-level public module docs no longer read like placeholders and instead explain the intended `Scoria`-first layering with deeper modules introduced deliberately."
  artifacts:
    - path: "README.md"
      provides: "Runtime-first public adoption story and quickstart"
    - path: "lib/scoria.ex"
      provides: "Top-level public facade docs aligned to the happy path"
    - path: "lib/scoria/runtime.ex"
      provides: "Advanced lifecycle-layer docs positioned behind `Scoria`"
    - path: "lib/scoria/identity.ex"
      provides: "Canonical identity-boundary docs for Phoenix edge normalization"
    - path: "lib/scoria/prompt_policy.ex"
      provides: "Governance-default docs introduced after the runtime basics"
  key_links:
    - from: "README.md"
      to: "lib/scoria.ex"
      via: "quickstart teaches the same public entrypoints exposed by the facade"
      pattern: "Scoria\\.(identity|start_run|resume_run|get_run|list_runs_for_session)"
    - from: "README.md"
      to: "test/scoria/runtime_integration_test.exs"
      via: "run/session semantics and approval resume match the existing runtime proof"
      pattern: "session_id|run_id|resume_run"
    - from: "lib/scoria.ex"
      to: "lib/scoria/runtime.ex"
      via: "facade-to-lifecycle layering remains explicit and documented"
      pattern: "Runtime\\.(start_run|resume_run|get_run|get_run_detail|list_runs_for_session)"
---

<objective>
Align the top-level docs and public module documentation with the shipped Keystone runtime surface.

Purpose: satisfy the runtime-first adoption posture before adding deeper example and verification guides.
Output: a rewritten `README.md` plus layered moduledocs for `Scoria`, `Scoria.Runtime`, `Scoria.Identity`, and `Scoria.PromptPolicy`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/MILESTONE-ARC.md
@.planning/phases/15-adoption-surface-docs-and-example-flow/15-CONTEXT.md
@.planning/phases/15-adoption-surface-docs-and-example-flow/15-RESEARCH.md
@.planning/phases/15-adoption-surface-docs-and-example-flow/15-PATTERNS.md
@.planning/phases/13-public-runtime-api-and-session-lifecycle/13-04-PLAN.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-03-PLAN.md
@README.md
@lib/scoria.ex
@lib/scoria/runtime.ex
@lib/scoria/identity.ex
@lib/scoria/prompt_policy.ex
@lib/mix/tasks/scoria.install.ex
@test/scoria/runtime_integration_test.exs

<interfaces>
From lib/scoria.ex:
```elixir
def identity(attrs \\ %{})
def start_run(identity, opts \\ [])
def resume_run(run_id, opts \\ [])
def get_run(run_id)
def get_run_detail(run_id)
def list_runs_for_session(session_id)
```

From lib/scoria/runtime.ex:
```elixir
def start_run(identity, opts \\ [])
def resume_run(run_id, opts \\ [])
def get_run(run_id)
def get_run_detail(run_id)
def list_runs_for_session(session_id)
```

From lib/scoria/identity.ex:
```elixir
def from_conn_assigns(assigns)
def from_session(session)
def normalize(attrs)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the README around the shipped runtime-first adoption path</name>
  <read_first>README.md, lib/scoria.ex, lib/mix/tasks/scoria.install.ex, test/scoria/runtime_integration_test.exs, .planning/phases/15-adoption-surface-docs-and-example-flow/15-PATTERNS.md</read_first>
  <files>README.md</files>
  <acceptance_criteria>`README.md` leads with the `Scoria`-first runtime story instead of a capability inventory per D-01 through D-05; the quickstart teaches install, identity normalization, `start_run/2`, `run_id` persistence, `session_id` continuity, exact `resume_run/2`, and `/scoria/workflows/:run_id` as operator evidence per D-03 and D-11; the default lane remains clearly separate from the optional knowledge lane per D-16 and D-17; the wording stays anchored to the existing `Scoria` facade and runtime integration test rather than inventing new runtime semantics.</acceptance_criteria>
  <action>Replace the stale feature-inventory-first README with a runtime-first quickstart per D-01 through D-05, D-19 through D-25. The opening sequence must explicitly follow D-03: what Scoria is, why Phoenix teams use it, install, start a run, persist `session_id` and `run_id` with the D-22 distinction, resume by exact `run_id`, open `/scoria` and `/scoria/workflows/:run_id` as evidence per D-11, then expand into capability layers below the quickstart. Keep the knowledge lane explicitly optional per D-16 and D-17, and do not teach `Scoria.Workflows` as the normal app entrypoint per D-25.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/runtime_integration_test.exs test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.install_route_smoke_test.exs && rg -n "Scoria.start_run|Scoria.resume_run|session_id|run_id|/scoria/workflows/:run_id|Optional knowledge lane" README.md</automated>
  </verify>
  <done>`README.md` now presents the actual Keystone public surface, preserves the run-versus-session contract, and demotes capability buckets below the primary quickstart.</done>
</task>

<task type="auto">
  <name>Task 2: Align public moduledocs to the `Scoria`-first teaching posture</name>
  <read_first>lib/scoria.ex, lib/scoria/runtime.ex, lib/scoria/identity.ex, lib/scoria/prompt_policy.ex, .planning/phases/15-adoption-surface-docs-and-example-flow/15-PATTERNS.md, .planning/phases/13-public-runtime-api-and-session-lifecycle/13-04-PLAN.md</read_first>
  <files>lib/scoria.ex, lib/scoria/runtime.ex, lib/scoria/identity.ex, lib/scoria/prompt_policy.ex</files>
  <acceptance_criteria>The edited moduledocs compile and are read from freshly built artifacts, not stale beams; `Scoria` is documented as the happy-path facade per D-19 and D-20; `Scoria.Identity` teaches edge normalization and the `session_id` versus `run_id` boundary per D-21 and D-22; `Scoria.Runtime` is clearly secondary/advanced per D-23; and `Scoria.PromptPolicy` appears later as a governance/defaults concept per D-24.</acceptance_criteria>
  <action>Update the public moduledocs so the code-level docs mirror the README posture without changing runtime semantics. `Scoria` must remain the obvious happy path per D-19 and D-20; `Scoria.Identity` must explain the Phoenix-edge normalization boundary early per D-21; `Scoria.Runtime` must be documented as the fuller advanced lifecycle layer per D-23; `Scoria.PromptPolicy` must be positioned as a later governance/defaults concept per D-24. Keep the language explicit that `session_id` is host-owned continuity and `run_id` is Scoria’s exact durable execution handle per D-22.</action>
  <verify>
    <automated>mix compile && mix run -e 'mods=[Scoria, Scoria.Runtime, Scoria.Identity, Scoria.PromptPolicy]; Enum.each(mods, fn mod -> case Code.fetch_docs(mod) do {:docs_v1, _, :elixir, _, moduledoc, _, _, _} -> if moduledoc in [nil, :none], do: raise(\"missing moduledoc for #{inspect(mod)}\"); _ -> raise(\"missing docs for #{inspect(mod)}\") end end)' && rg -n "session_id|run_id|happy path|advanced|governance" lib/scoria.ex lib/scoria/runtime.ex lib/scoria/identity.ex lib/scoria/prompt_policy.ex</automated>
  </verify>
  <done>The public module docs reflect the intended layered adoption surface instead of placeholder-only descriptions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public docs → host app integrator | README guidance shapes how Phoenix teams persist ids and call the runtime |
| top-level facade docs → advanced modules | Module ordering determines whether callers use `Scoria` or reach prematurely into substrate modules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | S | `README.md` | mitigate | Teach `run_id` as the exact durable handle per D-10 and D-22 so a shared `session_id` cannot masquerade as resume authority. |
| T-15-01-02 | T | `README.md` | mitigate | Keep the quickstart anchored to the current public facade and existing integration tests so docs do not invent unsupported runtime semantics. |
| T-15-01-03 | R | `lib/scoria.ex` | mitigate | Document the top-level API as the canonical host-app path so public guidance remains attributable to one stable surface. |
| T-15-01-04 | I | `lib/scoria/runtime.ex` | mitigate | Position advanced modules deliberately and avoid exposing substrate-first guidance that would leak internal boundaries as the normal contract. |
| T-15-01-05 | D | `README.md` | mitigate | Keep the knowledge lane explicitly optional so first-run adoption is not blocked on pgvector or retrieval setup. |
| T-15-01-06 | E | `README.md` | mitigate | State clearly that `/scoria` is operator evidence, not the app’s source of business truth, to prevent the dashboard from being treated as an authority escalation path. |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/runtime_integration_test.exs test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.install_route_smoke_test.exs`
- `rg -n "Scoria.start_run|Scoria.resume_run|session_id|run_id|/scoria/workflows/:run_id|Optional knowledge lane" README.md`
- `mix compile && mix run -e 'mods=[Scoria, Scoria.Runtime, Scoria.Identity, Scoria.PromptPolicy]; Enum.each(mods, fn mod -> case Code.fetch_docs(mod) do {:docs_v1, _, :elixir, _, moduledoc, _, _, _} -> if moduledoc in [nil, :none], do: raise("missing moduledoc for #{inspect(mod)}"); _ -> raise("missing docs for #{inspect(mod)}") end end)'`
</verification>

<success_criteria>
- `README.md` matches the shipped Keystone runtime and install surface instead of the old milestone state.
- The primary teaching path is `Scoria`-first, runtime-first, and explicit about `session_id` versus `run_id`.
- Public moduledocs reinforce the same layering without introducing new runtime semantics.
- `ADOP-01` and `ADOP-04` are satisfied through concrete README and module-doc updates.
</success_criteria>

<output>
After completion, create `.planning/phases/15-adoption-surface-docs-and-example-flow/15-01-SUMMARY.md`
</output>
