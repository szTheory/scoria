---
phase: 15-adoption-surface-docs-and-example-flow
plan: 03
type: execute
wave: 3
depends_on:
  - 15-01
  - 15-02
files_modified:
  - README.md
  - docs/operator_verification.md
  - lib/mix/tasks/scoria.install.ex
autonomous: true
nyquist_compliant: true
requirements:
  - ADOP-03
must_haves:
  truths:
    - "A Phoenix team can follow a default two-step proof: install preflight first, then one real core-lane run backed by operator evidence."
    - "The default proof explicitly does not require pgvector, knowledge tables, retrieval, grounding, or `mix scoria.test.knowledge`."
    - "The installer and docs tell the same verification story, and maintainers can still lean on the existing test-first lane for repo closeout."
  artifacts:
    - path: "docs/operator_verification.md"
      provides: "Operator-facing verification walkthrough for the default lane"
    - path: "README.md"
      provides: "Public verification summary that links to the deeper operator guide"
    - path: "lib/mix/tasks/scoria.install.ex"
      provides: "Installer next-step copy aligned to the Phase 15 default verification story"
  key_links:
    - from: "docs/operator_verification.md"
      to: "lib/mix/tasks/scoria.install.ex"
      via: "preflight commands and optional knowledge-lane messaging stay aligned"
      pattern: "mix scoria.install|mix ecto.migrate|mix test|mix scoria.test.knowledge"
    - from: "docs/operator_verification.md"
      to: "test/scoria/runtime_integration_test.exs"
      via: "default proof mirrors one real run plus operator evidence"
      pattern: "Scoria\\.start_run|Scoria\\.get_run|/scoria/workflows/"
    - from: "README.md"
      to: "docs/operator_verification.md"
      via: "quickstart points to deeper verification instructions instead of duplicating them"
      pattern: "operator verification|default lane"
---

<objective>
Close Phase 15 with an operator-facing verification story that matches the shipped install and runtime lanes.

Purpose: give adopters and maintainers one coherent proof path for the core lane while keeping the knowledge lane explicitly optional.
Output: verification docs plus installer next-step messaging aligned to the same default-lane story.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-adoption-surface-docs-and-example-flow/15-CONTEXT.md
@.planning/phases/15-adoption-surface-docs-and-example-flow/15-RESEARCH.md
@.planning/phases/15-adoption-surface-docs-and-example-flow/15-PATTERNS.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-03-PLAN.md
@.planning/phases/14-policy-defaults-and-install-ergonomics/14-VALIDATION.md
@README.md
@lib/mix/tasks/scoria.install.ex
@lib/mix/tasks/scoria.pgvector.bootstrap.ex
@lib/mix/tasks/scoria.test.knowledge.ex
@test/scoria/runtime_integration_test.exs
@test/mix/tasks/scoria.install_test.exs
@test/mix/tasks/scoria.install_route_smoke_test.exs
@test/scoria/bootstrap/migration_lane_compatibility_test.exs

<interfaces>
From lib/mix/tasks/scoria.install.ex:
```elixir
mix scoria.install
mix ecto.migrate
mix test
visit /scoria
mix scoria.pgvector.bootstrap
mix scoria.test.knowledge
```

From lib/scoria.ex:
```elixir
def start_run(identity, opts \\ [])
def get_run(run_id)
def list_runs_for_session(session_id)
```

From test/scoria/runtime_integration_test.exs:
```elixir
Scoria.start_run(...)
Runtime.get_run(started.run_id)
live(conn, "/scoria/workflows/#{started.run_id}")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the default operator verification lane and optional knowledge expansion</name>
  <read_first>lib/mix/tasks/scoria.install.ex, test/scoria/runtime_integration_test.exs, test/mix/tasks/scoria.install_test.exs, test/mix/tasks/scoria.install_route_smoke_test.exs, test/scoria/bootstrap/migration_lane_compatibility_test.exs, .planning/phases/15-adoption-surface-docs-and-example-flow/15-PATTERNS.md</read_first>
  <files>docs/operator_verification.md, README.md</files>
  <acceptance_criteria>The guide and README summary preserve the Phase 14 default-lane split, prove core success with install preflight plus one real run and operator evidence, and explicitly demote the knowledge lane to an optional expansion after core proof; they stay aligned with the installer’s commands and do not imply tests alone are the user-facing proof of success.</acceptance_criteria>
  <action>Create a dedicated verification guide and trim the README to a short link or summary that stays consistent with it. The guide must implement D-13 through D-18 exactly: preflight with `mix scoria.install`, `mix ecto.migrate`, and `mix test` per D-14; then prove success with one real `Scoria.start_run/2`, a readback through `Scoria.get_run/1` or `list_runs_for_session/1`, and visibility at `/scoria/workflows/:run_id` per D-15; explicitly state that pgvector, knowledge tables, retrieval, grounding, and `mix scoria.test.knowledge` are not required for core success per D-16; and present the knowledge lane only afterward as an optional expansion per D-17. Keep maintainer closeout grounded in the existing automated lanes per D-18.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/runtime_integration_test.exs test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.install_route_smoke_test.exs test/scoria/bootstrap/migration_lane_compatibility_test.exs && rg -n "mix scoria.install|mix ecto.migrate|mix test|Scoria.start_run|Scoria.get_run|list_runs_for_session|/scoria/workflows/:run_id|not require|Optional knowledge lane|mix scoria.test.knowledge" README.md docs/operator_verification.md</automated>
  </verify>
  <done>The docs give adopters one coherent core-lane verification story with a clearly labeled optional knowledge expansion path.</done>
</task>

<task type="auto">
  <name>Task 2: Align installer next-step messaging to the same default-lane proof</name>
  <read_first>lib/mix/tasks/scoria.install.ex, test/mix/tasks/scoria.install_test.exs, test/mix/tasks/scoria.install_route_smoke_test.exs, test/scoria/bootstrap/migration_lane_compatibility_test.exs, .planning/phases/14-policy-defaults-and-install-ergonomics/14-03-PLAN.md</read_first>
  <files>lib/mix/tasks/scoria.install.ex</files>
  <acceptance_criteria>The installer output remains additive and conservative, keeps the boring Phoenix lane first, preserves `/scoria` as the operator evidence step, and still labels `mix scoria.pgvector.bootstrap` plus `mix scoria.test.knowledge` as optional follow-on work instead of default requirements.</acceptance_criteria>
  <action>Adjust the installer output so its printed next steps reinforce the exact Phase 15 verification story without changing runtime semantics: the boring default lane first per D-13 and D-14, operator evidence tied to `/scoria` per D-15, and the knowledge lane clearly marked optional per D-16 and D-17. Keep the wording consistent with the README and operator guide, and do not widen the default lane to require pgvector or new commands.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.install_route_smoke_test.exs test/scoria/bootstrap/migration_lane_compatibility_test.exs && rg -n "default Phoenix lane|mix ecto.migrate|mix test|/scoria|Optional knowledge lane|mix scoria.pgvector.bootstrap|mix scoria.test.knowledge" lib/mix/tasks/scoria.install.ex</automated>
  </verify>
  <done>The installer’s next-step copy matches the public verification docs and keeps the knowledge lane explicitly non-default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer output → first-run operator workflow | Printed next steps steer how adopters verify the integration |
| public verification docs → optional knowledge lane | The default proof must remain separate from optional pgvector-backed expansion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | S | `docs/operator_verification.md` | mitigate | Require one real `Scoria.start_run/2` plus readback and `/scoria/workflows/:run_id` evidence so the default proof cannot be faked by dashboard-only setup. |
| T-15-03-02 | T | `lib/mix/tasks/scoria.install.ex` | mitigate | Keep installer copy aligned to the docs and existing tests so onboarding instructions do not drift from the shipped lane. |
| T-15-03-03 | R | `README.md` | mitigate | Preserve the repo’s test-first closeout commands while distinguishing them from the user-facing default proof. |
| T-15-03-04 | I | `docs/operator_verification.md` | mitigate | Keep verification guidance on the public runtime and operator evidence surfaces instead of exposing internal workflow details as required user knowledge. |
| T-15-03-05 | D | `docs/operator_verification.md` | mitigate | State unambiguously that core success does not require pgvector or knowledge tables, preventing optional dependencies from blocking first-run adoption. |
| T-15-03-06 | E | `docs/operator_verification.md` | mitigate | Tie operator evidence to the exact `run_id` route so a shared session cannot be mistaken for authority over another run. |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/runtime_integration_test.exs test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.install_route_smoke_test.exs test/scoria/bootstrap/migration_lane_compatibility_test.exs`
- `rg -n "mix scoria.install|mix ecto.migrate|mix test|Scoria.start_run|Scoria.get_run|list_runs_for_session|/scoria/workflows/:run_id|Optional knowledge lane|mix scoria.test.knowledge" README.md docs/operator_verification.md`
- `rg -n "default Phoenix lane|mix ecto.migrate|mix test|/scoria|Optional knowledge lane|mix scoria.pgvector.bootstrap|mix scoria.test.knowledge" lib/mix/tasks/scoria.install.ex`
</verification>

<success_criteria>
- Adopters have a clear two-step default verification lane and an explicitly optional knowledge lane.
- Installer output, README verification summary, and the deeper operator guide tell the same story.
- Existing runtime and install tests remain the automation backbone for repo closeout and docs verification.
- `ADOP-03` is satisfied without introducing any new runtime behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/15-adoption-surface-docs-and-example-flow/15-03-SUMMARY.md`
</output>
