---
phase: 15-adoption-surface-docs-and-example-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - docs/phoenix_runtime_example.md
autonomous: true
nyquist_compliant: true
requirements:
  - ADOP-02
must_haves:
  truths:
    - "A Phoenix team can follow one end-to-end example that maps request/session context into Scoria identity, starts a run, stores `run_id`, and resumes after approval."
    - "The example teaches that `session_id` groups conversation continuity while each new turn gets a fresh `run_id`."
    - "The operator page at `/scoria/workflows/:run_id` is shown as evidence linked from the host app rather than the source of business truth."
  artifacts:
    - path: "docs/phoenix_runtime_example.md"
      provides: "Canonical controller-triggered Phoenix integration guide"
  key_links:
    - from: "docs/phoenix_runtime_example.md"
      to: "lib/scoria/identity.ex"
      via: "identity normalization from `conn.assigns` and session storage"
      pattern: "Scoria\\.identity|from_conn_assigns|from_session"
    - from: "docs/phoenix_runtime_example.md"
      to: "lib/scoria.ex"
      via: "host app uses the top-level facade for start, inspect, and resume"
      pattern: "Scoria\\.(start_run|get_run|resume_run|list_runs_for_session)"
    - from: "docs/phoenix_runtime_example.md"
      to: "test/scoria/runtime_integration_test.exs"
      via: "approval pause and operator evidence mirror the existing runtime integration proof"
      pattern: "waiting_for_approval|/scoria/workflows/"
---

<objective>
Produce the canonical end-to-end Phoenix integration example for the Keystone public runtime surface.

Purpose: give Phoenix teams one concrete, controller-triggered adoption story that matches the shipped identity, runtime, and operator evidence contracts.
Output: a dedicated example guide derived from the existing runtime integration behavior.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-adoption-surface-docs-and-example-flow/15-CONTEXT.md
@.planning/phases/15-adoption-surface-docs-and-example-flow/15-RESEARCH.md
@.planning/phases/15-adoption-surface-docs-and-example-flow/15-PATTERNS.md
@.planning/phases/13-public-runtime-api-and-session-lifecycle/13-04-PLAN.md
@README.md
@lib/scoria.ex
@lib/scoria/runtime.ex
@lib/scoria/identity.ex
@lib/scoria/runtime/params.ex
@test/scoria/runtime_integration_test.exs

<interfaces>
From lib/scoria.ex:
```elixir
def identity(attrs \\ %{})
def start_run(identity, opts \\ [])
def resume_run(run_id, opts \\ [])
def get_run(run_id)
def get_run_detail(run_id)
def list_runs_for_session(session_id)
```

From lib/scoria/runtime/params.ex:
```elixir
def start(identity, opts \\ [])
def resume(run_id, opts \\ [])
```

From test/scoria/runtime_integration_test.exs:
```elixir
Scoria.start_run(identity, initial_step: %{kind: "approval", ...})
Scoria.resume_run(started.run_id, handlers: %{"approval" => {Handlers, :succeed}})
Scoria.list_runs_for_session("shared-session")
live(conn, "/scoria/workflows/#{started.run_id}")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the canonical controller-triggered Phoenix example guide</name>
  <read_first>test/scoria/runtime_integration_test.exs, lib/scoria.ex, lib/scoria/identity.ex, lib/scoria/runtime.ex, .planning/phases/15-adoption-surface-docs-and-example-flow/15-PATTERNS.md</read_first>
  <files>docs/phoenix_runtime_example.md</files>
  <acceptance_criteria>The guide is explicitly derived from the existing runtime integration test and keeps the controller-triggered posture locked by D-06; it shows `Scoria.identity/1`, `Scoria.start_run/2`, durable `run_id` persistence, same-session fresh-run semantics, exact `resume_run/2`, and `/scoria/workflows/:run_id` as operator evidence; it does not drift into LiveView-first, background-job-first, or workflow-substrate-first teaching.</acceptance_criteria>
  <action>Create a dedicated guide that implements D-06 through D-12 and D-20 through D-23 exactly. The example must be controller-triggered, use `Scoria.identity/1` to normalize `actor_id`, `tenant_id`, and `session_id` from Phoenix edge state per D-07, call `Scoria.start_run/2`, persist the returned `run_id` for exact continuation per D-08 and D-10, teach that later turns reuse `session_id` but start fresh runs with new `run_id` values per D-09, inspect progress via `Scoria.get_run/1` and optionally `list_runs_for_session/1`, and present `/scoria/workflows/:run_id` only as operator evidence per D-11. Do not make LiveView-first or background-job-first flows the primary path per D-06 and D-12.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/runtime_integration_test.exs && rg -n "Scoria.identity|Scoria.start_run|Scoria.get_run|Scoria.resume_run|list_runs_for_session|session_id|run_id|/scoria/workflows/:run_id|approval" docs/phoenix_runtime_example.md</automated>
  </verify>
  <done>The repo has one concrete Phoenix integration guide that teaches the exact Keystone runtime semantics already proven by the integration suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phoenix edge state → Scoria identity | Controller/session data is normalized into the canonical runtime identity envelope |
| host app persistence → runtime resume | Stored `run_id` and `session_id` are reused to inspect or continue the correct run |
| host app flow → operator evidence page | The app links users/operators to `/scoria/workflows/:run_id` without transferring business truth ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | S | `docs/phoenix_runtime_example.md` | mitigate | Show identity normalization explicitly through `Scoria.identity/1` so actor, tenant, and session inputs are not taught as loose ad hoc attrs. |
| T-15-02-02 | T | `docs/phoenix_runtime_example.md` | mitigate | Derive the example from `test/scoria/runtime_integration_test.exs` and existing public APIs so the guide cannot drift into unsupported semantics. |
| T-15-02-03 | R | `docs/phoenix_runtime_example.md` | mitigate | Persist and reuse `run_id` explicitly so approval resume and operator evidence stay attributable to one durable run. |
| T-15-02-04 | I | `docs/phoenix_runtime_example.md` | mitigate | Keep the example on curated public fields and avoid teaching workflow internals as required host-app state. |
| T-15-02-05 | D | `docs/phoenix_runtime_example.md` | mitigate | Keep the example on the core runtime lane and avoid making optional knowledge setup a prerequisite. |
| T-15-02-06 | E | `docs/phoenix_runtime_example.md` | mitigate | Teach `session_id` as continuity only and `run_id` as exact resume authority so callers do not escalate a shared session into the wrong run. |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/runtime_integration_test.exs`
- `rg -n "Scoria.identity|Scoria.start_run|Scoria.get_run|Scoria.resume_run|list_runs_for_session|session_id|run_id|/scoria/workflows/:run_id|approval" docs/phoenix_runtime_example.md`
</verification>

<success_criteria>
- The repo contains a canonical Phoenix example that matches the shipped runtime and approval-resume behavior.
- The guide is explicit about `session_id` continuity, fresh per-turn `run_id` values, and exact `run_id` resume.
- The example keeps `/scoria/workflows/:run_id` in the operator-evidence role instead of making it the app integration boundary.
- `ADOP-02` is satisfied without adding new runtime semantics or new infrastructure.
</success_criteria>

<output>
After completion, create `.planning/phases/15-adoption-surface-docs-and-example-flow/15-02-SUMMARY.md`
</output>
