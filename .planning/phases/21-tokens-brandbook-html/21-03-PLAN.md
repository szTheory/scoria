---
phase: 21-tokens-brandbook-html
plan: 03
type: execute
wave: 2
depends_on: ["21-01", "21-02"]
files_modified:
  - brandbook/index.html
  - brandbook/tools/check-consistency.mjs
autonomous: true
requirements: [BRAND-06]

must_haves:
  truths:
    - "brandbook/index.html opens from file:// with no load-bearing network refs; system fallback font stack first"
    - "index.html ≤90KB; styled with brand tokens, dark-first; covers hero / executive summary / brand DNA / logo system / color (full contrast table) / typography / voice / UI guidance / blueprints / artifact index / QA gate summary"
    - "every src= reference in index.html resolves to an existing sibling file"
    - "node brandbook/tools/check-consistency.mjs (now including brand-book.md hex source) exits 0"
  artifacts:
    - path: "brandbook/index.html"
      provides: "Professional standalone brand book, BRAND-06"
      contains: "Scoria Brand Book"
  key_links:
    - from: "brandbook/index.html"
      to: "brandbook/logo-primary.svg + examples/*.svg"
      via: "relative <img src> references to sibling files"
      pattern: "src=\"(logo|examples/)"
    - from: "brandbook/tools/check-consistency.mjs"
      to: "brandbook/brand-book.md"
      via: "hex extraction added to the consistency assertion"
      pattern: "brand-book\\.md"
---

<objective>
Build `brandbook/index.html` — the professional standalone brand book (BRAND-06): a single file that opens from `file://`, styled with the brand tokens (dark-first), with relative `<img>` references to the sibling logo SVGs and example specimens. Then wire `brand-book.md` into `tools/check-consistency.mjs` (so the gate now spans tokens.json ↔ tokens.css ↔ 02-tokens.css ↔ brand-book.md) and run full verification (file sizes, zero load-bearing network refs, all referenced files exist).

Purpose: BRAND-06 — a reviewer can open the brandbook offline as a polished, complete brand reference. This is the wave-2 integration plan depending on both wave-1 plans (it embeds their tokens + examples + brand-book content).
Output: index.html, extended check-consistency.mjs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-tokens-brandbook-html/21-CONTEXT.md

<read_first>
- brandbook/tokens.css (from 21-01) — the --scoria- :root tokens to style index.html with. Inline a copy; dark-first (Basalt-950 ground, warm text, ember accents).
- brandbook/brand-book.md (from 21-02) — source content to render as HTML sections (identity, logo system, color, typography, voice, UI guidance, blueprints, do/don't). The final copy blocks live here.
- brandbook/examples/*.svg (from 21-02) — embed via relative <img src="examples/...svg"> (preferred over inlining).
- brandbook/logo-primary.svg, logo-primary-light.svg, logo-mark.svg, social-card.svg — embed via relative <img src="logo-primary.svg"> for the logo-system section (both grounds). Inline only the small mark where currentColor decoration is needed.
- brandbook/tools/check-consistency.mjs (from 21-01) — has a TODO extension point for the brand-book.md hex source; wire it in here.
- brandbook/pressure-test.md §7 contrast table (lines ~510–584) — the FULL verdict table to render in the color section (52 pairings, PASS-AA/PASS-LARGE summary). §1 executive judgment for the summary section. §14 for the QA/quality-gate summary section.
- brandbook/tools/variant-spec.md — logo usage rules to surface in the logo-system section.
- /Users/jon/projects/threadline/brandbook/index.html (~33KB) — structure/section precedent to rhyme with (Scoria volcanic dark-first, not Threadline blue). Rhyme, do not clone.
- 21-CONTEXT.md §index.html — section skeleton + the network-ref rule (a Google-Fonts <link> permitted ONLY if the page is fully readable without it; system fallback stack must come first; SVG xmlns is not a load-bearing ref).
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build brandbook/index.html (standalone, dark-first, brand-token styled)</name>
  <files>brandbook/index.html</files>
  <action>Single-file standalone brand book. Title "Scoria Brand Book". Inline a <style> block using the --scoria- tokens (copy the :root token set from tokens.css so the page is self-contained; dark-first: Basalt-950 ground, warm Ash text, ember accents; add light sections only where the contrast comparison demands). Section skeleton (rhyme with Threadline's index.html, Scoria volcanic art direction): (1) hero — logo-primary.svg via relative <img>, primary tagline "AI ops for Phoenix apps.", the one-liner, then the three-line verb-triplet ("Trace the run." / "Prove the change." / "Ship the agent.") as the display treatment beneath to demonstrate the locked hierarchy; (2) executive judgment summary (from pressure-test §1); (3) brand DNA; (4) logo system — all variants on BOTH dark and light grounds via relative <img>, plus usage/clear-space/min-size rules; (5) color — interactive-ish swatch grid from the tokens + the FULL contrast verdict table from §7 (52 pairings, PASS-AA/PASS-LARGE/FAIL legend + summary); (6) typography (Plex/JetBrains specimen); (7) voice & microcopy — say/don't tables + the copy blocks; (8) UI guidance; (9) landing/docs blueprints; (10) artifact index (what lives where in brandbook/); (11) QA/quality-gate summary referencing the contrast table (from §14). Embed examples via relative <img src="examples/...svg">. NETWORK RULE: zero load-bearing network refs — system fallback font stack FIRST in every font-family; a Google-Fonts <link> is permitted ONLY if the page is fully readable without it (prefer omitting it entirely). SVG xmlns attributes and relative sibling src= refs are fine. Target ≤90KB. Professional > clever.</action>
  <verify>
    <automated>test -f brandbook/index.html && test $(wc -c < brandbook/index.html) -le 92160 && grep -q 'Scoria Brand Book' brandbook/index.html && grep -q 'AI ops for Phoenix apps' brandbook/index.html && grep -q 'Trace the run' brandbook/index.html && grep -q 'src="examples/' brandbook/index.html && grep -q 'src="logo-primary' brandbook/index.html</automated>
  </verify>
  <done>index.html ≤90KB, titled "Scoria Brand Book", contains the tagline + verb-triplet hero, relative img refs to examples/ and logo-primary, and the contrast table; opens standalone.</done>
</task>

<task type="auto">
  <name>Task 2: Wire brand-book.md into check-consistency.mjs and run full verification</name>
  <files>brandbook/tools/check-consistency.mjs</files>
  <action>Extend check-consistency.mjs (at the 21-01 TODO extension point): add brand-book.md as a fourth hex source — extract every hex literal that appears as a documented token value in brand-book.md (e.g. in the color section), normalize to lowercase, and assert each is consistent with the corresponding token hex in tokens.css / 02-tokens.css. The brand-book.md hexes are a subset (the doc shows representative palette values, not every token), so the check asserts agreement/membership, not completeness — any hex in brand-book.md that names a known token MUST match. Keep the existing tokens.json ↔ tokens.css ↔ 02-tokens.css assertions. Print a PASS line per source (4 sources now) and exit 0 on full agreement, exit 1 with a diff on any mismatch. Then perform full verification as part of this task: (a) node tools/check-consistency.mjs exits 0; (b) index.html file budget met; (c) zero load-bearing network refs — grep index.html for https?:// and confirm the only matches (if any) are SVG xmlns namespaces (www.w3.org) or a clearly-non-load-bearing optional font link; (d) every src= in index.html resolves to an existing file (scripted loop); (e) ls brandbook/examples/*.svg count = 7. Fix any failures before completing.</action>
  <verify>
    <automated>node brandbook/tools/check-consistency.mjs && grep -q 'brand-book.md' brandbook/tools/check-consistency.mjs && test $(ls brandbook/examples/*.svg | wc -l) -eq 7 && node -e "const fs=require('fs');const h=fs.readFileSync('brandbook/index.html','utf8');for(const m of h.matchAll(/src=\"([^\"]+)\"/g)){const s=m[1];if(/^https?:|^data:/.test(s))continue;if(!fs.existsSync('brandbook/'+s)){console.error('missing '+s);process.exit(1)}}const net=[...h.matchAll(/https?:\/\/[^\"']+/g)].map(x=>x[0]).filter(u=>!/w3\.org|fonts\.googleapis|fonts\.gstatic/.test(u));if(net.length){console.error('load-bearing net refs',net);process.exit(1)}console.log('ok')"</automated>
  </verify>
  <done>check-consistency.mjs now reads brand-book.md and exits 0 across 4 sources; every index.html src= resolves to an existing file; no load-bearing network refs; examples count = 7.</done>
</task>

</tasks>

<verification>
- node brandbook/tools/check-consistency.mjs exits 0 (4 sources: tokens.json, tokens.css, 02-tokens.css, brand-book.md).
- wc -c brandbook/index.html ≤ 90KB (92160 bytes allows the ≤90KB target with margin).
- Every src= in index.html resolves; only allowed network refs are SVG xmlns + optional non-load-bearing font link.
- ls brandbook/examples/*.svg count = 7.
- No edits to repo-root README.md, layouts.ex, mix.exs (Phase 22 scope).
</verification>

<success_criteria>
BRAND-06 satisfied: brandbook/index.html is a professional standalone brand book that opens offline, covers the full section skeleton, styled with brand tokens; the consistency gate spans all four hex sources and is green.
</success_criteria>

<output>
Create `.planning/phases/21-tokens-brandbook-html/21-03-SUMMARY.md` when done.
</output>
