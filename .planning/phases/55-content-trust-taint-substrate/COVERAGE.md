No external API integration: this phase builds an internal trust/taint substrate over Scoria's own seams (`Knowledge`, `MCP.Executor`, `Observe.Semconv`). The api-coverage detector fired on "wrapping"+"mcp" (D-07's `MCP.Executor` success choke point), which is Scoria wrapping its OWN tool-output at its own executor — not consuming an external API/SDK. The only external-facing seam is the BYO `scan/2` behaviour (TAINT-04), which a host implements against; Scoria ships only the `NoOp` default and no detector/classifier (scope doctrine), so there is no external capability surface to enumerate.
