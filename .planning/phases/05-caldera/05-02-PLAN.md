---
phase: 05-caldera
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - priv/repo/migrations/20260511000200_link_approvals_to_workflows.exs
  - lib/scoria/application.ex
  - lib/scoria/workflows.ex
  - lib/scoria/workflows/runtime.ex
  - lib/scoria/workflows/reconciler.ex
  - lib/scoria/workflows/resume.ex
  - lib/scoria/observe/approval.ex
  - test/scoria/workflows/runtime_test.exs
  - test/scoria/workflows/approval_test.exs
  - test/scoria/workflows/handoff_test.exs
  - test/scoria/workflows/integration_test.exs
autonomous: true
requirements:
  - WF-01
  - WF-02
  - WF-03
  - WF-04
must_haves:
  truths:
    - "Workflow steps execute under OTP supervision and persist their stable outcomes."
    - "Approval waits, failures, retries, and resumes derive from persisted state rather than transient memory."
  artifacts:
    - path: "lib/scoria/workflows/runtime.ex"
      provides: "Bounded workflow step execution runtime"
    - path: "lib/scoria/workflows/resume.ex"
      provides: "Exact resume and retry entrypoints"
  key_links:
    - from: "lib/scoria/workflows/runtime.ex"
      to: "Scoria.Workflows"
      via: "persisted lifecycle transitions"
      pattern: "Scoria\\.Workflows\\."
    - from: "lib/scoria/application.ex"
      to: "Task.Supervisor"
      via: "workflow execution supervision"
      pattern: "Task\\.Supervisor"
---

<objective>
Implement the Phase 5 runtime layer that executes bounded workflow steps under supervision and supports durable pause, failure, resume, and retry semantics.

Purpose: translate the persistence foundation into a crash-tolerant execution loop while keeping Ecto as the workflow source of truth.
Output: runtime executor, supervision wiring, resume/retry helpers, and lifecycle tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-caldera/05-CONTEXT.md
@.planning/phases/05-caldera/05-RESEARCH.md
@.planning/phases/05-caldera/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Workflow Runtime Supervision</name>
  <files>lib/scoria/application.ex, lib/scoria/workflows/runtime.ex, lib/scoria/workflows/reconciler.ex, test/scoria/workflows/runtime_test.exs, test/scoria/workflows/integration_test.exs</files>
  <behavior>
    - Test 1: Workflow runtime executes bounded step work under a named `Task.Supervisor`.
    - Test 2: Timeout or crash paths emit durable failure transitions instead of leaving silent transient state.
    - Test 3: Application boot and post-transition reconciliation scan persisted runnable steps and dispatch them safely.
  </behavior>
  <action>Extend the application supervision tree for workflow execution and add `Scoria.Workflows.Reconciler` as the repo-local startup reconciliation / dispatch boundary. On application boot, and after approval or retry transitions, the reconciler should scan persisted runnable steps, claim eligible work, and dispatch bounded executions under the workflow `Task.Supervisor`. Implement `Scoria.Workflows.Runtime` using the existing `MCP.Executor` isolation pattern as a guide. Each runtime action should execute one bounded step, collect its result, and hand the outcome back to `Scoria.Workflows` for durable state persistence. Do not create a GenServer-owned workflow ledger.</action>
  <verify>
    <automated>mix test test/scoria/workflows/runtime_test.exs test/scoria/workflows/integration_test.exs</automated>
  </verify>
  <done>Runtime execution is supervised, bounded, and failure-aware.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Durable Approval Waits and Side-Effect Boundaries</name>
  <files>priv/repo/migrations/20260511000200_link_approvals_to_workflows.exs, lib/scoria/observe/approval.ex, lib/scoria/workflows.ex, lib/scoria/workflows/runtime.ex, test/scoria/workflows/runtime_test.exs, test/scoria/workflows/approval_test.exs</files>
  <behavior>
    - Test 1: Entering approval wait persists `waiting_for_approval` before any broadcast/projection step.
    - Test 2: `ai_approvals` gains workflow foreign keys and optimistic locking so approvals can participate in durable workflow truth.
  </behavior>
  <action>Add an additive migration `priv/repo/migrations/20260511000200_link_approvals_to_workflows.exs` and extend `lib/scoria/observe/approval.ex` so approvals gain `workflow_run_id`, `step_id`, `checkpoint_id`, and `lock_version` while keeping the legacy string `run_id` temporarily. Then add runtime-to-context flows for `run_started`, post-decision stable actions, approval waits, side-effect completion, and terminal states. Keep the hybrid semantic checkpointing model strict: do not persist streaming token fragments, socket state, or internal mailbox data.</action>
  <verify>
    <automated>mix test test/scoria/workflows/runtime_test.exs test/scoria/workflows/approval_test.exs</automated>
  </verify>
  <done>Approval and side-effect lifecycle boundaries are durable and explicitly test-covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Root-Owned Handoff Runtime Contract</name>
  <files>lib/scoria/workflows/runtime.ex, lib/scoria/workflows.ex, test/scoria/workflows/handoff_test.exs</files>
  <behavior>
    - Test 1: Delegated steps preserve root run ownership while using stable `role_id` and typed `handoff_input` / `step_result` envelopes.
    - Test 2: Handoff execution passes projected context slices only, not full transcripts or provider session internals.
  </behavior>
  <action>Add explicit runtime and context support for bounded delegated-step execution under a root-owned run. Model handoffs as child steps with stable `role_id`, typed envelopes, and projected-context-only delegation. Verify the runtime contract directly rather than leaving handoffs as a schema-only concern.</action>
  <verify>
    <automated>mix test test/scoria/workflows/handoff_test.exs</automated>
  </verify>
  <done>Handoff behavior is planned as a real runtime capability with explicit tests for the root-owned delegation contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Exact Resume and Retry Failed Step</name>
  <files>lib/scoria/workflows/resume.ex, lib/scoria/workflows/reconciler.ex, lib/scoria/workflows.ex, test/scoria/workflows/runtime_test.exs, test/scoria/workflows/integration_test.exs</files>
  <behavior>
    - Test 1: Resume reads the latest durable checkpoint and chooses the correct next action.
    - Test 2: Retry failed step creates the expected retry state without replaying already-completed durable boundaries.
  </behavior>
  <action>Implement `Scoria.Workflows.Resume` as a thin entrypoint that reconstructs next action from stored run, step, and checkpoint records and hands runnable work back to `Scoria.Workflows.Reconciler` for dispatch. Cover both app-boot restart recovery and post-approval/post-retry dispatch through the same reconciler path. Support exact resume and retry failed step only; do not add fork/edit-current-run behavior. Make the API explicit enough that later operator UI actions can call it without needing private runtime knowledge.</action>
  <verify>
    <automated>mix test test/scoria/workflows/runtime_test.exs test/scoria/workflows/integration_test.exs</automated>
  </verify>
  <done>Tests prove workflow recovery behavior is derived from persisted state and respects Phase 5's recovery UX constraints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime -> Side effects | Bounded task execution against external effects or delegated steps |
| Runtime -> Projection | Durable state versus PubSub-visible UI updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Denial of Service | `Scoria.Workflows.Runtime` | mitigate | Bound execution with `Task.Supervisor`, timeout handling, and persisted failure states. |
| T-05-02-02 | Repudiation | Resume/retry flows | mitigate | Persist checkpoints and events at stable boundaries so operators can audit why a resume or retry occurred. |
</threat_model>

<verification>
- Runtime actions are supervised and bounded.
- Approval waits are durable before projection.
- Resume and retry logic reconstructs next action from persisted state.
</verification>

<success_criteria>
- Workflow execution survives pauses and failures without using transient process state as truth.
- Operators can resume or retry a run from durable records with predictable semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/05-caldera/05-02-SUMMARY.md`
</output>
