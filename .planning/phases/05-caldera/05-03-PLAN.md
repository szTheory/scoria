---
phase: 05-caldera
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - lib/scoria_web/live/workflow_live/show.ex
  - lib/scoria_web/components/workflow_tree_component.ex
  - lib/scoria_web/components/workflow_detail_panel_component.ex
  - lib/scoria_web/router.ex
  - lib/mix/tasks/scoria.install.ex
  - test/scoria_web/live/workflow_live_test.exs
  - test/scoria_web/components/workflow_tree_component_test.exs
  - test/mix/tasks/scoria.install_test.exs
  - test/scoria_web/router_test.exs
autonomous: true
requirements:
  - WF-05
  - WF-07
must_haves:
  truths:
    - "Operators can inspect workflow state through a trace-first LiveView that reflects persisted state."
    - "Workflow dashboard mounting remains installable through the existing router/install surface."
  artifacts:
    - path: "lib/scoria_web/live/workflow_live/show.ex"
      provides: "Workflow run LiveView"
    - path: "lib/mix/tasks/scoria.install.ex"
      provides: "Installer support for workflow dashboard routes/assets"
  key_links:
    - from: "lib/scoria_web/live/workflow_live/show.ex"
      to: "Scoria.Workflows"
      via: "run query and detail loads"
      pattern: "Scoria\\.Workflows\\."
    - from: "lib/scoria_web/router.ex"
      to: "ScoriaWeb.WorkflowLive.Show"
      via: "dashboard route"
      pattern: "WorkflowLive\\.Show"
---

<objective>
Add the Phase 5 operator surface: a trace-first workflow visualizer with lifecycle badges, durable detail drilldowns, and minimal install/router integration.

Purpose: make the durable workflow layer operator-readable without moving workflow truth into the LiveView process.
Output: workflow LiveView, reusable components, route wiring, installer updates, and LiveView/component tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-caldera/05-CONTEXT.md
@.planning/phases/05-caldera/05-RESEARCH.md
@.planning/phases/05-caldera/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trace-First Workflow Run LiveView</name>
  <files>lib/scoria_web/live/workflow_live/show.ex, test/scoria_web/live/workflow_live_test.exs</files>
  <behavior>
    - Test 1: LiveView mounts from persisted workflow records and subscribes for projection updates.
    - Test 2: Run page renders lifecycle badges and responds to run/step updates without owning truth.
  </behavior>
  <action>Create `ScoriaWeb.WorkflowLive.Show` as the primary operator view for a workflow run. Model it after the existing orchestrator patterns: subscribe on connect, stream or assign run/step projections, and render lifecycle states such as `running`, `waiting_for_approval`, `retrying`, `failed`, and `completed`. Keep the page trace-first rather than graph-first.</action>
  <verify>
    <automated>mix test test/scoria_web/live/workflow_live_test.exs</automated>
  </verify>
  <done>The run LiveView renders persisted workflow state and updates from projection events.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Workflow Tree and Detail Components</name>
  <files>lib/scoria_web/components/workflow_tree_component.ex, lib/scoria_web/components/workflow_detail_panel_component.ex, test/scoria_web/components/workflow_tree_component_test.exs</files>
  <behavior>
    - Test 1: Workflow tree renders nested steps and compact handoff markers.
    - Test 2: Detail panel loads checkpoint metadata and failure reasons lazily.
  </behavior>
  <action>Build reusable LiveView components for the workflow tree and right-side detail panel. Reuse the Phase 3 trace-tree interaction pattern where possible, but tailor the labels and metadata to workflow nouns: step inputs/results, checkpoint payloads, failure reasons, and handoff information. Timeline information should be a secondary drilldown, not the primary canvas.</action>
  <verify>
    <automated>mix test test/scoria_web/components/workflow_tree_component_test.exs</automated>
  </verify>
  <done>Operators can navigate nested workflow state and inspect details without overloading the default page.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Router and Install Integration</name>
  <files>lib/scoria_web/router.ex, lib/mix/tasks/scoria.install.ex, test/mix/tasks/scoria.install_test.exs, test/scoria_web/router_test.exs</files>
  <behavior>
    - Test 1: Router macro exposes the workflow dashboard route cleanly.
    - Test 2: Install task updates host router/assets without breaking the existing dashboard surface.
  </behavior>
  <action>Extend the Scoria router macro and install task so Phase 5's workflow visualizer is mountable through the existing `scoria_dashboard` surface. Keep the integration minimal and additive; do not require a separate app shell or custom JavaScript-heavy setup.</action>
  <verify>
    <automated>mix test test/mix/tasks/scoria.install_test.exs test/scoria_web/router_test.exs</automated>
  </verify>
  <done>Host apps can mount the workflow dashboard using the same install and router story Scoria already exposes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted state -> UI | Rendering operator-facing workflow details from durable records |
| Installer -> Host app | Automated route and asset injection into consumer applications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Information Disclosure | Workflow detail panel | mitigate | Render only persisted workflow payloads intended for operator visibility; avoid leaking provider internals or secrets. |
| T-05-03-02 | Tampering | `Mix.Tasks.Scoria.Install` | mitigate | Keep install mutations narrow and deterministic, matching the current router/tailwind injection pattern. |
</threat_model>

<verification>
- Workflow run page mounts from durable state.
- Tree and detail components render lifecycle and checkpoint metadata.
- Installer and router wiring expose the workflow dashboard surface.
</verification>

<success_criteria>
- Operators can inspect a durable workflow run through a trace-first UI.
- Host apps can mount the workflow view without bespoke integration steps.
</success_criteria>

<output>
After completion, create `.planning/phases/05-caldera/05-03-SUMMARY.md`
</output>
