---
phase: 05-caldera
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - lib/scoria/workflows/jido_adapter.ex
  - lib/scoria/workflows.ex
  - test/scoria/workflows/jido_adapter_test.exs
  - test/scoria/workflows/integration_test.exs
  - test/scoria_web/live/workflow_live_test.exs
autonomous: true
requirements:
  - WF-03
  - WF-06
must_haves:
  truths:
    - "Jido remains an optional adapter that maps into Scoria workflow nouns instead of replacing them."
    - "End-to-end tests cover durable recovery and operator-visible lifecycle behavior across the new workflow layer."
  artifacts:
    - path: "lib/scoria/workflows/jido_adapter.ex"
      provides: "Optional Jido directive mapping boundary"
    - path: "test/scoria/workflows/integration_test.exs"
      provides: "End-to-end recovery and approval workflow coverage"
  key_links:
    - from: "lib/scoria/workflows/jido_adapter.ex"
      to: "Scoria.Workflows"
      via: "adapter translation into durable workflow primitives"
      pattern: "Scoria\\.Workflows\\."
    - from: "test/scoria/workflows/integration_test.exs"
      to: "Scoria.Workflows.Resume"
      via: "resume and retry coverage"
      pattern: "Scoria\\.Workflows\\.Resume"
---

<objective>
Finish Phase 5 by containing Jido interoperability to an adapter boundary and adding end-to-end hardening tests for durable recovery and operator workflows.

Purpose: keep the public workflow model Scoria-owned while proving the complete workflow stack behaves correctly under real lifecycle transitions.
Output: optional Jido adapter, integration tests, and final hardening coverage.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-caldera/05-CONTEXT.md
@.planning/phases/05-caldera/05-RESEARCH.md
@.planning/phases/05-caldera/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Optional Jido Adapter Boundary</name>
  <files>lib/scoria/workflows/jido_adapter.ex, test/scoria/workflows/jido_adapter_test.exs</files>
  <behavior>
    - Test 1: Jido directives map into Scoria run/step/event primitives without changing the root workflow contract.
    - Test 2: Unsupported or overly-generic directives fail explicitly instead of mutating workflow truth ambiguously.
  </behavior>
  <action>Create an optional adapter module that translates Jido agent/server outputs into Scoria workflow primitives such as handoffs, step results, and lifecycle events. Keep the surface narrow and explicit. If the dependency is not available or a directive does not fit the Phase 5 contract, fail with a clear adapter error rather than silently widening the model.</action>
  <verify>
    <automated>mix test test/scoria/workflows/jido_adapter_test.exs</automated>
  </verify>
  <done>Jido users have a clean bridge into Scoria durability without making Jido the center of the workflow API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-End Durable Recovery Coverage</name>
  <files>test/scoria/workflows/integration_test.exs, test/scoria_web/live/workflow_live_test.exs</files>
  <behavior>
    - Test 1: A run that pauses for approval can be resumed exactly from stored state.
    - Test 2: A failed step can be retried without replaying already-persisted side-effect boundaries.
    - Test 3: Operator-visible LiveView state matches the durable recovery path.
  </behavior>
  <action>Add integration tests that exercise the complete Phase 5 lifecycle: run start, durable checkpointing, approval wait, exact resume, failure, retry failed step, and terminal completion. Extend LiveView coverage where needed so operator-facing lifecycle labels and detail metadata remain aligned with the durable state machine.</action>
  <verify>
    <automated>mix test test/scoria/workflows/integration_test.exs test/scoria_web/live/workflow_live_test.exs</automated>
  </verify>
  <done>Phase 5 has end-to-end verification for its core recovery promises, not just unit coverage of isolated modules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hardening Gaps and Final API Cleanup</name>
  <files>lib/scoria/workflows.ex, test/scoria/workflows/integration_test.exs</files>
  <behavior>
    - Test 1: Public context APIs expose only the intended Phase 5 operations.
    - Test 2: Final integration coverage catches any split-brain between runtime, persistence, and projection layers.
  </behavior>
  <action>Use the integration suite to close any remaining hardening gaps in naming, public API shape, transition invariants, or adapter containment. Keep the finish focused on Phase 5 promises: durability, operator readability, bounded handoffs, and exact resume/retry. Do not expand scope into graph authoring, in-place workflow editing, or general-purpose orchestration abstractions.</action>
  <verify>
    <automated>mix test</automated>
  </verify>
  <done>The workflow layer is internally coherent, bounded in scope, and verified through a full test pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External adapter -> Workflow core | Translating third-party runtime directives into Scoria-owned workflow truth |
| Full lifecycle verification | Ensuring recovery, UI projection, and persistence agree under failure conditions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Spoofing | Jido adapter inputs | mitigate | Accept only explicit directive mappings and reject unsupported shapes at the adapter boundary. |
| T-05-04-02 | Repudiation | Recovery lifecycle | mitigate | Cover resume/retry and approval flows end-to-end so durable event history stays auditable. |
</threat_model>

<verification>
- Jido interoperability is adapter-only and explicit.
- End-to-end lifecycle tests cover pause, resume, failure, retry, and completion.
- Final public APIs remain scoped to Phase 5's durable workflow contract.
</verification>

<success_criteria>
- Optional Jido users can map into Scoria durability without semantic lock-in.
- Phase 5's core promises are verified through end-to-end tests and a full suite run.
</success_criteria>

<output>
After completion, create `.planning/phases/05-caldera/05-04-SUMMARY.md`
</output>
