---
phase: 05-caldera
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/20260511000100_create_workflow_tables.exs
  - lib/scoria/workflows.ex
  - lib/scoria/workflows/run.ex
  - lib/scoria/workflows/step.ex
  - lib/scoria/workflows/checkpoint.ex
  - lib/scoria/workflows/event.ex
  - lib/scoria/workflows/handoff.ex
  - test/scoria/workflows_test.exs
autonomous: true
requirements:
  - WF-01
must_haves:
  truths:
    - "Stable workflow transitions persist durable run, step, checkpoint, and event state in Ecto."
    - "Operator-facing mutable workflow records enforce optimistic locking."
  artifacts:
    - path: "lib/scoria/workflows.ex"
      provides: "Workflow context and atomic transition APIs"
    - path: "lib/scoria/workflows/run.ex"
      provides: "Root run schema with status and optimistic locking"
  key_links:
    - from: "lib/scoria/workflows.ex"
      to: "Ecto.Multi"
      via: "atomic transition writes"
      pattern: "Ecto\\.Multi"
    - from: "lib/scoria/workflows/run.ex"
      to: "Ecto.Changeset"
      via: "optimistic lock"
      pattern: "optimistic_lock"
---

<objective>
Build the durable persistence foundation for Phase 5 by introducing workflow tables, schemas, and a context module that owns atomic run lifecycle writes.

Purpose: establish Ecto as the single source of truth for workflow durability before runtime or UI behavior is added.
Output: migration, workflow schemas, context APIs, and tests for durable transition semantics.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-caldera/05-CONTEXT.md
@.planning/phases/05-caldera/05-RESEARCH.md
@.planning/phases/05-caldera/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workflow Persistence Migration</name>
  <files>priv/repo/migrations/20260511000100_create_workflow_tables.exs, test/scoria/workflows_test.exs</files>
  <behavior>
    - Test 1: Workflow tables exist with binary IDs, timestamps, and required foreign keys.
    - Test 2: Root run records include `lock_version` for optimistic locking.
  </behavior>
  <action>Create `priv/repo/migrations/20260511000100_create_workflow_tables.exs` for `ai_workflow_runs`, `ai_workflow_steps`, `ai_workflow_checkpoints`, `ai_workflow_events`, and `ai_workflow_handoffs`. Use `binary_id` primary keys, `utc_datetime_usec` timestamps, foreign keys between run and child records, JSON/map payload fields for typed inputs/results/metadata, and a `lock_version` column on operator-facing mutable root records. Keep the schema focused on Scoria workflow nouns, not a generic graph engine.</action>
  <verify>
    <automated>mix test test/scoria/workflows_test.exs</automated>
  </verify>
  <done>Workflow tables can be migrated successfully and expose the fields required by the context layer.</done>
</task>

<task type="auto">
  <name>Task 2: Durable Workflow Schemas</name>
  <files>lib/scoria/workflows/run.ex, lib/scoria/workflows/step.ex, lib/scoria/workflows/checkpoint.ex, lib/scoria/workflows/event.ex, lib/scoria/workflows/handoff.ex, test/scoria/workflows_test.exs</files>
  <behavior>
    - Test 1: `Run` validates allowed lifecycle states and applies optimistic locking.
    - Test 2: `Step`, `Checkpoint`, `Event`, and `Handoff` changesets validate required links and typed payload fields.
  </behavior>
  <action>Create workflow schemas following the repo's Ecto conventions. `Run` should own root status, role metadata, and mutable operator actions. `Step` should model parent/child execution structure and side-effect results. `Checkpoint` and `Event` should be append-only records with explicit lifecycle names and metadata maps. `Handoff` should model bounded delegation using `role_id`, status, `handoff_input`, and `step_result`-oriented payloads.</action>
  <verify>
    <automated>mix test test/scoria/workflows_test.exs</automated>
  </verify>
  <done>Workflow schemas compile cleanly and enforce the intended lifecycle invariants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Workflow Context and Atomic Transition APIs</name>
  <files>lib/scoria/workflows.ex, test/scoria/workflows_test.exs</files>
  <behavior>
    - Test 1: Creating a run writes the root run plus its initial checkpoint and event atomically.
    - Test 2: Completing a step writes step state, checkpoint, and event in one transaction.
    - Test 3: Marking a run `waiting_for_approval` persists the wait state before any projection concerns.
  </behavior>
  <action>Implement `Scoria.Workflows` as the public context for Phase 5 durability. Add functions such as `create_run/1`, `append_checkpoint/3`, `append_event/3`, `complete_step/3`, and `mark_waiting_for_approval/2`, all backed by `Ecto.Multi`. Keep runtime-facing decisions out of this slice; the context owns durable writes and retrieval primitives only.</action>
  <verify>
    <automated>mix test test/scoria/workflows_test.exs</automated>
  </verify>
  <done>Tests prove workflow lifecycle records are persisted atomically and can be queried as a durable source of truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime -> DB | Side-effect outcomes becoming durable workflow truth |
| Operator -> DB | Concurrent retry/cancel mutations on root workflow records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | `Scoria.Workflows` transitions | mitigate | Wrap root and child record writes in `Ecto.Multi` so partial state cannot become durable truth. |
| T-05-01-02 | Elevation of Privilege | `Run` operator mutations | mitigate | Add `optimistic_lock` to mutable root workflow records to prevent stale concurrent updates. |
</threat_model>

<verification>
- Workflow tables migrate successfully.
- Schemas validate lifecycle and linkage constraints.
- Context APIs persist root and child records atomically.
</verification>

<success_criteria>
- Scoria has a durable workflow data model aligned to Phase 5 nouns.
- Stable transitions can be persisted without relying on LiveView or process memory.
</success_criteria>

<output>
After completion, create `.planning/phases/05-caldera/05-01-SUMMARY.md`
</output>
