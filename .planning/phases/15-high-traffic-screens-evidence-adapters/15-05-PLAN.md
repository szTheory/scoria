---
phase: 15-high-traffic-screens-evidence-adapters
plan: "05"
type: execute
wave: 4
depends_on:
  - "15-01"
  - "15-03"
  - "15-04"
files_modified:
  - lib/scoria_web/components/citation_evidence_component.ex
  - lib/scoria_web/components/delegated_evidence_component.ex
  - lib/scoria_web/components/memory_notebook_component.ex
  - lib/scoria_web/components/replay_evidence_notebook_component.ex
  - lib/scoria_web/components/semantic_evidence_notebook_component.ex
  - lib/scoria_web/components/remote_invocation_evidence_component.ex
  - lib/scoria_web/components/incident_evidence_component.ex
  - test/scoria_web/components/semantic_evidence_notebook_component_test.exs
  - test/scoria_web/components/incident_evidence_component_test.exs
  - test/scoria_web/components/memory_notebook_component_test.exs
  - test/scoria_web/live/workflow_live_test.exs
  - test/scoria_web/ui_component_test.exs
  - test/support/ds06_baseline.txt
autonomous: true
requirements:
  - SCREEN-04
user_setup: []
must_haves:
  truths:
    - "D-18: A thin adapter imports ScoriaWeb.UI, renders one notebook or drawer content section, maps existing assigned evidence into rows/sections/actions, and owns only projection/copy/event/link wiring."
    - "D-19/D-20: ui.ex owns shared evidence presentation; adapters must not own bespoke raised cards, raw palette classes, one-off raw JSON details, custom badge styling, or duplicated empty-state wrappers."
    - "D-21: Use the small evidence primitives from Plan 15-01; do not add descriptor renderers, plugin registries, adapter behaviours, or UI schema languages."
    - "D-22/D-23: In-scope evidence/detail components preserve existing data contracts while moving presentation to notebook/adapters."
    - "D-24/D-28: Touched adapter files reduce DS-06 rows and remain LiveViewTest/component-test first."
  artifacts:
    - path: "lib/scoria_web/components/semantic_evidence_notebook_component.ex"
      provides: "Thin semantic evidence notebook adapter"
      contains: "<.notebook"
    - path: "lib/scoria_web/components/replay_evidence_notebook_component.ex"
      provides: "Thin replay comparison notebook adapter"
      contains: "<.notebook"
    - path: "lib/scoria_web/components/delegated_evidence_component.ex"
      provides: "Thin delegated handoff evidence adapter"
      contains: "<.notebook"
    - path: "test/support/ds06_baseline.txt"
      provides: "Lowered or removed rows for remaining evidence adapters"
      contains: "semantic_evidence_notebook_component.ex"
  key_links:
    - from: "lib/scoria_web/components/replay_evidence_notebook_component.ex"
      to: "lib/scoria_web/live/workflow_live/show.ex"
      via: "selected-step replay evidence"
      pattern: "select_comparison_source"
    - from: "lib/scoria_web/components/memory_notebook_component.ex"
      to: "runtime presence"
      via: "Runtime context link"
      pattern: "runtime="
---

<objective>
Convert the remaining evidence components into thin notebook-shell adapters over the shared evidence primitives.

Purpose: This closes SCREEN-04 by collapsing scattered evidence chrome into `ScoriaWeb.UI` while preserving all current evidence values and event/link behavior.
Output: tokenized notebook adapters, updated component/Workflow Show tests, and DS-06 baseline reductions for remaining adapter rows.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-high-traffic-screens-evidence-adapters/15-CONTEXT.md
@.planning/phases/15-high-traffic-screens-evidence-adapters/15-RESEARCH.md
@.planning/phases/15-high-traffic-screens-evidence-adapters/15-VALIDATION.md
@.planning/phases/15-high-traffic-screens-evidence-adapters/15-PATTERNS.md
@.planning/phases/15-high-traffic-screens-evidence-adapters/15-UI-SPEC.md
@.planning/phases/14-least-iterated-screens-polish/14-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert retrieval, delegated, and memory adapters</name>
  <files>lib/scoria_web/components/citation_evidence_component.ex, lib/scoria_web/components/delegated_evidence_component.ex, lib/scoria_web/components/memory_notebook_component.ex, test/scoria_web/components/memory_notebook_component_test.exs, test/scoria_web/live/workflow_live_test.exs, test/support/ds06_baseline.txt</files>
  <read_first>
    - lib/scoria_web/components/citation_evidence_component.ex
    - lib/scoria_web/components/delegated_evidence_component.ex
    - lib/scoria_web/components/memory_notebook_component.ex
    - test/scoria_web/components/memory_notebook_component_test.exs
    - test/scoria_web/live/workflow_live_test.exs
    - test/support/ds06_baseline.txt
    - test/scoria_web/ds06_drift_guard_test.exs
    - lib/scoria_web/ui.ex
    - assets/css/04-components.css
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-CONTEXT.md
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-UI-SPEC.md
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: citation evidence still renders query text, freshness, citations, ranked chunks, and unsupported claims.
    - Test 2: delegated evidence still renders handoff lineage, child pending copy, projected context preview, full context disclosure, and capability tags.
    - Test 3: memory notebook still renders sequence range, summary text, archived raw tokens, and runtime context link.
    - Test 4: all three adapters import `ScoriaWeb.UI` and use notebook/evidence primitives instead of local card chrome.
  </behavior>
  <action>Convert `CitationEvidenceComponent`, `DelegatedEvidenceComponent`, and `MemoryNotebookComponent` to the thin adapter contract. Each adapter should import `ScoriaWeb.UI` and render through `<.notebook>` plus `evidence_section`, `evidence_rows`, `evidence_action_row`, `evidence_empty`, `badge`, and `raw_evidence` where appropriate. Preserve existing assigned data contracts and visible evidence values. `CitationEvidenceComponent` must preserve `query_text`, `freshness`, citation labels/titles/locators, ranked chunk rank/score/body, and unsupported claims. `DelegatedEvidenceComponent` must preserve `delegated_handoffs`, parent/child IDs, delegated kind, status labels, projected context preview, full handoff/projected context disclosure, and capability metadata. `MemoryNotebookComponent` must preserve runtime instance link semantics, sequence range, archived raw token count, and summary text; make runtime links mount-prefix-safe if the caller supplies a base path, or leave existing route behavior untouched if no base path is currently available. Do not create a generic descriptor renderer. Lower/remove DS-06 rows only after scans prove counts.</action>
  <acceptance_criteria>
    - Source assertion: all three adapter files contain `import ScoriaWeb.UI` and `<.notebook`.
    - Source assertion: all three adapter files contain no raw palette class strings.
    - Source assertion: `DelegatedEvidenceComponent` still contains `delegated_status_label` or equivalent explicit status-label mapping.
    - Behavior assertion: component/workflow tests still prove delegated empty, pending, and recorded states render.
    - Behavior assertion: memory component tests still prove runtime presence reciprocal link and archived raw token copy.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/workflow_live_test.exs test/scoria_web/components/memory_notebook_component_test.exs test/scoria_web/ds06_drift_guard_test.exs</automated>
  </verify>
  <done>Retrieval, delegated, and memory evidence are thin notebook adapters with preserved evidence values and reduced DS-06 leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert replay, semantic, remote invocation, and incident adapters</name>
  <files>lib/scoria_web/components/replay_evidence_notebook_component.ex, lib/scoria_web/components/semantic_evidence_notebook_component.ex, lib/scoria_web/components/remote_invocation_evidence_component.ex, lib/scoria_web/components/incident_evidence_component.ex, test/scoria_web/components/semantic_evidence_notebook_component_test.exs, test/scoria_web/components/incident_evidence_component_test.exs, test/scoria_web/live/workflow_live_test.exs, test/scoria_web/ui_component_test.exs, test/support/ds06_baseline.txt</files>
  <read_first>
    - lib/scoria_web/components/replay_evidence_notebook_component.ex
    - lib/scoria_web/components/semantic_evidence_notebook_component.ex
    - lib/scoria_web/components/remote_invocation_evidence_component.ex
    - lib/scoria_web/components/incident_evidence_component.ex
    - test/scoria_web/components/semantic_evidence_notebook_component_test.exs
    - test/scoria_web/components/incident_evidence_component_test.exs
    - test/scoria_web/live/workflow_live_test.exs
    - test/scoria_web/ui_component_test.exs
    - test/support/ds06_baseline.txt
    - test/scoria_web/ds06_drift_guard_test.exs
    - lib/scoria_web/ui.ex
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: replay evidence still supports `Original trace` / `Replay trace` comparison source selection and raw evidence disclosure.
    - Test 2: semantic evidence still renders Summary, Compatibility, Provenance, Lifecycle, Candidate, Append-only events, and Advanced raw evidence.
    - Test 3: remote invocation evidence still renders approval tool name, status, and approval ID inside a notebook.
    - Test 4: incident evidence still renders health rollup, budget strip, incident notebook, breaker/relay evidence, and delivery outcomes.
    - Test 5: unsafe evidence values remain HEEx-escaped.
  </behavior>
  <action>Convert `ReplayEvidenceNotebookComponent`, `SemanticEvidenceNotebookComponent`, `RemoteInvocationEvidenceComponent`, and `IncidentEvidenceComponent` to consistently use the Plan 15-01 evidence primitives. Preserve their current public attrs, helper functions, labels, status/tone semantics, and event names. For replay evidence, keep `select_comparison_source` and the `Original trace`/`Replay trace` toggle behavior; use shared button/action row styling without raw palette classes. For semantic evidence, preserve group names and `render_value/1` fallback copy. For remote invocation, preserve explicit key presence behavior in `approval_value/2`. For incident evidence, preserve Phase 14 notebook shape and all incident/budget/breaker/audit/delivery fields while replacing local tokenized panels with shared evidence sections/rows. Use `raw_evidence/1` for advanced payload disclosure. Do not introduce `raw/1`, descriptor renderers, plugin registries, or schema languages. Lower/remove DS-06 rows only after scans prove counts.</action>
  <acceptance_criteria>
    - Source assertion: all four adapter files contain `<.notebook` and no raw palette class strings.
    - Source assertion: replay adapter still contains `phx-click=\"select_comparison_source\"`.
    - Source assertion: semantic adapter still contains `Advanced raw evidence` and uses shared `raw_evidence` or equivalent `ScoriaWeb.UI.raw_evidence/1`.
    - Behavior assertion: semantic and incident component tests still pass, including unsafe value escaping.
    - Behavior assertion: workflow tests still pass replay promotion, semantic evidence, remote invocation visibility, and incident evidence paths.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/workflow_live_test.exs test/scoria_web/components/semantic_evidence_notebook_component_test.exs test/scoria_web/components/incident_evidence_component_test.exs test/scoria_web/ui_component_test.exs test/scoria_web/ds06_drift_guard_test.exs</automated>
  </verify>
  <done>Replay, semantic, remote invocation, and incident evidence adapters share notebook/evidence primitives while preserving evidence contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Final adapter contract and DS-06 sweep</name>
  <files>test/support/ds06_baseline.txt, test/scoria_web/ds06_drift_guard_test.exs</files>
  <read_first>
    - test/support/ds06_baseline.txt
    - test/scoria_web/ds06_drift_guard_test.exs
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-CONTEXT.md
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-RESEARCH.md
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: every in-scope adapter file touched by this plan has a DS-06 count of zero or a lower count than the prior baseline.
    - Test 2: no new raw palette rows appear under `lib/scoria_web/`.
    - Test 3: the final full Phase 15 adapter test command passes.
  </behavior>
  <action>Run the DS-06 scanner through `mix test test/scoria_web/ds06_drift_guard_test.exs` and inspect any remaining rows for the Phase 15 adapter files. For each touched adapter whose scanner count is zero, remove its line from `test/support/ds06_baseline.txt`. For any touched adapter that cannot reach zero without broad unrelated refactor, lower the row to the exact new count and document the residual reason in `15-05-SUMMARY.md`. Do not lower a row below the actual scanner count. Do not add new exclusions to the DS-06 guard.</action>
  <acceptance_criteria>
    - Source assertion: no `test/support/ds06_baseline.txt` row remains for a touched adapter file whose source scan count is zero.
    - Source assertion: no new DS-06 guard exclusions are added.
    - Behavior assertion: `mix test test/scoria_web/ds06_drift_guard_test.exs` exits 0.
    - Source assertion: final changed adapter files are thin adapters over `ScoriaWeb.UI` primitives and do not own duplicated section/card chrome.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/components/semantic_evidence_notebook_component_test.exs test/scoria_web/components/incident_evidence_component_test.exs test/scoria_web/ui_component_test.exs test/scoria_web/ds06_drift_guard_test.exs</automated>
  </verify>
  <done>Adapter baseline rows reflect actual source counts and no touched adapter regresses DS-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence DTOs to HEEx | Evidence maps/DTOs may contain application or user-originated strings. |
| raw disclosure | Advanced payloads may contain nested maps/lists and must remain escaped and intentionally disclosed. |
| adapter-to-shared UI contract | Adapters can accidentally reintroduce local chrome or raw palette classes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-01 | Information Disclosure | evidence adapter values | mitigate | Render values through HEEx, keep raw payloads inside `raw_evidence/1`, and preserve unsafe-value escaping tests. |
| T-15-05-02 | Tampering | replay source selection | mitigate | Preserve existing `select_comparison_source` event, accepted source values, and workflow tests. |
| T-15-05-03 | Spoofing | runtime/context links | mitigate | Preserve existing routes and make base-path handling explicit where a base is available. |
| T-15-05-04 | Tampering | DS-06 baseline | mitigate | Lower/remove baseline rows only after source scan proves the exact count; do not add guard exclusions. |
| T-15-05-SC | Tampering | package installs | accept | No package-manager install tasks exist. |
</threat_model>

<verification>
Run `mix test test/scoria_web/live/workflow_live_test.exs test/scoria_web/components/semantic_evidence_notebook_component_test.exs test/scoria_web/components/incident_evidence_component_test.exs test/scoria_web/components/memory_notebook_component_test.exs test/scoria_web/ui_component_test.exs test/scoria_web/ds06_drift_guard_test.exs`. Confirm all converted adapters preserve values, use shared notebook/evidence primitives, escape unsafe values, and reduce DS-06 rows.
</verification>

<success_criteria>
Plan 15-05 succeeds when all remaining evidence components are thin notebook-shell adapters over `ScoriaWeb.UI`, no duplicated local evidence chrome remains in touched adapters, data contracts and workflow tests still pass, and the DS-06 baseline reflects lower or zero raw-palette counts for every touched adapter.
</success_criteria>

<output>
Create `.planning/phases/15-high-traffic-screens-evidence-adapters/15-05-SUMMARY.md` when done.
</output>

