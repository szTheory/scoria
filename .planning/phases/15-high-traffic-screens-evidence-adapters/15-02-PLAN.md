---
phase: 15-high-traffic-screens-evidence-adapters
plan: "02"
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - lib/scoria_web/live/orchestrator_live.ex
  - lib/scoria_web/live/workflow_live/index.ex
  - test/scoria_web/live/orchestrator_live_test.exs
  - test/scoria_web/live/orchestrator_live_integration_test.exs
  - test/scoria_web/live/orchestrator_live_sre_test.exs
  - test/scoria_web/live/workflow_live_test.exs
  - test/support/ds06_baseline.txt
autonomous: true
requirements:
  - SCREEN-03
user_setup: []
must_haves:
  truths:
    - "D-01: Home stays mounted at / as a calm status-first triage surface with attention cards and live trace stream."
    - "D-02/D-03/D-04: Home stream cards become compact read-only summaries with design-system deep links; raw evidence loading, replay, and promotion actions move out of Home."
    - "D-05: The day-0 empty state still makes first trace appearance obvious."
    - "D-06: Runs index is a shared-component scan surface with <.table id=\"runs\"> and one primary action: Open trace."
    - "D-24/D-25/D-28: Touched files reduce DS-06 rows, use Scoria voice, and preserve LiveViewTest-first verification."
  artifacts:
    - path: "lib/scoria_web/live/orchestrator_live.ex"
      provides: "Status-first Home / Live Ops stream with read-only deep links"
      contains: "Open trace"
    - path: "lib/scoria_web/live/workflow_live/index.ex"
      provides: "Shared table Runs index"
      contains: "<.table"
    - path: "test/support/ds06_baseline.txt"
      provides: "Lowered or removed rows for Home and Runs index"
      contains: "workflow_live/index.ex"
  key_links:
    - from: "lib/scoria_web/live/orchestrator_live.ex"
      to: "/workflows/:id"
      via: "Open run/Open trace deep links"
      pattern: "assigns[:scoria_base] || \"\""
    - from: "lib/scoria_web/live/workflow_live/index.ex"
      to: "/workflows/:id"
      via: "Open trace table action"
      pattern: "Open trace"
---

<objective>
Convert Home / Live Ops and the Runs index to the shared component layer while keeping Home narrow and action-light.

Purpose: This closes the high-traffic entry surfaces for SCREEN-03 without turning Home into a workbench.
Output: compact read-only Home trace stream, shared-table Runs index, updated tests, and DS-06 baseline reductions.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-high-traffic-screens-evidence-adapters/15-CONTEXT.md
@.planning/phases/15-high-traffic-screens-evidence-adapters/15-RESEARCH.md
@.planning/phases/15-high-traffic-screens-evidence-adapters/15-VALIDATION.md
@.planning/phases/15-high-traffic-screens-evidence-adapters/15-PATTERNS.md
@.planning/phases/15-high-traffic-screens-evidence-adapters/15-UI-SPEC.md
@.planning/phases/13-orientation-spine-ia/13-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert Home stream cards to read-only summaries and deep links</name>
  <files>lib/scoria_web/live/orchestrator_live.ex, test/scoria_web/live/orchestrator_live_test.exs, test/scoria_web/live/orchestrator_live_integration_test.exs, test/scoria_web/live/orchestrator_live_sre_test.exs, test/support/ds06_baseline.txt</files>
  <read_first>
    - lib/scoria_web/live/orchestrator_live.ex
    - test/scoria_web/live/orchestrator_live_test.exs
    - test/scoria_web/live/orchestrator_live_integration_test.exs
    - test/scoria_web/live/orchestrator_live_sre_test.exs
    - test/support/ds06_baseline.txt
    - test/scoria_web/ds06_drift_guard_test.exs
    - lib/scoria_web/ui.ex
    - lib/scoria_web/components/trace_tree_component.ex
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-CONTEXT.md
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-UI-SPEC.md
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: Home still renders exact orientation copy and the day-0 empty state `No traces yet`.
    - Test 2: PubSub `:new_trace`, `:trace_opened`, `:trace_span`, and token preview behavior still render streamed spans.
    - Test 3: trace stream HTML no longer contains `Load Deep Metadata`, `Load Retrieval Evidence`, `Load Budget State`, `Load Incident Evidence`, `Replay Retrieval`, or `Promote Retrieval`.
    - Test 4: trace stream renders compact action labels only when backed by current trace data: `Open run`, `Open trace`, `Review approval`, or `Open incident`.
    - Test 5: stream links are mount-prefix-safe and use `assigns[:scoria_base] || ""`.
  </behavior>
  <action>In `OrchestratorLive`, preserve mount/session tenant handling, attention cards, PubSub subscription, trace hydration, token preview coalescing, and the `TraceTreeComponent` span summary. Replace the raw trace card shell and six inline workbench buttons with a compact read-only summary using shared components (`<.panel>` or tokenized `scoria-*` classes, `<.badge>`, `<.id>`, and deep links). Remove Home rendering for lazy retrieval/budget/incident evidence panels, replay notice, and promote notice after updating tests; delete the now-unused `load_metadata`, `load_retrieval_evidence`, `load_budget_state`, `load_incident_evidence`, `replay_retrieval`, `promote_retrieval`, `sample_evidence/1`, and `refresh_trace_badges/3` code only if no updated tests or other code paths reference them. Generate deep links with the existing base path pattern. Use `/workflows/#{workflow_run_id}` for `Open run`/`Open trace` when `workflow_run_id` exists. Render `Review approval` or `Open incident` only when existing trace badges/data support those destinations; otherwise omit instead of faking links. Keep the day-0 empty body aligned to UI-SPEC: `The first chat response, agent run, eval sample, or MCP request will appear here as a trace.` Remove raw palette matches from this file and update `test/support/ds06_baseline.txt` only after the scanner count is lower or zero.</action>
  <acceptance_criteria>
    - Source assertion: `lib/scoria_web/live/orchestrator_live.ex` does not contain `Load Deep Metadata`, `Load Retrieval Evidence`, `Load Budget State`, `Load Incident Evidence`, `Replay Retrieval`, or `Promote Retrieval`.
    - Source assertion: Home stream link construction uses `assigns[:scoria_base] || ""` or a helper that receives that base path.
    - Source assertion: `lib/scoria_web/live/orchestrator_live.ex` contains no new raw palette classes and has a lower DS-06 count than the previous `test/support/ds06_baseline.txt` row.
    - Behavior assertion: orchestrator tests prove trace streaming still works and old mutating Home controls are absent.
    - Behavior assertion: SRE/orchestrator tests prove incident/budget signals still appear as compact badges or summary text when trace data carries them, without requiring lazy Home workbench controls.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/orchestrator_live_test.exs test/scoria_web/live/orchestrator_live_integration_test.exs test/scoria_web/live/orchestrator_live_sre_test.exs test/scoria_web/ds06_drift_guard_test.exs</automated>
  </verify>
  <done>Home remains a live status surface with compact deep links, no inline provenance/replay/promote controls, and reduced DS-06 leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert Runs index to shared table</name>
  <files>lib/scoria_web/live/workflow_live/index.ex, test/scoria_web/live/workflow_live_test.exs, test/support/ds06_baseline.txt</files>
  <read_first>
    - lib/scoria_web/live/workflow_live/index.ex
    - test/scoria_web/live/workflow_live_test.exs
    - test/support/ds06_baseline.txt
    - test/scoria_web/ds06_drift_guard_test.exs
    - lib/scoria_web/ui.ex
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-CONTEXT.md
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-UI-SPEC.md
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: Runs index renders title `Runs` and subtitle `Inspect recorded workflow runs and open the trace that explains them.`
    - Test 2: when runs exist, the page contains `<table id="runs"` and shared table classes.
    - Test 3: rows preserve short run ID, status badge text, runtime/session value, timestamp formatting, and `Open trace` action.
    - Test 4: empty state uses exact UI-SPEC copy and does not imply unbacked trace search/analytics.
  </behavior>
  <action>In `WorkflowLive.Index`, replace the raw table markup with `<.table id="runs" rows={@runs} density={:compact}>`. Use columns `Run`, `Status`, `Runtime`, `Started`, and an action slot with the existing `Open trace` navigation to `(assigns[:scoria_base] || "") <> "/workflows/#{run.id}"`. Avoid keyed sortable columns unless a `sort` handler is also implemented; do not render clickable inert controls. If the density toggle needs parent support in tests, add a minimal `handle_event("set_density", %{"density" => density}, socket)` that validates `compact/default/comfortable` and stores `:run_table_density`, then pass that assign to `<.table>`. Preserve `list_runs/0`, 50-row ordering, `short_id/1`, `format_ts/1`, and real `Scoria.Workflows.Run` records. Remove raw `text-stone-*` classes from the file and lower/remove its DS-06 baseline row only after the count is proven.</action>
  <acceptance_criteria>
    - Source assertion: `lib/scoria_web/live/workflow_live/index.ex` contains `<.table` and `id="runs"` or `id={"runs"}`.
    - Source assertion: `lib/scoria_web/live/workflow_live/index.ex` does not contain a literal `<table class="scoria-table">`.
    - Source assertion: `lib/scoria_web/live/workflow_live/index.ex` contains the exact subtitle `Inspect recorded workflow runs and open the trace that explains them.`
    - Behavior assertion: workflow tests verify real persisted runs render and `Open trace` points to `/scoria/workflows/#{run.id}` under the dashboard mount.
    - Source assertion: the DS-06 row for `lib/scoria_web/live/workflow_live/index.ex` is lowered or removed according to the scanner count.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/workflow_live_test.exs test/scoria_web/ds06_drift_guard_test.exs</automated>
  </verify>
  <done>Runs index is a compact shared table with preserved real-run navigation and reduced DS-06 leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser-to-LiveView stream events | Trace and PubSub messages update Home stream state. |
| Home links to durable object pages | Trace/run/approval/incident IDs become internal navigation URLs. |
| Runs index to workflow detail | Run IDs from the database are rendered into dashboard links. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | Tampering | Home workbench events | mitigate | Remove or strand mutating lazy evidence/replay/promote controls from Home rendering; tests assert labels are absent. |
| T-15-02-02 | Spoofing | mount-prefix links | mitigate | Generate links from `assigns[:scoria_base] || ""`; tests assert `/scoria` prefixed routes under mounted router. |
| T-15-02-03 | Information Disclosure | raw evidence on Home | mitigate | Do not render raw evidence panels or raw JSON disclosures on Home. Inspection moves to Workflow Show and evidence notebooks. |
| T-15-02-SC | Tampering | package installs | accept | No package-manager install tasks exist. |
</threat_model>

<verification>
Run `mix test test/scoria_web/live/orchestrator_live_test.exs test/scoria_web/live/orchestrator_live_integration_test.exs test/scoria_web/live/orchestrator_live_sre_test.exs test/scoria_web/live/workflow_live_test.exs test/scoria_web/ds06_drift_guard_test.exs`. Confirm old Home mutating labels are absent, Home stream still updates from PubSub, Runs uses `<.table id="runs">`, and DS-06 baseline rows are reduced or removed only after source counts prove it.
</verification>

<success_criteria>
Plan 15-02 succeeds when Home is a read-only triage stream with deep links, Runs is a shared table entry point, old Home workbench actions are gone, real trace/run navigation still works, and both files move down the DS-06 baseline.
</success_criteria>

<output>
Create `.planning/phases/15-high-traffic-screens-evidence-adapters/15-02-SUMMARY.md` when done.
</output>

