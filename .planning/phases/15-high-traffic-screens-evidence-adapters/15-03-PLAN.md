---
phase: 15-high-traffic-screens-evidence-adapters
plan: "03"
type: execute
wave: 3
depends_on:
  - "15-01"
files_modified:
  - lib/scoria_web/live/workflow_live/show.ex
  - lib/scoria_web/components/workflow_tree_component.ex
  - lib/scoria_web/components/trace_tree_component.ex
  - lib/scoria_web/components/workflow_detail_panel_component.ex
  - test/scoria_web/live/workflow_live_test.exs
  - test/scoria_web/components/trace_tree_component_test.exs
  - test/scoria_web/components/workflow_tree_component_test.exs
  - test/support/ds06_baseline.txt
autonomous: true
requirements:
  - SCREEN-03
  - SCREEN-04
user_setup: []
must_haves:
  truths:
    - "D-07/D-08: Workflow Show is the canonical APM-style trace inspector with object header, durable run/span tree, selected-step detail, and evidence notebooks."
    - "D-09: Next-step verbs stay flat and evidence-based: Replay run, Promote in Dataset Builder, Open incident, Open prompt, View associated runtime presence."
    - "D-10: Timeline, replay provenance, semantic evidence, delegated evidence, memory evidence, remote invocation evidence, and raw details render through shared components or thin adapters."
    - "D-11: Do not add TraceQL search, flame graph/map modes, session-first replay, conversation browser, or new trace analytics."
    - "D-27: Use conventional operator-dashboard components: shared panels, drawers, modals, badges, empty states, object headers, next-step verb rows, tables, and notebook shells; avoid decorative cards, vanity metrics, unbacked graph modes, fake timelines, and marketing composition."
    - "D-24/D-28: Touched Workflow Show/tree files reduce DS-06 rows and remain LiveViewTest-first."
  artifacts:
    - path: "lib/scoria_web/live/workflow_live/show.ex"
      provides: "Shared-component Workflow Show trace inspector"
      contains: "View associated runtime presence"
    - path: "lib/scoria_web/components/workflow_detail_panel_component.ex"
      provides: "Shared selected-step detail shell"
      contains: "Promote in Dataset Builder"
    - path: "test/support/ds06_baseline.txt"
      provides: "Lowered or removed rows for Workflow Show and tree/detail components"
      contains: "workflow_live/show.ex"
  key_links:
    - from: "lib/scoria_web/live/workflow_live/show.ex"
      to: "/datasets?promote=workflow"
      via: "Promote in Dataset Builder"
      pattern: "promote=workflow"
    - from: "lib/scoria_web/live/workflow_live/show.ex"
      to: "/connectors or Home runtime focus"
      via: "View associated runtime presence"
      pattern: "runtime="
---

<objective>
Convert Workflow Show into the canonical shared-component trace inspector while preserving run loading, step selection, promotion, and quality-loop links.

Purpose: This closes the Workflows / Trace Explorer portion of SCREEN-03 and prepares selected-step evidence for the adapter sweep in SCREEN-04.
Output: tokenized Workflow Show shell, shared modal promotion boundary, tokenized tree/detail components, tests, and DS-06 baseline reductions.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-high-traffic-screens-evidence-adapters/15-CONTEXT.md
@.planning/phases/15-high-traffic-screens-evidence-adapters/15-RESEARCH.md
@.planning/phases/15-high-traffic-screens-evidence-adapters/15-VALIDATION.md
@.planning/phases/15-high-traffic-screens-evidence-adapters/15-PATTERNS.md
@.planning/phases/15-high-traffic-screens-evidence-adapters/15-UI-SPEC.md
@.planning/phases/14-least-iterated-screens-polish/14-CONTEXT.md
@.planning/phases/13-orientation-spine-ia/13-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert Workflow Show shell, notices, timeline, and promotion modal</name>
  <files>lib/scoria_web/live/workflow_live/show.ex, test/scoria_web/live/workflow_live_test.exs, test/support/ds06_baseline.txt</files>
  <read_first>
    - lib/scoria_web/live/workflow_live/show.ex
    - test/scoria_web/live/workflow_live_test.exs
    - test/support/ds06_baseline.txt
    - test/scoria_web/ds06_drift_guard_test.exs
    - lib/scoria_web/ui.ex
    - lib/scoria_web/live/dataset_live/promote_component.ex
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-CONTEXT.md
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-UI-SPEC.md
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: Workflow Show still renders `scoria-object-header`, run ID, status, and allowlisted origin chip.
    - Test 2: next-step verbs render exactly `Replay run`, `Promote in Dataset Builder`, `Open incident`, `Open prompt`, and `View associated runtime presence` when backed.
    - Test 3: Dataset Builder promotion URL still contains `promote=workflow`, `run_id`, `step_id`, `source_variant`, and `from=run:<id>`.
    - Test 4: promotion confirmation uses shared `<.modal>` markup and still renders `DatasetLive.PromoteComponent`.
    - Test 5: promotion and baseline notices render through shared tokenized components.
  </behavior>
  <action>In `WorkflowLive.Show`, replace the local `min-h-screen bg-stone-50 px-6 py-8 text-stone-900` shell with app-layout-native `scoria-dashboard`/page sections. Import the needed `ScoriaWeb.UI` components (`object_header`, `skeleton`, `panel`, `badge`, `button`, `modal`, `evidence_section`, `evidence_rows`, `evidence_action_row`, and `raw_evidence` as needed). Preserve `load_run/2`, `handle_params/3`, `select_step`, `select_comparison_source`, `open_promote_modal`, `close_modal`, promotion notices, and existing URL helpers. Convert replay provenance strip, promotion/baseline notices, review candidate evidence, timeline, memory failure state, and raw detail shells to shared panels/evidence primitives. Replace the custom fixed overlay promote modal with `<.modal id=\"promote-modal\" show={@promote_step_id != nil} on_dismiss=\"close_modal\">` while still rendering `ScoriaWeb.DatasetLive.PromoteComponent` with the selected step, promotion context, and `scoria_base`. Change the runtime presence link from hard-coded `/scoria?runtime=...` to a mount-prefix-safe URL using `assigns[:scoria_base] || \"\"`; keep the exact label `View associated runtime presence`. Do not add TraceQL, flame graph/map, service map, session-first replay, conversation browser, or analytics controls.</action>
  <acceptance_criteria>
    - Source assertion: `lib/scoria_web/live/workflow_live/show.ex` contains `<.modal`, `<.object_header`, and `View associated runtime presence`.
    - Source assertion: `lib/scoria_web/live/workflow_live/show.ex` does not contain `bg-stone-50`, `text-stone-900`, `border-blue-200`, `bg-emerald-50`, or `bg-black/50`.
    - Source assertion: runtime presence link no longer contains a hard-coded `href={\"/scoria?runtime=` expression.
    - Behavior assertion: workflow tests still pass quality-loop URL assertions for replay, dataset, incident, and prompt links.
    - Behavior assertion: promotion modal tests still prove blank notes field and `DatasetLive.PromoteComponent` behavior.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/workflow_live_test.exs test/scoria_web/ds06_drift_guard_test.exs</automated>
  </verify>
  <done>Workflow Show uses shared shell/modal/notice components, keeps all backed next-step links, and lowers DS-06 leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tokenize workflow tree, trace tree, and selected-step detail</name>
  <files>lib/scoria_web/components/workflow_tree_component.ex, lib/scoria_web/components/trace_tree_component.ex, lib/scoria_web/components/workflow_detail_panel_component.ex, test/scoria_web/components/trace_tree_component_test.exs, test/scoria_web/components/workflow_tree_component_test.exs, test/scoria_web/live/workflow_live_test.exs, test/support/ds06_baseline.txt</files>
  <read_first>
    - lib/scoria_web/components/workflow_tree_component.ex
    - lib/scoria_web/components/trace_tree_component.ex
    - lib/scoria_web/components/workflow_detail_panel_component.ex
    - test/scoria_web/components/trace_tree_component_test.exs
    - test/scoria_web/components/workflow_tree_component_test.exs
    - test/scoria_web/live/workflow_live_test.exs
    - test/support/ds06_baseline.txt
    - lib/scoria_web/ui.ex
    - assets/css/04-components.css
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: `TraceTreeComponent` still renders a flat DOM structure and dynamic `--indent-level` styles.
    - Test 2: token preview remains visible for active LLM spans without raw `emerald-*` classes.
    - Test 3: `WorkflowTreeComponent` still emits `select_step`, selected row state, visible status badge, role ID, kind, and handoff marker.
    - Test 4: `WorkflowDetailPanelComponent` still disables promotion until a frozen promotion snapshot exists and preserves helper copy.
  </behavior>
  <action>Convert `TraceTreeComponent`, `WorkflowTreeComponent`, and `WorkflowDetailPanelComponent` away from raw Tailwind palette classes and local raised-card chrome. Use existing `.scoria-span`, `.scoria-row-selected`, `<.badge>`, `<.button>` or semantic `scoria-button` markup, `<.panel>`, and Phase 15 evidence primitives from Plan 15-01. Preserve `TraceTreeComponent` as a LiveComponent with `active_span_id`, `load_metadata`, async metadata, flat sibling rows, token preview behavior, and `--indent-level` style. Preserve `WorkflowTreeComponent.workflow_tree/1` as a function component with `phx-click=\"select_step\"`, `phx-value-id`, status badge, role ID, kind, and explicit selected state. Preserve `WorkflowDetailPanelComponent.workflow_detail_panel/1`, promotion disabled logic, source variant helper copy, and nested `ReplayEvidenceNotebookComponent` / `SemanticEvidenceNotebookComponent` rendering. If `<.button>` cannot pass needed `phx-value-step-id`, use a semantic `<button class=\"scoria-button ...\">` or extend `button/1` in Plan 15-01 and keep tests green.</action>
  <acceptance_criteria>
    - Source assertion: the three touched component files contain no `stone-`, `gray-`, `emerald-`, `amber-`, `rose-`, `red-`, or `blue-` raw palette class strings.
    - Source assertion: `TraceTreeComponent` still contains `phx-target={@myself}` for `load_metadata`.
    - Source assertion: `WorkflowTreeComponent` still contains `phx-click=\"select_step\"` and `workflow-tree-row`.
    - Source assertion: `WorkflowDetailPanelComponent` still contains `promotion_disabled?` and renders `ReplayEvidenceNotebookComponent.render`.
    - Behavior assertion: trace tree and workflow tests still pass after visual-shell conversion.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/workflow_live_test.exs test/scoria_web/components/trace_tree_component_test.exs test/scoria_web/components/workflow_tree_component_test.exs test/scoria_web/ds06_drift_guard_test.exs</automated>
  </verify>
  <done>Workflow tree, trace tree, and selected-step detail use shared/tokenized primitives while preserving trace selection and promotion behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params to origin context | `from=` params are user-controlled and must stay allowlisted. |
| browser-to-LiveView events | Step selection, comparison source, and promotion modal events carry user-controlled IDs/source values. |
| Workflow Show to Dataset Builder | Promotion URL query params carry stable IDs and intent. |
| rendered evidence values | Run, step, event, and semantic values may contain application data and must remain HEEx-escaped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Tampering | promotion query params | mitigate | Preserve existing `workflow_dataset_builder_path/4` stable IDs and LiveView tests for `promote=workflow`, `run_id`, `step_id`, `source_variant`, and `from`. |
| T-15-03-02 | Spoofing | origin context | mitigate | Keep `@origin_nouns` allowlist and existing unknown-origin tests. |
| T-15-03-03 | Information Disclosure | raw workflow/evidence rendering | mitigate | Replace local raw blocks with shared `raw_evidence/1` and render all values through HEEx. |
| T-15-03-04 | Denial of Service | expensive trace UI | accept | Phase changes presentation only; no TraceQL/search/analytics or new query families are added. |
| T-15-03-SC | Tampering | package installs | accept | No package-manager install tasks exist. |
</threat_model>

<verification>
Run `mix test test/scoria_web/live/workflow_live_test.exs test/scoria_web/components/trace_tree_component_test.exs test/scoria_web/components/workflow_tree_component_test.exs test/scoria_web/ds06_drift_guard_test.exs`. Confirm Workflow Show still has object header, flat next-step verbs, mount-prefix-safe links, selected-step detail, Dataset Builder promotion modal behavior, and no raw palette regressions in touched files.
</verification>

<success_criteria>
Plan 15-03 succeeds when Workflow Show is the canonical trace inspector using shared components, backed quality-loop links still pass, selected-step detail still works, promotion stays Dataset Builder-owned, and Workflow Show/tree/detail DS-06 rows are lowered or removed.
</success_criteria>

<output>
Create `.planning/phases/15-high-traffic-screens-evidence-adapters/15-03-SUMMARY.md` when done.
</output>
