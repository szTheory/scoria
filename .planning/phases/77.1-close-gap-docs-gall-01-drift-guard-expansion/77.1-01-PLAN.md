---
phase: 77.1-close-gap-docs-gall-01-drift-guard-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/support_journey.ex
  - test/scoria/support_journey_source_test.exs
  - README.md
  - docs/operator_verification.md
autonomous: true
requirements: [DOCS-GALL-01]
must_haves:
  truths:
    - "SupportJourneySourceTest pins gallery-related fragments against README.md, docs/operator_verification.md, and docs/support_copilot_gallery.md."
    - "Drift between SupportJourney SSOT (gallery path, advisory lane command, fixture references) and any of the three adopter docs fails CI."
    - "VerificationLanes.closeout_order/0 remains unchanged; advisory gallery lane posture unchanged."
  artifacts:
    - path: "lib/scoria/support_journey.ex"
      provides: "Per-surface adopter doc fragment SSOT"
      contains: "adopter_doc_surfaces"
    - path: "test/scoria/support_journey_source_test.exs"
      provides: "Multi-surface drift guard contract tests"
      contains: "adopter_doc_surfaces"
    - path: "README.md"
      provides: "Gallery quick-start with SupportJourney cross-link"
      contains: "Scoria.SupportJourney"
    - path: "docs/operator_verification.md"
      provides: "Maintainer gallery lane guidance aligned to SSOT"
      contains: "mix scoria.test.support_copilot"
  key_links:
    - from: "lib/scoria/support_journey.ex"
      to: "test/scoria/support_journey_source_test.exs"
      via: "adopter_doc_surfaces/0 drives per-doc fragment assertions"
      pattern: "adopter_doc_surfaces"
    - from: "lib/scoria/support_journey.ex"
      to: "README.md"
      via: "readme_doc_fragments/0 pins gallery path, advisory command, SupportJourney ref"
      pattern: "examples/support_copilot"
---

<objective>
Close the v2.9 milestone audit partial on DOCS-GALL-01 by extending automated SupportJourney fragment pinning from the gallery guide alone to README and operator verification surfaces.

Purpose: Gallery fixtures, advisory lane command, and all three adopter docs stay linked via CI — not only `docs/support_copilot_gallery.md`.
Output: Per-surface fragment SSOT in `SupportJourney`, multi-surface contract tests, and minimal doc edits only where SSOT fragments are absent.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/v2.9-MILESTONE-AUDIT.md
@lib/scoria/support_journey.ex
@test/scoria/support_journey_source_test.exs
@README.md
@docs/operator_verification.md
@docs/support_copilot_gallery.md
@test/scoria/verification_lanes_test.exs

**Gap:** `SupportJourneySourceTest` only asserts `doc_fragments/0` against `docs/support_copilot_gallery.md`. README and `docs/operator_verification.md` document the gallery but are not in the drift chain.

**Design constraint:** Do not require full journey narrative fragments (handoff tools, ticket IDs, API call names) in README/operator — those belong in the gallery guide. Define **scoped per-surface fragment lists** sourced from `SupportJourney` accessors, not a blind copy of `doc_fragments/0` to every doc.

**Non-goals:** Do not widen `VerificationLanes.closeout_order/0`. Do not add gallery to merge-blocking closeout. Do not refactor unrelated adoption_surface_test pins.
</context>

<tasks>

<task type="auto">
  <name>Task 77.1-01-01: Add per-surface adopter doc fragment SSOT</name>
  <requirement_ids>DOCS-GALL-01</requirement_ids>
  <files>lib/scoria/support_journey.ex</files>
  <read_first>
    lib/scoria/support_journey.ex
    docs/support_copilot_gallery.md
    README.md
    docs/operator_verification.md
    .planning/v2.9-MILESTONE-AUDIT.md
  </read_first>
  <action>
    In `Scoria.SupportJourney`:

    1. Keep `doc_fragments/0` as the **gallery guide** full journey contract (unchanged fragment list).

    2. Add `readme_doc_fragments/0` returning gallery quick-start pins built from existing accessors plus stable literals:
       - `gallery_path/0`
       - `advisory_lane_command/0`
       - `"docs/support_copilot_gallery.md"`
       - `"not part of closeout order"` (advisory posture phrase)
       - `"Scoria.SupportJourney"` (fixture SSOT cross-link — README currently lacks this)

    3. Add `operator_doc_fragments/0` returning maintainer-surface pins:
       - `gallery_path/0`
       - `advisory_lane_command/0`
       - `"Scoria.SupportJourney"`
       - `"VerificationLanes.closeout_order/0"`
       - `"support_copilot_gallery.md"` (relative link target in operator guide)
       - `"Scoria.get_run_detail/1"` (handoff evidence API referenced in operator lane list)

    4. Add `adopter_doc_surfaces/0` returning a map of relative repo paths to fragment lists:
       - `"docs/support_copilot_gallery.md"` => `doc_fragments/0`
       - `"README.md"` => `readme_doc_fragments/0`
       - `"docs/operator_verification.md"` => `operator_doc_fragments/0`

    Document in `@moduledoc` that `adopter_doc_surfaces/0` is the SSOT for drift guards across all three adopter docs.
  </action>
  <acceptance_criteria>
    - `rg 'def readme_doc_fragments' lib/scoria/support_journey.ex` returns one match.
    - `rg 'def operator_doc_fragments' lib/scoria/support_journey.ex` returns one match.
    - `rg 'def adopter_doc_surfaces' lib/scoria/support_journey.ex` returns one match.
    - `rg 'doc_fragments/0' lib/scoria/support_journey.ex` still present (gallery guide contract preserved).
  </acceptance_criteria>
  <verify>
    <automated>rg 'adopter_doc_surfaces|readme_doc_fragments|operator_doc_fragments' lib/scoria/support_journey.ex</automated>
  </verify>
  <done>SupportJourney exposes scoped fragment lists per adopter doc surface without diluting the gallery guide contract.</done>
</task>

<task type="auto">
  <name>Task 77.1-01-02: Align README and operator docs to SSOT fragments</name>
  <requirement_ids>DOCS-GALL-01</requirement_ids>
  <files>README.md, docs/operator_verification.md</files>
  <read_first>
    README.md
    docs/operator_verification.md
    lib/scoria/support_journey.ex
    docs/support_copilot_gallery.md
  </read_first>
  <action>
    Make **minimal** doc edits so every fragment in `readme_doc_fragments/0` and `operator_doc_fragments/0` is present in the target file:

    **README.md** (`### Support copilot gallery (local demo)` section):
    - Add one sentence referencing shared fixtures in `Scoria.SupportJourney` and `priv/fixtures/support_journey/` (matching gallery guide tone). Do not duplicate the full journey narrative.

    **docs/operator_verification.md** (`## Support copilot gallery (advisory)` section):
    - Verify existing content already contains all `operator_doc_fragments/0` entries; add only if any fragment is missing after Task 01 defines the list.
    - Do not change closeout chain ordering or merge-blocking lane copy outside the gallery subsection.

    Do not edit `docs/support_copilot_gallery.md` unless a fragment assertion fails after Task 03.
  </action>
  <acceptance_criteria>
    - `rg 'Scoria\.SupportJourney' README.md` returns at least one match in the gallery section.
    - `rg 'mix scoria\.test\.support_copilot' docs/operator_verification.md` returns at least one match.
    - `rg 'VerificationLanes\.closeout_order/0' docs/operator_verification.md` returns at least one match in gallery context.
    - README gallery section still contains `cd examples/support_copilot` and `mix phx.server` quick-start blocks.
  </acceptance_criteria>
  <verify>
    <automated>rg 'Scoria\.SupportJourney' README.md && rg 'mix scoria\.test\.support_copilot' docs/operator_verification.md</automated>
  </verify>
  <done>README and operator verification contain every SSOT fragment for their surface without doc bloat.</done>
</task>

<task type="auto">
  <name>Task 77.1-01-03: Extend SupportJourneySourceTest for multi-surface drift guards</name>
  <requirement_ids>DOCS-GALL-01</requirement_ids>
  <files>test/scoria/support_journey_source_test.exs</files>
  <read_first>
    test/scoria/support_journey_source_test.exs
    lib/scoria/support_journey.ex
    test/scoria/adoption_surface_test.exs
    .planning/v2.9-MILESTONE-AUDIT.md
  </read_first>
  <action>
    Refactor `test/scoria/support_journey_source_test.exs`:

    1. Replace the single-gallery-doc test with a parameterized test over `SupportJourney.adopter_doc_surfaces/0`:
       - For each `{path, fragments}` pair, read the file from repo root and assert every fragment is a substring of the content.
       - Use descriptive test names per surface (e.g. "README stays aligned...", "operator verification stays aligned...", "gallery guide stays aligned...").

    2. Keep existing fixture-load and handoff-input tests unchanged.

    3. Do not duplicate fragment lists in the test module — always source from `SupportJourney`.

    4. Run `MIX_ENV=test mix test test/scoria/support_journey_source_test.exs` and fix any doc gaps surfaced by failures (prefer doc edits in Task 02 scope over weakening fragments).
  </action>
  <acceptance_criteria>
    - `rg 'adopter_doc_surfaces' test/scoria/support_journey_source_test.exs` returns assertion loop.
    - `MIX_ENV=test mix test test/scoria/support_journey_source_test.exs` exits 0.
    - `MIX_ENV=test mix test test/scoria/verification_lanes_test.exs` exits 0 (closeout_order unchanged).
    - Test module references at least three doc paths: README.md, docs/operator_verification.md, docs/support_copilot_gallery.md.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/support_journey_source_test.exs test/scoria/verification_lanes_test.exs</automated>
  </verify>
  <done>CI fails when gallery fixtures, advisory lane command, or adopter doc copy drifts from SupportJourney SSOT on any of the three surfaces.</done>
</task>

</tasks>

<threat_model>
## Security Gate

- Enforcement: enabled
- ASVS level: 1
- Block threshold: high

| ID | Threat | Severity | Mitigation in plan |
|----|--------|----------|-------------------|
| T-77.1-01 | Doc drift guard weakened by empty fragment lists | low | Fragment lists built from accessors; tests assert non-empty surfaces |
| T-77.1-02 | Advisory lane accidentally promoted to closeout | low | Explicit verification_lanes_test.exs gate in acceptance criteria |

No high-severity threats. Plan may proceed.
</threat_model>

<verification>
## Plan Verification

- [ ] `adopter_doc_surfaces/0` defined in SupportJourney
- [ ] README contains `Scoria.SupportJourney` gallery cross-link
- [ ] `support_journey_source_test.exs` asserts all three adopter doc surfaces
- [ ] `mix test test/scoria/support_journey_source_test.exs` green
- [ ] `VerificationLanes.closeout_order/0` unchanged (verification_lanes_test.exs green)
</verification>

<success_criteria>
1. DOCS-GALL-01 audit partial closed — drift guards cover README + operator verification + gallery guide.
2. No widening of merge-blocking closeout chain.
3. Fragment SSOT remains in `Scoria.SupportJourney`; tests do not hardcode duplicate lists.
</success_criteria>
