---
phase: 80-upgrade-smoke-in-adoption-lane
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/hex_consumer_contract.ex
  - test/fixtures/hex_consumer/scoria-0.1.0-unpack
  - test/fixtures/hex_consumer/.scoria-hex-baseline.stamp
  - lib/mix/tasks/scoria.hex_consumer.refresh_baseline_fixture.ex
  - test/scoria/hex_consumer_contract_test.exs
  - test/scoria/hex_consumer_baseline_fixture_test.exs
autonomous: true
requirements: [HEX-UPGRADE-01]
must_haves:
  truths:
    - "Committed frozen unpack at test/fixtures/hex_consumer/scoria-0.1.0-unpack/ from v0.1.0 tag — not HEAD rebuild in CI (D-50)."
    - "baseline_unpack_root!/0, baseline_package_fingerprint/0, same_semver_content_upgrade?/0 on HexConsumerContract (D-51, D-54)."
    - "Stamp file .scoria-hex-baseline.stamp records fingerprint + v0.1.0 git SHA; policy test fails on drift (D-51)."
    - "SCORIA_HEX_BASELINE_UNPACK_ROOT env override for maintainer only — never documented for CI (D-52)."
    - "Fixture not in mix.exs package files; fixture dir not gitignored (D-52)."
  artifacts:
    - path: "test/fixtures/hex_consumer/scoria-0.1.0-unpack"
      provides: "Frozen 0.1.0 hex.build unpack tree"
      contains: "mix.exs"
    - path: "lib/scoria/hex_consumer_contract.ex"
      provides: "baseline_unpack_root!/0 and baseline_package_fingerprint/0"
      contains: "def baseline_unpack_root!"
---

<objective>
Create the committed v0.1.0 baseline fixture and HexConsumerContract baseline APIs with drift guard and maintainer refresh task.

Purpose: Upgrade proof requires a true published baseline separate from current HEAD tarball (D-50–D-52).
Output: Fixture on disk, contract helpers, stamp + tests green.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/80-upgrade-smoke-in-adoption-lane/80-CONTEXT.md
@.planning/phases/80-upgrade-smoke-in-adoption-lane/80-RESEARCH.md
@lib/scoria/hex_consumer_contract.ex
@test/scoria/hex_consumer_contract_test.exs
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-80-01 | CI rebuilds baseline from HEAD — false upgrade signal | high | Committed fixture + stamp fingerprint guard |
| T-80-02 | Stale fixture passes drift test | medium | Stamp SHA + fingerprint recompute on refresh |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 80-01-01: Build and commit v0.1.0 baseline unpack fixture</name>
  <requirement_ids>HEX-UPGRADE-01</requirement_ids>
  <files>test/fixtures/hex_consumer/scoria-0.1.0-unpack, test/fixtures/hex_consumer/.scoria-hex-baseline.stamp</files>
  <read_first>
    lib/scoria/hex_consumer_contract.ex
    .planning/phases/80-upgrade-smoke-in-adoption-lane/80-CONTEXT.md
    .planning/phases/80-upgrade-smoke-in-adoption-lane/80-RESEARCH.md
  </read_first>
  <action>
1. From repo root, checkout content at git tag `v0.1.0` (SHA `49f2d60018c4c79fbc09969116526c48454a8e84`) in a temp worktree or subprocess.
2. Run `mix hex.build --unpack --output test/fixtures/hex_consumer/scoria-0.1.0-unpack-build` at that tag; use `HexConsumerContract.unpack_root!/1` to locate inner package root; move/copy final tree to `test/fixtures/hex_consumer/scoria-0.1.0-unpack/`.
3. Write stamp `test/fixtures/hex_consumer/.scoria-hex-baseline.stamp` with two lines: line 1 = baseline package fingerprint (computed from fixture tree using same algorithm as `package_fingerprint/0` but scoped to fixture files), line 2 = `v0.1.0` git SHA `49f2d60018c4c79fbc09969116526c48454a8e84`.
4. Ensure `test/fixtures/hex_consumer/scoria-0.1.0-unpack/mix.exs` exists and version in unpack is `0.1.0`.
5. Do NOT add fixture path to `mix.exs` `:package` `:files` list.
  </action>
  <acceptance_criteria>
    - `test -f test/fixtures/hex_consumer/scoria-0.1.0-unpack/mix.exs` exits 0.
    - `test -f test/fixtures/hex_consumer/.scoria-hex-baseline.stamp` exits 0.
    - `rg -n 'scoria-0.1.0-unpack' test/fixtures/hex_consumer` returns matches.
    - Baseline fingerprint in stamp differs from current `Scoria.HexConsumerContract.package_fingerprint()` when HEAD has diverged from v0.1.0.
  </acceptance_criteria>
  <verify>
    <automated>test -f test/fixtures/hex_consumer/scoria-0.1.0-unpack/mix.exs && test -f test/fixtures/hex_consumer/.scoria-hex-baseline.stamp</automated>
  </verify>
  <done>Committed 0.1.0 unpack fixture and stamp exist on disk.</done>
</task>

<task type="auto">
  <name>Task 80-01-02: Add HexConsumerContract baseline APIs</name>
  <requirement_ids>HEX-UPGRADE-01</requirement_ids>
  <files>lib/scoria/hex_consumer_contract.ex</files>
  <read_first>
    lib/scoria/hex_consumer_contract.ex
    test/scoria/hex_consumer_contract_test.exs
    .planning/phases/80-upgrade-smoke-in-adoption-lane/80-CONTEXT.md
  </read_first>
  <action>
In `Scoria.HexConsumerContract` add:

1. Module attributes: `@baseline_fixture_rel "test/fixtures/hex_consumer/scoria-0.1.0-unpack"`, `@baseline_stamp_rel "test/fixtures/hex_consumer/.scoria-hex-baseline.stamp"`.

2. `baseline_unpack_root!/0` — resolution order: `SCORIA_HEX_BASELINE_UNPACK_ROOT` env (expand + validate mix.exs) else `Path.expand(@baseline_fixture_rel)` relative to `File.cwd!()`; return `unpack_root!/1` result.

3. `baseline_package_fingerprint/0` — compute fingerprint from fixture tree (mirror `package_fingerprint/0` logic but read version `0.1.0` and file list from fixture `mix.exs` / package files at fixture root).

4. `same_semver_content_upgrade?/0` — returns `baseline_package_fingerprint() != package_fingerprint()`.

5. `@doc` on each public function noting Phase 80 content-revision vs Phase 81 registry semver.

Do not change `ensure_current_unpack_root!/0` behavior.
  </action>
  <acceptance_criteria>
    - `rg -n 'def baseline_unpack_root!' lib/scoria/hex_consumer_contract.ex` returns match.
    - `rg -n 'def baseline_package_fingerprint' lib/scoria/hex_consumer_contract.ex` returns match.
    - `rg -n 'def same_semver_content_upgrade' lib/scoria/hex_consumer_contract.ex` returns match.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <done>Baseline contract APIs callable from upgrade proof.</done>
</task>

<task type="auto">
  <name>Task 80-01-03: Refresh task, contract tests, fixture drift guard</name>
  <requirement_ids>HEX-UPGRADE-01</requirement_ids>
  <files>lib/mix/tasks/scoria.hex_consumer.refresh_baseline_fixture.ex, test/scoria/hex_consumer_contract_test.exs, test/scoria/hex_consumer_baseline_fixture_test.exs</files>
  <read_first>
    lib/scoria/hex_consumer_contract.ex
    test/scoria/hex_consumer_contract_test.exs
    lib/scoria/hex_consumer_contract.ex
    .planning/phases/80-upgrade-smoke-in-adoption-lane/80-CONTEXT.md
  </read_first>
  <action>
1. Create `Mix.Tasks.Scoria.HexConsumer.RefreshBaselineFixture` with `@shortdoc` describing maintainer-only refresh from `v0.1.0` tag (checkout → hex.build --unpack → rewrite stamp). Reuse logic from 80-01-01.

2. Extend `hex_consumer_contract_test.exs`:
   - `baseline_unpack_root!/0` returns path containing `scoria-0.1.0-unpack`
   - `same_semver_content_upgrade?/0` is true when HEAD fingerprint differs from baseline
   - `baseline_upgrade_version/0` still `"0.1.0"`

3. Add `hex_consumer_baseline_fixture_test.exs`:
   - Read stamp; assert first line == `HexConsumerContract.baseline_package_fingerprint()`
   - Assert second line == `49f2d60018c4c79fbc09969116526c48454a8e84` (v0.1.0 SHA)
  </action>
  <acceptance_criteria>
    - `MIX_ENV=test mix test test/scoria/hex_consumer_contract_test.exs test/scoria/hex_consumer_baseline_fixture_test.exs` exits 0.
    - `mix help scoria.hex_consumer.refresh_baseline_fixture` shows shortdoc (or `mix help | rg refresh_baseline`).
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/hex_consumer_contract_test.exs test/scoria/hex_consumer_baseline_fixture_test.exs</automated>
  </verify>
  <done>Drift guard and refresh path documented in executable tests.</done>
</task>

</tasks>

<verification>
- `MIX_ENV=test mix test test/scoria/hex_consumer_contract_test.exs test/scoria/hex_consumer_baseline_fixture_test.exs`
- `MIX_ENV=test mix compile --warnings-as-errors`
</verification>
