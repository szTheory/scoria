---
phase: 80-upgrade-smoke-in-adoption-lane
plan: 02
type: execute
wave: 2
depends_on: [80-01]
files_modified:
  - test/support/scoria/host_app_proof/runner.ex
  - test/support/scoria/host_app_proof/generator.ex
autonomous: true
requirements: [HEX-UPGRADE-01]
must_haves:
  truths:
    - "Runner.run_upgrade_proof!/2 runs baseline then upgrade on same host — run_full_proof!/1 unchanged (D-57)."
    - "Runner.expected_upgrade_steps/1 returns exact baseline ++ upgrade step lists — no subset/in assertions (D-58)."
    - "Generator.bump_unpack_dep!/2 rewrites mix.exs path dep and updates host.unpack_root (D-59)."
    - "run_mix! supports expect_exit and expect_trailer for :scoria_install_check, :scoria_install_check_pre_apply, :scoria_install_dry_run (D-60)."
    - "MANIFEST.txt includes baseline_unpack, current_unpack, upgrade_phase on failure (D-67)."
    - "preserve replay hint mentions --only host_upgrade for upgrade failures (D-62)."
    - "Upgrade phase runs :ecto_migrate against preserved DB after bump — real migrate step even when no-op today (D-64)."
    - "No synthetic ordering-trap migration in package; no-op migrate alone is not claimed as criterion #4 proof (D-64, D-65)."
  artifacts:
    - path: "test/support/scoria/host_app_proof/runner.ex"
      provides: "run_upgrade_proof!/2 and expected_upgrade_steps/1"
      contains: "def run_upgrade_proof!"
    - path: "test/support/scoria/host_app_proof/generator.ex"
      provides: "bump_unpack_dep!/2"
      contains: "def bump_unpack_dep!"
---

<objective>
Extend host proof Runner and Generator with upgrade orchestration, installer trailer assertions, and enhanced failure MANIFEST.

Purpose: Encapsulate two-phase proof (baseline install → content-revision bump → operator chain → re-smoke) without polluting consumer `run_full_proof!/1`.
Output: Callable `Runner.run_upgrade_proof!(host, current_unpack_root: path)` ready for upgrade test module.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/80-upgrade-smoke-in-adoption-lane/80-CONTEXT.md
@.planning/phases/80-upgrade-smoke-in-adoption-lane/80-RESEARCH.md
@test/support/scoria/host_app_proof/runner.ex
@test/support/scoria/host_app_proof/generator.ex
@test/mix/tasks/scoria.install_check_test.exs
@lib/scoria/install/contract.ex
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-80-03 | Pre-apply check passes when drift expected | high | Pin exit 1 + drift trailer on :scoria_install_check_pre_apply |
| T-80-04 | deps cache serves old scoria after bump | medium | Optional `mix deps.clean scoria` in bump step |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 80-02-01: Add Generator.bump_unpack_dep!/2</name>
  <requirement_ids>HEX-UPGRADE-01</requirement_ids>
  <files>test/support/scoria/host_app_proof/generator.ex</files>
  <read_first>
    test/support/scoria/host_app_proof/generator.ex
    lib/scoria/hex_consumer_contract.ex
    .planning/phases/80-upgrade-smoke-in-adoption-lane/80-CONTEXT.md
  </read_first>
  <action>
Add public `bump_unpack_dep!(host, new_unpack_root)` to `Scoria.TestSupport.HostAppProof.Generator`:

1. Read `Path.join(host.root, "mix.exs")`.
2. Replace existing `{:scoria, path: ...}` line with `HexConsumerContract.tarball_dep_snippet(new_unpack_root)` form (same regex approach as `patch_mix_exs!/2` for `:hex_tarball`).
3. Update returned host map: `Map.put(host, :unpack_root, new_unpack_root)`.
4. Return updated host struct.

Do not run `mix deps.get` here — Runner owns that as `:deps_get` step after `:bump_dep`.
  </action>
  <acceptance_criteria>
    - `rg -n 'def bump_unpack_dep!' test/support/scoria/host_app_proof/generator.ex` returns match.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <done>Host mix.exs can be repointed to current unpack between proof phases.</done>
</task>

<task type="auto">
  <name>Task 80-02-02: Extend run_mix! with expect_exit and expect_trailer</name>
  <requirement_ids>HEX-UPGRADE-01</requirement_ids>
  <files>test/support/scoria/host_app_proof/runner.ex</files>
  <read_first>
    test/support/scoria/host_app_proof/runner.ex
    test/mix/tasks/scoria.install_check_test.exs
    lib/mix/tasks/scoria.install.ex
    .planning/phases/80-upgrade-smoke-in-adoption-lane/80-CONTEXT.md
  </read_first>
  <action>
Refactor `run_mix!/4` to accept optional keyword opts (5th param map or keyword list):

- `expect_exit: integer` — assert `status == expect_exit` else snapshot + raise (same triage path).
- `expect_trailer: string` — assert `output` contains exact substring (e.g. `"SCORIA_CHECK_RESULT status=drift exit_code=1"`).

Add step helpers:
- `scoria_install_check!(host, opts \\ [])` → `run_mix!(host, :scoria_install_check, ["scoria.install", "--check"], opts)`
- `scoria_install_check_pre_apply!(host)` → same with `expect_exit: 1`, `expect_trailer: "SCORIA_CHECK_RESULT status=drift exit_code=1"`
- `scoria_install_dry_run!(host)` → `run_mix!(host, :scoria_install_dry_run, ["scoria.install", "--dry-run"], expect_exit: 0)`

Keep existing `scoria_install!/1` using default exit 0 behavior.
  </action>
  <acceptance_criteria>
    - `rg -n 'expect_trailer' test/support/scoria/host_app_proof/runner.ex` returns match.
    - `rg -n 'scoria_install_check_pre_apply' test/support/scoria/host_app_proof/runner.ex` returns match.
    - `rg -n 'scoria_install_dry_run' test/support/scoria/host_app_proof/runner.ex` returns match.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <done>Installer upgrade chain steps enforce tri-state trailers.</done>
</task>

<task type="auto">
  <name>Task 80-02-03: Add run_upgrade_proof!/2 and expected_upgrade_steps/1</name>
  <requirement_ids>HEX-UPGRADE-01</requirement_ids>
  <files>test/support/scoria/host_app_proof/runner.ex</files>
  <read_first>
    test/support/scoria/host_app_proof/runner.ex
    test/support/scoria/host_app_proof/generator.ex
    .planning/phases/80-upgrade-smoke-in-adoption-lane/80-CONTEXT.md
    .planning/phases/80-upgrade-smoke-in-adoption-lane/80-RESEARCH.md
  </read_first>
  <action>
1. Add `expected_upgrade_steps(host)`:
   - `overlay_atoms` = sorted overlay basenames → atoms (same as `expected_steps/1`)
   - `baseline = [:deps_get, :scoria_install, :ecto_create, :ecto_migrate] ++ overlay_atoms ++ [:scoria_install_check]`
   - `upgrade = [:bump_dep, :deps_get, :scoria_install_dry_run, :scoria_install_check_pre_apply, :scoria_install, :ecto_migrate, :scoria_install_check] ++ overlay_atoms`
   - Return `baseline ++ upgrade` (flat list for single assertion) OR `%{baseline: ..., upgrade: ..., all: ...}` — prefer flat `all` key in return map `proof.steps` for test `assert proof.steps == Runner.expected_upgrade_steps(host)`.

2. Add `run_upgrade_proof!(host, opts)` with `current_unpack_root:` required:
   - **Baseline phase** (track `upgrade_phase: :baseline` in Process.put or local for manifest): deps_get, scoria_install, ecto_create, ecto_migrate, smoke_pair!, scoria_install_check (compliant trailer).
   - **Bump**: `Generator.bump_unpack_dep!(host, current_unpack_root)` then optional `mix deps.clean scoria` if deps_get sticky — implement as part of `:bump_dep` step calling `run_mix!(host, :bump_dep, ["deps.clean", "scoria"], expect_exit: 0)` only on non-zero first deps.get failure OR always run clean before deps_get after bump (D-59 discretion: run `deps.clean scoria` once after bump).
   - **Upgrade phase** (`upgrade_phase: :upgrade`): deps_get, scoria_install_dry_run, scoria_install_check_pre_apply, scoria_install, ecto_migrate, scoria_install_check (compliant), smoke_pair! again.
   - Return `%{results: ..., steps: ..., phases: %{baseline: ..., upgrade: ...}}` (phases optional per D-73 discretion).

3. Extend `manifest_content/4` with lines when `Map.get(host, :upgrade_phase)` set:
   - `baseline_unpack: ...` from initial host.unpack_root
   - `current_unpack: ...` after bump
   - `upgrade_phase: baseline|upgrade`

4. Update `preserve` replay line in triage to: `SCORIA_PRESERVE_HOST=1 MIX_ENV=test mix test --only host_upgrade --trace`

Do NOT modify `run_full_proof!/1` or `expected_steps/1`.
  </action>
  <acceptance_criteria>
    - `rg -n 'def run_upgrade_proof!' test/support/scoria/host_app_proof/runner.ex` returns match.
    - `rg -n 'def expected_upgrade_steps' test/support/scoria/host_app_proof/runner.ex` returns match.
    - `rg -n 'upgrade_phase' test/support/scoria/host_app_proof/runner.ex` returns match in manifest or triage.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <done>Upgrade proof orchestration ready for host_app_upgrade_proof_test.</done>
</task>

</tasks>

<verification>
- `MIX_ENV=test mix compile --warnings-as-errors`
</verification>
