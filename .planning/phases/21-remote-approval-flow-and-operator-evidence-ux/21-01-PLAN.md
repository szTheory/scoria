---
phase: 21-remote-approval-flow-and-operator-evidence-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/20260518000100_enrich_remote_approval_truth.exs
  - lib/scoria/observe/approval.ex
  - lib/scoria/workflows.ex
  - lib/scoria/workflows/remote_approval_projection.ex
  - lib/scoria/connectors/auth.ex
  - lib/scoria/connectors/invocation.ex
  - test/scoria/workflows_test.exs
  - test/scoria/connectors/invocation_test.exs
autonomous: true
requirements:
  - POLI-03
  - EVID-02
  - EVID-03
must_haves:
  truths:
    - "Remote write, exec, and scope-widening pauses are stored as workflow-owned approvals instead of UI-local state."
    - "Approval rows and workflow events carry enough connector, local-tool, blocker, and evidence linkage truth for inbox and notebook projections."
    - "Telemetry stays low-cardinality while exact approval, connector, run, and audit ids remain recoverable through durable evidence refs."
  artifacts:
    - path: "priv/repo/migrations/20260518000100_enrich_remote_approval_truth.exs"
      provides: "Durable remote-approval columns and indexes for connector/local-tool/blocker lineage"
    - path: "lib/scoria/workflows/remote_approval_projection.ex"
      provides: "Reusable inbox/notebook query seam over workflow-owned approval truth"
    - path: "lib/scoria/workflows.ex"
      provides: "Workflow-owned remote approval request and decision APIs with durable evidence linkage"
  key_links:
    - from: "lib/scoria/connectors/auth.ex"
      to: "lib/scoria/workflows.ex"
      via: "remote blocker -> workflow-owned approval request"
      pattern: "mark_waiting_for_approval|request_remote_approval"
    - from: "lib/scoria/connectors/invocation.ex"
      to: "lib/scoria/workflows.ex"
      via: "pre-execution approval pause for remote write/exec actions"
      pattern: "waiting_for_approval|request_remote_approval"
    - from: "lib/scoria/workflows.ex"
      to: "lib/scoria/observe/approval.ex"
      via: "approval changeset writes"
      pattern: "Approval\\.changeset"
    - from: "lib/scoria/workflows/remote_approval_projection.ex"
      to: "lib/scoria/sre/telemetry_identity.ex"
      via: "durable refs instead of metric labels"
      pattern: "approval_id|connector_id|local_tool_id|audit_outbox_event_id"
---

<objective>
Create the workflow-owned remote approval truth and query seams that all Phase 21 UX surfaces depend on.

Purpose: Implement the durable approval spine for D-01 through D-05, D-16 through D-21, D-25, and D-28 before any inbox, drawer, or notebook UI starts projecting it.
Output: Migration-backed approval lineage fields, workflow-owned request/decision APIs for remote blockers, and reusable projection queries for later inbox and evidence views.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/MILESTONE-ARC.md
@.planning/phases/21-remote-approval-flow-and-operator-evidence-ux/21-CONTEXT.md
@.planning/phases/21-remote-approval-flow-and-operator-evidence-ux/21-RESEARCH.md
@.planning/phases/21-remote-approval-flow-and-operator-evidence-ux/21-PATTERNS.md
@.planning/phases/20-policy-stable-tool-identity-and-stateless-invocation/20-CONTEXT.md
@.planning/phases/20-policy-stable-tool-identity-and-stateless-invocation/20-RESEARCH.md
@lib/scoria/workflows.ex
@lib/scoria/observe/approval.ex
@lib/scoria/connectors/auth.ex
@lib/scoria/connectors/invocation.ex
@lib/scoria/sre/telemetry_identity.ex
@test/scoria/workflows_test.exs
@test/scoria/connectors/invocation_test.exs

<interfaces>
From lib/scoria/workflows.ex:
```elixir
def get_run_tree!(id)
def get_approval!(id)
def record_connector_auth_failure(run_id, step_id, payload)
def record_connector_scope_escalation(run_id, step_id, payload)
```

From lib/scoria/observe/approval.ex:
```elixir
schema "ai_approvals" do
  field(:tool_name, :string)
  field(:arguments, :map, default: %{})
  field(:status, :string, default: "pending")
  field(:actor_id, :string)
  field(:tenant_id, :string)
  field(:session_id, :string)
  field(:run_id, :string)
  field(:workflow_run_id, :binary_id)
  field(:step_id, :binary_id)
  field(:checkpoint_id, :binary_id)
end
```

From lib/scoria/connectors/auth.ex:
```elixir
def record_remote_auth_failure(%Connector{} = connector, local_tool, context, reason_code, opts \\ [])
def record_scope_escalation(%Connector{} = connector, local_tool, context, missing_scopes, opts \\ [])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enrich workflow-owned remote approval truth for connector blockers</name>
  <files>priv/repo/migrations/20260518000100_enrich_remote_approval_truth.exs, lib/scoria/observe/approval.ex, lib/scoria/workflows.ex, lib/scoria/connectors/auth.ex, test/scoria/workflows_test.exs, test/scoria/connectors/invocation_test.exs</files>
  <behavior>
    - Test 1: a remote write, exec, auth-required, or scope-escalation blocker persists a workflow-owned approval row that includes connector/local-tool/blocker lineage and exact durable evidence refs.
    - Test 2: approval semantics remain narrow; approve/reject governs only whether the blocked workflow step may proceed, while remediation state is not encoded as approval status.
    - Test 3: high-risk remote write/exec actions pause before outbound execution and enter `waiting_for_approval` instead of bypassing the workflow-owned approval seam.
    - Test 4: connector auth and scope blocker flows preserve run/step/audit linkage without leaking exact ids into telemetry labels.
  </behavior>
  <action>Add the durable truth fields Phase 21 needs per D-03, D-16, D-17, D-18, D-19, D-20, D-21, D-23, D-25, and D-28. Create a migration and schema updates so `ai_approvals` can store connector-aware lineage such as blocker kind, connector id, local tool id, grant subject/scope summary, audit/workflow event refs, and replay eligibility hints while keeping approval status limited to approval outcomes. Extend `Scoria.Workflows` with a remote-approval request API or equivalent wrapper around `mark_waiting_for_approval/3` so both `Scoria.Connectors.Auth` and `Scoria.Connectors.Invocation` can attach workflow-owned approval truth to materially risky remote paths. That includes `:auth_required` and `:scope_escalation_required` blockers and a pre-execution approval seam for remote write/exec actions that should pause in `waiting_for_approval` before outbound execution instead of relying on ad hoc UI review. Do not add statuses like `reauthed`, `synced`, or `adopted`; typed remediation belongs to Plan 21-03, not the approval row.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/workflows_test.exs test/scoria/connectors/invocation_test.exs</automated>
  </verify>
  <done>Remote blockers now create workflow-owned approval truth with connector and evidence lineage, and approval status still only answers approve versus reject.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add reusable remote approval projection queries for inbox and evidence surfaces</name>
  <files>lib/scoria/workflows/remote_approval_projection.ex, lib/scoria/workflows.ex, test/scoria/workflows_test.exs</files>
  <behavior>
    - Test 1: callers can list pending remote approvals for an operator inbox without scraping raw audit JSON in LiveView.
    - Test 2: callers can load a single approval lineage projection with run, step, connector, local-tool, blocker, and audit refs for notebook/detail surfaces.
    - Test 3: projection rows separate low-cardinality telemetry fields from exact durable ids and links.
  </behavior>
  <action>Create a workflow-owned query/projection seam per D-01, D-02, D-03, D-11, D-12, D-15, D-26, D-27, and D-28, for example `Scoria.Workflows.RemoteApprovalProjection`. It should expose a pending-approvals inbox query and a per-approval lineage query that later LiveViews can call directly. Build these projections from approval rows, workflow events, and audit refs rather than socket-local assembly. Keep telemetry posture aligned to D-15 by projecting exact ids for links/evidence while leaving metrics on stable blocker/outcome categories only.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/workflows_test.exs</automated>
  </verify>
  <done>Phase 21 has reusable remote approval projections that later plans can render directly into an inbox, connector drawer links, and run-centric evidence notebook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| connector blocker outcome -> workflow approval truth | Untrusted remote blocker context must be normalized before it becomes durable approval state |
| workflow approval truth -> operator projections | Operator UX must read durable rows rather than invent socket-local approval state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | T | remote approval schema | mitigate | Store connector/local-tool/blocker lineage explicitly so LiveView does not reconstruct truth from mutable JSON blobs |
| T-21-02 | E | approval semantics | mitigate | Keep approval rows narrow per D-16 through D-20; remediation actions stay outside approval status |
| T-21-03 | I | telemetry/evidence linkage | mitigate | Keep exact ids in durable refs and links only, while metrics use stable outcome/blocker categories per D-15 |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/workflows_test.exs test/scoria/connectors/invocation_test.exs`
- If this shell is not configured for the repo’s expected PostgreSQL port `55432`, these targeted `mix test` lanes may fail before assertions run; correct the DB port first, then rerun the exact commands above.
</verification>

<success_criteria>
Workflow-owned remote approval truth exists before any Phase 21 UI work: blocked remote actions persist connector-aware approval lineage durably, projections can load inbox and notebook data without LiveView-owned truth, and telemetry keeps low-cardinality labels while durable refs preserve exact linkage.
</success_criteria>

<output>
After completion, create `.planning/phases/21-remote-approval-flow-and-operator-evidence-ux/21-remote-approval-flow-and-operator-evidence-ux-01-SUMMARY.md`
</output>
