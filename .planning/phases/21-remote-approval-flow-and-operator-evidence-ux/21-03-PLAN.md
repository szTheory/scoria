---
phase: 21-remote-approval-flow-and-operator-evidence-ux
plan: 03
type: execute
wave: 3
depends_on:
  - 21-01
  - 21-02
files_modified:
  - lib/scoria/connectors/evidence_projection.ex
  - lib/scoria/connectors/remediation.ex
  - lib/scoria/connectors.ex
  - lib/scoria/sre.ex
  - test/scoria/connectors/invocation_test.exs
  - test/scoria/workflows_test.exs
autonomous: true
requirements:
  - POLI-03
  - EVID-02
must_haves:
  truths:
    - "Typed remediation actions exist around approvals without overloading approval status, and replay remains an explicit workflow-owned step."
    - "Backend evidence projection returns the durable remote invocation lineage required by the later notebook UI."
  artifacts:
    - path: "lib/scoria/connectors/remediation.ex"
      provides: "Typed connector remediation actions kept separate from approval state"
    - path: "lib/scoria/connectors/evidence_projection.ex"
      provides: "Run-centric remote invocation evidence projection joining workflow, connector, grant, and audit lineage"
  key_links:
    - from: "lib/scoria/connectors/remediation.ex"
      to: "lib/scoria/workflows.ex"
      via: "explicit replay/resume after remediation"
      pattern: "resume_run|replay"
---

<objective>
Add the backend remediation and evidence seams that the final Phase 21 notebook UX depends on.

Purpose: Implement D-17 through D-21 and the backend portion of D-11 through D-14 so replay/remediation and remote evidence joins exist before the notebook UI layer lands.
Output: Explicit non-approval remediation APIs and a run-centric evidence projection seam over durable workflow, connector, grant, and audit truth.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-remote-approval-flow-and-operator-evidence-ux/21-CONTEXT.md
@.planning/phases/21-remote-approval-flow-and-operator-evidence-ux/21-RESEARCH.md
@.planning/phases/21-remote-approval-flow-and-operator-evidence-ux/21-PATTERNS.md
@.planning/phases/21-remote-approval-flow-and-operator-evidence-ux/21-01-PLAN.md
@lib/scoria/connectors/invocation.ex
@lib/scoria/connectors/auth.ex
@lib/scoria/connectors.ex
@test/scoria/connectors/invocation_test.exs
@test/scoria/workflows_test.exs

<interfaces>
From lib/scoria/connectors/invocation.ex:
```elixir
def invoke(tool_module, local_tool_or_id, args, context, timeout \\ 5000)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed remediation actions and backend remote evidence projections</name>
  <files>lib/scoria/connectors/evidence_projection.ex, lib/scoria/connectors/remediation.ex, lib/scoria/connectors.ex, lib/scoria/sre.ex, test/scoria/connectors/invocation_test.exs, test/scoria/workflows_test.exs</files>
  <behavior>
    - Test 1: remediation actions are typed and eligibility-driven from durable blocker outcomes, separate from approval status.
    - Test 2: workflow approval decisions remain `approve` and `reject`, while the separate remediation action set includes `re-auth`, `sync/refresh`, `request scope escalation`, `adopt pending tool`, and `replay blocked step`, with explicit replay after successful remediation.
    - Test 3: evidence projection returns the required run-centric lineage data and exact durable refs for connector, grant, approval, run, step, and audit rows.
  </behavior>
  <action>Create non-approval remediation APIs per D-17 through D-21. Add a connector remediation module that models typed actions and eligibility from durable blocker states exactly as specified in D-18 and D-19, and keep `approve`/`reject` separate from connector maintenance actions per D-20. In parallel, add a run-centric evidence projection module that joins workflow events, approvals, connector/grant/local-tool state, and redacted audit rows in the reading order mandated by D-11 through D-14. Reuse workflow-owned resume/replay seams so recovery creates fresh durable evidence first and only then offers explicit replay per D-21.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/connectors/invocation_test.exs test/scoria/workflows_test.exs</automated>
  </verify>
  <done>Typed remediation and backend evidence projections exist as durable seams, with approval semantics still narrow and replay remaining explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| blocked invocation evidence -> remediation engine | Evidence-driven actions must stay typed and eligibility-checked instead of becoming arbitrary commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07 | E | remediation actions | mitigate | Gate actions by durable blocker outcome per D-18 and D-19; keep approval status separate from maintenance actions |
| T-21-08 | I | evidence projection | mitigate | Keep redacted request/response summaries and exact ids in durable evidence projections so later UI layers do not rehydrate secrets or invent lineage |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/connectors/invocation_test.exs test/scoria/workflows_test.exs`
- These targeted lanes may require the repo’s expected PostgreSQL port `55432`; if the shell points elsewhere, fix the DB config first so the verification proves plan intent instead of environment drift.
</verification>

<success_criteria>
Phase 21 has typed remediation around approvals plus backend remote evidence projections, leaving the final notebook UI and telemetry-link verification to a narrower follow-on plan.
</success_criteria>

<output>
After completion, create `.planning/phases/21-remote-approval-flow-and-operator-evidence-ux/21-remote-approval-flow-and-operator-evidence-ux-03-SUMMARY.md`
</output>
