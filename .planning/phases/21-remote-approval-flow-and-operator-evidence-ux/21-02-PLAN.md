---
phase: 21-remote-approval-flow-and-operator-evidence-ux
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - lib/scoria/connectors/operator_projection.ex
  - lib/scoria/connectors.ex
  - lib/scoria_web/live/orchestrator_live.ex
  - lib/scoria_web/components/workflow_detail_panel_component.ex
  - lib/scoria_web/components/approval_inbox_component.ex
  - lib/scoria_web/components/connector_detail_drawer_component.ex
  - test/scoria_web/live/orchestrator_live_test.exs
  - test/scoria_web/live/orchestrator_live_sre_test.exs
autonomous: true
requirements:
  - POLI-03
  - EVID-01
must_haves:
  truths:
    - "Operators have a first-class approvals inbox inside the embedded dashboard for pending remote write, exec, and scope-widening reviews."
    - "Operators can scan connector fleet state, grant posture, last refresh status, last good refresh, and pending local-tool adoption from one compact dashboard surface."
    - "Connector detail drilldown links back to exact approvals, runs, and evidence records without becoming a hosted control plane."
  artifacts:
    - path: "lib/scoria/connectors/operator_projection.ex"
      provides: "Fleet table and connector-detail drawer projections over durable connector/grant/snapshot/local-tool truth"
    - path: "lib/scoria_web/components/approval_inbox_component.ex"
      provides: "Canonical approvals inbox renderer for pending remote approvals"
    - path: "lib/scoria_web/components/connector_detail_drawer_component.ex"
      provides: "Scan-first connector drawer with health, scope, refresh, and linked evidence strips"
    - path: "lib/scoria_web/live/orchestrator_live.ex"
      provides: "Embedded dashboard integration point for inbox and fleet surfaces"
  key_links:
    - from: "lib/scoria_web/live/orchestrator_live.ex"
      to: "lib/scoria/workflows/remote_approval_projection.ex"
      via: "approvals inbox async loads"
      pattern: "RemoteApprovalProjection|approval_inbox"
    - from: "lib/scoria_web/live/orchestrator_live.ex"
      to: "lib/scoria/connectors/operator_projection.ex"
      via: "connector fleet and drawer loads"
      pattern: "OperatorProjection|connector_fleet"
    - from: "lib/scoria_web/components/connector_detail_drawer_component.ex"
      to: "lib/scoria_web/live/workflow_live/show.ex"
      via: "exact run/evidence links"
      pattern: "run_id|approval_id|connector_id"
---

<objective>
Ship the operator triage surface for Phase 21: approvals inbox plus scan-first connector fleet and detail drawer UX.

Purpose: Implement D-01 through D-10, D-22 through D-24, and D-26 through D-28 in the embedded dashboard using the workflow-owned seams from Plan 21-01.
Output: Dashboard projections and LiveView components for approvals inbox, compact connector fleet status, and evidence-rich connector drawer links.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-remote-approval-flow-and-operator-evidence-ux/21-CONTEXT.md
@.planning/phases/21-remote-approval-flow-and-operator-evidence-ux/21-RESEARCH.md
@.planning/phases/21-remote-approval-flow-and-operator-evidence-ux/21-PATTERNS.md
@.planning/phases/21-remote-approval-flow-and-operator-evidence-ux/21-01-PLAN.md
@lib/scoria/connectors.ex
@lib/scoria/connectors/discovery.ex
@lib/scoria/connectors/connector.ex
@lib/scoria/connectors/grant.ex
@lib/scoria/connectors/capability_snapshot.ex
@lib/scoria/connectors/local_tool.ex
@lib/scoria_web/live/orchestrator_live.ex
@lib/scoria_web/components/workflow_detail_panel_component.ex
@test/scoria_web/live/orchestrator_live_test.exs
@test/scoria_web/live/orchestrator_live_sre_test.exs

<interfaces>
From lib/scoria/connectors.ex:
```elixir
def list_connectors(filters \\ %{})
def get_connector!(id)
def list_local_tools(filters \\ %{})
def sync_connector(connector_or_id, opts \\ [])
```

From lib/scoria_web/live/orchestrator_live.ex:
```elixir
def handle_event("load_incident_evidence", params, socket)
def handle_event("approve", _, socket)
def handle_event("reject", _, socket)
```

From lib/scoria/connectors/connector.ex:
```elixir
schema "ai_connectors" do
  field(:health_state, :string, default: "unknown")
  field(:last_refresh_status, :string, default: "pending")
  field(:last_good_refresh_at, :utc_datetime_usec)
  has_many(:grants, Scoria.Connectors.Grant)
  has_one(:capability_snapshot, Scoria.Connectors.CapabilitySnapshot)
  has_many(:local_tools, Scoria.Connectors.LocalTool)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add approvals inbox and connector fleet projection modules</name>
  <files>lib/scoria/connectors/operator_projection.ex, lib/scoria/connectors.ex, test/scoria_web/live/orchestrator_live_sre_test.exs</files>
  <behavior>
    - Test 1: the operator surface can load a pending remote approvals inbox sorted and grouped for triage instead of relying on the inline modal as the primary home.
    - Test 2: the connector fleet query returns health, auth/grant posture, granted scopes, last refresh status, last good refresh, and pending local-tool adoption summaries in one compact row per connector.
    - Test 3: the drawer projection exposes auth provenance, refresh summary, scope challenges, pending approvals, and exact run/evidence links for one connector.
  </behavior>
  <action>Create durable read models for D-01 through D-10, D-22, D-23, D-26, D-27, and D-28. Add a connector operator projection module, for example `Scoria.Connectors.OperatorProjection`, that composes connector, grant, capability snapshot, local-tool, and remote-approval projection truth into two read paths: a compact fleet table and a per-connector detail drawer strip. Keep the top-level IA scan-first per D-06 and D-07, and present latest durable snapshot plus refresh history/evidence per D-10 rather than a faux live remote feed. Per D-08, the drawer projection must explicitly include auth provenance, refresh history summary, scope challenges, pending approvals, and exact run/evidence links for the selected connector. Expose only the explicit read APIs the LiveView needs; do not move connector mutation truth into socket assigns.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_sre_test.exs</automated>
  </verify>
  <done>The repo exposes reusable fleet and drawer projections that reflect durable connector truth and approval linkage without hosted-control-plane drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the embedded approvals inbox and connector drawer in OrchestratorLive</name>
  <files>lib/scoria_web/live/orchestrator_live.ex, lib/scoria_web/components/workflow_detail_panel_component.ex, lib/scoria_web/components/approval_inbox_component.ex, lib/scoria_web/components/connector_detail_drawer_component.ex, test/scoria_web/live/orchestrator_live_test.exs, test/scoria_web/live/orchestrator_live_sre_test.exs</files>
  <behavior>
    - Test 1: the dashboard shows a first-class approvals inbox for pending remote write, exec, and scope-widening decisions.
    - Test 2: the inline modal remains an interrupt affordance, but the canonical review surface is the inbox and linked drawer state.
    - Test 3: operators can open a connector drawer from the fleet row and see health, scope, refresh, pending adoption, and cross-links into exact approvals/runs/evidence records.
  </behavior>
  <action>Implement the embedded dashboard UX per D-01 through D-09, D-22 through D-24, and D-26 through D-28. Extend `OrchestratorLive` with async loads and events for the approvals inbox and connector fleet, render them through new components, and keep the current modal as a local interrupt affordance only per D-04. Reuse calm, dense operator patterns from the existing incident/evidence surface and `WorkflowDetailPanelComponent`, but keep the product center of gravity embedded and Phoenix-first per D-09. Default ordering, badge taxonomy, stale-vs-last-good heuristics, and grouping should be shifted left into Scoria per D-22 instead of turned into operator configuration.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_test.exs test/scoria_web/live/orchestrator_live_sre_test.exs</automated>
  </verify>
  <done>Operators can triage remote approvals and inspect connector fleet posture from one obvious embedded dashboard path, with exact links into durable evidence records.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable connector/grant/approval rows -> operator dashboard | Embedded UI must project durable truth without implying hosted remote control behavior |
| operator click actions -> connector/workflow APIs | Dashboard actions must remain explicit calls into context APIs, not hidden state mutation in LiveView |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | I | approvals inbox | mitigate | Make the inbox read from workflow-owned approval projections so pending decisions cannot be lost in transient modal state |
| T-21-05 | S | connector fleet drawer | mitigate | Present latest durable snapshot and refresh history only, avoiding a fake live-control-plane view of remote state |
| T-21-06 | T | dashboard action wiring | mitigate | Route all approval and connector actions through explicit context APIs with durable evidence, not socket-local mutation |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_test.exs test/scoria_web/live/orchestrator_live_sre_test.exs`
- If the current shell is not pointed at PostgreSQL port `55432` as expected by this repo, these LiveView test lanes may fail on boot/setup before the UI assertions run.
</verification>

<success_criteria>
The embedded Scoria dashboard now has the intended Phase 21 operator workflow: approvals inbox as the risk surface, connector fleet as the state surface, and exact cross-links into durable run and evidence records.
</success_criteria>

<output>
After completion, create `.planning/phases/21-remote-approval-flow-and-operator-evidence-ux/21-remote-approval-flow-and-operator-evidence-ux-02-SUMMARY.md`
</output>
