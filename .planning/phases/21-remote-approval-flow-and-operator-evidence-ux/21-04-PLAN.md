---
phase: 21-remote-approval-flow-and-operator-evidence-ux
plan: 04
type: execute
wave: 4
depends_on:
  - 21-03
files_modified:
  - lib/scoria/sre/telemetry_identity.ex
  - lib/scoria_web/live/workflow_live/show.ex
  - lib/scoria_web/components/incident_evidence_component.ex
  - lib/scoria_web/components/remote_invocation_evidence_component.ex
  - test/scoria_web/live/orchestrator_live_sre_test.exs
  - test/scoria/sre/telemetry_test.exs
autonomous: true
requirements:
  - EVID-02
  - EVID-03
must_haves:
  truths:
    - "Operators can inspect one remote invocation in a run-centric evidence notebook that follows identity -> policy -> approval -> connector -> payload -> replay order."
    - "Telemetry remains low-cardinality while notebook links recover exact connector, grant, approval, run, step, and audit evidence rows."
  artifacts:
    - path: "lib/scoria_web/components/remote_invocation_evidence_component.ex"
      provides: "Lineage-first notebook renderer for remote invocation evidence"
    - path: "lib/scoria_web/live/workflow_live/show.ex"
      provides: "Workflow run-page integration for remote evidence loading"
    - path: "lib/scoria/sre/telemetry_identity.ex"
      provides: "Low-cardinality telemetry contract with durable refs kept out of labels"
  key_links:
    - from: "lib/scoria_web/live/workflow_live/show.ex"
      to: "lib/scoria/connectors/evidence_projection.ex"
      via: "run notebook load"
      pattern: "EvidenceProjection|remote_invocation_evidence"
    - from: "lib/scoria/sre.ex"
      to: "lib/scoria/sre/telemetry_identity.ex"
      via: "labels vs refs split"
      pattern: "TelemetryIdentity\\.(labels|refs)"
---

<objective>
Finish Phase 21 with the run-centric evidence notebook and telemetry/evidence link verification.

Purpose: Implement the UI portion of D-11 through D-15 and D-26 through D-28 while preserving the embedded Phoenix product boundary and low-cardinality telemetry posture.
Output: A run-centric remote evidence notebook and verification that telemetry labels stay low-cardinality while exact ids live in durable evidence links.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-remote-approval-flow-and-operator-evidence-ux/21-CONTEXT.md
@.planning/phases/21-remote-approval-flow-and-operator-evidence-ux/21-RESEARCH.md
@.planning/phases/21-remote-approval-flow-and-operator-evidence-ux/21-PATTERNS.md
@.planning/phases/21-remote-approval-flow-and-operator-evidence-ux/21-02-PLAN.md
@.planning/phases/21-remote-approval-flow-and-operator-evidence-ux/21-03-PLAN.md
@lib/scoria/connectors/evidence_projection.ex
@lib/scoria/sre.ex
@lib/scoria/sre/telemetry_identity.ex
@lib/scoria_web/live/workflow_live/show.ex
@lib/scoria_web/components/incident_evidence_component.ex
@test/scoria_web/live/orchestrator_live_sre_test.exs
@test/scoria/sre/telemetry_test.exs

<interfaces>
From lib/scoria/sre/telemetry_identity.ex:
```elixir
def labels(attrs)
def refs(attrs)
def runtime_metadata(attrs)
```

From lib/scoria_web/live/workflow_live/show.ex:
```elixir
def mount(%{"id" => run_id}, _session, socket)
def handle_info({:workflow_updated, run_id}, socket)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the remote invocation evidence notebook and verify low-cardinality telemetry links</name>
  <files>lib/scoria/sre/telemetry_identity.ex, lib/scoria_web/live/workflow_live/show.ex, lib/scoria_web/components/incident_evidence_component.ex, lib/scoria_web/components/remote_invocation_evidence_component.ex, test/scoria_web/live/orchestrator_live_sre_test.exs, test/scoria/sre/telemetry_test.exs</files>
  <behavior>
    - Test 1: the workflow run page renders a lineage-first remote evidence notebook with secondary payload/policy panels rather than a blob-first layout.
    - Test 2: connector posture appears as side context, not the primary frame, and notebook links jump to exact durable records.
    - Test 3: telemetry labels remain low-cardinality and exclude exact connector/approval/invocation ids, while durable refs and notebook links expose those exact ids for operators.
  </behavior>
  <action>Implement the run-centric evidence UI per D-11 through D-15, D-26, D-27, and D-28. Extend `WorkflowLive.Show` with async loading for remote invocation evidence and render it via a new component that follows the required reading order: identity and run context, policy outcome, approval lineage, connector/grant/scope context, redacted request/response summaries, then replay/follow-on outcome. Per D-13, payload and policy summaries must render as secondary panels inside the notebook rather than the top-level IA. Reuse layout shell patterns from the existing workflow detail and incident evidence surfaces; Plan 21-02 may provide additional component styling patterns, but this plan depends only on the data seams already delivered by `21-03`. Update telemetry identity wiring so stable categories remain in labels and exact ids stay confined to refs/evidence links per D-15.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_sre_test.exs test/scoria/sre/telemetry_test.exs</automated>
  </verify>
  <done>The workflow run surface now includes a lineage-first remote evidence notebook, and telemetry/evidence verification proves low-cardinality labels with exact durable links preserved for operators.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| telemetry labels -> operator evidence links | Exact ids must not leak into metric labels even though operators still need precise durable links |
| durable evidence projection -> notebook UI | The notebook must preserve lineage and redaction without rehydrating blob-first or secret-bearing detail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08 | I | evidence notebook | mitigate | Render redacted request/response summaries as secondary panels and preserve exact ids only in durable links |
| T-21-09 | T | telemetry identity | mitigate | Verify `TelemetryIdentity` keeps labels low-cardinality and exact connector/approval/invocation ids in refs only |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_sre_test.exs test/scoria/sre/telemetry_test.exs`
- These targeted lanes may require the repo’s expected PostgreSQL port `55432`; if the shell points elsewhere, fix the DB config first so the verification proves plan intent instead of environment drift.
</verification>

<success_criteria>
Phase 21 finishes with a run-centric remote evidence notebook and a verified telemetry contract that stays low-cardinality while preserving exact durable evidence links for operators.
</success_criteria>

<output>
After completion, create `.planning/phases/21-remote-approval-flow-and-operator-evidence-ux/21-remote-approval-flow-and-operator-evidence-ux-04-SUMMARY.md`
</output>
