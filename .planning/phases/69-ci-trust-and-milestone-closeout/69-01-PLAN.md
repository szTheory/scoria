---
phase: 69-ci-trust-and-milestone-closeout
plan: 01
type: execute
wave: 1
depends_on: [69-00]
files_modified:
  - lib/mix/tasks/scoria.warning_ratchet.test.ex
  - test/scoria/warning_inventory/tmp_preflight_test.exs
autonomous: true
requirements: [CI-03]
must_haves:
  truths:
    - "warning_ratchet.test enforces empty test/tmp before run and cleans after (WR-01 symmetry)."
    - "Ratchet→inventory integration test exercises real capture via subprocess (WR-02)."
  artifacts:
    - path: "lib/mix/tasks/scoria.warning_ratchet.test.ex"
      provides: "symmetric tmp hygiene with warning_ratchet.check"
      contains: "ensure_clean_tmp!"
    - path: "test/scoria/warning_inventory/tmp_preflight_test.exs"
      provides: "subprocess ratchet check integration"
      contains: "scoria.warning_ratchet.check"
  key_links:
    - from: "lib/mix/tasks/scoria.warning_ratchet.test.ex"
      to: "lib/scoria/warning_inventory.ex"
      via: "ensure_clean_tmp!/0 and cleanup_transient_tmp!/0"
      pattern: "ensure_clean_tmp!"
---

<objective>
Remediate 68-REVIEW maintainer hygiene warnings WR-01 (ratchet.test tmp asymmetry) and WR-02 (neutered ratchet→inventory integration test).

Purpose: Preserve maintainer trust chain documented in Phase 69 CI gate map — ratchet commands must not block follow-on inventory.
Output: Symmetric tmp handling in `warning_ratchet.test` and subprocess-based integration test.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/69-ci-trust-and-milestone-closeout/69-CONTEXT.md
@.planning/phases/69-ci-trust-and-milestone-closeout/69-RESEARCH.md
@.planning/phases/68-full-suite-warning-closure/68-REVIEW.md
@lib/mix/tasks/scoria.warning_ratchet.test.ex
@lib/mix/tasks/scoria.warning_ratchet.check.ex
@lib/scoria/warning_inventory.ex
@test/scoria/warning_inventory/tmp_preflight_test.exs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Tmp pollution after ratchet.test breaks inventory | medium | ensure_clean_tmp! + after cleanup |
| Subprocess test hangs in CI | low | timeout tag; env MIX_ENV=test only |
| Subprocess picks wrong mix env | medium | Explicit env in System.cmd |
| Over-aggressive tmp cleanup deletes fixtures | high | Reuse cleanup_transient_tmp! (entries only) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 69-01-01: Symmetric tmp hygiene in warning_ratchet.test (WR-01)</name>
  <requirement_ids>CI-03</requirement_ids>
  <files>lib/mix/tasks/scoria.warning_ratchet.test.ex</files>
  <read_first>
    lib/mix/tasks/scoria.warning_ratchet.test.ex
    lib/mix/tasks/scoria.warning_ratchet.check.ex
    lib/scoria/warning_inventory.ex
    .planning/phases/68-full-suite-warning-closure/68-REVIEW.md
  </read_first>
  <action>
Refactor `Mix.Tasks.Scoria.WarningRatchet.Test.run/1` to mirror `WarningRatchet.Check`:

1. After `MIX_ENV=test` guard, call `WarningInventory.ensure_clean_tmp!()` (not only cleanup before).
2. Wrap the existing test invocation (`Mix.Task.run("test", test_args)`) in `try/after` with `WarningInventory.cleanup_transient_tmp!()` in the `after` block.
3. Remove the standalone pre-run-only `cleanup_transient_tmp!()` unless needed before ensure — final shape should match check: ensure → work → cleanup.

Do not change high_signal path list or `--warnings-as-errors` behavior.
  </action>
  <verify>
    <automated>rg "ensure_clean_tmp!" lib/mix/tasks/scoria.warning_ratchet.test.ex && rg "cleanup_transient_tmp!" lib/mix/tasks/scoria.warning_ratchet.test.ex</automated>
  </verify>
  <acceptance_criteria>
    - `lib/mix/tasks/scoria.warning_ratchet.test.ex` calls `WarningInventory.ensure_clean_tmp!()`
    - Same file calls `WarningInventory.cleanup_transient_tmp!()` inside `after`
    - `rg -c "cleanup_transient_tmp!" lib/mix/tasks/scoria.warning_ratchet.test.ex` returns at least 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 69-01-02: Subprocess ratchet→inventory integration test (WR-02)</name>
  <requirement_ids>CI-03</requirement_ids>
  <files>test/scoria/warning_inventory/tmp_preflight_test.exs</files>
  <read_first>
    test/scoria/warning_inventory/tmp_preflight_test.exs
    lib/mix/tasks/scoria.warning_ratchet.check.ex
    lib/scoria/warning_inventory.ex
    .planning/phases/68-full-suite-warning-closure/68-REVIEW.md
  </read_first>
  <action>
Replace the in-process test `"ratchet check cleans test/tmp so inventory preflight passes afterward"` with a subprocess-based flow:

```elixir
@tag timeout: :infinity
test "ratchet check subprocess cleans test/tmp so inventory preflight passes afterward" do
  WarningInventory.cleanup_transient_tmp!()

  {output, exit_status} =
    System.cmd("mix", ["scoria.warning_ratchet.check"],
      env: [{"MIX_ENV", "test"}],
      cd: File.cwd!(),
      stderr_to_stdout: true
    )

  assert exit_status in [0],
         "ratchet check failed: #{output}"

  assert File.ls(@tmp_dir) in [{:ok, []}, {:error, :enoent}]

  Mix.Tasks.Scoria.WarningInventory.run(["--quiet", "--format", "table"])
end
```

If exit code uses `{:shutdown, 0}` from check task, accept `exit_status == 0` only (subprocess normalizes).

Keep existing unit tests for `ensure_clean_tmp!/0` unchanged.

Tag module `async: false` (already set).
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/warning_inventory/tmp_preflight_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `tmp_preflight_test.exs` uses `System.cmd("mix", ["scoria.warning_ratchet.check"]`
    - Test no longer calls `Mix.Tasks.Scoria.WarningRatchet.Check.run/1` directly in the integration test
    - `MIX_ENV=test mix test test/scoria/warning_inventory/tmp_preflight_test.exs` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `MIX_ENV=test mix test test/scoria/warning_inventory/tmp_preflight_test.exs` — exit 0
2. Manual: run `mix scoria.warning_ratchet.test` then `mix scoria.warning_inventory --quiet` without manual `rm -rf test/tmp/*`
</verification>

<success_criteria>
- WR-01 and WR-02 from 68-REVIEW remediated
- Maintainer ratchet→inventory chain matches operator doc claims
</success_criteria>
