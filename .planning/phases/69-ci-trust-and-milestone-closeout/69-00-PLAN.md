---
phase: 69-ci-trust-and-milestone-closeout
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - docs/operator_verification.md
  - .github/workflows/ci.yml
  - README.md
  - test/scoria/ci_policy_contract_test.exs
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [CI-03]
must_haves:
  truths:
    - "Maintainers have a single operator doc section explaining policy→test CI topology (D-01, D-02)."
    - "ci.yml carries minimal intent comments without changing step order (D-03)."
    - "README links to operator CI gate map in at most two lines (D-04)."
    - "Contract test anchors operator doc CI gate map section (D-06)."
    - "CI-03 requirement prose matches shipped CI; no staged ratchet language (D-08, D-09)."
  artifacts:
    - path: "docs/operator_verification.md"
      provides: "CI gate map for maintainers"
      contains: "CI gate map"
    - path: "test/scoria/ci_policy_contract_test.exs"
      provides: "operator doc anchor contract"
      contains: "CI gate map"
  key_links:
    - from: "docs/operator_verification.md"
      to: ".github/workflows/ci.yml"
      via: "describes same job order as contract tests enforce"
      pattern: "policy"
---

<objective>
Ship the CI-03 documentation bundle: operator CI gate map, minimal ci.yml comments, README link, contract-test doc anchor, and REQUIREMENTS/PROJECT/ROADMAP prose sync — without changing CI step order or adding gates.

Purpose: Close the trust gap between executable CI (Phase 68) and contributor-facing docs (Phase 69).
Output: Updated operator docs, commented ci.yml, README pointer, extended contract test, aligned planning prose.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/69-ci-trust-and-milestone-closeout/69-CONTEXT.md
@.planning/phases/69-ci-trust-and-milestone-closeout/69-RESEARCH.md
@.planning/phases/69-ci-trust-and-milestone-closeout/69-VALIDATION.md
@.github/workflows/ci.yml
@test/scoria/ci_policy_contract_test.exs
@lib/scoria/verification_lanes.ex
@docs/operator_verification.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Doc claims wrong CI order trains maintainers to debug wrong step | high | Contract test asserts operator anchor; gate-order tests unchanged |
| Accidental ci.yml step reorder breaks WARN-07 | high | Comments only; run ci_policy_contract_test after edit |
| Duplicate docs/ci.md drifts from SSOT | medium | Reject new file per D-05 |
| README becomes gate encyclopedia | low | Max two lines per D-04 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 69-00-01: Add CI gate map section to operator_verification.md</name>
  <requirement_ids>CI-03</requirement_ids>
  <files>docs/operator_verification.md</files>
  <read_first>
    docs/operator_verification.md
    .github/workflows/ci.yml
    .planning/phases/69-ci-trust-and-milestone-closeout/69-CONTEXT.md
    lib/scoria/verification_lanes.ex
  </read_first>
  <action>
Insert a new section **`### CI gate map (maintainers)`** after the existing `### WARN-07 CI warning gates (full suite)` block (before any following top-level `##` if present). Content MUST include:

1. **Two-job overview:** `policy` job (no Postgres) runs first; `test` job (`needs: policy`, pgvector Postgres on port 55432) runs canonical closeout.
2. **Policy job steps (in order):** `mix scoria.warning_baseline.check` → `mix compile --warnings-as-errors` → lane-contract WAE (`mix test --warnings-as-errors test/scoria/verification_lanes_test.exs test/scoria/adoption_surface_test.exs`).
3. **Test job closeout order:** `MIX_ENV=dev mix scoria.release_preview` → `mix ecto.create` + `mix ecto.migrate` → `mix test.adoption` → `mix test.runtime_to_handoff` → `mix test --warnings-as-errors` → `mix test.knowledge`.
4. **Local parity:** `SCORIA_DB_PORT=55432`; `MIX_ENV=dev` for release preview only.
5. **Ratchet maintainer-only:** `mix scoria.warning_ratchet.test/check` are WARN-06 debugger commands — **not** CI steps after Phase 68.
6. **Failure diagnosis (3–5 bullets):** map failing step to next maintainer command (baseline.check, compile WAE, lane-contract, adoption lane, full WAE) — Field Engineer tone per D-29.

Do NOT duplicate full command lists that contradict `Scoria.VerificationLanes` or ci.yml.
  </action>
  <verify>
    <automated>rg -n "CI gate map \\(maintainers\\)" docs/operator_verification.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/operator_verification.md` contains heading `### CI gate map (maintainers)`
    - Section mentions both `policy` and `test` jobs
    - Section states ratchet is maintainer-only (not CI)
    - Section includes `SCORIA_DB_PORT=55432`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 69-00-02: Add minimal comments to ci.yml</name>
  <requirement_ids>CI-03</requirement_ids>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .github/workflows/ci.yml
    test/scoria/ci_policy_contract_test.exs
  </read_first>
  <action>
Add a workflow header comment block (5–8 lines) immediately after `permissions:` explaining:
- Two-job topology: policy (fail cheap, no DB) → test (canonical closeout + full WAE)
- SSOT: `Scoria.VerificationLanes` + `ci_policy_contract_test.exs`
- Maintainer narrative: `docs/operator_verification.md` CI gate map

Add one comment line above each job definition:
- `# policy: baseline expiry + compile WAE + lane-contract tests (no Postgres)`
- `# test: release_preview → closeout lanes → full-suite WAE → knowledge lane`

Do NOT reorder, rename, or add/remove steps.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains substring `policy: baseline expiry`
    - `.github/workflows/ci.yml` contains substring `test: release_preview`
    - `MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 69-00-03: README link to operator CI gate map</name>
  <requirement_ids>CI-03</requirement_ids>
  <files>README.md</files>
  <read_first>
    README.md
    docs/operator_verification.md
  </read_first>
  <action>
Near the existing CI badge (lines 3–4), add at most **two lines** linking maintainers to the CI gate map, e.g.:

`Maintainer CI topology: see [operator verification — CI gate map](docs/operator_verification.md#ci-gate-map-maintainers).`

Use the actual GitHub-compatible anchor for the `### CI gate map (maintainers)` heading. Do not add a gate catalog to README.
  </action>
  <verify>
    <automated>rg "operator_verification" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `operator_verification`
    - `README.md` adds no more than 2 new lines about CI gates (excluding existing badge line)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 69-00-04: Contract test anchor for operator CI gate map</name>
  <requirement_ids>CI-03</requirement_ids>
  <files>test/scoria/ci_policy_contract_test.exs</files>
  <read_first>
    test/scoria/ci_policy_contract_test.exs
    docs/operator_verification.md
  </read_first>
  <action>
Add a test to `Scoria.CiPolicyContractTest`:

```elixir
test "CI-03 documents CI gate map for maintainers" do
  operator_docs = File.read!("docs/operator_verification.md")
  assert operator_docs =~ "CI gate map"
  assert operator_docs =~ "policy"
  assert operator_docs =~ "needs: policy" or operator_docs =~ "`policy`"
end
```

Adjust assertions to match final section title if needed; do not assert full section body text.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/scoria/ci_policy_contract_test.exs` contains `CI-03 documents CI gate map`
    - `MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 69-00-05: Sync CI-03 prose in REQUIREMENTS, PROJECT, ROADMAP</name>
  <requirement_ids>CI-03</requirement_ids>
  <files>.planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/PROJECT.md
    .planning/ROADMAP.md
    .planning/phases/69-ci-trust-and-milestone-closeout/69-CONTEXT.md
  </read_first>
  <action>
1. In `.planning/REQUIREMENTS.md`, replace the CI-03 bullet (v2.6 section) with exact locked text from CONTEXT D-08:

> **CI-03**: CI preserves canonical closeout order (`release_preview` → `adoption` → `runtime_to_handoff`) in the Postgres test job, runs a Postgres-free policy job first (baseline expiry, `mix compile --warnings-as-errors`, lane-contract WAE), and enforces full-suite `mix test --warnings-as-errors` after closeout lanes and before `mix test.knowledge`.

Leave checkbox `[ ]` until Phase 69 verification (69-02 marks complete).

2. In `.planning/PROJECT.md`, update the CI-03 line to the same wording (no "staged WAE").

3. In `.planning/ROADMAP.md`:
   - Change Phase 69 **Goal** to: `Document CI trust and close v2.6 traceability (executable CI unchanged).`
   - Update progress table row 69 description to match (remove "wire staged gates" language).

Do not mark CI-03 `[x]` in REQUIREMENTS until 69-02 verification.
  </action>
  <verify>
    <automated>rg "Postgres-free policy job" .planning/REQUIREMENTS.md && ! rg "staged WAE" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` CI-03 bullet contains `Postgres-free policy job`
    - `.planning/REQUIREMENTS.md` CI-03 bullet does not contain `staged WAE`
    - `.planning/ROADMAP.md` Phase 69 goal contains `Document CI trust`
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs` — exit 0
2. `rg "CI gate map" docs/operator_verification.md` — match
3. Manual read: operator gate map closeout order matches ci.yml step order
</verification>

<success_criteria>
- CI-03 documentation bundle shipped with contract-test anchor
- No ci.yml step order changes
- REQUIREMENTS/ROADMAP prose aligned with Phase 68 executable truth
</success_criteria>
