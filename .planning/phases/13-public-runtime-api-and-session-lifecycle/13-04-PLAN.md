---
phase: 13-public-runtime-api-and-session-lifecycle
plan: 04
type: execute
wave: 4
depends_on:
  - 13-01
  - 13-02
  - 13-03
files_modified:
  - test/scoria/runtime_integration_test.exs
  - test/scoria/workflows/integration_test.exs
  - test/scoria/runtime_test.exs
autonomous: true
nyquist_compliant: true
requirements:
  - IDEN-03
  - RUNT-01
  - RUNT-02
  - RUNT-03
must_haves:
  truths:
    - "A host app can keep `session_id` for continuity and `run_id` for exact continuation without reconstructing hidden runtime state."
    - "Multiple runs can share one session while remaining distinct public lifecycle records."
    - "Public runtime summaries align with the operator-visible workflow state for the same run."
  artifacts:
    - path: "test/scoria/runtime_integration_test.exs"
      provides: "End-to-end public runtime continuity verification"
    - path: "test/scoria/workflows/integration_test.exs"
      provides: "Operator-visible regression checks aligned to the public runtime contract"
    - path: "test/scoria/runtime_test.exs"
      provides: "Public contract regressions for summary fields and continuity semantics"
  key_links:
    - from: "test/scoria/runtime_integration_test.exs"
      to: "lib/scoria.ex"
      via: "public lifecycle flow uses the top-level facade end to end"
      pattern: "Scoria\\.(start_run|resume_run)"
    - from: "test/scoria/runtime_integration_test.exs"
      to: "lib/scoria/runtime.ex"
      via: "same-session multi-run continuity and exact resume assertions"
      pattern: "session_id|run_id"
    - from: "test/scoria/workflows/integration_test.exs"
      to: "lib/scoria_web/live/workflow_live/show.ex"
      via: "operator-visible state remains aligned to public runtime status and ids"
      pattern: "waiting_for_approval|completed|/scoria/workflows/"
---

<objective>
Verify the full public runtime continuity story end to end, including same-session multi-run behavior and operator-visible alignment.

Purpose: close Phase 13 with automated proof that the public runtime surface is usable by a Phoenix app without workflow-module coupling or hidden-state reconstruction.
Output: end-to-end continuity tests on the public surface plus operator-alignment regressions on the existing workflow evidence page.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-public-runtime-api-and-session-lifecycle/13-CONTEXT.md
@.planning/phases/13-public-runtime-api-and-session-lifecycle/13-RESEARCH.md
@.planning/phases/13-public-runtime-api-and-session-lifecycle/13-PATTERNS.md
@.planning/phases/13-public-runtime-api-and-session-lifecycle/13-VALIDATION.md
@.planning/phases/13-public-runtime-api-and-session-lifecycle/13-02-PLAN.md
@.planning/phases/13-public-runtime-api-and-session-lifecycle/13-03-PLAN.md

<interfaces>
From lib/scoria/runtime.ex:
```elixir
def start_run(identity, opts \\ [])
def resume_run(run_id, opts \\ [])
```

From lib/scoria/runtime/run_summary.ex:
```elixir
defstruct [
  :run_id,
  :session_id,
  :status,
  :actor_id,
  :tenant_id,
  :current_step_id,
  :latest_checkpoint_id
]
```

From lib/scoria_web/live/workflow_live/show.ex:
```elixir
def mount(%{"id" => run_id}, _session, socket)
def handle_info({:workflow_updated, run_id}, socket)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove end-to-end session continuity through the public runtime facade</name>
  <read_first>test/scoria/runtime_integration_test.exs, test/scoria/runtime_test.exs, lib/scoria.ex, lib/scoria/runtime.ex, lib/scoria/runtime/run_summary.ex, .planning/phases/13-public-runtime-api-and-session-lifecycle/13-CONTEXT.md, .planning/phases/13-public-runtime-api-and-session-lifecycle/13-VALIDATION.md</read_first>
  <files>test/scoria/runtime_integration_test.exs, test/scoria/runtime_test.exs</files>
  <acceptance_criteria>The public runtime integration suite proves all required continuity cases from the Phase 13 specifics: same `session_id` with a new `run_id` for a new turn, same `run_id` for an approval resume, and session-based lookup/grouping without pretending it is execution truth. The tests assert that host apps can persist returned `run_id` and `session_id` from the public API and continue the flow without reconstructing hidden state manually, satisfying `IDEN-03` and the public runtime requirements together.</acceptance_criteria>
  <action>Extend the public runtime suites so the entire continuity story is proven through `Scoria` and `Scoria.Runtime`, not just workflow substrate tests. Cover D-12 through D-17 directly: store the returned `run_id`, reuse the same `session_id` across multiple starts, resume only by the stored `run_id`, and show that the host app never has to reverse-engineer checkpoint or handoff internals to continue the flow. Keep the assertions targeted to the validation lanes in `13-VALIDATION.md` and do not add manual verification steps.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/runtime_integration_test.exs test/scoria/workflows/integration_test.exs</automated>
  </verify>
  <done>Automated tests now prove the Phase 13 continuity contract end to end through the public runtime facade.</done>
</task>

<task type="auto">
  <name>Task 2: Verify operator-visible workflow state stays aligned with the public runtime contract</name>
  <read_first>test/scoria/workflows/integration_test.exs, test/scoria/runtime_integration_test.exs, lib/scoria_web/live/workflow_live/show.ex, lib/scoria/runtime/run_summary.ex, .planning/phases/13-public-runtime-api-and-session-lifecycle/13-RESEARCH.md, .planning/phases/13-public-runtime-api-and-session-lifecycle/13-PATTERNS.md</read_first>
  <files>test/scoria/runtime_integration_test.exs, test/scoria/workflows/integration_test.exs, test/scoria/runtime_test.exs</files>
  <acceptance_criteria>The existing workflow integration lane and the new public runtime lane both assert that the operator-visible page at `/scoria/workflows/:id` stays aligned to the same `run_id`, status, and lifecycle transitions returned by the public runtime summary. No verification path requires direct public use of `Scoria.Workflows`; the substrate remains truth, but the tested app-facing path is the `Scoria` facade. The combined targeted-lane command stays within the Phase 13 validation contract.</acceptance_criteria>
  <action>Extend the existing operator-facing integration assertions so the public runtime summary and the LiveView evidence page agree on run identity and lifecycle transitions for the same run. Use the existing `WorkflowLive.Show` route as the operator analog from Phase 13 context, but keep this plan out of UI-building scope: verify alignment only. Tighten any public-runtime regression assertions needed to ensure the canonical host-app path remains `Scoria`, while `Scoria.Workflows` continues as substrate truth underneath it per D-01 through D-03.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/runtime_integration_test.exs test/scoria/workflows/integration_test.exs</automated>
  </verify>
  <done>The public runtime contract and the operator-visible workflow evidence agree on the same run ids, statuses, and continuity behavior under automated verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host app persisted continuity data → public runtime facade | Stored `run_id` and `session_id` are reused to continue the flow |
| public runtime summary → operator-visible LiveView | Host-app polling data and operator evidence both describe the same durable run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | S | `test/scoria/runtime_integration_test.exs` | mitigate | Assert continuity only through stored `run_id` plus canonical public API calls so shared `session_id` cannot masquerade as exact resume authority. |
| T-13-04-02 | T | `test/scoria/runtime_integration_test.exs` | mitigate | Verify same-session fresh starts create distinct run ids and do not mutate prior run lifecycle state. |
| T-13-04-03 | R | `test/scoria/workflows/integration_test.exs` | mitigate | Cross-check public runtime summaries against the operator page so lifecycle evidence remains attributable to one durable run. |
| T-13-04-04 | I | `test/scoria/runtime_test.exs` | mitigate | Keep public continuity assertions on curated summary fields only and avoid surfacing internal checkpoint or preload details. |
| T-13-04-05 | D | `test/scoria/runtime_integration_test.exs` | mitigate | Reuse targeted test lanes from `13-VALIDATION.md` so end-to-end verification stays bounded and repeatable. |
| T-13-04-06 | E | `test/scoria/runtime_integration_test.exs` | mitigate | Prove that only exact `run_id` resumes prior execution, preventing callers from escalating into unrelated runs that share the same session. |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/runtime_integration_test.exs test/scoria/runtime_test.exs`
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/runtime_integration_test.exs test/scoria/workflows/integration_test.exs`
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test`
</verification>

<success_criteria>
- The public runtime facade proves same-session multi-run continuity and exact `run_id` resume under automated tests.
- Host apps can persist `session_id` for grouping and `run_id` for exact continuation without reconstructing hidden workflow state.
- Public runtime summaries and the existing operator evidence page agree on run identity and lifecycle state.
- Phase 13 closes with direct automated coverage for all required lifecycle behaviors.
</success_criteria>

<output>
After completion, create `.planning/phases/13-public-runtime-api-and-session-lifecycle/13-04-SUMMARY.md`
</output>
