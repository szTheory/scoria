---
phase: 13-public-runtime-api-and-session-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria.ex
  - lib/scoria/runtime.ex
  - test/scoria_test.exs
  - test/scoria/runtime_test.exs
autonomous: true
nyquist_compliant: true
requirements:
  - RUNT-01
must_haves:
  truths:
    - "A host app can start a Scoria run through `Scoria` instead of importing `Scoria.Workflows` directly."
    - "`Scoria` is the canonical happy-path facade while `Scoria.Runtime` is the deeper public layer for advanced callers."
    - "The public lifecycle surface teaches runtime nouns such as identity, run, and session instead of workflow-engine internals."
  artifacts:
    - path: "lib/scoria.ex"
      provides: "Canonical top-level runtime facade for host-app callers"
    - path: "lib/scoria/runtime.ex"
      provides: "Deeper public runtime layer that `Scoria` delegates into"
    - path: "test/scoria/runtime_test.exs"
      provides: "Wave 0 contract coverage for the public runtime facade"
  key_links:
    - from: "lib/scoria.ex"
      to: "lib/scoria/runtime.ex"
      via: "thin public delegates for lifecycle entrypoints"
      pattern: "def start_run|def resume_run|Scoria\\.Runtime"
    - from: "test/scoria_test.exs"
      to: "lib/scoria.ex"
      via: "top-level facade contract assertions"
      pattern: "Scoria\\.(identity|start_run|resume_run)"
    - from: "test/scoria/runtime_test.exs"
      to: "lib/scoria/runtime.ex"
      via: "public layer contract and delegation coverage"
      pattern: "Scoria\\.Runtime\\.(start_run|resume_run)"
---

<objective>
Establish `Scoria` as the obvious public runtime entrypoint and introduce `Scoria.Runtime` as the deeper stable layer behind it.

Purpose: satisfy the Keystone product-boundary decision that host apps start with `Scoria`, not workflow modules, while preserving `Scoria.Workflows` as substrate truth.
Output: a top-level runtime facade, a public runtime module boundary, and Wave 0 tests that lock the public entrypoint shape before deeper lifecycle semantics land.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-public-runtime-api-and-session-lifecycle/13-CONTEXT.md
@.planning/phases/13-public-runtime-api-and-session-lifecycle/13-RESEARCH.md
@.planning/phases/13-public-runtime-api-and-session-lifecycle/13-PATTERNS.md
@.planning/phases/13-public-runtime-api-and-session-lifecycle/13-VALIDATION.md
@.planning/phases/12-canonical-runtime-identity/12-CONTEXT.md
@.planning/phases/12-canonical-runtime-identity/12-RESEARCH.md

<interfaces>
From lib/scoria.ex:
```elixir
defmodule Scoria do
  def identity(attrs \\ %{}), do: Scoria.Identity.normalize(attrs)
end
```

From lib/scoria/identity.ex:
```elixir
@type t :: %__MODULE__{
  actor_id: String.t() | nil,
  tenant_id: String.t() | nil,
  session_id: String.t() | nil,
  metadata: map()
}

def normalize(attrs)
```

From lib/scoria/workflows.ex:
```elixir
def create_run(attrs \\ %{})
def get_run!(id)
```

From lib/scoria/workflows/resume.ex:
```elixir
def resume_run(run_id, opts \\ [])
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 facade-contract tests for the public runtime surface</name>
  <read_first>test/scoria_test.exs, lib/scoria.ex, lib/scoria/identity.ex, .planning/phases/13-public-runtime-api-and-session-lifecycle/13-CONTEXT.md, .planning/phases/13-public-runtime-api-and-session-lifecycle/13-VALIDATION.md, .planning/phases/13-public-runtime-api-and-session-lifecycle/13-PATTERNS.md</read_first>
  <files>test/scoria_test.exs, test/scoria/runtime_test.exs</files>
  <acceptance_criteria>`test/scoria/runtime_test.exs` exists per the Wave 0 requirement in `13-VALIDATION.md`; the new tests assert that `Scoria` is the taught public entrypoint per D-01 and D-02, that `Scoria.identity/1` remains available, and that explicit lifecycle verbs `start_run/2` and `resume_run/2` exist on both `Scoria` and `Scoria.Runtime` rather than teaching `Scoria.Workflows` as the normal host-app API per D-03 and D-05.</acceptance_criteria>
  <action>Create the missing Wave 0 public-facade test file `test/scoria/runtime_test.exs` and extend `test/scoria_test.exs` so Phase 13 starts from locked contract tests instead of substrate-only coverage. Anchor the tests to D-01, D-02, D-03, D-04, and D-18: the top-level module is the canonical happy path, the deeper public layer is `Scoria.Runtime`, and the taught verbs are explicit runtime lifecycle verbs rather than workflow-engine nouns. Keep these tests narrow to surface shape and delegation expectations; lifecycle semantics such as exact resume and stable DTOs are intentionally verified in later Phase 13 plans, not duplicated here.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria_test.exs test/scoria/runtime_test.exs</automated>
  </verify>
  <done>The repo now has direct automated coverage for the top-level public runtime surface, and the Phase 13 facade shape is locked before deeper implementation proceeds.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the placeholder top-level module with the canonical runtime facade</name>
  <read_first>lib/scoria.ex, lib/scoria/identity.ex, lib/scoria/workflows.ex, lib/scoria/workflows/resume.ex, test/scoria_test.exs, test/scoria/runtime_test.exs, .planning/phases/13-public-runtime-api-and-session-lifecycle/13-RESEARCH.md, .planning/phases/13-public-runtime-api-and-session-lifecycle/13-PATTERNS.md</read_first>
  <files>lib/scoria.ex, lib/scoria/runtime.ex, test/scoria_test.exs, test/scoria/runtime_test.exs</files>
  <acceptance_criteria>`lib/scoria.ex` exposes the happy-path runtime surface with `identity/1`, `start_run/2`, and `resume_run/2`; `lib/scoria/runtime.ex` exists as the deeper public layer behind those delegates; the public module docs and function names use runtime nouns per D-04 and keep `Scoria.Workflows` as implementation substrate rather than the primary product surface per D-03; the new facade tests pass without requiring host apps to import workflow modules directly.</acceptance_criteria>
  <action>Turn `lib/scoria.ex` into the real Phase 13 public facade per D-01 and D-18: keep `identity/1`, add explicit `start_run/2` and `resume_run/2`, and delegate into a new `Scoria.Runtime` module rather than teaching callers to assemble workflow calls themselves. Implement `Scoria.Runtime` as the deeper public boundary per D-02, aliasing existing workflow substrate modules but not rewriting them per D-03 and the Phase 13 boundary rule that `Scoria.Workflows` remains substrate truth. Use explicit lifecycle verbs per D-05 and keep the top-level module thin and boring; do not reintroduce workflow-engine nouns such as checkpoint or reconciler into public docs or APIs.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria_test.exs test/scoria/runtime_test.exs</automated>
  </verify>
  <done>`Scoria` is no longer placeholder-only: host apps have one obvious top-level runtime facade, and the deeper public layer is established without changing workflow-truth ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host app caller → `Scoria` facade | Untrusted app input enters the public runtime API |
| `Scoria` facade → `Scoria.Runtime` | Public calls are translated into a narrower runtime boundary |
| `Scoria.Runtime` → workflow substrate | Public lifecycle calls delegate into durable workflow truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | S | `lib/scoria.ex` | mitigate | Expose explicit lifecycle verbs and keep identity normalization through `Scoria.Identity` so callers cannot spoof public contract shape via workflow-only functions. |
| T-13-01-02 | T | `lib/scoria/runtime.ex` | mitigate | Keep `Scoria.Runtime` as a thin boundary over workflow truth and forbid facade-side lifecycle mutations outside substrate seams. |
| T-13-01-03 | R | `test/scoria/runtime_test.exs` | mitigate | Lock the public entrypoint names and module layering with automated tests so future refactors cannot quietly drift back to `Scoria.Workflows`-first teaching. |
| T-13-01-04 | I | `lib/scoria.ex` | accept | This plan only exposes entrypoint verbs and identity helpers; no new detailed run data is exposed yet. |
| T-13-01-05 | D | `lib/scoria.ex` | mitigate | Keep the facade small and delegate-heavy so the top-level surface does not add extra runtime work or duplicate workflow queries. |
| T-13-01-06 | E | `lib/scoria/runtime.ex` | mitigate | Preserve workflow ownership of durable mutations so public API callers cannot bypass substrate checks by calling the facade. |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria_test.exs test/scoria/runtime_test.exs`
</verification>

<success_criteria>
- `Scoria` is the documented public runtime happy path instead of a placeholder module.
- `Scoria.Runtime` exists as the deeper public layer behind `Scoria`.
- The public surface teaches explicit runtime lifecycle verbs and keeps workflow-engine modules out of the normal host-app path.
- Wave 0 facade tests exist and pass.
</success_criteria>

<output>
After completion, create `.planning/phases/13-public-runtime-api-and-session-lifecycle/13-01-SUMMARY.md`
</output>
