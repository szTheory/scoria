---
phase: 22-curated-connector-profiles-and-boring-adoption-path
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified: ["lib/mix/tasks/test.adoption.ex", "docs/operator_verification.md"]
autonomous: true
requirements: ["DX-02"]
must_haves:
  truths:
    - "Remote connector verification runs seamlessly as part of the default `mix test.adoption` lane"
    - "Operators have clear documentation on how to register and verify remote connectors using profiles"
  artifacts:
    - path: "lib/mix/tasks/test.adoption.ex"
      provides: "Runner for default adoption lane"
      contains: "profiles_test.exs"
    - path: "docs/operator_verification.md"
      provides: "Human verification instructions"
      contains: "Profiles.build_attrs"
  key_links:
    - from: "lib/mix/tasks/test.adoption.ex"
      to: "test/scoria/connectors/profiles_test.exs"
      via: "test runner execution"
      pattern: "profiles_test.exs"
---

<objective>
Wire the profile verification test into the default adoption proof lane and update the operator documentation to guide a user through the boring adoption path.

Purpose: Assure that `mix test.adoption` guards the curated remote connector adoption flow and that developers have an executable, offline-capable verification lane properly documented.
Output: Updated `mix test.adoption` and `operator_verification.md` guide.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Needed to reference the newly created verification test
@.planning/phases/22-curated-connector-profiles-and-boring-adoption-path/22-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Verification to Default Adoption Lane</name>
  <files>lib/mix/tasks/test.adoption.ex</files>
  <action>
    Append `"test/scoria/connectors/profiles_test.exs"` to the `@adoption_test_files` list in `lib/mix/tasks/test.adoption.ex`.
    This guarantees that the automated profile translation and sync-mock verification run exactly alongside the rest of Scoria's executable proofs.
  </action>
  <verify>
    <automated>mix test.adoption</automated>
  </verify>
  <done>The test runner succeeds and explicitly includes `profiles_test.exs` in its output run.</done>
</task>

<task type="auto">
  <name>Task 2: Document Remote Connector Registration Proof</name>
  <files>docs/operator_verification.md</files>
  <action>
    Update `docs/operator_verification.md` to include a new section on "Remote Connector Verification".
    - Explain that registering a remote connector safely is done using `Scoria.Connectors.Profiles.build_attrs/2` combined with `Scoria.register_connector/1`.
    - Provide a code snippet of how a generic or GitHub connector is registered this way.
    - Reiterate that verification runs locally and correctly mocks HTTP calls during testing.
    - Confirm the operator surface correctly surfaces remote connector identity, health, and sync truth.
  </action>
  <verify>
    <automated>grep -i "Remote Connector" docs/operator_verification.md</automated>
  </verify>
  <done>Documentation plainly shows the boring remote adoption path aligned with executable truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → Operator | Ensure documentation correctly instructs operators to rely on Ecto as the source of truth, not a secondary config store. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Spoofing | `test.adoption` | accept | The adoption tests do not reach out to real infrastructure, thus are isolated from remote spoofing attacks. |
</threat_model>

<verification>
- `mix test.adoption` integrates `profiles_test.exs` directly.
- The operator guide accurately reflects the offline-first, database-first connector lifecycle truth without invoking hosted platform paradigms.
</verification>

<success_criteria>
- The adoption test harness includes the new remote connector integration tests.
- Documentation accurately reflects the new capabilities and their underlying mechanisms.
</success_criteria>

<output>
After completion, create `.planning/phases/22-curated-connector-profiles-and-boring-adoption-path/22-02-SUMMARY.md`
</output>