---
phase: 22-curated-connector-profiles-and-boring-adoption-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["lib/scoria/connectors/profiles.ex", "test/scoria/connectors/profiles_test.exs"]
autonomous: true
requirements: ["DX-01"]
must_haves:
  truths:
    - "Developer can retrieve Ecto-ready attributes for remote connector using curated profile atom"
    - "The :github profile sets safe defaults like oauth_pkce and explicit discovery paths"
    - "The profile layer does not bypass or replace Ecto-backed connector truth"
    - "The operator surface reflects the same truth as the durable registration"
  artifacts:
    - path: "lib/scoria/connectors/profiles.ex"
      provides: "Profile normalization logic"
      exports: ["build_attrs/2"]
    - path: "test/scoria/connectors/profiles_test.exs"
      provides: "Tests for profile data transformations and explicit integration"
  key_links:
    - from: "lib/scoria/connectors/profiles.ex"
      to: "lib/scoria/connectors.ex"
      via: "output attrs feed into registration"
      pattern: "build_attrs"
---

<objective>
Introduce a curated connector profile layer that normalizes a predefined set of profiles (`:generic`, `:github`) into Ecto-ready attributes for remote connector registration, and provide integration tests validating this behavior.

Purpose: Lower the barrier to remote connector adoption by providing boring, stateless-first, secure defaults, without inventing a secondary configuration system or a connector marketplace.
Output: `Scoria.Connectors.Profiles` module and its test suite with an integration proof.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From lib/scoria/connectors.ex:
```elixir
  def register_connector(attrs \\ %{}) do ... end
  def list_connector_fleet() do ... end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Curated Profiles Normalization Layer</name>
  <files>lib/scoria/connectors/profiles.ex, test/scoria/connectors/profiles_test.exs</files>
  <behavior>
    - Test 1: `build_attrs(:generic, overrides)` returns `streamable_http` and `none` auth by default, merged with overrides.
    - Test 2: `build_attrs(:github, overrides)` returns `streamable_http`, `oauth_pkce` auth, and explicit discovery path defaults, merged with overrides.
    - Test 3: Unknown profile atom raises an `ArgumentError`.
  </behavior>
  <action>
    Implement `Scoria.Connectors.Profiles.build_attrs/2`.
    - Accept a profile name (`:generic` or `:github`) and an optional map of `overrides`.
    - Apply deep merge of `overrides` on top of curated profile defaults.
    - Ensure `:generic` defaults strictly to `streamable_http` transport and `none` auth mode.
    - Ensure `:github` defaults to `streamable_http`, `oauth_pkce`, and `https://api.github.com/.well-known/oauth-authorization-server` as discovery_metadata_url.
    - The output MUST be a plain map suitable to pass to `Scoria.register_connector/1` (from Phase 19/20).
  </action>
  <verify>
    <automated>mix test test/scoria/connectors/profiles_test.exs</automated>
  </verify>
  <done>Module is created with `build_attrs/2` behaving correctly under test without database interaction.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Execution Proof for GitHub Profile</name>
  <files>test/scoria/connectors/profiles_test.exs</files>
  <action>
    Add an integration test to `profiles_test.exs` proving that `Profiles.build_attrs(:github, %{tenant_id: "test", key: "github-remote", endpoint_url: "https://api.github.com/mcp"})` produces valid attributes for `Scoria.Connectors.register_connector/1`.
    - Use `Req.Test` to stub the discovery/sync endpoint `Scoria.Connectors.Discovery` if the registration triggers it. (Return a mock catalog: `{"catalog" => %{"tools" => [%{"name" => "issues.list"}]}}`).
    - Assert that `Scoria.register_connector/1` successfully persists the connector with `oauth_pkce` auth mode.
    - Assert that the `Scoria.Connectors.DiscoveryJob` was enqueued to process the newly registered connector.
    - Per D-16: Assert that the newly registered connector appears correctly in the operator surface via `Scoria.Connectors.list_connector_fleet/1`.
  </action>
  <verify>
    <automated>mix test test/scoria/connectors/profiles_test.exs</automated>
  </verify>
  <done>Integration test verifies profile registration and operator-surface visibility without live HTTP calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Profile Layer → Ecto Schema | Profile overrides merge user input into defaults. The input is structurally validated down the line by Ecto via `Scoria.Connectors.Params.register/1`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | `Profiles.build_attrs` | mitigate | `build_attrs` does not interact with the database. It delegates all validation/persistence to the robust existing `Scoria.register_connector/1` logic. |
| T-22-02 | Info Disclosure | `ProfilesTest` | accept | Test only stubs endpoints, uses fake tenant ID and keys, does not handle real credentials. |
</threat_model>

<verification>
- Profile normalization is pure data transformation.
- Registration uses the existing safe boundary `Scoria.register_connector/1`.
- Tests mock HTTP correctly and assert durable state updates via the database and job queue.
- Operator surface visibility is explicitly verified in integration proofs.
</verification>

<success_criteria>
- The `Profiles.build_attrs/2` function securely maps known profiles.
- Tests prove end-to-end integration without real network side-effects.
- Operator evidence agrees with registration truth.
</success_criteria>

<output>
After completion, create `.planning/phases/22-curated-connector-profiles-and-boring-adoption-path/22-01-SUMMARY.md`
</output>
