---
phase: 12-design-system-component-layer
plan: 05
type: execute
wave: 5
depends_on: ["12-04"]
files_modified:
  - lib/scoria_web/live/approvals_live/index.ex
  - lib/scoria_web/live/workflow_live/show.ex
  - lib/scoria_web/components/remote_invocation_evidence_component.ex
  - test/scoria_web/live/approvals_live_test.exs
  - test/scoria_web/live/workflow_live_test.exs
  - test/scoria_web/ui_component_test.exs
  - test/support/ds06_baseline.txt
  - test/scoria_web/ds06_drift_guard_test.exs
autonomous: true
requirements: [DS-05, DS-04, DS-06]
nyquist_compliant: true

must_haves:
  truths:
    - "One real server-triggered toast path renders a <.toast> in the approvals view after an action"
    - "One real <.skeleton> loading state replaces bespoke loading markup on an async screen"
    - "One evidence component (RemoteInvocationEvidenceComponent) renders through the <.notebook> shell as proof-of-API"
    - "The DS-06 ratchet guard is committed, wired into mix test, and passes — raw palette cannot grow or return"
  artifacts:
    - path: "test/support/ds06_baseline.txt"
      provides: "committed path:count baseline (grandfathered files; ui.ex + remote_invocation + zeroed files excluded)"
      contains: "lib/scoria_web/live/review_queue_live.ex"
    - path: "lib/scoria_web/components/remote_invocation_evidence_component.ex"
      provides: "proof-of-API notebook adapter (zero raw palette)"
      contains: "ScoriaWeb.UI"
    - path: "lib/scoria_web/live/approvals_live/index.ex"
      provides: "@toasts assign + put_toast/2 + toast render region"
      contains: "put_toast"
  key_links:
    - from: "lib/scoria_web/live/approvals_live/index.ex record_approval_decision"
      to: "<.toast> render region"
      via: "put_toast/2 updates @toasts; <.toast :for={t <- @toasts}>"
      pattern: "put_toast"
    - from: "lib/scoria_web/live/workflow_live/show.ex <:loading>"
      to: "<.skeleton>"
      via: "skeleton replaces bespoke loading markup"
      pattern: "skeleton"
    - from: "test/scoria_web/ds06_drift_guard_test.exs"
      to: "test/support/ds06_baseline.txt"
      via: "load_baseline reads committed file; ratchet guard active"
      pattern: "ds06_baseline"
---

<objective>
Prove the components are real and lock the drift guard. This plan does the ONLY real wiring Phase 12 ships (DS-05/DS-04 mandate): (1) wire one genuine server-triggered toast into the approvals decision handler; (2) replace one bespoke async `<:loading>` block with `<.skeleton>` on `workflow_live/show.ex`; (3) convert `RemoteInvocationEvidenceComponent` (the thinnest evidence component, 8 palette occurrences) into a `<.notebook>` adapter — outer shell only — as the DS-04 proof-of-API. Then (4) generate the DS-06 baseline by running the scanner once against the post-sweep codebase, commit `test/support/ds06_baseline.txt`, drop the `:ui_ex_zero` test tag and the File.exists? guard, and confirm the full ratchet guard is green.

Purpose: This closes DS-04 (proof adapter), DS-05 (real toast + real skeleton), and DS-06 (committed, active ratchet guard). It is the last plan — afterward raw palette cannot grow or return, and the gateway is proven against three real consumers.
Output: Toast-wired approvals, skeleton-wired workflow show, notebook-adapter remote-invocation component, committed baseline, active guard.

NO full-screen migration: only the three specified wiring/conversion targets are touched (D-08). The 13-component evidence conversion and the ~487-occurrence sweep stay in Phases 15/14/17.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-design-system-component-layer/12-UI-SPEC.md
@.planning/phases/12-design-system-component-layer/12-PATTERNS.md
@.planning/phases/12-design-system-component-layer/12-CONTEXT.md
@lib/scoria_web/live/approvals_live/index.ex
@lib/scoria_web/live/workflow_live/show.ex
@lib/scoria_web/components/remote_invocation_evidence_component.ex

<interfaces>
<!-- ui.ex now exports all 8 components: table, drawer, modal, field, form_section, notebook, skeleton, toast + fixed flash_group. -->
<!-- put_flash analog in approvals: index.ex line ~192 `put_flash(socket, :error, approval_error_message(status, reason))`. -->
<!-- flash_group already imported in approvals: `import ScoriaWeb.UI, only: [flash_group: 1]` (line ~9). -->
<!-- assign_async + <.async_result> + bespoke <:loading> in workflow_live/show.ex lines ~30-32, ~210-222. -->
<!-- remote_invocation current shell: <section class="mt-6 rounded-2xl border border-stone-200 bg-white ..."> lines ~1-43. -->
<!-- ds06_drift_guard_test.exs: load_baseline/0 currently has a File.exists? guard + :ui_ex_zero tag from plan 12-01. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire one real toast (approvals) and one real skeleton (workflow show)</name>
  <files>lib/scoria_web/live/approvals_live/index.ex, lib/scoria_web/live/workflow_live/show.ex, test/scoria_web/live/approvals_live_test.exs, test/scoria_web/live/workflow_live_test.exs</files>
  <behavior>
    - After a real approval decision action, the approvals view renders a <.toast> (HTML contains "scoria-toast")
    - The workflow show async-loading state renders <.skeleton> (HTML contains "scoria-skeleton" / aria-label="Loading…") instead of the bespoke "Loading compacted memories..." markup
    - approvals_live/index.ex and the touched part of workflow_live/show.ex introduce NO new raw palette
  </behavior>
  <read_first>
    - lib/scoria_web/live/approvals_live/index.ex (read mount/3 assigns, the `import ScoriaWeb.UI, only: [flash_group: 1]` line ~9, the `record_approval_decision/2` clauses with `put_flash` ~lines 177-194, and the render/1 region where flash_group is rendered)
    - lib/scoria_web/live/workflow_live/show.ex (read the assign_async ~lines 30-32 and the `<.async_result>` with bespoke `<:loading>` ~lines 210-222)
    - .planning/phases/12-design-system-component-layer/12-PATTERNS.md (lines 345-403 put_toast/2 + @toasts + toast render region; lines 407-450 skeleton swap — replace ONLY the `<:loading>` inner content, keep assign_async unchanged)
    - .planning/phases/12-design-system-component-layer/12-RESEARCH.md (D-10/D-11/D-12 toast contract; Open Question 1 lines 764-767 — wire put_toast as a supplement to put_flash in record_approval_decision, add @toasts + toast region to the approvals view, NOT the shared layout)
    - test/scoria_web/live/approvals_live_test.exs (the `render(view) =~` assertion pattern ~line 248 and the Endpoint/secret_key_base setup)
    - test/scoria_web/live/workflow_live_test.exs (the existing live-test setup to add a skeleton assertion)
  </read_first>
  <action>
    APPROVALS toast wiring: in `lib/scoria_web/live/approvals_live/index.ex`, change the import to `import ScoriaWeb.UI, only: [flash_group: 1, toast: 1]`. Add `|> assign(:toasts, [])` in `mount/3` alongside existing assigns. Add a private `put_toast/2` (copy from PATTERNS.md lines 366-374): builds a toast map (`id`, `tone`, `message`, `duration_ms`) and `update(socket, :toasts, fn toasts -> [toast | toasts] end)`. In `record_approval_decision/2`, supplement the existing `put_flash` calls with a `put_toast/2` call in BOTH the success and error branches (success uses `tone: :pass`, error uses `tone: :fail`, reusing `approval_error_message(status, reason)` for the error message). In `render/1`, after the existing `<.flash_group flash={@flash} />`, add a `<div id="toast-region" class="scoria-toast-region">` containing `<.toast :for={t <- @toasts} id={t.id} tone={t.tone} message={t.message} duration_ms={t.duration_ms} />`. Do NOT touch the shared layout. Zero any raw palette in lines you edit.
    WORKFLOW skeleton wiring: in `lib/scoria_web/live/workflow_live/show.ex`, ensure `<.skeleton>` is in scope (add `import ScoriaWeb.UI, only: [skeleton: 1]` if not already imported). Replace ONLY the bespoke `<:loading>` slot inner content of the compacted-memories `<.async_result>` with `<.skeleton rows={3} class="mt-6" />`. Leave `assign_async`, `<:failed>`, and the success branch unchanged. This removes the bespoke `border-stone-200`/`text-stone-500` loading markup (reducing this file's raw-palette count); do NOT sweep the rest of the 45-occurrence file (out of scope — it stays baselined at its post-edit count).
    TESTS: in `test/scoria_web/live/approvals_live_test.exs`, add a `test` block that drives a real approval decision action (mirror the existing `render_click`/decision flow) and asserts `render(view) =~ "scoria-toast"`. In `test/scoria_web/live/workflow_live_test.exs`, add (or extend) a test asserting the async loading state renders `"scoria-skeleton"` or `aria-label="Loading…"` (assert against the async-pending render via the documented LiveViewTest async pattern).
  </action>
  <acceptance_criteria>
    - Source: `approvals_live/index.ex` contains `put_toast` and `import ScoriaWeb.UI, only: [flash_group: 1, toast: 1]`
    - Source: `approvals_live/index.ex` contains `class="scoria-toast-region"` and a `<.toast` call
    - Source: `workflow_live/show.ex` contains a `<.skeleton` call
    - Source: `workflow_live/show.ex` no longer contains the bespoke "Loading compacted memories..." `<:loading>` markup
    - Behavior: after a real approval action, `render(view) =~ "scoria-toast"`
    - Behavior: the workflow show async-loading render contains `scoria-skeleton` (or `aria-label="Loading…"`)
    - Test: `mix test test/scoria_web/live/approvals_live_test.exs test/scoria_web/live/workflow_live_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/approvals_live_test.exs test/scoria_web/live/workflow_live_test.exs</automated>
  </verify>
  <done>One real server-triggered toast renders in the approvals view after a decision action; one real `<.skeleton>` replaces the bespoke async loading markup on workflow show; both wiring tests pass; no new raw palette is introduced in edited lines.</done>
</task>

<task type="auto">
  <name>Task 2: Convert RemoteInvocationEvidenceComponent to a <.notebook> adapter (DS-04 proof-of-API)</name>
  <files>lib/scoria_web/components/remote_invocation_evidence_component.ex, test/scoria_web/ui_component_test.exs</files>
  <read_first>
    - lib/scoria_web/components/remote_invocation_evidence_component.ex (read the FULL current file — outer `<section class="mt-6 rounded-2xl border border-stone-200 bg-white ...">` shell, header divs, and inner `<article>` blocks; 8 raw palette occurrences total)
    - .planning/phases/12-design-system-component-layer/12-PATTERNS.md (lines 454-529: exact before/after — replace the outer `<section>` shell with `<.notebook>`, keep inner content; the file must reach ZERO palette to stay excluded from the baseline, so the interior `<article>` raw classes must also be converted to semantic tokens)
    - .planning/phases/12-design-system-component-layer/12-UI-SPEC.md (DS-04 lines 304-318: the evidence-panel tab map — RemoteInvocationEvidenceComponent → tab key "remote_invocation", label "Remote")
    - lib/scoria_web/ui.ex (the `notebook/1` API built in plan 12-04: id, title, eyebrow, selected_tab, on_tab_change, <:tab key label>)
  </read_first>
  <action>
    Convert `lib/scoria_web/components/remote_invocation_evidence_component.ex` to render through `<.notebook>`. Add `import ScoriaWeb.UI, only: [notebook: 1]`. Add `attr :selected_tab, :string, default: "remote_invocation"` and `attr :on_tab_change, :string, default: nil`. Replace the outer `<section>` shell + bespoke header divs with a `<.notebook id="remote-invocation-notebook" title="Remote invocation evidence" eyebrow="Remote evidence notebook" selected_tab={@selected_tab} on_tab_change={@on_tab_change}>` wrapping a single `<:tab key="remote_invocation" label="Remote">` whose body is the existing approvals list markup. Convert the interior `<article>` raw palette classes (`border-stone-200`, `bg-stone-50`, `text-stone-700`, etc.) to semantic equivalents using existing project semantic tokens (e.g. `--scoria-surface-panel-raised`, `--scoria-border`, `--scoria-text`), mirroring how other converted components express cards. The file MUST reach ZERO raw-palette occurrences so it stays in the DS-06 `@excluded` list (it is already listed there from plan 12-01). Keep the approval-rendering logic and data shape unchanged — only the shell + class names change.
    In `test/scoria_web/ui_component_test.exs`, add a DS-04 proof test: render `ScoriaWeb.RemoteInvocationEvidenceComponent` with a sample `evidence` map containing one approval; assert the output contains `scoria-notebook` and the tab label "Remote", proving the component now renders through the shared shell.
  </action>
  <acceptance_criteria>
    - Source: `remote_invocation_evidence_component.ex` contains `import ScoriaWeb.UI` and a `<.notebook` call
    - Source: `remote_invocation_evidence_component.ex` no longer contains a raw `<section class="...stone...">` shell
    - Source: file scans to ZERO raw palette: `grep -cE '\b(stone|rose|sky|emerald|amber|blue|gray|slate|zinc|neutral|red|green|yellow|purple|pink|indigo|teal|cyan|lime|orange|violet|fuchsia)-[0-9]' lib/scoria_web/components/remote_invocation_evidence_component.ex` returns 0
    - Behavior: rendering the component produces `scoria-notebook` and the "Remote" tab label
    - Test: `mix test test/scoria_web/ui_component_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/ui_component_test.exs && test "$(grep -cE '\b(stone|rose|sky|emerald|amber|blue|gray|slate|zinc|neutral|red|green|yellow|purple|pink|indigo|teal|cyan|lime|orange|violet|fuchsia)-[0-9]' lib/scoria_web/components/remote_invocation_evidence_component.ex)" = "0"</automated>
  </verify>
  <done>RemoteInvocationEvidenceComponent renders through the shared `<.notebook>` shell with the "Remote" tab and reaches zero raw palette, proving the DS-04 slot/tab API against a real evidence component; the proof test passes.</done>
</task>

<task type="auto">
  <name>Task 3: Generate + commit the DS-06 baseline and activate the full ratchet guard</name>
  <files>test/support/ds06_baseline.txt, test/scoria_web/ds06_drift_guard_test.exs</files>
  <read_first>
    - test/scoria_web/ds06_drift_guard_test.exs (read the current module from plan 12-01 — the `File.exists?` guard wrapping the ratchet assertion, the `:ui_ex_zero` tag, the `@excluded` list, and `load_baseline/0`)
    - .planning/phases/12-design-system-component-layer/12-PATTERNS.md (lines 725-760: the baseline GENERATION procedure — run the scanner once, exclude the zeroed files, write `path:count` lines; the exact format)
    - .planning/phases/12-design-system-component-layer/12-RESEARCH.md (D-02/D-03 ratchet semantics; the DS-06 baseline seed table lines 519-537 — files Phase 12 zeroes/excludes vs grandfathers)
    - .planning/phases/12-design-system-component-layer/12-CONTEXT.md (D-03 — every file Phase 12 touches drops to zero and is excluded; D-04 — Phase 17 later deletes the baseline)
  </read_first>
  <action>
    Confirm the `@excluded` list in `ds06_drift_guard_test.exs` covers every file Phase 12 zeroed: `lib/scoria_web/ui.ex` and `lib/scoria_web/components/remote_invocation_evidence_component.ex` at minimum. For `lib/scoria_web/live/approvals_live/index.ex` and `lib/scoria_web/live/workflow_live/show.ex` (touched in Task 1): check their post-edit palette counts with grep. If a touched file reached zero, add it to `@excluded`; if it still has raw palette (e.g. workflow show retains its other ~40 occurrences), leave it OUT of `@excluded` and let it appear in the baseline at its CURRENT post-edit count (not its pre-Phase-12 count) so the entry does not immediately fail.
    GENERATE the baseline: run the scanner procedure from PATTERNS.md lines 731-748 via `mix run -e "..."` (or an IEx one-off) — walk `Path.wildcard("lib/scoria_web/**/*.{ex,heex}")`, reject `@excluded`, keep files with count > 0, emit `path:count` lines joined by newline. Write the output to `test/support/ds06_baseline.txt` (no trailing spaces around `:`, one entry per line, sorted for stable diffs). The baseline MUST reflect the post-sweep state (after Tasks 1-2), so generate it as the final step.
    ACTIVATE the guard: remove the `File.exists?` guard around the ratchet assertion (the baseline now exists) and remove the `:ui_ex_zero` tag from the ui.ex-zero assertion so both tests run by default in `mix test`. Confirm the full guard passes.
  </action>
  <acceptance_criteria>
    - Artifact: `test/support/ds06_baseline.txt` exists and contains `path:count` lines (e.g. a line for `lib/scoria_web/live/review_queue_live.ex`)
    - Source: baseline does NOT contain a line for `lib/scoria_web/ui.ex` or `lib/scoria_web/components/remote_invocation_evidence_component.ex` (excluded, zeroed)
    - Source: `ds06_drift_guard_test.exs` no longer wraps the ratchet assertion in `File.exists?` and no longer tags the ui.ex-zero test `:ui_ex_zero`
    - Behavior: the guard passes on the current (post-sweep) codebase — no regressions, no new violations
    - Test: `mix test test/scoria_web/ds06_drift_guard_test.exs` exits 0 (full guard, no excludes)
    - Test: `mix test` exits 0 (full suite green — the guard is wired into the default run)
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/ds06_drift_guard_test.exs && mix test</automated>
  </verify>
  <done>The DS-06 baseline is generated from the post-sweep codebase and committed; ui.ex + the proof adapter are excluded/zeroed; the ratchet guard runs by default in `mix test` and passes — raw palette can no longer grow or return under lib/scoria_web/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator action → approvals LiveView | Approval decision triggers a server-held toast carrying a status message |
| Test scanner → filesystem | The DS-06 guard reads project source + baseline at test time (read-only) |
| Evidence data → notebook adapter | Approval evidence map interpolated into the notebook tab body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-13 | Information disclosure / XSS | toast message, notebook adapter approval content | mitigate | Toast `message` and adapter approval fields rendered via auto-escaped HEEx `{...}`; no `raw/1` on user/runtime-derived content. The error message reuses the existing `approval_error_message/2` (already safe, already used by put_flash). |
| T-12-14 | Tampering | DS-06 ratchet guard activation | mitigate | Baseline generated mechanically by the scanner (not hand-written), excludes only files proven at zero; guard scans `.ex` + `.heex`, runs in default `mix test`, asserts ui.ex at zero. A developer cannot grow raw palette without failing CI. |
| T-12-15 | Spoofing/Elevation | approvals decision path | accept | Toast is purely presentational; it does not change authorization. The decision authorization logic is unchanged by this plan (existing put_flash path retained). |
| T-12-16 | Tampering | npm/pip/cargo installs | accept | No package installs this phase. |
</threat_model>

<verification>
- `mix test test/scoria_web/live/approvals_live_test.exs test/scoria_web/live/workflow_live_test.exs` exits 0 (real toast + real skeleton).
- `mix test test/scoria_web/ui_component_test.exs` exits 0 (notebook proof adapter).
- remote_invocation component grep count is 0.
- `mix test test/scoria_web/ds06_drift_guard_test.exs` exits 0 (full guard, baseline committed, ui.ex + adapter excluded).
- `mix test` exits 0 (full suite green; guard wired into default run).
</verification>

<success_criteria>
- One real server-triggered toast renders in the approvals view after a decision.
- One real `<.skeleton>` replaces bespoke async loading on workflow show.
- RemoteInvocationEvidenceComponent renders through `<.notebook>` at zero raw palette (DS-04 proof).
- The DS-06 ratchet baseline is committed and the guard passes by default — raw palette cannot grow or return.
</success_criteria>

<output>
Create `.planning/phases/12-design-system-component-layer/12-05-SUMMARY.md` when done.
</output>
