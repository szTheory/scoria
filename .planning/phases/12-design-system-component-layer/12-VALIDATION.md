---
phase: 12
slug: design-system-component-layer
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-04
---

# Phase 12 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | ExUnit (built into Elixir) |
| **Config file** | `test/test_helper.exs` (existing — no new config needed) |
| **Quick run command** | `mix test test/scoria_web/` |
| **Full suite command** | `mix test` |
| **Estimated runtime** | ~30 seconds (web subset); ~90s full suite |

---

## Sampling Rate

- **After every task commit:** Run `mix test test/scoria_web/`
- **After every plan wave:** Run `mix test`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

> Task IDs are provisional until plans are written; the planner finalizes them. Each DS requirement maps to a `render_component` unit test (component APIs) or a `render(view) =~` integration assertion (real-wired paths), plus the DS-06 guard test.

| Requirement | Behavior | Test Type | Automated Command | File Exists |
|-------------|----------|-----------|-------------------|-------------|
| DS-01 | `<.table>` renders headers, rows, empty state; sort indicator; density modifier | unit (`render_component`) | `mix test test/scoria_web/ui_component_test.exs` | ❌ W0 |
| DS-02 | `<.drawer>`/`<.modal>` render shells, dismiss button w/ aria-label, hidden when `show={false}` | unit | same | ❌ W0 |
| DS-03 | `<.field>` renders label + help + error; required asterisk + visually-hidden span | unit | same | ❌ W0 |
| DS-04 | `<.notebook>` renders tab bar + active indicator; proof adapter wraps `RemoteInvocationEvidenceComponent` | unit | same | ❌ W0 |
| DS-05 | `flash_group` renders `scoria-flash--fail/info` by **string** kind; `<.skeleton>` + `<.toast>` render | unit | same | ❌ W0 |
| DS-05 | Real toast wiring: action handler renders toast in approvals view | integration | `mix test test/scoria_web/live/approvals_live_test.exs` | ✅ (add assertion) |
| DS-05 | Real skeleton wiring: async loading shows skeleton | integration | `mix test test/scoria_web/live/workflow_live_test.exs` | ✅ (add assertion) |
| DS-06 | Drift guard passes on clean codebase; baseline loads; `ui.ex` at zero | unit | `mix test test/scoria_web/ds06_drift_guard_test.exs` | ❌ W0 |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky — tracked per-task in plans.*

---

## Wave 0 Requirements

- [ ] `test/scoria_web/ui_component_test.exs` — `render_component` coverage for DS-01..DS-05 component APIs
- [ ] `test/scoria_web/ds06_drift_guard_test.exs` — ratchet-baseline drift guard for DS-06
- [ ] `test/support/ds06_baseline.txt` — generated by running the scanner once, then committed
- [ ] Framework already installed (ExUnit) — no install step needed

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Toast auto-dismiss after N ms | DS-05 | `phx-mounted={JS.hide(... time:)}` timer fires client-side; invisible to LiveViewTest (per D-11) | Boot dashboard, trigger a real toast action, observe toast fades after duration |
| Drawer/modal open/dismiss animation | DS-02 | `Phoenix.LiveView.JS` transitions render in markup but visual motion is browser-only | Boot dashboard, open a drawer/modal, observe slide/pop + scrim fade |

*All component structure, accessibility attributes, and the DS-06 guard have automated verification; only client-side animation timing is manual.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (ui_component_test, ds06_drift_guard_test, ds06_baseline.txt)
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
