---
phase: 12-design-system-component-layer
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - lib/scoria_web/ui.ex
  - test/scoria_web/ui_component_test.exs
autonomous: true
requirements: [DS-02, DS-03]
nyquist_compliant: true

must_haves:
  truths:
    - "Drawers and modals render through shared slot-based shells with a consistent dismiss contract (close button + scrim + Escape)"
    - "Both overlays render nothing when show={false} and render the panel when show={true}"
    - "Forms render through a shared <.field> with consistent label, help text, and validation-error display (error never by color alone)"
  artifacts:
    - path: "lib/scoria_web/ui.ex"
      provides: "drawer/1, modal/1, field/1, form_section/1"
      contains: "def drawer("
    - path: "test/scoria_web/ui_component_test.exs"
      provides: "render_component assertions for drawer, modal, field, form_section"
      contains: "Close dialog"
  key_links:
    - from: "lib/scoria_web/ui.ex modal/1"
      to: "parent LiveView dismiss handler"
      via: "phx-click={@on_dismiss} on scrim + close button; phx-window-keydown={@on_dismiss} phx-key=\"Escape\""
      pattern: "phx-window-keydown"
    - from: "lib/scoria_web/ui.ex field/1"
      to: "caller-provided input element"
      via: "render_slot(@inner_block) inside label/error/help wrapper"
      pattern: "render_slot\\(@inner_block\\)"
---

<objective>
Add the overlay and form contracts to `ui.ex`. (1) `<.drawer>` and `<.modal>` (DS-02): slot-based shells wrapping the existing `.scoria-scrim` / `.scoria-modal__panel` / `.scoria-drawer` CSS, with a consistent triple dismiss contract (close button + scrim click + Escape key) and server-owned `show`. (2) `<.field>` + `<.form_section>` (DS-03): a shared form-control wrapper that renders label + caller-provided input slot + help text + validation error, with the error surfaced via icon + text (never by color alone).

Purpose: Drawers/modals are duplicated bespoke markup across many screens; the shared shells standardize open/dismiss so Phase 14/15 conversions become "call a component". The `<.field>` wrapper standardizes the eval and prompt editors' form controls.
Output: Updated `lib/scoria_web/ui.ex` (drawer, modal, field, form_section); real `render_component` assertions in `ui_component_test.exs`.

This plan owns `lib/scoria_web/ui.ex` exclusively for wave 3 — it runs after plan 12-02 (wave 2) and before plan 12-04 (wave 4), never concurrently, because all three edit the same file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-design-system-component-layer/12-UI-SPEC.md
@.planning/phases/12-design-system-component-layer/12-PATTERNS.md
@.planning/phases/12-design-system-component-layer/12-CONTEXT.md
@lib/scoria_web/ui.ex

<interfaces>
<!-- ui.ex now contains (after plans 12-02): alias Phoenix.LiveView.JS, fixed flash_group, table/1. -->
<!-- Copy panel/1's named-optional-slot + `:if={@slot != []}` guard pattern (PATTERNS.md 70-99). -->
<!-- Existing CSS (plan 12-01 added headers/footers): .scoria-scrim, .scoria-modal, .scoria-modal__panel, -->
<!--   .scoria-modal__header, .scoria-modal__footer, .scoria-drawer, .scoria-drawer__header, -->
<!--   .scoria-field, .scoria-field__label/__error/__help, .scoria-form-section, .scoria-input. -->
<!-- Keyboard dismiss analog (existing inline modal): workflow_live/show.ex line ~239 -->
<!--   `phx-window-keydown="close_modal" phx-key="escape"` — new components use @on_dismiss + uppercase "Escape". -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add <.drawer> and <.modal> slot-based overlay shells (DS-02)</name>
  <files>lib/scoria_web/ui.ex, test/scoria_web/ui_component_test.exs</files>
  <behavior>
    - <.modal> with show={true} renders the .scoria-modal__panel with role="dialog" aria-modal="true"
    - <.modal>/<.drawer> with show={false} renders nothing (no panel, no scrim)
    - The modal close button has aria-label="Close dialog"; the drawer close button label is "Close drawer"
    - Scrim and Escape both bind phx-click / phx-window-keydown to @on_dismiss
    - <:footer> renders only when provided; <:eyebrow>/<:title>/<:actions> guarded by :if
  </behavior>
  <read_first>
    - lib/scoria_web/ui.ex (read panel/1's named-slot + guard pattern; file already has table/1, flash fix, JS alias)
    - .planning/phases/12-design-system-component-layer/12-UI-SPEC.md (DS-02 lines 184-234: drawer attrs/slots/animation/dismiss/z-index; modal attrs/slots/animation/dismiss/footer layout; Interaction Contracts lines 457-469 open/dismiss + focus)
    - .planning/phases/12-design-system-component-layer/12-PATTERNS.md (lines 70-99 panel slot guards; lines 807-825 phx-window-keydown + uppercase "Escape")
    - .planning/phases/12-design-system-component-layer/12-RESEARCH.md (Pattern 2 lines 243-279 conditional-show via server assign; Open Question 2 lines 769-772 autofocus over JS hook; Pitfall 1 lines 543-548 window-keydown scope)
    - lib/scoria_web/live/workflow_live/show.ex (existing inline modal ~line 239 — the keyboard-dismiss analog)
  </read_first>
  <action>
    Add `def modal/1` and `def drawer/1` to `ui.ex`, copying panel/1's slot-guard pattern.
    `<.modal>`: attrs `id` (req), `show` (boolean, req), `on_dismiss` (string, req), `title` (default nil), `max_width` (default "560px"), `rest :global`; slots `:title`, `:inner_block` (required), `:footer`. Render `<div :if={@show} id={@id} class="scoria-modal" phx-window-keydown={@on_dismiss} phx-key="Escape">` containing `<div class="scoria-scrim" phx-click={@on_dismiss} aria-hidden="true"></div>` and `<div class="scoria-modal__panel" role="dialog" aria-modal="true" style={"max-width: #{@max_width}"}>`. Panel header `.scoria-modal__header`: optional `<:title>` (or `@title`) + an X-icon close `<button autofocus phx-click={@on_dismiss} class="scoria-button scoria-button--ghost scoria-button--sm" aria-label="Close dialog" title="Close dialog">` with a 16×16 X SVG. Body = `render_slot(@inner_block)`. Footer `.scoria-modal__footer` rendered `:if={@footer != []}`. Use `autofocus` on the close button (NOT a JS hook — hooks are no-ops in LiveViewTest).
    `<.drawer>`: attrs `id` (req), `show` (boolean, req), `on_dismiss` (string, req), `title` (default nil), `rest :global`; slots `:eyebrow`, `:title`, `:actions`, `:inner_block` (required). Render `<div :if={@show} id={@id} ...>` with `<div class="scoria-scrim" phx-click={@on_dismiss} phx-window-keydown={@on_dismiss} phx-key="Escape" aria-hidden="true"></div>` and `<aside class="scoria-drawer" role="dialog" aria-modal="true">`. Header `.scoria-drawer__header`: `<:eyebrow>`/`<:title>` guarded by `:if={@slot != []}`, `<:actions>` guarded, plus a "Close drawer" ghost button (`.scoria-button--ghost.scoria-button--sm`, label text "Close drawer") emitting `phx-click={@on_dismiss}`. Body = `render_slot(@inner_block)`.
    Emit only semantic class names (ui.ex stays at zero raw palette). Use uppercase `"Escape"` (not the existing lowercase `"escape"` analog).
    In `ui_component_test.exs`, add DS-02 tests (module-reference render_component form): modal `show: true` renders `role="dialog"` and `aria-label="Close dialog"`; modal `show: false` renders empty/no panel; drawer renders "Close drawer"; scrim/Escape bindings reference the on_dismiss event name; `<:footer>` content appears only when passed.
  </action>
  <acceptance_criteria>
    - Source: `ui.ex` contains `def modal(` and `def drawer(`
    - Behavior: modal `show: true` renders `role="dialog"` and `aria-modal="true"`
    - Behavior: modal close button renders `aria-label="Close dialog"`
    - Behavior: modal/drawer `show: false` renders no `.scoria-modal__panel` / `.scoria-drawer`
    - Behavior: drawer renders the literal "Close drawer"
    - Source: `ui.ex` contains `phx-window-keydown={@on_dismiss}` and `phx-key="Escape"`
    - Source: ui.ex still scans to zero raw palette (grep count 0)
    - Test: `mix test test/scoria_web/ui_component_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/ui_component_test.exs && test "$(grep -cE '\b(stone|rose|sky|emerald|amber|blue|gray|slate|zinc|neutral|red|green|yellow|purple|pink|indigo|teal|cyan|lime|orange|violet|fuchsia)-[0-9]' lib/scoria_web/ui.ex)" = "0"</automated>
  </verify>
  <done>`<.modal>` and `<.drawer>` render slot-based shells with role="dialog", the consistent triple dismiss contract (close button + scrim + Escape), server-owned show, and guarded optional slots; DS-02 unit tests pass; ui.ex stays at zero raw palette.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add <.field> and <.form_section> form controls (DS-03)</name>
  <files>lib/scoria_web/ui.ex, test/scoria_web/ui_component_test.exs</files>
  <behavior>
    - <.field> renders a <label for={@id}> with the label text
    - required={true} renders the "*" asterisk (aria-hidden) AND a visually-hidden "(required)" span
    - help text renders below the input slot when @help is set
    - error renders below with an exclamation icon (12×12) when @error is set; never error by color alone
    - <:inner_block> (the caller's <input>/<select>) is rendered inside the field wrapper
    - <.form_section> renders its title and optional description above the field stack
  </behavior>
  <read_first>
    - lib/scoria_web/ui.ex (read badge/1 attr pattern; file already has table, modal, drawer)
    - .planning/phases/12-design-system-component-layer/12-UI-SPEC.md (DS-03 lines 237-277: field attrs, label contract, input contract, validation error display "exclamation icon... never error by color alone", help text, inner_block slot rule; form_section attrs; Copywriting lines 422-425 required suffix + screen-reader "(required)" + validation copy)
    - .planning/phases/12-design-system-component-layer/12-PATTERNS.md (badge attr-list pattern; field CSS classes added in plan 12-01)
  </read_first>
  <action>
    Add `def field/1` to `ui.ex`: attrs `id` (string, req), `label` (string, req), `help` (string, default nil), `error` (string, default nil), `required` (boolean, default false), `rest :global`; `slot :inner_block, required: true`. Render `<div class="scoria-field">` containing `<label for={@id} class="scoria-field__label">{@label}<span :if={@required} class="scoria-field__required" aria-hidden="true" style="color: var(--scoria-danger-action)">*</span><span :if={@required} class="sr-only">(required)</span></label>`, then `render_slot(@inner_block)` (caller provides the `<input>`/`<select>`/`<textarea>` styled with `scoria-input` — the wrapper does NOT generate the input), then `<p :if={@help} class="scoria-field__help">{@help}</p>` and `<p :if={@error} class="scoria-field__error">` with a 12×12 inline exclamation SVG icon followed by `{@error}` (a11y: error never by color alone). Use the visually-hidden helper class `sr-only` if present in the CSS, otherwise a `class="scoria-sr-only"` already used elsewhere — confirm by grep before choosing.
    Add `def form_section/1`: attrs `title` (string, req), `description` (string, default nil), `rest :global`; `slot :inner_block, required: true`. Render `<section class="scoria-form-section">` with an `<h3>`/heading from `@title`, an optional `<p :if={@description}>`, then `render_slot(@inner_block)`.
    Emit only semantic class names. Do NOT introduce `--scoria-fs-badge` in HEEx (use the CSS classes which bind tokens).
    In `ui_component_test.exs`, add DS-03 tests (function-capture or module form): `<.field id="email" label="Email" required>` renders `for="email"`, the asterisk, and a visually-hidden "(required)"; `error: "Email is required."` renders the error text and an svg icon; `help: "We never share it."` renders the help text; the caller's inner_block `<input>` is rendered; `<.form_section title="Contact" description="...">` renders both.
  </action>
  <acceptance_criteria>
    - Source: `ui.ex` contains `def field(` and `def form_section(`
    - Behavior: `<.field>` renders `<label for={id}>` with the label text
    - Behavior: `required: true` renders both the aria-hidden `*` and a visually-hidden "(required)" span
    - Behavior: `error: "..."` renders the error text AND an inline svg (icon present so status is not color-alone)
    - Behavior: the caller's `<:inner_block>` `<input>` is rendered inside the field
    - Behavior: `<.form_section>` renders its title and (when set) description
    - Source: ui.ex still scans to zero raw palette (grep count 0)
    - Test: `mix test test/scoria_web/ui_component_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/ui_component_test.exs && test "$(grep -cE '\b(stone|rose|sky|emerald|amber|blue|gray|slate|zinc|neutral|red|green|yellow|purple|pink|indigo|teal|cyan|lime|orange|violet|fuchsia)-[0-9]' lib/scoria_web/ui.ex)" = "0"</automated>
  </verify>
  <done>`<.field>` renders consistent label + caller input slot + help + icon-bearing error (never color-alone) with required a11y affordances; `<.form_section>` groups fields under a heading; DS-03 unit tests pass; ui.ex stays at zero raw palette.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller LiveView → field/modal | Caller-provided label, help, error, and slot content are interpolated into HEEx |
| Browser → parent LiveView | dismiss (scrim/Escape/button) events cross client→server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Information disclosure / XSS | field error/help/label, modal title, slot bodies | mitigate | All caller strings rendered via auto-escaped HEEx `{...}`. No `raw/1` on `@error`, `@help`, `@label`, `@title`. Icons are static SVG literals. |
| T-12-08 | Tampering | ui.ex gateway self-consistency | mitigate | ui.ex zero-palette grep asserted per task. |
| T-12-09 | Denial of service | phx-window-keydown global scope | accept | Documented (Pitfall 1): binding present only when `show={true}` via `:if`; acceptable for internal operator UI. |
</threat_model>

<verification>
- `mix test test/scoria_web/ui_component_test.exs` exits 0 (drawer/modal/field/form_section assertions).
- ui.ex raw-palette grep count is 0.
- `mix test test/scoria_web/` exits 0 (no web-subset regression).
</verification>

<success_criteria>
- `<.drawer>` and `<.modal>` render shared slot-based shells with a consistent dismiss contract and correct aria attributes.
- Both overlays render nothing when `show={false}`.
- `<.field>` renders consistent label/help/error with error surfaced via icon + text; `<.form_section>` groups fields.
- ui.ex remains at zero raw palette.
</success_criteria>

<output>
Create `.planning/phases/12-design-system-component-layer/12-03-SUMMARY.md` when done.
</output>
