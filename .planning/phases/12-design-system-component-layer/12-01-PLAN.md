---
phase: 12-design-system-component-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/css/04-components.css
  - assets/css/05-motion.css
  - test/scoria_web/ui_component_test.exs
  - test/scoria_web/ds06_drift_guard_test.exs
autonomous: true
requirements: [DS-01, DS-02, DS-03, DS-04, DS-05, DS-06]
nyquist_compliant: true

must_haves:
  truths:
    - "All net-new CSS classes the Phase 12 components bind to exist in 04-components.css"
    - "The skeleton-pulse keyframe exists and is suppressed by the existing prefers-reduced-motion block"
    - "The DS-06 ratchet drift-guard test module exists and reads test/support/ds06_baseline.txt"
    - "The ui_component_test scaffold exists with the shared test header so later component tasks add assertions, not boilerplate"
  artifacts:
    - path: "assets/css/04-components.css"
      provides: ".scoria-flash*, .scoria-skeleton*, .scoria-toast*, .scoria-notebook*, .scoria-table--compact/--comfortable, .scoria-modal__header/__footer, .scoria-drawer__header, .scoria-field*, .scoria-form-section, .scoria-toast-region"
      contains: ".scoria-flash--fail"
    - path: "assets/css/05-motion.css"
      provides: "scoria-skeleton-pulse keyframe"
      contains: "scoria-skeleton-pulse"
    - path: "test/scoria_web/ds06_drift_guard_test.exs"
      provides: "DS-06 ratchet baseline guard"
      contains: "Path.wildcard"
    - path: "test/scoria_web/ui_component_test.exs"
      provides: "render_component test scaffold for all 8 components + flash_group"
      contains: "import Phoenix.LiveViewTest"
  key_links:
    - from: "test/scoria_web/ds06_drift_guard_test.exs"
      to: "test/support/ds06_baseline.txt"
      via: "File.read! at test time"
      pattern: "ds06_baseline"
    - from: "assets/css/05-motion.css"
      to: "prefers-reduced-motion block"
      via: "existing unlayered .scoria-root * animation suppression"
      pattern: "prefers-reduced-motion"
---

<objective>
Lay the CSS and test foundation for the Phase 12 component layer BEFORE any `ui.ex` component is written. This plan touches zero Elixir component code — it adds every net-new CSS class the components will bind to, the one new motion keyframe, and the two test scaffolds (DS-06 ratchet guard + `render_component` test module).

This is Wave 0 of the Nyquist validation contract: it stands up the test files so every downstream component task has an `<automated>` verify command to extend rather than create.

Purpose: Components are thin HEEx wrappers over CSS. The CSS must exist first or the components emit class names that resolve to nothing. The DS-06 guard and component test module must exist first so component plans add assertions instead of scaffolding.
Output: `04-components.css` (net-new classes), `05-motion.css` (skeleton keyframe), `ds06_drift_guard_test.exs`, `ui_component_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-design-system-component-layer/12-UI-SPEC.md
@.planning/phases/12-design-system-component-layer/12-PATTERNS.md
@.planning/phases/12-design-system-component-layer/12-CONTEXT.md

<interfaces>
<!-- Net-new CSS classes are fully specified in UI-SPEC DS-01..DS-05 and PATTERNS.md. -->
<!-- Existing tone-token triplets (color/background/border-color) to copy verbatim live at -->
<!-- 04-components.css lines ~226-232 (.scoria-badge--{tone}). The drift-guard regex and -->
<!-- ds06_drift_guard_test.exs full module body are given verbatim in PATTERNS.md lines 669-718. -->
<!-- The ui_drift_guard_test.exs analog (file-walk + assert structure) lives at test/scoria_web/ui_drift_guard_test.exs. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add net-new component CSS to 04-components.css and the skeleton keyframe to 05-motion.css</name>
  <files>assets/css/04-components.css, assets/css/05-motion.css</files>
  <read_first>
    - assets/css/04-components.css (read the existing `.scoria-badge--{tone}` tone-modifier block ~lines 226-232, the `.scoria-table` block ~lines 271-290, `.scoria-modal__panel` ~lines 308-318, `.scoria-drawer` ~lines 319-325 — these are the exact analogs to extend)
    - assets/css/05-motion.css (read the existing `scoria-approval-pulse` keyframe ~lines 19-22 and the unlayered `prefers-reduced-motion` block ~lines 37-46)
    - .planning/phases/12-design-system-component-layer/12-UI-SPEC.md (DS-01 density class map lines 177-181; DS-03 field CSS contract lines 251-264; DS-04 notebook frame lines 296-318; DS-05 `.scoria-flash` CSS lines 354-369, `.scoria-toast` lines 382-388, `.scoria-skeleton` lines 333-335)
    - .planning/phases/12-design-system-component-layer/12-PATTERNS.md (lines 207-341 — exact CSS to add for flash, drawer/modal header/footer, table density, skeleton keyframe)
  </read_first>
  <action>
    Append net-new classes to `assets/css/04-components.css`, copying the existing `.scoria-badge--{tone}` three-property triplet pattern (`color` / `background` / `border-color` using `--scoria-tone-{tone}-{fg,bg,border}`) for every tone variant:
    - Table density: `.scoria-table--compact tbody td { padding: var(--scoria-space-2); }` and `.scoria-table--comfortable tbody td { padding: var(--scoria-space-4); }`. Default density is the existing `var(--scoria-space-3)` (12px) — do NOT change it (locked token exception).
    - Modal: `.scoria-modal__header` (flex, space-between, `margin-bottom: var(--scoria-space-4)`) and `.scoria-modal__footer` (`flex justify-end gap: var(--scoria-space-2)`).
    - Drawer: `.scoria-drawer__header` (flex, space-between, `gap: var(--scoria-space-3)`, `margin-bottom: var(--scoria-space-4)`).
    - Field: `.scoria-field` (column stack), `.scoria-field__label` (`font-size: var(--scoria-fs-label)`, weight 600), `.scoria-field__error` (`color: var(--scoria-danger-action)`, `font-size: var(--scoria-fs-label)`, `margin-top: var(--scoria-space-1)`), `.scoria-field__help` (`color: var(--scoria-text-subtle)`, `font-size: var(--scoria-fs-label)`). `.scoria-form-section` (heading + description + field stack).
    - Notebook: `.scoria-notebook` (`border: 1px solid var(--scoria-border)`, `border-radius: var(--scoria-radius-lg)`, `background: var(--scoria-surface-panel)`), `.scoria-notebook__tabbar` (border-bottom), `.scoria-notebook__tab` (active = bottom border `2px solid var(--scoria-action)`, `font-size: var(--scoria-fs-body)`, weight 600), `.scoria-notebook__panel` (`background: var(--scoria-surface-panel-raised)`, `padding: var(--scoria-space-4)`).
    - Flash: `.scoria-flash` base (neutral tone tokens, `border-radius: var(--scoria-radius-md)`, `padding: var(--scoria-space-2) var(--scoria-space-4)`, `font-size: var(--scoria-fs-body)`, `margin-bottom: var(--scoria-space-4)`, `display: flex`, `align-items: flex-start`, `gap: var(--scoria-space-2)`) plus `.scoria-flash--fail/--info/--warn/--pass` tone triplets — copy exactly from PATTERNS.md lines 228-243.
    - Skeleton: `.scoria-skeleton` (`background: var(--scoria-tone-neutral-bg)`, `border-radius: var(--scoria-radius-sm)`, `animation: scoria-skeleton-pulse 1.5s ease-in-out infinite`) and `.scoria-skeleton--text` (`height: 1em`).
    - Toast: `.scoria-toast` (`position: fixed`, `bottom: var(--scoria-space-5)`, `right: var(--scoria-space-5)`, `z-index: var(--scoria-z-toast)`, `min-width: 280px`, `max-width: 400px`, `box-shadow: var(--scoria-shadow-raised)`, `border-radius: var(--scoria-radius-md)`) plus `.scoria-toast--{tone}` triplets for pass/fail/warn/info/neutral, and `.scoria-toast-region` (fixed stacking container).
    Use only `--scoria-*` tokens — never a raw palette class (the DS-06 guard does not scan CSS, but the token-first posture is locked). Bind table default density, field input inline padding to `--scoria-space-3` per the locked exception; do NOT introduce `--scoria-fs-badge` in any new class except where the spec already maps it (`.raw_evidence` later — not this plan).
    In `assets/css/05-motion.css`, add the `@keyframes scoria-skeleton-pulse { 0%, 100% { opacity: 0.4; } 50% { opacity: 0.8; } }` keyframe inside the `@layer scoria.components` block immediately after `scoria-approval-pulse`. Add NO new `prefers-reduced-motion` media query — the existing unlayered `.scoria-root *` block already suppresses it.
  </action>
  <acceptance_criteria>
    - Source: `grep -q '.scoria-flash--fail' assets/css/04-components.css` succeeds
    - Source: `grep -q '.scoria-skeleton' assets/css/04-components.css` succeeds
    - Source: `grep -q '.scoria-toast--pass' assets/css/04-components.css` succeeds
    - Source: `grep -q '.scoria-notebook__tab' assets/css/04-components.css` succeeds
    - Source: `grep -q '.scoria-table--compact' assets/css/04-components.css` succeeds
    - Source: `grep -q '.scoria-field__error' assets/css/04-components.css` succeeds
    - Source: `grep -q 'scoria-skeleton-pulse' assets/css/05-motion.css` succeeds
    - Source: no raw hex color literal is introduced — `grep -nE '#[0-9a-fA-F]{3,6}' assets/css/04-components.css` shows no NEW lines beyond pre-existing ones (every new declaration uses `var(--scoria-...)`)
  </acceptance_criteria>
  <verify>
    <automated>mix compile 2>&1 | grep -qiv error && grep -q '.scoria-flash--fail' assets/css/04-components.css && grep -q 'scoria-skeleton-pulse' assets/css/05-motion.css</automated>
  </verify>
  <done>All net-new CSS classes from UI-SPEC DS-01..DS-05 exist in 04-components.css using only semantic tokens; the skeleton-pulse keyframe is in 05-motion.css; nothing raw-hex was introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Create the DS-06 ratchet drift-guard test (no baseline yet) and the ui_component_test scaffold</name>
  <files>test/scoria_web/ds06_drift_guard_test.exs, test/scoria_web/ui_component_test.exs</files>
  <read_first>
    - test/scoria_web/ui_drift_guard_test.exs (the exact file-walk + `Path.wildcard` + `assert offenders == []` analog to copy — module structure, `use ExUnit.Case, async: true`)
    - test/scoria_web/components/memory_notebook_component_test.exs (the `render_component` unit-test header analog: `use ExUnit.Case, async: true` + `import Phoenix.LiveViewTest`)
    - test/scoria_web/components/trace_tree_component_test.exs (the `render_component(Module, assigns)` module-reference form for slot-bearing components, ~line 15)
    - .planning/phases/12-design-system-component-layer/12-PATTERNS.md (lines 624-721 — the full DS06DriftGuardTest module body and the ui_component_test scaffold guidance; lines 596-602 module-vs-function-capture rule for slots)
    - .planning/phases/12-design-system-component-layer/12-RESEARCH.md (Pattern 4 lines 338-414 — guard mechanics; D-02 ratchet semantics; Pitfall 6 flash string-key bug)
    - .planning/phases/12-design-system-component-layer/12-VALIDATION.md (Per-Task Verification Map — which behaviors each test must eventually assert)
  </read_first>
  <action>
    Create `test/scoria_web/ds06_drift_guard_test.exs` as module `ScoriaWeb.DS06DriftGuardTest` (`use ExUnit.Case, async: true`), copying the module body from PATTERNS.md lines 669-718 verbatim. Define `@palette_regex ~r/\b(stone|rose|sky|emerald|amber|blue|gray|slate|zinc|neutral|red|green|yellow|purple|pink|indigo|teal|cyan|lime|orange|violet|fuchsia)-\d/` and `@excluded ~w(lib/scoria_web/ui.ex lib/scoria_web/components/remote_invocation_evidence_component.ex)`. The test walks `Path.wildcard("lib/scoria_web/**/*.{ex,heex}")` (brace expansion covers `.heex` — the original analog only scans `.ex`), counts `Regex.scan` matches per file, loads `test/support/ds06_baseline.txt` via `load_baseline/0`, and fails on `count > baseline_count` (`:regression`) OR `baseline_count == 0 and count > 0` (`:new_violation`). Include `format_failure/1` producing the multi-line message from the analog. Add a SECOND test in the same module asserting `ui.ex` is at zero: `assert ScoriaWeb.UI |> ... ` — concretely, read `lib/scoria_web/ui.ex`, assert `Regex.scan(@palette_regex, File.read!("lib/scoria_web/ui.ex")) == []`.
    NOTE on sequencing: `test/support/ds06_baseline.txt` does NOT exist yet (generated in plan 12-05 after the sweep). Guard `load_baseline/0` so that until the baseline exists the ratchet test is tagged `@tag :ds06_baseline_pending` and `@moduletag` is NOT used to skip the ui.ex-zero assertion — instead split: the ui.ex-zero assertion runs unconditionally; the ratchet-comparison test is tagged `@tag :pending_baseline` and excluded by default via a `test_helper.exs`-free mechanism: use `if File.exists?("test/support/ds06_baseline.txt")` inside the ratchet test body and `ExUnit.skip`/early-return is not available — instead make the ratchet test READ the baseline with a clear failure if missing. Simpler and required: in this plan, make `load_baseline/0` return `%{}` when the file is absent (so the ratchet test passes vacuously by treating every file as a new_violation? NO). Correct approach: in this plan the ratchet test must `@tag :skip_until_baseline` and be excluded; document in the test that plan 12-05 generates the baseline and removes the skip. Use `@tag :pending` on the ratchet test and add `ExUnit.configure(exclude: [:pending])` is global — instead use `@tag :ds06` and rely on plan 12-05 to commit the baseline + drop the tag. Pick the simplest mechanism that keeps `mix test` green now: wrap the ratchet assertion in `if File.exists?(@baseline_path) do ... else assert true end` and leave a `# TODO(12-05): remove File.exists? guard once baseline committed` comment. The ui.ex-zero assertion runs unconditionally and must pass (ui.ex is swept in plan 12-02).
    Create `test/scoria_web/ui_component_test.exs` as module `ScoriaWeb.UIComponentTest` (`use ExUnit.Case, async: true` + `import Phoenix.LiveViewTest`). In THIS plan it is a scaffold: include the module header, a leading doc comment listing the DS-01..DS-05 test groups that plans 12-02/03/04 will fill in, and ONE smoke test that does not depend on unbuilt components — assert the module compiles and `Phoenix.LiveViewTest` is imported by rendering nothing (a trivial `assert true` placeholder test named `"ui_component_test scaffold present"`). Downstream plans replace the placeholder with real `render_component` assertions. Use `render_component(ScoriaWeb.UI.table, assigns)` module/function rules from PATTERNS.md (slot-bearing components need the `render_component(&fun/1, assigns)` or a wrapper; pure-attr components use function capture).
  </action>
  <acceptance_criteria>
    - Source: `test/scoria_web/ds06_drift_guard_test.exs` contains `defmodule ScoriaWeb.DS06DriftGuardTest`
    - Source: ds06 test contains the literal regex alternation `stone|rose|sky|emerald|amber`
    - Source: ds06 test contains `Path.wildcard("lib/scoria_web/**/*.{ex,heex}")`
    - Source: ds06 test contains `@excluded` listing both `lib/scoria_web/ui.ex` and `lib/scoria_web/components/remote_invocation_evidence_component.ex`
    - Source: ds06 test contains an unconditional assertion that `lib/scoria_web/ui.ex` scans to zero palette matches
    - Source: `test/scoria_web/ui_component_test.exs` contains `import Phoenix.LiveViewTest` and `defmodule ScoriaWeb.UIComponentTest`
    - Test: `mix test test/scoria_web/ds06_drift_guard_test.exs test/scoria_web/ui_component_test.exs` exits 0 (the ui.ex-zero assertion will FAIL here because ui.ex still has 3 raw palette lines until plan 12-02 — therefore this plan's verify excludes the ui.ex-zero test via `--exclude` tag OR the ui.ex-zero assertion is itself tagged `@tag :ui_ex_zero` and excluded until 12-02). Tag the ui.ex-zero test `@tag :ui_ex_zero`; this plan's verify runs `mix test ... --exclude ui_ex_zero` and exits 0. Plan 12-02 removes nothing — the tag is dropped in 12-05 when the full guard goes green.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/ds06_drift_guard_test.exs test/scoria_web/ui_component_test.exs --exclude ui_ex_zero</automated>
  </verify>
  <done>Both test files exist and compile; the DS-06 guard module has the correct regex, excluded list, brace-expansion wildcard, and a ui.ex-zero assertion (tagged so it stays red until ui.ex is swept in 12-02); the ui_component_test scaffold compiles with the shared header and a placeholder test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS author → rendered DOM | New CSS classes are static, token-bound; no user input crosses here |
| Test scanner → filesystem | The drift guard reads project source files at test time (read-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | DS-06 drift guard | mitigate | Guard regex anchored with `\b...-\d` (PATTERNS.md verified); brace-expansion wildcard covers `.heex` so a developer cannot evade by moving raw palette into a template. ui.ex-zero asserted unconditionally so the gateway cannot silently regress. |
| T-12-02 | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH "No New Packages"); supply-chain surface absent. |
| T-12-03 | Information disclosure | new CSS classes | accept | Static token-bound CSS; no user data, no dynamic interpolation. |
</threat_model>

<verification>
- `mix compile` clean.
- `mix test test/scoria_web/ds06_drift_guard_test.exs test/scoria_web/ui_component_test.exs --exclude ui_ex_zero` exits 0.
- All net-new CSS classes present (grep checks in acceptance criteria).
- No raw hex / raw palette introduced in CSS (token-only).
</verification>

<success_criteria>
- Every CSS class the Phase 12 components emit exists and is token-bound.
- `scoria-skeleton-pulse` keyframe exists and is covered by the existing reduced-motion suppression.
- The DS-06 guard module exists with correct regex, exclusions, `.heex` coverage, and a ui.ex-zero assertion.
- The `ui_component_test` scaffold exists with the shared header so later plans add assertions only.
</success_criteria>

<output>
Create `.planning/phases/12-design-system-component-layer/12-01-SUMMARY.md` when done.
</output>
