---
phase: 12-design-system-component-layer
plan: 04
type: execute
wave: 4
depends_on: ["12-03"]
files_modified:
  - lib/scoria_web/ui.ex
  - test/scoria_web/ui_component_test.exs
autonomous: true
requirements: [DS-04, DS-05]
nyquist_compliant: true

must_haves:
  truths:
    - "Evidence panels can render through one unified <.notebook> shell with a tabbed slot API"
    - "Loading feedback can use a shared <.skeleton> component with aria-label=\"Loading…\" that respects prefers-reduced-motion"
    - "Transient feedback can use a shared <.toast> component driven by a server assign with phx-mounted auto-dismiss"
  artifacts:
    - path: "lib/scoria_web/ui.ex"
      provides: "notebook/1 with :tab/:empty slots, raw_evidence/1, skeleton/1, toast/1"
      contains: "def notebook("
    - path: "test/scoria_web/ui_component_test.exs"
      provides: "render_component assertions for notebook, skeleton, toast"
      contains: "scoria-toast--pass"
  key_links:
    - from: "lib/scoria_web/ui.ex notebook/1 tab button"
      to: "parent LiveView selected_tab assign"
      via: "phx-click={@on_tab_change} phx-value-tab={key}"
      pattern: "phx-value-tab"
    - from: "lib/scoria_web/ui.ex toast/1"
      to: "auto-dismiss"
      via: "phx-mounted={JS.hide(transition: ..., time: @duration_ms)}"
      pattern: "phx-mounted"
---

<objective>
Complete the `ui.ex` component set with the three remaining contracts. (1) `<.notebook>` (DS-04): the unified tabbed evidence shell — `<:tab>` slots with `key`/`label`, active-tab indicator, empty state, plus the `<.raw_evidence>` `<details>` sub-component. (2) `<.skeleton>` (DS-05): a token-bound loading placeholder with `aria-label="Loading…"` that obeys `prefers-reduced-motion`. (3) `<.toast>` (DS-05): a transient notification driven by a server `@toasts` assign, auto-dismissing via `phx-mounted={JS.hide(... time:)}` — NOT a JS hook (hooks are no-ops in LiveViewTest).

Purpose: The `<.notebook>` shell's slot/tab contract is the contract Phase 15 will convert all 13 evidence components onto; building it here (and proving it on one adapter in plan 12-05) validates the API. The skeleton/toast complete the DS-05 loading/transient-feedback set.
Output: Updated `lib/scoria_web/ui.ex` (notebook, raw_evidence, skeleton, toast); real `render_component` assertions in `ui_component_test.exs`.

This plan owns `lib/scoria_web/ui.ex` exclusively for wave 4 — it runs after plan 12-03 (wave 3). All `ui.ex`-editing plans are serialized by single-file ownership.

Decisions addressed: D-06: (build only the unified `<.notebook>` shell per UI-SPEC DS-04 — the 13-component bulk conversion is Phase 15, out of scope here).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-design-system-component-layer/12-UI-SPEC.md
@.planning/phases/12-design-system-component-layer/12-PATTERNS.md
@.planning/phases/12-design-system-component-layer/12-CONTEXT.md
@lib/scoria_web/ui.ex

<interfaces>
<!-- ui.ex now contains (after plans 12-02, 12-03): JS alias, flash fix, table, modal, drawer, field, form_section. -->
<!-- Copy table/1's slot-with-typed-attrs pattern for notebook :tab (key/label). -->
<!-- CSS classes (plan 12-01): .scoria-notebook, .scoria-notebook__tabbar/__tab/__panel, -->
<!--   .scoria-skeleton, .scoria-skeleton--text, .scoria-toast, .scoria-toast--{tone}, .scoria-toast-region. -->
<!-- Keyframe scoria-skeleton-pulse exists in 05-motion.css; reduced-motion suppression is automatic. -->
<!-- alias Phoenix.LiveView.JS is already in the module header (plan 12-02). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add <.notebook> shell + <.raw_evidence> sub-component (DS-04 shell only)</name>
  <files>lib/scoria_web/ui.ex, test/scoria_web/ui_component_test.exs</files>
  <behavior>
    - <.notebook> renders a <nav role="tablist"> with one <button role="tab"> per <:tab> slot
    - The tab whose key == @selected_tab carries aria-selected="true" and the active indicator; others aria-selected="false"
    - Clicking a tab emits phx-click={@on_tab_change} phx-value-tab={key}
    - empty={true} renders the <:empty> body instead of tabs/panels
    - The panel has role="tabpanel"
    - <.raw_evidence label="..."> renders a <details>/<summary> with a <pre> code block
  </behavior>
  <read_first>
    - lib/scoria_web/ui.ex (read table/1's typed-slot pattern — reuse it for :tab; file already has table/modal/drawer/field)
    - .planning/phases/12-design-system-component-layer/12-UI-SPEC.md (DS-04 lines 279-318: notebook attrs, <:tab>/<:empty> slots, tab bar contract, panel body, frame, the 7-adapter tab map, raw_evidence sub-component; Interaction Contracts lines 471-475 tab switching aria)
    - .planning/phases/12-design-system-component-layer/12-PATTERNS.md (lines 162-179 slot-with-typed-attrs; lines 70-99 slot guards)
    - .planning/phases/12-design-system-component-layer/12-RESEARCH.md (Pattern 1 slot-with-attrs; Pitfall 2 render_slot nil guard for optional <:empty>)
  </read_first>
  <action>
    Add `def notebook/1` to `ui.ex`: attrs `id` (req), `title` (string, req), `eyebrow` (string, default nil), `empty` (boolean, default false), `selected_tab` (string, default nil), `on_tab_change` (string, default nil), `rest :global`; typed `slot :tab do attr :key, :string, required: true; attr :label, :string, required: true end`; `slot :empty`. Render `<div id={@id} class="scoria-notebook" {@rest}>` with a header (`@eyebrow` eyebrow + `@title` heading). When `@empty`, render `render_slot(@empty)` inside the panel area. Otherwise render `<nav class="scoria-notebook__tabbar" role="tablist">` with `<button :for={tab <- @tab} role="tab" id={"#{@id}-tab-#{tab.key}"} aria-selected={to_string(tab.key == @selected_tab)} class={["scoria-notebook__tab", tab.key == @selected_tab && "scoria-notebook__tab--active"]} phx-click={@on_tab_change} phx-value-tab={tab.key}>{tab.label}</button>`, then for the active tab render `<div role="tabpanel" class="scoria-notebook__panel" aria-labelledby={"#{@id}-tab-#{tab.key}"}>{render_slot(tab)}</div>` (render the panel for the tab whose key matches `@selected_tab`; if `@selected_tab` is nil, default to the first tab). Guard against nil/empty slot lists.
    Add `def raw_evidence/1`: attr `label` (string, default "Advanced raw evidence"), `slot :inner_block, required: true`. Render `<details class="scoria-raw-evidence"><summary>{@label}</summary><pre class="scoria-raw-evidence__pre">{render_slot(@inner_block)}</pre></details>`. The `<pre>` binds `--scoria-font-mono` / `--scoria-fs-badge` via its CSS class — do NOT set those tokens in HEEx.
    Emit only semantic class names. If `.scoria-raw-evidence*` classes are not yet in CSS, add minimal token-bound rules to `04-components.css` in this task (background `--scoria-surface-sunken`, mono font via existing class binding) — but prefer reusing existing `.scoria-input`/sunken patterns; confirm by grep first.
    In `ui_component_test.exs`, add DS-04 tests (module-reference render_component for the slot-bearing notebook): two `<:tab>` slots with `selected_tab` set; assert `role="tablist"`, one `role="tab"` per tab, the active tab `aria-selected="true"`, `phx-value-tab` on the buttons, `role="tabpanel"` present; assert `empty: true` renders the `<:empty>` body; assert `<.raw_evidence>` renders `<details>` + `<summary>` + `<pre>`.
  </action>
  <acceptance_criteria>
    - Source: `ui.ex` contains `def notebook(` and `slot :tab`
    - Source: `ui.ex` contains `def raw_evidence(`
    - Behavior: `<.notebook>` renders `role="tablist"` and one `role="tab"` button per `<:tab>`
    - Behavior: the tab matching `@selected_tab` renders `aria-selected="true"`; others `"false"`
    - Behavior: tab buttons emit `phx-value-tab` with the tab key
    - Behavior: `empty: true` renders the `<:empty>` content and no tab buttons
    - Behavior: `<.raw_evidence>` renders `<details>` + `<summary>` + `<pre>`
    - Source: ui.ex still scans to zero raw palette (grep count 0)
    - Test: `mix test test/scoria_web/ui_component_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/ui_component_test.exs && test "$(grep -cE '\b(stone|rose|sky|emerald|amber|blue|gray|slate|zinc|neutral|red|green|yellow|purple|pink|indigo|teal|cyan|lime|orange|violet|fuchsia)-[0-9]' lib/scoria_web/ui.ex)" = "0"</automated>
  </verify>
  <done>`<.notebook>` renders a tabbed slot-based shell with correct aria-selected/tablist/tabpanel roles, emits tab-change events, supports an empty state, and `<.raw_evidence>` renders the standardized details/pre block; DS-04 shell unit tests pass; ui.ex stays at zero raw palette.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add <.skeleton> and <.toast> components (DS-05)</name>
  <files>lib/scoria_web/ui.ex, test/scoria_web/ui_component_test.exs</files>
  <behavior>
    - <.skeleton> renders a container with class scoria-skeleton and aria-label="Loading…"
    - <.skeleton rows={3}> renders 3 stacked skeleton lines
    - <.toast> renders class scoria-toast and scoria-toast--{tone} for the given tone (e.g. scoria-toast--pass)
    - <.toast> carries role="status" and the message text
    - <.toast> includes phx-mounted with JS.hide and the duration_ms time value (auto-dismiss serialized into markup)
    - <.toast> has a manual dismiss button
  </behavior>
  <read_first>
    - lib/scoria_web/ui.ex (read badge/1 attr + class-list pattern; the JS alias is already present)
    - .planning/phases/12-design-system-component-layer/12-UI-SPEC.md (DS-05 skeleton lines 322-335: attrs, visual, prefers-reduced-motion, aria-label "Loading…"; toast lines 373-388: attrs, visual, open/dismiss animation, auto-dismiss; Copywriting lines 429-430 toast silent + skeleton label)
    - .planning/phases/12-design-system-component-layer/12-PATTERNS.md (lines 207-218 tone-modifier pattern for toast)
    - .planning/phases/12-design-system-component-layer/12-RESEARCH.md (Pattern 3 lines 281-335 server-held toast + phx-mounted; Pitfall 3 lines 557-562 — omit `to:` so JS.hide defaults to self; D-11 auto-dismiss contract)
  </read_first>
  <action>
    Add `def skeleton/1` to `ui.ex`: attrs `class` (string, default nil), `rows` (integer, default 1), `rest :global`. Render a `<div class="scoria-skeleton-group" aria-label="Loading…" role="status" {@rest}>` containing `<div :for={_ <- 1..@rows} class={["scoria-skeleton", "scoria-skeleton--text", @class]}></div>`. The pulse animation + reduced-motion suppression come from CSS (plan 12-01) — no animation in HEEx. Use the exact label string "Loading…" (with the ellipsis character per the copy contract).
    Add `def toast/1`: attrs `id` (string, req), `tone` (atom, default :neutral, values [:pass, :fail, :warn, :info, :neutral]), `message` (string, req), `duration_ms` (integer, default 4000). Render `<div id={@id} class={["scoria-toast", "scoria-toast--#{@tone}"]} role="status" phx-mounted={JS.hide(transition: {"scoria-fade", "opacity-100", "opacity-0"}, time: @duration_ms)}>` containing a tone-keyed 16×16 SVG icon, `<p>{@message}</p>`, and a manual dismiss `<button phx-click={JS.hide(transition: {"scoria-fade", "opacity-100", "opacity-0"}, time: 100)} class="scoria-button scoria-button--ghost scoria-button--sm" aria-label="Dismiss">` with an X SVG. OMIT the `to:` option on JS.hide so it targets self (Pitfall 3). Do NOT use push_event or a JS hook (D-10/D-11 — untestable in LiveViewTest).
    Emit only semantic class names (ui.ex stays at zero raw palette).
    In `ui_component_test.exs`, add DS-05 tests (function-capture form — both are attr-only): `<.skeleton rows={3}>` renders `aria-label="Loading…"` and three `.scoria-skeleton` elements; `<.toast tone={:pass} message="Saved" duration_ms={2000}>` renders `scoria-toast--pass`, `role="status"`, the text "Saved", and `phx-mounted` containing the JS.hide command (assert the rendered markup includes the serialized hide directive / the duration). Assert the manual dismiss button has `aria-label="Dismiss"`.
  </action>
  <acceptance_criteria>
    - Source: `ui.ex` contains `def skeleton(` and `def toast(`
    - Behavior: `<.skeleton>` renders `aria-label="Loading…"` and `class` containing `scoria-skeleton`
    - Behavior: `<.skeleton rows={3}>` renders 3 skeleton line elements
    - Behavior: `<.toast tone={:pass}>` renders `scoria-toast--pass`
    - Behavior: `<.toast>` renders `role="status"`, the message text, and a `phx-mounted` attribute carrying a JS.hide directive
    - Behavior: `<.toast>` renders a manual dismiss button with `aria-label="Dismiss"`
    - Source: `ui.ex` toast uses `JS.hide` WITHOUT a `to:` option (targets self)
    - Source: ui.ex still scans to zero raw palette (grep count 0)
    - Test: `mix test test/scoria_web/ui_component_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/ui_component_test.exs && test "$(grep -cE '\b(stone|rose|sky|emerald|amber|blue|gray|slate|zinc|neutral|red|green|yellow|purple|pink|indigo|teal|cyan|lime|orange|violet|fuchsia)-[0-9]' lib/scoria_web/ui.ex)" = "0"</automated>
  </verify>
  <done>`<.skeleton>` renders an accessible, token-bound loading placeholder (reduced-motion handled in CSS) and `<.toast>` renders a tone-keyed transient notification with server-assign-friendly markup and `phx-mounted` auto-dismiss (no JS hook); DS-05 component unit tests pass; ui.ex stays at zero raw palette.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller LiveView → notebook/toast | Caller-provided title, eyebrow, tab content, toast message interpolated into HEEx |
| Server assign → client JS | toast `duration_ms` serialized into a `phx-mounted` JS.hide directive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Information disclosure / XSS | toast `{@message}`, notebook title/tab bodies, raw_evidence pre | mitigate | All caller content via auto-escaped HEEx `{...}`. No `raw/1` on `@message`, `@title`, or tab/inner_block slots. `<pre>` content is escaped by HEEx. Icons are static literals. |
| T-12-11 | Tampering | toast auto-dismiss path | mitigate | Server-assign + `phx-mounted` JS.hide (D-10/D-11) — no client hook that could be tampered to suppress dismissal in a security-relevant way; toast carries no privileged action. |
| T-12-12 | Tampering | ui.ex gateway self-consistency | mitigate | ui.ex zero-palette grep asserted per task. |
</threat_model>

<verification>
- `mix test test/scoria_web/ui_component_test.exs` exits 0 (notebook/skeleton/toast assertions).
- ui.ex raw-palette grep count is 0 — all 8 components now built and ui.ex is a clean gateway.
- `mix test test/scoria_web/` exits 0 (no web-subset regression).
</verification>

<success_criteria>
- `<.notebook>` renders the unified tabbed evidence shell with correct aria roles and tab-change emission.
- `<.skeleton>` renders an accessible, reduced-motion-aware loading placeholder.
- `<.toast>` renders a server-assign-driven transient notification with `phx-mounted` auto-dismiss.
- All 8 Phase 12 components now exist in ui.ex; ui.ex remains at zero raw palette.
</success_criteria>

<output>
Create `.planning/phases/12-design-system-component-layer/12-04-SUMMARY.md` when done.
</output>
