---
phase: 12-design-system-component-layer
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - lib/scoria_web/ui.ex
  - test/scoria_web/ui_component_test.exs
autonomous: true
requirements: [DS-05, DS-01]
nyquist_compliant: true

must_haves:
  truths:
    - "ui.ex has zero raw palette occurrences (the gateway is self-consistent)"
    - "flash_group renders semantic scoria-flash--{tone} classes keyed by the STRING flash kind (not atom)"
    - "Operators can render a sortable, density-aware <.table> with a first-class empty state from one shared component"
  artifacts:
    - path: "lib/scoria_web/ui.ex"
      provides: "alias Phoenix.LiveView.JS, fixed flash_group/1, flash_modifier/1, table/1 with :col/:empty/:action/:filter slots"
      contains: "def table("
    - path: "test/scoria_web/ui_component_test.exs"
      provides: "render_component assertions for flash_group + table"
      contains: "scoria-flash--fail"
  key_links:
    - from: "lib/scoria_web/ui.ex flash_group/1"
      to: ".scoria-flash--{tone} CSS"
      via: "flash_modifier/1 string-keyed clauses"
      pattern: "flash_modifier\\(\"error\"\\)"
    - from: "lib/scoria_web/ui.ex table/1 :col header"
      to: "parent LiveView sort handler"
      via: "phx-click=\"sort\" phx-value-by={key}"
      pattern: "phx-value-by"
---

<objective>
Make `ui.ex` self-consistent and ship the first two component contracts. This plan (1) fixes `flash_group` to route through semantic `scoria-flash--{tone}` tokens with string-keyed clauses (closing the latent string/atom bug), zeroing `ui.ex`'s 3 raw-palette lines, and (2) adds the `<.table>` component (DS-01): sortable headers, density toggle, pagination strip, filter slot, and a first-class empty state.

Purpose: `ui.ex` is the enforced token gateway — it MUST contain zero raw palette before the DS-06 guard can assert it (the ui.ex-zero assertion from plan 12-01 goes green here). The `<.table>` is the highest-leverage shared component (review queue, incidents, eval workbench all consume it in Phase 14).
Output: Updated `lib/scoria_web/ui.ex` (flash fix + `table/1`); real `render_component` assertions added to `ui_component_test.exs`.

This plan owns `lib/scoria_web/ui.ex` exclusively for wave 2 — plans 12-03 and 12-04 edit the same file in later waves, never concurrently.

Decisions addressed: D-13: (replace raw-palette `flash_tone_class/1` with semantic `scoria-flash--{tone}` BEM modifiers + `.scoria-flash` CSS, status never by color alone); D-09: (component APIs validated via `render_component`/LiveViewTest unit tests — the `<.table>` assertions added here establish that pattern for the downstream component plans).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-design-system-component-layer/12-UI-SPEC.md
@.planning/phases/12-design-system-component-layer/12-PATTERNS.md
@.planning/phases/12-design-system-component-layer/12-CONTEXT.md
@lib/scoria_web/ui.ex

<interfaces>
<!-- Existing ui.ex components to compose with and copy from: -->
<!--   badge/1 (lines ~59-73): attr+slot+class-list pattern -->
<!--   panel/1 (lines ~110-132): named optional slots + `:if={@slot != []}` guards -->
<!--   empty_state/1 (lines ~164-178): empty-state structure for the table's <:empty> -->
<!--   tone/1, status_label/1, badge/1, id/1: callers use these inside <:col> slots -->
<!--   flash_group/1 (lines ~183-197): the function to rewrite; flash_tone_class/1 (195-197): delete -->
<!-- Slot-with-typed-attrs pattern (NOT yet in ui.ex, confirmed in LV 1.1.30): PATTERNS.md lines 162-179. -->
<!-- CSS classes .scoria-flash*, .scoria-table--compact/--comfortable already exist (plan 12-01). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix flash_group to semantic tokens (string-keyed) and zero ui.ex raw palette</name>
  <files>lib/scoria_web/ui.ex, test/scoria_web/ui_component_test.exs</files>
  <behavior>
    - flash_group renders class "scoria-flash--fail" when @flash has key "error"
    - flash_group renders class "scoria-flash--info" when @flash has key "info"
    - flash_group renders class "scoria-flash--pass" when @flash has key "success"
    - flash_group renders class "scoria-flash--warn" for any unknown/warning kind
    - Each flash div carries role="alert"
    - ui.ex contains zero raw palette class occurrences after this task
  </behavior>
  <read_first>
    - lib/scoria_web/ui.ex (read the FULL current flash_group/1 + flash_tone_class/1 at lines ~183-197, and the module header lines 1-13 where `alias Phoenix.LiveView.JS` must be added)
    - .planning/phases/12-design-system-component-layer/12-UI-SPEC.md (DS-05 flash_group fix lines 337-371: tone→modifier map, role="alert", 16×16 SVG tone icon "status never by color alone")
    - .planning/phases/12-design-system-component-layer/12-PATTERNS.md (lines 103-154: the exact required replacement — string-keyed `flash_modifier/1` clauses; the string/atom bug explanation)
    - .planning/phases/12-design-system-component-layer/12-RESEARCH.md (Pitfall 6 lines 578-583: confirmed `@flash` yields STRING keys; the old `:error` atom clause never matched)
  </read_first>
  <action>
    Add `alias Phoenix.LiveView.JS` immediately after `use Phoenix.Component` in the `ui.ex` module header (required for JS-driven components in later plans; safe to add now).
    Rewrite `flash_group/1`: keep `attr(:flash, :map, default: %{})`; iterate `:for={{kind, message} <- @flash}`; each item is a `<div id={"flash-#{kind}"} role="alert" class={["scoria-flash", flash_modifier(kind)]}>` containing a tone-keyed 16×16 inline SVG icon (so status is never color-alone) followed by `{message}`. Add `flash_icon/1` returning the per-kind SVG (a simple inline heroicon-style path is fine; one icon per tone: fail=x-circle, info=information-circle, pass=check-circle, warn=exclamation-triangle).
    Replace the three `flash_tone_class/1` clauses with STRING-keyed `flash_modifier/1`: `defp flash_modifier("error"), do: "scoria-flash--fail"`; `defp flash_modifier("info"), do: "scoria-flash--info"`; `defp flash_modifier("success"), do: "scoria-flash--pass"`; `defp flash_modifier(_kind), do: "scoria-flash--warn"`. DELETE `flash_tone_class/1` entirely.
    After this edit, `ui.ex` must contain zero matches of the DS-06 palette regex (the 3 raw-palette lines were the flash_tone_class clauses). This makes the `@tag :ui_ex_zero` assertion from plan 12-01 pass — REMOVE the `:ui_ex_zero` tag from that assertion in `ds06_drift_guard_test.exs`? NO — leave the tag (plan 12-05 owns the guard going fully green); just ensure the assertion would pass. This plan's verify runs the assertion directly.
    In `test/scoria_web/ui_component_test.exs`, replace the placeholder test with a DS-05 flash group: render `flash_group/1` with `%{"error" => "boom"}`, `%{"info" => "fyi"}`, `%{"success" => "done"}`, and an unknown kind; assert the rendered HTML contains `scoria-flash--fail`, `scoria-flash--info`, `scoria-flash--pass`, `scoria-flash--warn` respectively and `role="alert"`.
  </action>
  <acceptance_criteria>
    - Source: `ui.ex` contains `alias Phoenix.LiveView.JS`
    - Source: `ui.ex` contains `defp flash_modifier("error")` returning `scoria-flash--fail`
    - Source: `ui.ex` no longer contains `flash_tone_class`
    - Source: ui.ex raw-palette scan is empty: `grep -cE '\b(stone|rose|sky|emerald|amber|blue|gray|slate|zinc|neutral|red|green|yellow|purple|pink|indigo|teal|cyan|lime|orange|violet|fuchsia)-[0-9]' lib/scoria_web/ui.ex` returns 0
    - Behavior: `flash_group` renders `scoria-flash--fail` for string kind `"error"` (not atom)
    - Behavior: each flash div carries `role="alert"`
    - Test: `mix test test/scoria_web/ui_component_test.exs` exits 0
    - Guard: the ui.ex-zero assertion in `ds06_drift_guard_test.exs` passes — `mix test test/scoria_web/ds06_drift_guard_test.exs --only ui_ex_zero` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/ui_component_test.exs && mix test test/scoria_web/ds06_drift_guard_test.exs --only ui_ex_zero</automated>
  </verify>
  <done>flash_group routes through semantic tokens with correct string-keyed clauses, carries role="alert" + tone icon, flash_tone_class is deleted, ui.ex scans to zero raw palette, and the flash render tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the <.table> component (DS-01) with sort, density, pagination, filter slot, empty state</name>
  <files>lib/scoria_web/ui.ex, test/scoria_web/ui_component_test.exs</files>
  <behavior>
    - <.table> renders a column header for each <:col> with its label
    - A sortable column header (key present) emits phx-click="sort" phx-value-by={key}
    - The active sort column shows an indicator; sort_dir distinguishes asc/desc
    - density={:compact} adds scoria-table--compact; density={:comfortable} adds scoria-table--comfortable; density={:default} adds neither
    - rows=[] renders the default empty state ("No records found"); a provided <:empty> overrides it
    - render_slot(col, row) renders per-row cell content
  </behavior>
  <read_first>
    - lib/scoria_web/ui.ex (read badge/1 class-list pattern, panel/1 slot-guard pattern, empty_state/1 — all are copied here; the file now has the flash fix + JS alias from Task 1)
    - .planning/phases/12-design-system-component-layer/12-UI-SPEC.md (DS-01 lines 144-181: full attr table, <:col>/<:empty>/<:action>/<:filter> slots, sort affordance, density class map, pagination contract, density toggle contract, row selection, empty copy)
    - .planning/phases/12-design-system-component-layer/12-PATTERNS.md (lines 158-197: slot-with-typed-attrs pattern, descriptive loop var to avoid shadowing, empty_state reuse for <:empty>)
    - .planning/phases/12-design-system-component-layer/12-RESEARCH.md (Pattern 1 lines 194-241, Pattern 5 lines 416-431; Pitfall 2 lines 550-555 render_slot nil guard; Pitfall 5 lines 571-576 slot loop var shadowing)
    - test/scoria_web/components/trace_tree_component_test.exs (the render_component(Module, assigns) module-reference form for slot-bearing components)
  </read_first>
  <action>
    Add `def table/1` to `ui.ex`. Declare typed slot `slot :col do attr :label, :string, required: true; attr :key, :atom, default: nil; attr :class, :string, default: nil end`, plus `slot :empty`, `slot :action`, `slot :filter`. Declare attrs: `attr :rows, :list, required: true`; `attr :sort_by, :any, default: nil`; `attr :sort_dir, :atom, default: :asc, values: [:asc, :desc]`; `attr :density, :atom, default: :default, values: [:compact, :default, :comfortable]`; `attr :id, :string, required: true`; `attr :rest, :global`.
    Render a wrapper `<div class={["scoria-table-shell"]}>` containing: the `<:filter>` region (`:if={@filter != []}` above the table); a density toggle pill (`role="group" aria-label="Row density"` with three buttons emitting `phx-click="set_density" phx-value-density={value}`, active = `.scoria-button--primary.scoria-button--sm`, inactive = `.scoria-button--ghost.scoria-button--sm`); the `<table class={["scoria-table", density_class(@density)]} id={@id} {@rest}>`. Header `<th :for={column <- @col}>` (use `column`, not `col`, to avoid shadowing) emits `phx-click={column.key && "sort"} phx-value-by={column.key}` only when `column.key` is non-nil, with a 16×16 up/down chevron SVG colored `--scoria-action` when `@sort_by == column.key` (direction from `@sort_dir`) and `--scoria-text-subtle` otherwise. Body: when `@rows == []`, render a single `<tr><td colspan={length(@col)}>` containing `render_slot(@empty)` if `@empty != []` else the default `<.empty_state title="No records found">` with body copy "Adjust your filters or check back when data is available."; otherwise `<tr :for={row <- @rows}>` with `<td :for={column <- @col}>{render_slot(column, row)}</td>`. Add a pagination `<nav aria-label="Pagination">` strip rendered only when applicable — to keep the component pure and stateless, accept optional `attr :page, :integer, default: 1`, `attr :total_pages, :integer, default: 1`, `attr :on_page_change, :string, default: nil`; render prev/next `.scoria-button--ghost.scoria-button--sm` chevrons + "Page X of Y" only when `@total_pages > 1`.
    Add a private `density_class/1`: `:compact -> "scoria-table--compact"`, `:comfortable -> "scoria-table--comfortable"`, `:default -> nil`.
    Emit ONLY semantic class names — no raw palette (ui.ex must stay at zero).
    In `ui_component_test.exs`, add DS-01 tests using the module-reference `render_component` form (slots): assert a `<:col label="Status" key={:status}>` header renders "Status"; assert a sortable header emits `phx-value-by="status"`; assert `density: :compact` produces `scoria-table--compact`; assert `rows: []` renders "No records found"; assert a row cell renders via `render_slot`.
  </action>
  <acceptance_criteria>
    - Source: `ui.ex` contains `def table(`
    - Source: `ui.ex` contains `slot :col`
    - Source: `ui.ex` contains `density_class(` private clause mapping `:compact`/`:comfortable`/`:default`
    - Behavior: `<.table>` with a `key`-bearing `<:col>` renders `phx-value-by` with that key
    - Behavior: `density: :compact` yields `scoria-table--compact` in the rendered class list; `:default` yields neither modifier
    - Behavior: `rows: []` renders "No records found"; a `<:empty>` slot overrides the default
    - Source: ui.ex still scans to zero raw palette (grep count 0)
    - Test: `mix test test/scoria_web/ui_component_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/ui_component_test.exs && test "$(grep -cE '\b(stone|rose|sky|emerald|amber|blue|gray|slate|zinc|neutral|red|green|yellow|purple|pink|indigo|teal|cyan|lime|orange|violet|fuchsia)-[0-9]' lib/scoria_web/ui.ex)" = "0"</automated>
  </verify>
  <done>`<.table>` renders headers from typed `:col` slots, emits sort events on keyed columns, applies the correct density modifier, falls back to the default empty state (overridable via `<:empty>`), renders pagination when total_pages > 1, and binds only semantic tokens; DS-01 unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller LiveView → flash_group/table | Caller-provided flash messages and row data are interpolated into HEEx |
| Browser → parent LiveView | sort / set_density / page-change events cross from client to server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Information disclosure / XSS | flash_group, table cell `{message}` / `render_slot` | mitigate | Use standard HEEx `{...}` interpolation (auto HTML-escaped). Do NOT use `raw/1` on flash messages or row data. Tone SVG icon is a static literal, not interpolated user content. |
| T-12-05 | Tampering | ui.ex gateway self-consistency | mitigate | Task asserts ui.ex scans to zero raw palette; the DS-06 ui_ex_zero assertion gates the gateway against regression. |
| T-12-06 | Tampering | npm/pip/cargo installs | accept | No package installs this phase. |
</threat_model>

<verification>
- `mix test test/scoria_web/ui_component_test.exs` exits 0 (flash + table assertions).
- `mix test test/scoria_web/ds06_drift_guard_test.exs --only ui_ex_zero` exits 0 (gateway clean).
- ui.ex raw-palette grep count is 0.
- `mix test test/scoria_web/` exits 0 (no regression in the web subset).
</verification>

<success_criteria>
- flash_group routes through semantic tokens, string-keyed, role="alert", tone icon present.
- ui.ex contains zero raw palette occurrences.
- `<.table>` supports sort emission, density modifiers, pagination strip, filter slot, and a first-class empty state from one shared component.
</success_criteria>

<output>
Create `.planning/phases/12-design-system-component-layer/12-02-SUMMARY.md` when done.
</output>
