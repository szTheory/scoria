---
context: phase
phase: 58-safety-hooks-security-boundary-govern-surface
task: null
total_tasks: null
status: paused
last_updated: 2026-07-29T17:18:13.963Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure
> in this session or an immediately prior one. Acknowledge each explicitly before proceeding.

- [ ] CONSTRAINT: **D-19 doc-build blocker** — `mix docs --warnings-as-errors` is already RED on the current tree (broken-window ledger entry 4). Phase 58's security-boundary guide adds seven ExDoc registrations that must survive `mix scoria.release_preview`. — **Structural mitigation:** clearing the doc build must be its own wave-0 plan in Phase 58, not a task tucked inside a later plan.
- [ ] CONSTRAINT: **`lib/scoria/workflows.ex` is line-pinned** — `approval_write_invariant_guard` asserts hardcoded line numbers in that file, so *any* insertion above the call sites false-positives. Three separate plans hit this independently in one phase. — **Structural mitigation:** plans that touch approval writes must state explicitly whether they modify `workflows.ex`; if they must, the guard's pinned line numbers get updated in the same commit.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Decision-coverage gate parse trap | The plan-phase §13a gate fails with "could-not-parse" when a `- **D-NN …**` CONTEXT bullet contains a literal `*` inside the bold run. It also matches exact sub-IDs (`D-06a`, not `D-06`). | blocking | Before running `/gsd-plan-phase 58`, grep `58-CONTEXT.md` for `*` inside `- **D-NN` bold runs. It was clean at pause time — re-check after any edit. |
| Resuming a stalled executor instead of replacing it | A `autonomous: true` plan's executor emitted a blocking tracer checkpoint mid-run and had burned 196k tokens. | advisory | Verify the agent's claims independently (commit contents, file diffs, re-run the tests yourself), then spawn a **fresh continuation agent** — GSD's protocol is fresh agent, not resume. |
| Treating the CaptureParity failure as a regression | `Scoria.WarningInventory.CaptureParityTest` at `capture_parity_test.exs:53` fails intermittently in the full suite. | advisory | It is the documented baseline flake (`.planning/todos/pending/2026-07-18-flaky-capture-parity-test.md`). Expected full-suite state is **1748 tests, 1 failure**. |
| Losing the failure header when capturing test output | Piping `tail -40` into a file showed "1 failure" but not *which* test. | advisory | Capture complete `mix test` output to a file, then grep for the failure header. Never `tail` a test run you intend to diagnose. |
| Dirty `.dag` artifacts blocking worktree merge | Tracked rebar3 `source.dag` files under `examples/support_copilot/deps` get rewritten by any compile and block `cleanup-wave`. | advisory | Executors must `git checkout --` those paths before returning. |

<current_state>
Paused at a clean seam. Phase 57 is fully closed; Phase 58 is `discussed` with 40 locked
decisions and **zero plans written**. Working tree is clean, everything is committed and
pushed, and draft PR #14 is open. Nothing is half-finished.

The natural resume point is `/gsd-plan-phase 58` — but see the D-19 blocking constraint above.
</current_state>

<completed_work>

- **Phase 55** — content trust taint substrate (`Scoria.Trust.Scanner` BYO seam). Done.
- **Phase 56 / 56.1** — tool-declared trifecta classification + per-run rails. Done.
- **Phase 57** — confluence escalation gate ("Portcullis"). Done, with all four gates green:
  - Verification: 5/5 success criteria (`57-VERIFICATION.md`)
  - Code review: 1 critical + 2 warnings, all advisory, all tracked (`57-REVIEW.md`)
  - Validation: Nyquist compliant, 0 gaps (`57-VALIDATION.md`)
  - Security: 68 rows / 60 distinct threats, 67 closed, `threats_open: 0` (`57-SECURITY.md`)
  - Gap-closure plans 57-11 and 57-12 wire escalation-time confluence evidence into the
    reviewer projection, proven end-to-end through a real `Scoria.MCP.Executor.execute/4`
    escalation in `test/scoria/confluence_reviewer_evidence_test.exs`.
- **Phase 58 discuss** — `58-CONTEXT.md` locked: 40 decisions (D-01..D-40) from 5 parallel
  researchers + 2 opposed-lens red teams. Commit `2813f03f`.
- **Phase 58 bookkeeping** — 58.1 split added to ROADMAP; HOOK-01/HOOK-02 amended (HOOK-02
  labelled *reduced scope*); BOUND-01 filename amended to `guides/security-boundary.md`;
  HOOK-03 + GOVERN-02 added; GOVERN-01 given an explicit non-amendment note; coverage 16 → 18;
  57-CONTEXT obligations corrected/re-homed; PROJECT.md claim-precision paragraph added.
  Commit `1cdc4340`.
</completed_work>

<remaining_work>

- `/gsd-plan-phase 58` — not started. D-19 must land as wave 0.
- Execute Phase 58 — safety hooks, security boundary document, govern surface.
- Plan + execute Phase 58.1 — scanner-to-gate wiring and escalation operability
  (HOOK-03, GOVERN-02). This is what actually closes the v3.7 milestone.
</remaining_work>

<decisions_made>

- **Phase 58 split into 58 + 58.1.** Scope inventory came to ~15–17 plans against Phase 57's
  12, in a milestone-closing phase with no Phase 59 to absorb overflow. Followed the 56/56.1
  split precedent exactly (checklist line + detail block + Progress row).
- **D-06 (reversal): span evidence only for model-output taint.** Both red teams overturned
  the draft's researcher-B design. Code-truth showed B's "100% pause rate day one"
  justification was *false* — enforcement gates on `combination == "exfiltration_path"`, and
  `exfil` only comes from a declared `can_exfiltrate`, so unconditional tainting is inert at
  rung 1. Adopter-harm showed a correctly-written Phase-55 scanner receives a third undefined
  content shape, raises `FunctionClauseError`, fail-closes to `:scanner_error`, and under
  `strict: true` becomes unconditional escalate.
- **D-20: GOVERN-01 deliberately NOT amended.** Genuine red-team disagreement — code-truth said
  the facts warrant amending, adopter-lens called it laundering. Sided with adopter-lens and
  wrote a *general* amendment-legitimacy test into D-20 so it isn't relitigated per-phase.
- **D-02/D-39: milestone claim revised to precision** rather than pulling scanner→gate wiring
  back into Phase 58. The gate escalates when a tool **declares** all three legs — a
  self-declaration gate, not an observation gate (`executor.ex:705-731`, `:737-739`).
- **Two dropped Phase 57 cross-phase obligations re-homed to 58.1.** Both red teams found
  obligations 4 and 6 independently. Obligation 2 was factually wrong and is struck through
  in place in `57-CONTEXT.md` with the full evidence trail.
</decisions_made>

<blockers>
- **D-19 / broken-window entry 4**: `mix docs --warnings-as-errors` is red. → wave-0 plan.
- **CR-01**: `halt_run` confluence-cleanup `StaleEntryError` race. → tracked todo, advisory.
  Fix is to wrap the post-commit cleanup call in its own `try/rescue` like its siblings.
- **Baseline flake**: CaptureParityTest. → known, do not chase.
</blockers>

## Required Reading (in order)

1. `.planning/phases/58-safety-hooks-security-boundary-govern-surface/58-CONTEXT.md` — the 40 locked decisions. Entries marked **(revised)** were changed by a red team; **(reversal)** overturned a researcher's recommendation. Read those two classes especially carefully — they are where the naive answer is wrong.
2. `.planning/ROADMAP.md` — Phase 58 and 58.1 blocks, including the italic rationale note on Phase 58 SC 1–3 (SC4 deliberately untouched).
3. `.planning/REQUIREMENTS.md` — HOOK-01/02/03, BOUND-01, GOVERN-01/02 and the non-amendment note on GOVERN-01.
4. `.planning/phases/57-confluence-escalation-gate/57-CONTEXT.md` — for the corrected obligation 2 and the re-homed obligations 4 and 6.
5. `.planning/WINDOWS.md` — broken-window ledger; entry 4 is the D-19 blocker.
6. `.planning/METHODOLOGY.md` (if it exists) — project analytical lenses; apply before any assumption analysis.

## Infrastructure State

- **Branch**: `worktree-phase-55-content-trust-taint`, pushed to origin, tracking set.
  419 commits ahead of `origin/main`; 475 files, +66,311/−2,857.
- **Draft PR**: https://github.com/szTheory/scoria/pull/14 — open, awaiting review. Not merged.
- **Working tree**: clean at pause time.
- **Test baseline**: `mix test --warnings-as-errors` → 1748 tests, 1 failure (the known flake).
- **Worktree**: this is `/Users/jon/projects/scoria/.claude/worktrees/phase-55-content-trust-taint`.
  A fresh worktree needs `mix deps.get` + pgvector on port 55432. Run `make native-db` **once**,
  never from an executor — it collides across worktrees.
- **Doc build**: `mix docs --warnings-as-errors` currently FAILS. See D-19.
- No background servers or watchers were left running. No async external jobs outstanding.

<context>
The through-line of the last stretch was that the *shipped record was wrong in three places*,
and the Phase 58 discuss pass caught it. The most important of those: Scoria's flagship
confluence gate escalates on **declared** capability, not observed behaviour. That is a real
and defensible product — a self-declaration gate — but it is not what PROJECT.md claimed, and
the honest fix was to correct the claim rather than to widen Phase 58 until the old claim
became true. That instinct (correct the record, don't launder it) is also what settled the
GOVERN-01 disagreement, and it's the disposition to carry into planning.

The other thing worth internalising: the red teams were right and my draft was wrong, on the
strength of *code truth* rather than argument. When Phase 58 planning hits a design fork,
go read the enforcement predicate before picking a side.
</context>

<next_action>
Start with: `/gsd-plan-phase 58`. Before the planner writes anything, confirm D-19 is scheduled
as a wave-0 plan (clear the red `mix docs --warnings-as-errors` build) — the seven new ExDoc
registrations for the security-boundary guide cannot land on a broken doc build.
</next_action>
