---
phase: 67-high-signal-warning-ratchet
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - lib/scoria/warning_ratchet.ex
  - lib/mix/tasks/scoria.warning_ratchet.test.ex
  - lib/mix/tasks/scoria.warning_ratchet.check.ex
  - lib/mix/tasks/scoria.warning_inventory.ex
  - test/scoria/warning_ratchet_test.exs
  - docs/operator_verification.md
  - .planning/WARNING-INVENTORY.md
  - .planning/warning-inventory.baseline.json
autonomous: true
requirements: [WARN-06]
must_haves:
  truths:
    - "WarningRatchet.high_signal_wae_paths/0 is non-empty code SSOT (D-18)."
    - "mix scoria.warning_ratchet.test runs scoped WAE over SSOT paths only (D-24)."
    - "mix scoria.warning_ratchet.check fails on :unclassified_compile in high-signal paths (D-07, D-10)."
    - "Committed inventory artifacts refreshed with Phase 67 fixed/deferred queue sections (D-06)."
  artifacts:
    - path: "lib/scoria/warning_ratchet.ex"
      provides: "high_signal_wae_paths/0"
      contains: "defmodule Scoria.WarningRatchet"
    - path: "lib/mix/tasks/scoria.warning_ratchet.test.ex"
      provides: "scoped WAE runner"
      contains: "defmodule Mix.Tasks.Scoria.WarningRatchet.Test"
  key_links:
    - from: "lib/scoria/warning_ratchet.ex"
      to: "lib/mix/tasks/test.adoption.ex"
      via: "adoption_test_files/0"
      pattern: "adoption_test_files"
---

<objective>
Establish WARN-06 infrastructure: `Scoria.WarningRatchet` path SSOT, maintainer Mix tasks, inventory refresh with Phase 67 queue documentation, and `67-VALIDATION.md` wiring.

Purpose: Downstream plans 67-02–67-04 verify against the same path list; Phase 68 CI step reuses this SSOT.
Output: WarningRatchet module, `warning_ratchet.test` / `warning_ratchet.check`, refreshed `.planning/warning-inventory.baseline.json` + `WARNING-INVENTORY.md`.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/67-high-signal-warning-ratchet/67-CONTEXT.md
@.planning/phases/67-high-signal-warning-ratchet/67-RESEARCH.md
@.planning/phases/67-high-signal-warning-ratchet/67-VALIDATION.md
@lib/scoria/warning_inventory.ex
@lib/mix/tasks/scoria.warning_inventory.ex
@lib/mix/tasks/test.adoption.ex
@docs/operator_verification.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Path injection via wildcards | low | Only static globs under `test/`; no user args for paths in v1 |
| Empty SSOT bypassing WARN-06 | medium | Unit test asserts `length(paths) > 10` and includes `test/scoria/verification_lanes_test.exs` |
| Inventory run with polluted test/tmp | medium | Existing preflight in warning_inventory; document cleanup in operator_verification.md |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 67-00-01: Implement Scoria.WarningRatchet path SSOT</name>
  <requirement_ids>WARN-06</requirement_ids>
  <files>lib/scoria/warning_ratchet.ex, test/scoria/warning_ratchet_test.exs</files>
  <read_first>
    .planning/phases/67-high-signal-warning-ratchet/67-CONTEXT.md
    lib/mix/tasks/test.adoption.ex
    lib/scoria/warning_inventory.ex
  </read_first>
  <action>
Create `lib/scoria/warning_ratchet.ex`:

```elixir
defmodule Scoria.WarningRatchet do
  @moduledoc """
  SSOT for high-signal warning-as-errors test paths (WARN-06).
  Paths are composed in code — not derived from warning-inventory.baseline.json.
  """

  @live_glob "test/scoria_web/live/**/*_test.exs"
  @scoria_glob "test/scoria/**/*_test.exs"

  @spec high_signal_wae_paths() :: [String.t()]
  def high_signal_wae_paths do
  end

  @spec high_signal_path?(String.t()) :: boolean()
  def high_signal_path?(file) when is_binary(file) do
  end
end
```

Implement `high_signal_wae_paths/0` as sorted unique list of:
1. `Mix.Tasks.Scoria.Test.Adoption.adoption_test_files/0`
2. `Path.wildcard(@scoria_glob)`
3. `Path.wildcard(@live_glob)`

Implement `high_signal_path?/1` as membership in that set (normalize with `Path.expand/1` relative to `File.cwd!()`).

Add `test/scoria/warning_ratchet_test.exs`:
- Assert `high_signal_wae_paths/0` includes `"test/scoria/verification_lanes_test.exs"`
- Assert every path exists on disk (`File.exists?/1`)
- Assert list equals `Enum.sort(list)` (deterministic ordering)
- Assert length >= 15
  </action>
  <acceptance_criteria>
    - `rg -n "defmodule Scoria\\.WarningRatchet" lib/scoria/warning_ratchet.ex` returns one match.
    - `rg -n "def high_signal_wae_paths" lib/scoria/warning_ratchet.ex` returns one match.
    - `MIX_ENV=test mix test test/scoria/warning_ratchet_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/warning_ratchet_test.exs</automated>
  </verify>
  <done>WarningRatchet exposes deterministic high-signal path list for WARN-06.</done>
</task>

<task type="auto">
  <name>Task 67-00-02: Add mix scoria.warning_ratchet.test and .check</name>
  <requirement_ids>WARN-06</requirement_ids>
  <files>lib/mix/tasks/scoria.warning_ratchet.test.ex, lib/mix/tasks/scoria.warning_ratchet.check.ex</files>
  <read_first>
    lib/scoria/warning_ratchet.ex
    lib/mix/tasks/scoria.warning_inventory.ex
    lib/scoria/warning_inventory/cluster.ex
  </read_first>
  <action>
Create `Mix.Tasks.Scoria.WarningRatchet.Test`:
- `@shortdoc "Runs WARN-06 high-signal tests with optional --warnings-as-errors"`
- Parse `--warnings-as-errors` boolean switch
- `paths = Scoria.WarningRatchet.high_signal_wae_paths()`
- Build args: if WAE flag, `["--warnings-as-errors" | paths]` else `paths`
- `Mix.Task.run("test", args)` after `loadpaths`

Create `Mix.Tasks.Scoria.WarningRatchet.Check`:
- Run capture same as inventory: `mix do compile --force + test` with `MIX_ENV=test`, `stderr_to_stdout: true` (reuse private logic via small shared function in `WarningInventory` or duplicate minimal capture — prefer extracting `WarningInventory.capture_output/0` if not public yet)
- Parse + classify with `Scoria.WarningInventory`
- Filter rows where `cluster_id == :unclassified_compile` AND `Scoria.WarningRatchet.high_signal_path?(row.file)`
- If any, `Mix.raise` with Field Engineer tone: bullet list of `file:line message`, plus remediation "add Cluster.match rule or fix code; do not baseline unclassified in high-signal scope"
- Exit 0 when none

Operator remediation tone example prefix: `"WARN-06 high-signal paths contain unclassified compile warnings:"`
  </action>
  <acceptance_criteria>
    - `rg -n "defmodule Mix\\.Tasks\\.Scoria\\.WarningRatchet\\.Test" lib/mix/tasks/scoria.warning_ratchet.test.ex` matches.
    - `rg -n "defmodule Mix\\.Tasks\\.Scoria\\.WarningRatchet\\.Check" lib/mix/tasks/scoria.warning_ratchet.check.ex` matches.
    - `MIX_ENV=test mix help scoria.warning_ratchet.test` exits 0.
    - `MIX_ENV=test mix help scoria.warning_ratchet.check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix help scoria.warning_ratchet.test && MIX_ENV=test mix help scoria.warning_ratchet.check</automated>
  </verify>
  <done>Maintainer commands exist for scoped WAE and unclassified gate.</done>
</task>

<task type="auto">
  <name>Task 67-00-03: Refresh inventory artifacts and document Phase 67 queue</name>
  <requirement_ids>WARN-06</requirement_ids>
  <files>.planning/WARNING-INVENTORY.md, .planning/warning-inventory.baseline.json, lib/mix/tasks/scoria.warning_inventory.ex, docs/operator_verification.md</files>
  <read_first>
    lib/mix/tasks/scoria.warning_inventory.ex
    .planning/WARNING-BASELINE.md
    docs/operator_verification.md
  </read_first>
  <action>
1. Ensure `test/tmp/` is empty (remove entries if present).

2. Run `MIX_ENV=test mix scoria.warning_inventory --write --scope full`.

3. Extend `render_markdown/2` in `scoria.warning_inventory.ex` to append after ratchet queue table:

```markdown
## Phase 67 — Fixed vs Deferred

| Cluster | Action | Owner | Expiry / Notes |
|---------|--------|-------|----------------|
| (populate from CONTEXT D-02–D-05) | fix / defer | @scoria-core | ... |
```

Seed rows from CONTEXT:
- **Fix in 67:** `:test_unused_binding`, `:test_dead_default_args`, `:knowledge_migration_redefine`, `:unclassified_compile` (high-signal)
- **Defer:** `:host_proof_*` (p2 guard only), `:liveview_async_teardown` (p4 until 2026-06-30)

4. Re-run `--write` after markdown template update so committed file includes the section.

5. Add `docs/operator_verification.md` subsection `### WARN-06 high-signal ratchet` with commands:
   - `mix scoria.warning_baseline.check`
   - `rm -rf test/tmp/*` preflight note
   - `mix scoria.warning_inventory --write --scope full`
   - `mix scoria.warning_ratchet.check`
   - `mix scoria.warning_ratchet.test --warnings-as-errors`
  </action>
  <acceptance_criteria>
    - `test -f .planning/warning-inventory.baseline.json`
    - `rg -n "Phase 67 — Fixed vs Deferred" .planning/WARNING-INVENTORY.md` returns a match.
    - `rg -n "WARN-06 high-signal ratchet" docs/operator_verification.md` returns a match.
    - `mix scoria.warning_baseline.check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix scoria.warning_baseline.check && test -f .planning/warning-inventory.baseline.json && rg -q "Phase 67 — Fixed vs Deferred" .planning/WARNING-INVENTORY.md</automated>
  </verify>
  <done>Inventory baseline committed with Phase 67 queue documentation and operator commands.</done>
</task>

</tasks>

<verification>
- `MIX_ENV=test mix test test/scoria/warning_ratchet_test.exs`
- `mix scoria.warning_baseline.check`
- `test -f .planning/warning-inventory.baseline.json`
</verification>

<success_criteria>
- WarningRatchet SSOT and Mix tasks exist and are documented
- Inventory artifacts committed with fixed/deferred table
- `67-VALIDATION.md` wave 0 rows can be marked green after task 67-00-01/02 tests pass
</success_criteria>
