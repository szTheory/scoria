---
phase: 67-high-signal-warning-ratchet
plan: 03
type: execute
wave: 3
depends_on: [67-02]
files_modified:
  - test/scoria/**/*.exs
  - lib/scoria/warning_inventory/cluster.ex
autonomous: true
requirements: [WARN-06]
must_haves:
  truths:
    - "Zero :test_unused_binding and :test_dead_default_args rows under test/scoria/ in full inventory (D-02)."
    - "Zero :unclassified_compile in test/scoria/ high-signal paths (D-07)."
    - "Scoped WAE passes for test/scoria/**/*_test.exs paths excluding live/ (D-24)."
  artifacts:
    - path: ".planning/warning-inventory.baseline.json"
      provides: "p3 cluster counts zero for test/scoria"
      contains: "\"test_unused_binding\": 0"
  key_links:
    - from: "mix scoria.warning_ratchet.test"
      to: "test/scoria/"
      via: "high_signal_wae_paths glob"
      pattern: "test/scoria/"
---

<objective>
Clear p3 warning clusters in `test/scoria/` (excluding `test/scoria_web/live/` handled in 67-04): unused bindings, dead default args, and unclassified compile warnings.

Purpose: Core WARN-06 unit-test surface clean under scoped WAE.
Output: Green scoped WAE for non-live scoria tests; updated inventory baseline.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/67-high-signal-warning-ratchet/67-CONTEXT.md
@.planning/warning-inventory.baseline.json
@lib/scoria/warning_inventory/cluster.ex
@lib/scoria/warning_ratchet.ex
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Prefixing vars with _ to silence without fixing | low | Acceptance uses inventory cluster counts, not just compile |
| Removing assertions when fixing unused | medium | Run affected test files after each change |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 67-03-01: Inventory-driven fixes for test/scoria p3 clusters</name>
  <requirement_ids>WARN-06</requirement_ids>
  <files>test/scoria/**/*.exs, lib/scoria/warning_inventory/cluster.ex</files>
  <read_first>
    .planning/warning-inventory.baseline.json
    .planning/WARNING-INVENTORY.md
    lib/scoria/warning_inventory/cluster.ex
    tmp/warning-inventory/latest.json
  </read_first>
  <action>
1. Read `tmp/warning-inventory/latest.json` if present, else run `MIX_ENV=test mix scoria.warning_inventory --scope full` and use table output.

2. Filter rows where:
   - `String.starts_with?(file, "test/scoria/")`
   - NOT `String.contains?(file, "scoria_web/live")`
   - `cluster_id in [:test_unused_binding, :test_dead_default_args, :unclassified_compile]`

3. For each row, apply minimal fix:
   - **unused binding:** remove binding, use `_name`, or use value
   - **dead default args:** remove unused default parameters from function heads
   - **unclassified_compile:** fix root cause OR add `Cluster.match/1` clause with fixture in `test/scoria/warning_inventory/cluster_test.exs`

4. Do NOT modify `test/scoria_web/live/**` in this plan.

5. After all rows addressed, run:
```bash
MIX_ENV=test mix test --warnings-as-errors $(comm -23 <(ls test/scoria/**/*_test.exs | sort) <(ls test/scoria_web/live/**/*_test.exs 2>/dev/null | sort))
```
If comm unavailable, use explicit Path.wildcard list excluding live paths.

6. `MIX_ENV=test mix scoria.warning_inventory --write --scope full`
7. `mix scoria.warning_baseline.check`
  </action>
  <acceptance_criteria>
    - `MIX_ENV=test mix scoria.warning_ratchet.check` exits 0.
    - `jq '.clusters.test_unused_binding // 0' .planning/warning-inventory.baseline.json` outputs 0 OR key missing.
    - `jq '.clusters.test_dead_default_args // 0' .planning/warning-inventory.baseline.json` outputs 0 OR key missing.
    - No inventory row under `test/scoria/` (non-live) with cluster_id unclassified_compile — verify via `MIX_ENV=test mix scoria.warning_inventory --scope full --format json` piped check or grep of latest.json.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix scoria.warning_ratchet.check && mix scoria.warning_baseline.check</automated>
  </verify>
  <done>test/scoria p3 clusters at zero; scoped ratchet check passes for non-live paths.</done>
</task>

</tasks>

<verification>
MIX_ENV=test mix scoria.warning_ratchet.check
MIX_ENV=test mix scoria.warning_inventory --write --scope full
mix scoria.warning_baseline.check
</verification>

<success_criteria>
- Inventory shows zero p3 clusters for test/scoria (excl. live)
- warning_ratchet.check green
</success_criteria>
