---
phase: 67-high-signal-warning-ratchet
plan: 01
type: execute
wave: 1
depends_on: [67-00]
files_modified:
  - test/scoria/ci_policy_contract_test.exs
  - docs/operator_verification.md
autonomous: true
requirements: [WARN-05]
must_haves:
  truths:
    - "mix compile --warnings-as-errors passes (p0)."
    - "Policy lane-contract WAE tests pass unchanged (p1)."
    - "ci_policy_contract_test asserts WarningRatchet SSOT documented (D-19)."
  artifacts:
    - path: "test/scoria/ci_policy_contract_test.exs"
      provides: "CI + WarningRatchet contract"
      contains: "WarningRatchet"
  key_links:
    - from: "test/scoria/ci_policy_contract_test.exs"
      to: ".github/workflows/ci.yml"
      via: "grep policy job order"
      pattern: "warning_baseline.check"
---

<objective>
Verify and guard WARN-05: compile WAE and canonical lane-contract tests remain green; extend CI policy contract for WarningRatchet SSOT without expanding policy job scope.

Purpose: Regression shield before cluster-fix plans mutate test files.
Output: Green WARN-05 verification evidence; contract test for ratchet path SSOT.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/67-high-signal-warning-ratchet/67-CONTEXT.md
@.planning/phases/67-high-signal-warning-ratchet/67-RESEARCH.md
@lib/scoria/warning_ratchet.ex
@test/scoria/ci_policy_contract_test.exs
@.github/workflows/ci.yml
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Accidental policy job DB tests | medium | Contract test keeps postgres only on test job |
| WarningRatchet drift undetected | low | Contract asserts module + non-empty paths in operator docs |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 67-01-01: Verify WARN-05 gates (verify-first)</name>
  <requirement_ids>WARN-05</requirement_ids>
  <files></files>
  <read_first>
    .planning/phases/67-high-signal-warning-ratchet/67-CONTEXT.md
    .github/workflows/ci.yml
  </read_first>
  <action>
Run and record (no code change unless failure):

1. `MIX_ENV=test mix compile --warnings-as-errors`
2. `MIX_ENV=test mix test --warnings-as-errors test/scoria/verification_lanes_test.exs test/scoria/adoption_surface_test.exs`
3. `mix scoria.warning_baseline.check`

If (1) or (2) fail, fix only p0/p1 regressions (lib/ compile or lane-contract test files) — do not start p3 fixes in this plan.

If all pass, add brief note to `docs/operator_verification.md` under WARN-05 subsection listing the two commands as canonical WARN-05 maintainer proof.
  </action>
  <acceptance_criteria>
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
    - `MIX_ENV=test mix test --warnings-as-errors test/scoria/verification_lanes_test.exs test/scoria/adoption_surface_test.exs` exits 0.
    - `mix scoria.warning_baseline.check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors && MIX_ENV=test mix test --warnings-as-errors test/scoria/verification_lanes_test.exs test/scoria/adoption_surface_test.exs && mix scoria.warning_baseline.check</automated>
  </verify>
  <done>WARN-05 canonical surfaces verified green.</done>
</task>

<task type="auto">
  <name>Task 67-01-02: Extend ci_policy_contract_test for WarningRatchet SSOT</name>
  <requirement_ids>WARN-05, WARN-06</requirement_ids>
  <files>test/scoria/ci_policy_contract_test.exs</files>
  <read_first>
    test/scoria/ci_policy_contract_test.exs
    lib/scoria/warning_ratchet.ex
    docs/operator_verification.md
  </read_first>
  <action>
Add test `"WARN-06 documents WarningRatchet maintainer commands"`:

- Read `docs/operator_verification.md`
- Assert content contains `mix scoria.warning_ratchet.test`
- Assert content contains `mix scoria.warning_ratchet.check`
- Assert `length(Scoria.WarningRatchet.high_signal_wae_paths()) > 0`

Add test `"policy job does not run warning_ratchet.test"` (D-17):

- Read `.github/workflows/ci.yml` policy job section (reuse `split_jobs/1`)
- Refute policy section contains `scoria.warning_ratchet`

Do NOT add `warning_ratchet.test` to ci.yml in Phase 67.
  </action>
  <acceptance_criteria>
    - `rg -n "WarningRatchet" test/scoria/ci_policy_contract_test.exs` returns at least one match.
    - `rg -n "warning_ratchet" test/scoria/ci_policy_contract_test.exs` returns at least one match.
    - `MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs</automated>
  </verify>
  <done>CI contract documents WARN-06 SSOT without expanding policy job.</done>
</task>

</tasks>

<verification>
MIX_ENV=test mix compile --warnings-as-errors
MIX_ENV=test mix test --warnings-as-errors test/scoria/verification_lanes_test.exs test/scoria/adoption_surface_test.exs
MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs
</verification>

<success_criteria>
- WARN-05 commands pass
- Contract tests encode D-17/D-19 constraints
</success_criteria>
