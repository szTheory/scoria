---
phase: 67-high-signal-warning-ratchet
plan: 04
type: execute
wave: 4
depends_on: [67-03]
files_modified:
  - test/scoria_web/live/**/*.exs
  - .planning/WARNING-INVENTORY.md
  - .planning/warning-inventory.baseline.json
autonomous: true
requirements: [WARN-06]
must_haves:
  truths:
    - "Inventory-targeted test/scoria_web/live/ p3 clusters at zero (D-02)."
    - "mix scoria.warning_ratchet.test --warnings-as-errors passes (WARN-06 maintainer proof)."
    - "WARNING-INVENTORY.md fixed/deferred table reflects final Phase 67 state (D-06)."
  artifacts:
    - path: ".planning/WARNING-INVENTORY.md"
      provides: "Phase 67 closeout queue"
      contains: "Fixed vs Deferred"
  key_links:
    - from: "mix scoria.warning_ratchet.test"
      to: "test/scoria_web/live/"
      via: "high_signal_wae_paths live glob"
      pattern: "scoria_web/live"
---

<objective>
Complete WARN-06 by clearing p3 clusters in targeted LiveView tests and producing final inventory + maintainer verification evidence for Phase 67 closeout.

Purpose: Last high-signal surface; enables Phase 68 to wire `warning_ratchet.test` into CI test job.
Output: Green `mix scoria.warning_ratchet.test --warnings-as-errors`; final committed inventory artifacts.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/67-high-signal-warning-ratchet/67-CONTEXT.md
@.planning/phases/67-high-signal-warning-ratchet/67-VALIDATION.md
@lib/scoria/warning_ratchet.ex
@.planning/WARNING-BASELINE.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Broad LiveView refactor scope creep | medium | Only files appearing in inventory rows |
| Declaring WARN-06 done with failing ratchet.test | high | Final verify is explicit mix task |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 67-04-01: Fix test/scoria_web/live p3 inventory clusters</name>
  <requirement_ids>WARN-06</requirement_ids>
  <files>test/scoria_web/live/**/*.exs</files>
  <read_first>
    .planning/warning-inventory.baseline.json
    lib/scoria/warning_inventory/cluster.ex
    lib/scoria/warning_ratchet.ex
  </read_first>
  <action>
1. Filter inventory rows where `String.contains?(file, "test/scoria_web/live/")` and cluster in `[:test_unused_binding, :test_dead_default_args, :unclassified_compile]`.

2. Apply same fix patterns as 67-03 (minimal diff per file).

3. Do NOT baseline `:liveview_async_teardown` — remains p4 deferred per D-04 unless it appears as compile_warning (unlikely).

4. Run `MIX_ENV=test mix test --warnings-as-errors` on each modified live test file individually, then full live glob:
```bash
MIX_ENV=test mix test --warnings-as-errors test/scoria_web/live/
```
  </action>
  <acceptance_criteria>
    - `MIX_ENV=test mix test --warnings-as-errors test/scoria_web/live/` exits 0.
    - Inventory full scope shows no p3 clusters for paths under `test/scoria_web/live/` except documented p4 runtime clusters (if any, must have baseline owner in WARNING-BASELINE.md).
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/scoria_web/live/</automated>
  </verify>
  <done>LiveView targeted tests compile clean under WAE.</done>
</task>

<task type="auto">
  <name>Task 67-04-02: Phase 67 closeout — ratchet.test + final inventory</name>
  <requirement_ids>WARN-06</requirement_ids>
  <files>.planning/WARNING-INVENTORY.md, .planning/warning-inventory.baseline.json</files>
  <read_first>
    docs/operator_verification.md
    .planning/phases/67-high-signal-warning-ratchet/67-VALIDATION.md
  </read_first>
  <action>
1. Empty `test/tmp/` if needed.

2. Run maintainer proof stack:
```bash
mix scoria.warning_baseline.check
MIX_ENV=test mix compile --warnings-as-errors
MIX_ENV=test mix test --warnings-as-errors test/scoria/verification_lanes_test.exs test/scoria/adoption_surface_test.exs
MIX_ENV=test mix scoria.warning_ratchet.test --warnings-as-errors
MIX_ENV=test mix scoria.warning_inventory --write --scope full
```

3. Update `.planning/WARNING-INVENTORY.md` **Phase 67 — Fixed vs Deferred** table to final truth:
   - Mark fixed: p3 clusters, knowledge_migration_redefine, high-signal unclassified
   - Mark deferred with owner+expiry: p2 adoption CI WAE, p4 liveview_async_teardown (2026-06-30)

4. Create `.planning/phases/67-high-signal-warning-ratchet/67-VERIFICATION.md` with command outputs and timestamps (mirror 66-VERIFICATION.md structure).

5. Set `nyquist_compliant: true` in `67-VALIDATION.md` frontmatter when all automated rows green.
  </action>
  <acceptance_criteria>
    - `MIX_ENV=test mix scoria.warning_ratchet.test --warnings-as-errors` exits 0.
    - `test -f .planning/phases/67-high-signal-warning-ratchet/67-VERIFICATION.md`
    - `rg -n "WARN-06" .planning/phases/67-high-signal-warning-ratchet/67-VERIFICATION.md` returns a match.
    - `mix scoria.warning_baseline.check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix scoria.warning_ratchet.test --warnings-as-errors && mix scoria.warning_baseline.check</automated>
  </verify>
  <done>Phase 67 WARN-06 maintainer proof documented; inventory artifacts final.</done>
</task>

</tasks>

<verification>
MIX_ENV=test mix scoria.warning_ratchet.test --warnings-as-errors
MIX_ENV=test mix scoria.warning_inventory --write --scope full
mix scoria.warning_baseline.check
MIX_ENV=test mix compile --warnings-as-errors
</verification>

<success_criteria>
- WARN-06 scoped ratchet.test passes
- 67-VERIFICATION.md records evidence
- Deferred debt explicitly documented for Phase 68
</success_criteria>
