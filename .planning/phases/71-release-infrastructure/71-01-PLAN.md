---
phase: 71-release-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - .tool-versions
  - mix.exs
  - test/scoria/changelog_contract_test.exs
autonomous: true
requirements:
  - HEX-02
must_haves:
  truths:
    - "CHANGELOG.md exists with planning-vs-semver preamble and hand-authored [0.1.0] capability bullets."
    - ".tool-versions pins OTP 27 and Elixir 1.19 matching CI."
    - "mix.exs package/0 includes CHANGELOG.md and name scoria for Hex tarball."
  artifacts:
    - path: CHANGELOG.md
      provides: "Release SSOT for 0.1.0 narrative and maintainer traceability"
    - path: .tool-versions
      provides: "Local and publish workflow Erlang pin"
  key_links:
    - from: mix.exs
      to: CHANGELOG.md
      via: package files list
---

<objective>
Bootstrap release documentation and toolchain pins: hand-authored CHANGELOG for first Hex release, `.tool-versions`, and `mix.exs` package metadata — without any publish workflow changes yet.

Purpose: HEX-02 prep (CHANGELOG, version namespace clarity) before release-please references `changelog-path`.
Output: Green optional `changelog_contract_test.exs`; `MIX_ENV=test mix compile` passes.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/71-release-infrastructure/71-CONTEXT.md
@.planning/phases/71-release-infrastructure/71-RESEARCH.md
@lib/scoria/adopter_doc_contract.ex
@.planning/MILESTONES.md
@mix.exs
</context>

<threat_model>
<trust_boundaries>
- CHANGELOG is public adopter/maintainer narrative — must not leak secrets or internal-only credentials.
- mix.exs package files list controls Hex tarball contents.
</trust_boundaries>
<stride_register>
- threat_id: T-71-01
  category: Tampering
  component: CHANGELOG.md / mix.exs
  disposition: mitigate
  mitigation_plan: Contract test refutes milestone-banner patterns; no env vars in CHANGELOG; package files whitelist only public paths.
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 71-01-01: Add CHANGELOG.md with preamble and [0.1.0] section</name>
  <requirement_ids>HEX-02</requirement_ids>
  <files>CHANGELOG.md</files>
  <read_first>
    .planning/phases/71-release-infrastructure/71-CONTEXT.md
    lib/scoria/adopter_doc_contract.ex
    .planning/MILESTONES.md
    README.md
  </read_first>
  <action>
Create `CHANGELOG.md` at repo root with:

1. Keep a Changelog header and semver link boilerplate (bootstrap skill template).

2. Section **`## Planning milestones vs Hex releases`** (verbatim intent from CONTEXT D-01): explain `[0.x.y]` = published Hex releases; `v2.x` in `.planning/MILESTONES.md` = internal planning tranches, not a second install axis; no Hex `2.7.0` to match GSD milestone.

3. `## [Unreleased]` with empty or minimal placeholder (release-please owns post-bootstrap).

4. `## [0.1.0]` with:
   - 2–4 sentence integrator lead (first public Hex packaging of in-repo capability through v2.6 closeout).
   - `### Added` with **five capability bullets** using nouns from `Scoria.AdopterDocContract.shipped_capability_nouns/0` themes: default runtime, bounded handoff, semantic fast path, optional knowledge, upgrade-safe install — each bullet includes one proof command or public API reference.
   - One **maintainer CI trust** bullet (warning baseline/ratchet, policy→test topology, `mix scoria.test.ci_trust`) — not framed as adopter feature.
   - `### Roadmap traceability` table or bullets for **v2.1, v2.3, v2.4, v2.5, v2.6** with ship dates from MILESTONES.md and links to `.planning/MILESTONES.md` anchors — **do not include v2.2**.
   - **No** `### Summary` subsection under any version heading (D-06).
   - **No** milestone theme names (Relay, Crucible, etc.) in `### Added`.
  </action>
  <acceptance_criteria>
    - `rg 'Planning milestones vs Hex releases' CHANGELOG.md` matches
    - `rg '## \[0.1.0\]' CHANGELOG.md` matches
    - `rg '### Summary' CHANGELOG.md` returns no match under version sections (or zero matches)
    - `rg 'v2.2' CHANGELOG.md` returns no match
    - `rg 'default runtime' CHANGELOG.md` matches (case-insensitive OK via rg -i)
  </acceptance_criteria>
  <verify>
    <automated>rg -q 'Planning milestones vs Hex releases' CHANGELOG.md && rg -q '## \[0.1.0\]' CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG.md committed with preamble, [0.1.0], and traceability; no Summary subsections.</done>
</task>

<task type="auto">
  <name>Task 71-01-02: Add .tool-versions and extend mix.exs package metadata</name>
  <requirement_ids>HEX-02</requirement_ids>
  <files>.tool-versions, mix.exs</files>
  <read_first>
    .github/workflows/ci.yml
    mix.exs
    CHANGELOG.md
  </read_first>
  <action>
Create `.tool-versions` with lines matching CI (CONTEXT D-20):
```
erlang 27.3.2
elixir 1.19.5-otp-27
```
(Use patch versions from erlef/setup-beam in ci.yml if different — OTP **27** and Elixir **1.19** major/minor are mandatory.)

Update `mix.exs` `package/0`:
- Add `name: "scoria"` inside the keyword list (Hex tarball name; app remains `:scoria`).
- Add `"CHANGELOG.md"` to the `files:` list.

Do not change `@version` from `"0.1.0"`.
  </action>
  <acceptance_criteria>
    - `rg '^erlang 27' .tool-versions` matches
    - `rg '^elixir 1.19' .tool-versions` matches
    - `rg 'name: "scoria"' mix.exs` matches
    - `rg 'CHANGELOG.md' mix.exs` matches
    - `MIX_ENV=test mix compile` exits 0
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix compile</automated>
  </verify>
  <done>.tool-versions and mix.exs package metadata aligned for Hex bootstrap.</done>
</task>

<task type="auto">
  <name>Task 71-01-03: Add changelog_contract_test.exs</name>
  <requirement_ids>HEX-02</requirement_ids>
  <files>test/scoria/changelog_contract_test.exs</files>
  <read_first>
    CHANGELOG.md
    lib/scoria/adopter_doc_contract.ex
    test/scoria/adoption_surface_test.exs
  </read_first>
  <action>
Create `test/scoria/changelog_contract_test.exs` asserting:
- CHANGELOG contains preamble heading text `Planning milestones vs Hex releases`
- For each noun substring in `Scoria.AdopterDocContract.shipped_capability_nouns/0`, at least one appears in CHANGELOG (downcase compare)
- CHANGELOG does **not** contain `Scoria is shipped through` (milestone banner refute)
- CHANGELOG does **not** match regex for `### Summary` immediately under a `## [` version heading (simple refute: refute content =~ `### Summary` after `## [0.1.0]` block start, or global refute `### Summary` if none allowed anywhere)

Keep test async: true, no DB.
  </action>
  <acceptance_criteria>
    - `MIX_ENV=test mix test test/scoria/changelog_contract_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/changelog_contract_test.exs</automated>
  </verify>
  <done>Executable CHANGELOG contract guards capability nouns and refutes milestone banner.</done>
</task>

</tasks>

<verification>
- `MIX_ENV=test mix test test/scoria/changelog_contract_test.exs test/scoria/ci_policy_contract_test.exs` exits 0
- `curl -s -o /dev/null -w "%{http_code}" https://hex.pm/api/packages/scoria` prints `404` (record in plan 04 verification artifact)
</verification>
