---
phase: 71-release-infrastructure
plan: 04
type: execute
wave: 4
depends_on:
  - 71-01-PLAN.md
  - 71-02-PLAN.md
  - 71-03-PLAN.md
files_modified:
  - docs/operator_verification.md
  - README.md
  - .planning/phases/71-release-infrastructure/71-VERIFICATION.md
  - test/scoria/ci_policy_contract_test.exs
autonomous: false
requirements:
  - HEX-01
  - HEX-02
must_haves:
  truths:
    - "Operator guide documents Hex release, HEX_API_KEY, Release Please flow, and manual recovery."
    - "README has one-line maintainer tease linking to operator Hex section."
    - "71-VERIFICATION.md records Hex 404 attestation and gate-zero CI green or bounded waiver."
  artifacts:
    - path: docs/operator_verification.md
      provides: "Maintainer Hex release & recovery SSOT"
    - path: .planning/phases/71-release-infrastructure/71-VERIFICATION.md
      provides: "Phase 71 verification ledger"
  key_links:
    - from: README.md
      to: docs/operator_verification.md
      via: anchor hex-release--recovery-maintainers
---

<objective>
Document maintainer release operations, add README tease, and complete phase verification artifacts including gate-zero remote CI attestation (or bounded waiver).

Purpose: HEX-01/HEX-02 maintainer prep + ROADMAP success criteria 3–5.
Output: `71-VERIFICATION.md` with Hex name check, workflow permissions note, Release PR 0.1.0 observation checklist, gate-zero evidence.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/71-release-infrastructure/71-CONTEXT.md
@docs/operator_verification.md
@README.md
@.planning/milestones/v2.6-MILESTONE-AUDIT.md
</context>

<threat_model>
<trust_boundaries>
- Operator docs describe secret names only — never commit HEX_API_KEY values.
- Manual recovery docs must warn against re-publishing existing Hex versions.
</trust_boundaries>
<stride_register>
- threat_id: T-71-03
  category: Information Disclosure
  component: docs/operator_verification.md
  disposition: mitigate
  mitigation_plan: Document gh secret set pattern; no example API keys; recovery warns to check hex.pm first.
- threat_id: T-71-04
  category: Repudiation
  component: 71-VERIFICATION.md
  disposition: mitigate
  mitigation_plan: Record workflow run URL + SHA for gate-zero; waiver requires waiver_id and deferred_to Phase 72.
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 71-04-01: Add Hex release & recovery section to operator guide</name>
  <requirement_ids>HEX-01, HEX-02</requirement_ids>
  <files>docs/operator_verification.md</files>
  <read_first>
    docs/operator_verification.md
    .planning/phases/71-release-infrastructure/71-CONTEXT.md
    .github/workflows/release-please.yml
    .github/workflows/hex-publish.yml
  </read_first>
  <action>
Insert new section **`## Hex release & recovery (maintainers)`** immediately **after** the `### CI gate map (maintainers)` section (before the next `###` sibling), per CONTEXT D-26.

Include subsections:

1. **Version namespaces** — cross-link planning milestones vs Hex semver (point to CHANGELOG preamble; do not duplicate full text).

2. **HEX_API_KEY** (D-27) — `mix hex.user key generate` (api scope) → `gh secret set HEX_API_KEY --repo szTheory/scoria`; note org keys if `organization:` added later.

3. **GitHub workflow permissions** (D-17) — `gh api -X PUT /repos/szTheory/scoria/actions/permissions/workflow -f default_workflow_permissions=write -F can_approve_pull_request_reviews=true`

4. **RELEASE_PLEASE_TOKEN** (D-18) — optional PAT when GITHUB_TOKEN cannot open Release PRs or chain-trigger CI.

5. **Default path** (D-28) — Release Please on main → review Release PR (CI green on `release-please--**` branch) → merge → publish-hex (Phase 72).

6. **Manual recovery** (D-29) — `hex-publish.yml` with `gh workflow run hex-publish.yml --ref vX.Y.Z -f tag=vX.Y.Z -f release_version=X.Y.Z`; do not re-publish version already on hex.pm; sync `.release-please-manifest.json` if needed.

7. **Phase 71 boundary** — no production `mix hex.publish`; Release PR must target **0.1.0**; do not merge Release PR until Phase 72.

8. **Executable SSOT** — `.github/workflows/ci-verify.yml`, `release-please.yml`, `hex-publish.yml`.

Add markdown anchor `{#hex-release--recovery-maintainers}` on the heading for README/deep links.
  </action>
  <acceptance_criteria>
    - `rg '## Hex release & recovery' docs/operator_verification.md` matches
    - `rg 'HEX_API_KEY' docs/operator_verification.md` matches
    - `rg 'hex-publish.yml' docs/operator_verification.md` matches
    - `rg 'release-please--' docs/operator_verification.md` matches
    - `rg 'default_workflow_permissions=write' docs/operator_verification.md` matches
    - Section appears after `### CI gate map (maintainers)` (index_of gate map &lt; index_of Hex section)
  </acceptance_criteria>
  <verify>
    <automated>rg -q '## Hex release & recovery' docs/operator_verification.md && rg -q 'HEX_API_KEY' docs/operator_verification.md</automated>
  </verify>
  <done>Maintainer Hex runbook lives in operator guide after CI gate map.</done>
</task>

<task type="auto">
  <name>Task 71-04-02: README maintainer tease and ci_policy doc anchors</name>
  <requirement_ids>HEX-02</requirement_ids>
  <files>README.md, test/scoria/ci_policy_contract_test.exs</files>
  <read_first>
    README.md
    docs/operator_verification.md
    test/scoria/ci_policy_contract_test.exs
  </read_first>
  <action>
In `README.md`, add **one line** near existing maintainer/CI links (D-30), e.g.:
`Maintainers: Hex release & recovery — [operator guide](docs/operator_verification.md#hex-release--recovery-maintainers).`
Do not add HEX_API_KEY instructions or full release runbook to README.

Extend `ci_policy_contract_test.exs` with test asserting operator guide Hex section exists and README links to `#hex-release--recovery-maintainers`.
  </action>
  <acceptance_criteria>
    - `rg 'hex-release--recovery-maintainers' README.md` matches
    - `MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs</automated>
  </verify>
  <done>README tease and doc contract tests pass.</done>
</task>

<task type="checkpoint">
  <name>Task 71-04-03: Gate-zero remote CI attestation or waiver</name>
  <requirement_ids>HEX-01</requirement_ids>
  <files>.planning/phases/71-release-infrastructure/71-VERIFICATION.md</files>
  <read_first>
    .planning/phases/71-release-infrastructure/71-CONTEXT.md
    .planning/milestones/v2.6-MILESTONE-AUDIT.md
    .github/workflows/ci.yml
  </read_first>
  <action>
Create or update `.planning/phases/71-release-infrastructure/71-VERIFICATION.md` with:

**Hex name availability (D-21):**
- Record `curl -s -o /dev/null -w "%{http_code}" https://hex.pm/api/packages/scoria` result (expect `404`)
- Record date and `package/0` name field from mix.exs

**release-please bootstrap SHA:** SHA used in `release-please-config.json` bootstrap-sha

**Gate-zero (D-22–D-24) — preferred path:**
- Open integration PR with phase 71 tree (if not already on main)
- Record green `ci.yml` / `CI Verify` workflow run: URL, commit SHA, `policy` + `test`/`verify` job success
- Note whether attestation is pre- or post- `release-please.yml` landing

**Waiver path (only if remote CI blocked):**
- `waiver_id: gate-zero-71`
- Local evidence: `mix scoria.test.ci_trust` output summary, v2.6 audit reference
- `deferred_to: Phase 72 publish commit`
- Not open-ended

**Phase 71 Release PR check (D-19):** checklist item — after merge to main, confirm Release PR targets `0.1.0`; do not merge in Phase 71

**Workflow permissions:** checkbox that D-17 gh api command was run or scheduled

Human must complete checkpoint before phase verify-work passes.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/71-release-infrastructure/71-VERIFICATION.md`
    - File contains substring `Hex name` and (`workflow run` OR `waiver_id`)
    - File contains `404` for hex.pm check OR documents taken name with remediation
  </acceptance_criteria>
  <verify>
    <manual>Review 71-VERIFICATION.md for gate-zero URL or valid waiver block</manual>
  </verify>
  <done>Verification ledger complete with Hex attestation and gate-zero evidence or waiver.</done>
</task>

</tasks>

<verification>
- `MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs test/scoria/changelog_contract_test.exs` exits 0
- `MIX_ENV=test mix scoria.test.ci_trust` exits 0 (local parity)
- Human: Release PR targets 0.1.0 after main merge (documented in 71-VERIFICATION.md)
</verification>
