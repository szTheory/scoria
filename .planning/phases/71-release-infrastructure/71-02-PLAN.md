---
phase: 71-release-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 71-01-PLAN.md
files_modified:
  - .github/workflows/ci-verify.yml
  - .github/workflows/ci.yml
  - test/scoria/ci_policy_contract_test.exs
autonomous: true
requirements:
  - HEX-01
must_haves:
  truths:
    - "policy and test jobs live in ci-verify.yml callable via workflow_call."
    - "ci.yml triggers include release-please branches and workflow_dispatch."
    - "ci_policy_contract_test asserts lane order in ci-verify.yml and ci.yml uses reusable workflow."
  artifacts:
    - path: .github/workflows/ci-verify.yml
      provides: "Reusable CI SSOT for PR, release, and hex-publish verify"
    - path: .github/workflows/ci.yml
      provides: "Thin trigger wrapper"
  key_links:
    - from: .github/workflows/ci.yml
      to: .github/workflows/ci-verify.yml
      via: "uses: ./.github/workflows/ci-verify.yml"
---

<objective>
Extract the existing two-job CI topology into a reusable `ci-verify.yml` and slim `ci.yml` to triggers + `workflow_call`, extending triggers for release-please branches.

Purpose: HEX-01 prep — publish and release workflows will reuse the same verify bar (CONTEXT D-08–D-14).
Output: Green `ci_policy_contract_test.exs` against new layout.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/71-release-infrastructure/71-CONTEXT.md
@.planning/phases/71-release-infrastructure/71-RESEARCH.md
@.github/workflows/ci.yml
@test/scoria/ci_policy_contract_test.exs
@lib/scoria/verification_lanes.ex
</context>

<threat_model>
<trust_boundaries>
- CI workflows execute arbitrary Mix commands with repository secrets only on publish jobs (Phase 72).
- Reusable workflow must not widen permissions beyond current ci.yml (contents: read).
</trust_boundaries>
<stride_register>
- threat_id: T-71-01
  category: Tampering
  component: .github/workflows/ci-verify.yml
  disposition: mitigate
  mitigation_plan: Contract tests lock step order; no secret env in ci-verify; postgres only on test job.
- threat_id: T-71-05
  category: Denial of Service
  component: CI policy job
  disposition: accept
  mitigation_plan: Preserve baseline check before expensive compile (existing topology).
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 71-02-01: Create ci-verify.yml with policy and test jobs</name>
  <requirement_ids>HEX-01</requirement_ids>
  <files>.github/workflows/ci-verify.yml</files>
  <read_first>
    .github/workflows/ci.yml
    docs/operator_verification.md
  </read_first>
  <action>
Create `.github/workflows/ci-verify.yml`:

```yaml
name: CI Verify

on:
  workflow_call:

permissions:
  contents: read
```

Move **verbatim** the existing `jobs:` block from `ci.yml` (`policy` and `test` jobs including comments, services, env, steps) under this file's `jobs:` key. Preserve:
- OTP 27 / Elixir 1.19 in setup-beam
- policy: baseline → deps → compile WAE → lane-contract test paths
- test: postgres 55432, release_preview, ecto, adoption, runtime_to_handoff, tmp_preflight, full WAE, knowledge

Add header comment noting SSOT for PR CI, release-please verify, and hex-publish recovery; pointer to `docs/operator_verification.md#ci-gate-map-maintainers`.
  </action>
  <acceptance_criteria>
    - `rg 'workflow_call' .github/workflows/ci-verify.yml` matches
    - `rg 'mix scoria.warning_baseline.check' .github/workflows/ci-verify.yml` matches
    - `rg 'MIX_ENV=dev mix scoria.release_preview' .github/workflows/ci-verify.yml` matches
    - `rg 'services:' .github/workflows/ci-verify.yml` matches with postgres
  </acceptance_criteria>
  <verify>
    <automated>test -f .github/workflows/ci-verify.yml && rg -q 'workflow_call' .github/workflows/ci-verify.yml</automated>
  </verify>
  <done>ci-verify.yml contains full policy+test topology.</done>
</task>

<task type="auto">
  <name>Task 71-02-02: Slim ci.yml and extend triggers</name>
  <requirement_ids>HEX-01</requirement_ids>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .github/workflows/ci-verify.yml
    .planning/phases/71-release-infrastructure/71-CONTEXT.md
  </read_first>
  <action>
Replace `ci.yml` job definitions with:

```yaml
jobs:
  verify:
    uses: ./.github/workflows/ci-verify.yml
```

Update `on:` block (CONTEXT D-12):
```yaml
on:
  push:
    branches:
      - main
      - 'release-please--**'
  pull_request:
    branches:
      - main
  workflow_dispatch:
```

Keep top-of-file maintainer comment block (≥5 comment lines) updated to mention reusable verify and release-please branch CI. Preserve `permissions: contents: read`.
  </action>
  <acceptance_criteria>
    - `rg 'uses: ./.github/workflows/ci-verify.yml' .github/workflows/ci.yml` matches
    - `rg "release-please--" .github/workflows/ci.yml` matches
    - `rg 'workflow_dispatch' .github/workflows/ci.yml` matches
    - `rg '^  policy:' .github/workflows/ci.yml` returns no match (jobs not inlined)
  </acceptance_criteria>
  <verify>
    <automated>rg -q 'uses: ./.github/workflows/ci-verify.yml' .github/workflows/ci.yml</automated>
  </verify>
  <done>ci.yml is trigger-only wrapper with release-please branch coverage.</done>
</task>

<task type="auto">
  <name>Task 71-02-03: Update ci_policy_contract_test for ci-verify SSOT</name>
  <requirement_ids>HEX-01</requirement_ids>
  <files>test/scoria/ci_policy_contract_test.exs</files>
  <read_first>
    test/scoria/ci_policy_contract_test.exs
    .github/workflows/ci-verify.yml
    .github/workflows/ci.yml
    lib/scoria/verification_lanes.ex
  </read_first>
  <action>
Refactor `Scoria.CiPolicyContractTest` to read **`.github/workflows/ci-verify.yml`** for policy/test job assertions (replace `File.read!(".github/workflows/ci.yml")` in topology tests).

Add tests:
- `ci.yml` contains `uses: ./.github/workflows/ci-verify.yml`
- `ci.yml` triggers include `release-please--**` and `workflow_dispatch`
- `ci-verify.yml` exists and contains `on: workflow_call`

Update `split_jobs/1` to split `ci-verify.yml` content (same `\n  test:` delimiter).

Keep existing ordering assertions (baseline before compile, release_preview before adoption, etc.) against ci-verify content.

Update operator doc reference strings only if tests assert `ci.yml` path in prose — gate map may still mention ci.yml as entrypoint; add note "executable jobs in ci-verify.yml" if a test fails on doc string.
  </action>
  <acceptance_criteria>
    - `MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs` exits 0
    - `rg 'ci-verify.yml' test/scoria/ci_policy_contract_test.exs` matches
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs</automated>
  </verify>
  <done>Contract tests enforce reusable CI verify layout.</done>
</task>

</tasks>

<verification>
- `MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs` exits 0
- `actionlint .github/workflows/ci.yml .github/workflows/ci-verify.yml` exits 0 if actionlint installed; otherwise skip with note in SUMMARY
</verification>
