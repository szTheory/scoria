---
phase: 01.1-address-tech-debt-orchestrator-live-wiring-follow-ups
plan: 03
type: execute
wave: 3
depends_on: [01.1-01, 01.1-02]
files_modified:
  - lib/scoria/observe/telemetry.ex
  - lib/scoria_web/live/orchestrator_live.ex
  - test/scoria_web/live/orchestrator_live_integration_test.exs
autonomous: true
requirements: [ORCH-LIVE-01]
must_haves:
  truths:
    - "Public Scoria.Observe.Telemetry.emit_span_delta/1 wraps :telemetry.execute for [:scoria, :observe, :span, :delta] (D-139)."
    - "Integration test proves Runtime.start_run → handler emit_span_delta → coalesced token_previews on active LLM span in DOM (D-140)."
    - "maybe_clear_token_preview/2 cancels pending token_timers via Process.cancel_timer/1 when span completes (D-141)."
    - "Integration tests do not use raw :telemetry.execute for delta proof — use emit_span_delta/1 (D-129 spirit)."
    - "ReqLLM streaming adapter not shipped; closeout_order unchanged."
  artifacts:
    - path: "lib/scoria/observe/telemetry.ex"
      provides: "emit_span_delta/1 public API"
      contains: "def emit_span_delta"
    - path: "lib/scoria_web/live/orchestrator_live.ex"
      provides: "timer cancel on span complete"
      contains: "cancel_timer"
    - path: "test/scoria_web/live/orchestrator_live_integration_test.exs"
      provides: "token coalesce E2E proof"
      contains: "emit_span_delta"
  key_links:
    - from: "lib/scoria/observe/telemetry.ex"
      to: "lib/scoria_web/live/orchestrator_live.ex"
      via: "PubSub {:trace_delta, _} → append_trace_delta"
      pattern: "emit_span_delta"
    - from: "lib/scoria_web/live/orchestrator_live.ex"
      to: "lib/scoria_web/live/orchestrator_live.ex"
      via: "maybe_clear_token_preview + token_timers"
      pattern: "Process.cancel_timer"
---

<objective>
Close token-delta audit gap: public emit API, LiveView coalesce proof, stale timer fix.

Purpose: Prove {:trace_delta} → 75ms coalesce → TraceTreeComponent preview without shipping ReqLLM streaming (D-138–D-141).
Output: emit_span_delta/1 exported; integration test shows coalesced preview text; timer cancelled on span stop.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01.1-address-tech-debt-orchestrator-live-wiring-follow-ups/01.1-CONTEXT.md
@.planning/phases/01.1-address-tech-debt-orchestrator-live-wiring-follow-ups/01.1-RESEARCH.md
@lib/scoria/observe/telemetry.ex
@lib/scoria_web/live/orchestrator_live.ex
@test/scoria_web/live/orchestrator_live_integration_test.exs
@lib/scoria/observe/adapters/req_llm.ex

Plan 01.1-01 must land first (delta redaction in handler). Do NOT implement ReqLLM streaming adapter.
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-01.1-05 | DoS — stale token timer flush after span complete | low | Process.cancel_timer in maybe_clear_token_preview (D-141) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 01.1-03-01: Add Telemetry.emit_span_delta/1 public API</name>
  <files>lib/scoria/observe/telemetry.ex</files>
  <read_first>
    lib/scoria/observe/telemetry.ex
    lib/scoria/observe/operator_broadcast.ex
    .planning/phases/01.1-address-tech-debt-orchestrator-live-wiring-follow-ups/01.1-CONTEXT.md
  </read_first>
  <action>
1. Add public function to `Scoria.Observe.Telemetry`:

```elixir
def emit_span_delta(metadata) when is_map(metadata) do
  :telemetry.execute([:scoria, :observe, :span, :delta], %{}, metadata)
end
```

2. Add `@doc` noting: future ReqLLM streaming adapter should call this; integration tests must use this instead of raw `:telemetry.execute` for delta events (D-139).

3. No behavior change beyond export — handler from 01.1-01 already redacts + broadcasts.
  </action>
  <acceptance_criteria>
    - `rg -n 'def emit_span_delta' lib/scoria/observe/telemetry.ex` returns match.
    - `rg -n ':telemetry.execute\\(\\[:scoria, :observe, :span, :delta\\]' lib/scoria/observe/telemetry.ex` returns match inside emit_span_delta only (not duplicated elsewhere).
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Public delta emit API ready for v2.12 streaming adapter.</done>
</task>

<task type="auto">
  <name>Task 01.1-03-02: Cancel token timers when span completes</name>
  <files>lib/scoria_web/live/orchestrator_live.ex</files>
  <read_first>
    lib/scoria_web/live/orchestrator_live.ex
    .planning/phases/01-orchestrator-live-wiring-runtime-pubsub-trace-broadcast-and-/01-REVIEW.md
  </read_first>
  <action>
Update `maybe_clear_token_preview/2` (D-141):

When `Map.get(span_view, :end_time)` is truthy:
1. If `socket.assigns.token_timers` has entry for `span_view.id`, call `Process.cancel_timer(ref)` (ignore `:error` if already fired)
2. Delete span_id from `:token_timers` assign
3. Delete span_id from `:token_buffers` assign (avoid stale buffer)
4. Existing: delete from `:token_previews`

When no end_time, return socket unchanged.
  </action>
  <acceptance_criteria>
    - `rg -n 'cancel_timer' lib/scoria_web/live/orchestrator_live.ex` returns match inside `maybe_clear_token_preview`.
    - `rg -n 'token_timers' lib/scoria_web/live/orchestrator_live.ex` shows delete on span complete path.
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Stale coalesce timer cannot fire after span stop.</done>
</task>

<task type="auto">
  <name>Task 01.1-03-03: Integration test token delta coalesce on trace tree</name>
  <files>test/scoria_web/live/orchestrator_live_integration_test.exs</files>
  <read_first>
    test/scoria_web/live/orchestrator_live_integration_test.exs
    lib/scoria/observe/telemetry.ex
    lib/scoria/observe/adapters/req_llm.ex
    lib/scoria_web/live/orchestrator_live.ex
    lib/scoria_web/components/trace_tree_component.ex
  </read_first>
  <action>
1. Add handler `emit_token_deltas/2` in integration `Handlers` module:
   - Create trace + LLM span via Repo (or ReqLLM stop) so trace tree has an active span with known `span_id`
   - Call `Scoria.Observe.Telemetry.emit_span_delta/1` twice with same `tenant_id`, `trace_id`, `span_id`, chunks `"Hello "` and `"world"` (metadata must include required keys)
   - Return `{:ok, %{}}`

2. Add workflow step kind `"token_delta"` (or reuse `"llm"` with new handler) in `start_run` for this test only.

3. Add test `"token delta coalesces into span preview via producer path"`:
   - `live` + `Runtime.start_run` with token-delta handler
   - **No** `:telemetry.execute` in test file for delta (use handler → emit_span_delta only)
   - `eventually` assert `render(view)` contains coalesced substring `"Hello world"` (or both parts after 75ms+ coalesce — use `Process.sleep(100)` inside eventually if needed)
   - Assert preview appears in context of span name from handler (e.g. `Handlers.span_name()` or fixture span name visible in tree)

4. Update `setup` workflow_runtime_handlers if needed to register new handler key.

5. Semantic fast-path: same file already pinned — no mix task change unless new test count requires doc update only.
  </action>
  <acceptance_criteria>
    - `rg -n 'emit_span_delta' test/scoria_web/live/orchestrator_live_integration_test.exs` returns match in Handlers module (not raw telemetry.execute for delta).
    - `rg -n 'token delta coalesces' test/scoria_web/live/orchestrator_live_integration_test.exs` returns match.
    - `rg -n ':telemetry.execute\\(\\[:scoria, :observe, :span, :delta\\]' test/scoria_web/live/orchestrator_live_integration_test.exs` returns no matches.
    - `MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_integration_test.exs` exits 0.
    - `MIX_ENV=test mix test.semantic_fast_path --warnings-as-errors` exits 0 (with SCORIA_DB_* env if project requires).
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_integration_test.exs</automated>
  </verify>
  <done>End-to-end token preview path proven without ReqLLM streaming.</done>
</task>

</tasks>

<verification>
```bash
MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_integration_test.exs
SCORIA_DB_PORT=55432 SCORIA_DB_PASSWORD=postgres MIX_ENV=test mix test.semantic_fast_path --warnings-as-errors
mix compile --warnings-as-errors
```
</verification>
