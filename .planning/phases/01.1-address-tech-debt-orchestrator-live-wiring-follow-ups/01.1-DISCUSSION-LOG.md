# Phase 01.1: Address tech debt — orchestrator live wiring follow-ups — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `01.1-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-05-30
**Phase:** 01.1-address-tech-debt-orchestrator-live-wiring-follow-ups
**Areas discussed:** Scope triage, Approval decision E2E, Redaction hardening, Token delta path
**Mode:** User selected all areas; requested research-backed one-shot recommendations (no per-question loop)

---

## Scope triage

| Option | Description | Selected |
|--------|-------------|----------|
| Full audit backlog in 01.1 | UAT + E2E + redaction + streaming + UI stubs + shim removal | |
| Surgical hardening slice (3 waves) | Redaction + approval E2E + token delta proof only | ✓ |
| Defer all to v2.12 | Ship v2.11 as-is with audit notes | |

**User's choice:** Approve surgical hardening slice after research-backed recommendations presented.
**Notes:** UAT is workflow not code. ReqLLM streaming explicitly deferred since D-125/D-128. UI stubs pre-existing outside ORCH-LIVE core.

---

## Approval decision E2E

| Option | Description | Selected |
|--------|-------------|----------|
| Leave unit-tier send/2 only | Accept audit partial-proof status | |
| Integration test approve + reject paths | Runtime.start_run → click Approve/Reject → modal + inbox | ✓ |
| Widen closeout lane | Add to adoption/closeout proof | |

**User's choice:** Integration tests for approve and reject without send/2; closeout unchanged.
**Notes:** Matches LangGraph interrupt + signal trust model. Low cost, high operator-trust ROI.

---

## Redaction hardening

| Option | Description | Selected |
|--------|-------------|----------|
| Defer until ReqLLM streaming lands | Fix when deltas carry real LLM output | |
| Fix delta + hydrate paths now | Redactor on telemetry delta + span_view_from_record | ✓ |
| UI-only redaction in LiveView | Scrub at render time only | |

**User's choice:** Observe-layer delta redaction + hydrate re-redact with unit tests.
**Notes:** Langfuse/Braintrust redact-at-every-egress. Mirror D-118 ordering before production chunks on PubSub.

---

## Token delta path

| Option | Description | Selected |
|--------|-------------|----------|
| Ship full ReqLLM streaming in 01.1 | Adapter emits real stream deltas | |
| emit_span_delta/1 + integration test + timer polish | Prove coalesce path; defer adapter | ✓ |
| Defer entirely to v2.12 | No delta tests in 01.1 | |

**User's choice:** Public Telemetry.emit_span_delta/1 API, workflow-handler integration test, cancel token_timers on span complete.
**Notes:** D-125/D-128 streaming adapter deferred. Prompts §9.3 coalesce footguns addressed. ReqLLM remains stop-only until v2.12+.

---

## Claude's Discretion

- Exact test handler naming and one vs two approval test cases
- `emit_span_delta/1` module placement (Telemetry vs Observe helper)
- Plan wave file split within D-132–D-141 bounds

## Deferred Ideas

- `/gsd-verify-work` UAT
- Full ReqLLM streaming adapter
- load_metadata / sample_evidence UI stubs
- {:new_trace} shim removal
- Host Application Observe auto-attach code change
- OTel, Presence, mobile modal polish
