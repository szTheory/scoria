---
status: clean
phase: 01.1-address-tech-debt-orchestrator-live-wiring-follow-ups
reviewed: 2026-05-30
depth: standard
files_reviewed: 5
critical: 0
warning: 0
info: 5
total: 5
---

# Phase 01.1 Code Review

Standard-depth review of ORCH-LIVE-01 hardening: delta/hydrate redaction defense-in-depth, approval producer-path integration tests, `emit_span_delta/1`, and token coalesce + timer cancel.

**Scope:** `01.1-01-SUMMARY.md`, `01.1-02-SUMMARY.md`, `01.1-03-SUMMARY.md` — five source files listed in review request (plus planning context).

**Prior art:** Phase 01 `01-REVIEW.md` warnings #1–#2 and optional follow-up #3 are addressed in this phase.

## Summary

Phase 01.1 delivers the intended surgical hardening. Delta telemetry follows **redact → scrub chunk → broadcast**; DB hydrate re-redacts before projection; approval approve/reject paths are proven on the `Runtime.start_run/2` producer path without `send/2`; secrets do not appear in DOM in integration tests. No blocking defects found for ORCH-LIVE-01 hardening scope (D-132).

## Findings

| Severity | Area | File | Finding |
|----------|------|------|---------|
| info | Redaction — streaming chunks | `lib/scoria/observe/redactor.ex`, `lib/scoria/observe/telemetry.ex` | `scrub_text/1` replaces deny-list `key=value` tokens (`~r/(key)=([^\s]+)/iu`). Secrets in JSON (`{"api_key":"…"}`), Bearer headers, or `key=value with spaces` are not scrubbed. Acceptable for 01.1 proof (`api_key=super-secret-key`); document/enhance before production ReqLLM streaming (D-125/D-128). |
| info | Redaction — API discipline | `lib/scoria/observe/telemetry.ex` | `emit_span_delta/1` is the documented entry point; nothing prevents raw `:telemetry.execute([:scoria, :observe, :span, :delta], …)`. BEAM trust boundary — same class as public `OperatorBroadcast.broadcast/2`. |
| info | Correctness — timer cancel | `lib/scoria_web/live/orchestrator_live.ex` | D-141: `maybe_clear_token_preview/2` calls `Process.cancel_timer/1` and clears buffers/previews. No automated test; stale `{:flush_tokens, _}` after span stop can re-insert an empty `token_previews` entry but `TraceTreeComponent.llm_token_preview?/2` gates on `is_nil(end_time)`. |
| info | Test quality — approve path | `test/scoria_web/live/orchestrator_live_integration_test.exs` | `"approve decision clears modal via producer path"` asserts approval `approved` and DOM cleared, not that the workflow run advances beyond `waiting_for_approval` after `Resume.resume_run/1`. Reject path coverage is stronger (run stays paused). |
| info | Redaction — HITL strings | `lib/scoria_web/live/orchestrator_live.ex`, `lib/scoria/workflows/remote_approval_projection.ex` | Modal renders redacted `arguments_preview` only (D-121). `reason` and other string projection fields are not passed through `Redactor`/`scrub_text` — out of 01.1 scope but relevant if operators paste secrets into `reason`. |

## Phase 01 warning closure

| Phase 01 finding | 01.1 resolution |
|------------------|-----------------|
| Delta path skipped `Redactor.redact/1` | **Closed** — D-133 + `scrub_text` for embedded `key=value` |
| Hydrate skipped re-redact | **Closed** — D-134 `span_view_from_record/1` |
| Optional timer cancel on span complete | **Closed** — D-141 `cancel_timer` in `maybe_clear_token_preview/2` |

## Recommended follow-ups (non-blocking)

1. Before ReqLLM streaming: extend chunk scrubbing (JSON paths, quoted values, or adapter-side redaction) and add regression tests for realistic token streams.
2. Add integration test: span `:stop` with `end_time` after deltas → assert no preview flash and no stale coalesce (proves D-141 under race).
3. Extend approve integration to assert workflow run status after successful `Resume.resume_run/1`.
4. Consider `scrub_text/1` on `reason` if operators can supply free-form text with embedded secrets.

---
*Phase: 01.1-address-tech-debt-orchestrator-live-wiring-follow-ups*
*Reviewed: 2026-05-30*
