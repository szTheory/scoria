---
phase: 53-operator-evidence-and-lane-guidance
plan: 03
type: execute
wave: 2
depends_on: [53-02]
files_modified:
  - test/scoria/adoption_surface_test.exs
  - test/support/scoria/adoption_example.ex
  - test/scoria/phoenix_example_source_test.exs
  - test/scoria/handoff_example_source_test.exs
autonomous: true
requirements: [EVID-01, DOCS-01]
must_haves:
  truths:
    - "Public docs drift tests fail if bounded handoff becomes framed as first-adoption work."
    - "Source-fragment tests fail if the example drops Scoria.identity/1, Scoria.start_run/2, Scoria.start_handoff_run/3, Scoria.get_run_detail/1, delegated_handoffs, or run_id/session_id wording."
    - "Drift checks stay at the public-contract layer and do not add snapshot or rendered-doc tooling."
  artifacts:
    - path: "test/scoria/adoption_surface_test.exs"
      provides: "Docs invariant checks for lane wording and forbidden internals"
      contains: "mix test.adoption"
    - path: "test/support/scoria/adoption_example.ex"
      provides: "Shared public API fragments for docs/source tests"
      contains: "Scoria.start_handoff_run/3"
    - path: "test/scoria/phoenix_example_source_test.exs"
      provides: "Phoenix runtime example source-fragment check"
      contains: "AdoptionExample"
    - path: "test/scoria/handoff_example_source_test.exs"
      provides: "Bounded handoff guide source-fragment check"
      contains: "AdoptionExample"
  key_links:
    - from: "test/support/scoria/adoption_example.ex"
      to: "docs/phoenix_runtime_example.md"
      via: "shared doc fragments"
      pattern: "Scoria\\.TestSupport\\.AdoptionExample"
    - from: "test/scoria/adoption_surface_test.exs"
      to: "README.md docs/adoption_lanes.md docs/operator_verification.md"
      via: "File.read! docs invariant assertions"
      pattern: "File\\.read!"
---

<objective>
Add source or docs drift checks that keep the example lane wording consistent with runtime truth.

Purpose: Lock EVID-01 and DOCS-01 at the public-contract layer per D-09 through D-12, while preserving the Phase 54 boundary in D-13 through D-15.
Output: ExUnit drift checks that pin default-lane-first wording, curated delegated readback, public facade API fragments, and run/session ID distinctions.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-operator-evidence-and-lane-guidance/53-CONTEXT.md
@.planning/phases/53-operator-evidence-and-lane-guidance/53-RESEARCH.md
@.planning/phases/53-operator-evidence-and-lane-guidance/53-PATTERNS.md
@.planning/phases/53-operator-evidence-and-lane-guidance/53-VALIDATION.md
@README.md
@docs/adoption_lanes.md
@docs/operator_verification.md
@docs/phoenix_runtime_example.md
@docs/bounded_handoffs.md
@test/scoria/adoption_surface_test.exs
@test/support/scoria/adoption_example.ex
@test/scoria/phoenix_example_source_test.exs
@test/scoria/handoff_example_source_test.exs

<interfaces>
Existing source-fragment pattern:
```elixir
defmodule Scoria.TestSupport.AdoptionExample do
  def doc_fragments do
    [
      "Scoria.identity",
      "Scoria.start_run",
      "Scoria.start_handoff_run",
      "Scoria.get_run_detail",
      "delegated = detail.delegated_handoffs"
    ]
  end
end
```

Existing docs invariant pattern:
```elixir
content = File.read!("docs/adoption_lanes.md")
assert content =~ "Default runtime lane"
refute content =~ "workflow_handoffs"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 53-03-01: Pin broad docs invariants for Phase 53 lane wording</name>
  <files>test/scoria/adoption_surface_test.exs</files>
  <read_first>
    .planning/phases/53-operator-evidence-and-lane-guidance/53-CONTEXT.md
    .planning/phases/53-operator-evidence-and-lane-guidance/53-VALIDATION.md
    test/scoria/adoption_surface_test.exs
    README.md
    docs/adoption_lanes.md
    docs/operator_verification.md
    docs/phoenix_runtime_example.md
    docs/bounded_handoffs.md
  </read_first>
  <behavior>
    - Broad docs tests assert default runtime lane appears before optional lanes.
    - Broad docs tests assert bounded handoff is an escalation, not a first-adoption prerequisite.
    - Broad docs tests assert `mix test.adoption` proves the default runtime lane.
    - Broad docs tests refute placeholder Phase 54 proof commands and raw workflow internals.
  </behavior>
  <action>Add or tighten assertions in `test/scoria/adoption_surface_test.exs` using existing `File.read!` patterns only. Assert these exact strings where they appear after Plan 53-02: `Start with the default runtime lane`, `Add bounded handoff only when`, `mix test.adoption`, `Scoria.get_run_detail/1`, and `delegated_handoffs`. Add refutations for `runtime-to-handoff proof`, `mix test.runtime_to_handoff`, `mix test.handoff`, `workflow_steps`, `workflow_handoffs`, `Repo.all`, and `Scoria.Workflows.create_run` in public adopter docs. Do not introduce snapshots, browser rendering, Markdown parsers, or new test dependencies.</action>
  <acceptance_criteria>
    - `grep -n "Start with the default runtime lane\\|Add bounded handoff only when" test/scoria/adoption_surface_test.exs` returns matches.
    - `grep -n "mix test.adoption" test/scoria/adoption_surface_test.exs` returns a match.
    - `grep -n "runtime-to-handoff proof\\|mix test.runtime_to_handoff\\|mix test.handoff" test/scoria/adoption_surface_test.exs` returns refutation lines.
    - `grep -n "workflow_steps\\|workflow_handoffs\\|Repo.all\\|Scoria.Workflows.create_run" test/scoria/adoption_surface_test.exs` returns refutation lines.
    - `grep -n "Floki\\|snapshot\\|Markdown" test/scoria/adoption_surface_test.exs` returns no new rendered-doc tooling for this task.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/adoption_surface_test.exs</automated>
  </verify>
  <done>Docs invariant tests pin Phase 53 public lane wording and fail on unsupported proof commands or raw internal support paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 53-03-02: Pin source fragments for public facade and ID semantics</name>
  <files>test/support/scoria/adoption_example.ex, test/scoria/phoenix_example_source_test.exs, test/scoria/handoff_example_source_test.exs</files>
  <read_first>
    .planning/phases/53-operator-evidence-and-lane-guidance/53-CONTEXT.md
    .planning/phases/53-operator-evidence-and-lane-guidance/53-PATTERNS.md
    test/support/scoria/adoption_example.ex
    test/scoria/phoenix_example_source_test.exs
    test/scoria/handoff_example_source_test.exs
    docs/phoenix_runtime_example.md
    docs/bounded_handoffs.md
  </read_first>
  <behavior>
    - Source-fragment tests pin `Scoria.identity/1`, `Scoria.start_run/2`, `Scoria.start_handoff_run/3`, `Scoria.get_run_detail/1`, and `delegated_handoffs`.
    - Source-fragment tests pin that `session_id` is continuity context and `run_id` is the durable inspection/readback handle.
    - Source-fragment tests pin `handoff_run.run_id` readback for the handoff example.
  </behavior>
  <action>Extend `Scoria.TestSupport.AdoptionExample` fragment lists using the existing helper shape. Include exact fragments for `Scoria.identity/1`, `Scoria.start_run/2`, `Scoria.start_handoff_run/3`, `Scoria.get_run_detail/1`, `delegated = detail.delegated_handoffs`, `session_id`, `run_id`, and `handoff_run.run_id`. If the existing tests already iterate every fragment, add fragments only; if they do not, update `test/scoria/phoenix_example_source_test.exs` and `test/scoria/handoff_example_source_test.exs` to assert all shared fragments against the matching docs. Keep fragment tests as file-content assertions and do not add Phase 54 proof-command fragments.</action>
  <acceptance_criteria>
    - `grep -n "Scoria.identity/1\\|Scoria.start_run/2\\|Scoria.start_handoff_run/3\\|Scoria.get_run_detail/1" test/support/scoria/adoption_example.ex` returns matches for all four public API fragments.
    - `grep -n "delegated = detail.delegated_handoffs\\|handoff_run.run_id\\|session_id\\|run_id" test/support/scoria/adoption_example.ex` returns matches.
    - `grep -n "AdoptionExample" test/scoria/phoenix_example_source_test.exs test/scoria/handoff_example_source_test.exs` returns matches in both test files.
    - `grep -n "runtime-to-handoff proof\\|mix test.runtime_to_handoff\\|mix test.handoff" test/support/scoria/adoption_example.ex test/scoria/phoenix_example_source_test.exs test/scoria/handoff_example_source_test.exs` returns no placeholder Phase 54 proof fragments.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/phoenix_example_source_test.exs test/scoria/handoff_example_source_test.exs</automated>
  </verify>
  <done>Shared source-fragment tests fail if the adopter examples drift away from the public facade APIs, curated delegated_handoffs readback, or run/session ID boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs source -> executable tests | Static docs become support truth through ExUnit assertions. |
| shared fragments -> public API contract | Source-fragment helpers define which public API strings must remain in guides. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-53-07 | Repudiation | medium | `test/scoria/adoption_surface_test.exs` | mitigate | Pin default-lane verifier and refute unsupported Phase 54 proof command names so docs cannot claim unshipped proof. | `MIX_ENV=test mix test test/scoria/adoption_surface_test.exs`. |
| T-53-08 | Information Disclosure | medium | docs/source drift checks | mitigate | Refute raw workflow internals and pin curated `Scoria.get_run_detail/1` / `delegated_handoffs` readback. | Grep acceptance criteria and source-fragment tests. |
| T-53-09 | Tampering | low | test-support fragment list | accept | Test-support fragments do not execute production behavior or mutate runtime state; risk is bounded to false-positive/false-negative test coverage. | `MIX_ENV=test mix test test/scoria/phoenix_example_source_test.exs test/scoria/handoff_example_source_test.exs`. |
</threat_model>

<verification>
Run the plan-local commands after each task, then run the full Phase 53 command from `53-VALIDATION.md`:

```bash
MIX_ENV=test mix test test/scoria/runtime_test.exs test/scoria/adoption_surface_test.exs test/scoria/phoenix_example_source_test.exs test/scoria/handoff_example_source_test.exs test/scoria_web/live/workflow_live_test.exs
```
</verification>

<success_criteria>
- EVID-01 and DOCS-01 drift checks are pinned at the public-contract layer.
- Tests use ExUnit file assertions and existing source-fragment helpers only.
- Tests refute raw workflow internals and placeholder Phase 54 proof commands.
- Tests pin public facade API fragments and `session_id` versus `run_id` semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/53-operator-evidence-and-lane-guidance/53-03-SUMMARY.md`.
</output>
