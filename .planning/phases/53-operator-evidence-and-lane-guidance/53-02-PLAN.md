---
phase: 53-operator-evidence-and-lane-guidance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/adoption_lanes.md
  - docs/operator_verification.md
  - docs/phoenix_runtime_example.md
  - docs/bounded_handoffs.md
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - "Phoenix adopter sees the default runtime lane before bounded handoff guidance."
    - "Docs explain bounded handoff as escalation only for same-run delegation with narrow projected context and operator-visible delegated lineage."
    - "Docs state `mix test.adoption` proves the default runtime lane and do not imply bounded handoff is required for first adoption."
  artifacts:
    - path: "README.md"
      provides: "Top-level lane hierarchy and verification wording"
      contains: "Default runtime lane"
    - path: "docs/adoption_lanes.md"
      provides: "Adopter lane selection guidance"
      contains: "Add this only"
    - path: "docs/operator_verification.md"
      provides: "Default-lane operator verification boundary"
      contains: "mix test.adoption"
    - path: "docs/phoenix_runtime_example.md"
      provides: "Runtime-to-handoff adopter example wording"
      contains: "Scoria.start_handoff_run/3"
    - path: "docs/bounded_handoffs.md"
      provides: "Bounded handoff ownership and projected-context contract"
      contains: "Scoria.get_run_detail/1"
  key_links:
    - from: "docs/phoenix_runtime_example.md"
      to: "docs/bounded_handoffs.md"
      via: "same default-first bounded handoff decision wording"
      pattern: "default runtime lane|normal runtime lane"
    - from: "README.md"
      to: "docs/operator_verification.md"
      via: "`mix test.adoption` as default-lane verifier"
      pattern: "mix test\\.adoption"
---

<objective>
Align README, operator verification, and adopter-guide wording around the default-to-handoff decision point.

Purpose: Satisfy DOCS-01 by making the support truth explicit: start with the default runtime lane; add bounded handoff only for a real same-run delegation with narrow projected context and operator-visible delegated lineage, per D-05 through D-08 and D-13 through D-15.
Output: Public docs use the same lane boundary language without naming a Phase 54 placeholder proof command.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-operator-evidence-and-lane-guidance/53-CONTEXT.md
@.planning/phases/53-operator-evidence-and-lane-guidance/53-RESEARCH.md
@.planning/phases/53-operator-evidence-and-lane-guidance/53-PATTERNS.md
@.planning/phases/53-operator-evidence-and-lane-guidance/53-VALIDATION.md
@.planning/phases/53-operator-evidence-and-lane-guidance/53-UI-SPEC.md
@README.md
@docs/adoption_lanes.md
@docs/operator_verification.md
@docs/phoenix_runtime_example.md
@docs/bounded_handoffs.md
</context>

<tasks>

<task type="auto">
  <name>Task 53-02-01: Align public lane-decision wording</name>
  <files>README.md, docs/adoption_lanes.md, docs/operator_verification.md</files>
  <read_first>
    .planning/phases/53-operator-evidence-and-lane-guidance/53-CONTEXT.md
    .planning/phases/53-operator-evidence-and-lane-guidance/53-RESEARCH.md
    .planning/phases/53-operator-evidence-and-lane-guidance/53-PATTERNS.md
    README.md
    docs/adoption_lanes.md
    docs/operator_verification.md
  </read_first>
  <action>Update the top-level support surfaces to use this concrete lane wording: `Start with the default runtime lane. It proves identity-aware durable runs, approvals, and operator evidence with mix test.adoption. Add bounded handoff only when the same durable run needs a narrow same-run delegation, host-controlled projected context, and operator-visible delegated lineage.` Preserve the four-lane hierarchy in this order: default runtime lane, bounded handoff lane, semantic fast-path lane, optional knowledge lane. In `docs/operator_verification.md`, keep `mix test.adoption` as the canonical default-lane verifier and do not add a runtime-to-handoff proof command. Use direct modal phrases from D-08: `start here`, `add this only when`, and `you do not need`.</action>
  <acceptance_criteria>
    - `grep -n "Start with the default runtime lane" README.md docs/adoption_lanes.md docs/operator_verification.md` returns at least one match.
    - `grep -n "Add bounded handoff only when" README.md docs/adoption_lanes.md docs/operator_verification.md` returns at least one match.
    - `grep -n "mix test.adoption" README.md docs/adoption_lanes.md docs/operator_verification.md` returns matches in all three files.
    - `grep -n "semantic fast-path lane" README.md docs/adoption_lanes.md docs/operator_verification.md` and `grep -n "optional knowledge lane" README.md docs/adoption_lanes.md docs/operator_verification.md` return matches preserving optional-lane wording.
    - `grep -n "runtime-to-handoff proof\\|mix test.runtime\\|mix test.handoff\\|mix test.runtime_to_handoff" README.md docs/adoption_lanes.md docs/operator_verification.md` returns no placeholder Phase 54 proof command.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/adoption_surface_test.exs</automated>
  </verify>
  <done>README, adoption lane guide, and operator verification docs present default runtime as the first adoption lane and bounded handoff as an optional same-run escalation.</done>
</task>

<task type="auto">
  <name>Task 53-02-02: Align example and handoff guide wording</name>
  <files>docs/phoenix_runtime_example.md, docs/bounded_handoffs.md</files>
  <read_first>
    .planning/phases/53-operator-evidence-and-lane-guidance/53-CONTEXT.md
    .planning/phases/53-operator-evidence-and-lane-guidance/53-RESEARCH.md
    .planning/phases/53-operator-evidence-and-lane-guidance/53-PATTERNS.md
    docs/phoenix_runtime_example.md
    docs/bounded_handoffs.md
    test/support/scoria/adoption_example.ex
  </read_first>
  <action>Update the example docs to preserve the Phase 52 ownership split with concrete wording: `The host app owns identity, escalation policy, prompt or draft selection, and projected-context selection. Scoria owns durable run creation, projected-context validation, queued delegated child creation, and curated readback through Scoria.get_run_detail/1.` In `docs/phoenix_runtime_example.md`, keep the example order as `Scoria.identity/1`, `Scoria.start_run/2`, conditional `Scoria.start_handoff_run/3`, then `Scoria.get_run_detail(handoff_run.run_id)` and `delegated = detail.delegated_handoffs`. In `docs/bounded_handoffs.md`, state that bounded handoffs are added only after `mix test.adoption` proves the normal runtime lane. Preserve `{:error, :unsafe_projected_context}` and the unsafe projected-context key list.</action>
  <acceptance_criteria>
    - `grep -n "The host app owns identity, escalation policy, prompt or draft selection, and projected-context selection" docs/phoenix_runtime_example.md docs/bounded_handoffs.md` returns at least one match.
    - `grep -n "Scoria owns durable run creation, projected-context validation, queued delegated child creation, and curated readback through Scoria.get_run_detail/1" docs/phoenix_runtime_example.md docs/bounded_handoffs.md` returns at least one match.
    - `grep -n "Scoria.start_run/2\\|Scoria.start_handoff_run/3\\|Scoria.get_run_detail" docs/phoenix_runtime_example.md` returns matches for all three public facade calls.
    - `grep -n "handoff_run.run_id\\|delegated = detail.delegated_handoffs" docs/phoenix_runtime_example.md docs/bounded_handoffs.md` returns matches for curated handoff readback.
    - `grep -n "{:error, :unsafe_projected_context}" docs/bounded_handoffs.md` returns a match.
    - `grep -n "workflow_handoffs\\|workflow_steps\\|Repo.all\\|Scoria.Workflows.create_run" docs/phoenix_runtime_example.md docs/bounded_handoffs.md` returns no public raw-internal guidance.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/adoption_surface_test.exs test/scoria/phoenix_example_source_test.exs test/scoria/handoff_example_source_test.exs</automated>
  </verify>
  <done>Runtime example and bounded handoff guide use public facade APIs, preserve host/Scoria ownership boundaries, and keep bounded handoff framed as optional escalation from the default runtime lane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> adopter implementation | Public docs influence what host apps call and what data they pass into Scoria. |
| support wording -> proof expectation | Public verification wording defines which commands adopters and maintainers trust. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-53-04 | Information Disclosure | medium | `docs/phoenix_runtime_example.md`, `docs/bounded_handoffs.md` | mitigate | Keep projected context described as narrow and host-controlled; preserve unsafe projected-context rejection and avoid raw workflow table guidance. | Grep acceptance criteria and `MIX_ENV=test mix test test/scoria/adoption_surface_test.exs test/scoria/handoff_example_source_test.exs`. |
| T-53-05 | Repudiation | medium | README and operator verification docs | mitigate | Do not publish placeholder Phase 54 runtime-to-handoff proof commands; keep `mix test.adoption` as the default-lane verifier only. | Grep refuting placeholder command names and `MIX_ENV=test mix test test/scoria/adoption_surface_test.exs`. |
| T-53-06 | Spoofing | low | docs identity guidance | mitigate | Preserve host ownership of identity and keep examples on `Scoria.identity/1` rather than implying Scoria owns application authentication. | Source-fragment grep for `Scoria.identity/1` and ownership-boundary copy. |
</threat_model>

<verification>
Run the plan-local commands after each task, then run:

```bash
MIX_ENV=test mix test test/scoria/adoption_surface_test.exs test/scoria/phoenix_example_source_test.exs test/scoria/handoff_example_source_test.exs
```
</verification>

<success_criteria>
- DOCS-01 is satisfied across README, operator verification, and adopter guides.
- Docs preserve the v2.2 lane hierarchy and default runtime lane first.
- Docs say `mix test.adoption` proves the default runtime lane.
- Docs do not name a placeholder Phase 54 runtime-to-handoff proof command.
- Docs keep raw workflow internals out of public adopter guidance.
</success_criteria>

<output>
After completion, create `.planning/phases/53-operator-evidence-and-lane-guidance/53-02-SUMMARY.md`.
</output>
