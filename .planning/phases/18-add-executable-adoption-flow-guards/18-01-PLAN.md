---
phase: 18-add-executable-adoption-flow-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - lib/scoria.ex
  - lib/scoria/identity.ex
  - test/scoria_test.exs
  - test/scoria/identity_doctest_test.exs
  - test/scoria/adoption_surface_test.exs
autonomous: true
nyquist_compliant: true
requirements:
  - ADOP-01
must_haves:
  truths:
    - "The README keeps a curated runtime-first adoption narrative while executable guards only lock stable public semantics."
    - "Pure `Scoria` and `Scoria.Identity` examples compile and run as doctests so facade argument shapes and identity normalization cannot silently drift."
    - "README/public-surface guards prove durable public nouns like `session_id`, `run_id`, `Scoria.start_run/2`, and `/scoria/workflows/:run_id` without snapshotting prose."
  artifacts:
    - path: "README.md"
      provides: "Curated public adoption story with stable semantic anchors"
    - path: "lib/scoria.ex"
      provides: "Doctested happy-path public facade examples"
    - path: "lib/scoria/identity.ex"
      provides: "Doctested pure identity normalization examples"
    - path: "test/scoria_test.exs"
      provides: "Facade doctest execution lane"
    - path: "test/scoria/identity_doctest_test.exs"
      provides: "Dedicated identity doctest execution lane"
    - path: "test/scoria/adoption_surface_test.exs"
      provides: "Semantic README/public-surface guardrails"
  key_links:
    - from: "README.md"
      to: "test/scoria/adoption_surface_test.exs"
      via: "semantic assertions lock the public nouns without freezing prose layout"
      pattern: "Scoria\\.start_run|Scoria\\.resume_run|session_id|run_id|/scoria/workflows/:run_id"
    - from: "lib/scoria.ex"
      to: "test/scoria_test.exs"
      via: "doctest Scoria executes pure facade examples under ExUnit"
      pattern: "doctest Scoria"
    - from: "lib/scoria/identity.ex"
      to: "test/scoria/identity_doctest_test.exs"
      via: "identity normalization examples stay executable but side-effect free"
      pattern: "doctest Scoria\\.Identity"
---

<objective>
Add executable guardrails for README-level public surface truth without turning the full README into the executable spec.

Purpose: harden `ADOP-01` against drift while preserving Scoria’s curated docs voice and keeping stateful adoption proof in integration tests.
Output: doctested pure facade/identity examples plus tighter semantic README/public-surface assertions.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/MILESTONE-ARC.md
@.planning/phases/18-add-executable-adoption-flow-guards/18-CONTEXT.md
@.planning/phases/18-add-executable-adoption-flow-guards/18-RESEARCH.md
@.planning/phases/18-add-executable-adoption-flow-guards/18-PATTERNS.md
@.planning/phases/15-adoption-surface-docs-and-example-flow/15-01-PLAN.md
@README.md
@lib/scoria.ex
@lib/scoria/identity.ex
@test/scoria_test.exs
@test/scoria/adoption_surface_test.exs

<interfaces>
From lib/scoria.ex:
```elixir
def identity(attrs \\ %{})
def start_run(identity, opts \\ [])
def resume_run(run_id, opts \\ [])
def get_run(run_id)
def get_run_detail(run_id)
def list_runs_for_session(session_id)
```

From lib/scoria/identity.ex:
```elixir
def new(attrs \\ %{})
def from_conn_assigns(assigns)
def from_session(session)
def from_mount(attrs)
def normalize(attrs)
def to_map(%Scoria.Identity{})
```

From test/scoria/adoption_surface_test.exs:
```elixir
assert content =~ "Scoria.start_run"
assert content =~ "Scoria.resume_run"
assert content =~ "session_id"
assert content =~ "run_id"
assert content =~ "/scoria/workflows/:run_id"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add doctested pure public examples for the facade and identity boundary</name>
  <files>lib/scoria.ex, lib/scoria/identity.ex, test/scoria_test.exs, test/scoria/identity_doctest_test.exs</files>
  <action>Per D-01, D-03, D-04, D-07, D-17, D-18, and D-20, add only narrow pure examples to `Scoria` and `Scoria.Identity` moduledocs. Keep them side-effect free: identity normalization, stable return-shape expectations, and public noun usage are in scope; controller/session/router/operator flows are not. Reuse `test/scoria_test.exs` for `doctest Scoria`, add a focused `test/scoria/identity_doctest_test.exs` for `doctest Scoria.Identity`, and do not introduce `doctest_file` over the whole README or any stateful guide.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria_test.exs test/scoria/identity_doctest_test.exs</automated>
  </verify>
  <done>`Scoria` and `Scoria.Identity` each expose executable pure examples, and no stateful Phoenix adoption step has been pushed into doctests.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten README/public-surface guards around semantic contract anchors</name>
  <files>README.md, test/scoria/adoption_surface_test.exs</files>
  <action>Per D-01, D-02, D-03, D-04, D-13, D-17, D-19, and D-20, keep `README.md` as curated narrative text while strengthening `test/scoria/adoption_surface_test.exs` to assert semantic anchors instead of prose snapshots. The assertions must cover the shipped public facade, the `session_id` versus `run_id` rule, `/scoria/workflows/:run_id`, the default-lane versus optional-knowledge-lane split, and the presence of executable pure snippet sources where practical. Do not assert whole headings or frozen paragraph text, and do not move controller/operator walkthroughs out of their dedicated integration seams.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/adoption_surface_test.exs test/scoria_test.exs test/scoria/identity_doctest_test.exs && rg -n "Scoria.start_run|Scoria.resume_run|session_id|run_id|/scoria/workflows/:run_id|Optional knowledge lane" README.md</automated>
  </verify>
  <done>The README remains readable and curated, while the public contract semantics are now executable and drift-resistant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public docs → host app integrator | README and moduledoc examples shape how adopters call the public facade |
| pure examples → stateful runtime proof | Doctests must stop at pure contracts so they do not fake end-to-end adoption coverage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | S | `README.md` | mitigate | Keep `run_id` and `session_id` assertions explicit so continuity ids cannot masquerade as resume authority. |
| T-18-01-02 | T | `test/scoria/adoption_surface_test.exs` | mitigate | Assert stable public nouns and snippet presence instead of brittle prose snapshots per D-01 and D-02. |
| T-18-01-03 | R | `lib/scoria.ex` | mitigate | Execute public facade examples with doctests so documented argument shapes stay attributable to compiled code. |
| T-18-01-04 | I | `lib/scoria/identity.ex` | mitigate | Keep examples at the identity boundary only and avoid leaking internal workflow setup into public docs. |
| T-18-01-05 | D | `README.md` | mitigate | Preserve the default lane versus optional knowledge lane split so core adoption is not blocked by heavier prerequisites. |
| T-18-01-06 | E | doctest coverage | mitigate | Refuse to treat doctests as proof of stateful controller or operator flows; those stay in integration tests per D-04. |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria_test.exs test/scoria/identity_doctest_test.exs test/scoria/adoption_surface_test.exs`
- `rg -n "Scoria.start_run|Scoria.resume_run|session_id|run_id|/scoria/workflows/:run_id|Optional knowledge lane" README.md`
</verification>

<success_criteria>
- Pure public examples for `Scoria` and `Scoria.Identity` execute under ExUnit.
- README/public-surface guards enforce semantics, not snapshots.
- Stateful Phoenix adoption proof still lives outside README-wide doctests.
- `ADOP-01` gains executable drift resistance without changing the product-teaching posture.
</success_criteria>

<output>
After completion, create `.planning/phases/18-add-executable-adoption-flow-guards/18-01-SUMMARY.md`
</output>
