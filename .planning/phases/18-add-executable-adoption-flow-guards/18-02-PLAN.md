---
phase: 18-add-executable-adoption-flow-guards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/phoenix_runtime_example.md
  - test/support/scoria/adoption_example.ex
  - test/scoria/runtime_integration_test.exs
  - test/scoria/phoenix_example_source_test.exs
autonomous: true
nyquist_compliant: true
requirements:
  - ADOP-02
must_haves:
  truths:
    - "The canonical Phoenix guide remains docs-first, but its code shapes are derived from checked helper truth rather than disconnected prose."
    - "The controller-triggered flow still teaches `Scoria.identity/1`, `Scoria.start_run/2`, exact `run_id` resume, same-session new runs, and operator evidence at `/scoria/workflows/:run_id`."
    - "Runtime integration remains the behavioral source of truth instead of a fixture Phoenix app or whole-guide execution lane."
  artifacts:
    - path: "docs/phoenix_runtime_example.md"
      provides: "Canonical controller-triggered Phoenix adoption guide"
    - path: "test/support/scoria/adoption_example.ex"
      provides: "Shared checked fragments or helper truth for the guide"
    - path: "test/scoria/runtime_integration_test.exs"
      provides: "Runtime seam executing the same behavioral contracts taught by the guide"
    - path: "test/scoria/phoenix_example_source_test.exs"
      provides: "Docs-to-helper drift guard for the canonical example"
  key_links:
    - from: "docs/phoenix_runtime_example.md"
      to: "test/support/scoria/adoption_example.ex"
      via: "guide fragments are sourced from checked helpers or literal shared truth"
      pattern: "start_run|resume_run|get_run|list_runs_for_session|/scoria/workflows/:run_id"
    - from: "test/scoria/runtime_integration_test.exs"
      to: "test/support/scoria/adoption_example.ex"
      via: "runtime proof executes the same identity and run semantics encoded in the guide source"
      pattern: "shared-session|waiting_for_approval|completed"
    - from: "test/scoria/phoenix_example_source_test.exs"
      to: "docs/phoenix_runtime_example.md"
      via: "semantic fragment assertions keep the docs aligned without whole-guide execution"
      pattern: "session_id|run_id|Scoria\\.start_run|Scoria\\.resume_run"
---

<objective>
Derive the canonical Phoenix example from checked runtime-example truth and keep it executable at the right seam.

Purpose: harden `ADOP-02` without introducing a fixture Phoenix host app or overloading the docs with browser-style verification.
Output: shared checked example source, runtime integration coverage wired to it, and a docs drift guard for `docs/phoenix_runtime_example.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-add-executable-adoption-flow-guards/18-CONTEXT.md
@.planning/phases/18-add-executable-adoption-flow-guards/18-RESEARCH.md
@.planning/phases/18-add-executable-adoption-flow-guards/18-PATTERNS.md
@.planning/phases/15-adoption-surface-docs-and-example-flow/15-01-PLAN.md
@docs/phoenix_runtime_example.md
@lib/scoria.ex
@lib/scoria/identity.ex
@lib/scoria/runtime.ex
@test/scoria/runtime_integration_test.exs

<interfaces>
From lib/scoria.ex:
```elixir
def identity(attrs \\ %{})
def start_run(identity, opts \\ [])
def resume_run(run_id, opts \\ [])
def get_run(run_id)
def list_runs_for_session(session_id)
```

From test/scoria/runtime_integration_test.exs:
```elixir
Scoria.start_run(identity, ...)
Runtime.get_run(started.run_id)
Scoria.resume_run(started.run_id, ...)
Scoria.list_runs_for_session("shared-session")
live(conn, "/scoria/workflows/#{started.run_id}")
```

From docs/phoenix_runtime_example.md:
```text
session_id is not run_id
controller-triggered start
read back the same run
resume after approval
same session, fresh run
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a checked Phoenix example source shared with the runtime seam</name>
  <files>test/support/scoria/adoption_example.ex, test/scoria/runtime_integration_test.exs</files>
  <action>Per D-05, D-06, D-07, D-08, D-17, D-18, and D-19, create one shared truth source for the canonical Phoenix example. Use a support module or equivalent checked fragment store under `test/support` that exposes the exact identity map, `start_run`, `get_run`, `resume_run`, same-session new-run semantics, and `/scoria/workflows/:run_id` linkage taught by the guide. Update `test/scoria/runtime_integration_test.exs` to exercise or reference that shared truth while keeping the current public `Scoria` facade as the driver. Do not create a fixture Phoenix app, and do not move this flow into doctests.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/runtime_integration_test.exs</automated>
  </verify>
  <done>The runtime integration seam and the shared example source encode the same controller-triggered adoption contract.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire the Phoenix guide to the checked source and add a docs drift guard</name>
  <files>docs/phoenix_runtime_example.md, test/scoria/phoenix_example_source_test.exs</files>
  <action>Per D-05, D-06, D-08, D-13, D-17, D-19, and D-20, update `docs/phoenix_runtime_example.md` so its code examples are traceable to the checked source from Task 1 while the surrounding narrative stays human-readable. Add `test/scoria/phoenix_example_source_test.exs` to assert the guide still carries the canonical identity, exact-`run_id` resume, same-session fresh-run, and operator-evidence semantics without whole-guide execution or prose snapshots. Preserve the controller-first adoption story and avoid any fixture-app or browser-E2E shape.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/runtime_integration_test.exs test/scoria/phoenix_example_source_test.exs && rg -n "Scoria.identity|Scoria.start_run|Scoria.get_run|Scoria.resume_run|list_runs_for_session|session_id|run_id|/scoria/workflows/:run_id" docs/phoenix_runtime_example.md</automated>
  </verify>
  <done>The canonical Phoenix guide is now sourced from checked truth and protected by an executable docs guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime integration seam → docs example | The public guide must mirror real behavior instead of inventing controller/runtime semantics |
| checked example source → guide prose | Shared fragments should constrain code shapes without forcing whole-guide execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | S | `docs/phoenix_runtime_example.md` | mitigate | Keep the exact `run_id` versus `session_id` contract executable via shared-source and runtime tests. |
| T-18-02-02 | T | guide code fragments | mitigate | Derive the canonical fragments from checked helper truth per D-05 and D-06 instead of maintaining disconnected prose. |
| T-18-02-03 | R | `test/support/scoria/adoption_example.ex` | mitigate | Centralize the guide contract in one checked seam so docs drift has a single executable owner. |
| T-18-02-04 | I | docs example | mitigate | Keep the guide on public `Scoria` and Phoenix edge normalization, not internal workflow-only APIs. |
| T-18-02-05 | D | fixture-app pressure | mitigate | Explicitly reject a sample Phoenix app and whole-guide execution so the phase stays lightweight and maintainable. |
| T-18-02-06 | E | operator guide boundary | mitigate | Limit this plan to the controller-triggered runtime path and leave operator acceptance to the dedicated Phase 18-03 harness. |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/runtime_integration_test.exs test/scoria/phoenix_example_source_test.exs`
- `rg -n "Scoria.identity|Scoria.start_run|Scoria.get_run|Scoria.resume_run|list_runs_for_session|session_id|run_id|/scoria/workflows/:run_id" docs/phoenix_runtime_example.md`
</verification>

<success_criteria>
- The Phoenix example is derived from checked runtime-example truth.
- Runtime integration continues to be the canonical behavioral source of truth.
- No fixture Phoenix app, browser E2E, or whole-guide execution is introduced.
- `ADOP-02` is hardened with docs-to-runtime traceability rather than disconnected prose.
</success_criteria>

<output>
After completion, create `.planning/phases/18-add-executable-adoption-flow-guards/18-02-SUMMARY.md`
</output>
