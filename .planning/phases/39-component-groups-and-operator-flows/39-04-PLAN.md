---
phase: 39-component-groups-and-operator-flows
plan: 04
type: execute
wave: 2
depends_on: [39-01, 39-02]
files_modified:
  - lib/scoria_web/live/orchestrator_live.ex
  - lib/scoria_web/live/workflow_live/index.ex
  - lib/scoria_web/live/eval_spec_live/index.ex
  - lib/scoria_web/live/prompt_live/index.ex
  - lib/scoria_web/live/coming_soon_live.ex
autonomous: true
requirements: [FLOW-01, FLOW-02, COPY-01]
must_haves:
  truths:
    - "orchestrator_live Home, workflow_live index, eval_spec_live index, and prompt_live index each render their single page-outline <h1> through page_header/1 instead of hand-rolled .scoria-pagehead markup (D-01/D-03)."
    - "coming_soon_live reconciles its TWO hand-rolled header shapes: the .scoria-pagehead branch routes through page_header/1, and the .scoria-stub not-found branch (\"Capability not found\" <h1>) routes through a sanctioned page-level not-found header (stub_page/1 or an equivalent sanctioned header) — no bare page-body <h1> remains (D-02/D-03)."
    - "The eval rubrics <h1> drops its parenthetical schema-module-name suffix; the title is operator language only (D-23)."
    - "prompt_live leads with a human name in its edit title and \"Prompt\" column; the opaque entity_id is demoted to labelled ID evidence via <.id> (D-23)."
  artifacts:
    - lib/scoria_web/live/coming_soon_live.ex
  key_links:
    - "Each migrated header consumes page_header/1 from Plan 01; no page adds new CSS (page_header reuses .scoria-pagehead*)."
    - "Dialog-scoped headings (drawer/modal/palette) are exempt from the single-header rule and are not touched here (D-03)."
---

<objective>
Adopt the page-orientation convention (Area 1, FLOW-01/D-01/D-03) and fix the microcopy offenders
(Area 5, COPY-01/D-23) on the first group of pages: orchestrator Home, workflow detail index,
eval-spec index, prompt index, and the coming-soon/not-found page. Each page ends with exactly one
sanctioned page-outline header and operator-first titles/columns.

Purpose: This is one of two parallel Wave-2 page-migration plans (Plan 05 owns the other page group,
disjoint files). Migrating headers + fixing microcopy per page keeps each file under a single owner
and makes the D-05/D-26 guards (Plan 08) green on arrival.

Output: five migrated LiveViews, including the two-header reconciliation of coming_soon_live (D-03,
flagged in PATTERNS "Material drift").
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md
@.planning/phases/39-component-groups-and-operator-flows/39-PATTERNS.md
@lib/scoria_web/ui.ex
@lib/scoria_web/live/coming_soon_live.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate orchestrator Home + workflow index headers to page_header/1</name>
  <files>lib/scoria_web/live/orchestrator_live.ex, lib/scoria_web/live/workflow_live/index.ex</files>
  <read_first>
    - lib/scoria_web/ui.ex page_header/1 (added in Plan 01 — the target component and its attr :title /
      slot :summary / slot :actions contract).
    - lib/scoria_web/live/orchestrator_live.ex (~94-95 — Home hand-rolled .scoria-pagehead + <h1>).
    - lib/scoria_web/live/workflow_live/index.ex (~31 — pagehead header).
    - .planning/phases/39-component-groups-and-operator-flows/39-PATTERNS.md lines 90-94 (caller
      migration list) and 47-94 (page_header shape).
    - .planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md D-01, D-03, D-04.
  </read_first>
  <action>
    Replace the hand-rolled `.scoria-pagehead` markup + bare `<h1>` in orchestrator_live Home (~94) and
    workflow_live/index (~31) with `<.page_header title="...">` (title as a plain operator string),
    moving the existing description into `<:summary>` and any existing header action into `<:actions>`
    (0..1 only). Grep for the header markup by symbol (`scoria-pagehead` / `<h1>`) rather than trusting
    exact line numbers. Ensure exactly one page-outline `<h1>` per module and that no region
    `page_section`/`panel` `:title` restates the page title (D-04 — drop or render flush if it does).
    Do NOT touch dialog-scoped headings. Add no CSS.
  </action>
  <verify>
    <automated>mix test test/scoria_web/live/orchestrator_live_test.exs test/scoria_web/live/workflow_live --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Both modules render their page title via page_header/1; grep shows exactly one <h1> literal per
      module and no residual hand-rolled .scoria-pagehead__title block.
    - Existing orchestrator/workflow LiveView tests pass.
  </acceptance_criteria>
  <done>orchestrator Home and workflow index render their single header through page_header/1 with no residual hand-rolled pagehead markup.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate eval-spec + prompt headers and fix their microcopy offenders</name>
  <files>lib/scoria_web/live/eval_spec_live/index.ex, lib/scoria_web/live/prompt_live/index.ex</files>
  <read_first>
    - lib/scoria_web/live/eval_spec_live/index.ex (~70-71 — bare <h1> "Evaluation Rubrics" carrying a
      parenthetical schema-module-name suffix; no pagehead div).
    - lib/scoria_web/live/prompt_live/index.ex (~99 — edit title interpolating entity_id; ~140 — a
      column rendering entity_id as the primary "Prompt" cell).
    - lib/scoria_web/ui.ex id/1 (~276 — the <.id> copyable-evidence primitive) and page_header/1.
    - .planning/phases/39-component-groups-and-operator-flows/39-PATTERNS.md lines 319-333 (offender
      table) and 47-94 (page_header).
    - .planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md D-23.
  </read_first>
  <action>
    Route the eval-spec index `<h1>` through `<.page_header>` and drop the parenthetical
    schema-module-name suffix from its title so the heading is operator language only (D-23). In
    prompt_live: lead the edit `:title` and the "Prompt" column with a human-readable template name,
    and demote the opaque `entity_id` to labelled ID evidence via `<.id>` (the copyable log-grep
    affordance) rather than using it as the primary orientation/cell (D-23). Migrate prompt_live's page
    header to `page_header/1` if it renders one. Grep for offenders by symbol; keep exactly one page
    `<h1>` per module; add no CSS.
  </action>
  <verify>
    <automated>mix test test/scoria_web/live/eval_spec_live test/scoria_web/live/prompt_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - The eval rubrics <h1> no longer carries a parenthetical module-name suffix.
    - prompt_live leads with a human name; entity_id renders only as <.id> evidence, not as a title or
      primary cell.
    - Relevant LiveView tests pass (add/adjust assertions if the title/cell text changed).
  </acceptance_criteria>
  <done>eval-spec and prompt pages orient with operator language; opaque IDs are demoted to <.id> evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile coming_soon_live's two header shapes (D-03 not-found)</name>
  <files>lib/scoria_web/live/coming_soon_live.ex</files>
  <read_first>
    - lib/scoria_web/live/coming_soon_live.ex (~29 — a .scoria-pagehead branch; ~40 — a hand-inlined
      .scoria-stub not-found branch with a bare `<h1>Capability not found</h1>`). PATTERNS "Material
      drift" (lines 443-450) documents these are TWO distinct header shapes, not one pagehead-nested h1.
    - lib/scoria_web/ui.ex stub_page/1 (~489 — the sanctioned reserved/not-found header) and
      page_header/1.
    - .planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md D-02 (page-level
      not-found is a sanctioned page-level header + empty_state-style body, never a table :empty slot)
      and D-03 (sanctioned not-found header; no bare page-body <h1>).
  </read_first>
  <action>
    Reconcile BOTH header shapes: route the `.scoria-pagehead` render branch (~29) through
    `<.page_header>`, and route the `.scoria-stub` "Capability not found" not-found branch (~40) through
    a sanctioned page-level not-found header — either `stub_page/1` (ui.ex:489) or an equivalent
    sanctioned page-level header + `empty_state`-style body (D-02/D-03) — so no bare page-body `<h1>`
    remains in either branch. Confirm by grep that both `<h1>` emissions now sit inside sanctioned
    headers and there is no hand-inlined `.scoria-stub`/`.scoria-pagehead` duplicate left. Add no CSS.
  </action>
  <verify>
    <automated>mix test test/scoria_web/live/coming_soon_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Both the reserved-capability branch and the not-found branch render through sanctioned headers
      (page_header/1 and stub_page/1-or-equivalent).
    - No bare page-body <h1> and no hand-inlined .scoria-stub/.scoria-pagehead duplicate remain.
    - coming_soon_live_test.exs passes.
  </acceptance_criteria>
  <done>coming_soon_live's two hand-rolled headers are reconciled into sanctioned page-outline headers with no bare <h1>.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| record fields → page <h1>/columns | Opaque IDs / schema-module names can leak into page orientation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-04-I | Information Disclosure | page titles / columns | medium | mitigate | Opaque entity_id and schema-module names removed from orientation and demoted to <.id> evidence (D-23); page_header title is a :string attr so IDs cannot be interpolated into the <h1> |
| T-39-04-I2 | Information Disclosure | coming_soon not-found | low | mitigate | Not-found renders a sanctioned page-level header + empty_state body (D-02); no raw object identifiers surfaced as orientation |
| T-39-04-SC | Tampering | dependency installs | low | accept | No package installs this phase |
</threat_model>

<verification>
- `mix test test/scoria_web/live/orchestrator_live_test.exs test/scoria_web/live/workflow_live test/scoria_web/live/eval_spec_live test/scoria_web/live/prompt_live_test.exs test/scoria_web/live/coming_soon_live_test.exs --warnings-as-errors` passes.
- `mix compile --warnings-as-errors` succeeds.
- Grep confirms exactly one `<h1>` literal per migrated module and no residual hand-rolled `.scoria-pagehead__title`/`.scoria-stub` header blocks in these files.
</verification>

<success_criteria>
- All five pages render a single sanctioned page-outline header via the Phase-39 primitives.
- Microcopy offenders (eval-spec module-name, prompt entity_id) are fixed.
- coming_soon_live's two header shapes are reconciled with no bare <h1>.
</success_criteria>

<artifacts_this_phase_produces>
- Migrated headers in orchestrator_live, workflow_live/index, eval_spec_live/index, prompt_live/index, coming_soon_live (consumers of page_header/1 and stub_page/1)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/39-component-groups-and-operator-flows/39-04-SUMMARY.md` when done.
</output>
