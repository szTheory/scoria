---
phase: 39-component-groups-and-operator-flows
plan: 08
type: execute
wave: 4
depends_on: [39-04, 39-05, 39-06, 39-07]
files_modified:
  - test/scoria_web/single_header_guard_test.exs
  - test/scoria_web/scan_convention_guard_test.exs
  - test/scoria_web/copy_guard_test.exs
  - priv/dev/e2e/ia_orientation.spec.mjs
autonomous: true
requirements: [FLOW-01, FLOW-02, COPY-01]
must_haves:
  truths:
    - "The single-header guard (D-05) asserts exactly one <h1> literal per page LiveView, emitted inside a sanctioned header, scoped to page LiveViews only and EXCLUDING lib/scoria_web/ui.ex and dialog-scoped component files; the region-title≠page-<h1> check is warning-grade over static literals only. It is GREEN on arrival because Plans 04/05/06 migrated every page (depends_on enforces this ordering) (D-05/D-03)."
    - "The scan-convention guard (D-11) asserts filter/scope state is not held only in socket assigns (target already migrated: review_queue) and must NOT red-flag dataset_live; SORT is explicitly EXEMPT (D-09 option B was locked in Plan 05) (D-11)."
    - "The copy guard (D-26) asserts: no schema/module names in headings; no opaque IDs (entity_id/*_run_id/*_session_id) interpolated into <h1>/<h2>/:title/:eyebrow; status renders only via an allow-list of approved label fns (status_label, state_label, delegated_status_label, ApprovalCopy.*, *Copy.*) — never a raw atom. Green because Plans 04/05/06 fixed the offenders (D-26)."
    - "All three guards are WARNING-GRADE this phase (explicit-offender allow-list; Phase 41 hardens) and all pass on arrival; DS-06, token-contrast, and the D-20 write-invariant guard stay green (cross-cutting invariants)."
    - "An automated flow proof exercises page orientation and the approval decision + decision-history flows against the running dashboard, not a manual UAT checkpoint (automation preference)."
  artifacts:
    - test/scoria_web/single_header_guard_test.exs
    - test/scoria_web/scan_convention_guard_test.exs
    - test/scoria_web/copy_guard_test.exs
  key_links:
    - "All three guards mirror the Phase-38 source-scan idiom (ui_drift_guard_test.exs File.read! + Path.wildcard + Regex; ds06 @excluded / allow-list style)."
    - "This plan runs in Wave 4 (depends_on 04/05/06/07) so every guard's targets are already migrated/fixed — the CONTEXT D-03 'green on arrival' precondition is satisfied by wave ordering."
    - "The e2e flow proof extends the existing priv/dev/e2e specs and runs via mix scoria.ui.e2e against the dev harness."
---

<objective>
Add the four milestone's-worth of warning-grade source-scan guards this phase is responsible for —
single-header (D-05), scan-convention (D-11), and copy (D-26) — and prove the operator flows with an
automated e2e spec rather than a manual UAT checkpoint. Every guard is green on arrival because it runs
in Wave 4, after Plans 04/05/06/07 migrated the headers, moved review_queue's filter to the URL, and
fixed the microcopy offenders (depends_on enforces the ordering that the CONTEXT D-03 precondition
demands).

Purpose: Lock the Phase-39 conventions against regression at warning grade (Phase 41 hardens them into
DOM-level proof), and close the loop with an automated flow proof per the project's shift-left
preference. The D-20 write-invariant guard already shipped in Plan 03.

Output: three new guard test files + an extended e2e flow spec, all green, with the full suite passing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md
@.planning/phases/39-component-groups-and-operator-flows/39-PATTERNS.md
@test/scoria_web/ui_drift_guard_test.exs
@test/scoria_web/ds06_drift_guard_test.exs
@priv/dev/e2e/ia_orientation.spec.mjs
@lib/mix/tasks/scoria.ui.e2e.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Single-header (D-05) + scan-convention (D-11) guards</name>
  <files>test/scoria_web/single_header_guard_test.exs, test/scoria_web/scan_convention_guard_test.exs</files>
  <read_first>
    - test/scoria_web/ui_drift_guard_test.exs:19-40 (the minimal File.read! + Path.wildcard + Regex
      offender-collection skeleton to copy).
    - test/scoria_web/ds06_drift_guard_test.exs (the @excluded list ~34 and allow-list/descriptive-failure
      style to mirror for scope exclusion).
    - .planning/phases/39-component-groups-and-operator-flows/39-PATTERNS.md lines 335-359 (guard idioms;
      D-05 and D-11 specifics).
    - .planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md D-05, D-11, D-04 (semantic
      redundancy is human-reviewed, not string-equality); NOTE Plan 05 locked D-09 option B (sort in
      assigns, EXEMPT from this guard).
  </read_first>
  <action>
    Create the single-header guard (D-05): scan page LiveViews only (Path.wildcard over the live/ page
    modules), EXCLUDING lib/scoria_web/ui.ex and dialog-scoped component files (drawer/modal/palette/
    notebook) via an @excluded list mirroring ds06. Assert (a) exactly one page-outline heading literal
    per page module and (b) it is emitted inside a sanctioned header (page_header/object_header/stub_page/
    the sanctioned not-found header). The region-title-restates-page check is warning-grade over static
    string literals ONLY (dynamic/interpolated titles and the D-04 semantic case are not string-matchable
    and are deferred to the Phase-41 DOM assertion). Create the scan-convention guard (D-11): assert
    filter/scope state is not held only in socket assigns for scan pages (the target, review_queue, was
    migrated in Plan 05); the guard must NOT red-flag dataset_live and must EXEMPT sort (D-09 option B is
    locked). Both guards are warning-grade with an explicit-offender allow-list; use descriptive failure
    messages. Collect offenders and assert the offender list is empty (green on arrival because Wave 4
    runs after the migrations).
  </action>
  <verify>
    <automated>mix test test/scoria_web/single_header_guard_test.exs test/scoria_web/scan_convention_guard_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - single_header_guard scans page LiveViews only, excludes ui.ex + dialog-scoped files, asserts one
      sanctioned page-outline header per page, and is green.
    - scan_convention_guard flags socket-only filter/scope state, does not red-flag dataset_live, exempts
      sort, and is green.
    - Both mirror the Phase-38 source-scan idiom.
  </acceptance_criteria>
  <done>The single-header and scan-convention guards exist, are warning-grade, and pass on arrival.</done>
</task>

<task type="auto">
  <name>Task 2: Copy guard (D-26)</name>
  <files>test/scoria_web/copy_guard_test.exs</files>
  <read_first>
    - test/scoria_web/ds06_drift_guard_test.exs (the "ui.ex zero" allow-list assertion style ~96-107 to
      mirror for the approved-label-fn allow-list).
    - test/scoria_web/ui_drift_guard_test.exs:19-40 (offender-collection skeleton).
    - .planning/phases/39-component-groups-and-operator-flows/39-PATTERNS.md lines 356-359 (D-26 copy
      guard spec) and the offender table (319-333).
    - .planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md D-26, D-23, D-25.
  </read_first>
  <action>
    Create `test/scoria_web/copy_guard_test.exs` (Phase-38 source-scan, explicit-offender / warning-grade)
    asserting: (a) no schema/module names appear in page headings; (b) no opaque IDs (the entity_id /
    *_run_id / *_session_id families) are interpolated into page-outline or region headings or into
    :title/:eyebrow slots; (c) status renders only via an allow-list of approved label functions
    (status_label, state_label, delegated_status_label, ApprovalCopy.*, and the *Copy.* per-domain
    modules) — never a raw status atom (this is the connectors runtime-status catch). Assert the known
    offenders are fixed and no NEW literal offender pattern is present; do not attempt semantic proof over
    dynamic assigns (Phase 41 hardens). Use the ds06 allow-list assertion style and descriptive failure
    messages. Green on arrival because Plans 04/05/06 fixed the offenders.
  </action>
  <verify>
    <automated>mix test test/scoria_web/copy_guard_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - copy_guard_test asserts (a) no module names in headings, (b) no opaque IDs in headings/:title/
      :eyebrow, (c) status only via the approved-label-fn allow-list.
    - The guard is warning-grade with an explicit-offender allow-list and is green.
  </acceptance_criteria>
  <done>The copy guard enforces operator-first orientation and the status-label allow-list, and passes on arrival.</done>
</task>

<task type="auto">
  <name>Task 3: Automated flow proof + full-suite green</name>
  <files>priv/dev/e2e/ia_orientation.spec.mjs</files>
  <read_first>
    - priv/dev/e2e/ia_orientation.spec.mjs (existing orientation spec to extend) and
      priv/dev/e2e/uat.spec.mjs (existing decision-flow spec conventions; the toast specs make
      destructive approval decisions — reuse that top-up pattern).
    - lib/mix/tasks/scoria.ui.e2e.ex (the runner; specs run against a running dev server; seeded demo
      ids are resolved from dev_seed.exs).
    - .planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md success criteria 1-5;
      MEMORY note (shift-left: prefer ExUnit + mix scoria.ui.e2e over manual UAT).
  </read_first>
  <action>
    Extend the e2e flow proof (priv/dev/e2e/ia_orientation.spec.mjs, and add assertions to the existing
    decision-flow spec if needed) to prove Phase-39 outcomes against the running dashboard: (1) each
    primary page shows exactly one page-outline header with a single obvious scan/action path (FLOW-01);
    (2) the approval drawer opens decision-first with actions near the consequence and no uppercase warn
    banner (FLOW-03); (3) the Pending|Decided scope toggles via URL, a decided approval opens a read-only
    receipt with no Approve/Deny buttons, and the ?approval=<id> deep-link opens the drawer (FLOW-04).
    Reuse the seeded demo ids resolved by the mix task. This replaces a manual UAT checkpoint with an
    automated spec (project automation preference). Then run the full ExUnit suite to confirm nothing
    regressed.
  </action>
  <verify>
    <automated>mix test --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - ia_orientation.spec.mjs (and the decision-flow spec) assert one-header-per-page, decision-first
      drawer, Pending|Decided URL scope, read-only decided receipt, and the ?approval deep-link.
    - `mix test --warnings-as-errors` passes (full suite green, including all Phase-39 guards, DS-06,
      token-contrast, and the D-20 write-invariant guard).
  </acceptance_criteria>
  <done>An automated e2e flow proof covers orientation + the approval decision/history flows, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree → drift guards | Guards read source files to prevent regression of Phase-39 conventions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-08-T | Tampering | orientation/copy regression | medium | mitigate | Warning-grade single-header, scan-convention, and copy guards fail the build if a page reintroduces a redundant header, socket-only filter, module-name heading, or raw status atom |
| T-39-08-Sp | Spoofing | decided-receipt regression | high | mitigate | The e2e flow proof asserts a decided approval opens a read-only receipt with no Approve/Deny buttons, backstopping the D-19/D-27 structural guarantees at the browser level |
| T-39-08-SC | Tampering | dependency installs | low | accept | No package installs this phase; the e2e harness reuses the existing Playwright toolchain |
</threat_model>

<verification>
- `mix test --warnings-as-errors` passes (full suite: three new Phase-39 guards + D-20 write-invariant guard + DS-06 + token-contrast + all LiveView tests).
- `mix scoria.ui.e2e` flow proof passes against the dev harness (boot per the dev-harness runbook).
- Grep confirms the three new guard files exist and each collects offenders + asserts an empty list.
</verification>

<success_criteria>
- Single-header (D-05), scan-convention (D-11), and copy (D-26) guards exist, are warning-grade, and are green on arrival.
- The scan-convention guard exempts sort and does not red-flag dataset_live (D-09 option B).
- An automated flow proof covers orientation + approval decision/history; the full suite is green.
</success_criteria>

<artifacts_this_phase_produces>
- `test/scoria_web/single_header_guard_test.exs` (D-05)
- `test/scoria_web/scan_convention_guard_test.exs` (D-11)
- `test/scoria_web/copy_guard_test.exs` (D-26)
- Extended `priv/dev/e2e/ia_orientation.spec.mjs` flow proof
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/39-component-groups-and-operator-flows/39-08-SUMMARY.md` when done.
</output>
