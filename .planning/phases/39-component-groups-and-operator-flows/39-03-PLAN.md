---
phase: 39-component-groups-and-operator-flows
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria_web/approval_copy.ex
  - lib/scoria/workflows/remote_approval_projection.ex
  - priv/repo/dev_seed.exs
  - test/scoria_web/approval_copy_test.exs
  - test/scoria/workflows/approval_write_invariant_guard_test.exs
autonomous: true
requirements: [FLOW-03, FLOW-04, COPY-01]
must_haves:
  truths:
    - "ApprovalCopy is the single SSOT for decision copy: status_line/1, eyebrow/1, decision_outcome/1 (produces \"Denied\", D-24d), impact_lead/1, and decided-receipt helpers exist, templated on the module's own clause families (D-16, D-27)."
    - "The raw status-atom evidence row {\"Status\", field(approval, :status)} is DELETED from ApprovalCopy.evidence_rows/1 (approval_copy.ex:215) — it duplicated the badge and leaked a raw atom (D-16/D-23)."
    - "Workflows.list_decided_approvals/1 exists, scoping status in [\"approved\",\"rejected\",\"expired\"], reusing apply_filters/project_approval/normalize_filters, ordered desc updated_at/desc id as a proxy sort only, bounded (capped + load-more) (D-20/D-10)."
    - "Decided-receipt helpers state the RECORDED DECISION only — never that the tool side-effect or run continuation succeeded; \"Expired\" renders with NO fabricated time/actor unless a real approval.expired audit event exists (D-27/D-18/D-21)."
    - "dev_seed.exs routes decided/expired fixtures through Workflows.approve(id, <status>), NOT Repo.update_all(set: [status: ...]) — only the real path emits the audit event and bumps updated_at (D-21)."
  artifacts:
    - lib/scoria_web/approval_copy.ex
    - lib/scoria/workflows/remote_approval_projection.ex
    - test/scoria/workflows/approval_write_invariant_guard_test.exs
  key_links:
    - "list_decided_approvals/1 mirrors list_pending_approvals/1 (projection.ex:16-25), swapping only the where clause and reusing apply_filters/project_approval."
    - "decided-at/decider SSOT is the decision AuditOutboxEvent (inserted_at, actor_ref), NOT updated_at — the query's updated_at sort is a proxy; the displayed values come from the audit event (Plan 07)."
    - "The write-invariant guard asserts no runtime path writes an approval row after it leaves pending (decided-at integrity depends on it)."
---

<objective>
Build the approval-domain data + copy foundation that the drawer (Plan 06) and decision-history
(Plan 07) plans compose: extend `ApprovalCopy` as the decision-copy SSOT (D-16), add
`Workflows.list_decided_approvals/1` (D-20), route decided/expired fixtures through the real
`approve/3` path (D-21), and guard the decided-at write invariant (D-20). Encode the ⚠ SAFETY D-27
honest-receipt copy contract here so the drawer/history consumers cannot present a failed resume as a
clean success.

Purpose: This is the append-only, migration-free history substrate. Approval statuses are already
terminal-complete and decider/time are transactionally recorded in the audit outbox, so history needs
a query scope + an event lookup + honest copy — not a schema change. Shipping it in Wave 1 unblocks
Plans 06/07.

Output: extended `ApprovalCopy`, `list_decided_approvals/1`, real-path fixtures, and a write-invariant
guard. None of these files overlaps another Wave 1 plan (no approvals_live/index.ex edits here).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md
@.planning/phases/39-component-groups-and-operator-flows/39-PATTERNS.md
@lib/scoria_web/approval_copy.ex
@lib/scoria/workflows/remote_approval_projection.ex
@lib/scoria/workflows.ex
@priv/repo/dev_seed.exs
@test/scoria_web/ds06_drift_guard_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ApprovalCopy as the decision-copy SSOT and delete the raw status row</name>
  <files>lib/scoria_web/approval_copy.ex, test/scoria_web/approval_copy_test.exs</files>
  <read_first>
    - lib/scoria_web/approval_copy.ex — the whole module; specifically the existing clause families:
      decision_badge/1 (~182-184, 3-clause shape to mirror for status_line/1 and eyebrow/1),
      reject_label/1 (~176, mirror for decision_outcome/1 producing "Denied"), impact/1 (~106-134,
      extract its lead clause for impact_lead/1), evidence_rows/1 (~209-217, DELETE the :215 raw
      {"Status", field(approval, :status)} row), field/1/present?/1/blank?/1 helpers (~237-313).
    - .planning/phases/39-component-groups-and-operator-flows/39-PATTERNS.md lines 156-178 (ApprovalCopy
      extensions + dedup + the exact row to delete).
    - .planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md D-16, D-18, D-24d, D-27.
  </read_first>
  <behavior>
    - status_line/1 returns one canonical decision-status string for pending/approved/rejected(→"Denied"
      via decision_outcome/1)/expired.
    - eyebrow/1 returns tool context (not the generic "Approval request").
    - decision_outcome/1 returns "Denied" for a rejected approval (D-24d — the ONLY home for "Denied").
    - impact_lead/1 returns the concrete irreversible-effect lead clause (e.g. "This issues a
      $10,000.00 refund to cust_889.") reused by the confirm modal and the drawer consequence.
    - A decided-receipt helper produces past-tense agentful copy "Approved by {actor} · {time}" /
      "Denied by {actor} · {time}", and for "expired" renders WITHOUT a fabricated `· {time}`/actor
      when no real audit event/values are supplied.
    - evidence_rows/1 no longer includes a {"Status", ...} row.
    - No receipt helper asserts run-continuation or side-effect success — decision statement only (D-27).
  </behavior>
  <action>
    Extend `ApprovalCopy` with `status_line/1` and `eyebrow/1` (mirror decision_badge/1's 3-clause
    shape), `decision_outcome/1` producing "Denied" (mirror reject_label/1; D-24d — this is the single
    home for the operator word "Denied"), `impact_lead/1` (extract the lead clause of impact/1 ~110-120),
    and decided-receipt helpers producing past-tense agentful copy. ⚠ SAFETY (D-27): receipt helpers
    state the recorded decision ONLY and take decided-at/decider as explicit arguments (sourced by the
    caller from the audit event in Plan 07) — they must never claim the resume/side-effect succeeded,
    and must render "expired" with no fabricated time/actor when those arguments are absent (accept a
    "time unavailable"-style rendering, never invent values, per D-18/D-21/D-27). DELETE the raw
    status-atom evidence row at evidence_rows/1:215 (the `{"Status", field(approval, :status)}` entry)
    — it duplicates the badge and leaks a raw atom (D-16/D-23). Add approval_copy_test.exs coverage for
    each new function, for decision_outcome/1 == "Denied" on a rejected approval, for the expired
    no-fabrication path, and asserting evidence_rows/1 no longer contains a status row.
  </action>
  <verify>
    <automated>mix test test/scoria_web/approval_copy_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - status_line/1, eyebrow/1, decision_outcome/1, impact_lead/1, and decided-receipt helper(s) exist
      and are tested.
    - decision_outcome/1 returns "Denied" for rejected; expired receipt renders with no fabricated
      time/actor when none supplied.
    - evidence_rows/1 no longer emits a status row (asserted by test).
  </acceptance_criteria>
  <done>ApprovalCopy is the decision-copy SSOT with honest (D-27) receipt helpers, "Denied" lives here, and the raw status evidence row is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Workflows.list_decided_approvals/1 (bounded projection)</name>
  <files>lib/scoria/workflows/remote_approval_projection.ex, test/scoria/workflows/approval_write_invariant_guard_test.exs</files>
  <read_first>
    - lib/scoria/workflows/remote_approval_projection.ex:16-25 (list_pending_approvals/1 — exact
      analog: where → apply_filters → order_by → Repo.all → Enum.map(&project_approval/1)); :12 and
      :88-105 (@filter_fields / normalize_filters / apply_filters to reuse); :27-31
      (get_approval_lineage!/1 — hydrates any status but carries the requesting actor, not the decider).
    - .planning/phases/39-component-groups-and-operator-flows/39-PATTERNS.md lines 182-204
      (list_decided_approvals shape + data caveat).
    - .planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md D-20, D-10.
  </read_first>
  <behavior>
    - list_decided_approvals/1 returns projected approvals with status in
      ["approved","rejected","expired"], ordered desc updated_at then desc id.
    - It reuses apply_filters/project_approval/normalize_filters unchanged and accepts the outcome
      sub-filter (All/Approved/Denied/Expired) via the existing @filter_fields.
    - The result is bounded (capped) so it supports a capped + load-more history surface (D-10), not
      an unbounded Repo.all.
  </behavior>
  <action>
    Add `list_decided_approvals/1` to `remote_approval_projection.ex`, mirroring
    `list_pending_approvals/1` exactly and swapping only the where clause to
    `where([a], a.status in ["approved","rejected","expired"])`, with
    `order_by([a], desc: a.updated_at, desc: a.id)` as a cheap query-level PROXY sort only (D-20 —
    decided-at/decider display comes from the audit event in Plan 07, not from updated_at). Reuse
    `apply_filters/1`, `project_approval/1`, and `normalize_filters/1` / `@filter_fields` unchanged so
    the outcome sub-filter works. Make it bounded (capped) to support capped + load-more (D-10 — do NOT
    add streaming or change table/1). Add a projection test asserting the where scope and ordering, and
    that the outcome sub-filter narrows results.
  </action>
  <verify>
    <automated>mix test test/scoria/workflows/approval_write_invariant_guard_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - list_decided_approvals/1 exists, scopes to the three terminal statuses, reuses the shared
      helpers, and is bounded.
    - A test asserts scope, ordering, and outcome-filter narrowing.
  </acceptance_criteria>
  <done>list_decided_approvals/1 returns bounded, filterable decided approvals reusing the existing projection helpers.</done>
</task>

<task type="auto">
  <name>Task 3: Route decided/expired fixtures through approve/3 and guard the write invariant</name>
  <files>priv/repo/dev_seed.exs, test/scoria/workflows/approval_write_invariant_guard_test.exs</files>
  <read_first>
    - priv/repo/dev_seed.exs (~271-304 — the `Repo.update_all(set: [status: "expired"])` block to
      replace with the real approve/3 path).
    - lib/scoria/workflows.ex — approve/3 (~665 guard, ~679 decision write, ~683-720 audit-event write)
      and the three approval-row writers (insert! ~393, second changeset|>update! ~430); the shape the
      D-20 invariant guards against.
    - test/scoria_web/ds06_drift_guard_test.exs (source-scan File.read! + regex + @excluded style to
      mirror for the write-invariant guard).
    - .planning/phases/39-component-groups-and-operator-flows/39-PATTERNS.md lines 335-369 (guard idiom)
      and 363-369 (dev_seed fixtures).
    - .planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md D-20, D-21.
  </read_first>
  <action>
    In `priv/repo/dev_seed.exs`, replace the `Repo.update_all(set: [status: "expired"])` fixture path
    (~271-304) with `Workflows.approve(id, "expired")` (and route any other decided fixtures through
    `approve(id, <status>)`), so fixtures emit the audit event and bump updated_at — only the real path
    exercises the history surface (decider + time). No fabricated history in seeds (D-21). Then add a
    warning-grade source-scan write-invariant guard (mirroring the ds06 File.read! + regex + @excluded
    idiom) that asserts no runtime path writes an approval row after it leaves the pending state —
    decided-at integrity depends on this. Scope the scan to lib/scoria and priv/repo; allow-list the
    single legitimate creation-time writer path; flag any additional post-pending row write (the
    codebase already contains the fragile shape at workflows.ex ~430 and the seed update_all now being
    removed). Keep the guard warning-grade this phase (Phase 41 hardens). Ensure ds06 and token-contrast
    guards remain green.
  </action>
  <verify>
    <automated>mix test test/scoria/workflows/approval_write_invariant_guard_test.exs test/scoria_web/ds06_drift_guard_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - dev_seed.exs no longer uses update_all to set a decided status; decided/expired fixtures go
      through approve/3.
    - A warning-grade write-invariant guard exists asserting no post-pending approval-row write outside
      the allow-listed creation path, and it is green.
    - ds06 drift guard stays green.
  </acceptance_criteria>
  <done>Fixtures exercise the real decision path and a guard protects the decided-at write invariant; guards green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| decision write path → history display | Recorded decision provenance (decider, time) is consumed by later history/receipt UI |
| seed/fixture writes → approval rows | Fixture code can bypass the real decision path and forge history |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-03-R | Repudiation | decided-at/decider provenance | high | mitigate | Decided-at/decider SSOT is the append-only audit AuditOutboxEvent, never updated_at; list_decided_approvals/1 uses updated_at only as a proxy sort; the write-invariant guard asserts no post-pending row write so decided-at integrity holds (D-20) |
| T-39-03-T | Tampering | fabricated history (fixtures/UI) | high | mitigate | dev_seed routes decided/expired through the real approve/3 path so history is real; expired renders with no fabricated time/actor absent a real audit event (D-21/D-18) |
| T-39-03-Sp | Spoofing | receipt implying success | high | mitigate | D-27 receipt helpers assert the recorded decision only, never resume/side-effect success (⚠ SAFETY) |
| T-39-03-SC | Tampering | dependency installs | low | accept | No package installs this phase; "no new Hex runtime dep" invariant holds |
</threat_model>

<verification>
- `mix test test/scoria_web/approval_copy_test.exs test/scoria/workflows/approval_write_invariant_guard_test.exs --warnings-as-errors` passes.
- `mix compile --warnings-as-errors` succeeds.
- `mix test test/scoria_web/ds06_drift_guard_test.exs test/scoria_web/token_contrast_guard_test.exs` stays green.
- Grep confirms no `{"Status"` row remains in ApprovalCopy.evidence_rows/1 and no `update_all` decided-status write remains in dev_seed.exs.
</verification>

<success_criteria>
- ApprovalCopy is the decision-copy SSOT with honest D-27 receipt helpers and no raw status row.
- list_decided_approvals/1 provides bounded, filterable decided history reusing existing helpers.
- Fixtures exercise the real decision path; the write-invariant guard is green.
</success_criteria>

<artifacts_this_phase_produces>
- `ApprovalCopy.status_line/1`, `ApprovalCopy.eyebrow/1`, `ApprovalCopy.decision_outcome/1`, `ApprovalCopy.impact_lead/1`, decided-receipt helper(s)
- `Workflows.list_decided_approvals/1` (remote_approval_projection.ex)
- `test/scoria/workflows/approval_write_invariant_guard_test.exs` (D-20 write-invariant guard)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/39-component-groups-and-operator-flows/39-03-SUMMARY.md` when done.
</output>
