---
phase: 39-component-groups-and-operator-flows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria_web/ui.ex
  - test/scoria_web/ui_component_test.exs
autonomous: true
requirements: [FLOW-01, COPY-01]
must_haves:
  truths:
    - "page_header/1 renders exactly one <h1> from a required :string title attr (not a slot), so module names / IDs structurally cannot leak into the page <h1> (D-01, D-23/D-26)."
    - "page_header/1 exposes slot :summary (one-line <p>) and slot :actions (0..1 header action) and reuses .scoria-pagehead* classes with zero new CSS classes or tokens (D-01)."
    - "status_label/1 gains curated operator-label clauses ABOVE its retained generic String.replace fallback and status_label(_) -> \"Unknown\" clause; an unseen status still returns a titleized/Unknown string, never raising FunctionClauseError (D-24 / D-24a — the hybrid additive mechanism, page-500 prevention; D-24b/c copy modules land in Plan 39-02)."
    - "status_label/1 does NOT curate \"rejected\" — \"Denied\" is approval-domain only (D-24d)."
  artifacts:
    - lib/scoria_web/ui.ex
  key_links:
    - "page_header/1 reuses .scoria-pagehead / .scoria-pagehead__title / .scoria-pagehead__title--with-actions / .scoria-pagehead__description already in assets/css/04-components.css:1161."
    - "status_label/1 remains the atom fallback inside evidence_text/1 (ui.ex ~1376) and object_header/1 (ui.ex ~441) across 6 disjoint domains — the generic fallback backstops all of them."
---

<objective>
Add the two shared `ScoriaWeb.UI` primitives every Phase 39 page consumes: a thin `page_header/1`
(D-01) that owns each page's single page-outline `<h1>`, and an ADDITIVE upgrade to `status_label/1`
(D-24a) that adds curated operator labels while preserving the generic fallback that backstops six
domains against a render-time 500.

Purpose: These are the foundation of the Coherence Spine — Area 1 (page orientation) and Area 5
(operator-first status copy) both depend on them. They ship first so downstream page-migration and
microcopy plans have the vocabulary to adopt.

Output: `page_header/1` and an upgraded `status_label/1` in `lib/scoria_web/ui.ex`, with unit-test
coverage. No caller changes yet (that is Plans 04/05/06), so this plan has zero page-file overlap.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md
@.planning/phases/39-component-groups-and-operator-flows/39-PATTERNS.md
@lib/scoria_web/ui.ex
@assets/css/04-components.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add page_header/1 to ScoriaWeb.UI</name>
  <files>lib/scoria_web/ui.ex, test/scoria_web/ui_component_test.exs</files>
  <read_first>
    - lib/scoria_web/ui.ex:193-214 (page_section/1 — the structural template: a container with a
      header block + right-aligned :actions slot conditional-render idiom at ~207. Mirror this shape
      but emit <h1> and use .scoria-pagehead* classes.)
    - .planning/phases/39-component-groups-and-operator-flows/39-PATTERNS.md lines 47-94 (page_header
      pattern assignment: exact attr/slot shape, richest analog review_queue_live.ex:52, simplest
      analog dataset_live/index.ex:62, CSS to reuse).
    - assets/css/04-components.css:1161-1175 (the .scoria-pagehead* classes to reuse; do NOT add
      classes).
    - test/scoria_web/ui_component_test.exs (existing render-based unit test style to mirror).
  </read_first>
  <behavior>
    - Rendering page_header with title "Datasets" emits exactly one <h1> containing "Datasets".
    - title is a required :string attr (per D-01) — passing markup/a slot for the heading is not the
      contract; the <h1> body is the plain string only.
    - A :summary slot renders a <p class="scoria-pagehead__description"> below the title when given.
    - A single :actions entry renders in the action region and applies the
      .scoria-pagehead__title--with-actions modifier; zero :actions entries render no action region.
    - The component adds no CSS class outside the existing .scoria-pagehead* family.
  </behavior>
  <action>
    Add a new `page_header/1` function component near `page_section/1` (~ui.ex:193). Declare
    `attr :title, :string, required: true` (renders the page's ONLY `<h1>` — a plain string attr, NOT
    a slot, so IDs/module names structurally cannot leak into the heading per D-01/D-23/D-26),
    `attr :class, :string, default: nil`, `attr :rest, :global`, `slot :summary` (one-line operator
    orientation `<p>`), and `slot :actions` (0..1 header action — a primary action OR a ghost/secondary
    nav link, e.g. review_queue's "Back to dashboard" ghost link). Mirror `page_section/1`'s conditional
    action-render idiom (~ui.ex:207): apply the existing `.scoria-pagehead__title--with-actions`
    modifier only when an actions entry is present. Reuse `.scoria-pagehead`,
    `.scoria-pagehead__title`, `.scoria-pagehead__description` from 04-components.css:1161 — add NO new
    tokens or classes. Name the `:actions` slot deliberately after Phoenix core_components `<.header>`
    for adopter familiarity, diverging only on `attr :title` and the single-action convention.
    Add render-based unit tests in ui_component_test.exs covering the behaviors above.
  </action>
  <verify>
    <automated>mix test test/scoria_web/ui_component_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - page_header/1 exists in lib/scoria_web/ui.ex with attr :title (:string, required) and slots
      :summary + :actions.
    - Rendered output contains exactly one <h1> equal to the title string; no new CSS class beyond
      .scoria-pagehead* appears in the component.
    - New unit tests pass under --warnings-as-errors.
  </acceptance_criteria>
  <done>page_header/1 renders a single string-only <h1>, optional summary <p>, and 0..1 action, reusing existing pagehead CSS, with passing unit coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Additively upgrade status_label/1 (keep the generic fallback)</name>
  <files>lib/scoria_web/ui.ex, test/scoria_web/ui_component_test.exs</files>
  <read_first>
    - lib/scoria_web/ui.ex:52-58 (status_label/1 — the atom→binary delegation, the generic
      String.replace("_"," ")|>capitalize binary clause, and the status_label(_) -> "Unknown"
      fallback that MUST be retained).
    - lib/scoria_web/ui.ex:21-49 (tone/1 — the `case status do s when s in ~w(...) -> ...` curated
      idiom to mirror for the new operator-label clauses).
    - .planning/phases/39-component-groups-and-operator-flows/39-PATTERNS.md lines 98-119
      (status_label additive-upgrade rules and the closed-set 500 hazard).
    - .planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md D-24a and D-25 (canonical
      status vocabulary; "Denied" is NOT curated here).
  </read_first>
  <behavior>
    - status_label("running") returns "Running"; curated operator labels from the D-25 vocabulary
      (e.g. Pending, Approved, Expired, Passed, Failed, Regressed, Running, Promoted, Draft,
      Published, Connected, Disconnected, Idle) return their curated forms.
    - status_label("some_unseen_status") still returns a titleized "Some unseen status" via the
      retained generic clause — it does NOT raise.
    - status_label(:pending) (atom) delegates through the binary path unchanged.
    - status_label("rejected") returns "Rejected" (NOT "Denied") — "Denied" stays approval-domain only
      (D-24d).
  </behavior>
  <action>
    Insert curated binary clauses ABOVE the generic `String.replace` binary clause in `status_label/1`,
    mirroring `tone/1`'s curated-case-above-fallback structure (ui.ex:21-49). Curate only the D-25
    canonical status vocabulary. CRITICAL (D-24a, ⚠ page-500 prevention): retain BOTH the generic
    `String.replace("_"," ") |> capitalize` binary clause AND the `status_label(_) -> "Unknown"`
    clause — this function is the atom fallback inside `evidence_text/1` (~ui.ex:1376) and
    `object_header/1` (~ui.ex:441) across six disjoint domains, so a closed allow-list would raise
    FunctionClauseError inside render/1 and 500 the page on any unseen status. Do NOT curate
    "rejected" (per D-24d — the operator word "Denied" is produced only by ApprovalCopy.decision_outcome/1
    in Plan 03). Add unit tests asserting: curated labels resolve, an arbitrary unseen status returns a
    non-raising titleized string, and "rejected" resolves to "Rejected".
  </action>
  <verify>
    <automated>mix test test/scoria_web/ui_component_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - status_label/1 has curated clauses plus the retained generic String.replace clause and the
      Unknown catch-all.
    - A test asserts an unseen status string returns a titleized value without raising.
    - A test asserts status_label("rejected") == "Rejected".
  </acceptance_criteria>
  <done>status_label/1 curates the D-25 vocabulary additively, never raises on unseen input, and does not curate "rejected".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| record status atom → rendered UI | Arbitrary/unseen status values flow into status_label/1 during render/1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01-DoS | Denial of Service | status_label/1 closed-set regression | high | mitigate | Retain generic String.replace clause + Unknown catch-all (D-24a); unit test asserts unseen status returns a string without FunctionClauseError, preventing a render-time 500 |
| T-39-01-I | Information Disclosure | page_header/1 <h1> | medium | mitigate | title is a :string attr, not a slot (D-01) — module names/IDs structurally cannot be interpolated into the page-outline <h1> |
| T-39-01-SC | Tampering | dependency installs | low | accept | No package installs this phase; the "no new Hex runtime dep" invariant holds |
</threat_model>

<verification>
- `mix test test/scoria_web/ui_component_test.exs --warnings-as-errors` passes.
- `mix compile --warnings-as-errors` succeeds.
- `mix test test/scoria_web/ds06_drift_guard_test.exs test/scoria_web/token_contrast_guard_test.exs`
  stays green (no raw palette / contrast drift introduced).
</verification>

<success_criteria>
- page_header/1 and the additively-upgraded status_label/1 exist in ui.ex with passing unit tests.
- No new CSS classes/tokens; DS-06 and token-contrast guards remain green.
- status_label/1 provably never raises on an unseen status.
</success_criteria>

<artifacts_this_phase_produces>
- `page_header/1` (new function component in lib/scoria_web/ui.ex)
- upgraded `status_label/1` (additive curated clauses in lib/scoria_web/ui.ex)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/39-component-groups-and-operator-flows/39-01-SUMMARY.md` when done.
</output>
