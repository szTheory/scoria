---
phase: 24-trace-to-dataset-curation-via-liveview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/20260518000000_create_ai_eval_datasets.exs
  - lib/scoria/eval/dataset.ex
  - lib/scoria/eval/dataset_item.ex
  - test/scoria/eval/dataset_test.exs
  - test/scoria/eval/dataset_item_test.exs
autonomous: true
requirements:
  - DATA-02
must_haves:
  truths:
    - "System persists datasets and items in PostgreSQL natively."
    - "Dataset items store multi-turn input context exactly matching runtime shape."
    - "Datasets enforce strict open/sealed immutability validation."
  artifacts:
    - path: "lib/scoria/eval/dataset.ex"
      provides: "Ecto schema and state machine for datasets"
      contains: "schema \"ai_eval_datasets\""
    - path: "lib/scoria/eval/dataset_item.ex"
      provides: "Ecto schema for multi-turn test case storage"
      contains: "schema \"ai_eval_dataset_items\""
  key_links:
    - from: "lib/scoria/eval/dataset_item.ex"
      to: "lib/scoria/eval/dataset.ex"
      via: "belongs_to association and state validation"
      pattern: "belongs_to :dataset, Scoria.Eval.Dataset"
---

<objective>
Create foundational Ecto relational models for evaluation datasets and items.

Purpose: Enable durable, relational storage for evaluation baselines that can store exact multi-turn JSONB runtime arguments, while preventing subsequent CI drift by providing a strict `:sealed` state.
Output: Migrations and Schemas for `Dataset` and `DatasetItem`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-trace-to-dataset-curation-via-liveview/24-PATTERNS.md
@.planning/phases/24-trace-to-dataset-curation-via-liveview/24-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Generate Datasets Migration</name>
  <files>priv/repo/migrations/*_create_ai_eval_datasets.exs</files>
  <action>
    Create a new Ecto migration to define `ai_eval_datasets` and `ai_eval_dataset_items` tables.
    
    `ai_eval_datasets`:
    - `name` (string, null: false)
    - `version` (string, null: false)
    - `description` (string)
    - `tags` (array of strings)
    - `state` (string, default: "open")
    - timestamps
    - Unique index on `[:name, :version]`
    
    `ai_eval_dataset_items`:
    - `dataset_id` (references `ai_eval_datasets`, on_delete: :delete_all)
    - `source_trace_id` (string, nullable)
    - `input` (map/jsonb, null: false)
    - `expected_output` (map/jsonb)
    - `metadata` (map/jsonb)
    - timestamps
  </action>
  <verify>
    <automated>mix ecto.migrate</automated>
  </verify>
  <done>Migration runs successfully and creates the tables.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Dataset Schema</name>
  <files>lib/scoria/eval/dataset.ex, test/scoria/eval/dataset_test.exs</files>
  <behavior>
    - Test: Validates required fields.
    - Test: Modifying an `:open` dataset works.
    - Test: Modifying a `:sealed` dataset (other than its state perhaps, or entirely) returns an error "cannot be modified once sealed".
  </behavior>
  <action>
    Create `Scoria.Eval.Dataset`. Reference analog `lib/scoria/prompt_registry/prompt_template.ex` from PATTERNS.md for the core schema and changeset validation patterns.
    - Use `Ecto.Enum` for `state` (`[:open, :sealed]`).
    - Define `changeset/2` validating required fields (`name`, `version`, `state`).
    - Implement `validate_immutable_if_sealed/1` in the changeset: if the existing `data.state` is `:sealed`, return a validation error on `:state` to prevent modification.
  </action>
  <verify>
    <automated>mix test test/scoria/eval/dataset_test.exs</automated>
  </verify>
  <done>Schema defined and immutable state correctly validated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement DatasetItem Schema</name>
  <files>lib/scoria/eval/dataset_item.ex, test/scoria/eval/dataset_item_test.exs</files>
  <behavior>
    - Test: Validates required fields (`input`).
    - Test: Validation succeeds when dataset is `:open`.
    - Test: Validation fails when dataset state passed is `:sealed`.
  </behavior>
  <action>
    Create `Scoria.Eval.DatasetItem`. Reference analog `lib/scoria/prompt_registry/prompt_template.ex` from PATTERNS.md for the core schema architecture.
    - Belongs to `Scoria.Eval.Dataset`.
    - Provide a `changeset/3` function: `def changeset(item, attrs, dataset_state \\ :open)`.
    - Add custom validation that returns an error on `:dataset_id` ("cannot add or modify items in a sealed dataset") if `dataset_state` is `:sealed`.
  </action>
  <verify>
    <automated>mix test test/scoria/eval/dataset_item_test.exs</automated>
  </verify>
  <done>Schema defined and state constraint verified via argument passing.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DB Validation | State immutability validated directly in the application layer before reaching PG. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | Dataset/DatasetItem | mitigate | Use Ecto changeset state enforcement to strictly prevent writes to `:sealed` datasets. |
</threat_model>

<verification>
mix ecto.migrate passes. Ecto schema tests pass.
</verification>

<success_criteria>
Schemas fully enforce the `:sealed` constraint preventing data drift.
</success_criteria>

<output>
After completion, create `.planning/phases/24-trace-to-dataset-curation-via-liveview/24-01-SUMMARY.md`
</output>