---
phase: 24-trace-to-dataset-curation-via-liveview
plan: 03
type: execute
wave: 3
depends_on: 
  - 24-02
files_modified:
  - lib/scoria_web/live/dataset_live/promote_component.ex
  - lib/scoria_web/components/workflow_detail_panel_component.ex
  - lib/scoria_web/live/workflow_live/show.ex
  - test/scoria_web/live/dataset_live/promote_component_test.exs
  - test/scoria_web/live/workflow_live/show_test.exs
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - "Operator can click a 'Promote to Dataset' button on a trace."
    - "A LiveView modal extracts trace multi-turn context and presents it for manual redaction."
    - "Operator can save the redacted payload to a dataset."
  artifacts:
    - path: "lib/scoria_web/live/dataset_live/promote_component.ex"
      provides: "Interactive dataset promotion and PII redaction modal"
    - path: "lib/scoria_web/live/workflow_live/show.ex"
      provides: "Parent LiveView that manages modal state and rendering"
  key_links:
    - from: "lib/scoria_web/live/workflow_live/show.ex"
      to: "lib/scoria_web/live/dataset_live/promote_component.ex"
      via: "LiveView rendering the modal"
      pattern: "live_component module={ScoriaWeb.DatasetLive.PromoteComponent}"
    - from: "lib/scoria_web/components/workflow_detail_panel_component.ex"
      to: "lib/scoria_web/live/workflow_live/show.ex"
      via: "Sending open event"
      pattern: "phx-click=\"open_promote_modal\""
---

<objective>
Implement the "Promote to Dataset" LiveView flow for operators.

Purpose: Allow operators to seamlessly extract a production trace, review and redact the multi-turn JSON context (to prevent PII leakage), and save it as an expected test case directly into an Ecto-backed dataset.
Output: The `PromoteComponent` and integration into the existing trace explorer.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-trace-to-dataset-curation-via-liveview/24-PATTERNS.md
@.planning/phases/24-trace-to-dataset-curation-via-liveview/24-RESEARCH.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement Dataset Promotion Component</name>
  <files>lib/scoria_web/live/dataset_live/promote_component.ex, test/scoria_web/live/dataset_live/promote_component_test.exs</files>
  <behavior>
    - Test: Component renders the extracted `input` json in a textarea for redaction.
    - Test: Submitting the form successfully calls `Scoria.Eval.add_dataset_item/2`.
  </behavior>
  <action>
    Create `ScoriaWeb.DatasetLive.PromoteComponent`. Reference analog `lib/scoria_web/live/prompt_live/index.ex` from PATTERNS.md for the LiveView component structure and event handling logic.
    - Component takes the `trace` or `step` context as an assign.
    - Uses Ecto changeset forms without a backing schema (schemaless changeset) for the UI redaction.
    - Form fields: `dataset_id` (select from existing `:open` datasets), `input` (textarea for JSON), `expected_output` (textarea for JSON).
    - `handle_event("save", %{"dataset_id" => id, "input" => input_json, "expected_output" => exp_json}, socket)` parses the JSON strings and saves via `Scoria.Eval.add_dataset_item/2`.
  </action>
  <verify>
    <automated>mix test test/scoria_web/live/dataset_live/promote_component_test.exs</automated>
  </verify>
  <done>Promote component supports JSON redaction and dataset selection.</done>
</task>

<task type="auto">
  <name>Task 2: Inject Promote Button into Trace Explorer</name>
  <files>lib/scoria_web/components/workflow_detail_panel_component.ex</files>
  <action>
    Update `workflow_detail_panel(assigns)` to include a "Promote to Dataset" action.
    - Under the role/kind list (or in a new action section), add a button that triggers a `phx-click` to open the modal (or handles client-side toggling).
    - Send a `phx-click="open_promote_modal"` with `phx-value-step-id={@step.id}` to the parent LiveView.
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>The Promote button is wired to trigger the open event.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate Promote Modal in Workflow Show LiveView</name>
  <files>lib/scoria_web/live/workflow_live/show.ex, test/scoria_web/live/workflow_live/show_test.exs</files>
  <behavior>
    - Test: Sending `open_promote_modal` updates assigns to show the modal.
    - Test: The modal renders `PromoteComponent` when open.
  </behavior>
  <action>
    Update the parent `ScoriaWeb.WorkflowLive.Show` LiveView to support the dataset promotion modal.
    - Add state to `assigns` (e.g., `:promote_step_id`) initially set to `nil`.
    - Implement a `handle_event("open_promote_modal", %{"step-id" => step_id}, socket)` to update the assign and show the modal.
    - Ensure there is an event to close the modal (e.g., `handle_event("close_modal", _, socket)` setting `:promote_step_id` to `nil`).
    - In the template, conditionally render `<.modal id="promote-modal" show={@promote_step_id != nil}>` containing the `<.live_component module={ScoriaWeb.DatasetLive.PromoteComponent} id="promote-component" step_id={@promote_step_id} />`.
  </action>
  <verify>
    <automated>mix test test/scoria_web/live/workflow_live/show_test.exs</automated>
  </verify>
  <done>The parent LiveView correctly handles the state and rendering of the promote modal.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Operator -> CI Data | The curation modal is the explicit human boundary preventing production PII from entering CI datasets. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | Information Disclosure | PromoteComponent | mitigate | The UI explicitly forces the operator to review and edit the raw JSON payload before saving, ensuring manual PII redaction is part of the flow. |
</threat_model>

<verification>
mix test passes. The workflow detail panel renders the new button without syntax errors.
</verification>

<success_criteria>
Operator can click a trace step and promote it seamlessly into a structured dataset.
</success_criteria>

<output>
After completion, create `.planning/phases/24-trace-to-dataset-curation-via-liveview/24-03-SUMMARY.md`
</output>