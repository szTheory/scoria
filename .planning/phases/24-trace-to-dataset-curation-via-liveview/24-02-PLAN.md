---
phase: 24-trace-to-dataset-curation-via-liveview
plan: 02
type: execute
wave: 2
depends_on: 
  - 24-01
files_modified:
  - lib/scoria/eval.ex
  - test/scoria/eval_test.exs
autonomous: true
requirements:
  - DATA-02
must_haves:
  truths:
    - "System provides a context boundary for creating, sealing, and reading datasets."
    - "System provides a context boundary for adding items to open datasets."
  artifacts:
    - path: "lib/scoria/eval.ex"
      provides: "Context API for Dataset management"
  key_links:
    - from: "lib/scoria/eval.ex"
      to: "Scoria.Eval.DatasetItem"
      via: "add_dataset_item/2 validating dataset state"
      pattern: "Repo.get"
---

<objective>
Implement the context interface for Dataset and DatasetItem CRUD operations.

Purpose: Provide a clean boundary in `Scoria.Eval` to manage datasets, handling the orchestration of state machine validation (open vs sealed) when adding new test cases.
Output: Extended `Scoria.Eval` context module with dataset capabilities.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-trace-to-dataset-curation-via-liveview/24-PATTERNS.md
@.planning/phases/24-trace-to-dataset-curation-via-liveview/24-RESEARCH.md

<interfaces>
From lib/scoria/eval/dataset.ex:
```elixir
def changeset(dataset, attrs) do
  # ...
end
```

From lib/scoria/eval/dataset_item.ex:
```elixir
def changeset(item, attrs, dataset_state \\ :open) do
  # ...
end
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add Dataset CRUD Context Functions</name>
  <files>lib/scoria/eval.ex, test/scoria/eval_test.exs</files>
  <behavior>
    - Test: `create_dataset/1` creates an `:open` dataset.
    - Test: `seal_dataset/1` updates dataset state to `:sealed`.
    - Test: `add_dataset_item/2` adds an item when dataset is `:open`.
    - Test: `add_dataset_item/2` returns error changeset when dataset is `:sealed`.
  </behavior>
  <action>
    Update `Scoria.Eval` to include dataset management:
    - `list_datasets/0` and `get_dataset!/1`
    - `create_dataset/1`
    - `seal_dataset/1` (Updates state to `:sealed`)
    - `add_dataset_item/2` (Takes `dataset_id` and `attrs`. First fetches the dataset, then passes `dataset.state` as the 3rd argument to `DatasetItem.changeset/3` to ensure we cannot add items to a sealed dataset).
  </action>
  <verify>
    <automated>mix test test/scoria/eval_test.exs</automated>
  </verify>
  <done>Context functions correctly manage dataset state and validation constraints.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Context Boundary | `Scoria.Eval` encapsulates rules ensuring items cannot bypass the `:sealed` dataset check. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02 | Tampering | Scoria.Eval | mitigate | `add_dataset_item/2` fetches the authoritative dataset state from the database before delegating to the item changeset. |
</threat_model>

<verification>
mix test passes for the `Scoria.Eval` dataset operations.
</verification>

<success_criteria>
Context boundary provides a robust way to interact with datasets, honoring the immutability logic.
</success_criteria>

<output>
After completion, create `.planning/phases/24-trace-to-dataset-curation-via-liveview/24-02-SUMMARY.md`
</output>