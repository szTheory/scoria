---
phase: 42-delegated-evidence-adoption-story
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/runtime/run_detail.ex
  - lib/scoria/runtime.ex
  - test/scoria/runtime_test.exs
  - test/scoria/runtime_view_test.exs
autonomous: true
requirements:
  - EVID-01
nyquist_compliant: true
must_haves:
  truths:
    - "Public runtime detail publishes one primary curated delegated projection instead of forcing callers to reconstruct the handoff story from raw workflow rows."
    - "The delegated projection stays projection-only over Phase 41 durable truth and does not introduce new durable authority or alternate execution semantics."
    - "Projection precedence is explicit about how handoff rows, parent steps, child steps, and projected context are linked and summarized."
  artifacts:
    - path: "lib/scoria/runtime/run_detail.ex"
      provides: "Primary delegated evidence DTO projection over existing same-run truth"
    - path: "test/scoria/runtime_test.exs"
      provides: "Facade-level delegated inspectability regressions"
    - path: "test/scoria/runtime_view_test.exs"
      provides: "Curated detail-shape regressions that keep delegated evidence stable"
  key_links:
    - from: "lib/scoria/runtime.ex"
      to: "lib/scoria/runtime/run_detail.ex"
      via: "get_run_detail!/1 remains the public seam that shapes delegated evidence for apps and UI"
      pattern: "get_run_detail|RunDetail"
    - from: "lib/scoria/runtime/run_detail.ex"
      to: "test/scoria/runtime_test.exs"
      via: "delegated projection exposes delegated role, kind, lineage, status, and projected-context summary through the public facade"
      pattern: "delegated_handoffs|delegated_role_id|child_step_id|projected_context"
---

<objective>
Create the primary delegated evidence read model in the public runtime DTO.

Purpose: close the public inspectability gap before UI polish by publishing one boring, curated delegated projection over the Phase 41 substrate.
Output: `RunDetail` exposes a stable delegated-evidence collection apps and operator surfaces can consume directly.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-delegated-evidence-adoption-story/42-CONTEXT.md
@.planning/phases/42-delegated-evidence-adoption-story/42-DISCUSSION-LOG.md
@.planning/phases/42-delegated-evidence-adoption-story/42-UI-SPEC.md
@.planning/phases/42-delegated-evidence-adoption-story/42-RESEARCH.md
@.planning/phases/41-bounded-handoff-contract-safety/41-CONTEXT.md
@lib/scoria/runtime.ex
@lib/scoria/runtime/run_detail.ex
@lib/scoria/workflows/handoff.ex
@test/scoria/runtime_test.exs
@test/scoria/runtime_view_test.exs

<interfaces>
From `lib/scoria/runtime.ex`:
```elixir
def get_run_detail!(run_id) do
```

From `lib/scoria/runtime/run_detail.ex`:
```elixir
defstruct [
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the curated delegated evidence collection to `RunDetail`</name>
  <files>lib/scoria/runtime/run_detail.ex, test/scoria/runtime_test.exs, test/scoria/runtime_view_test.exs</files>
  <read_first>.planning/phases/42-delegated-evidence-adoption-story/42-CONTEXT.md, .planning/phases/42-delegated-evidence-adoption-story/42-RESEARCH.md, lib/scoria/runtime/run_detail.ex, test/scoria/runtime_test.exs, test/scoria/runtime_view_test.exs</read_first>
  <behavior>
    - Test 1: `Scoria.get_run_detail/1` returns one delegated projection entry per persisted handoff, without removing the existing raw `handoffs` and `steps` arrays.
    - Test 2: each delegated projection exposes delegated role, delegated kind, handoff input, parent step -> child step lineage, child status, and projected-context readback in one object.
    - Test 3: runs with no delegated handoffs return an empty delegated collection rather than `nil` or a malformed shape.
  </behavior>
  <action>Extend `Scoria.Runtime.RunDetail` with one primary delegated-evidence collection such as `delegated_handoffs`. Build it from existing durable same-run truth only: handoff row, root handoff step, queued/running/completed child step, and the child step projected-context slice. Preserve the current raw arrays as secondary evidence. Keep the projection stable and boring for callers by exposing explicit IDs/roles/status fields rather than raw schema structs or inferred orchestration state.</action>
  <acceptance_criteria>
    - `rg -n 'delegated_handoffs' lib/scoria/runtime/run_detail.ex test/scoria/runtime_test.exs test/scoria/runtime_view_test.exs` returns matches.
    - `rg -n 'child_step_id|parent_step_id|delegated_kind|handoff_input|projected_context' lib/scoria/runtime/run_detail.ex` returns matches inside the delegated projection path.
    - `rg -n 'detail\\.handoffs|detail\\.steps' test/scoria/runtime_test.exs test/scoria/runtime_view_test.exs` still returns matches proving raw evidence remains available.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/runtime_test.exs test/scoria/runtime_view_test.exs</automated>
  </verify>
  <done>Public runtime detail now exposes one obvious delegated evidence noun without removing lower-level proof surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Make delegated projection precedence and pending-lineage behavior explicit</name>
  <files>lib/scoria/runtime/run_detail.ex, lib/scoria/runtime.ex, test/scoria/runtime_test.exs, test/scoria/runtime_view_test.exs</files>
  <read_first>.planning/phases/42-delegated-evidence-adoption-story/42-DISCUSSION-LOG.md, lib/scoria/runtime/run_detail.ex, lib/scoria/runtime.ex, test/scoria/runtime_test.exs, test/scoria/runtime_view_test.exs</read_first>
  <behavior>
    - Test 1: if a handoff exists before a child step is readable, the delegated projection reports an explicit pending child-step state instead of blank lineage.
    - Test 2: `capability_tags` or similar adjunct metadata render as secondary fields and do not replace core lineage facts.
    - Test 3: the projection sorts delegated entries in workflow sequence order so multi-handoff runs read in execution order.
  </behavior>
  <action>Document and encode the delegated projection’s precedence rules inside `RunDetail`: how to find the parent handoff step, how to locate the delegated child step, how to surface `child step pending` when that lineage is not yet available, how to read projected context from the child step instead of reinterpreting handoff input, and how to keep adjunct metadata like `capability_tags` secondary. If helper functions are needed, keep them inside the runtime detail boundary so the LiveView can treat delegated evidence as a stable public DTO rather than its own join algorithm.</action>
  <acceptance_criteria>
    - `rg -n 'pending|child_step_pending|capability_tags|sequence' lib/scoria/runtime/run_detail.ex test/scoria/runtime_test.exs test/scoria/runtime_view_test.exs` returns matches.
    - `rg -n 'sort|Enum\\.sort' lib/scoria/runtime/run_detail.ex` returns matches for deterministic delegated ordering.
    - `mix test test/scoria/runtime_test.exs test/scoria/runtime_view_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/runtime_test.exs test/scoria/runtime_view_test.exs</automated>
  </verify>
  <done>The delegated DTO is explicit about precedence, ordering, and pending-state semantics instead of leaving those decisions to each caller.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable handoff truth -> public DTO | a weak projection could force apps/operators back into raw workflow reconstruction and misstate the intended contract |
| adjunct metadata -> primary delegated story | secondary hints could incorrectly appear more authoritative than persisted lineage truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | T | `Scoria.Runtime.RunDetail` | mitigate | keep delegated evidence projection linked only to persisted handoff + child-step truth with explicit precedence rules |
| T-42-02 | I | delegated DTO consumers | mitigate | expose stable lineage/status/projected-context fields so apps and UI do not scrape raw workflow rows |
</threat_model>

<verification>
- `mix test test/scoria/runtime_test.exs test/scoria/runtime_view_test.exs`
</verification>

<success_criteria>
- Public runtime detail exposes a primary delegated evidence collection.
- Delegated lineage remains projection-only over existing durable truth.
- Pending and multi-handoff cases have stable, explicit semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/42-delegated-evidence-adoption-story/42-delegated-evidence-adoption-story-01-SUMMARY.md`
</output>
