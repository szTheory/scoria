---
phase: 42-delegated-evidence-adoption-story
plan: 2
type: execute
wave: 2
depends_on:
  - 42-01
files_modified:
  - lib/scoria_web/live/workflow_live/show.ex
  - lib/scoria_web/components/workflow_tree_component.ex
  - lib/scoria_web/components/workflow_detail_panel_component.ex
  - lib/scoria_web/components/delegated_evidence_component.ex
  - test/scoria_web/live/workflow_live_test.exs
autonomous: true
requirements:
  - EVID-01
  - ADPT-01
nyquist_compliant: true
must_haves:
  truths:
    - "The normal workflow page gains one run-level delegated evidence section instead of forcing operators to infer handoff truth from the tree or step rail alone."
    - "The workflow tree stays topology-first and the right rail stays selected-step detail; delegated inspection belongs primarily to the new run-level section."
    - "Summary-first delegated cards keep projected context, handoff input, and adjunct metadata behind progressive disclosure rather than raw JSON walls."
  artifacts:
    - path: "lib/scoria_web/live/workflow_live/show.ex"
      provides: "Run-level delegated evidence section wiring from curated runtime detail"
    - path: "lib/scoria_web/components/delegated_evidence_component.ex"
      provides: "Low-noise delegated evidence card rendering with disclosure behavior"
    - path: "test/scoria_web/live/workflow_live_test.exs"
      provides: "Workflow-surface regressions for delegated inspectability and empty/pending states"
  key_links:
    - from: "lib/scoria/runtime/run_detail.ex"
      to: "lib/scoria_web/live/workflow_live/show.ex"
      via: "workflow page consumes delegated projection assigns instead of rebuilding lineage from raw workflow rows"
      pattern: "delegated_handoffs|run_detail"
    - from: "lib/scoria_web/live/workflow_live/show.ex"
      to: "lib/scoria_web/components/delegated_evidence_component.ex"
      via: "run-level delegated evidence section renders summary-first cards with progressive disclosure"
      pattern: "Delegated Evidence|projected context|handoff input"
---

<objective>
Make delegated evidence visible on the normal workflow page without widening the product surface.

Purpose: close the operator-inspection gap with one calm run-level section built on the curated delegated runtime DTO.
Output: `/scoria/workflows/:run_id` exposes delegated lineage, bounded projected context, and same-run child execution in one obvious place.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/42-delegated-evidence-adoption-story/42-CONTEXT.md
@.planning/phases/42-delegated-evidence-adoption-story/42-UI-SPEC.md
@.planning/phases/42-delegated-evidence-adoption-story/42-RESEARCH.md
@.planning/phases/42-delegated-evidence-adoption-story/42-01-PLAN.md
@lib/scoria/runtime.ex
@lib/scoria/runtime/run_detail.ex
@lib/scoria_web/live/workflow_live/show.ex
@lib/scoria_web/components/workflow_tree_component.ex
@lib/scoria_web/components/workflow_detail_panel_component.ex
@test/scoria_web/live/workflow_live_test.exs

<interfaces>
From `lib/scoria_web/live/workflow_live/show.ex`:
```elixir
defp load_run(socket, run_id) do
```

From `lib/scoria_web/components/workflow_detail_panel_component.ex`:
```elixir
def workflow_detail_panel(assigns) do
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the run-level delegated evidence section to the workflow page</name>
  <files>lib/scoria_web/live/workflow_live/show.ex, lib/scoria_web/components/delegated_evidence_component.ex, test/scoria_web/live/workflow_live_test.exs</files>
  <read_first>.planning/phases/42-delegated-evidence-adoption-story/42-UI-SPEC.md, .planning/phases/42-delegated-evidence-adoption-story/42-01-PLAN.md, lib/scoria_web/live/workflow_live/show.ex, test/scoria_web/live/workflow_live_test.exs</read_first>
  <behavior>
    - Test 1: runs with delegated handoffs render a `Delegated Evidence` section populated from the curated runtime DTO.
    - Test 2: runs with no delegated handoffs render the empty-state card without altering the rest of the workflow page.
    - Test 3: pending child-step lineage renders explicit helper copy instead of blank fields.
  </behavior>
  <action>Update `WorkflowLive.Show` so `load_run/2` assigns the delegated projection from `RunDetail` and renders a run-level `Delegated Evidence` section between the tree/detail frame and lower notebooks/timeline, matching the UI spec. Implement the section as a dedicated component rather than bloating the LiveView template. The component should render summary-first cards showing root role -> delegated role, delegated kind, parent step -> child step lineage, current status, and a 2-3 row projected-context preview by default. Include a calm empty state when the run has no delegated handoffs.</action>
  <acceptance_criteria>
    - `rg -n 'Delegated Evidence|No Delegated Handoffs Recorded' lib/scoria_web/live/workflow_live/show.ex lib/scoria_web/components/delegated_evidence_component.ex test/scoria_web/live/workflow_live_test.exs` returns matches.
    - `rg -n 'delegated_handoffs' lib/scoria_web/live/workflow_live/show.ex lib/scoria_web/components/delegated_evidence_component.ex` returns matches.
    - `mix test test/scoria_web/live/workflow_live_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/workflow_live_test.exs</automated>
  </verify>
  <done>The workflow page has one obvious delegated inspection anchor without changing the topology-first tree or selected-step rail roles.</done>
</task>

<task type="auto">
  <name>Task 2: Keep delegated inspection compact, progressive, and aligned with existing page responsibilities</name>
  <files>lib/scoria_web/live/workflow_live/show.ex, lib/scoria_web/components/workflow_tree_component.ex, lib/scoria_web/components/workflow_detail_panel_component.ex, lib/scoria_web/components/delegated_evidence_component.ex, test/scoria_web/live/workflow_live_test.exs</files>
  <read_first>.planning/phases/42-delegated-evidence-adoption-story/42-DISCUSSION-LOG.md, .planning/phases/42-delegated-evidence-adoption-story/42-UI-SPEC.md, lib/scoria_web/components/workflow_tree_component.ex, lib/scoria_web/components/workflow_detail_panel_component.ex, test/scoria_web/live/workflow_live_test.exs</read_first>
  <behavior>
    - Test 1: workflow-tree row clicks still drive the right rail only and do not replace the delegated section.
    - Test 2: full `handoff_input`, full projected context, and optional metadata appear behind disclosure instead of as default raw dumps.
    - Test 3: a same-page inspectability link or CTA can move users to the delegated section without creating a new route family.
  </behavior>
  <action>Preserve the tree/detail contract while adding delegated-inspection ergonomics. Keep the tree compact and topology-first, keep the detail rail focused on the selected step, and move full handoff/projected-context detail behind low-noise disclosure controls in the delegated section. If a header CTA or anchor link is added, keep it on the same `/scoria/workflows/:run_id` page. Add LiveView coverage for delegated cards, disclosure behavior, and step-selection coexistence so the UI remains support-truthful as handoff data evolves.</action>
  <acceptance_criteria>
    - `rg -n 'View full context|handoff input|Inspect Delegated Evidence|delegated evidence' lib/scoria_web/live/workflow_live/show.ex lib/scoria_web/components/delegated_evidence_component.ex test/scoria_web/live/workflow_live_test.exs` returns matches.
    - `rg -n 'select_step' test/scoria_web/live/workflow_live_test.exs` returns matches that continue to verify right-rail interaction.
    - `rg -n 'raw JSON|Jason\\.encode' lib/scoria_web/components/delegated_evidence_component.ex` returns no default-summary raw-dump path.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/workflow_live_test.exs</automated>
  </verify>
  <done>The delegated evidence section stays calm and useful instead of turning the workflow page into a second orchestration cockpit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime DTO -> workflow UI | rebuilding lineage ad hoc in LiveView would let support truth drift away from the public runtime contract |
| workflow layout responsibilities | overloading tree rows or the right rail would create noisy or misleading operator behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03 | R | workflow operator surface | mitigate | render delegated evidence from the curated DTO in a dedicated run-level section with explicit empty/pending states |
| T-42-04 | T | component responsibilities | mitigate | keep tree topology-first and detail rail step-focused while using disclosure for deeper delegated detail |
</threat_model>

<verification>
- `mix test test/scoria_web/live/workflow_live_test.exs`
</verification>

<success_criteria>
- `/scoria/workflows/:run_id` exposes delegated evidence clearly enough for operator inspection.
- The page stays on the existing workflow route and responsibility split.
- Summary-first cards and disclosures replace raw reconstruction and JSON-wall reading.
</success_criteria>

<output>
After completion, create `.planning/phases/42-delegated-evidence-adoption-story/42-delegated-evidence-adoption-story-02-SUMMARY.md`
</output>
