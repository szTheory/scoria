---
phase: 25-ci-cd-regression-and-evaluation-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/20260519000000_converge_eval_persistence.exs
  - lib/scoria/eval/eval_spec.ex
  - lib/scoria/eval/eval_run.ex
  - lib/scoria/eval/score.ex
  - lib/scoria/eval.ex
  - test/scoria/eval/eval_run_persistence_test.exs
autonomous: true
requirements:
  - EVAL-04
must_haves:
  truths:
    - "Scoria stores one canonical eval persistence lineage rooted in `ai_eval_datasets` and `ai_eval_dataset_items`."
    - "`EvalSpec` is an explicit immutable contract with typed subject, scorer, threshold, and mode fields."
    - "`EvalRun` stores durable run-header facts and separate per-item evidence rows that Phase 26 can query directly."
  artifacts:
    - path: "priv/repo/migrations/20260519000000_converge_eval_persistence.exs"
      provides: "Schema convergence from the old eval tables to the Phase 24 dataset lineage"
      contains: "alter table(:ai_eval_runs)"
    - path: "lib/scoria/eval/eval_spec.ex"
      provides: "Typed eval contract schema"
      contains: "embeds_many :scorers"
    - path: "lib/scoria/eval/eval_run.ex"
      provides: "Durable run header facts"
      contains: "field :runner_mode"
    - path: "lib/scoria/eval/score.ex"
      provides: "Per-item evidence rows"
      contains: "field :explanation"
  key_links:
    - from: "lib/scoria/eval/eval_run.ex"
      to: "lib/scoria/eval/dataset.ex"
      via: "belongs_to association on canonical dataset rows"
      pattern: "belongs_to :dataset, Scoria.Eval.Dataset"
    - from: "lib/scoria/eval/score.ex"
      to: "lib/scoria/eval/dataset_item.ex"
      via: "per-item evidence tied to dataset items"
      pattern: "belongs_to :dataset_item, Scoria.Eval.DatasetItem"
    - from: "lib/scoria/eval.ex"
      to: "lib/scoria/eval/eval_run.ex"
      via: "public persistence API for run headers and scores"
      pattern: "def create_eval_run"
---

<objective>
Converge eval persistence onto the Phase 24 dataset lineage and define explicit typed truth models for `EvalSpec`, `EvalRun`, and per-item evidence.

Purpose: Phase 25 cannot add deterministic replay or live judge execution on top of split-brain eval storage. This plan establishes the single durable truth boundary required by D-10 through D-26 and gives later plans a stable persistence API.
Output: One convergence migration, typed eval schemas, and context functions for creating run headers plus evidence rows.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-ci-cd-regression-and-evaluation-framework/25-CONTEXT.md
@.planning/phases/25-ci-cd-regression-and-evaluation-framework/25-RESEARCH.md
@.planning/phases/25-ci-cd-regression-and-evaluation-framework/25-PATTERNS.md
@.planning/phases/24-trace-to-dataset-curation-via-liveview/24-SUMMARY.md
@lib/scoria/eval.ex
@lib/scoria/eval/dataset.ex
@lib/scoria/eval/dataset_item.ex
@lib/scoria/eval/eval_spec.ex
@lib/scoria/eval/eval_run.ex
@lib/scoria/eval/score.ex
@priv/repo/migrations/20260510174619_create_eval_tables.exs
@priv/repo/migrations/20260518220533_create_ai_eval_datasets.exs

<interfaces>
From lib/scoria/eval.ex:
```elixir
def create_eval_spec(attrs \\ %{})
def update_eval_spec(%EvalSpec{} = old_spec, attrs)
```

From lib/scoria/eval/eval_run.ex:
```elixir
schema "ai_eval_runs" do
  field :status, :string, default: "pending"
  field :duration_ms, :integer
  belongs_to :dataset, Scoria.Eval.Dataset
  belongs_to :eval_spec, Scoria.Eval.EvalSpec
  has_many :scores, Scoria.Eval.Score
end
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Converge the old eval tables onto the Phase 24 dataset lineage</name>
  <files>priv/repo/migrations/20260519000000_converge_eval_persistence.exs, test/scoria/eval/eval_run_persistence_test.exs</files>
  <read_first>
    priv/repo/migrations/20260510174619_create_eval_tables.exs
    priv/repo/migrations/20260518220533_create_ai_eval_datasets.exs
    lib/scoria/eval/dataset.ex
    lib/scoria/eval/dataset_item.ex
    test/scoria/eval_test.exs
  </read_first>
  <acceptance_criteria>
    - `priv/repo/migrations/20260519000000_converge_eval_persistence.exs` rewires `ai_eval_runs.dataset_id` to reference `ai_eval_datasets` and rewires `ai_scores.dataset_item_id` to reference `ai_eval_dataset_items`.
    - The migration either drops `ai_datasets` / `ai_dataset_items` or renames/migrates them so `rg -n "references\\(:ai_datasets|references\\(:ai_dataset_items" priv/repo/migrations lib/scoria/eval` returns no matches after completion.
    - `mix test test/scoria/eval/eval_run_persistence_test.exs` proves old eval rows can be inserted through the new canonical associations.
  </acceptance_criteria>
  <action>
    Write a convergence migration per D-24, D-25, and D-26 that makes `ai_eval_datasets` and `ai_eval_dataset_items` the only dataset lineage used by eval runs and scores.

    Use concrete migration steps, not a placeholder:
    - Move any required columns or ids from the old `ai_datasets` / `ai_dataset_items` lineage into the new tables only if needed for referential continuity.
    - Change `ai_eval_runs.dataset_id` to reference `ai_eval_datasets`.
    - Change `ai_scores.dataset_item_id` to reference `ai_eval_dataset_items`.
    - Preserve `ai_eval_specs`, `ai_eval_runs`, and `ai_scores` table names so downstream code and future phases keep stable nouns.
    - Remove or retire the old dataset tables in the same migration so no second truth boundary remains.

    Add the regression test file `test/scoria/eval/eval_run_persistence_test.exs` to match the Phase 25 pattern map. It must insert a sealed dataset, a typed eval spec, an eval run, and evidence rows through the canonical tables and prove the associations resolve from `Scoria.Eval`.
  </action>
  <verify>
    <automated>mix test test/scoria/eval/eval_run_persistence_test.exs</automated>
  </verify>
  <done>Canonical eval persistence is rooted in Phase 24 dataset tables only, and tests prove eval runs plus score rows attach to that lineage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the untyped eval spec contract with explicit typed embeds and immutable version rules</name>
  <files>lib/scoria/eval/eval_spec.ex, lib/scoria/eval.ex, test/scoria/eval/eval_run_persistence_test.exs</files>
  <read_first>
    lib/scoria/eval/eval_spec.ex
    lib/scoria/eval.ex
    lib/scoria/prompt_registry/prompt_template.ex
    .planning/phases/25-ci-cd-regression-and-evaluation-framework/25-CONTEXT.md
    .planning/phases/25-ci-cd-regression-and-evaluation-framework/25-PATTERNS.md
  </read_first>
  <acceptance_criteria>
    - `lib/scoria/eval/eval_spec.ex` contains `embeds_one :subject`, `embeds_many :scorers`, and `embeds_one :threshold_policy`.
    - `lib/scoria/eval/eval_spec.ex` contains `field :eval_mode, Ecto.Enum, values: [:offline_replay, :live_judge]`.
    - `rg -n "field :rubric, :map" lib/scoria/eval/eval_spec.ex` returns no matches.
    - `mix test test/scoria/eval/eval_run_persistence_test.exs` proves `create_eval_spec/1` persists prompt version identity, sealed dataset identity, scorer config, and threshold policy without using mutable aliases.
  </acceptance_criteria>
  <action>
    Evolve `Scoria.Eval.EvalSpec` per D-10 through D-14. Replace the single `rubric` map with explicit embedded schemas:
    - `subject` embed with `subject_kind`, `prompt_template_id`, `prompt_entity_id`, and `prompt_version`.
    - `scorers` embed list with `metric_key`, `scorer_kind`, `judge_prompt_template_id`, `judge_prompt_version`, `judge_provider`, `judge_model`, and `weight`.
    - `threshold_policy` embed with `pass_rate_gte`, `mean_score_gte`, and `max_latency_ms`.
    - top-level `dataset_id`, `dataset_version`, and `eval_mode`.

    Update `Scoria.Eval.create_eval_spec/1` and `update_eval_spec/2` so immutable version bumps keep `entity_id`, mark old rows `is_current: false`, and reject any attempt to treat mutable aliases like “current dataset” or “default judge model” as durable truth.
  </action>
  <verify>
    <automated>mix test test/scoria/eval/eval_run_persistence_test.exs</automated>
  </verify>
  <done>`EvalSpec` is an explicit typed contract row with immutable versioning and no opaque rubric blob.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand eval runs and score rows into durable header facts plus evidence APIs</name>
  <files>lib/scoria/eval/eval_run.ex, lib/scoria/eval/score.ex, lib/scoria/eval.ex, test/scoria/eval/eval_run_persistence_test.exs</files>
  <read_first>
    lib/scoria/eval/eval_run.ex
    lib/scoria/eval/score.ex
    lib/scoria/eval.ex
    lib/scoria/knowledge/retrieval_run.ex
    lib/scoria/knowledge/grounding_score.ex
    .planning/phases/25-ci-cd-regression-and-evaluation-framework/25-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `lib/scoria/eval/eval_run.ex` contains fields for `runner_mode`, `prompt_template_id`, `prompt_version`, `dataset_version`, `eval_spec_version`, `provider`, `model`, `judge_provider`, `judge_model`, `fixture_key`, `fixture_sha256`, `total_items`, `passed_items`, `failed_items`, `avg_latency_ms`, `total_cost_usd`, `threshold_verdict`, and `baseline_eval_run_id`.
    - `lib/scoria/eval/score.ex` contains fields for `scorer_kind`, `status`, `explanation`, `judge_model`, `rubric_version`, and `evidence_refs`.
    - `rg -n "field :reasoning, :string" lib/scoria/eval/score.ex` returns no matches.
    - `lib/scoria/eval.ex` exports concrete run persistence functions named `create_eval_run/1`, `complete_eval_run/2`, and `record_eval_scores/2`.
    - `mix test test/scoria/eval/eval_run_persistence_test.exs` proves aggregate header counts and per-item evidence rows are both persisted.
  </acceptance_criteria>
  <action>
    Expand `EvalRun` and `Score` per D-19 through D-23.

    In `Scoria.Eval.EvalRun`, add explicit run-header fields:
    - `runner_mode` as `Ecto.Enum` with `[:offline_replay, :live_judge, :refresh_capture]`
    - prompt identity: `prompt_template_id`, `prompt_version`
    - dataset/spec snapshot fields: `dataset_version`, `eval_spec_version`
    - execution environment: `provider`, `model`, `judge_provider`, `judge_model`
    - replay provenance: `fixture_key`, `fixture_path`, `fixture_sha256`
    - aggregate facts: `total_items`, `passed_items`, `failed_items`, `avg_latency_ms`, `total_cost_usd`, `threshold_verdict`
    - release comparison anchor: `baseline_eval_run_id`

    In `Scoria.Eval.Score`, add explicit per-item evidence fields:
    - `scorer_kind`, `status`, `explanation`, `judge_model`, `rubric_version`, `evidence_refs`, `metadata`
    - keep `dataset_item_id` and `eval_run_id`
    - store concise explanations only; do not persist raw chain-of-thought text per D-23

    In `Scoria.Eval`, add context functions that insert a run row first, then bulk-persist score rows through `Ecto.Multi`, and finally update the completed aggregate counts in one transaction.
  </action>
  <verify>
    <automated>mix test test/scoria/eval/eval_run_persistence_test.exs</automated>
  </verify>
  <done>The eval subsystem has one canonical persistence boundary with explicit spec, run, and evidence truth models usable by later execution lanes and Phase 26 comparisons.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mix/test code -> eval persistence | Untrusted test or task inputs become durable run facts and evidence rows. |
| Old eval tables -> converged canonical tables | Historical data moves across a schema boundary that can silently corrupt references if migration steps are wrong. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | convergence migration | mitigate | Rewire foreign keys in one migration, then add regression tests proving `ai_eval_runs` and `ai_scores` only attach to `ai_eval_datasets` lineage. |
| T-25-02 | Repudiation | eval run persistence | mitigate | Persist immutable run header facts including prompt, dataset, spec, runner mode, and fixture provenance on the run row. |
| T-25-03 | Information Disclosure | score evidence | mitigate | Replace freeform `reasoning` blobs with bounded `explanation` plus `evidence_refs`; do not store chain-of-thought content. |
</threat_model>

<verification>
`mix test test/scoria/eval/eval_run_persistence_test.exs`
</verification>

<success_criteria>
Phase 25 has a single canonical eval storage model rooted in sealed datasets, typed specs, explicit run headers, and separate evidence rows.
</success_criteria>

<output>
After completion, create `.planning/phases/25-ci-cd-regression-and-evaluation-framework/25-01-SUMMARY.md`
</output>
