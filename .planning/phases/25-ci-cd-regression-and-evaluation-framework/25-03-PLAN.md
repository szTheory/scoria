---
phase: 25-ci-cd-regression-and-evaluation-framework
plan: 03
type: execute
wave: 3
depends_on:
  - 25-01
  - 25-02
files_modified:
  - lib/scoria/eval/judge_runner.ex
  - lib/mix/tasks/scoria.eval.ex
  - test/scoria/eval/judge_runner_test.exs
  - test/mix/tasks/scoria.eval_test.exs
autonomous: true
requirements:
  - EVAL-03
  - EVAL-04
must_haves:
  truths:
    - "`mix scoria.eval` remains the explicit live/judge lane and is clearly separate from offline `mix test`."
    - "Live judge runs persist the same explicit run-header facts and per-item evidence shape as offline replay runs."
    - "Judge verdicts use structured output fields rather than opaque blobs."
  artifacts:
    - path: "lib/scoria/eval/judge_runner.ex"
      provides: "Live judge execution service"
      contains: "def run_live"
    - path: "lib/mix/tasks/scoria.eval.ex"
      provides: "Explicit live evaluation command"
      contains: "Missing --dataset option"
  key_links:
    - from: "lib/mix/tasks/scoria.eval.ex"
      to: "lib/scoria/eval/judge_runner.ex"
      via: "Mix task delegates to the live judge service"
      pattern: "Scoria.Eval.JudgeRunner"
    - from: "lib/scoria/eval/judge_runner.ex"
      to: "lib/scoria/eval.ex"
      via: "persist live run headers and score rows"
      pattern: "Scoria.Eval.create_eval_run"
---

<objective>
Turn `mix scoria.eval` into the explicit live judge lane with durable structured verdict persistence that matches the canonical Phase 25 truth model.

Purpose: This plan implements D-15 through D-18 and EVAL-03 without contaminating the default CI lane. It also proves that live judge runs write the same run/evidence facts Phase 26 will rely on.
Output: `Scoria.Eval.JudgeRunner`, an upgraded `mix scoria.eval`, and tests for live command behavior plus structured verdict persistence.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-ci-cd-regression-and-evaluation-framework/25-CONTEXT.md
@.planning/phases/25-ci-cd-regression-and-evaluation-framework/25-RESEARCH.md
@mix.exs
@lib/mix/tasks/scoria.eval.ex
@lib/scoria/eval.ex
@lib/scoria/eval/eval_spec.ex
@lib/scoria/eval/eval_run.ex
@lib/scoria/eval/score.ex
@test/mix/tasks/scoria.eval_test.exs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Use the live judge transport from Plan 25-02 and create the runner with explicit structured verdict fields</name>
  <files>lib/scoria/eval/judge_runner.ex, test/scoria/eval/judge_runner_test.exs</files>
  <read_first>
    lib/scoria/eval.ex
    lib/scoria/eval/eval_spec.ex
    lib/scoria/eval/eval_run.ex
    lib/scoria/eval/score.ex
    .planning/phases/25-ci-cd-regression-and-evaluation-framework/25-CONTEXT.md
    .planning/phases/25-ci-cd-regression-and-evaluation-framework/25-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `lib/scoria/eval/judge_runner.ex` exports `run_live/1`.
    - `lib/scoria/eval/judge_runner.ex` uses the `req_llm` transport already installed by Plan 25-02 for structured judge calls rather than a hand-rolled JSON transport.
    - `run_live/1` persists `runner_mode: :live_judge` runs through the Phase 25 context API from Plan 25-01.
    - Judge result mapping writes `scorer_kind`, `score`, `status`, `explanation`, `judge_model`, `rubric_version`, and `evidence_refs` into `ai_scores`.
    - `mix test test/scoria/eval/judge_runner_test.exs` passes.
  </acceptance_criteria>
  <action>
    Implement `Scoria.Eval.JudgeRunner` for the explicit live lane described by D-15 and EVAL-03.

    Consume the `req_llm` dependency that Plan 25-02 already added to `mix.exs` as the concrete structured-output transport required by the resolved research decision for EVAL-03. Do not modify `mix.exs` in this plan.

    The service must:
    - load a sealed dataset and immutable eval spec
    - execute the subject prompt version against the runtime/model requested for the run
    - evaluate each item with a judge scorer using `req_llm`-backed structured verdict fields that map directly into the explicit `Score` schema from Plan 25-01
    - persist the run header with prompt, dataset, spec, provider/model, judge provider/model, aggregate counts, latency, and threshold verdict
    - avoid opaque result blobs; project only the explicit fields required by D-20 through D-23
  </action>
  <verify>
    <automated>mix test test/scoria/eval/judge_runner_test.exs</automated>
  </verify>
  <done>Live judge execution exists as a dedicated service that produces the same durable evidence shape as offline runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upgrade `mix scoria.eval` into the explicit online command surface</name>
  <files>lib/mix/tasks/scoria.eval.ex, test/mix/tasks/scoria.eval_test.exs</files>
  <read_first>
    lib/mix/tasks/scoria.eval.ex
    lib/scoria/eval/judge_runner.ex
    .planning/phases/25-ci-cd-regression-and-evaluation-framework/25-CONTEXT.md
    .planning/phases/25-ci-cd-regression-and-evaluation-framework/25-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - The task module docs state that `mix scoria.eval` is the live/judge lane and that offline regressions stay under `mix test`.
    - The task parses exact switches `--dataset`, `--eval-spec`, `--prompt`, `--provider`, `--model`, and optional `--baseline-run`.
    - The task output includes the dataset id and the chosen provider/model before starting execution.
    - `mix test test/mix/tasks/scoria.eval_test.exs` passes, including a failure case for missing required options.
  </acceptance_criteria>
  <action>
    Replace the current stub `Mix.Tasks.Scoria.Eval` with an explicit online command surface per D-15 through D-18.

    Keep the task name `mix scoria.eval`, but make its docs and runtime behavior unambiguous:
    - state that the task performs live or judge-backed evaluation
    - parse `--dataset`, `--eval-spec`, `--prompt`, `--provider`, `--model`, and optional `--baseline-run`
    - delegate execution to `Scoria.Eval.JudgeRunner.run_live/1`
    - print a concise run header summary that includes dataset id, eval spec id, prompt id, provider/model, and baseline anchor if present
    - do not add any path that makes this task part of ordinary CI or `mix test`
  </action>
  <verify>
    <automated>mix test test/mix/tasks/scoria.eval_test.exs</automated>
  </verify>
  <done>`mix scoria.eval` is a clearly online, explicit live judge command and no longer a placeholder stub.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mix task arguments -> live judge runner | User-supplied CLI inputs select prompt, dataset, provider, and baseline anchors for an online run. |
| Judge model output -> durable score rows | External model verdicts become durable release evidence that later phases will trust. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07 | Spoofing | CLI run identity | mitigate | Require explicit dataset, eval spec, prompt, provider, and model flags; persist each selected identity on the run row. |
| T-25-08 | Tampering | judge verdict persistence | mitigate | Map structured judge output directly into explicit score fields and reject opaque payload blobs as primary truth. |
| T-25-09 | Repudiation | release evidence lineage | mitigate | Persist baseline anchor, provider/model, judge model, and threshold verdict on each live run so Phase 26 comparisons are auditable. |
</threat_model>

<verification>
`mix test test/scoria/eval/judge_runner_test.exs test/mix/tasks/scoria.eval_test.exs`
</verification>

<success_criteria>
Scoria has a clearly separated live judge execution lane that persists durable structured evidence without altering the deterministic default regression contract.
</success_criteria>

<output>
After completion, create `.planning/phases/25-ci-cd-regression-and-evaluation-framework/25-03-SUMMARY.md`
</output>
