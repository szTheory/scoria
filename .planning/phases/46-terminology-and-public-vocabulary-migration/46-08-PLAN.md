---
phase: 46-terminology-and-public-vocabulary-migration
plan: 08
type: execute
wave: 5
depends_on: ["46-07"]
files_modified:
  - README.md
  - CHANGELOG.md
  - test/scoria/changelog_contract_test.exs
autonomous: true
requirements: [TERM-04, TERM-03, TERM-02]
must_haves:
  truths:
    - "Per D-04, CHANGELOG and README install notes explain pre-1.0 terminology changes, compatibility aliases, unchanged Hex 0.1.2 status, unchanged RAG/citation evidence, and no database migration."
    - "Historical changelog sections retain old terminology as history while the current `[Unreleased]` section explains final vocabulary."
    - "Per TERM-03, no schema migration, no trace-reference storage field, and no global `evidence_refs` rename is introduced."
  artifacts:
    - path: "README.md"
      provides: "D-04 install-section terminology upgrade note"
      contains: "Pre-1.0 terminology migration"
    - path: "CHANGELOG.md"
      provides: "D-04 Unreleased terminology migration note before 0.1.2"
      contains: "## [Unreleased]"
    - path: "test/scoria/changelog_contract_test.exs"
      provides: "CHANGELOG and README upgrade-note contract"
  key_links:
    - from: "README.md"
      to: "CHANGELOG.md"
      via: "install-adjacent upgrade note and Unreleased migration note"
      pattern: "Pre-1\\.0 terminology migration"
    - from: "test/scoria/changelog_contract_test.exs"
      to: "CHANGELOG.md"
      via: "section order and compatibility-status assertions"
      pattern: "Unreleased"
---

<objective>
Finish the D-04 public upgrade-note contract after the docs vocabulary migration is in place.

Purpose: Give adopters explicit pre-1.0 terminology migration guidance without rewriting historical release entries or implying a database migration.
Output: README install-section note, single ordered CHANGELOG `[Unreleased]` entry, changelog contract updates, and final focused phase verification.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-terminology-and-public-vocabulary-migration/46-CONTEXT.md
@.planning/phases/46-terminology-and-public-vocabulary-migration/46-RESEARCH.md
@.planning/phases/46-terminology-and-public-vocabulary-migration/46-PATTERNS.md
@.planning/phases/46-terminology-and-public-vocabulary-migration/46-VALIDATION.md
@README.md
@CHANGELOG.md
@test/scoria/changelog_contract_test.exs
@test/scoria/terminology_contract_test.exs
@test/scoria/package_surface_test.exs
@test/scoria/hex_consumer_contract_test.exs
</context>

## Artifacts This Phase Produces

- D-04 `CHANGELOG.md` `## [Unreleased]` terminology migration entry before `## [0.1.2]`.
- D-04 README install-section upgrade note under Install and before `Upgrading or re-running install`.
- Updated `test/scoria/changelog_contract_test.exs` for section order, single Unreleased section, compatibility aliases, Hex status, unchanged evidence storage, and no database migration.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add D-04 CHANGELOG and README install upgrade notes, then run focused phase verification</name>
  <files>README.md, CHANGELOG.md, test/scoria/changelog_contract_test.exs</files>
  <read_first>
  - `.planning/phases/46-terminology-and-public-vocabulary-migration/46-CONTEXT.md`
  - `.planning/phases/46-terminology-and-public-vocabulary-migration/46-RESEARCH.md`
  - `README.md`
  - `CHANGELOG.md`
  - `test/scoria/changelog_contract_test.exs`
  - `test/scoria/terminology_contract_test.exs`
  - `test/scoria/package_surface_test.exs`
  - `test/scoria/hex_consumer_contract_test.exs`
  </read_first>
  <behavior>
  - CHANGELOG has one `## [Unreleased]` section before `## [0.1.2]`.
  - CHANGELOG `## [Unreleased]` contains `### Changed` with a `Pre-1.0 terminology migration` note and old-to-new status table.
  - CHANGELOG and README state that Hex remains `0.1.2` until the release phase, the notes describe unreleased main-branch changes, RAG/citation evidence and `evidence_refs` are unchanged, no database migration is required, and legacy aliases remain accepted.
  - README includes the upgrade note under Install before `Upgrading or re-running install`.
  - Historical changelog sections are not rewritten for terminology except where a line is current upgrade guidance.
  </behavior>
  <action>Move or consolidate the existing empty CHANGELOG unreleased marker so there is exactly one top-level `## [Unreleased]` section before `## [0.1.2]`. Add a `### Changed` entry titled `Pre-1.0 terminology migration` with a status table covering reviewer, trace, verification suite, scoped context, semantic cache, optional knowledge base, compatibility aliases, unchanged RAG/citation evidence, unchanged evidence reference storage, no database migration, and Hex `0.1.2` status until Phase 50. Add the README Install-section upgrade note before `Upgrading or re-running install` with the same constraints. Update `changelog_contract_test.exs` to assert section order, single Unreleased section, table/status content, README note placement, and historical section preservation.</action>
  <acceptance_criteria>
  - Source assertion: `CHANGELOG.md` contains exactly one `## [Unreleased]`.
  - Source assertion: the `## [Unreleased]` section appears before `## [0.1.2]`.
  - Source assertion: `CHANGELOG.md` contains `### Changed` and `Pre-1.0 terminology migration`.
  - Source assertion: `README.md` contains `Pre-1.0 terminology migration` after its Install heading and before `Upgrading or re-running install`.
    - Source assertion: `CHANGELOG.md` current Unreleased section states historical sections can retain old terminology as history, or the contract documents this by not scanning historical sections as current copy.
    - Behavior assertion: `test/scoria/changelog_contract_test.exs` fails if the Unreleased section is duplicated, misplaced, or missing D-04 compatibility/no-migration/status language.
    - CLI assertion: `! rg -n 'trace_refs' lib config priv mix.exs docs README.md CHANGELOG.md` exits successfully.
    - CLI assertion: `MIX_ENV=dev mix scoria.release_preview` exits successfully as part of final Phase 46 verification.
    - Automated test assertion: the focused validation command from `46-VALIDATION.md` passes, including `test/scoria/hex_consumer_contract_test.exs`.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; ! rg -n "trace_refs" lib config priv mix.exs docs README.md CHANGELOG.md; MIX_ENV=dev mix scoria.release_preview; MIX_ENV=test mix test --warnings-as-errors test/scoria/glossary_contract_test.exs test/scoria/adoption_surface_test.exs test/scoria/package_surface_test.exs test/scoria/hex_consumer_contract_test.exs test/scoria/changelog_contract_test.exs test/scoria/terminology_contract_test.exs'</automated>
  </verify>
  <done>CHANGELOG and README explain the pre-1.0 terminology migration and compatibility posture exactly where D-04 requires, and focused Phase 46 verification passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upgrade notes -> migration decisions | CHANGELOG and README install notes can cause adopters to run or skip migrations and code changes. |
| terminology guard -> repository surfaces | Contract tests inspect docs/config/source surfaces to prevent vocabulary drift and storage rename regressions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-08-01 | Tampering | schema/storage terminology | mitigate | Keep no-schema guard active for trace-reference storage and evidence reference preservation; do not edit ORM/schema/migration files. |
| T-46-08-02 | Repudiation | CHANGELOG upgrade notes | mitigate | Add contract tests for a single ordered Unreleased section and explicit compatibility/no-migration/Hex-status language. |
| T-46-08-03 | Information Disclosure | trace wording | mitigate | Avoid OpenInference-compatible trace substrate/export claims; keep trace described as reviewer vocabulary and evidence as RAG/citation/support proof. |
| T-46-08-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned for this phase. |
</threat_model>

<verification>
Run `bash -lc 'set -euo pipefail; ! rg -n "trace_refs" lib config priv mix.exs docs README.md CHANGELOG.md; MIX_ENV=dev mix scoria.release_preview; MIX_ENV=test mix test --warnings-as-errors test/scoria/glossary_contract_test.exs test/scoria/adoption_surface_test.exs test/scoria/package_surface_test.exs test/scoria/hex_consumer_contract_test.exs test/scoria/changelog_contract_test.exs test/scoria/terminology_contract_test.exs'`.
</verification>

<success_criteria>
Plan 46-08 succeeds when README/CHANGELOG explain the pre-1.0 terminology migration and compatibility status, historical changelog sections remain historical, evidence storage/citation semantics remain intact, and focused Phase 46 verification passes.
</success_criteria>

<output>
Create `.planning/phases/46-terminology-and-public-vocabulary-migration/46-08-SUMMARY.md` when done.
</output>
