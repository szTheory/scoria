---
phase: 46-terminology-and-public-vocabulary-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/verification_suites.ex
  - lib/scoria/verification_lanes.ex
  - lib/scoria/observe/reviewer_broadcast.ex
  - lib/scoria/observe/operator_broadcast.ex
  - test/scoria/verification_suites_test.exs
  - test/scoria/verification_lanes_test.exs
  - test/scoria/observe/reviewer_broadcast_test.exs
  - test/scoria/observe/operator_broadcast_test.exs
autonomous: true
requirements: [TERM-02, TERM-04]
must_haves:
  truths:
    - "Public proof-command vocabulary exposes `Scoria.VerificationSuites` while `Scoria.VerificationLanes` remains accepted during the 0.1.x line per D-01 and D-05."
    - "Reviewer live-event broadcast vocabulary exposes `Scoria.Observe.ReviewerBroadcast` while `Scoria.Observe.OperatorBroadcast` remains accepted during the 0.1.x line per D-01 and D-05."
    - "Broadcast behavior still drops missing tenant IDs and never falls back to a global topic."
  artifacts:
    - path: "lib/scoria/verification_suites.ex"
      provides: "Final-vocabulary verification suite SSOT"
      exports: ["all/0", "ids/0", "fetch!/1", "command/1", "ci_command/1", "env/1", "prerequisites/1", "exclusions/1", "closeout_order/0", "closeout_chain/0", "boundary_sentence/1"]
    - path: "lib/scoria/observe/reviewer_broadcast.ex"
      provides: "Final-vocabulary reviewer broadcast module"
      exports: ["tenant_topic/1", "broadcast/2", "span_stopped/1", "span_delta/1", "hitl_request/2", "approval_decided/3", "reset_trace_seen!/0"]
    - path: "lib/scoria/verification_lanes.ex"
      provides: "0.1.x compatibility wrapper"
    - path: "lib/scoria/observe/operator_broadcast.ex"
      provides: "0.1.x compatibility wrapper"
  key_links:
    - from: "lib/scoria/verification_lanes.ex"
      to: "lib/scoria/verification_suites.ex"
      via: "defdelegate wrapper functions"
      pattern: "Scoria.VerificationSuites"
    - from: "lib/scoria/observe/operator_broadcast.ex"
      to: "lib/scoria/observe/reviewer_broadcast.ex"
      via: "defdelegate wrapper functions"
      pattern: "Scoria.Observe.ReviewerBroadcast"
---

<objective>
Create the first final-vocabulary public compatibility surfaces required by D-01 before any docs recommend the new names.

Purpose: downstream docs and examples can teach `VerificationSuites` and `ReviewerBroadcast` without breaking current 0.1.x callers.
Output: final modules, soft compatibility wrappers, and focused tests proving behavior parity.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-terminology-and-public-vocabulary-migration/46-CONTEXT.md
@.planning/phases/46-terminology-and-public-vocabulary-migration/46-RESEARCH.md
@.planning/phases/46-terminology-and-public-vocabulary-migration/46-PATTERNS.md
@.planning/phases/46-terminology-and-public-vocabulary-migration/46-VALIDATION.md
@lib/scoria/verification_lanes.ex
@lib/scoria/observe/operator_broadcast.ex
@test/scoria/verification_lanes_test.exs
@test/scoria/observe/operator_broadcast_test.exs
</context>

## Artifacts This Phase Produces

- `Scoria.VerificationSuites` in `lib/scoria/verification_suites.ex`
- `Scoria.VerificationLanes` compatibility wrapper delegating to `Scoria.VerificationSuites`
- `Scoria.Observe.ReviewerBroadcast` in `lib/scoria/observe/reviewer_broadcast.ex`
- `Scoria.Observe.OperatorBroadcast` compatibility wrapper delegating to `Scoria.Observe.ReviewerBroadcast`
- `test/scoria/verification_suites_test.exs`
- `test/scoria/observe/reviewer_broadcast_test.exs`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create VerificationSuites and keep VerificationLanes as a wrapper</name>
  <files>lib/scoria/verification_suites.ex, lib/scoria/verification_lanes.ex, test/scoria/verification_suites_test.exs, test/scoria/verification_lanes_test.exs</files>
  <read_first>
  - `.planning/phases/46-terminology-and-public-vocabulary-migration/46-CONTEXT.md`
  - `.planning/phases/46-terminology-and-public-vocabulary-migration/46-PATTERNS.md`
  - `lib/scoria/verification_lanes.ex`
  - `test/scoria/verification_lanes_test.exs`
  </read_first>
  <behavior>
  - `Scoria.VerificationSuites.ids/0` returns the same ordered atom list currently returned by `Scoria.VerificationLanes.ids/0`.
  - `Scoria.VerificationSuites.closeout_order/0` remains `[:release_preview, :adoption, :runtime_to_handoff]`.
  - `Scoria.VerificationLanes.command(:adoption)` delegates to `Scoria.VerificationSuites.command(:adoption)`.
  </behavior>
  <action>Per D-01 and D-05, move the canonical verification command data and functions into `Scoria.VerificationSuites`; update module documentation to say "verification suite" and "suite" for proof-command vocabulary. Convert `Scoria.VerificationLanes` into a 0.1.x compatibility wrapper with soft documentation or `@moduledoc false`; delegate the full public function set instead of duplicating command data. Update tests so new coverage leads with `VerificationSuites`, while existing compatibility assertions prove `VerificationLanes` still works and CI closeout order remains byte-stable.</action>
  <acceptance_criteria>
  - Source assertion: `lib/scoria/verification_suites.ex` defines `defmodule Scoria.VerificationSuites`.
  - Source assertion: `lib/scoria/verification_lanes.ex` contains `Scoria.VerificationSuites` and does not own an independent `@lanes` list.
  - Behavior assertion: `Scoria.VerificationSuites.command(:adoption) == "mix test.adoption"`.
  - Behavior assertion: `Scoria.VerificationLanes.closeout_chain() == Scoria.VerificationSuites.closeout_chain()`.
  - Source assertion: final module docs use `verification suite` for proof commands; compatibility wrapper docs, if visible, mark the old name as legacy for 0.1.x.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/scoria/verification_suites_test.exs test/scoria/verification_lanes_test.exs</automated>
  </verify>
  <done>`VerificationSuites` is the canonical public proof-command module, `VerificationLanes` remains compatible, and focused verification suite tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ReviewerBroadcast and keep OperatorBroadcast as a wrapper</name>
  <files>lib/scoria/observe/reviewer_broadcast.ex, lib/scoria/observe/operator_broadcast.ex, test/scoria/observe/reviewer_broadcast_test.exs, test/scoria/observe/operator_broadcast_test.exs</files>
  <read_first>
  - `.planning/phases/46-terminology-and-public-vocabulary-migration/46-CONTEXT.md`
  - `.planning/phases/46-terminology-and-public-vocabulary-migration/46-PATTERNS.md`
  - `.planning/milestones/v3.4-phases/44-dashboard-auth-seam/44-01-SUMMARY.md`
  - `lib/scoria/observe/operator_broadcast.ex`
  - `test/scoria/observe/operator_broadcast_test.exs`
  </read_first>
  <behavior>
  - `ReviewerBroadcast.tenant_topic("tenant-a")` returns the same tenant-scoped topic shape as the old module.
  - `ReviewerBroadcast.span_stopped/1` emits `:trace_opened` and `:trace_span` for the first span in a trace.
  - `OperatorBroadcast.span_delta/1` delegates to `ReviewerBroadcast.span_delta/1` and preserves missing-tenant drop behavior.
  </behavior>
  <action>Per D-01 and D-05, move the broadcast implementation into `Scoria.Observe.ReviewerBroadcast`; keep tenant topic prefix, ETS first-span tracking, missing-tenant drop behavior, and PubSub message tuple shapes unchanged. Convert `Scoria.Observe.OperatorBroadcast` into a 0.1.x compatibility wrapper that delegates `tenant_topic/1`, `broadcast/2`, `span_stopped/1`, `span_delta/1`, `hitl_request/2`, `approval_decided/3`, and `reset_trace_seen!/0`. Update debug log module names to reviewer vocabulary in the new module while keeping wrapper behavior quiet.</action>
  <acceptance_criteria>
  - Source assertion: `lib/scoria/observe/reviewer_broadcast.ex` defines `defmodule Scoria.Observe.ReviewerBroadcast`.
  - Source assertion: `lib/scoria/observe/operator_broadcast.ex` references `Scoria.Observe.ReviewerBroadcast` and delegates every public function listed in the action.
  - Behavior assertion: missing `tenant_id` still returns `:dropped` for span stop, span delta, HITL request, and approval decision.
  - Behavior assertion: PubSub messages remain `{:trace_opened, header}`, `{:trace_span, trace_id, span_view}`, `{:trace_delta, delta}`, `{:hitl_request, projection}`, and `{:approval_decided, approval_id, status}`.
  - Security assertion: no global PubSub topic fallback is introduced.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/scoria/observe/reviewer_broadcast_test.exs test/scoria/observe/operator_broadcast_test.exs</automated>
  </verify>
  <done>`ReviewerBroadcast` is canonical, `OperatorBroadcast` remains compatible, and tenant-scoped broadcast tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime telemetry -> PubSub | Telemetry metadata crosses into reviewer LiveView broadcasts. |
| Public module name -> compatibility wrapper | External caller code may still use old 0.1.x module names. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01-I | Information Disclosure | `Scoria.Observe.ReviewerBroadcast.span_stopped/1` | mitigate | Preserve non-empty tenant guard and missing-tenant `:dropped` result before any PubSub broadcast. |
| T-46-01-D | Denial of Service | `Scoria.VerificationSuites.closeout_order/0` | mitigate | Preserve the exact closeout order and command strings used by CI policy tests. |
| T-46-01-R | Repudiation | compatibility wrappers | mitigate | Tests prove old and new module calls produce identical outputs, so upgrade notes can truthfully claim aliases remain accepted. |
| T-46-01-SC | Tampering | package-manager installs | accept | This plan adds no npm, pip, or cargo package installs. |
</threat_model>

<verification>
Run both focused commands in the task verifications. If the executor changes shared compile behavior, also run `MIX_ENV=test mix compile --warnings-as-errors`.
</verification>

<success_criteria>
- `Scoria.VerificationSuites` and `Scoria.Observe.ReviewerBroadcast` are the canonical public names.
- Old modules remain accepted as compatibility aliases for the 0.1.x line.
- No command order, CI required-check name, tenant topic behavior, or PubSub message shape changes.
</success_criteria>

<output>
Create `.planning/phases/46-terminology-and-public-vocabulary-migration/46-01-SUMMARY.md` when done.
</output>
