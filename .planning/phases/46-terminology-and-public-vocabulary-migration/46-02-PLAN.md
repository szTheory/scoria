---
phase: 46-terminology-and-public-vocabulary-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria_web/reviewer_surface.ex
  - lib/scoria_web/operator_surface.ex
  - lib/scoria_web/live/orchestrator_live.ex
  - lib/scoria_web/live/workflow_live/index.ex
  - lib/scoria_web/live/workflow_live/show.ex
  - lib/scoria_web/live/connectors_live/index.ex
  - lib/scoria_web/live/incidents_live/index.ex
  - lib/scoria_web/live/incidents_live/show.ex
  - lib/scoria_web/live/dataset_live/index.ex
  - lib/scoria/semantic_cache/profile.ex
  - lib/scoria/semantic_lane.ex
  - lib/scoria/runtime/params.ex
  - test/scoria_web/reviewer_surface_test.exs
  - test/scoria/semantic_cache/profile_test.exs
  - test/scoria/semantic_cache/lane_test.exs
  - test/scoria/runtime_test.exs
  - test/scoria/runtime/semantic_fast_path_test.exs
autonomous: true
requirements: [TERM-02, TERM-04]
must_haves:
  truths:
    - "Reviewer read-model vocabulary exposes `ScoriaWeb.ReviewerSurface` while `ScoriaWeb.OperatorSurface` remains accepted during the 0.1.x line per D-01 and D-05."
    - "Semantic cache admission exposes `Scoria.SemanticCache.Profile` and `cache_key:` while `Scoria.SemanticLane`, `lane:`, and stored `lane_key` remain accepted per D-01."
    - "Bounded handoff input accepts `scoped_context:` as the public option while `projected_context:` and the persisted storage field remain accepted per D-01."
  artifacts:
    - path: "lib/scoria_web/reviewer_surface.ex"
      provides: "Final-vocabulary reviewer dashboard read model"
    - path: "lib/scoria_web/operator_surface.ex"
      provides: "0.1.x compatibility wrapper for reviewer read model"
    - path: "lib/scoria/semantic_cache/profile.ex"
      provides: "Final-vocabulary semantic cache profile behavior/macro"
      exports: ["__using__/1", "describe/1"]
    - path: "lib/scoria/semantic_lane.ex"
      provides: "0.1.x compatibility wrapper for semantic cache profile"
    - path: "lib/scoria/runtime/params.ex"
      provides: "`scoped_context:` and semantic cache `profile:` alias normalization"
  key_links:
    - from: "lib/scoria_web/live/workflow_live/show.ex"
      to: "lib/scoria_web/reviewer_surface.ex"
      via: "LiveView alias and tenant-scoped run detail lookup"
      pattern: "ScoriaWeb.ReviewerSurface"
    - from: "lib/scoria/runtime/params.ex"
      to: "lib/scoria/semantic_cache/profile.ex"
      via: "semantic cache profile normalization"
      pattern: "SemanticCache.Profile"
---

<objective>
Complete the remaining D-01 compatibility aliases before copy, docs, and glossary work depend on final terminology.

Purpose: make reviewer, semantic cache, and scoped context vocabulary real in public/discoverable code while preserving 0.1.x compatibility and storage names.
Output: final reviewer read model, semantic cache profile API, scoped context input alias, and focused regression tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-terminology-and-public-vocabulary-migration/46-CONTEXT.md
@.planning/phases/46-terminology-and-public-vocabulary-migration/46-RESEARCH.md
@.planning/phases/46-terminology-and-public-vocabulary-migration/46-PATTERNS.md
@.planning/phases/46-terminology-and-public-vocabulary-migration/46-VALIDATION.md
@.planning/milestones/v3.4-phases/44-dashboard-auth-seam/44-01-SUMMARY.md
@.planning/milestones/v3.4-phases/44-dashboard-auth-seam/44-07-SUMMARY.md
@lib/scoria_web/operator_surface.ex
@lib/scoria/semantic_lane.ex
@lib/scoria/runtime/params.ex
</context>

## Artifacts This Phase Produces

- `ScoriaWeb.ReviewerSurface` in `lib/scoria_web/reviewer_surface.ex`
- `ScoriaWeb.OperatorSurface` compatibility wrapper delegating to `ScoriaWeb.ReviewerSurface`
- LiveView call sites updated to alias `ScoriaWeb.ReviewerSurface`
- `Scoria.SemanticCache.Profile` in `lib/scoria/semantic_cache/profile.ex`
- `Scoria.SemanticLane` compatibility wrapper accepting `lane_key:` while final docs can use `cache_key:`
- `Runtime.Params` support for `scoped_context:` and `semantic_cache: [profile: Module]`
- `test/scoria_web/reviewer_surface_test.exs`
- `test/scoria/semantic_cache/profile_test.exs`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ReviewerSurface and migrate dashboard read-model call sites</name>
  <files>lib/scoria_web/reviewer_surface.ex, lib/scoria_web/operator_surface.ex, lib/scoria_web/live/orchestrator_live.ex, lib/scoria_web/live/workflow_live/index.ex, lib/scoria_web/live/workflow_live/show.ex, lib/scoria_web/live/connectors_live/index.ex, lib/scoria_web/live/incidents_live/index.ex, lib/scoria_web/live/incidents_live/show.ex, lib/scoria_web/live/dataset_live/index.ex, test/scoria_web/reviewer_surface_test.exs</files>
  <read_first>
  - `.planning/phases/46-terminology-and-public-vocabulary-migration/46-CONTEXT.md`
  - `.planning/phases/46-terminology-and-public-vocabulary-migration/46-PATTERNS.md`
  - `.planning/milestones/v3.4-phases/44-dashboard-auth-seam/44-01-SUMMARY.md`
  - `.planning/milestones/v3.4-phases/44-dashboard-auth-seam/44-07-SUMMARY.md`
  - `lib/scoria_web/operator_surface.ex`
  - every LiveView listed in `<files>`
  </read_first>
  <behavior>
  - `ReviewerSurface.fetch_tenant_run_detail/2` preserves tenant-qualified lookup behavior and returns `nil` for invalid or foreign run IDs.
  - `OperatorSurface.fetch_tenant_run_detail/2` delegates to `ReviewerSurface.fetch_tenant_run_detail/2`.
  - Dashboard LiveViews import or alias `ReviewerSurface` for reviewer-facing read-model calls.
  </behavior>
  <action>Per D-01 and D-05, move the implementation from `ScoriaWeb.OperatorSurface` into `ScoriaWeb.ReviewerSurface` and update the moduledoc to reviewer language without weakening Phase 44 tenant authority wording. Keep every public read function name and return shape unchanged. Convert `ScoriaWeb.OperatorSurface` to a 0.1.x compatibility wrapper that delegates all current public functions. Update the listed LiveViews to alias `ReviewerSurface` and keep URL params as selectors only; do not add query-param tenant authority or default tenant fallbacks.</action>
  <acceptance_criteria>
  - Source assertion: `lib/scoria_web/reviewer_surface.ex` defines `defmodule ScoriaWeb.ReviewerSurface`.
  - Source assertion: every listed LiveView references `ReviewerSurface` instead of `OperatorSurface`.
  - Source assertion: `lib/scoria_web/operator_surface.ex` references `ScoriaWeb.ReviewerSurface` and contains no independent Ecto query implementation.
  - Behavior assertion: focused tests prove at least `list_tenant_runs/1`, `fetch_tenant_run_detail/2`, `fetch_tenant_incident/2`, and `compact_trace_badges/2` are callable through both module names with matching results.
  - Security assertion: `test/scoria_web/dashboard_scope_source_guard_test.exs` still passes after alias migration.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/scoria_web/reviewer_surface_test.exs test/scoria_web/dashboard_scope_source_guard_test.exs test/scoria_web/live/dashboard_auth_home_connectors_incidents_test.exs test/scoria_web/live/dashboard_auth_workflows_test.exs</automated>
  </verify>
  <done>`ReviewerSurface` is canonical, dashboard call sites use it, `OperatorSurface` remains compatible, and dashboard tenant authority guards stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SemanticCache.Profile and scoped_context aliases</name>
  <files>lib/scoria/semantic_cache/profile.ex, lib/scoria/semantic_lane.ex, lib/scoria/runtime/params.ex, test/scoria/semantic_cache/profile_test.exs, test/scoria/semantic_cache/lane_test.exs, test/scoria/runtime_test.exs, test/scoria/runtime/semantic_fast_path_test.exs</files>
  <read_first>
  - `.planning/phases/46-terminology-and-public-vocabulary-migration/46-CONTEXT.md`
  - `.planning/phases/46-terminology-and-public-vocabulary-migration/46-PATTERNS.md`
  - `.planning/milestones/v3.4-phases/45-correctness-sweep-fail-closed-proof-closeout/45-05-SUMMARY.md`
  - `lib/scoria/semantic_lane.ex`
  - `lib/scoria/runtime/params.ex`
  - `test/scoria/semantic_cache/lane_test.exs`
  - `test/scoria/runtime_test.exs`
  - `test/scoria/runtime/semantic_fast_path_test.exs`
  </read_first>
  <behavior>
  - A module using `Scoria.SemanticCache.Profile, cache_key: "account_faq"` describes successfully with stored metadata key `lane_key` still equal to `"account_faq"`.
  - `semantic_cache: [profile: MyProfile]` and legacy `semantic_cache: [lane: MyProfile]` both normalize through the same validation path.
  - `scoped_context: %{}` and legacy `projected_context: %{}` both create the same stored handoff projected context, and unsafe keys still return `{:error, :unsafe_projected_context}`.
  </behavior>
  <action>Per D-01 and D-05, add `Scoria.SemanticCache.Profile` as the final public semantic cache profile surface with `cache_key:` in `__using__/1`; keep callbacks compatible with the existing `lane_key/0` storage contract. Convert or adapt `Scoria.SemanticLane` so old `use Scoria.SemanticLane, lane_key: ...` still works as a 0.1.x alias. In `Scoria.Runtime.Params`, normalize `semantic_cache: [profile: Module]` before falling back to legacy `:lane`, and normalize `scoped_context:` before falling back to legacy `:projected_context` for public handoff input; keep persisted handoff keys and semantic cache runtime metadata storage unchanged.</action>
  <acceptance_criteria>
  - Source assertion: `lib/scoria/semantic_cache/profile.ex` defines `defmodule Scoria.SemanticCache.Profile`.
  - Behavior assertion: `Scoria.SemanticCache.Profile.describe(ProfileModule)` returns `{:ok, %{lane_key: "account_faq", default_scope: :tenant_shared, safe_read_only: true}}` for a valid profile.
  - Behavior assertion: `Scoria.SemanticLane.describe(LegacyModule)` remains accepted for a legacy module using `lane_key:`.
  - Behavior assertion: `Runtime.start_run(identity, semantic_cache: [profile: ProfileModule])` persists runtime semantic cache metadata with `"lane_key" => "account_faq"`.
  - Behavior assertion: `Runtime.start_handoff_run(identity, "critic", scoped_context: %{"task" => "review"}, ...)` stores the projected handoff context with `"task" => "review"`.
  - Security assertion: unsafe scoped/projected context keys still fail before durable writes.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/scoria/semantic_cache/profile_test.exs test/scoria/semantic_cache/lane_test.exs test/scoria/runtime_test.exs test/scoria/runtime/semantic_fast_path_test.exs</automated>
  </verify>
  <done>Final semantic cache and scoped context input names exist, legacy aliases still work, and storage names are preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser route params -> dashboard reads | Public route/query values can select objects but cannot assert tenant authority. |
| Host runtime opts -> persisted workflow/semantic state | Caller input aliases normalize before validation and storage. |
| Public old names -> final modules | Compatibility wrappers preserve behavior without duplicating authority logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-02-E | Elevation of Privilege | `ScoriaWeb.ReviewerSurface` call sites | mitigate | Preserve DashboardScope tenant assigns and rerun dashboard source guard plus tenant-spoof focused tests. |
| T-46-02-T | Tampering | `Scoria.Runtime.Params` scoped-context alias | mitigate | Route `scoped_context:` through the existing projected-context map validation and unsafe-key rejection before durable writes. |
| T-46-02-I | Information Disclosure | semantic cache profile alias | mitigate | Route `profile:` through the same profile/lane validation and tenant-scoped cache metadata path; do not widen cache scope. |
| T-46-02-D | Denial of Service | compatibility wrappers | mitigate | Delegation tests ensure old module names remain callable during the 0.1.x line. |
| T-46-02-SC | Tampering | package-manager installs | accept | This plan adds no npm, pip, or cargo package installs. |
</threat_model>

<verification>
Run the task-specific focused tests. If both tasks touch compile-time aliases, also run `MIX_ENV=test mix compile --warnings-as-errors`.
</verification>

<success_criteria>
- `ReviewerSurface`, `SemanticCache.Profile`, `scoped_context:`, and `semantic_cache: [profile: ...]` are implemented before docs reference them.
- `OperatorSurface`, `SemanticLane`, `projected_context:`, `lane:`, and stored `projected_context`/`lane_key` remain compatible.
- Dashboard tenant authority and unsafe-context validation behavior are unchanged.
</success_criteria>

<output>
Create `.planning/phases/46-terminology-and-public-vocabulary-migration/46-02-SUMMARY.md` when done.
</output>
