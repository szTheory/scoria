---
phase: 47-release-packaging-and-docs-truth
plan: 03
type: execute
wave: 3
depends_on:
  - "47-01"
  - "47-02"
files_modified:
  - .github/workflows/ci.yml
  - lib/mix/tasks/scoria.release_preview.ex
  - test/mix/tasks/scoria.release_preview_test.exs
  - docs/operator_verification.md
autonomous: true
requirements:
  - ADPT-03
  - ADPT-04
user_setup: []
must_haves:
  truths:
    - "Maintainers have one bounded release-preview command that proves docs build and package inventory together."
    - "CI fails before merge when the publish-facing docs or package inventory drift."
  artifacts:
    - path: "lib/mix/tasks/scoria.release_preview.ex"
      provides: "Canonical release-preview lane"
      exports: ["run/1"]
    - path: "test/mix/tasks/scoria.release_preview_test.exs"
      provides: "Release-preview task contract assertions"
    - path: ".github/workflows/ci.yml"
      provides: "Merge-blocking release-preview execution"
    - path: "docs/operator_verification.md"
      provides: "Discoverable maintainer command for release-preview proof"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "lib/mix/tasks/scoria.release_preview.ex"
      via: "release-preview CI step"
      pattern: "mix scoria.release_preview"
    - from: "test/mix/tasks/scoria.release_preview_test.exs"
      to: "lib/mix/tasks/scoria.release_preview.ex"
      via: "task loading and required inventory assertions"
      pattern: "Mix.Task.get\\(\"scoria.release_preview\"\\)"
---

<objective>
Ship one bounded release-preview verification lane and make CI enforce it.

Purpose: Even after docs and package inventory are fixed, Scoria still needs one boring command that proves both surfaces together and blocks drift before merge.
Output: `mix scoria.release_preview`, task-contract tests, CI wiring, and maintainer-facing documentation for the canonical release-preview command.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/47-release-packaging-and-docs-truth/47-RESEARCH.md
@.planning/phases/47-release-packaging-and-docs-truth/47-VALIDATION.md
@.planning/phases/47-release-packaging-and-docs-truth/47-01-SUMMARY.md
@.planning/phases/47-release-packaging-and-docs-truth/47-02-SUMMARY.md
@.github/workflows/ci.yml
@docs/operator_verification.md
@lib/mix/tasks/scoria.test.semantic_fast_path.ex
@test/mix/tasks/test.semantic_fast_path_test.exs
@test/scoria/package_surface_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a contract test for the canonical release-preview task</name>
  <files>test/mix/tasks/scoria.release_preview_test.exs</files>
  <read_first>
    <file>lib/mix/tasks/scoria.test.semantic_fast_path.ex</file>
    <file>test/mix/tasks/test.semantic_fast_path_test.exs</file>
    <file>test/scoria/package_surface_test.exs</file>
  </read_first>
  <behavior>
    - Test 1: the task is loadable through `Mix.Task.get("scoria.release_preview")`.
    - Test 2: the task exposes or documents the required package-guide inventory it enforces.
    - Test 3: the test does not rerun the full release preview; it verifies task contract and discoverability only.
  </behavior>
  <action>Create `test/mix/tasks/scoria.release_preview_test.exs` as a bounded contract test. Load all tasks with `Mix.Task.load_all()`, assert `Mix.Task.get("scoria.release_preview")`, and assert the module is loaded. If the task exposes helper functions such as `required_package_paths/0` or `release_preview_output_dir/0`, assert those values include the five guide files plus representative migration/runtime files. Keep the test fast and non-shelling; the shelling proof belongs to `mix scoria.release_preview` itself and CI.</action>
  <acceptance_criteria>
    <criterion>`test/mix/tasks/scoria.release_preview_test.exs` asserts `Mix.Task.get("scoria.release_preview")`</criterion>
    <criterion>The test asserts the task contract includes the exact docs guide paths required by Plan 47-02</criterion>
    <criterion>The test avoids calling the full release-preview shell path directly</criterion>
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/mix/tasks/scoria.release_preview_test.exs --trace</automated>
  </verify>
  <done>The release-preview lane has a fast contract guard independent of the heavier command itself.</done>
</task>

<task type="auto">
  <name>Task 2: Implement `mix scoria.release_preview` as the canonical bounded publish-surface proof</name>
  <files>lib/mix/tasks/scoria.release_preview.ex, docs/operator_verification.md</files>
  <read_first>
    <file>.planning/phases/47-release-packaging-and-docs-truth/47-RESEARCH.md</file>
    <file>test/scoria/package_surface_test.exs</file>
    <file>test/mix/tasks/scoria.release_preview_test.exs</file>
    <file>docs/operator_verification.md</file>
  </read_first>
  <behavior>
    - Test 1: the command runs `mix docs`.
    - Test 2: the command runs a local unpack preview and fails if required files are missing.
    - Test 3: maintainers can discover the command from repo docs without having to infer it from CI YAML.
  </behavior>
  <action>Implement `Mix.Tasks.Scoria.ReleasePreview` in `lib/mix/tasks/scoria.release_preview.ex`. Reuse the package inventory defined in Plan 47-02: either call a shared helper or keep one authoritative required-path list in the task and one mirrored assertion in the contract test. In `run/1`, load paths, run `mix docs`, build an unpacked preview to a deterministic temp directory, and raise with a clear message if any required path is missing. Update `docs/operator_verification.md` with a maintainer-focused section that names `mix scoria.release_preview` as the release/package/docs proof lane, distinct from `mix test.adoption`, `mix test.semantic_fast_path`, and `mix scoria.test.knowledge`.</action>
  <acceptance_criteria>
    <criterion>`lib/mix/tasks/scoria.release_preview.ex` defines `Mix.Tasks.Scoria.ReleasePreview` and `run/1`</criterion>
    <criterion>Running `mix scoria.release_preview` executes `mix docs` before checking package inventory</criterion>
    <criterion>`docs/operator_verification.md` names `mix scoria.release_preview` as the canonical maintainer release-preview lane</criterion>
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/mix/tasks/scoria.release_preview_test.exs --trace && mix scoria.release_preview</automated>
  </verify>
  <done>Maintainers now have one bounded command that proves publish-facing docs and package inventory together.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the release-preview lane into CI so publish-surface drift blocks merge</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    <file>.github/workflows/ci.yml</file>
    <file>lib/mix/tasks/scoria.release_preview.ex</file>
    <file>docs/operator_verification.md</file>
  </read_first>
  <behavior>
    - Test 1: CI runs `mix scoria.release_preview` after dependencies are installed and before the broad suite can mask publish-surface failures.
    - Test 2: the step uses repo-default Mix env behavior appropriate for docs build and package preview.
  </behavior>
  <action>Update `.github/workflows/ci.yml` to add a dedicated `Run release preview lane` step that executes `mix scoria.release_preview`. Place it after dependency installation and before the broad `mix test` step so publish-surface regressions fail fast. Keep the existing adoption, full suite, and knowledge lanes intact; this lane complements them rather than replacing them.</action>
  <acceptance_criteria>
    <criterion>`.github/workflows/ci.yml` contains a step running `mix scoria.release_preview`</criterion>
    <criterion>The release-preview step appears before the broad `mix test` step</criterion>
    <criterion>Existing adoption and knowledge lane steps remain present</criterion>
  </acceptance_criteria>
  <verify>
    <automated>rg -n "mix scoria.release_preview|Run release preview lane" .github/workflows/ci.yml && MIX_ENV=test mix test test/mix/tasks/scoria.release_preview_test.exs --trace && mix scoria.release_preview</automated>
  </verify>
  <done>CI now blocks docs/package publish-surface drift before merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer command -> release decision | Maintainers trust one bounded command to decide whether docs and package surfaces are publish-ready |
| CI -> merge gate | CI must fail when publish-facing docs or package inventory drift, even if runtime tests stay green |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-03-01 | Repudiation | release-preview lane | mitigate | Create one canonical `mix scoria.release_preview` command and document it explicitly. |
| T-47-03-02 | Tampering | CI coverage | mitigate | Add a dedicated CI step that runs the release-preview lane before the broad suite. |
| T-47-03-03 | Spoofing | maintainer verification guidance | mitigate | Update `docs/operator_verification.md` so release-preview proof is discoverable and distinct from runtime/semantic/knowledge lanes. |
</threat_model>

<verification>
- `MIX_ENV=test mix test test/mix/tasks/scoria.release_preview_test.exs --trace`
- `mix scoria.release_preview`
- `rg -n "mix scoria.release_preview|Run release preview lane" .github/workflows/ci.yml`
</verification>

<success_criteria>
- Scoria has one canonical release-preview command for docs and package truth.
- CI enforces that command before merge.
</success_criteria>

<output>
After completion, create `.planning/phases/47-release-packaging-and-docs-truth/47-03-SUMMARY.md`
</output>
