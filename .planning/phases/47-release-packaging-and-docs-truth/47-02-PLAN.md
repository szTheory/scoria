---
phase: 47-release-packaging-and-docs-truth
plan: 02
type: execute
wave: 2
depends_on:
  - "47-01"
files_modified:
  - mix.exs
  - test/scoria/package_surface_test.exs
autonomous: true
requirements:
  - ADPT-04
user_setup: []
must_haves:
  truths:
    - "A local Hex unpack preview contains Scoria's required runtime code, migrations, README, and maintainer guides."
    - "Package inventory no longer depends on Hex defaults that silently exclude `docs/`."
  artifacts:
    - path: "mix.exs"
      provides: "Explicit Hex package files list for the first public release surface"
      exports: ["package/0"]
    - path: "test/scoria/package_surface_test.exs"
      provides: "Executable unpacked-artifact inventory proof"
  key_links:
    - from: "mix.exs"
      to: "test/scoria/package_surface_test.exs"
      via: "expected package files list"
      pattern: "docs/adoption_lanes.md|priv/repo/migrations"
---

<objective>
Define the first-release package inventory explicitly and prove it with a local unpacked Hex artifact.

Purpose: Phase 47 cannot claim package truth while the tarball silently excludes the maintainer/adoption guides that README and docs config both depend on.
Output: explicit `package[:files]` configuration and a deterministic ExUnit inventory proof based on `mix hex.build --unpack`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/47-release-packaging-and-docs-truth/47-RESEARCH.md
@.planning/phases/47-release-packaging-and-docs-truth/47-VALIDATION.md
@.planning/phases/47-release-packaging-and-docs-truth/47-01-SUMMARY.md
@mix.exs
@test/scoria/package_surface_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the package surface test to inspect a real unpacked artifact</name>
  <files>test/scoria/package_surface_test.exs</files>
  <read_first>
    <file>.planning/phases/47-release-packaging-and-docs-truth/47-RESEARCH.md</file>
    <file>test/scoria/package_surface_test.exs</file>
    <file>mix.exs</file>
  </read_first>
  <behavior>
    - Test 1: the test builds an unpacked local artifact into a temp directory and asserts required files exist in the unpacked result.
    - Test 2: the required inventory includes runtime code, repo migrations, knowledge migrations, README, LICENSE, and the five maintainer/adoption guides under `docs/`.
    - Test 3: the required inventory does not rely on broad directory glob assumptions inside the assertion logic; it names the required paths explicitly.
  </behavior>
  <action>Extend `test/scoria/package_surface_test.exs` with an unpacked-artifact test that uses `System.cmd/3` to run `mix hex.build --unpack --output <tmp_dir>` from the repo root. Assert exit status 0, then assert the unpacked directory contains at least `README.md`, `LICENSE`, `mix.exs`, `lib/scoria.ex`, `priv/repo/migrations/20260511000100_create_workflow_tables.exs`, `priv/repo/knowledge_migrations/20260511000300_create_knowledge_tables.exs`, `docs/adoption_lanes.md`, `docs/phoenix_runtime_example.md`, `docs/bounded_handoffs.md`, `docs/semantic_fast_path.md`, and `docs/operator_verification.md`. Clean up the temp output in `on_exit`.</action>
  <acceptance_criteria>
    <criterion>`test/scoria/package_surface_test.exs` shells out to `mix hex.build --unpack` and asserts exit status 0</criterion>
    <criterion>`test/scoria/package_surface_test.exs` names the five exact docs guide paths as required files</criterion>
    <criterion>`test/scoria/package_surface_test.exs` asserts at least one runtime file and both migration trees are present in the unpacked artifact</criterion>
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/package_surface_test.exs --trace</automated>
  </verify>
  <done>The repo has an executable artifact-inventory proof instead of relying on maintainer inspection alone.</done>
</task>

<task type="auto">
  <name>Task 2: Declare the first-release Hex package files list explicitly in `mix.exs`</name>
  <files>mix.exs</files>
  <read_first>
    <file>mix.exs</file>
    <file>test/scoria/package_surface_test.exs</file>
    <file>.planning/phases/47-release-packaging-and-docs-truth/47-RESEARCH.md</file>
  </read_first>
  <behavior>
    - Test 1: the unpacked package now contains the required maintainer guides because `package[:files]` includes them explicitly.
    - Test 2: the package surface stays bounded; it does not accidentally ship `.planning/`, `test/`, or other non-release artifacts.
  </behavior>
  <action>Update `package/0` in `mix.exs` to add an explicit `files:` list for the first public release. Include `lib`, `priv`, `mix.exs`, `.formatter.exs`, `README.md`, `LICENSE`, and the five publish-facing guide files under `docs/`. Keep the list narrow and intentional; do not include `test`, `.planning`, `_build`, or unrelated repo internals. Preserve existing `licenses` and `links` entries.</action>
  <acceptance_criteria>
    <criterion>`mix.exs` sets `package[:files]` explicitly</criterion>
    <criterion>`package[:files]` includes the five exact `docs/*.md` guide paths</criterion>
    <criterion>`package[:files]` does not include `test` or `.planning`</criterion>
    <criterion>`mix hex.build --unpack --output tmp/scoria-hex-preview` yields an unpacked artifact containing the required guide and migration files</criterion>
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/package_surface_test.exs --trace && mix hex.build --unpack --output tmp/scoria-hex-preview</automated>
  </verify>
  <done>Scoria's release tarball surface is explicit, bounded, and locally provable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mix package config -> Hex tarball | Maintainers trust `package[:files]` to ship the public package surface they document |
| Unpacked artifact -> release decision | Local unpack preview must prove required files are present before the first public publish |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-02-01 | Tampering | package inventory | mitigate | Replace Hex default file selection with an explicit `package[:files]` list that includes the maintainer docs and migration truth. |
| T-47-02-02 | Repudiation | release preview | mitigate | Add an unpacked-artifact ExUnit proof that names required files explicitly and fails fast when inventory drifts. |
| T-47-02-03 | Information Disclosure | package surface | accept and bound | Keep the package files list intentionally narrow so tests, planning docs, and local build artifacts are not shipped accidentally. |
</threat_model>

<verification>
- `MIX_ENV=test mix test test/scoria/package_surface_test.exs --trace`
- `mix hex.build --unpack --output tmp/scoria-hex-preview`
</verification>

<success_criteria>
- `mix hex.build --unpack` produces an artifact containing required runtime files, migrations, README, and maintainer guides.
- Package inventory is explicit and bounded in `mix.exs`.
</success_criteria>

<output>
After completion, create `.planning/phases/47-release-packaging-and-docs-truth/47-02-SUMMARY.md`
</output>
