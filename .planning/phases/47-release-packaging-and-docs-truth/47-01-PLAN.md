---
phase: 47-release-packaging-and-docs-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - README.md
  - test/scoria/package_surface_test.exs
autonomous: true
requirements:
  - ADPT-03
user_setup: []
must_haves:
  truths:
    - "Maintainers can run `mix docs` from repo state because Scoria carries a real docs-build dependency."
    - "Publish-facing docs metadata, source links, and docs extras match the README-guided public package story."
  artifacts:
    - path: "mix.exs"
      provides: "Executable ExDoc dependency plus locked docs metadata"
      exports: ["project/0", "deps/0", "docs/0", "package/0"]
    - path: "test/scoria/package_surface_test.exs"
      provides: "Source-of-truth assertions for docs and package metadata"
    - path: "README.md"
      provides: "Maintainer-facing package messaging aligned with Hex-ready docs truth"
  key_links:
    - from: "mix.exs"
      to: "README.md"
      via: "package install/status wording and docs extras list"
      pattern: "tagged GitHub release|Hex-ready package metadata"
    - from: "test/scoria/package_surface_test.exs"
      to: "mix.exs"
      via: "Mix.Project.config assertions"
      pattern: "docs|source_url|homepage_url|package"
---

<objective>
Make `mix docs` a real local proof command and lock Scoria's docs metadata to the public package story it already advertises.

Purpose: Phase 47 cannot claim publish readiness while `mix docs` is missing locally and docs/source metadata can drift without detection.
Output: real ExDoc dependency wiring, hardened docs metadata in `mix.exs`, and a bounded source-truth test covering docs extras, source links, and package-facing README messaging.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/47-release-packaging-and-docs-truth/47-RESEARCH.md
@.planning/phases/47-release-packaging-and-docs-truth/47-VALIDATION.md
@mix.exs
@README.md
@test/scoria/adoption_surface_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a docs/package source-of-truth test before touching project metadata</name>
  <files>test/scoria/package_surface_test.exs</files>
  <read_first>
    <file>mix.exs</file>
    <file>README.md</file>
    <file>test/scoria/adoption_surface_test.exs</file>
  </read_first>
  <behavior>
    - Test 1: `Mix.Project.config()` proves `source_url`, `homepage_url`, `docs`, and `package` all describe the same publish surface.
    - Test 2: the docs extras list is explicit and contains `README.md`, `docs/adoption_lanes.md`, `docs/phoenix_runtime_example.md`, `docs/bounded_handoffs.md`, `docs/semantic_fast_path.md`, and `docs/operator_verification.md`.
    - Test 3: README package messaging still says GitHub-tag install is the current path until Hex publish actually lands.
  </behavior>
  <action>Create `test/scoria/package_surface_test.exs` as an async ExUnit source-of-truth test. Read `Mix.Project.config()` rather than parsing text by hand. Assert `source_url == "https://github.com/szTheory/scoria"` and `homepage_url` matches it. Assert `project[:docs][:main] == "readme"` and that `project[:docs][:extras]` exactly matches the six publish-facing guide files in the adoption order already used by README. Read `README.md` and assert it still contains the tagged-GitHub install wording plus references to the same docs guide paths. Keep this test narrow: it should fail on docs/package drift, not on unrelated README prose.</action>
  <acceptance_criteria>
    <criterion>`test/scoria/package_surface_test.exs` uses `Mix.Project.config()` to assert docs and package metadata</criterion>
    <criterion>`test/scoria/package_surface_test.exs` asserts the six exact docs extras paths in order</criterion>
    <criterion>`test/scoria/package_surface_test.exs` asserts README still advertises tagged GitHub install until first Hex publish lands</criterion>
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/package_surface_test.exs --trace</automated>
  </verify>
  <done>Metadata drift is now caught by one deterministic test before `mix.exs` changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Make `mix docs` executable and lock docs metadata to the versioned package story</name>
  <files>mix.exs, README.md</files>
  <read_first>
    <file>.planning/phases/47-release-packaging-and-docs-truth/47-RESEARCH.md</file>
    <file>mix.exs</file>
    <file>README.md</file>
    <file>test/scoria/package_surface_test.exs</file>
  </read_first>
  <behavior>
    - Test 1: `mix docs` now resolves because `:ex_doc` is present as a non-runtime dev dependency.
    - Test 2: generated docs use explicit publish-facing metadata instead of relying on implied defaults.
    - Test 3: README wording remains aligned with the docs/package story after the metadata hardening.
  </behavior>
  <action>Update `mix.exs` so `project/0` binds the version string once and passes it into `docs(version)`. Add `{:ex_doc, "~> 0.38", only: :dev, runtime: false}` to `deps/0`. Expand `docs/0` to `docs(version)` and set `source_ref: "v#{version}"` so source links resolve against the publish tag story instead of an implicit branch. Keep `main: "readme"` and preserve the six guide extras exactly. Update README only if needed to keep package-story wording aligned with the docs metadata; do not claim that Hex publish has already happened.</action>
  <acceptance_criteria>
    <criterion>`mix.exs` contains `{:ex_doc, "~> 0.38", only: :dev, runtime: false}`</criterion>
    <criterion>`mix.exs` sets `source_ref: "v#{version}"` inside the docs config</criterion>
    <criterion>`mix docs` completes successfully from repo state</criterion>
    <criterion>`README.md` does not claim Scoria is already published on Hex</criterion>
  </acceptance_criteria>
  <verify>
    <automated>mix docs && MIX_ENV=test mix test test/scoria/package_surface_test.exs --trace</automated>
  </verify>
  <done>Scoria's local docs build is real and its docs metadata now matches the current public package story.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer CLI -> docs build | Maintainers trust `mix docs` as the local proof that Hex docs will build from repo state |
| Mix project metadata -> public package story | README, source links, and docs extras must describe the same package surface or publish trust breaks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01-01 | Repudiation | docs build | mitigate | Add the real `:ex_doc` dependency and require `mix docs` in verification so docs proof is executable, not implied. |
| T-47-01-02 | Tampering | package/docs metadata | mitigate | Lock docs/source/homepage/extras assertions in `test/scoria/package_surface_test.exs`. |
| T-47-01-03 | Spoofing | README package messaging | mitigate | Keep README install/status wording aligned with actual pre-publish state and assert it in the source-of-truth test. |
</threat_model>

<verification>
- `MIX_ENV=test mix test test/scoria/package_surface_test.exs --trace`
- `mix docs`
</verification>

<success_criteria>
- `mix docs` works locally from repo state.
- Docs metadata, source links, and README package messaging are locked to the same public package story.
</success_criteria>

<output>
After completion, create `.planning/phases/47-release-packaging-and-docs-truth/47-01-SUMMARY.md`
</output>
