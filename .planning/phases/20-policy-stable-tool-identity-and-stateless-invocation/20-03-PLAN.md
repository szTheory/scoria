---
phase: 20-policy-stable-tool-identity-and-stateless-invocation
plan: 03
type: execute
wave: 3
depends_on:
  - 20-01
  - 20-02
files_modified:
  - lib/scoria/connectors/auth.ex
  - lib/scoria/connectors/grant.ex
  - lib/scoria/connectors/invocation.ex
  - lib/scoria/mcp/executor.ex
  - lib/scoria/sre.ex
  - lib/scoria/workflows.ex
  - test/scoria/connectors/auth_test.exs
  - test/scoria/connectors/invocation_test.exs
  - test/scoria/mcp/executor_test.exs
  - test/scoria/workflows_test.exs
autonomous: true
requirements:
  - AUTH-03
  - POLI-01
  - POLI-02
must_haves:
  truths:
    - "A remote tool call only executes when local Scoria policy and the current remote grant both allow it."
    - "Expired, missing, invalid, or insufficient remote auth states return stable Scoria-owned result categories instead of opaque transport errors."
    - "The default remote invocation contract is request-scoped and stateless; hidden remote session brokering is absent from the normal path."
  artifacts:
    - path: "lib/scoria/connectors/invocation.ex"
      provides: "Connector-aware invocation gate and typed outcome envelope"
    - path: "lib/scoria/connectors/auth.ex"
      provides: "Typed auth/scope evidence hooks for remote grant failure paths"
    - path: "test/scoria/connectors/invocation_test.exs"
      provides: "Dual-plane policy, scope escalation, and stateless contract coverage"
  key_links:
    - from: "lib/scoria/connectors/invocation.ex"
      to: "lib/scoria/mcp/executor.ex"
      via: "pre-execution policy gate"
      pattern: "(policy_denied|auth_required|scope_escalation_required|tool_unavailable)"
    - from: "lib/scoria/connectors/invocation.ex"
      to: "lib/scoria/connectors/grant.ex"
      via: "grant scope and expiry checks"
      pattern: "granted_scopes|expires_at|status"
    - from: "lib/scoria/sre.ex"
      to: "test/scoria/connectors/auth_test.exs"
      via: "durable audit evidence"
      pattern: "connector.auth_failed|scope_escalation|missing_scopes"
    - from: "lib/scoria/workflows.ex"
      to: "test/scoria/workflows_test.exs"
      via: "workflow-compatible escalation lineage"
      pattern: "run_id|step_id|scope_escalation|audit_outbox_event_id"
---

<objective>
Finish Phase 20 by making invocation policy-safe, auth-explicit, and stateless-first by default.

Purpose: Implement D-12 through D-25 so remote connector execution uses stable local-tool truth, dual-plane enforcement, typed auth/scope outcomes, and durable evidence without hiding stateful sessions in the default path.
Output: A connector invocation seam, typed local outcome envelopes, durable auth/scope evidence, and tests that prove stateless request-scoped invocation remains the default contract.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-policy-stable-tool-identity-and-stateless-invocation/20-CONTEXT.md
@.planning/phases/20-policy-stable-tool-identity-and-stateless-invocation/20-RESEARCH.md
@lib/scoria/connectors/auth.ex
@lib/scoria/connectors/grant.ex
@lib/scoria/mcp/executor.ex
@lib/scoria/sre.ex
@lib/scoria/workflows.ex
@test/scoria/connectors/auth_test.exs
@test/scoria/mcp/executor_test.exs
@test/scoria/workflows_test.exs

<interfaces>
From lib/scoria/mcp/executor.ex:
```elixir
def execute(tool_module, args, context, timeout \\ 5000)
```

From lib/scoria/connectors/grant.ex:
```elixir
schema "ai_connector_grants" do
  field(:status, :string, default: "pending")
  field(:granted_scopes, {:array, :string}, default: [])
  field(:expires_at, :utc_datetime_usec)
  field(:last_refresh_status, :string, default: "pending")
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add connector-aware invocation gating with stable local outcomes</name>
  <read_first>
    - lib/scoria/mcp/executor.ex
    - lib/scoria/connectors/grant.ex
    - lib/scoria/connectors.ex
    - .planning/phases/20-policy-stable-tool-identity-and-stateless-invocation/20-CONTEXT.md
    - test/scoria/mcp/executor_test.exs
  </read_first>
  <files>lib/scoria/connectors/invocation.ex, lib/scoria/mcp/executor.ex, test/scoria/connectors/invocation_test.exs, test/scoria/mcp/executor_test.exs</files>
  <behavior>
    - Test 1: invocation succeeds only when the local tool is active, local policy allows the action class, and the current grant covers the required remote scopes.
    - Test 2: expired or missing grants return `:auth_required`; valid-but-insufficient grants return `:scope_insufficient` or `:scope_escalation_required`; unavailable or quarantined tools return `:tool_unavailable`.
    - Test 3: blocked outcomes happen before outbound execution whenever local data is sufficient.
    - Test 4: the default invocation API is request-scoped and does not create or reuse hidden remote sessions.
  </behavior>
  <action>Create `Scoria.Connectors.Invocation` as the Phase 20 gate for remote execution. It must evaluate both planes per D-12 through D-15: current durable grant scope and local Scoria policy against the stable local-tool id. Reuse the typed-envelope style from `Scoria.MCP.Executor`, returning Scoria-owned statuses instead of transport leaks. Update `Scoria.MCP.Executor` only where needed to accept or emit the new connector-aware envelopes cleanly; do not turn the executor into a session broker. Per D-21 through D-25, keep the default contract stateless and request-scoped, and if explicit stateful scaffolding is added, make it clearly separate from the normal path.</action>
  <acceptance_criteria>
    - `rg -n 'scope_escalation_required|auth_required|policy_denied|tool_unavailable' lib/scoria/connectors/invocation.ex lib/scoria/mcp/executor.ex` finds the stable outcome taxonomy.
    - `mix test test/scoria/connectors/invocation_test.exs test/scoria/mcp/executor_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/connectors/invocation_test.exs test/scoria/mcp/executor_test.exs</automated>
  </verify>
  <done>Remote execution now goes through a dual-plane gate using stable local-tool truth, and callers receive typed Scoria-owned blocked outcomes before outbound transport when local evidence is sufficient.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist auth-failure and scope-escalation evidence without leaking secrets</name>
  <read_first>
    - lib/scoria/connectors/auth.ex
    - lib/scoria/sre.ex
    - lib/scoria/sre/audit_outbox_event.ex
    - test/scoria/connectors/auth_test.exs
    - test/scoria/mcp/executor_test.exs
  </read_first>
  <files>lib/scoria/connectors/auth.ex, lib/scoria/connectors/grant.ex, lib/scoria/sre.ex, lib/scoria/workflows.ex, test/scoria/connectors/auth_test.exs, test/scoria/connectors/invocation_test.exs, test/scoria/workflows_test.exs</files>
  <behavior>
    - Test 1: expired, invalid, or missing grants emit durable audit evidence with connector id, local tool id, identity context, policy outcome, and redacted request/response summaries.
    - Test 2: step-up scope requirements emit a distinct scope-escalation outcome with exact missing scopes recorded in evidence.
    - Test 3: auth failure and scope-escalation evidence preserve workflow-compatible lineage fields including `run_id`, `step_id`, and linked audit/event ids so Phase 21 can attach without redefining the seam.
    - Test 4: raw tokens, raw auth payloads, and secret-bearing transport details do not appear in metadata or operator-facing evidence.
  </behavior>
  <action>Extend the auth/evidence seam per D-16 through D-20. Distinguish ordinary auth failure from scope escalation, persist workflow-compatible escalation intent and missing-scope metadata, and include connector id, local tool id, actor/tenant/run identity, `run_id`, `step_id`, policy key/outcome, linked audit ids, and any linked workflow event/checkpoint ids needed for the later approval attachment seam. Reuse the existing `Scoria.SRE` outbox pattern and current `Scoria.Workflows` lineage style instead of inventing a second evidence model. Add focused tests proving auth failure and scope escalation preserve `run_id`, `step_id`, missing scopes, policy outcome, and linked audit/event ids in durable evidence.</action>
  <acceptance_criteria>
    - `rg -n 'connector\\.auth_failed|scope_escalation|missing_scopes|local_tool_id' lib/scoria/connectors/auth.ex lib/scoria/sre.ex test/scoria/connectors/auth_test.exs test/scoria/connectors/invocation_test.exs` finds the new evidence fields and event types.
    - `rg -n 'run_id|step_id|audit_outbox_event_id|workflow_event_id|scope_escalation' lib/scoria/connectors/auth.ex lib/scoria/workflows.ex test/scoria/workflows_test.exs test/scoria/connectors/invocation_test.exs` finds workflow-lineage coverage for the escalation seam.
    - `mix test test/scoria/connectors/auth_test.exs test/scoria/connectors/invocation_test.exs test/scoria/workflows_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/connectors/auth_test.exs test/scoria/connectors/invocation_test.exs test/scoria/workflows_test.exs</automated>
  </verify>
  <done>Auth and scope problems surface as explicit Scoria events with durable redacted evidence, and the default remote invocation lane remains stateless-first and unsurprising.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host app invocation -> remote connector gate | Untrusted caller input crosses into policy, scope, and transport decisions |
| remote auth response -> Scoria evidence | Remote auth failures may contain sensitive payloads that must not leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-06 | E | connector invocation gate | mitigate | Require both local policy and current grant scope before remote execution, using stable local-tool ids |
| T-20-07 | I | auth/scope evidence | mitigate | Redact request/response summaries and exclude raw tokens or token payload fragments from durable metadata |
| T-20-08 | T | stateless invocation contract | mitigate | Keep default APIs request-scoped and forbid hidden session creation/reuse on the normal invocation path |
</threat_model>

<verification>
- `mix test test/scoria/connectors/invocation_test.exs test/scoria/connectors/auth_test.exs test/scoria/mcp/executor_test.exs`
- Optional grep to confirm typed statuses and missing-scope evidence fields exist
</verification>

<success_criteria>
Remote invocation is conservative and unsurprising: both policy planes must allow execution, auth and scope failures are typed and durable, and the default remote path remains stateless-first.
</success_criteria>

<output>
After completion, create `.planning/phases/20-policy-stable-tool-identity-and-stateless-invocation/20-policy-stable-tool-identity-and-stateless-invocation-03-SUMMARY.md`
</output>
