---
phase: 20-policy-stable-tool-identity-and-stateless-invocation
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - lib/scoria/connectors/discovery.ex
  - lib/scoria/connectors/local_tool.ex
  - lib/scoria/connectors/tool_reconciliation.ex
  - test/scoria/connectors_test.exs
  - test/scoria/connectors/tool_reconciliation_test.exs
autonomous: true
requirements:
  - CONN-03
  - POLI-01
must_haves:
  truths:
    - "Capability refresh preserves stable local-tool ids across safe drift."
    - "Ambiguous, widened, or higher-risk remote changes fail closed into pending/disabled local-tool states."
    - "Removed remote tools remain durably visible instead of disappearing from policy and evidence history."
  artifacts:
    - path: "lib/scoria/connectors/tool_reconciliation.ex"
      provides: "Deterministic rebinding and drift classification"
    - path: "lib/scoria/connectors/discovery.ex"
      provides: "Refresh transaction that updates snapshots and local-tool truth together"
    - path: "test/scoria/connectors/tool_reconciliation_test.exs"
      provides: "Proof for rename carry-forward, widening quarantine, and removal handling"
  key_links:
    - from: "lib/scoria/connectors/discovery.ex"
      to: "lib/scoria/connectors/tool_reconciliation.ex"
      via: "refresh transaction"
      pattern: "ToolReconciliation"
    - from: "lib/scoria/connectors/tool_reconciliation.ex"
      to: "lib/scoria/connectors/local_tool.ex"
      via: "stable rebinding writes"
      pattern: "(LocalTool|LocalToolAlias)"
    - from: "lib/scoria/connectors/discovery.ex"
      to: "test/scoria/connectors_test.exs"
      via: "integration refresh expectations"
      pattern: "local_tool|pending|removed|stale"
---

<objective>
Turn refresh-time capability snapshots into durable local-tool reconciliation without silently widening callable power.

Purpose: Implement D-03 through D-11 so refreshes improve fidelity while preserving stable local ids and quarantining risky drift.
Output: A reconciliation service wired into discovery refresh, with tests for non-widening carry-forward, widening quarantine, ambiguous matches, and removed-tool lifecycle transitions.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-policy-stable-tool-identity-and-stateless-invocation/20-CONTEXT.md
@.planning/phases/20-policy-stable-tool-identity-and-stateless-invocation/20-RESEARCH.md
@.planning/phases/20-policy-stable-tool-identity-and-stateless-invocation/20-PATTERNS.md
@lib/scoria/connectors/discovery.ex
@lib/scoria/connectors.ex
@lib/scoria/connectors/local_tool.ex
@test/scoria/connectors_test.exs

<interfaces>
From lib/scoria/connectors/discovery.ex:
```elixir
def refresh_connector(connector_id, opts \\ []) do
  ...
  |> Multi.update(:connector, Connector.changeset(connector, connector_attrs))
  |> Multi.run(:snapshot, fn repo, _changes -> upsert_snapshot(repo, connector.id, snapshot_attrs) end)
end
```

From lib/scoria/connectors.ex:
```elixir
def list_connectors(filters \\ %{})
def sync_connector(connector_or_id, opts \\ [])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic local-tool reconciliation and drift classification</name>
  <read_first>
    - lib/scoria/connectors/discovery.ex
    - lib/scoria/connectors/local_tool.ex
    - .planning/phases/20-policy-stable-tool-identity-and-stateless-invocation/20-CONTEXT.md
    - test/scoria/connectors_test.exs
  </read_first>
  <files>lib/scoria/connectors/tool_reconciliation.ex, lib/scoria/connectors/local_tool.ex, test/scoria/connectors/tool_reconciliation_test.exs</files>
  <behavior>
    - Test 1: explicit stable metadata or prior alias/history rebinds the refreshed remote tool to the existing local-tool id.
    - Test 2: exact-name plus compatible schema fingerprint carries forward policy only for non-widening drift.
    - Test 3: ambiguous matches, widened schemas, scope widening, or newly destructive behavior create disabled-pending candidates instead of mutating the existing row.
    - Test 4: remote removals mark the prior local tool stale/removed without deleting the row.
  </behavior>
  <action>Create `Scoria.Connectors.ToolReconciliation` to encode D-03 through D-11 exactly: attempt rebinding using explicit stable metadata, then alias/history, then exact remote name plus compatible schema fingerprint, then name-only only when drift is clearly non-widening; otherwise fail closed by preserving the prior local-tool row and creating a pending candidate per D-04 and D-09. Represent removed tools with a stale/removed lifecycle state per D-05, and keep non-widening versus widening logic explicit and conservative. Use local policy carry-forward only for non-widening drift per D-08 and D-10.</action>
  <acceptance_criteria>
    - `rg -n 'explicit remote stable metadata|alias|schema fingerprint|pending|removed|stale' lib/scoria/connectors/tool_reconciliation.ex` finds the reconciliation pathways and fail-closed states.
    - `mix test test/scoria/connectors/tool_reconciliation_test.exs` passes with rename, widening, ambiguity, and removal coverage.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/connectors/tool_reconciliation_test.exs</automated>
  </verify>
  <done>A dedicated reconciliation service exists and proves stable local ids survive safe drift while risky changes are quarantined instead of auto-enabled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire discovery refresh to reconcile local-tool truth transactionally</name>
  <read_first>
    - lib/scoria/connectors/discovery.ex
    - lib/scoria/connectors/tool_reconciliation.ex
    - test/scoria/connectors_test.exs
    - test/scoria/connectors/schema_test.exs
  </read_first>
  <files>lib/scoria/connectors/discovery.ex, test/scoria/connectors_test.exs, test/scoria/connectors/tool_reconciliation_test.exs</files>
  <behavior>
    - Test 1: a refresh updates the capability snapshot and local-tool reconciliation outcomes in one durable flow.
    - Test 2: refresh never silently widens callable surface area; new/high-risk tools remain disabled-pending after sync.
    - Test 3: existing allowed tools keep the same local-tool id across rename-only or tightening drift.
  </behavior>
  <action>Extend `Scoria.Connectors.Discovery.refresh_connector/2` so refresh writes snapshot evidence and then runs `ToolReconciliation` in the same explicit durable flow. Keep `CapabilitySnapshot` as evidence input, not callable identity truth, per D-07 and D-11. Ensure refreshed connector/local-tool metadata captures reconciliation outcomes needed by later operator review, but do not add Phase 21 UI or approval handling. Preserve the explicit job-driven refresh posture from Phase 19.</action>
  <acceptance_criteria>
    - `rg -n 'ToolReconciliation' lib/scoria/connectors/discovery.ex` confirms refresh now invokes reconciliation.
    - `mix test test/scoria/connectors_test.exs test/scoria/connectors/tool_reconciliation_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/connectors_test.exs test/scoria/connectors/tool_reconciliation_test.exs</automated>
  </verify>
  <done>Connector refresh now yields both snapshot evidence and durable local-tool reconciliation outcomes, with no silent policy widening on catalog drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote catalog snapshot -> reconciliation engine | Untrusted tool metadata may try to widen power or spoof continuity |
| prior local policy -> refreshed catalog | Existing policy must only carry forward when drift is provably non-widening |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | S | tool reconciliation | mitigate | Rebinding order prefers stable metadata and alias history before name-based fallbacks, reducing spoofed continuity |
| T-20-04 | E | drift classifier | mitigate | Quarantine widened, ambiguous, or higher-risk tools into disabled-pending states so refresh cannot grant new effective power |
| T-20-05 | T | removed-tool handling | mitigate | Mark rows stale/removed instead of deleting them, preserving policy and evidence referential integrity |
</threat_model>

<verification>
- `mix test test/scoria/connectors_test.exs test/scoria/connectors/tool_reconciliation_test.exs`
- Optional grep to confirm `ToolReconciliation` is called from `Discovery.refresh_connector/2`
</verification>

<success_criteria>
Refresh-time catalog drift is conservative and deterministic: stable local ids persist for safe drift, risky changes are quarantined, and removed tools remain durably visible.
</success_criteria>

<output>
After completion, create `.planning/phases/20-policy-stable-tool-identity-and-stateless-invocation/20-policy-stable-tool-identity-and-stateless-invocation-02-SUMMARY.md`
</output>
