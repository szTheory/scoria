---
phase: 20-policy-stable-tool-identity-and-stateless-invocation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/20260517000200_create_connector_local_tool_tables.exs
  - lib/scoria/connectors/connector.ex
  - lib/scoria/connectors/local_tool.ex
  - lib/scoria/connectors/local_tool_alias.ex
  - lib/scoria/connectors.ex
  - test/scoria/connectors/schema_test.exs
  - test/scoria/connectors_test.exs
autonomous: true
requirements:
  - CONN-03
must_haves:
  truths:
    - "Scoria owns a durable local-tool identity that survives remote catalog churn."
    - "Local policy and audit references can target a stable Scoria local-tool id instead of a remote name."
    - "Alias/history rows exist so later reconciliation can bind refresh evidence without rewriting identity truth."
  artifacts:
    - path: "priv/repo/migrations/20260517000200_create_connector_local_tool_tables.exs"
      provides: "Durable local-tool and alias/history tables tied to connectors"
    - path: "lib/scoria/connectors/local_tool.ex"
      provides: "Stable local-tool schema with lifecycle state and binding metadata"
    - path: "lib/scoria/connectors/local_tool_alias.ex"
      provides: "Alias/history schema for deterministic rebinding"
    - path: "lib/scoria/connectors.ex"
      provides: "Scoria-owned API for querying durable local-tool truth"
  key_links:
    - from: "lib/scoria/connectors/connector.ex"
      to: "lib/scoria/connectors/local_tool.ex"
      via: "has_many association"
      pattern: "has_many\\(:local_tools"
    - from: "lib/scoria/connectors/local_tool_alias.ex"
      to: "lib/scoria/connectors/local_tool.ex"
      via: "belongs_to association"
      pattern: "belongs_to\\(:local_tool"
    - from: "lib/scoria/connectors.ex"
      to: "lib/scoria/connectors/local_tool.ex"
      via: "public query helpers"
      pattern: "(list_local_tools|get_local_tool!|get_local_tool)"
---

<objective>
Create the durable local-tool identity seam Phase 20 needs before any refresh or invocation logic changes.

Purpose: Establish Scoria-owned tool identity truth per D-01 through D-06 so remote catalog snapshots stop acting like runtime identity.
Output: Migration-backed local-tool schemas, alias/history rows, and public connector-context helpers for downstream reconciliation and invocation work.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-policy-stable-tool-identity-and-stateless-invocation/20-CONTEXT.md
@.planning/phases/20-policy-stable-tool-identity-and-stateless-invocation/20-RESEARCH.md
@.planning/phases/20-policy-stable-tool-identity-and-stateless-invocation/20-PATTERNS.md
@lib/scoria/connectors.ex
@lib/scoria/connectors/connector.ex
@lib/scoria/connectors/capability_snapshot.ex
@test/scoria/connectors/schema_test.exs

<interfaces>
From lib/scoria/connectors.ex:
```elixir
def register_connector(attrs \\ %{})
def update_connector(%Connector{} = connector, attrs)
def update_connector(connector_id, attrs)
def get_connector(id)
def get_connector!(id)
def list_connectors(filters \\ %{})
def sync_connector(connector_or_id, opts \\ [])
```

From lib/scoria/connectors/connector.ex:
```elixir
schema "ai_connectors" do
  field(:key, :string)
  field(:status, :string, default: "registered")
  has_many(:grants, Scoria.Connectors.Grant)
  has_one(:capability_snapshot, Scoria.Connectors.CapabilitySnapshot)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add durable local-tool and alias/history schemas</name>
  <read_first>
    - priv/repo/migrations/20260517000100_create_connector_boundary_tables.exs
    - lib/scoria/connectors/connector.ex
    - lib/scoria/connectors/capability_snapshot.ex
    - test/scoria/connectors/schema_test.exs
  </read_first>
  <files>priv/repo/migrations/20260517000200_create_connector_local_tool_tables.exs, lib/scoria/connectors/connector.ex, lib/scoria/connectors/local_tool.ex, lib/scoria/connectors/local_tool_alias.ex, test/scoria/connectors/schema_test.exs</files>
  <behavior>
    - Test 1: inserting a local tool persists a stable surrogate id, optional local slug, lifecycle state, current binding metadata, and connector ownership.
    - Test 2: alias/history rows enforce referential integrity and uniqueness so rebinding can prefer prior aliases deterministically.
    - Test 3: removed/stale lifecycle states stay queryable without deleting the local-tool row.
  </behavior>
  <action>Create the local-tool durability layer per D-01 through D-06: a migration for connector-owned local-tool rows plus alias/history rows; a `Scoria.Connectors.LocalTool` schema with stable Scoria id, optional human-readable slug, lifecycle state, remote binding fields, schema fingerprint, risk/classification metadata, optimistic locking, and bounded metadata; a `Scoria.Connectors.LocalToolAlias` schema that records alias/binding history separately from the current row. Update `Scoria.Connectors.Connector` associations to point at the new durable noun. Do not treat `CapabilitySnapshot.catalog` as identity truth anywhere in this task.</action>
  <acceptance_criteria>
    - `rg -n 'create table\\(:ai_connector_local_tools|create table\\(:ai_connector_local_tool_aliases' priv/repo/migrations/20260517000200_create_connector_local_tool_tables.exs` finds both tables.
    - `rg -n 'field\\(:local_slug|field\\(:lifecycle_state|field\\(:schema_fingerprint' lib/scoria/connectors/local_tool.ex` finds the stable identity fields.
    - `mix test test/scoria/connectors/schema_test.exs` passes with new local-tool coverage.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/connectors/schema_test.exs</automated>
  </verify>
  <done>Durable local-tool and alias/history tables exist, schemas compile, and schema tests prove stable identity columns exist independently from capability snapshots.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose local-tool query helpers from the Scoria connector boundary</name>
  <read_first>
    - lib/scoria/connectors.ex
    - lib/scoria/connectors/connector.ex
    - lib/scoria/connectors/local_tool.ex
    - test/scoria/connectors_test.exs
  </read_first>
  <files>lib/scoria/connectors.ex, test/scoria/connectors_test.exs</files>
  <behavior>
    - Test 1: callers can fetch one local tool or list local tools for a connector without reading raw capability snapshots directly.
    - Test 2: preloads preserve connector/local-tool relationships needed by later reconciliation and invocation plans.
  </behavior>
  <action>Add `Scoria.Connectors` helpers such as `get_local_tool/1`, `get_local_tool!/1`, and `list_local_tools/1` or equivalent public queries so later phases consume durable local-tool truth per D-02 and D-06. Keep these helpers preload-friendly and connector-scoped. Add API-level tests in `test/scoria/connectors_test.exs` that create local-tool rows, fetch them through the public context boundary, and assert connector/local-tool relationship preloads are present. Do not add reconciliation or invocation behavior here; this plan only establishes the contract and query surface.</action>
  <acceptance_criteria>
    - `rg -n 'def (get_local_tool|get_local_tool!|list_local_tools)' lib/scoria/connectors.ex` finds the public helpers.
    - `mix test test/scoria/connectors_test.exs` passes with explicit local-tool fetch/list/preload coverage.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/connectors_test.exs</automated>
  </verify>
  <done>The connector context exposes a stable local-tool query surface, and downstream plans can depend on local-tool rows instead of snapshot catalog entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| connector refresh evidence -> local-tool truth | Untrusted remote catalog data must not overwrite Scoria identity truth directly |
| operator query surface -> durable tool rows | Operator-visible records must stay intelligible after remote rename/removal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | T | local-tool migration/schema | mitigate | Keep stable Scoria ids and alias/history tables separate so remote names cannot silently rewrite identity truth |
| T-20-02 | R | local-tool lifecycle state | mitigate | Preserve stale/removed rows instead of deleting them so prior policy and audit evidence remain attributable |
</threat_model>

<verification>
- `mix test test/scoria/connectors/schema_test.exs`
- Optional sanity grep for new helper exports and migration tables
</verification>

<success_criteria>
Phase 20 now has a durable local-tool noun with stable ids, optional readable slugs, lifecycle state, and alias/history rows. No downstream work needs to treat capability snapshots as callable identity truth anymore.
</success_criteria>

<output>
After completion, create `.planning/phases/20-policy-stable-tool-identity-and-stateless-invocation/20-policy-stable-tool-identity-and-stateless-invocation-01-SUMMARY.md`
</output>
