---
phase: 23
plan: 03
type: execute
wave: 2
depends_on:
  - "23-01"
  - "23-02"
files_modified:
  - lib/scoria/prompt_registry.ex
  - test/scoria/prompt_registry_test.exs
autonomous: true
requirements:
  - REG-01
  - REG-02
  - REG-03
must_haves:
  truths:
    - "Operator can save a structured prompt template and it persists as an immutable, versioned Ecto record"
    - "System rejects in-place edits to active prompts, forcing a new version instead"
  artifacts:
    - path: "lib/scoria/prompt_registry.ex"
      provides: "Context API for prompt templates"
  key_links:
    - from: "lib/scoria/prompt_registry.ex"
      to: "lib/scoria/prompt_registry/prompt_template.ex"
      via: "changeset creation and queries"
      pattern: "PromptTemplate.changeset"
    - from: "lib/scoria/prompt_registry.ex"
      to: "lib/scoria/prompt_registry/tokenizer.ex"
      via: "estimating tokens on save"
      pattern: "Tokenizer.estimate_tokens"
---

<objective>
Implement the primary context interface for prompt registry operations, including token estimation injection and immutable lifecycle transitions.

Purpose: Provide the public API for managing prompt templates, ensuring active templates are immutable and automatically checking token limits, satisfying all Phase 23 requirements.
Output: `Scoria.PromptRegistry` context module and comprehensive tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-ecto-backed-prompt-registry-and-lifecycle/23-RESEARCH.md
@.planning/phases/23-ecto-backed-prompt-registry-and-lifecycle/23-PATTERNS.md

<interfaces>
From lib/scoria/prompt_registry/tokenizer.ex:
```elixir
defmodule Scoria.PromptRegistry.Tokenizer do
  def estimate_tokens(prompt_map_or_string)
end
```

From lib/scoria/prompt_registry/prompt_template.ex:
```elixir
defmodule Scoria.PromptRegistry.PromptTemplate do
  # Fields: entity_id, version, is_current, status, system_message, few_shot_examples, user_template, estimated_tokens
  def changeset(template, attrs)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Context Logic</name>
  <files>lib/scoria/prompt_registry.ex, test/scoria/prompt_registry_test.exs</files>
  <behavior>
    - Test 1: `create_draft_template/1` calculates token estimates before saving and inserts a draft version 1 template.
    - Test 2: `update_prompt_template/2` for an active template correctly inserts a new version, decrements the old one (`is_current: false`), via `Ecto.Multi`.
    - Test 3: `transition_status/2` successfully changes a template from draft to active, or active to archived.
    - Test 4: Rejects direct in-place modification of `system_message` on active templates without version bump.
  </behavior>
  <action>
    Implement `Scoria.PromptRegistry` module. Create CRUD functions using `Scoria.Repo` matching the immutable update pattern of analog `lib/scoria/eval.ex`.
    - `create_draft_template(attrs)`: calls `Tokenizer.estimate_tokens(attrs)` and merges `estimated_tokens` into attrs, then inserts a new `PromptTemplate` with `version: 1`, `status: "draft"`. Provide an explicit UUID `entity_id` if not in attrs.
    - `update_prompt_template(old_template, attrs)`: Uses `Ecto.Multi` to deprecate `old_template` (`is_current: false`) and insert a new struct with `version: old_template.version + 1`, calculating `estimated_tokens` on the merged new state.
    - `transition_status(template, new_status)`: Updates the status without bumping version (status transitions don't change content).
    - Validate logic preventing in-place mutations of content fields (`system_message`, `few_shot_examples`, `user_template`) unless the status is "draft" (drafts can be mutated in place, active/archived must use `update_prompt_template` to generate new versions if content changes).
  </action>
  <verify>
    <automated>mix test test/scoria/prompt_registry_test.exs</automated>
  </verify>
  <done>Immutable version bumping and token estimation flow natively through the Context API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator -> Context API | Operator invokes API to mutate prompt state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Tampering | PromptRegistry | mitigate | Immutable updates enforced by Context logic. Active records cannot be directly edited via the standard `update` API. |
</threat_model>

<verification>
mix test test/scoria/prompt_registry_test.exs completes with 0 failures, verifying all state transitions and token estimations.
</verification>

<success_criteria>
`Scoria.PromptRegistry` orchestrates the complete prompt lifecycle and immutable updates securely.
</success_criteria>

<output>
After completion, create `.planning/phases/23-ecto-backed-prompt-registry-and-lifecycle/23-03-SUMMARY.md`
</output>