---
phase: 23
plan: 04
type: execute
wave: 3
depends_on:
  - "23-01"
  - "23-02"
  - "23-03"
files_modified:
  - lib/scoria_web/live/prompt_live/index.ex
  - lib/scoria_web/router.ex
  - test/scoria_web/live/prompt_live_test.exs
autonomous: true
requirements:
  - REG-01
  - REG-03
must_haves:
  truths:
    - "Operator can manage structured prompt templates via a LiveView interface"
    - "System calculates and displays token count estimations when changing/saving a prompt"
  artifacts:
    - path: "lib/scoria_web/live/prompt_live/index.ex"
      provides: "LiveView UI for prompt template management"
  key_links:
    - from: "lib/scoria_web/live/prompt_live/index.ex"
      to: "lib/scoria/prompt_registry.ex"
      via: "context calls for listing and saving templates"
      pattern: "PromptRegistry.update_prompt_template"
    - from: "lib/scoria_web/live/prompt_live/index.ex"
      to: "lib/scoria/prompt_registry/tokenizer.ex"
      via: "token estimation on change"
      pattern: "Tokenizer.estimate_tokens"
---

<objective>
Create the LiveView interface for managing prompt templates and displaying token estimations.

Purpose: Fulfill Phase 23 Success Criteria #1 and #3 by allowing operators to save structured prompt templates through the UI and seeing calculated token count estimations during edit/save.
Output: `ScoriaWeb.PromptLive.Index` module, router update, and LiveView tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-ecto-backed-prompt-registry-and-lifecycle/23-RESEARCH.md
@.planning/phases/23-ecto-backed-prompt-registry-and-lifecycle/23-PATTERNS.md

<interfaces>
From lib/scoria/prompt_registry.ex:
```elixir
defmodule Scoria.PromptRegistry do
  def list_prompt_templates()
  def get_prompt_template!(id)
  def create_draft_template(attrs)
  def update_prompt_template(old_template, attrs)
end
```

From lib/scoria/prompt_registry/tokenizer.ex:
```elixir
defmodule Scoria.PromptRegistry.Tokenizer do
  def estimate_tokens(prompt_map_or_string)
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Route</name>
  <files>lib/scoria_web/router.ex</files>
  <action>
    Add a live route in `lib/scoria_web/router.ex` under the appropriate authenticated browser scope (e.g. `/prompts`) pointing to `ScoriaWeb.PromptLive.Index`.
  </action>
  <verify>
    <automated>mix format lib/scoria_web/router.ex && mix compile</automated>
  </verify>
  <done>Route compiles successfully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement LiveView Interface</name>
  <files>lib/scoria_web/live/prompt_live/index.ex, test/scoria_web/live/prompt_live_test.exs</files>
  <behavior>
    - Test 1: Renders the list of prompt templates correctly.
    - Test 2: Editing a template shows the form with existing data.
    - Test 3: Changing form inputs (`phx-change`) dynamically calculates and displays the `estimated_tokens` limit warning/info.
    - Test 4: Saving the form calls `PromptRegistry.update_prompt_template` (or create) and handles validation errors.
  </behavior>
  <action>
    Implement `ScoriaWeb.PromptLive.Index` based on the form/edit pattern from analog `lib/scoria_web/live/eval_spec_live/index.ex`. 
    Include the list of prompt templates, and a form (either inline or modal) for creating/editing. The form should handle `phx-change` to calculate tokens interactively using `Tokenizer.estimate_tokens/1` and display the estimated count to the user in the UI, highlighting or warning if it exceeds standard bounds.
    Handle `phx-submit` to call `PromptRegistry.create_draft_template/1` or `update_prompt_template/2` appropriately.
  </action>
  <verify>
    <automated>mix test test/scoria_web/live/prompt_live_test.exs</automated>
  </verify>
  <done>LiveView renders, handles dynamic token estimation, and saves templates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> LiveView | Operator submitting prompt forms via WebSocket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | Tampering | PromptLive | mitigate | Leverage Ecto changesets directly in the LiveView to validate input structures before attempting context mutations. |
</threat_model>

<verification>
mix test test/scoria_web/live/prompt_live_test.exs passes cleanly.
</verification>

<success_criteria>
Operator can view, edit, and dynamically see token estimations for prompt templates in the web UI.
</success_criteria>

<output>
After completion, create `.planning/phases/23-ecto-backed-prompt-registry-and-lifecycle/23-04-SUMMARY.md`
</output>