---
phase: 23
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - lib/scoria/prompt_registry/tokenizer.ex
  - test/scoria/prompt_registry/tokenizer_test.exs
autonomous: true
requirements:
  - REG-03
must_haves:
  truths:
    - "System can estimate token counts for string and map structures"
  artifacts:
    - path: "lib/scoria/prompt_registry/tokenizer.ex"
      provides: "Token estimation utility"
    - path: "mix.exs"
      provides: "Tiktoken dependency"
  key_links: []
---

<objective>
Setup the Tiktoken dependency and create the token estimation utility for prompt templates.

Purpose: Accurately estimate token usage for prompt templates to warn operators before they hit context window limits, satisfying requirement REG-03.
Output: Tokenizer utility module and tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-ecto-backed-prompt-registry-and-lifecycle/23-RESEARCH.md
@.planning/phases/23-ecto-backed-prompt-registry-and-lifecycle/23-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Tiktoken Dependency</name>
  <files>mix.exs</files>
  <action>
    Add `{:tiktoken, "~> 0.4.2"}` to the dependencies list in `mix.exs`. Run `mix deps.get` to install the dependency. This provides the Rust NIF bindings for token counting.
  </action>
  <verify>
    <automated>mix deps.get && mix compile</automated>
  </verify>
  <done>Dependency is successfully downloaded and compiled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Tokenizer Utility</name>
  <files>lib/scoria/prompt_registry/tokenizer.ex, test/scoria/prompt_registry/tokenizer_test.exs</files>
  <behavior>
    - Test 1: `estimate_tokens/1` with a simple string returns the correct token count using the default model (e.g. `gpt-4o`).
    - Test 2: `estimate_tokens/1` with a map containing `system_message`, `few_shot_examples` (array of maps or strings), and `user_template` concatenates the content conceptually and returns a safe upper-bound token count.
    - Test 3: handles nil or empty values gracefully without crashing.
  </behavior>
  <action>
    Implement `Scoria.PromptRegistry.Tokenizer`. Create a function `estimate_tokens/1` that takes a structured prompt map (with `:system_message`, `:few_shot_examples`, `:user_template`) or a plain string, and returns an integer representing the estimated token count. Use `Tiktoken.encode/2` (or the appropriate Tiktoken API for the library) under the hood. Handle potential Tiktoken errors gracefully by returning a safe default (like 0) or error tuple.
  </action>
  <verify>
    <automated>mix test test/scoria/prompt_registry/tokenizer_test.exs</automated>
  </verify>
  <done>Tokenizer utility is tested and correctly counts tokens for structured prompt maps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal -> Tiktoken | Input text is passed to Rust NIF. NIF is isolated but large inputs could cause resource consumption. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Denial of Service | Tokenizer | accept | Internal usage only for operator prompt saving. Payload sizes are naturally bounded by HTTP limits. |
</threat_model>

<verification>
mix test test/scoria/prompt_registry/tokenizer_test.exs passes.
</verification>

<success_criteria>
`Scoria.PromptRegistry.Tokenizer.estimate_tokens/1` is available and correctly delegates to `tiktoken`.
</success_criteria>

<output>
After completion, create `.planning/phases/23-ecto-backed-prompt-registry-and-lifecycle/23-01-SUMMARY.md`
</output>