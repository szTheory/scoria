---
phase: 23
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/*_create_ai_prompt_templates.exs
  - lib/scoria/prompt_registry/prompt_template.ex
  - test/scoria/prompt_registry/prompt_template_test.exs
autonomous: true
requirements:
  - REG-01
  - REG-02
must_haves:
  truths:
    - "System can persist prompt templates as Ecto records"
    - "System captures structured prompt fields instead of raw strings"
  artifacts:
    - path: "lib/scoria/prompt_registry/prompt_template.ex"
      provides: "Ecto schema for templates"
    - path: "priv/repo/migrations/*_create_ai_prompt_templates.exs"
      provides: "Table definition"
  key_links:
    - from: "lib/scoria/prompt_registry/prompt_template.ex"
      to: "ai_prompt_templates"
      via: "ecto schema mapping"
      pattern: "schema \"ai_prompt_templates\""
---

<objective>
Create the Ecto schema and database migration for storing versioned, structured prompt templates.

Purpose: Provide a durable backend for prompts with explicit structure (System, Few-Shot, User) rather than raw strings, supporting requirements REG-01 and REG-02.
Output: Migration file, Ecto schema module, and changeset tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-ecto-backed-prompt-registry-and-lifecycle/23-RESEARCH.md
@.planning/phases/23-ecto-backed-prompt-registry-and-lifecycle/23-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate and Implement Migration</name>
  <files>priv/repo/migrations/*_create_ai_prompt_templates.exs</files>
  <action>
    Use `mix ecto.gen.migration create_ai_prompt_templates` to create the migration file. Implement the `change/0` function to create the `ai_prompt_templates` table. Use `:binary_id` for primary key. Include columns: `entity_id` (:binary_id, null: false), `version` (:integer, null: false, default: 1), `is_current` (:boolean, null: false, default: true), `status` (:string, null: false, default: "draft"), `system_message` (:text), `few_shot_examples` (:map), `user_template` (:text), `estimated_tokens` (:integer). Add a unique index on `[:entity_id, :version]`.
  </action>
  <verify>
    <automated>mix ecto.migrate</automated>
  </verify>
  <done>Migration runs successfully and table exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Ecto Schema</name>
  <files>lib/scoria/prompt_registry/prompt_template.ex, test/scoria/prompt_registry/prompt_template_test.exs</files>
  <behavior>
    - Test 1: Changeset requires `entity_id`, `version`, `is_current`, `status`.
    - Test 2: Changeset validates `status` is one of `draft`, `active`, `archived`.
    - Test 3: Changeset successfully casts the structured map fields (`system_message`, `few_shot_examples`, `user_template`, `estimated_tokens`).
  </behavior>
  <action>
    Create the `Scoria.PromptRegistry.PromptTemplate` module. Use `Ecto.Schema` mapped to `ai_prompt_templates`. Define fields matching the migration. Create a `changeset/2` function that casts `[:entity_id, :version, :is_current, :status, :system_message, :few_shot_examples, :user_template, :estimated_tokens]`. Validate inclusion of `:status` in `~w(draft active archived)`. Validate required fields. Add `unique_constraint([:entity_id, :version])`.
  </action>
  <verify>
    <automated>mix test test/scoria/prompt_registry/prompt_template_test.exs</automated>
  </verify>
  <done>Schema parses database rows correctly and changeset validations pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator -> Database | Operator submitting prompt templates to be saved in Postgres. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02 | Tampering | PromptTemplate | mitigate | Use Ecto strong typing and changeset validations to prevent injection. Ensure structured format limits payload shapes. |
</threat_model>

<verification>
mix test test/scoria/prompt_registry/prompt_template_test.exs passes cleanly.
</verification>

<success_criteria>
`ai_prompt_templates` table exists and `PromptTemplate.changeset/2` correctly validates structures.
</success_criteria>

<output>
After completion, create `.planning/phases/23-ecto-backed-prompt-registry-and-lifecycle/23-02-SUMMARY.md`
</output>