---
phase: 29
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["priv/repo/migrations/YYYYMMDDHHMMSS_create_runtime_instances.exs", "lib/scoria/runtime/instance.ex", "lib/scoria/runtime.ex"]
autonomous: true
requirements: ["OUTRIDER-02", "OUTRIDER-06"]

must_haves:
  truths:
    - "System tracks durable runtime identity in Ecto, separating identity from ephemeral presence"
    - "Offline reasons are durably captured when a runtime disconnects"
  artifacts:
    - path: "lib/scoria/runtime/instance.ex"
      provides: "Ecto schema for durable runtime truth"
  key_links:
    - from: "lib/scoria/runtime/instance.ex"
      to: "Scoria.Repo"
      via: "durable Ecto storage"
      pattern: "Scoria.Repo"
---

<objective>
Implement durable Ecto-backed truth for runtime identity and linkage (D-13, D-10).

Purpose: Ensure operators have durable runtime identity and linkage beyond the life of a live connection.
Output: Ecto schema `Scoria.Runtime.Instance` and context functions for tracking first/last seen and offline reasons.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-external-runtime-observability-and-operator-ux/29-CONTEXT.md
@.planning/phases/29-external-runtime-observability-and-operator-ux/29-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Scoria.Runtime.Instance schema and migration</name>
  <files>priv/repo/migrations/*_create_runtime_instances.exs, lib/scoria/runtime/instance.ex, test/scoria/runtime/instance_test.exs</files>
  <behavior>
    - Test 1: Schema inserts successfully with required fields.
  </behavior>
  <action>Create migration and schema for `runtime_instances`. Per D-13, fields must include: `id` (binary_id, primary key), `tenant_id` (string), `host_session_id` (string, nullable), `current_run_id` (string, nullable), `first_seen_at` (utc_datetime), `last_seen_at` (utc_datetime), `terminal_offline_reason` (string, nullable), and `transport_kind` (string). Implement changeset validating required fields.</action>
  <verify>
    <automated>mix test test/scoria/runtime/instance_test.exs</automated>
  </verify>
  <done>Durable schema exists to track runtime lifecycle beyond ephemeral connections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement context functions for runtime registration</name>
  <files>lib/scoria/runtime.ex, test/scoria/runtime_test.exs</files>
  <behavior>
    - Test 1: `register_instance/1` creates or updates the first_seen_at/last_seen_at.
    - Test 2: `mark_offline/2` sets the `terminal_offline_reason` and updates `last_seen_at`.
  </behavior>
  <action>Add functions to `Scoria.Runtime` context to `register_instance(attrs)` (upsert based on id or host_session_id if applicable, updating `last_seen_at` and clearing `terminal_offline_reason`) and `mark_offline(instance_id, reason)` (recording reasons like `transport_closed`, `presence_left` per D-10).</action>
  <verify>
    <automated>mix test test/scoria/runtime_test.exs</automated>
  </verify>
  <done>Context API allows registering runtimes and marking them offline with typed reasons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Ext Agent -> Runtime Context | Identity params must be validated to prevent ID spoofing |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01A | Spoofing | `Scoria.Runtime` | mitigate | Require strong authentication context when updating instance records. |
</threat_model>

<verification>
mix compile --warnings-as-errors
mix test
</verification>

<success_criteria>
Durable runtime truth is successfully established in Ecto, separating permanent identity from transient Phoenix Presence.
</success_criteria>

<output>
After completion, create `.planning/phases/29-external-runtime-observability-and-operator-ux/29-01-SUMMARY.md`
</output>
