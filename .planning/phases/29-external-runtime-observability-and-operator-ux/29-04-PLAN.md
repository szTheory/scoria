---
phase: 29
plan: 04
type: execute
wave: 3
depends_on: ["29-02"]
files_modified: ["lib/scoria_web/components/memory_notebook_component.ex", "lib/scoria_web/live/workflow_live/show.ex"]
autonomous: true
requirements: ["OUTRIDER-07"]

must_haves:
  truths:
    - "Operator can audit the transition from raw events to compacted memory"
    - "Reciprocal links connect workflow to runtime drawer and compaction block to runtime snapshot"
  artifacts:
    - path: "lib/scoria_web/components/memory_notebook_component.ex"
      provides: "Notebook-style audit view of memory compaction"
  key_links:
    - from: "lib/scoria_web/live/workflow_live/show.ex"
      to: "Scoria.Runtime.CompactedMemory"
      via: "Ecto queries fetching memories for the session"
      pattern: "CompactedMemory"
---

<objective>
Build the Compaction Memory Notebook in the Workflow LiveView detail page.

Purpose: Provide an evidence-first, chronological notebook of compacted memory blocks, integrating reciprocal links to runtime context per D-27.
Output: A new `MemoryNotebookComponent` integrated into `WorkflowLive.Show` that lazy-loads and renders memory ranges.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-external-runtime-observability-and-operator-ux/29-CONTEXT.md
@.planning/phases/29-external-runtime-observability-and-operator-ux/29-RESEARCH.md
@.planning/phases/29-external-runtime-observability-and-operator-ux/PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Memory Notebook component</name>
  <files>lib/scoria_web/components/memory_notebook_component.ex, test/scoria_web/components/memory_notebook_component_test.exs</files>
  <behavior>
    - Test 1: Renders sequence ranges and summary text for a memory block.
    - Test 2: Compaction block includes a reciprocal link to runtime presence context.
  </behavior>
  <action>Create the notebook component. **Strict constraint:** use `incident_evidence_component.ex` as the structural analog (per PATTERNS.md). It must display `start_sequence`, `end_sequence`, and the summary text. Per D-27, each compaction block must include a reciprocal link to the exact runtime presence snapshot (e.g., linking back to the Orchestrator runtime drawer with the runtime instance ID).</action>
  <verify>
    <automated>mix test test/scoria_web/components/memory_notebook_component_test.exs</automated>
  </verify>
  <done>Component securely displays bounded memory summaries using the incident_evidence analog pattern.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate Notebook into Workflow Show</name>
  <files>lib/scoria_web/live/workflow_live/show.ex, test/scoria_web/live/workflow_live_test.exs</files>
  <behavior>
    - Test 1: Fetches associated `CompactedMemory` records for the workflow.
    - Test 2: Renders reciprocal link back to runtime card/drawer.
  </behavior>
  <action>Update `WorkflowLive.Show` to fetch memory contexts (from `Scoria.Runtime.CompactedMemory` context). Ensure memory data is fetched iteratively. Use LiveView `assign_async/3` to lazy-load raw events when a block is expanded. Per D-27, render a top-level reciprocal link in the workflow page pointing back to the dashboard's runtime drawer for the associated runtime.</action>
  <verify>
    <automated>mix test test/scoria_web/live/workflow_live_test.exs</automated>
  </verify>
  <done>Operator can load a workflow and see the chronological compaction log with reciprocal links.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Ecto -> UI | Raw events and LLM summaries rendered in the DOM |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04A | Spoofing | `WorkflowLive.Show` | mitigate | Ensure raw text and summaries from Ecto are safely encoded by Phoenix.HTML. |
</threat_model>

<verification>
mix compile --warnings-as-errors
mix test
</verification>

<success_criteria>
An operator viewing an active or completed session can see how raw event ranges were collapsed into summaries chronologically.
</success_criteria>

<output>
After completion, create `.planning/phases/29-external-runtime-observability-and-operator-ux/29-04-SUMMARY.md`
</output>
