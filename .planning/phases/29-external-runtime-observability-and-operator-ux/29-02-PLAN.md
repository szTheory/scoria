---
phase: 29
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified: ["lib/scoria_web/presence.ex", "lib/scoria_web/controllers/mcp_controller.ex", "lib/scoria/application.ex"]
autonomous: true
requirements: ["OUTRIDER-02", "OUTRIDER-06"]

must_haves:
  truths:
    - "System tracks external agent connections using Phoenix Presence keyed by Scoria runtime id"
    - "System captures typed offline reasons when transport drops"
  artifacts:
    - path: "lib/scoria_web/presence.ex"
      provides: "Phoenix Presence module for external runtimes"
    - path: "lib/scoria_web/controllers/mcp_controller.ex"
      provides: "Agent attachment tracker"
  key_links:
    - from: "lib/scoria_web/controllers/mcp_controller.ex"
      to: "Scoria.Runtime.Instance"
      via: "upserting durable truth before tracking presence"
      pattern: "Scoria.Runtime"
---

<objective>
Setup Phoenix Presence tracking for external runtime SSE connections, securely binding ephemeral state to the new durable Ecto truth.

Purpose: Provide the real-time foundation for runtime observability without confusing ephemeral transport state with durable Ecto truth.
Output: Configured `ScoriaWeb.Presence` module, updated supervision tree, and presence tracking in `MCPController.sse/2`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-external-runtime-observability-and-operator-ux/29-CONTEXT.md
@.planning/phases/29-external-runtime-observability-and-operator-ux/29-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Setup Phoenix Presence module and supervision</name>
  <files>lib/scoria_web/presence.ex, lib/scoria/application.ex, test/scoria_web/presence_test.exs</files>
  <behavior>
    - Test 1: Presence module starts successfully under supervisor.
  </behavior>
  <action>Create `ScoriaWeb.Presence` using `use Phoenix.Presence, otp_app: :scoria, pubsub_server: Scoria.PubSub`. Add `ScoriaWeb.Presence` to the children list in `Scoria.Application.start/2`.</action>
  <verify>
    <automated>mix test test/scoria_web/presence_test.exs</automated>
  </verify>
  <done>Presence module is defined and supervised.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Track MCP connections and terminal reasons</name>
  <files>lib/scoria_web/controllers/mcp_controller.ex, test/scoria_web/controllers/mcp_controller_test.exs</files>
  <behavior>
    - Test 1: SSE connection registers the Scoria runtime instance ID in Presence, not the session_id.
    - Test 2: Controller traps exit/disconnect and records `transport_closed` as the terminal reason in Ecto.
  </behavior>
  <action>Update `ScoriaWeb.MCPController.sse/2`. First, call `Scoria.Runtime.register_instance/1` to establish durable truth. The returned `instance.id` MUST be used as the tracking key for `ScoriaWeb.Presence.track` (per D-12, do not use `session_id` as the presence key). Track minimal metadata (status: "connected"). Ensure the process traps exits or uses `Plug.Conn.register_before_send` / connection monitors to call `Scoria.Runtime.mark_offline(instance.id, "transport_closed")` when the SSE stream ends (D-10).</action>
  <verify>
    <automated>mix test test/scoria_web/controllers/mcp_controller_test.exs</automated>
  </verify>
  <done>Active SSE connections reflect in Presence and capture typed terminal reasons on disconnect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Ext Agent -> MCP Controller | Agent metadata tracked in Presence must be sanitized |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02A | Denial of Service | `ScoriaWeb.Presence` | mitigate | Ensure tracked metadata is bounded. |
</threat_model>

<verification>
mix compile --warnings-as-errors
mix test
</verification>

<success_criteria>
Phoenix Presence tracks agents using the correct Scoria runtime ID, and disconnects record typed reasons.
</success_criteria>

<output>
After completion, create `.planning/phases/29-external-runtime-observability-and-operator-ux/29-02-SUMMARY.md`
</output>
