---
phase: 29
plan: 03
type: execute
wave: 3
depends_on: ["29-02"]
files_modified: ["lib/scoria_web/components/runtime_detail_drawer_component.ex", "lib/scoria_web/live/orchestrator_live.ex"]
autonomous: true
requirements: ["OUTRIDER-02", "OUTRIDER-06"]

must_haves:
  truths:
    - "Dashboard operator view shows a compact rail of connected external runtimes"
    - "Runtime cards include reciprocal links to linked workflows"
    - "Offline runtimes display typed offline reasons"
  artifacts:
    - path: "lib/scoria_web/components/runtime_detail_drawer_component.ex"
      provides: "Compact card/drawer rendering for runtime presence"
  key_links:
    - from: "lib/scoria_web/live/orchestrator_live.ex"
      to: "ScoriaWeb.Presence"
      via: "PubSub subscription and track/list"
      pattern: "Phoenix.PubSub.subscribe"
---

<objective>
Implement the real-time External Runtime Posture rail and detail drawer on the main Orchestrator dashboard.

Purpose: Allow operators to view attached agents and their online/offline state at a glance, aligned with Phase 29 UI constraints (compact rail). Provide reciprocal links to related workflow truth.
Output: LiveView Presence subscription in `OrchestratorLive` and `RuntimeDetailDrawerComponent` rendering presence cards.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-external-runtime-observability-and-operator-ux/29-CONTEXT.md
@.planning/phases/29-external-runtime-observability-and-operator-ux/29-RESEARCH.md
@.planning/phases/29-external-runtime-observability-and-operator-ux/29-UI-SPEC.md
</context>

<interfaces>
From lib/scoria_web/presence.ex:
```elixir
defmodule ScoriaWeb.Presence do
  use Phoenix.Presence, otp_app: :scoria, pubsub_server: Scoria.PubSub
end
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Runtime Detail Drawer component</name>
  <files>lib/scoria_web/components/runtime_detail_drawer_component.ex, test/scoria_web/components/runtime_detail_drawer_component_test.exs</files>
  <behavior>
    - Test 1: Renders runtime ID, status chips, and typed offline reason.
    - Test 2: Renders reciprocal hyperlink to the active workflow/session.
  </behavior>
  <action>Create `RuntimeDetailDrawerComponent` for a compact rail/drawer of runtimes (modeled precisely on `connector_detail_drawer_component.ex`). Per D-10, if the status is offline, render the `terminal_offline_reason` from the associated Ecto record. Per D-27, include a mandatory reciprocal `Phoenix.Component.link/1` pointing to the associated workflow/session page (`/workflows/:id`) when `current_run_id` is present.</action>
  <verify>
    <automated>mix test test/scoria_web/components/runtime_detail_drawer_component_test.exs</automated>
  </verify>
  <done>Component correctly renders empty/populated states, offline reasons, and workflow links.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate Presence into Orchestrator LiveView</name>
  <files>lib/scoria_web/live/orchestrator_live.ex, test/scoria_web/live/orchestrator_live_test.exs</files>
  <behavior>
    - Test 1: Subscribes to the runtime presence PubSub topic on mount.
    - Test 2: Merges Presence data with durable Ecto truth to assign `runtimes`.
  </behavior>
  <action>Update `OrchestratorLive.mount/3` to subscribe to Presence topic. Implement `handle_info` for `"presence_diff"`. Fetch durable `Scoria.Runtime.Instance` records for known keys to augment ephemeral presence (giving access to `terminal_offline_reason` and `current_run_id`). Render the `RuntimeDetailDrawerComponent` adjacent to the connector posture section.</action>
  <verify>
    <automated>mix test test/scoria_web/live/orchestrator_live_test.exs</automated>
  </verify>
  <done>Dashboard accurately reflects live presence and updates instantly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| LiveView -> Browser | Data pushed via WebSockets to operator dashboard |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03A | Information Disclosure | `OrchestratorLive` | mitigate | Presence payloads must be scoped to authorized tenants. |
</threat_model>

<verification>
mix compile --warnings-as-errors
mix test
</verification>

<success_criteria>
Dashboard displays a compact rail of connected runtimes with offline reasons and reciprocal workflow links.
</success_criteria>

<output>
After completion, create `.planning/phases/29-external-runtime-observability-and-operator-ux/29-03-SUMMARY.md`
</output>
