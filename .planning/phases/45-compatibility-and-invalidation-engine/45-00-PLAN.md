---
phase: 45-compatibility-and-invalidation-engine
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - mix.exs
  - config/test.exs
autonomous: true
requirements:
  - LOOK-01
must_haves:
  truths:
    - "Automated verification for Phase 45 runs against the live `5432` pgvector test database with compile-time and runtime repo config aligned."
    - "Executors do not trust semantic-cache test output until the repo has been cleanly recompiled for `SCORIA_DB_PORT=5432`."
  artifacts:
    - path: "config/test.exs"
      provides: "Test repo configuration surface the executor must verify against the chosen DB port"
    - path: "mix.exs"
      provides: "Compile surface the executor must rebuild after switching the repo port"
  key_links:
    - from: "config/test.exs"
      to: "Scoria.Repo"
      via: "runtime DB port configuration"
      pattern: "Scoria\\.Repo|hostname|port"
    - from: "mix.exs"
      to: "MIX_ENV=test"
      via: "clean recompile before verification"
      pattern: "project|application"
---

<objective>
Establish the Wave 0 verification prerequisite for Phase 45 by aligning test compilation and runtime execution to the live `5432` pgvector database path before any semantic-cache tests are trusted.

Purpose: the repo is currently compiled for `55432`, but the locked execution strategy for this revision is the live `5432` database path. Every later verify command depends on this alignment step completing first.
Output: a clean `MIX_ENV=test` recompile against `SCORIA_DB_PORT=5432` plus a proof command showing the runtime/compile mismatch is gone.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-compatibility-and-invalidation-engine/45-RESEARCH.md
@.planning/phases/45-compatibility-and-invalidation-engine/45-VALIDATION.md
@config/test.exs
@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recompile test artifacts against the live 5432 pgvector database</name>
  <files>mix.exs, config/test.exs</files>
  <read_first>
    <file>mix.exs</file>
    <file>config/test.exs</file>
    <file>.planning/phases/45-compatibility-and-invalidation-engine/45-RESEARCH.md</file>
    <file>.planning/phases/45-compatibility-and-invalidation-engine/45-VALIDATION.md</file>
  </read_first>
  <action>Use the locked DB-alignment strategy from the revision context: the live `5432` database path. Before any Phase 45 tests run, verify `localhost:5432` is reachable and has the `vector` extension, then run `mix clean` followed by `SCORIA_DB_PORT=5432 MIX_ENV=test mix compile`. If `config/test.exs` or related repo config already honors `SCORIA_DB_PORT`, do not edit it; only patch those files if the executor finds a hardcoded test-port assumption that prevents the clean recompile from binding to `5432`. After recompiling, run a targeted proof command to confirm the previous `different value set for key Scoria.Repo during runtime compared to compile time` failure is gone. All later Phase 45 verify commands assume this Wave 0 step already succeeded.</action>
  <acceptance_criteria>
    <criterion>`SCORIA_DB_PORT=5432 MIX_ENV=test mix compile` exits 0</criterion>
    <criterion>`SCORIA_DB_PORT=5432 MIX_ENV=test mix test test/scoria/semantic_cache_test.exs --max-cases 1` exits 0 or fails for a Phase 45 code assertion, not for compile/runtime repo-port mismatch</criterion>
    <criterion>No command output includes `different value set for key Scoria.Repo during runtime compared to compile time` after the clean recompile</criterion>
    <criterion>`config/test.exs` is only modified if a hardcoded port assumption blocked the `5432` strategy</criterion>
  </acceptance_criteria>
  <verify>
    <automated>SCORIA_DB_PORT=5432 MIX_ENV=test mix clean && SCORIA_DB_PORT=5432 MIX_ENV=test mix compile && SCORIA_DB_PORT=5432 MIX_ENV=test mix test test/scoria/semantic_cache_test.exs --max-cases 1</automated>
  </verify>
  <done>Phase 45 has a concrete Wave 0 prerequisite: the repo is recompiled and testable against the live `5432` pgvector database path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runtime -> database | Verification commands cross from local test execution into the live `5432` semantic-cache test database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-00-01 | Tampering | verification environment | mitigate | Clean recompile against `SCORIA_DB_PORT=5432` before trusting any semantic-cache lane output. |
| T-45-00-02 | Repudiation | test evidence | mitigate | Use an explicit proof command that fails if compile-time and runtime repo ports still differ. |
</threat_model>

<verification>
- `mix clean` and `SCORIA_DB_PORT=5432 MIX_ENV=test mix compile` complete successfully.
- A targeted ExUnit command runs without the prior compile/runtime repo-port mismatch.
</verification>

<success_criteria>
- Later Phase 45 verify commands have a real prerequisite instead of an unresolved environment caveat.
- The phase now follows the locked `5432` strategy from the revision context.
</success_criteria>

<output>
After completion, create `.planning/phases/45-compatibility-and-invalidation-engine/45-00-SUMMARY.md`
</output>
