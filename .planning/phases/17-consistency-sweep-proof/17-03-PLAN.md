---
phase: 17-consistency-sweep-proof
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria_web/ui.ex
  - docs/MAINTAINERS.md
autonomous: true
requirements: [PROOF-03]
must_haves:
  truths:
    - "mix docs renders a drift-free component catalog for ScoriaWeb.UI by construction (SSOT in code, not parallel prose)"
    - "The 7 sparse single-liner components have multi-line @doc strings with usage context, attrs, and slots"
    - "docs/MAINTAINERS.md has a Design-system component catalog entry-point section pointing to mix docs"
    - "The existing harness section in MAINTAINERS.md stays accurate"
  artifacts:
    - path: "lib/scoria_web/ui.ex"
      provides: "Backfilled @doc strings for the 7 sparse components (catalog SSOT)"
      contains: "@moduledoc"
    - path: "docs/MAINTAINERS.md"
      provides: "Design-system component catalog section (components-at-a-glance table + mix docs pointer + DS-06 note)"
      contains: "Design-system component catalog"
  key_links:
    - from: "docs/MAINTAINERS.md"
      to: "lib/scoria_web/ui.ex"
      via: "mix docs / ExDoc reference (catalog SSOT)"
      pattern: "mix docs"
    - from: "docs/MAINTAINERS.md"
      to: "test/scoria_web/ds06_drift_guard_test.exs"
      via: "raw-palette drift protection note"
      pattern: "ds06_drift_guard_test"
---

<objective>
Close PROOF-03: make `mix docs` (ExDoc, already wired `~> 0.38`) render a drift-free `ScoriaWeb.UI` component catalog by construction (D-09), then add a concise "Design-system component catalog" entry-point section to `docs/MAINTAINERS.md` (D-10) and verify the existing harness section stays accurate (D-11). The code change is bounded: backfill the `ui.ex` `@moduledoc` (already adequate — verify only) and expand the 7 sparse single-liner `@doc`s — `attention_card/1`, `evidence_section/1`, `evidence_rows/1`, `eyebrow/1`, `kbd/1`, `evidence_action_row/1`, `evidence_empty/1` — leaving the ~23 already-adequate `@doc`s untouched. Do NOT create a standalone `docs/design_system.md` (rejected — would drift).

Purpose: PROOF-03 names `docs/MAINTAINERS.md` as the catalog entry-point and requires the design-system catalog + harness usage be documented; the SSOT must live in code (`ui.ex` attr/slot/@doc via `mix docs`), not parallel prose that drifts.
Output: `lib/scoria_web/ui.ex` (7 expanded @docs), `docs/MAINTAINERS.md` (catalog section).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-consistency-sweep-proof/17-CONTEXT.md
@.planning/phases/17-consistency-sweep-proof/17-RESEARCH.md
@.planning/phases/17-consistency-sweep-proof/17-PATTERNS.md

# The file being modified + its in-file analogs (rich @docs to mirror):
@lib/scoria_web/ui.ex

# Cross-check slot/attr semantics before expanding @docs (Pitfall 5):
@.planning/phases/12-design-system-component-layer/12-UI-SPEC.md

# Catalog entry-point target + harness-section pattern + ExDoc wiring:
@docs/MAINTAINERS.md
@mix.exs

# Raw-palette drift protection cited in the catalog section:
@test/scoria_web/ds06_drift_guard_test.exs

# Brand voice for doc copy:
@brandbook/brand-book.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill the 7 sparse @doc strings in lib/scoria_web/ui.ex</name>
  <files>lib/scoria_web/ui.ex</files>
  <read_first>
    - lib/scoria_web/ui.ex — the RICH multi-line @docs to mirror: modal/1 (~line 421), drawer/1 (~474), field/1 (~530), toast/1 (~596), id/1 (~159), notebook/1 (~695), table/1 (~904); AND the 7 sparse targets to expand: eyebrow/1 (~105), attention_card/1 (~186), kbd/1, evidence_section/1 (~803), evidence_rows/1 (~830), evidence_action_row/1 (~848), evidence_empty/1 (~862). Read each target's `attr`/`slot` declarations directly above the function so the expanded @doc matches the real contract.
    - .planning/phases/17-consistency-sweep-proof/17-PATTERNS.md §ui.ex (the exact pattern-to-copy @docs and the per-component expansion guidance: what each of the 7 must cover)
    - .planning/phases/17-consistency-sweep-proof/17-RESEARCH.md §Component Catalog via ExDoc (Backfill Priority Tiers 1/2; the @moduledoc is already adequate — Tier 3 = no change)
    - .planning/phases/12-design-system-component-layer/12-UI-SPEC.md (Pitfall 5 — cross-check slot/attr semantics, especially notebook-scoped evidence_section/evidence_rows, before describing slot behavior; do NOT claim a slot is optional when it is required: true)
  </read_first>
  <action>
    Expand ONLY the 7 sparse single-liner @docs to multi-line @docs that ExDoc renders as a complete catalog entry, mirroring the rich-@doc style of `modal/1`/`field/1`/`toast/1`/`id/1` (state purpose, attrs, slots, usage context). Tier 1 (full multi-line): `attention_card/1` — purpose (Status Home attention strip), attrs (count, label, detail, cta, path, tone), and that it renders an `<a>` (callers pass a path, not a click handler); `evidence_section/1` — usage inside `<.notebook>` `:tab` slot panels, attrs (title, description, tone, badge), `:actions` slot, `:inner_block` for evidence rows/action rows; `evidence_rows/1` — the `:rows` input format (list of `%{label:, value:}` maps or `{label, value}` tuples, normalized by `normalize_evidence_rows/1`) and that it renders a `<dl>` of `<dt>/<dd>` pairs. Tier 2 (light context addition): `eyebrow/1` — used in panel/object headers + card hierarchy labeling; `kbd/1` — used inline in command-palette rows and help text; `evidence_action_row/1` — used for per-section action links (e.g. "View trace", "Open replay") in evidence panels; `evidence_empty/1` — used for empty `:tab` panels in `<.notebook>` when no evidence data is available (`:title` required). Do NOT touch the ~23 already-adequate @docs (RESEARCH Tier 3) and do NOT modify the `@moduledoc` (already complete — verify only). Add usage context, NOT invented specifications (Pitfall 5 anti-pattern). No raw-palette classes introduced (DS-06 guard will catch any). Brand voice: calm, exact, useful.
  </action>
  <verify>
    <automated>mix docs && test -f doc/ScoriaWeb.UI.html && mix test test/scoria_web/ds06_drift_guard_test.exs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `mix docs` exits 0 and produces `doc/ScoriaWeb.UI.html`
    - Each of the 7 components (`attention_card`, `evidence_section`, `evidence_rows`, `eyebrow`, `kbd`, `evidence_action_row`, `evidence_empty`) now has a multi-line `@doc` (its @doc string spans more than one line in `lib/scoria_web/ui.ex`)
    - `mix test test/scoria_web/ds06_drift_guard_test.exs` exits 0 (no raw palette class introduced by the edit)
    - The `@moduledoc` is unchanged (still present, lines ~2–11)
    - The ~23 already-adequate @docs are unchanged (only the 7 named functions' @docs differ in `git diff`)
  </acceptance_criteria>
  <done>The 7 sparse @docs expanded to multi-line catalog entries; `mix docs` renders cleanly; DS-06 green; moduledoc + other @docs untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Design-system component catalog section to docs/MAINTAINERS.md</name>
  <files>docs/MAINTAINERS.md</files>
  <read_first>
    - docs/MAINTAINERS.md (the existing "Screenshot + Critique Harness (dev-only)" section ~line 161–243 — the section anatomy to model the new section on; verify the harness section stays accurate, D-11)
    - .planning/phases/17-consistency-sweep-proof/17-PATTERNS.md §MAINTAINERS.md (the exact "Design-system component catalog" section to add: intro framing, components-at-a-glance table covering all public ui.ex components, the `MIX_ENV=dev mix docs` pointer, the raw-palette drift-protection note)
    - .planning/phases/17-consistency-sweep-proof/17-RESEARCH.md §MAINTAINERS.md + DS-06 Citation (component inventory, doc/ output location, the three DS-06 assertions)
    - lib/scoria_web/ui.ex (confirm the component list in the at-a-glance table matches the actual public functions after Task 1)
  </read_first>
  <action>
    Add a `## Design-system component catalog` section to `docs/MAINTAINERS.md` (D-10) modeled on the existing harness-section anatomy, per 17-PATTERNS.md §MAINTAINERS.md. Content: (1) a brief statement that `ScoriaWeb.UI` is the single enforced token gateway (every component emits brand-book semantic classes driven by tokens; raw palette classes are blocked in `ui.ex` by the DS-06 guard); (2) a "Components at a glance" table listing the shared components with one-line purposes (badge, button, eyebrow, panel, metric, id, attention_card, object_header, stub_page, kbd, command_palette, empty_state, modal, drawer, field, form_section, skeleton, toast, notebook, raw_evidence, evidence_section, evidence_rows, evidence_action_row, evidence_empty, table, flash_group, plus the `tone/1` and `status_label/1` utilities) — the table is a glance index, NOT the SSOT; (3) a "Full attribute/slot reference" subsection with the `MIX_ENV=dev mix docs` command and a note the rendered catalog lives in `doc/` (gitignored standard ExDoc output) — open `doc/ScoriaWeb.UI.html`; (4) a "Raw-palette drift protection" subsection with `mix test test/scoria_web/ds06_drift_guard_test.exs` and a note that the three assertions guard zero-tolerance and `test/support/ds06_baseline.txt` is empty. Placement: immediately before or after the existing "Screenshot + Critique Harness" section — both must remain reachable from `docs/MAINTAINERS.md` (CONTEXT discretion). Do NOT create `docs/design_system.md` (rejected). Do NOT rewrite the harness section — only verify it stays accurate (the contact-sheet subsection is owned by Plan 02; this plan must NOT add it, to avoid a same-file collision). Brand voice: calm, exact, useful.
  </action>
  <verify>
    <automated>grep -q "Design-system component catalog" docs/MAINTAINERS.md && grep -q "mix docs" docs/MAINTAINERS.md && grep -q "ds06_drift_guard_test" docs/MAINTAINERS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/MAINTAINERS.md` contains a `## Design-system component catalog` section (grep finds the heading)
    - The section contains a components-at-a-glance table and the `MIX_ENV=dev mix docs` command
    - The section names `test/scoria_web/ds06_drift_guard_test.exs` for raw-palette drift protection
    - `docs/design_system.md` does NOT exist (`test ! -f docs/design_system.md`)
    - The existing "Screenshot + Critique Harness" section heading is still present and unmodified by this plan (Plan 02 owns its contact-sheet subsection)
  </acceptance_criteria>
  <done>MAINTAINERS.md has the catalog entry-point section pointing to `mix docs` + the DS-06 note; no standalone design_system.md; harness section intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev tooling → committed repo | `@doc` strings (documentation only) + a Markdown doc section; no runtime behavior change, no user input, no network surface |
| Hex package boundary | `ui.ex` @docs ship with the package (they are documentation, intended); `docs/MAINTAINERS.md` is in ExDoc extras — no secrets involved |

Dev-only documentation plan: no new routes, no backend capability, no auth surface, no committed binaries, no new dependency, no runtime code change (only `@doc` attributes). Minimal threat surface. The DS-06 guard (run in Task 1 verify) ensures the doc edit introduces no raw-palette regression.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06 | Tampering | `lib/scoria_web/ui.ex` (token gateway) | mitigate | Task 1 verify runs `mix test test/scoria_web/ds06_drift_guard_test.exs` — the @doc backfill cannot introduce raw-palette classes without failing the build |
| T-17-07 | Information Disclosure | `docs/MAINTAINERS.md` catalog section | accept | Section documents component names + `mix docs` usage only; no secrets, no env values; low risk |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No package installs — ExDoc `~> 0.38` already a dev dep; `mix.exs` and lockfile untouched |
</threat_model>

<verification>
- `mix docs` exits 0 and produces `doc/ScoriaWeb.UI.html`
- `mix test test/scoria_web/ds06_drift_guard_test.exs` exits 0
- `docs/MAINTAINERS.md` has the `Design-system component catalog` section pointing to `mix docs` and the DS-06 guard
- `docs/design_system.md` does NOT exist
- `git diff` on `ui.ex` touches only the 7 named functions' @docs (moduledoc + other @docs untouched)
</verification>

<success_criteria>
PROOF-03 closed: `mix docs` renders a drift-free `ScoriaWeb.UI` catalog by construction; `docs/MAINTAINERS.md` has a catalog entry-point section reaching the full `mix docs` reference and the DS-06 guard; the existing harness usage stays accurate; no standalone design-system doc created.
</success_criteria>

<output>
Create `.planning/phases/17-consistency-sweep-proof/17-03-SUMMARY.md` when done.
</output>
