---
phase: 17-consistency-sweep-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/shots/2026-06-13/
  - priv/shots/gap_register_final.md
autonomous: true
requirements: [PROOF-01]
must_haves:
  truths:
    - "A maintainer can read one committed report showing the baseline → final rubric delta for all 9 audited screens"
    - "Each of the 11 baseline P1 findings has a deterministic, no-API-key resolution row naming the resolving phase and concrete evidence"
    - "The report asserts raw-color count is 0 by citing the existing DS-06 guard, not a new counting script"
    - "The committed 2026-06-04 baseline gap_register.md is NOT overwritten by the final critique re-run"
  artifacts:
    - path: "priv/shots/gap_register_final.md"
      provides: "Final audit report: per-screen rubric delta + 11-P1 checklist + raw-color-zero citation"
      contains: "anthropic:claude-sonnet-4-5"
    - path: "priv/shots/2026-06-13/"
      provides: "Fresh seed-first 'final' screenshot set (the 'after' side for PROOF-02)"
  key_links:
    - from: "priv/shots/gap_register_final.md"
      to: "test/scoria_web/ds06_drift_guard_test.exs"
      via: "raw-color-zero citation (D-13)"
      pattern: "ds06_drift_guard_test"
    - from: "priv/shots/gap_register_final.md"
      to: "priv/shots/gap_register.md"
      via: "baseline reference"
      pattern: "gap_register\\.md \\(2026-06-04\\)"
---

<objective>
Produce the ONE committed final-audit report `priv/shots/gap_register_final.md` (D-12) that closes PROOF-01. It holds three sections: (1) a per-screen baseline→final rubric delta table over the SAME 9 screens as the 2026-06-04 baseline, (2) the deterministic 11-P1 resolution checklist mapping each baseline P1 → resolving phase + concrete evidence (D-04, re-verifiable with NO API key), and (3) a raw-color-zero section that CITES the existing DS-06 guard (D-13). This plan also owns the shared "capture a fresh seed-first final shot set" step (D-08) that Plan 02 (contact sheets) consumes.

Purpose: PROOF-01 requires a measurable, method-matched improvement record and a falsifiable raw-color-zero claim. The deterministic P1 checklist — not the non-deterministic LLM scores — is the proof a future maintainer re-verifies.
Output: `priv/shots/gap_register_final.md` (committed); a fresh dated shot dir under `priv/shots/{final-date}/` (gitignored).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-consistency-sweep-proof/17-CONTEXT.md
@.planning/phases/17-consistency-sweep-proof/17-RESEARCH.md
@.planning/phases/17-consistency-sweep-proof/17-PATTERNS.md

# Authoring source-of-truth (the analog to mirror):
@priv/shots/gap_register.md

# Harness invocation chain + model id + screen list:
@lib/mix/tasks/scoria.ui.shots.ex
@lib/scoria/ui_critique.ex

# Raw-color-zero citation target (D-13):
@test/scoria_web/ds06_drift_guard_test.exs
@test/support/ds06_baseline.txt

# Brand voice for the report copy:
@brandbook/brand-book.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture fresh seed-first final shot set + re-run LLM critique</name>
  <files>priv/shots/2026-06-13/ (final dated dir)</files>
  <read_first>
    - lib/mix/tasks/scoria.ui.shots.ex (invocation chain, @screens list line ~56, --skip-shots flag, render_gap_register output path)
    - lib/scoria/ui_critique.ex (@default_model line ~11 = anthropic:claude-sonnet-4-5, critique_screen/3, populated_dark_desktop canonical state)
    - .planning/phases/17-consistency-sweep-proof/17-RESEARCH.md §Harness Re-Run Mechanics + Pitfall 1 (gap_register.md overwrite)
    - priv/shots/.gitignore (confirm dated dirs + *.png + *.json stay gitignored)
  </read_first>
  <action>
    Capture the "after" side of the audit using the EXISTING harness UNCHANGED (D-02 method-match — do NOT invent a new rubric or task). Sequence: `mix run priv/repo/dev_seed.exs` (idempotent seed) → `mix phx.server` in a separate terminal → `mix scoria.ui.shots` (screenshot pass, writes `priv/shots/{Date.utc_today()}/{screen}/*.png`) → `mix scoria.ui.shots --skip-shots --critique` (critique pass over the 9 screens in `@screens`: live_ops approvals workflows incidents connectors reviews eval_specs prompts prompt_release; canonical state populated_dark_desktop; writes per-screen `populated_dark_desktop.json` under the dated dir). Use the existing `@default_model` `anthropic:claude-sonnet-4-5` — do NOT override `--model` (D-02/D-03). The dated dir name is `{final-date}` = the UTC date the capture runs (e.g. 2026-06-13); record the actual value for downstream use. CRITICAL (Pitfall 1, RESEARCH finding #1): the critique pass auto-overwrites `priv/shots/gap_register.md` — the committed 2026-06-04 baseline. Immediately after the critique run, restore the baseline with `git checkout priv/shots/gap_register.md` so the committed baseline is byte-identical to its pre-run state. The auto-written `gap_register.md` content is consumed only as the data source for the final report (Task 2), never committed over the baseline. If `ANTHROPIC_API_KEY` is unavailable, the critique scores cannot be produced — the deterministic P1 checklist (Task 2) and the DS-06 citation are API-key-free and remain the falsifiable proof; in that case capture screenshots only and note the LLM-score columns as "not re-run (no API key)" in Task 2.
  </action>
  <verify>
    <automated>test -d priv/shots/$(date -u +%Y-%m-%d)/live_ops && test -f priv/shots/$(date -u +%Y-%m-%d)/prompts/populated_dark_desktop.png && git diff --quiet priv/shots/gap_register.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - A dated dir `priv/shots/{final-date}/` exists on disk with one subdir per screen and `populated_dark_desktop.png` present for at least the 9 audited screens
    - `git diff --quiet priv/shots/gap_register.md` exits 0 (committed 2026-06-04 baseline was NOT overwritten)
    - The dated dir is gitignored: `git status --porcelain priv/shots/{final-date}/` shows no tracked entries
    - If critique ran: per-screen `populated_dark_desktop.json` files exist under the dated dir for the audited screens
  </acceptance_criteria>
  <done>Fresh seed-first final shot set captured under a dated dir; baseline gap_register.md intact; per-screen critique JSON available (or API-key absence noted for Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Author priv/shots/gap_register_final.md (delta + 11-P1 checklist + raw-color-zero citation)</name>
  <files>priv/shots/gap_register_final.md</files>
  <read_first>
    - priv/shots/gap_register.md (the exact analog: header, "Ranked Findings" format `### {screen} — {dimension}: {score}/5`, Fix Backlog table — D-12 mirrors this file)
    - .planning/phases/17-consistency-sweep-proof/17-PATTERNS.md §gap_register_final.md (header, delta table, P1 checklist, raw-color-zero section — exact layouts to copy)
    - .planning/phases/17-consistency-sweep-proof/17-RESEARCH.md §Baseline Artifact Structure (the 11 P1 findings verbatim + resolving phase + concrete evidence table) and §DS-06 Raw-Color-Zero Citation (exact citation text)
    - test/scoria_web/ds06_drift_guard_test.exs (the three assertion names to name in the citation) + test/support/ds06_baseline.txt (confirm empty)
    - The per-screen JSON written in Task 1 under priv/shots/{final-date}/{screen}/populated_dark_desktop.json (the final-score data source)
    - brandbook/brand-book.md (calm/exact/useful voice for the prose)
  </read_first>
  <action>
    Hand-author `priv/shots/gap_register_final.md` (D-12 — ONE committed file; do NOT split across dirs; do NOT let the harness write it). Header: title `# Design-System Gap Register — Final {final-date}`, then `**Run date:** {final-date}`, `**Model:** anthropic:claude-sonnet-4-5` (D-03 stamp the exact model id), `**Baseline:** priv/shots/gap_register.md (2026-06-04)`, `**Final shots:** priv/shots/{final-date}/`, and a Summary block (Baseline 0 P0 / 11 P1 / 71 passing → Final 0 P0 / N P1 / M passing → net improvement). Section 1 — `## Per-Screen Rubric Delta`: a table with columns Screen | Dimension | Baseline | Final | Change, covering all 9 baseline screens — the 11 baseline P1 rows MUST appear (their baseline scores are 2/5 each from gap_register.md); pull each Final score from the Task 1 per-screen JSON. The table MUST cover the SAME 9 screens the baseline covered so the delta is complete, not cherry-picked (CONTEXT discretion note). If the LLM pass did not run (no API key), fill the Final column with "not re-run (no API key)" and lean on Section 2 as the proof. Section 2 — `## Deterministic 11-P1 Resolution Checklist` with the lead line "This checklist requires no API key. Each item is verifiable by code review or `mix test`." Then 11 checklist rows `- [x] P1-NN {screen} / {dimension}: ...` each naming the resolving phase + concrete evidence (component/CSS/test) taken EXACTLY from the RESEARCH §Baseline Artifact Structure table (P1-01 flash→Phase 12; P1-02 connectors density→Phase 16; P1-03/05 eval_specs a11y/responsive→Phase 16; P1-04 eval_specs density→Phase 14; P1-06/08 prompt_release a11y/responsive→Phase 16; P1-07 prompt_release density→Phase 14; P1-09/11 prompts a11y/responsive→Phase 16; P1-10 prompts density→Phase 14). Per RESEARCH Open Question #3, each row proves the WORK was done (cite phase/plan/component); do NOT claim the LLM necessarily scored the dimension higher (non-determinism, D-03). Section 3 — `## Raw-Color-Zero Assertion` copied verbatim from RESEARCH §DS-06 Raw-Color-Zero Citation: state "Raw palette class count: **0**", name `test/scoria_web/ds06_drift_guard_test.exs` and its three assertions, note `test/support/ds06_baseline.txt` is empty (zero headroom), and give the run command `mix test test/scoria_web/ds06_drift_guard_test.exs` (D-13 — cite the existing guard; do NOT add a new counting script). Note the known empty-state limitation (reviews/eval_specs/prompts/prompt_release are populated-only) so missing empty-state shots are not misread as a gap (CONTEXT specifics). Voice: calm, exact, useful — no hype, no anthropomorphism. Ensure NO API key, tenant PII, or secret is written into the file (only the seeded tenant name `acme-corp` and the model id are acceptable).
  </action>
  <verify>
    <automated>mix test test/scoria_web/ds06_drift_guard_test.exs && grep -q "anthropic:claude-sonnet-4-5" priv/shots/gap_register_final.md && grep -q "ds06_drift_guard_test.exs" priv/shots/gap_register_final.md && [ $(grep -c "P1-" priv/shots/gap_register_final.md) -ge 11 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `priv/shots/gap_register_final.md` exists and contains `anthropic:claude-sonnet-4-5` (D-03 model stamp)
    - File contains a `## Per-Screen Rubric Delta` table with a row for each of the 11 baseline P1s and the table covers all 9 baseline screens
    - File contains `## Deterministic 11-P1 Resolution Checklist` with at least 11 `P1-` rows, each naming a resolving phase
    - File contains `## Raw-Color-Zero Assertion` naming `test/scoria_web/ds06_drift_guard_test.exs` and `test/support/ds06_baseline.txt`
    - `mix test test/scoria_web/ds06_drift_guard_test.exs` exits 0 (raw-color = 0 still guarded — the cited claim is true)
    - File header references `priv/shots/gap_register.md (2026-06-04)` as the baseline
    - No API key / secret / tenant PII string appears in the file (grep for `KEY`, `sk-`, `ANTHROPIC` returns nothing beyond the env-var name in prose if mentioned)
  </acceptance_criteria>
  <done>`gap_register_final.md` committed with all three sections; DS-06 guard green; baseline untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev tooling → committed repo | Hand-authored report + gitignored shot dir; no runtime, no user input, no network-facing surface |
| critique pass → Anthropic API | Existing ReqLLM + ANTHROPIC_API_KEY path (unchanged); read-only screenshot upload |

This is a dev-only proof/documentation plan: no new routes, no new backend capability, no auth surface, no committed binaries, no new dependency. The realistic threat surface is minimal — the only genuine hygiene item is ensuring no secret / API key / tenant PII leaks into the committed `.md` artifact or the dated dir names.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information Disclosure | `priv/shots/gap_register_final.md` | mitigate | Task 2 acceptance criteria assert no API key / `sk-` / `ANTHROPIC` secret / tenant PII is written into the committed report; only the model id and seeded `acme-corp` tenant name are permitted |
| T-17-02 | Tampering | `priv/shots/gap_register.md` (baseline) | mitigate | Task 1 restores the baseline via `git checkout` after the critique auto-overwrite (Pitfall 1); verify asserts `git diff --quiet` |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — nothing to audit; `mix.exs` and lockfile untouched |
</threat_model>

<verification>
- `mix test test/scoria_web/ds06_drift_guard_test.exs` exits 0 (raw-color-zero claim is live and true)
- `git diff --quiet priv/shots/gap_register.md` (committed baseline preserved)
- `priv/shots/gap_register_final.md` exists with all three required sections and the model stamp
- `git status --porcelain priv/shots/{final-date}/` shows the dated dir is gitignored
</verification>

<success_criteria>
PROOF-01 closed: one committed `gap_register_final.md` holds the 9-screen delta table, the deterministic 11-P1 checklist, and the DS-06 raw-color-zero citation; the 2026-06-04 baseline is intact; a fresh dated final shot set exists on disk for Plan 02 to consume.
</success_criteria>

<output>
Create `.planning/phases/17-consistency-sweep-proof/17-01-SUMMARY.md` when done. Record the actual `{final-date}` dir name so Plan 02 can point `--after` at it.
</output>
