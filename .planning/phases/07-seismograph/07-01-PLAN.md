---
phase: 07-seismograph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - mix.lock
  - lib/scoria/sre.ex
  - lib/scoria/sre/audit_sink.ex
  - lib/scoria/sre/alert_sink.ex
  - test/scoria/sre_test.exs
autonomous: true
requirements:
  - SRE-08
must_haves:
  truths:
    - "Phase 7 exposes a single public `Scoria.SRE` API instead of scattering new nouns across workflows, MCP, and LiveView layers."
    - "Optional audit and alert integrations stay behind narrow behaviors with no-op defaults rather than becoming hard runtime dependencies."
  artifacts:
    - path: "lib/scoria/sre.ex"
      provides: "Public SRE context contract and stable entrypoints for later plan waves"
    - path: "lib/scoria/sre/audit_sink.ex"
      provides: "Optional audit export behavior seam"
    - path: "lib/scoria/sre/alert_sink.ex"
      provides: "Optional alert export behavior seam"
  key_links:
    - from: "mix.exs"
      to: "Hammer and Fuse"
      via: "runtime governance dependencies"
      pattern: "hammer|fuse"
    - from: "lib/scoria/sre.ex"
      to: "later SRE modules"
      via: "single public context boundary"
      pattern: "def create_budget_policy|def reserve_usage|def open_incident"
---

<objective>
Establish the narrow Phase 7 API surface and dependency posture before durable storage and runtime enforcement land.

Purpose: give the remaining plans a stable `Scoria.SRE` contract and optional sink seams without forcing broad rewrites or hard vendor coupling.
Output: dependencies, public context contracts, behavior seams, and targeted context tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/MILESTONES.md
@.planning/STATE.md
@.planning/phases/07-seismograph/07-CONTEXT.md
@.planning/phases/07-seismograph/07-RESEARCH.md
@.planning/phases/07-seismograph/07-PATTERNS.md
@.planning/phases/07-seismograph/07-VALIDATION.md
@lib/scoria/workflows.ex
@lib/scoria/observe/approval.ex

<interfaces>
From lib/scoria/workflows.ex:
```elixir
def create_run(attrs \\ %{})
def append_event(run_id, step_id, attrs)
def append_checkpoint(run_id, step_id, attrs)
def mark_waiting_for_approval(run_id, step_id, attrs)
```

From lib/scoria/observe/approval.ex:
```elixir
schema "ai_approvals" do
  field(:tool_name, :string)
  field(:arguments, :map, default: %{})
  field(:status, :string, default: "pending")
  field(:workflow_run_id, :binary_id)
  field(:step_id, :binary_id)
end
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Bootstrap SRE Dependencies and Public Contracts</name>
  <files>mix.exs, mix.lock, lib/scoria/sre.ex, test/scoria/sre_test.exs</files>
  <read_first>
    - mix.exs
    - lib/scoria/workflows.ex
    - .planning/phases/07-seismograph/07-CONTEXT.md
    - .planning/phases/07-seismograph/07-RESEARCH.md
  </read_first>
  <action>Add `Hammer` and `Fuse` as first-class Phase 7 dependencies per D-01 and D-03. Create `Scoria.SRE` as the single public context with explicit function heads and placeholder delegation points for `create_budget_policy/1`, `reserve_usage/1`, `reconcile_usage/2`, `record_breaker_trip/1`, `record_alert_event/1`, `open_incident/1`, `append_incident_event/2`, `create_audit_outbox_event/1`, and `deliver_notification/2`. Keep the public API additive and boring per D-17 and D-21 so later plans can fill in persistence and runtime behavior without widening call sites.</action>
  <acceptance_criteria>
    - `mix.exs` contains dependencies for `hammer` and `fuse`
    - `lib/scoria/sre.ex` contains `def create_budget_policy(`
    - `lib/scoria/sre.ex` contains `def reserve_usage(`
    - `lib/scoria/sre.ex` contains `def create_audit_outbox_event(`
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/sre_test.exs</automated>
  </verify>
  <done>The repo exposes one public SRE context boundary and the dependency graph is ready for budget and breaker execution without hard vendor coupling.</done>
</task>

<task type="tdd">
  <name>Task 2: Define Optional Sink Behavior Seams</name>
  <files>lib/scoria/sre/audit_sink.ex, lib/scoria/sre/alert_sink.ex, test/scoria/sre_test.exs</files>
  <read_first>
    - lib/scoria/observe/redactor.ex
    - .planning/phases/07-seismograph/07-CONTEXT.md
    - .planning/phases/07-seismograph/07-RESEARCH.md
  </read_first>
  <action>Define `Scoria.SRE.AuditSink` and `Scoria.SRE.AlertSink` as narrow behavior seams with no-op defaults resolved from application config instead of compile-time coupling per D-14 and D-18 through D-20. Keep the contracts envelope-oriented so later relay plans deliver redacted audit facts and severity-shaped alert messages rather than raw DB structs.</action>
  <acceptance_criteria>
    - `lib/scoria/sre/audit_sink.ex` defines a callback-based contract
    - `lib/scoria/sre/alert_sink.ex` defines a callback-based contract
    - `test/scoria/sre_test.exs` proves default sink resolution does not require optional libraries
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/sre_test.exs</automated>
  </verify>
  <done>Phase 7 integration seams exist as explicit behaviors with safe defaults, so later relay work does not need to invent new contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime governance config -> public context | New SRE behavior enters the repo through explicit contracts and dependency choices |
| Core control plane -> optional sinks | External integrations are represented as narrow behaviors rather than direct calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Elevation of Privilege | sink contract resolution | mitigate | Resolve sinks through narrow behaviors and no-op defaults instead of arbitrary runtime code injection or hard dependencies. |
| T-07-01-02 | Tampering | public SRE API | mitigate | Keep a single `Scoria.SRE` boundary so later plans write through explicit functions rather than ad hoc helper calls. |
| T-07-01-03 | Repudiation | dependency posture | mitigate | Introduce Hammer and Fuse explicitly in `mix.exs` so enforcement logic uses inspectable first-class dependencies instead of implicit side libraries. |
</threat_model>

<verification>
- The dependency set includes Hammer and Fuse, but optional ecosystem integrations remain behind behaviors.
- `Scoria.SRE` exposes stable entrypoints that later plans can implement without changing call sites.
- No optional sink library is required for the core app to compile and test.
</verification>

<success_criteria>
- Phase 7 starts from a single public SRE context and explicit behavior seams.
- Later runtime, alert, and UI plans can depend on contracts instead of inventing their own surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/07-seismograph/07-01-SUMMARY.md`
</output>
