---
phase: 07-seismograph
plan: 02
type: execute
wave: 3
depends_on: [06]
files_modified:
  - lib/scoria/sre/budget_engine.ex
  - lib/scoria/workflows/runtime.ex
  - lib/scoria/mcp/executor.ex
  - test/scoria/sre/budget_engine_test.exs
  - test/scoria/workflows/runtime_test.exs
  - test/scoria/mcp/executor_test.exs
autonomous: true
requirements:
  - SRE-01
  - SRE-02
  - SRE-08
must_haves:
  truths:
    - "Estimated spend and high-risk tool usage are reserved before side effects begin."
    - "Actual provider or tool usage reconciles back onto the same durable reservation record."
    - "Small explicit loop guards stop runaway workflows before they burn more cost or tool quota."
  artifacts:
    - path: "lib/scoria/sre/budget_engine.ex"
      provides: "Budget reservation, reconciliation, and loop-guard evaluation"
    - path: "lib/scoria/workflows/runtime.ex"
      provides: "Preflight budget guard at workflow-step execution"
  key_links:
    - from: "lib/scoria/workflows/runtime.ex"
      to: "lib/scoria/sre/budget_engine.ex"
      via: "reserve-before-effect step preflight"
      pattern: "reserve"
    - from: "lib/scoria/mcp/executor.ex"
      to: "lib/scoria/sre/budget_engine.ex"
      via: "tool-call reservation and reconciliation"
      pattern: "reconcile"
---

<objective>
Enforce Phase 7 budget reservations and loop guards at the real execution seams Scoria already owns.

Purpose: stop overspend and runaway runs before external work starts, while preserving reconciled durable evidence after execution completes or fails.
Output: budget engine module, runtime and MCP preflight integration, and targeted tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-seismograph/07-CONTEXT.md
@.planning/phases/07-seismograph/07-RESEARCH.md
@.planning/phases/07-seismograph/07-PATTERNS.md
@.planning/phases/07-seismograph/07-06-PLAN.md
@lib/scoria/workflows/runtime.ex
@lib/scoria/mcp/executor.ex
@test/scoria/workflows/runtime_test.exs
@test/scoria/mcp/executor_test.exs

<interfaces>
From lib/scoria/workflows/runtime.ex:
```elixir
def execute_step(step_id, opts \\ [])
```

From lib/scoria/mcp/executor.ex:
```elixir
def execute(tool_module, args, context, timeout \\ 5000)
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Implement the Budget Engine</name>
  <files>lib/scoria/sre/budget_engine.ex, test/scoria/sre/budget_engine_test.exs</files>
  <read_first>
    - lib/scoria/sre.ex
    - .planning/phases/07-seismograph/07-RESEARCH.md
    - .planning/phases/07-seismograph/07-CONTEXT.md
  </read_first>
  <action>Create `Scoria.SRE.BudgetEngine` as the single module that loads `BudgetPolicy`, calculates reservation envelopes, runs Hammer short-window checks per D-01, and evaluates the default guard set from D-04: 80% warn, 100% trip, max workflow steps, repeated tool hash, and consecutive-failure caps. Support preflight inputs keyed by `tenant_id`, `actor_id`, `run_id`, `step_id`, `trace_id`, `resource`, `reason_code`, `estimated_units`, and `integration_kind`. Implement explicit reconciliation APIs so actual provider or tool usage updates the matching reservation after execution per D-02.</action>
  <acceptance_criteria>
    - `lib/scoria/sre/budget_engine.ex` contains `def reserve_step(`
    - `lib/scoria/sre/budget_engine.ex` contains `def reconcile_usage(`
    - `lib/scoria/sre/budget_engine.ex` contains logic for warn and trip thresholds
    - `test/scoria/sre/budget_engine_test.exs` covers repeated tool hash and consecutive failure guards
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/sre/budget_engine_test.exs</automated>
  </verify>
  <done>The budget engine deterministically decides warn, trip, reserve, release, and reconcile outcomes from durable policy plus short-window counters.</done>
</task>

<task type="tdd">
  <name>Task 2: Enforce Runtime and MCP Preflight Budgets</name>
  <files>lib/scoria/workflows/runtime.ex, lib/scoria/mcp/executor.ex, test/scoria/workflows/runtime_test.exs, test/scoria/mcp/executor_test.exs</files>
  <read_first>
    - lib/scoria/workflows/runtime.ex
    - lib/scoria/mcp/executor.ex
    - test/scoria/workflows/runtime_test.exs
    - test/scoria/mcp/executor_test.exs
  </read_first>
  <action>Modify `Scoria.Workflows.Runtime.execute_step/2` and `Scoria.MCP.Executor.execute/4` so reservation preflight happens before `Task.Supervisor` launches any paid LLM step, remote MCP step, or policy-sensitive tool call per D-02 and D-03. Use explicit context flags such as `estimated_cost_usd`, `estimated_tokens`, `sensitive_tool`, `integration_kind`, and `tenant_id`. If reservation or loop guards trip, fail the step or tool call with a reason-coded envelope and no side effect. If execution completes, times out, or fails, reconcile actual usage back onto the reservation record and preserve the evidence for later alerting.</action>
  <acceptance_criteria>
    - `lib/scoria/workflows/runtime.ex` contains a budget preflight before `Task.Supervisor.async_nolink`
    - `lib/scoria/mcp/executor.ex` contains reservation and reconciliation paths
    - `test/scoria/workflows/runtime_test.exs` asserts side effects do not run after a budget trip
    - `test/scoria/mcp/executor_test.exs` asserts timeout and crash paths reconcile reservation status
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/workflows/runtime_test.exs test/scoria/mcp/executor_test.exs</automated>
  </verify>
  <done>Workflow and MCP execution now reject over-budget or loop-guarded work before external effects and reconcile durable usage after any outcome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow intent -> runtime execution | Requested AI work crosses into paid or side-effecting execution |
| Tool context -> MCP executor | Untrusted tool arguments become external work requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Denial of Service | budget engine | mitigate | Reserve before effect and enforce loop guards so runaway workflows cannot continue consuming resources. |
| T-07-02-02 | Tampering | runtime and MCP reconciliation | mitigate | Reconcile actual usage onto durable reservation rows for success, timeout, and failure outcomes instead of trusting transient telemetry only. |
</threat_model>

<verification>
- Budget reservations exist before external work begins.
- Over-budget and loop-guarded executions fail fast without launching side effects.
- Success, timeout, and failure paths reconcile durable usage evidence.
</verification>

<success_criteria>
- SRE-02 is enforced at the repo’s actual execution seams, not at ingress-only middleware.
- Budget evidence is durable enough to drive later incidents and operator review.
</success_criteria>

<output>
After completion, create `.planning/phases/07-seismograph/07-02-SUMMARY.md`
</output>
