---
phase: 07-seismograph
plan: 08
type: execute
wave: 6
depends_on: [04]
files_modified:
  - lib/scoria/application.ex
  - lib/scoria/sre/relay.ex
  - lib/scoria/sre/adapters/threadline.ex
  - lib/scoria/sre/adapters/chimeway.ex
  - lib/scoria/sre/adapters/mailglass.ex
  - test/scoria/sre/relay_test.exs
autonomous: true
requirements:
  - SRE-06
  - SRE-08
must_haves:
  truths:
    - "Optional Threadline, Chimeway, and Mailglass relays deliver from durable rows, default to no-op behavior when unconfigured, and start under the real application supervision tree."
    - "Retry state and delivery errors stay durable so notification fanout can fail safely without losing the local evidence trail."
  artifacts:
    - path: "lib/scoria/sre/relay.ex"
      provides: "Supervised outbox and notification relay worker logic"
    - path: "lib/scoria/application.ex"
      provides: "Runtime supervision wiring that starts the Phase 7 relay in the real app"
    - path: "lib/scoria/sre/adapters/threadline.ex"
      provides: "Optional audit sink adapter"
  key_links:
    - from: "lib/scoria/application.ex"
      to: "lib/scoria/sre/relay.ex"
      via: "OTP child supervision"
      pattern: "Scoria\\.SRE\\.Relay"
    - from: "lib/scoria/sre/relay.ex"
      to: "lib/scoria/sre/adapters/threadline.ex"
      via: "optional sink delivery from durable rows"
      pattern: "deliver|claim_pending"
---

<objective>
Add the supervised relay runtime and optional first-party adapters after transactional outbox and incident routing are already durable.

Purpose: keep external sink fanout isolated from local truth capture so retries, no-op defaults, and supervision can be built as a separate operational slice.
Output: supervised relay worker, application wiring, optional Threadline/Chimeway/Mailglass adapters, and focused relay tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-seismograph/07-CONTEXT.md
@.planning/phases/07-seismograph/07-RESEARCH.md
@.planning/phases/07-seismograph/07-04-PLAN.md
@lib/scoria/application.ex
@lib/scoria/sre/audit_sink.ex
@lib/scoria/sre/alert_sink.ex

<interfaces>
From lib/scoria/sre/audit_sink.ex:
```elixir
@callback deliver(map()) :: :ok | {:error, term()}
```

From lib/scoria/sre/alert_sink.ex:
```elixir
@callback deliver(map()) :: :ok | {:error, term()}
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Supervise a Durable Relay Worker</name>
  <files>lib/scoria/application.ex, lib/scoria/sre/relay.ex, test/scoria/sre/relay_test.exs</files>
  <read_first>
    - lib/scoria/application.ex
    - lib/scoria/sre/audit_sink.ex
    - lib/scoria/sre/alert_sink.ex
    - .planning/phases/07-seismograph/07-CONTEXT.md
  </read_first>
  <action>Create a supervised `Scoria.SRE.Relay` that claims pending `AuditOutboxEvent` and `NotificationDelivery` rows, attempts delivery through configured sinks, records attempt counts and last-error state, and retries idempotently without requiring Oban. Wire the worker into the real OTP runtime by adding it to `Scoria.Application` or a dedicated child supervisor started from `Scoria.Application`, and keep the child enabled in normal app boots with a test-friendly configuration gate only if existing app conventions require one.</action>
  <acceptance_criteria>
    - `lib/scoria/application.ex` starts `Scoria.SRE.Relay` or a dedicated supervisor that starts it
    - `lib/scoria/sre/relay.ex` contains claim and retry logic over durable rows
    - `test/scoria/sre/relay_test.exs` asserts the relay child starts under the application supervision tree
    - `test/scoria/sre/relay_test.exs` asserts failed deliveries remain retryable
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/sre/relay_test.exs</automated>
  </verify>
  <done>Durable relay processing runs under the real supervision tree and fails safely without losing pending delivery state.</done>
</task>

<task type="tdd">
  <name>Task 2: Implement Optional First-Party Sink Adapters</name>
  <files>lib/scoria/sre/adapters/threadline.ex, lib/scoria/sre/adapters/chimeway.ex, lib/scoria/sre/adapters/mailglass.ex, test/scoria/sre/relay_test.exs</files>
  <read_first>
    - .planning/phases/07-seismograph/07-RESEARCH.md
    - .planning/phases/07-seismograph/07-CONTEXT.md
    - lib/scoria/sre/audit_sink.ex
    - lib/scoria/sre/alert_sink.ex
  </read_first>
  <action>Implement first-party optional adapters for Threadline, Chimeway, and Mailglass per D-14, D-18, and D-19, but keep the default behavior no-op and configurable. `threadline.ex` should send audit-style envelopes rather than raw DB structs; `chimeway.ex` and `mailglass.ex` should accept review/page severity input and delivery metadata. Do not add complex routing trees, auto-remediation, or deep vendor coupling.</action>
  <acceptance_criteria>
    - `lib/scoria/sre/adapters/threadline.ex` sends audit-style envelopes, not raw DB structs
    - `lib/scoria/sre/adapters/chimeway.ex` and `mailglass.ex` accept review/page severity input
    - `test/scoria/sre/relay_test.exs` asserts no-op behavior when sinks are unconfigured
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/sre/relay_test.exs</automated>
  </verify>
  <done>Phase 7 can fan out durable audit and incident evidence through optional sinks while keeping the core control plane installable and boring by default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Durable rows -> external sinks | Audit and alert data leaves the local system through optional relays |
| Application boot -> relay worker | Durable fanout logic only runs if the relay is actually supervised |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-01 | Denial of Service | relay runtime wiring | mitigate | Start the relay under the real application supervision tree and verify the child comes up in tests so durable rows are not left permanently undelivered. |
| T-07-08-02 | Information Disclosure | sink adapters | mitigate | Relay only redacted references, hashes, severity metadata, and evidence links through optional sinks. |
| T-07-08-03 | Tampering | retry state | mitigate | Persist attempt counts and last-error state on durable rows so repeated failures remain inspectable and retryable. |
</threat_model>

<verification>
- Relay retries are durable, harmless when optional sinks are absent, and started by the real application supervision tree.
- Optional adapters remain narrow and envelope-oriented rather than turning core Scoria into a vendor-coupled integration layer.
</verification>

<success_criteria>
- The notification-fanout portion of SRE-06 is delivered in its own operational slice.
- External sink delivery can fail or stay unconfigured without weakening the local evidence model.
</success_criteria>

<output>
After completion, create `.planning/phases/07-seismograph/07-08-SUMMARY.md`
</output>
