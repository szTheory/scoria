---
phase: 07-seismograph
plan: 05
type: execute
wave: 6
depends_on: [02, 03, 04]
files_modified:
  - lib/scoria_web/components/incident_evidence_component.ex
  - lib/scoria_web/live/orchestrator_live.ex
  - test/scoria_web/live/orchestrator_live_sre_test.exs
  - test/scoria_web/live/orchestrator_live_test.exs
autonomous: true
requirements:
  - SRE-04
  - SRE-07
  - SRE-08
must_haves:
  truths:
    - "Operators can inspect a compact composite Scoria health rollup plus budget state, breaker state, incidents, audit evidence, and notification outcomes inside the existing trace-first LiveView."
    - "Incident rows deep-link back to traces, runs, approvals, and versioned scorer or baseline evidence."
    - "Heavy evidence payloads load asynchronously and do not bloat socket state."
  artifacts:
    - path: "lib/scoria_web/components/incident_evidence_component.ex"
      provides: "Composite health rollup plus budget, breaker, incident, audit, and delivery evidence notebook"
    - path: "lib/scoria_web/live/orchestrator_live.ex"
      provides: "Trace-first operator projection of Phase 7 incident state"
  key_links:
    - from: "lib/scoria_web/live/orchestrator_live.ex"
      to: "assign_async"
      via: "lazy incident evidence loading"
      pattern: "assign_async"
    - from: "lib/scoria_web/components/incident_evidence_component.ex"
      to: "trace or run deep links"
      via: "evidence-first incident notebook"
      pattern: "trace_id|run_id|approval"
---

<objective>
Project the new SRE evidence into Scoria’s existing operator surface without adding a second app or a graph-first incident product.

Purpose: give operators a calm, trace-first control room where every alert or page can be explained from durable evidence.
Output: incident evidence component with composite health rollup, LiveView wiring, and focused LiveView tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-seismograph/07-CONTEXT.md
@.planning/phases/07-seismograph/07-RESEARCH.md
@.planning/phases/07-seismograph/07-04-PLAN.md
@lib/scoria_web/live/orchestrator_live.ex
@lib/scoria_web/components/citation_evidence_component.ex
@test/scoria_web/live/orchestrator_live_test.exs

<interfaces>
From lib/scoria_web/live/orchestrator_live.ex:
```elixir
def handle_event("load_retrieval_evidence", %{"id" => trace_id}, socket)
def handle_event("replay_retrieval", %{"id" => trace_id}, socket)
def handle_event("promote_retrieval", %{"id" => trace_id}, socket)
```

From lib/scoria_web/components/citation_evidence_component.ex:
```elixir
def render(assigns)
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Build a Trace-First Incident Evidence Component</name>
  <files>lib/scoria_web/components/incident_evidence_component.ex, test/scoria_web/live/orchestrator_live_sre_test.exs</files>
  <read_first>
    - lib/scoria_web/components/citation_evidence_component.ex
    - .planning/phases/07-seismograph/07-CONTEXT.md
    - .planning/phases/07-seismograph/07-RESEARCH.md
  </read_first>
  <action>Create `ScoriaWeb.IncidentEvidenceComponent` as a calm operator notebook per D-10 and D-21 through D-24. Render a compact composite Scoria health rollup at the top of the notebook that aggregates current budget status, breaker state, open review incidents, open page incidents, and audit relay health for the selected run or trace, but do not turn that rollup into the main pager source. Beneath it, render the selected run’s budget strip, reservation actuals, breaker badges, incident summary, alert reason code, scorer or baseline version, linked approval and audit rows, and notification delivery outcomes. Keep the evidence language precise and additive: page vs review severity must be visually distinct, but the surface should still feel like trace evidence rather than a separate incident management app.</action>
  <acceptance_criteria>
    - `lib/scoria_web/components/incident_evidence_component.ex` defines `ScoriaWeb.IncidentEvidenceComponent`
    - the component renders a compact composite health rollup with budget, breaker, incident, and relay status signals
    - the component renders budget status, breaker state, and delivery outcomes
    - `test/scoria_web/live/orchestrator_live_sre_test.exs` asserts deep-link evidence fields are visible
    - tests assert review vs page severity is distinguishable in the rendered HTML
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_sre_test.exs</automated>
  </verify>
  <done>The operator surface has a dedicated, evidence-first component for Phase 7 incident state without leaving the dashboard.</done>
</task>

<task type="tdd">
  <name>Task 2: Extend OrchestratorLive for Incident and Audit Projection</name>
  <files>lib/scoria_web/live/orchestrator_live.ex, test/scoria_web/live/orchestrator_live_sre_test.exs, test/scoria_web/live/orchestrator_live_test.exs</files>
  <read_first>
    - lib/scoria_web/live/orchestrator_live.ex
    - test/scoria_web/live/orchestrator_live_test.exs
    - lib/scoria_web/components/citation_evidence_component.ex
  </read_first>
  <action>Extend `ScoriaWeb.OrchestratorLive` to load SRE evidence with `assign_async/3` or streamed updates when a trace, run, or incident row is selected. Add explicit events such as `load_incident_evidence` and `load_budget_state`, surface compact run-level badges for budget warn/trip, breaker open, review incident, and page incident states, and feed the component-level D-10 composite health rollup from the same derived evidence. Deep-link every incident row back to the underlying trace or run evidence per D-24. Do not preload full incident notebooks, outbox payloads, or delivery histories into mount assigns.</action>
  <acceptance_criteria>
    - `lib/scoria_web/live/orchestrator_live.ex` contains `load_incident_evidence`
    - `lib/scoria_web/live/orchestrator_live.ex` contains `assign_async`
    - tests assert the composite health rollup updates from incident, breaker, and budget evidence without becoming the only operator focus
    - tests assert incident evidence loads lazily and renders budget or breaker state
    - existing retrieval evidence tests continue to pass alongside the new SRE panel
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_sre_test.exs test/scoria_web/live/orchestrator_live_test.exs</automated>
  </verify>
  <done>Operators can review budget, breaker, incident, audit, and notification evidence from the same trace-first dashboard surface they already use.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted SRE evidence -> LiveView | Incident, delivery, and audit data enters the operator-facing projection layer |
| LiveView event -> async evidence loader | User interaction triggers retrieval of heavy evidence payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Denial of Service | OrchestratorLive evidence panel | mitigate | Load heavy incident notebooks asynchronously and keep mount assigns compact. |
| T-07-05-02 | Information Disclosure | incident evidence projection | mitigate | Render redacted summaries and deep links instead of raw payload bodies by default. |
</threat_model>

<verification>
- The incident panel loads on demand inside the existing LiveView.
- The dashboard shows a compact composite Scoria health rollup without replacing the trace-first incident notebook.
- Budget, breaker, and incident badges are visible from the trace-first surface.
- Deep links and version metadata remain visible without bloating socket state.
</verification>

<success_criteria>
- SRE-07 is delivered inside the existing dashboard path without a second product surface.
- Phase 7 evidence remains calm, auditable, and trace-first during operator review.
</success_criteria>

<output>
After completion, create `.planning/phases/07-seismograph/07-05-SUMMARY.md`
</output>
