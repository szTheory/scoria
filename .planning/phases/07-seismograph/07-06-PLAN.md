---
phase: 07-seismograph
plan: 06
type: execute
wave: 2
depends_on: [01]
files_modified:
  - priv/repo/migrations/20260511170000_create_sre_budget_and_breaker_tables.exs
  - lib/scoria/sre.ex
  - lib/scoria/sre/budget_policy.ex
  - lib/scoria/sre/budget_reservation.ex
  - lib/scoria/sre/breaker_trip.ex
  - test/scoria/sre_test.exs
autonomous: true
requirements:
  - SRE-01
  - SRE-08
must_haves:
  truths:
    - "Scoria owns durable budget policy, reservation, and breaker-trip nouns in Ecto before runtime enforcement depends on them."
    - "The public `Scoria.SRE` context exposes explicit budget and breaker persistence helpers before runtime and MCP budget slices wire into it."
  artifacts:
    - path: "priv/repo/migrations/20260511170000_create_sre_budget_and_breaker_tables.exs"
      provides: "Additive budget and breaker storage"
    - path: "lib/scoria/sre/budget_policy.ex"
      provides: "Versioned budget policy schema"
    - path: "lib/scoria/sre/budget_reservation.ex"
      provides: "Durable reservation record with reconciliation fields"
    - path: "lib/scoria/sre/breaker_trip.ex"
      provides: "Append-only breaker trip history"
    - path: "lib/scoria/sre.ex"
      provides: "Public persistence helpers for budget policy, reservations, and breaker trips"
  key_links:
    - from: "lib/scoria/sre.ex"
      to: "Ecto.Multi"
      via: "atomic budget reservation and reconciliation writes"
      pattern: "Ecto\\.Multi"
    - from: "priv/repo/migrations/20260511170000_create_sre_budget_and_breaker_tables.exs"
      to: "lib/scoria/sre/budget_policy.ex"
      via: "durable table and schema alignment"
      pattern: "ai_budget_policies|ai_budget_reservations|ai_breaker_trips"
---

<objective>
Land the narrow durable substrate for budget policy, usage reservations, and breaker trip history before runtime enforcement starts consuming it.

Purpose: keep the budget-and-breaker foundation small enough to execute safely while still giving the later runtime slice the exact persistence helpers it needs.
Output: additive budget and breaker migration, three durable schemas, minimal `Scoria.SRE` persistence helpers, and focused context tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/MILESTONES.md
@.planning/STATE.md
@.planning/phases/07-seismograph/07-CONTEXT.md
@.planning/phases/07-seismograph/07-RESEARCH.md
@.planning/phases/07-seismograph/07-PATTERNS.md
@.planning/phases/07-seismograph/07-VALIDATION.md
@.planning/phases/07-seismograph/07-01-PLAN.md
@lib/scoria/workflows/run.ex
@lib/scoria/workflows/checkpoint.ex
@lib/scoria/workflows/event.ex

<interfaces>
From lib/scoria/workflows/run.ex:
```elixir
schema "ai_workflow_runs" do
  field :status, :string, default: "running"
  field :lock_version, :integer, default: 1
  field :metadata, :map, default: %{}
end
```

From lib/scoria/workflows/checkpoint.ex:
```elixir
schema "ai_workflow_checkpoints" do
  field :sequence, :integer
  field :status, :string
  field :snapshot, :map, default: %{}
end
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Create Budget and Breaker Durable Tables and Schemas</name>
  <files>priv/repo/migrations/20260511170000_create_sre_budget_and_breaker_tables.exs, lib/scoria/sre/budget_policy.ex, lib/scoria/sre/budget_reservation.ex, lib/scoria/sre/breaker_trip.ex, test/scoria/sre_test.exs</files>
  <read_first>
    - lib/scoria/workflows/run.ex
    - lib/scoria/workflows/checkpoint.ex
    - lib/scoria/workflows/event.ex
    - .planning/phases/07-seismograph/07-CONTEXT.md
  </read_first>
  <action>Create additive binary-id tables and matching schemas for `BudgetPolicy`, `BudgetReservation`, and `BreakerTrip` per D-01, D-02, D-04, D-17, and D-22. Root policy rows should carry `lock_version`, scope keys, resource kind, warn/trip thresholds, and loop-guard caps; reservation rows should persist estimated units, actual units, policy snapshots, reconciliation status, provider or tool refs, and trace or run linkage; breaker trip rows should stay append-only with integration key, reason code, state transition, and evidence refs. Index `tenant_id`, `policy_key`, `scope_key`, `status`, `breaker_key`, and `inserted_at` so the runtime and incident layers can query without table scans.</action>
  <acceptance_criteria>
    - `priv/repo/migrations/20260511170000_create_sre_budget_and_breaker_tables.exs` contains `create table(:ai_budget_policies`
    - `priv/repo/migrations/20260511170000_create_sre_budget_and_breaker_tables.exs` contains `create table(:ai_breaker_trips`
    - `lib/scoria/sre/budget_reservation.ex` contains `field :estimated_units`
    - `lib/scoria/sre/breaker_trip.ex` contains `field :breaker_key`
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/sre_test.exs</automated>
  </verify>
  <done>The repo has compile-checked durable storage for the exact nouns the budget and breaker runtime slices need.</done>
</task>

<task type="tdd">
  <name>Task 2: Expose Minimal Public Budget and Breaker Persistence APIs</name>
  <files>lib/scoria/sre.ex, test/scoria/sre_test.exs</files>
  <read_first>
    - lib/scoria/sre.ex
    - .planning/phases/07-seismograph/07-RESEARCH.md
    - .planning/phases/07-seismograph/07-CONTEXT.md
  </read_first>
  <action>Fill in only the `Scoria.SRE` helpers the next execution slice needs: budget policy CRUD, reservation creation, reservation reconciliation, reservation release, and breaker trip recording per D-01 and D-02. Use `Ecto.Multi` for writes that combine reservation state with breaker or reconciliation outcomes. Keep this plan intentionally narrow: do not add incident, delivery, or audit-outbox helpers here; those land in the later durable-nouns slice so this foundation stays focused.</action>
  <acceptance_criteria>
    - `lib/scoria/sre.ex` contains `Ecto.Multi`
    - `lib/scoria/sre.ex` contains `def create_budget_policy(`
    - `lib/scoria/sre.ex` contains `def reserve_usage(`
    - `lib/scoria/sre.ex` contains `def reconcile_usage(`
    - `lib/scoria/sre.ex` contains `def record_breaker_trip(`
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/sre_test.exs</automated>
  </verify>
  <done>`Scoria.SRE` is a usable, narrow public context for budget reservations and breaker trip persistence without pulling in later incident or relay concerns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime governance -> DB | Budget and breaker decisions become durable policy and reservation truth |
| External failure evidence -> DB | Breaker trips become append-only evidence that later incident logic can trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Tampering | budget and breaker schemas | mitigate | Keep policy, reservation, and breaker-trip fields explicit and indexed so enforcement state is queryable and not buried in opaque blobs. |
| T-07-06-02 | Repudiation | budget reservation persistence | mitigate | Use durable reservation and breaker-trip rows with trace, run, and policy refs so enforcement evidence survives process crashes. |
| T-07-06-03 | Elevation of Privilege | `Scoria.SRE` helper API | mitigate | Keep all budget and breaker writes behind explicit `Scoria.SRE` functions and `Ecto.Multi` boundaries instead of scattering repo writes. |
</threat_model>

<verification>
- Budget policies, reservations, and breaker trips compile as durable Ecto nouns.
- `Scoria.SRE` exposes only the budget and breaker persistence helpers needed by `07-02`.
- The foundation stays small enough that later incident and relay work can build on it without reopening this slice.
</verification>

<success_criteria>
- Phase 7 has a narrow, durable budget-and-breaker substrate aligned to SRE-01 and SRE-08.
- The runtime reservation plan can depend on `Scoria.SRE` without inheriting unrelated incident or relay scope.
</success_criteria>

<output>
After completion, create `.planning/phases/07-seismograph/07-06-SUMMARY.md`
</output>
