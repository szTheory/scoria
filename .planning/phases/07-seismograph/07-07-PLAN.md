---
phase: 07-seismograph
plan: 07
type: execute
wave: 3
depends_on: [06]
files_modified:
  - priv/repo/migrations/20260511171000_create_sre_incident_and_audit_tables.exs
  - lib/scoria/sre/alert_policy.ex
  - lib/scoria/sre/alert_event.ex
  - lib/scoria/sre/incident.ex
  - lib/scoria/sre/incident_event.ex
  - lib/scoria/sre/notification_delivery.ex
  - lib/scoria/sre/audit_outbox_event.ex
  - test/scoria/sre_test.exs
autonomous: true
requirements:
  - SRE-01
  - SRE-08
must_haves:
  truths:
    - "Scoria owns durable alert-policy, alert-event, incident, delivery, and audit-outbox nouns in Ecto before incident routing and relay logic depend on them."
    - "Sensitive audit rows and reviewable incident rows have explicit, additive fields for redacted refs, payload hashes, and stable incident keys."
  artifacts:
    - path: "priv/repo/migrations/20260511171000_create_sre_incident_and_audit_tables.exs"
      provides: "Additive alert, incident, delivery, and audit storage"
    - path: "lib/scoria/sre/incident.ex"
      provides: "Stable incident root record with dedupe key and severity"
    - path: "lib/scoria/sre/audit_outbox_event.ex"
      provides: "Durable audit export row with redacted reference fields"
  key_links:
    - from: "priv/repo/migrations/20260511171000_create_sre_incident_and_audit_tables.exs"
      to: "lib/scoria/sre/incident.ex"
      via: "durable table and schema alignment"
      pattern: "ai_incidents|ai_incident_events|ai_notification_deliveries|ai_audit_outbox_events"
    - from: "lib/scoria/sre/audit_outbox_event.ex"
      to: "later workflow and MCP integration"
      via: "transactional durable audit rows"
      pattern: "payload_hash|trace_id|run_id|step_id"
---

<objective>
Add the second durable-storage slice for incidents, notifications, and audit export without mixing it into the earlier budget-and-breaker foundation.

Purpose: give incident routing and relay plans the exact nouns they need while keeping the storage layer split along durable domain boundaries.
Output: additive alert, incident, delivery, and audit migration, six durable schemas, and focused context tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/MILESTONES.md
@.planning/STATE.md
@.planning/phases/07-seismograph/07-CONTEXT.md
@.planning/phases/07-seismograph/07-RESEARCH.md
@.planning/phases/07-seismograph/07-PATTERNS.md
@.planning/phases/07-seismograph/07-VALIDATION.md
@.planning/phases/07-seismograph/07-06-PLAN.md
@lib/scoria/workflows/run.ex
@lib/scoria/workflows/event.ex
@lib/scoria/observe/approval.ex

<interfaces>
From lib/scoria/workflows/event.ex:
```elixir
schema "ai_workflow_events" do
  field :event_type, :string
  field :payload, :map, default: %{}
end
```

From lib/scoria/observe/approval.ex:
```elixir
schema "ai_approvals" do
  field(:tool_name, :string)
  field(:status, :string, default: "pending")
  field(:workflow_run_id, :binary_id)
  field(:step_id, :binary_id)
end
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Create Incident, Delivery, and Audit Durable Tables</name>
  <files>priv/repo/migrations/20260511171000_create_sre_incident_and_audit_tables.exs, lib/scoria/sre/alert_policy.ex, lib/scoria/sre/alert_event.ex, lib/scoria/sre/incident.ex, lib/scoria/sre/incident_event.ex, lib/scoria/sre/notification_delivery.ex, lib/scoria/sre/audit_outbox_event.ex, test/scoria/sre_test.exs</files>
  <read_first>
    - lib/scoria/workflows/run.ex
    - lib/scoria/workflows/event.ex
    - lib/scoria/observe/approval.ex
    - .planning/phases/07-seismograph/07-CONTEXT.md
  </read_first>
  <action>Create additive binary-id tables and matching schemas for `AlertPolicy`, `AlertEvent`, `Incident`, `IncidentEvent`, `NotificationDelivery`, and `AuditOutboxEvent` per D-06 through D-16, D-22, and D-24. Root rows such as `AlertPolicy` and `Incident` should carry `lock_version`, routing fields, and stable keys; append-only rows should preserve reason codes, scorer and baseline version refs, sink status, attempt counts, payload hashes, and trace or run linkage. Index `tenant_id`, `policy_key`, `incident_key`, `severity`, `status`, `delivery_status`, `pending_at`, and `inserted_at` so later incident routing, relay claiming, and LiveView evidence queries stay targeted.</action>
  <acceptance_criteria>
    - `priv/repo/migrations/20260511171000_create_sre_incident_and_audit_tables.exs` contains `create table(:ai_alert_policies`
    - `priv/repo/migrations/20260511171000_create_sre_incident_and_audit_tables.exs` contains `create table(:ai_audit_outbox_events`
    - `lib/scoria/sre/incident.ex` contains `field :incident_key`
    - `lib/scoria/sre/notification_delivery.ex` contains delivery attempt state fields
    - `lib/scoria/sre/audit_outbox_event.ex` contains `field :payload_hash`
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/sre_test.exs</automated>
  </verify>
  <done>The repo has compile-checked durable nouns for alerts, incidents, notifications, and audit export before execution and UI slices wire into them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sensitive execution evidence -> DB | Audit and incident evidence becomes the canonical durable record |
| Durable rows -> future relays and UI | Delivery and incident state will later leave storage for operator projection and sink fanout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Information Disclosure | audit outbox schema | mitigate | Store hashes, refs, and policy classes instead of raw arguments so durable evidence remains exportable without leaking sensitive payloads. |
| T-07-07-02 | Repudiation | incident and delivery rows | mitigate | Preserve append-only incident-event and delivery-attempt evidence with stable incident keys and version refs. |
| T-07-07-03 | Tampering | alert and incident root rows | mitigate | Use explicit root schemas with indexed routing keys and optimistic-lock-ready structure so later routing logic updates auditable state instead of ad hoc maps. |
</threat_model>

<verification>
- Incident, notification, and audit durable nouns compile and validate through targeted context tests.
- The later incident-routing and relay plans can rely on concrete storage objects instead of inventing schema details mid-plan.
</verification>

<success_criteria>
- Phase 7 durable storage is split into two focused slices rather than one overloaded foundation plan.
- Alert, incident, delivery, and audit nouns exist with enough structure to support routing, fanout, and trace-first UI evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/07-seismograph/07-07-SUMMARY.md`
</output>
