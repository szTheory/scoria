---
phase: 07-seismograph
plan: 04
type: execute
wave: 5
depends_on: [02, 03, 07]
files_modified:
  - lib/scoria/workflows.ex
  - lib/scoria/observe/approval.ex
  - lib/scoria/observe/redactor.ex
  - lib/scoria/mcp/executor.ex
  - lib/scoria/sre.ex
  - lib/scoria/sre/incident_manager.ex
  - test/scoria/sre/audit_outbox_test.exs
  - test/scoria/sre/incident_test.exs
  - test/scoria/mcp/executor_test.exs
autonomous: true
requirements:
  - SRE-04
  - SRE-05
  - SRE-06
  - SRE-08
must_haves:
  truths:
    - "Sensitive approval, MCP-access, and policy-sensitive tool events create durable audit-outbox rows in the same transaction as the local truth change at the actual workflow and MCP execution seams."
    - "Alerts dedupe into stable reviewable incidents with scorer and baseline evidence attached."
  artifacts:
    - path: "lib/scoria/sre/incident_manager.ex"
      provides: "Incident dedupe, severity routing, and alert-event fan-in"
    - path: "lib/scoria/sre.ex"
      provides: "Public outbox and incident APIs consumed from workflow and MCP seams"
  key_links:
    - from: "lib/scoria/workflows.ex"
      to: "lib/scoria/sre.ex"
      via: "same-transaction audit outbox writes"
      pattern: "audit_outbox"
    - from: "lib/scoria/mcp/executor.ex"
      to: "lib/scoria/sre.ex"
      via: "MCP access and policy-sensitive tool audit capture inside execution transactions"
      pattern: "audit_outbox|create_audit_outbox_event"
    - from: "lib/scoria/sre.ex"
      to: "lib/scoria/sre/incident_manager.ex"
      via: "public alert APIs route through incident dedupe"
      pattern: "open_incident|append_incident_event|record_alert_event"
---

<objective>
Land the durable audit and incident-routing layer for Phase 7 without bundling relay runtime and adapter wiring into the same slice.

Purpose: preserve trustworthy local evidence first, then shape review alerts and pages into stable incidents before any external fanout plan starts consuming them.
Output: transactional outbox integration at workflow and MCP seams, public outbox and incident APIs, incident manager, and focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-seismograph/07-CONTEXT.md
@.planning/phases/07-seismograph/07-RESEARCH.md
@.planning/phases/07-seismograph/07-03-PLAN.md
@.planning/phases/07-seismograph/07-07-PLAN.md
@lib/scoria/workflows.ex
@lib/scoria/observe/approval.ex
@lib/scoria/observe/redactor.ex
@lib/scoria/mcp/executor.ex

<interfaces>
From lib/scoria/observe/redactor.ex:
```elixir
def redact(data)
```

From lib/scoria/workflows.ex:
```elixir
def mark_waiting_for_approval(run_id, step_id, attrs)
def approve(approval_id, decision)
def append_event(run_id, step_id, attrs)
```

From lib/scoria/mcp/executor.ex:
```elixir
def execute(tool_module, args, context, timeout \\ 5000)
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Insert Audit Outbox Rows Transactionally</name>
  <files>lib/scoria/workflows.ex, lib/scoria/observe/approval.ex, lib/scoria/observe/redactor.ex, lib/scoria/mcp/executor.ex, lib/scoria/sre.ex, test/scoria/sre/audit_outbox_test.exs, test/scoria/mcp/executor_test.exs</files>
  <read_first>
    - lib/scoria/workflows.ex
    - lib/scoria/observe/approval.ex
    - lib/scoria/observe/redactor.ex
    - lib/scoria/mcp/executor.ex
    - .planning/phases/07-seismograph/07-CONTEXT.md
  </read_first>
  <action>Extend approval and policy-sensitive workflow transitions so `AuditOutboxEvent` rows are created inside the same `Ecto.Multi` as the protected truth change per D-12 through D-16. Cover approval requested, approved, rejected, expired, sensitive MCP access granted or denied, and policy-sensitive tool invocations. Add the MCP seam explicitly in `lib/scoria/mcp/executor.ex`: when the executor authorizes a sensitive MCP access decision or begins a policy-sensitive tool invocation, wrap the reservation or local execution-state write and `create_audit_outbox_event/1` in one transactional handoff so the audit row exists before external fanout. Reuse `Scoria.Observe.Redactor` at the boundary so outbox rows store hashes, policy classes, trace IDs, run IDs, step IDs, and redacted references instead of raw arguments. Emit telemetry only after commit per D-13.</action>
  <acceptance_criteria>
    - `lib/scoria/workflows.ex` contains outbox creation inside workflow transactions
    - `lib/scoria/mcp/executor.ex` contains same-transaction outbox creation for sensitive MCP access grants or denials and policy-sensitive tool invocation attempts
    - `lib/scoria/observe/redactor.ex` is reused for audit payload shaping
    - `test/scoria/sre/audit_outbox_test.exs` proves rollback safety when the outbox insert fails
    - `test/scoria/mcp/executor_test.exs` proves sensitive MCP audit rows are written at the executor seam before the side effect is treated as complete
    - tests assert raw tool arguments are not persisted in outbox payloads
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/sre/audit_outbox_test.exs test/scoria/mcp/executor_test.exs</automated>
  </verify>
  <done>Audit delivery is durable, transactional, and redacted before any relay or external sink logic is involved.</done>
</task>

<task type="tdd">
  <name>Task 2: Build Incident Dedupe and Severity Routing</name>
  <files>lib/scoria/sre.ex, lib/scoria/sre/incident_manager.ex, test/scoria/sre/incident_test.exs</files>
  <read_first>
    - lib/scoria/sre.ex
    - .planning/phases/07-seismograph/07-RESEARCH.md
    - .planning/phases/07-seismograph/07-CONTEXT.md
  </read_first>
  <action>Create `Scoria.SRE.IncidentManager` and route `Scoria.SRE` alert APIs through it. Deduplicate incidents with a stable low-cardinality `incident_key` per D-22, attach `reason_code`, `severity`, `policy_key`, `trace_id`, `run_id`, `scorer_version`, and `baseline_version`, and apply the routing rules from D-08 through D-11 and D-23: fast-burning user-facing budgets and breaker trips page, while CI baseline dips and slower cost or quality regressions become review alerts or tickets by default. Preserve append-only `IncidentEvent` rows for every state transition and delivery-attempt append even though the actual relay worker lands in the next plan.</action>
  <acceptance_criteria>
    - `lib/scoria/sre/incident_manager.ex` contains stable incident-key generation
    - `lib/scoria/sre/incident_manager.ex` distinguishes review vs page severities
    - `test/scoria/sre/incident_test.exs` asserts scorer and baseline version evidence is preserved
    - `test/scoria/sre/incident_test.exs` asserts repeated equivalent alerts dedupe into one incident
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/sre/incident_test.exs</automated>
  </verify>
  <done>Alerts and regression signals become reviewable incidents with stable keys and explicit routing semantics before any adapter or relay runtime is introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local truth change -> audit outbox | Sensitive state changes become externalizable evidence |
| MCP execution seam -> audit outbox | Sensitive access decisions and policy-sensitive tool invocations become durable evidence |
| Telemetry and runtime signals -> incident routing | Review/page classification becomes durable operator-facing state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Repudiation | audit outbox | mitigate | Insert outbox rows in the same transaction as approval, MCP access, and policy-sensitive tool truth changes so crash windows cannot erase audit evidence. |
| T-07-04-02 | Denial of Service | incident routing | mitigate | Deduplicate with stable incident keys and separate review severity from paging severity to avoid alert storms. |
| T-07-04-03 | Information Disclosure | outbox payload shaping | mitigate | Reuse `Scoria.Observe.Redactor` so durable audit rows store redacted refs and hashes instead of raw payloads. |
</threat_model>

<verification>
- Approval, sensitive MCP access, and policy-sensitive tool events create outbox rows transactionally at the real execution seams.
- Equivalent alerts dedupe into stable incidents with correct routing.
- Relay startup and adapter delivery remain out of this slice, reducing context concentration and execution drift.
</verification>

<success_criteria>
- SRE-05 and the incident-routing portion of SRE-06 are implemented without sacrificing local durability.
- Operators can trust that later notifications will point back to preserved internal evidence rather than transient signals.
</success_criteria>

<output>
After completion, create `.planning/phases/07-seismograph/07-04-SUMMARY.md`
</output>
