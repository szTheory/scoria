---
phase: 07-seismograph
plan: 03
type: execute
wave: 4
depends_on: [02]
files_modified:
  - lib/scoria/sre/breaker_registry.ex
  - lib/scoria/sre/telemetry.ex
  - lib/scoria/sre/adapters/parapet.ex
  - lib/scoria/workflows/runtime.ex
  - lib/scoria/mcp/executor.ex
  - test/scoria/sre/telemetry_test.exs
  - test/scoria/workflows/runtime_test.exs
  - test/scoria/mcp/executor_test.exs
autonomous: true
requirements:
  - SRE-03
  - SRE-04
  - SRE-08
must_haves:
  truths:
    - "Provider, remote MCP, and other side-effecting integrations fail fast when a breaker is open."
    - "Local pure code paths and Ecto writes are not breaker-wrapped."
    - "Latency, cost, quality, and critical tool reliability signals emit reason-coded, low-cardinality evidence with version refs."
  artifacts:
    - path: "lib/scoria/sre/breaker_registry.ex"
      provides: "Integration-scoped Fuse registry and wrapper helpers"
    - path: "lib/scoria/sre/telemetry.ex"
      provides: "Reason-coded SLI/SLO telemetry helpers"
  key_links:
    - from: "lib/scoria/mcp/executor.ex"
      to: "lib/scoria/sre/breaker_registry.ex"
      via: "external-effect breaker gate"
      pattern: "fuse|breaker"
    - from: "lib/scoria/sre/telemetry.ex"
      to: "lib/scoria/sre/adapters/parapet.ex"
      via: "telemetry-driven external helper contract"
      pattern: "incident_key|scorer_version|baseline_version"
---

<objective>
Add circuit breakers and SLO-grade telemetry at the external-effect boundaries Scoria already controls.

Purpose: keep user-facing failures bounded, emit durable incident evidence with stable keys, and expose Parapet-friendly signals without introducing a hard dependency.
Output: breaker registry, runtime/executor breaker wiring, telemetry helpers, Parapet adapter, and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-seismograph/07-CONTEXT.md
@.planning/phases/07-seismograph/07-RESEARCH.md
@.planning/phases/07-seismograph/07-PATTERNS.md
@.planning/phases/07-seismograph/07-02-PLAN.md
@lib/scoria/workflows/runtime.ex
@lib/scoria/mcp/executor.ex
@lib/scoria/observe/telemetry.ex

<interfaces>
From lib/scoria/mcp/executor.ex:
```elixir
def execute(tool_module, args, context, timeout \\ 5000)
```

From lib/scoria/observe/telemetry.ex:
```elixir
def attach(buffer_name \\ Buffer)
def handle_event(event, measurements, metadata, config)
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Wrap External Effects in Integration-Scoped Breakers</name>
  <files>lib/scoria/sre/breaker_registry.ex, lib/scoria/workflows/runtime.ex, lib/scoria/mcp/executor.ex, test/scoria/workflows/runtime_test.exs, test/scoria/mcp/executor_test.exs</files>
  <read_first>
    - lib/scoria/workflows/runtime.ex
    - lib/scoria/mcp/executor.ex
    - .planning/phases/07-seismograph/07-CONTEXT.md
    - https://hexdocs.pm/fuse/fuse.html
  </read_first>
  <action>Create `Scoria.SRE.BreakerRegistry` to install and resolve stable Fuse keys for provider calls, remote MCP servers, and other side-effecting integrations per D-03. Update runtime and executor flows so external effects run through the breaker wrapper only when the step or tool context identifies a remote integration. Use integration-scoped keys such as provider name, MCP endpoint, or policy-sensitive tool target per D-22; do not key breakers by run or trace. Map blown breakers into reason-coded trip records and result envelopes that later alert slices can consume.</action>
  <acceptance_criteria>
    - `lib/scoria/sre/breaker_registry.ex` contains Fuse install and run helpers
    - `lib/scoria/mcp/executor.ex` handles breaker-open failures distinctly from timeouts
    - `lib/scoria/workflows/runtime.ex` does not wrap pure local handlers by default
    - tests assert only external-effect paths trip the breaker
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/workflows/runtime_test.exs test/scoria/mcp/executor_test.exs</automated>
  </verify>
  <done>External-effect failures are breaker-governed while local workflow logic remains unwrapped and simple.</done>
</task>

<task type="tdd">
  <name>Task 2: Emit Reason-Coded SLI Telemetry and Parapet Helper Envelopes</name>
  <files>lib/scoria/sre/telemetry.ex, lib/scoria/sre/adapters/parapet.ex, test/scoria/sre/telemetry_test.exs</files>
  <read_first>
    - lib/scoria/observe/telemetry.ex
    - .planning/phases/07-seismograph/07-RESEARCH.md
    - .planning/memory/parapet-synergy.md
  </read_first>
  <action>Implement `Scoria.SRE.Telemetry` as the public helper for Phase 7 signal emission per D-06 through D-11, D-20, and D-24. Emit low-cardinality events for latency, cost, quality, critical tool reliability, budget burn, and breaker state using stable `incident_key`, `reason_code`, `severity`, `trace_id`, `run_id`, `policy_key`, `scorer_version`, and `baseline_version` fields. Add `Scoria.SRE.Adapters.Parapet` as a small translation helper that consumes those events and reshapes them for external SLO consumers; keep it telemetry-driven and dependency-free instead of adding a deep Parapet adapter stack.</action>
  <acceptance_criteria>
    - `lib/scoria/sre/telemetry.ex` contains emitters for latency, cost, quality, and tool reliability
    - `lib/scoria/sre/telemetry.ex` includes `incident_key`
    - `lib/scoria/sre/adapters/parapet.ex` consumes telemetry-shaped envelopes, not DB structs directly
    - `test/scoria/sre/telemetry_test.exs` asserts scorer and baseline version metadata are preserved
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/sre/telemetry_test.exs</automated>
  </verify>
  <done>Phase 7 emits stable SLO evidence that external consumers can subscribe to without becoming a hard dependency of core Scoria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External integrations -> breaker wrapper | Remote failures cross into Scoria runtime control |
| Internal evidence -> external SLO consumers | SLI events leave Scoria through telemetry helpers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Denial of Service | breaker registry | mitigate | Use Fuse only on stable integration keys so repeated external failures open a bounded circuit instead of cascading across users. |
| T-07-03-02 | Information Disclosure | SRE telemetry | mitigate | Emit reason-coded, low-cardinality evidence with refs and versions instead of raw prompts or arguments. |
</threat_model>

<verification>
- Breakers open only for external-effect seams.
- Breaker-open and timeout paths remain distinguishable.
- SLI telemetry includes stable incident keys and version metadata for later incident dedupe.
</verification>

<success_criteria>
- SRE-03 is enforced without over-wrapping local code paths.
- SRE-04 has a telemetry contract rich enough for incidenting and external SLO consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/07-seismograph/07-03-SUMMARY.md`
</output>
