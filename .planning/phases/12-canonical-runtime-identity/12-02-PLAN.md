---
phase: 12-canonical-runtime-identity
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - lib/scoria/workflows.ex
  - lib/scoria/workflows/runtime.ex
  - lib/scoria/observe/approval.ex
  - lib/scoria/mcp/executor.ex
  - priv/repo/migrations/20260513000200_add_canonical_identity_to_approvals.exs
  - test/scoria/workflows/runtime_test.exs
  - test/scoria/workflows/integration_test.exs
  - test/scoria/mcp/executor_test.exs
  - test/scoria/sre/audit_outbox_test.exs
autonomous: true
requirements:
  - IDEN-02
must_haves:
  truths:
    - "Approval requests and decisions preserve the same actor, tenant, and session identity as the run root."
    - "Workflow runtime and MCP execution inherit root identity by default while keeping execution-only context separate."
    - "Current callers that still pass loose identity attrs continue to work, but those attrs are normalized and cannot silently replace immutable run-root identity."
  artifacts:
    - path: "priv/repo/migrations/20260513000200_add_canonical_identity_to_approvals.exs"
      provides: "Canonical approval identity columns and indexes"
    - path: "lib/scoria/observe/approval.ex"
      provides: "Approval schema with first-class actor_id, tenant_id, and session_id"
    - path: "lib/scoria/workflows.ex"
      provides: "Approval creation/decision propagation from immutable run-root identity"
    - path: "lib/scoria/mcp/executor.ex"
      provides: "Execution context contract that separates root identity from transient execution metadata"
  key_links:
    - from: "lib/scoria/workflows.ex"
      to: "lib/scoria/observe/approval.ex"
      via: "approval attrs inherit root identity columns"
      pattern: "actor_id|tenant_id|session_id"
    - from: "lib/scoria/workflows/runtime.ex"
      to: "lib/scoria/workflows.ex"
      via: "runtime derives root identity from run and passes only transient execution overlays separately"
      pattern: "run\\.(actor_id|tenant_id|session_id)"
    - from: "lib/scoria/workflows/runtime.ex"
      to: "lib/scoria/mcp/executor.ex"
      via: "runtime/execution context carries canonical root identity plus transient overlays"
      pattern: "actor_id|tenant_id|session_id|Executor\\.execute"
---

<objective>
Propagate immutable root identity through workflow, approval, and execution seams without letting approval fallbacks or request-scoped maps redefine canonical run truth.

Purpose: make approvals, workflow resume, MCP execution, and audit producers inherit the same actor, tenant, and session ids from the root run.
Output: approval identity migration/schema updates, workflow approval lineage fixes, runtime/executor identity propagation, and focused propagation tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-canonical-runtime-identity/12-CONTEXT.md
@.planning/phases/12-canonical-runtime-identity/12-RESEARCH.md
@.planning/phases/12-canonical-runtime-identity/12-PATTERNS.md
@.planning/phases/12-canonical-runtime-identity/12-VALIDATION.md
@.planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-01-SUMMARY.md
@.planning/phases/12-canonical-runtime-identity/12-01-PLAN.md

<interfaces>
From lib/scoria/workflows.ex:
```elixir
def mark_waiting_for_approval(run_id, step_id, attrs) do
  approval_attrs = Map.merge(attrs, %{workflow_run_id: run.id, step_id: step.id, checkpoint_id: checkpoint.id})
end

def approve(approval_id, status, attrs) when status in ["approved", "rejected", "expired"] do
  audit_context = approval_decision_context(repo, approval, attrs)
end
```

From lib/scoria/observe/approval.ex:
```elixir
schema "ai_approvals" do
  field(:session_id, :string)
  field(:run_id, :string)
  field(:workflow_run_id, :binary_id)
end
```

From lib/scoria/mcp/executor.ex:
```elixir
def execute(tool_module, args, context, timeout \\ 5000)
defp reserve_budget(tool_module, args, context)
defp policy_sensitive_audit_envelope(tool_module, args, context)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist approval identity and remove approval fallback drift</name>
  <read_first>lib/scoria/workflows.ex, lib/scoria/observe/approval.ex, priv/repo/migrations/20260510160812_create_ai_approvals.exs, priv/repo/migrations/20260511000200_link_approvals_to_workflows.exs, test/scoria/workflows/runtime_test.exs, test/scoria/workflows/integration_test.exs, test/scoria/sre/audit_outbox_test.exs, .planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-01-SUMMARY.md, .planning/phases/12-canonical-runtime-identity/12-PATTERNS.md</read_first>
  <files>lib/scoria/workflows.ex, lib/scoria/observe/approval.ex, priv/repo/migrations/20260513000200_add_canonical_identity_to_approvals.exs, test/scoria/workflows/runtime_test.exs, test/scoria/workflows/integration_test.exs, test/scoria/sre/audit_outbox_test.exs</files>
  <acceptance_criteria>The approval migration adds first-class `actor_id` and `tenant_id` columns plus useful indexes while preserving `session_id`; `Scoria.Observe.Approval` casts and validates the canonical fields; `mark_waiting_for_approval/3` copies canonical identity from the root run into approval rows and request audit evidence instead of rebuilding from loose attrs; `approval_decision_context/3` no longer falls back to `approval.session_id` as actor identity; runtime/integration/audit tests prove approved, rejected, and expired paths all retain the same root actor, tenant, and session ids.</acceptance_criteria>
  <action>Add `priv/repo/migrations/20260513000200_add_canonical_identity_to_approvals.exs` so `ai_approvals` becomes a first-class identity carrier per D-05, following the existing approval schema/migration analogs in `12-PATTERNS.md`. Update `Scoria.Observe.Approval` and `Scoria.Workflows.mark_waiting_for_approval/3` to inherit immutable run-root identity per D-09 and write it into both approval rows and approval-request audit evidence while keeping all truth changes inside the existing transaction shape. Refactor `Scoria.Workflows.approve/3` and `approval_decision_context/3` so approval decisions project canonical identity from durable run/approval truth, not from ad hoc fallback chains, removing the current `approval.session_id` actor proxy per D-11 and D-12. Keep metadata/evidence additive only and never allow it to override canonical columns per D-07.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/workflows/runtime_test.exs test/scoria/workflows/integration_test.exs test/scoria/sre/audit_outbox_test.exs</automated>
  </verify>
  <done>Approval rows and decision audit lineage now carry the same immutable actor, tenant, and session identity as the run root, and the old `session_id`-as-actor fallback is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Separate root identity from transient execution context across workflow and MCP seams</name>
  <read_first>lib/scoria/workflows/runtime.ex, lib/scoria/mcp/executor.ex, lib/scoria/identity.ex, test/scoria/mcp/executor_test.exs, test/scoria/workflows/runtime_test.exs, .planning/phases/12-canonical-runtime-identity/12-RESEARCH.md, .planning/phases/12-canonical-runtime-identity/12-PATTERNS.md</read_first>
  <files>lib/scoria/workflows.ex, lib/scoria/workflows/runtime.ex, lib/scoria/mcp/executor.ex, test/scoria/mcp/executor_test.exs, test/scoria/workflows/runtime_test.exs</files>
  <acceptance_criteria>`Scoria.Workflows.Runtime` derives canonical `actor_id`, `tenant_id`, and `session_id` from the persisted run before budget, breaker, approval, or handoff code runs; execution-only values such as trace, provider, cost, or tool metadata stay in separate transient maps; `Scoria.MCP.Executor` clearly distinguishes canonical identity fields from transient execution attrs while remaining backwards-compatible for already-normalized callers and current loose caller attrs; focused tests prove both seams preserve root identity and still accept current loose inputs without reintroducing edge-owned fallback rules.</acceptance_criteria>
  <action>Refactor `Scoria.Workflows.Runtime` per D-10 using the exact execution-seam analog already mapped in `12-PATTERNS.md`: derive immutable root identity from the persisted `run` first, then overlay trace/provider/tool metadata from separate transient maps before budget, breaker, or telemetry calls. Update any helper flow in `Scoria.Workflows` needed to carry `Scoria.Identity` through runtime and approval seams without mutating root ids. Because D-15 edge-adapter work is covered in `12-01`, keep this plan focused on downstream propagation: update `Scoria.MCP.Executor` to consume canonical identity plus explicit execution metadata while preserving its current pipeline shape from `12-PATTERNS.md`, without widening into new Phase 13 runtime APIs. Preserve backwards-compatible loose `tenant_id` / `actor_id` / `session_id` callers by normalizing them immediately per D-03 and D-16, but do not add new Plug/LiveView adapter scope here.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/mcp/executor_test.exs test/scoria/workflows/runtime_test.exs test/scoria/workflows/integration_test.exs</automated>
  </verify>
  <done>Workflow runtime and MCP execution inherit one immutable root identity contract, execution-time overlays are clearly separate, and current callers still work through immediate normalization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow root row → approval row | Canonical ids are copied from run truth into approval truth and audit evidence |
| workflow/runtime identity → MCP executor | Canonical root identity and explicit execution overlays enter tool execution |
| runtime/executor → budget/audit seams | Downstream policy and evidence systems consume inherited identity and execution metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | S | `lib/scoria/mcp/executor.ex` | mitigate | Consume canonical identity fields and immediate normalization helpers instead of trusting ad hoc execution maps as durable truth. |
| T-12-02-02 | T | `lib/scoria/workflows.ex` | mitigate | Copy canonical root identity into approval rows and block fallback chains that would silently rewrite actor/tenant lineage. |
| T-12-02-03 | R | `lib/scoria/workflows.ex` | mitigate | Persist approval identity and keep request/decision audit links so operator evidence can prove who requested and decided. |
| T-12-02-04 | I | `lib/scoria/mcp/executor.ex` | mitigate | Keep app-specific execution metadata in transient context and avoid storing framework structs or secrets as canonical identity. |
| T-12-02-05 | D | migration `20260513000200_add_canonical_identity_to_approvals.exs` | mitigate | Add indexes on approval identity fields used for joins and operator lookups instead of metadata scans. |
| T-12-02-06 | E | `lib/scoria/workflows/runtime.ex` | mitigate | Derive root identity from persisted runs and keep overlays separate so execution callers cannot escalate by mutating root ids mid-run. |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/workflows/runtime_test.exs test/scoria/workflows/integration_test.exs test/scoria/sre/audit_outbox_test.exs`
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/mcp/executor_test.exs`
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/workflows/runtime_test.exs test/scoria/workflows/integration_test.exs test/scoria/mcp/executor_test.exs test/scoria/sre/audit_outbox_test.exs`
</verification>

<success_criteria>
- Approval rows and approval audit evidence inherit canonical actor, tenant, and session identity from the run root.
- Workflow runtime and MCP execution use immutable root identity by default and keep transient execution context separate.
- Plug/LiveView/on-mount adapter scope remains owned by `12-01`; this plan only propagates the normalized contract downstream.
- Existing loose identity callers remain compatible through immediate normalization instead of silent fallback chains.
</success_criteria>

<output>
After completion, create `.planning/phases/12-canonical-runtime-identity/12-02-SUMMARY.md`
</output>
