---
phase: 12-canonical-runtime-identity
plan: 03
type: execute
wave: 3
depends_on:
  - 12-01
  - 12-02
files_modified:
  - lib/scoria/workflows/runtime.ex
  - lib/scoria/mcp/executor.ex
  - lib/scoria/sre/telemetry.ex
  - lib/scoria/sre/telemetry_identity.ex
  - test/scoria/workflows/runtime_telemetry_test.exs
  - test/scoria/mcp/executor_telemetry_test.exs
  - test/scoria/sre/telemetry_test.exs
  - test/scoria/sre/audit_outbox_test.exs
autonomous: true
requirements:
  - IDEN-02
must_haves:
  truths:
    - "Runtime, MCP, telemetry, and audit projections all agree on the same canonical run-root actor, tenant, and session identity."
    - "Telemetry keeps low-cardinality SRE labels while actor and session remain refs or evidence fields rather than grouping keys."
    - "Approval and tool audit evidence project canonical identity without metadata or transient execution attrs overwriting root truth."
  artifacts:
    - path: "lib/scoria/sre/telemetry_identity.ex"
      provides: "Low-cardinality telemetry projection sourced from canonical runtime identity"
    - path: "lib/scoria/workflows/runtime.ex"
      provides: "Runtime telemetry attrs built from canonical root identity plus explicit transient metadata"
    - path: "lib/scoria/mcp/executor.ex"
      provides: "MCP telemetry/audit attrs aligned to canonical identity contract"
    - path: "test/scoria/workflows/runtime_telemetry_test.exs"
      provides: "Workflow telemetry identity regression coverage"
  key_links:
    - from: "lib/scoria/workflows/runtime.ex"
      to: "lib/scoria/sre/telemetry_identity.ex"
      via: "runtime attrs derive telemetry labels/refs from canonical identity"
      pattern: "Telemetry\\.emit|tenant_id|run_id"
    - from: "lib/scoria/mcp/executor.ex"
      to: "lib/scoria/sre/telemetry_identity.ex"
      via: "MCP attrs project canonical identity without actor/session labels"
      pattern: "base_attrs|Telemetry\\.emit"
    - from: "test/scoria/sre/audit_outbox_test.exs"
      to: "lib/scoria/workflows.ex"
      via: "audit lineage assertions match canonical approval and run identity"
      pattern: "actor|tenant|session"
---

<objective>
Align telemetry and audit projections to the canonical runtime identity contract while preserving the existing low-cardinality SRE posture.

Purpose: finish the identity boundary correction so observability and evidence consumers read from the same root truth without turning actor/session into noisy telemetry labels.
Output: telemetry identity alignment in workflow and MCP seams, audit-lineage assertions, and focused low-cardinality regression coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-canonical-runtime-identity/12-CONTEXT.md
@.planning/phases/12-canonical-runtime-identity/12-RESEARCH.md
@.planning/phases/12-canonical-runtime-identity/12-PATTERNS.md
@.planning/phases/12-canonical-runtime-identity/12-VALIDATION.md
@.planning/phases/10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap/10-01-SUMMARY.md
@.planning/phases/10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap/10-02-SUMMARY.md
@.planning/phases/12-canonical-runtime-identity/12-02-PLAN.md

<interfaces>
From lib/scoria/sre/telemetry_identity.ex:
```elixir
@label_keys [:tenant_id, :subject_kind, :policy_key, :reason_code, :window_bucket, ...]
@ref_keys [:trace_id, :run_id, :workflow_run_id, ...]
def runtime_metadata(attrs)
def incident_metadata(attrs)
```

From lib/scoria/workflows/runtime.ex:
```elixir
defp base_runtime_attrs(step, run, budget_context, outcome) do
  %{tenant_id: Map.get(budget_context, :tenant_id, "system"), trace_id: Map.get(budget_context, :trace_id, step.id)}
end
```

From lib/scoria/mcp/executor.ex:
```elixir
defp base_attrs(tool_module, context, outcome) do
  %{tenant_id: Map.get(context, :tenant_id, "system"), trace_id: Map.get(context, :trace_id), run_id: Map.get(context, :run_id)}
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source telemetry projections from canonical identity without widening label cardinality</name>
  <read_first>lib/scoria/workflows/runtime.ex, lib/scoria/mcp/executor.ex, lib/scoria/sre/telemetry.ex, lib/scoria/sre/telemetry_identity.ex, test/scoria/workflows/runtime_telemetry_test.exs, test/scoria/mcp/executor_telemetry_test.exs, test/scoria/sre/telemetry_test.exs, .planning/phases/10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap/10-01-SUMMARY.md, .planning/phases/12-canonical-runtime-identity/12-PATTERNS.md</read_first>
  <files>lib/scoria/workflows/runtime.ex, lib/scoria/mcp/executor.ex, lib/scoria/sre/telemetry.ex, lib/scoria/sre/telemetry_identity.ex, test/scoria/workflows/runtime_telemetry_test.exs, test/scoria/mcp/executor_telemetry_test.exs, test/scoria/sre/telemetry_test.exs</files>
  <acceptance_criteria>`TelemetryIdentity` still groups on low-cardinality operational labels and explicitly does not add `actor_id` or `session_id` to label keys; runtime and MCP telemetry attrs source `tenant_id` and run refs from canonical run-root identity instead of ad hoc transient maps; actor/session appear only where needed as refs or evidence metadata, not grouping labels; focused telemetry tests prove completed, timeout, breaker, and access/audit paths keep low-cardinality labels while preserving canonical lineage.</acceptance_criteria>
  <action>Update `Scoria.Workflows.Runtime`, `Scoria.MCP.Executor`, `Scoria.SRE.Telemetry`, and `Scoria.SRE.TelemetryIdentity` so telemetry producers read canonical identity from the Phase 12 contract per D-12 and D-13 while preserving the low-cardinality label discipline established in Phase 10. Follow the concrete telemetry analogs in `12-PATTERNS.md`: keep `TelemetryIdentity` map-driven, source runtime/MCP base attrs from canonical identity first, and overlay only trace/provider/tool metadata after that. Keep `actor_id` and `session_id` out of grouping labels per the locked phase note and Phase 10 identity posture; if needed, project them only as refs or local evidence metadata. Remove remaining defaults that source tenant/run identity from transient execution maps when canonical root truth is available, but do not widen into new public runtime APIs or non-essential telemetry surface changes.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/workflows/runtime_telemetry_test.exs test/scoria/mcp/executor_telemetry_test.exs test/scoria/sre/telemetry_test.exs</automated>
  </verify>
  <done>Workflow and MCP telemetry both source canonical identity from the same root contract, while SRE labels remain low-cardinality and actor/session are not promoted to grouping keys.</done>
</task>

<task type="auto">
  <name>Task 2: Align audit evidence assertions to canonical identity lineage</name>
  <read_first>lib/scoria/workflows.ex, lib/scoria/mcp/executor.ex, test/scoria/sre/audit_outbox_test.exs, test/scoria/workflows/runtime_telemetry_test.exs, test/scoria/mcp/executor_telemetry_test.exs, .planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-01-SUMMARY.md, .planning/phases/12-canonical-runtime-identity/12-VALIDATION.md, .planning/phases/12-canonical-runtime-identity/12-PATTERNS.md</read_first>
  <files>lib/scoria/workflows/runtime.ex, lib/scoria/mcp/executor.ex, test/scoria/sre/audit_outbox_test.exs, test/scoria/workflows/runtime_telemetry_test.exs, test/scoria/mcp/executor_telemetry_test.exs</files>
  <acceptance_criteria>`test/scoria/sre/audit_outbox_test.exs` asserts approval-request, approval-decision, and policy-sensitive tool evidence preserve canonical actor, tenant, and session lineage from the run root; telemetry-focused tests cover any audit/telemetry boundary assumptions changed by the canonical identity refactor; no assertion depends on metadata overriding canonical columns; verify commands use the current targeted-lane constraint `SCORIA_DB_PORT=55432`.</acceptance_criteria>
  <action>Extend the audit and execution regression suites so approval evidence, policy-sensitive tool invocation evidence, and any runtime-to-audit projection all prove the same canonical identity lineage end to end per D-07, D-09, and D-12. Tighten assertions around metadata-secondary behavior so audit rows may carry extra execution context but cannot override root identity columns. Keep this slice strictly in Phase 12 scope: helper/adaptor work needed for identity alignment is allowed, but do not add Phase 13 start/resume/inspect APIs or broader operator UI work.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/sre/audit_outbox_test.exs test/scoria/workflows/runtime_telemetry_test.exs test/scoria/mcp/executor_telemetry_test.exs test/scoria/sre/telemetry_test.exs</automated>
  </verify>
  <done>Audit and telemetry regression coverage both prove canonical identity lineage is preserved end to end and that metadata remains secondary to durable identity columns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical run/approval truth → telemetry projection | Durable identity is translated into low-cardinality operational metadata |
| canonical truth → audit evidence | Root identity is copied into attributable evidence records and policy-sensitive tool traces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | S | `lib/scoria/sre/telemetry_identity.ex` | mitigate | Source labels/refs from canonical truth so transient caller maps cannot spoof operator identity. |
| T-12-03-02 | T | `lib/scoria/workflows/runtime.ex` | mitigate | Use canonical run identity for telemetry attrs and reject metadata as an override path for durable ids. |
| T-12-03-03 | R | `test/scoria/sre/audit_outbox_test.exs` | mitigate | Assert request/decision/tool evidence keeps attributable actor, tenant, session, and request linkage. |
| T-12-03-04 | I | `lib/scoria/sre/telemetry_identity.ex` | mitigate | Keep actor/session out of grouping labels to avoid leaking high-cardinality or sensitive identifiers into SRE dimensions. |
| T-12-03-05 | D | `lib/scoria/sre/telemetry.ex` | mitigate | Preserve low-cardinality labels and shared identity keys so telemetry volume and grouping remain operationally stable. |
| T-12-03-06 | E | `lib/scoria/mcp/executor.ex` | mitigate | Carry canonical identity into policy-sensitive audit envelopes so tool seams cannot attribute actions to mutated transient identities. |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/workflows/runtime_telemetry_test.exs test/scoria/mcp/executor_telemetry_test.exs test/scoria/sre/telemetry_test.exs`
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/sre/audit_outbox_test.exs`
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test`
</verification>

<success_criteria>
- Telemetry projections use canonical run-root identity as source while preserving the existing low-cardinality SRE label posture.
- Audit evidence and policy-sensitive tool traces preserve canonical actor, tenant, and session lineage end to end.
- Phase 12 finishes without widening into Phase 13 public runtime lifecycle APIs beyond helper/adaptor work required for identity normalization.
</success_criteria>

<output>
After completion, create `.planning/phases/12-canonical-runtime-identity/12-03-SUMMARY.md`
</output>
