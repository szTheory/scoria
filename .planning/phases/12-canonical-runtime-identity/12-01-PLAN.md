---
phase: 12-canonical-runtime-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria.ex
  - lib/scoria/identity.ex
  - lib/scoria/mcp/router.ex
  - lib/scoria/workflows/run.ex
  - lib/scoria/workflows.ex
  - priv/repo/migrations/20260513000100_add_canonical_identity_to_workflow_runs.exs
  - test/scoria/identity_test.exs
  - test/scoria/workflows_test.exs
autonomous: true
requirements:
  - IDEN-01
must_haves:
  truths:
    - "A caller can supply actor, tenant, and session identity through one canonical Scoria runtime envelope."
    - "Plug assigns, LiveView session data, and on-mount extracted identity all normalize into the same Scoria.Identity contract before entering runtime seams."
    - "Run creation persists actor_id, tenant_id, and session_id on the workflow root row instead of hiding them in metadata."
    - "Legacy callers that still pass loose actor_id, tenant_id, or session_id attrs are normalized into the same canonical contract."
  artifacts:
    - path: "lib/scoria/identity.ex"
      provides: "Canonical runtime identity envelope plus Plug/LiveView/on-mount edge adapters"
    - path: "lib/scoria/mcp/router.ex"
      provides: "Concrete Plug boundary normalization into the canonical identity contract"
    - path: "lib/scoria/workflows/run.ex"
      provides: "First-class durable actor_id, tenant_id, and session_id run fields"
    - path: "priv/repo/migrations/20260513000100_add_canonical_identity_to_workflow_runs.exs"
      provides: "Workflow-run canonical identity columns and indexes"
    - path: "test/scoria/identity_test.exs"
      provides: "Normalization and edge-adapter compatibility coverage"
  key_links:
    - from: "lib/scoria.ex"
      to: "lib/scoria/identity.ex"
      via: "public helper/export for canonical runtime noun"
      pattern: "alias Scoria\\.Identity|def .*identity"
    - from: "lib/scoria/mcp/router.ex"
      to: "lib/scoria/identity.ex"
      via: "Plug edge assigns normalize before executor/runtime use"
      pattern: "Identity\\.(from_conn_assigns|from_assigns|normalize)"
    - from: "lib/scoria/workflows.ex"
      to: "lib/scoria/identity.ex"
      via: "create_run normalization before persistence"
      pattern: "Identity\\.(new|normalize)"
    - from: "lib/scoria/workflows/run.ex"
      to: "priv/repo/migrations/20260513000100_add_canonical_identity_to_workflow_runs.exs"
      via: "schema fields match migrated columns"
      pattern: "field :actor_id|field :tenant_id|field :session_id"
---

<objective>
Establish `Scoria.Identity` as the one canonical runtime identity envelope and make workflow runs persist immutable root identity at creation time.

Purpose: remove ad hoc app-facing identity nouns before approval, MCP, telemetry, and audit seams inherit them.
Output: a canonical identity module, explicit host-edge adapters, minimal public helper exposure, workflow-run identity migration, and focused normalization/persistence tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-canonical-runtime-identity/12-CONTEXT.md
@.planning/phases/12-canonical-runtime-identity/12-RESEARCH.md
@.planning/phases/12-canonical-runtime-identity/12-PATTERNS.md
@.planning/phases/12-canonical-runtime-identity/12-VALIDATION.md
@.planning/phases/05-caldera/05-01-SUMMARY.md

<interfaces>
From lib/scoria/workflows/run.ex:
```elixir
schema "ai_workflow_runs" do
  field :session_id, :string
  field :root_role_id, :string
  field :metadata, :map, default: %{}
end

def changeset(run, attrs)
```

From lib/scoria/workflows.ex:
```elixir
def create_run(attrs \\ %{}) do
  {initial_step, run_attrs} = Map.pop(attrs, :initial_step)
  Multi.insert(:run, Run.changeset(%Run{}, Map.merge(%{status: "running", started_at: now}, run_attrs)))
end
```

From lib/scoria.ex:
```elixir
def hello do
  :world
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the canonical identity envelope and compatibility normalizers</name>
  <read_first>lib/scoria.ex, lib/scoria_web/router.ex, lib/scoria/sre/telemetry_identity.ex, test/scoria/sre/telemetry_test.exs, lib/scoria/workflows.ex, lib/scoria/mcp/router.ex, .planning/phases/12-canonical-runtime-identity/12-CONTEXT.md, .planning/phases/12-canonical-runtime-identity/12-RESEARCH.md, .planning/phases/12-canonical-runtime-identity/12-PATTERNS.md</read_first>
  <files>lib/scoria.ex, lib/scoria/identity.ex, lib/scoria/mcp/router.ex, test/scoria/identity_test.exs</files>
  <acceptance_criteria>`lib/scoria/identity.ex` defines a `%Scoria.Identity{actor_id, tenant_id, session_id, metadata}` struct; normalization accepts a struct, a plain map, convenience top-level `actor` / `tenant` / `session` inputs, explicit Plug `conn.assigns` maps, LiveView session assigns, and on-mount extracted attrs; legacy loose `actor_id` / `tenant_id` / `session_id` attrs still normalize into the envelope; `lib/scoria.ex` exposes only identity-helper/adaptor work and does not add Phase 13 start/resume/inspect APIs; `lib/scoria/mcp/router.ex` uses the canonical adapter instead of acting as its own identity contract; `test/scoria/identity_test.exs` proves each adapter shape with passing assertions.</acceptance_criteria>
  <action>Create `Scoria.Identity` per D-01 and D-02 as the single canonical runtime noun with flat `actor_id`, `tenant_id`, `session_id`, and bounded `metadata`, using `Scoria.SRE.TelemetryIdentity` as the concrete normalization/helper analog from `12-PATTERNS.md`: map-driven input, immediate atom/string-key normalization, and small helper functions instead of caller-side fallback chains. Implement immediate normalization per D-03 so callers may still pass top-level sugar or loose attrs, but every downstream seam receives the envelope. Cover D-15 explicitly in this plan by adding adapter helpers for common host-app patterns: Plug `conn.assigns`, LiveView session assigns, and on-mount extracted identity maps should all normalize into `Scoria.Identity` without leaking framework structs into durable internals per D-04 and D-14. Update `lib/scoria/mcp/router.ex` as the concrete Plug seam to call the adapter helper rather than passing raw assigns downstream, and add targeted tests in `test/scoria/identity_test.exs` for constructor, conn-assigns, session-assigns, and on-mount extraction cases. Do not add public run start/resume/inspect APIs in `lib/scoria.ex`; Phase 12 only exposes helper/adaptor work needed for the identity noun.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/identity_test.exs</automated>
  </verify>
  <done>`Scoria.Identity` is the only public identity envelope, constructor and edge-adapter inputs all normalize into it before runtime use, the Plug boundary delegates to the shared adapter, and no new Phase 13 runtime entrypoints were introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Persist immutable run-root identity on workflow truth rows</name>
  <read_first>lib/scoria/workflows/run.ex, lib/scoria/workflows.ex, priv/repo/migrations/20260511000100_create_workflow_tables.exs, .planning/phases/05-caldera/05-CONTEXT.md, .planning/phases/12-canonical-runtime-identity/12-VALIDATION.md, .planning/phases/12-canonical-runtime-identity/12-PATTERNS.md</read_first>
  <files>lib/scoria/workflows/run.ex, lib/scoria/workflows.ex, priv/repo/migrations/20260513000100_add_canonical_identity_to_workflow_runs.exs, test/scoria/workflows_test.exs</files>
  <acceptance_criteria>The migration adds `actor_id` and `tenant_id` columns plus operator-query indexes to `ai_workflow_runs` while preserving `session_id`; `Scoria.Workflows.Run` exposes first-class fields and still treats `metadata` as secondary; `Scoria.Workflows.create_run/1` normalizes once through `Scoria.Identity`, persists canonical columns, snapshots canonical identity in initial checkpoint/event metadata without allowing metadata to override columns, and preserves backwards-compatible loose attr callers; `test/scoria/workflows_test.exs` proves a run stores the canonical ids exactly once.</acceptance_criteria>
  <action>Add `priv/repo/migrations/20260513000100_add_canonical_identity_to_workflow_runs.exs` to extend `ai_workflow_runs` with first-class `actor_id` and `tenant_id` plus indexes needed for operator filtering and joins per D-05, following the existing workflow-table and run-schema analogs called out in `12-PATTERNS.md`. Update `Scoria.Workflows.Run` and `Scoria.Workflows.create_run/1` so root identity is normalized once, persisted as durable columns, and treated as immutable root truth per D-07 and D-09 while keeping the current `Ecto.Multi` transaction shape intact. Keep any app-specific labels in `metadata` only as additive context per D-06, and explicitly avoid any new normalized envelope table per D-08. Preserve compatibility for existing callers that still pass loose `tenant_id`, `actor_id`, or `session_id` attrs by normalizing them before insert rather than forcing a breaking contract rewrite.</action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/workflows_test.exs test/scoria/identity_test.exs</automated>
  </verify>
  <done>Workflow root rows persist canonical actor, tenant, and session identity as durable columns, run creation is the single normalization seam, and metadata can no longer silently replace canonical identity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host app caller / Plug or LiveView edge → `Scoria.Identity` | Untrusted app/runtime input becomes the canonical identity contract |
| `Scoria.Identity` → workflow persistence | Canonical ids cross from normalized input into durable root-truth rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | S | `lib/scoria/identity.ex` | mitigate | Accept only explicit canonical fields and documented sugar plus named edge adapters; normalize once and reject implicit framework-struct identity as root truth. |
| T-12-01-02 | T | `lib/scoria/workflows.ex` | mitigate | Persist canonical ids to dedicated columns and ignore metadata attempts to override them after normalization. |
| T-12-01-03 | R | `lib/scoria/workflows.ex` | mitigate | Snapshot canonical root identity at run creation so later approvals/audit rows can attribute to immutable root truth. |
| T-12-01-04 | I | `lib/scoria.ex` | accept | Public helper exposure is limited to canonical id construction; no additional runtime data is exposed in this phase. |
| T-12-01-05 | D | migration `20260513000100_add_canonical_identity_to_workflow_runs.exs` | mitigate | Add narrow indexes for `tenant_id`, `actor_id`, and existing `session_id` operator queries instead of forcing metadata scans. |
| T-12-01-06 | E | `lib/scoria/workflows.ex` | mitigate | Keep root identity immutable after create so callers cannot escalate privileges by mutating root ids mid-run. |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/identity_test.exs`
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/workflows_test.exs`
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test test/scoria/identity_test.exs test/scoria/workflows_test.exs`
</verification>

<success_criteria>
- `Scoria.Identity` exists as the single public runtime identity envelope for actor, tenant, and session ids.
- Plug assigns, LiveView session maps, and on-mount extracted attrs all normalize through explicit helpers into the same envelope.
- `Scoria.Workflows.create_run/1` persists canonical run-root identity with backwards-compatible normalization for loose attrs.
- Workflow truth now stores canonical ids in durable columns and no new public run lifecycle API was introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/12-canonical-runtime-identity/12-01-SUMMARY.md`
</output>
