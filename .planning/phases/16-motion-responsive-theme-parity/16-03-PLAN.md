---
phase: 16-motion-responsive-theme-parity
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - assets/css/06-utilities.css
  - assets/css/04-components.css
  - lib/scoria_web/live/incidents_live/index.ex
  - lib/scoria_web/live/review_queue_live.ex
  - lib/scoria_web/live/workflow_live/show.ex
  - lib/scoria_web/components/incident_evidence_component.ex
  - lib/scoria_web/components/semantic_evidence_notebook_component.ex
  - lib/scoria_web/components/replay_evidence_notebook_component.ex
autonomous: true
requirements: [MOTION-03]
must_haves:
  truths:
    - "No HEEx callsite under lib/scoria_web/ uses unsupported responsive utility syntax (sm:grid-cols-*, arbitrary lg:/xl:grid-cols-[...])"
    - "Every replaced responsive grid renders its intended multi-column layout at md/lg/xl via a supported utility or named Scoria component class"
    - "Replacements are reusable design-system classes (named Scoria grid classes / supported utilities) rather than per-screen one-offs, so the fix pays dividends across screens (D-33)"
    - "Workflow show mobile layout stacks trace/span list first, then selected detail, then evidence notebook (no fake card clone)"
    - "Evidence/comparison grids resolve to named two-column component classes bound to semantic tokens"
  artifacts:
    - path: "assets/css/04-components.css"
      provides: "Named two-column evidence/comparison grid component classes (responsive at the intended breakpoint)"
    - path: "assets/css/06-utilities.css"
      provides: "Any added general token-bound responsive utilities required by replacements"
    - path: "lib/scoria_web/live/workflow_live/show.ex"
      provides: "Mobile-first stacked workflow layout using a supported/named responsive class"
  key_links:
    - from: "lib/scoria_web/live/incidents_live/index.ex"
      to: "supported responsive class"
      via: "replaced sm:/xl: arbitrary grid utilities"
      pattern: "scoria-"
    - from: "lib/scoria_web/components/incident_evidence_component.ex"
      to: "named evidence grid class in 04-components.css"
      via: "class replacement"
      pattern: "scoria-"
---

<objective>
Eliminate every unsupported responsive utility callsite (`sm:grid-cols-*`, arbitrary `lg:grid-cols-[...]` / `xl:grid-cols-[...]`) under `lib/scoria_web/` (D-12, Pitfall #2). These classes look valid in HEEx but have NO matching CSS, so the layouts silently fail to go multi-column. Replace each with either a general token-bound utility added to `06-utilities.css` or a named Scoria component class in `04-components.css`, modeled on the existing named two-column evidence class (`04-components.css:885`). Per D-33, favor reusable design-system classes (named grid component classes and supported utilities) over per-screen one-offs so the responsive grid fix pays dividends across every screen that needs the same split. Make the Workflow show object page stack correctly on mobile per D-11: object header → next-step verbs → trace/span list → selected detail → evidence notebook stack.

Purpose: Closes the second baseline gap (unsupported utility syntax) so responsive layouts actually adapt at md/lg/xl. Implements D-11, D-12, D-33, and the "Unsupported utilities must be replaced" clause of the UI design contract.
Output: Named/general responsive grid CSS plus 7 callsite replacements (3 LiveViews, 3 evidence components) with intentional mobile-first stacking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-motion-responsive-theme-parity/16-CONTEXT.md
@.planning/phases/16-motion-responsive-theme-parity/16-UI-SPEC.md
@.planning/phases/16-motion-responsive-theme-parity/16-PATTERNS.md

<interfaces>
<!-- Supported responsive utility block (06-utilities.css:166-204) — these are the ONLY -->
<!--   responsive utilities that exist; anything else has no CSS: -->
<!--   @media (min-width: 768px) { .md\:grid-cols-2, .md\:grid-cols-3 } -->
<!--   @media (min-width: 1024px) { .lg\:grid-cols-2, .lg\:flex-row } -->
<!--   @media (min-width: 1280px) { .xl\:grid-cols-4 } -->
<!-- Named two-column evidence/comparison precedent (04-components.css:885): -->
<!--   grid-template-columns: minmax(120px, 0.36fr) minmax(0, 1fr) -->
<!-- Color-bearing utilities resolve to semantic tokens (06-utilities.css:120-143) — preserve so DS-06 stays green. -->

<!-- The 7 callsites with unsupported syntax (verified by grep): -->
<!--   lib/scoria_web/live/incidents_live/index.ex:67  grid gap-4 sm:grid-cols-3 mb-6 -->
<!--   lib/scoria_web/live/incidents_live/index.ex:73  grid gap-6 xl:grid-cols-[0.85fr,1.15fr] -->
<!--   lib/scoria_web/live/review_queue_live.ex:74      grid gap-6 lg:grid-cols-[minmax(0,1.05fr)_minmax(22rem,0.95fr)] -->
<!--   lib/scoria_web/live/workflow_live/show.ex:223    grid gap-6 lg:grid-cols-[minmax(0,1.2fr)_minmax(20rem,0.8fr)]  (D-11 stack target) -->
<!--   lib/scoria_web/components/incident_evidence_component.ex:57         grid gap-4 xl:grid-cols-[1.25fr,0.9fr] -->
<!--   lib/scoria_web/components/semantic_evidence_notebook_component.ex:22 grid gap-4 xl:grid-cols-[1.1fr,0.9fr] -->
<!--   lib/scoria_web/components/replay_evidence_notebook_component.ex:47   grid gap-4 xl:grid-cols-[1.25fr,0.9fr] -->

<!-- NOTE: review_queue_live.ex:74 also gets mobile summary adoption in plan 16-04, which edits a DIFFERENT region of that file. -->
<!-- This plan edits ONLY the table block + named-grid block of 04-components.css; plan 16-01 owns the shell block, 16-02 owns the viewport block. Append new named classes at end of file to avoid overlap. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add named/general responsive grid CSS for evidence + comparison + page-split layouts</name>
  <files>assets/css/04-components.css, assets/css/06-utilities.css</files>
  <read_first>
    - assets/css/06-utilities.css (supported responsive block lines 166-204; token-bound color utilities lines 120-143)
    - assets/css/04-components.css (named two-column evidence precedent line 885; nearby evidence/notebook grid blocks)
    - .planning/phases/16-motion-responsive-theme-parity/16-PATTERNS.md (06-utilities.css section + callsite table)
    - .planning/phases/16-motion-responsive-theme-parity/16-UI-SPEC.md (Tables and Scan Surfaces — utilities replacement clause)
  </read_first>
  <action>
    Define the responsive grid CSS the callsite replacements will use, choosing named Scoria component classes (preferred for the bespoke fr ratios) over arbitrary utilities. Per D-33, design these as reusable, dividend-paying design-system classes that any screen needing the same split can adopt — not single-callsite one-offs. Collapse near-identical ratios into one shared class so the same primitive serves all the evidence callsites. Append to `04-components.css` (end of file, to avoid overlap with the shell block owned by 16-01 and the table viewport block owned by 16-02):

    - `.scoria-evidence-split` — single column by default (mobile-first), two columns at the intended breakpoint matching the original ratios. The three evidence/comparison callsites used `xl:grid-cols-[1.25fr,0.9fr]` / `[1.1fr,0.9fr]` — model these on the existing named two-column evidence class at line 885 (`grid-template-columns: minmax(120px, 0.36fr) minmax(0, 1fr)`), using `@media (min-width: 1280px)` (the `xl` tier) so behavior matches the original intent. A single `.scoria-evidence-split` with `~1.25fr 0.9fr` is acceptable for all three since the ratios are near-identical (this single shared class is the D-33 dividend); if a distinct ratio is genuinely needed, add a second modifier rather than arbitrary inline cols.
    - `.scoria-page-split` (or two modifiers) — for the Review Queue (`lg:grid-cols-[minmax(0,1.05fr)_minmax(22rem,0.95fr)]`) and Workflow show (`lg:grid-cols-[minmax(0,1.2fr)_minmax(20rem,0.8fr)]`) primary/detail page splits: single column by default, two columns at `@media (min-width: 1024px)` (the `lg` tier) preserving the `minmax` detail-rail behavior. One shared page-split primitive serving both screens is the intended D-33 outcome.
    - For the Incidents stat strip (`sm:grid-cols-3`): the `sm` tier is unsupported entirely. Either reuse the existing supported `.md\:grid-cols-3` utility (shifts the 3-up to >=768px) or add a general token-bound `.scoria-stat-grid` that goes 3-up at `@media (min-width: 768px)`. Prefer the existing `.md\:grid-cols-3` utility to avoid a new class. The Incidents content split (`xl:grid-cols-[0.85fr,1.15fr]`) gets a `.scoria-page-split--reverse` (or its own named class) two-column at the `xl` tier.

    All new grid classes are scoped under `.scoria-root`, mobile-first (single column default), use `gap` via `var(--scoria-space-*)` tokens matching the original `gap-4`/`gap-6`, and contain no color (so DS-06 is unaffected). Do not add arbitrary-value utilities to `06-utilities.css`; only add a general token-bound utility there if a replacement genuinely needs a broadly reusable one.
  </action>
  <verify>
    <automated>grep -qE "scoria-evidence-split|scoria-page-split" assets/css/04-components.css && grep -qE "min-width: 1024px|min-width: 1280px" assets/css/04-components.css && mix test test/scoria_web/ds06_drift_guard_test.exs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `04-components.css` defines named responsive grid classes (`.scoria-evidence-split` and a page-split class) that are single-column by default and multi-column inside `@media (min-width: 1024px)` and/or `@media (min-width: 1280px)`
    - The new grid classes are reusable design-system primitives (shared across the evidence/page-split callsites) rather than single-callsite one-offs (D-33)
    - New grid classes contain no raw hex / palette colors (gaps use `--scoria-space-*` tokens)
    - No arbitrary-value utility (`grid-cols-[...]`) is introduced into `06-utilities.css`
    - `mix test test/scoria_web/ds06_drift_guard_test.exs` passes
  </acceptance_criteria>
  <done>Named, mobile-first, token-bound responsive grid classes exist for evidence splits and page splits at the correct lg/xl breakpoints; DS-06 green.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all 7 unsupported responsive utility callsites</name>
  <files>lib/scoria_web/live/incidents_live/index.ex, lib/scoria_web/live/review_queue_live.ex, lib/scoria_web/live/workflow_live/show.ex, lib/scoria_web/components/incident_evidence_component.ex, lib/scoria_web/components/semantic_evidence_notebook_component.ex, lib/scoria_web/components/replay_evidence_notebook_component.ex</files>
  <read_first>
    - lib/scoria_web/live/incidents_live/index.ex (lines 60-80 — stat strip line 67, content split line 73)
    - lib/scoria_web/live/review_queue_live.ex (lines 68-80 — page split line 74)
    - lib/scoria_web/live/workflow_live/show.ex (lines 215-240 — page split line 223 + surrounding render order for D-11 stacking)
    - lib/scoria_web/components/incident_evidence_component.ex (line 57 evidence grid)
    - lib/scoria_web/components/semantic_evidence_notebook_component.ex (line 22 evidence grid)
    - lib/scoria_web/components/replay_evidence_notebook_component.ex (line 47 comparison grid)
    - assets/css/04-components.css (the named grid classes added in Task 1)
    - .planning/phases/16-motion-responsive-theme-parity/16-UI-SPEC.md (workflow/evidence mobile layout clause, D-11)
  </read_first>
  <action>
    Replace each unsupported `class` value with the named class added in Task 1 (or the supported `.md\:grid-cols-3` utility), keeping the original `gap` intent. Reuse the shared design-system classes across all callsites with the same split rather than minting per-screen variants (D-33):

    - `incidents_live/index.ex:67` `grid gap-4 sm:grid-cols-3 mb-6` → use the supported `md:grid-cols-3` utility (e.g. `grid gap-4 md:grid-cols-3 mb-6`) or the `.scoria-stat-grid` named class so the 3-up resolves at >=768px.
    - `incidents_live/index.ex:73` `grid gap-6 xl:grid-cols-[0.85fr,1.15fr]` → the named page-split class (xl tier) defined in Task 1.
    - `review_queue_live.ex:74` `grid gap-6 lg:grid-cols-[minmax(0,1.05fr)_minmax(22rem,0.95fr)]` → the named page-split class (lg tier). Edit ONLY this line's class attribute; do not touch other regions (plan 16-04 adds mobile summaries elsewhere in this file).
    - `workflow_live/show.ex:223` `grid gap-6 lg:grid-cols-[minmax(0,1.2fr)_minmax(20rem,0.8fr)]` → the named page-split class (lg tier). Verify the mobile (single-column) DOM order produces the D-11 stack: object header, next-step verbs, trace/span list, then selected detail, then evidence notebook stack. If the current source order would render the detail rail before the trace list on mobile, reorder the markup so the trace/span list comes first in source order (single column follows source order); keep desktop two-column behavior identical via the named class.
    - `incident_evidence_component.ex:57`, `semantic_evidence_notebook_component.ex:22`, `replay_evidence_notebook_component.ex:47` (`xl:grid-cols-[...]`) → the `.scoria-evidence-split` named class.

    Do not introduce any raw palette classes (D-27). Preserve all other classes on each element (e.g. `gap-*`, `mb-*` if those are supported utilities — verify against 06-utilities.css; if a `gap-*` is unsupported, fold it into the named grid class's own `gap`).
  </action>
  <verify>
    <automated>! grep -rnE "sm:grid-cols|grid-cols-\[" lib/scoria_web/ | grep -v "_test" && mix compile --warnings-as-errors 2>&1 | tail -5 && mix test test/scoria_web/ds06_drift_guard_test.exs 2>&1 | tail -5 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "sm:grid-cols|grid-cols-\[" lib/scoria_web/` (excluding tests) returns NO matches
    - Each of the 7 callsites now references a class defined in `04-components.css` or a supported utility in `06-utilities.css`
    - Callsites with the same split reuse the shared design-system class rather than per-screen one-offs (D-33)
    - `workflow_live/show.ex` mobile (single-column) source order is: object header → next-step verbs → trace/span list → selected detail → evidence notebook (D-11)
    - `mix compile --warnings-as-errors` succeeds
    - `mix test test/scoria_web/ds06_drift_guard_test.exs` passes
  </acceptance_criteria>
  <done>Zero unsupported responsive utilities remain under lib/scoria_web/; all replaced layouts use supported/named classes; workflow show stacks per D-11; app compiles clean and DS-06 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CSS class renaming and HEEx presentation changes only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. |

Assessment: No new attack surface — these are CSS class definitions and HEEx `class` attribute replacements only. No new inputs, routes, auth boundaries, or data flows; HEEx escaping is unchanged.
</threat_model>

<verification>
- `grep -rnE "sm:grid-cols|grid-cols-\[" lib/scoria_web/` (non-test) returns nothing.
- `mix compile --warnings-as-errors` clean.
- `mix test test/scoria_web/ds06_drift_guard_test.exs` green.
- (Wave 5 plan adds Playwright proof that these layouts go multi-column at md/lg/xl and single-column at 375px without overflow.)
</verification>

<success_criteria>
- No unsupported responsive utility syntax remains under `lib/scoria_web/`.
- Each replaced grid renders multi-column at its intended md/lg/xl breakpoint and single-column on mobile.
- Replacements are reusable design-system classes that pay dividends across screens (D-33), not per-screen one-offs.
- Workflow show stacks trace/span list before selected detail/evidence on mobile.
- App compiles clean; DS-06 ratchet green.
</success_criteria>

<output>
Create `.planning/phases/16-motion-responsive-theme-parity/16-03-SUMMARY.md` when done.
</output>
</output>
