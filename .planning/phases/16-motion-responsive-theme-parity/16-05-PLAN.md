---
phase: 16-motion-responsive-theme-parity
plan: 05
type: execute
wave: 5
depends_on: ["16-01", "16-02", "16-03"]
files_modified:
  - assets/css/01-reset.css
  - assets/css/04-components.css
  - assets/css/05-motion.css
  - lib/scoria_web/ui.ex
  - test/scoria_web/ui_component_test.exs
autonomous: false
requirements: [MOTION-01, MOTION-02, MOTION-04]
must_haves:
  truths:
    - "Every interactive element shows a visible focus-visible indicator in both light and dark themes, not clipped by table/evidence/drawer overflow containers"
    - "Status (badges, selected rows, sort, nav active, approval highlight, evidence outcome) carries text/icon/ARIA/structure in addition to tone color"
    - "All interaction motion is transform/opacity only, <=200ms, and collapses under prefers-reduced-motion; no transition:all, no infinite interaction loops, no layout-property animation"
    - "Allowlisted exceptions (skeleton pulse, approval/attention cue) stay finite/opacity-only and static under reduced motion"
  artifacts:
    - path: "assets/css/04-components.css"
      provides: "Focus-ring preservation inside overflow/raised surfaces in both themes"
    - path: "test/scoria_web/ui_component_test.exs"
      provides: "Assertions that status indicators carry non-color text/structure"
    - path: "assets/css/05-motion.css"
      provides: "Verified transform/opacity-only, <=200ms interaction motion with reduced-motion guard intact"
  key_links:
    - from: "assets/css/01-reset.css :focus-visible"
      to: "component overflow/raised surfaces"
      via: "outline not clipped; sufficient contrast in both themes"
      pattern: "focus-visible"
    - from: "lib/scoria_web/ui.ex status indicators"
      to: "text/aria/structure"
      via: "non-color-only status"
      pattern: "aria-"
---

<objective>
Audit and harden focus visibility, non-color-only status, and motion-contract conformance across the now-responsive component system (after shell, table viewport, and grid changes land). Ensure `:focus-visible` stays visible in both light and dark themes and is not clipped by the new table/evidence/drawer overflow containers (D-23), reinforce that every status surface pairs tone color with text/icon/ARIA/structure (D-24/MOTION-02), and verify all interaction motion is transform/opacity-only, ≤200ms, reduced-motion-safe, with allowlisted skeleton/attention exceptions kept finite and static under reduced motion (D-15 through D-22). A human checkpoint confirms brand-fit of motion and focus visibility in both themes (judgment-based, per the validation strategy's manual-only items).

Purpose: Closes the focus/status/motion polish bar in both themes — the cross-cutting quality gate of the phase. Implements the Motion and Focus/Status/Accessibility sections of the UI design contract.
Output: Focus-ring overflow/contrast fixes at the component layer, status-indicator reinforcement, a motion-contract verification pass, component-test assertions, and a human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-motion-responsive-theme-parity/16-CONTEXT.md
@.planning/phases/16-motion-responsive-theme-parity/16-UI-SPEC.md
@.planning/phases/16-motion-responsive-theme-parity/16-PATTERNS.md
@.planning/phases/16-motion-responsive-theme-parity/16-01-SUMMARY.md
@.planning/phases/16-motion-responsive-theme-parity/16-02-SUMMARY.md

<interfaces>
<!-- Global focus-visible baseline (01-reset.css:64-68): -->
<!--   .scoria-root :where(:focus-visible) { outline: 2px solid var(--scoria-focus-ring); outline-offset: 2px; ... } -->
<!-- Focus-ring token (02-tokens.css): light --scoria-focus-ring: var(--scoria-molten-400) (line 117); -->
<!--   dark --scoria-focus-ring: var(--scoria-600) (line 187). Token edits only for a real WCAG defect (D-25). -->
<!-- Risk: outline-offset:2px can be clipped by overflow:hidden/auto containers (new table viewport, drawers). -->
<!--   Fix at the container (allow ring to show / overflow-clip-margin / inset focus style) NOT by removing the ring. -->

<!-- Status patterns already non-color-only (reinforce, don't regress): -->
<!--   badge/1 (ui.ex:68-70) tone class + label text; sort SVG icon + aria-sort (added in 16-02); -->
<!--   nav aria-current="page" + inset box-shadow (04-components.css:92-96). -->
<!-- Motion baseline already clean: no `transition: all`, no `infinite` in 05-motion.css (verified). -->
<!-- Reduced-motion kill switch (05-motion.css:55-64) targets .scoria-root * — must stay authoritative/unlayered. -->
<!-- Allowlisted exceptions: scoria-attention 2-cycle approval pulse, scoria-skeleton-pulse opacity-only. -->

<!-- This plan edits the focus/status REGIONS of 04-components.css (drawer/table/evidence overflow + selected-row -->
<!--   structure). Append/adjust at those blocks; shell block (16-01), viewport block (16-02), and named-grid -->
<!--   block (16-03) already landed in prior waves — do not revert them. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Focus-visible preservation + non-color-only status reinforcement</name>
  <files>assets/css/01-reset.css, assets/css/04-components.css, lib/scoria_web/ui.ex, test/scoria_web/ui_component_test.exs</files>
  <read_first>
    - assets/css/01-reset.css (focus-visible rule lines 64-68)
    - assets/css/02-tokens.css (focus-ring token light line 117, dark line 187 — read only; edit only for a WCAG defect per D-25)
    - assets/css/04-components.css (table viewport block from 16-02; drawer/evidence overflow blocks ~593-621, ~885; selected-row + sort + nav-active structure)
    - lib/scoria_web/ui.ex (badge/1 lines 68-70; tone/1 lines 21-49; table/1 sort + selection from 16-02)
    - test/scoria_web/ui_component_test.exs (existing badge/table/drawer describe blocks; render_component/Floki style)
    - .planning/phases/16-motion-responsive-theme-parity/16-UI-SPEC.md (Focus, Status, and Accessibility section)
    - .planning/phases/16-motion-responsive-theme-parity/16-01-SUMMARY.md and 16-02-SUMMARY.md (what the drawer/viewport overflow containers actually look like)
  </read_first>
  <action>
    Focus audit: ensure the `.scoria-root :where(:focus-visible)` outline (2px, offset 2px, `var(--scoria-focus-ring)`) remains visible when an interactive element is focused inside the new overflow containers — the table viewport (`.scoria-table__viewport`, `overflow-x: auto`), the mobile drawer panel, and the raised modal/drawer/evidence surfaces. Where `overflow` clipping would hide the 2px offset ring, fix it at the container level (e.g. add sufficient inner padding, use `overflow-clip-margin`, or apply an inset/box-shadow focus style on the focusable viewport itself) — do NOT remove or suppress the ring. Verify the focus-ring token meets ≥3:1 against adjacent surfaces in BOTH themes (light `--scoria-focus-ring` on panel/raised surfaces; dark likewise). Only if a real WCAG <3:1 defect is found may you adjust the focus-ring token in 02-tokens.css (D-25); otherwise leave tokens untouched and note the assessment in the SUMMARY.

    Status reinforcement: confirm every status surface pairs tone color with text/icon/ARIA/structure (D-24). Specifically verify and, where missing, add: selected/active rows expose `aria-selected` or `aria-current` plus a visible label/structure (not tone alone); the table sort header exposes both the SVG indicator and `aria-sort` (landed in 16-02 — confirm present); nav active uses `aria-current="page"` + the inset box-shadow rail; approval/attention highlights pair the `scoria-attention` cue with visible explanatory text; evidence outcomes use a badge/label with text. Make minimal additive changes in `ui.ex` (and component CSS for structural rails) — do not introduce per-component status-color helpers (the `ui_drift_guard_test.exs` forbids them) and no raw palette (D-27).

    Tests: add assertions to `ui_component_test.exs` that (a) a selected/active row carries `aria-selected` or `aria-current`, (b) the sorted table header carries `aria-sort`, and (c) `badge/1` output contains visible text content (label), not just a tone class. Use the existing `render_component`/Floki assertion style.
  </action>
  <verify>
    <automated>mix test test/scoria_web/ui_component_test.exs test/scoria_web/ui_drift_guard_test.exs test/scoria_web/ds06_drift_guard_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `01-reset.css` still defines the global `:focus-visible` outline; any clipping fix lives at the overflow-container level (no rule removes/suppresses the focus ring)
    - `02-tokens.css` focus-ring tokens are unchanged unless a WCAG <3:1 defect is documented in the SUMMARY with the measured ratio
    - `ui_component_test.exs` asserts a selected/active row carries `aria-selected`/`aria-current`, the sorted header carries `aria-sort`, and `badge/1` renders visible label text
    - `mix test test/scoria_web/ui_component_test.exs`, `ui_drift_guard_test.exs`, and `ds06_drift_guard_test.exs` all pass
    - No per-component status-color helper and no raw palette class introduced
  </acceptance_criteria>
  <done>Focus rings stay visible in both themes inside the new overflow/raised containers, status surfaces carry non-color structure/text/ARIA, and component tests assert these guarantees; all targeted tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Motion-contract verification pass</name>
  <files>assets/css/05-motion.css, assets/css/04-components.css</files>
  <read_first>
    - assets/css/05-motion.css (full file — keyframes lines 7-32, attention/skeleton exceptions lines 19-32, command-fade lines 39-51, reduced-motion kill switch lines 55-64)
    - assets/css/04-components.css (hover/transition conventions lines 85-96; any transition declarations on nav/button/drawer/table/evidence touched this phase)
    - .planning/phases/16-motion-responsive-theme-parity/16-UI-SPEC.md (Motion section — durations, allowed properties, prohibitions)
    - .planning/phases/16-motion-responsive-theme-parity/16-01-SUMMARY.md (the drawer motion primitive added in wave 1)
  </read_first>
  <action>
    Sweep all motion declarations touched or relied on this phase and bring any deviation into the contract (D-15–D-22). Confirm: every user-triggered transition animates only `opacity` and/or `transform` (never width/height/left/right/grid/layout properties); durations are within caps (scrim 100ms, command/toast fade 100–120ms, modal pop 150ms, drawer slide ≤200ms); there is no `transition: all` anywhere under `assets/css/` or `lib/scoria_web/` (baseline verified clean — keep it clean); there are no `infinite` interaction animations; the only allowlisted looping/exception cues are `scoria-skeleton-pulse` (opacity-only, no shimmer, no transform) and `scoria-attention` (finite, max 2 cycles, paired with visible text). Verify the drawer primitive added in wave 1 is transform/opacity-only and registered in `05-motion.css` (the only allowed keyframe home, D-16) — if any one-off keyframe leaked into a component/LiveView, move it into `05-motion.css`. Confirm the reduced-motion kill switch (lines 55-64) remains unlayered/authoritative and collapses every transition/animation including the new drawer and any summary transitions; do not weaken it. Per D-21, if a strict motion guard is desired, prefer outline/opacity/filter over the existing `scoria-attention` border-color animation — make that swap only if it does not regress the cue's visibility.

    Make only the minimal edits needed to bring deviations into contract; if the audit finds zero deviations, document that explicitly in the SUMMARY (the codebase is already clean of `transition: all` and `infinite`).
  </action>
  <verify>
    <automated>! grep -rnE "transition:\s*all" assets/css/ lib/scoria_web/ && ! grep -rnE "(width|height|left|right|grid-template-columns)" assets/css/05-motion.css | grep -i "keyframe\|@keyframes\|transform\|0%\|100%" | grep -iE "width:|height:|left:|right:" && grep -q "prefers-reduced-motion" assets/css/05-motion.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - No `transition: all` exists under `assets/css/` or `lib/scoria_web/`
    - No `@keyframes` in `05-motion.css` animates a layout property (width/height/left/right/grid); only opacity/transform (plus the allowlisted border-color attention cue) appear
    - No `infinite` iteration count on any interaction animation; only finite/exception cues remain
    - All keyframes/named primitives live in `05-motion.css` (no one-off keyframes in components/LiveViews)
    - The `prefers-reduced-motion: reduce` kill switch remains present and authoritative in `05-motion.css`
  </acceptance_criteria>
  <done>Every interaction motion is transform/opacity-only, within duration caps, reduced-motion-safe, with only finite allowlisted exceptions; keyframes centralized; contract deviations (if any) fixed and documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (checkpoint): Human verification of motion brand-fit, both-theme focus, and responsive density</name>
  <action>PAUSE for human verification. Present the how-to-verify steps below to the operator and wait for the resume signal before marking the plan complete. This is a judgment-based check (motion brand-fit, both-theme focus visibility, responsive density) that browser automation cannot fully decide.</action>
  <what-built>Mobile-first shell + nav drawer (wave 1), responsive table viewport + mobile summaries (waves 2/4), responsive grid replacements (wave 3), and focus/status/motion hardening (this plan). The validation strategy marks brand-fit of motion and responsive information-density as judgment-based manual checks.</what-built>
  <how-to-verify>
    1. Start the dev server: `mix dev.setup` (if needed) then `mix phx.server`; open `http://localhost:4000/scoria`.
    2. Resize to ~375px (or use device emulation). Confirm: no horizontal page overflow; the desktop sidebar is gone; the sticky mobile topbar shows brand, page label, Menu, command palette, and theme toggle.
    3. Open the Menu drawer. Confirm: Operate/Improve/Configure groups with active state + Soon badges; scrim-click and Escape dismiss; focus returns to Menu; the open/close motion is a calm slide/fade (no bounce/spring/sparkle), not "alive for its own sake".
    4. Visit Runs, Review Queue, Connectors, Dataset Builder at 375px. Confirm each shows terse mobile summaries (label, status text, one scalar/time, a domain-verb action) and that critical state is not hidden.
    5. Visit Workflow show at 375px. Confirm the stack order: object header → next-step verbs → trace/span list → selected detail → evidence notebook.
    6. Tab through representative controls (a link, a button, an input, a table action, a command palette item, a mobile nav item) in BOTH light and dark themes. Confirm a visible focus ring on each, not clipped by table/drawer overflow.
    7. Toggle theme on Home, one table screen, one evidence screen, and one overlay. Confirm both themes are readable with no color-only status.
    8. Enable OS "reduce motion" and re-open the drawer/modal and observe a skeleton/attention cue — confirm motion collapses to effectively instant.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (per screen) to revise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CSS focus/motion declarations + minimal SSR ARIA/structure additions only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | Denial of Service | focus visibility in overflow containers | mitigate | Fix focus-ring clipping at the container level so keyboard users always see focus; never suppress the ring to "solve" clipping. |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. |

Assessment: No new attack surface beyond presentation. Changes are CSS focus/motion declarations and minimal SSR ARIA/text additions; no new inputs, routes, auth boundaries, or data flows.
</threat_model>

<verification>
- `mix test test/scoria_web/ui_component_test.exs test/scoria_web/ui_drift_guard_test.exs test/scoria_web/ds06_drift_guard_test.exs` green.
- No `transition: all`, no layout-property keyframes, no `infinite` interaction loops.
- Reduced-motion kill switch authoritative.
- Human checkpoint confirms motion brand-fit, focus visibility in both themes, and responsive density.
- (Wave 6 plan adds Playwright proof for focus outline, reduced-motion collapse, and theme-toggle smoke.)
</verification>

<success_criteria>
- Focus is visible in both themes and not clipped by the new overflow/raised containers.
- Status always pairs tone color with text/icon/ARIA/structure.
- All interaction motion is transform/opacity-only, ≤200ms, reduced-motion-safe, with only finite allowlisted exceptions.
- Human verification confirms brand-fit and both-theme readability.
</success_criteria>

<output>
Create `.planning/phases/16-motion-responsive-theme-parity/16-05-SUMMARY.md` when done.
</output>
