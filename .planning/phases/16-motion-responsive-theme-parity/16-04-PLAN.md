---
phase: 16-motion-responsive-theme-parity
plan: 04
type: execute
wave: 4
depends_on: ["16-02", "16-03"]
files_modified:
  - lib/scoria_web/live/workflow_live/index.ex
  - lib/scoria_web/live/review_queue_live.ex
  - lib/scoria_web/live/connectors_live/index.ex
  - lib/scoria_web/live/dataset_live/index.ex
autonomous: true
requirements: [MOTION-03, MOTION-02]
must_haves:
  truths:
    - "Runs, Review Queue, Connectors, and Dataset Builder tables render an opt-in mobile summary on mobile width"
    - "Each summary exposes the object label, a status badge with text, one key scalar/time, and a primary action"
    - "Each summary's primary action uses the domain verb (Open trace / Open run / Inspect connector / Open dataset)"
    - "Empty states on touched scan screens use the domain-specific heading copy from the design contract"
    - "Mobile summaries do not hide critical state and preserve real desktop table semantics"
  artifacts:
    - path: "lib/scoria_web/live/workflow_live/index.ex"
      provides: "Runs mobile summary slot (run/trace id, status badge, started-at/root role, Open trace)"
      contains: "mobile_summary"
    - path: "lib/scoria_web/live/connectors_live/index.ex"
      provides: "Connectors mobile summary slot (runtime/connector label, text status, last seen, Inspect)"
      contains: "mobile_summary"
  key_links:
    - from: "lib/scoria_web/live/workflow_live/index.ex"
      to: "ScoriaWeb.UI.table/1 mobile_summary slot"
      via: "<:mobile_summary> slot usage"
      pattern: "mobile_summary"
    - from: "lib/scoria_web/live/dataset_live/index.ex"
      to: "ScoriaWeb.UI.table/1 mobile_summary slot"
      via: "<:mobile_summary> slot usage"
      pattern: "mobile_summary"
---

<objective>
Adopt the opt-in `mobile_summary` slot (built in plan 16-02) on the four scan-heavy object-picking tables called out by D-08/D-10: Runs (Workflows index), Review Queue, Connectors, and Dataset Builder. Each summary is terse and object-shaped — object label, status badge with text, one key scalar/time, and a single primary action using the domain verb. Thread the design-contract empty-state copy through each touched table's `<:empty>` slot.

Purpose: Turns the shared mobile-summary contract into real per-screen scan affordances so wide tables become usable when choosing an object on mobile, while dense comparison/detail tables keep honest overflow (untouched here). Implements D-08/D-10, the Tables and Scan Surfaces summary clause, and the Copywriting contract for these surfaces.
Output: `<:mobile_summary>` slots + domain empty-state copy on the four scan LiveViews.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-motion-responsive-theme-parity/16-CONTEXT.md
@.planning/phases/16-motion-responsive-theme-parity/16-UI-SPEC.md
@.planning/phases/16-motion-responsive-theme-parity/16-PATTERNS.md
@.planning/phases/16-motion-responsive-theme-parity/16-02-SUMMARY.md

<interfaces>
<!-- The mobile_summary slot API comes from plan 16-02: a per-row slot rendering one block per row -->
<!--   inside .scoria-table__mobile-summaries (shown below md, hidden >=768px), enabled by a modifier. -->
<!--   READ 16-02-SUMMARY.md for the EXACT slot name, :let binding shape, and the enabling modifier. -->

<!-- UI-SPEC per-domain summary content (terse, object-shaped): -->
<!--   Runs: run/trace ID, status badge, started-at/root role, action "Open trace" -->
<!--   Review Queue: candidate rationale/trace, severity/score, promotion state, action "Open run" or selected detail -->
<!--   Connectors: runtime/connector label, text status, last seen/refresh, action "Inspect connector" -->
<!--   Dataset Builder: dataset name/version/state/count, primary "Open dataset"/promote action -->

<!-- UI-SPEC empty-state headings to thread via <:empty>: -->
<!--   "No runs match this view" / "No review candidates match this view" / -->
<!--   "No connectors match this view" / "No datasets match this view" -->
<!--   body fallback: "Adjust your filters or check back when data is available." -->

<!-- Status badges must carry text, not color alone (D-24): reuse ScoriaWeb.UI.badge/1 (tone class + label text). -->
<!-- Empty-slot usage locations (verified): workflow index ~line 70, review_queue ~line 134, -->
<!--   connectors ~lines 117/159, dataset index ~line 97. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mobile summaries for Runs + Review Queue</name>
  <files>lib/scoria_web/live/workflow_live/index.ex, lib/scoria_web/live/review_queue_live.ex</files>
  <read_first>
    - .planning/phases/16-motion-responsive-theme-parity/16-02-SUMMARY.md (exact mobile_summary slot name, :let binding, enabling modifier)
    - lib/scoria_web/live/workflow_live/index.ex (the <.table> callsite + each :col, the <:empty> at line ~70, row data/assigns shape)
    - lib/scoria_web/live/review_queue_live.ex (the <.table> callsite + :col, the <:empty> at line ~134; note plan 16-03 changed the page-split class at line 74 — do not revert it)
    - lib/scoria_web/ui.ex (badge/1 for status text; table/1 mobile_summary slot signature)
    - .planning/phases/16-motion-responsive-theme-parity/16-UI-SPEC.md (Tables summary clause + Copywriting CTAs/empty states)
  </read_first>
  <action>
    On the Runs table in `workflow_live/index.ex`, add a `mobile_summary` per-row slot (matching the exact slot name and `:let` binding from 16-02-SUMMARY.md) and enable summaries via the modifier the component expects. The summary must expose: the run/trace ID (mono), a status badge via `<.badge>` carrying tone + visible text (not color alone), one key scalar/time (started-at or root role), and a single primary action linking to the trace with copy `Open trace`. Use the same row field accessors the existing `:col` definitions use so data stays consistent. Update the `<:empty>` slot heading to `No runs match this view` with body `Adjust your filters or check back when data is available.` unless the screen already has sharper domain copy.

    On the Review Queue table in `review_queue_live.ex`, add a `mobile_summary` per-row slot exposing: candidate rationale/trace label, severity/score (badge with text), promotion state, and a primary action (`Open run` or the existing select-detail action the screen uses). Update the `<:empty>` heading to `No review candidates match this view` with the same body fallback. Do not modify the line-74 page-split class set by plan 16-03.

    Use only existing component classes / semantic tokens — no raw palette (D-27). Keep summaries terse; do not hide critical state (D-08).
  </action>
  <verify>
    <automated>grep -q "mobile_summary" lib/scoria_web/live/workflow_live/index.ex && grep -q "mobile_summary" lib/scoria_web/live/review_queue_live.ex && grep -q "No runs match this view" lib/scoria_web/live/workflow_live/index.ex && mix compile --warnings-as-errors 2>&1 | tail -5 && mix test test/scoria_web/ds06_drift_guard_test.exs 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `workflow_live/index.ex` and `review_queue_live.ex` each use the `mobile_summary` slot with the enabling modifier from 16-02
    - The Runs summary contains an `Open trace` action; the Review Queue summary contains an `Open run` (or existing select-detail) action
    - Each summary status uses `<.badge>` (tone + visible text), not a color-only indicator
    - `workflow_live/index.ex` `<:empty>` heading is `No runs match this view`; `review_queue_live.ex` `<:empty>` heading is `No review candidates match this view`
    - `mix compile --warnings-as-errors` succeeds and `mix test test/scoria_web/ds06_drift_guard_test.exs` passes
  </acceptance_criteria>
  <done>Runs and Review Queue tables expose terse, domain-shaped mobile summaries with status text and a domain-verb action, plus domain empty-state copy; app compiles clean, DS-06 green.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile summaries for Connectors + Dataset Builder</name>
  <files>lib/scoria_web/live/connectors_live/index.ex, lib/scoria_web/live/dataset_live/index.ex</files>
  <read_first>
    - .planning/phases/16-motion-responsive-theme-parity/16-02-SUMMARY.md (exact mobile_summary slot name, :let binding, enabling modifier)
    - lib/scoria_web/live/connectors_live/index.ex (the <.table> callsite(s) + :col, the <:empty> at lines ~117/159, row data shape)
    - lib/scoria_web/live/dataset_live/index.ex (the <.table> callsite + :col, the <:empty> at line ~97, row data shape)
    - lib/scoria_web/ui.ex (badge/1; table/1 mobile_summary slot signature)
    - .planning/phases/16-motion-responsive-theme-parity/16-UI-SPEC.md (Tables summary clause + Copywriting CTAs/empty states)
  </read_first>
  <action>
    On the Connectors scan table in `connectors_live/index.ex`, add a `mobile_summary` per-row slot exposing: runtime/connector label, text status (badge with visible text), last-seen/refresh time, and a primary action with copy `Inspect connector` (or the existing drawer-open action's verb if the screen already uses `Inspect`). If the screen renders more than one table, apply the summary only to the scan table where the user picks a connector to inspect; leave dense/detail tables on honest overflow. Update the relevant `<:empty>` heading(s) to `No connectors match this view` with body `Adjust your filters or check back when data is available.`

    On the Dataset Builder table in `dataset_live/index.ex`, add a `mobile_summary` per-row slot exposing: dataset name/version, state, record count, and the primary `Open dataset` (or existing open/promote) action. Update the `<:empty>` heading to `No datasets match this view` with the same body fallback.

    Use only existing component classes / semantic tokens — no raw palette (D-27). Keep summaries terse and do not hide critical state.
  </action>
  <verify>
    <automated>grep -q "mobile_summary" lib/scoria_web/live/connectors_live/index.ex && grep -q "mobile_summary" lib/scoria_web/live/dataset_live/index.ex && grep -q "No connectors match this view" lib/scoria_web/live/connectors_live/index.ex && grep -q "No datasets match this view" lib/scoria_web/live/dataset_live/index.ex && mix compile --warnings-as-errors 2>&1 | tail -5 && mix test test/scoria_web/ds06_drift_guard_test.exs 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `connectors_live/index.ex` and `dataset_live/index.ex` each use the `mobile_summary` slot with the enabling modifier from 16-02
    - The Connectors summary contains an `Inspect connector` (or existing Inspect-verb) action; the Dataset summary contains an `Open dataset` (or existing open/promote) action
    - Each summary status uses `<.badge>` (tone + visible text), not a color-only indicator
    - `connectors_live/index.ex` `<:empty>` heading is `No connectors match this view`; `dataset_live/index.ex` `<:empty>` heading is `No datasets match this view`
    - `mix compile --warnings-as-errors` succeeds and `mix test test/scoria_web/ds06_drift_guard_test.exs` passes
  </acceptance_criteria>
  <done>Connectors and Dataset Builder scan tables expose terse, domain-shaped mobile summaries with status text and a domain-verb action, plus domain empty-state copy; app compiles clean, DS-06 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller→mobile_summary slot | LiveViews pass row fields into the summary slot for SSR rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Tampering / Information Disclosure | mobile_summary row content | mitigate | Render summary fields via standard HEEx interpolation so auto-escaping applies; do not use `Phoenix.HTML.raw` for any row field. |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. |

Assessment: No new attack surface beyond presentation. These are SSR slot adoptions using existing row data already rendered in the desktop table; no new inputs, routes, auth, or data flows, and HEEx escaping is preserved.
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean.
- `mix test test/scoria_web/ds06_drift_guard_test.exs` green.
- All four scan tables use the `mobile_summary` slot and domain empty-state copy.
- (Wave 6 plan adds the Playwright proof that summaries appear at 375px and the table viewport appears at desktop width.)
</verification>

<success_criteria>
- Runs, Review Queue, Connectors, and Dataset Builder expose terse, object-shaped mobile summaries with status text and a domain-verb primary action.
- Touched scan screens use the design-contract domain empty-state headings.
- Desktop table semantics are preserved; dense/detail tables keep honest overflow.
- App compiles clean; DS-06 ratchet green.
</success_criteria>

<output>
Create `.planning/phases/16-motion-responsive-theme-parity/16-04-SUMMARY.md` when done.
</output>
