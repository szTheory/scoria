---
phase: 41-bounded-handoff-contract-safety
plan: 2
type: execute
wave: 2
depends_on:
  - 41-01
files_modified:
  - lib/scoria/runtime/params.ex
  - lib/scoria/runtime.ex
  - lib/scoria/workflows/runtime.ex
  - test/scoria/runtime_test.exs
  - test/scoria/workflows/runtime_test.exs
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
nyquist_compliant: true
must_haves:
  truths:
    - "Unsafe projected-context state is rejected as a public contract error before bounded handoff persistence accepts it."
    - "Projected-context validation is deep enough to catch nested or aliased broad runtime-state blobs, not just shallow top-level keys."
    - "The public lane explains the narrow host-controlled delegated-context boundary explicitly instead of silently coercing unsafe state."
  artifacts:
    - path: "lib/scoria/runtime/params.ex"
      provides: "Public projected-context validation and explicit contract error surface"
    - path: "lib/scoria/workflows/runtime.ex"
      provides: "Defense-in-depth handoff safety checks on already-normalized attrs"
    - path: "test/scoria/runtime_test.exs"
      provides: "Regression coverage for explicit rejection and bounded-context success cases"
  key_links:
    - from: "lib/scoria/runtime/params.ex"
      to: "lib/scoria/runtime.ex"
      via: "start_handoff_run/3 fails before durable writes when projected context is unsafe"
      pattern: "unsafe_projected_context|projected_context"
    - from: "lib/scoria/runtime/params.ex"
      to: "lib/scoria/workflows/runtime.ex"
      via: "deep validation at the public boundary is mirrored by workflow-runtime defense in depth"
      pattern: "transcript|provider_session|secrets|socket_state"
---

<objective>
Harden projected-context safety and make unsafe delegated-state rejection explicit at the public boundary.

Purpose: enforce the bounded delegated-context contract as shipped product truth rather than relying on late, shallow runtime guards.
Output: deep projected-context validation, explicit public error semantics, and regressions that prove unsafe state never becomes accepted contract truth.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-bounded-handoff-contract-safety/41-CONTEXT.md
@.planning/phases/41-bounded-handoff-contract-safety/41-RESEARCH.md
@.planning/phases/41-bounded-handoff-contract-safety/41-PATTERNS.md
@.planning/phases/41-bounded-handoff-contract-safety/41-VALIDATION.md
@.planning/phases/41-bounded-handoff-contract-safety/41-01-PLAN.md
@lib/scoria/runtime/params.ex
@lib/scoria/runtime.ex
@lib/scoria/workflows/runtime.ex
@test/scoria/runtime_test.exs
@test/scoria/workflows/runtime_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deep projected-context validation before durable persistence</name>
  <files>lib/scoria/runtime/params.ex, lib/scoria/runtime.ex, test/scoria/runtime_test.exs</files>
  <read_first>.planning/phases/41-bounded-handoff-contract-safety/41-CONTEXT.md, lib/scoria/runtime/params.ex, lib/scoria/runtime.ex, test/scoria/runtime_test.exs</read_first>
  <behavior>
    - Test 1: projected contexts containing `transcript`, `messages`, `history`, `provider_session`, `session`, `socket`, `assigns`, `private`, `cookies`, `headers`, `secrets`, or equivalent nested aliases are rejected.
    - Test 2: nested unsafe keys such as `%{"safe" => %{"provider_session" => %{...}}}` are rejected the same as top-level unsafe keys.
    - Test 3: small host-controlled slices such as `%{"task" => "review"}` and `%{}` remain accepted.
  </behavior>
  <action>Add a dedicated projected-context validator in `lib/scoria/runtime/params.ex` that recursively walks maps/lists and rejects broad or platform-owned state named in the Phase 41 context: transcript/history/messages blobs, provider-session/runtime state, Plug/LiveView/socket state, assigns/private/session/cookies/headers, secrets-bearing state, and nested equivalents. Call this validator from `start_handoff/3` before `Scoria.Runtime.start_handoff_run/3` persists any run or step rows. Return a stable public error tuple that distinguishes unsafe projected context from generic invalid input. Preserve `%{}` and narrow host-controlled slices as valid. Do not broaden validation to a rigid allowlist schema in this phase.</action>
  <acceptance_criteria>
    - `rg -n 'messages|history|assigns|private|cookies|headers|session|socket|provider_session|secrets' lib/scoria/runtime/params.ex` returns matches.
    - `rg -n 'defp .*projected_context.*validate|unsafe_projected_context' lib/scoria/runtime/params.ex` returns matches.
    - `rg -n 'nested|provider_session|transcript' test/scoria/runtime_test.exs` returns matches for recursive rejection coverage.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/runtime_test.exs</automated>
  </verify>
  <done>Unsafe delegated context is rejected early and deeply enough that broad runtime state cannot slip through by nesting or aliasing.</done>
</task>

<task type="auto">
  <name>Task 2: Keep workflow-runtime safety honest and expose explicit failure semantics</name>
  <files>lib/scoria/workflows/runtime.ex, lib/scoria/runtime.ex, test/scoria/runtime_test.exs, test/scoria/workflows/runtime_test.exs</files>
  <read_first>.planning/phases/41-bounded-handoff-contract-safety/41-PATTERNS.md, lib/scoria/workflows/runtime.ex, lib/scoria/runtime.ex, test/scoria/runtime_test.exs, test/scoria/workflows/runtime_test.exs</read_first>
  <behavior>
    - Test 1: workflow-runtime handoff handling still refuses unsafe projected context as defense in depth even if called below the public facade.
    - Test 2: public failure semantics clearly identify the bounded-context contract error instead of a vague generic failure.
    - Test 3: no implicit fallback path silently strips unsafe keys and continues the handoff.
  </behavior>
  <action>Refactor `Scoria.Workflows.Runtime.handle_handoff/3` so it reuses the same unsafe projected-context rules as defense in depth, but no longer acts as the only place safety is enforced. Preserve explicit failure behavior when unsafe attrs reach this seam directly, using a stable error reason or envelope that points to the bounded projected-context contract rather than only `"unsafe_projected_context"`. Update runtime/workflow tests so they prove both the public facade path and the lower workflow seam reject unsafe delegated state without silently sanitizing or continuing execution.</action>
  <acceptance_criteria>
    - `rg -n 'unsafe_projected_context|bounded.*context|projected_context' lib/scoria/workflows/runtime.ex test/scoria/runtime_test.exs test/scoria/workflows/runtime_test.exs` returns matches.
    - `rg -n 'Map\\.drop|Map\\.delete' lib/scoria/workflows/runtime.ex lib/scoria/runtime/params.ex` returns no silent sanitize-only path for unsafe keys.
    - `mix test test/scoria/runtime_test.exs test/scoria/workflows/runtime_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/runtime_test.exs test/scoria/workflows/runtime_test.exs</automated>
  </verify>
  <done>The bounded-context contract fails explicitly and consistently at both the public and workflow-runtime seams.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host-provided projected context -> runtime persistence | broad runtime-state blobs could become durable delegated authority if validation is shallow or late |
| public facade -> workflow substrate | inconsistent rejection semantics could leave the bounded lane ambiguous to adopters and operators |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-03 | I | projected-context contract | mitigate | reject broad transcript/session/secrets/runtime-state keys recursively before handoff persistence |
| T-41-04 | T | workflow-runtime handoff seam | mitigate | keep a mirrored defense-in-depth check and explicit failure envelope so unsafe state cannot be silently coerced or accepted |
</threat_model>

<verification>
- `mix test test/scoria/runtime_test.exs`
- `mix test test/scoria/runtime_test.exs test/scoria/workflows/runtime_test.exs`
</verification>

<success_criteria>
- Unsafe projected context is rejected early and explicitly.
- Nested or aliased broad runtime state cannot bypass the bounded handoff lane.
- The public surface remains narrow and host-controlled by default.
</success_criteria>

<output>
After completion, create `.planning/phases/41-bounded-handoff-contract-safety/41-bounded-handoff-contract-safety-02-SUMMARY.md`
</output>

