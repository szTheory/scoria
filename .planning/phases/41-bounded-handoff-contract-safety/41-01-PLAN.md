---
phase: 41-bounded-handoff-contract-safety
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/runtime/params.ex
  - lib/scoria/runtime.ex
  - lib/scoria/workflows/runtime.ex
  - lib/scoria/workflows/handoff.ex
  - lib/scoria/runtime/run_detail.ex
  - test/scoria/runtime_test.exs
  - test/scoria/workflows/runtime_test.exs
autonomous: true
requirements:
  - HAND-01
  - HAND-02
nyquist_compliant: true
must_haves:
  truths:
    - "The public bounded handoff lane requires explicit root-role, delegated-kind, handoff-input, and projected-context contract inputs instead of relying on hidden semantic defaults."
    - "Delegated work remains under the same durable run with a persisted root handoff step, durable handoff row, and queued child step rather than ownership transfer."
    - "Public runtime detail exposes the minimum truthful delegated-lineage fields callers need to inspect the narrow handoff lane."
  artifacts:
    - path: "lib/scoria/runtime/params.ex"
      provides: "Explicit public handoff normalization and contract validation entrypoint"
    - path: "lib/scoria/workflows/runtime.ex"
      provides: "Same-run durable handoff creation that consumes already-validated handoff attrs"
    - path: "lib/scoria/runtime/run_detail.ex"
      provides: "Curated public delegated-lineage DTO truth"
  key_links:
    - from: "lib/scoria/runtime.ex"
      to: "lib/scoria/runtime/params.ex"
      via: "start_handoff_run/3 only persists runs after explicit contract normalization succeeds"
      pattern: "start_handoff|start_handoff_run"
    - from: "lib/scoria/workflows/runtime.ex"
      to: "lib/scoria/workflows/handoff.ex"
      via: "validated handoff attrs persist durable same-run lineage truth"
      pattern: "create_handoff|delegated_kind|handoff_input"
    - from: "lib/scoria/runtime/run_detail.ex"
      to: "lib/scoria/workflows/runtime.ex"
      via: "runtime detail projects persisted handoff and child-step contract facts"
      pattern: "delegated_role_id|delegated_kind|parent_step_id"
---

<objective>
Lock the public bounded handoff contract and same-run lineage truth before the safety tightening work starts.

Purpose: remove hidden semantic defaults at the public boundary and make the existing same-run delegated lineage inspectable as shipped truth.
Output: explicit handoff normalization, durable same-run lineage persistence, and curated runtime DTO readback for the public lane.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-bounded-handoff-contract-safety/41-CONTEXT.md
@.planning/phases/41-bounded-handoff-contract-safety/41-RESEARCH.md
@.planning/phases/41-bounded-handoff-contract-safety/41-PATTERNS.md
@.planning/phases/41-bounded-handoff-contract-safety/41-VALIDATION.md
@lib/scoria/runtime.ex
@lib/scoria/runtime/params.ex
@lib/scoria/workflows/runtime.ex
@lib/scoria/workflows/handoff.ex
@lib/scoria/runtime/run_detail.ex
@test/scoria/runtime_test.exs
@test/scoria/workflows/runtime_test.exs

<interfaces>
From `lib/scoria/runtime.ex`:
```elixir
def start_handoff_run(identity, delegated_role_id, opts \\ []) do
```

From `lib/scoria/runtime/params.ex`:
```elixir
def start_handoff(identity, delegated_role_id, opts \\ [])
```

From `lib/scoria/runtime/run_detail.ex`:
```elixir
def from_run_tree(%Run{} = run, opts \\ []) do
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the public handoff contract explicit at the params boundary</name>
  <files>lib/scoria/runtime/params.ex, lib/scoria/runtime.ex, test/scoria/runtime_test.exs</files>
  <read_first>.planning/phases/41-bounded-handoff-contract-safety/41-CONTEXT.md, .planning/phases/41-bounded-handoff-contract-safety/41-RESEARCH.md, lib/scoria/runtime/params.ex, lib/scoria/runtime.ex, test/scoria/runtime_test.exs</read_first>
  <behavior>
    - Test 1: `Scoria.Runtime.start_handoff_run/3` rejects missing or empty `root_role_id`, missing or empty `delegated_kind`, and non-map `handoff_input` / `projected_context` inputs with explicit public contract errors.
    - Test 2: `Scoria.Runtime.start_handoff_run/3` stops implicitly copying `payload` or `input` into `projected_context`.
    - Test 3: `projected_context: %{}` remains a valid explicit no-context choice.
  </behavior>
  <action>Update `Scoria.Runtime.Params.start_handoff/3` so the public bounded handoff lane requires the exact contract fields Phase 41 locked in context: `root_role_id`, `delegated_kind`, `handoff_input`, and `projected_context` must be normalized explicitly from caller input instead of falling back to `root_role_id: "executor"`, `delegated_kind: "delegated_task"`, or `Map.put_new(context, "payload", payload)`. Add a dedicated validation path that returns stable contract errors for missing/invalid handoff inputs before `Scoria.Runtime.start_handoff_run/3` calls `Workflows.create_run/1`. Keep `delegated_role_id` as the positional argument, keep `projected_context: %{}` valid, and leave non-handoff `start_run/2` behavior unchanged.</action>
  <acceptance_criteria>
    - `rg -n 'root_role_id: value\\(opts, runtime, :root_role_id\\) \\|\\| "executor"' lib/scoria/runtime/params.ex` returns no matches.
    - `rg -n 'delegated_task|Map.put_new\\(context, "payload", payload\\)' lib/scoria/runtime/params.ex` returns no matches.
    - `rg -n 'invalid_root_role_id|invalid_delegated_kind|invalid_handoff_input|invalid_projected_context' lib/scoria/runtime/params.ex test/scoria/runtime_test.exs` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/runtime_test.exs</automated>
  </verify>
  <done>The public handoff boundary is explicit, narrow, and no longer relies on semantic defaults or payload projection magic.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve same-run lineage and expose truthful delegated readback</name>
  <files>lib/scoria/workflows/runtime.ex, lib/scoria/workflows/handoff.ex, lib/scoria/runtime/run_detail.ex, test/scoria/runtime_test.exs, test/scoria/workflows/runtime_test.exs</files>
  <read_first>.planning/phases/41-bounded-handoff-contract-safety/41-PATTERNS.md, lib/scoria/workflows/runtime.ex, lib/scoria/workflows/handoff.ex, lib/scoria/runtime/run_detail.ex, test/scoria/runtime_test.exs, test/scoria/workflows/runtime_test.exs</read_first>
  <behavior>
    - Test 1: successful handoffs still produce one root `handoff` step and one queued child step under the same run.
    - Test 2: the durable handoff/readback path exposes `delegated_role_id`, `delegated_kind`, `handoff_input`, and parent/child lineage facts through public runtime detail.
    - Test 3: adjunct metadata such as `capability_tags` remains secondary and does not replace the core handoff truth.
  </behavior>
  <action>Keep `Scoria.Workflows.Runtime.handle_handoff/3` on the existing same-run substrate, but persist and project the exact bounded handoff facts Phase 41 needs for truthful inspection. If the current `Scoria.Workflows.Handoff` row is missing core contract truth such as `delegated_kind`, extend that schema/changeset and wire the value through `create_handoff/2`. Update `Scoria.Runtime.RunDetail` so its handoff/step items expose the minimum inspectability baseline from the phase context: delegated role, delegated kind, handoff input, root step vs child step lineage through `parent_step_id`, and bounded projected-context readback on the child step. Do not create a second run, do not transfer root ownership, and do not expand into richer Phase 42 operator presentation work.</action>
  <acceptance_criteria>
    - `rg -n 'delegated_kind' lib/scoria/workflows/handoff.ex lib/scoria/workflows/runtime.ex lib/scoria/runtime/run_detail.ex` returns matches.
    - `rg -n 'parent_step_id|handoff_input|projected_context' lib/scoria/runtime/run_detail.ex test/scoria/runtime_test.exs test/scoria/workflows/runtime_test.exs` returns matches.
    - `rg -n 'create_run\\(' lib/scoria/workflows/runtime.ex` returns no new alternate run-creation path inside handoff handling.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/runtime_test.exs test/scoria/workflows/runtime_test.exs</automated>
  </verify>
  <done>The same-run delegated lineage is durable, explicit, and readable through the public runtime DTO boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host-app inputs -> `Scoria.Runtime.Params` | hidden defaults or coercions could mis-state the public bounded-handoff contract |
| validated handoff attrs -> durable workflow truth | same-run lineage could drift if delegated kind or handoff input is not persisted faithfully |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | T | `Scoria.Runtime.Params` | mitigate | reject missing/implicit contract inputs before any run write so the host app cannot persist ambiguous handoff truth |
| T-41-02 | R | runtime detail / handoff persistence | mitigate | persist and project delegated kind, handoff input, and parent/child lineage so delegated work remains auditable under the same root run |
</threat_model>

<verification>
- `mix test test/scoria/runtime_test.exs`
- `mix test test/scoria/runtime_test.exs test/scoria/workflows/runtime_test.exs`
</verification>

<success_criteria>
- The public bounded handoff lane requires explicit contract inputs.
- Same-run lineage remains the only delegated execution shape.
- Public runtime detail exposes truthful delegated lineage without raw workflow inspection.
</success_criteria>

<output>
After completion, create `.planning/phases/41-bounded-handoff-contract-safety/41-bounded-handoff-contract-safety-01-SUMMARY.md`
</output>

