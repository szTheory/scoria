---
phase: 41-bounded-handoff-contract-safety
plan: 3
type: execute
wave: 3
depends_on:
  - 41-01
  - 41-02
files_modified:
  - docs/bounded_handoffs.md
  - test/support/scoria/adoption_example.ex
  - test/scoria/adoption_surface_test.exs
  - test/scoria/handoff_example_source_test.exs
  - test/mix/tasks/test.adoption_test.exs
  - test/scoria/runtime_test.exs
autonomous: true
requirements:
  - HAND-01
  - HAND-02
  - SAFE-01
  - SAFE-02
nyquist_compliant: true
must_haves:
  truths:
    - "Docs, checked source fragments, and the adoption lane all teach the same explicit bounded handoff contract the code now enforces."
    - "The public handoff story stays narrow by default: one explicit same-run handoff, one host-controlled projected-context slice, and explicit unsafe-context rejection."
    - "Phase 41 closes with executable proof lanes, not prose-only claims."
  artifacts:
    - path: "docs/bounded_handoffs.md"
      provides: "Canonical bounded handoff guide aligned to the explicit contract and safety rules"
    - path: "test/support/scoria/adoption_example.ex"
      provides: "Checked example fragments that mirror the public guide"
    - path: "test/mix/tasks/test.adoption_test.exs"
      provides: "Canonical adoption-lane proof that the public bounded handoff surface stays green"
  key_links:
    - from: "docs/bounded_handoffs.md"
      to: "test/support/scoria/adoption_example.ex"
      via: "guide and checked example teach the same explicit contract fields"
      pattern: "root_role_id|delegated_kind|handoff_input|projected_context"
    - from: "test/scoria/adoption_surface_test.exs"
      to: "test/mix/tasks/test.adoption_test.exs"
      via: "support-truth assertions and canonical adoption lane prove the same narrow surface"
      pattern: "bounded_handoffs|test.adoption"
---

<objective>
Backfill the docs and proof lanes that demonstrate the bounded handoff lane stays narrow and explicit by default.

Purpose: make support truth part of the shipped Phase 41 outcome, not a follow-on cleanup item.
Output: aligned guide/source fragments plus adoption-lane and runtime regression proof for the explicit bounded handoff contract.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/METHODOLOGY.md
@.planning/memory/bounded-handoff-productization-lessons.md
@.planning/phases/41-bounded-handoff-contract-safety/41-CONTEXT.md
@.planning/phases/41-bounded-handoff-contract-safety/41-RESEARCH.md
@.planning/phases/41-bounded-handoff-contract-safety/41-VALIDATION.md
@.planning/phases/41-bounded-handoff-contract-safety/41-01-PLAN.md
@.planning/phases/41-bounded-handoff-contract-safety/41-02-PLAN.md
@docs/bounded_handoffs.md
@test/support/scoria/adoption_example.ex
@test/scoria/adoption_surface_test.exs
@test/scoria/handoff_example_source_test.exs
@test/mix/tasks/test.adoption_test.exs
@test/scoria/runtime_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align the bounded handoff guide and checked source fragments to the explicit contract</name>
  <files>docs/bounded_handoffs.md, test/support/scoria/adoption_example.ex, test/scoria/adoption_surface_test.exs, test/scoria/handoff_example_source_test.exs</files>
  <read_first>.planning/memory/bounded-handoff-productization-lessons.md, docs/bounded_handoffs.md, test/support/scoria/adoption_example.ex, test/scoria/adoption_surface_test.exs, test/scoria/handoff_example_source_test.exs</read_first>
  <behavior>
    - Test 1: the guide states `root_role_id`, `delegated_kind`, `handoff_input`, and `projected_context` as explicit caller responsibilities.
    - Test 2: the guide and checked example both state that unsafe broad runtime-state keys are rejected explicitly.
    - Test 3: the docs do not claim implicit payload projection, hidden delegated-kind defaults, or general orchestration-platform behavior.
  </behavior>
  <action>Update `docs/bounded_handoffs.md` and `test/support/scoria/adoption_example.ex` so the canonical bounded handoff story teaches the exact Phase 41 contract: the host app passes explicit `root_role_id`, explicit `delegated_kind`, explicit `handoff_input`, and explicit `projected_context`; `projected_context: %{}` is allowed; broad runtime-state keys such as transcript/session/provider-session/secrets/socket state are rejected explicitly; and delegated work stays under the same durable run instead of transferring root ownership. Extend `test/scoria/adoption_surface_test.exs` and `test/scoria/handoff_example_source_test.exs` to assert those exact strings and to reject drift back toward implicit payload projection or broader orchestration framing.</action>
  <acceptance_criteria>
    - `rg -n 'root_role_id|delegated_kind|handoff_input|projected_context' docs/bounded_handoffs.md test/support/scoria/adoption_example.ex test/scoria/adoption_surface_test.exs` returns matches.
    - `rg -n 'transcript|provider_session|secrets|socket_state|session' docs/bounded_handoffs.md test/scoria/adoption_surface_test.exs` returns matches.
    - `rg -n 'payload' docs/bounded_handoffs.md test/support/scoria/adoption_example.ex` returns no matches that teach implicit projection semantics.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/adoption_surface_test.exs test/scoria/handoff_example_source_test.exs</automated>
  </verify>
  <done>The guide and checked example teach one explicit narrow bounded handoff lane with no hidden semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the bounded handoff lane through the canonical adoption and runtime tests</name>
  <files>test/mix/tasks/test.adoption_test.exs, test/scoria/runtime_test.exs, test/scoria/adoption_surface_test.exs</files>
  <read_first>.planning/phases/41-bounded-handoff-contract-safety/41-VALIDATION.md, test/mix/tasks/test.adoption_test.exs, test/scoria/runtime_test.exs, test/scoria/adoption_surface_test.exs</read_first>
  <behavior>
    - Test 1: `mix test.adoption` continues to cover the bounded handoff guide/source alignment and explicit public runtime lane.
    - Test 2: runtime tests prove both the happy-path same-run lineage and explicit unsafe-context failure behavior.
    - Test 3: no manual-only knowledge lane is required to verify the public bounded handoff surface.
  </behavior>
  <action>Extend the adoption-lane assertions in `test/mix/tasks/test.adoption_test.exs` and any supporting runtime/adoption tests so the canonical proof path covers the explicit Phase 41 handoff contract and unsafe-context rejection behavior together. Keep the verification lane boring and local: the adoption task should continue to run normal ExUnit-backed checks without optional knowledge features or browser-only steps. If the runtime contract changes produced new stable error strings or DTO fields in Plans `41-01` and `41-02`, assert those exact strings/fields here so support truth and executable proof stay coupled.</action>
  <acceptance_criteria>
    - `rg -n 'bounded_handoffs|start_handoff_run|unsafe projected|projected_context' test/mix/tasks/test.adoption_test.exs test/scoria/runtime_test.exs test/scoria/adoption_surface_test.exs` returns matches.
    - `mix test test/mix/tasks/test.adoption_test.exs test/scoria/adoption_surface_test.exs test/scoria/runtime_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mix/tasks/test.adoption_test.exs test/scoria/adoption_surface_test.exs test/scoria/runtime_test.exs</automated>
  </verify>
  <done>The canonical adoption lane proves the explicit bounded handoff contract and its narrow safety boundary together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped docs/examples -> adopters | stale support truth could teach hidden defaults or broader runtime-state delegation than the code allows |
| canonical adoption lane -> milestone closeout | weak proof could let contract drift ship unnoticed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-05 | R | docs and checked source | mitigate | assert the exact explicit contract fields and unsafe-key guidance in checked guide/source tests |
| T-41-06 | T | adoption verification lane | mitigate | keep the canonical `mix test.adoption` path exercising the explicit bounded handoff facade and support-truth surfaces together |
</threat_model>

<verification>
- `mix test test/scoria/adoption_surface_test.exs test/scoria/handoff_example_source_test.exs`
- `mix test test/mix/tasks/test.adoption_test.exs test/scoria/adoption_surface_test.exs test/scoria/runtime_test.exs`
</verification>

<success_criteria>
- Docs and checked source fragments teach the same explicit bounded handoff contract as the code.
- The lane remains narrow, same-run rooted, and host-controlled by default.
- Phase 41 closes with executable proof, not prose-only confidence.
</success_criteria>

<output>
After completion, create `.planning/phases/41-bounded-handoff-contract-safety/41-bounded-handoff-contract-safety-03-SUMMARY.md`
</output>

