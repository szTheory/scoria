---
phase: 06-corpus
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - lib/scoria/knowledge/grounding.ex
  - lib/scoria/knowledge/grounding_score.ex
  - lib/scoria/knowledge.ex
  - lib/scoria/eval.ex
  - test/scoria/knowledge/grounding_test.exs
  - test/scoria/eval_test.exs
autonomous: true
requirements:
  - RAG-05
must_haves:
  truths:
    - "Deterministic grounding and citation checks gate before judge-based review."
    - "Judge prompts/models/rubrics are versioned and auditable."
  artifacts:
    - path: "lib/scoria/knowledge/grounding.ex"
      provides: "Deterministic and judge-backed grounding evaluators"
    - path: "lib/scoria/knowledge/grounding_score.ex"
      provides: "Versioned persisted grounding score records"
  key_links:
    - from: "lib/scoria/knowledge/grounding.ex"
      to: "lib/scoria/knowledge/citation_formatter.ex"
      via: "citation validity checks"
      pattern: "validate_anchor"
    - from: "lib/scoria/eval.ex"
      to: "lib/scoria/knowledge/grounding_score.ex"
      via: "score/rubric persistence"
      pattern: "GroundingScore"
---

<objective>
Implement the Phase 6 grounding evaluation ladder.

Purpose: make citation and groundedness verification reproducible in CI first, then extensible with versioned semantic judge review.
Output: deterministic grounding checks, judge-score persistence hooks, and evaluation-focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-corpus/06-CONTEXT.md
@.planning/phases/06-corpus/06-RESEARCH.md
@.planning/phases/06-corpus/06-VALIDATION.md
@lib/scoria/eval.ex
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Deterministic Citation and Grounding Checks</name>
  <files>lib/scoria/knowledge/grounding.ex, lib/scoria/knowledge.ex, test/scoria/knowledge/grounding_test.exs</files>
  <read_first>
    - lib/scoria/eval.ex
    - .planning/phases/06-corpus/06-RESEARCH.md
    - .planning/phases/06-corpus/06-VALIDATION.md
  </read_first>
  <action>Implement `Scoria.Knowledge.Grounding` with explicit functions `score_citation_presence/1`, `score_citation_validity/1`, `score_chunk_membership/2`, `score_unsupported_claims/2`, `score_retrieval_hits/2`, and `score_retrieval_ranking/2`. The retrieval hit/rank functions must activate when labeled fixtures or expected chunk IDs exist and must persist numeric metrics such as hit rate, reciprocal rank, or equivalent explicit rank signals. Each deterministic function must return a structured result containing `status`, numeric `score`, and machine-readable failure details. Add a `Scoria.Knowledge.score_grounding/2` orchestration function that runs deterministic checks before any judge review and persists their outputs as `GroundingScore` rows.</action>
  <acceptance_criteria>
    - `lib/scoria/knowledge/grounding.ex` contains `def score_citation_presence(`
    - `lib/scoria/knowledge/grounding.ex` contains `def score_unsupported_claims(`
    - `lib/scoria/knowledge/grounding.ex` contains `def score_retrieval_hits(`
    - `lib/scoria/knowledge/grounding.ex` contains `def score_retrieval_ranking(`
    - `lib/scoria/knowledge.ex` contains `def score_grounding(`
    - `test/scoria/knowledge/grounding_test.exs` contains `unsupported_claims`
    - `test/scoria/knowledge/grounding_test.exs` contains `retrieval_ranking`
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/knowledge/grounding_test.exs</automated>
  </verify>
  <done>Deterministic grounding coverage includes citation integrity, unsupported-claim detection, and labeled retrieval hit/rank metrics, all persisted in machine-readable score records.</done>
</task>

<task type="tdd">
  <name>Task 2: Versioned Judge Review Persistence</name>
  <files>lib/scoria/knowledge/grounding_score.ex, lib/scoria/knowledge/grounding.ex, lib/scoria/eval.ex, test/scoria/knowledge/grounding_test.exs, test/scoria/eval_test.exs</files>
  <read_first>
    - lib/scoria/eval/eval_spec.ex
    - lib/scoria/eval.ex
    - .planning/phases/06-corpus/06-RESEARCH.md
  </read_first>
  <action>Extend grounding score persistence so judge-based review stores `scorer_kind`, `rubric_version`, `model`, `prompt_version`, `score`, `reasoning`, and `evidence_refs`. Reuse the repo's immutable versioning approach from `Scoria.Eval` for rubric evolution. The judge path should be optional and should append its results after deterministic checks instead of replacing them.</action>
  <acceptance_criteria>
    - `lib/scoria/knowledge/grounding_score.ex` contains `field(:prompt_version, :string)`
    - `lib/scoria/knowledge/grounding_score.ex` contains `field(:evidence_refs, :map)`
    - `lib/scoria/knowledge/grounding.ex` contains `scorer_kind`
    - `test/scoria/eval_test.exs` or `test/scoria/knowledge/grounding_test.exs` contains `rubric_version`
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/knowledge/grounding_test.exs test/scoria/eval_test.exs</automated>
  </verify>
  <done>Judge-based review persists rubric version, prompt version, model, score, reasoning, and evidence refs without replacing deterministic gate outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deterministic gate -> judge review | Required hard checks before softer semantic review |
| Eval persistence -> operator trust | Versioned score records used for CI and operator decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Elevation of Privilege | judge review | mitigate | Run deterministic grounding checks first and persist them separately so judge output cannot override hard failures. |
| T-06-04-02 | Repudiation | grounding score records | mitigate | Store rubric version, prompt version, model, and evidence refs with every judge score. |
</threat_model>

<verification>
- Deterministic scoring is reproducible and CI-friendly.
- Judge review is versioned, optional, and appended after deterministic scoring.
</verification>

<success_criteria>
- Scoria can fail groundedness on deterministic evidence issues before semantic review.
- Later review of a score can identify exactly which rubric and prompt version produced it.
</success_criteria>

<output>
After completion, create `.planning/phases/06-corpus/06-04-SUMMARY.md`
</output>
