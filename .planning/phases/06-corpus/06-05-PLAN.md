---
phase: 06-corpus
plan: 05
type: execute
wave: 5
depends_on: [02, 03, 04]
files_modified:
  - lib/scoria_web/components/citation_evidence_component.ex
  - lib/scoria_web/live/orchestrator_live.ex
  - lib/scoria_web/router.ex
  - lib/mix/tasks/scoria.install.ex
  - test/scoria_web/live/orchestrator_live_test.exs
  - test/scoria_web/router_test.exs
  - test/mix/tasks/scoria.install_test.exs
autonomous: true
requirements:
  - RAG-06
  - RAG-08
must_haves:
  truths:
    - "Retrieval evidence is projected inside the existing trace-first dashboard surface."
    - "Large evidence lists load asynchronously and do not become socket-state bloat."
  artifacts:
    - path: "lib/scoria_web/components/citation_evidence_component.ex"
      provides: "Evidence drilldown component for chunks, scores, and unsupported-claim flags"
    - path: "lib/scoria_web/live/orchestrator_live.ex"
      provides: "Trace-first operator projection of retrieval evidence"
  key_links:
    - from: "lib/scoria_web/live/orchestrator_live.ex"
      to: "assign_async"
      via: "lazy loading of evidence payloads"
      pattern: "assign_async"
    - from: "lib/mix/tasks/scoria.install.ex"
      to: "lib/scoria_web/router.ex"
      via: "idempotent route/install wiring"
      pattern: "scoria_dashboard"
---

<objective>
Expose Phase 6 retrieval evidence and grounding signals in the existing operator surface.

Purpose: make provenance and unsupported-claim signals inspectable without introducing a second product surface or blowing up LiveView socket state.
Output: evidence component, orchestrator/dashboard updates, additive install wiring, and UI/integration tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-corpus/06-CONTEXT.md
@.planning/phases/06-corpus/06-RESEARCH.md
@lib/scoria_web/live/orchestrator_live.ex
@lib/scoria_web/components/trace_tree_component.ex
@lib/mix/tasks/scoria.install.ex
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Evidence Drilldown Component</name>
  <files>lib/scoria_web/components/citation_evidence_component.ex, lib/scoria_web/live/orchestrator_live.ex, test/scoria_web/live/orchestrator_live_test.exs</files>
  <read_first>
    - lib/scoria_web/components/trace_tree_component.ex
    - lib/scoria_web/live/orchestrator_live.ex
    - .planning/phases/06-corpus/06-CONTEXT.md
  </read_first>
  <action>Create `ScoriaWeb.CitationEvidenceComponent` to render retrieval query text, ranked chunks, scores, freshness metadata, citation anchors, grounding scorer output, unsupported-claim flags, and explicit replay/promote affordances for the currently selected trace/span. The evidence presentation must include either citation hover details or a side-by-side evidence panel; choose side-by-side as the default Phase 6 mode so operators can compare answer and evidence without hiding provenance behind hover-only UI. Extend `ScoriaWeb.OrchestratorLive` to load evidence with `assign_async/3` or streamed updates when a trace or retrieval span is selected. Add event wiring such as `replay_retrieval` and `promote_retrieval` that re-run a persisted retrieval query and bridge evidence into the existing Eval promotion flow. Do not preload full chunk bodies into mount assigns; fetch the selected evidence payload only on demand.</action>
  <acceptance_criteria>
    - `lib/scoria_web/components/citation_evidence_component.ex` contains `defmodule ScoriaWeb.CitationEvidenceComponent`
    - `lib/scoria_web/live/orchestrator_live.ex` contains `assign_async`
    - `lib/scoria_web/live/orchestrator_live.ex` contains `unsupported`
    - `lib/scoria_web/live/orchestrator_live.ex` contains `freshness`
    - `lib/scoria_web/components/citation_evidence_component.ex` contains `side-by-side`
    - `lib/scoria_web/live/orchestrator_live.ex` contains `replay_retrieval`
    - `lib/scoria_web/live/orchestrator_live.ex` contains `promote_retrieval`
    - `test/scoria_web/live/orchestrator_live_test.exs` contains `citation`
    - `test/scoria_web/live/orchestrator_live_test.exs` contains `freshness`
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_test.exs</automated>
  </verify>
  <done>The operator surface can inspect side-by-side evidence with freshness and scorer metadata, replay a persisted retrieval query, and promote retrieval evidence into the eval workflow without leaving the trace-first dashboard.</done>
</task>

<task type="tdd">
  <name>Task 2: Additive Router and Install Wiring</name>
  <files>lib/scoria_web/router.ex, lib/mix/tasks/scoria.install.ex, test/scoria_web/router_test.exs, test/mix/tasks/scoria.install_test.exs</files>
  <read_first>
    - lib/scoria_web/router.ex
    - lib/mix/tasks/scoria.install.ex
    - test/scoria_web/router_test.exs
    - test/mix/tasks/scoria.install_test.exs
  </read_first>
  <action>Expose any new evidence view through the existing `scoria_dashboard` router surface only if the orchestrator page needs an additive route or component hook. Update `Mix.Tasks.Scoria.Install` so router/tailwind injection stays idempotent after the phase-6 additions. Keep the install story local to the existing task; do not add a second installer or separate mount path.</action>
  <acceptance_criteria>
    - `lib/scoria_web/router.ex` contains `scoria_dashboard`
    - `lib/mix/tasks/scoria.install.ex` contains `scoria_dashboard`
    - `test/scoria_web/router_test.exs` contains `scoria`
    - `test/mix/tasks/scoria.install_test.exs` contains `Scoria`
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria_web/router_test.exs test/mix/tasks/scoria.install_test.exs</automated>
  </verify>
  <done>Any phase-6 route or install changes remain additive, idempotent, and mounted through the existing `scoria_dashboard` surface only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Evidence API -> LiveView | Persisted retrieval/citation data entering operator-facing projection |
| Installer -> host app router | Automated host app edits for dashboard mounting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Denial of Service | LiveView evidence panel | mitigate | Load evidence asynchronously and avoid storing full retrieved bodies in long-lived socket assigns. |
| T-06-05-02 | Tampering | install task | mitigate | Keep router/tailwind injection idempotent and bounded to existing `scoria_dashboard` hooks. |
</threat_model>

<verification>
- Operators can inspect retrieval evidence from the current trace-first surface.
- Evidence loading is async and additive.
- Install and route changes remain minimal and idempotent.
</verification>

<success_criteria>
- Phase 6 evidence is visible where operators already work today.
- Retrieval provenance and grounding signals are inspectable without a second UI surface.
</success_criteria>

<output>
After completion, create `.planning/phases/06-corpus/06-05-SUMMARY.md`
</output>
