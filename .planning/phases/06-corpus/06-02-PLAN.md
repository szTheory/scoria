---
phase: 06-corpus
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - mix.exs
  - lib/scoria/knowledge/chunker.ex
  - lib/scoria/knowledge/embedder.ex
  - lib/scoria/knowledge/backends/pgvector.ex
  - lib/scoria/knowledge/retrievers/scrypath.ex
  - lib/scoria/knowledge.ex
  - test/scoria/knowledge/pgvector_test.exs
  - test/scoria/knowledge/scrypath_test.exs
autonomous: false
requirements:
  - RAG-01
  - RAG-02
  - RAG-07
  - RAG-08
must_haves:
  truths:
    - "pgvector is the default retrieval backend, but the public API stays backend-agnostic."
    - "Scrypath remains an optional normalized retrieval-source adapter."
  artifacts:
    - path: "lib/scoria/knowledge/backends/pgvector.ex"
      provides: "Default vector index and similarity-query implementation"
    - path: "lib/scoria/knowledge/retrievers/scrypath.ex"
      provides: "Optional Scrypath-to-Scoria retrieval normalization boundary"
  key_links:
    - from: "lib/scoria/knowledge/backends/pgvector.ex"
      to: "lib/scoria/knowledge/chunk.ex"
      via: "embedding query and result hydration"
      pattern: "Chunk"
    - from: "lib/scoria/knowledge/retrievers/scrypath.ex"
      to: "lib/scoria/knowledge/retrieval_result.ex"
      via: "normalized optional adapter outputs"
      pattern: "RetrievalResult"
---

<objective>
Implement the retrieval engine boundary for Phase 6 with pgvector as the boring default and Scrypath as an optional adapter.

Purpose: make retrieval executable without collapsing the public API into a single hard-coded implementation detail.
Output: chunking/embedder behaviors, pgvector backend, optional Scrypath adapter, and backend-focused tests once Wave 0 pgvector bootstrap is satisfied.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-corpus/06-CONTEXT.md
@.planning/phases/06-corpus/06-RESEARCH.md
@.planning/phases/06-corpus/06-PATTERNS.md
@.planning/phases/06-corpus/06-01-PLAN.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Chunker and Embedder Boundaries</name>
  <files>lib/scoria/knowledge/chunker.ex, lib/scoria/knowledge/embedder.ex, lib/scoria/knowledge.ex, test/scoria/knowledge/pgvector_test.exs</files>
  <read_first>
    - lib/scoria/eval.ex
    - .planning/phases/06-corpus/06-RESEARCH.md
    - .planning/phases/06-corpus/06-PATTERNS.md
  </read_first>
  <action>Create `Scoria.Knowledge.Chunker` and `Scoria.Knowledge.Embedder` behaviors. The chunker contract must accept a source payload and return ordered chunks with `body`, `heading_path`, `start_offset`, `end_offset`, `token_count`, `chunk_digest`, and a stable chunk identity derived from `source.digest <> \":\" <> start_offset <> \":\" <> end_offset`. The default chunker must implement heading/paragraph-aware chunking with configurable overlap and must preserve deterministic chunk identity across repeat ingests of identical source material. The embedder contract must return one embedding per chunk. Wire `Scoria.Knowledge.ingest_source/2` so it accepts `chunker:` and `embedder:` options while defaulting to conservative overlapping chunking and backend-provided embedding persistence.</action>
  <acceptance_criteria>
    - `lib/scoria/knowledge/chunker.ex` contains `@callback chunk(`
    - `lib/scoria/knowledge/embedder.ex` contains `@callback embed_chunks(`
    - `lib/scoria/knowledge.ex` contains `opts \\\\ []`
    - `lib/scoria/knowledge.ex` contains `chunker:`
    - `test/scoria/knowledge/pgvector_test.exs` contains `start_offset`
    - `test/scoria/knowledge/pgvector_test.exs` contains `repeat ingest`
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/knowledge/pgvector_test.exs</automated>
  </verify>
  <done>Chunking and embedding boundaries are explicit, option-driven, and usable from `Scoria.Knowledge.ingest_source/2`, with overlapping chunk output and deterministic stable chunk IDs proven across repeat ingest runs.</done>
</task>

<task type="tdd">
  <name>Task 2: Default pgvector Backend</name>
  <files>mix.exs, lib/scoria/knowledge/backends/pgvector.ex, lib/scoria/knowledge.ex, test/scoria/knowledge/pgvector_test.exs</files>
  <read_first>
    - mix.exs
    - lib/scoria/knowledge.ex
    - .planning/phases/06-corpus/06-RESEARCH.md
  </read_first>
  <action>Add the repo dependency needed to persist and query vector fields from Ecto, then implement `Scoria.Knowledge.Backends.Pgvector` with explicit functions `upsert_chunk_embeddings/2`, `similar_chunks/2`, and `delete_source_embeddings/1`. `similar_chunks/2` must accept `query_embedding`, `limit`, and optional filters, then return normalized ranked results with `chunk_id`, `source_id`, `rank`, `score`, and `metadata`. Set pgvector as the default backend in `Scoria.Knowledge.retrieve/2` and related reindex flows.</action>
  <acceptance_criteria>
    - `mix.exs` contains `pgvector`
    - `lib/scoria/knowledge/backends/pgvector.ex` contains `def similar_chunks(`
    - `lib/scoria/knowledge/backends/pgvector.ex` contains `def upsert_chunk_embeddings(`
    - `lib/scoria/knowledge.ex` contains `Scoria.Knowledge.Backends.Pgvector`
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/knowledge/pgvector_test.exs</automated>
  </verify>
  <done>The default pgvector backend can write embeddings, run similarity search, and return normalized ranked results through the public knowledge context.</done>
</task>

<task type="tdd">
  <name>Task 3: Optional Scrypath Retrieval Adapter</name>
  <files>lib/scoria/knowledge/retrievers/scrypath.ex, lib/scoria/knowledge.ex, test/scoria/knowledge/scrypath_test.exs</files>
  <read_first>
    - .planning/memory/scrypath-rag-synergy.md
    - lib/scoria/observe/adapters/jido.ex
    - .planning/phases/06-corpus/06-RESEARCH.md
  </read_first>
  <action>Implement `Scoria.Knowledge.Retrievers.Scrypath` as an optional module that normalizes external Scrypath query results into the same result shape returned by the pgvector backend. Keep the adapter opt-in through explicit `retriever: Scoria.Knowledge.Retrievers.Scrypath` options. Do not let the adapter own source or chunk persistence. Instead, require one of two explicit reconciliation paths before returning results: either resolve every Scrypath hit to an existing Scoria-owned `Source`/`Chunk` row by durable locator, digest, and offset metadata, or reject the hit as unsupported for citation-grade provenance. The adapter may only return results that already have Scoria-owned `source_id`, `chunk_id`, and `chunk_digest` values available for later retrieval logging, citation persistence, and grounding checks.</action>
  <acceptance_criteria>
    - `lib/scoria/knowledge/retrievers/scrypath.ex` contains `def normalize_results(`
    - `lib/scoria/knowledge/retrievers/scrypath.ex` contains `def retrieve(`
    - `lib/scoria/knowledge/retrievers/scrypath.ex` contains `source_id`
    - `lib/scoria/knowledge/retrievers/scrypath.ex` contains `chunk_id`
    - `lib/scoria/knowledge.ex` contains `retriever`
    - `test/scoria/knowledge/scrypath_test.exs` contains `Scrypath`
    - `test/scoria/knowledge/scrypath_test.exs` contains `chunk_digest`
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/knowledge/scrypath_test.exs</automated>
  </verify>
  <done>Scrypath retrieval remains optional and only returns results that have been reconciled to Scoria-owned source and chunk IDs, so later citation and grounding flows can rely on one durable provenance contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend adapter -> context | Similarity search outputs becoming ranked evidence |
| External retriever -> Scoria rows | Optional Scrypath responses entering Scoria-owned provenance records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | backend normalization | mitigate | Normalize backend results into explicit `chunk_id`, `source_id`, `rank`, and `score` fields before persistence. |
| T-06-02-02 | Elevation of Privilege | Scrypath adapter scope | mitigate | Keep Scrypath adapter opt-in and prohibit it from becoming corpus truth owner or default backend. |
</threat_model>

<verification>
- Retrieval boundaries are explicit and test-covered.
- pgvector is the default backend without leaking backend specifics into the public API.
- Scrypath stays optional and normalized.
</verification>

<success_criteria>
- Scoria can execute retrieval through a default pgvector backend.
- Future slices can switch retrievers via ordinary function options without changing the provenance contract.
</success_criteria>

<output>
After completion, create `.planning/phases/06-corpus/06-02-SUMMARY.md`
</output>
