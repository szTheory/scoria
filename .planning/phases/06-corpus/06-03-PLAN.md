---
phase: 06-corpus
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - lib/scoria/knowledge.ex
  - lib/scoria/knowledge/citation_formatter.ex
  - lib/scoria/knowledge/retrieval_run.ex
  - lib/scoria/knowledge/retrieval_result.ex
  - lib/scoria/knowledge/citation.ex
  - test/scoria/knowledge/retrieval_test.exs
  - test/scoria/knowledge/citation_formatter_test.exs
autonomous: true
requirements:
  - RAG-03
  - RAG-04
  - RAG-07
must_haves:
  truths:
    - "Retrieval-backed answers and traces share one durable provenance contract."
    - "Citations are machine-readable anchors first and rendered labels second."
  artifacts:
    - path: "lib/scoria/knowledge/citation_formatter.ex"
      provides: "Strict inline citation rendering and anchor validation"
    - path: "lib/scoria/knowledge.ex"
      provides: "Retrieval logging and citation creation APIs"
  key_links:
    - from: "lib/scoria/knowledge.ex"
      to: "lib/scoria/repo/span.ex"
      via: "trace/span provenance linkage"
      pattern: "span_id|trace_id"
    - from: "lib/scoria/knowledge/citation.ex"
      to: "lib/scoria/knowledge/chunk.ex"
      via: "durable anchor identity"
      pattern: "chunk_id"
---

<objective>
Implement retrieval provenance and citation contracts for Phase 6.

Purpose: make every retrieval-backed answer challengeable by persisting ranked evidence and machine-readable citation anchors that link back into Scoria's trace model.
Output: retrieval logging APIs, citation formatter/validator, and provenance-focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-corpus/06-CONTEXT.md
@.planning/phases/06-corpus/06-RESEARCH.md
@.planning/phases/06-corpus/06-PATTERNS.md
@lib/scoria/repo/span.ex
@lib/scoria_web/components/trace_tree_component.ex
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Retrieval Run Logging</name>
  <files>lib/scoria/knowledge.ex, lib/scoria/knowledge/retrieval_run.ex, lib/scoria/knowledge/retrieval_result.ex, test/scoria/knowledge/retrieval_test.exs</files>
  <read_first>
    - lib/scoria/workflows.ex
    - lib/scoria/repo/span.ex
    - .planning/phases/06-corpus/06-RESEARCH.md
  </read_first>
  <action>Extend `Scoria.Knowledge.retrieve/2` so it writes a `RetrievalRun` row plus ordered `RetrievalResult` rows in the same transaction. The retrieval run must persist `query_text`, `backend`, `retriever`, `top_k`, `filters`, `trace_id`, `span_id`, `status`, and `latency_ms`. Each result row must persist `chunk_id`, `source_id`, `rank`, `score`, `metadata`, and any normalized backend payload needed for later evidence display.</action>
  <acceptance_criteria>
    - `lib/scoria/knowledge.ex` contains `def retrieve(`
    - `lib/scoria/knowledge.ex` contains `append_retrieval_results`
    - `lib/scoria/knowledge/retrieval_run.ex` contains `field(:query_text, :string)`
    - `test/scoria/knowledge/retrieval_test.exs` contains `RetrievalRun`
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/knowledge/retrieval_test.exs</automated>
  </verify>
  <done>Every retrieval operation writes one durable retrieval run plus ordered persisted result rows that later UX and eval code can query directly.</done>
</task>

<task type="tdd">
  <name>Task 2: Strict Citation Contract</name>
  <files>lib/scoria/knowledge/citation.ex, lib/scoria/knowledge/citation_formatter.ex, lib/scoria/knowledge.ex, test/scoria/knowledge/citation_formatter_test.exs</files>
  <read_first>
    - .planning/phases/06-corpus/06-RESEARCH.md
    - lib/scoria/repo/span.ex
    - lib/scoria/knowledge/chunk.ex
  </read_first>
  <action>Create `Scoria.Knowledge.CitationFormatter` with explicit functions `build_anchors/2`, `render_inline/1`, and `validate_anchor/2`. The anchor shape must include `source_id`, `chunk_id`, `chunk_digest`, `start_offset`, `end_offset`, `label`, and `locator`. `render_inline/1` should produce the blessed inline string form while leaving the durable anchor map intact. `validate_anchor/2` must reject anchors whose offsets fall outside the cited chunk or whose `chunk_digest` does not match persisted corpus state.</action>
  <acceptance_criteria>
    - `lib/scoria/knowledge/citation_formatter.ex` contains `def build_anchors(`
    - `lib/scoria/knowledge/citation_formatter.ex` contains `def validate_anchor(`
    - `lib/scoria/knowledge/citation.ex` contains `field(:locator, :map)`
    - `test/scoria/knowledge/citation_formatter_test.exs` contains `validate_anchor`
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/knowledge/citation_formatter_test.exs</automated>
  </verify>
  <done>Citation anchors are machine-readable, offset-valid, and renderable into the blessed inline citation form without losing durable provenance.</done>
</task>

<task type="tdd">
  <name>Task 3: Trace-Provenance Integration</name>
  <files>lib/scoria/knowledge.ex, lib/scoria/knowledge/citation.ex, test/scoria/knowledge/retrieval_test.exs</files>
  <read_first>
    - lib/scoria/repo/trace.ex
    - lib/scoria/repo/span.ex
    - .planning/phases/06-corpus/06-CONTEXT.md
  </read_first>
  <action>Ensure citation creation and retrieval logging accept `trace_id` and `span_id` so retrieval becomes explicit evidence inside the existing trace model. Add helper functions that let callers store citations for a given answer or span without inventing a second provenance tree. Keep the trace-first mental model explicit in names and comments.</action>
  <acceptance_criteria>
    - `lib/scoria/knowledge/citation.ex` contains `field(:trace_id, :binary_id)`
    - `lib/scoria/knowledge/citation.ex` contains `field(:span_id, :binary_id)`
    - `lib/scoria/knowledge.ex` contains `trace_id`
    - `lib/scoria/knowledge.ex` contains `span_id`
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/knowledge/retrieval_test.exs test/scoria/knowledge/citation_formatter_test.exs</automated>
  </verify>
  <done>Retrieval and citation persistence are explicitly linked to trace and span IDs so evidence stays inside Scoria's existing trace-first mental model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Retrieval results -> answer rendering | Ranked evidence becoming visible answer citations |
| Trace linkage -> operator inspection | Provenance references used to challenge or accept model output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Repudiation | citation anchors | mitigate | Persist trace/span/source/chunk linkage and validate offsets against stored chunk bodies. |
| T-06-03-02 | Tampering | rendered citation text | mitigate | Treat rendered strings as derived output from machine-readable anchors rather than source of truth. |
</threat_model>

<verification>
- Retrieval rows and citation rows share a durable provenance contract.
- Citation validation rejects stale or malformed anchors.
- Trace linkage is explicit and test-covered.
</verification>

<success_criteria>
- Retrieval-backed answers can be challenged with durable source and chunk evidence.
- Operators and evaluators read the same provenance chain rather than separate ad hoc payloads.
</success_criteria>

<output>
After completion, create `.planning/phases/06-corpus/06-03-SUMMARY.md`
</output>
