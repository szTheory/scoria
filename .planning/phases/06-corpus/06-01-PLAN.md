---
phase: 06-corpus
plan: 01
type: execute
wave: 1
depends_on: [00]
files_modified:
  - priv/repo/migrations/20260511_create_knowledge_tables.exs
  - lib/scoria/knowledge.ex
  - lib/scoria/knowledge/source.ex
  - lib/scoria/knowledge/chunk.ex
  - lib/scoria/knowledge/citation.ex
  - lib/scoria/knowledge/retrieval_run.ex
  - lib/scoria/knowledge/retrieval_result.ex
  - lib/scoria/knowledge/grounding_score.ex
  - test/scoria/knowledge_test.exs
autonomous: true
requirements:
  - RAG-01
  - RAG-02
  - RAG-03
  - RAG-08
must_haves:
  truths:
    - "Scoria owns durable knowledge nouns in Ecto before any retrieval UX or scoring layers depend on them."
    - "The public API is `Scoria.Knowledge`, not a DSL or config maze."
  artifacts:
    - path: "lib/scoria/knowledge.ex"
      provides: "Public context for source ingest, chunk listing, retrieval logging, and score persistence"
    - path: "priv/repo/migrations/20260511_create_knowledge_tables.exs"
      provides: "Additive corpus, retrieval, citation, and grounding schema"
  key_links:
    - from: "lib/scoria/knowledge.ex"
      to: "Ecto.Multi"
      via: "atomic ingest and retrieval persistence boundaries"
      pattern: "Ecto\\.Multi"
    - from: "lib/scoria/knowledge/chunk.ex"
      to: "pgvector"
      via: "default embedding storage field"
      pattern: "embedding"
---

<objective>
Build the durable knowledge foundation for Phase 6 by introducing corpus, provenance, retrieval, and grounding tables plus the public `Scoria.Knowledge` context.

Purpose: make Ecto the source of truth for retrieval evidence before backend-specific retrieval logic or operator UI is added.
Output: migration, schemas, context APIs, and tests for durable corpus lifecycle primitives.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/MILESTONES.md
@.planning/STATE.md
@.planning/phases/06-corpus/06-CONTEXT.md
@.planning/phases/06-corpus/06-RESEARCH.md
@.planning/phases/06-corpus/06-PATTERNS.md
@.planning/phases/06-corpus/06-VALIDATION.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Knowledge Persistence Migration</name>
  <files>priv/repo/migrations/20260511_create_knowledge_tables.exs, test/scoria/knowledge_test.exs</files>
  <read_first>
    - priv/repo/migrations/20260510174619_create_eval_tables.exs
    - lib/scoria/repo/trace.ex
    - lib/scoria/repo/span.ex
  </read_first>
  <action>Create `priv/repo/migrations/20260511_create_knowledge_tables.exs` with additive binary-id tables named `ai_knowledge_sources`, `ai_knowledge_chunks`, `ai_knowledge_citations`, `ai_retrieval_runs`, `ai_retrieval_results`, and `ai_grounding_scores`. Add `execute(\"CREATE EXTENSION IF NOT EXISTS vector\")` before chunk table creation, store chunk embeddings on `ai_knowledge_chunks`, add foreign keys between source, chunk, citation, retrieval, and score rows, and create indexes on `source_id`, `chunk_digest`, `retrieval_run_id`, `trace_id`, and rank/score lookup fields.</action>
  <acceptance_criteria>
    - `priv/repo/migrations/20260511_create_knowledge_tables.exs` contains `create table(:ai_knowledge_sources`
    - `priv/repo/migrations/20260511_create_knowledge_tables.exs` contains `create table(:ai_knowledge_chunks`
    - `priv/repo/migrations/20260511_create_knowledge_tables.exs` contains `create table(:ai_knowledge_citations`
    - `priv/repo/migrations/20260511_create_knowledge_tables.exs` contains `CREATE EXTENSION IF NOT EXISTS vector`
  </acceptance_criteria>
  <verify>
    <automated>mix scoria.pgvector.bootstrap --check && MIX_ENV=test mix test test/scoria/knowledge_test.exs</automated>
  </verify>
  <done>The migration defines the full phase-6 knowledge storage surface, including vector extension setup and all additive corpus/provenance tables and indexes.</done>
</task>

<task type="tdd">
  <name>Task 2: Durable Knowledge Schemas</name>
  <files>lib/scoria/knowledge/source.ex, lib/scoria/knowledge/chunk.ex, lib/scoria/knowledge/citation.ex, lib/scoria/knowledge/retrieval_run.ex, lib/scoria/knowledge/retrieval_result.ex, lib/scoria/knowledge/grounding_score.ex, test/scoria/knowledge_test.exs</files>
  <read_first>
    - lib/scoria/repo/trace.ex
    - lib/scoria/repo/span.ex
    - lib/scoria/eval/dataset.ex
    - lib/scoria/eval/score.ex
  </read_first>
  <action>Create `Scoria.Knowledge.Source`, `Chunk`, `Citation`, `RetrievalRun`, `RetrievalResult`, and `GroundingScore` schemas using `@primary_key {:id, :binary_id, autogenerate: true}` and `@foreign_key_type :binary_id`. `Source` must store `entity_id`, `version`, `is_current`, `kind`, `uri`, `title`, `digest`, and `metadata`. `Chunk` must store `source_id`, `chunk_digest`, `body`, `heading_path`, `start_offset`, `end_offset`, `token_count`, `embedding`, and `metadata`. `Citation` must store `source_id`, `chunk_id`, `trace_id`, `span_id`, `label`, `start_offset`, `end_offset`, `quote`, and `locator`. `RetrievalRun` and `RetrievalResult` must persist query, adapter/backend, filters, rank, score, and trace linkage. `GroundingScore` must persist scorer kind, rubric version, value, reasoning, and evidence refs.</action>
  <acceptance_criteria>
    - `lib/scoria/knowledge/chunk.ex` contains `schema \"ai_knowledge_chunks\" do`
    - `lib/scoria/knowledge/citation.ex` contains `field(:label, :string)`
    - `lib/scoria/knowledge/retrieval_run.ex` contains `field(:backend, :string)`
    - `lib/scoria/knowledge/grounding_score.ex` contains `field(:rubric_version, :string)`
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/knowledge_test.exs</automated>
  </verify>
  <done>All durable knowledge schemas compile, validate required fields, and encode the locked corpus/provenance nouns explicitly.</done>
</task>

<task type="tdd">
  <name>Task 3: Public Knowledge Context</name>
  <files>lib/scoria/knowledge.ex, test/scoria/knowledge_test.exs</files>
  <read_first>
    - lib/scoria/eval.ex
    - lib/scoria/workflows.ex
    - .planning/phases/06-corpus/06-RESEARCH.md
  </read_first>
  <action>Implement `Scoria.Knowledge` with explicit public functions `create_source/1`, `ingest_source/2`, `reembed_source/2`, `reindex_source/2`, `list_source_chunks/1`, `create_retrieval_run/1`, `append_retrieval_results/2`, and `create_grounding_score/1`. Use `Ecto.Multi` anywhere a source write also creates chunk, retrieval, citation, or score rows. Keep backend/chunker choice injectable through options maps, not module attributes or DSL declarations.</action>
  <acceptance_criteria>
    - `lib/scoria/knowledge.ex` contains `def create_source(`
    - `lib/scoria/knowledge.ex` contains `def ingest_source(`
    - `lib/scoria/knowledge.ex` contains `def create_retrieval_run(`
    - `lib/scoria/knowledge.ex` contains `Ecto.Multi`
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/knowledge_test.exs</automated>
  </verify>
  <done>`Scoria.Knowledge` provides explicit lifecycle functions for source, retrieval, and score persistence without introducing a DSL or hidden configuration contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ingest pipeline -> DB | Source payloads and chunk outputs becoming durable corpus truth |
| Retrieval provenance -> DB | Machine-readable evidence records that future evals and UI trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | knowledge migration/schema | mitigate | Keep corpus, citation, retrieval, and grounding rows explicit and relational so provenance cannot hide in opaque blobs only. |
| T-06-01-02 | Repudiation | `Scoria.Knowledge` persistence | mitigate | Use explicit retrieval and score rows plus trace linkage so later operator review can audit the evidence chain. |
</threat_model>

<verification>
- Wave 0 prerequisite is satisfied before this plan runs.
- Corpus tables, schemas, and public context compile cleanly.
- Durable knowledge nouns can be created without relying on transient UI or runtime state.
</verification>

<success_criteria>
- Phase 6 has a durable knowledge data model aligned to the locked nouns in context.
- Future retrieval, scoring, and UI slices can depend on persisted evidence rather than ad hoc maps.
</success_criteria>

<output>
After completion, create `.planning/phases/06-corpus/06-01-SUMMARY.md`
</output>
