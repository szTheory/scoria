---
phase: 06-corpus
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - lib/mix/tasks/scoria.pgvector.bootstrap.ex
  - dev/pgvector-compose.yml
  - test/test_helper.exs
  - test/support/knowledge_case.exs
autonomous: false
requirements:
  - RAG-02
  - RAG-08
must_haves:
  truths:
    - "pgvector availability is an explicit prerequisite, not an assumption hidden inside a migration."
    - "Vector-backed test scaffolding runs only after the repo can positively verify a pgvector-capable database."
  artifacts:
    - path: "lib/mix/tasks/scoria.pgvector.bootstrap.ex"
      provides: "Wave 0 bootstrap command that verifies or provisions pgvector support before Phase 6 migrations/tests"
    - path: "test/support/knowledge_case.exs"
      provides: "Shared test setup for vector-backed knowledge tests"
  key_links:
    - from: "lib/mix/tasks/scoria.pgvector.bootstrap.ex"
      to: "pg_extension"
      via: "positive verification of vector support"
      pattern: "pg_extension"
    - from: "test/test_helper.exs"
      to: "Mix.Tasks.Scoria.Pgvector.Bootstrap"
      via: "pre-migration/test prerequisite enforcement"
      pattern: "Pgvector.Bootstrap"
---

<objective>
Create the Wave 0 environment prerequisite for Phase 6 so pgvector-backed work can execute predictably on machines where the `vector` extension is not already available.

Purpose: provision or positively verify pgvector support and add prerequisite test scaffolding before any vector-backed migration or retrieval verification runs.
Output: bootstrap mix task, repo-local dev bootstrap asset, and shared test foundation for vector-backed knowledge tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-corpus/06-RESEARCH.md
@.planning/phases/06-corpus/06-VALIDATION.md
@mix.exs
@test/test_helper.exs
</context>

<tasks>

<task type="tdd">
  <name>Task 1: pgvector Bootstrap Command</name>
  <files>lib/mix/tasks/scoria.pgvector.bootstrap.ex, dev/pgvector-compose.yml, test/test_helper.exs</files>
  <read_first>
    - .planning/phases/06-corpus/06-RESEARCH.md
    - mix.exs
    - lib/mix/tasks/scoria.install.ex
  </read_first>
  <action>Create `Mix.Tasks.Scoria.Pgvector.Bootstrap` with a `--check` path that connects to the configured dev/test database and positively verifies `vector` support via `pg_extension`. When support is missing, the task must either provision a pgvector-capable service through a committed `dev/pgvector-compose.yml` asset or fail with exact next-step commands that the executor can run before continuing. Update prerequisite test or setup entrypoints so Phase 6 vector-backed verification starts with `mix scoria.pgvector.bootstrap --check` and cannot silently skip the environment requirement.</action>
  <acceptance_criteria>
    - `lib/mix/tasks/scoria.pgvector.bootstrap.ex` contains `defmodule Mix.Tasks.Scoria.Pgvector.Bootstrap`
    - `lib/mix/tasks/scoria.pgvector.bootstrap.ex` contains `--check`
    - `lib/mix/tasks/scoria.pgvector.bootstrap.ex` contains `pg_extension`
    - `dev/pgvector-compose.yml` exists
    - `test/test_helper.exs` contains `Pgvector.Bootstrap`
  </acceptance_criteria>
  <verify>
    <automated>mix scoria.pgvector.bootstrap --check</automated>
  </verify>
  <done>The repo has a concrete bootstrap command and asset that verifies or provisions pgvector support before any Phase 6 vector-backed migration or test command runs.</done>
</task>

<task type="tdd">
  <name>Task 2: Shared Wave 0 Test Scaffold</name>
  <files>test/support/knowledge_case.exs, test/test_helper.exs</files>
  <read_first>
    - test/test_helper.exs
    - test/support/eval_case.exs
    - .planning/phases/06-corpus/06-VALIDATION.md
  </read_first>
  <action>Create `test/support/knowledge_case.exs` as the shared prerequisite layer for upcoming Phase 6 tests. It must centralize pgvector bootstrap assumptions, expose a clear failure when the prerequisite was not satisfied, and keep future `test/scoria/knowledge*.exs` files from duplicating environment-setup logic. Update `test/test_helper.exs` so this support module is loaded before Wave 1 knowledge tests run.</action>
  <acceptance_criteria>
    - `test/support/knowledge_case.exs` contains `defmodule Scoria.KnowledgeCase`
    - `test/support/knowledge_case.exs` contains `pgvector`
    - `test/test_helper.exs` contains `Code.require_file`
  </acceptance_criteria>
  <verify>
    <automated>mix scoria.pgvector.bootstrap --check</automated>
  </verify>
  <done>Phase 6 has a reusable test foundation that enforces the pgvector prerequisite and unblocks Wave 1 knowledge tests without duplicated setup logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local/CI environment -> database setup | Host environment prerequisites for vector-backed work |
| Test runner -> bootstrap command | Verification entrypoints that must not bypass pgvector availability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-00-01 | Denial of Service | pgvector prerequisite | mitigate | Add an explicit bootstrap command and committed provisioning asset before Phase 6 migrations/tests run. |
| T-06-00-02 | Repudiation | test foundation | mitigate | Centralize prerequisite enforcement in shared test setup so vector-backed failures have one clear entrypoint and error path. |
</threat_model>

<verification>
- pgvector availability is positively verified before vector-backed migration/test work starts.
- Shared test scaffolding exists for future knowledge tests.
</verification>

<success_criteria>
- Phase 6 execution no longer assumes `vector` support is already present.
- Wave 1 and later plans can depend on a concrete prerequisite instead of an environment guess.
</success_criteria>

<output>
After completion, create `.planning/phases/06-corpus/06-00-SUMMARY.md`
</output>
