---
phase: 40-online-scoring-review-queue
plan: 3
type: execute
wave: 3
depends_on: ["40-02"]
files_modified:
  - lib/scoria/eval/online_scoring.ex
  - lib/scoria/eval/campaign_worker.ex
  - lib/scoria/eval/judge_runner.ex
  - lib/scoria/eval.ex
  - test/scoria/eval/campaign_worker_test.exs
autonomous: true
requirements: [SCOR-02]
nyquist_compliant: true
must_haves:
  truths:
    - "Deterministic scoring can complete a sampled trace without any judge call when rules already decide the verdict."
    - "Optional judge scoring adds model-backed rationale without erasing deterministic evidence."
    - "Candidate review state is derived from persisted score evidence, not from UI inference."
  artifacts:
    - path: "lib/scoria/eval/online_scoring.ex"
      provides: "Deterministic-first scoring and candidate review-state updates"
    - path: "lib/scoria/eval/judge_runner.ex"
      provides: "Optional judge-backed score rows that preserve richer evidence fields"
    - path: "test/scoria/eval/campaign_worker_test.exs"
      provides: "Worker-level proof for deterministic, judge, and retry behavior"
  key_links:
    - from: "lib/scoria/eval/campaign_worker.ex"
      to: "lib/scoria/eval/online_scoring.ex"
      via: "worker completion path updates candidate state from persisted score evidence"
      pattern: "perform|execute_campaign_target|complete_campaign_target"
    - from: "lib/scoria/eval/judge_runner.ex"
      to: "lib/scoria/eval.ex"
      via: "replace_eval_scores/record_eval_scores maintain idempotent score truth"
      pattern: "replace_eval_scores|record_eval_scores"
---

<objective>
Turn the persisted Phase 40 substrate into real online scoring behavior.

Purpose: score sampled traces deterministically first, add judge reasoning only when configured or needed, and derive reviewable candidate state from durable evidence.
Output: deterministic scoring path, optional judge augmentation, and candidate review-state updates hanging off the existing worker lifecycle.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-online-scoring-review-queue/40-RESEARCH.md
@.planning/phases/40-online-scoring-review-queue/40-VALIDATION.md
@.planning/phases/40-online-scoring-review-queue/40-PATTERNS.md
@lib/scoria/eval.ex
@lib/scoria/eval/campaign_worker.ex
@lib/scoria/eval/judge_runner.ex
@.planning/phases/40-online-scoring-review-queue/40-02-PLAN.md

<interfaces>
From `lib/scoria/eval.ex`:
```elixir
def execute_campaign_target(%{target: %EvalCampaignTarget{}, eval_run: %EvalRun{}, eval_spec: %EvalSpec{}})
def complete_campaign_target(context, %{eval_run: %EvalRun{}})
def fail_campaign_target(context, reason, opts \\ [])
def replace_eval_scores(%EvalRun{} = eval_run, score_attrs_list)
```

From `lib/scoria/eval/judge_runner.ex`:
```elixir
JudgeRunner.run_existing(eval_run, %{eval_spec: eval_spec, dataset: dataset, provider: provider, model: model})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic-first candidate scoring and review-state derivation</name>
  <files>lib/scoria/eval/online_scoring.ex, lib/scoria/eval/campaign_worker.ex, lib/scoria/eval.ex, test/scoria/eval/campaign_worker_test.exs</files>
  <behavior>
    - Test 1: deterministic rule/policy scorers persist one or more score rows with scorer kind/version and sampling provenance.
    - Test 2: when deterministic evidence already reaches a terminal verdict, the worker finishes without invoking judge scoring and updates candidate review state accordingly.
  </behavior>
  <action>Extend the campaign worker execution path so online-scoring candidates run through a deterministic-first evaluator before any judge call. Keep the worker on the existing `CampaignWorker`/`Eval.execute_campaign_target/1` contract; do not create a second worker type. `Scoria.Eval.OnlineScoring` should own rule evaluation and candidate summarization, using the new score-evidence fields to persist scorer kind/version, explanation, evidence refs, and sampling metadata on every score row. Candidate state should move through explicit durable values such as `queued`, `scored`, `needs_review`, and `promotion_candidate` based on low-score thresholds, policy triggers, or pass conditions. Never promote datasets or request approvals from this worker path.</action>
  <verify>
    <automated>mix test test/scoria/eval/campaign_worker_test.exs</automated>
  </verify>
  <done>Deterministic scorers run inside the existing async worker lane, persist additive evidence on each score, and mark the candidate with a durable review-state summary without any queue/UI participation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append optional judge rationale without overwriting deterministic evidence</name>
  <files>lib/scoria/eval/judge_runner.ex, lib/scoria/eval/online_scoring.ex, lib/scoria/eval.ex, test/scoria/eval/campaign_worker_test.exs</files>
  <behavior>
    - Test 1: configured judge scoring records model-backed rationale on the same eval run while preserving deterministic rows.
    - Test 2: worker retries remain idempotent by replacing/rebuilding the eval run’s score set and re-deriving the candidate summary from persisted rows.
  </behavior>
  <action>Adapt `JudgeRunner` and the Phase 40 worker path so judge scoring becomes an optional second scorer kind on the same eval run, not a replacement for deterministic evidence. Preserve the current richer attrs (`status`, `score`, `explanation`, `judge_model`, `rubric_version`, `evidence_refs`, `metadata`) and make the Phase 40 candidate summary choose the final queue severity from all persisted score rows. Respect the research recommendation that deterministic scorers may skip the judge when they already produce a terminal review verdict. Keep retry semantics aligned with existing `replace_eval_scores/2` idempotency so a replayed job does not duplicate score truth or flip a candidate into multiple states.</action>
  <verify>
    <automated>mix test test/scoria/eval/campaign_worker_test.exs</automated>
  </verify>
  <done>Judge-backed rationale is additive, durable, and optional; deterministic evidence remains visible, retries stay idempotent, and review state still derives from persisted score rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Oban worker -> score persistence | Async execution writes durable quality evidence |
| deterministic scorer -> judge scorer | A secondary scorer may enrich but must not corrupt first-pass evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-05 | Tampering | `Scoria.Eval.OnlineScoring` candidate summary | mitigate | Derive candidate review status from persisted score rows and explicit thresholds, not from ephemeral worker-local booleans. |
| T-40-06 | Repudiation | `Scoria.Eval.JudgeRunner` | mitigate | Persist judge model, explanation, rubric version, and evidence refs on every judge score row instead of opaque blobs. |
</threat_model>

<verification>
Use the worker smoke lane after each task. After this plan lands, wave-level validation still runs `mix test`.
</verification>

<success_criteria>
Sampled traces can finish scoring end to end through the existing worker flow, with deterministic-first evidence, optional judge rationale, and durable candidate review states.
</success_criteria>

<output>
After completion, create `.planning/phases/40-online-scoring-review-queue/40-online-scoring-review-queue-03-SUMMARY.md`
</output>
