---
phase: 40-online-scoring-review-queue
plan: 5
type: execute
wave: 5
depends_on: ["40-04"]
files_modified:
  - lib/scoria_web/router.ex
  - lib/scoria_web/live/orchestrator_live.ex
  - lib/scoria_web/live/review_queue_live.ex
  - lib/scoria_web/live/workflow_live/show.ex
  - test/scoria_web/live/orchestrator_live_test.exs
  - test/scoria_web/live/review_queue_live_test.exs
  - test/scoria_web/live/workflow_live_test.exs
autonomous: true
requirements: [SCOR-03]
nyquist_compliant: true
must_haves:
  truths:
    - "Operators can open a dedicated review queue page and inspect one candidate at a time."
    - "Opening a workflow from the queue keeps the selected candidate’s rationale and provenance visible."
    - "Opening runtime context from the queue preserves the candidate’s trace context on `/scoria?runtime=...`."
  artifacts:
    - path: "lib/scoria_web/live/review_queue_live.ex"
      provides: "Two-column operator queue surface matching the Phase 40 UI contract"
    - path: "lib/scoria_web/live/orchestrator_live.ex"
      provides: "Runtime deep-link landing surface that preserves queue-selected evidence context"
    - path: "lib/scoria_web/live/workflow_live/show.ex"
      provides: "Workflow deep-link evidence strip for queue-selected candidates"
  key_links:
    - from: "lib/scoria_web/live/review_queue_live.ex"
      to: "lib/scoria/eval/review_queue.ex"
      via: "mount/filter/select handlers load curated DTOs only"
      pattern: "list_review_queue|get_review_candidate"
    - from: "lib/scoria_web/live/review_queue_live.ex"
      to: "lib/scoria_web/live/orchestrator_live.ex"
      via: "runtime deep link carries runtime id and queue candidate context into `/scoria?runtime=...`"
      pattern: "runtime=|review_candidate_id"
    - from: "lib/scoria_web/live/review_queue_live.ex"
      to: "lib/scoria_web/live/workflow_live/show.ex"
      via: "deep link carries candidate id into workflow evidence page"
      pattern: "review_candidate_id"
---

<objective>
Render the operator review queue and the two deep-link landing surfaces on top of the projection API.

Purpose: expose the dedicated queue UX without losing the runtime deep-link and workflow evidence behavior already resolved in prior revisions.
Output: review queue route/page, workflow deep-link preservation, and runtime deep-link preservation on `/scoria?runtime=...`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-online-scoring-review-queue/40-UI-SPEC.md
@.planning/phases/40-online-scoring-review-queue/40-VALIDATION.md
@.planning/phases/40-online-scoring-review-queue/40-PATTERNS.md
@lib/scoria_web/router.ex
@lib/scoria_web/live/orchestrator_live.ex
@lib/scoria_web/live/workflow_live/show.ex
@.planning/phases/40-online-scoring-review-queue/40-04-PLAN.md

<interfaces>
From `lib/scoria_web/router.ex`:
```elixir
live("/", ScoriaWeb.OrchestratorLive, :index)
live("/workflows/:id", ScoriaWeb.WorkflowLive.Show, :show)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the operator queue surface and preserve both workflow and runtime deep-link evidence</name>
  <files>lib/scoria_web/router.ex, lib/scoria_web/live/orchestrator_live.ex, lib/scoria_web/live/review_queue_live.ex, lib/scoria_web/live/workflow_live/show.ex, test/scoria_web/live/orchestrator_live_test.exs, test/scoria_web/live/review_queue_live_test.exs, test/scoria_web/live/workflow_live_test.exs</files>
  <behavior>
    - Test 1: `/scoria/reviews` renders the queue list, summary strip, filter bar, and detail rail from projection DTOs using the approved selection hierarchy.
    - Test 2: opening a workflow from a queue row preserves the selected candidate’s scoring rationale/provenance on the workflow page, and opening runtime context from the detail rail resolves `/scoria?runtime=...` with the same candidate context visible.
  </behavior>
  <action>Add a dedicated `ScoriaWeb.ReviewQueueLive` page under the existing dashboard macro, using a two-column desktop layout and the exact interaction contract from `40-UI-SPEC.md`: left queue list, right evidence/detail rail, explicit status/severity badges, one-line rationale, summary strip, and filter bar defaulted to `needs review`. Keep all data loading behind `Scoria.Eval.ReviewQueue` so the LiveView only handles filter/selection state. Add workflow deep links of the form `/scoria/workflows/:id?review_candidate_id=...` and runtime deep links of the form `/scoria?runtime=...&review_candidate_id=...` from the detail rail. Extend both `WorkflowLive.Show` and `OrchestratorLive` to read `review_candidate_id`, resolve it through the projection boundary, and keep the candidate’s scoring rationale/provenance visible alongside the existing workflow or runtime evidence instead of forcing operators to reconstruct it from raw events.</action>
  <verify>
    <automated>mix test test/scoria_web/live/review_queue_live_test.exs test/scoria_web/live/orchestrator_live_test.exs test/scoria_web/live/workflow_live_test.exs</automated>
  </verify>
  <done>Operators can open a dedicated review queue page, inspect one candidate at a time with durable rationale and provenance, and jump into either the workflow page or `/scoria?runtime=...` runtime view with queue-selected evidence still visible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review queue page -> workflow page deep link | Candidate identity crosses between operator surfaces |
| review queue page -> runtime deep link | Queue-selected evidence context crosses into the dashboard landing surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-09 | Spoofing | queue/workflow deep links | mitigate | Resolve queue selection from persisted candidate ids and projected `workflow_run_id`/`trace_id`, not from arbitrary template params. |
| T-40-10 | Information Disclosure | queue-selected evidence strip rendering | mitigate | Render only the curated rationale/provenance fields from the projection boundary on workflow/runtime landing surfaces. |
</threat_model>

<verification>
Follow the LiveView/workflow smoke after the task lands. Manual readability review from `40-VALIDATION.md` still applies after execution.
</verification>

<success_criteria>
Scoria exposes a dedicated operator review queue backed by persisted candidates and score evidence, with workflow deep links and `/scoria?runtime=...` runtime deep links that preserve rationale and provenance context.
</success_criteria>

<output>
After completion, create `.planning/phases/40-online-scoring-review-queue/40-online-scoring-review-queue-05-SUMMARY.md`
</output>
