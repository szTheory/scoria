---
phase: 40-online-scoring-review-queue
plan: 6
type: execute
wave: 6
depends_on: ["40-05"]
files_modified:
  - lib/scoria/eval/review_queue.ex
  - lib/scoria/eval/dataset_promotion.ex
  - lib/scoria/eval.ex
  - lib/scoria_web/live/review_queue_live.ex
  - lib/scoria_web/live/dataset_live/promote_component.ex
  - test/scoria/eval_test.exs
  - test/scoria_web/live/review_queue_live_test.exs
  - test/scoria_web/live/dataset_live/promote_component_test.exs
autonomous: false
requirements: [SCOR-04]
nyquist_compliant: true
must_haves:
  truths:
    - "Promotion candidates stay reviewable until an operator explicitly promotes or dismisses them."
    - "Open-dataset promotion reuses frozen Phase 39 promotion seams instead of inventing a second dataset path."
  artifacts:
    - path: "lib/scoria/eval/review_queue.ex"
      provides: "Explicit review transitions and promotion-candidate state updates"
    - path: "lib/scoria_web/live/review_queue_live.ex"
      provides: "Detail-rail dismiss/promote actions and success feedback"
    - path: "lib/scoria_web/live/dataset_live/promote_component.ex"
      provides: "Shared or reused open-dataset promotion UI contract from Phase 39"
  key_links:
    - from: "lib/scoria_web/live/review_queue_live.ex"
      to: "lib/scoria/eval/dataset_promotion.ex"
      via: "open-dataset candidate promotion reuses frozen workflow-source attrs"
      pattern: "promote_workflow_source|promotion_snapshot"
---

<objective>
Finish the open-dataset half of the operator action loop before taking on sealed-baseline approval wiring.

Purpose: keep review-state transitions and draft promotion tight enough for a dedicated verification checkpoint, while preserving the Phase 39 promotion seam exactly.
Output: dismiss/promote queue actions, durable review-state transitions, and open-dataset draft promotion feedback on the review queue.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-online-scoring-review-queue/40-UI-SPEC.md
@.planning/phases/40-online-scoring-review-queue/40-VALIDATION.md
@.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-05-SUMMARY.md
@lib/scoria/eval/dataset_promotion.ex
@lib/scoria_web/live/dataset_live/promote_component.ex
@.planning/phases/40-online-scoring-review-queue/40-05-PLAN.md

<interfaces>
From `lib/scoria/eval/dataset_promotion.ex` / `lib/scoria/eval.ex`:
```elixir
def preview_workflow_source_promotion(attrs) when is_map(attrs)
def promote_workflow_source(attrs) when is_map(attrs)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit candidate review actions and draft promotion flow</name>
  <files>lib/scoria/eval/review_queue.ex, lib/scoria/eval/dataset_promotion.ex, lib/scoria/eval.ex, lib/scoria_web/live/review_queue_live.ex, lib/scoria_web/live/dataset_live/promote_component.ex, test/scoria/eval_test.exs, test/scoria_web/live/review_queue_live_test.exs, test/scoria_web/live/dataset_live/promote_component_test.exs</files>
  <behavior>
    - Test 1: dismissing a candidate removes it from the default queue ordering without deleting score/workflow evidence.
    - Test 2: promoting a candidate into an open dataset reuses a frozen workflow-source promotion payload and records the resulting dataset target on the candidate.
  </behavior>
  <action>Add explicit review-queue state transitions owned by the backend, not the LiveView: at minimum `dismissed`, `promotion_candidate`, and `promoted` (or equivalent explicit durable values). The queue detail rail should offer `Dismiss candidate` and `Promote candidate` actions that disable with `phx-disable-with` during submit per the UI spec. Reuse the Phase 39 promotion contract rather than duplicating JSON parsing or snapshot shaping: either embed the existing `PromoteComponent` flow where it fits cleanly or extract a shared helper that both surfaces use. Open-dataset promotion must call `Scoria.Eval.promote_workflow_source/1` with the candidate’s frozen workflow/score evidence, then keep the candidate visible long enough to show success feedback and the resulting dataset reference instead of disappearing immediately.</action>
  <verify>
    <automated>mix test test/scoria/eval_test.exs test/scoria_web/live/review_queue_live_test.exs test/scoria_web/live/dataset_live/promote_component_test.exs</automated>
  </verify>
  <done>Operators can dismiss or draft-promote review candidates explicitly, and those actions mutate only queue review state or open-dataset draft state while preserving the original score evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify draft-promotion flow before sealed-baseline wiring</name>
  <files>lib/scoria/eval/review_queue.ex, lib/scoria/eval/dataset_promotion.ex, lib/scoria/eval.ex, lib/scoria_web/live/review_queue_live.ex, lib/scoria_web/live/dataset_live/promote_component.ex, test/scoria/eval_test.exs, test/scoria_web/live/review_queue_live_test.exs, test/scoria_web/live/dataset_live/promote_component_test.exs</files>
  <action>Pause after the open-dataset draft-promotion and dismissal actions land. Confirm that queue detail-rail behavior, disabled-submit states, and success feedback are correct before taking on the sealed-baseline approval wiring in Plan 40-07.</action>
  <verify>
    <automated>mix test test/scoria/eval_test.exs test/scoria_web/live/review_queue_live_test.exs test/scoria_web/live/dataset_live/promote_component_test.exs</automated>
  </verify>
  <done>Human verification approves the draft-promotion queue flow or returns issues that must be fixed before sealed-baseline approval wiring proceeds.</done>
  <what-built>Draft-promotion review actions are wired on the queue detail rail, including dismiss and open-dataset promotion flows with success feedback.</what-built>
  <how-to-verify>
    1. Run `mix test test/scoria/eval_test.exs test/scoria_web/live/review_queue_live_test.exs test/scoria_web/live/dataset_live/promote_component_test.exs`.
    2. Open the review queue in the browser and select an open-dataset candidate.
    3. Confirm the detail rail shows `Dismiss candidate` and `Promote candidate`, both disable during submit, and success feedback keeps the candidate visible long enough to show the dataset reference.
    4. Confirm no sealed-baseline approval CTA is exercised in this checkpoint yet; that wiring belongs to Plan 40-07.
  </how-to-verify>
  <resume-signal>Type "approved" to continue with sealed-baseline approval wiring, or describe issues that should change Plan 40-07 first.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator review action -> dataset promotion service | Human queue actions can request durable draft dataset changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-11 | Tampering | candidate dismissal/promotion state | mitigate | Persist explicit review-state transitions on the candidate row and keep score evidence immutable. |
| T-40-12 | Repudiation | promotion lineage | mitigate | Carry workflow ids, source variant, scoring rationale, and promotion snapshot through candidate actions and promotion reads. |
</threat_model>

<verification>
Run the draft-promotion smoke lane after Task 1. Manual copy/readability checks from `40-VALIDATION.md` still apply during the checkpoint.
</verification>

<success_criteria>
Operators can take explicit open-dataset actions on scored candidates without blurring additive online evidence with draft dataset truth, and the queue flow is verified before sealed-baseline approval work starts.
</success_criteria>

<output>
After completion, create `.planning/phases/40-online-scoring-review-queue/40-online-scoring-review-queue-06-SUMMARY.md`
</output>
