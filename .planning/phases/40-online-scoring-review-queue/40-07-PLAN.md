---
phase: 40-online-scoring-review-queue
plan: 7
type: execute
wave: 7
depends_on: ["40-06"]
files_modified:
  - lib/scoria/eval/review_queue.ex
  - lib/scoria/workflows/dataset_promotion.ex
  - lib/scoria/workflows/remote_approval_projection.ex
  - lib/scoria_web/live/review_queue_live.ex
  - test/scoria/workflows/dataset_promotion_test.exs
  - test/scoria/workflows/remote_approval_projection_test.exs
  - test/scoria_web/live/review_queue_live_test.exs
autonomous: true
requirements: [SCOR-04]
nyquist_compliant: true
must_haves:
  truths:
    - "Sealed baseline targets still require workflow-owned approval and never auto-mutate from scoring completion."
    - "Review candidates stay visible with approval lineage after a sealed-baseline request is recorded."
  artifacts:
    - path: "lib/scoria/workflows/dataset_promotion.ex"
      provides: "Baseline-approval path fed from score-review candidates without bypassing workflow approvals"
    - path: "lib/scoria/workflows/remote_approval_projection.ex"
      provides: "Approval projection reads that keep review queue lineage visible"
    - path: "lib/scoria_web/live/review_queue_live.ex"
      provides: "Amber approval-required CTA and resulting approval feedback on the queue detail rail"
  key_links:
    - from: "lib/scoria_web/live/review_queue_live.ex"
      to: "lib/scoria/workflows/dataset_promotion.ex"
      via: "sealed baseline actions route through workflow-owned approval requests"
      pattern: "request_baseline_promotion"
    - from: "lib/scoria_web/live/review_queue_live.ex"
      to: "lib/scoria/workflows/remote_approval_projection.ex"
      via: "queue detail rail retains approval lineage after request creation"
      pattern: "approval|lineage"
---

<objective>
Finish the sealed-baseline half of the operator action loop through the workflow approval boundary.

Purpose: keep the final approval-gated slice narrow enough that sealed-baseline immutability and approval lineage can be verified independently of open-dataset draft promotion.
Output: queue-triggered baseline approval requests plus approval lineage surfaced back into the review queue.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-online-scoring-review-queue/40-UI-SPEC.md
@.planning/phases/40-online-scoring-review-queue/40-VALIDATION.md
@.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-05-SUMMARY.md
@lib/scoria/workflows/dataset_promotion.ex
@lib/scoria/workflows/remote_approval_projection.ex
@.planning/phases/40-online-scoring-review-queue/40-06-PLAN.md

<interfaces>
From `lib/scoria/workflows/dataset_promotion.ex`:
```elixir
def request_baseline_promotion(attrs) when is_map(attrs)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve approval-gated sealed baseline promotion from the review queue</name>
  <files>lib/scoria/eval/review_queue.ex, lib/scoria/workflows/dataset_promotion.ex, lib/scoria/workflows/remote_approval_projection.ex, lib/scoria_web/live/review_queue_live.ex, test/scoria/workflows/dataset_promotion_test.exs, test/scoria/workflows/remote_approval_projection_test.exs, test/scoria_web/live/review_queue_live_test.exs</files>
  <behavior>
    - Test 1: requesting sealed-baseline promotion from a review candidate creates a workflow-owned approval with candidate lineage and leaves the baseline untouched.
    - Test 2: the queue and approval projections expose the resulting approval and dataset refs so operators can continue review without losing context.
  </behavior>
  <action>Wire the queue’s sealed-baseline CTA to `Scoria.Workflows.request_baseline_promotion/1` using the candidate’s frozen promotion snapshot, replay provenance, and scoring rationale. Keep the exact `dataset_baseline_promotion` tool identity and reuse `RemoteApprovalProjection` patterns so later operator reads show baseline target details and lineage without extra template joins. Candidate state may move to an explicit `approval_requested` review value once the approval row persists, but scoring completion itself must never call this path automatically. Keep sealed baselines immutable throughout and ensure queue copy clearly distinguishes additive score evidence from any mutation of release truth.</action>
  <verify>
    <automated>mix test test/scoria/workflows/dataset_promotion_test.exs test/scoria/workflows/remote_approval_projection_test.exs test/scoria_web/live/review_queue_live_test.exs</automated>
  </verify>
  <done>Review candidates can request sealed-baseline approval only through the workflow boundary, the queue retains approval lineage/context, and no automated scoring path mutates sealed dataset truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator review action -> workflow approval boundary | Sealed-baseline requests cross into approval-owned truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-13 | Elevation of privilege | review queue baseline CTA | mitigate | Route sealed-baseline actions only through `Workflows.request_baseline_promotion/1`; scoring workers never call it. |
| T-40-14 | Repudiation | approval lineage from review candidates | mitigate | Carry workflow ids, source variant, scoring rationale, and promotion snapshot through approval creation and projection reads. |
</threat_model>

<verification>
Run the workflow-approval smoke lane after the task lands. After the full phase wave finishes, `mix test` remains the integrated gate.
</verification>

<success_criteria>
Operators can request sealed-baseline approval from the review queue without mutating sealed truth directly, and the queue preserves approval lineage after the request is recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/40-online-scoring-review-queue/40-online-scoring-review-queue-07-SUMMARY.md`
</output>
