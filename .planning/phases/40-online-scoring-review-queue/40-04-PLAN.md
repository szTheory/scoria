---
phase: 40-online-scoring-review-queue
plan: 4
type: execute
wave: 4
depends_on: ["40-03"]
files_modified:
  - lib/scoria/eval/review_queue.ex
  - lib/scoria/eval.ex
  - test/scoria/eval/review_queue_test.exs
autonomous: true
requirements: [SCOR-03]
nyquist_compliant: true
must_haves:
  truths:
    - "Operators can load a dedicated review queue backed by persisted candidate DTOs, not template-side joins."
    - "Queue rows surface score summary, trigger reason, and deep-link payloads for workflow and runtime context."
  artifacts:
    - path: "lib/scoria/eval/review_queue.ex"
      provides: "Projection API for filtered review rows, summary strip counts, candidate detail DTOs, and workflow/runtime deep-link payloads"
    - path: "test/scoria/eval/review_queue_test.exs"
      provides: "Projection-level proof for ordering, filtering, and deep-link DTO shape"
  key_links:
    - from: "lib/scoria/eval.ex"
      to: "lib/scoria/eval/review_queue.ex"
      via: "public list/detail/summary seams expose curated queue DTOs to LiveView"
      pattern: "list_review_queue|get_review_candidate|summarize_review_queue"
---

<objective>
Project scored candidates into a backend queue projection before any LiveView rendering work.

Purpose: give the UI a curated queue/data boundary so filters, detail rails, and deep links do not depend on page-local joins or inference.
Output: review queue projection API and tests for rows, filters, summary counts, and deep-link payloads.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-online-scoring-review-queue/40-RESEARCH.md
@.planning/phases/40-online-scoring-review-queue/40-VALIDATION.md
@.planning/phases/40-online-scoring-review-queue/40-PATTERNS.md
@lib/scoria/workflows/remote_approval_projection.ex
@.planning/phases/40-online-scoring-review-queue/40-03-PLAN.md

<interfaces>
From `lib/scoria/workflows/remote_approval_projection.ex`:
```elixir
def list_pending_approvals(filters \\ %{})
def get_approval_lineage!(approval_id)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a candidate-backed review queue projection</name>
  <files>lib/scoria/eval/review_queue.ex, lib/scoria/eval.ex, test/scoria/eval/review_queue_test.exs</files>
  <behavior>
    - Test 1: projection filters by review status, severity, scorer kind, and promotion state while returning stable ordering for `needs review`.
    - Test 2: projected rows expose score rationale, sampling provenance, `trace_id`, `workflow_run_id`, runtime deep-link params, and dataset-readiness details without requiring template-side joins.
  </behavior>
  <action>Create an `Scoria.Eval.ReviewQueue` projection module that mirrors the shape discipline of `RemoteApprovalProjection` but stays inside the eval boundary. It should expose list/detail/summary functions via `Scoria.Eval`, returning operator-ready maps for the review queue page: queue row summary, detail-rail evidence, workflow deep-link ids, runtime deep-link params for `/scoria?runtime=...`, and promotion-readiness facts. Persisted `OnlineScoreCandidate` rows remain the source of truth; join out only inside the projection module to load the related run/score/dataset facts needed for UI DTOs. Do not make LiveView query raw Ecto associations or derive review severity in templates.</action>
  <verify>
    <automated>mix test test/scoria/eval/review_queue_test.exs</automated>
  </verify>
  <done>The queue has a dedicated curated projection layer with filterable rows, summary counts, detail DTOs, and deep-link ids that can feed LiveView directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review queue projection -> LiveView | Curated backend DTOs feed operator-visible queue state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-07 | Information Disclosure | queue row projection | mitigate | Surface only curated rationale summaries, ids, and readiness facts in DTOs; keep raw payload detail behind the workflow/runtime evidence pages. |
| T-40-08 | Tampering | queue filter/query boundary | mitigate | Normalize filter params in the projection boundary and never query via template-built joins. |
</threat_model>

<verification>
Follow the projection smoke after the task lands. Manual readability review still applies later once the LiveView exists.
</verification>

<success_criteria>
Scoria has a dedicated operator queue projection backed by persisted candidates and score evidence, with stable filters, summary counts, and deep-link payloads ready for UI consumption.
</success_criteria>

<output>
After completion, create `.planning/phases/40-online-scoring-review-queue/40-online-scoring-review-queue-04-SUMMARY.md`
</output>
