---
phase: 40-online-scoring-review-queue
plan: 2
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - lib/scoria/eval/online_score_sampler.ex
  - lib/scoria/eval/online_scoring.ex
  - lib/scoria/eval.ex
  - lib/scoria/eval/campaign_enqueuer.ex
  - test/scoria/eval/online_score_sampler_test.exs
  - test/scoria/eval/campaign_enqueue_test.exs
autonomous: true
requirements: [SCOR-01]
nyquist_compliant: true
must_haves:
  truths:
    - "Eligibility selection happens in an explicit off-path sampler boundary with persisted sampling provenance."
    - "Sampled production traces become durable scoring candidates before any worker runs."
    - "Online scoring is enqueued asynchronously through Oban and never runs on the request path."
  artifacts:
    - path: "lib/scoria/eval/online_score_sampler.ex"
      provides: "Async eligibility sampler and trigger seam for production traces"
    - path: "lib/scoria/eval/online_scoring.ex"
      provides: "Sampler/coordinator that persists candidates and enqueues scoring work"
    - path: "test/scoria/eval/online_score_sampler_test.exs"
      provides: "Proof that the sampler runs off-path and records provenance"
  key_links:
    - from: "lib/scoria/eval/online_score_sampler.ex"
      to: "lib/scoria/eval/online_scoring.ex"
      via: "eligible persisted traces are sampled off-path and handed to candidate persistence"
      pattern: "sample_|enqueue_|candidate"
    - from: "lib/scoria/eval/online_scoring.ex"
      to: "lib/scoria/eval/campaign_enqueuer.ex"
      via: "persist candidate/campaign lineage before enqueueing worker jobs"
      pattern: "CampaignEnqueuer|create_and_enqueue_campaign"
---

<objective>
Define the async sampling and enqueue boundary on top of the durable storage contract.

Purpose: make production-trace eligibility and queue insertion explicit, durable, and off the request path before scorer execution work begins.
Output: async sampler trigger seam plus a coordinator that persists candidates and reuses the existing eval enqueue path.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-online-scoring-review-queue/40-RESEARCH.md
@.planning/phases/40-online-scoring-review-queue/40-VALIDATION.md
@.planning/phases/40-online-scoring-review-queue/40-PATTERNS.md
@lib/scoria/eval.ex
@lib/scoria/eval/campaign_enqueuer.ex
@.planning/phases/40-online-scoring-review-queue/40-01-PLAN.md

<interfaces>
From `lib/scoria/eval.ex`:
```elixir
def create_and_enqueue_campaign(attrs, opts \\ []) when is_map(attrs)
```

From `lib/scoria/eval/campaign_enqueuer.ex`:
```elixir
def enqueue_campaign(attrs, opts \\ []) when is_map(attrs)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the async eligibility sampler and trigger seam</name>
  <files>lib/scoria/eval/online_score_sampler.ex, lib/scoria/eval/online_scoring.ex, lib/scoria/eval.ex, test/scoria/eval/online_score_sampler_test.exs</files>
  <behavior>
    - Test 1: only eligible persisted production traces are selected by the sampler, and the sampler runs asynchronously after trace/workflow durability instead of on the request path.
    - Test 2: the sampler records sampler version, sample reason, and dedupe identity for each selected trace before handing off to the scoring coordinator.
  </behavior>
  <action>Add an explicit off-path sampler boundary, preferably `Scoria.Eval.OnlineScoreSampler`, that owns trace eligibility selection for `SCOR-01` instead of assuming callers hand in already-sampled facts. The trigger must stay after production trace/workflow persistence has committed, via an async seam such as an Oban job insert or equivalent deferred enqueue call from the runtime ingestion boundary. Define the eligibility contract narrowly and durably: production traces only, must have `trace_id`, tenant identity, and workflow lineage when present; include rule-based inclusion/exclusion inputs there rather than in LiveView. The sampler must emit a normalized payload containing sampler version, sample reason, sample window/dedupe key, and trace/workflow references for the scoring coordinator, and it must not execute scoring inline.</action>
  <verify>
    <automated>mix test test/scoria/eval/online_score_sampler_test.exs</automated>
  </verify>
  <done>Phase 40 has an explicit async sampler that chooses eligible persisted production traces, records sampling provenance inputs, and hands normalized payloads to the scoring coordinator without touching request latency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist sampled candidates and enqueue scoring work off-path</name>
  <files>lib/scoria/eval/online_scoring.ex, lib/scoria/eval.ex, lib/scoria/eval/campaign_enqueuer.ex, test/scoria/eval/campaign_enqueue_test.exs, test/scoria/eval/online_score_sampler_test.exs</files>
  <behavior>
    - Test 1: a sampler-selected production trace persists candidate lineage and enqueues eval work through Oban without executing scoring inline.
    - Test 2: repeated sampling for the same dedupe key reuses or rejects the existing active candidate instead of double-enqueueing jobs.
  </behavior>
  <action>Add a dedicated `Scoria.Eval.OnlineScoring` coordinator that accepts sampler output, normalizes it into one `OnlineScoreCandidate` row, and then reuses the existing campaign/enqueue path to create the child campaign/target/eval-run lineage. Persist first with `Ecto.Multi`, enqueue second, matching the existing `CampaignEnqueuer` pattern from Phase 33. Expose the seam from `Scoria.Eval` with an explicit public function such as `sample_trace_for_online_scoring/2` or equivalently named API, but require the sampler payload to include the persisted provenance fields from Task 1. Candidate metadata must record sampler version, sample reason, sample window/dedupe key, trace/workflow ids, scorer configuration snapshot, and promotion-relevant frozen evidence refs. Keep this additive only: no dataset promotion calls, no baseline approval calls, and no request-path judge work.</action>
  <verify>
    <automated>mix test test/scoria/eval/campaign_enqueue_test.exs test/scoria/eval/online_score_sampler_test.exs</automated>
  </verify>
  <done>Sampling a production trace produces durable candidate/campaign/run truth and one Oban-backed scoring job fan-out, with idempotent enough dedupe behavior to prevent duplicate queue entries for the same sampled trace window.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime trace sampler -> eval persistence | Untrusted production-trace metadata becomes durable queue/scoring input |
| eval coordinator -> Oban worker envelope | Persisted lineage is handed to asynchronous execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-03 | Denial of service | sampler trigger and candidate sampling/enqueue path | mitigate | Run eligibility selection asynchronously after trace/workflow commit, enforce dedupe on active candidate identity, and reuse the existing Oban enqueue path instead of inline scoring. |
| T-40-04 | Tampering | `Scoria.Eval.OnlineScoring` candidate creation | mitigate | Persist candidate lineage, dedupe key, and sampler metadata in one transaction before enqueue so callers cannot create ambiguous queue state. |
</threat_model>

<verification>
Run the sampler smoke after Task 1 and the enqueue smoke after Task 2. Wave validation still requires `mix test`.
</verification>

<success_criteria>
Production-trace sampling and queue insertion are explicit, durable, and asynchronous: eligible traces are sampled off-path, persisted as candidates, and enqueued through Oban without scoring inline.
</success_criteria>

<output>
After completion, create `.planning/phases/40-online-scoring-review-queue/40-online-scoring-review-queue-02-SUMMARY.md`
</output>
