---
phase: 40-online-scoring-review-queue
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/20260523000300_expand_ai_scores_and_create_online_score_candidates.exs
  - lib/scoria/eval/score.ex
  - lib/scoria/eval/online_score_candidate.ex
  - lib/scoria/eval.ex
  - test/scoria/eval/campaign_worker_test.exs
autonomous: true
requirements: [SCOR-01, SCOR-02]
nyquist_compliant: true
must_haves:
  truths:
    - "Each persisted score can store the provenance and rationale fields Phase 40 needs downstream."
    - "Sampled production traces have a durable candidate row shape before any sampler or worker flow is added."
  artifacts:
    - path: "priv/repo/migrations/20260523000300_expand_ai_scores_and_create_online_score_candidates.exs"
      provides: "Score-evidence columns plus durable online scoring candidate storage"
    - path: "lib/scoria/eval/score.ex"
      provides: "Expanded score schema and changeset for additive online-scoring evidence"
    - path: "lib/scoria/eval/online_score_candidate.ex"
      provides: "Candidate schema and explicit review-state contract for reviewable scored traces"
  key_links:
    - from: "lib/scoria/eval.ex"
      to: "lib/scoria/eval/score.ex"
      via: "record_eval_scores/replace_eval_scores accept richer score attrs"
      pattern: "persist_eval_scores|Score.changeset"
    - from: "lib/scoria/eval/online_score_candidate.ex"
      to: "priv/repo/migrations/20260523000300_expand_ai_scores_and_create_online_score_candidates.exs"
      via: "candidate schema matches durable queue/review columns and dedupe indexes"
      pattern: "ai_online_score_candidates|dedupe"
---

<objective>
Lock the durable storage contract for Phase 40 before any sampler, queue, or UI work starts.

Purpose: resolve the known score-schema mismatch and define the candidate row shape every later slice depends on.
Output: richer score persistence plus a durable online-scoring candidate table/schema.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-online-scoring-review-queue/40-RESEARCH.md
@.planning/phases/40-online-scoring-review-queue/40-VALIDATION.md
@.planning/phases/40-online-scoring-review-queue/40-PATTERNS.md
@lib/scoria/eval.ex
@lib/scoria/eval/score.ex

<interfaces>
From `lib/scoria/eval.ex`:
```elixir
def record_eval_scores(%EvalRun{} = eval_run, score_attrs_list) when is_list(score_attrs_list)
def replace_eval_scores(%EvalRun{} = eval_run, score_attrs_list) when is_list(score_attrs_list)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile score evidence storage and candidate durability</name>
  <files>priv/repo/migrations/20260523000300_expand_ai_scores_and_create_online_score_candidates.exs, lib/scoria/eval/score.ex, lib/scoria/eval/online_score_candidate.ex, lib/scoria/eval.ex, test/scoria/eval/campaign_worker_test.exs</files>
  <behavior>
    - Test 1: score persistence accepts scorer metadata required by `SCOR-02` without dropping judge/model/provenance fields.
    - Test 2: online score candidates persist one durable row per sampled trace/dedupe key with review-state defaults and workflow/trace lineage.
  </behavior>
  <action>Create the next migration to do two things together: expand `ai_scores` away from the legacy `reasoning/details`-only shape, and add an `ai_online_score_candidates` table keyed for durable review work. The score contract must carry additive evidence fields used elsewhere already: `status`, `scorer_kind`, `scorer_version`, `explanation`, `judge_model`, `rubric_version`, `evidence_refs`, `metadata`, plus any compatibility rename needed so current callers/tests stop depending on a mismatched schema. Add `Scoria.Eval.OnlineScoreCandidate` with explicit candidate/review statuses, tenant identity, `trace_id`, `workflow_run_id`, `workflow_step_id`, campaign/eval-run refs, sampling metadata, score summary fields, frozen promotion snapshot fields, and a dedupe key/index so the same sampled trace window does not generate duplicate active candidates. Update `Scoria.Eval` score persistence helpers only as needed to accept the richer attrs; do not introduce a second score store or move request-path work into the worker layer.</action>
  <verify>
    <automated>mix test test/scoria/eval/campaign_worker_test.exs</automated>
  </verify>
  <done>`Score.changeset/2` and the underlying table accept the richer evidence shape Phase 40 requires, and `OnlineScoreCandidate` exists as durable queue truth with explicit lineage and dedupe fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| score persistence caller -> `Scoria.Eval.Score` | Untrusted scorer payload becomes durable operator-visible evidence |
| candidate creator -> `ai_online_score_candidates` | Sampled trace metadata becomes durable queue truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | `Scoria.Eval.Score` persistence | mitigate | Validate and persist scorer kind/version, explanation, evidence refs, and metadata at the score row boundary instead of relying on transient worker state. |
| T-40-02 | Repudiation | `Scoria.Eval.OnlineScoreCandidate` | mitigate | Persist candidate lineage, review-state defaults, and dedupe identity so later queue actions can prove which sampled trace created each review row. |
</threat_model>

<verification>
Run the worker smoke lane after the migration/schema task lands. Phase-level validation still requires `mix test`.
</verification>

<success_criteria>
Phase 40 has a durable storage substrate: richer score evidence persists successfully, and sampled traces have an explicit candidate row contract before async sampling or operator queue work begins.
</success_criteria>

<output>
After completion, create `.planning/phases/40-online-scoring-review-queue/40-online-scoring-review-queue-01-SUMMARY.md`
</output>
