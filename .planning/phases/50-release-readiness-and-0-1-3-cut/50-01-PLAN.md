---
phase: 50-release-readiness-and-0-1-3-cut
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/scoria/ci_policy_contract_test.exs
  - guides/maintainers.md
autonomous: true
requirements: [REL-01]
must_haves:
  truths:
    - "MIX_ENV=test mix test --no-start --warnings-as-errors test/scoria/ci_policy_contract_test.exs exits 0 (7 previously-failing tests now pass, none regressed)"
    - "The v2.15 Connector Adoption Lane roadmap breadcrumb assertion still passes (Success Criterion 1) — it is verified present, never removed"
    - "Docs-contract tests read canonical guides/ source, not docs/*.md compatibility stubs"
  artifacts:
    - test/scoria/ci_policy_contract_test.exs
    - guides/maintainers.md
  key_links:
    - "@maintainer_docs / @operator_docs constants → guides/maintainers.md (canonical), per D-16"
    - "guides/maintainers.md restored maintainer content ↔ literal string assertions in ci_policy_contract_test.exs"
---

<objective>
Repair the REL-01 policy-lane failure. Per the RESEARCH.md correction, REL-01 is NOT a missing breadcrumb (the `v2.15 Connector Adoption Lane` roadmap-breadcrumb test already passes). The 7 real `CiPolicyContractTest` failures come from `@maintainer_docs`/`@operator_docs` constants still reading Phase-48 compatibility stubs (`docs/MAINTAINERS.md`, `docs/operator_verification.md`) as if they held canonical maintainer content that Phase 48 moved into `guides/maintainers.md` and partly dropped.

Per D-05/D-06/D-16: repoint the constants to the canonical `guides/maintainers.md` path AND restore the genuinely-dropped maintainer content into `guides/maintainers.md` in the guide's current voice. Do NOT loosen the contract. Do NOT copy full guide content back into `docs/` stubs.

Purpose: Turn the `verify / policy` CI lane green so PR #12 can reach `ci-gate` (REL-01, blocks REL-04).
Output: Repointed docs-contract constants + restored canonical maintainer content; focused policy lane green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-release-readiness-and-0-1-3-cut/50-RESEARCH.md
@.planning/phases/50-release-readiness-and-0-1-3-cut/50-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint docs-contract constants and fix stale README link assertions</name>
  <files>test/scoria/ci_policy_contract_test.exs</files>
  <read_first>
    - test/scoria/ci_policy_contract_test.exs lines 11-12 (constants), 91-97, 325-331, 494-495, 502-524, 600-645 (the assertions that read @maintainer_docs / @operator_docs and README)
    - .planning/phases/50-release-readiness-and-0-1-3-cut/50-PATTERNS.md ("ci_policy_contract_test.exs" section — analog is the file's own passing v2.15 breadcrumb test near line 692)
    - RESEARCH.md Pitfall 1 and Code Example 3
  </read_first>
  <action>
    Per D-16, edit the two module attribute constants at lines 11-12 so both point at the single canonical guide:
    set `@maintainer_docs` to `"guides/maintainers.md"` and `@operator_docs` to `"guides/maintainers.md"`.
    Do NOT split `@operator_docs` to `guides/reviewer-verification.md` — the WARN-06 ratchet commands and the Hex-release section are maintainer-only content that Phase 48 consolidated into `guides/maintainers.md` (RESEARCH.md Assumption A2; verify against that guide's own intro paragraph which states maintainer-only commands stay out of adopter/reviewer docs).
    Fix the two stale README link assertions (lines 511 and 642): change `assert readme =~ "docs/MAINTAINERS.md"` to `assert readme =~ "guides/maintainers.md"` (README already links the guide path per Phase 48 — confirmed by repo grep, README is NOT modified here).
    Do NOT weaken, delete, skip, or relax any assertion; only repoint the two path constants and the two README-link literals. Every content `assert ... =~ "literal"` stays byte-identical — those literals are restored in the guide by Task 2, not removed from the test.
    Do NOT touch the `v2.15 Connector Adoption Lane` breadcrumb test (~line 692) — it already passes and must stay passing (Success Criterion 1).
  </action>
  <verify>
    <automated>grep -c 'docs/MAINTAINERS.md\|docs/operator_verification.md' test/scoria/ci_policy_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '@maintainer_docs\|@operator_docs' test/scoria/ci_policy_contract_test.exs` shows both constants set to `"guides/maintainers.md"`.
    - `grep -c 'docs/MAINTAINERS.md\|docs/operator_verification.md' test/scoria/ci_policy_contract_test.exs` returns `0`.
    - The v2.15 breadcrumb test still exists unchanged (`grep -c 'v2.15 Connector Adoption Lane' test/scoria/ci_policy_contract_test.exs` > 0).
  </acceptance_criteria>
  <done>Both docs-contract constants and both README-link assertions point at `guides/maintainers.md`; no assertion weakened; breadcrumb test untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Restore genuinely-dropped canonical maintainer content into guides/maintainers.md</name>
  <files>guides/maintainers.md</files>
  <read_first>
    - guides/maintainers.md in full (canonical current source; note existing headings `## CI gate map` line 7, `## Hex release and recovery` line 109, and the section voice/format of `## Warning baseline and inventory` line 133 and `## Installer contract proofs` line 86)
    - test/scoria/ci_policy_contract_test.exs lines 91-97, 325-331, 494-495, 502-524, 600-636 (every literal that a test reading @maintainer_docs / @operator_docs now requires in this guide)
    - RESEARCH.md Pitfall 1, Code Example 3, Assumption A1/A2, Open Question 2
    - .planning/phases/50-release-readiness-and-0-1-3-cut/50-PATTERNS.md ("guides/maintainers.md" section)
    - Drafting reference only (adapt, do not paste verbatim): `git show c40bc630:docs/MAINTAINERS.md`
  </read_first>
  <action>
    Because Task 1 repointed both constants to `guides/maintainers.md`, this guide must now satisfy the UNION of every positive literal asserted by any test reading @maintainer_docs or @operator_docs — restore each missing one in the guide's current numbered-list/fenced-command voice (do NOT paste pre-Phase-48 content verbatim; adapt it). Run the focused policy lane iteratively and restore until green. The required positive literals include, keyed to their owning heading:
    Under/after `## Hex release and recovery` (line 109): change the heading text to `## Hex release & recovery` (ampersand form the contract asserts) and add an explicit anchor carrying the literal `hex-release--recovery-maintainers`; ensure the section mentions `RELEASE_PLEASE_TOKEN`, `HEX_API_KEY`, `hex-publish.yml`, `release-please--` (the release-branch prefix), `Normal patch release (fully automated)`, and `no manual merge`; also ensure `mix scoria.warning_ratchet.test` and `mix scoria.warning_ratchet.check` appear somewhere in the guide (WARN-06).
    Under `## CI gate map` (line 7): ensure `policy`, a `needs: policy` (or backtick-quoted `` `policy` ``) reference, `mix test.semantic_fast_path`, `mix test.connector`, `mix scoria.warning_inventory.check_baseline`, `mix scoria.test.install_contract`, `post-publish-smoke.yml`, `v2.x`, a `Version namespaces` subsection, plus the gate-map topology literals `Policy job`, `Parallel verify jobs`, `ratchet`, `knowledge`, `connector`, `full-suite`, `verify-summary`, `Local parity`, `Ratchet is maintainer-only`, `When CI fails, run the matching maintainer command next`, and `mix compile --warnings-as-errors`.
    Add a `**PR vs release proof depth**` subsection under `## CI gate map` containing `mix test.adoption`, `content-revision upgrade`, `mix hex.build` (or `Tarball consumer full overlay`), `mix scoria.post_publish_smoke`, `publish-hex`, `scoria-0.1.0-unpack`, `HEAD tarball`, `baseline exact previous`, and `target just-published`.
    Preserve the required NEGATIVE constraints while restoring: the guide must NOT contain the phrase the automerge test refutes about manually merging the release PR, must NOT contain the `Test job closeout` phrase, and the gate-map must NOT reintroduce the older per-job compile-diagnostic bullet the topology test refutes (use the generic "When CI fails..." phrasing instead). When adapting pre-Phase-48 content, drop those refuted phrases.
    Keep the content real maintainer guidance (D-17 boring/copy-pasteable, D-19 operator-grade, D-20 current vocabulary), not padding to satisfy a test. Do NOT edit any `docs/*.md` stub. Do NOT edit generated `doc/` output (D-15).
  </action>
  <verify>
    <automated>MIX_ENV=test mix test --no-start --warnings-as-errors test/scoria/ci_policy_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `MIX_ENV=test mix test --no-start --warnings-as-errors test/scoria/ci_policy_contract_test.exs` exits 0 with 0 failures (all 7 previously-failing tests pass; none regressed).
    - `grep -c '## Hex release & recovery' guides/maintainers.md` returns 1 and `grep -c 'hex-release--recovery-maintainers' guides/maintainers.md` > 0.
    - `grep -c 'Version namespaces' guides/maintainers.md` > 0 and `grep -c 'PR vs release proof depth' guides/maintainers.md` > 0.
    - `guides/maintainers.md` does not contain the `Test job closeout` phrase.
  </acceptance_criteria>
  <done>Focused `ci_policy_contract_test.exs` lane green; canonical maintainer content restored in the guide's current voice; no stub or generated-doc edits; negative-constraint phrases absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs/tests → CI `policy` job | Static-file assertions only; no runtime, no untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-01-01 | Tampering | Weakening `CiPolicyContractTest` to force green | low | mitigate | Plan forbids loosening assertions (D-05/D-16); fix is repoint-path + restore-content only; acceptance criteria assert no `docs/*` paths remain and content literals are present, not that assertions were removed |
| T-50-01-02 | Repudiation | Silent loss of the v2.15 breadcrumb proof | low | mitigate | Breadcrumb test is verified untouched (Success Criterion 1); grep acceptance criterion confirms it still exists |

No net-new high-severity threats. This plan edits static docs/tests only; no runtime, auth, crypto, or tenant surface is touched.
</threat_model>

<verification>
Focused policy lane (VALIDATION.md quick command):
`MIX_ENV=test mix test --no-start --warnings-as-errors test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs test/scoria/adoption_surface_test.exs`
must be green (the wider lane confirms the `& recovery` heading change did not regress any currently-passing adopter/adoption assertion).
</verification>

<success_criteria>
- All `CiPolicyContractTest` tests pass; `v2.15` breadcrumb assertion still passes (ROADMAP Success Criterion 1).
- Docs-contract constants read canonical `guides/maintainers.md`; no `docs/*.md` canonical references remain in the test.
- No assertion weakened; no compatibility stub or generated `doc/` output edited.
</success_criteria>

## Artifacts this phase produces (plan 01)

Since this plan edits existing files, the changed contracts are listed explicitly so drift verification excludes them:
- `test/scoria/ci_policy_contract_test.exs`: `@maintainer_docs` and `@operator_docs` constants repointed from `docs/MAINTAINERS.md` / `docs/operator_verification.md` to `guides/maintainers.md`; two README-link assertions changed from `docs/MAINTAINERS.md` to `guides/maintainers.md`.
- `guides/maintainers.md`: heading `## Hex release and recovery` → `## Hex release & recovery` with explicit anchor `hex-release--recovery-maintainers`; restored `Version namespaces` subsection, `**PR vs release proof depth**` subsection, and `RELEASE_PLEASE_TOKEN` documentation; gate-map topology literals confirmed present.

<output>
Create `.planning/phases/50-release-readiness-and-0-1-3-cut/50-01-SUMMARY.md` when done
</output>
