---
phase: 50-release-readiness-and-0-1-3-cut
plan: 04
type: execute
wave: 2
depends_on: ["50-01", "50-02", "50-03"]
files_modified:
  - .planning/STATE.md
autonomous: false
requirements: [REL-04]
user_setup:
  - service: github-actions
    why: "Release Please + hex-publish + post-publish-smoke workflows run in CI; a maintainer must merge PR #12 and authorize publish"
    env_vars:
      - name: HEX_API_KEY
        source: "GitHub Actions secret (already configured; used by hex-publish.yml)"
      - name: RELEASE_PLEASE_TOKEN
        source: "GitHub Actions secret (already configured; used by release-please.yml)"
must_haves:
  truths:
    - "mix scoria.release_preview and mix docs --warnings-as-errors build clean on the tip of main after REL-01/02/03 land"
    - "CI / ci-gate is green on PR #12's latest head (both policy and e2e lanes pass)"
    - "Hex lists 0.1.3 (curl https://hex.pm/api/packages/scoria/releases/0.1.3 returns the release)"
    - "SCORIA_REGISTRY_VERSION=0.1.3 mix scoria.post_publish_smoke proves fresh install AND live-lineage upgrade"
    - "The publish workflow/run URL or ID is captured in closeout state per D-04"
  artifacts:
    - .planning/STATE.md
  key_links:
    - "PR #12 -> CI / ci-gate (required aggregate gate) -> release-please merge/tag -> publish-hex -> post-publish-attest"
    - "post-publish smoke run URL/ID -> STATE.md closeout evidence (D-04)"
---

<objective>
Drive REL-04 to done. Per RESEARCH.md, REL-04 is a CONSEQUENCE of REL-01/REL-02 going green plus the existing publish/proof pipeline — NOT a new build. PR #12 is OPEN + MERGEABLE; the release/publish/smoke workflow topology already matches D-01..D-04. No workflow-topology change is needed (D-03).

The flow: confirm release preview/docs build clean and `ci-gate` is green on PR #12 (autonomous, read-only), then hand off to a maintainer to merge PR #12 through the normal Release Please path, let Release Please tag `v0.1.3`, publish through the existing `hex-publish`/`publish-hex` workflow, and finally run post-publish smoke and capture the run URL/ID in closeout state.

Per D-01 (normal Release Please path), D-02 (manual `hex-publish.yml` is emergency recovery only, gated by its own `gate-ci-green`), D-03 (keep `ci-gate` required; do not bypass), D-04 (unproven until Hex lists `0.1.3` and post-publish smoke passes both fresh install and upgrade; capture run URL/ID), D-17 (boring, copy-pasteable maintainer path).

Purpose: Hex publishes `0.1.3` with inspectable post-publish proof (ROADMAP Success Criteria 4 and 5).
Output: Green `ci-gate` on PR #12; `0.1.3` live on Hex; post-publish smoke green; run URL/ID recorded in STATE.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-release-readiness-and-0-1-3-cut/50-RESEARCH.md
@.planning/phases/50-release-readiness-and-0-1-3-cut/50-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm release preview, docs build, and ci-gate status are green (autonomous, read-only)</name>
  <files>(no source files — verification only)</files>
  <read_first>
    - lib/mix/tasks/scoria.release_preview.ex (cleans doc/, runs mix docs --warnings-as-errors, builds package preview, checks required package paths)
    - .github/workflows/ci.yml and ci-verify.yml (the policy + e2e lanes that roll up into ci-gate)
    - RESEARCH.md REL-04 row + Validation Architecture "Phase gate"; 50-VALIDATION.md Manual-Only Verifications row
  </read_first>
  <action>
    On the tip of main (after 50-01/50-02/50-03 have merged), run `MIX_ENV=dev mix scoria.release_preview` and `MIX_ENV=dev mix docs --warnings-as-errors` and confirm both exit clean (do NOT edit generated doc/ output — the preview cleans and regenerates it; D-15).
    Re-verify the research inputs are still current before handing off (research valid-until is 7 days and this is a release-in-flight): run `gh pr view 12 --json state,mergeable` (expect OPEN + MERGEABLE) and `gh pr checks 12` and record which checks pass/fail. If policy or e2e are still red, STOP and route back — REL-01/REL-02 fixes have not fully landed on the PR head yet (per D-01, Release Please refreshes PR #12 from main; the PR head must carry the fixes). Do NOT change branch-protection topology to force green (D-03).
    Do NOT merge, tag, or publish in this task — that is the maintainer checkpoint in Task 2.
  </action>
  <verify>
    <automated>MIX_ENV=dev mix scoria.release_preview</automated>
  </verify>
  <acceptance_criteria>
    - `MIX_ENV=dev mix scoria.release_preview` exits 0 and `MIX_ENV=dev mix docs --warnings-as-errors` exits 0.
    - `gh pr view 12 --json state,mergeable` shows OPEN + MERGEABLE.
    - `gh pr checks 12` shows the `policy` and `e2e` lanes passing (or the task halts and routes back if not).
  </acceptance_criteria>
  <done>Release preview + docs build clean; PR #12 OPEN/MERGEABLE; policy and e2e CI lanes green (or explicitly routed back if not).</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Maintainer merges PR #12 through Release Please and publishes 0.1.3 to Hex</name>
  <action>Maintainer-only release actions the executor cannot perform (merge authority + Hex publish credentials): confirm ci-gate green on PR #12, let the normal Release Please path merge/tag/publish (manual hex-publish.yml only as gate-ci-green-gated emergency recovery per D-02), and confirm Hex lists 0.1.3. See how-to-verify for exact commands and expected evidence.</action>
  <what-built>REL-01/02/03 fixes are on main; release preview + docs build clean; PR #12 policy and e2e lanes are green. Everything the executor can automate is done.</what-built>
  <how-to-verify>
    A maintainer must perform the release actions the executor cannot (merge authority, Hex publish credentials):
    1. Confirm `CI / ci-gate` (the required aggregate gate) is green on PR #12's latest head: `gh pr checks 12` — all rollup checks success. Do NOT proceed if `ci-gate` is not green (D-03).
    2. Let the normal Release Please path run (D-01): on `ci-gate` success on the `release-please--` branch, `release-pr-automerge.yml` merges PR #12; Release Please then tags `v0.1.3` and the `publish-hex` job publishes via `mix hex.publish --yes` (idempotent — skips if the version is already listed).
    3. Only if the automated path stalls (tag exists but Hex lacks `0.1.3`) use the manual `hex-publish.yml` emergency-recovery path, which is itself gated by `gate-ci-green` (D-02). Do NOT publish manually before a green release gate.
    4. Confirm Hex lists the release: `curl -s https://hex.pm/api/packages/scoria/releases/0.1.3` returns the `0.1.3` release JSON.
    Expected evidence: the release-please/publish-hex workflow run URL or ID, and a non-error Hex API response for `0.1.3`.
  </how-to-verify>
  <resume-signal>Reply with the publish workflow run URL/ID and confirmation that `curl https://hex.pm/api/packages/scoria/releases/0.1.3` returns the release, or describe where the pipeline stalled.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run post-publish smoke and capture release proof in closeout state</name>
  <action>Run SCORIA_REGISTRY_VERSION=0.1.3 mix scoria.post_publish_smoke against the live registry, confirm fresh install + live-lineage upgrade proof, and record the publish and post-publish-smoke run URL/ID in .planning/STATE.md closeout evidence (D-04). See how-to-verify for exact steps.</action>
  <what-built>Hex lists 0.1.3 (confirmed in the prior checkpoint). Post-publish smoke can now run against the live registry.</what-built>
  <how-to-verify>
    1. Run `SCORIA_REGISTRY_VERSION=0.1.3 mix scoria.post_publish_smoke` and confirm it passes BOTH proofs: fresh install of `0.1.3` and live-lineage upgrade (the task verifies upgrade for versions newer than 0.1.0; expect the `0.1.0 -> 0.1.3` lineage proof, mirroring the Phase 35 `0.1.0 -> 0.1.2` closeout).
    2. Capture the post-publish workflow/run URL or ID and the smoke result. Per D-04, release completion is unproven until this passes.
    3. Record the publish run URL/ID and the post-publish smoke run URL/ID in `.planning/STATE.md` closeout evidence (Session Continuity / Performance Metrics), so the release proof is inspectable (D-04, D-17).
  </how-to-verify>
  <resume-signal>Type "approved" with the post-publish smoke result and the captured run URL/ID, or describe the failure.</resume-signal>
</task>

</tasks>

<verification>
Phase-gate proof (D-04):
- `gh pr checks 12` shows `CI / ci-gate` green on PR #12's latest head.
- `curl -s https://hex.pm/api/packages/scoria/releases/0.1.3` returns the release.
- `SCORIA_REGISTRY_VERSION=0.1.3 mix scoria.post_publish_smoke` passes (fresh install + live-lineage upgrade).
- Publish + post-publish-smoke run URL/ID captured in STATE.md.
</verification>

<success_criteria>
- The release PR reaches green `ci-gate` and merges through the intended Release Please path (or a documented maintainer recovery path) — ROADMAP Success Criterion 4.
- Hex lists `0.1.3`; post-publish smoke proves fresh install plus live-lineage upgrade — ROADMAP Success Criterion 5.
- Run URL/ID captured in closeout state (D-04); no workflow-topology change; no manual publish before a green gate (D-02/D-03).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer -> GitHub Actions -> Hex registry (external) | Merge/publish authority crosses into an external package registry |
| published tarball -> throwaway consumer overlay (post-publish smoke) | Fresh-install + upgrade proof against the live registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-04-01 | Tampering / Elevation of Privilege | Manual Hex publish before CI is green (bypassing the release gate) | high | mitigate | Already mitigated by `hex-publish.yml`'s own `gate-ci-green` job (unchanged) and D-02/D-03 (manual publish is emergency-recovery only, never before a green gate); this plan adds no new control and forbids bypass |
| T-50-04-02 | Tampering | Re-publishing an already-listed Hex version | medium | mitigate | Already mitigated by the existing "Skip if version already on Hex" idempotency step in both `hex-publish.yml` and release-please's `publish-hex` job (unchanged) |
| T-50-04-03 | Repudiation | Release claimed done without inspectable proof | low | mitigate | D-04 requires capturing the publish + post-publish-smoke run URL/ID in STATE.md; the human-verify checkpoint gates on that evidence |
| T-50-04-SC | Tampering | npm/hex installs during smoke | low | accept | No new packages installed this phase (RESEARCH.md Package Legitimacy Audit: not applicable); smoke installs the just-published Scoria tarball from Hex via the existing task |

The one high-severity threat (T-50-04-01) is already mitigated by an existing, unchanged control — no net-new high-severity threat is introduced by this phase.
</threat_model>

## Artifacts this phase produces (plan 04)

- No source-file contract changes. This plan gates on existing CI/release/publish infrastructure and records release proof.
- `.planning/STATE.md`: closeout evidence updated with the publish workflow run URL/ID and the post-publish smoke run URL/ID (D-04).

<output>
Create `.planning/phases/50-release-readiness-and-0-1-3-cut/50-04-SUMMARY.md` when done
</output>
