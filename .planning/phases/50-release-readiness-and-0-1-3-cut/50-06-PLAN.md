---
phase: 50-release-readiness-and-0-1-3-cut
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - test/scoria/phoenix_example_source_test.exs
  - test/scoria/handoff_example_source_test.exs
  - test/scoria/semantic_fast_path_example_source_test.exs
  - lib/scoria/support_journey.ex
autonomous: true
gap_closure: true
requirements: [REL-04]
must_haves:
  truths:
    - "mix test test/scoria/phoenix_example_source_test.exs test/scoria/handoff_example_source_test.exs test/scoria/semantic_fast_path_example_source_test.exs test/scoria/support_journey_source_test.exs exits 0"
    - "Each docs-source test asserts fragments against the canonical guides/ path where Phase 48 relocated the SSOT, with the fragment set confirmed present at that path"
  artifacts:
    - test/scoria/phoenix_example_source_test.exs
    - test/scoria/handoff_example_source_test.exs
    - test/scoria/semantic_fast_path_example_source_test.exs
    - lib/scoria/support_journey.ex
  key_links:
    - "docs/*.md compat stub -> canonical guides/ SSOT (Phase 48 restructure) -> docs-source test path constant"
---

<objective>
Close Bucket A — docs-source alignment (7 failing cases across 4 files). Phase 48's ExDoc/guide
restructure moved the SSOT content out of `docs/*.md` (now thin compatibility stubs) into `guides/`.
The `*_example_source_test.exs` / `support_journey_source_test.exs` tests still read the old
`docs/*.md` paths and assert fragments (`Scoria.identity/1`, `Scoria.start_handoff_run/3`,
`use Scoria.SemanticCache.Profile`, SupportJourney SSOT) that no longer live there.

Per D-16: repoint the docs-contract tests to the canonical `guides/` paths (do NOT copy full guide
content back into `docs/` stubs). Per the scope-reduction prohibition: do NOT weaken or delete the
fragment assertions — repoint the path and confirm every fragment is present at the new path.

Purpose: turn the 7 default-lane example-source failures green so the verify lane can pass.
Output: each docs-source test reads the canonical `guides/` SSOT and its fragment set passes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-release-readiness-and-0-1-3-cut/50-CI-GAP-INVENTORY.md
@.planning/phases/50-release-readiness-and-0-1-3-cut/50-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint the three example-source tests to the canonical guides/ paths</name>
  <files>
    test/scoria/phoenix_example_source_test.exs
    test/scoria/handoff_example_source_test.exs
    test/scoria/semantic_fast_path_example_source_test.exs
  </files>
  <read_first>
    - test/scoria/phoenix_example_source_test.exs (constant `@phoenix_example "docs/phoenix_runtime_example.md"`, reads `AdoptionExample.doc_fragments/0`)
    - test/scoria/handoff_example_source_test.exs (constant `@handoff_guide "docs/bounded_handoffs.md"`, reads `AdoptionExample.handoff_doc_fragments/0`)
    - test/scoria/semantic_fast_path_example_source_test.exs (constant `@semantic_guide "docs/semantic_fast_path.md"`, inline fragment list `use Scoria.SemanticCache.Profile`, `cache_key: "account_faq"`, ...)
    - guides/capabilities/ (Phase 48 destination — candidate canonical files: default-runtime.md, bounded-handoffs.md, semantic-cache.md)
    - test/scoria/package_surface_test.exs:70-77 (the authoritative docs-basename -> guides-slug map: `bounded_handoffs` -> `bounded-handoffs`, `semantic_fast_path` -> `semantic-cache`, `support_copilot_gallery` -> `support-copilot-gallery`, etc.)
  </read_first>
  <action>
    For each of the three tests, repoint the source path constant from `docs/<name>.md` to the
    canonical `guides/` file that now holds the fragment SSOT, using the docs-basename -> guides-slug
    map in package_surface_test.exs:70-77 as the authoritative rename key. Likely mapping (CONFIRM
    each by grepping the fragment set at the candidate path before editing):
    - `docs/phoenix_runtime_example.md` -> the guides/ runtime example (candidate `guides/capabilities/default-runtime.md`); confirm it contains every fragment in `AdoptionExample.doc_fragments/0` (e.g. `Scoria.identity/1`, `Scoria.start_run`).
    - `docs/bounded_handoffs.md` -> `guides/capabilities/bounded-handoffs.md`; confirm every fragment in `AdoptionExample.handoff_doc_fragments/0` (e.g. `Scoria.start_handoff_run/3`) is present.
    - `docs/semantic_fast_path.md` -> `guides/capabilities/semantic-cache.md`; confirm each inline fragment (`use Scoria.SemanticCache.Profile`, `cache_key: "account_faq"`, `default_scope: :tenant_shared`, `safe_read_only: true`, `Scoria.start_run(identity,`, `semantic_cache: [profile: MyApp.AI.AccountFaqCache]`, and the `mix test.semantic_fast_path` command line) is present.

    CONFIRM-FRAGMENT-PRESENT is mandatory: before changing a constant, `grep -F` each asserted
    fragment at the candidate guides/ path. If a fragment is genuinely absent from every guides/
    file (i.e. Phase 48 changed the wording, not just the location), STOP and report — do NOT edit
    the fragment list to match, and do NOT restore content into the docs/ stub. Only the path
    constant changes; the fragment assertions stay byte-for-byte.
  </action>
  <verify>
    <automated>mix test test/scoria/phoenix_example_source_test.exs test/scoria/handoff_example_source_test.exs test/scoria/semantic_fast_path_example_source_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - All three test files exit 0.
    - git diff shows ONLY the path constants changed to `guides/...` — no fragment string was added, removed, or altered.
    - The chosen guides/ path for each test contains every asserted fragment (grep-confirmed).
  </acceptance_criteria>
  <done>The three example-source tests read canonical guides/ SSOT paths and pass with unchanged fragment assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint SupportJourney adopter doc surfaces to canonical paths</name>
  <files>
    lib/scoria/support_journey.ex
  </files>
  <read_first>
    - test/scoria/support_journey_source_test.exs:5-18 (iterates `SupportJourney.adopter_doc_surfaces/0`; each `{path, fragments}` is `File.read!`-checked — 4 failing surfaces: README.md, docs/connector_adoption.md, docs/operator_verification.md, docs/support_copilot_gallery.md)
    - lib/scoria/support_journey.ex:144-150 (`adopter_doc_surfaces/0` map: `"docs/support_copilot_gallery.md" => doc_fragments()`, `"docs/connector_adoption.md" => connector_doc_fragments()`, `"docs/operator_verification.md" => operator_doc_fragments()`) and the fragment helper defs
    - test/scoria/package_surface_test.exs:70-77 (docs-basename -> guides-slug map: `connector_adoption` -> `connectors-and-mcp`, `operator_verification` -> `reviewer-verification`, `support_copilot_gallery` -> `support-copilot-gallery`)
    - README.md (confirm the README fragment set is still present at README.md OR its relocated home)
  </read_first>
  <action>
    In `lib/scoria/support_journey.ex` `adopter_doc_surfaces/0`, repoint each stale `docs/*.md` map
    key to the canonical `guides/` path per the package_surface_test rename map, AFTER grep-confirming
    the corresponding fragment set is present at the new path:
    - `docs/connector_adoption.md` -> the `connectors-and-mcp` guides file (confirm `connector_doc_fragments/0` fragments present).
    - `docs/operator_verification.md` -> the `reviewer-verification` guides file (`guides/reviewer-verification.md`; confirm `operator_doc_fragments/0` fragments present).
    - `docs/support_copilot_gallery.md` -> the `support-copilot-gallery` guides file (confirm `doc_fragments/0` fragments present).
    - README.md: if its fragment set still lives in README.md, leave the key as README.md; only repoint if Phase 48 moved that SSOT. Confirm by grep before deciding.

    Keep the fragment helper contents byte-for-byte — repoint keys only. If a fragment set cannot be
    located at any guides/ path, STOP and report rather than weakening the fragment list or restoring
    docs/ stub content (D-16, scope-reduction prohibition). Do not touch `lib/scoria/hex_consumer_contract.ex`
    or `lib/mix/tasks/scoria.release_preview.ex` unless a grep proves they feed the SAME failing
    assertion (they govern package-file presence, which REL-03 already left green — avoid collateral edits).
  </action>
  <verify>
    <automated>mix test test/scoria/support_journey_source_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scoria/support_journey_source_test.exs` exits 0 (all 4 adopter-doc-surface cases pass plus the fixture cases).
    - git diff shows only map-key path changes in `adopter_doc_surfaces/0` — no fragment helper content changed.
    - Each new guides/ path contains its asserted fragment set (grep-confirmed).
  </acceptance_criteria>
  <done>SupportJourney adopter doc surfaces point at canonical guides/ SSOT; support_journey_source_test passes with unchanged fragments.</done>
</task>

</tasks>

<verification>
- `mix test test/scoria/phoenix_example_source_test.exs test/scoria/handoff_example_source_test.exs test/scoria/semantic_fast_path_example_source_test.exs test/scoria/support_journey_source_test.exs` exits 0.
- `git diff` shows only path/key changes; no fragment strings altered.
</verification>

<success_criteria>
- All 7 Bucket-A cases green.
- Docs-contract tests read canonical guides/ SSOT (D-16); no assertion weakened; no docs/ stub content restored.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs-contract test -> guides/ SSOT | Tests must assert against the canonical source, not a compatibility stub |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-06-01 | Tampering | Docs-source fragment assertions | medium | mitigate | Confirm-fragment-present gate: only path constants/keys change; fragment lists stay byte-for-byte (acceptance criterion + git diff); STOP-and-report if a fragment is genuinely missing |
| T-50-06-SC | Tampering | package installs | low | accept | No new packages; test + source path edits only |
</threat_model>

## Artifacts this phase produces (plan 06)

- `test/scoria/phoenix_example_source_test.exs`: `@phoenix_example` repointed to canonical guides/ path.
- `test/scoria/handoff_example_source_test.exs`: `@handoff_guide` repointed to canonical guides/ path.
- `test/scoria/semantic_fast_path_example_source_test.exs`: `@semantic_guide` repointed to canonical guides/ path.
- `lib/scoria/support_journey.ex`: `adopter_doc_surfaces/0` map keys repointed to canonical guides/ paths.

<output>
Create `.planning/phases/50-release-readiness-and-0-1-3-cut/50-06-SUMMARY.md` when done
</output>
</content>
