---
phase: 50-release-readiness-and-0-1-3-cut
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - test/scoria/package_surface_test.exs
  - test/scoria/warning_inventory/capture_parity_test.exs
autonomous: true
gap_closure: true
requirements: [REL-04]
must_haves:
  truths:
    - "mix test test/scoria/package_surface_test.exs exits 0 (one-publish-surface asserts the REL-03 subdomain homepage_url https://scoria.hexdocs.pm)"
    - "capture_parity_test:53 is confirmed against the release head: either it genuinely fails and is repaired, or it is proven local-env-only and left unchanged"
    - "No publish-surface or warning-inventory contract is loosened — the 'one publish surface' invariant stays intact"
  artifacts:
    - test/scoria/package_surface_test.exs
    - test/scoria/warning_inventory/capture_parity_test.exs
  key_links:
    - "mix.exs @hexdocs_url subdomain (REL-03) -> package_surface_test homepage_url assertion"
---

<objective>
Close Buckets B and F — small, adjacent.

Bucket B (package/publish surface, 1): `test/scoria/package_surface_test.exs:79` asserts
`project[:homepage_url] == "https://hexdocs.pm/scoria"`, but REL-03's HexDocs subdomain migration
(`@hexdocs_url`) set mix.exs to `"https://scoria.hexdocs.pm"`. The test lags the SSOT. Fix: align the
assertion to the subdomain — but FIRST confirm the subdomain is the intended canonical publish
surface (it is the REL-03 / D-14 decision), and keep the "one publish surface" contract intact.

Bucket F (warning inventory, 1) — VERIFY FIRST, may need NO change:
`test/scoria/warning_inventory/capture_parity_test.exs:53` appeared in the local `mix test` repro but
did NOT fail in any partition of CI run 29137880790. Reproduce against the release head before
touching it; only fix if it actually fails in the release lane.

Purpose: close the last two default-lane buckets without loosening the publish-surface invariant.
Output: package_surface_test green on the subdomain SSOT; capture_parity_test resolved (fixed only if
it genuinely fails).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-release-readiness-and-0-1-3-cut/50-CI-GAP-INVENTORY.md
@.planning/phases/50-release-readiness-and-0-1-3-cut/50-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align package_surface_test to the REL-03 HexDocs subdomain SSOT</name>
  <files>
    test/scoria/package_surface_test.exs
  </files>
  <read_first>
    - test/scoria/package_surface_test.exs:79 (`assert project[:homepage_url] == "https://hexdocs.pm/scoria"`) and the surrounding "project metadata describes one publish surface" test (:79-90+)
    - mix.exs `@hexdocs_url` (the REL-03 canonical value `"https://scoria.hexdocs.pm"`; confirm it feeds `homepage_url` and `docs[:canonical]`/related keys)
    - .planning/phases/50-release-readiness-and-0-1-3-cut/50-03-SUMMARY.md (REL-03 subdomain migration, D-14) and CONTEXT D-14 (HexDocs 2026 URL change = release-metadata correctness)
  </read_first>
  <action>
    Confirm the canonical publish surface is the subdomain `https://scoria.hexdocs.pm` (mix.exs
    `@hexdocs_url`, REL-03 / D-14). Then update the assertion at :79 from
    `"https://hexdocs.pm/scoria"` to `"https://scoria.hexdocs.pm"` so the test tracks the mix.exs SSOT.
    Scan the rest of this test for any other `hexdocs.pm/scoria` path-style expectation that should be
    the subdomain, and align those too — but keep the test asserting exactly ONE publish surface
    (do not add a second URL or relax the single-surface contract). The value must come from / match
    mix.exs; do not hardcode a divergent string.
  </action>
  <verify>
    <automated>mix test test/scoria/package_surface_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scoria/package_surface_test.exs` exits 0 (the one-publish-surface test at :79 passes).
    - The asserted `homepage_url` matches mix.exs `@hexdocs_url` (`https://scoria.hexdocs.pm`).
    - The test still asserts a single publish surface (no second URL added, contract not loosened).
  </acceptance_criteria>
  <done>package_surface_test asserts the subdomain SSOT and passes; one-publish-surface invariant intact.</done>
</task>

<task type="auto">
  <name>Task 2: Verify-first reconcile capture_parity_test against the release head</name>
  <files>
    test/scoria/warning_inventory/capture_parity_test.exs
  </files>
  <read_first>
    - test/scoria/warning_inventory/capture_parity_test.exs:53 ("optimized compile-only capture catches high-signal unclassified warning (injected)") and the capture/warning-inventory harness it drives
    - 50-CI-GAP-INVENTORY.md Bucket F note (did NOT fail in any partition of run 29137880790; treat as verify-first — may be local-env-only)
  </read_first>
  <action>
    Reproduce FIRST: run `mix test test/scoria/warning_inventory/capture_parity_test.exs` on the
    current (release-head) tree.
    - If it PASSES: make NO change. Record in the SUMMARY that Bucket F was a local-env-only artifact
      that does not fail on the release head (matches CI run 29137880790, where it did not fail in any
      partition). This is the expected outcome.
    - If it FAILS: diagnose the compile-only capture parity regression and fix the ROOT CAUSE
      (restore the optimized compile-only capture so it catches the injected high-signal unclassified
      warning). Do NOT weaken the injected-warning assertion or delete the parity check.
  </action>
  <verify>
    <automated>mix test test/scoria/warning_inventory/capture_parity_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scoria/warning_inventory/capture_parity_test.exs` exits 0.
    - If unchanged, the SUMMARY states it passed on the release head with no edit (verify-first outcome); if changed, git diff shows a root-cause capture fix, not a weakened assertion.
  </acceptance_criteria>
  <done>capture_parity_test passes on the release head, fixed only if it genuinely failed.</done>
</task>

</tasks>

<verification>
- `mix test test/scoria/package_surface_test.exs test/scoria/warning_inventory/capture_parity_test.exs` exits 0.
</verification>

<success_criteria>
- Bucket B green on the subdomain SSOT with the one-publish-surface invariant intact.
- Bucket F resolved verify-first (no change if it passes on the release head).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mix.exs publish metadata -> package_surface_test | The test must track the single canonical publish surface, not diverge from mix.exs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-10-01 | Tampering | One-publish-surface contract | medium | mitigate | Align to the mix.exs subdomain SSOT; keep exactly one surface asserted (acceptance criterion forbids adding a second URL / loosening) |
| T-50-10-02 | Repudiation | Warning-inventory capture parity | low | mitigate | Verify-first; fix root cause only if it genuinely fails; never weaken the injected-warning parity assertion |
| T-50-10-SC | Tampering | package installs | low | accept | No new packages; test edits only |
</threat_model>

## Artifacts this phase produces (plan 10)

- `test/scoria/package_surface_test.exs`: `homepage_url` assertion aligned to mix.exs `@hexdocs_url` subdomain (`https://scoria.hexdocs.pm`); single-surface invariant preserved.
- `test/scoria/warning_inventory/capture_parity_test.exs`: unchanged if it passes on the release head (verify-first); otherwise a root-cause capture fix.

<output>
Create `.planning/phases/50-release-readiness-and-0-1-3-cut/50-10-SUMMARY.md` when done
</output>
</content>
