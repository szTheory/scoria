---
phase: 50-release-readiness-and-0-1-3-cut
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/dev_seed.exs
  - priv/dev/e2e/phase16_parity.spec.mjs
autonomous: true
requirements: [REL-02]
must_haves:
  truths:
    - "Re-running the dev seed (mix run priv/repo/dev_seed.exs or mix dev.setup) prints no 'start_release_workflow/2 is undefined' skipped-warning; block (f) and block (g) success checkmarks all print"
    - "priv/dev/e2e/ia_orientation.spec.mjs (home trace stream, incident run path, eval Open prompt release) passes against a freshly-seeded dev server"
    - "priv/dev/e2e/modal_focus.spec.mjs finds the seeded pending prompt_release approval (Reject Release visible)"
    - "priv/dev/e2e/phase16_parity.spec.mjs theme-toggle is clickable at desktop viewport (locator binds to the visible desktop control, not the hidden mobile one)"
  artifacts:
    - priv/repo/dev_seed.exs
    - priv/dev/e2e/phase16_parity.spec.mjs
  key_links:
    - "dev_seed.exs call sites → Scoria.Workflows.PromptRelease.start_release_workflow/3 (tenant-scoped, Phase 44-06 contract)"
    - "phase16_parity theme-toggle locator → .filter({ visible: true }).first() convention (analog: ia_orientation.spec.mjs:160,210,231)"
---

<objective>
Repair the REL-02 e2e-lane failures. Per RESEARCH.md, 4 of the 5 PR #12 e2e failures trace to ONE root cause: `priv/repo/dev_seed.exs` calls `Scoria.Workflows.PromptRelease.start_release_workflow/2` at two sites (lines 959, 1047), but the function is arity-3 since the Phase 44-06 tenant-scoping change (`lib/scoria/workflows/prompt_release.ex:13`). The seed's own `try/rescue` silently swallows the `UndefinedFunctionError` and skips the rest of each block, so the seeded IA trace stream, incident run path, eval prompt-release link, and pending `prompt_release` approval are all absent. The 5th failure is a locator bug: `phase16_parity.spec.mjs`'s combined desktop/mobile theme-toggle selector with a bare `.first()` binds to the hidden mobile control at desktop width.

Per D-07/D-08/D-09/D-10/D-11/D-18: fix both seed call sites to the already-secured tenant-scoped arity-3 contract (do NOT weaken or re-widen tenant scope), and harden the theme-toggle locator with the codebase's existing `.filter({ visible: true })` idiom (no `force: true`, no fixed sleep). Preserve the silent-rescue seed pattern itself (out of scope to refactor).

Purpose: Turn the `e2e` CI lane green so PR #12 can reach `ci-gate` (REL-02, blocks REL-04).
Output: Two corrected seed call sites; three hardened theme-toggle locators; all 5 previously-failing specs green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-release-readiness-and-0-1-3-cut/50-RESEARCH.md
@.planning/phases/50-release-readiness-and-0-1-3-cut/50-PATTERNS.md
@lib/scoria/workflows/prompt_release.ex
@lib/scoria_web/live/prompt_live/release_workbench_live.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix both stale start_release_workflow/2 call sites in dev_seed.exs to the tenant-scoped arity-3 contract</name>
  <files>priv/repo/dev_seed.exs</files>
  <read_first>
    - priv/repo/dev_seed.exs around line 959 (block "(f) Prompt Registry + Release Workbench") and line 1047 (block "(g) Phase 13 IA linkage scenario"); confirm `tenant_id` binding is in scope at each site (bound earlier in the script; block g binds it ~line 991 via `SupportJourney.tenant_id()`)
    - lib/scoria/workflows/prompt_release.ex:13 (the current `start_release_workflow/3` contract requiring `tenant_id` in `attrs`)
    - lib/scoria_web/live/prompt_live/release_workbench_live.ex:117 (the correct in-repo caller analog passing `tenant_id: tenant_id`)
    - RESEARCH.md Pitfall 2 + Code Example 1; 50-PATTERNS.md "dev_seed.exs" section
  </read_first>
  <action>
    At `priv/repo/dev_seed.exs:959`, change the call `PromptRelease.start_release_workflow(draft_template.id, "operator-1")` to pass the third argument `tenant_id: tenant_id` (matching the arity-3 contract; `tenant_id` is already bound earlier in the seed script). At `priv/repo/dev_seed.exs:1047`, change `PromptRelease.start_release_workflow(draft.id, "operator-1")` to pass `tenant_id: tenant_id` (bound at ~line 991 via `SupportJourney.tenant_id()`). Use the exact `tenant_id: tenant_id` kwarg shape from the `release_workbench_live.ex:117` analog.
    Do NOT remove or restructure the surrounding `try/rescue` blocks (keeping the resilience pattern is required; broad seed-determinism refactor is out of scope per D-11 / CONTEXT scope fence). Do NOT weaken or re-widen tenant scope — pass the real bound `tenant_id`, never a hardcoded default or lineage fallback (D-18, preserves the Phase 44-06 security contract).
    After editing, run the seed end-to-end against a fresh database and confirm stdout no longer prints any `start_release_workflow/2 is undefined` skipped-warning, and that ALL of block (f) and block (g) success checkmark lines print (including the downstream eval-runs/dataset-linkage lines after line 1047 that never executed in the failing CI run — per RESEARCH.md Open Question 1, this is the first time that code path runs in this form; if a latent downstream failure surfaces, root-cause it within this seed block scope before proceeding).
  </action>
  <verify>
    <automated>grep -nc 'start_release_workflow([^,]*, "operator-1")' priv/repo/dev_seed.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'start_release_workflow' priv/repo/dev_seed.exs` shows both call sites with a third `tenant_id: tenant_id` argument; zero bare 2-arity calls remain.
    - Running `MIX_ENV=dev mix run priv/repo/dev_seed.exs` (or `mix dev.setup`) against a fresh DB completes with no `start_release_workflow/2 is undefined` line in stdout, and both blocks' success checkmarks print.
  </acceptance_criteria>
  <done>Both call sites use the arity-3 tenant-scoped contract with the real bound `tenant_id`; seed run prints no arity skip-warning; rescue structure preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Harden the theme-toggle locator in phase16_parity.spec.mjs</name>
  <files>priv/dev/e2e/phase16_parity.spec.mjs</files>
  <read_first>
    - priv/dev/e2e/phase16_parity.spec.mjs lines 533, 562, 575 (the three `page.locator('#scoria-theme-toggle, #scoria-theme-toggle-mobile').first()` sites) AND lines 513, 611, 620 (the `querySelector` string and single-ID locators that must NOT be changed — confirm each site's surrounding context before editing)
    - priv/dev/e2e/ia_orientation.spec.mjs:160,210,231 (the `.filter({ visible: true }).first()` convention analog)
    - lib/scoria_web/components/layouts/app.html.heex (mobile toggle rendered before desktop toggle) and assets/css/04-components.css:194-197 (`.scoria-mobile-topbar { display: none }` at >=768px) for the DOM-order/visibility rationale
    - RESEARCH.md Pitfall 3 + Code Example 2; 50-PATTERNS.md "phase16_parity.spec.mjs" section
  </read_first>
  <action>
    At the three combined-selector sites (lines 533, 562, 575), insert `.filter({ visible: true })` between `.locator('#scoria-theme-toggle, #scoria-theme-toggle-mobile')` and `.first()`, matching the existing `ia_orientation.spec.mjs:160,210,231` idiom, so the locator resolves to the actually-visible desktop control at Playwright's default desktop viewport instead of the hidden mobile control (which sorts first in DOM order).
    Do NOT change line 513 (a `toggleSelector` string passed into `page.evaluate` / plain DOM `querySelector`) or lines 611/620 (already viewport-scoped single-ID locators used independently) — those are not the dual-selector-with-bare-`.first()` bug. Do NOT use `force: true` or a fixed `sleep` (forbidden by D-09).
  </action>
  <verify>
    <automated>grep -c "locator('#scoria-theme-toggle, #scoria-theme-toggle-mobile').first()" priv/dev/e2e/phase16_parity.spec.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "locator('#scoria-theme-toggle, #scoria-theme-toggle-mobile').first()" priv/dev/e2e/phase16_parity.spec.mjs` returns `0` (no bare combined-selector `.first()` remains).
    - `grep -c "filter({ visible: true })" priv/dev/e2e/phase16_parity.spec.mjs` returns at least `3`.
    - Lines 513, 611, 620 are unchanged (single-ID / querySelector sites intact); no `force: true` introduced.
  </acceptance_criteria>
  <done>All three combined-selector theme-toggle locators use `.filter({ visible: true }).first()`; single-ID and querySelector sites untouched; no force/sleep added.</done>
</task>

</tasks>

<verification>
With a dev server running against the freshly-seeded database (Task 1), run the full e2e suite:
`mix scoria.ui.e2e --base-url http://localhost:4799/scoria`
All previously-failing specs must pass: `ia_orientation.spec.mjs` (home trace stream, incident run path, eval "Open prompt release"), `modal_focus.spec.mjs` (release workbench "Reject Release" from the seeded pending approval), and `phase16_parity.spec.mjs` (theme toggle clickable at desktop width). Verify Node/Playwright is installed first (`npm --prefix priv/dev ci` + `npx playwright install --with-deps chromium`) per RESEARCH.md Environment Availability.
</verification>

<success_criteria>
- Dev seed runs clean (no arity skip-warning); IA trace/incident/eval/approval evidence exists in the expected tenant context (D-08).
- All 5 previously-failing PR #12 e2e specs pass; no e2e was skipped, downgraded, or blanket-descoped (D-07); no product UI redesign (D-10).
- Tenant scope preserved via the already-secured arity-3 contract (D-18).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev seed script → Scoria.Workflows API | Dev-only fixture generation writes through the same tenant-scoped workflow API adopters use |
| browser (Playwright) → LiveView dashboard | e2e drives the real rendered DOM against a seeded dev server |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-02-01 | Tampering / Info Disclosure | dev_seed.exs re-widening tenant scope to satisfy the arity-3 call | medium | mitigate | Fix passes the real bound `tenant_id` (never a hardcoded default/lineage fallback); this restores usage of Phase 44-06's already-hardened contract rather than weakening it — no new control added, existing control correctly consumed (V4, incidental) |
| T-50-02-02 | Tampering (of test evidence) | Silent-rescue seed hiding a regression as a flaky e2e failure | low | mitigate | Task 1 requires grepping the seed's own stdout for the swallowed skip-warning as an explicit verification step before trusting DOM assertions |

No net-new high-severity threats. Per the security contribution note, the tenant-scoping control is already mitigated by Phase 44-06; this plan is a consumer that must not weaken it.
</threat_model>

## Artifacts this phase produces (plan 02)

- `priv/repo/dev_seed.exs`: two `start_release_workflow` call sites (lines 959, 1047) changed from arity-2 to arity-3 with `tenant_id: tenant_id`. The seed now creates the block-(f) pending `prompt_release` approval and block-(g) IA demo run / trace spans / incident / eval-linked prompt release that were previously silently skipped.
- `priv/dev/e2e/phase16_parity.spec.mjs`: three theme-toggle locators (lines 533, 562, 575) changed to `.filter({ visible: true }).first()`.

<output>
Create `.planning/phases/50-release-readiness-and-0-1-3-cut/50-02-SUMMARY.md` when done
</output>
