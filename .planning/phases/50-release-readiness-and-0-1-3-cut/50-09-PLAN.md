---
phase: 50-release-readiness-and-0-1-3-cut
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/support_copilot/test/support_copilot/journey_test.exs
  - examples/support_copilot/test/support_copilot_web/orchestrator_producer_test.exs
  - examples/support_copilot/lib
autonomous: true
gap_closure: true
requirements: [REL-04]
must_haves:
  truths:
    - "The nested examples/support_copilot gallery suite passes: knowledge-lane refund-policy grounded journey (journey_test:110), approvals-page-from-producer-path (orchestrator_producer_test:31), and advisory adoption journey are green"
    - "mix test test/scoria/support_copilot_gallery_test.exs exits 0 (Runner.run! completes deps_get -> gallery_db -> gallery_test)"
    - "Demo-journey fixes preserve approval semantics and grounding contracts — no assertion is deleted or loosened"
  artifacts:
    - examples/support_copilot/test/support_copilot/journey_test.exs
    - examples/support_copilot/test/support_copilot_web/orchestrator_producer_test.exs
  key_links:
    - "Scoria.TestSupport.SupportCopilotGallery.Runner (examples/support_copilot) -> nested mix test -> gallery proof steps"
    - "knowledge-lane seeded refund policy -> grounded journey render; producer approval path -> approvals page"
---

<objective>
Close Bucket E — SupportCopilot demo-app journeys (3 failures). These live in the NESTED example
project `examples/support_copilot/` (driven by `Scoria.TestSupport.SupportCopilotGallery.Runner`,
which runs `mix deps.get` -> gallery DB -> `mix test` inside that project). The parent test
`test/scoria/support_copilot_gallery_test.exs:8` asserts the runner completes all steps, so it fails
whenever any nested test fails. Failing nested tests:
`examples/support_copilot/test/support_copilot/journey_test.exs:110` (knowledge lane seeds refund
policy and surfaces grounded journey) and
`examples/support_copilot/test/support_copilot_web/orchestrator_producer_test.exs:31` (approvals page
shows approval from producer path). Under the connector tag these re-run as the same failures (no
additional unique tests).

Do NOT weaken assertions: repair the demo-app seeding/grounding/approval wiring so the journeys pass;
preserve approval semantics and grounding contracts (D-08, D-10).

Purpose: turn the SupportCopilot gallery journeys green so the gallery proof (and the connector lane)
passes.
Output: nested gallery suite green; `support_copilot_gallery_test` proof passes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-release-readiness-and-0-1-3-cut/50-CI-GAP-INVENTORY.md
@.planning/phases/50-release-readiness-and-0-1-3-cut/50-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair the knowledge-lane grounded journey and producer-path approvals in the nested gallery app</name>
  <files>
    examples/support_copilot/test/support_copilot/journey_test.exs
    examples/support_copilot/test/support_copilot_web/orchestrator_producer_test.exs
    examples/support_copilot/lib
  </files>
  <read_first>
    - examples/support_copilot/test/support_copilot/journey_test.exs:110 ("knowledge lane seeds refund policy and surfaces grounded journey") and its seed/setup block
    - examples/support_copilot/test/support_copilot_web/orchestrator_producer_test.exs:31 ("approvals page shows approval from producer path") and its setup
    - test/support/scoria/support_copilot_gallery/runner.ex (how the parent drives the nested project: `@gallery_root examples/support_copilot`, steps deps_get -> gallery_db -> gallery_test)
    - the nested app source under examples/support_copilot/lib that owns knowledge-lane seeding, refund-policy grounding, and the producer->approval path (locate via the failing tests)
    - .planning/phases/50-release-readiness-and-0-1-3-cut/50-02-SUMMARY.md (REL-02 tenant-scoped seeding shape — the same grounding/approval seed determinism applies here; D-08)
  </read_first>
  <action>
    Reproduce the nested suite first to see the concrete failure text:
    run the gallery via `mix test test/scoria/support_copilot_gallery_test.exs` (which drives the
    nested project) and/or directly inside the nested project to read the exact assertion diffs at
    journey_test:110 and orchestrator_producer_test:31.

    Then repair the ROOT CAUSE in the nested app (seed determinism / grounding wiring / producer->
    approval path), mirroring the v3.5 vocabulary + tenant-scoped seeding contracts:
    - journey_test:110: ensure the knowledge lane deterministically seeds the refund policy and the
      grounded journey surfaces the expected grounded evidence/copy. If the assertion string drifted
      due to the D-19/D-20 vocabulary migration, repoint it to the exact current rendered string
      (confirm in source); if the seeding regressed, fix the seed/grounding so the journey grounds.
    - orchestrator_producer_test:31: ensure the producer path produces the approval that the approvals
      page renders. Preserve approval semantics (D-10) — do not stub or bypass the approval to force
      the page to show it.

    Do NOT loosen or delete assertions; preserve the approval and grounding contracts.
  </action>
  <verify>
    <automated>mix test test/scoria/support_copilot_gallery_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scoria/support_copilot_gallery_test.exs` exits 0 (Runner completes deps_get -> gallery_db -> gallery_test; proof.steps == [:deps_get, :gallery_db, :gallery_test]).
    - The nested journey_test:110 and orchestrator_producer_test:31 pass.
    - git diff shows seeding/grounding/approval-wiring fixes (or a grep-confirmed copy repoint), not weakened assertions or bypassed approvals.
  </acceptance_criteria>
  <done>The nested SupportCopilot gallery journeys pass; gallery proof completes; approval + grounding contracts preserved.</done>
</task>

</tasks>

<verification>
- `mix test test/scoria/support_copilot_gallery_test.exs` exits 0 (drives the full nested gallery suite).
</verification>

<success_criteria>
- All 3 Bucket-E failures green (and their connector-tag re-runs).
- Demo journeys pass with approval semantics and grounding contracts preserved (D-08, D-10).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| producer path -> approval record -> approvals page | Approvals must originate from the real producer path, not a bypass |
| knowledge lane seed -> grounded journey render | Grounding must come from seeded evidence, not a stubbed answer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-09-01 | Tampering / Elevation of Privilege | Approval semantics in producer->approvals path | medium | mitigate | Fix the producer->approval wiring; do not stub/bypass the approval to force the page (D-10); acceptance criterion forbids bypass |
| T-50-09-02 | Spoofing | Grounded-journey evidence | low | mitigate | Grounding must come from the seeded refund policy, not a hardcoded answer (D-08) |
| T-50-09-SC | Tampering | nested `mix deps.get` in examples/support_copilot | low | accept | Existing gallery infra; no NEW packages introduced by this plan (RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

## Artifacts this phase produces (plan 09)

- `examples/support_copilot/` nested app: knowledge-lane seeding / grounding and producer->approval path repaired so `journey_test:110` and `orchestrator_producer_test:31` pass.
- No parent-repo assertion changes; `test/scoria/support_copilot_gallery_test.exs` proof now passes.

<output>
Create `.planning/phases/50-release-readiness-and-0-1-3-cut/50-09-SUMMARY.md` when done
</output>
</content>
