---
phase: 50-release-readiness-and-0-1-3-cut
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - test/scoria_web/ui_component_test.exs
  - test/scoria_web/dev_lab_boundary_test.exs
autonomous: true
gap_closure: true
requirements: [REL-04]
must_haves:
  truths:
    - "mix test test/scoria_web/ui_component_test.exs exits 0 (:286 flush-panel gutters, :1289 table density-out-of-public-API)"
    - "mix test test/scoria_web/dev_lab_boundary_test.exs exits 0 (guard #7 canonical PRIM-*/GROUP-* inventory-ID reference)"
    - "docs/MAINTAINERS.md reads are repointed to the canonical guides/maintainers.md SSOT (Phase 48 / REL-01 relocation); no CSS/UI contract assertion is weakened"
  artifacts:
    - test/scoria_web/ui_component_test.exs
    - test/scoria_web/dev_lab_boundary_test.exs
  key_links:
    - "docs/MAINTAINERS.md compat stub -> guides/maintainers.md canonical SSOT -> ui_component_test File.read! source"
    - "canonical PRIM-*/GROUP-* inventory IDs -> dev/lab/** references -> dev_lab_boundary guard #7"
---

<objective>
Close Bucket D — UI component / dev-lab contracts (3 failures). `ui_component_test.exs:286` and
`:1289` both `File.read!("docs/MAINTAINERS.md")` — but REL-01 and the Phase 48 restructure made
`guides/maintainers.md` the canonical maintainer-docs SSOT (docs/MAINTAINERS.md is now a compat
stub), so the maintainer-doc fragments those two tests assert have moved. `dev_lab_boundary_test.exs:161`
(guard #7) asserts every canonical PRIM-*/GROUP-* inventory ID is referenced under `dev/lab/**`.

Do NOT weaken assertions: repoint the docs source read to `guides/maintainers.md` after confirming
the asserted fragments are present there; for guard #7, restore the missing inventory-ID reference
under dev/lab/** (or repoint to the canonical inventory) rather than loosening the guard.

Purpose: turn the 3 Bucket-D failures green.
Output: ui-component + dev-lab contracts pass against canonical sources.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-release-readiness-and-0-1-3-cut/50-CI-GAP-INVENTORY.md
@.planning/phases/50-release-readiness-and-0-1-3-cut/50-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint ui_component_test maintainer-doc reads to guides/maintainers.md</name>
  <files>
    test/scoria_web/ui_component_test.exs
  </files>
  <read_first>
    - test/scoria_web/ui_component_test.exs:286-300 ("flush panel gutters use component variables…"; `docs_source = File.read!("docs/MAINTAINERS.md")` then `assert docs_source =~ ...`)
    - test/scoria_web/ui_component_test.exs:1289-1300 ("table source and CSS keep density out of the public API"; also `File.read!("docs/MAINTAINERS.md")`)
    - guides/maintainers.md (the canonical maintainer SSOT per REL-01 / 50-01-SUMMARY.md; confirm the asserted doc fragments live here)
    - .planning/phases/50-release-readiness-and-0-1-3-cut/50-01-SUMMARY.md (REL-01 repointed docs contracts from docs/*.md to guides/maintainers.md)
  </read_first>
  <action>
    In both failing tests, change `File.read!("docs/MAINTAINERS.md")` to
    `File.read!("guides/maintainers.md")`, AFTER grep-confirming each `docs_source =~ ...` fragment is
    present in guides/maintainers.md. Only the read path changes; keep every `assert`/`refute`
    fragment on `docs_source` (and the css_source/ui_source assertions) byte-for-byte. Note these
    tests read multiple sources (assets/css/04-components.css, lib/scoria_web/ui.ex) — leave those
    reads unchanged; only the docs/MAINTAINERS.md read is stale.
    If any docs_source fragment is genuinely absent from guides/maintainers.md, STOP and report — do
    not restore content into the docs/ stub and do not weaken the assertion.
  </action>
  <verify>
    <automated>mix test test/scoria_web/ui_component_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scoria_web/ui_component_test.exs` exits 0 (including :286 and :1289).
    - git diff shows only `docs/MAINTAINERS.md` -> `guides/maintainers.md` read-path changes; no fragment altered.
  </acceptance_criteria>
  <done>Both ui_component_test maintainer-doc contracts read the canonical guides/maintainers.md and pass.</done>
</task>

<task type="auto">
  <name>Task 2: Restore the guard #7 canonical PRIM-*/GROUP-* inventory-ID reference under dev/lab/**</name>
  <files>
    test/scoria_web/dev_lab_boundary_test.exs
  </files>
  <read_first>
    - test/scoria_web/dev_lab_boundary_test.exs:161 (guard #7: "every canonical PRIM-*/GROUP-* inventory ID is referenced under dev/lab/**") and the guard's definition of the canonical inventory source + how it scans dev/lab/**
    - the canonical PRIM-*/GROUP-* inventory source the guard reads (locate via the test) and the dev/lab/** files it scans for references
  </read_first>
  <action>
    Determine which canonical PRIM-*/GROUP-* inventory ID is no longer referenced under dev/lab/**
    (the guard reports the unreferenced IDs). Two legitimate fixes — pick per root cause:
    (a) if a dev/lab reference was dropped/renamed during the v3.5 restructure, restore the reference
    to the canonical ID under dev/lab/** so the inventory ID is covered again; or
    (b) if the inventory ID itself was renamed/retired in the canonical source, align the guard's
    canonical-inventory read to the current SSOT.
    Do NOT loosen guard #7 (e.g. skip the ID, relax the "every … is referenced" contract). The guard
    is a dev-lab boundary invariant; keep it strict (mirrors D-05/D-16 "don't loosen the contract").
    Confirm the fix by reproducing the guard locally before and after.
  </action>
  <verify>
    <automated>mix test test/scoria_web/dev_lab_boundary_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scoria_web/dev_lab_boundary_test.exs` exits 0 (guard #7 at :161 passes).
    - The guard's "every canonical PRIM-*/GROUP-* inventory ID is referenced under dev/lab/**" contract is unchanged in strictness (git diff shows a restored reference or a canonical-source repoint, not a relaxed guard).
  </acceptance_criteria>
  <done>Guard #7 passes with every canonical inventory ID referenced under dev/lab/**; contract strictness preserved.</done>
</task>

</tasks>

<verification>
- `mix test test/scoria_web/ui_component_test.exs test/scoria_web/dev_lab_boundary_test.exs` exits 0.
</verification>

<success_criteria>
- All 3 Bucket-D failures green.
- Maintainer-doc reads use canonical guides/maintainers.md; guard #7 stays strict; no assertion weakened.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ui/dev-lab contract test -> canonical SSOT (guides/maintainers.md, PRIM-*/GROUP-* inventory) | Contracts must read the canonical source, not a stub |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-08-01 | Tampering | UI/dev-lab contract assertions | medium | mitigate | Confirm-fragment-present + strict-guard preservation: repoint reads only; never loosen guard #7 or a css/ui assertion (acceptance criteria + git diff) |
| T-50-08-SC | Tampering | package installs | low | accept | No new packages; test edits only |
</threat_model>

## Artifacts this phase produces (plan 08)

- `test/scoria_web/ui_component_test.exs`: two `docs/MAINTAINERS.md` reads repointed to `guides/maintainers.md`.
- `test/scoria_web/dev_lab_boundary_test.exs`: guard #7 canonical inventory-ID reference restored/repointed (strictness preserved).

<output>
Create `.planning/phases/50-release-readiness-and-0-1-3-cut/50-08-SUMMARY.md` when done
</output>
</content>
