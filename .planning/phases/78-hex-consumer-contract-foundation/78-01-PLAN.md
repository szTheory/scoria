---
phase: 78-hex-consumer-contract-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/hex_consumer_contract.ex
  - test/scoria/hex_consumer_contract_test.exs
  - test/scoria/package_surface_test.exs
autonomous: true
requirements: [DOCS-HEX-01]
must_haves:
  truths:
    - "Scoria.HexConsumerContract is a public lib SSOT module separate from AdopterDocContract (D-01, D-05)."
    - "published_version/0 reads Application.spec(:scoria, :vsn); @hex_requirement is explicit ~> 0.1, not auto-derived (D-02)."
    - "hex_dep_snippet/0 byte-matches README active dep line; tarball helpers exist but adoption proof waits for plan 78-03 (D-03, D-04, D-07)."
    - "package_surface_test Hex-primary guard imports contract instead of hardcoded dep strings (D-04, D-22)."
    - "ensure_current_unpack_root!/0 exists as a stub; full fingerprint cache lands in plan 78-02 (D-10 wave split)."
    - "D-14: baseline_upgrade_version/0 returns 0.1.0 as Phase 80 committed-fixture hook only — fixture dir not created in Phase 78."
    - "D-21: Phase 78 delivers executable + SSOT tests only — no README/operator/adoption_lanes prose sweep."
    - "D-29: Phase 80 reuses same create_host! host struct with dep_mode :hex_tarball and pinned 0.1.0 unpack — no second phx.new."
  artifacts:
    - path: "lib/scoria/hex_consumer_contract.ex"
      provides: "Hex dep + tarball tuple SSOT; stub unpack resolver"
      contains: "defmodule Scoria.HexConsumerContract"
    - path: "test/scoria/hex_consumer_contract_test.exs"
      provides: "Version and snippet drift guards"
      contains: "hex_dep_snippet"
    - path: "test/scoria/package_surface_test.exs"
      provides: "README dep alignment via contract"
      contains: "HexConsumerContract"
  key_links:
    - from: "lib/scoria/hex_consumer_contract.ex"
      to: "test/scoria/hex_consumer_contract_test.exs"
      via: "published_version/0 and hex_dep_snippet/0 assertions"
      pattern: "HexConsumerContract"
    - from: "lib/scoria/hex_consumer_contract.ex"
      to: "test/scoria/package_surface_test.exs"
      via: "Hex-primary install test imports snippet helpers"
      pattern: "hex_dep_snippet"
---

<objective>
Ship `Scoria.HexConsumerContract` as the executable SSOT for Hex dep snippets, version policy, and tarball tuple builders; add unit drift guards; refactor README dep assertions in `package_surface_test` to import the contract.

Purpose: Establish the two-surface SSOT (adopter Hex snippet vs CI tarball path tuple) before unpack caching and adoption harness wiring.
Output: `lib/scoria/hex_consumer_contract.ex`, `test/scoria/hex_consumer_contract_test.exs`, refactored Hex-primary test in `package_surface_test.exs`.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/78-hex-consumer-contract-foundation/78-CONTEXT.md
@.planning/phases/78-hex-consumer-contract-foundation/78-RESEARCH.md
@.planning/phases/78-hex-consumer-contract-foundation/78-VALIDATION.md
@lib/scoria/adopter_doc_contract.ex
@test/scoria/package_surface_test.exs
@README.md
@mix.exs

Locked decisions D-01 through D-05, D-21, D-22 apply. Do not merge into AdopterDocContract. Do not touch Generator, Runner, consumer proof, or CI in this plan. Leave `hex preview includes the required release surface` test on inline `mix hex.build` until plan 78-02.
</context>

<tasks>

<task type="auto">
  <name>Task 78-01-01: Create Scoria.HexConsumerContract SSOT module</name>
  <requirement_ids>DOCS-HEX-01</requirement_ids>
  <files>lib/scoria/hex_consumer_contract.ex</files>
  <read_first>
    .planning/phases/78-hex-consumer-contract-foundation/78-CONTEXT.md
    .planning/phases/78-hex-consumer-contract-foundation/78-RESEARCH.md
    lib/scoria/adopter_doc_contract.ex
    mix.exs
    README.md
  </read_first>
  <action>
Create `lib/scoria/hex_consumer_contract.ex` with `@moduledoc` stating non-runtime SSOT for Hex consumer dep shapes (mirror AdopterDocContract tone). Module attributes: `@app :scoria`, `@hex_requirement "~> 0.1"`, `@baseline_upgrade_version "0.1.0"`, `@github_repo "szTheory/scoria"`. Implement public functions: `app/0`, `hex_requirement/0`, `published_version/0` (`Application.spec(:scoria, :vsn) |> to_string()`), `baseline_upgrade_version/0`, `hex_dep_tuple/0` returning `{:scoria, "~> 0.1", hex: :scoria}`, `hex_dep_snippet/0` returning exactly `"{:scoria, \"~> 0.1\", hex: :scoria}"` (byte-match README.md line 57), `github_fallback_tuple/1` returning `{:scoria, github: @github_repo, tag: "v#{version}"}`, `github_fallback_snippet/1` returning `"{:scoria, github: \"szTheory/scoria\", tag: \"v#{version}\"}"`, `tarball_dep_tuple/1` returning `{:scoria, path: unpack_root}`, `tarball_dep_snippet/1` returning `"{:scoria, path: #{inspect(unpack_root)}}"`. Add `ensure_current_unpack_root!/0` stub that raises `Mix.raise("HexConsumerContract unpack cache not implemented — complete plan 78-02")` so callers compile but cache lands in wave 2. Do not add compile-time `@unpack_root` attributes (D-15).
  </action>
  <acceptance_criteria>
    - `rg -n "defmodule Scoria\\.HexConsumerContract" lib/scoria/hex_consumer_contract.ex` returns one match.
    - `rg -n 'def hex_dep_snippet' lib/scoria/hex_consumer_contract.ex` returns function.
    - `rg -n 'def tarball_dep_tuple' lib/scoria/hex_consumer_contract.ex` returns function.
    - `rg -n 'def ensure_current_unpack_root!' lib/scoria/hex_consumer_contract.ex` returns stub.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <done>HexConsumerContract exposes adopter Hex snippet, tarball tuple builders, version policy, and a compile-safe unpack stub.</done>
</task>

<task type="auto">
  <name>Task 78-01-02: Add hex_consumer_contract_test.exs drift guards</name>
  <requirement_ids>DOCS-HEX-01</requirement_ids>
  <files>test/scoria/hex_consumer_contract_test.exs</files>
  <read_first>
    lib/scoria/hex_consumer_contract.ex
    mix.exs
    README.md
    test/scoria/package_surface_test.exs
  </read_first>
  <action>
Create `test/scoria/hex_consumer_contract_test.exs` with `use ExUnit.Case, async: true`. Tests: (1) `assert HexConsumerContract.published_version() == Mix.Project.config()[:version]`; (2) read README, extract active `{:scoria,` lines (trim leading `#`), assert exactly one line and it equals `HexConsumerContract.hex_dep_snippet()`; (3) `assert elem(HexConsumerContract.hex_dep_tuple(), 2) == [hex: :scoria]`; (4) `unpack = "/tmp/scoria-unpack-stub"` then `assert HexConsumerContract.tarball_dep_tuple(unpack) == {:scoria, path: unpack}` and refute Keyword.has_key?(elem(HexConsumerContract.tarball_dep_tuple(unpack), 2) || [], :hex)` — assert third element is not a keyword with `:hex` (tuple is two-element `{:scoria, path: ...}`); (5) `assert HexConsumerContract.baseline_upgrade_version() == "0.1.0"`.
  </action>
  <acceptance_criteria>
    - `test/scoria/hex_consumer_contract_test.exs` exists.
    - `rg -n "published_version\\(\\)" test/scoria/hex_consumer_contract_test.exs` returns mix.exs alignment test.
    - `rg -n "hex_dep_snippet" test/scoria/hex_consumer_contract_test.exs` returns README guard.
    - `MIX_ENV=test mix test test/scoria/hex_consumer_contract_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/hex_consumer_contract_test.exs</automated>
  </verify>
  <done>Unit tests guard version SSOT, README active dep line, Hex tuple shape, and tarball/adopter separation.</done>
</task>

<task type="auto">
  <name>Task 78-01-03: Refactor package_surface_test Hex-primary guard to use contract</name>
  <requirement_ids>DOCS-HEX-01</requirement_ids>
  <files>test/scoria/package_surface_test.exs</files>
  <read_first>
    test/scoria/package_surface_test.exs
    test/scoria/hex_consumer_contract_test.exs
    lib/scoria/hex_consumer_contract.ex
    README.md
  </read_first>
  <action>
In `test/scoria/package_surface_test.exs`, alias `Scoria.HexConsumerContract`. In test `"Hex-primary install with optional GitHub fallback"`: replace hardcoded `"{:scoria, \"~> 0.1\", hex: :scoria}"` with `HexConsumerContract.hex_dep_snippet()`; replace hardcoded GitHub tag string with `HexConsumerContract.github_fallback_snippet(HexConsumerContract.published_version())`; keep active-dep-line count logic but compare collected line to `HexConsumerContract.hex_dep_snippet()`. Optionally in `"project metadata describes one publish surface"`, add `assert project[:version] == HexConsumerContract.published_version()`. Do **not** modify `"hex preview includes the required release surface"` in this task — it stays on per-test inline `hex.build` until plan 78-02.
  </action>
  <acceptance_criteria>
    - `rg -n 'HexConsumerContract\\.hex_dep_snippet' test/scoria/package_surface_test.exs` returns at least one match.
    - `rg -n '\\{:scoria, \\"~> 0\\.1\\", hex: :scoria\\}' test/scoria/package_surface_test.exs` returns zero matches (no hardcoded active dep string).
    - `MIX_ENV=test mix test test/scoria/hex_consumer_contract_test.exs test/scoria/package_surface_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/hex_consumer_contract_test.exs test/scoria/package_surface_test.exs</automated>
  </verify>
  <done>package_surface_test README dep assertions import HexConsumerContract; hex preview test unchanged for wave 2.</done>
</task>

</tasks>

<verification>
After all tasks:

```bash
MIX_ENV=test mix test test/scoria/hex_consumer_contract_test.exs test/scoria/package_surface_test.exs
MIX_ENV=test mix compile --warnings-as-errors
```
</verification>

<success_criteria>
- HexConsumerContract module compiles under `lib/scoria/` with all D-03 API functions except full unpack cache.
- README active dep line and mix.exs version are guarded by executable tests.
- No changes to host proof harness, adoption lane, or CI topology.
</success_criteria>

<must_haves>
- `Scoria.HexConsumerContract.hex_dep_snippet/0` equals README active dep line byte-for-byte.
- `Scoria.HexConsumerContract.published_version/0` equals `Mix.Project.config()[:version]`.
- Tarball tuple helpers never include `:hex` key.
- AdopterDocContract remains separate module.
</must_haves>
