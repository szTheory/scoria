---
phase: 78-hex-consumer-contract-foundation
plan: 03
type: execute
wave: 3
depends_on: [78-02]
files_modified:
  - test/support/scoria/host_app_proof/generator.ex
  - test/support/scoria/host_app_proof/runner.ex
  - test/scoria/host_app_consumer_proof_test.exs
  - .github/workflows/ci-verify.yml
autonomous: true
requirements: [HEX-CONSUMER-01, DOCS-HEX-01]
must_haves:
  truths:
    - "HostAppProof.Generator.create_host!/1 requires explicit dep_mode :hex_tarball | :path — no silent default (D-25, D-26)."
    - ":hex_tarball injects HexConsumerContract.tarball_dep_tuple(unpack_root); adoption test refutes repo-root path (D-06, D-18, D-27)."
    - "Runner.run_route_proof!/1 runs only host_route_smoke_test.exs overlay — not runtime or handoff (D-16, D-17)."
    - "host_app_consumer_proof_test uses setup_all ensure_current_unpack_root!/0 once per module (D-11)."
    - "CI test job sets SCORIA_HEX_UNPACK_ROOT=tmp/scoria-release-preview after release preview (D-12)."
    - "mix test.adoption is green with route-only tarball proof (HEX-CONSUMER-01 partial; full overlay in Phase 79)."
    - "D-08: Reject path to raw .tar files and MIX_EX_PATH-style env overrides — proof uses unpack dir path tuple only."
    - "D-09: Live {:scoria, ~> 0.1, hex: :scoria} registry resolution stays Phase 81 post-publish-smoke — not Phase 78."
    - "D-19: Phase 79 switches consumer test to run_full_proof!/1 with full overlay step list — out of Phase 78 scope."
    - "D-20: Preserve @moduletag timeout 180_000, async false, one phx.new host per run."
    - "D-24: Optional README Verification sentence deferred to late Phase 78 or early Phase 79 — not required in foundation PR."
    - "D-28: HostInstallFixtures and installer contract tests stay on repo path — do not route through Generator."
  artifacts:
    - path: "test/support/scoria/host_app_proof/generator.ex"
      provides: "Required dep_mode with :hex_tarball and :path branches"
      contains: "Keyword.fetch!(opts, :dep_mode)"
    - path: "test/support/scoria/host_app_proof/runner.ex"
      provides: "Route-only overlay filter for run_route_proof!/1"
      contains: "host_route_smoke_test.exs"
    - path: "test/scoria/host_app_consumer_proof_test.exs"
      provides: "Tarball adoption proof with setup_all unpack root"
      contains: "dep_mode: :hex_tarball"
    - path: ".github/workflows/ci-verify.yml"
      provides: "Unpack root env reuse after release preview"
      contains: "SCORIA_HEX_UNPACK_ROOT"
  key_links:
    - from: "test/scoria/host_app_consumer_proof_test.exs"
      to: "lib/scoria/hex_consumer_contract.ex"
      via: "setup_all ensure_current_unpack_root!/0 and tarball_dep_snippet assertion"
      pattern: "ensure_current_unpack_root!"
    - from: "test/support/scoria/host_app_proof/generator.ex"
      to: "lib/scoria/hex_consumer_contract.ex"
      via: ":hex_tarball patches tarball_dep_tuple"
      pattern: "tarball_dep_tuple"
    - from: ".github/workflows/ci-verify.yml"
      to: "lib/scoria/hex_consumer_contract.ex"
      via: "SCORIA_HEX_UNPACK_ROOT skips duplicate hex.build"
      pattern: "SCORIA_HEX_UNPACK_ROOT"
---

<objective>
Wire tarball consumer proof through HostAppProof harness: required `dep_mode`, route-only runner filter, consumer test tarball assertions, and CI unpack-root reuse.

Purpose: Make merge-blocking adoption prove packaged artifact install → migrate → route smoke without repo-root `path:` dep.
Output: Updated Generator, Runner, `host_app_consumer_proof_test.exs`, and `ci-verify.yml` env wiring; green `mix test.adoption`.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/78-hex-consumer-contract-foundation/78-CONTEXT.md
@.planning/phases/78-hex-consumer-contract-foundation/78-RESEARCH.md
@.planning/phases/78-hex-consumer-contract-foundation/78-VALIDATION.md
@test/support/scoria/host_app_proof/generator.ex
@test/support/scoria/host_app_proof/runner.ex
@test/scoria/host_app_consumer_proof_test.exs
@lib/scoria/hex_consumer_contract.ex
@.github/workflows/ci-verify.yml
@lib/mix/tasks/test.adoption.ex

Depends on plans 78-01 and 78-02 for HexConsumerContract API and unpack cache. Preserve `@moduletag timeout: 180_000`, `async: false`, one phx.new host per run (D-20). Do not switch consumer test to `run_full_proof!/1` (Phase 79). Do not modify ci_policy_contract_test, adoption_surface_test, or VerificationLanes.closeout_order/0.
</context>

<tasks>

<task type="auto">
  <name>Task 78-03-01: Add required dep_mode to HostAppProof.Generator</name>
  <requirement_ids>HEX-CONSUMER-01</requirement_ids>
  <files>test/support/scoria/host_app_proof/generator.ex</files>
  <read_first>
    test/support/scoria/host_app_proof/generator.ex
    lib/scoria/hex_consumer_contract.ex
    .planning/phases/78-hex-consumer-contract-foundation/78-CONTEXT.md
  </read_first>
  <action>
Refactor `Scoria.TestSupport.HostAppProof.Generator.create_host!/1`:

1. Remove default `opts \\ []` — require keyword list argument.
2. At start: `dep_mode = Keyword.fetch!(opts, :dep_mode)`; validate `dep_mode in [:hex_tarball, :path]` else raise with message listing allowed atoms.
3. Replace `patch_mix_exs!(host_root, repo_root)` with `patch_mix_exs!(host_root, dep_mode: dep_mode, unpack_root: Keyword.get(opts, :unpack_root), repo_root: repo_root)`.
4. In `patch_mix_exs!/2` (rename to multi-clause or keyword opts):
   - `:path` → inject `{:scoria, path: repo_root}` (maintainer debug, D-27).
   - `:hex_tarball` → require `unpack_root` via `Keyword.fetch!/2`; inject `Scoria.HexConsumerContract.tarball_dep_tuple(unpack_root)` using same regex anchor `~r/(defp deps do\s*\n\s*\[)/`.
5. Keep `copy_overlay!`, router/runtime marker patching, and host struct fields unchanged.

Do not add `:hex_tarball` default. Only caller is consumer test — update in task 78-03-03 same plan.
  </action>
  <acceptance_criteria>
    - `rg -n 'Keyword\\.fetch!\\(opts, :dep_mode\\)' test/support/scoria/host_app_proof/generator.ex` returns match.
    - `rg -n 'tarball_dep_tuple' test/support/scoria/host_app_proof/generator.ex` returns :hex_tarball branch.
    - `rg -n 'create_host!\\(opts \\\\ \\[\\]\\)' test/support/scoria/host_app_proof/generator.ex` returns zero matches (no default opts).
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0 after consumer test updated in 78-03-03.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <done>Generator requires explicit dep_mode and patches tarball or repo path deps via HexConsumerContract.</done>
</task>

<task type="auto">
  <name>Task 78-03-02: Implement route-only filter in HostAppProof.Runner</name>
  <requirement_ids>HEX-CONSUMER-01</requirement_ids>
  <files>test/support/scoria/host_app_proof/runner.ex</files>
  <read_first>
    test/support/scoria/host_app_proof/runner.ex
    test/support/scoria/host_app_proof/generator.ex
    priv/host_app_proof/overlay/test/
    .planning/phases/78-hex-consumer-contract-foundation/78-CONTEXT.md
  </read_first>
  <action>
In `Scoria.TestSupport.HostAppProof.Runner`:

1. Add module attribute `@route_overlay_test "host_route_smoke_test.exs"`.
2. Refactor `smoke_pair!/1` to `smoke_pair!(host, overlay_files \\ host.overlay_tests)` — build `test_args` from provided overlay file list only.
3. Change `run_route_proof!/1` last step to `fn h -> smoke_pair!(h, [@route_overlay_test]) end` (pass module attribute value).
4. Change `run_full_proof!/1` last step to `&smoke_pair!/1` (all overlays — unchanged behavior for Phase 79).
5. Preserve step atom derivation: `:host_route_smoke_test` from filename root.

Expected `run_route_proof!/1` step list: `[:deps_get, :scoria_install, :ecto_create, :ecto_migrate, :host_route_smoke_test]` — must not include `:host_runtime_smoke_test` or `:host_handoff_smoke_test`.
  </action>
  <acceptance_criteria>
    - `rg -n '@route_overlay_test' test/support/scoria/host_app_proof/runner.ex` returns attribute.
    - `rg -n 'smoke_pair!\\(h, \\[@route_overlay_test\\]\\)' test/support/scoria/host_app_proof/runner.ex` returns route-only call in run_route_proof!.
    - `rg -n 'def run_full_proof!' test/support/scoria/host_app_proof/runner.ex` still invokes all overlays via `&smoke_pair!/1`.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <done>run_route_proof!/1 executes route overlay smoke only; run_full_proof!/1 unchanged for Phase 79.</done>
</task>

<task type="auto">
  <name>Task 78-03-03: Rewire host_app_consumer_proof_test for tarball route proof</name>
  <requirement_ids>HEX-CONSUMER-01, DOCS-HEX-01</requirement_ids>
  <files>test/scoria/host_app_consumer_proof_test.exs</files>
  <read_first>
    test/scoria/host_app_consumer_proof_test.exs
    test/support/scoria/host_app_proof/generator.ex
    test/support/scoria/host_app_proof/runner.ex
    lib/scoria/hex_consumer_contract.ex
    .planning/phases/78-hex-consumer-contract-foundation/78-CONTEXT.md
  </read_first>
  <action>
Rewrite `Scoria.HostAppConsumerProofTest`:

1. Keep `@moduletag timeout: 180_000` and `async: false`.
2. Add `setup_all` returning `{:ok, unpack_root: Scoria.HexConsumerContract.ensure_current_unpack_root!()}`.
3. Rename test to `"generated Phoenix host proves tarball adoption path"` (or equivalent tarball wording).
4. In test body: `host = Generator.create_host!(dep_mode: :hex_tarball, unpack_root: unpack_root, cleanup: &on_exit/1)`.
5. Read generated `mix.exs`:
   - `refute mix_exs =~ "{:scoria, path: #{inspect(host.repo_root)}}"` (D-18).
   - `assert mix_exs =~ Scoria.HexConsumerContract.tarball_dep_snippet(unpack_root)`.
6. Call `Runner.run_route_proof!(host)` (not `run_full_proof!/1`).
7. Assert `proof.steps == [:deps_get, :scoria_install, :ecto_create, :ecto_migrate, :host_route_smoke_test]`.

Remove old repo-root path assertion (`assert mix_exs =~ "{:scoria, path: "`) and full overlay step list.
  </action>
  <acceptance_criteria>
    - `rg -n 'setup_all' test/scoria/host_app_consumer_proof_test.exs` returns unpack_root setup.
    - `rg -n 'dep_mode: :hex_tarball' test/scoria/host_app_consumer_proof_test.exs` returns match.
    - `rg -n 'run_route_proof!' test/scoria/host_app_consumer_proof_test.exs` returns match.
    - `rg -n 'host_handoff_smoke_test|host_runtime_smoke_test' test/scoria/host_app_consumer_proof_test.exs` returns zero matches in step assertions.
    - `MIX_ENV=test mix test test/scoria/host_app_consumer_proof_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/host_app_consumer_proof_test.exs</automated>
  </verify>
  <done>Adoption consumer test proves tarball dep install chain with route-only overlay smoke.</done>
</task>

<task type="auto">
  <name>Task 78-03-04: Wire SCORIA_HEX_UNPACK_ROOT in ci-verify.yml test job</name>
  <requirement_ids>HEX-CONSUMER-01</requirement_ids>
  <files>.github/workflows/ci-verify.yml</files>
  <read_first>
    .github/workflows/ci-verify.yml
    lib/mix/tasks/scoria.release_preview.ex
    lib/scoria/hex_consumer_contract.ex
    .planning/phases/78-hex-consumer-contract-foundation/78-CONTEXT.md
  </read_first>
  <action>
In `.github/workflows/ci-verify.yml` `test` job, add to existing job-level `env:` block (lines ~75–81, alongside `MIX_ENV`, `SCORIA_DB_*`):

```yaml
SCORIA_HEX_UNPACK_ROOT: tmp/scoria-release-preview
```

This must apply to all steps after `Run release preview lane` (`MIX_ENV=dev mix scoria.release_preview`) so `mix test.adoption` and subsequent test steps reuse the unpack tree without a second `hex.build`. Value must match `Mix.Tasks.Scoria.ReleasePreview.release_preview_output_dir/0` (`"tmp/scoria-release-preview"`). Do **not** modify `ci_policy_contract_test.exs` or add workflow topology pins (D-23, Phase 82).
  </action>
  <acceptance_criteria>
    - `rg -n 'SCORIA_HEX_UNPACK_ROOT' .github/workflows/ci-verify.yml` returns `tmp/scoria-release-preview`.
    - `rg -n 'SCORIA_HEX_UNPACK_ROOT' .github/workflows/ci-verify.yml` appears under `test:` job `env:` block.
    - `MIX_ENV=test mix test test/scoria/verification_lanes_test.exs` exits 0 (closeout order unchanged).
    - `MIX_ENV=test mix test.adoption` exits 0 locally (Postgres required).
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test.adoption</automated>
  </verify>
  <done>CI test job reuses release preview unpack root; adoption lane green with tarball route proof.</done>
</task>

</tasks>

<verification>
After all tasks:

```bash
MIX_ENV=test mix test test/scoria/host_app_consumer_proof_test.exs
MIX_ENV=test mix test test/scoria/verification_lanes_test.exs
MIX_ENV=test mix test test/mix/tasks/test.adoption_test.exs
MIX_ENV=test mix test.adoption
# CI parity probe:
MIX_ENV=dev mix scoria.release_preview && SCORIA_HEX_UNPACK_ROOT=tmp/scoria-release-preview MIX_ENV=test mix test.adoption
```
</verification>

<success_criteria>
- Generated host `mix.exs` uses unpack-root tarball dep, not monorepo repo root.
- Adoption lane proves deps.get → scoria.install → ecto.create → ecto.migrate → route smoke only.
- CI avoids duplicate hex.build between release preview and test.adoption.
- VerificationLanes.closeout_order/0 unchanged; no new closeout lane.
</success_criteria>

<must_haves>
- `mix test.adoption` passes with tarball consumer proof.
- `run_full_proof!/1` still runs all three overlays for Phase 79 switch.
- HostInstallFixtures remains on repo `path:` (untouched).
- HEX-CONSUMER-01 remains partial until Phase 79 full overlay; DOCS-HEX-01 prose sweep deferred to Phase 82.
</must_haves>
