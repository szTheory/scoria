---
phase: 44-semantic-cache-contract-and-persistence
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - lib/scoria/runtime.ex
  - lib/scoria/workflows/runtime.ex
  - lib/scoria/semantic_cache.ex
  - test/scoria/runtime/semantic_fast_path_test.exs
autonomous: true
requirements:
  - FAST-01
  - SAFE-01
  - FAST-02
must_haves:
  truths:
    - "Eligible safe lanes attempt semantic lookup without bypassing Scoria's durable runtime/workflow seams."
    - "Cache miss or bypass always falls through to the normal runtime path without mutating workflow truth."
    - "Successful eligible completions can write back semantic cache state with origin lineage and reuse events."
  artifacts:
    - path: "lib/scoria/workflows/runtime.ex"
      provides: "Runtime fast-path orchestration and fallback behavior"
    - path: "test/scoria/runtime/semantic_fast_path_test.exs"
      provides: "Executable proof of fallback, partitioning, and write-back semantics"
  key_links:
    - from: "lib/scoria/workflows/runtime.ex"
      to: "lib/scoria/semantic_cache.ex"
      via: "lookup and admit calls"
      pattern: "SemanticCache\\."
    - from: "lib/scoria/workflows/runtime.ex"
      to: "lib/scoria/workflows.ex"
      via: "normal durable run path remains authoritative"
      pattern: "create_run|execute_step|metadata"
---

<objective>
Integrate semantic-cache lookup and write-back into the Scoria runtime path so explicitly safe lanes can reuse durable answers conservatively, while misses and bypasses continue through the normal workflow path unchanged.

Purpose: Phase 44 is only complete when the new contract and durable truth actually participate in runtime execution without weakening support truth.
Output: runtime integration changes, lifecycle write-back hooks, and targeted integration tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-semantic-cache-contract-and-persistence/44-CONTEXT.md
@.planning/phases/44-semantic-cache-contract-and-persistence/44-RESEARCH.md
@.planning/phases/44-semantic-cache-contract-and-persistence/44-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add conservative runtime lookup orchestration</name>
  <files>lib/scoria/runtime.ex, lib/scoria/workflows/runtime.ex, lib/scoria/semantic_cache.ex</files>
  <read_first>
    <file>lib/scoria/runtime.ex</file>
    <file>lib/scoria/workflows/runtime.ex</file>
    <file>lib/scoria/runtime/params.ex</file>
    <file>lib/scoria/semantic_cache.ex</file>
    <file>.planning/phases/44-semantic-cache-contract-and-persistence/44-CONTEXT.md</file>
  </read_first>
  <action>Wire semantic-cache evaluation into the Scoria-owned runtime path. Before normal execution of an explicit semantic lane, call the eligibility engine and then `Scoria.SemanticCache.lookup/1`. If lookup returns `{:hit, entry}`, short-circuit with a cached answer payload while recording a `reused` event and preserving origin lineage in runtime metadata. If lookup returns `:miss` or `{:bypass, reason_code}`, continue through the existing durable run/workflow path exactly as today. The integration must never attempt lookup when no semantic lane was supplied.</action>
  <acceptance_criteria>
    <criterion>`lib/scoria/workflows/runtime.ex` contains `SemanticCache.lookup`</criterion>
    <criterion>`lib/scoria/workflows/runtime.ex` contains `:miss`</criterion>
    <criterion>`lib/scoria/workflows/runtime.ex` contains `{:bypass,`</criterion>
    <criterion>`lib/scoria/workflows/runtime.ex` contains logic that keeps the normal path for miss or bypass outcomes</criterion>
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/runtime/semantic_fast_path_test.exs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Persist write-back and lifecycle lineage</name>
  <files>lib/scoria/workflows/runtime.ex, lib/scoria/semantic_cache.ex</files>
  <read_first>
    <file>lib/scoria/workflows/runtime.ex</file>
    <file>lib/scoria/knowledge.ex</file>
    <file>lib/scoria/knowledge/retrieval_run.ex</file>
    <file>lib/scoria/workflows/run.ex</file>
  </read_first>
  <action>After a normal successful response in an eligible lane, write back a semantic cache entry only when the response remained safe and reusable. Persist `origin_run_id`, any `origin_span_id`, and `origin_retrieval_run_id` when available. Record `writeback_rejected` events with stable reason codes when a lane started eligible but later runtime evidence proves the answer should not be admitted. Do not create semantic-cache rows for plain misses or bypasses.</action>
  <acceptance_criteria>
    <criterion>`lib/scoria/semantic_cache.ex` contains `record_writeback_rejection`</criterion>
    <criterion>`lib/scoria/semantic_cache.ex` contains `origin_run_id`</criterion>
    <criterion>`lib/scoria/semantic_cache.ex` contains `origin_retrieval_run_id`</criterion>
    <criterion>`lib/scoria/workflows/runtime.ex` contains `writeback_rejected`</criterion>
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/runtime/semantic_fast_path_test.exs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Add runtime integration proof lane</name>
  <files>test/scoria/runtime/semantic_fast_path_test.exs</files>
  <read_first>
    <file>test/scoria/runtime_integration_test.exs</file>
    <file>test/scoria/workflows/runtime_test.exs</file>
    <file>test/scoria/knowledge/retrieval_test.exs</file>
    <file>test/scoria/semantic_cache_test.exs</file>
  </read_first>
  <action>Create `test/scoria/runtime/semantic_fast_path_test.exs` that proves four behaviors with explicit assertions: runs without `semantic_cache` do not call the semantic fast path; eligible tenant-safe lanes attempt lookup and reuse only tenant-matching entries; approval-sensitive or personalized-tool-backed flows return bypass reason codes and still create the normal workflow run; and eligible misses fall through, then successful safe completions admit an entry plus lifecycle events with the correct `tenant_id`, `scope_kind`, and origin lineage fields.</action>
  <acceptance_criteria>
    <criterion>`test/scoria/runtime/semantic_fast_path_test.exs` contains `assert {:bypass, :approval_required}` or equivalent asserted bypass behavior</criterion>
    <criterion>`test/scoria/runtime/semantic_fast_path_test.exs` contains an assertion that a normal workflow run is still created after `:miss`</criterion>
    <criterion>`test/scoria/runtime/semantic_fast_path_test.exs` contains tenant-partition assertions for cached reuse</criterion>
    <criterion>`mix test test/scoria/runtime/semantic_fast_path_test.exs` exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/runtime/semantic_fast_path_test.exs</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime orchestration -> cache service | Scoria decides whether a request may bypass normal execution |
| Cache hit path -> workflow truth | Cached answers must not bypass durable evidence or mutate workflow truth incorrectly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-03-01 | Tampering | fallback contract | mitigate | Treat `:miss` and `{:bypass, code}` as strict fall-through signals to the existing runtime path. |
| T-44-03-02 | Information Disclosure | cache reuse | mitigate | Enforce tenant-first lookup and actor narrowing before returning a cached payload. |
| T-44-03-03 | Repudiation | hit/write-back lineage | mitigate | Persist `reused` and `writeback_rejected` events with origin run/span/retrieval references. |
</threat_model>

<verification>
- Runtime integration only evaluates explicit semantic lanes.
- Miss and bypass outcomes continue through the existing workflow path without mutating semantic-cache truth.
- Successful eligible completions persist reusable entries and lifecycle lineage.
</verification>

<success_criteria>
- The semantic fast path is explicit, conservative, and Scoria-owned.
- Fallback semantics remain support-truth-safe on miss and ineligible cases.
- Phase 45 can build compatibility and invalidation logic on top of stable runtime and persistence contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/44-semantic-cache-contract-and-persistence/44-03-SUMMARY.md`
</output>
