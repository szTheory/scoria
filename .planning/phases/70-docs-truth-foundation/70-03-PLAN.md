---
phase: 70-docs-truth-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 70-01-PLAN.md
  - 70-02-PLAN.md
files_modified:
  - docs/operator_verification.md
  - test/scoria/adoption_surface_test.exs
  - test/scoria/ci_policy_contract_test.exs
autonomous: true
requirements:
  - DOCS-03
  - DOCS-04
  - INST-DX-01
must_haves:
  truths:
    - "adoption_surface_test asserts capability nouns via AdopterDocContract and refutes milestone + maintainer commands on README."
    - "CI gate map documents semantic lane Not in PR CI and Version namespaces."
    - "Operator guide documents mix scoria.test.install_contract under maintainer installer proofs."
  artifacts:
    - path: docs/operator_verification.md
      provides: "Maintainer CI gate map matrix and version namespace explanation"
    - path: test/scoria/ci_policy_contract_test.exs
      provides: "Executable gate-map string contract"
  key_links:
    - from: test/scoria/adoption_surface_test.exs
      to: lib/scoria/adopter_doc_contract.ex
      via: imported noun/refute lists
---

<objective>
Lock docs truth with maintainer CI gate map additions, operator-only install_contract documentation, and executable policy tests for README + operator guide contracts.

Purpose: DOCS-04, complete DOCS-03/INST-DX-01 test enforcement.
Output: Green adoption_surface + ci_policy_contract tests; ROADMAP success criteria 3–5 met.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/70-docs-truth-foundation/70-CONTEXT.md
@lib/scoria/adopter_doc_contract.ex
@docs/operator_verification.md
@test/scoria/adoption_surface_test.exs
@test/scoria/ci_policy_contract_test.exs
@lib/scoria/verification_lanes.ex
</context>

<tasks>

<task type="auto">
  <name>Task 70-03-01: Extend operator_verification CI gate map and install_contract section</name>
  <requirement_ids>DOCS-04, INST-DX-01</requirement_ids>
  <files>docs/operator_verification.md</files>
  <read_first>
    docs/operator_verification.md
    .planning/phases/70-docs-truth-foundation/70-CONTEXT.md
    lib/scoria/verification_lanes.ex
    .github/workflows/ci.yml
  </read_first>
  <action>
In `docs/operator_verification.md` under `### CI gate map (maintainers)`:

1. After the numbered **Test job closeout** list (before **Local parity**), insert markdown table **Verification lanes in PR CI** with columns: Lane | Command | In PR CI? | Notes

Rows minimum:
| Default runtime | mix test.adoption | Yes | PR closeout lane 3 |
| Runtime-to-handoff | mix test.runtime_to_handoff | Yes | PR closeout lane 3 |
| Semantic fast-path | mix test.semantic_fast_path | Not in PR CI | Local maintainer command; link to `#semantic-fast-path-troubleshooting-lane`; SEM-CI-01 deferred; semantic tests still run via full-suite WAE |
| Optional knowledge | mix test.knowledge | Yes | After full-suite WAE |

2. Immediately under the table, add **Version namespaces** (D-07) two sentences:
   - Hex/git releases use semver (`0.1.0`, `v0.1.0`, `{:scoria, "~> 0.1"}`).
   - Planning milestones (`v2.x` in `.planning/`) are internal shipped-work tranches, not a second installable version axis.

3. Add section **Installer contract proofs (maintainers)** (before or after gate map — maintainer area) documenting:
   - `mix scoria.test.install_contract` (and `mix test.install_contract` alias)
   - Purpose: deep installer contract proofs (report, mode_equivalence, install, install_check, planner tests)
   - Explicit: not a PR CI step, not in adoption closeout, not in README

4. Update semantic troubleshooting subsection (~L70–76): replace `v2.1` adopter phrasing with capability wording; keep env vars and command.

5. Update maintainer closeout bullets that say `canonical v2.1 semantic` → capability wording (D-06). SEM-CI-01 may appear only in gate map Notes.

Do not edit `.github/workflows/ci.yml` or `VerificationLanes`.
  </action>
  <acceptance_criteria>
    - `rg 'Verification lanes in PR CI' docs/operator_verification.md` matches
    - `rg 'Not in PR CI' docs/operator_verification.md` matches
    - `rg 'Version namespaces' docs/operator_verification.md` matches
    - `rg 'mix scoria.test.install_contract' docs/operator_verification.md` matches
    - `rg 'Installer contract proofs' docs/operator_verification.md` matches
    - `rg 'mix test.semantic_fast_path' docs/operator_verification.md` matches
  </acceptance_criteria>
  <verify>
    <automated>rg 'Version namespaces' docs/operator_verification.md && rg 'Not in PR CI' docs/operator_verification.md</automated>
  </verify>
  <done>Operator guide documents gate map matrix, version namespaces, install_contract.</done>
</task>

<task type="auto">
  <name>Task 70-03-02: Migrate adoption_surface_test and extend ci_policy_contract_test</name>
  <requirement_ids>DOCS-03, DOCS-04, INST-DX-01</requirement_ids>
  <files>
    test/scoria/adoption_surface_test.exs
    test/scoria/ci_policy_contract_test.exs
  </files>
  <read_first>
    test/scoria/adoption_surface_test.exs
    test/scoria/ci_policy_contract_test.exs
    lib/scoria/adopter_doc_contract.ex
    README.md
    docs/operator_verification.md
  </read_first>
  <action>
**adoption_surface_test.exs:**

1. Add `alias Scoria.AdopterDocContract`.

2. Replace or split test `"README documents the shipped lane model..."`:
   - Remove `assert content =~ "Scoria is shipped through \`v2.1 Tenant-scoped semantic fast path\`"`
   - Add new test `"README shipped truth is capability-based"`:
     - For each noun in `AdopterDocContract.shipped_capability_nouns/0`, assert README contains it (case-insensitive OK)
     - For each marker in `AdopterDocContract.upgrade_safe_install_markers/0`, assert README contains it
     - For each refute in `AdopterDocContract.milestone_banner_refutes/0` + `readme_maintainer_command_refutes/0`, `refute content =~ pattern` on README only

3. Keep existing `VerificationLanes.command/1` asserts for adoption, runtime_to_handoff, semantic, knowledge in README.

4. Add test (or extend operator guide test) that `docs/operator_verification.md` contains `mix scoria.test.install_contract` and does NOT require it in README (refute already covers README).

**ci_policy_contract_test.exs:**

Extend `"CI-03 documents CI gate map for maintainers"` or add dedicated test asserting operator_docs contains:
- `Not in PR CI`
- `mix test.semantic_fast_path`
- `Version namespaces`
- `mix scoria.test.install_contract`

Do not add install_contract to CI workflow assertions.
  </action>
  <acceptance_criteria>
    - `MIX_ENV=test mix test test/scoria/adoption_surface_test.exs` exits 0
    - `MIX_ENV=test mix test test/scoria/ci_policy_contract_test.exs` exits 0
    - `rg 'Scoria is shipped through' test/scoria/adoption_surface_test.exs` returns no positive assert lines
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/adoption_surface_test.exs test/scoria/ci_policy_contract_test.exs</automated>
  </verify>
  <done>Executable contracts enforce capability README and maintainer gate map.</done>
</task>

</tasks>

<threat_model>
## Security Gate

| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-70-04 | Policy test false green if gate map table malformed | low | Substring asserts on required labels |

No high-severity threats.
</threat_model>

<verification>
- [ ] `mix test test/scoria/adoption_surface_test.exs` green
- [ ] `mix test test/scoria/ci_policy_contract_test.exs` green
- [ ] Policy job lane-contract WAE files still listed in ci.yml (unchanged)
</verification>

<success_criteria>
ROADMAP Phase 70 success criteria 3–5 satisfied.
</success_criteria>

## PLANNING COMPLETE
