---
phase: 70-docs-truth-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/adopter_doc_contract.ex
  - lib/mix/tasks/scoria.test.install_contract.ex
  - mix.exs
  - test/mix/tasks/test.install_contract_test.exs
autonomous: true
requirements:
  - INST-DX-01
  - DOCS-03
must_haves:
  truths:
    - "Scoria.AdopterDocContract exports noun lists and README refute patterns for downstream tests."
    - "mix scoria.test.install_contract and mix test.install_contract run the 5-file maintainer bundle."
    - "install_contract is not in VerificationLanes closeout or adoption discoverability."
  artifacts:
    - path: lib/scoria/adopter_doc_contract.ex
      provides: "Adopter doc SSOT for capability nouns and milestone refutes"
    - path: test/mix/tasks/test.install_contract_test.exs
      provides: "Lane boundary parity test for install_contract"
  key_links:
    - from: test/mix/tasks/test.install_contract_test.exs
      to: lib/mix/tasks/scoria.test.install_contract.ex
      via: install_contract_test_files/0 parity
---

<objective>
Establish executable SSOT for adopter doc contracts and commit the maintainer-only `install_contract` verification lane with Mix CLI registration and boundary tests.

Purpose: Wave-1 foundation so README/doc edits and adoption_surface_test can consume one module (INST-DX-01, DOCS-03 harness).
Output: Green `test.install_contract_test.exs`; `mix scoria.test.install_contract` runnable in `:test`.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/70-docs-truth-foundation/70-CONTEXT.md
@.planning/phases/70-docs-truth-foundation/70-RESEARCH.md
@lib/scoria/verification_lanes.ex
@lib/mix/tasks/test.adoption.ex
@lib/mix/tasks/scoria.test.install_contract.ex
@test/mix/tasks/test.adoption_test.exs
@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 70-01-01: Add Scoria.AdopterDocContract SSOT module</name>
  <requirement_ids>DOCS-03</requirement_ids>
  <files>lib/scoria/adopter_doc_contract.ex</files>
  <read_first>
    lib/scoria/verification_lanes.ex
    .planning/phases/70-docs-truth-foundation/70-CONTEXT.md
    test/scoria/adoption_surface_test.exs
  </read_first>
  <action>
Create `lib/scoria/adopter_doc_contract.ex` with `@moduledoc` noting adopter README/support-doc contract (not maintainer CI commands).

Export these functions returning lists of strings (lowercase matching is OK in tests via `String.downcase/1` if needed):

1. `shipped_capability_nouns/0` — must include substrings for:
   - default runtime (accept `"default runtime"` or `"default runtime lane"`)
   - bounded handoff
   - semantic fast path
   - optional knowledge
   - upgrade-safe install (or upgrade-safe)

2. `upgrade_safe_install_markers/0` — exact markers:
   - `mix scoria.install --check`
   - `mix scoria.install --dry-run`

3. `milestone_banner_refutes/0` — patterns README must NOT contain:
   - `Scoria is shipped through`
   - `shipped through \`v` (regex-friendly: use separate literal `shipped through \`v` entry)

4. `readme_maintainer_command_refutes/0` — literals from CONTEXT D-18:
   - `mix scoria.test.install_contract`
   - `mix test.install_contract`
   - `mix scoria.test.ci_trust`
   - `mix scoria.warning_ratchet`
   - `mix scoria.warning_inventory`
   - `mix scoria.warning_baseline`
   - `mix scoria.eval`
   - `mix scoria.milestone`
   - `mix scoria.test.knowledge`

Do not import Mix or File — data-only module.
  </action>
  <acceptance_criteria>
    - `rg 'def shipped_capability_nouns' lib/scoria/adopter_doc_contract.ex` matches
    - `rg 'mix scoria.install --check' lib/scoria/adopter_doc_contract.ex` matches
    - `rg 'Scoria is shipped through' lib/scoria/adopter_doc_contract.ex` matches inside refute list
    - `MIX_ENV=test mix compile` exits 0
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix compile</automated>
  </verify>
  <done>AdopterDocContract compiles and exports all four function groups.</done>
</task>

<task type="auto">
  <name>Task 70-01-02: Register install_contract lane and add boundary test</name>
  <requirement_ids>INST-DX-01</requirement_ids>
  <files>
    lib/mix/tasks/scoria.test.install_contract.ex
    mix.exs
    test/mix/tasks/test.install_contract_test.exs
  </files>
  <read_first>
    lib/mix/tasks/scoria.test.install_contract.ex
    lib/mix/tasks/test.adoption.ex
    test/mix/tasks/test.adoption_test.exs
    mix.exs
    lib/scoria/verification_lanes.ex
  </read_first>
  <action>
Ensure `lib/mix/tasks/scoria.test.install_contract.ex` is committed as-is (5 files per CONTEXT D-22):
- test/scoria/install/report_test.exs
- test/scoria/install/mode_equivalence_test.exs
- test/mix/tasks/scoria.install_test.exs
- test/mix/tasks/scoria.install_check_test.exs
- test/scoria/install/planner_test.exs

In `mix.exs` `cli/0` `preferred_envs`, add:
- `"scoria.test.install_contract": :test`
- `"test.install_contract": :test`

Create `test/mix/tasks/test.install_contract_test.exs` mirroring `test.adoption_test.exs`:
- Assert `Mix.Tasks.Scoria.Test.InstallContract.install_contract_test_files/0` equals the 5 paths above in order
- `Mix.Task.get("scoria.test.install_contract")` and `Mix.Task.get("test.install_contract")` non-nil
- `refute VerificationLanes.command(:adoption) == "mix scoria.test.install_contract"`
- `refute :install_contract in VerificationLanes.ids()` (or equivalent: install_contract not a lane id)
- `refute Enum.member?(VerificationLanes.closeout_order(), :install_contract)` if closeout_order is only atoms — use `refute Enum.any?(VerificationLanes.closeout_order(), &(&1 == :install_contract))`

Optional: `mix scoria.test.install_contract --help` or shortdoc assert if other lane tests do.
  </action>
  <acceptance_criteria>
    - `rg 'scoria.test.install_contract' mix.exs` matches
    - `MIX_ENV=test mix test test/mix/tasks/test.install_contract_test.exs` exits 0
    - `MIX_ENV=test mix scoria.test.install_contract --max-failures 1` starts ExUnit (exit 0 or test failures only from env, not Unknown task)
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/mix/tasks/test.install_contract_test.exs</automated>
  </verify>
  <done>install_contract discoverable, preferred_envs set, not a closeout lane.</done>
</task>

</tasks>

<threat_model>
## Security Gate

- Enforcement: enabled
- ASVS level: 1
- Block threshold: high

| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-70-01 | Widening install_contract into CI closeout | low | Explicit refute in boundary test; no ci.yml edits |
| T-70-02 | Maintainer commands exposed in README | low | refute list consumed in plan 03 adoption_surface_test |

No high-severity threats.
</threat_model>

<verification>
- [ ] `Scoria.AdopterDocContract` compiles
- [ ] `test.install_contract_test.exs` green
- [ ] install_contract absent from `VerificationLanes.closeout_order/0`
</verification>

<success_criteria>
1. INST-DX-01 lane machinery committed and test-guarded
2. AdopterDocContract ready for README policy tests in plan 03
</success_criteria>

## PLANNING COMPLETE
