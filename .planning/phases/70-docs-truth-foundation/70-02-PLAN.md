---
phase: 70-docs-truth-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 70-01-PLAN.md
files_modified:
  - README.md
  - docs/semantic_fast_path.md
autonomous: true
requirements:
  - DOCS-03
must_haves:
  truths:
    - "README intro uses capability headline and five bullets; no milestone shipped-through lines."
    - "README Install documents dry-run/check/apply upgrade path with operator guide deep link."
    - "README Status is a short pre-Hex publish note without milestone codenames."
    - "semantic_fast_path.md uses capability wording for the troubleshooting lane, not v2.1 as shipped label."
  artifacts:
    - path: README.md
      provides: "Adopter-facing capability truth and upgrade-safe install guidance"
  key_links:
    - from: README.md
      to: docs/operator_verification.md
      via: "Installer verification modes (upgrade-safe)" anchor link
---

<objective>
Rewrite adopter-facing README and semantic fast-path guide copy to capability-based shipped truth and document the upgrade-safe install path without duplicating operator SSOT.

Purpose: DOCS-03 adopter doc alignment before policy tests lock strings in plan 03.
Output: README free of `Scoria is shipped through` / `v2.1` milestone banner language.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/70-docs-truth-foundation/70-CONTEXT.md
@lib/scoria/adopter_doc_contract.ex
@README.md
@docs/semantic_fast_path.md
@docs/operator_verification.md
</context>

<tasks>

<task type="auto">
  <name>Task 70-02-01: Replace README shipped banner and collapse Status</name>
  <requirement_ids>DOCS-03</requirement_ids>
  <files>README.md</files>
  <read_first>
    README.md
    lib/scoria/adopter_doc_contract.ex
    .planning/phases/70-docs-truth-foundation/70-CONTEXT.md
    prompts/scoria-brand-book-deep-research.md
  </read_first>
  <action>
In `README.md`:

1. **Remove** lines matching milestone shipped-through (currently ~L13–L18 area):
   - Delete `Scoria is shipped through \`v2.1 Tenant-scoped semantic fast path\`.`
   - Delete the old four-lane bullet list that duplicates Choose Your Lane without upgrade-safe install.

2. **Insert** after the intro paragraph (before `## Who This Is For`) a capability block:
   - One headline sentence: Phoenix-native runtime with **start with the default runtime**, add lanes only when needed (paraphrase D-02).
   - Five bullets (D-03), each bold label + short clause:
     - **Default runtime** — durable runs, approvals, operator evidence
     - **Bounded handoff** — narrow same-run delegation, projected context, visible lineage
     - **Semantic fast path** — opt-in, tenant-partitioned reuse for explicitly safe read-only work
     - **Optional knowledge** — pgvector retrieval/grounding when chosen
     - **Upgrade-safe install** — `mix scoria.install` with plan/check/apply paths
   - Do NOT embed `mix test.adoption` or lane verifier commands in these bullets (D-04).

3. Keep the following paragraph about "Start with the default runtime lane" and `mix test.adoption` / `mix test.runtime_to_handoff` in Verification territory (may stay after banner or under Choose Your Lane — do not delete lane proof commands from Verification sections).

4. **Replace `## Status` section** (~L220+) with a short pre-Hex publish note (D-05):
   - Hex package metadata is ready; first publish will be `0.1.0` from a tagged GitHub release (`v0.1.0`).
   - No milestone codenames, no duplicate capability bullet list.

5. In Verification section (~L203), change `validating \`v2.1\` semantic` → capability wording e.g. `semantic fast-path` behavior.

Run `rg 'shipped through' README.md` — must return no matches.
  </action>
  <acceptance_criteria>
    - `rg 'shipped through' README.md` returns no matches
    - `rg 'default runtime' README.md` matches
    - `rg 'upgrade-safe install|Upgrade-safe install' README.md` matches
    - `rg 'bounded handoff' README.md` matches
    - `rg 'semantic fast path' README.md` matches
    - `rg 'optional knowledge' README.md` matches
    - `rg 'v2\.1 Tenant-scoped' README.md` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>rg 'shipped through' README.md; test $? -ne 0</automated>
  </verify>
  <done>README banner and Status are capability-based; milestone banner absent.</done>
</task>

<task type="auto">
  <name>Task 70-02-02: Add README Install upgrade subsection and fix semantic guide copy</name>
  <requirement_ids>DOCS-03</requirement_ids>
  <files>
    README.md
    docs/semantic_fast_path.md
  </files>
  <read_first>
    README.md
    docs/semantic_fast_path.md
    docs/operator_verification.md
    .planning/phases/70-docs-truth-foundation/70-CONTEXT.md
  </read_first>
  <action>
**README Install** (after Tailwind-optional paragraph, before `## Quickstart`):

Add `### Upgrading or re-running install` with:
- Ordered list (4 steps, no code fence): `--dry-run` → `--check` → remediate `manual_review` → `mix scoria.install` apply (D-11)
- Three guardrail bullets (D-12): no-write modes; `manual_review` never silently overwritten; apply blocks if managed files drift between check and apply
- Single link: `[Installer verification modes (upgrade-safe)](docs/operator_verification.md#installer-verification-modes-upgrade-safe)` (D-13)
- Do NOT add `mix scoria.test.install_contract`, exit-code tables, or `SCORIA_CHECK_RESULT` format (D-14)

**docs/semantic_fast_path.md:**
- Change title/intro references from "`v2.1` semantic fast path" to capability phrasing (e.g. "Semantic fast path" / "tenant-scoped semantic fast path").
- Replace "canonical `v2.1` troubleshooting lane" with "canonical semantic fast-path troubleshooting lane" or similar.
- Replace "shipped `v2.1` scope" with capability-scoped language.
- Keep `mix test.semantic_fast_path` command blocks intact.
- Maintainer deferral names (SEM-CI-01) stay out of this file — operator guide only.

Do not edit `docs/adoption_lanes.md` except if a stray `v2.1` shipped-through line exists (grep first).
  </action>
  <acceptance_criteria>
    - `rg 'Upgrading or re-running install' README.md` matches
    - `rg 'mix scoria.install --check' README.md` matches
    - `rg 'installer-verification-modes-upgrade-safe' README.md` matches
    - `rg 'mix scoria.test.install_contract' README.md` returns no matches
    - `rg 'v2\.1' docs/semantic_fast_path.md` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>rg 'Upgrading or re-running install' README.md && ! rg 'mix scoria.test.install_contract' README.md</automated>
  </verify>
  <done>Install upgrade path documented; semantic guide free of milestone codenames.</done>
</task>

</tasks>

<threat_model>
## Security Gate

| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-70-03 | Overclaiming production-ready semantic/knowledge | low | Keep opt-in language per CONTEXT specifics |

No high-severity threats.
</threat_model>

<verification>
- [ ] README has no `shipped through` strings
- [ ] Install upgrade subsection present with operator guide link
- [ ] semantic_fast_path.md has no `v2.1` strings
</verification>

<success_criteria>
ROADMAP Phase 70 success criteria 1–2 satisfied at doc level.
</success_criteria>

## PLANNING COMPLETE
