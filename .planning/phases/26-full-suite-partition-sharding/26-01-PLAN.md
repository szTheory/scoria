---
phase: 26-full-suite-partition-sharding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci-verify.yml
  - test/test_helper.exs
  - test/scoria/ci_policy_contract_test.exs
  - test/scoria/verification_lanes_test.exs
  - docs/MAINTAINERS.md
  - docs/operator_verification.md
  - README.md
autonomous: true
requirements: [SHARD-01]

must_haves:
  truths:
    - "The full ExUnit suite runs as `mix test --warnings-as-errors --partitions 4` across a 4-way GHA matrix job (full-suite), each leg exporting MIX_TEST_PARTITION from matrix.partition"
    - "Each shard uses an isolated DB scoria_test{k} because SCORIA_DB_NAME is absent from full-suite: env (config/test.exs || fallback fires); config/test.exs is unchanged"
    - "The final `mix test --warnings-as-errors` step is gone from the test: job; the test: closeout chain (release_preview → adoption → runtime_to_handoff → semantic_fast_path) still runs once"
    - "full-suite is wired into verify-summary.needs; verify-summary keeps if: always() and the name-agnostic skipped=fail loop; no continue-on-error anywhere"
    - "An after_suite guard in test_helper.exs (gated on MIX_TEST_PARTITION) exits non-zero when a partition runs 0 tests"
    - "The three pre-existing contract tests that asserted `run: mix test --warnings-as-errors` inside test_body are corrected and green; the lane-contract WAE command stays green"
    - "MAINTAINERS.md / operator_verification.md / README topology lines name full-suite, committed in the same change"
  artifacts:
    - path: ".github/workflows/ci-verify.yml"
      provides: "full-suite: 4-way matrix job; test: job with the WAE step removed; verify-summary.needs including full-suite"
      contains: "full-suite:"
    - path: "test/test_helper.exs"
      provides: "after_suite zero-test partition guard gated on MIX_TEST_PARTITION"
      contains: "MIX_TEST_PARTITION"
    - path: "test/scoria/ci_policy_contract_test.exs"
      provides: "B-1/B-2 fixes + D-03 structural coverage-proof asserts + D-04 fan-in pin + U-1/U-2/U-3 updates"
      contains: "full-suite"
    - path: "test/scoria/verification_lanes_test.exs"
      provides: "B-3 fix (remove stale WAE-in-test_body assert; add cross-workflow --partitions 4 assert)"
      contains: "--partitions 4"
    - path: "docs/MAINTAINERS.md"
      provides: "full-suite (k/4) gate-map row + topology line + narrative + failure-diagnosis row"
      contains: "full-suite"
  key_links:
    - from: ".github/workflows/ci-verify.yml full-suite: env"
      to: "config/test.exs database fallback"
      via: "absence of SCORIA_DB_NAME + MIX_TEST_PARTITION job-level env"
      pattern: "MIX_TEST_PARTITION: \\$\\{\\{ matrix.partition \\}\\}"
    - from: ".github/workflows/ci-verify.yml verify-summary.needs"
      to: "full-suite job result aggregation"
      via: "inline needs list"
      pattern: "needs:\\s*\\[.*full-suite.*\\]"
    - from: "test/scoria/ci_policy_contract_test.exs"
      to: ".github/workflows/ci-verify.yml full-suite: block"
      via: "job_blocks/1 + Map.fetch!(blocks, \"full-suite\")"
      pattern: "Map.fetch!\\(.*\"full-suite\"\\)"
---

<objective>
Shard the full ExUnit suite across a 4-way GitHub Actions matrix (`full-suite:`) on the shared
`build-test-env` artifact, with per-shard DB isolation keyed by `MIX_TEST_PARTITION`, zero
coverage loss (proven by partition math + structural contract + an `after_suite` zero-test guard),
and a single `verify-summary` fan-in — satisfying SHARD-01 / ROADMAP Phase 26 SC#1–SC#4.

This is a surgical YAML + test-file + docs edit. The non-obvious work: moving the
`mix test --warnings-as-errors` step OUT of `test:` turns THREE existing contract assertions RED
(B-1, B-2 in `ci_policy_contract_test.exs`; B-3 in `verification_lanes_test.exs`). All of it — the
YAML move, the new `full-suite:` job, the fan-in wiring, the `after_suite` guard, the contract-test
fixes, and the docs — MUST land in ONE atomic commit so the policy lane (which runs those contract
tests with WAE) never goes red mid-change.

Purpose: cut full-suite wall-clock ~4× without weakening the verification bar (SEED-003 / v3.1).
Output: a sharded `full-suite:` job, a coverage-loss-proof contract + runtime guard, and
docs-as-contract updates — all green under the local lane-contract command.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-full-suite-partition-sharding/26-CONTEXT.md
@.planning/phases/26-full-suite-partition-sharding/26-RESEARCH.md
@.planning/phases/26-full-suite-partition-sharding/26-PATTERNS.md

<interfaces>
<!-- Load-bearing facts the executor needs. Do NOT re-derive; do NOT change config/test.exs. -->

config/test.exs (lines 8-10) — DO NOT CHANGE (SC#1):
  database:
    System.get_env("SCORIA_DB_NAME") ||
      "scoria_test#{System.get_env("MIX_TEST_PARTITION")}"
  → When SCORIA_DB_NAME is absent (nil) and MIX_TEST_PARTITION=2, resolves "scoria_test2".
    Dropping SCORIA_DB_NAME from full-suite: env is what ACTIVATES the partition DB path.

Mix partitioning (Elixir 1.19.5): there is NO `--partition N` flag. Each leg runs the IDENTICAL
  `mix test --partitions 4`; the MIX_TEST_PARTITION env (set job-level) selects which leg.
  filter_by_partition/3 = sort |> with_index |> filter rem(i, total) == partition-1 → the 4
  partitions {1,2,3,4} cover residues {0,1,2,3} → union == full suite BY CONSTRUCTION.

job_blocks/1 (ci_policy_contract_test.exs lines 537-567): parses `jobs:` into %{name => body}.
  Map.fetch!(blocks, "full-suite") RAISES (not silent "") if the key is absent — anti-vacuous-pass.

verify-summary.needs parser regex (line 285): ~r/needs:\s*\[([^\]]+)\]/ — matches ONLY the inline
  [..] list form. Keep verify-summary.needs as an inline list; never multi-line.

ExUnit.after_suite/1 signature (test_helper.exs line 28): `fn %{total: total} -> ... end`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the full-suite: matrix job, move the WAE step out of test:, wire the fan-in, and add the after_suite zero-test guard</name>
  <files>.github/workflows/ci-verify.yml, test/test_helper.exs</files>
  <read_first>
    - .github/workflows/ci-verify.yml (FULL — the file being modified; analog jobs: knowledge: lines 217-274, connector: lines 277-337; test: lines 102-185; verify-summary: lines 339-356)
    - test/test_helper.exs (FULL — the file being modified; analog: Layer 2 knowledge guard lines 24-34)
    - .planning/phases/26-full-suite-partition-sharding/26-PATTERNS.md (§ "new full-suite: matrix job" lines 24-151; § "after_suite zero-test partition guard" lines 391-430)
    - .planning/phases/26-full-suite-partition-sharding/26-RESEARCH.md (§ "New full-suite: Job Structure" lines 339-441; VF-1, VF-4, VF-5; Pitfalls 1-5)
  </read_first>
  <action>
    In `.github/workflows/ci-verify.yml`:

    (a) REMOVE the `- name: Run tests` step (`run: mix test --warnings-as-errors`) at lines 184-185
    from the `test:` job. The `test:` job now ends at the semantic fast-path step (`mix
    test.semantic_fast_path --warnings-as-errors`, lines 181-182). Do NOT touch the closeout chain
    (release_preview, adoption, runtime_to_handoff, semantic) — it stays and runs once (D-01).
    Update the stale `test:` intent comment at line 102 to note the full-suite WAE moved out
    (e.g. ending with `… → semantic (full-suite WAE moved to full-suite: matrix job)`) per RESEARCH OQ-3.

    (b) ADD a new `full-suite:` job positioned AFTER `connector:` (ends ~line 337) and BEFORE
    `verify-summary:` (starts line 339) — this keeps the `connector_body` slice boundary in
    `verification_lanes_test.exs` clean (RESEARCH Pitfall 7 / OQ-1). Copy the `knowledge:` job
    preamble verbatim, then apply these differences (D-01, D-02):
      - intent comment line `# full-suite: sharded full ExUnit suite (WAE) — 4-way matrix on shared build artifact`
      - `name: "full-suite (${{ matrix.partition }}/${{ strategy.job-total }})"` above `runs-on:`
      - `needs: build`
      - a `strategy:` block: `fail-fast: false` and `matrix:` → `partition: [1, 2, 3, 4]` (NEVER `continue-on-error`)
      - keep the Postgres `services:` block, but change the health-cmd to `pg_isready -U postgres`
        (DROP `-d scoria_test` — the partition DB does not exist at health-check time, VF-5).
        `POSTGRES_DB` may stay `scoria_test` (sibling-parity, planner discretion — least-surprising diff).
      - job-level `env:` keeps `MIX_ENV: test` + the four `SCORIA_DB_HOST/PORT/USERNAME/PASSWORD`
        values identical to siblings; ADD `MIX_TEST_PARTITION: ${{ matrix.partition }}` at JOB level
        (never per-step — Pitfall 2); OMIT `SCORIA_DB_NAME` entirely (absence is load-bearing,
        Pitfall 1); OMIT `SCORIA_HEX_UNPACK_ROOT` (test:-specific).
      - steps: checkout → setup-beam (`version-file: .tool-versions`, `version-type: strict`) →
        download-artifact `build-test-env` → `tar -xzf build-test-env.tar.gz` → `mix deps.get`.
      - the DB-prep block MUST be BYTE-IDENTICAL to the siblings: the `# DB-prep: keep in sync with
        sibling parallel jobs` marker comment plus the literal lines `mix ecto.create`,
        `mix ecto.migrate --to 20260511000300`, `mix eval 'Scoria.TestSupport.Migrations.migrate_knowledge!()'`,
        `mix ecto.migrate` (the contract greps depend on these exact strings).
      - final step: `run: mix test --warnings-as-errors --partitions 4` (NO `--partition N` — VF-3 / Pitfall 3).

    (c) UPDATE `verify-summary.needs` (line 342) from `[policy, build, test, ratchet, knowledge,
    connector]` to `[policy, build, test, ratchet, knowledge, connector, full-suite]`. Keep it as an
    INLINE list (Pitfall 4 — the contract regex only matches `[...]`). Do NOT remove `if: always()`
    and do NOT alter the `join(needs.*.result, ' ')` skipped=fail loop (D-04, CodeQL #712 false-green).

    (d) ALSO update the header topology comment at ci-verify.yml line 9 to read
    `{ test, ratchet, knowledge, connector, full-suite }` for reader consistency (RESEARCH U-4).

    In `test/test_helper.exs`: AFTER the existing Layer 2 knowledge guard block (after line 34),
    add a partition zero-test guard gated on `System.get_env("MIX_TEST_PARTITION")` (bare truthy
    check — the value is `"1".."4"`, nil locally). Inside, `ExUnit.after_suite(fn %{total: total} ->`
    and when `total == 0`, write a stderr diagnostic that includes the partition number, then
    `exit({:shutdown, 1})` (D-03 Layer 3 / VF-4). It must be placed after `ExUnit.start/1` (line 22)
    and must NOT fire locally or in lane runs (MIX_TEST_PARTITION is absent there).

    Do NOT edit `config/test.exs`, `.github/workflows/ci.yml` job structure, or branch protection.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/scoria && grep -q 'full-suite:' .github/workflows/ci-verify.yml && grep -q 'mix test --warnings-as-errors --partitions 4' .github/workflows/ci-verify.yml && grep -q 'MIX_TEST_PARTITION: ${{ matrix.partition }}' .github/workflows/ci-verify.yml && grep -q 'partition: \[1, 2, 3, 4\]' .github/workflows/ci-verify.yml && grep -q 'fail-fast: false' .github/workflows/ci-verify.yml && ! grep -q 'continue-on-error' .github/workflows/ci-verify.yml && grep -q 'needs: \[policy, build, test, ratchet, knowledge, connector, full-suite\]' .github/workflows/ci-verify.yml && grep -q 'if: always()' .github/workflows/ci-verify.yml && grep -c 'mix ecto.migrate --to 20260511000300' .github/workflows/ci-verify.yml | grep -qx 4 && grep -q "if System.get_env(\"MIX_TEST_PARTITION\")" test/test_helper.exs && echo OK_T1</automated>
  </verify>
  <acceptance_criteria>
    - `ci-verify.yml` contains a `full-suite:` job with `needs: build`, `strategy.fail-fast: false`, `matrix.partition: [1, 2, 3, 4]`, `MIX_TEST_PARTITION: ${{ matrix.partition }}` at job-level env, and a final step `mix test --warnings-as-errors --partitions 4`.
    - `ci-verify.yml` `full-suite:` block does NOT contain `SCORIA_DB_NAME` and its health-cmd is `pg_isready -U postgres` (no `-d scoria_test`).
    - `ci-verify.yml` `test:` job no longer contains a `run: mix test --warnings-as-errors` step (only the `--partitions 4` form survives, inside full-suite:); the semantic fast-path step is the test: job's last lane step.
    - `ci-verify.yml` `mix ecto.migrate --to 20260511000300` appears exactly 4 times (test, knowledge, connector, full-suite — DB-prep byte-identical) and the `# DB-prep: keep in sync with sibling parallel jobs` marker is present in full-suite:.
    - `ci-verify.yml` `verify-summary.needs` is the inline list including `full-suite`; `if: always()` retained; `continue-on-error` absent from the whole file.
    - `test/test_helper.exs` has an `after_suite` guard gated on `System.get_env("MIX_TEST_PARTITION")` that `exit({:shutdown, 1})` when `total == 0`, placed after the existing knowledge guard.
    - `config/test.exs` is byte-unchanged.
  </acceptance_criteria>
  <done>full-suite: matrix job exists and is fan-in-wired; the WAE step is gone from test:; the after_suite partition guard is in place; config/test.exs untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the three breaking contract assertions and add the D-03/D-04 structural coverage-proof + fan-in pins</name>
  <files>test/scoria/ci_policy_contract_test.exs, test/scoria/verification_lanes_test.exs</files>
  <read_first>
    - test/scoria/ci_policy_contract_test.exs (the file being modified — breaking tests at lines 203-221 (B-1) and 223-240 (B-2); fan-in test 266-299; postgres-services test 179-193 (U-1); intent-comment test 410-419 (U-2); gate-map test 440-454 (U-3); job_blocks/1 537-567)
    - test/scoria/verification_lanes_test.exs (the file being modified — breaking assert at line 100 (B-3); test_body slice 73-86; connector_body slice 126-138)
    - .planning/phases/26-full-suite-partition-sharding/26-PATTERNS.md (§ ci_policy_contract_test.exs lines 155-356; § verification_lanes_test.exs lines 360-388)
    - .planning/phases/26-full-suite-partition-sharding/26-RESEARCH.md (§ Breaking Changes B-1/B-2/B-3 lines 215-281; § Additional Tests U-1/U-2/U-3 lines 285-318; § Validation Architecture SC#1-SC#4)
  </read_first>
  <action>
    Edit `test/scoria/ci_policy_contract_test.exs`:

    (B-1, lines 212-214) In test "test job runs semantic lane after runtime_to_handoff": REMOVE the
    comment `# Semantic precedes full-suite WAE inside test: job` and the
    `assert index_of(test_body, @semantic_lane) < index_of(test_body, "run: mix test --warnings-as-errors")`
    assertion (it flunks now that the needle is gone from test_body). Replace with cross-job asserts
    on the full-suite block: fetch `full_suite_body = Map.fetch!(job_blocks(ci_verify), "full-suite")`
    and assert it `=~ "mix test --warnings-as-errors"` and `=~ "--partitions 4"`.

    (B-2, lines 223-240) RENAME test "test job runs full suite WAE after closeout lanes; knowledge is
    a parallel job" to "test job ends with semantic lane; full-suite is a parallel matrix job". REMOVE
    the two `"run: mix test --warnings-as-errors"` assertions (lines 229, 231-232). Add
    `refute test_body =~ "run: mix test --warnings-as-errors"` (teeth: fails if the step is left in
    test:), then fetch `full_suite_body = Map.fetch!(blocks, "full-suite")` and assert it
    `=~ "mix test --warnings-as-errors"`, `=~ "--partitions 4"`, `=~ "needs: build"`. Keep the
    existing knowledge-parallel-job asserts in that test.

    (D-03, new test) Add a new test e.g. "full-suite job is a 4-way matrix with correct partition
    wiring and no DB-name collision". Include a non-empty guard `assert map_size(blocks) > 0`
    (broken job_blocks/1 can't vacuously pass). Fetch `full_suite_body = Map.fetch!(blocks, "full-suite")`.
    Assert SC#1: `=~ "needs: build"`, `=~ "mix test --warnings-as-errors"`, `=~ "--partitions 4"`,
    `=~ "MIX_TEST_PARTITION"`, `=~ "${{ matrix.partition }}"`, `=~ "partition: [1, 2, 3, 4]"`,
    `=~ "fail-fast: false"`, and `refute full_suite_body =~ "continue-on-error"`. Assert SC#2:
    `refute full_suite_body =~ "SCORIA_DB_NAME"`, `assert full_suite_body =~ "MIX_TEST_PARTITION: ${{ matrix.partition }}"`,
    `assert full_suite_body =~ "services:"`. Document the rem-completeness proof (filter_by_partition/3,
    Elixir 1.19.5, residues {0,1,2,3} → union == full suite BY CONSTRUCTION) as a comment beside the
    `--partitions 4` + `partition: [1, 2, 3, 4]` sync asserts (docs-as-contract, D-03).

    (D-04, new test) Add a targeted pin "full-suite is explicitly wired into verify-summary fan-in":
    fetch `verify_summary_body = Map.fetch!(blocks, "verify-summary")` and
    `assert verify_summary_body =~ "full-suite"` with a message explaining a missing wire = false-green.
    (The existing derived subset test at 266-299 needs NO edit — it auto-includes full-suite once it
    has `needs: build`.)

    (U-1, line 179-193) In "postgres service is configured only for test, knowledge, and connector
    jobs": add `assert Map.fetch!(blocks, "full-suite") =~ "services:"`.
    (U-2, line 410-419) In "ci-verify.yml documents per-job intent comments for policy and test": add
    `assert ci_verify =~ "# full-suite:"`.
    (U-3, line 440-454) In "maintainer CI gate map documents topology, parity, ratchet, and failure
    diagnosis": add `assert gate_map =~ "full-suite"`.

    Edit `test/scoria/verification_lanes_test.exs`:
    (B-3, line 100) REMOVE `assert index_of(test_body, semantic) < index_of(test_body, "run: mix test
    --warnings-as-errors")` (the needle is gone from the test_body slice → flunk). The last legit
    intra-test: order assert is now line 99 (`runtime_to_handoff` < `semantic`). Add a cross-workflow
    assert `assert ci_workflow =~ "mix test --warnings-as-errors --partitions 4"` (full-suite carries
    the moved WAE step). Leave the test_body and connector_body slice extractions unchanged.

    Per Claude's Discretion (CONTEXT): keep all additions inside `ci_policy_contract_test.exs` —
    splitting into a sibling file is unnecessary at this size.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/scoria && SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs && ! grep -n 'index_of(test_body, "run: mix test --warnings-as-errors")' test/scoria/verification_lanes_test.exs && grep -q 'Map.fetch!(blocks, "full-suite")' test/scoria/ci_policy_contract_test.exs && grep -q 'verify_summary_body =~ "full-suite"' test/scoria/ci_policy_contract_test.exs && echo OK_T2</automated>
  </verify>
  <acceptance_criteria>
    - `SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs` passes (the policy lane contract is green with the moved YAML step).
    - No assertion anywhere references `index_of(test_body, "run: mix test --warnings-as-errors")` (all three breaking sites B-1/B-2/B-3 are fixed).
    - A D-03 test asserts the full-suite block contains `--partitions 4`, `partition: [1, 2, 3, 4]`, `MIX_TEST_PARTITION`, `${{ matrix.partition }}`, `needs: build`, `fail-fast: false`, refutes `SCORIA_DB_NAME` and `continue-on-error`, and has a non-empty `job_blocks` guard.
    - A D-04 test asserts `full-suite` is in the verify-summary block.
    - The rem-completeness proof is present as a comment beside the partition-sync asserts.
    - U-1/U-2/U-3 additions present (`full-suite` in the postgres-services test, `# full-suite:` in the intent-comment test, `full-suite` in the gate-map test).
  </acceptance_criteria>
  <done>The local lane-contract WAE command is green; the three breaking assertions are corrected; the D-03 structural proof + D-04 fan-in pin + U-1/U-2/U-3 stale-test updates are in place and provably non-vacuous (Map.fetch! + non-empty guard).</done>
</task>

<task type="auto">
  <name>Task 3: Update docs-as-contract topology lines and the MAINTAINERS gate map for full-suite</name>
  <files>docs/MAINTAINERS.md, docs/operator_verification.md, README.md</files>
  <read_first>
    - docs/MAINTAINERS.md (the file being modified — topology line 18; job→command table 22-27; test: job section 29-35 step 5; "Optional knowledge" / verification-lanes table ~57; failure-diagnosis section ~82)
    - docs/operator_verification.md (the file being modified — CI topology paragraph line 292; full-suite line 286)
    - README.md (the file being modified — CI topology reference line 281)
    - .planning/phases/26-full-suite-partition-sharding/26-PATTERNS.md (§ docs/MAINTAINERS.md lines 433-492)
    - .planning/phases/26-full-suite-partition-sharding/26-RESEARCH.md (§ U-3 lines 303-318; U-5 lines 325-328; U-6 lines 330-333)
  </read_first>
  <action>
    Update all three topology strings (currently `policy → build → { test, ratchet, knowledge,
    connector } → verify-summary`) to include `full-suite[×4]`:
      - `docs/MAINTAINERS.md` line 18 topology line.
      - `docs/operator_verification.md` line 292 CI-topology paragraph.
      - `README.md` line 281 maintainer-guide topology reference.

    In `docs/MAINTAINERS.md`:
      - Add a row to the job→command table (lines 22-27):
        `| `full-suite (k/4)` | `SCORIA_DB_PORT=55432 MIX_TEST_PARTITION=k mix test --warnings-as-errors --partitions 4` |`
        (NO `SCORIA_DB_NAME` — absence is load-bearing; the maintainer substitutes `k` for the failing shard).
      - Update the `test:` job section (lines 29-35): REMOVE step 5 `mix test --warnings-as-errors`
        — full-suite WAE has moved out; the section ends at the semantic lane step.
      - Add a `full-suite` job narrative section (analog: the knowledge/connector sections): note the
        4-way matrix on Postgres 55432, `mix test --warnings-as-errors --partitions 4`, that
        `MIX_TEST_PARTITION=k` (no `SCORIA_DB_NAME`) activates `scoria_testk` DB isolation, and that a
        failed `full-suite (2/4)` maps to `SCORIA_DB_PORT=55432 MIX_TEST_PARTITION=2 mix test
        --warnings-as-errors --partitions 4`.
      - Update the failure-diagnosis line ~82 (`- Test: full-suite WAE failed → SCORIA_DB_PORT=55432
        mix test --warnings-as-errors`) to the partition form:
        `- Full-suite (k/4): WAE failed → SCORIA_DB_PORT=55432 MIX_TEST_PARTITION=k mix test
        --warnings-as-errors --partitions 4`.
      - In the "Verification lanes in PR CI" table, fix any stale "After full-suite WAE" notes (~line
        57) so they no longer imply the WAE step lives in the test: job.

    Keep wording consistent with the gate-map contract test (U-3) — `gate_map` must still contain
    `full-suite`, `Policy job`, `Parallel verify jobs`, `ratchet`, `knowledge`, `connector`,
    `verify-summary`, `Local parity`, etc. (do not remove existing asserted substrings).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/scoria && grep -q 'full-suite' docs/MAINTAINERS.md && grep -q 'MIX_TEST_PARTITION=k mix test --warnings-as-errors --partitions 4' docs/MAINTAINERS.md && grep -q 'full-suite' docs/operator_verification.md && grep -q 'full-suite' README.md && ! grep -n '^5\. `mix test --warnings-as-errors` — full-suite WAE' docs/MAINTAINERS.md && SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs && echo OK_T3</automated>
  </verify>
  <acceptance_criteria>
    - `docs/MAINTAINERS.md`, `docs/operator_verification.md`, and `README.md` topology lines all name `full-suite` (with `[×4]`).
    - `docs/MAINTAINERS.md` job→command table has the `full-suite (k/4)` row with `MIX_TEST_PARTITION=k mix test --warnings-as-errors --partitions 4` and no `SCORIA_DB_NAME`.
    - The `test:` job doc section no longer lists `mix test --warnings-as-errors` as a full-suite step.
    - A `full-suite` job narrative + failure-diagnosis row exist.
    - The gate-map contract test (U-3) and all lane-contract tests stay green (`gate_map` still contains every previously-asserted substring plus `full-suite`).
  </acceptance_criteria>
  <done>All topology docs name full-suite, the gate map has the (k/4) row + narrative + failure-diagnosis, the stale test:-step-5 line is gone, and the docs-as-contract tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a CI-contract phase — there is no auth/crypto/PII/runtime-input surface. The "trust
boundary" is the CI verification contract itself: the merge gate must report green ONLY when every
test in every shard actually ran and passed. Threats are framed as **false-green / coverage-evasion**.

| Boundary | Description |
|----------|-------------|
| matrix shard result → verify-summary fan-in | An individual shard's success/failure must aggregate honestly into the single required check |
| YAML contract → contract tests | The contract test must fail loudly on real drift and must not vacuously pass on a broken parser |
| ExUnit exit code → CI step result | A shard that runs 0 tests must NOT report success |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Spoofing (false-green) | `full-suite:` matrix exit code | mitigate | `after_suite` guard (Task 1) gated on `MIX_TEST_PARTITION` exits `{:shutdown, 1}` when `total == 0` — closes the ExUnit `{:noop,_}` exit-0-on-0-tests hole (D-03 Layer 3) |
| T-26-02 | Spoofing (false-green) | matrix strategy result aggregation | mitigate | Use `fail-fast: false` ONLY; NEVER `continue-on-error: true` (reports failed legs as `success` to `needs.*.result` — community #45546). Task 2 D-03 test `refute full_suite_body =~ "continue-on-error"` (Task 1 verify also greps `! continue-on-error`) |
| T-26-03 | Tampering / Repudiation | `verify-summary` fan-in skipped-as-success | mitigate | Keep `if: always()` + the `join(needs.*.result)` skipped=fail loop (a fan-in without `if: always()` is SKIPPED on upstream cancel and branch protection reads skipped as success — CodeQL #712). D-04 pin asserts `full-suite ∈ verify-summary.needs` |
| T-26-04 | Tampering (vacuous-pass) | `job_blocks/1` contract parser | mitigate | Non-empty guard (`map_size(blocks) > 0` / `MapSet.size(parallel_lanes) > 0`) + `Map.fetch!(blocks, "full-suite")` which RAISES on a missing key — a broken regex cannot silently pass (Task 2 D-03) |
| T-26-05 | Information disclosure / coverage loss | partition math sync | mitigate | Structural asserts pin `--partitions 4` AND `partition: [1, 2, 3, 4]` as separate agreeing facts; a `--partitions 3` / `[1,2,3,4]` mismatch goes RED before merge (Task 2 D-03 Layer 2). Union completeness proven by rem-residue math, documented as a contract comment (Layer 1) |
| T-26-06 | Tampering (DB collision masking failures) | per-shard DB isolation | mitigate | `refute full_suite_body =~ "SCORIA_DB_NAME"` + `assert MIX_TEST_PARTITION: ${{ matrix.partition }}` (Task 2). Absence of `SCORIA_DB_NAME` forces `config/test.exs` to `scoria_test{k}`; a stray `SCORIA_DB_NAME` (all shards share one DB → races) is caught by the refute |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase (zero new dependencies — RESEARCH "Standard Stack"). No `[ASSUMED]`/`[SUS]` packages; legitimacy gate not applicable |
</threat_model>

<verification>
Before commit, the executor MUST run the lane-contract command and confirm green:

  SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors \
    test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs

This is the exact command the CI `policy` job runs (plus `adoption_surface_test.exs`); it catches
the three breaking assertions and proves the D-03/D-04 structural asserts pass. All three tasks land
in ONE atomic commit — the YAML move and the contract-test fixes cannot be separated (RESEARCH
"Primary recommendation: One wave. Atomic commit").

Post-merge (manual, not gated): the first real sharded CI run should show four green
`full-suite (k/4)` shards under `verify-summary`; branch protection still requires only `CI / ci-gate`.
</verification>

<success_criteria>
- SC#1: `full-suite:` runs `mix test --warnings-as-errors --partitions 4` across `matrix.partition:
  [1, 2, 3, 4]`, each leg exporting `MIX_TEST_PARTITION` from `matrix.partition`; `config/test.exs` unchanged.
- SC#2: each shard's DB is `scoria_test{k}` (SCORIA_DB_NAME absent from full-suite: env); shards never collide.
- SC#3: zero coverage loss — proven by rem-completeness math (Layer 1), pinned by structural contract
  (`--partitions 4` + `[1,2,3,4]` sync, Layer 2), and backstopped by the `after_suite` zero-test guard (Layer 3).
- SC#4: only `verify-summary` is required; `full-suite ∈ verify-summary.needs`; no per-shard check added;
  `if: always()` + `fail-fast: false` retained; `continue-on-error` absent.
- Lane-contract WAE command green; three breaking assertions fixed; docs name full-suite in lockstep.
</success_criteria>

<output>
Create `.planning/phases/26-full-suite-partition-sharding/26-01-SUMMARY.md` when done.
</output>
