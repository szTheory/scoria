---
phase: 54-docs-accuracy-conformance-check
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/scoria/observe/conformance_test.exs
  - lib/mix/tasks/test.adoption.ex
autonomous: true
requirements:
  - DOCS-02
must_haves:
  truths:
    - "Running `mix test test/scoria/observe/conformance_test.exs` drives the real ReqLLM, MCP, and Jido adapters live and passes."
    - "The test derives the post-`Bounds.enforce/2` record of truth by replaying the exact production pipeline (`Redactor.redact/1 |> Bounds.enforce/2`) in-test — no hand-copied allow-list, no golden fixture, no DB."
    - "Every surviving attribute key in the record of truth is SSOT-admitted (proven by calling `Bounds.enforce/2`, not by re-listing keys); `span_kind ∈ SpanKind.kinds()`; and `attributes[Semconv.openinference_span_kind_key()] == SpanKind.to_openinference(span_kind)`."
    - "Each of the 3 adapter-reachable kinds (llm/mcp/tool) is exercised with a non-empty corpus per adapter."
    - "A deliberately bogus key/kind turns the check RED (negative self-test bites); failure messages name the offending key/kind AND the adapter."
    - "`test/scoria/observe/conformance_test.exs` is registered in `Mix.Tasks.Scoria.Test.Adoption.@adoption_test_files`; `mix test.adoption` includes it and passes."
  artifacts:
    - "test/scoria/observe/conformance_test.exs (new — `Scoria.Observe.ConformanceTest`, `@moduletag :conformance`)"
    - "The 14th `@adoption_test_files` entry: `\"test/scoria/observe/conformance_test.exs\"`"
  key_links:
    - "Emit-layer capture (`:telemetry.attach` on `[:scoria, :observe, :span, :stop]`) <-> in-test replay (`Redactor.redact/1 |> Bounds.enforce/2`) — reaches the identical post-Bounds record `Buffer.cast_span/2` would persist (`telemetry.ex:66-79`)."
    - "The test's key predicate <-> `Bounds.enforce/2` (the production SSOT function) — calling it directly is the anti-drift seam (D-05 corrected: no public `admit?/1` exists)."
  prohibitions:
    - "Do NOT wire this test into `VerificationLanes.closeout_order/0` or any CI/lane contract (D-01/D-02); registration is `@adoption_test_files` only."
    - "Do NOT modify the jido adapter, `Semconv.attribute_registry/0`, or add `jido.*` keys to the registry (jido is compliant-by-enforcement via the record-of-truth layer — D-07)."
    - "Do NOT hand-reconstruct the admission rule from the four `Semconv` functions, and do NOT inline a `~w(...)` allow-list literal (call `Bounds.enforce/2` / `SpanKind` functions directly — the ANTI-INLINE-GUARD discipline)."
    - "Do NOT use a committed golden-span fixture or a seeded-DB corpus as the primary source (D-04)."
    - "Do NOT attempt to exercise all 8 `SpanKind.kinds()` values — 5 are unreachable from these 3 adapters (D-06 scoping, RESEARCH §4)."
---

<objective>
Add a falsifiable ExUnit conformance check proving every span the three span-emitting
adapters (ReqLLM, MCP, Jido) produce carries only SSOT-allow-listed convention keys and a
`span_kind` from the shared whitelist — the executable backing for Plan 01's claim flip
(DOCS-02, D-01..D-07).

Purpose: Structurally link the "OpenInference-compatible" doc claim to the runtime, so any
future drift in an adapter's key/kind emission trips this test RED.

Output: `test/scoria/observe/conformance_test.exs` (`Scoria.Observe.ConformanceTest`,
`@moduletag :conformance`, `async: true`) registered in `@adoption_test_files`.

## Corrected seams (RESEARCH overrides CONTEXT where they differ)
- D-05: `Bounds` exposes NO public `admit?/1`. Call `Bounds.enforce/2` DIRECTLY on captured
  attributes and inspect surviving keys — test and production share one rule.
- D-07: there is NO telemetry event re-emitted after Bounds. Capture the emit-layer span
  (`req_llm_test.exs` `:telemetry.attach` → `assert_receive` idiom) then replay
  `Redactor.redact/1 |> Bounds.enforce(:span)` IN-TEST. Do NOT lift `mcp_test.exs`'s
  DB-backed harness.
- D-06: exhaustiveness is scoped to the 3 adapter-reachable kinds only:
  ReqLLM→`"llm"`, MCP→`"mcp"` (hardcoded), Jido→`"tool"`. Assert each is exercised + corpus
  non-empty per adapter — NOT all 8 kinds.
</objective>

<artifacts>
Symbols/files this plan produces:
- New file `test/scoria/observe/conformance_test.exs`
- New module `Scoria.Observe.ConformanceTest` with `@moduletag :conformance`
- New `@adoption_test_files` entry `"test/scoria/observe/conformance_test.exs"` (14th) in `lib/mix/tasks/test.adoption.ex`
</artifacts>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-docs-accuracy-conformance-check/54-CONTEXT.md
@.planning/phases/54-docs-accuracy-conformance-check/54-RESEARCH.md
@.planning/phases/54-docs-accuracy-conformance-check/54-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conformance test — capture, in-test replay, positive assertions</name>
  <files>test/scoria/observe/conformance_test.exs</files>
  <read_first>
    - test/scoria/observe/adapters/req_llm_test.exs (lines 1-22 setup + 52-56 capture helper — the emit-layer `:telemetry.attach` → `assert_receive` idiom to lift; use a DISTINCT handler id)
    - lib/scoria/observe/telemetry.ex (lines 66-79 — the production pipeline order `redact/1` → `Bounds.enforce/2` this test replays)
    - lib/scoria/observe/bounds.ex (line 137 `enforce/2` signature; moduledoc — the 3-tier admission rule, for understanding only, NOT reconstruction)
    - lib/scoria/observe/semconv.ex (`attribute_registry/0`, `vendor_key_prefixes/0`, `openinference_span_kind_key/0`)
    - lib/scoria/observe/span_kind.ex (`kinds/0`, `kind?/1`, `to_openinference/1`)
    - lib/scoria/observe/adapters/req_llm.ex, mcp.ex, jido.ex (the emit events + default kinds — RESEARCH §4 table: ReqLLM `[:req_llm, :request, :stop]`→llm, MCP `[:scoria, :tool, :completed|:timeout|:failed]`→mcp, Jido `[:jido, :action, :stop]`→tool)
    - test/scoria/observe/span_kind_test.exs (lines 94-106 canary/exhaustiveness idiom to mirror)
  </read_first>
  <action>
    Create `test/scoria/observe/conformance_test.exs` — `defmodule Scoria.Observe.ConformanceTest`,
    `use ExUnit.Case, async: true`, `@moduletag :conformance` (D-01). No DB/Sandbox (D-07 corrected).

    Setup (D-04 — drive real adapters live): attach a capture handler under a DISTINCT id
    (e.g. `"scoria-observe-telemetry-test-conformance"` — NEVER reuse a production id like
    `scoria-observe-reqllm`/`-mcp`/`-jido`/`-telemetry`) to `[:scoria, :observe, :span, :stop]`
    that `send`s `{:span, metadata}` to the test process; `on_exit` detaches it. Call
    `ReqLLM.attach()` and `Jido.attach()` (idempotent — tolerate `{:error, :already_exists}`);
    the MCP handler is already boot-attached, so fire its upstream events directly.

    Capture helper: `:telemetry.execute(event, %{}, metadata)` then `assert_receive {:span, span}`.
    Record-of-truth helper (D-07 corrected — replay production order, both public fns):
    `raw |> Scoria.Observe.Redactor.redact() |> Scoria.Observe.Bounds.enforce(:span)` → match
    `{:ok, bounded}`; `bounded` is byte-identical to what `Buffer.cast_span/2` would persist.

    Drive each of the 3 adapters with realistic metadata (RESEARCH pitfalls: MCP must fire a
    TERMINAL event `:completed`/`:timeout`/`:failed`, never `:started`; read req_llm kind via
    `metadata[:span_kind]`, never `metadata[:operation]`; never assert on MCP `:reason`). For
    the record of truth of each adapter's span, assert (D-05 corrected — call the SSOT, never
    re-list keys):
    - Every surviving key in `bounded.attributes` is SSOT-admitted — because it survived
      `Bounds.enforce/2` (the direct-call proof); add a belt-and-suspenders check that each key
      is either in `Semconv.attribute_registry/0` or matches a `Semconv.vendor_key_prefixes/0`
      prefix, with a failure message naming the offending key AND adapter.
    - `SpanKind.kind?(bounded.span_kind)` is true and `bounded.span_kind ∈ SpanKind.kinds()`.
    - `bounded.attributes[Semconv.openinference_span_kind_key()] == SpanKind.to_openinference(bounded.span_kind)`.

    Exhaustiveness + non-empty corpus (D-06, scoped to adapter-reachable kinds per RESEARCH §4):
    assert the corpus for each adapter is non-empty and that the set of observed default kinds
    equals exactly `["llm", "mcp", "tool"]` (ReqLLM/MCP/Jido respectively). Add a comment
    stating this is deliberately scoped to the 3 adapter-reachable kinds (the other 5 live in
    `Workflows.Runtime`/`Knowledge`/`JudgeRunner`, out of scope). Optionally include ONE
    host-override probe (e.g. ReqLLM with `metadata[:span_kind] => "agent"`) to prove the
    override path is honored end-to-end through Bounds/SpanKind (RESEARCH Open Question 1).
  </action>
  <verify>
    <automated>mix test test/scoria/observe/conformance_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/scoria/observe/conformance_test.exs` exists: `Scoria.Observe.ConformanceTest`, `@moduletag :conformance`, `async: true`, distinct capture handler id.
    - The record of truth is derived via in-test `Redactor.redact/1 |> Bounds.enforce(:span)` replay (no DB, no golden fixture, no hand-copied allow-list).
    - All three adapters are driven live; corpus is non-empty per adapter; observed default kinds are exactly llm/mcp/tool.
    - Key/kind/OI-mirror assertions call `Bounds.enforce/2`, `SpanKind.kind?/1`, `SpanKind.to_openinference/1`, `Semconv.*` directly (no inline `~w(...)` allow-list).
    - `mix test test/scoria/observe/conformance_test.exs` passes.
  </acceptance_criteria>
  <done>The conformance test drives all three adapters live, derives the post-Bounds record via in-test replay, and asserts allow-listed keys + whitelisted `span_kind` + OI mirror with a non-empty per-adapter corpus — green.</done>
</task>

<task type="auto">
  <name>Task 2: Negative self-test + dropped-key bite + adoption registration</name>
  <files>test/scoria/observe/conformance_test.exs, lib/mix/tasks/test.adoption.ex</files>
  <read_first>
    - test/scoria/observe/span_kind_test.exs (lines 132-148 — the "guard must bite" fallback self-test shape to mirror)
    - lib/scoria/observe/adapters/jido.ex (lines 48-54 — jido puts raw `jido.action_name`/`jido.status` keys that Bounds drops; RESEARCH §4)
    - lib/mix/tasks/test.adoption.ex (lines 5-21 — `@adoption_test_files`, currently 13 entries; append is a one-line diff, no companion test asserts exact length per RESEARCH §3)
  </read_first>
  <action>
    Extend `test/scoria/observe/conformance_test.exs` with the vacuity guards (D-06 "the guard
    must bite") and register the file for lane visibility (D-03).

    1. Negative self-test: feed a deliberately bogus attribute key (e.g. `"totally.not.allowed"`)
       and a bogus `span_kind` (e.g. `"not_a_kind"`) through the SAME `Bounds.enforce/2` /
       `SpanKind.kind?/1` calls the positive tests use, and assert the guard visibly BITES —
       the bogus key is ABSENT from the post-`enforce` attributes, and `SpanKind.kind?("not_a_kind")`
       is false. This proves the check would go RED on real drift (not merely not-crash).
    2. Dropped-key classification (D-07 secondary bite, RESEARCH §8): for the jido corpus,
       compute `MapSet.difference/2` between the pre-Bounds and post-Bounds attribute key sets
       and assert the dropped set is a subset of the documented drop-list
       (`jido.action_name`, `jido.status`) — so a NEW silently-dropped key becomes a loud
       failure. Keep the failure message naming the offending key AND adapter.
    3. Register the test: in `lib/mix/tasks/test.adoption.ex`, APPEND
       `"test/scoria/observe/conformance_test.exs"` as the 14th `@adoption_test_files` entry
       (after `"test/scoria/bootstrap/migration_lane_compatibility_test.exs"`). Do NOT touch
       `closeout_order/0` or any lane/CI contract (D-01/D-02).
  </action>
  <verify>
    <automated>mix test test/scoria/observe/conformance_test.exs && mix test.adoption</automated>
  </verify>
  <acceptance_criteria>
    - A negative self-test proves a bogus key is dropped by `Bounds.enforce/2` and a bogus kind is `SpanKind.kind?/1`-false; failure messages name the offending key/kind and adapter.
    - The jido dropped-key set is asserted ⊆ the documented drop-list (`jido.action_name`, `jido.status`).
    - `@adoption_test_files` has the new 14th entry `"test/scoria/observe/conformance_test.exs"`; `closeout_order/0` and lane/CI contracts are untouched.
    - `mix test.adoption` includes and passes the conformance test.
  </acceptance_criteria>
  <done>A bogus key/kind visibly trips the guard RED, the jido dropped-key set is asserted against the documented drop-list, the file is registered in `@adoption_test_files` (closeout contract untouched), and `mix test.adoption` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds one read-only ExUnit test that captures telemetry and calls `Redactor.redact/1`/`Bounds.enforce/2` in-process, plus a one-line test-file registration. No new inputs, endpoints, auth, or data flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-02 | Information Disclosure | A future adapter change silently widens the persisted key surface (e.g. a new vendor key that leaks free-text) | medium | mitigate | This conformance test IS the mitigation — it fails RED the moment an adapter emits a key `Bounds.enforce/2` does not drop and that is not in the registry, or a `span_kind` outside `SpanKind.kinds()`. The dropped-key classification also flags a NEW silently-dropped key. |

ASVS L1 review: this phase adds **no attack surface**. `Bounds.enforce/2`'s closed-registry admission is the existing SEC-01 (V5 Input Validation) control this test *proves*, not one it introduces. No new threat is manufactured.
</threat_model>

<verification>
- `mix test test/scoria/observe/conformance_test.exs` green (fast, no DB — `async: true`).
- `mix test.adoption` green (conformance test now a member).
- `mix format --check-formatted` clean for the new test file and `test.adoption.ex`.
- Manual diff review: `closeout_order/0` and `verification_lanes_test.exs`/`ci_policy_contract_test.exs` unchanged; no adapter or `Semconv.attribute_registry/0` edit.
</verification>

<success_criteria>
- DOCS-02 satisfied: an ExUnit test drives all three span-emitting adapters live and asserts every emitted span's record of truth uses only allow-listed convention keys + a whitelisted `span_kind`, failing on an unlisted key/kind; a negative self-test proves the guard bites; the file is registered for adoption-lane visibility without touching the byte-stable closeout contract.
</success_criteria>

<output>
Create `.planning/phases/54-docs-accuracy-conformance-check/54-02-SUMMARY.md` when done.
</output>
