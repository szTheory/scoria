---
phase: 54-docs-accuracy-conformance-check
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/adopter_doc_contract.ex
  - lib/scoria/ai_doc_contract.ex
  - guides/capabilities/trace-observability.md
  - guides/reference/glossary.md
  - guides/scoria-vs-external-llm-ops.md
  - llms.txt
  - README.md
  - AGENTS.md
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "The comparison guide's `## What Scoria currently owns` section literally contains the substring `OpenInference-compatible convention keys` (satisfies the newly required-present safe claim)."
    - "`guides/capabilities/trace-observability.md` exists and opens with the D-10 canonical sentence (version-pinned to OTel-GenAI schema 1.37.0 via `req_llm ~> 1.13`)."
    - "`llms.txt` literally contains the string `guides/capabilities/trace-observability.md` (required by the live `required_llms_paths/0` iteration in `ai_doc_contract_test.exs`)."
    - "The glossary no longer uses the `-style` softener on line ~31 and no longer carries the `does not claim ... until that future work ships` softener on line ~36."
    - "`mix test test/scoria/adoption_surface_test.exs test/scoria/ai_doc_contract_test.exs` passes."
  artifacts:
    - "guides/capabilities/trace-observability.md (new canonical trace-observability guide)"
    - "The `\"OpenInference-compatible convention keys\"` entry appended to `@comparison_safe_current_claims`"
    - "The `\"guides/capabilities/trace-observability.md\"` entry appended to `@required_llms_paths`"
  key_links:
    - "`@required_llms_paths` new entry <-> the literal path string in `llms.txt` (both must change together or `ai_doc_contract_test.exs:107-127` goes RED)."
    - "`@comparison_safe_current_claims` new entry <-> the D-10 sentence in the comparison guide's `currently owns` section (the safe-claims loop asserts the substring is present)."
  prohibitions:
    - "Do NOT remove or alter any `export`-bearing entry: `@comparison_forbidden_current_claims` (`OpenInference export`, `OpenInference-compatible export`), `@comparison_deferred_not_current_claims` (`OpenInference export is not a current Scoria claim`), and `@forbidden_ai_doc_fragments` (`OpenInference export`) all stay byte-stable."
    - "Do NOT add the bare phrase `OpenInference-compatible` to ANY forbidden/banned list (the D-11 TRAP — it would substring-`refute`-match the very claim being added)."
    - "Do NOT touch `lib/scoria/verification_lanes.ex` `closeout_order/0` or the CI/lane contract tests."
    - "Do NOT modify any adapter, `Semconv.attribute_registry/0`, or any runtime/schema/migration code."
---

<objective>
Flip the adopter-facing observability claim from the v3.5-softened "OpenInference-style"
to an honest, version-pinned "OpenInference-compatible convention keys" claim, and update
the doc-contract banned/allowed lists in the SAME change — additively only (DOCS-01,
D-08..D-12).

Purpose: Make the claim both *allowed* by Scoria's own doc-contract guards and *honest*
(named keys + pinned schema + "not an exporter" rider), backed by Plan 02's conformance test.

Output: One new canonical trace guide, two additive module-attribute list edits, the D-10
canonical sentence landed on every adopter surface, and the glossary softeners removed.

## D-10 canonical sentence (reuse VERBATIM on every surface)

"Scoria records OpenTelemetry-GenAI / OpenInference-compatible convention keys (`gen_ai.*`,
`server.*`, `openinference.span.kind`) in your host Postgres, pinned to OpenTelemetry GenAI
semantic-conventions schema 1.37.0 (via `req_llm ~> 1.13`; these GenAI conventions are still
experimental upstream). Scoria is not an OpenTelemetry exporter — sending these traces onward
to Langfuse, Datadog, or Arize Phoenix is host-owned and opt-in."
</objective>

<artifacts>
Symbols/files this plan produces (per D-08/D-10/D-11):
- New file: `guides/capabilities/trace-observability.md`
- New `@comparison_safe_current_claims` entry string: `"OpenInference-compatible convention keys"` (in `lib/scoria/adopter_doc_contract.ex`)
- New `@required_llms_paths` entry string: `"guides/capabilities/trace-observability.md"` (in `lib/scoria/ai_doc_contract.ex`)
- The D-10 canonical sentence landed in: README.md, AGENTS.md, `guides/reference/glossary.md` (L36 replacement), `guides/scoria-vs-external-llm-ops.md` (`currently owns` section), and the new trace guide
</artifacts>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-docs-accuracy-conformance-check/54-CONTEXT.md
@.planning/phases/54-docs-accuracy-conformance-check/54-RESEARCH.md
@.planning/phases/54-docs-accuracy-conformance-check/54-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive safe-claim edit + comparison-guide sentence</name>
  <files>lib/scoria/adopter_doc_contract.ex, guides/scoria-vs-external-llm-ops.md</files>
  <read_first>
    - lib/scoria/adopter_doc_contract.ex (lines 59-106 — the three `@comparison_*` lists; RESEARCH.md §6 quotes them verbatim)
    - guides/scoria-vs-external-llm-ops.md (lines 18-34 — `## What Scoria currently owns`)
    - test/scoria/adoption_surface_test.exs (lines 253-261 — the safe-claims `=~` loop and the forbidden `refute` loop that auto-cover this edit)
  </read_first>
  <action>
    Per D-11 (additive-only) and D-10: in `lib/scoria/adopter_doc_contract.ex`, APPEND the
    exact string `"OpenInference-compatible convention keys"` as a new final element of the
    `@comparison_safe_current_claims` list (currently 12 entries ending
    `"no required egress for Scoria governance records"`). Do NOT modify
    `@comparison_forbidden_current_claims` or `@comparison_deferred_not_current_claims` — both
    keep every `export`-bearing entry byte-for-byte (D-11 KEEP). Do NOT add the bare phrase
    `OpenInference-compatible` to any forbidden list (the D-11 TRAP: it would substring-match
    and `refute`-fail the claim just added).

    Because appending to `@comparison_safe_current_claims` flips that string to
    required-PRESENT (the `for current_claim <- comparison_safe_current_claims()` loop asserts
    `current_section =~ current_claim`), land the D-10 canonical sentence (see objective) in
    `guides/scoria-vs-external-llm-ops.md` under the existing `## What Scoria currently owns`
    heading — append it as a new bullet/paragraph after the current-claims bullet block
    (before `Those are current package and docs claims.`). The sentence contains the literal
    substring `OpenInference-compatible convention keys`, satisfying the loop. Leave the
    `## Not current Scoria claims` section (the `OpenInference export is not a current Scoria
    claim` line) untouched — export genuinely stays deferred.
  </action>
  <verify>
    <automated>mix test test/scoria/adoption_surface_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `@comparison_safe_current_claims` has exactly one new final entry: `"OpenInference-compatible convention keys"`; the other two `@comparison_*` lists are unchanged.
    - `guides/scoria-vs-external-llm-ops.md` `## What Scoria currently owns` section contains the D-10 sentence verbatim (and thus the substring `OpenInference-compatible convention keys`).
    - No `export`-bearing forbidden/deferred entry was removed or altered.
    - `mix test test/scoria/adoption_surface_test.exs` passes.
  </acceptance_criteria>
  <done>Safe-claim list gains the one additive entry, the comparison guide's `currently owns` section carries the D-10 sentence, no export ban touched, and `adoption_surface_test.exs` is green.</done>
</task>

<task type="auto">
  <name>Task 2: New trace-observability guide + docs-source registration (load-bearing)</name>
  <files>guides/capabilities/trace-observability.md, lib/scoria/ai_doc_contract.ex, llms.txt</files>
  <read_first>
    - lib/scoria/ai_doc_contract.ex (lines 17-49 `@required_llms_paths`, 79-91 `@forbidden_ai_doc_fragments`; RESEARCH.md §6)
    - test/scoria/ai_doc_contract_test.exs (lines 107-127 — iterates live `required_llms_paths/0` and asserts `llms.txt` content `=~ path` for EVERY entry; this makes the `llms.txt` literal add mandatory)
    - guides/capabilities/support-copilot-gallery.md (structural analog for the new guide — heading shape, cross-link style)
    - llms.txt (lines 33-39 — the `## Capability Guides` list, currently 5 capability entries)
  </read_first>
  <action>
    Per D-08 (add a canonical home) and D-11 (`@required_llms_paths` ADD): create
    `guides/capabilities/trace-observability.md`. Open it with the D-10 canonical sentence
    (see objective) verbatim, then write supporting prose (writer's discretion, must not
    overclaim): what keys are recorded (`gen_ai.*`, `server.*`, `openinference.span.kind`),
    the D-09 honesty rider (OTel-GenAI conventions are experimental/Development status; Scoria
    pins to the schema `req_llm` emits — `https://opentelemetry.io/schemas/1.37.0`), and the
    explicit "Scoria is not an OpenTelemetry exporter; export to Langfuse/Datadog/Arize is
    host-owned and opt-in" framing. Cross-link `guides/reference/glossary.md` and
    `guides/scoria-vs-external-llm-ops.md`. Do NOT confuse this with the pre-existing
    `guides/capabilities/llm-and-tool-adapters.md` (different file, out of scope).

    Then register the new guide so the doc-contract stays green in this SAME task (they are
    coupled — registering without the `llms.txt` literal would turn `ai_doc_contract_test.exs`
    RED):
    1. `lib/scoria/ai_doc_contract.ex`: APPEND `"guides/capabilities/trace-observability.md"`
       as a new final entry of `@required_llms_paths`. Do NOT touch `@forbidden_ai_doc_fragments`
       (D-11 KEEP `"OpenInference export"`).
    2. `llms.txt`: add a 6th bullet to the `## Capability Guides` list (lines 33-39), matching
       the existing markdown-link item shape:
       `- [guides/capabilities/trace-observability.md](guides/capabilities/trace-observability.md) - ...`
       The literal path string `guides/capabilities/trace-observability.md` MUST appear in the
       file body (the `content =~ path` assertion).
  </action>
  <verify>
    <automated>mix test test/scoria/ai_doc_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `guides/capabilities/trace-observability.md` exists and opens with the D-10 sentence verbatim.
    - `@required_llms_paths` ends with the new `"guides/capabilities/trace-observability.md"` entry; `@forbidden_ai_doc_fragments` unchanged.
    - `llms.txt` literally contains the string `guides/capabilities/trace-observability.md`.
    - `mix test test/scoria/ai_doc_contract_test.exs` passes (including the `llms.txt` path-coverage and no-duplicates checks).
  </acceptance_criteria>
  <done>New trace guide exists opening with the D-10 sentence, its path is registered in both `@required_llms_paths` and `llms.txt`, and `ai_doc_contract_test.exs` is green.</done>
</task>

<task type="auto">
  <name>Task 3: Land canonical sentence on README/AGENTS + glossary softener removal (D-12)</name>
  <files>guides/reference/glossary.md, README.md, AGENTS.md</files>
  <read_first>
    - guides/reference/glossary.md (lines 31 and 36 — the two `-style`/softener lines, quoted verbatim in RESEARCH.md §7)
    - README.md and AGENTS.md (no existing `OpenInference` mention — pure additions of the D-10 sentence)
  </read_first>
  <action>
    Per D-10 (one voice across surfaces) and D-12 (glossary rewrite):

    1. `guides/reference/glossary.md` line ~31: drop the `-style` softener. Current:
       `- Industry equivalent: trace or span tree in OpenTelemetry/OpenInference-style observability.`
       → change `OpenInference-style observability` to `OpenInference-compatible observability`
       (drop only the `-style` softener; keep the bullet otherwise intact).
    2. `guides/reference/glossary.md` line ~36: REPLACE the entire softener sentence
       (`Trace vocabulary aligns with OpenTelemetry/OpenInference-style observability language,
       but Scoria does not claim an OpenInference-compatible export or trace substrate until
       that future work ships.`) with the D-10 canonical sentence (see objective) followed by a
       link to the new guide, e.g. `See [Trace Observability](guides/capabilities/trace-observability.md).`
       The convention/substrate shipped; only export stays deferred — so the "until that future
       work ships" framing is removed for the convention claim.
    3. `README.md`: add the D-10 canonical sentence verbatim in the observability/concepts area
       (a pure addition — README currently has zero `OpenInference` mentions).
    4. `AGENTS.md`: add the D-10 canonical sentence verbatim (pure addition).

    Keep the D-10 sentence byte-identical across all four surfaces (one voice, not variants).
  </action>
  <verify>
    <automated>mix test test/scoria/adoption_surface_test.exs test/scoria/ai_doc_contract_test.exs test/scoria/terminology_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Glossary line ~31 no longer contains `OpenInference-style`; line ~36 no longer contains the `does not claim ... until that future work ships` softener and instead carries the D-10 sentence + a link to the new trace guide.
    - README.md and AGENTS.md each contain the D-10 sentence verbatim.
    - The D-10 sentence is byte-identical across glossary L36, comparison guide, README, AGENTS, and the new trace guide.
    - The listed contract/terminology tests pass.
  </acceptance_criteria>
  <done>Glossary softeners removed (L31 `-style` dropped, L36 replaced with D-10 sentence + guide link), README and AGENTS carry the byte-identical D-10 sentence, and contract/terminology tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits static Markdown + two compile-time module-attribute lists. No new inputs, endpoints, auth, data flows, or runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-01 | Tampering (of adopter trust — marketing/doc drift) | The `OpenInference-compatible` claim in docs | low | mitigate | The existing `AdopterDocContract`/`AiDocContract` ExUnit substring guards (extended additively here) plus Plan 02's conformance test back the claim with executable proof; the claim cannot be present without the pinned convention actually being enforced. |

ASVS L1 review: this phase adds **no attack surface** (no new inputs, endpoints, auth, or data flows — docs + module-attribute list appends only). The sole sensitivity is doc-honesty, which is the very property the doc-contract guards and Plan 02's conformance test enforce. No threat is manufactured.
</threat_model>

<verification>
- `mix test test/scoria/adoption_surface_test.exs test/scoria/ai_doc_contract_test.exs test/scoria/terminology_contract_test.exs` all green.
- `mix format --check-formatted` clean for the two edited `.ex` files.
- Grep confirms `guides/capabilities/trace-observability.md` appears in both `@required_llms_paths` and `llms.txt`.
- No `export`-bearing forbidden/deferred entry changed (git diff review of the two contract modules shows only the two additive appends).
</verification>

<success_criteria>
- DOCS-01 satisfied: the version-pinned "OpenInference-compatible" claim is present on adopter surfaces, the banned/allowed lists were updated additively in the same change, the glossary softener is replaced, and the export-deferral disclaimer is retained.
- All contract tests pass with the new claim string present.
</success_criteria>

<output>
Create `.planning/phases/54-docs-accuracy-conformance-check/54-01-SUMMARY.md` when done.
</output>
