---
phase: 46-operator-evidence-and-verification
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - mix.exs
  - lib/mix/tasks/scoria.test.semantic_fast_path.ex
  - lib/mix/tasks/test.semantic_fast_path.ex
  - test/mix/tasks/test.semantic_fast_path_test.exs
autonomous: true
requirements:
  - PROOF-01
user_setup: []
must_haves:
  truths:
    - "Maintainers have one explicit semantic fast-path verification command instead of relying on the broad test suite."
    - "Phase 46 proof claims name the real pgvector-backed verification lane instead of implying that the blocked `5432` path is trustworthy."
    - "The semantic verification lane is discoverable through Mix task tests and pinned to the test environment."
  artifacts:
    - path: "lib/mix/tasks/scoria.test.semantic_fast_path.ex"
      provides: "Canonical bounded semantic fast-path verification lane"
      exports: ["run/1", "semantic_fast_path_test_files/0"]
    - path: "lib/mix/tasks/test.semantic_fast_path.ex"
      provides: "Compatibility wrapper for the canonical semantic lane"
      exports: ["run/1"]
    - path: "test/mix/tasks/test.semantic_fast_path_test.exs"
      provides: "Discoverability and bounded-file-list assertions"
  key_links:
    - from: "mix.exs"
      to: "lib/mix/tasks/test.semantic_fast_path.ex"
      via: "preferred_envs"
      pattern: "test\\.semantic_fast_path"
    - from: "test/mix/tasks/test.semantic_fast_path_test.exs"
      to: "lib/mix/tasks/scoria.test.semantic_fast_path.ex"
      via: "explicit expected_files assertion"
      pattern: "semantic_fast_path_test_files"
---

<objective>
Lock the Phase 46 verification posture to the real pgvector-backed lane and scaffold the dedicated semantic proof command before later plans rely on proof claims.

Purpose: Phase 45 proved that `5432` recompiles cleanly but remains externally blocked for semantic-cache work because `pgvector` is unavailable there, while `55432` is the trusted executable lane. Phase 46 must preserve that reality explicitly before it adds UI proof or docs.
Output: named `mix scoria.test.semantic_fast_path` and `mix test.semantic_fast_path` tasks, discoverability tests, and `preferred_envs` wiring aligned to the current `55432` verification posture.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-operator-evidence-and-verification/46-CONTEXT.md
@.planning/phases/46-operator-evidence-and-verification/46-RESEARCH.md
@.planning/phases/46-operator-evidence-and-verification/46-PATTERNS.md
@.planning/phases/46-operator-evidence-and-verification/46-VALIDATION.md
@.planning/phases/45-compatibility-and-invalidation-engine/45-00-SUMMARY.md
@mix.exs
@lib/mix/tasks/test.adoption.ex
@test/mix/tasks/test.adoption_test.exs

<interfaces>
From [mix.exs](/Users/jon/projects/scoria/mix.exs:15):
```elixir
def cli do
  [
    preferred_envs: [
      "scoria.test.adoption": :test,
      "test.adoption": :test,
      "scoria.test.knowledge": :test,
      "test.knowledge": :test
    ]
  ]
end
```

From [lib/mix/tasks/test.adoption.ex](/Users/jon/projects/scoria/lib/mix/tasks/test.adoption.ex:1):
```elixir
defmodule Mix.Tasks.Scoria.Test.Adoption do
  use Mix.Task
  def adoption_test_files, do: @adoption_test_files
  def run(args) do
    Mix.Task.run("loadpaths")
    Mix.Task.reenable("test")
    Mix.Task.run("test", args ++ @adoption_test_files)
  end
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve `55432` as the canonical Phase 46 semantic verification lane</name>
  <files>mix.exs, lib/mix/tasks/scoria.test.semantic_fast_path.ex, lib/mix/tasks/test.semantic_fast_path.ex</files>
  <read_first>
    <file>.planning/phases/46-operator-evidence-and-verification/46-CONTEXT.md</file>
    <file>.planning/phases/46-operator-evidence-and-verification/46-RESEARCH.md</file>
    <file>.planning/phases/45-compatibility-and-invalidation-engine/45-00-SUMMARY.md</file>
    <file>mix.exs</file>
    <file>lib/mix/tasks/test.adoption.ex</file>
  </read_first>
  <behavior>
    - Test 1: `mix test.semantic_fast_path --help` resolves in `MIX_ENV=test` via `preferred_envs`.
    - Test 2: the semantic lane exposes a stable `semantic_fast_path_test_files/0` function with the bounded file list expected by its discoverability test.
    - Test 3: the task implementation does not mention `5432` as a trusted proof lane while Phase 45's blocker remains unresolved.
  </behavior>
  <action>Per D-16, D-17, D-18, and the Phase 45 Wave 0 summary, add `Mix.Tasks.Scoria.Test.SemanticFastPath` plus `Mix.Tasks.Test.SemanticFastPath` by copying the adoption-lane pattern exactly and renaming only the task/module identifiers and file-list function. In `mix.exs`, add `\"scoria.test.semantic_fast_path\": :test` and `\"test.semantic_fast_path\": :test` to `preferred_envs`. Preserve `SCORIA_DB_PORT=55432` as the canonical executable verification lane for this phase by keeping the task bounded to semantic fast-path files only; do not silently switch the proof story back to `5432`, and do not widen the lane to the broad suite.</action>
  <acceptance_criteria>
    <criterion>`mix.exs` contains both `\"scoria.test.semantic_fast_path\": :test` and `\"test.semantic_fast_path\": :test`</criterion>
    <criterion>`lib/mix/tasks/scoria.test.semantic_fast_path.ex` exports `semantic_fast_path_test_files/0` and `run/1`</criterion>
    <criterion>`lib/mix/tasks/test.semantic_fast_path.ex` calls `Mix.Tasks.Scoria.Test.SemanticFastPath.run/1`</criterion>
    <criterion>`lib/mix/tasks/scoria.test.semantic_fast_path.ex` does not contain the string `5432`</criterion>
  </acceptance_criteria>
  <verify>
    <automated>SCORIA_DB_PORT=55432 SCORIA_DB_PASSWORD=postgres MIX_ENV=test mix test test/mix/tasks/test.semantic_fast_path_test.exs --trace</automated>
  </verify>
  <done>The repo has a named semantic verification lane wired to `:test`, and the Phase 46 proof posture no longer depends on implied suite knowledge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a discoverability test that locks the semantic lane inventory</name>
  <files>test/mix/tasks/test.semantic_fast_path_test.exs</files>
  <read_first>
    <file>.planning/phases/46-operator-evidence-and-verification/46-VALIDATION.md</file>
    <file>test/mix/tasks/test.adoption_test.exs</file>
    <file>lib/mix/tasks/scoria.test.semantic_fast_path.ex</file>
    <file>lib/mix/tasks/test.semantic_fast_path.ex</file>
  </read_first>
  <behavior>
    - Test 1: the lane test fails if the canonical file list drifts away from the semantic fast-path contract.
    - Test 2: the wrapper task is loaded and callable by both task names.
  </behavior>
  <action>Per D-16 through D-19 and the validation contract, create `test/mix/tasks/test.semantic_fast_path_test.exs` as the semantic analogue of `test/mix/tasks/test.adoption_test.exs`. Assert that the expected bounded file list includes `test/scoria/runtime/semantic_fast_path_test.exs`, `test/scoria/semantic_cache/lookup_test.exs`, `test/scoria/semantic_cache/invalidation_test.exs`, `test/scoria_web/live/workflow_live_test.exs`, `test/scoria_web/components/runtime_detail_drawer_component_test.exs`, and the lane test itself. Keep the file list explicit in the test so later plans must intentionally extend it when they add workflow notebook proof.</action>
  <acceptance_criteria>
    <criterion>`test/mix/tasks/test.semantic_fast_path_test.exs` asserts `Code.ensure_loaded?(Mix.Tasks.Scoria.Test.SemanticFastPath)`</criterion>
    <criterion>`test/mix/tasks/test.semantic_fast_path_test.exs` asserts `function_exported?(Mix.Tasks.Scoria.Test.SemanticFastPath, :semantic_fast_path_test_files, 0)`</criterion>
    <criterion>`test/mix/tasks/test.semantic_fast_path_test.exs` includes `test/scoria/runtime/semantic_fast_path_test.exs` and `test/scoria_web/live/workflow_live_test.exs` in `expected_files`</criterion>
    <criterion>`test/mix/tasks/test.semantic_fast_path_test.exs` includes `Mix.Task.get(\"test.semantic_fast_path\")`</criterion>
  </acceptance_criteria>
  <verify>
    <automated>SCORIA_DB_PORT=55432 SCORIA_DB_PASSWORD=postgres MIX_ENV=test mix test test/mix/tasks/test.semantic_fast_path_test.exs --trace</automated>
  </verify>
  <done>The semantic verification lane has an executable guard that prevents accidental drift in command naming or file coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer CLI -> verification lane | Human operators trust the named Mix task output as milestone proof |
| Verification lane -> database | Semantic proofs depend on the pgvector-backed test database and can be misleading if they run against the wrong environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-00-01 | Tampering | verification environment | mitigate | Preserve `SCORIA_DB_PORT=55432` as the explicit semantic proof lane per D-16 and the Phase 45 summary instead of implying that `5432` is ready. |
| T-46-00-02 | Repudiation | Mix task discoverability | mitigate | Add a task test that asserts both task names and the exact file inventory. |
| T-46-00-03 | Spoofing | proof command naming | mitigate | Keep one canonical user-facing command, `mix test.semantic_fast_path`, backed by a matching `scoria.test.semantic_fast_path` implementation. |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 SCORIA_DB_PASSWORD=postgres MIX_ENV=test mix test test/mix/tasks/test.semantic_fast_path_test.exs --trace`
</verification>

<success_criteria>
- Phase 46 has an explicit semantic proof command before later plans extend its file list.
- The plan records the real pgvector-backed verification lane instead of hiding the `5432` blocker.
</success_criteria>

<output>
After completion, create `.planning/phases/46-operator-evidence-and-verification/46-00-SUMMARY.md`
</output>
