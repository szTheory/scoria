---
phase: 46-operator-evidence-and-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 46-01
files_modified:
  - lib/scoria_web/live/orchestrator_live.ex
  - lib/scoria_web/components/runtime_detail_drawer_component.ex
  - test/scoria_web/live/orchestrator_live_test.exs
  - test/scoria_web/components/runtime_detail_drawer_component_test.exs
autonomous: true
requirements:
  - EVID-01
user_setup: []
must_haves:
  truths:
    - "The runtime surface answers what happened, whether Scoria reused or fell back, why, and where to click next."
    - "The runtime drawer stays summary-first and does not duplicate the workflow notebook."
    - "Fallback remains visible evidence instead of disappearing behind a generic miss."
  artifacts:
    - path: "lib/scoria_web/live/orchestrator_live.ex"
      provides: "Runtime drawer assigns that include semantic summary truth and workflow deep links"
    - path: "lib/scoria_web/components/runtime_detail_drawer_component.ex"
      provides: "Compact semantic summary UI"
    - path: "test/scoria_web/live/orchestrator_live_test.exs"
      provides: "Runtime drawer loading-path proof"
    - path: "test/scoria_web/components/runtime_detail_drawer_component_test.exs"
      provides: "Summary-first rendering proof"
  key_links:
    - from: "lib/scoria_web/live/orchestrator_live.ex"
      to: "lib/scoria_web/components/runtime_detail_drawer_component.ex"
      via: "runtime_drawer.semantic"
      pattern: "semantic"
    - from: "lib/scoria_web/components/runtime_detail_drawer_component.ex"
      to: "/workflows/:id"
      via: "workflow evidence link"
      pattern: "View workflow evidence"
---

<objective>
Project the curated semantic-evidence contract into the runtime presence surface without turning it into a second notebook.

Purpose: D-01 through D-03 and D-11 require the runtime drawer to stay concise, reason-coded, and deep-link oriented.
Output: runtime drawer assigns and markup showing `lookup_status`, fallback outcome, lane, scope, one decisive reason chip, and links to workflow evidence and origin run when relevant.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-operator-evidence-and-verification/46-CONTEXT.md
@.planning/phases/46-operator-evidence-and-verification/46-RESEARCH.md
@.planning/phases/46-operator-evidence-and-verification/46-PATTERNS.md
@lib/scoria/runtime.ex
@lib/scoria/runtime/run_detail.ex
@lib/scoria_web/live/orchestrator_live.ex
@lib/scoria_web/components/runtime_detail_drawer_component.ex
@test/scoria_web/components/runtime_detail_drawer_component_test.exs

<interfaces>
From [lib/scoria_web/live/orchestrator_live.ex](/Users/jon/projects/scoria/lib/scoria_web/live/orchestrator_live.ex:875):
```elixir
%{
  id: inst.id,
  status: status,
  host_session_id: inst.host_session_id,
  transport_kind: inst.transport_kind,
  terminal_offline_reason: inst.terminal_offline_reason,
  current_run_id: inst.current_run_id
}
```

From [lib/scoria_web/components/runtime_detail_drawer_component.ex](/Users/jon/projects/scoria/lib/scoria_web/components/runtime_detail_drawer_component.ex:4):
```elixir
attr(:drawer, :map, default: nil)
def render(assigns) do
  ~H"""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Feed the runtime drawer one semantic summary assign from the curated contract</name>
  <files>lib/scoria_web/live/orchestrator_live.ex, test/scoria_web/live/orchestrator_live_test.exs</files>
  <read_first>
    <file>.planning/phases/46-operator-evidence-and-verification/46-CONTEXT.md</file>
    <file>lib/scoria_web/live/orchestrator_live.ex</file>
    <file>lib/scoria/runtime.ex</file>
    <file>lib/scoria/runtime/run_detail.ex</file>
    <file>test/scoria_web/live/orchestrator_live_test.exs</file>
  </read_first>
  <behavior>
    - Test 1: runtime rows with `current_run_id` enrich the drawer with semantic summary data from `Runtime.get_run_detail!/1`.
    - Test 2: runtime rows without a current run still render the existing drawer shell without semantic errors.
    - Test 3: the drawer receives only summary fields and deep links, not the full compatibility matrix or raw JSON payloads.
  </behavior>
  <action>Per D-01, D-02, D-03, D-06, and D-11, extend the runtime drawer loading path in `lib/scoria_web/live/orchestrator_live.ex` so a runtime with `current_run_id` loads the curated `semantic_evidence` from `Runtime.get_run_detail!/1` and projects a compact `semantic` map onto the drawer assign. Populate exact summary keys such as `lookup_status`, `fallback_outcome`, `lane_key`, `scope_kind`, `scope_reason`, `reason_code`, `workflow_href`, and `origin_run_href` when relevant. Do not query semantic entry tables directly in the LiveView, and do not attach the full notebook groups here. Expand `test/scoria_web/live/orchestrator_live_test.exs` to mount the LiveView, exercise a runtime row with and without `current_run_id`, and assert the drawer-loading path integrates `Runtime.get_run_detail!/1` without breaking the existing shell.</action>
  <acceptance_criteria>
    <criterion>`lib/scoria_web/live/orchestrator_live.ex` calls `Runtime.get_run_detail!/1` or a helper wrapping it when `current_run_id` is present</criterion>
    <criterion>`lib/scoria_web/live/orchestrator_live.ex` builds a nested drawer field named `semantic` or `semantic_summary`</criterion>
    <criterion>The drawer projection includes exact keys `lookup_status`, `lane_key`, `scope_kind`, and `scope_reason`</criterion>
    <criterion>The drawer projection includes a workflow deep link string containing `/workflows/`</criterion>
    <criterion>`test/scoria_web/live/orchestrator_live_test.exs` mounts the LiveView and asserts the drawer semantic summary for a runtime with `current_run_id`</criterion>
  </acceptance_criteria>
  <verify>
    <automated>SCORIA_DB_PORT=55432 SCORIA_DB_PASSWORD=postgres MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_test.exs --trace</automated>
  </verify>
  <done>The runtime drawer input is sourced from the curated semantic-evidence contract instead of ad hoc semantic logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render a compact runtime semantic summary with explicit fallback evidence and deep links</name>
  <files>lib/scoria_web/components/runtime_detail_drawer_component.ex, test/scoria_web/components/runtime_detail_drawer_component_test.exs</files>
  <read_first>
    <file>.planning/phases/46-operator-evidence-and-verification/46-CONTEXT.md</file>
    <file>lib/scoria_web/components/runtime_detail_drawer_component.ex</file>
    <file>test/scoria_web/components/runtime_detail_drawer_component_test.exs</file>
    <file>lib/scoria_web/live/orchestrator_live.ex</file>
  </read_first>
  <behavior>
    - Test 1: hit outcomes render `lookup_status`, lane, scope, decisive reason, and workflow evidence links.
    - Test 2: bypass/miss/reject outcomes render an explicit fallback sentence rather than hiding the fast-path evaluation.
    - Test 3: actor identity is only shown when `scope_kind == "actor_scoped"` or equivalent scope reasoning requires it.
  </behavior>
  <action>Per D-02, D-03, D-05, D-06, D-08, D-11, and D-12, extend `lib/scoria_web/components/runtime_detail_drawer_component.ex` with one compact semantic block below the existing host-session/transport cards. Render exact summary fields from the drawer assign: `lookup_status`, a fallback sentence, `lane_key`, `scope_kind`, `scope_reason`, one decisive reason chip derived from `eligibility_reason_code` or `lookup_reason_code`, and links labeled `View workflow evidence` plus `View origin run` when an origin run exists. Keep the component summary-only: no compatibility checklist, no lifecycle event timeline, and no raw JSON. Expand `test/scoria_web/components/runtime_detail_drawer_component_test.exs` with one hit case and one fallback case that assert those exact strings and refute notebook-only content.</action>
  <acceptance_criteria>
    <criterion>`lib/scoria_web/components/runtime_detail_drawer_component.ex` contains the label `View workflow evidence`</criterion>
    <criterion>`lib/scoria_web/components/runtime_detail_drawer_component.ex` renders `lookup_status` and `scope_kind`</criterion>
    <criterion>`test/scoria_web/components/runtime_detail_drawer_component_test.exs` asserts a fallback string such as `Normal runtime path executed`</criterion>
    <criterion>`test/scoria_web/components/runtime_detail_drawer_component_test.exs` refutes notebook-only strings like `Advanced raw evidence`</criterion>
  </acceptance_criteria>
  <verify>
    <automated>SCORIA_DB_PORT=55432 SCORIA_DB_PASSWORD=postgres MIX_ENV=test mix test test/scoria_web/components/runtime_detail_drawer_component_test.exs --trace</automated>
  </verify>
  <done>The runtime surface stays summary-first while making semantic reuse or fallback explicit and actionable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Curated runtime detail -> runtime drawer | Summary-only UI must not widen scope or hide non-hit semantics |
| Operator click-through -> workflow page | Runtime deep links point from summary evidence to canonical deep inspection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-02-01 | Information Disclosure | runtime drawer | mitigate | Show actor data only when `scope_kind` is actor-scoped or materially explanatory per D-12. |
| T-46-02-02 | Spoofing | runtime summary copy | mitigate | Render exact stored nouns (`bypass`, `miss`, `reject`, `hit`) and reason codes from the curated contract instead of freehand UI-only taxonomy. |
| T-46-02-03 | Tampering | fallback semantics | mitigate | Include explicit fallback wording and a workflow evidence link for non-hit outcomes so fallback cannot be mistaken for absence. |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 SCORIA_DB_PASSWORD=postgres MIX_ENV=test mix test test/scoria_web/components/runtime_detail_drawer_component_test.exs --trace`
</verification>

<success_criteria>
- Operators can answer first-line runtime questions from one compact drawer.
- The runtime surface remains a summary surface and links to the canonical workflow notebook for depth.
</success_criteria>

<output>
After completion, create `.planning/phases/46-operator-evidence-and-verification/46-02-SUMMARY.md`
</output>
