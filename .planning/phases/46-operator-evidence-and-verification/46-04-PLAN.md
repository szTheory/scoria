---
phase: 46-operator-evidence-and-verification
plan: 04
type: execute
wave: 3
depends_on:
  - 46-00
  - 46-02
  - 46-03
files_modified:
  - lib/mix/tasks/scoria.test.semantic_fast_path.ex
  - test/mix/tasks/test.semantic_fast_path_test.exs
  - docs/operator_verification.md
  - test/scoria/adoption_surface_test.exs
autonomous: true
requirements:
  - PROOF-01
  - EVID-01
user_setup: []
must_haves:
  truths:
    - "One named verification lane proves partitioning, fallback, invalidation, and operator-surface projection together."
    - "The docs and source assertions tell the same verification story as the shipped Mix task."
    - "Support truth points users to `mix test.semantic_fast_path` instead of broad or ambiguous alternatives."
  artifacts:
    - path: "lib/mix/tasks/scoria.test.semantic_fast_path.ex"
      provides: "Final semantic proof lane inventory including UI/operator evidence tests"
    - path: "docs/operator_verification.md"
      provides: "Golden-path semantic verification and troubleshooting guide"
    - path: "test/scoria/adoption_surface_test.exs"
      provides: "Docs/source contract assertions for the new lane"
  key_links:
    - from: "docs/operator_verification.md"
      to: "lib/mix/tasks/scoria.test.semantic_fast_path.ex"
      via: "canonical command text"
      pattern: "mix test\\.semantic_fast_path"
    - from: "test/scoria/adoption_surface_test.exs"
      to: "docs/operator_verification.md"
      via: "exact string assertions"
      pattern: "semantic_fast_path"
---

<objective>
Close the phase with one checked semantic proof lane and matching docs.

Purpose: D-16 through D-19 require executable support truth, and the user-facing lane must prove the same fallback, partitioning, invalidation, and operator vocabulary that the UI now renders.
Output: final semantic Mix task file list, updated operator verification docs, and adoption-surface assertions that keep the docs and task discoverable.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-operator-evidence-and-verification/46-CONTEXT.md
@.planning/phases/46-operator-evidence-and-verification/46-VALIDATION.md
@docs/operator_verification.md
@lib/mix/tasks/scoria.test.semantic_fast_path.ex
@test/mix/tasks/test.semantic_fast_path_test.exs
@test/scoria/adoption_surface_test.exs

<interfaces>
From [test/scoria/adoption_surface_test.exs](/Users/jon/projects/scoria/test/scoria/adoption_surface_test.exs:84):
```elixir
test "operator verification guide documents the core automated lane without knowledge requirements" do
  content = File.read!(@operator_guide)
  assert content =~ "mix test.adoption"
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finalize the semantic proof lane inventory around shipped operator evidence</name>
  <files>lib/mix/tasks/scoria.test.semantic_fast_path.ex, test/mix/tasks/test.semantic_fast_path_test.exs</files>
  <read_first>
    <file>.planning/phases/46-operator-evidence-and-verification/46-VALIDATION.md</file>
    <file>lib/mix/tasks/scoria.test.semantic_fast_path.ex</file>
    <file>test/mix/tasks/test.semantic_fast_path_test.exs</file>
    <file>test/scoria/runtime/semantic_fast_path_test.exs</file>
    <file>test/scoria/semantic_cache/lookup_test.exs</file>
    <file>test/scoria/semantic_cache/invalidation_test.exs</file>
    <file>test/scoria_web/live/orchestrator_live_test.exs</file>
    <file>test/scoria_web/components/runtime_detail_drawer_component_test.exs</file>
    <file>test/scoria_web/components/semantic_evidence_notebook_component_test.exs</file>
    <file>test/scoria_web/live/workflow_live_test.exs</file>
  </read_first>
  <behavior>
    - Test 1: the named lane runs semantic runtime, lookup, invalidation, orchestrator runtime-surface wiring, runtime drawer, semantic notebook component, workflow notebook, and task discoverability tests together.
    - Test 2: the task-level verify stays smaller than the full named lane, while the lane remains bounded and does not silently expand to `mix test`.
  </behavior>
  <action>Per D-16, D-17, and the validation matrix, update `lib/mix/tasks/scoria.test.semantic_fast_path.ex` and its test so the final file inventory includes the runtime semantic path tests, lookup tests, invalidation tests, `test/scoria_web/live/orchestrator_live_test.exs`, runtime drawer component tests, `test/scoria_web/components/semantic_evidence_notebook_component_test.exs`, workflow LiveView tests, and the lane-discoverability test itself. Keep the explicit file list in both the task and the test. Do not add unrelated adoption or knowledge lane files here. Reserve `mix test.semantic_fast_path` for the wave/phase gate; for this task’s local verify, use the smaller lane-inventory task test so feedback stays fast while still proving the final file list changed as intended.</action>
  <acceptance_criteria>
    <criterion>`semantic_fast_path_test_files/0` includes `test/scoria/runtime/semantic_fast_path_test.exs`</criterion>
    <criterion>`semantic_fast_path_test_files/0` includes `test/scoria_web/live/orchestrator_live_test.exs`</criterion>
    <criterion>`semantic_fast_path_test_files/0` includes `test/scoria_web/components/runtime_detail_drawer_component_test.exs`</criterion>
    <criterion>`semantic_fast_path_test_files/0` includes `test/scoria_web/components/semantic_evidence_notebook_component_test.exs`</criterion>
    <criterion>`semantic_fast_path_test_files/0` includes `test/scoria_web/live/workflow_live_test.exs`</criterion>
    <criterion>`semantic_fast_path_test_files/0` does not include `test/scoria/adoption_surface_test.exs`</criterion>
  </acceptance_criteria>
  <verify>
    <automated>SCORIA_DB_PORT=55432 SCORIA_DB_PASSWORD=postgres MIX_ENV=test mix test test/mix/tasks/test.semantic_fast_path_test.exs --trace</automated>
  </verify>
  <done>The named semantic lane now proves the shipped operator evidence and cache semantics end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align docs and source assertions to the semantic fast-path support story</name>
  <files>docs/operator_verification.md, test/scoria/adoption_surface_test.exs</files>
  <read_first>
    <file>.planning/phases/46-operator-evidence-and-verification/46-CONTEXT.md</file>
    <file>docs/operator_verification.md</file>
    <file>test/scoria/adoption_surface_test.exs</file>
    <file>lib/mix/tasks/scoria.test.semantic_fast_path.ex</file>
  </read_first>
  <behavior>
    - Test 1: docs mention `mix test.semantic_fast_path` as the canonical troubleshooting lane for v2.1.
    - Test 2: docs use the same semantic nouns and fallback story as the shipped UI.
    - Test 3: adoption-surface tests fail if the operator verification guide drifts away from the lane or vocabulary.
  </behavior>
  <action>Per D-18 and D-19, update `docs/operator_verification.md` so it adds a semantic fast-path section with the concrete command `SCORIA_DB_PORT=55432 SCORIA_DB_PASSWORD=postgres MIX_ENV=test mix test.semantic_fast_path`, explains that this is the canonical `v2.1` troubleshooting lane, and uses the exact operator nouns `bypass`, `miss`, `reject`, `hit`, `active`, `stale`, `invalidated`, and `writeback_rejected`. Keep `mix test.adoption` positioned as the public runtime/adoption proof and `mix test` as the broader umbrella. Extend `test/scoria/adoption_surface_test.exs` so it asserts those exact strings and the semantic lane command appear in the guide.</action>
  <acceptance_criteria>
    <criterion>`docs/operator_verification.md` contains `mix test.semantic_fast_path`</criterion>
    <criterion>`docs/operator_verification.md` contains `SCORIA_DB_PORT=55432`</criterion>
    <criterion>`docs/operator_verification.md` contains each semantic noun `bypass`, `miss`, `reject`, and `hit`</criterion>
    <criterion>`test/scoria/adoption_surface_test.exs` asserts `mix test.semantic_fast_path` and `SCORIA_DB_PORT=55432` in the operator guide test</criterion>
  </acceptance_criteria>
  <verify>
    <automated>SCORIA_DB_PORT=55432 SCORIA_DB_PASSWORD=postgres MIX_ENV=test mix test test/scoria/adoption_surface_test.exs test/mix/tasks/test.semantic_fast_path_test.exs --trace</automated>
  </verify>
  <done>The docs and source assertions tell the same semantic verification story as the shipped Mix task and UI vocabulary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Support docs -> operator action | Users follow documented commands and vocabulary as the system of guidance |
| Mix lane inventory -> milestone proof | Final proof claims rely on the bounded semantic lane including the right test files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-04-01 | Repudiation | proof documentation | mitigate | Keep docs and adoption-surface tests aligned on one exact command and one exact vocabulary set. |
| T-46-04-02 | Spoofing | verification scope | mitigate | Assert the explicit file inventory in the lane test so `mix test.semantic_fast_path` cannot silently become a broad or incomplete proof lane. |
| T-46-04-03 | Tampering | fallback/rejection semantics | mitigate | Require docs to name `bypass`, `miss`, `reject`, `hit`, `stale`, `invalidated`, and `writeback_rejected` explicitly so hidden semantics cannot creep back in. |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 SCORIA_DB_PASSWORD=postgres MIX_ENV=test mix test.semantic_fast_path --trace`
- `SCORIA_DB_PORT=55432 SCORIA_DB_PASSWORD=postgres MIX_ENV=test mix test test/scoria/adoption_surface_test.exs test/mix/tasks/test.semantic_fast_path_test.exs --trace`
</verification>

<success_criteria>
- `mix test.semantic_fast_path` is the single named proof answer for `v2.1`.
- Docs, tests, and UI vocabulary all align on the same semantic evidence story.
</success_criteria>

<output>
After completion, create `.planning/phases/46-operator-evidence-and-verification/46-04-SUMMARY.md`
</output>
