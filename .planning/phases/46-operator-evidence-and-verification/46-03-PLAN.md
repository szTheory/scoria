---
phase: 46-operator-evidence-and-verification
plan: 03
type: execute
wave: 2
depends_on:
  - 46-01
files_modified:
  - lib/scoria_web/components/semantic_evidence_notebook_component.ex
  - lib/scoria_web/components/workflow_detail_panel_component.ex
  - lib/scoria_web/live/workflow_live/show.ex
  - test/scoria_web/components/semantic_evidence_notebook_component_test.exs
  - test/scoria_web/live/workflow_live_test.exs
autonomous: true
requirements:
  - EVID-01
user_setup: []
must_haves:
  truths:
    - "The workflow page is the canonical deep-inspection surface for semantic evidence."
    - "Operators can inspect verdict, compatibility, provenance, lifecycle, and candidate refusal truth on one page."
    - "Rejected or stale candidates remain inspectable as found-but-refused evidence instead of collapsing into plain misses."
  artifacts:
    - path: "lib/scoria_web/components/semantic_evidence_notebook_component.ex"
      provides: "Dedicated semantic evidence notebook UI"
    - path: "test/scoria_web/components/semantic_evidence_notebook_component_test.exs"
      provides: "Notebook component rendering proof"
    - path: "lib/scoria_web/components/workflow_detail_panel_component.ex"
      provides: "Right-rail shell that hosts semantic evidence alongside existing workflow evidence"
    - path: "lib/scoria_web/live/workflow_live/show.ex"
      provides: "Workflow LiveView wiring for semantic notebook assigns"
    - path: "test/scoria_web/live/workflow_live_test.exs"
      provides: "Workflow deep-evidence projection proof"
  key_links:
    - from: "lib/scoria_web/live/workflow_live/show.ex"
      to: "lib/scoria_web/components/semantic_evidence_notebook_component.ex"
      via: "run_detail.semantic_evidence"
      pattern: "semantic_evidence"
    - from: "lib/scoria_web/components/workflow_detail_panel_component.ex"
      to: "lib/scoria_web/components/semantic_evidence_notebook_component.ex"
      via: "panel composition"
      pattern: "SemanticEvidenceNotebookComponent"
---

<objective>
Make the workflow run page the canonical semantic evidence notebook.

Purpose: D-01, D-04, D-09, D-10, D-13, D-14, and D-15 require the workflow page to own dense but comprehensible semantic inspection while preserving one shared evidence contract.
Output: a semantic notebook component and workflow-panel wiring that show summary, compatibility checklist, provenance links, lifecycle truth, entry events, and a raw metadata escape hatch.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-operator-evidence-and-verification/46-CONTEXT.md
@.planning/phases/46-operator-evidence-and-verification/46-RESEARCH.md
@.planning/phases/46-operator-evidence-and-verification/46-PATTERNS.md
@lib/scoria/runtime.ex
@lib/scoria/runtime/run_detail.ex
@lib/scoria_web/live/workflow_live/show.ex
@lib/scoria_web/components/workflow_detail_panel_component.ex
@lib/scoria_web/components/replay_evidence_notebook_component.ex
@test/scoria_web/live/workflow_live_test.exs

<interfaces>
From [lib/scoria_web/components/workflow_detail_panel_component.ex](/Users/jon/projects/scoria/lib/scoria_web/components/workflow_detail_panel_component.ex:6):
```elixir
attr :step, :map, default: nil
attr :checkpoint, :map, default: nil
attr :comparison, :map, default: nil
def workflow_detail_panel(assigns) do
```

From [lib/scoria_web/components/replay_evidence_notebook_component.ex](/Users/jon/projects/scoria/lib/scoria_web/components/replay_evidence_notebook_component.ex:7):
```elixir
def render(assigns) do
  ~H"""
  <section class="mt-4 rounded-xl border ...">
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a semantic evidence notebook component with progressive disclosure layers</name>
  <files>lib/scoria_web/components/semantic_evidence_notebook_component.ex, test/scoria_web/components/semantic_evidence_notebook_component_test.exs</files>
  <read_first>
    <file>.planning/phases/46-operator-evidence-and-verification/46-CONTEXT.md</file>
    <file>lib/scoria_web/components/replay_evidence_notebook_component.ex</file>
    <file>lib/scoria/runtime/run_detail.ex</file>
    <file>test/scoria_web/components/runtime_detail_drawer_component_test.exs</file>
  </read_first>
  <behavior>
    - Test 1: the notebook leads with verdict, fallback, age, lane, scope, and reason before deeper fields.
    - Test 2: the notebook includes grouped sections for compatibility, provenance, lifecycle, and append-only events.
    - Test 3: raw JSON remains behind a details/summary escape hatch.
  </behavior>
  <action>Create `lib/scoria_web/components/semantic_evidence_notebook_component.ex` as a new function component modeled after the replay notebook structure, but wired to the semantic evidence contract from Plan 46-01. Per D-09 and D-10, implement four disclosure layers in concrete groups: `summary` (verdict, fallback, age, lane, scope, decisive reason), `compatibility` (prompt, policy, source, freshness, similarity), `provenance` (source run, cache entry, writeback event, invalidation event), `lifecycle` (status, `expires_at`, `invalidated_at`, `hit_count`, `last_hit_at`, append-only events), plus a raw JSON details block. Use the exact Phase 44/45 nouns from D-05; do not invent friendly replacement taxonomy. Add `test/scoria_web/components/semantic_evidence_notebook_component_test.exs` with component-render assertions that prove the new section labels, append-only event rendering, and `Advanced raw evidence` disclosure block before the workflow LiveView wiring lands.</action>
  <acceptance_criteria>
    <criterion>`lib/scoria_web/components/semantic_evidence_notebook_component.ex` contains the section labels `Compatibility`, `Provenance`, and `Lifecycle`</criterion>
    <criterion>`lib/scoria_web/components/semantic_evidence_notebook_component.ex` contains `Advanced raw evidence` inside a `details` block</criterion>
    <criterion>`lib/scoria_web/components/semantic_evidence_notebook_component.ex` renders `lookup_status` or an equivalent verdict field without renaming the persisted nouns</criterion>
    <criterion>`lib/scoria_web/components/semantic_evidence_notebook_component.ex` includes append-only event rendering for semantic entry events</criterion>
    <criterion>`test/scoria_web/components/semantic_evidence_notebook_component_test.exs` asserts `Compatibility`, `Provenance`, `Lifecycle`, and `Advanced raw evidence`</criterion>
  </acceptance_criteria>
  <verify>
    <automated>SCORIA_DB_PORT=55432 SCORIA_DB_PASSWORD=postgres MIX_ENV=test mix test test/scoria_web/components/semantic_evidence_notebook_component_test.exs --trace</automated>
  </verify>
  <done>The workflow page has a dedicated semantic notebook component that can render the full semantic-evidence contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the workflow detail rail to render the canonical semantic notebook</name>
  <files>lib/scoria_web/components/workflow_detail_panel_component.ex, lib/scoria_web/live/workflow_live/show.ex, test/scoria_web/live/workflow_live_test.exs</files>
  <read_first>
    <file>.planning/phases/46-operator-evidence-and-verification/46-CONTEXT.md</file>
    <file>lib/scoria_web/components/workflow_detail_panel_component.ex</file>
    <file>lib/scoria_web/live/workflow_live/show.ex</file>
    <file>test/scoria_web/live/workflow_live_test.exs</file>
    <file>lib/scoria_web/components/semantic_evidence_notebook_component.ex</file>
  </read_first>
  <behavior>
    - Test 1: workflow pages with semantic evidence render the notebook summary strip and provenance/lifecycle groups.
    - Test 2: reject/stale candidate cases remain inspectable as refused candidates and mention fallback explicitly.
    - Test 3: the right rail still works for existing replay and promotion behaviors.
  </behavior>
  <action>Per D-01, D-04, D-10, D-13, D-14, and D-15, update `lib/scoria_web/live/workflow_live/show.ex` to assign `run_detail.semantic_evidence` and pass it through `WorkflowDetailPanelComponent`. Update `lib/scoria_web/components/workflow_detail_panel_component.ex` to host the new semantic notebook beneath the existing step/replay shell instead of replacing the panel. Expand `test/scoria_web/live/workflow_live_test.exs` with semantic hit and semantic reject/stale assertions that check for exact summary strings like `Normal runtime path executed`, status chips for `active|stale|invalidated|writeback_rejected`, and provenance/lifecycle labels. Preserve existing replay evidence assertions; the new notebook must compose beside them rather than breaking them.</action>
  <acceptance_criteria>
    <criterion>`lib/scoria_web/live/workflow_live/show.ex` assigns or passes `semantic_evidence` from `@run_detail`</criterion>
    <criterion>`lib/scoria_web/components/workflow_detail_panel_component.ex` references `SemanticEvidenceNotebookComponent`</criterion>
    <criterion>`test/scoria_web/live/workflow_live_test.exs` asserts semantic notebook labels such as `Lifecycle` and `Provenance`</criterion>
    <criterion>`test/scoria_web/live/workflow_live_test.exs` asserts a non-hit fallback string rather than only checking `miss`</criterion>
  </acceptance_criteria>
  <verify>
    <automated>SCORIA_DB_PORT=55432 SCORIA_DB_PASSWORD=postgres MIX_ENV=test mix test test/scoria_web/live/workflow_live_test.exs --trace</automated>
  </verify>
  <done>The workflow page becomes the canonical semantic deep-inspection surface while preserving existing workflow evidence behaviors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Curated semantic DTO -> workflow notebook | Deep operator evidence must stay faithful to durable cache truth |
| Workflow notebook -> operator understanding | Dense UI can accidentally hide fallback or candidate refusal semantics if grouped poorly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-03-01 | Information Disclosure | workflow notebook | mitigate | Surface actor identity only when scope is narrowed or explanatory, and keep tenant-shared outcomes actor-light per D-12. |
| T-46-03-02 | Spoofing | notebook provenance | mitigate | Render provenance from the curated DTO assembled from entry rows and entry events, not LiveView-local joins or prose-only composition. |
| T-46-03-03 | Tampering | reject/stale evidence | mitigate | Keep `reject`, `stale`, and `invalidated` distinct in labels, lifecycle chips, and fallback copy so hidden rejection semantics cannot collapse into a miss. |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 SCORIA_DB_PASSWORD=postgres MIX_ENV=test mix test test/scoria_web/live/workflow_live_test.exs --trace`
</verification>

<success_criteria>
- The workflow page clearly owns the deep semantic evidence story.
- Operators can inspect provenance and lifecycle truth for both reused and refused candidates on one page.
</success_criteria>

<output>
After completion, create `.planning/phases/46-operator-evidence-and-verification/46-03-SUMMARY.md`
</output>
