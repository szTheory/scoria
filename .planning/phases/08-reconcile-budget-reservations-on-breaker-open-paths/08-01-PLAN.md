---
phase: 08-reconcile-budget-reservations-on-breaker-open-paths
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/sre/budget_engine.ex
  - test/scoria/sre/budget_engine_test.exs
  - test/scoria/sre_test.exs
autonomous: true
requirements:
  - SRE-01
  - SRE-02
must_haves:
  truths:
    - "Breaker-open exits reconcile the durable reservation row instead of leaving it in reserved state."
    - "Breaker-open reconciliation records zero actual usage and an explicit breaker-open outcome."
    - "The same durable reservation contract is reused by workflow and MCP execution seams."
  artifacts:
    - path: "lib/scoria/sre/budget_engine.ex"
      provides: "Shared reservation closeout contract for breaker-open outcomes"
    - path: "test/scoria/sre/budget_engine_test.exs"
      provides: "Unit coverage for zero-actual-unit breaker-open reconciliation"
  key_links:
    - from: "lib/scoria/sre/budget_engine.ex"
      to: "lib/scoria/sre.ex"
      via: "durable reservation reconciliation"
      pattern: "reconcile"
---

<objective>
Make breaker-open reconciliation an explicit, reusable reservation closeout contract before Phase 8 touches the runtime and MCP seams.

Purpose: keep the durable reservation semantics for breaker-open exits as simple and verifiable as the existing timeout and execution-failed paths.
Output: one shared BudgetEngine helper or equivalent explicit contract plus focused SRE regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-reconcile-budget-reservations-on-breaker-open-paths/08-RESEARCH.md
@.planning/phases/08-reconcile-budget-reservations-on-breaker-open-paths/08-PATTERNS.md
@lib/scoria/sre/budget_engine.ex
@lib/scoria/sre.ex
@test/scoria/sre/budget_engine_test.exs
@test/scoria/sre_test.exs

<interfaces>
From lib/scoria/sre/budget_engine.ex:
```elixir
def reconcile_usage(reservation, attrs)
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Codify Breaker-Open Reservation Closeout</name>
  <files>lib/scoria/sre/budget_engine.ex, test/scoria/sre/budget_engine_test.exs, test/scoria/sre_test.exs</files>
  <read_first>
    - lib/scoria/sre/budget_engine.ex
    - lib/scoria/sre.ex
    - test/scoria/sre/budget_engine_test.exs
    - test/scoria/sre_test.exs
    - .planning/phases/08-reconcile-budget-reservations-on-breaker-open-paths/08-RESEARCH.md
  </read_first>
  <action>Add an explicit breaker-open reconciliation entry point in `Scoria.SRE.BudgetEngine` or make the existing `reconcile_usage/2` contract unambiguous for this case, but keep the resulting durable state exact: `status` becomes `\"reconciled\"`, `actual_units` becomes `Decimal.new(\"0\")`, and metadata contains `\"outcome\" => \"breaker_open\"`. If you add a helper, it must accept the reservation plus optional metadata and delegate through the existing `Scoria.SRE.reconcile_usage/2` path rather than creating a second write path. Update the SRE unit tests so breaker-open closeout is asserted directly and existing timeout/execution-failed semantics still pass unchanged.</action>
  <acceptance_criteria>
    - `lib/scoria/sre/budget_engine.ex` contains `breaker_open`
    - `lib/scoria/sre/budget_engine.ex` contains either `def reconcile_breaker_open(` or explicit `metadata` merge logic for `\"outcome\" => \"breaker_open\"`
    - `test/scoria/sre/budget_engine_test.exs` asserts breaker-open reconciliation writes zero actual units
    - `test/scoria/sre_test.exs` asserts the reservation status is `reconciled` after breaker-open closeout
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/sre/budget_engine_test.exs test/scoria/sre_test.exs</automated>
  </verify>
  <done>Phase 8 has one durable reservation closeout contract that both execution seams can reuse for breaker-open exits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime/MCP outcome classification -> durable reservation state | Terminal execution outcomes become persistent billing and governance evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | reservation reconciliation | mitigate | Reuse the existing `Scoria.SRE.reconcile_usage/2` write path so breaker-open outcomes cannot drift into a second inconsistent persistence flow. |
| T-08-01-02 | Repudiation | reservation evidence | mitigate | Persist an explicit `breaker_open` outcome in metadata so zero-usage closeout remains auditable instead of looking like an unexplained release. |
</threat_model>

<verification>
- Breaker-open closeout writes `actual_units = 0`.
- Breaker-open closeout leaves the reservation in `reconciled` status.
- Existing reconciliation semantics for other terminal outcomes remain intact.
</verification>

<success_criteria>
- Phase 8 has a single, test-backed reservation contract for breaker-open exits.
</success_criteria>

<output>
After completion, create `.planning/phases/08-reconcile-budget-reservations-on-breaker-open-paths/08-01-SUMMARY.md`
</output>
