---
phase: 08-reconcile-budget-reservations-on-breaker-open-paths
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/scoria/mcp/executor.ex
  - test/scoria/mcp/executor_test.exs
autonomous: true
requirements:
  - SRE-01
  - SRE-02
  - SRE-03
must_haves:
  truths:
    - "MCP breaker-open exits reconcile the reservation before returning the breaker-open error."
    - "Breaker-open remains distinct from timeout and execution_failed telemetry and return values."
    - "Remote integration breaker-open paths now preserve durable zero-usage evidence."
  artifacts:
    - path: "lib/scoria/mcp/executor.ex"
      provides: "MCP breaker-open reservation reconciliation"
    - path: "test/scoria/mcp/executor_test.exs"
      provides: "Regression coverage for reserve -> breaker-open -> reconcile at the MCP seam"
  key_links:
    - from: "lib/scoria/mcp/executor.ex"
      to: "lib/scoria/sre/budget_engine.ex"
      via: "breaker-open closeout before telemetry and error return"
      pattern: "breaker_open|reconcile"
---

<objective>
Repair the MCP executor branch that currently returns breaker-open without reconciling its preflight reservation.

Purpose: make remote-MCP and tool-side external-effect blocking preserve the same durable reservation guarantees as timeout and execution-failure branches.
Output: executor branch fix and targeted MCP regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-reconcile-budget-reservations-on-breaker-open-paths/08-RESEARCH.md
@.planning/phases/08-reconcile-budget-reservations-on-breaker-open-paths/08-PATTERNS.md
@.planning/phases/08-reconcile-budget-reservations-on-breaker-open-paths/08-01-PLAN.md
@lib/scoria/mcp/executor.ex
@lib/scoria/sre/budget_engine.ex
@test/scoria/mcp/executor_test.exs

<interfaces>
From lib/scoria/mcp/executor.ex:
```elixir
def execute(tool_module, args, context, timeout \\ 5000)
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Reconcile MCP Reservations on Breaker-Open Returns</name>
  <files>lib/scoria/mcp/executor.ex, test/scoria/mcp/executor_test.exs</files>
  <read_first>
    - lib/scoria/mcp/executor.ex
    - test/scoria/mcp/executor_test.exs
    - lib/scoria/sre/budget_engine.ex
    - .planning/phases/08-reconcile-budget-reservations-on-breaker-open-paths/08-RESEARCH.md
  </read_first>
  <action>Update the `{:error, %{status: :breaker_open} = envelope}` branch in `Scoria.MCP.Executor.execute/4` so it reconciles any real reservation before emitting the failed telemetry event and returning `{:error, envelope}`. Use the Phase 8 breaker-open closeout contract from Plan 01 so the durable row ends as `reconciled` with `actual_units = 0` and metadata outcome `\"breaker_open\"`. Preserve the existing distinctions that matter downstream: breaker-open must still emit the failed telemetry event with `reason: :breaker_open`, return the breaker-open envelope rather than `:timeout` or `:execution_failed`, and skip reconciliation only when the execution context contains an audit-only outbox event instead of a reservation. Extend `test/scoria/mcp/executor_test.exs` to create a budget policy, open a remote-MCP breaker, then issue a second budget-governed call against the same remote endpoint and assert the durable reservation row is reconciled with zero actual usage and breaker-open metadata.</action>
  <acceptance_criteria>
    - `lib/scoria/mcp/executor.ex` contains a breaker-open branch that calls `reconcile_budget` or the Plan 01 breaker-open helper before returning `{:error, envelope}`
    - `test/scoria/mcp/executor_test.exs` asserts breaker-open reservation rows are `reconciled`
    - `test/scoria/mcp/executor_test.exs` asserts breaker-open reservation metadata contains `\"outcome\" => \"breaker_open\"`
    - `test/scoria/mcp/executor_test.exs` still distinguishes breaker-open from timeout and execution_failed
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/mcp/executor_test.exs</automated>
  </verify>
  <done>The MCP executor now closes durable budget state correctly when the breaker blocks the external call before the tool runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP execution seam -> durable budget evidence | Remote tool-control failures become persistent reservation evidence and telemetry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Denial of Service | MCP breaker-open path | mitigate | Reconcile blocked reservations immediately so remote breaker churn cannot accumulate false active usage. |
| T-08-03-02 | Repudiation | MCP telemetry and reservation evidence | mitigate | Keep breaker-open as a distinct telemetry reason and durable metadata outcome so operators can separate policy block from crash or timeout. |
</threat_model>

<verification>
- MCP breaker-open exits do not leave reservations in `reserved` state.
- MCP breaker-open exits still return a breaker-open envelope and failed telemetry reason.
- Audit-only paths without reservations remain untouched.
</verification>

<success_criteria>
- The MCP half of the `reserve -> breaker-open -> reconcile` gap is closed and regression-tested.
</success_criteria>

<output>
After completion, create `.planning/phases/08-reconcile-budget-reservations-on-breaker-open-paths/08-03-SUMMARY.md`
</output>
