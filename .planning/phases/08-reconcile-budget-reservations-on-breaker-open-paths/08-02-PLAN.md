---
phase: 08-reconcile-budget-reservations-on-breaker-open-paths
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/scoria/workflows/runtime.ex
  - test/scoria/workflows/runtime_test.exs
autonomous: true
requirements:
  - SRE-01
  - SRE-02
  - SRE-03
must_haves:
  truths:
    - "Workflow-step breaker-open exits reconcile the reservation before the step is failed."
    - "A breaker-open step still fails fast and still does not rerun the blocked side effect."
    - "Workflow durable evidence keeps the budget reservation id and breaker-open reason code together."
  artifacts:
    - path: "lib/scoria/workflows/runtime.ex"
      provides: "Workflow breaker-open reservation reconciliation"
    - path: "test/scoria/workflows/runtime_test.exs"
      provides: "Regression coverage for reserve -> breaker-open -> reconcile at the workflow seam"
  key_links:
    - from: "lib/scoria/workflows/runtime.ex"
      to: "lib/scoria/sre/budget_engine.ex"
      via: "breaker-open closeout before fail_step"
      pattern: "breaker_open|reconcile"
---

<objective>
Repair the workflow runtime branch that currently fails a breaker-open step without reconciling its preflight reservation.

Purpose: make `Scoria.Workflows.Runtime.execute_step/2` preserve durable spend governance even when the external-effect breaker blocks execution before the side effect starts.
Output: runtime branch fix and a targeted workflow regression test.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-reconcile-budget-reservations-on-breaker-open-paths/08-RESEARCH.md
@.planning/phases/08-reconcile-budget-reservations-on-breaker-open-paths/08-PATTERNS.md
@.planning/phases/08-reconcile-budget-reservations-on-breaker-open-paths/08-01-PLAN.md
@lib/scoria/workflows/runtime.ex
@lib/scoria/sre/budget_engine.ex
@test/scoria/workflows/runtime_test.exs

<interfaces>
From lib/scoria/workflows/runtime.ex:
```elixir
def execute_step(step_id, opts \\ [])
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Reconcile Workflow Reservations on Breaker-Open Failures</name>
  <files>lib/scoria/workflows/runtime.ex, test/scoria/workflows/runtime_test.exs</files>
  <read_first>
    - lib/scoria/workflows/runtime.ex
    - test/scoria/workflows/runtime_test.exs
    - lib/scoria/sre/budget_engine.ex
    - .planning/phases/08-reconcile-budget-reservations-on-breaker-open-paths/08-RESEARCH.md
  </read_first>
  <action>Update the `{:error, %{status: :breaker_open} = envelope}` branch in `Scoria.Workflows.Runtime.execute_step/2` so it reconciles the reservation before calling `Workflows.fail_step/2`. Use the Phase 8 breaker-open closeout contract from Plan 01 so the reservation ends with `actual_units = 0`, `status = \"reconciled\"`, and metadata outcome `\"breaker_open\"`. Keep the existing step failure envelope behavior intact: the step must still fail with the breaker-open reason code, preserve `budget_reservation_id` evidence via `attach_budget_evidence/2`, and never rerun the blocked side effect. Extend `test/scoria/workflows/runtime_test.exs` to open the breaker on the first external-effect step, execute a second budget-governed step against the same breaker key, then assert the resulting durable reservation row is reconciled and the second side effect never runs.</action>
  <acceptance_criteria>
    - `lib/scoria/workflows/runtime.ex` contains a breaker-open branch that calls `reconcile_budget` or the Plan 01 breaker-open helper before `Workflows.fail_step`
    - `test/scoria/workflows/runtime_test.exs` asserts breaker-open reservation rows are `reconciled`
    - `test/scoria/workflows/runtime_test.exs` asserts breaker-open reservation metadata contains `\"outcome\" => \"breaker_open\"`
    - `test/scoria/workflows/runtime_test.exs` still refutes receipt of the blocked side effect message
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/workflows/runtime_test.exs</automated>
  </verify>
  <done>The workflow runtime now closes durable budget state correctly when a breaker blocks execution before the side effect runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow execution seam -> durable budget evidence | A runtime control-flow branch decides whether reservation evidence is closed or left dangling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Denial of Service | workflow breaker-open path | mitigate | Reconcile and fail fast on breaker-open exits so blocked workflows cannot strand reserved budget and accumulate false active usage. |
| T-08-02-02 | Tampering | workflow error evidence | mitigate | Keep `budget_reservation_id` attached to the failure envelope while persisting the reconciled zero-usage outcome in durable state. |
</threat_model>

<verification>
- Workflow breaker-open exits do not leave reservations in `reserved` state.
- Workflow breaker-open exits still fail the step without executing the side effect.
- Workflow failure evidence still includes the durable reservation reference.
</verification>

<success_criteria>
- The workflow half of the `reserve -> breaker-open -> reconcile` gap is closed and regression-tested.
</success_criteria>

<output>
After completion, create `.planning/phases/08-reconcile-budget-reservations-on-breaker-open-paths/08-02-SUMMARY.md`
</output>
