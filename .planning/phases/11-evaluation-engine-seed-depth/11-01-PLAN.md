---
phase: 11-evaluation-engine-seed-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/dev_seed.exs
autonomous: true
requirements: [EVAL-04]
must_haves:
  truths:
    - "Running `mix run priv/repo/dev_seed.exs` populates every dashboard screen so none render thin/empty when clicked through"
    - "Re-running the seed does not double record counts (idempotent — safe to run repeatedly)"
    - "Approvals has ≥1 pending approval with a real UUID so the harness can open its detail modal"
    - "Connectors fleet includes a degraded connector so the overlay/state matrix has a non-healthy variant"
    - "Seed identities (tenant_id, session_id, connector_key) come from Scoria.SupportJourney, not inlined literals"
  artifacts:
    - path: "priv/repo/dev_seed.exs"
      provides: "Idempotent dashboard seed across Reviews, Incidents, Eval, Prompts, Approvals, Connectors, Workflows, Live Ops"
      contains: "SupportJourney spine — do not inline these values"
  key_links:
    - from: "priv/repo/dev_seed.exs"
      to: "Scoria.SupportJourney.tenant_id/0"
      via: "spine identity reuse"
      pattern: "SupportJourney\\.(tenant_id|session_id|connector_key)"
    - from: "priv/repo/dev_seed.exs"
      to: "Scoria.SRE.IncidentManager.open_incident/1"
      via: "incident seeding"
      pattern: "IncidentManager\\.open_incident"
---

<objective>
Create the net-new `priv/repo/dev_seed.exs` that populates every one of the 9 dashboard
screens with realistic, idempotent data so the harness (Plan 03) never screenshots a thin
or empty screen. Implements EVAL-04.

Purpose: Seed depth is the prerequisite for meaningful screenshots and critiques. The
SupportJourney spine keeps the dashboard's "Acme" story aligned with the support_copilot
gallery; the additional domains (incidents, review candidates, eval specs, prompt release
gates, degraded connectors) are added here because SupportJourney does not model them (D-08).
Output: One idempotent seed script that brings every screen to its UI-SPEC populated minimum.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase contract + research (read before touching anything)
@.planning/phases/11-evaluation-engine-seed-depth/11-UI-SPEC.md
@.planning/phases/11-evaluation-engine-seed-depth/11-RESEARCH.md
@.planning/phases/11-evaluation-engine-seed-depth/11-PATTERNS.md

# The exact analog seed to mirror structurally
@examples/support_copilot/priv/repo/dev_seed.exs
# Seed spine SSOT
@lib/scoria/support_journey.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spine + runtime/incidents/connectors seed core</name>
  <read_first>
    - examples/support_copilot/priv/repo/dev_seed.exs (structural analog — header, try/rescue per domain, IO.puts "  ✓ ..." convention)
    - lib/scoria/support_journey.ex (tenant_id/0 = "acme-corp", session_id/0 = "support-session-42", connector_key/0 = "billing", runtime_identity/0)
    - .planning/phases/11-evaluation-engine-seed-depth/11-RESEARCH.md §"Seed Depth: Screen-by-Screen Data Map" (Live Ops, Approvals, Workflows, Incidents, Connectors field requirements + the Creating an Incident code example)
    - lib/scoria/sre/incident_manager.ex + lib/scoria/sre/incident.ex (open_incident/1 required fields: tenant_id, incident_key, severity, status, summary, routing_class, dedupe_key, first_seen_at, last_seen_at)
    - .planning/phases/11-evaluation-engine-seed-depth/11-UI-SPEC.md §"Seed Depth Contract (EVAL-04)" (per-screen minimums)
  </read_first>
  <action>
    Create priv/repo/dev_seed.exs. Open with a header docstring mirroring the support_copilot
    analog (purpose line, the `mix run priv/repo/dev_seed.exs` invocation line, and a
    "Safe to run repeatedly" line). Immediately establish the spine with the load-bearing
    comment block `# SupportJourney spine — do not inline these values` (D-09) and bind
    tenant_id/session_id/connector_key from SupportJourney.tenant_id/0, session_id/0,
    connector_key/0 — never literals (anti-pattern per RESEARCH).
    Seed, each domain block wrapped in its own try/rescue that prints
    `  ! {domain} skipped: #{Exception.message(e)}` on failure so one broken lane does not
    abort the whole seed:
    (a) Runs / Workflows + Live Ops: use Scoria.start_run/2 with the SupportJourney runtime
        identity to create ≥4 runs spanning statuses completed, in_progress (start but do not
        complete), failed, and pending_approval (UI-SPEC minimum for Workflows; Live Ops
        metrics derive from these — non-zero counts per D-07).
    (b) Approvals: ensure ≥1 run leaves a pending remote approval for tenant "acme-corp"
        (start a run with an approval step and do NOT approve it). This pending approval MUST
        have a real UUID — Plan 03's Approvals overlay reads the first approval_inbox row ID
        from the DOM to open the detail modal (Pitfall 2). Print `  ✓ refund-review run (pending approval)`.
    (c) Incidents: use Scoria.SRE.IncidentManager.open_incident/1 with tenant_id="acme-corp"
        to create ≥2 incidents — one severity "warning"/status "open" and one "critical"/"resolved";
        link one to a seeded workflow run via workflow_run_id (UI-SPEC minimum). Use a stable
        dedupe_key per incident and guard with Repo.get_by before inserting.
    (d) Connectors: ensure ≥2 connectors for the fleet with a mix of health states INCLUDING
        one health_state "degraded" (D-08, UI-SPEC online/degraded/offline mix). Reuse
        connector_key "billing" from the spine for the primary connector. The degraded connector
        gives the state matrix a non-healthy variant and Plan 03's connector_drawer a target.
    Idempotency rule for every non-run entity: Repo.get_by(Schema, dedupe_key/natural_key) and
    only insert when nil (Pattern 5); runs use start_run which is naturally additive — note this
    in a comment so re-running adds runs but never crashes. Do NOT reset or truncate the DB
    (D-11 — the harness relies on a live DB). Use IO.puts (not Mix.shell) since this is a script.
  </action>
  <verify>
    <automated>mix run priv/repo/dev_seed.exs && mix run priv/repo/dev_seed.exs</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c 'SupportJourney spine — do not inline these values' priv/repo/dev_seed.exs` returns 1 (the load-bearing D-09 guard comment present exactly once).
    - SOURCE: `grep -E 'SupportJourney\.(tenant_id|session_id|connector_key)' priv/repo/dev_seed.exs` matches (identities sourced from the spine, not literal "acme-corp").
    - SOURCE: `grep -v '^#' priv/repo/dev_seed.exs | grep -c '"acme-corp"'` returns 0 (no inlined tenant literal outside comments).
    - SOURCE: `grep 'health_state' priv/repo/dev_seed.exs` includes a "degraded" value.
    - BEHAVIOR: `mix run priv/repo/dev_seed.exs` exits 0 the FIRST time AND a SECOND consecutive time without error (idempotent — running twice does not raise).
  </acceptance_criteria>
  <done>
    dev_seed.exs exists; runs twice in a row exiting 0 both times; spine guard comment present;
    Approvals/Incidents/Connectors/Workflows/Live Ops reach their UI-SPEC minimums with a
    pending approval (real UUID) and a degraded connector.
  </done>
</task>

<task type="auto">
  <name>Task 2: Eval, Review Queue, Prompt Registry + Release seed depth</name>
  <read_first>
    - priv/repo/dev_seed.exs (the file from Task 1 — extend it, do not recreate)
    - .planning/phases/11-evaluation-engine-seed-depth/11-RESEARCH.md §"Seed Depth" (Eval Workbench, Review Queue, Prompt Registry, Prompt Release Workbench requirements) + Pitfall 3 (eval spec needs a SEALED dataset) + the "Creating an EvalSpec" and "Creating a Review Candidate" code examples
    - lib/scoria/eval.ex (create_dataset/1, seal_dataset/1, create_eval_spec/1, create_eval_run/1, complete_eval_run/2, list_review_queue/1)
    - lib/scoria/eval/online_score_candidate.ex (review candidate schema: tenant_id, dedupe_key, status @statuses, review_status @review_statuses, score, scorer_kind, ...)
    - lib/scoria/eval/eval_spec.ex (eval spec required attrs: name, description, dataset_id, dataset_version, eval_mode, subject, scorers, threshold_policy)
    - lib/scoria/prompt_registry.ex (or PromptRegistry.create_draft_template/1 + PromptRelease.start_release_workflow/2)
    - .planning/phases/11-evaluation-engine-seed-depth/11-UI-SPEC.md §"Seed Depth Contract" (Review Queue ≥3, Eval ≥2 + 1 completed run, Prompt Registry ≥2 + release gate + pending release)
  </read_first>
  <action>
    Extend priv/repo/dev_seed.exs (append domain blocks, each in its own try/rescue printing
    `  ! {domain} skipped: ...` on failure):
    (a) Eval Workbench: FIRST create a dataset via Eval.create_dataset/1 then seal it via
        Eval.seal_dataset/1 (Pitfall 3 — create_eval_spec validates a real sealed dataset_id,
        and live_judge runs require sealed datasets). Then create ≥2 eval specs via
        Eval.create_eval_spec/1 (is_current: true) referencing the sealed dataset. For ≥1 spec,
        create a completed eval run via Eval.create_eval_run/1 + Eval.complete_eval_run/2 so the
        Eval Workbench shows a score (UI-SPEC minimum). Idempotency-guard eval specs by name.
    (b) Review Queue: insert ≥3 Scoria.Eval.OnlineScoreCandidate rows via direct Repo.insert!
        (no high-level seed API) with a mix of status ("needs_review", "promotion_candidate",
        one "approval_requested") and review_status ("pending", "in_review", "approved"). Set
        tenant_id from the spine. At least one row carries a trace_id + workflow_run_id pointing
        to a run seeded in Task 1 (UI-SPEC "≥1 with populated trace evidence"). Guard each by a
        stable dedupe_key (Repo.get_by → insert when nil).
    (c) Prompt Registry + Release Workbench: create ≥2 prompt templates via
        PromptRegistry.create_draft_template/1 — promote ≥1 to status "active" and leave ≥1
        "draft". For the draft, call PromptRelease.start_release_workflow(draft_id, actor_id)
        so /prompts/:id/release is navigable with a pending release gate (UI-SPEC: ≥1 release
        gate, ≥1 pending release). Capture and IO.puts the seeded draft template ID so a
        maintainer knows which /prompts/<id>/release URL the harness targets. Guard templates by
        entity_id/version.
    End the script with a closing banner `IO.puts("Done. Open http://localhost:4000/scoria")`
    (mirror the analog's closing banner; use the dev server port for this app).
  </action>
  <verify>
    <automated>mix run priv/repo/dev_seed.exs && mix run priv/repo/dev_seed.exs</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: after `mix run priv/repo/dev_seed.exs`, `mix run -e 'IO.inspect(length(Scoria.Eval.list_review_queue(%{})))'` (or the list fn's required arg) returns ≥3.
    - BEHAVIOR: `mix run -e 'IO.inspect(length(Scoria.Eval.list_eval_specs()))'` returns ≥2.
    - BEHAVIOR: seed output (stdout) contains the seeded draft prompt template ID line for the /prompts/:id/release URL.
    - BEHAVIOR: running the seed a SECOND consecutive time exits 0 and the above counts do NOT increase (idempotent for guarded entities).
    - SOURCE: `grep -E 'seal_dataset' priv/repo/dev_seed.exs` matches (dataset sealed before eval spec creation per Pitfall 3).
  </acceptance_criteria>
  <done>
    Eval Workbench (≥2 specs, ≥1 completed run), Review Queue (≥3 candidates, ≥1 with trace),
    and Prompt Registry/Release (≥2 templates, ≥1 active, ≥1 draft with a started release
    workflow) all reach UI-SPEC minimums; seed remains idempotent across consecutive runs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → Postgres | Seed writes synthetic "Acme" data into the dev DB. No external/untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | dev_seed.exs committed to git | mitigate | Seed contains only synthetic "Acme" SupportJourney data and stable dedupe keys — no real tenant PII, no secrets, no API keys. Reviewed at write time. |
| T-11-02 | Tampering | re-running seed corrupting DB state | mitigate | Idempotency via Repo.get_by + conditional insert (Pattern 5); no DB reset/truncate (D-11). Re-runs are additive for runs only, guarded for all other entities. |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs zero packages (RESEARCH Package Legitimacy Audit: zero new Hex deps). No install tasks → no slopcheck checkpoint required. |
</threat_model>

<verification>
- `mix run priv/repo/dev_seed.exs` exits 0 on a freshly migrated DB.
- Running it a second consecutive time exits 0 and does not double guarded record counts.
- After seeding, `mix phx.server` + manual click-through of all 9 screens shows populated
  content matching the UI-SPEC minimums (manual — Plan 05 / EVAL-04 verification).
</verification>

<success_criteria>
- All 9 screens reach their UI-SPEC "Seed Depth Contract" minimums.
- Seed is idempotent (two consecutive runs, no doubling, no crash).
- SupportJourney spine is the identity source; the D-09 guard comment is present.
</success_criteria>

<output>
Create `.planning/phases/11-evaluation-engine-seed-depth/11-01-SUMMARY.md` when done.
</output>
