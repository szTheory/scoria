---
phase: 11-evaluation-engine-seed-depth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/ui_critique.ex
  - test/scoria/ui_critique_test.exs
autonomous: true
requirements: [EVAL-02]
must_haves:
  truths:
    - "A captured PNG can be sent to a Claude vision model and produce a per-screen findings map with exactly the 9 rubric keys, each {score: 1–5, findings: string[]}"
    - "Parsing a critique response validates the 9-key shape and is unit-tested without any real API call (CI-automatable)"
    - "When ANTHROPIC_API_KEY is absent, the critique path fails with an actionable message instead of a cryptic error"
    - "ReqLLM (the product's own LLM layer) is the vision vehicle — no raw HTTP to Anthropic"
  artifacts:
    - path: "lib/scoria/ui_critique.ex"
      provides: "Vision critique against the 9-dimension rubric + findings-JSON parsing/validation"
      contains: "ContentPart.image"
    - path: "test/scoria/ui_critique_test.exs"
      provides: "Unit test for the 9-key findings-JSON shape (EVAL-02) with a stubbed ReqLLM module"
      exports: []
  key_links:
    - from: "lib/scoria/ui_critique.ex"
      to: "ReqLLM.Generation.generate_text/3"
      via: "vision call with image ContentPart"
      pattern: "Generation\\.generate_text|ReqLLM\\.Generation"
    - from: "test/scoria/ui_critique_test.exs"
      to: "lib/scoria/ui_critique.ex"
      via: "parse_findings_json/2 shape assertion"
      pattern: "parse_findings_json"
---

<objective>
Create `lib/scoria/ui_critique.ex` — the in-harness ReqLLM vision critique service that scores
a screenshot against the 9-dimension rubric and parses/validates the findings JSON — plus its
unit test `test/scoria/ui_critique_test.exs`. Implements EVAL-02.

Purpose: This is the ONLY merge-blocking-CI-automatable slice of Phase 11 (browser + live LLM
behaviors are manual-only per D-01/D-04). The module dogfoods Scoria's own ReqLLM layer (D-03)
via `ContentPart.image/2` and enforces the locked findings-JSON contract (9 keys, each
`{score, findings}`). The test stubs ReqLLM so the JSON-shape validation runs with no API call.
Output: A critique module Plan 03's Mix task will call, and a green unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase contract + research (read before touching anything)
@.planning/phases/11-evaluation-engine-seed-depth/11-UI-SPEC.md
@.planning/phases/11-evaluation-engine-seed-depth/11-RESEARCH.md
@.planning/phases/11-evaluation-engine-seed-depth/11-PATTERNS.md
@.planning/phases/11-evaluation-engine-seed-depth/11-VALIDATION.md

# Analogs to mirror
@lib/scoria/eval/judge_runner.ex
@test/scoria/eval/judge_runner_test.exs

<interfaces>
<!-- Verified ReqLLM contracts the executor uses directly — no deps exploration needed. -->
From deps/req_llm/lib/req_llm/message/content_part.ex:66
  ContentPart.image(data, media_type \\ "image/png")  # %ContentPart{type: :image, data:, media_type:}
  ContentPart.text(string)
From deps/req_llm/lib/req_llm/context.ex:239
  Context.system(string)
  Context.user(content_part_list)   # accepts a list of ContentPart structs
From deps/req_llm/lib/req_llm/generation.ex:73
  ReqLLM.Generation.generate_text(model_spec, messages, opts) :: {:ok, response} | {:error, reason}
  ReqLLM.Response.text(response) :: String.t()
ANTHROPIC_API_KEY is loaded from env by ReqLLM via dotenvy (deps/req_llm/lib/req_llm.ex:55).

LOCKED findings-JSON contract (UI-SPEC §"9-Dimension Rubric Contract"):
  9 keys exactly — brand_fit, consistency, hierarchy, affordance, a11y, responsive,
  motion, microcopy, density — each => %{"score" => 1..5 (integer), "findings" => [string]}.
  Score semantics: 1=critical, 2=significant gap, 3=acceptable, 4=good, 5=excellent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UICritique module — vision call, rubric prompt, findings parse/validate</name>
  <read_first>
    - lib/scoria/eval/judge_runner.ex (analog: module aliases, ReqLLM call with {:ok,_}/{:error,_} handling, the optional req_llm_module injection pattern at lines 66–69 for testability, @moduledoc false convention)
    - deps/req_llm/lib/req_llm/message/content_part.ex (ContentPart.image/2 + text/1 signatures)
    - deps/req_llm/lib/req_llm/context.ex (Context.system/1, Context.user/1 with content-part list)
    - .planning/phases/11-evaluation-engine-seed-depth/11-UI-SPEC.md §"9-Dimension Rubric Contract" + §"Gap Register Format Contract" (the dimensions, score semantics, P0=any score 1)
    - .planning/phases/11-evaluation-engine-seed-depth/11-RESEARCH.md §"Pattern 4: ReqLLM Vision Call" (system instruction anchor, key-absent handling) + §"Don't Hand-Roll" (use pattern-match + Jason.decode!, no validation dep)
    - lib/mix/tasks/scoria.post_publish_smoke.ex (Mix.raise heredoc pattern for env-prerequisite errors)
  </read_first>
  <action>
    Create lib/scoria/ui_critique.ex with `@moduledoc false`. Alias ReqLLM.Generation,
    ReqLLM.Message.ContentPart, ReqLLM.Context, ReqLLM.Response (mirror JudgeRunner header).
    Define a module attribute listing the 9 canonical rubric keys in UI-SPEC order:
    brand_fit, consistency, hierarchy, affordance, a11y, responsive, motion, microcopy, density.
    Implement:
    - `system_instruction/0` — returns the rubric system prompt anchored on RESEARCH Pattern 4:
      instruct the model it is a design-system auditor for the Scoria operator dashboard, list
      the 9 dimension keys, demand a JSON object only with each key => {"score":1-5,
      "findings":[...]}, and state the score semantics (1=critical … 5=excellent). Encode the
      per-dimension "what it measures" descriptions from the UI-SPEC rubric table so the model
      scores the right thing (brand-fit = warm basalt+ember/IBM Plex/voice; consistency =
      token-only no raw palette; a11y = focus-visible/contrast/no-color-only; etc.).
    - `critique_screen/3` (or arity that accepts png_path, screen_name, and opts including an
      optional `:req_llm_module` defaulting to ReqLLM.Generation for testability — mirror
      JudgeRunner lines 66–69). Read the PNG with File.read!/1, build
      ContentPart.image(binary, "image/png") + ContentPart.text(rubric instruction), assemble
      [Context.system(system_instruction()), Context.user([text_part, image_part])], call
      generate_text(model_spec, messages, max_tokens: 2048). Default model_spec to a current
      capable Claude vision model (Claude's discretion per CONTEXT — e.g. "anthropic:claude-..."
      a current vision-capable Claude). On {:ok, response} extract text via Response.text/1 and
      delegate to parse_findings_json/2. On {:error, reason}: if ANTHROPIC_API_KEY is nil,
      Mix.raise the exact actionable message "ANTHROPIC_API_KEY is not set. Set it to run the
      critique pass." else Mix.raise "Critique failed for #{screen_name}: #{inspect(reason)}".
    - `parse_findings_json/2` (png-independent, PURE — this is the unit-tested surface): accept
      a JSON string + screen_name, Jason.decode! it, then validate/normalize to a map containing
      exactly the 9 keys, each %{"score" => integer 1..5, "findings" => list of strings}. If the
      model omitted a key or returned an out-of-range/non-integer score, raise a clear error
      naming the screen and offending key (do NOT silently default — the contract is strict).
      Strip any markdown code fences the model may wrap the JSON in before decoding.
    Do NOT call Mix.Task.run("app.start") here — the Mix task (Plan 03) owns app startup.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c 'ContentPart.image' lib/scoria/ui_critique.ex` ≥ 1 (vision uses image content part, not raw HTTP).
    - SOURCE: `grep -E 'def parse_findings_json' lib/scoria/ui_critique.ex` matches (pure parse fn exists and is public for the test).
    - SOURCE: all 9 rubric keys (brand_fit consistency hierarchy affordance a11y responsive motion microcopy density) appear in lib/scoria/ui_critique.ex.
    - SOURCE: `grep 'ANTHROPIC_API_KEY' lib/scoria/ui_critique.ex` matches (key-absent path handled).
    - BEHAVIOR: `mix compile --warnings-as-errors` succeeds with no warnings.
  </acceptance_criteria>
  <done>
    UICritique compiles warning-free; exposes system_instruction/0, critique_screen (with
    injectable req_llm_module), and a pure parse_findings_json/2 that enforces the 9-key
    {score,findings} contract and the ANTHROPIC_API_KEY-absent message.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UICritique unit test — 9-key findings-JSON shape + key-absent error</name>
  <read_first>
    - lib/scoria/ui_critique.ex (the module under test from Task 1)
    - test/scoria/eval/judge_runner_test.exs (analog: local defmodule ReqLLMStub injection, send(self(), ...) assertion style)
    - .planning/phases/11-evaluation-engine-seed-depth/11-PATTERNS.md §"test/scoria/ui_critique_test.exs" (the exact @valid_keys list + the "parse_findings_json/2 returns map with all 9 rubric keys" test body + async:true guidance)
    - .planning/phases/11-evaluation-engine-seed-depth/11-VALIDATION.md §"Wave 0 Requirements" (this is the Wave 0 unit test for EVAL-02)
    - test/scoria_web/ui_drift_guard_test.exs (async: true pure-test convention)
  </read_first>
  <action>
    Create test/scoria/ui_critique_test.exs with `use ExUnit.Case, async: true` (pure logic —
    no DB, no app start). Define `@valid_keys ~w(brand_fit consistency hierarchy affordance a11y
    responsive motion microcopy density)`. Tests:
    (1) parse_findings_json/2 returns a map with ALL 9 keys, each with an integer score and a
        list findings (mirror the PATTERNS.md test body — build a valid 9-key JSON via
        Jason.encode!, parse it, assert Map.has_key? for every @valid_key plus is_integer(score)
        and is_list(findings)).
    (2) parse_findings_json/2 strips a ```json fenced wrapper before decoding (feed the same
        valid JSON wrapped in a markdown code fence and assert it still parses to the 9-key map).
    (3) parse_findings_json/2 RAISES (assert_raise) when a rubric key is missing OR a score is
        out of the 1..5 range — the error message must name the screen and the offending key.
    (4) critique_screen with an injected ReqLLMStub returning a valid {:ok, response}: define a
        local `defmodule ReqLLMStub` whose generate_text/3 returns a stub response wrapping the
        valid 9-key JSON; pass req_llm_module: ReqLLMStub and assert the returned map has the 9
        keys. (No real API call — covers the happy path through critique_screen without network.)
    Keep all tests free of File.read! on a real PNG by exercising parse_findings_json directly
    and, for the critique_screen happy-path test, stub at the generate_text boundary (the PNG
    read can be skipped by passing a small fixture path or by testing parse path directly per
    the analog's boundary-stub approach).
  </action>
  <verify>
    <automated>mix test test/scoria/ui_critique_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: `mix test test/scoria/ui_critique_test.exs` passes (0 failures).
    - SOURCE: `grep -c 'brand_fit' test/scoria/ui_critique_test.exs` ≥ 1 and the @valid_keys list contains all 9 dimension names.
    - BEHAVIOR: the test file includes at least one assert_raise covering a malformed/missing-key findings JSON (negative case present, not just the happy path).
    - SOURCE: `grep 'defmodule ReqLLMStub' test/scoria/ui_critique_test.exs` matches (ReqLLM stubbed — no real API call in CI).
  </acceptance_criteria>
  <done>
    `mix test test/scoria/ui_critique_test.exs` is green; it validates the 9-key
    {score,findings} contract (positive + fence-strip + negative cases) and the ReqLLM
    happy path via a stub, with no network/API dependency.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UICritique → Anthropic (via ReqLLM) | Sends screenshot binaries + rubric prompt to the vision API. ANTHROPIC_API_KEY crosses here. |
| model response → parse_findings_json | Untrusted LLM output parsed into the strict 9-key contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Information Disclosure | ANTHROPIC_API_KEY in logs/output | mitigate | ReqLLM owns auth headers; UICritique never logs or echoes the key. Key-absent path emits only "ANTHROPIC_API_KEY is not set." — never the value. |
| T-11-03 | Tampering | malformed/hostile LLM JSON response | mitigate | parse_findings_json strips fences then strictly validates the 9-key {score 1..5, findings:[string]} shape via pattern match + Jason.decode!; raises (naming screen + key) on any deviation rather than trusting the model output. |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs zero packages (RESEARCH Package Legitimacy Audit: zero new Hex deps; req_llm + jason already in lockfile). No install tasks → no slopcheck checkpoint required. |
</threat_model>

<verification>
- `mix test test/scoria/ui_critique_test.exs` passes (the EVAL-02 CI-automatable subset).
- `mix compile --warnings-as-errors` clean.
- Live vision behavior (real screenshots → real scores) is manual-only per D-04 and is exercised
  in Plan 05's baseline-audit checkpoint, not here.
</verification>

<success_criteria>
- 9-key findings-JSON contract enforced and unit-tested (positive + negative + fence cases).
- ReqLLM is the vision vehicle (ContentPart.image), no raw HTTP.
- ANTHROPIC_API_KEY-absent path produces an actionable message.
</success_criteria>

<output>
Create `.planning/phases/11-evaluation-engine-seed-depth/11-02-SUMMARY.md` when done.
</output>
