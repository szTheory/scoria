---
phase: 11-evaluation-engine-seed-depth
plan: 04
type: execute
wave: 3
depends_on: ["11-03"]
files_modified:
  - mix.exs
  - .gitignore
  - priv/shots/.gitignore
  - docs/MAINTAINERS.md
autonomous: true
requirements: [EVAL-03]
must_haves:
  truths:
    - "The shipped Hex package excludes priv/dev/shots.mjs and the transient priv/shots/ captures — adopters never receive the dev-only harness"
    - "priv/dev/shots.mjs is checked into git (committed dev-only tooling) and screenshot PNG/JSON captures are gitignored, but priv/shots/gap_register.md is tracked"
    - "docs/MAINTAINERS.md documents how to run the harness, the Playwright prerequisite, and the empty-state limitation for the 4 non-tenant-scoped screens"
  artifacts:
    - path: "mix.exs"
      provides: "package.files refactored from bare priv to explicit subdirs excluding priv/dev + priv/shots"
      contains: "priv/repo"
    - path: "priv/shots/.gitignore"
      provides: "Ignores screenshot captures, tracks gap_register.md"
      contains: "gap_register.md"
    - path: "docs/MAINTAINERS.md"
      provides: "Harness usage + Playwright prerequisite + empty-state limitation docs"
      contains: "scoria.ui.shots"
  key_links:
    - from: "mix.exs"
      to: "priv/dev exclusion"
      via: "package.files explicit inclusion list"
      pattern: "priv/static"
---

<objective>
Make the harness committed dev-only tooling that never ships to adopters: refactor mix.exs
`package.files` from the bare `"priv"` glob to explicit subdirectory inclusions (excluding
`priv/dev/` and `priv/shots/`), add the `.gitignore` rules that track `gap_register.md` while
ignoring screenshot captures, and document harness usage + prerequisites in docs/MAINTAINERS.md.
Implements EVAL-03. Resolves research open question #2 (whether `priv/repo/dev_seed.exs` ships).

Purpose: Preserves the package's deliberate "no browser automation in the shipped package /
clean hex.audit" posture (D-01/D-02) as a supply-chain control, and ties off the documented-usage
half of EVAL-03 (PROOF-03 references the same MAINTAINERS section).
Output: A clean package manifest, the screenshot .gitignore policy, and maintainer docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/11-evaluation-engine-seed-depth/11-RESEARCH.md
@.planning/phases/11-evaluation-engine-seed-depth/11-PATTERNS.md
@.planning/phases/11-evaluation-engine-seed-depth/11-UI-SPEC.md

# Files modified in this plan
@mix.exs
@docs/MAINTAINERS.md

<interfaces>
<!-- Verified current state — the executor edits in place. -->
mix.exs package.files CURRENT (verified): ["lib", "priv", "mix.exs", ".formatter.exs",
  "CHANGELOG.md", "README.md", "LICENSE", "docs/adoption_lanes.md",
  "docs/phoenix_runtime_example.md", "docs/bounded_handoffs.md", "docs/semantic_fast_path.md",
  "docs/operator_verification.md", "docs/connector_adoption.md",
  "docs/support_copilot_gallery.md", "docs/MAINTAINERS.md"]
Actual priv/ subdirs (verified `ls priv/`): fixtures, host_app_proof, repo, static
  → bare "priv" currently ships ALL of priv/, which after Plan 03 would leak priv/dev/shots.mjs
    and transient priv/shots/ to adopters.

OPEN QUESTION #2 RESOLUTION (planner decision): EXCLUDE priv/repo/dev_seed.exs from the shipped
  package. Rationale: dev_seed.exs is a maintainer dev tool, not a runtime artifact for adopters;
  the safer default is exclusion (RESEARCH open question #2 recommendation). Migrations are NOT in
  priv/repo (this project ships no priv/repo/migrations to adopters via the package — confirm by
  inspecting priv/repo before excluding). Therefore omit "priv/repo" from package.files entirely.
  If priv/repo contains adopter-required files beyond dev_seed.exs, narrow the exclusion instead
  and note the deviation in the SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor mix.exs package.files + add gitignore rules</name>
  <read_first>
    - mix.exs (the package/0 stanza — files: list; verified current contents in <interfaces>)
    - .planning/phases/11-evaluation-engine-seed-depth/11-RESEARCH.md §"Hex-Package Hygiene Analysis" (the inclusion-list approach, priv/dev + priv/shots exclusion, .gitignore additions) + open question #3 (dev_seed.exs inclusion)
    - .planning/phases/11-evaluation-engine-seed-depth/11-PATTERNS.md §"mix.exs — package.files modification" (replace bare "priv" with explicit subdirs; verify with `ls priv/` first)
    - .gitignore (current — confirmed no priv/shots entry exists)
  </read_first>
  <action>
    First run `ls priv/` and `ls priv/repo/` to re-confirm the live subdir set before editing
    (PATTERNS instruction). Then in mix.exs package/0, replace the single "priv" entry in the
    files: list with explicit subdirectory inclusions that EXCLUDE the dev-only harness:
    "priv/fixtures", "priv/host_app_proof", "priv/static" — and intentionally OMIT "priv/dev"
    (shots.mjs — not for adopters), "priv/shots" (screenshot output — not for adopters), and
    "priv/repo" (dev_seed.exs is a maintainer tool — open question #2 resolution: exclude). Add
    inline comments documenting WHY each is excluded (mirror the existing docs/ inclusion-list
    style). If `ls priv/repo/` reveals adopter-required files beyond dev_seed.exs, keep
    "priv/repo" but this is unexpected — flag it in the SUMMARY.
    Then add screenshot-harness gitignore rules. Create priv/shots/.gitignore containing rules
    that ignore all date-stamped capture subdirs and all *.png/*.json while tracking
    gap_register.md (RESEARCH .gitignore section: `*/`, `*.png`, `*.json` so only top-level
    gap_register.md is committed). Also add a top-level .gitignore note referencing
    priv/shots/**/*.png and priv/shots/**/*.json as a belt-and-suspenders guard, leaving
    priv/shots/gap_register.md tracked.
    Do NOT remove or reorder the existing non-priv entries in package.files (lib, mix.exs,
    .formatter.exs, CHANGELOG/README/LICENSE, the docs/* list).
  </action>
  <verify>
    <automated>mix compile 2>&1 | tail -3 && grep -E 'priv/static' mix.exs && grep -E 'gap_register' priv/shots/.gitignore</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c '"priv"' mix.exs` returns 0 (bare priv glob removed; no longer ships all of priv/).
    - SOURCE: mix.exs package.files contains "priv/static" and "priv/fixtures" and does NOT contain "priv/dev" or "priv/shots".
    - SOURCE: `grep -E 'priv/dev' mix.exs` returns only comment lines (exclusion documented), not an inclusion entry.
    - SOURCE: `grep 'gap_register.md' priv/shots/.gitignore` matches AND the file ignores *.png and *.json.
    - BEHAVIOR: `mix compile` exits 0 (mix.exs still valid Elixir).
    - BEHAVIOR (supply-chain): `mix hex.build` (or inspecting the produced file list) shows neither priv/dev/shots.mjs nor any priv/shots/ entry in the package contents.
  </acceptance_criteria>
  <done>
    package.files lists explicit priv subdirs excluding priv/dev, priv/shots, and priv/repo with
    documented rationale; priv/shots/.gitignore tracks gap_register.md and ignores captures;
    mix compiles; hex.build excludes the harness.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document harness usage in docs/MAINTAINERS.md</name>
  <read_first>
    - docs/MAINTAINERS.md (existing 159-line file — append a new section, do not rewrite)
    - .planning/phases/11-evaluation-engine-seed-depth/11-CONTEXT.md §"Specific Ideas" (the maintainer prerequisite line: `npm install -g playwright` + `npx playwright install chromium`) + D-02
    - .planning/phases/11-evaluation-engine-seed-depth/11-RESEARCH.md §"Common Pitfalls" 5 (the empty-state limitation for the 4 non-tenant-scoped screens) + §"Environment Availability" (Node, Playwright, ANTHROPIC_API_KEY)
    - lib/mix/tasks/scoria.ui.shots.ex (the task usage + flags — from Plan 03)
    - priv/dev/shots.mjs (the capture script being documented — from Plan 03)
  </read_first>
  <action>
    Append a "Screenshot + Critique Harness (dev-only)" section to docs/MAINTAINERS.md (mirror
    the document's existing heading + prose style). Cover:
    - Prerequisites: Node.js, Playwright + chromium via `npm install -g playwright && npx
      playwright install chromium` (D-02), and ANTHROPIC_API_KEY for the critique pass.
    - The seed-first workflow: `mix run priv/repo/dev_seed.exs` (idempotent) so screens render
      populated before capture.
    - Running the screenshot pass: start `mix phx.server`, then `mix scoria.ui.shots`; outputs
      land under priv/shots/{date}/{screen}/{state}.png (gitignored except gap_register.md).
    - Running the decoupled critique pass: `mix scoria.ui.shots --critique` (D-04 — separate
      gated step, run at phase-milestone boundaries; needs ANTHROPIC_API_KEY); it writes
      per-screen findings JSON for the canonical populated·desktop·dark state (D-05).
    - The empty-state limitation: Review Queue, Eval Workbench, Prompt Registry, and Workflow
      Index do not support ?tenant= switching, so the harness captures populated-only for those
      four; empty state for them is a freshly-migrated-DB artifact (RESEARCH Pitfall 5 — resolves
      open question #1). Document this explicitly.
    - Note that the harness is dev-only and excluded from the shipped Hex package and from
      merge-blocking CI (D-01).
    Do NOT alter unrelated sections of MAINTAINERS.md.
  </action>
  <verify>
    <automated>grep -E 'scoria.ui.shots' docs/MAINTAINERS.md && grep -E 'playwright install chromium' docs/MAINTAINERS.md</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c 'scoria.ui.shots' docs/MAINTAINERS.md` ≥ 1 (harness usage documented).
    - SOURCE: `grep 'playwright install chromium' docs/MAINTAINERS.md` matches (D-02 prerequisite).
    - SOURCE: docs/MAINTAINERS.md mentions the --critique flag and ANTHROPIC_API_KEY.
    - SOURCE: docs/MAINTAINERS.md documents the empty-state limitation for the non-tenant-scoped screens (e.g. mentions Review Queue / Eval Workbench / Prompt Registry / Workflow Index populated-only).
  </acceptance_criteria>
  <done>
    docs/MAINTAINERS.md gains a harness section covering prerequisites, the seed-first workflow,
    screenshot + --critique passes, the empty-state limitation, and the dev-only/CI-excluded
    posture — satisfying the documented-usage half of EVAL-03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → shipped Hex package | package.files governs what reaches adopters — a supply-chain boundary. |
| working tree → git | .gitignore governs which capture artifacts get committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Tampering (supply chain) | priv/dev/shots.mjs leaking into shipped package | mitigate | package.files refactored to an explicit inclusion list excluding priv/dev and priv/shots; verified via hex.build package contents (RESEARCH Hex-Package Hygiene, A5 high-risk). |
| T-11-02 | Information Disclosure | screenshot captures committed to git | mitigate | priv/shots/.gitignore ignores */ + *.png + *.json; only synthetic-data gap_register.md is tracked. |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs zero packages; it documents Playwright as a maintainer prerequisite (D-02) without adding it to mix.exs. No install tasks → no slopcheck checkpoint required. |
</threat_model>

<verification>
- `mix compile` and `mix hex.build` succeed; built package excludes priv/dev and priv/shots.
- `git ls-files priv/dev/shots.mjs` shows the script is tracked (committed dev tooling).
- `priv/shots/.gitignore` tracks gap_register.md and ignores PNG/JSON captures.
- docs/MAINTAINERS.md documents the harness usage and prerequisites.
</verification>

<success_criteria>
- Shipped package excludes the dev-only harness (priv/dev, priv/shots) and dev_seed.exs.
- Screenshot captures are gitignored; gap_register.md is tracked.
- MAINTAINERS.md documents harness usage, prerequisites, and the empty-state limitation.
</success_criteria>

<output>
Create `.planning/phases/11-evaluation-engine-seed-depth/11-04-SUMMARY.md` when done.
</output>
