---
phase: 11-evaluation-engine-seed-depth
plan: 05
type: execute
wave: 4
depends_on: ["11-03", "11-04"]
files_modified:
  - lib/mix/tasks/scoria.ui.shots.ex
  - priv/shots/gap_register.md
autonomous: false
requirements: [EVAL-05]
must_haves:
  truths:
    - "The critique pass aggregates per-screen findings JSON into a ranked design-system gap register at priv/shots/gap_register.md"
    - "The gap register ranks findings worst-first and includes a prioritized fix backlog (P0/P1)"
    - "The baseline gap register surfaces the flash_tone_class raw-palette known issue as a ranked finding (not fixed in Phase 11)"
    - "gap_register.md is committed so later phases (12–17) diff against a stable baseline"
  artifacts:
    - path: "priv/shots/gap_register.md"
      provides: "Committed baseline ranked gap register + prioritized fix backlog"
      contains: "Design-System Gap Register"
    - path: "lib/mix/tasks/scoria.ui.shots.ex"
      provides: "Gap-register rendering/aggregation in the --critique pass"
      contains: "gap_register"
  key_links:
    - from: "lib/mix/tasks/scoria.ui.shots.ex"
      to: "priv/shots/gap_register.md"
      via: "File.write! after aggregating per-screen findings JSON"
      pattern: "gap_register"
    - from: "priv/shots/gap_register.md"
      to: "flash_tone_class known issue"
      via: "ranked baseline finding"
      pattern: "flash_tone_class|consistency"
---

<objective>
Render and commit the baseline design-system gap register: extend the `--critique` pass to
aggregate the per-screen findings JSON into a ranked `priv/shots/gap_register.md` (worst-first
findings + prioritized P0/P1 fix backlog) per the UI-SPEC format, then run the live baseline
audit and commit the result. Implements EVAL-05.

Purpose: The committed baseline is the stable reference every later v3.0 phase (12–17) diffs
against (D-06). It must surface the known `flash_tone_class/1` raw-palette issue as a ranked
finding without fixing it (scope fence — fixing is Phase 12). The live capture+critique run is
manual-only (browser + non-deterministic LLM, D-01/D-04), so this plan combines an autonomous
render step with a human-verify checkpoint for the live run + commit.
Output: A committed `priv/shots/gap_register.md` baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/11-evaluation-engine-seed-depth/11-UI-SPEC.md
@.planning/phases/11-evaluation-engine-seed-depth/11-RESEARCH.md
@.planning/phases/11-evaluation-engine-seed-depth/11-PATTERNS.md
@.planning/phases/11-evaluation-engine-seed-depth/11-VALIDATION.md

# The task being extended + the known-issue source
@lib/mix/tasks/scoria.ui.shots.ex
@lib/scoria_web/ui.ex
@lib/scoria/ui_critique.ex

<interfaces>
<!-- LOCKED gap-register format — UI-SPEC §"Gap Register Format Contract" -->
priv/shots/gap_register.md MUST follow:
  # Design-System Gap Register — Baseline {YYYY-MM-DD}
  ## Summary
  - Screens audited: {N}
  - P0 issues (score 1): {N}
  - P1 issues (score 2): {N}
  - Passing (score ≥ 3): {N}
  ## Ranked Findings (worst first)
  ### {screen_name} — {dimension}: {score}/5
  > {finding text}
  ## Fix Backlog (prioritized)
  | Priority | Screen | Dimension | Action |
  |----------|--------|-----------|--------|
  | P0 | … | … | … |
Score semantics (UI-SPEC §"9-Dimension Rubric Contract"): 1=critical (P0), 2=significant (P1),
  ≥3 passing. Any score of 1 → P0 in the backlog.
Known issue to expect (UI-SPEC Implementation Notes 6 / RESEARCH Pitfall 4):
  lib/scoria_web/ui.ex:195 flash_tone_class/1 uses raw palette (border-rose-200 bg-rose-50
  text-rose-900) → will score low on `consistency`. Must appear ranked in the register. Do NOT
  fix it in Phase 11 (scope fence — Phase 12 / DS-05).
Source per-screen findings JSON: priv/shots/{date}/{screen}/populated_dark_desktop.json
  (the canonical critique state, D-05), shape per Plan 02's 9-key contract.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render gap_register.md from per-screen findings JSON</name>
  <read_first>
    - lib/mix/tasks/scoria.ui.shots.ex (the --critique branch from Plan 03 — extend it to aggregate findings into the register; the private render helper may already be stubbed there)
    - .planning/phases/11-evaluation-engine-seed-depth/11-UI-SPEC.md §"Gap Register Format Contract" + §"9-Dimension Rubric Contract" (exact heading structure, P0/P1 thresholds)
    - .planning/phases/11-evaluation-engine-seed-depth/11-PATTERNS.md §"priv/shots/gap_register.md" (File.write! + Mix.shell().info confirmation pattern; heredoc rendering; mkdir_p! parent first)
    - lib/mix/tasks/scoria.warning_inventory.ex (render_markdown/2 heredoc + File.write! analog at lines ~140, ~180)
    - .planning/phases/11-evaluation-engine-seed-depth/11-UI-SPEC.md §"Copywriting Contract" (gap-register heading copy "Design-system gap register — baseline {date}", "No gaps found in this dimension.")
  </read_first>
  <action>
    Extend lib/mix/tasks/scoria.ui.shots.ex so the --critique pass, after writing per-screen
    findings JSON, aggregates them into priv/shots/gap_register.md. Add a private function
    (e.g. render_gap_register/2) that:
    - Reads each screen's populated_dark_desktop.json from the date-stamped out_dir (Jason.decode!),
      tolerating missing files with a logged skip.
    - Computes the Summary counts: screens audited (N), P0 issues (count of dimensions scoring 1
      across all screens), P1 issues (count scoring 2), passing (count scoring ≥3).
    - Builds the "Ranked Findings (worst first)" section: flatten every {screen, dimension, score,
      findings} entry, sort ascending by score (1 first), and for each emit
      `### {screen} — {dimension}: {score}/5` followed by `> {finding}` lines for each finding
      string. Skip dimensions with empty findings, or render "No gaps found in this dimension."
      where appropriate per the copy contract.
    - Builds the "Fix Backlog (prioritized)" markdown table with one row per scored-low dimension:
      Priority P0 for score 1, P1 for score 2; Screen, Dimension, and a concise Action derived
      from the finding text. Order P0 rows before P1.
    - Uses the exact heading `# Design-System Gap Register — Baseline {YYYY-MM-DD}` (UI-SPEC).
    - File.mkdir_p! the priv/shots dir, File.write! the rendered markdown to
      priv/shots/gap_register.md, then Mix.shell().info("==> Wrote priv/shots/gap_register.md").
    Wire this render call into the --critique branch after the per-screen JSON loop. Do NOT
    modify lib/scoria_web/ui.ex or any LiveView template (scope fence). Keep the render function
    pure enough that it operates on already-written JSON files (so it is re-runnable without a new
    LLM call).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: `mix compile --warnings-as-errors` succeeds.
    - SOURCE: `grep -c 'gap_register' lib/mix/tasks/scoria.ui.shots.ex` ≥ 1 (register rendering wired into the task).
    - SOURCE: the render helper emits the exact UI-SPEC headings — `grep -E 'Design-System Gap Register|Ranked Findings|Fix Backlog' lib/mix/tasks/scoria.ui.shots.ex` matches all three.
    - SOURCE: `grep -v '^#' lib/scoria_web/ui.ex` is byte-identical to its pre-plan state (scope fence: flash_tone_class NOT fixed) — verify `git diff --stat lib/scoria_web/ui.ex` shows no changes.
  </acceptance_criteria>
  <done>
    The --critique pass renders priv/shots/gap_register.md in the locked UI-SPEC format
    (Summary + worst-first Ranked Findings + prioritized P0/P1 Fix Backlog) from the per-screen
    findings JSON; ui.ex is untouched; the task compiles warning-free.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full Phase 11 proof loop is in place: idempotent dev_seed.exs (Plan 01), the UICritique
    module + unit test (Plan 02), the screenshot + --critique harness (Plan 03), Hex-hygiene +
    docs (Plan 04), and the gap-register renderer (Task 1 above). This checkpoint runs the live
    baseline audit (browser + LLM — manual-only per D-01/D-04) and commits the resulting baseline
    gap register.
  </what-built>
  <how-to-verify>
    1. Prerequisites: `npx playwright install chromium` done; `ANTHROPIC_API_KEY` exported.
    2. Seed: `mix run priv/repo/dev_seed.exs` (run twice — second run must not error or double
       counts; confirms EVAL-04 idempotency).
    3. Start the dev server: `mix phx.server`.
    4. In a second shell, run the screenshot pass: `mix scoria.ui.shots`. Confirm PNGs appear
       under `priv/shots/{today}/{screen}/{state}.png` for all 9 screens, including overlay
       states for approvals (modal), connectors (connector_drawer + runtime_drawer), and
       prompt_release (approve_modal), and that empty captures exist ONLY for the 5 tenant-scoped
       screens (EVAL-01).
    5. Run the critique pass: `mix scoria.ui.shots --critique`. Confirm per-screen
       `populated_dark_desktop.json` files are written with the 9-key shape, and that
       `priv/shots/gap_register.md` is generated in the UI-SPEC format (EVAL-02 + EVAL-05).
    6. Open `priv/shots/gap_register.md` and confirm: it is non-empty, has Summary +
       Ranked Findings + Fix Backlog sections, and surfaces the `flash_tone_class` raw-palette
       known issue as a ranked `consistency` finding (EVAL-05). Confirm `lib/scoria_web/ui.ex`
       was NOT modified (scope fence).
    7. Commit the baseline: `git add priv/shots/gap_register.md` and confirm
       `git ls-files priv/shots/gap_register.md` lists it while `git status` shows the
       date-stamped PNG/JSON captures are ignored (EVAL-03 gitignore policy from Plan 04).
  </how-to-verify>
  <resume-signal>
    Type "approved" once the baseline gap_register.md is committed and surfaces the
    flash_tone_class finding, or describe what is missing/wrong.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-screen findings JSON → gap_register.md | LLM-derived findings aggregated into a committed artifact. |
| gap_register.md → git | Committed baseline — must contain no secrets/PII. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Information Disclosure | gap_register.md committed to git | mitigate | Register contains only rubric scores + design-finding prose over synthetic "Acme" screens; the human-verify checkpoint inspects it before commit. No screenshots, no API key, no tenant PII committed (only gap_register.md is tracked per Plan 04 gitignore). |
| T-11-07 | Repudiation | baseline drift across phases | accept | Committing the dated baseline (D-06) gives later phases a stable diff target; non-determinism of LLM scoring is accepted (canonical single state per D-05 keeps signal stable). |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs zero packages. The live run uses the already-documented Playwright prerequisite (D-02) and existing ReqLLM dep. No install tasks → no slopcheck checkpoint required. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean after the render helper is added.
- `git diff lib/scoria_web/ui.ex` shows no changes (flash_tone_class not fixed — scope fence).
- Human-verify checkpoint: live seed → screenshot → critique → gap_register.md generated in
  UI-SPEC format, surfaces flash_tone_class, and is committed (EVAL-05 + EVAL-01/02/03/04 end-to-end).
</verification>

<success_criteria>
- priv/shots/gap_register.md exists, is committed, and follows the UI-SPEC format.
- It ranks findings worst-first with a prioritized P0/P1 fix backlog.
- It surfaces the flash_tone_class raw-palette known issue without fixing it.
</success_criteria>

<output>
Create `.planning/phases/11-evaluation-engine-seed-depth/11-05-SUMMARY.md` when done.
</output>
