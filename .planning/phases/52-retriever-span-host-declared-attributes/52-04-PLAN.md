---
phase: 52-retriever-span-host-declared-attributes
plan: 04
type: execute
wave: 3
depends_on: ["52-01", "52-02", "52-03"]
files_modified:
  - lib/scoria/knowledge.ex
  - test/scoria/knowledge/retrieval_test.exs
autonomous: true
requirements: [RETR-01, RETR-02, ATTR-01]
must_haves:
  truths:
    - "Knowledge.retrieve/2 produces both a persisted ai_retrieval_runs row (kept, system-of-record) and a linked RETRIEVER span sharing trace_id/span_id; a join never comes up empty for a successful call (SC#1)."
    - "embedding_model/index_version/reranker appear as convention keys on BOTH the span attributes and run.metadata from one shared value origin, with a consistency guard (SC#2)."
    - "A host-supplied feature/route/archetype/intent value flows through to persisted RETRIEVER span attributes unmodified — Scoria never infers or overwrites (SC#3)."
    - "Span emission never fails retrieval — a raise on the emit path still returns {:ok, ...} (D-R6)."
  artifacts:
    - lib/scoria/knowledge.ex
    - test/scoria/knowledge/retrieval_test.exs
  key_links:
    - "The one config_map computed in retrieve/2 feeds both create_retrieval_run's metadata and emit_retriever_span's attributes via a single Semconv.retrieval_config_attributes/1 projection — the divergence RETR-02 exists to prevent."
    - "span_id is written to BOTH run.span_id and the emitted span's :id — the join key linking table and span."
---

<objective>
Wire the RETR-01 spine: edit `Knowledge.retrieve/2` to mint IDs, capture wall-clock start, resolve the embedder, build the one canonical retrieval-config map, normalise opts→map for the host-declared seam, write `span_id` to both the run row and the emitted span, call `Scoria.Observe.emit_retriever_span/1` after the `with`-chain succeeds (try/rescue-isolated), and return additively. Migrate the mandatory `retrieval_test.exs` linkage assertion (D-R2b) and add the RETR-01 join, RETR-02 real-Postgres equality, ATTR-01 pass-through, and D-R6 emit-never-fails integration tests.

Purpose: This is the load-bearing reliable core — all host config + host-declared values arrive directly in `retrieve/2` opts and Scoria emits the RETRIEVER span itself (no D-ATTR01-7 dependency). It delivers SC#1, SC#2, and SC#3-on-the-retriever-span.
Output: an extended `retrieve/2`; the migrated + expanded `retrieval_test.exs`.

## Artifacts This Plan Produces
- `Scoria.Knowledge.retrieve/2` — additive return fields `trace_id`/`span_id`; new `:embedder` opt; redefined `opts[:span_id]` semantics (this retrieval's OWN span id) and `opts[:parent_id]` (caller's originating span)
(Full phase artifact roll-up is in 52-01-PLAN.md.)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/52-retriever-span-host-declared-attributes/52-CONTEXT.md
@.planning/phases/52-retriever-span-host-declared-attributes/52-RESEARCH.md
@.planning/phases/52-retriever-span-host-declared-attributes/52-PATTERNS.md
@lib/scoria/knowledge.ex
@lib/scoria/knowledge/retrieval_run.ex
@lib/scoria/observe/buffer.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit retrieve/2 — mint IDs, one config map, emit RETRIEVER span</name>
  <files>lib/scoria/knowledge.ex, test/scoria/knowledge/retrieval_test.exs</files>
  <read_first>
    - lib/scoria/knowledge.ex lines 215-257 (the current retrieve/2 body — PATTERNS reproduces it verbatim with the 8-step delta list)
    - .planning/phases/52-retriever-span-host-declared-attributes/52-RESEARCH.md § Recommendation 2 (the exact 8 concrete edits, all verified feasible against knowledge.ex:215-257)
    - .planning/phases/52-retriever-span-host-declared-attributes/52-CONTEXT.md § D-R2/R3/R4, D-RETR02-1/2/5/6, D-ATTR01-3, D-R6, D-R7
    - lib/scoria/knowledge/retrieval_run.ex (metadata :map column at :20, cast at :42 — no migration; config + host keys ride here)
    - lib/scoria/observe.ex (the emit_retriever_span/1 input contract from 52-03)
  </read_first>
  <action>
    Apply the RESEARCH Recommendation 2 edit list to `retrieve/2`, in order:
    1. Add `started_wall = DateTime.utc_now()` at the top alongside the existing monotonic `started_at`; keep the monotonic delta as the sole `latency_ms` authority — do NOT derive start from end minus latency (D-R4).
    2. Mint `trace_id = opts[:trace_id] || Ecto.UUID.generate()` and `span_id = opts[:span_id] || Ecto.UUID.generate()` (D-R2), replacing the current bare `opts[:trace_id]`/`opts[:span_id]` reads passed to `create_retrieval_run`.
    3. Add `embedder = Keyword.get(opts, :embedder, Embedder.Deterministic)` and use it BOTH for the nil-retriever embed path (replacing the hardcoded `Embedder.Deterministic.embed_query`) AND for the `embedding_model` precedence lookup.
    4. Build ONE `config_map = %{embedding_model: ..., index_version: ..., reranker: ...}` per the D-RETR02-2 precedence: embedding_model = `opts[:embedding_model]` else, only when `function_exported?(embedder, :model_name, 0)`, `embedder.model_name()` else `"none"` (guarded — a host embedder without model_name/0 must fall through, never `UndefinedFunctionError`); index_version = `opts[:index_version]` else `Application.get_env(:scoria, :index_version)` else `"none"`; reranker = `opts[:reranker]` else `"none"`. When `opts[:query_embedding]` is supplied (Scoria did not embed), do not assert a model name — fall through to host declaration → `"none"`.
    5. Normalise `host_metadata = Map.new(opts)` before the host-declared seam (D-ATTR01-3 — opts is a keyword list; the seam needs a map to avoid BadMapError).
    6. Pass `Semconv.retrieval_config_attributes(config_map)` merged into `create_retrieval_run`'s `metadata:` map together with the host-declared keys (merge host keys via the same seam), and write `span_id` to the run row's `span_id:` field. This is the SINGLE computation of the config projection (D-RETR02-1) — the same `config_map`/projection feeds the span.
    7. After the `with`-chain succeeds (run + results persisted), build the emit_retriever_span/1 input (config_map, host_metadata, trace_id, span_id as the own id, parent_id = `opts[:parent_id]`, started_wall) and call `Scoria.Observe.emit_retriever_span/1` wrapped `try/rescue _ -> :ok` — never before the with, success-path only (D-R6, RETR-01).
    8. Change the final return to `{:ok, %{run: run, results: persisted_results, trace_id: trace_id, span_id: span_id}}` (additive, non-breaking — existing callers use partial map patterns).
    Add/extend the `retrieve/2` @doc to document both ID axes: `opts[:parent_id]` = the caller's/originating span (nil ⇒ trace root); `opts[:span_id]` = THIS retrieval's own span id (the join key; must be fresh/unique — a reused ai_spans PK collides). Never inline a config/host key string — route through Semconv.
  </action>
  <verify>
    <automated>mix scoria.test.knowledge --only knowledge</automated>
  </verify>
  <acceptance_criteria>
    - `retrieve/2` returns `{:ok, %{run: run, results: results, trace_id: trace_id, span_id: span_id}}` where `run.span_id == span_id`.
    - `run.metadata` contains all three retrieval-config keys with `"none"` sentinels when unsupplied and the supplied values otherwise; host-declared keys present in opts appear in `run.metadata` unmodified and are absent when not supplied.
    - Passing a host embedder module without `model_name/0` does not raise — `embedding_model` falls through to `opts[:embedding_model]` or `"none"`.
    - `retrieve/2` still succeeds (`{:ok, ...}`) even if the emit path raises (try/rescue isolation).
    - No config/host key string literal appears in `knowledge.ex` (52-01 anti-inline grep stays green).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrate retrieval_test.exs (D-R2b) + RETR-01/RETR-02/ATTR-01/D-R6 integration</name>
  <files>lib/scoria/knowledge.ex, test/scoria/knowledge/retrieval_test.exs</files>
  <read_first>
    - test/scoria/knowledge/retrieval_test.exs lines 27-72 (the current test — the mandatory D-R2b edit target: it inserts a Span and calls retrieve with `span_id: span.id`, asserting `run.span_id == span.id`)
    - .planning/phases/52-retriever-span-host-declared-attributes/52-RESEARCH.md § "The RETR-02 consistency guard" code example (the exact flush_now → Repo.get_by! → Keyword.values(Semconv.retrieval_config_keys()) → Map.take equality shape)
    - .planning/phases/52-retriever-span-host-declared-attributes/52-CONTEXT.md § D-R2b (the mandatory span_id→parent_id migration), D-RETR02-7, D-ATTR01-6
    - lib/scoria/observe/buffer.ex (flush_now/1 test hook at :23 — call it before asserting the join)
  </read_first>
  <action>
    Migrate the existing test (D-R2b, mandatory): change the `retrieve/2` call's `span_id: span.id` to `parent_id: span.id`; bind the returned own id (`{:ok, %{span_id: returned_span_id}} = ...`) and change the assertion `run.span_id == span.id` to `run.span_id == returned_span_id`. After `Scoria.Observe.Buffer.flush_now()`, load the persisted RETRIEVER span via `Repo.get_by!(Scoria.Repo.Span, id: run.span_id)` and assert its `parent_id == span.id` — proving the caller's span became the parent and Scoria minted a fresh own id.
    Add a RETR-01 join test: after a successful `retrieve/2` + `Buffer.flush_now()`, assert the `ai_retrieval_runs` row exists AND a RETRIEVER-kind span with `id == run.span_id` and `trace_id == run.trace_id` exists — the join never comes up empty (SC#1). Assert the span's `span_kind` normalises to retriever and `status_code == "OK"`.
    Add the RETR-02 real-Postgres equality guard: call `retrieve/2` with `embedding_model`/`index_version` opts + `Buffer.flush_now()`; fetch the span; compute `keys = Keyword.values(Semconv.retrieval_config_keys())`; assert `map_size(Map.take(run.metadata, keys)) == 3` and `Map.take(span.attributes, keys) == Map.take(run.metadata, keys)` (single-origin equality — would catch a rename-one-side or add-to-one-sink edit, D-RETR02-7).
    Add the ATTR-01 pass-through test: call `retrieve/2` with a host `feature:` value that is NOT on the Redactor deny-list (e.g. `"support-copilot"`); after flush, assert the persisted span attributes carry `"feature" => "support-copilot"` byte-for-byte and that an omitted host key (e.g. archetype) is ABSENT (`refute Map.has_key?`) — the never-default proof on a REAL emission (D-ATTR01-6, not a hand-synthesized event).
    Add the D-R6 emit-never-fails test: force a raise on the emit path (e.g. attach a telemetry handler that raises, or stub the emitter path) and assert `retrieve/2` still returns `{:ok, ...}`.
  </action>
  <verify>
    <automated>mix scoria.test.knowledge --only knowledge</automated>
  </verify>
  <acceptance_criteria>
    - The migrated test asserts `run.span_id == returned_span_id` (minted own id) AND the persisted RETRIEVER span's `parent_id == span.id` (the passed caller span).
    - The RETR-01 join test proves a run row and a RETRIEVER span share `trace_id`/`span_id` and the join is non-empty; span `status_code == "OK"`.
    - The RETR-02 equality test asserts `Map.take(span.attributes, keys) == Map.take(run.metadata, keys)` with all three keys present on both sides.
    - The ATTR-01 test proves a non-deny-listed host `feature` value passes through byte-for-byte on a real `retrieve/2` emission and an omitted key is absent.
    - The D-R6 test proves `retrieve/2` returns `{:ok, ...}` even when the emit path raises.
    - `mix scoria.test.knowledge` is green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → retrieve/2 opts | Host supplies trace_id/span_id/parent_id/embedder/config/host-declared values as keyword opts. |
| retrieve/2 → :telemetry emit | Emit runs Redactor/ReviewerBroadcast synchronously in the retrieval caller. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-04a | Denial of Service | emit_retriever_span/1 raising inside retrieve/2 | high | mitigate | Emit is wrapped `try/rescue → :ok` and called only after the with-chain succeeds — span emission never fails retrieval (D-R6, FOUND-01, ASVS V7). |
| T-52-04b | Tampering | reused span_id PK collision | medium | mitigate | `span_id` minted fresh when absent; @doc documents the own-id semantics; migrated test proves the fresh-own-id contract (D-R2/R2b). |
| T-52-04c | Information Disclosure | deny-listed host key value | low | accept | Existing key-based Redactor recurses and redacts `token`/`secret`/etc.; ATTR-01 test uses a non-deny-listed value and documents the redaction as intended (Pitfall 3, D-ATTR01-4). |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `mix scoria.test.knowledge` green (RETR-01 join, RETR-02 equality, ATTR-01 pass-through, D-R2b migration, D-R6 isolation on real Postgres after flush_now/1).
- `mix test test/scoria/observe/semconv_test.exs` green (anti-inline grep now scans the edited knowledge.ex).
- `mix compile --warnings-as-errors` clean.
</verification>

<success_criteria>
- SC#1 (join never empty), SC#2 (config keys equal on both sinks from one origin), and SC#3-on-the-retriever-span (host keys unmodified) are proven by green real-Postgres integration tests.
</success_criteria>

<output>
Create `.planning/phases/52-retriever-span-host-declared-attributes/52-04-SUMMARY.md` when done.
</output>
