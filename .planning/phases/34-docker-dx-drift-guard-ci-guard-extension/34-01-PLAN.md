---
phase: 34-docker-dx-drift-guard-ci-guard-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/scoria/docker_dx_doc_contract_test.exs
autonomous: true
requirements:
  - DOCS-03
must_haves:
  truths:
    - "DOCS-03: Docker DX reader-facing commands, URLs, secrets nouns, and cache-table strings are pinned by a file-read-only ExUnit contract."
    - "DOCS-03: Stale browser-start guidance for localhost or fixed loopback port 4000 fails the doc contract while qualified Docker-internal 4000 mechanics remain allowed."
    - "The new contract runs under mix test --no-start without starting Scoria.Application, DB, Docker, network, or external services."
  prohibitions:
    - "Do not add packages, support helper modules, application aliases, Docker commands, network calls, DB access, or CI topology changes."
    - "Do not fold sibling-repo migration, CI cache-key cleanup, approval-toast polish, release publish, or live registry smoke into this plan."
  artifacts:
    - path: "test/scoria/docker_dx_doc_contract_test.exs"
      provides: "Scoria.DockerDxDocContractTest file-read-only DOCS-03 contract"
      contains: "defmodule Scoria.DockerDxDocContractTest do"
      exports: []
  key_links:
    - from: "test/scoria/docker_dx_doc_contract_test.exs"
      to: "docs/docker_dev_dx.md"
      via: "File.read! of docs/docker_dev_dx.md"
      pattern: "File\\.read!\\(@doc_path\\)"
---

<!-- planner-discipline-allow: localhost:4000 -->
<!-- planner-discipline-allow: 127.0.0.1:4000 -->

<objective>
Create the dedicated Docker DX documentation contract required by DOCS-03 and locked by D-01, D-10, D-14, D-17, D-20, and D-24.

Purpose: Make the corrected Phase 33 Docker/native dev-DX truth mechanically durable without broad prose linting, app start, DB, Docker daemon, network, new dependencies, or helper modules.

Output: `test/scoria/docker_dx_doc_contract_test.exs` defining `Scoria.DockerDxDocContractTest`.

This plan intentionally does not implement sibling-repo convergence (D-02), CI cache-key cleanup (D-03), approval toast UI polish (D-04), release publish, or live post-publish smoke.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-CONTEXT.md
@.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-RESEARCH.md
@.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-VALIDATION.md
@docs/docker_dev_dx.md
@test/scoria/ci_policy_contract_test.exs
@brandbook/brand-book.md
</context>

## Artifacts This Phase Produces

| Artifact | Created/Changed By | Contract |
|----------|--------------------|----------|
| `test/scoria/docker_dx_doc_contract_test.exs` | Plan 01 | Defines `Scoria.DockerDxDocContractTest`; pins Docker DX doc tokens, stale URL semantics, and cache-table strings. |
| `test/scoria/ci_policy_contract_test.exs` | Plan 02 | Extends FLAKE-01 scanner to `.github/workflows/post-publish-smoke.yml` and removes duplicated Docker DX doc-token ownership. |
| `.github/workflows/post-publish-smoke.yml` | Plan 02 | Changes only `55432:5432` and `SCORIA_DB_PORT: 55432` to `5432`. |
| `.github/workflows/ci-verify.yml` | Plan 03 | Appends `test/scoria/docker_dx_doc_contract_test.exs` to the existing policy-lane command only. |
| CLI contract | Plans 01-03 | `SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/docker_dx_doc_contract_test.exs test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs test/scoria/adoption_surface_test.exs`. |
| Static contracts | Plans 01-03 | Zero `55432` hits in post-publish workflow; zero stale browser-start `localhost:4000` or fixed loopback `127.0.0.1:4000` hits in Docker DX docs. |

<source_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 34 | Pin Docker dev-DX commands and URLs, extend post-publish FLAKE-01 guard | 01, 02, 03 | COVERED | Plan 01 creates doc contract; Plan 02 extends/fixes port guard; Plan 03 wires policy lane. |
| REQ | DOCS-03 | Policy-lane Docker DX doc contract asserts canonical commands/URLs and stale dev-start absence | 01, 03 | COVERED | New ExUnit file plus policy-lane command wiring. |
| REQ | REL-03 context | Post-publish `55432` fix and scanner extension before release | 02 | COVERED | Planned as Phase 34 locked scope, not release execution. |
| RESEARCH | Dedicated file-read ExUnit contract, no packages, no helper modules | 01 | COVERED | Implements research Pattern 1. |
| RESEARCH | Context-aware 4000 guard allowing Docker-internal mechanics | 01 | COVERED | Implements research Pattern 2. |
| RESEARCH | Existing scanner extension and two-line workflow fix | 02 | COVERED | Implements research Pattern 3. |
| RESEARCH | Existing policy-lane explicit file list only | 03 | COVERED | No new jobs, services, matrices, or protected checks. |
| CONTEXT | D-01-D-26 | Locked decisions from Phase 34 context | 01, 02, 03 | COVERED | Each task action cites the decision IDs it implements or preserves. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create positive Docker DX doc-token contract</name>
  <files>test/scoria/docker_dx_doc_contract_test.exs</files>
  <read_first>
    - `.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-CONTEXT.md`
    - `.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-RESEARCH.md`
    - `.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-VALIDATION.md`
    - `docs/docker_dev_dx.md`
    - `test/scoria/ci_policy_contract_test.exs`
    - `brandbook/brand-book.md`
  </read_first>
  <behavior>
    - Test: `docs/docker_dev_dx.md` contains `make up`, `make dev`, `make nuke`, `ANTHROPIC_API_KEY`, and either `4799` or `http://localhost:4799/scoria`.
    - Test: `docs/docker_dev_dx.md` contains at least one process-scoped secrets setup token from `direnv` or `1Password`.
    - Test: `docs/docker_dev_dx.md` contains cache-table reader strings `mix deps.get`, `mix deps.compile`, and `app compile only`.
  </behavior>
  <action>Create `test/scoria/docker_dx_doc_contract_test.exs` per D-10 with `defmodule Scoria.DockerDxDocContractTest do` and `use ExUnit.Case, async: true`. Use a local `@doc_path "docs/docker_dev_dx.md"` and direct `File.read!(@doc_path)` only. Add positive assertion tests per D-14 and D-20 for the exact fragments named in the behavior block. Failure messages must follow D-15, D-16, and D-23: name the lost contract, include the fragment with `inspect/1`, and tell the maintainer to restore the Docker/native dev-DX contract or update the guard with Phase 34 rationale. Keep helpers private and local to the file per D-12 and D-22. Do not import or alias Scoria application modules; do not call `Application.ensure_all_started`, Repo, DB helpers, Docker commands, `System.cmd`, HTTP clients, or network APIs per D-24.</action>
  <verify>
    <automated>SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/docker_dx_doc_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/scoria/docker_dx_doc_contract_test.exs` exists.
    - The file contains `defmodule Scoria.DockerDxDocContractTest do`.
    - The file contains `use ExUnit.Case, async: true`.
    - The file reads `docs/docker_dev_dx.md` directly and does not read `.env`, `.envrc`, `.env.op`, shell history, logs, or token-bearing files.
    - The focused `mix test --no-start` command exits 0 without app, DB, Docker, or network startup.
  </acceptance_criteria>
  <done>DOCS-03 positive Docker DX and cache-table tokens are protected by the new file-read-only contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add context-aware stale browser-start guard</name>
  <files>test/scoria/docker_dx_doc_contract_test.exs</files>
  <read_first>
    - `test/scoria/docker_dx_doc_contract_test.exs`
    - `docs/docker_dev_dx.md`
    - `compose.yml`
    - `Makefile`
    - `.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-CONTEXT.md`
    - `.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-RESEARCH.md`
  </read_first>
  <behavior>
    - Test: direct browser-start URL forms using the localhost 4000 route fail.
    - Test: command-context stale forms such as open, visit, browse, browser, go to, or curl with localhost/fixed loopback port 4000 fail.
    - Test: browser-start or fallback paragraphs containing `4000` pass only when they include an allowed qualifier: Docker-internal, container, Traefik, service target, `loadbalancer.server.port=4000`, `web:4000`, `127.0.0.1::4000`, `docker compose port web 4000`, CI, or ephemeral fallback.
    - Test: the current anti-footgun/no-juggling sentence in `docs/docker_dev_dx.md` is explicitly allowed even though it mentions `4000`, because it tells readers not to juggle fixed ports rather than telling them where to start a browser.
  </behavior>
  <action>Extend `Scoria.DockerDxDocContractTest` per D-17, D-18, and D-19 with a separate stale URL test. Use two layers: a direct refute for browser-start URL patterns and a context-aware scan scoped to doc paragraphs or lines that pair `4000` with browser-start or fallback wording. The contextual scan must allow the current top-level anti-footgun/no-juggling copy and require one of the allowed qualifiers listed in the behavior block only for browser-start/fallback contexts. The failure text must name the repair path from D-19: Docker `make up`/`make url`/`http://<instance>.localhost/scoria` or native `make dev`/`http://localhost:4799/scoria`. Do not ban all `:4000` references, do not scan `compose.yml` as if it were browser guidance, and do not rewrite `docs/docker_dev_dx.md` unless the new guard reveals a real stale browser-start hit in the current doc.</action>
  <verify>
    <automated>SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/docker_dx_doc_contract_test.exs</automated>
    <automated>if rg -n "localhost:4000|127\\.0\\.0\\.1:4000" docs/docker_dev_dx.md; then exit 1; else exit 0; fi</automated>
  </verify>
  <acceptance_criteria>
    - The stale URL test rejects fixed browser-start copy for localhost or fixed loopback port 4000.
    - The current allowed `4000` occurrences in `docs/docker_dev_dx.md`, including the anti-footgun/no-juggling copy, pass the new contract without a docs rewrite.
    - Injecting browser-start/dev-start forms using localhost port 4000 or fixed loopback port 4000 would fail the contract.
    - Qualified Docker-internal/container/Traefik/ephemeral fallback mechanics in `docs/docker_dev_dx.md` remain allowed.
    - The static stale browser-start `rg` command returns zero matches.
    - The contract remains file-read-only and `mix test --no-start` safe.
  </acceptance_criteria>
  <done>DOCS-03 stale browser-start drift is mechanically rejected without invalidating legitimate Docker-internal `4000` mechanics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs -> ExUnit policy test | Repository prose is parsed by a test that must stay file-read-only and side-effect free. |
| maintainer copy -> local dev behavior | Incorrect docs can direct maintainers to stale fixed-port browser-start commands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | `docs/docker_dev_dx.md` canonical command/URL copy | mitigate | Assert load-bearing command, URL, secrets, and cache-table fragments in `Scoria.DockerDxDocContractTest`. |
| T-34-02 | Denial of Service | Local dev browser-start docs | mitigate | Reject stale fixed-port browser-start forms while allowing qualified Docker-internal mechanics. |
| T-34-03 | Information Disclosure | Secrets docs contract | mitigate | Assert only `ANTHROPIC_API_KEY` variable name plus process-scoped setup nouns; do not inspect local secret files. |
| T-34-SC | Tampering | package installs | accept | No npm, pip, cargo, Hex package, or helper-module install is planned; package legitimacy gate is not applicable. |
</threat_model>

<verification>
Run after this plan:
- `SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/docker_dx_doc_contract_test.exs`
- `if rg -n "localhost:4000|127\\.0\\.0\\.1:4000" docs/docker_dev_dx.md; then exit 1; else exit 0; fi`

Wave-level verification after Plans 01, 02, and 03:
- `SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/docker_dx_doc_contract_test.exs test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs test/scoria/adoption_surface_test.exs`
- `if rg -n "55432" .github/workflows/post-publish-smoke.yml; then exit 1; else exit 0; fi`
- `if rg -n "localhost:4000|127\\.0\\.0\\.1:4000" docs/docker_dev_dx.md; then exit 1; else exit 0; fi`
</verification>

<success_criteria>
- DOCS-03 doc-token ownership lives in `test/scoria/docker_dx_doc_contract_test.exs`.
- `mix test --no-start` runs the new contract without application start, DB, Docker daemon, network, packages, or helper modules.
- No Phase 35 release publish, registry smoke, sibling-repo migration, CI topology change, or UI polish work is included.
</success_criteria>

<output>
Create `.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-01-SUMMARY.md` when done.
</output>
