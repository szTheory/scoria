---
phase: 34-docker-dx-drift-guard-ci-guard-extension
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/post-publish-smoke.yml
  - test/scoria/ci_policy_contract_test.exs
autonomous: true
requirements: [DOCS-03]
must_haves:
  truths:
    - "DOCS-03 plus REL-03 context: FLAKE-01 fixed-host-port enforcement scans post-publish-smoke.yml and rejects host ports >= 32768."
    - "post-publish-smoke.yml uses `5432:5432` and `SCORIA_DB_PORT: 5432`, with no remaining `55432` hits."
    - "ci_policy_contract_test.exs remains focused on CI policy, Compose, Makefile, Dockerfile structure, and the post-publish workflow guard rather than Docker DX doc-token ownership."
  prohibitions:
    - "Do not add an allowlist for post-publish-smoke.yml or leave an intentionally red guard."
    - "Do not switch to random host ports, job.services port plumbing, new YAML parser dependencies, new CI jobs, or release publishing work."
  artifacts:
    - path: ".github/workflows/post-publish-smoke.yml"
      provides: "Post-publish Postgres bind below Linux ephemeral range"
      contains: "5432:5432"
    - path: "test/scoria/ci_policy_contract_test.exs"
      provides: "Extended FLAKE-01 scanner covering post-publish-smoke.yml"
      contains: "@post_publish_smoke"
  key_links:
    - from: "test/scoria/ci_policy_contract_test.exs"
      to: ".github/workflows/post-publish-smoke.yml"
      via: "File.read!(@post_publish_smoke) inside the FLAKE-01 scanner"
      pattern: "post_publish.*File\\.read!\\(@post_publish_smoke\\)"
---

<!-- planner-discipline-allow: 55432 -->
<!-- planner-discipline-allow: localhost:4000 -->
<!-- planner-discipline-allow: http://localhost:4000/scoria -->
<!-- planner-discipline-allow: @docker_dx_docs -->
<!-- planner-discipline-allow: Docker DX guide documents the collision-resistant workflow -->

<objective>
Close the v3.1 FLAKE-01 blind spot by fixing and scanning the post-publish workflow per D-05, D-06, D-07, D-08, and D-09, while narrowing old Docker DX guide assertions in `ci_policy_contract_test.exs` per D-11.

Purpose: The new guard must pass in the same phase that introduces it, and `ci_policy_contract_test.exs` must stop owning duplicated Docker DX doc tokens now owned by Plan 01.

Output: A two-line post-publish port fix plus an extended FLAKE-01 scanner in the existing policy contract.

This plan does not publish `0.1.2`, merge release PR #3, run live registry smoke, change CI topology, change `CI / ci-gate`, or alter `Scoria.VerificationLanes.closeout_order/0`.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-CONTEXT.md
@.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-RESEARCH.md
@.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-VALIDATION.md
@.github/workflows/post-publish-smoke.yml
@.github/workflows/ci.yml
@.github/workflows/ci-verify.yml
@test/scoria/ci_policy_contract_test.exs
@docs/docker_dev_dx.md
@.env.example
</context>

## Artifacts This Phase Produces

| Artifact | Created/Changed By | Contract |
|----------|--------------------|----------|
| `test/scoria/docker_dx_doc_contract_test.exs` | Plan 01 | Defines `Scoria.DockerDxDocContractTest`; pins Docker DX doc tokens, stale URL semantics, and cache-table strings. |
| `test/scoria/ci_policy_contract_test.exs` | Plan 02 | Extends FLAKE-01 scanner to `.github/workflows/post-publish-smoke.yml` and removes duplicated Docker DX doc-token ownership. |
| `.github/workflows/post-publish-smoke.yml` | Plan 02 | Changes only `55432:5432` and `SCORIA_DB_PORT: 55432` to `5432`. |
| `.github/workflows/ci-verify.yml` | Plan 03 | Appends `test/scoria/docker_dx_doc_contract_test.exs` to the existing policy-lane command only. |
| CLI contract | Plans 01-03 | `SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/docker_dx_doc_contract_test.exs test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs test/scoria/adoption_surface_test.exs`. |
| Static contracts | Plans 01-03 | Zero `55432` hits in post-publish workflow; zero stale browser-start `localhost:4000` or fixed loopback `127.0.0.1:4000` hits in Docker DX docs. |

## Objective Decision Coverage

- D-05: Absorb the tiny post-publish workflow port fix in Phase 34 so the new guard passes now.
- D-06: Change only `55432:5432` to `5432:5432` and `SCORIA_DB_PORT: 55432` to `SCORIA_DB_PORT: 5432`.
- D-07: Do not add an allowlist, exception, or intentionally failing guard for `post-publish-smoke.yml`.
- D-08: Do not switch post-publish smoke to random host ports or `job.services.*.ports[...]`.
- D-09: Make Phase 35 treat the REL-03 port-fix portion as already satisfied and re-verify it with zero `55432` hits plus the extended policy test.
- D-10: Leave the dedicated `Scoria.DockerDxDocContractTest` ownership to Plan 01.
- D-11: Narrow the old Docker DX guide assertions in `ci_policy_contract_test.exs` so the omnibus file stays focused on CI policy and structural guards.
- D-21: Preserve existing structural Dockerfile/cache guards in `ci_policy_contract_test.exs`.
- D-23: Keep policy-test failure messages calm, exact, and useful.
- D-24: Avoid app start, DB, Docker daemon, network, package, parser dependency, and CI topology changes.

<source_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 34 | Pin Docker dev-DX commands and URLs, extend post-publish FLAKE-01 guard | 01, 02, 03 | COVERED | Plan 02 owns the post-publish blind spot and old ownership cleanup. |
| REQ | DOCS-03 | Policy-lane Docker DX doc contract asserts canonical commands/URLs and stale dev-start absence | 01, 03 | COVERED | Plan 02 removes duplicate doc-token ownership from the old omnibus file. |
| REQ | REL-03 context | Post-publish `55432` fix and scanner extension before release | 02 | COVERED | This plan fixes and guards the workflow before Phase 35 release work. |
| RESEARCH | Existing scanner extension and two-line workflow fix | 02 | COVERED | Reuses `job_blocks/1` and current port-bind regex. |
| RESEARCH | No YAML parser dependency or random-port redesign | 02 | COVERED | Explicitly prohibited by the action. |
| CONTEXT | D-05-D-11 | Post-publish and ownership decisions | 02 | COVERED | Cited in tasks below. |
| CONTEXT | D-21, D-23, D-24, D-25, D-26 | Structural guard split, voice, boring implementation, verification | 02 | COVERED | Cited in action and verification. |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Fix post-publish Postgres host-port bind</name>
  <files>.github/workflows/post-publish-smoke.yml</files>
  <read_first>
    - `.github/workflows/post-publish-smoke.yml`
    - `.github/workflows/ci.yml`
    - `.github/workflows/ci-verify.yml`
    - `.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-CONTEXT.md`
    - `.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-RESEARCH.md`
  </read_first>
  <action>Modify only `.github/workflows/post-publish-smoke.yml` per D-05 and D-06: change the Postgres service port mapping from `55432:5432` to `5432:5432`, and change `SCORIA_DB_PORT: 55432` to `SCORIA_DB_PORT: 5432`. Preserve the workflow name, triggers, concurrency, job name, service shape, env names, steps, summary text, attest command, and release wiring. Do not switch to random host ports or `job.services.*.ports[...]` per D-08. Do not add allowlists or known-red behavior per D-07.</action>
  <verify>
    <automated>if rg -n "55432" .github/workflows/post-publish-smoke.yml; then exit 1; else exit 0; fi</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/post-publish-smoke.yml` contains `5432:5432`.
    - `.github/workflows/post-publish-smoke.yml` contains `SCORIA_DB_PORT: 5432`.
    - `rg -n "55432" .github/workflows/post-publish-smoke.yml` returns zero matches.
    - No other post-publish smoke behavior changes are made.
  </acceptance_criteria>
  <done>The post-publish workflow no longer binds a fixed host port in the Linux ephemeral range.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend FLAKE-01 scanner and narrow old doc ownership</name>
  <files>test/scoria/ci_policy_contract_test.exs</files>
  <read_first>
    - `test/scoria/ci_policy_contract_test.exs`
    - `.github/workflows/post-publish-smoke.yml`
    - `.github/workflows/ci.yml`
    - `.github/workflows/ci-verify.yml`
    - `docs/docker_dev_dx.md`
    - `.env.example`
    - `.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-CONTEXT.md`
    - `.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-RESEARCH.md`
  </read_first>
  <behavior>
    - Test: `ci_policy_contract_test.exs` reads `.github/workflows/post-publish-smoke.yml` inside the existing `no CI Postgres job binds a host port in the ephemeral range (>= 32768)` contract.
    - Test: Postgres job blocks from `ci.yml`, `ci-verify.yml`, and `post-publish-smoke.yml` are all scanned with the existing host-port regex and per-block non-empty guard.
    - Test: the combined Postgres block map explicitly contains the post-publish smoke job key, such as `post-publish-smoke.yml:smoke`, so a missing post-publish scan cannot pass only by satisfying a count threshold.
    - Test: `ci_policy_contract_test.exs` no longer owns canonical Docker DX doc-token assertions that are covered by `Scoria.DockerDxDocContractTest`.
  </behavior>
  <action>Add `@post_publish_smoke ".github/workflows/post-publish-smoke.yml"` near the existing workflow path attributes. In the existing FLAKE-01 test, read `post_publish = File.read!(@post_publish_smoke)`, derive `post_publish_postgres` with `job_blocks(post_publish)`, filter bodies containing `postgres:`, and prefix each key as `post-publish-smoke.yml:#{job}` before combining with `verify_postgres` and `entry_postgres`. Keep the current `Regex.scan(~r/-\s*["']?(\d+):\d+(?:\/tcp)?["']?/, body)` extraction and `host_port < @ephemeral_range_min` assertion. Raise the non-empty map-size guard to at least 6, update the failure text to name `ci.yml`, `ci-verify.yml`, and `post-publish-smoke.yml`, and add an exact post-publish inclusion assertion such as `Map.has_key?(postgres_blocks, "post-publish-smoke.yml:smoke")` with failure text that says the FLAKE-01 scanner must include the post-publish smoke workflow. The map-size guard is only a breadth check; the exact key assertion is required so a missing post-publish scan cannot pass by count alone. Then replace the broad `Docker DX guide documents the collision-resistant workflow` test per D-11 with a non-doc `.env.example` instance-example guard only: read `.env.example`, assert it contains `scoria-myfeature-a1b2c3d4`, and remove the `docs/docker_dev_dx.md` read, `@docker_dx_docs` attribute, canonical doc-token assertions, and stale URL refute from this file. Keep Dockerfile, Compose, Makefile, and secrets-structure tests in `ci_policy_contract_test.exs` per D-21. Use calm, exact failure messages per D-23 and do not add packages, parser dependencies, app start, DB, Docker, or network work per D-24.</action>
  <verify>
    <automated>SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/ci_policy_contract_test.exs</automated>
    <automated>if rg -n "@docker_dx_docs|Docker DX guide documents the collision-resistant workflow|http://localhost:4000/scoria" test/scoria/ci_policy_contract_test.exs; then exit 1; else exit 0; fi</automated>
    <automated>if rg -n "55432" .github/workflows/post-publish-smoke.yml; then exit 1; else exit 0; fi</automated>
  </verify>
  <acceptance_criteria>
    - The FLAKE-01 scanner reads `.github/workflows/post-publish-smoke.yml`.
    - The FLAKE-01 scanner asserts the combined Postgres block map includes `post-publish-smoke.yml:smoke` or an equivalent exact post-publish smoke job key.
    - A post-publish host port of `32768` or higher would fail the existing policy test.
    - The post-publish workflow fix and scanner pass together with no allowlist.
    - `ci_policy_contract_test.exs` no longer contains the broad Docker DX guide doc-token ownership block.
    - Existing CI policy, Compose, Makefile, Dockerfile, and structural secrets assertions remain in `ci_policy_contract_test.exs`.
  </acceptance_criteria>
  <done>The existing CI policy contract now covers post-publish fixed host ports and no longer duplicates the dedicated Docker DX doc-token contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions workflow -> policy test | Workflow YAML is parsed by regex contracts instead of executed. |
| release smoke service config -> GitHub runner | Fixed host ports can collide with Linux ephemeral allocations on runners. |
| old doc assertions -> new doc contract | Ownership transfer must not leave duplicate or missing doc-token guards. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04 | Denial of Service | `.github/workflows/post-publish-smoke.yml` Postgres service | mitigate | Replace ephemeral-range fixed host bind with `5432:5432` and env `SCORIA_DB_PORT: 5432`. |
| T-34-05 | Tampering | FLAKE-01 scanner coverage | mitigate | Add `.github/workflows/post-publish-smoke.yml` to the same scanner as `ci.yml` and `ci-verify.yml`; fail if regex extracts no binds. |
| T-34-06 | Repudiation | Docker DX doc-token ownership | mitigate | Remove duplicated doc-token assertions from the omnibus CI policy test so failures have one owning contract. |
| T-34-SC | Tampering | package installs | accept | No new package installation or YAML parser dependency is planned. |
</threat_model>

<verification>
Run after this plan:
- `SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/ci_policy_contract_test.exs`
- `if rg -n "55432" .github/workflows/post-publish-smoke.yml; then exit 1; else exit 0; fi`
- `if rg -n "@docker_dx_docs|Docker DX guide documents the collision-resistant workflow|http://localhost:4000/scoria" test/scoria/ci_policy_contract_test.exs; then exit 1; else exit 0; fi`

Wave-level verification after Plans 01, 02, and 03:
- `SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/docker_dx_doc_contract_test.exs test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs test/scoria/adoption_surface_test.exs`
- `if rg -n "55432" .github/workflows/post-publish-smoke.yml; then exit 1; else exit 0; fi`
</verification>

<success_criteria>
- `.github/workflows/post-publish-smoke.yml` has no `55432` hits.
- `ci_policy_contract_test.exs` scans post-publish smoke workflow Postgres binds for host ports greater than or equal to `32768`.
- Docker DX doc-token ownership is moved to Plan 01's dedicated file without deleting existing Compose, Makefile, Dockerfile, CI policy, or secrets-structure assertions.
- No release publish, live smoke, random-port redesign, YAML dependency, CI topology change, or protected-check rename is planned or implemented.
</success_criteria>

<output>
Create `.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-02-SUMMARY.md` when done.
</output>
