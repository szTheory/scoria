---
phase: 34-docker-dx-drift-guard-ci-guard-extension
plan: 03
type: execute
wave: 2
depends_on:
  - 34-01
  - 34-02
files_modified:
  - .github/workflows/ci-verify.yml
autonomous: true
requirements:
  - DOCS-03
must_haves:
  truths:
    - "The existing CI policy lane runs the new Docker DX doc contract with `mix test --no-start --warnings-as-errors`."
    - "The policy lane remains the same job and protected-check topology; only the explicit test-file list changes."
    - "Final Phase 34 verification passes the combined policy-lane command and both static drift checks."
  prohibitions:
    - "Do not add CI jobs, services, matrices, dependencies, workflow needs, or required-check name changes."
    - "Do not modify `CI / ci-gate` or `Scoria.VerificationLanes.closeout_order/0`."
  artifacts:
    - path: ".github/workflows/ci-verify.yml"
      provides: "Existing policy lane includes Docker DX doc contract"
      contains: "test/scoria/docker_dx_doc_contract_test.exs"
  key_links:
    - from: ".github/workflows/ci-verify.yml"
      to: "test/scoria/docker_dx_doc_contract_test.exs"
      via: "explicit policy-lane `mix test --no-start --warnings-as-errors` file list"
      pattern: "mix test --no-start --warnings-as-errors .*docker_dx_doc_contract_test\\.exs"
---

<!-- planner-discipline-allow: 55432 -->
<!-- planner-discipline-allow: localhost:4000 -->
<!-- planner-discipline-allow: 127.0.0.1:4000 -->

<objective>
Wire the new DOCS-03 contract into the existing policy lane and run the final Phase 34 guard set after Plans 01 and 02 land.

Purpose: Convert the new file-read-only guard into CI policy coverage without changing CI topology, `CI / ci-gate`, required check names, services, matrices, or `Scoria.VerificationLanes.closeout_order/0`.

Output: `.github/workflows/ci-verify.yml` policy-lane command appends `test/scoria/docker_dx_doc_contract_test.exs`.

This plan implements D-13, D-24, D-25, and D-26 and preserves the milestone constraints in `.planning/STATE.md`.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-CONTEXT.md
@.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-RESEARCH.md
@.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-VALIDATION.md
@.github/workflows/ci-verify.yml
@.github/workflows/ci.yml
@.github/workflows/post-publish-smoke.yml
@test/scoria/docker_dx_doc_contract_test.exs
@test/scoria/ci_policy_contract_test.exs
@test/scoria/verification_lanes_test.exs
@test/scoria/adoption_surface_test.exs
@docs/MAINTAINERS.md
</context>

## Artifacts This Phase Produces

| Artifact | Created/Changed By | Contract |
|----------|--------------------|----------|
| `test/scoria/docker_dx_doc_contract_test.exs` | Plan 01 | Defines `Scoria.DockerDxDocContractTest`; pins Docker DX doc tokens, stale URL semantics, and cache-table strings. |
| `test/scoria/ci_policy_contract_test.exs` | Plan 02 | Extends FLAKE-01 scanner to `.github/workflows/post-publish-smoke.yml` and removes duplicated Docker DX doc-token ownership. |
| `.github/workflows/post-publish-smoke.yml` | Plan 02 | Changes only `55432:5432` and `SCORIA_DB_PORT: 55432` to `5432`. |
| `.github/workflows/ci-verify.yml` | Plan 03 | Appends `test/scoria/docker_dx_doc_contract_test.exs` to the existing policy-lane command only. |
| CLI contract | Plans 01-03 | `SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/docker_dx_doc_contract_test.exs test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs test/scoria/adoption_surface_test.exs`. |
| Static contracts | Plans 01-03 | Zero `55432` hits in post-publish workflow; zero stale browser-start `localhost:4000` or fixed loopback `127.0.0.1:4000` hits in Docker DX docs. |

<source_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 34 | Pin Docker dev-DX commands and URLs, extend post-publish FLAKE-01 guard | 01, 02, 03 | COVERED | Plan 03 is final policy-lane integration and verification. |
| REQ | DOCS-03 | Policy-lane Docker DX doc contract asserts canonical commands/URLs and stale dev-start absence | 01, 03 | COVERED | Plan 03 appends the test file to the explicit policy lane. |
| REQ | REL-03 context | Post-publish `55432` fix and scanner extension before release | 02, 03 | COVERED | Plan 03 verifies the zero-hit static check after Plan 02. |
| RESEARCH | Existing policy-lane explicit file list only | 03 | COVERED | The only CI edit is the existing command's file list. |
| RESEARCH | Final validation architecture | 03 | COVERED | Runs the combined command and static `rg` checks from validation strategy. |
| CONTEXT | D-13 | Append new file to existing policy-lane command only | 03 | COVERED | Implemented by Task 1. |
| CONTEXT | D-24-D-26 | Boring implementation and required verification | 03 | COVERED | Implemented by Task 1 verify and success criteria. |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Append Docker DX doc contract to existing policy lane</name>
  <files>.github/workflows/ci-verify.yml</files>
  <read_first>
    - `.github/workflows/ci-verify.yml`
    - `.github/workflows/ci.yml`
    - `.github/workflows/post-publish-smoke.yml`
    - `test/scoria/docker_dx_doc_contract_test.exs`
    - `test/scoria/ci_policy_contract_test.exs`
    - `test/scoria/verification_lanes_test.exs`
    - `test/scoria/adoption_surface_test.exs`
    - `.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-CONTEXT.md`
    - `.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-RESEARCH.md`
    - `.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-VALIDATION.md`
  </read_first>
  <action>Update only the existing `policy` job step named `Verify lane-contract tests with warnings as errors` in `.github/workflows/ci-verify.yml` per D-13. Append `test/scoria/docker_dx_doc_contract_test.exs` to the explicit `mix test --no-start --warnings-as-errors` file list, preferably between `test/scoria/ci_policy_contract_test.exs` and `test/scoria/verification_lanes_test.exs`. Preserve `SCORIA_LANE_CONTRACT_ONLY: "true"`, the step name, job name, workflow name, `workflow_call`, cache steps, service-free policy job, all downstream jobs, all `needs`, `verify-summary`, and `CI / ci-gate`. Do not add jobs, services, matrices, dependencies, protected check names, or changes to `Scoria.VerificationLanes.closeout_order/0` per D-24.</action>
  <verify>
    <automated>SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/docker_dx_doc_contract_test.exs test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs test/scoria/adoption_surface_test.exs</automated>
    <automated>if rg -n "55432" .github/workflows/post-publish-smoke.yml; then exit 1; else exit 0; fi</automated>
    <automated>if rg -n "localhost:4000|127\\.0\\.0\\.1:4000" docs/docker_dev_dx.md; then exit 1; else exit 0; fi</automated>
  </verify>
  <acceptance_criteria>
    - The `ci-verify.yml` policy lane command contains `test/scoria/docker_dx_doc_contract_test.exs`.
    - No new `jobs:` entries, services under the policy job, matrices, workflow dependencies, or protected-check names are added.
    - The combined D-25 policy command exits 0.
    - The D-26 static checks for post-publish `55432` and stale browser-start doc URLs both return zero matches.
  </acceptance_criteria>
  <done>The existing policy lane runs the new Docker DX doc contract and the final Phase 34 verification set is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow config -> protected check | Small file-list edits must not alter branch-protection topology. |
| policy lane -> repo file contracts | Policy tests must remain DB-free and app-start-free under `--no-start`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-07 | Tampering | `.github/workflows/ci-verify.yml` policy lane | mitigate | Append only the new file path to the existing command; verify no topology edits. |
| T-34-08 | Denial of Service | policy job execution | mitigate | Keep policy job service-free and run file-read-only contracts under `mix test --no-start`. |
| T-34-09 | Repudiation | Phase 34 release-readiness proof | mitigate | Run combined policy command plus static drift checks from D-25 and D-26. |
| T-34-SC | Tampering | package installs | accept | No package installation or dependency change is planned. |
</threat_model>

<verification>
Run after this plan:
- `SCORIA_LANE_CONTRACT_ONLY=true mix test --no-start --warnings-as-errors test/scoria/docker_dx_doc_contract_test.exs test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs test/scoria/adoption_surface_test.exs`
- `if rg -n "55432" .github/workflows/post-publish-smoke.yml; then exit 1; else exit 0; fi`
- `if rg -n "localhost:4000|127\\.0\\.0\\.1:4000" docs/docker_dev_dx.md; then exit 1; else exit 0; fi`

Do not substitute a full release flow for these checks in Phase 34. `mix docs`, release PR merge, Hex publish, Hex info, and live `mix scoria.post_publish_smoke` remain Phase 35 work.
</verification>

<success_criteria>
- The existing `ci-verify.yml` policy lane includes the new Docker DX doc contract file.
- The final policy-lane command from D-25 passes.
- The static drift checks from D-26 pass.
- `CI / ci-gate`, `ci.yml`, and `Scoria.VerificationLanes.closeout_order/0` remain unchanged.
- Phase 34 is ready for Phase 35 to verify REL-03 without carrying the old post-publish port blind spot.
</success_criteria>

<output>
Create `.planning/phases/34-docker-dx-drift-guard-ci-guard-extension/34-03-SUMMARY.md` when done.
</output>
