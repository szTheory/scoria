---
phase: 79-tarball-consumer-overlay-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/support/scoria/host_app_proof/generator.ex
  - test/support/scoria/host_app_proof/runner.ex
  - test/scoria/host_app_consumer_proof_test.exs
autonomous: true
requirements: [HEX-CONSUMER-01]
must_haves:
  truths:
    - "host_app_consumer_proof_test calls Runner.run_full_proof!/1 — not run_route_proof!/1 (D-30, D-33)."
    - "Tarball dep assertions unchanged: dep_mode :hex_tarball, refute repo-root path, assert tarball_dep_snippet (D-31)."
    - "proof.steps asserted as install prefix ++ overlay atoms derived from host.overlay_tests sorted basenames — not hardcoded tuple (D-32)."
    - "proof.steps must NOT include :overlay_smoke — only per-file atoms from smoke_pair!/1 (D-32)."
    - "Overlay step order follows sorted filenames: handoff → route → runtime — not requirement prose order."
    - "run_route_proof!/1 remains as route-only debug seam; consumer test must not call it (D-33)."
    - "@moduletag timeout 180_000 and async false preserved (D-34, D-20)."
    - "D-36: Warm full overlay proof target ≤90s p50, ≤120s p95; module tag 180_000 unchanged — no mix test.adoption contract or global ExUnit widen."
    - "D-38: Reject 420_000 module tag, global adoption timeout widen, and per-overlay separate ExUnit cases (one phx.new host per run)."
    - "D-37 escalation (240_000) only if CI exceeds 120s on two consecutive runs — documented in 79-VALIDATION.md, not default in this plan."
    - "@moduletag :host_proof enables mix test --only host_proof local iteration (D-43)."
    - "Host map includes working_root and unpack_root for downstream snapshot/preserve (79-02 dependency)."
  artifacts:
    - path: "test/scoria/host_app_consumer_proof_test.exs"
      provides: "Full overlay tarball consumer proof with derived seven-step contract"
      contains: "run_full_proof!"
    - path: "test/support/scoria/host_app_proof/runner.ex"
      provides: "expected_steps/1 SSOT helper shared with consumer assertion"
      contains: "def expected_steps"
    - path: "test/support/scoria/host_app_proof/generator.ex"
      provides: "Host map enriched with working_root, unpack_root, dep_mode"
      contains: "working_root:"
  key_links:
    - from: "test/scoria/host_app_consumer_proof_test.exs"
      to: "test/support/scoria/host_app_proof/runner.ex"
      via: "run_full_proof!/1 and expected_steps/1"
      pattern: "run_full_proof!"
    - from: "test/scoria/host_app_consumer_proof_test.exs"
      to: "test/support/scoria/host_app_proof/generator.ex"
      via: "host.overlay_tests drives derived overlay atoms"
      pattern: "host.overlay_tests"
    - from: "test/support/scoria/host_app_proof/generator.ex"
      to: "priv/host_app_proof/overlay/test/*.exs"
      via: "overlay_test_files/0 Enum.sort basename SSOT"
      pattern: "overlay_test_files"
---

<objective>
Switch merge-blocking adoption consumer proof from route-only to full v2.9 overlay depth on tarball dep, with derived step contract SSOT and local iteration tag.

Purpose: Complete the Phase 78 seam (D-19) — prove packaged `mix hex.build` artifact runs route + HOST-01 runtime + HOST-02 handoff overlays inside `mix test.adoption`.
Output: Green `host_app_consumer_proof_test.exs` with seven-step derived assertion; `@moduletag :host_proof`; enriched host map for diagnostics wave.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/79-tarball-consumer-overlay-proof/79-CONTEXT.md
@.planning/phases/79-tarball-consumer-overlay-proof/79-RESEARCH.md
@.planning/phases/79-tarball-consumer-overlay-proof/79-VALIDATION.md
@.planning/phases/79-tarball-consumer-overlay-proof/79-PATTERNS.md
@test/scoria/host_app_consumer_proof_test.exs
@test/support/scoria/host_app_proof/runner.ex
@test/support/scoria/host_app_proof/generator.ex
@lib/scoria/hex_consumer_contract.ex
@priv/host_app_proof/overlay/test/

Preserve `@moduletag timeout: 180_000`, `async: false`, one phx.new host per run (D-34, D-38). Do not modify `lib/mix/tasks/test.adoption.ex`, `HexConsumerContract`, overlay priv sources, or `VerificationLanes.closeout_order/0`. Do not mark HEX-CONSUMER-01 Complete in REQUIREMENTS.md — ceremony deferred to 79-03 (D-45).
</context>

<tasks>

<task type="auto">
  <name>Task 79-01-01: Enrich Generator host map with diagnostic fields</name>
  <requirement_ids>HEX-CONSUMER-01</requirement_ids>
  <files>test/support/scoria/host_app_proof/generator.ex</files>
  <read_first>
    test/support/scoria/host_app_proof/generator.ex
    .planning/phases/79-tarball-consumer-overlay-proof/79-CONTEXT.md
    .planning/phases/79-tarball-consumer-overlay-proof/79-RESEARCH.md
  </read_first>
  <action>
Extend `Scoria.TestSupport.HostAppProof.Generator.create_host!/1` return map (lines 62–68) with fields required by D-39/D-41 and 79-02 snapshot/preserve:

1. Add `working_root: working_root` — the parent directory passed to `register_cleanup/2` (line 47), not only `host.root`.
2. Add `dep_mode: dep_mode` — the validated atom from `Keyword.fetch!(opts, :dep_mode)`.
3. Add `unpack_root: Keyword.get(opts, :unpack_root)` — present for `:hex_tarball`, `nil` for `:path`.

Keep existing keys (`app_name`, `db_name`, `root`, `repo_root`, `overlay_tests`) unchanged. Do not change dep patching, overlay copy, or cleanup behavior in this task.
  </action>
  <acceptance_criteria>
    - `rg -n 'working_root:' test/support/scoria/host_app_proof/generator.ex` returns match in create_host! return map.
    - `rg -n 'dep_mode: dep_mode' test/support/scoria/host_app_proof/generator.ex` returns match in return map.
    - `rg -n 'unpack_root: Keyword.get' test/support/scoria/host_app_proof/generator.ex` returns match in return map.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <done>Host struct carries working_root, dep_mode, and unpack_root for Runner diagnostics in 79-02.</done>
</task>

<task type="auto">
  <name>Task 79-01-02: Add Runner.expected_steps/1 derived SSOT helper</name>
  <requirement_ids>HEX-CONSUMER-01</requirement_ids>
  <files>test/support/scoria/host_app_proof/runner.ex</files>
  <read_first>
    test/support/scoria/host_app_proof/runner.ex
    test/support/scoria/host_app_proof/generator.ex
    .planning/phases/79-tarball-consumer-overlay-proof/79-CONTEXT.md
  </read_first>
  <action>
In `Scoria.TestSupport.HostAppProof.Runner`, add public function `expected_steps/1`:

```elixir
def expected_steps(host) do
  install = [:deps_get, :scoria_install, :ecto_create, :ecto_migrate]

  overlay =
    host.overlay_tests
    |> Enum.map(fn file -> file |> Path.rootname() |> String.to_atom() end)

  install ++ overlay
end
```

Place after `run_full_proof!/1` and before `run_steps/2`. Do not modify `run_route_proof!/1`, `run_full_proof!/1`, or `smoke_pair!/1` behavior. Do not expose `:overlay_smoke` in returned list — only install atoms plus per-file overlay atoms matching `run_steps/2` flat-map output.

Expected value today (sorted overlay basenames from `priv/host_app_proof/overlay/test/`):

`[:deps_get, :scoria_install, :ecto_create, :ecto_migrate, :host_handoff_smoke_test, :host_route_smoke_test, :host_runtime_smoke_test]`
  </action>
  <acceptance_criteria>
    - `rg -n 'def expected_steps' test/support/scoria/host_app_proof/runner.ex` returns match.
    - `expected_steps/1` return value does not include `:overlay_smoke` — verify with `MIX_ENV=test mix run -e 'h=%{overlay_tests: Scoria.TestSupport.HostAppProof.Generator.overlay_test_files()}; refute :overlay_smoke in Scoria.TestSupport.HostAppProof.Runner.expected_steps(h)'` exits 0.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <done>Single SSOT helper derives install ++ overlay step list from host.overlay_tests sort order.</done>
</task>

<task type="auto">
  <name>Task 79-01-03: Switch consumer test to run_full_proof! with derived steps and :host_proof tag</name>
  <requirement_ids>HEX-CONSUMER-01</requirement_ids>
  <files>test/scoria/host_app_consumer_proof_test.exs</files>
  <read_first>
    test/scoria/host_app_consumer_proof_test.exs
    test/support/scoria/host_app_proof/runner.ex
    test/support/scoria/host_app_proof/generator.ex
    lib/scoria/hex_consumer_contract.ex
    .planning/phases/79-tarball-consumer-overlay-proof/79-CONTEXT.md
  </read_first>
  <action>
Rewrite `Scoria.HostAppConsumerProofTest`:

1. Add `@moduletag :host_proof` below existing `@moduletag timeout: 180_000` (D-43).
2. Keep `async: false`, `setup_all` with `ensure_current_unpack_root!/0`, tarball dep assertions unchanged (D-31):
   - `refute mix_exs =~ "{:scoria, path: #{inspect(host.repo_root)}}"`
   - `assert mix_exs =~ Scoria.HexConsumerContract.tarball_dep_snippet(unpack_root)`
3. Replace `Runner.run_route_proof!(host)` with `Runner.run_full_proof!(host)` (D-30).
4. Replace hardcoded five-step tuple with derived assertion (D-32):

```elixir
assert proof.steps == Runner.expected_steps(host)
```

Do **not** assert `:overlay_smoke`. Do **not** use subset/`in` assertions. Do **not** call `run_route_proof!/1`. Do **not** reorder overlay atoms to match requirement prose (route → runtime → handoff) — execution order follows sorted filenames (handoff → route → runtime).
  </action>
  <acceptance_criteria>
    - `rg -n '@moduletag :host_proof' test/scoria/host_app_consumer_proof_test.exs` returns match.
    - `rg -n 'run_full_proof!' test/scoria/host_app_consumer_proof_test.exs` returns match.
    - `rg -n 'run_route_proof!' test/scoria/host_app_consumer_proof_test.exs` returns zero matches.
    - `rg -n 'expected_steps' test/scoria/host_app_consumer_proof_test.exs` returns match.
    - `rg -n ':overlay_smoke' test/scoria/host_app_consumer_proof_test.exs` returns zero matches.
    - `MIX_ENV=test mix test test/scoria/host_app_consumer_proof_test.exs` exits 0 (Postgres required; expect ~60–90s warm full overlay).
    - `MIX_ENV=test mix test --only host_proof` exits 0 and runs consumer test only.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/host_app_consumer_proof_test.exs</automated>
  </verify>
  <done>Merge-blocking consumer proof exercises full overlay depth on tarball dep with derived seven-step contract.</done>
</task>

</tasks>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-79-01 | False confidence: route-only proof marked complete while runtime/handoff untested on tarball | High | Switch to `run_full_proof!/1`; assert all three overlay step atoms via derived `expected_steps/1` |
| T-79-02 | Brittle hardcoded step tuple drifts when overlay priv files change | Medium | Derive overlay atoms from `host.overlay_tests` SSOT (sorted basenames) |
| T-79-03 | Asserting `:overlay_smoke` breaks contract vs `smoke_pair!/1` flat-map design | Medium | Explicit rejection in acceptance criteria; only per-file atoms in assertion |
| T-79-04 | CI timeout regression from full overlay (~5–10s delta) | Low | Preserve 180_000 module tag; D-37 escalation only after CI >120s twice |
| T-79-05 | Premature HEX-CONSUMER-01 Complete in ledger | Medium | Defer REQUIREMENTS reconciliation to 79-03 ceremony (D-45) |

**Blockers:** T-79-01 must be mitigated in this plan — no partial overlay assertion accepted.
</threat_model>

<verification>
After all tasks:

```bash
MIX_ENV=test mix compile --warnings-as-errors
MIX_ENV=test mix test test/scoria/host_app_consumer_proof_test.exs
MIX_ENV=test mix test --only host_proof
MIX_ENV=test mix test test/scoria/verification_lanes_test.exs
```

Record consumer test wall time in commit message or plan summary; if >120s locally warm, note for D-37 watch in 79-03.
</verification>

<success_criteria>
- Consumer test proves deps.get → scoria.install → ecto.create → ecto.migrate → handoff → route → runtime overlay smokes on tarball dep.
- Step assertion derives from `host.overlay_tests` without hardcoded seven-tuple.
- `mix test --only host_proof` targets consumer module for fast local iteration.
- `run_route_proof!/1` still compiles and remains callable for debug (not used by consumer test).
</success_criteria>
