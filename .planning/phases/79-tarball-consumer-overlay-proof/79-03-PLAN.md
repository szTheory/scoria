---
phase: 79-tarball-consumer-overlay-proof
plan: 03
type: execute
wave: 3
depends_on: [79-02]
files_modified:
  - .github/workflows/ci-verify.yml
  - README.md
  - docs/operator_verification.md
  - .planning/phases/79-tarball-consumer-overlay-proof/79-VERIFICATION.md
  - .planning/phases/78-hex-consumer-contract-foundation/78-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/79-tarball-consumer-overlay-proof/79-VALIDATION.md
autonomous: true
requirements: [HEX-CONSUMER-01]
must_haves:
  truths:
    - "ci-verify.yml uploads tmp/scoria-host-proof-last-failure/ on job failure only, retention 7 days, if-no-files-found ignore (D-42)."
    - "Artifact path scoped — no _build or hex cache upload."
    - "79-VERIFICATION.md written with status passed after all gates green (D-45)."
    - "REQUIREMENTS.md reconciled: HEX-CONSUMER-01 checkbox and traceability Complete with full overlay evidence narrative (D-45)."
    - "ROADMAP.md Phase 79 marked Complete with date; STATE.md points to Phase 80 (D-45)."
    - "DOCS-HEX-01 remains NOT milestone Complete — prose sweep deferred to Phase 82 (D-46)."
    - "Optional README Verification one-liner under ## Verification after adoption commands (D-47)."
    - "Optional 78-VERIFICATION.md retro note: milestone Complete deferred to 79 (D-48)."
    - "VerificationLanes.closeout_order/0 unchanged — no new closeout lane."
  artifacts:
    - path: ".github/workflows/ci-verify.yml"
      provides: "Conditional failure artifact upload for host proof snapshot"
      contains: "upload-artifact"
    - path: ".planning/phases/79-tarball-consumer-overlay-proof/79-VERIFICATION.md"
      provides: "Phase 79 passed verification ledger"
      contains: "status: passed"
    - path: ".planning/REQUIREMENTS.md"
      provides: "HEX-CONSUMER-01 traceability reconciled after full overlay proof"
      contains: "HEX-CONSUMER-01"
    - path: "README.md"
      provides: "Optional tarball consumer Verification one-liner"
      contains: "mix hex.build"
  key_links:
    - from: ".github/workflows/ci-verify.yml"
      to: "tmp/scoria-host-proof-last-failure/"
      via: "upload-artifact path on failure()"
      pattern: "scoria-host-proof-last-failure"
    - from: ".planning/phases/79-tarball-consumer-overlay-proof/79-VERIFICATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "D-45 ceremony bundle same PR tail"
      pattern: "HEX-CONSUMER-01"
---

<objective>
Close Phase 79 with CI failure artifact wiring, milestone verification ceremony, and optional maintainer doc polish.

Purpose: Complete HEX-CONSUMER-01 at milestone level with auditable ledger reconciliation; give CI operators snapshot artifacts on adoption failures.
Output: Green `mix test.adoption` evidence, `79-VERIFICATION.md`, updated planning ledgers, conditional GHA artifact upload.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/79-tarball-consumer-overlay-proof/79-CONTEXT.md
@.planning/phases/79-tarball-consumer-overlay-proof/79-RESEARCH.md
@.planning/phases/79-tarball-consumer-overlay-proof/79-VALIDATION.md
@.planning/phases/78-hex-consumer-contract-foundation/78-VERIFICATION.md
@.github/workflows/ci-verify.yml
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
@docs/operator_verification.md

Depends on 79-01 and 79-02. Phase gate is green `mix test.adoption` with full overlay consumer proof. Do not modify `adoption_surface_test`, `ci_policy_contract_test`, or mark DOCS-HEX-01 Complete. Do not widen `VerificationLanes.closeout_order/0`.
</context>

<tasks>

<task type="auto">
  <name>Task 79-03-01: Add conditional CI failure artifact upload in ci-verify.yml</name>
  <requirement_ids>HEX-CONSUMER-01</requirement_ids>
  <files>.github/workflows/ci-verify.yml</files>
  <read_first>
    .github/workflows/ci-verify.yml
    .planning/phases/79-tarball-consumer-overlay-proof/79-CONTEXT.md
    .planning/phases/79-tarball-consumer-overlay-proof/79-RESEARCH.md
  </read_first>
  <action>
In `.github/workflows/ci-verify.yml` `test` job, add step **immediately after** `Run adoption closure lane` (after line 120 `mix test.adoption`):

```yaml
      - name: Upload host proof failure snapshot
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: scoria-host-proof-last-failure
          path: tmp/scoria-host-proof-last-failure/
          retention-days: 7
          if-no-files-found: ignore
```

Constraints (D-42):
- `if: failure()` only — not on green runs.
- Path scoped to `tmp/scoria-host-proof-last-failure/` — do **not** upload `_build`, `deps`, or `tmp/scoria-hex-consumer/`.
- First `upload-artifact` in repo workflows.

Do not change existing `SCORIA_HEX_UNPACK_ROOT: tmp/scoria-release-preview` job env (line 82).
  </action>
  <acceptance_criteria>
    - `rg -n 'upload-artifact' .github/workflows/ci-verify.yml` returns match with `scoria-host-proof-last-failure`.
    - `rg -n 'if: failure\\(\\)' .github/workflows/ci-verify.yml` appears on upload step.
    - `rg -n 'retention-days: 7' .github/workflows/ci-verify.yml` returns match.
    - `rg -n 'if-no-files-found: ignore' .github/workflows/ci-verify.yml` returns match.
    - YAML step appears after `Run adoption closure lane` and before `Run runtime-to-handoff proof lane`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'upload-artifact|scoria-host-proof-last-failure' .github/workflows/ci-verify.yml</automated>
  </verify>
  <done>CI uploads host proof failure snapshot when adoption lane fails.</done>
</task>

<task type="auto">
  <name>Task 79-03-02: Run phase gate and write 79-VERIFICATION.md</name>
  <requirement_ids>HEX-CONSUMER-01</requirement_ids>
  <files>.planning/phases/79-tarball-consumer-overlay-proof/79-VERIFICATION.md</files>
  <read_first>
    .planning/phases/78-hex-consumer-contract-foundation/78-VERIFICATION.md
    .planning/phases/79-tarball-consumer-overlay-proof/79-VALIDATION.md
    .planning/phases/79-tarball-consumer-overlay-proof/79-CONTEXT.md
    test/scoria/host_app_consumer_proof_test.exs
  </read_first>
  <action>
1. Run phase gate commands (all must exit 0 before writing verification):

```bash
MIX_ENV=test mix test test/scoria/host_app_consumer_proof_test.exs
MIX_ENV=test mix test --only host_proof
MIX_ENV=test mix test.adoption
MIX_ENV=test mix test test/scoria/verification_lanes_test.exs
MIX_ENV=test mix test test/mix/tasks/test.adoption_test.exs
MIX_ENV=dev mix scoria.release_preview && SCORIA_HEX_UNPACK_ROOT=tmp/scoria-release-preview MIX_ENV=test mix test.adoption
```

2. Record consumer test wall time. If CI or local warm run exceeds **120s**, document in verification and evaluate D-37 escalation (240_000 module tag + update `79-VALIDATION.md`) — do **not** escalate without two consecutive CI >120s evidence.

3. Create `.planning/phases/79-tarball-consumer-overlay-proof/79-VERIFICATION.md` modeled on `78-VERIFICATION.md` with frontmatter:

```yaml
---
status: passed
phase: 79-tarball-consumer-overlay-proof
verified: <ISO8601 UTC>
requirements:
  - HEX-CONSUMER-01
---
```

4. Include evidence table: `run_full_proof!/1`, seven-step derived assertion (`handoff → route → runtime` order), `@moduletag :host_proof`, triage raise fields, `SCORIA_PRESERVE_HOST`, snapshot path, CI artifact step.
5. Explicitly state HEX-CONSUMER-01 **milestone Complete** (full overlay on tarball); DOCS-HEX-01 remains partial until Phase 82.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/79-tarball-consumer-overlay-proof/79-VERIFICATION.md` succeeds.
    - `rg -n 'status: passed' .planning/phases/79-tarball-consumer-overlay-proof/79-VERIFICATION.md` returns match.
    - `rg -n 'run_full_proof!' .planning/phases/79-tarball-consumer-overlay-proof/79-VERIFICATION.md` returns match.
    - `rg -n 'host_handoff_smoke_test.*host_route_smoke_test.*host_runtime_smoke_test' .planning/phases/79-tarball-consumer-overlay-proof/79-VERIFICATION.md` returns match (ordered overlay evidence).
    - All phase gate commands listed in action exit 0 at verification write time.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test.adoption</automated>
  </verify>
  <done>Executable verification ledger proves full overlay tarball consumer proof green.</done>
</task>

<task type="auto">
  <name>Task 79-03-03: Milestone closeout ceremony — REQUIREMENTS, ROADMAP, STATE reconciliation</name>
  <requirement_ids>HEX-CONSUMER-01</requirement_ids>
  <files>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/phases/78-hex-consumer-contract-foundation/78-VERIFICATION.md
  </files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/phases/79-tarball-consumer-overlay-proof/79-VERIFICATION.md
    .planning/phases/79-tarball-consumer-overlay-proof/79-CONTEXT.md
  </read_first>
  <action>
Same PR tail as green 79-VERIFICATION.md (D-45):

1. **REQUIREMENTS.md:**
   - Confirm `[x]` on HEX-CONSUMER-01 checkbox with prose stating full overlay chain (route + HOST-01 runtime + HOST-02 handoff) on tarball dep — fix any route-only narrative drift from Phase 78 summaries.
   - Traceability row: `HEX-CONSUMER-01 | 78, 79 | Complete`.
   - **Do not** mark DOCS-HEX-01 checkbox Complete or change its traceability to Complete (D-46) — remains `[x]` from Phase 78 SSOT only; prose sweep Phase 82.

2. **ROADMAP.md:**
   - Phase 79 Plans: `3/3 plans complete`
   - Phase 79 Status: `Complete` with date `2026-05-29` (or execution date)
   - Phase map row 79 → Complete

3. **STATE.md:**
   - `completed_phases: 2` (78 + 79), `completed_plans: 6`, update `percent`
   - Current focus → Phase 80 upgrade smoke
   - Add Phase 79 complete evidence pointer to `79-VERIFICATION.md`
   - Remove deferred "Full overlay tarball consumer proof" from Deferred Items (delivered)

4. **78-VERIFICATION.md (optional D-48):** Add short retro note at top or in HEX-CONSUMER-01 row: "Milestone Complete deferred to Phase 79 full overlay — see 79-VERIFICATION.md."
  </action>
  <acceptance_criteria>
    - `rg -n 'HEX-CONSUMER-01.*Complete' .planning/REQUIREMENTS.md` returns traceability match with `78, 79`.
    - `rg -n 'HOST-01|HOST-02|handoff' .planning/REQUIREMENTS.md` in HEX-CONSUMER-01 prose returns match (full overlay narrative).
    - `rg -n 'Phase 80' .planning/STATE.md` returns current focus pointer.
    - `rg -n '79.*Complete|3/3 plans complete' .planning/ROADMAP.md` returns Phase 79 completion.
    - `rg -n 'DOCS-HEX-01.*82.*Complete' .planning/REQUIREMENTS.md` traceability still shows Phase 82 — not flipped to milestone-only Complete in 79.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/verification_lanes_test.exs test/mix/tasks/test.adoption_test.exs</automated>
  </verify>
  <done>Planning ledgers reconcile HEX-CONSUMER-01 Complete with full overlay evidence; DOCS-HEX-01 prose sweep remains Phase 82.</done>
</task>

<task type="auto">
  <name>Task 79-03-04: Optional README and operator debug blurb (D-47, D-48 discretion)</name>
  <requirement_ids>HEX-CONSUMER-01</requirement_ids>
  <files>README.md, docs/operator_verification.md</files>
  <read_first>
    README.md
    docs/operator_verification.md
    .planning/phases/79-tarball-consumer-overlay-proof/79-CONTEXT.md
  </read_first>
  <action>
Optional polish — not a phase gate; skip if trivial time pressure, but prefer inclusion:

1. **README.md** under `## Verification` (after adoption command block ~line 199), insert single sentence (D-47):

> Adoption closeout in CI exercises Scoria via a `mix hex.build --unpack` tarball (`{:scoria, path: unpack_root}`), not a monorepo root `path:` — see `Scoria.HexConsumerContract` and `SCORIA_HEX_UNPACK_ROOT` in maintainer CI.

No `adoption_surface_test` pin (Phase 82).

2. **docs/operator_verification.md** — add one paragraph near CI gate / adoption triage section covering:
   - Tarball consumer proof in `mix test.adoption` (`run_full_proof!/1`)
   - Local iteration: `mix test --only host_proof`
   - Debug: `SCORIA_PRESERVE_HOST=1`, failure snapshot at `tmp/scoria-host-proof-last-failure/`, CI artifact `scoria-host-proof-last-failure`

Do not add `ci_policy_contract_test` or `adoption_surface_test` anchors (D-46).
  </action>
  <acceptance_criteria>
    - If README updated: `rg -n 'mix hex.build' README.md` in Verification section returns tarball sentence.
    - If operator doc updated: `rg -n 'SCORIA_PRESERVE_HOST|scoria-host-proof-last-failure|host_proof' docs/operator_verification.md` returns at least two matches.
    - Task may complete with "skipped optional docs" noted in 79-VERIFICATION.md if intentionally deferred — but README one-liner strongly preferred (D-47).
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'host_proof|SCORIA_PRESERVE_HOST|scoria-host-proof-last-failure' README.md docs/operator_verification.md || true</automated>
  </verify>
  <done>Maintainers have minimal prose pointers for tarball proof and failure triage without Phase 82 drift pins.</done>
</task>

</tasks>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-79-11 | Premature HEX-CONSUMER-01 Complete without green full overlay adoption | High | Write 79-VERIFICATION only after `mix test.adoption` green; ceremony bundle in same PR (D-45) |
| T-79-12 | CI artifact uploads secrets from generated host snapshot | Medium | Scoped path only; 7-day retention; test Cloak key is fixture-only; no _build upload |
| T-79-13 | Accidentally marking DOCS-HEX-01 Complete in 79 | Medium | Explicit acceptance criteria: traceability stays Phase 82 for prose Complete (D-46) |
| T-79-14 | closeout_order/0 widened | Low | verification_lanes_test in gate commands |
| T-79-15 | Empty artifact fails CI on unrelated job failures | Low | `if-no-files-found: ignore` on upload step |

**Blockers:** T-79-11 and T-79-13 must be mitigated in closeout task.
</threat_model>

<verification>
Phase gate (all exit 0):

```bash
MIX_ENV=test mix test test/scoria/host_app_consumer_proof_test.exs
MIX_ENV=test mix test --only host_proof
MIX_ENV=test mix test.adoption
MIX_ENV=test mix test test/scoria/verification_lanes_test.exs test/mix/tasks/test.adoption_test.exs
MIX_ENV=dev mix scoria.release_preview && SCORIA_HEX_UNPACK_ROOT=tmp/scoria-release-preview MIX_ENV=test mix test.adoption
```

Ledger checks:

```bash
test -f .planning/phases/79-tarball-consumer-overlay-proof/79-VERIFICATION.md
rg -n 'upload-artifact' .github/workflows/ci-verify.yml
rg -n 'HEX-CONSUMER-01' .planning/REQUIREMENTS.md
```

Manual (post-merge): confirm first GHA failure on adoption lane uploads `scoria-host-proof-last-failure` artifact when snapshot exists.
</verification>

<success_criteria>
- `mix test.adoption` green with full overlay tarball consumer proof.
- CI uploads failure snapshot artifact on adoption failure.
- HEX-CONSUMER-01 milestone Complete in planning ledgers with auditable 79-VERIFICATION evidence.
- DOCS-HEX-01 prose sweep and drift pins remain Phase 82 scope.
- Optional maintainer README/operator blurb documents tarball proof and triage paths.
</success_criteria>
