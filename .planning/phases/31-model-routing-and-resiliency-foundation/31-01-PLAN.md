---
phase: 31-model-routing-and-resiliency-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/observe/circuit_breaker.ex
  - test/scoria/observe/circuit_breaker_test.exs
  - lib/scoria/observe/circuit_breaker/manager.ex
  - test/scoria/observe/circuit_breaker_manager_test.exs
  - lib/scoria/application.ex
autonomous: true
requirements:
  - ORCH-02
must_haves:
  truths:
    - "System tracks failure counts and state per model in memory"
    - "Concurrent requests can read and write state without bottlenecks"
    - "Open circuits automatically transition to half-open after a timeout"
  artifacts:
    - path: "lib/scoria/observe/circuit_breaker.ex"
      provides: "ETS-backed circuit breaker state access"
    - path: "lib/scoria/observe/circuit_breaker/manager.ex"
      provides: "Periodic state sweeps for half-open transitions"
  key_links:
    - from: "lib/scoria/application.ex"
      to: "lib/scoria/observe/circuit_breaker/manager.ex"
      via: "Supervision tree child"
---

<objective>
Build the zero-dependency, ETS-backed circuit breaker state engine.
Purpose: Allow high-concurrency model routing decisions without GenServer bottlenecks.
Output: `Scoria.Observe.CircuitBreaker` and its sweeping `Manager`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-model-routing-and-resiliency-foundation/31-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ETS Circuit Breaker State API</name>
  <files>lib/scoria/observe/circuit_breaker.ex, test/scoria/observe/circuit_breaker_test.exs</files>
  <behavior>
    - Test 1: `init_table/0` creates named public ETS table `:scoria_circuit_breakers`
    - Test 2: `open?/1` returns false for unknown or `:closed` models
    - Test 3: `record_failure/2` increments failure count atomically. When threshold reached, transitions status to `:open` and records timeout
    - Test 4: `open?/1` returns true when status is `:open` and timeout has not expired
    - Test 5: `record_success/1` resets failure count to 0 and status to `:closed`
  </behavior>
  <action>
    Implement ETS-backed circuit breaker using `:ets.update_counter/4` for atomic increments.
    Data format in ETS: `{model_id, status, failure_count, last_failure_at}`.
    Default status: `:closed`.
    
    Analog reference: `lib/scoria/sre/breaker_registry.ex` (uses `:ets.new(@open_table, [:named_table, :public, :set, read_concurrency: true])` for table setup). Implement `init_table/0` using this exact `:ets.new` pattern.
    
    Provide `init_table/0`, `open?(model_id)`, `record_failure(model_id, opts \\ [])`, `record_success(model_id)`, and `sweep_half_open/0` (finds `:open` circuits past their timeout and transitions them to `:half_open` with count 0).
    
    MUST implement locked decision: Read threshold and timeout defaults from `Application.get_env(:scoria, :circuit_breaker_opts)` with fallbacks of 5 failures and a 30s timeout (30_000ms) per CONTEXT.md. Apply these in `record_failure` if thresholds are exceeded.
    Use `System.system_time(:millisecond)` for timeouts.
  </action>
  <verify>
    <automated>mix test test/scoria/observe/circuit_breaker_test.exs</automated>
  </verify>
  <done>Tests pass and ETS table handles state transitions using config defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Circuit Breaker Sweep Manager</name>
  <files>lib/scoria/observe/circuit_breaker/manager.ex, test/scoria/observe/circuit_breaker_manager_test.exs</files>
  <behavior>
    - Test 1: GenServer starts and initializes the ETS table via `CircuitBreaker.init_table/0`
    - Test 2: Sends itself a `:sweep` message periodically
    - Test 3: On `:sweep`, calls `CircuitBreaker.sweep_half_open/0`
  </behavior>
  <action>
    Implement a simple GenServer that manages the sweeping of the circuit breaker table.
    In `init/1`, call `Scoria.Observe.CircuitBreaker.init_table/0` and schedule the first sweep.
    
    Analog reference: `lib/scoria/observe/buffer.ex`. Emulate the `Process.send_after/3` scheduling pattern seen in `schedule_flush` and handling the recurring timer in `handle_info`.
    
    MUST implement locked decision: Read `:sweep_interval` from `Application.get_env(:scoria, :circuit_breaker_opts)` falling back to 5000ms.
    Handle the `:sweep` info message to perform the sweep using `CircuitBreaker.sweep_half_open/0` and reschedule itself.
  </action>
  <verify>
    <automated>mix test test/scoria/observe/circuit_breaker_manager_test.exs</automated>
  </verify>
  <done>Manager correctly sweeps the ETS table on a configured timer.</done>
</task>

<task type="auto">
  <name>Task 3: Application Supervision</name>
  <files>lib/scoria/application.ex</files>
  <action>
    Add `Scoria.Observe.CircuitBreaker.Manager` to the application supervision tree in `Scoria.Application.start/2`.
    Ensure it starts after core dependencies but before anything that might make outbound HTTP calls.
  </action>
  <verify>
    <automated>mix test</automated>
  </verify>
  <done>Application boots successfully with the manager running.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| System -> ETS | Lock-free concurrent writes to ETS table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Denial of Service | `CircuitBreaker` | mitigate | Prevent atom exhaustion by ensuring `model_id` in ETS is a String, not dynamically generated atoms. Use standard pattern matching. |
| T-31-02 | Tampering | `CircuitBreaker` | mitigate | ETS table is `:public` for performance, but only modified via controlled API functions (`record_failure`, `record_success`) to prevent malformed data. |
</threat_model>

<verification>
mix test test/scoria/observe/circuit_breaker_test.exs test/scoria/observe/circuit_breaker_manager_test.exs
</verification>

<success_criteria>
ETS table correctly tracks failure counts and transitions state atomically. Manager sweeps open circuits to half-open safely using configuration fallbacks.
</success_criteria>

<output>
After completion, create `.planning/phases/31-model-routing-and-resiliency-foundation/31-01-SUMMARY.md`
</output>