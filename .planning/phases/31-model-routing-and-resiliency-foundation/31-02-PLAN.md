---
phase: 31-model-routing-and-resiliency-foundation
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - lib/scoria/req/steps/circuit_breaker.ex
  - test/scoria/req/steps/circuit_breaker_test.exs
  - lib/scoria/req/steps/resiliency.ex
  - test/scoria/req/steps/resiliency_test.exs
  - lib/scoria/req/steps.ex
  - test/scoria/req/steps_test.exs
autonomous: true
requirements:
  - ORCH-03
must_haves:
  truths:
    - "Req requests fail instantly if the target model's circuit is open"
    - "Req responses track HTTP outcomes (429/5xx vs 2xx) to update ETS state"
    - "Retries happen under the hood without over-counting failures"
  artifacts:
    - path: "lib/scoria/req/steps/circuit_breaker.ex"
      provides: "Halts request if circuit is open"
    - path: "lib/scoria/req/steps/resiliency.ex"
      provides: "Inspects responses and errors to record success or failure"
    - path: "lib/scoria/req/steps.ex"
      provides: "Registers options and attaches steps to a Req pipeline"
  key_links:
    - from: "lib/scoria/req/steps/circuit_breaker.ex"
      to: "lib/scoria/observe/circuit_breaker.ex"
      via: "Calls open?(model_id)"
---

<objective>
Implement the `Req` middleware steps that bridge HTTP execution with the ETS circuit breaker engine.
Purpose: Automatically short-circuit requests to unhealthy models and track failure states without modifying business logic.
Output: `Scoria.Req.Steps` and its corresponding request/response step implementations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-model-routing-and-resiliency-foundation/31-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Circuit Breaker Request Step</name>
  <files>lib/scoria/req/steps/circuit_breaker.ex, test/scoria/req/steps/circuit_breaker_test.exs</files>
  <behavior>
    - Test 1: Given a `request` where `options[:model_id]` has an `:open` circuit, halts and returns `{:error, %RuntimeError{message: "Circuit breaker is open"}}`
    - Test 2: Given a `request` with a `:closed` circuit, returns the `request` unmodified
  </behavior>
  <action>
    Implement `Scoria.Req.Steps.CircuitBreaker.run(%Req.Request{} = request)`.
    Extract the `:model_id` from `request.options`.
    Call `Scoria.Observe.CircuitBreaker.open?(model_id)`.
    
    Reference RESEARCH.md patterns: As per the "Halting a Req Pipeline" section in 31-RESEARCH.md, explicitly use `Req.Request.halt(request, error_struct)` to stop the pipeline when the circuit is open. Returning an error struct (e.g. `RuntimeError{message: "Circuit breaker open"}`) properly transitions the pipeline away from outbound execution.
    Otherwise, return the request unmodified.
  </action>
  <verify>
    <automated>mix test test/scoria/req/steps/circuit_breaker_test.exs</automated>
  </verify>
  <done>Request step correctly halts open circuits using Req.Request.halt/2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resiliency Response and Error Steps</name>
  <files>lib/scoria/req/steps/resiliency.ex, test/scoria/req/steps/resiliency_test.exs</files>
  <behavior>
    - Test 1: On 2xx response, calls `record_success/1` for the `:model_id`
    - Test 2: On 429 or 5xx response, calls `record_failure/2`
    - Test 3: On error (e.g. timeout exception), calls `record_failure/2`
  </behavior>
  <action>
    Implement `Scoria.Req.Steps.Resiliency`.
    Provide `handle_response({request, response})`: if `response.status` is 429 or 5xx, call `Scoria.Observe.CircuitBreaker.record_failure(model_id)`. If 2xx, call `record_success`. Return `{request, response}`.
    Provide `handle_error({request, exception})`: call `record_failure` and return `{request, exception}`.
  </action>
  <verify>
    <automated>mix test test/scoria/req/steps/resiliency_test.exs</automated>
  </verify>
  <done>Resiliency steps correctly record successes and failures to ETS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Steps Wrapper Module</name>
  <files>lib/scoria/req/steps.ex, test/scoria/req/steps_test.exs</files>
  <behavior>
    - Test 1: `attach/2` registers `:model_id` option
    - Test 2: `attach/2` appends the `CircuitBreaker` and `Resiliency` steps
    - Test 3: Integration test asserting a full `Req` pipeline correctly fails after threshold when returning simulated 500s via `Req.Test.stub`
  </behavior>
  <action>
    Implement `Scoria.Req.Steps.attach(req, opts \\ [])`.
    
    Reference RESEARCH.md patterns: Apply the precise `Req.Request` step appendage workflow:
    - Use `Req.Request.register_options/2` for `[:model_id]`.
    - Use `Req.Request.merge_options/2` to mix in `opts`.
    - Use `Req.Request.append_request_steps/2` for `Scoria.Req.Steps.CircuitBreaker.run/1`.
    - Use `Req.Request.append_response_steps/2` and `append_error_steps/2` for `Scoria.Req.Steps.Resiliency`.
    
    Write an integration test using `Req.Test` to verify the built-in `:transient` retries combine correctly with our steps (e.g. 3 retries shouldn't trip a 5-failure threshold immediately, proving step ordering matches assumptions in RESEARCH.md).
  </action>
  <verify>
    <automated>mix test test/scoria/req/steps_test.exs</automated>
  </verify>
  <done>Req integration module assembles the pipeline correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> External Model | Outbound HTTP requests intercepted by Req middleware |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Denial of Service | `Req.Steps.Resiliency` | mitigate | Handle external 429s/500s efficiently to prevent runaway backoffs or memory leaks. `Req` built-in transient retries handle this with jitter. |
</threat_model>

<verification>
mix test test/scoria/req/steps/circuit_breaker_test.exs test/scoria/req/steps/resiliency_test.exs test/scoria/req/steps_test.exs
</verification>

<success_criteria>
Req pipelines attached with our steps automatically short-circuit on open models and appropriately record success/failures.
</success_criteria>

<output>
After completion, create `.planning/phases/31-model-routing-and-resiliency-foundation/31-02-SUMMARY.md`
</output>