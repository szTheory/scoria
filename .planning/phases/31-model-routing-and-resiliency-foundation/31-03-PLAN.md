---
phase: 31-model-routing-and-resiliency-foundation
plan: 03
type: execute
wave: 3
depends_on: ["31-02"]
files_modified:
  - lib/scoria/req/steps.ex
  - test/scoria/req/steps_test.exs
  - lib/scoria/compaction/summarize_worker.ex
  - lib/scoria/eval/judge_runner.ex
autonomous: true
requirements:
  - ORCH-03
must_haves:
  truths:
    - "Summarize worker outbound calls are protected by the circuit breaker"
    - "Judge runner outbound calls are protected by the circuit breaker"
  artifacts:
    - path: "lib/scoria/compaction/summarize_worker.ex"
      provides: "Passes circuit breaker options to ReqLLM"
    - path: "lib/scoria/eval/judge_runner.ex"
      provides: "Passes circuit breaker options to ReqLLM"
  key_links:
    - from: "lib/scoria/compaction/summarize_worker.ex"
      to: "lib/scoria/req/steps.ex"
      via: "Calls req_options(model_id)"
---

<objective>
Wire the newly created Req middleware steps into actual outbound LLM requests.
Purpose: Apply the circuit breaking and resiliency tracking to real application boundaries (`SummarizeWorker` and `JudgeRunner`).
Output: Modified workers injecting the correct `req_options`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-model-routing-and-resiliency-foundation/31-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Req Options Helper</name>
  <files>lib/scoria/req/steps.ex, test/scoria/req/steps_test.exs</files>
  <behavior>
    - Test 1: `req_options/1` returns a list of `{Req.Request, atom, [args]}` tuples
    - Test 2: The options list includes registration for `:model_id`, setting the `:model_id`, and appending the three custom steps
  </behavior>
  <action>
    Modify `Scoria.Req.Steps` to expose `req_options(model_id)`.
    Since `ReqLLM` allows passing `req_options` directly, this helper should return the declarative format expected by `ReqLLM`/`Req.new`:
    `[{Req.Request, :register_options, [[:model_id]]}, {Req.Request, :merge_options, [[model_id: model_id]]}, {Req.Request, :append_request_steps, [scoria_circuit_breaker: &Scoria.Req.Steps.CircuitBreaker.run/1]}, ...]`
    Ensure `append_response_steps` and `append_error_steps` for `Resiliency` are included.
  </action>
  <verify>
    <automated>mix test test/scoria/req/steps_test.exs</automated>
  </verify>
  <done>Helper outputs correct declarative pipeline structure.</done>
</task>

<task type="auto">
  <name>Task 2: Worker Integration</name>
  <files>lib/scoria/compaction/summarize_worker.ex, lib/scoria/eval/judge_runner.ex</files>
  <action>
    Update `generate_summary!/1` in `SummarizeWorker` to inject `req_options: Scoria.Req.Steps.req_options(summary_model())` into the `req_llm_module.generate_text` call options.
    Update `run_eval!/3` in `JudgeRunner` to do the same, extracting the model ID from `attrs[:model] || "default_model"`.
  </action>
  <verify>
    <automated>mix test</automated>
  </verify>
  <done>Production callers pass the circuit breaker options through to ReqLLM.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application -> External API | Bound requests passing through ReqLLM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Tampering | `JudgeRunner` | mitigate | `ReqLLM` calls already properly escape injection. Circuit breaking adds resiliency without exposing new attack surface. |
</threat_model>

<verification>
mix test
</verification>

<success_criteria>
Outbound LLM calls dynamically attach the circuit breaker and resiliency steps based on their target model ID.
</success_criteria>

<output>
After completion, create `.planning/phases/31-model-routing-and-resiliency-foundation/31-03-SUMMARY.md`
</output>