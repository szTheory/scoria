---
phase: 48-host-app-install-contract-and-consumer-proof
plan: 04
type: execute
wave: 4
depends_on:
  - "48-01"
  - "48-02"
  - "48-03"
files_modified:
  - lib/mix/tasks/test.adoption.ex
  - test/mix/tasks/test.adoption_test.exs
  - test/scoria/adoption_surface_test.exs
  - docs/operator_verification.md
autonomous: true
requirements:
  - INST-02
  - PROOF-01
  - PROOF-02
user_setup: []
must_haves:
  truths:
    - "`mix test.adoption` remains the single canonical default-lane verifier."
    - "The canonical verifier now includes the generated fresh-host proof alongside existing installer, migration, and runtime checks."
    - "Default-lane guidance keeps Tailwind, semantic fast path, and knowledge setup explicit optional lanes instead of hidden prerequisites."
  artifacts:
    - path: "lib/mix/tasks/test.adoption.ex"
      provides: "Canonical default-lane verifier file list"
      exports: ["adoption_test_files/0", "run/1"]
    - path: "test/mix/tasks/test.adoption_test.exs"
      provides: "Exact file-list and discoverability guard for the adoption lane"
    - path: "test/scoria/adoption_surface_test.exs"
      provides: "Source-truth assertions for default-vs-optional lane messaging"
    - path: "docs/operator_verification.md"
      provides: "Adopter-facing explanation of the canonical default-lane verification command"
  key_links:
    - from: "lib/mix/tasks/test.adoption.ex"
      to: "test/scoria/host_app_consumer_proof_test.exs"
      via: "bounded default-lane file list"
      pattern: "host_app_consumer_proof_test.exs"
    - from: "docs/operator_verification.md"
      to: "test/scoria/adoption_surface_test.exs"
      via: "canonical command and optional-lane wording assertions"
      pattern: "mix test.adoption|Tailwind|Optional knowledge lane"
---

<objective>
Finish the phase by making `mix test.adoption` the one boring default-lane proof that now covers fresh-host install and runtime truth.

Purpose: Phase 48 only closes when adopters and maintainers still get one canonical command, and that command proves the default lane without accidentally absorbing optional Tailwind, semantic, or knowledge prerequisites.
Output: updated adoption task file list, task-contract tests, and narrow operator-verification/source-truth guards around the default-lane vocabulary.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/48-host-app-install-contract-and-consumer-proof/48-CONTEXT.md
@.planning/phases/48-host-app-install-contract-and-consumer-proof/48-RESEARCH.md
@.planning/phases/48-host-app-install-contract-and-consumer-proof/48-VALIDATION.md
@.planning/phases/48-host-app-install-contract-and-consumer-proof/48-02-PLAN.md
@.planning/phases/48-host-app-install-contract-and-consumer-proof/48-03-PLAN.md
@lib/mix/tasks/test.adoption.ex
@test/mix/tasks/test.adoption_test.exs
@test/scoria/adoption_surface_test.exs
@docs/operator_verification.md

<interfaces>
From lib/mix/tasks/test.adoption.ex:
```elixir
@adoption_test_files [...]
def adoption_test_files, do: @adoption_test_files
def run(args)
```

Canonical lane naming that must remain stable:
```text
mix test.adoption
SCORIA_DB_PORT="${SCORIA_DB_PORT:-5432}" SCORIA_DB_PASSWORD="${SCORIA_DB_PASSWORD:-postgres}" MIX_ENV=test mix test.semantic_fast_path
mix scoria.test.knowledge
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update the adoption-lane contract test before wiring in the generated-host proof</name>
  <files>test/mix/tasks/test.adoption_test.exs</files>
  <read_first>
    <file>test/mix/tasks/test.adoption_test.exs</file>
    <file>lib/mix/tasks/test.adoption.ex</file>
    <file>test/scoria/host_app_consumer_proof_test.exs</file>
    <file>.planning/phases/48-host-app-install-contract-and-consumer-proof/48-VALIDATION.md</file>
  </read_first>
  <behavior>
    - Test 1: `Mix.Tasks.Scoria.Test.Adoption.adoption_test_files/0` now includes `test/scoria/host_app_consumer_proof_test.exs`.
    - Test 2: `mix test.adoption` remains discoverable through both `scoria.test.adoption` and `test.adoption` task names per D-08.
    - Test 3: the adoption-lane file list still excludes semantic and knowledge-lane tests from the default verifier per D-11 and D-14.
  </behavior>
  <action>Update `test/mix/tasks/test.adoption_test.exs` first so the exact expected file list includes `test/scoria/host_app_consumer_proof_test.exs` in the default-lane bundle while continuing to omit semantic-fast-path and knowledge-lane execution files. Keep the task discoverability assertions intact for both task names because `mix test.adoption` remains the single public verifier per D-08.</action>
  <acceptance_criteria>
    <criterion>`test/mix/tasks/test.adoption_test.exs` asserts `test/scoria/host_app_consumer_proof_test.exs` is in the expected default-lane file list</criterion>
    <criterion>The test still asserts both `scoria.test.adoption` and `test.adoption` are loadable</criterion>
    <criterion>The expected file list does not add semantic-fast-path or knowledge-lane execution files</criterion>
  </acceptance_criteria>
  <verify>
    <automated>SCORIA_DB_PORT="${SCORIA_DB_PORT:-5432}" SCORIA_DB_PASSWORD="${SCORIA_DB_PASSWORD:-postgres}" MIX_ENV=test mix do clean, test test/mix/tasks/test.adoption_test.exs --trace</automated>
  </verify>
  <done>The canonical adoption-lane contract fails until the new host proof is actually wired into the public verifier.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the generated-host proof into `mix test.adoption` and document the canonical default lane</name>
  <files>lib/mix/tasks/test.adoption.ex, docs/operator_verification.md</files>
  <read_first>
    <file>lib/mix/tasks/test.adoption.ex</file>
    <file>docs/operator_verification.md</file>
    <file>test/mix/tasks/test.adoption_test.exs</file>
    <file>test/scoria/host_app_consumer_proof_test.exs</file>
  </read_first>
  <behavior>
    - Test 1: `mix test.adoption` runs the new fresh-host proof alongside the existing installer, route, migration-lane, and runtime tests per D-09.
    - Test 2: operator verification docs describe `mix test.adoption` as the canonical default-lane verifier without claiming semantic or knowledge setup is required.
    - Test 3: the semantic-fast-path and knowledge lanes remain separate named commands per D-14.
  </behavior>
  <action>Update `lib/mix/tasks/test.adoption.ex` so `@adoption_test_files` includes `test/scoria/host_app_consumer_proof_test.exs` and preserves the existing bounded default-lane bundle. Keep `mix test.adoption` as the only public default-lane verifier per D-08; do not create a second public task. Update `docs/operator_verification.md` narrowly so the Step 1/maintainer language explains that `mix test.adoption` now covers installer truth, fresh-host install/migrate/route proof, and the default runtime/operator-evidence subset, while semantic fast path and knowledge remain separate optional lanes with their own commands.</action>
  <acceptance_criteria>
    <criterion>`lib/mix/tasks/test.adoption.ex` includes `test/scoria/host_app_consumer_proof_test.exs` in `@adoption_test_files`</criterion>
    <criterion>`docs/operator_verification.md` explicitly states `mix test.adoption` is the canonical default-lane verifier</criterion>
    <criterion>`docs/operator_verification.md` keeps semantic fast path and knowledge lanes on their own commands</criterion>
  </acceptance_criteria>
  <verify>
    <automated>SCORIA_DB_PORT="${SCORIA_DB_PORT:-5432}" SCORIA_DB_PASSWORD="${SCORIA_DB_PASSWORD:-postgres}" MIX_ENV=test mix do clean, test test/mix/tasks/test.adoption_test.exs --trace && SCORIA_DB_PORT="${SCORIA_DB_PORT:-5432}" SCORIA_DB_PASSWORD="${SCORIA_DB_PASSWORD:-postgres}" MIX_ENV=test mix do clean, test.adoption</automated>
  </verify>
  <done>The public default-lane command now executes the fresh-host consumer proof without widening the lane boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Lock optional-lane boundaries in adoption surface tests</name>
  <files>test/scoria/adoption_surface_test.exs</files>
  <read_first>
    <file>test/scoria/adoption_surface_test.exs</file>
    <file>docs/operator_verification.md</file>
    <file>README.md</file>
    <file>docs/adoption_lanes.md</file>
    <file>lib/mix/tasks/scoria.install.ex</file>
  </read_first>
  <behavior>
    - Test 1: the docs/source-truth suite explicitly says Tailwind is optional for the install task and the default lane still installs cleanly.
    - Test 2: default-lane guidance names `mix test.adoption` while semantic-fast-path and knowledge commands stay separate.
    - Test 3: the default lane does not imply pgvector, retrieval, or semantic cache setup as prerequisites.
  </behavior>
  <action>Strengthen `test/scoria/adoption_surface_test.exs` so it asserts the current adoption docs and README still say Tailwind is optional, `mix test.adoption` is the canonical default-lane proof, and the optional semantic/knowledge lanes keep their own commands and prerequisite boundaries. Keep this change narrow and phase-owned: assert the phrases needed for Phase 48 truth, not a full wording rewrite across every guide. If the current docs are missing one of those exact truths, patch only the smallest relevant sentence in `docs/operator_verification.md` or the already-referenced guide files while preserving Phase 49 as the owner of full support-language reconciliation.</action>
  <acceptance_criteria>
    <criterion>`test/scoria/adoption_surface_test.exs` asserts Tailwind is optional for the install task</criterion>
    <criterion>`test/scoria/adoption_surface_test.exs` asserts `mix test.adoption`, `mix test.semantic_fast_path`, and `mix scoria.test.knowledge` each retain distinct lane roles</criterion>
    <criterion>`test/scoria/adoption_surface_test.exs` refutes wording that makes pgvector, retrieval, or semantic setup sound mandatory for the default lane</criterion>
  </acceptance_criteria>
  <verify>
    <automated>SCORIA_DB_PORT="${SCORIA_DB_PORT:-5432}" SCORIA_DB_PASSWORD="${SCORIA_DB_PASSWORD:-postgres}" MIX_ENV=test mix do clean, test test/scoria/adoption_surface_test.exs test/mix/tasks/test.adoption_test.exs --trace && SCORIA_DB_PORT="${SCORIA_DB_PORT:-5432}" SCORIA_DB_PASSWORD="${SCORIA_DB_PASSWORD:-postgres}" MIX_ENV=test mix do clean, test.adoption</automated>
  </verify>
  <done>Default-lane versus optional-lane support truth is now executable and tied to the one canonical verifier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical verifier -> maintainer/adopter trust | Users rely on one named command to know whether the default adoption lane is truly green |
| Docs/source tests -> lane expectations | The docs and adoption-surface tests define whether optional lanes are truthfully separated from the default lane |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-04-01 | Repudiation | `mix test.adoption` coverage | mitigate | Add the generated-host proof to `@adoption_test_files` and lock the exact file list in `test/mix/tasks/test.adoption_test.exs`. |
| T-48-04-02 | Spoofing | optional-lane documentation | mitigate | Assert that Tailwind, semantic fast path, and knowledge remain explicitly optional or separate lanes in `test/scoria/adoption_surface_test.exs`. |
| T-48-04-03 | Misconfiguration | default-lane support guidance | mitigate | Keep `docs/operator_verification.md` aligned to one canonical default verifier and distinct optional-lane commands. |
</threat_model>

<verification>
- `SCORIA_DB_PORT="${SCORIA_DB_PORT:-5432}" SCORIA_DB_PASSWORD="${SCORIA_DB_PASSWORD:-postgres}" MIX_ENV=test mix do clean, test test/mix/tasks/test.adoption_test.exs --trace`
- `SCORIA_DB_PORT="${SCORIA_DB_PORT:-5432}" SCORIA_DB_PASSWORD="${SCORIA_DB_PASSWORD:-postgres}" MIX_ENV=test mix do clean, test test/scoria/adoption_surface_test.exs --trace`
- `SCORIA_DB_PORT="${SCORIA_DB_PORT:-5432}" SCORIA_DB_PASSWORD="${SCORIA_DB_PASSWORD:-postgres}" MIX_ENV=test mix do clean, test.adoption`
</verification>

<success_criteria>
- `mix test.adoption` is still the one public default-lane verifier and now includes the fresh-host proof.
- Source-truth tests prevent optional Tailwind, semantic, or knowledge setup from drifting into hidden default-lane prerequisites.
</success_criteria>

<output>
After completion, create `.planning/phases/48-host-app-install-contract-and-consumer-proof/48-04-SUMMARY.md`
</output>
