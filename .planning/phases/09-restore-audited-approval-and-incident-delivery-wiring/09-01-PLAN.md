---
phase: 09-restore-audited-approval-and-incident-delivery-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/workflows.ex
  - lib/scoria/workflows/resume.ex
  - lib/scoria_web/live/orchestrator_live.ex
  - test/scoria/sre/audit_outbox_test.exs
  - test/scoria/workflows/runtime_test.exs
  - test/scoria/workflows/integration_test.exs
  - test/scoria_web/live/orchestrator_live_test.exs
autonomous: true
requirements:
  - SRE-05
must_haves:
  truths:
    - "Operator approve and reject actions mutate approvals only through `Scoria.Workflows.approve/3`."
    - "Approval request and decision rows leave durable audit-outbox evidence with actor, tenant, and trace attribution."
    - "A successful approval resumes the paused run only through workflow-owned resume logic after commit."
  artifacts:
    - path: "lib/scoria_web/live/orchestrator_live.ex"
      provides: "LiveView approval handlers that delegate to workflow-owned mutation and resume seams"
    - path: "lib/scoria/workflows.ex"
      provides: "Durable approval mutation contract and audit insertion path"
    - path: "test/scoria_web/live/orchestrator_live_test.exs"
      provides: "LiveView regression coverage for audited approval handling"
  key_links:
    - from: "lib/scoria_web/live/orchestrator_live.ex"
      to: "lib/scoria/workflows.ex"
      via: "approve/reject event delegation"
      pattern: "Workflows\\.approve"
    - from: "lib/scoria_web/live/orchestrator_live.ex"
      to: "lib/scoria/workflows/resume.ex"
      via: "post-commit run continuation"
      pattern: "Resume\\.resume_run"
---

<objective>
Restore the approval boundary so the operator UI stops mutating `Approval` rows directly and instead uses the existing workflow-owned audited mutation seam.

Purpose: close SRE-05 exactly where the milestone audit found the bypass, while preserving the boring transactional contract already established in `Scoria.Workflows`.
Output: LiveView approval handlers that call `Scoria.Workflows.approve/3`, targeted approval durability tests, and focused resume-path coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-CONTEXT.md
@.planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-RESEARCH.md
@.planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-VALIDATION.md
@.planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-UI-SPEC.md
@.planning/v1.3-MILESTONE-AUDIT.md
@lib/scoria/workflows.ex
@lib/scoria/workflows/resume.ex
@lib/scoria_web/live/orchestrator_live.ex
@test/scoria/sre/audit_outbox_test.exs
@test/scoria/workflows/runtime_test.exs
@test/scoria/workflows/integration_test.exs
@test/scoria_web/live/orchestrator_live_test.exs

<interfaces>
From lib/scoria/workflows.ex:
```elixir
def approve(approval_or_id, status, attrs \\ %{})
def resume_run(run_id)
def mark_waiting_for_approval(run_id, step_id, attrs)
```

From lib/scoria/workflows/resume.ex:
```elixir
def resume_run(run_id, opts \\ [])
```

From lib/scoria_web/live/orchestrator_live.ex:
```elixir
def handle_event("approve", _, socket)
def handle_event("reject", _, socket)
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Route Approval UI Actions Through Workflow-Owned Mutation</name>
  <files>lib/scoria_web/live/orchestrator_live.ex, lib/scoria/workflows/resume.ex, test/scoria_web/live/orchestrator_live_test.exs, test/scoria/workflows/integration_test.exs</files>
  <read_first>
    - lib/scoria_web/live/orchestrator_live.ex
    - lib/scoria/workflows.ex
    - lib/scoria/workflows/resume.ex
    - test/scoria_web/live/orchestrator_live_test.exs
    - test/scoria/workflows/integration_test.exs
    - .planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-CONTEXT.md
  </read_first>
  <action>Replace the direct `Repo.update/2` approval mutations in `ScoriaWeb.OrchestratorLive` with one workflow-owned path per D-01, D-02, D-04, D-05, and D-06: call `Scoria.Workflows.approve/3` with the active approval id plus actor, tenant, and trace attrs derived from the approval/session/trace context already available to the LiveView, then invoke `Scoria.Workflows.Resume.resume_run/2` only after the approval transaction succeeds. Keep the UI surface inside the existing modal, but update the visible labels and explanatory copy to match the Phase 9 UI contract without introducing any new dashboard surface. Expand the LiveView and workflow integration tests so approve and reject assertions prove workflow-owned effects, not just row-status changes.</action>
  <acceptance_criteria>
    - `lib/scoria_web/live/orchestrator_live.ex` no longer calls `Scoria.Repo.update` inside `handle_event("approve"` or `handle_event("reject"` per D-02
    - `lib/scoria_web/live/orchestrator_live.ex` calls `Scoria.Workflows.approve/3` and `Scoria.Workflows.Resume.resume_run/2` per D-01 and D-06
    - `test/scoria_web/live/orchestrator_live_test.exs` asserts the modal uses `Approve Decision` and `Reject Decision` copy from `09-UI-SPEC.md`
    - `test/scoria/workflows/integration_test.exs` proves the approval path resumes from durable workflow state rather than a UI-side row mutation
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_test.exs test/scoria/workflows/integration_test.exs</automated>
  </verify>
  <done>The operator approval surface now records approval intent through the workflow context and resumes only through workflow-owned post-commit logic.</done>
</task>

<task type="tdd">
  <name>Task 2: Lock Approval Audit Durability and Attribution</name>
  <files>lib/scoria/workflows.ex, test/scoria/sre/audit_outbox_test.exs, test/scoria/workflows/runtime_test.exs</files>
  <read_first>
    - lib/scoria/workflows.ex
    - test/scoria/sre/audit_outbox_test.exs
    - test/scoria/workflows/runtime_test.exs
    - .planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-RESEARCH.md
  </read_first>
  <action>Keep approval truth and durable audit evidence in the same transaction per D-03 and D-05. If the targeted approval durability suite still hits the pre-existing audit unique-index naming mismatch noted in research, normalize the test-local DDL or assertion setup inside `test/scoria/sre/audit_outbox_test.exs` before adding new Phase 9 expectations; do not widen this into the broader Phase 10 bootstrap work. Extend approval request and decision tests so requested, approved, rejected, and expired transitions assert actor, tenant, trace, approval id, and decision lineage in the outbox row. If approval expiration is currently implicit or under-covered, make the narrowest workflow-owned change needed in `lib/scoria/workflows.ex` to expose or exercise that path without widening the public contract beyond the existing workflow boundary. Keep runtime approval-wait tests focused on post-commit resume behavior rather than direct DB seeding.</action>
  <acceptance_criteria>
    - `test/scoria/sre/audit_outbox_test.exs` verifies `approval.requested`, `approval.approved`, `approval.rejected`, and `approval.expired` rows carry attributable audit context per D-03 and D-05
    - `test/scoria/sre/audit_outbox_test.exs` remains trustworthy under the targeted suite even if the pre-existing unique-index name drift must be normalized locally
    - `test/scoria/workflows/runtime_test.exs` or another focused workflow test proves expiration leaves workflow truth and audit evidence aligned before any later resume or retry path is considered
    - `test/scoria/workflows/runtime_test.exs` continues to prove approval waits persist before resume and that resume happens only after approval success per D-03
    - `lib/scoria/workflows.ex` remains the sole public approval mutation contract per D-04
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/sre/audit_outbox_test.exs test/scoria/workflows/runtime_test.exs</automated>
  </verify>
  <done>Approval request and decision transitions are durably audited, attributable, and covered by focused tests that do not depend on unrelated bootstrap cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator LiveView event -> workflow approval mutation | An interactive UI action crosses into durable approval truth and audit evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | `ScoriaWeb.OrchestratorLive` approval handlers | mitigate | Remove direct `Repo.update/2` mutations and route every decision through `Scoria.Workflows.approve/3` per D-01 and D-02. |
| T-09-01-02 | Repudiation | approval audit lineage | mitigate | Require actor, tenant, trace, approval id, and decision evidence in the same transaction as the approval row update per D-03 and D-05. |
| T-09-01-03 | Elevation of Privilege | resume trigger after approval | mitigate | Invoke resume only through `Scoria.Workflows.Resume.resume_run/2` after approval commit so the UI cannot bypass workflow-owned continuation rules per D-06. |
</threat_model>

<verification>
- Approve and reject clicks mutate approvals only through `Scoria.Workflows.approve/3`.
- Approval requested, approved, rejected, and expired transitions create durable audit rows with attributable actor, tenant, and trace context.
- Resuming a paused run remains a workflow-owned post-commit action, not a UI-side row mutation.
</verification>

<success_criteria>
- SRE-05 is closed with focused LiveView, workflow, and audit tests that prove the audited approval boundary is back in production.
</success_criteria>

<output>
After completion, create `.planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-01-SUMMARY.md`
</output>
