---
phase: 09-restore-audited-approval-and-incident-delivery-wiring
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - lib/scoria/sre/relay.ex
  - lib/scoria_web/live/orchestrator_live.ex
  - lib/scoria_web/components/incident_evidence_component.ex
  - test/scoria/sre/relay_test.exs
  - test/scoria/workflows/integration_test.exs
  - test/scoria_web/live/orchestrator_live_sre_test.exs
autonomous: true
requirements:
  - SRE-07
must_haves:
  truths:
    - "Relay drains real `NotificationDelivery` rows after commit and persists delivery outcome evidence durably."
    - "The trace-first notebook lazily renders approval, audit, incident, and delivery lineage produced by the real path rather than seeded placeholders alone."
    - "The operator surface remains compact and trace-first, with no new dashboard surface or mount-time eager loading."
  artifacts:
    - path: "lib/scoria/sre/relay.ex"
      provides: "Durable relay outcome evidence for delivered, failed, noop, or unconfigured notification attempts"
    - path: "lib/scoria_web/live/orchestrator_live.ex"
      provides: "Lazy trace-first incident evidence loading wired to real lineage"
    - path: "test/scoria_web/live/orchestrator_live_sre_test.exs"
      provides: "End-to-end notebook proof using live incident and relay rows"
  key_links:
    - from: "lib/scoria/sre/relay.ex"
      to: "lib/scoria_web/live/orchestrator_live.ex"
      via: "durable delivery rows queried into notebook evidence"
      pattern: "delivery_status|metadata"
    - from: "lib/scoria_web/live/orchestrator_live.ex"
      to: "lib/scoria_web/components/incident_evidence_component.ex"
      via: "lazy incident evidence projection"
      pattern: "load_incident_evidence|incident_evidence"
---

<objective>
Prove the repaired end-to-end path: approval audit lineage and incident delivery lineage must survive relay fanout and render truthfully inside the existing trace-first notebook.

Purpose: close SRE-07 with real production-path evidence instead of seeded placeholders, while preserving the existing calm LiveView surface and lazy evidence loading contract.
Output: relay outcome evidence updates, notebook projection tweaks where needed, and focused end-to-end LiveView plus relay verification.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-CONTEXT.md
@.planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-RESEARCH.md
@.planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-VALIDATION.md
@.planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-UI-SPEC.md
@.planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-01-PLAN.md
@.planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-02-PLAN.md
@lib/scoria/sre/relay.ex
@lib/scoria_web/live/orchestrator_live.ex
@lib/scoria_web/components/incident_evidence_component.ex
@test/scoria/sre/relay_test.exs
@test/scoria/workflows/integration_test.exs
@test/scoria_web/live/orchestrator_live_sre_test.exs

<interfaces>
From lib/scoria/sre/relay.ex:
```elixir
def drain_once(opts \\ [])
```

From lib/scoria_web/live/orchestrator_live.ex:
```elixir
def handle_event("load_incident_evidence", params, socket)
```

From lib/scoria_web/components/incident_evidence_component.ex:
```elixir
attr :evidence, :map, required: true
def render(assigns)
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Persist Relay Outcome Evidence for Real Delivery Rows</name>
  <files>lib/scoria/sre/relay.ex, test/scoria/sre/relay_test.exs</files>
  <read_first>
    - lib/scoria/sre/relay.ex
    - test/scoria/sre/relay_test.exs
    - .planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-RESEARCH.md
  </read_first>
  <action>Extend relay outcome persistence so producer-created delivery rows retain explicit outcome evidence per D-08 and D-10 after `Scoria.SRE.Relay.drain_once/1` runs. Keep relay post-commit and asynchronous; do not move any vendor call into `IncidentManager`. When adapters return a noop-style success or the dispatcher is effectively unconfigured, preserve that semantic in durable delivery metadata or another narrow local field while still leaving the row queryable by the notebook. Keep the current durable retry behavior for failures intact, including attempt counts and `last_error`, and make the focused relay tests prove these semantics on real producer-shaped rows rather than only on manually invented minimal fixtures.</action>
  <acceptance_criteria>
    - `lib/scoria/sre/relay.ex` persists explicit outcome semantics for delivered, failed, and noop or unconfigured deliveries per D-10
    - `test/scoria/sre/relay_test.exs` proves relay drains rows created from the Phase 9 producer contract instead of depending only on hand-seeded bare fixtures
    - `test/scoria/sre/relay_test.exs` keeps attempt-count and retry expectations intact for failed deliveries
    - Relay remains the only place where notification adapters publish per D-08
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/sre/relay_test.exs</automated>
  </verify>
  <done>Relay leaves durable, queryable evidence explaining what happened to each delivery row after fanout.</done>
</task>

<task type="tdd">
  <name>Task 2: Prove Real Notebook Lineage Without Expanding the UI Surface</name>
  <files>lib/scoria_web/live/orchestrator_live.ex, lib/scoria_web/components/incident_evidence_component.ex, test/scoria/workflows/integration_test.exs, test/scoria_web/live/orchestrator_live_sre_test.exs</files>
  <read_first>
    - lib/scoria_web/live/orchestrator_live.ex
    - lib/scoria_web/components/incident_evidence_component.ex
    - test/scoria/workflows/integration_test.exs
    - test/scoria_web/live/orchestrator_live_sre_test.exs
    - .planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-UI-SPEC.md
  </read_first>
  <action>Keep the notebook trace-first and lazy per D-13 through D-16: no new page, no mount-time eager load, and no new operator chrome. Update the incident evidence projection only as needed so delivery cards surface sink kind, delivery status, routing key, attempts, and the durable outcome semantics from Task 1, with lineage labels back to the same trace, run, and approval context. Add one focused end-to-end proof that uses real workflow approval, `SRE.record_alert_event/1`, `Scoria.SRE.Relay.drain_once/1`, and `load_incident_evidence` in LiveView rather than directly seeding the delivery row. Keep the existing seeded rendering tests if they still provide value, but make the real-path proof the acceptance gate for SRE-07. Use focused test commands and avoid depending on full-migration `pgvector` availability; if a test helper needs narrow table setup to stay green, keep that fix inside the targeted test file only.</action>
  <acceptance_criteria>
    - `test/scoria_web/live/orchestrator_live_sre_test.exs` includes at least one real-path incident -> delivery -> relay -> notebook assertion and no longer treats seeded delivery rows as the only proof per D-14
    - `test/scoria_web/live/orchestrator_live_sre_test.exs` explicitly asserts the rendered approval, audit, incident, and delivery evidence links back to the same run id and trace id lineage required by D-14
    - `lib/scoria_web/live/orchestrator_live.ex` still loads incident evidence only through explicit `load_incident_evidence` actions per D-15
    - `lib/scoria_web/components/incident_evidence_component.ex` surfaces delivery outcome semantics without adding a new dashboard surface per D-13 and D-16
    - The targeted LiveView/integration suite avoids widening into the unrelated pgvector/bootstrap cleanup called out in research
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/workflows/integration_test.exs test/scoria_web/live/orchestrator_live_sre_test.exs</automated>
  </verify>
  <done>The existing notebook now proves real approval and delivery lineage lazily, on the real path, without any new UI surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Relay fanout -> durable delivery evidence -> operator notebook | External delivery outcomes become operator-visible evidence used for incident decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Repudiation | `Scoria.SRE.Relay` delivery outcomes | mitigate | Persist explicit relay outcome evidence for noop, unconfigured, failed, and delivered paths so the notebook can explain transport history per D-10 and D-14. |
| T-09-03-02 | Information Disclosure | incident evidence notebook | mitigate | Keep the notebook evidence trace-first and metadata-oriented, surfacing lineage and status rather than widening into raw payload dumps or a new dashboard per D-13 and D-16. |
| T-09-03-03 | Denial of Service | notebook loading path | mitigate | Preserve explicit lazy loading through `load_incident_evidence` and do not preload heavy evidence on mount per D-15. |
</threat_model>

<verification>
- Relay drains real delivery rows and records durable outcome evidence.
- The notebook renders approval, audit, incident, and delivery lineage from the real path after `load_incident_evidence`, with same-run and same-trace drilldowns preserved.
- Focused Phase 9 verification passes without depending on unrelated pgvector/bootstrap cleanup.
</verification>

<success_criteria>
- SRE-07 is closed with real end-to-end proof on the existing trace-first LiveView surface.
</success_criteria>

<output>
After completion, create `.planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-03-SUMMARY.md`
</output>
