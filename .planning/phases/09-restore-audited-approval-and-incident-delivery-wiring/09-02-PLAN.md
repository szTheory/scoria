---
phase: 09-restore-audited-approval-and-incident-delivery-wiring
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/scoria/sre/incident_manager.ex
  - lib/scoria/sre/notification_delivery.ex
  - test/scoria/sre/incident_test.exs
  - test/scoria/sre/relay_test.exs
autonomous: true
requirements:
  - SRE-06
must_haves:
  truths:
    - "Opening a live incident creates durable `NotificationDelivery` intent rows in the same transaction as incident graph writes."
    - "Deduped repeats do not create extra responder noise, but routing escalation from `review` to `page` does create the new delivery intent row."
    - "Delivery rows preserve enough local metadata for relay and the notebook to explain routing even when transports are noop or unconfigured."
  artifacts:
    - path: "lib/scoria/sre/incident_manager.ex"
      provides: "Transactional incident-plus-delivery production seam"
    - path: "lib/scoria/sre/notification_delivery.ex"
      provides: "Validated durable delivery intent schema"
    - path: "test/scoria/sre/incident_test.exs"
      provides: "Producer-side regression coverage for delivery-row creation and dedupe/escalation rules"
  key_links:
    - from: "lib/scoria/sre/incident_manager.ex"
      to: "lib/scoria/sre/notification_delivery.ex"
      via: "Ecto.Multi delivery insertion"
      pattern: "NotificationDelivery|deliver"
    - from: "lib/scoria/sre/incident_manager.ex"
      to: "lib/scoria/sre/relay.ex"
      via: "durable pending rows claimed post-commit"
      pattern: "delivery_status|pending"
---

<objective>
Restore the missing delivery producer so incident routing writes durable notification intent rows instead of leaving relay with nothing to drain.

Purpose: close SRE-06 by reconnecting the exact transactional seam the milestone audit identified, while keeping external sends strictly post-commit and adapter-free inside the incident transaction.
Output: `IncidentManager` delivery-row production, producer-side dedupe/escalation logic, and focused incident/relay regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-CONTEXT.md
@.planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-RESEARCH.md
@.planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-VALIDATION.md
@.planning/v1.3-MILESTONE-AUDIT.md
@lib/scoria/sre.ex
@lib/scoria/sre/incident_manager.ex
@lib/scoria/sre/notification_delivery.ex
@lib/scoria/sre/relay.ex
@test/scoria/sre/incident_test.exs
@test/scoria/sre/relay_test.exs

<interfaces>
From lib/scoria/sre.ex:
```elixir
def record_alert_event(envelope) when is_map(envelope)
```

From lib/scoria/sre/incident_manager.ex:
```elixir
def record_alert_event(envelope) when is_map(envelope)
```

From lib/scoria/sre/notification_delivery.ex:
```elixir
def changeset(delivery, attrs)
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Produce Delivery Intent Rows Inside the Incident Transaction</name>
  <files>lib/scoria/sre/incident_manager.ex, lib/scoria/sre/notification_delivery.ex, test/scoria/sre/incident_test.exs</files>
  <read_first>
    - lib/scoria/sre/incident_manager.ex
    - lib/scoria/sre/notification_delivery.ex
    - test/scoria/sre/incident_test.exs
    - .planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-CONTEXT.md
  </read_first>
  <action>Add a delivery-production step to `Scoria.SRE.IncidentManager.record_alert_event/1` per D-07, D-09, D-11, and D-12. Use the existing `Ecto.Multi` so incident root writes, alert event writes, incident event writes, and `NotificationDelivery` inserts all commit or roll back together. Default routing must stay boring and local per D-09: `review` routes to `chimeway`, `page` routes to `mailglass`. Create delivery rows only when the incident is first opened or when the routing class escalates from `review` to `page`; deduped repeats that do not escalate must update evidence without emitting extra responder noise per D-11. Keep the metadata explicit enough for later notebook/relay proof, but do not call adapters from this task.</action>
  <acceptance_criteria>
    - `lib/scoria/sre/incident_manager.ex` contains a delivery-creation step in the same `Ecto.Multi` as incident, alert event, and incident event writes per D-07
    - `test/scoria/sre/incident_test.exs` asserts new incidents create at least one `NotificationDelivery` row with default sink/routing per D-09
    - `test/scoria/sre/incident_test.exs` asserts non-escalating deduped repeats do not create extra delivery rows per D-11
    - `test/scoria/sre/incident_test.exs` asserts escalation from `review` to `page` creates the new delivery row rather than mutating the original responder intent in place per D-11
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/sre/incident_test.exs</automated>
  </verify>
  <done>Live incident routing now produces durable delivery intent rows transactionally, with explicit default routing and dedupe/escalation rules.</done>
</task>

<task type="tdd">
  <name>Task 2: Preserve Noop and Unconfigured Transport Evidence Without Sending In-Transaction</name>
  <files>lib/scoria/sre/notification_delivery.ex, test/scoria/sre/incident_test.exs, test/scoria/sre/relay_test.exs</files>
  <read_first>
    - lib/scoria/sre/notification_delivery.ex
    - test/scoria/sre/incident_test.exs
    - test/scoria/sre/relay_test.exs
    - .planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-RESEARCH.md
  </read_first>
  <action>Make the produced delivery rows evidence-first per D-08 and D-10: the row itself is the durable local truth, and any transport ambiguity must be representable locally without silently dropping the notification. Keep `delivery_status` in the current validated lifecycle unless a schema expansion is strictly necessary, and prefer explicit metadata fields for transport semantics (`noop`, `unconfigured`, routing provenance, summary, severity, routing class) because research left that choice to implementation discretion. Extend the focused tests so producer-created rows already contain the routing and evidence fields relay will need, and keep `test/scoria/sre/relay_test.exs` aligned with those producer rows instead of relying only on manually seeded bare-minimum deliveries.</action>
  <acceptance_criteria>
    - Producer-created `NotificationDelivery` rows carry routing and evidence metadata that can distinguish noop or unconfigured transport semantics per D-10
    - `test/scoria/sre/incident_test.exs` checks the delivery row shape needed for relay/notebook lineage instead of only counting rows
    - `test/scoria/sre/relay_test.exs` can consume producer-shaped deliveries without inventing a second schema contract
    - No task code calls a dispatcher from inside `IncidentManager` or any transaction boundary per D-08
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/sre/incident_test.exs test/scoria/sre/relay_test.exs</automated>
  </verify>
  <done>Durable delivery rows preserve explicit local evidence for later relay and notebook proof, without moving external sends into the transaction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alert envelope -> incident transaction -> delivery intent rows | Incoming alert data becomes durable incident lineage and queued notification intent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Repudiation | `Scoria.SRE.IncidentManager.record_alert_event/1` | mitigate | Insert `NotificationDelivery` rows in the same `Ecto.Multi` as the incident graph so there is durable proof of routing intent per D-07. |
| T-09-02-02 | Denial of Service | incident dedupe/escalation routing | mitigate | Create new delivery rows only for first-open or review-to-page escalation paths so repeated alerts do not create duplicate responder noise per D-11. |
| T-09-02-03 | Tampering | delivery routing defaults | mitigate | Keep the default sink mapping explicit and local (`review` -> `chimeway`, `page` -> `mailglass`) so routing intent is deterministic and testable per D-09 and D-12. |
</threat_model>

<verification>
- `record_alert_event/1` writes incident, alert, incident-event, and delivery rows atomically.
- Default routing produces the expected sink kind and routing key for review versus page incidents.
- Deduped repeats do not fan out duplicate responder noise unless routing escalates.
</verification>

<success_criteria>
- SRE-06 is closed with producer-side tests proving relay now has real durable rows to drain.
</success_criteria>

<output>
After completion, create `.planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-02-SUMMARY.md`
</output>
