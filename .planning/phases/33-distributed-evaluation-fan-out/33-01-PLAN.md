---
phase: 33-distributed-evaluation-fan-out
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/eval.ex
  - lib/scoria/eval/eval_campaign.ex
  - lib/scoria/eval/eval_campaign_target.ex
  - lib/scoria/eval/eval_run.ex
  - priv/repo/migrations/TIMESTAMP_create_eval_campaigns_and_targets.exs
  - test/scoria/eval/eval_campaign_persistence_test.exs
  - test/scoria/eval/eval_run_persistence_test.exs
autonomous: false
requirements:
  - EVAL-02
must_haves:
  truths:
    - "A durable eval campaign parent exists and carries explicit tenant identity, immutable eval contract lineage, and eager aggregate counters."
    - "Each execution target is persisted as a narrow runtime-only child row rather than a second eval-spec system."
    - "Each EvalRun keeps campaign and target lineage while remaining the canonical child execution truth for scores."
    - "Existing `ai_eval_runs` rows created before Phase 33 remain readable and valid after the lineage migration without forced synthetic backfill."
    - "The schema can be migrated on a real database before fan-out or worker code depends on it."
  artifacts:
    - path: "lib/scoria/eval/eval_campaign.ex"
      provides: "Campaign aggregate schema with status and counter validation"
      contains: "schema \"ai_eval_campaigns\""
    - path: "lib/scoria/eval/eval_campaign_target.ex"
      provides: "Target shard schema with explicit tenant and execution metadata"
      contains: "schema \"ai_eval_campaign_targets\""
    - path: "priv/repo/migrations/TIMESTAMP_create_eval_campaigns_and_targets.exs"
      provides: "Database tables, indexes, and EvalRun lineage columns for campaign fan-out"
      contains: "create table(:ai_eval_campaigns"
    - path: "test/scoria/eval/eval_campaign_persistence_test.exs"
      provides: "Persistence proof for campaign and target truth"
      contains: "create_eval_campaign"
  key_links:
    - from: "lib/scoria/eval.ex"
      to: "lib/scoria/eval/eval_run.ex"
      via: "campaign-aware create/update APIs"
      pattern: "campaign_id|campaign_target_id|tenant_id"
    - from: "priv/repo/migrations/TIMESTAMP_create_eval_campaigns_and_targets.exs"
      to: "lib/scoria/eval/eval_campaign.ex"
      via: "table and field parity"
      pattern: "ai_eval_campaigns|status|total_targets"
---

<objective>
Establish the durable persistence foundation for distributed evaluation campaigns, targets, and child run lineage.

Purpose: Phase 33 cannot safely enqueue or execute distributed work until campaign truth, target truth, and `EvalRun` lineage are explicit in Ecto and proven against a real migrated schema.
Output: campaign/target schemas, migration, `Scoria.Eval` persistence APIs, and focused persistence coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.8-REQUIREMENTS.md
@.planning/phases/33-distributed-evaluation-fan-out/33-CONTEXT.md
@.planning/phases/33-distributed-evaluation-fan-out/33-RESEARCH.md
@.planning/phases/33-distributed-evaluation-fan-out/33-PATTERNS.md
@lib/scoria/eval.ex
@lib/scoria/eval/eval_run.ex
@lib/scoria/eval/score.ex
@test/scoria/eval/eval_run_persistence_test.exs

<interfaces>
From `lib/scoria/eval.ex`:
```elixir
def create_eval_run(attrs \\ %{})
def complete_eval_run(%EvalRun{} = eval_run, attrs)
def record_eval_scores(%EvalRun{} = eval_run, score_attrs_list)
```

From `lib/scoria/eval/eval_run.ex`:
```elixir
schema "ai_eval_runs" do
  field :status, :string, default: "pending"
  field :provider, :string
  field :model, :string
  belongs_to :dataset, Scoria.Eval.Dataset, type: :id
  belongs_to :eval_spec, Scoria.Eval.EvalSpec
  has_many :scores, Scoria.Eval.Score
end
```

From `test/scoria/eval/eval_run_persistence_test.exs`:
```elixir
assert {:ok, eval_run} = Eval.create_eval_run(%{...})
assert {:ok, eval_run, [%Score{}]} = Eval.record_eval_scores(eval_run, [...])
assert {:ok, eval_run} = Eval.complete_eval_run(eval_run, %{status: "completed"})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add persistence coverage for campaign truth, target guardrails, and EvalRun lineage</name>
  <files>test/scoria/eval/eval_campaign_persistence_test.exs, test/scoria/eval/eval_run_persistence_test.exs</files>
  <behavior>
    - Test 1: `create_eval_campaign/1` persists one campaign row plus target rows with the same `eval_spec_id`, explicit `tenant_id`, eager counters, and initial `queued`/`pending` status facts.
    - Test 2: target attrs reject semantic override fields such as prompt version, dataset slice, threshold policy, or judge definition while allowing only runtime knobs like provider/model/queue/priority per D-03 and D-04.
    - Test 3: `create_eval_run/1` persists `campaign_id`, `campaign_target_id`, and `tenant_id` lineage while existing score aggregation behavior still passes.
    - Test 4: a legacy `ai_eval_runs` row created without campaign lineage or tenant identity before this migration remains loadable and compatible with existing persistence flows after the migration.
  </behavior>
  <action>
    Create `test/scoria/eval/eval_campaign_persistence_test.exs` and extend `test/scoria/eval/eval_run_persistence_test.exs` first.
    Per D-01, D-02, D-05, D-06, D-08, D-15, and D-20, write tests that lock the durable hierarchy to one immutable `EvalSpec`, one `EvalCampaign` parent, many runtime-only target rows, and `EvalRun` child truth.
    Make the guardrail test explicit: attempts to pass prompt-version, judge, threshold, or dataset-slice mutations in target attrs must fail with a changeset error per D-03 and D-04 instead of being silently accepted.
    Include expectations for the default aggregate state per D-19 and D-20: `total_targets`, `queued_targets`, zeroed running/completed/failed/cancelled counts, and nil `finished_at`.
    Add explicit compatibility coverage for pre-Phase-33 `ai_eval_runs` rows: the tests should prove historical rows with nil campaign lineage still load, still aggregate scores, and are not rejected by the updated schema or context APIs.
  </action>
  <verify>
    <automated>mix test test/scoria/eval/eval_campaign_persistence_test.exs test/scoria/eval/eval_run_persistence_test.exs</automated>
  </verify>
  <done>Focused persistence tests fail before implementation and define the exact durable truth Scoria must ship for Phase 33.</done>
</task>

<task type="auto">
  <name>Task 2: Implement campaign schemas, migration, and canonical `Scoria.Eval` persistence APIs</name>
  <files>lib/scoria/eval.ex, lib/scoria/eval/eval_campaign.ex, lib/scoria/eval/eval_campaign_target.ex, lib/scoria/eval/eval_run.ex, priv/repo/migrations/TIMESTAMP_create_eval_campaigns_and_targets.exs</files>
  <action>
    Implement `Scoria.Eval.EvalCampaign` and `Scoria.Eval.EvalCampaignTarget` plus the migration and context API needed by Task 1.
    Per D-01 and D-02, add an `ai_eval_campaigns` parent table keyed to one immutable `eval_spec_id`, and an `ai_eval_campaign_targets` child table for runtime-only execution targets.
    Per D-03 and D-04, keep target columns narrow: `tenant_id`, `provider`, `model`, `queue`, `priority`, status/timestamps, and optional runtime metadata. Do not add prompt-version, judge-definition, threshold-policy, dataset-slice, or other contract-mutating columns.
    Per D-15, D-16, and D-17, add explicit `tenant_id` to campaign, target, and `ai_eval_runs`, with indexed lookup paths on tenant-scoped campaign drill-downs.
    Extend `EvalRun` and its changeset with `campaign_id` and `campaign_target_id` foreign keys per D-08, but preserve `EvalRun` as the child execution truth rather than creating a parallel result hierarchy per D-06 and D-07.
    Make the legacy-row migration strategy explicit in both schema and migration comments: add the new `ai_eval_runs.tenant_id`, `campaign_id`, and `campaign_target_id` columns as nullable for historical compatibility, do not synthesize fake campaign/target parents for old rows, and require the new lineage only in Phase 33 campaign creation paths. If an existing source of truth already provides tenant identity for some legacy rows, backfill only that safe field; otherwise leave legacy `tenant_id` null and cover the read compatibility in tests.
    In `Scoria.Eval`, add the minimum persistence APIs needed for later plans, such as `create_eval_campaign/1`, `list_campaign_targets/1`, and campaign-aware `create_eval_run/1` defaults, using `Ecto.Multi` where parent and child rows must be inserted atomically.
    Keep campaign statuses explicit and operator-legible per D-12, including at least `queued`, `running`, `completed`, `completed_partial`, `failed_fatal`, and `cancelled`.
  </action>
  <verify>
    <automated>mix test test/scoria/eval/eval_campaign_persistence_test.exs test/scoria/eval/eval_run_persistence_test.exs</automated>
  </verify>
  <done>Campaign, target, and EvalRun lineage persist through canonical `Scoria.Eval` APIs and the new tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the schema against a real migrated database before fan-out work proceeds</name>
  <action>Run the new migration on a real local dev database, confirm the campaign and target tables plus EvalRun lineage columns exist as planned, then rollback and re-apply once to prove the migration is reversible before Plan 33-02 starts. This blocking check is required because Phase 33 adds durable schema and later plans depend on the database truth, not only types.</action>
  <what-built>Task 2 added the campaign/target migration, new schemas, and `Scoria.Eval` persistence APIs.</what-built>
  <how-to-verify>
    1. Run `mix ecto.migrate`.
    2. Confirm the new tables and indexes exist with your usual schema inspection command or psql `.d` output.
    3. Run `mix ecto.rollback --step 1`.
    4. Run `mix ecto.migrate` again.
    5. Return `approved` only if the migration applies cleanly twice and the schema shape matches the plan.
  </how-to-verify>
  <acceptance_criteria>
    - `ai_eval_campaigns` and `ai_eval_campaign_targets` exist after migration.
    - `ai_eval_runs` includes campaign lineage and tenant identity columns.
    - The rollback/re-apply cycle succeeds without manual patching.
  </acceptance_criteria>
  <resume-signal>Type `approved` after the migrate/rollback/migrate cycle succeeds, or describe the exact migration failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| campaign API -> Ecto persistence | Untrusted operator inputs become durable eval campaign truth |
| migration -> existing eval tables | New lineage columns alter existing `ai_eval_runs` truth used by prior eval features |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | `EvalCampaignTarget` attrs | mitigate | Reject semantic override fields in the target changeset so runtime targets cannot mutate the eval contract per D-03 and D-04. |
| T-33-02 | Information Disclosure | tenant lineage columns | mitigate | Require explicit `tenant_id` on campaign, target, and EvalRun plus indexed FK joins so later reads do not infer tenant scope from worker-local state. |
| T-33-03 | Denial of Service | migration / existing eval truth | mitigate | Block Plan 33-02 on a real migrate/rollback/migrate verification so incompatible schema changes are caught before fan-out depends on them. |
| T-33-10 | Tampering | legacy eval-run backfill | mitigate | Keep historical `ai_eval_runs` lineage columns nullable unless a safe existing source exists, avoiding fabricated tenant/campaign truth on old evidence rows. |
</threat_model>

<verification>
- `mix test test/scoria/eval/eval_campaign_persistence_test.exs test/scoria/eval/eval_run_persistence_test.exs`
- Blocking checkpoint: `mix ecto.migrate`, `mix ecto.rollback --step 1`, `mix ecto.migrate`
</verification>

<success_criteria>
Phase 33 has a proven durable foundation: one campaign parent per immutable eval contract, narrow target rows, explicit tenant identity on new durable execution artifacts, and campaign-aware EvalRun lineage validated against both tests and a real migration cycle without breaking legacy rows.
</success_criteria>

<output>
After completion, create `.planning/phases/33-distributed-evaluation-fan-out/33-01-SUMMARY.md`
</output>
