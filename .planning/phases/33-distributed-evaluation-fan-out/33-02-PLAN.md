---
phase: 33-distributed-evaluation-fan-out
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - lib/scoria/eval.ex
  - lib/scoria/eval/campaign_enqueuer.ex
  - lib/scoria/eval/campaign_worker.ex
  - test/scoria/eval/campaign_enqueue_test.exs
autonomous: true
requirements:
  - EVAL-02
must_haves:
  truths:
    - "Operators can create one campaign for one immutable eval contract and fan it out across many tenant/model targets."
    - "Campaign fan-out uses `Scoria.Workflows.BatchEnqueue` on the dedicated `:evals` queue instead of per-job insert loops."
    - "Every enqueued job carries explicit campaign, target, eval run, and tenant identity needed for inspection and replay."
  artifacts:
    - path: "lib/scoria/eval/campaign_enqueuer.ex"
      provides: "Target normalization and batch job building for campaign fan-out"
      exports: ["enqueue_campaign/2"]
    - path: "lib/scoria/eval/campaign_worker.ex"
      provides: "Minimal worker contract exported early so coordinator fan-out builds real jobs against a stable async interface"
      contains: "def new_job"
    - path: "lib/scoria/eval.ex"
      provides: "Public coordinator API for campaign creation and enqueue kickoff"
      contains: "create_and_enqueue_campaign"
    - path: "test/scoria/eval/campaign_enqueue_test.exs"
      provides: "Focused fan-out and queue assignment coverage"
      contains: "assert_enqueued"
  key_links:
    - from: "lib/scoria/eval/campaign_enqueuer.ex"
      to: "lib/scoria/workflows/batch_enqueue.ex"
      via: "chunked Oban.insert_all handoff"
      pattern: "BatchEnqueue\\.enqueue_all"
    - from: "lib/scoria/eval/campaign_enqueuer.ex"
      to: "lib/scoria/eval/campaign_worker.ex"
      via: "worker job construction"
      pattern: "CampaignWorker\\.new_job"
---

<objective>
Implement the coordinator fan-out path that creates a campaign, normalizes targets, creates child runs, and enqueues worker jobs through the shared eval queue.

Purpose: The distributed-eval product shape is only real once Scoria can turn a single immutable eval contract into many explicit target shards and batch-enqueue them safely.
Output: a campaign coordinator API, target normalization/enqueue service, and focused enqueue tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.8-REQUIREMENTS.md
@.planning/phases/33-distributed-evaluation-fan-out/33-CONTEXT.md
@.planning/phases/33-distributed-evaluation-fan-out/33-RESEARCH.md
@.planning/phases/33-distributed-evaluation-fan-out/33-PATTERNS.md
@lib/scoria/eval.ex
@lib/scoria/workflows/batch_enqueue.ex
@lib/scoria/compaction/summarize_worker.ex
@lib/scoria/connectors/discovery_job.ex
@test/scoria/workflows/batch_enqueue_test.exs

<interfaces>
From `lib/scoria/workflows/batch_enqueue.ex`:
```elixir
def enqueue_all(jobs, opts \\ []) do
  jobs
  |> Enum.chunk_every(chunk_size)
  |> Enum.with_index()
  |> Enum.reduce(Ecto.Multi.new(), fn {chunk, idx}, multi ->
    Oban.insert_all(multi, :"batch_#{idx}", chunk)
  end)
  |> Scoria.Repo.transaction()
end
```

From `lib/scoria/connectors/discovery_job.ex`:
```elixir
def new_job(args, opts \\ []) do
  args
  |> normalize_args()
  |> new(opts)
end
```

From `lib/scoria/compaction/summarize_worker.ex`:
```elixir
use Oban.Worker, queue: :compaction, unique: [...]

def new_job(args, opts \\ []) do
  args
  |> Map.new()
  |> normalize_args()
  |> new(opts)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the coordinator contract with focused campaign enqueue tests</name>
  <files>test/scoria/eval/campaign_enqueue_test.exs</files>
  <behavior>
    - Test 1: creating a campaign with multiple targets persists one campaign, N targets, and one pending EvalRun per target, then enqueues one `CampaignWorker` job per target on `:evals`.
    - Test 2: job args include `campaign_id`, `campaign_target_id`, `eval_run_id`, `tenant_id`, `eval_spec_id`, and queue/priority metadata per D-15 and D-16.
    - Test 3: duplicate or contract-mutating target attrs are rejected during normalization so the coordinator never enqueues silently divergent semantics per D-01 through D-04.
    - Test 4: `CampaignWorker.new_job/2` exists and produces the normalized async contract the coordinator uses, so fan-out does not depend on a future plan to define the worker envelope.
  </behavior>
  <action>
    Create `test/scoria/eval/campaign_enqueue_test.exs` using `Oban.Testing` and the existing batch-enqueue assertions as the analog.
    Per D-23 and D-24, the tests must prove the coordinator routes all work through the dedicated `:evals` queue and `BatchEnqueue` rather than direct `Oban.insert/1` loops.
    Per D-14, D-15, and D-16, assert that tenant identity is present both in durable rows and in job args/meta so queued work is inspectable before any worker executes.
    Include one normalization failure case for semantic override keys and one de-duplication or uniqueness case for repeated identical targets so the coordinator cannot inflate campaigns accidentally.
  </action>
  <verify>
    <automated>mix test test/scoria/eval/campaign_enqueue_test.exs</automated>
  </verify>
  <done>The enqueue contract is explicit and failing until the coordinator implementation honors the queueing and identity rules.</done>
</task>

<task type="auto">
  <name>Task 2: Implement campaign creation, target normalization, and batch fan-out through `BatchEnqueue`</name>
  <files>lib/scoria/eval.ex, lib/scoria/eval/campaign_enqueuer.ex, lib/scoria/eval/campaign_worker.ex</files>
  <action>
    Add the coordinator path in `Scoria.Eval` and `Scoria.Eval.CampaignEnqueuer`.
    Per D-01, D-02, and D-28, expose one canonical entrypoint such as `create_and_enqueue_campaign/1` that accepts one `eval_spec_id` plus a list of runtime targets and does not accept per-target semantic contract overrides.
    In `CampaignEnqueuer`, normalize each target into the narrow execution shape from Plan 33-01 per D-03 and D-04, create the child `EvalRun` rows up front so queued jobs have durable lineage, and default queue assignment to `:evals` per D-23 while allowing only narrow priority/queue hints per D-03.
    Create the minimal `Scoria.Eval.CampaignWorker` contract in this plan so the coordinator builds against a real interface instead of a future placeholder. The file should define `use Oban.Worker, queue: :evals`, normalized `new_job/2`, and the exact arg keys required for replay-safe enqueueing; defer the full `perform/1` execution semantics to Plan 33-03.
    Use `Scoria.Workflows.BatchEnqueue.enqueue_all/2` to insert all jobs per D-24. Do not hand-roll `Enum.each(&Oban.insert/1)` or `Task.async_stream`.
    Ensure each built worker job envelope carries `campaign_id`, `campaign_target_id`, `eval_run_id`, `tenant_id`, `eval_spec_id`, `provider`, `model`, and any bounded runtime metadata needed for replay-safe execution per D-15 and D-16.
    Mark campaign counters/status eagerly as queued per D-19 and D-20 once job insertion succeeds, and keep the implementation inside `Scoria.Eval` rather than a second domain root.
  </action>
  <verify>
    <automated>mix test test/scoria/eval/campaign_enqueue_test.exs</automated>
  </verify>
  <done>Scoria can create a campaign, persist target/run lineage, and enqueue eval worker jobs via the dedicated batch seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator campaign request -> coordinator | User-supplied target definitions become queued distributed work |
| coordinator -> Oban job envelope | Durable row identity is copied into async job payloads that may be replayed later |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04 | Tampering | coordinator target normalization | mitigate | Strip or reject semantic override keys during normalization so campaigns cannot drift into hidden per-target eval contracts. |
| T-33-05 | Repudiation | queued campaign jobs | mitigate | Include `campaign_id`, `campaign_target_id`, `eval_run_id`, and `tenant_id` on every job envelope and define that contract in `CampaignWorker.new_job/2` during this plan so queued work can be inspected and tied back to durable truth. |
| T-33-06 | Denial of Service | enqueue path | mitigate | Force the coordinator through `BatchEnqueue.enqueue_all/2` on `:evals` to avoid N direct inserts and connection-pool contention during large fan-outs. |
</threat_model>

<verification>
- `mix test test/scoria/eval/campaign_enqueue_test.exs`
</verification>

<success_criteria>
Phase 33 can turn one immutable eval contract into many tenant/model target shards, persist the lineage, and batch-enqueue dedicated `:evals` jobs with inspectable tenant and campaign identity on every envelope.
</success_criteria>

<output>
After completion, create `.planning/phases/33-distributed-evaluation-fan-out/33-02-SUMMARY.md`
</output>
