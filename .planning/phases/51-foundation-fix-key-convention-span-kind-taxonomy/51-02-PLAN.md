---
phase: 51-foundation-fix-key-convention-span-kind-taxonomy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/observe/semconv.ex
  - test/scoria/observe/semconv_test.exs
autonomous: true
requirements: [FOUND-03]

must_haves:
  truths:
    - "every gen_ai.*/openinference.* key string used anywhere in the codebase traces back to one version-pinned mapping module (Scoria.Observe.Semconv), never inline string literals at multiple call sites — SC#4"
    - "Semconv DELEGATES to ReqLLM.OpenTelemetry.Attributes for the gen_ai.* key set (version-pinned by the req_llm ~> 1.13 dependency) and does NOT hand-duplicate those ~20 constants — RESEARCH Pattern 3"
    - "Semconv owns the one key Scoria itself writes: openinference.span.kind"
  artifacts:
    - lib/scoria/observe/semconv.ex
    - test/scoria/observe/semconv_test.exs
  key_links:
    - "Semconv.merge_req_llm_attributes/2 is the SOLE call site of ReqLLM.OpenTelemetry.Attributes.start/1 + .terminal/1 so adapters never inline a gen_ai.* string"
---

<objective>
Create the version-pinned `Scoria.Observe.Semconv` module (FOUND-03) as the single source for every semconv key Scoria itself defines, plus the one delegation seam that merges the `req_llm`-owned `gen_ai.*` attribute set — so no adapter ever writes a `gen_ai.*`/`openinference.*` string literal directly.

Purpose: Satisfy SC#4 (single version-pinned key origin) without inventing a second, driftable copy of `req_llm`'s already-correct `gen_ai.*` vocabulary — the module DELEGATES rather than re-declares.

Output: `Semconv` module + its unit test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-CONTEXT.md
@.planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-RESEARCH.md
@.planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-PATTERNS.md
@deps/req_llm/lib/req_llm/open_telemetry/attributes.ex
</context>

## Artifacts this phase produces (this plan)

- `Scoria.Observe.Semconv` module with `openinference_span_kind_key/0` (returns `"openinference.span.kind"`) and `merge_req_llm_attributes/2` (delegates to `ReqLLM.OpenTelemetry.Attributes.start/1` + `.terminal/1`)
- `test/scoria/observe/semconv_test.exs`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Scoria.Observe.Semconv delegating module</name>
  <files>lib/scoria/observe/semconv.ex</files>
  <read_first>
    - .planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-RESEARCH.md (Architecture Patterns §Pattern 3 — the full module given verbatim; §Code Examples for the exact key set; §Anti-Patterns "Re-deriving gen_ai.* by hand")
    - .planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-CONTEXT.md (D-16 — module must exist before/with adapter edits; pin the enum version in a moduledoc)
    - .planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-PATTERNS.md (§semconv.ex — verbatim shape, plain-module-with-constants analog)
    - deps/req_llm/lib/req_llm/open_telemetry/attributes.ex (the delegate target: public start/1, terminal/1, request_model/1)
  </read_first>
  <behavior>
    - Test: openinference_span_kind_key/0 returns exactly "openinference.span.kind"
    - Test: merge_req_llm_attributes/2 given a realistic metadata map returns a map containing gen_ai.request.model, gen_ai.request.temperature, gen_ai.request.top_p, gen_ai.request.max_tokens, gen_ai.request.seed, and at least one gen_ai.usage.* key
    - Test: merge_req_llm_attributes/2 preserves the caller-supplied base attributes (e.g. tenant_id) it is passed as the first argument
  </behavior>
  <action>
    Create `Scoria.Observe.Semconv` as a plain module. Define `@openinference_span_kind_key "openinference.span.kind"` with accessor `openinference_span_kind_key/0`. Define `merge_req_llm_attributes(attributes, metadata)` that pipes `attributes` through `Map.merge(ReqLLM.OpenTelemetry.Attributes.start(metadata))` then `Map.merge(ReqLLM.OpenTelemetry.Attributes.terminal(metadata))` — copy verbatim from RESEARCH Pattern 3, it is real code, not pseudocode. Do NOT hand-declare any `gen_ai.*` constant (RESEARCH Anti-Pattern: those ~20 keys are owned + version-pinned by `req_llm ~> 1.13`; re-declaring risks drift). Moduledoc must (a) state that gen_ai.* key STRINGS are owned/version-pinned by the req_llm dependency at OTel-GenAI schema 1.37.0 (cite deps/req_llm/lib/req_llm/open_telemetry.ex @otel_schema_url), (b) instruct not to hand-duplicate them, (c) note this module owns the one Scoria-written key openinference.span.kind (and reserves feature/route/archetype/intent for Phase 52+). Add `@spec`s per the shapes in RESEARCH Pattern 3.
  </action>
  <verify>
    <automated>mix test test/scoria/observe/semconv_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '"gen_ai\.' lib/scoria/observe/semconv.ex` == 0 (no hand-declared gen_ai.* string literal — delegation only)
    - Source assertion: `grep -c 'ReqLLM.OpenTelemetry.Attributes.start' lib/scoria/observe/semconv.ex` >= 1 and `grep -c 'ReqLLM.OpenTelemetry.Attributes.terminal' lib/scoria/observe/semconv.ex` >= 1
    - Behavior: `Semconv.openinference_span_kind_key()` == `"openinference.span.kind"`
  </acceptance_criteria>
  <done>Module exists; delegates to the req_llm builder; owns the openinference key; no hand-declared gen_ai.* constants; moduledoc pins the version.</done>
</task>

<task type="auto">
  <name>Task 2: Semconv unit test — single-origin + delegation proof</name>
  <files>test/scoria/observe/semconv_test.exs</files>
  <read_first>
    - .planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-RESEARCH.md (§Code Examples — the exact gen_ai.* key set start/1 + terminal/1 produce, and Pitfall 1 for realistic metadata[:model] shape as %LLMDB.Model{})
    - test/scoria/observe/adapters/req_llm_test.exs (existing metadata-map fixture style to mirror)
    - deps/llm_db/lib/llm_db/model.ex (the %LLMDB.Model{} struct shape for a realistic model fixture)
  </read_first>
  <action>
    Create `test/scoria/observe/semconv_test.exs` (`async: true`). Build a realistic metadata map matching a `[:req_llm, :request, :stop]` event: `model:` as an `%LLMDB.Model{}` struct (or a struct-shaped map with an `:id`) NOT a bare string (Pitfall 1), a `request_options` map carrying temperature/top_p/max_tokens/seed, a `usage` map carrying input/output tokens, plus provider/operation. Assert `merge_req_llm_attributes(%{"tenant_id" => "t1"}, metadata)` returns a map that (a) preserves `"tenant_id" => "t1"`, (b) contains `"gen_ai.request.model"`, `"gen_ai.request.temperature"`, `"gen_ai.request.top_p"`, `"gen_ai.request.max_tokens"`, `"gen_ai.request.seed"` TOGETHER, and (c) contains `"gen_ai.usage.input_tokens"`. Assert `openinference_span_kind_key/0` returns the exact string. Add a single-origin guard: `grep`-style source assertion (via `File.read!/1` on the module) that the module body contains no literal `"gen_ai."` string, proving the keys come only from the delegated builder.
  </action>
  <verify>
    <automated>mix test test/scoria/observe/semconv_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Test exits 0; the SPAN-01 key-completeness assertion (all four model-config params + a usage key present together) is exercised here at the Semconv layer, complementing the adapter-layer proof in plan 04
    - The single-origin source assertion fails if a future edit hand-writes a gen_ai.* literal into semconv.ex
  </acceptance_criteria>
  <done>Test green; delegation and single-origin proven with a realistic %LLMDB.Model{}-shaped fixture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| req_llm telemetry metadata -> Semconv.merge_req_llm_attributes/2 | Adapter-supplied metadata is transformed into stored attribute keys; values are scalar/categorical, not free text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-06 | Information Disclosure | Semconv key set (gen_ai.*/openinference.*) | low | accept | All keys produced are scalar/categorical (model id, token counts, temperatures) — none are free-text prompt/completion content; the existing Redactor deny-list (exact-key-match) does not collide with any new dotted key (RESEARCH Security Domain, VERIFIED). No redactor change needed |
| T-51-SC | Tampering | package installs | n/a | accept | No package installs (mix.exs unchanged); Phase 51 consumes only the already-locked req_llm 1.13.0 public API |
</threat_model>

<verification>
- `mix test test/scoria/observe/semconv_test.exs` green
- `mix test test/scoria/observe/` (regression) green
</verification>

<success_criteria>
- Ties to ROADMAP SC#4: one version-pinned mapping module is the origin of every gen_ai.*/openinference.* key. Adapter consumers (plans 04/05) wire through it in Wave 2.
- FOUND-03 complete.
</success_criteria>

<output>
Create `.planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-02-SUMMARY.md` when done
</output>
