---
phase: 51-foundation-fix-key-convention-span-kind-taxonomy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/observe/span_kind.ex
  - lib/scoria_web/components/workflow_tree_component.ex
  - lib/scoria_web/components/trace_tree_component.ex
  - assets/css/04-components.css
  - test/scoria/observe/span_kind_test.exs
  - test/scoria_web/components/trace_tree_component_test.exs
  - test/scoria_web/components/workflow_tree_component_test.exs
autonomous: true
requirements: [FOUND-02, SPAN-02]

must_haves:
  truths:
    - "span_kind is drawn from one shared SpanKind whitelist module consumed by both WorkflowTreeComponent and TraceTreeComponent (no independently-hardcoded lists) — SC#3"
    - "SpanKind.kinds/0 is exactly the 8-value list agent/llm/prompt/tool/mcp/retriever/guardrail/eval; error is removed (it is a status, not a kind) — D-12"
    - "an unrecognized span_kind value coerces to a safe default AND emits an observable [:scoria,:observe,:span_kind,:fallback] telemetry event + Logger.warning (never silent) — D-14/FOUND-01 principle"
    - "every kind k in SpanKind.kinds/0 has a matching .scoria-span--#{k} CSS rail in assets/css/04-components.css — D-15 item 3"
  artifacts:
    - lib/scoria/observe/span_kind.ex
    - test/scoria/observe/span_kind_test.exs
    - "assets/css/04-components.css (.scoria-span--status-error present, .scoria-span--error rail removed)"
  key_links:
    - "both UI components delegate to Scoria.Observe.SpanKind.normalize/2 instead of inline ~w(...) whitelists (anti-inline drift guard enforces this structurally)"
    - "SpanKind.to_openinference/1 is the single seam deriving the UPPERCASE openinference.span.kind portability value from lowercase-native kinds (mcp collapses to TOOL, eval to EVALUATOR)"
---

<objective>
Create the shared `Scoria.Observe.SpanKind` taxonomy module (FOUND-02), route both trace-rendering UI components through it (removing their drifted inline whitelists), repurpose the CSS `--error` rail into a `--status-error` status overlay (D-12), and lock the taxonomy with the mandatory 4-part drift-guard test (D-15).

Purpose: Establish one canonical, version-pinned source of truth for the OpenInference span-kind vocabulary so writers (adapters, Wave 2) and readers (UI components) can never diverge again — fixing the live casing bug where every production span currently renders as generic `agent`.

Output: `SpanKind` module + drift-guard test; both UI components delegating to it; CSS status overlay.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-CONTEXT.md
@.planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-RESEARCH.md
@.planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-PATTERNS.md
</context>

## Artifacts this phase produces (this plan)

- `Scoria.Observe.SpanKind` module with `kinds/0`, `kind?/1`, `normalize/2` (default `"agent"`), `to_openinference/1`
- Telemetry event `[:scoria, :observe, :span_kind, :fallback]` (measurements `%{}`, metadata `%{value: value, default: default}`) — the fallback observability seam (name adopted from RESEARCH Open Question 3; documented in the module moduledoc)
- CSS class `.scoria-span--status-error` (replaces `.scoria-span--error`)
- `test/scoria/observe/span_kind_test.exs` (drift-guard suite)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Scoria.Observe.SpanKind taxonomy module</name>
  <files>lib/scoria/observe/span_kind.ex</files>
  <read_first>
    - .planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-RESEARCH.md (Architecture Patterns §Pattern 2 — the full module is given verbatim; Open Question 3 for the fallback event name)
    - .planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-CONTEXT.md (D-10, D-11, D-14 — casing, the 8 kinds, API surface)
    - .planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-PATTERNS.md (§span_kind.ex — plain-module-with-constants analog + Logger/telemetry conventions)
    - lib/scoria/observe/circuit_breaker.ex (in-repo plain-module shape analog)
  </read_first>
  <behavior>
    - Test: kinds/0 returns exactly ["agent","llm","prompt","tool","mcp","retriever","guardrail","eval"] in that order
    - Test: kind?/1 is true for every canonical kind (any casing) and false for "error", "internal", "chain", "embedding", "reranker", nil
    - Test: normalize/1 downcases + validates ("LLM" -> "llm", "TOOL" -> "tool")
    - Test: normalize/2 on an unrecognized value returns the supplied default and emits [:scoria,:observe,:span_kind,:fallback] with metadata %{value: ..., default: ...} AND logs a warning
    - Test: normalize/1 default fallback is "agent"
    - Test: to_openinference/1 maps agent->AGENT, llm->LLM, prompt->PROMPT, tool->TOOL, mcp->TOOL, retriever->RETRIEVER, guardrail->GUARDRAIL, eval->EVALUATOR (never raises for any kind in kinds/0)
  </behavior>
  <action>
    Create `Scoria.Observe.SpanKind` as a plain compile-time-constant module (NOT `Ecto.Enum` — D-14 rejects it because Ecto.Enum would reject drifted rows on load and bind casing into the schema). Module attributes: `@kinds ~w(agent llm prompt tool mcp retriever guardrail eval)` (canonical order == UI order) and `@openinference_map` with the 8 native->UPPERCASE entries per D-11 (mcp->"TOOL", eval->"EVALUATOR"). Public functions: `kinds/0` returns `@kinds`; `kind?/1` returns `to_string(value) |> String.downcase()` membership in `@kinds`; `normalize/2` as `normalize(value, default \\ "agent")` that downcases+validates and on fallback (a) `Logger.warning` in the terse interpolated-string style used in buffer.ex, (b) emits `[:scoria, :observe, :span_kind, :fallback]` via `:telemetry.execute/3` with metadata `%{value: value, default: default}`, then returns `default`; `to_openinference/1` as `def to_openinference(kind) when kind in @kinds, do: Map.fetch!(@openinference_map, kind)`. `require Logger`. Wrap the fallback `:telemetry.execute/3` defensively (rescue any handler exception back to the normalize return path) so a misbehaving host telemetry handler cannot crash the caller (T-51-01). Moduledoc must pin the OpenInference enum by fetch-date (per RESEARCH — no upstream semver) and name the fallback event so the naming decision is documented in-repo, not just in research.
  </action>
  <verify>
    <automated>mix test test/scoria/observe/span_kind_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'Ecto.Enum' lib/scoria/observe/span_kind.ex` returns 0
    - Behavior: `SpanKind.kinds()` equals the 8-value list; `SpanKind.to_openinference("mcp") == "TOOL"` and `SpanKind.to_openinference("eval") == "EVALUATOR"`
    - Behavior: `SpanKind.normalize("nonsense")` returns `"agent"` and an attached handler on `[:scoria,:observe,:span_kind,:fallback]` receives the event (proven in the drift-guard test, Task 3)
  </acceptance_criteria>
  <done>Module exists with all four public functions; behavior tests (created in Task 3) pass; no Ecto.Enum.</done>
</task>

<task type="auto">
  <name>Task 2: Route both UI components through SpanKind; convert CSS --error rail into a --status-error overlay</name>
  <files>lib/scoria_web/components/workflow_tree_component.ex, lib/scoria_web/components/trace_tree_component.ex, assets/css/04-components.css</files>
  <read_first>
    - .planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-PATTERNS.md (§workflow_tree_component.ex, §trace_tree_component.ex, §04-components.css — exact current line ranges + target shapes)
    - .planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-CONTEXT.md (D-12 error-as-status-overlay, D-15 CSS coherence)
    - lib/scoria_web/components/workflow_tree_component.ex (current inline whitelist at :37-44 — DIFFERENT data source: step vocab approval/handoff/answer)
    - lib/scoria_web/components/trace_tree_component.ex (current inline whitelist at :86-95 — reads real ai_spans.span_kind, still contains stale "error")
    - assets/css/04-components.css (rail block :1082-1091)
  </read_first>
  <action>
    In `trace_tree_component.ex`: replace the whole `span_kind/1` downcase/`case` chain (which reads the real `ai_spans.span_kind`) with a direct delegate — fetch `:span_kind`/`"span_kind"` from the span and pass through `Scoria.Observe.SpanKind.normalize/1`. This drops the stale `error` list entry (D-12) and removes the inline `~w(...)` literal. In `workflow_tree_component.ex`: this component maps a DIFFERENT input (workflow-step `kind`: `approval`/`handoff`/`answer`), so KEEP the three step-vocab mapping clauses (`"approval"->"guardrail"`, `"handoff"->"agent"`, `"answer"->"llm"`) but replace the inline `~w(...)` guard clause and the bare `defp span_kind(_), do: "agent"` fallback with a single trailing `defp span_kind(kind), do: Scoria.Observe.SpanKind.normalize(kind)`. In `04-components.css`: rename `.scoria-span--error .scoria-span__rail { ... }` (line ~1090) to `.scoria-span--status-error` and change its semantics from a rail-color swap to a status OVERLAY — a left-border + alert-icon affordance plus a visually-hidden/aria "errored" cue (NOT color-only, per WCAG dark/light/system, D-12). Leave the 8 kind rails and `.scoria-span--redacted` untouched. Do NOT add a `.scoria-span--error` rail back anywhere.
  </action>
  <verify>
    <automated>mix test test/scoria_web/components/trace_tree_component_test.exs test/scoria_web/components/workflow_tree_component_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: neither component file contains a `~w(` span-kind literal — `grep -E '~w\([^)]*(llm|guardrail|retriever)' lib/scoria_web/components/workflow_tree_component.ex lib/scoria_web/components/trace_tree_component.ex` returns nothing
    - Source assertion: `grep -c 'scoria-span--status-error' assets/css/04-components.css` >= 1 and `grep -c 'scoria-span--error ' assets/css/04-components.css` == 0
    - Source assertion: `grep -c 'SpanKind.normalize' lib/scoria_web/components/trace_tree_component.ex lib/scoria_web/components/workflow_tree_component.ex` >= 2
    - Behavior: component tests render an `llm` span with the `scoria-span--llm` rail class (lowercase-native)
  </acceptance_criteria>
  <done>Both components delegate to SpanKind; CSS ships `--status-error` overlay; the 8 kind rails remain; component tests updated to lowercase-native assertions pass.</done>
</task>

<task type="auto">
  <name>Task 3: SpanKind drift-guard test suite (D-15, FOUND-02 mandatory)</name>
  <files>test/scoria/observe/span_kind_test.exs</files>
  <read_first>
    - .planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-CONTEXT.md (D-15 — the four required assertions)
    - test/scoria/observe/telemetry_test.exs (existing convention for asserting a Scoria telemetry event)
    - lib/scoria_web/components/workflow_tree_component.ex, lib/scoria_web/components/trace_tree_component.ex (the anti-inline grep targets, updated in Task 2)
    - assets/css/04-components.css (the CSS-coherence grep target)
  </read_first>
  <action>
    Create `test/scoria/observe/span_kind_test.exs` with the four D-15 assertions plus the Task 1 behavior tests: (1) CANARY — `assert SpanKind.kinds() == ~w(agent llm prompt tool mcp retriever guardrail eval)` (forces reviewer to touch CSS + OI map on any change); (2) EXHAUSTIVENESS — for every `k <- SpanKind.kinds()`, assert `SpanKind.kind?(k)` and that `SpanKind.to_openinference(k)` returns a non-nil UPPERCASE string without raising; (3) CSS COHERENCE — read `assets/css/04-components.css` via `File.read!/1` and assert `"scoria-span--#{k}"` appears for every `k <- SpanKind.kinds()`; also assert `"scoria-span--error "` (rail form) is absent and `"scoria-span--status-error"` is present; (4) ANTI-INLINE GUARD — read both component source files via `File.read!/1` and assert no residual span-kind `~w(...)` whitelist literal remains (match the guardrail/retriever token set, mirroring the `single_header_guard_test.exs` source-scan convention). Add the fallback-observability test: attach a handler to `[:scoria,:observe,:span_kind,:fallback]`, call `SpanKind.normalize("bogus")`, `assert_receive` the event with `%{value: "bogus"}` metadata, and assert the return is `"agent"`. Use `async: true` (pure source-scan + pure functions, no DB).
  </action>
  <verify>
    <automated>mix test test/scoria/observe/span_kind_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Test command exits 0 with the canary, exhaustiveness, CSS-coherence, anti-inline, and fallback-telemetry cases all present and green
    - Behavior: mutating the CSS to drop any `scoria-span--<kind>` rail, or re-adding an inline `~w(...)` list to a component, makes this suite fail (the guard is real, not decorative)
  </acceptance_criteria>
  <done>Drift-guard suite green; all four D-15 assertions + fallback telemetry present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host/adapter-supplied span_kind value -> SpanKind.normalize/2 | Untrusted categorical input crosses into Scoria's stored taxonomy; normalize is the validation boundary |
| Scoria fallback telemetry emit -> host-attached handler | A host handler on the fallback event runs synchronously in the caller process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-01 | Denial of Service | SpanKind.normalize/2 fallback `:telemetry.execute` | low | mitigate | Defensively wrap the fallback emit so a raising host handler cannot crash the normalize caller or re-enter the write path (D-09 iv discipline applied to this seam) |
| T-51-05 | Tampering (input validation) | SpanKind.normalize/2 | low | mitigate | normalize fails closed to a safe default ("agent") rather than raising/persisting an unlisted kind (ASVS V5); never inferred from action names (host-declares doctrine) |
| T-51-SC | Tampering | package installs | n/a | accept | No package installs this plan (mix.exs unchanged) — legitimacy gate not triggered |
</threat_model>

<verification>
- `mix test test/scoria/observe/span_kind_test.exs test/scoria_web/components/trace_tree_component_test.exs test/scoria_web/components/workflow_tree_component_test.exs` green
- `mix test test/scoria/observe/` (regression) green
- Anti-inline grep guards return zero residual whitelists
</verification>

<success_criteria>
- Ties to ROADMAP SC#3: span_kind drawn from one shared whitelist consumed by both components, mirrored openinference.span.kind derivable via `to_openinference/1`, mcp->TOOL. (The adapter write-side of SC#3 lands in Wave 2 plans 04/05.)
- FOUND-02 complete: shared module + mandatory drift-guard test.
</success_criteria>

<output>
Create `.planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-01-SUMMARY.md` when done
</output>
