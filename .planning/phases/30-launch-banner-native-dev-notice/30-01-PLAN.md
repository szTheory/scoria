---
phase: 30-launch-banner-native-dev-notice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - docker/dev-entrypoint.sh
autonomous: true
requirements:
  - DXCLI-05
must_haves:
  truths:
    - "Running `make dev` prints `==> Scoria dev (native) → http://localhost:4799/scoria` (honoring $PORT) before Phoenix boot noise"
    - "`make dev PORT=5000` prints the matching URL with `:5000`"
    - "The Docker banner includes the bare Traefik admin link `http://localhost:8080` on its own line"
    - "The Docker banner includes a `Native dev server: make dev → http://localhost:4799/scoria` notice on its own line"
    - "The Docker banner route list is derived live from the router and lists exactly the 9 literal GET /scoria paths (incl. /scoria/datasets), one per line, no `:` param routes"
    - "The entrypoint still prints its banner and boots even if the route derivation fails (no `set -e` abort)"
  artifacts:
    - path: "Makefile"
      provides: "`@echo` native startup URL line in the `dev:` recipe interpolating $(PORT)"
      contains: "http://localhost:$(PORT)/scoria"
    - path: "docker/dev-entrypoint.sh"
      provides: "router-derived /scoria route list + Traefik admin link + native-dev notice in the banner heredoc; boot-safe derivation"
      contains: "mix phx.routes ScoriaWeb.DevRouter"
  key_links:
    - from: "docker/dev-entrypoint.sh"
      to: "lib/scoria_web/router.ex"
      via: "mix phx.routes ScoriaWeb.DevRouter filtered to literal GET /scoria paths — banner reflects router, never a hand-maintained list"
      pattern: "mix phx.routes ScoriaWeb.DevRouter"
    - from: "Makefile dev: recipe"
      to: "Makefile PORT ?= 4799"
      via: "$(PORT) interpolation in the @echo URL — single source of truth for the native port"
      pattern: "http://localhost:\\$\\(PORT\\)/scoria"
---

<objective>
Deliver the three DXCLI-05 launch-banner pieces so starting the dev server (native or Docker) immediately shows the operator where to go — no "where do I poke around?" puzzle.

This phase edits exactly two files:
1. `Makefile` `dev:` recipe — add one `@echo` startup line printing the populated `/scoria` URL honoring `$(PORT)` (success criterion 1; D-01/D-02/D-03/D-04).
2. `docker/dev-entrypoint.sh` banner — derive the `/scoria` route list live from the router (replacing the stale hand-maintained `Screens:` block that drifted to omit `/datasets`), add the bare Traefik admin link, and add the native-dev notice — all on distinct copy-pasteable lines, with boot-safe derivation (success criteria 2 & 3; D-05..D-12).

Purpose: The banner cannot lie — every advertised element is derived from a single source of truth (router for routes, Makefile `$(PORT)` for the native port, `docker/traefik/compose.yml` for `:8080`), encoding Phase 29's drift-resistance DNA.

Output: One `@echo` line in the Makefile; a router-derived route block + Traefik link + native notice in the Docker entrypoint banner. No new source modules. The route derivation is left testable as a seam for Phase 34's banner↔router parity contract test (do NOT build that test here).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-launch-banner-native-dev-notice/30-CONTEXT.md
@.planning/phases/30-launch-banner-native-dev-notice/30-RESEARCH.md
@.planning/phases/30-launch-banner-native-dev-notice/30-VALIDATION.md
@.planning/phases/29-makefile-hardening/29-CONTEXT.md
</context>

<artifacts_produced>
## Artifacts this phase produces

No new source modules are created. This phase changes text in two existing files:

- **`Makefile` (`dev:` recipe, ~L89–91):** a new `@echo "==> Scoria dev (native) → http://localhost:$(PORT)/scoria"` line, placed BEFORE the existing `SCORIA_DEV_LIVE_RELOAD=1 PORT=$(PORT) mix phx.server` exec line.
- **`docker/dev-entrypoint.sh`:**
  - a new pre-heredoc `ROUTES` shell variable holding the router-derived `/scoria` route list (computed via `mix phx.routes ScoriaWeb.DevRouter`, filtered, boot-safe with `|| true` + empty-check fallback).
  - inside the existing `cat <<BANNER … BANNER` heredoc: the stale hand-maintained `Screens:` block REPLACED by a `Key routes (derived live from the router):` block interpolating `${ROUTES}`.
  - a new Traefik admin link line: bare `http://localhost:8080` on its own line.
  - a new native-dev notice line: `Native dev server: make dev → http://localhost:4799/scoria` on its own line.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add the native startup URL line to the Makefile `dev:` recipe</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (~L33 `PORT ?= 4799`; ~L89–91 the `dev:` recipe — the file being edited)
    - .planning/phases/30-launch-banner-native-dev-notice/30-RESEARCH.md ("Makefile `dev:` recipe pattern" / "Code Examples" — verified recipe shape)
    - .planning/phases/29-makefile-hardening/29-CONTEXT.md (D-07/D-08: `PORT ?= 4799` is the Makefile-only single source of truth; do NOT touch config/dev.exs)
  </read_first>
  <action>
    Add exactly ONE `@echo` line to the `dev:` recipe, placed immediately BEFORE the existing `SCORIA_DEV_LIVE_RELOAD=1 PORT=$(PORT) mix phx.server` exec line (D-01: exactly one line before the server boots). The line prints the populated native URL: `==> Scoria dev (native) → http://localhost:$(PORT)/scoria`.

    Use `localhost` (not `127.0.0.1`) per D-03. Interpolate `$(PORT)` (already in scope from `PORT ?= 4799` at ~L33) per D-02 so `make dev PORT=5000` echoes `:5000`. Use the `@` prefix so Make does not echo the `echo` command itself — only the URL line prints. Use the `→` (U+2192) arrow to match the user-endorsed shape in D-01 (RESEARCH Open Question 2 confirms it renders fine on UTF-8 terminals).

    Do NOT add the route list to this line (D-04: routes live in the Docker banner only). Do NOT modify `config/dev.exs` or any container `:4000` wiring (Phase 29 D-08/D-10). Do NOT change `PORT ?= 4799` or any other recipe.

    Because each Make recipe line runs in its own shell, the `@echo` runs and completes before `mix phx.server` is exec'd, guaranteeing the URL prints above Phoenix's boot logs (satisfies success criterion 1: visible without scrolling past server log noise).
  </action>
  <verify>
    <automated>make -n dev | grep -F 'echo "==> Scoria dev (native) → http://localhost:$(PORT)/scoria"' && make -n dev PORT=5000 | grep -F 'http://localhost:5000/scoria'</automated>
  </verify>
  <acceptance_criteria>
    - `make -n dev` shows the `@echo` recipe line interpolating to `http://localhost:4799/scoria` (default PORT).
    - `make -n dev PORT=5000` shows the URL as `http://localhost:5000/scoria` (PORT override honored — D-02).
    - The `@echo` line appears BEFORE the `mix phx.server` line in the recipe (D-01).
    - URL uses `localhost`, not `127.0.0.1` (D-03).
    - No route list on the native line (D-04); `config/dev.exs` and `PORT ?= 4799` unchanged.
  </acceptance_criteria>
  <done>`make -n dev` and `make -n dev PORT=5000` both emit the correct populated `/scoria` URL line before the server exec; no other Makefile changes.</done>
</task>

<task type="auto">
  <name>Task 2: Add router-derived route list, Traefik link, and native notice to the Docker banner (boot-safe)</name>
  <files>docker/dev-entrypoint.sh</files>
  <read_first>
    - docker/dev-entrypoint.sh (the file being edited — note `set -euo pipefail` at L4, the unquoted `cat <<BANNER` heredoc at L15, the stale `Screens:` block at L28–37, and the `mix dev.setup` at L10 that compiles the app before the banner)
    - lib/scoria_web/router.ex (SOURCE OF TRUTH for the derived route list — read but DO NOT edit; the `scoria_dashboard` macro's `live_session :scoria_dashboard` block defines the 12 routes the filter reduces to 9 literal GET paths)
    - .planning/phases/30-launch-banner-native-dev-notice/30-RESEARCH.md ("VERIFIED: the filter pipeline and its expected output", "Banner integration: compute-then-interpolate", "Boot-safety detail", "Common Pitfalls P1–P5" — copy-paste-ready, verified pipeline)
    - .planning/phases/29-makefile-hardening/29-CONTEXT.md (drift-resistance convention; awk-alignment idiom — paths-only needs none, but referenced for consistency)
  </read_first>
  <action>
    Make three banner changes in `docker/dev-entrypoint.sh`, keeping `set -euo pipefail` intact and the existing Open/demo-data/screenshot-harness sections.

    (1) ROUTE DERIVATION (D-05/D-07/D-09 — boot-safe). BEFORE the `cat <<BANNER` heredoc (after the existing `HOST`/`INSTANCE` assignments), compute a `ROUTES` shell variable from the live router:
    - Run `mix phx.routes ScoriaWeb.DevRouter` — the DevRouter argument is MANDATORY (RESEARCH Pitfall P2: bare `mix phx.routes` crashes with UndefinedFunctionError because it resolves to the macro-only `ScoriaWeb.Router`). CONTEXT D-09 said "mix phx.routes" generically — use the DevRouter-qualified form.
    - Pipe through `2>/dev/null` (isolates the route table on stdout from compile noise on stderr — Pitfall P5), then `awk '$2 == "GET" && $3 ~ /^\/scoria/ && $3 !~ /:/ { print $3 }'`, then `sort -u`, then `sed 's/^/    /'` (4-space indent to match the existing banner indentation). Anchor the filter on the verb column ($2) and path column ($3) — NEVER assume col 1 is blank (Pitfall P1: `as: false` does NOT blank the route-helper column; col 1 holds helper names). The `$3 !~ /:/` predicate drops every parameterized/non-pokeable route (D-07: `/workflows/:id`, `/prompts/:id/release`, `/coming/:screen`, the two `/connectors/:connector_id/auth/*` rows).
    - Suffix the entire assignment with `|| true` and follow with an empty-check fallback so the banner ALWAYS prints even if derivation fails (D-09, Pitfall P3). On empty `$ROUTES`, set a static fallback string such as `    (route list unavailable — open http://${HOST}/scoria)`. Scope the `|| true` tolerance to this one assignment only — do NOT weaken `set -e` globally (it must keep protecting `mix deps.get` / `mix dev.setup`). `set -u` is satisfied because the `|| true` guarantees `ROUTES` is always assigned.
    - Render paths-only (D-08/D-06: single column-aligned flat list, one path per line, no hand-maintained label map — a label map is exactly the drift D-05 kills; RESEARCH recommends paths-only). Do NOT reintroduce friendly labels.

    (2) REPLACE THE STALE `Screens:` BLOCK (D-05). Inside the unquoted `<<BANNER` heredoc, delete the entire hand-maintained `Screens:` block (the heading plus all `/scoria…` label lines) and replace it with a `Key routes (derived live from the router):` heading followed by `${ROUTES}` interpolated on its own line. Keep the heredoc delimiter UNQUOTED (`<<BANNER`, not `<<'BANNER'`) so `${ROUTES}`/`${HOST}`/`${INSTANCE}` expand (Pitfall P4). The stale block omitted `/scoria/datasets`; the derived block MUST include it (proves D-05's premise).

    (3) ADD THE TWO NEW DISTINCT LINES (D-10/D-11/D-12). Inside the heredoc, on their own distinct copy-pasteable lines (near the Open/Traefik area):
    - Traefik admin link as the BARE `http://localhost:8080` (D-10 — Traefik auto-redirects `/` → `/dashboard/`; matches the criterion text literally).
    - Native-dev notice: `Native dev server: make dev → http://localhost:4799/scoria` (D-11 — 4799 is hardcoded here ON PURPOSE; it states the native default policy mirroring the Makefile `PORT ?= 4799`. The container still listens on `:4000` — do NOT change any container `:4000` wiring or conflate the two).

    Do NOT introduce category headers or a nav-group taxonomy (D-06, Deferred Ideas). Do NOT edit `lib/scoria_web/router.ex`, `config/dev.exs`, `compose.yml`, or `docker/traefik/compose.yml`. Keep the route derivation a clean, single-pipeline seam (Phase 34 will add the parity contract test — leave the seam, do NOT build that test here).
  </action>
  <verify>
    <automated>shellcheck docker/dev-entrypoint.sh && bash -n docker/dev-entrypoint.sh && grep -F 'mix phx.routes ScoriaWeb.DevRouter' docker/dev-entrypoint.sh && grep -F 'http://localhost:8080' docker/dev-entrypoint.sh && grep -F 'Native dev server' docker/dev-entrypoint.sh && grep -q '|| true' docker/dev-entrypoint.sh && ! grep -q '^  Screens:' docker/dev-entrypoint.sh</automated>
  </verify>
  <acceptance_criteria>
    - `shellcheck docker/dev-entrypoint.sh` is clean (no warnings/errors) and `bash -n docker/dev-entrypoint.sh` passes (VALIDATION boot-safety + Wave 0).
    - The script contains `mix phx.routes ScoriaWeb.DevRouter` (DevRouter-qualified; D-05/D-09).
    - The filter pipeline `mix phx.routes ScoriaWeb.DevRouter 2>/dev/null | awk '$2 == "GET" && $3 ~ /^\/scoria/ && $3 !~ /:/ { print $3 }' | sort -u` returns exactly the 9 literal GET `/scoria` paths including `/scoria/datasets`, with no `:`-param routes (RESEARCH verified set; this is the Phase-34 seam).
    - The route derivation assignment is suffixed with `|| true` and followed by an empty-check fallback so the banner still prints on failure (D-09); `set -euo pipefail` remains at the top of the script unchanged.
    - The heredoc delimiter remains unquoted (`<<BANNER`) so `${ROUTES}` expands (Pitfall P4).
    - `grep -F 'http://localhost:8080' docker/dev-entrypoint.sh` matches on its own line (D-10).
    - `grep -F 'Native dev server' docker/dev-entrypoint.sh` matches; the line reads `Native dev server: make dev → http://localhost:4799/scoria` (D-11).
    - The stale hand-maintained `Screens:` block is gone (`grep -q '^  Screens:'` returns no match); replaced by the derived `Key routes` block including `/scoria/datasets` (D-05).
    - `lib/scoria_web/router.ex`, `config/dev.exs`, `compose.yml`, `docker/traefik/compose.yml` unchanged.
  </acceptance_criteria>
  <done>Banner derives the 9-path `/scoria` route list live from `ScoriaWeb.DevRouter` (incl. `/scoria/datasets`), prints the bare Traefik link and the native-dev notice on distinct lines, is boot-safe (`|| true` + fallback under `set -euo pipefail`), shellcheck-clean, and the stale `Screens:` block is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This phase adds dev-only banner/Makefile text. No untrusted input crosses any new boundary; no auth surface; no network-exposed code path changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information Disclosure | Banner advertises bare `http://localhost:8080` Traefik dashboard (insecure, no auth) | accept | Dev-only; Traefik dashboard is bound to `127.0.0.1` (`docker/traefik/compose.yml:31`) and `make proxy` already echoes the same link. Established pattern; out of scope to change. No new exposure. |
| T-30-02 | Denial of Service | Route-derivation pipeline (`mix phx.routes`) runs at container boot under `set -euo pipefail` — a routes hiccup could abort boot | mitigate | Wrap the derivation assignment with `|| true` + empty-check fallback (D-09, Task 2) so the banner always prints and boot proceeds; `2>/dev/null` isolates compile noise. The input is `mix phx.routes` output on the already-trusted local app — no untrusted/external input. |

No new packages installed (no Package Legitimacy Gate needed). No new attack surface introduced.
</threat_model>

<verification>
## Phase verification (run before /gsd-verify-work)

- `make -n dev` shows the `@echo` native URL line interpolating `$(PORT)`; `make -n dev PORT=5000` shows `:5000` (criterion 1; DXCLI-05 native line).
- `mix phx.routes ScoriaWeb.DevRouter 2>/dev/null | awk '$2=="GET" && $3 ~ /^\/scoria/ && $3 !~ /:/ {print $3}' | sort -u` returns exactly the 9-path set incl. `/scoria/datasets` (criterion 2; DXCLI-05 route list — the Phase-34 seam).
- `grep -F 'http://localhost:8080' docker/dev-entrypoint.sh` matches (criterion 2; Traefik link).
- `grep -F 'Native dev server' docker/dev-entrypoint.sh` matches (criterion 3; native notice).
- `shellcheck docker/dev-entrypoint.sh` clean; `|| true` + empty-check fallback present; `set -euo pipefail` intact (boot-safety).
- `mix test` shows no regression.
- Manual (documented in 30-VALIDATION.md): `make dev` prints the URL above Phoenix boot noise; `docker compose up` renders the three banner elements on distinct copy-pasteable lines.
</verification>

<success_criteria>
- [ ] `make dev` prints `http://localhost:4799/scoria` (or active `$PORT`) before the server starts, without scrolling past server log noise (ROADMAP criterion 1).
- [ ] Docker banner includes the Traefik admin link `http://localhost:8080` and a grouped key-route list covering the `/scoria` screens, all on distinct copy-pasteable lines (ROADMAP criterion 2).
- [ ] Banner contains the `Native dev server: make dev → http://localhost:4799/scoria` notice (ROADMAP criterion 3).
- [ ] Route list derived live from `ScoriaWeb.DevRouter` (9 paths incl. `/scoria/datasets`), boot-safe, shellcheck-clean; stale `Screens:` block removed.
- [ ] No edits to `lib/scoria_web/router.ex`, `config/dev.exs`, or container `:4000` wiring.
</success_criteria>

<output>
Create `.planning/phases/30-launch-banner-native-dev-notice/30-01-SUMMARY.md` when done.
</output>
