---
phase: 64-adoption-lane-discoverability-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/mix/tasks/test.adoption_test.exs
autonomous: true
requirements: [INST-08]
must_haves:
  truths:
    - "Discoverability meta-test expected_files equals Mix.Tasks.Scoria.Test.Adoption.adoption_test_files/0."
    - "Isolation run mix test test/mix/tasks/test.adoption_test.exs passes."
    - "Runtime adoption lane mix test.adoption remains green."
  artifacts:
    - path: "test/mix/tasks/test.adoption_test.exs"
      provides: "Adoption lane discoverability contract aligned with SSOT"
      contains: "test/scoria/install/report_test.exs"
  key_links:
    - from: "test/mix/tasks/test.adoption_test.exs"
      to: "lib/mix/tasks/test.adoption.ex"
      via: "expected_files mirrors @adoption_test_files order"
      pattern: "adoption_test_files()"
---

<objective>
Sync adoption lane discoverability `expected_files` with the live `adoption_test_files/0` SSOT so isolation runs match runtime lane behavior.

Purpose: Close v2.5 audit gap `adoption-lane-discoverability-drift` (INST-08 integration hardening — discoverability parity).
Output: Green `test.adoption_test.exs` in isolation; unchanged lane runtime behavior.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v2.5-MILESTONE-AUDIT.md
@.planning/phases/64-adoption-lane-discoverability-sync/64-RESEARCH.md
@.planning/phases/64-adoption-lane-discoverability-sync/64-PATTERNS.md
@.planning/phases/64-adoption-lane-discoverability-sync/64-VALIDATION.md
@lib/mix/tasks/test.adoption.ex
@test/mix/tasks/test.adoption_test.exs
@test/mix/tasks/test.semantic_fast_path_test.exs

Do not modify `lib/mix/tasks/test.adoption.ex` — SSOT is already correct. Do not add or remove lane files.
</context>

<tasks>

<task type="auto">
  <name>Task 64-01-01: Sync adoption discoverability expected_files with SSOT</name>
  <requirement_ids>INST-08</requirement_ids>
  <files>test/mix/tasks/test.adoption_test.exs</files>
  <read_first>
    lib/mix/tasks/test.adoption.ex
    test/mix/tasks/test.adoption_test.exs
    test/mix/tasks/test.semantic_fast_path_test.exs
    .planning/phases/64-adoption-lane-discoverability-sync/64-RESEARCH.md
  </read_first>
  <action>
In `test/mix/tasks/test.adoption_test.exs`, update the `expected_files` list so it matches `@adoption_test_files` in `lib/mix/tasks/test.adoption.ex` exactly (same strings, same order).

After `"test/scoria/install/planner_test.exs",` insert:
- `"test/scoria/install/report_test.exs",`
- `"test/scoria/install/mode_equivalence_test.exs",`

before `"test/scoria/bootstrap/migration_lane_compatibility_test.exs"`.

Do not change any other assertions (`VerificationLanes` prerequisites/exclusions, `refute` knowledge/semantic_cache lines, `Mix.Task.get` checks).
  </action>
  <acceptance_criteria>
    - `rg 'report_test.exs' test/mix/tasks/test.adoption_test.exs` returns at least one match inside `expected_files`.
    - `rg 'mode_equivalence_test.exs' test/mix/tasks/test.adoption_test.exs` returns at least one match inside `expected_files`.
    - `MIX_ENV=test mix test test/mix/tasks/test.adoption_test.exs` exits 0.
    - `MIX_ENV=test mix test.adoption` exits 0.
    - `MIX_ENV=test mix test test/scoria/verification_lanes_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/mix/tasks/test.adoption_test.exs</automated>
  </verify>
  <done>Discoverability test passes in isolation; adoption lane and verification_lanes guardrails remain green.</done>
</task>

</tasks>

<threat_model>
## Security Gate

- Enforcement: enabled
- ASVS level: 1
- Block threshold: high

| ID | Threat | Severity | Mitigation in plan |
|----|--------|----------|-------------------|
| T-64-01 | Weakening lane boundary by adding non-adoption tests to expected list | low | Only add files already in `@adoption_test_files` SSOT |
| T-64-02 | Removing exclusion refutes hides knowledge-lane bleed | low | Do not modify refute assertions |

No high-severity threats. Plan may proceed.
</threat_model>

<verification>
## Plan Verification

- [ ] `expected_files` includes `report_test.exs` and `mode_equivalence_test.exs` in SSOT order
- [ ] `mix test test/mix/tasks/test.adoption_test.exs` green in isolation
- [ ] `mix test.adoption` green
- [ ] `verification_lanes_test.exs` green
</verification>

<success_criteria>
1. ROADMAP success criteria 1–3 satisfied
2. v2.5 audit gap `adoption-lane-discoverability-drift` closed
</success_criteria>

## PLANNING COMPLETE
