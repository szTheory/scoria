---
phase: 20-logo-convergence-variant-set
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/tools/converge.mjs
  - brandbook/tools/lib/root-variants.mjs
  - brandbook/logo-primary.svg
  - brandbook/logo-primary-light.svg
  - brandbook/logo-mark.svg
  - brandbook/logo-monochrome.svg
  - brandbook/logo-lockup-subtitle.svg
  - brandbook/logotype-integrated.svg
  - brandbook/favicon.svg
  - brandbook/social-card.svg
  - brandbook/tools/variant-spec.md
  - brandbook/tools/verify-logos.mjs
  - brandbook/tools/package.json
autonomous: true
requirements: [BRAND-04]

must_haves:
  truths:
    - "All 8 root variant SVGs exist at brandbook/ root with the exact census filenames"
    - "Every root logo SVG (except social-card's documented card-ground rect) has no <rect>, uses fill-rule=evenodd, fills only (no active strokes), tight viewBox, coords ≤2 decimals"
    - "favicon.svg renders the TV-1 holes16 3-hole simplified path, pixel-grid snapped, ≤1KB on disk"
    - "logo-monochrome.svg and logotype-integrated.svg use currentColor with holes intact"
    - "node brandbook/tools/verify-logos.mjs exits 0 with the new ROOT-* checks covering all 8 root files"
    - "An optical-correction pass is documented (single-vs-two-tone call, ink-density numbers, overshoot/spacing notes) in variant-spec.md"
    - "Clear-space (cap-height/2) and min-size rules are written in variant-spec.md"
    - "Losing candidates and both gallery HTML files are deleted; candidates/ retains only TV-1 lineage + LK-B files"
    - "du -sk brandbook/ (excluding node_modules) lands well under 500KB"
  artifacts:
    - path: "brandbook/logo-primary.svg"
      provides: "LK-B fused lockup, dark-surface colorway"
      contains: "fill-rule=\"evenodd\""
    - path: "brandbook/logo-primary-light.svg"
      provides: "LK-B fused lockup, light-surface colorway"
      contains: "fill-rule=\"evenodd\""
    - path: "brandbook/logo-mark.svg"
      provides: "TV-1 mark alone"
      contains: "role=\"img\""
    - path: "brandbook/logo-monochrome.svg"
      provides: "LK-B fused lockup, single currentColor fill"
      contains: "currentColor"
    - path: "brandbook/logo-lockup-subtitle.svg"
      provides: "LK-B + tagline subtitle (the only subtitle variant)"
      contains: "AI ops for Phoenix apps."
    - path: "brandbook/logotype-integrated.svg"
      provides: "Fused wordmark single-color (canonical typemark deliverable)"
      contains: "currentColor"
    - path: "brandbook/favicon.svg"
      provides: "TV-1 holes16 3-hole simplified path, ≤1KB"
      contains: "fill-rule=\"evenodd\""
    - path: "brandbook/social-card.svg"
      provides: "1280×640 bounded card artwork, LK-B lockup + tagline"
      contains: "1280"
    - path: "brandbook/tools/variant-spec.md"
      provides: "Clear-space/min-size spec + documented optical-correction pass"
      contains: "Clear space"
  key_links:
    - from: "brandbook/tools/converge.mjs"
      to: "brandbook/tools/lib/root-variants.mjs"
      via: "import of root-variant composers"
      pattern: "import.*root-variants"
    - from: "brandbook/tools/lib/root-variants.mjs"
      to: "brandbook/tools/presets.mjs"
      via: "TV-1 / holes16 lineage source"
      pattern: "presets|TV_1"
    - from: "brandbook/tools/verify-logos.mjs"
      to: "brandbook/ root SVGs"
      via: "ROOT-* structural checks over the 8 files"
      pattern: "ROOT-"
---

<objective>
Graduate the locked TV-1 "Span rail" mark + LK-B "Mark-as-o" fused lockup into the complete 8-variant committed set at `brandbook/` ROOT, per the 20-CONTEXT census table. Apply a documented manual optical-correction pass, write the clear-space/min-size spec, extend `verify-logos.mjs` to gate the root variants (with the documented social-card rect exemption), and prune all losing candidates + both gallery HTML files.

Purpose: BRAND-04 — the chosen direction becomes the canonical logo system every later phase (21 brand book, 22 integration) consumes. The mark↔wordmark fusion artwork already exists in `candidates/LK-B-lockup.svg` and `candidates/TV-1-mark.svg`; this plan recolors, derives, optically tunes, and canonicalizes it — it does NOT redraw geometry (geometry is frozen at gate #2).

Output: 8 root SVGs, `variant-spec.md`, an extended root-aware verifier, a pruned `candidates/` + deleted galleries, and a brandbook size well under 500KB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-logo-convergence-variant-set/20-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# The locked artwork (already on disk — recolor/derive, do not redraw geometry):
@brandbook/tools/candidates/LK-B-lockup.svg
@brandbook/tools/candidates/TV-1-mark.svg
@brandbook/tools/candidates/TV-1-fav.svg

# Toolchain shape + craft numbers:
@brandbook/tools/presets.mjs
@brandbook/tools/verify-logos.mjs
@brandbook/tools/generate-round2.mjs
@.planning/phases/19-logo-divergence-user-choice/19-03-SUMMARY.md

<interfaces>
<!-- Contracts the executor uses directly — no codebase exploration needed. -->

The fused LK-B lockup (candidates/LK-B-lockup.svg) is ALREADY the complete
mark-as-o artwork. Its root <svg> carries:
  viewBox="2.9 -75.6 284 77.8"  role="img"  data-variant="LK-B"  data-integrated="true"
It contains TWO <path> elements both fill="#E65A32":
  (1) the "Sc...ria" letterform path (the wordmark MINUS its 'o'),
  (2) a <g transform="translate(141.57,-23.23) scale(0.58)"> wrapping the TV-1
      mark path (which sits AS the 'o', x-height sized, holes = counter).
The mark sub-path's first interior hole (the root vesicle) was optically
micro-tuned in 19-02b: `M-3,-22C-3,-29.18...` (root r≈19 vs the standalone
TV-1 mark's r≈13) so the mark's ink density 0.61 ≈ the letters' 0.57.

TV-1 mark alone (candidates/TV-1-mark.svg): single evenodd compound path,
viewBox="-53.32 -52.98 99.66 96.07", fill="#E65A32", role="img" + <title>.

TV-1 favicon source (candidates/TV-1-fav.svg): the holes16 3-hole
simplification, single evenodd path, same viewBox as the mark.

presets.mjs exports TV_1 (with .holes16 = 3 holes) and PALETTE
{ ember500:#E65A32, molten400:#FF7A4D, scoria600:#B94F31 }.

verify-logos.mjs reads candidates/ + options-gallery.html today. It exposes
pass()/fail()/check(id,cond,passMsg,failMsg) helpers and exits non-zero on any
failure. Extend it with ROOT-* checks over brandbook/*.svg.

Census colorways (20-CONTEXT):
  Dark surface  : letters White-Hot #FFF9F3 ; mark-'o' MAY be Ember-500 #E65A32
  Light surface : letters Basalt-950 #11100F (or Scoria-900) ; mark-'o' Scoria-600 #B94F31 if two-tone
  Social-card ground: Basalt-950 #11100F (the ONLY allowed root rect — documented exemption)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root-variant composers + converge.mjs → emit the 8 root SVGs</name>
  <files>brandbook/tools/lib/root-variants.mjs, brandbook/tools/converge.mjs, brandbook/tools/package.json, brandbook/logo-primary.svg, brandbook/logo-primary-light.svg, brandbook/logo-mark.svg, brandbook/logo-monochrome.svg, brandbook/logo-lockup-subtitle.svg, brandbook/logotype-integrated.svg, brandbook/favicon.svg, brandbook/social-card.svg</files>
  <read_first>brandbook/tools/candidates/LK-B-lockup.svg (the fused source — copy its two paths verbatim, only recolor), brandbook/tools/candidates/TV-1-mark.svg, brandbook/tools/candidates/TV-1-fav.svg, brandbook/tools/presets.mjs (PALETTE + TV_1.holes16), brandbook/tools/generate-round2.mjs (writer pattern), .planning/phases/20-logo-convergence-variant-set/20-CONTEXT.md (census table §decisions), brandbook/pressure-test.md §SECTION 8 (LOGO-01..07)</read_first>
  <action>
Create `lib/root-variants.mjs` exporting one composer per root variant, and `converge.mjs` that writes all 8 files to `brandbook/` root (mirror the writer loop in generate-round2.mjs: array of [filename, builderFn], writeFileSync each + '\n', print a count, exit 0). Geometry is FROZEN — reuse the exact path `d` strings from the LK-B lockup, TV-1 mark, and TV-1 fav candidates; the only per-variant changes are fill colors, currentColor substitution, viewBox tightening, the favicon pixel-snap, and (for subtitle/social) added text. Do NOT regenerate or perturb any path coordinates.

The 8 builders, per the 20-CONTEXT census (use exact hexes):
1. `logo-primary.svg` — LK-B fused lockup, dark colorway. Letters White-Hot #FFF9F3. Mark-'o': per audit §8 "reserve ember 'o' for primary", make the mark sub-path Ember-500 #E65A32 (TWO-TONE — this is the audit's lean; if Task 2's ink-density review overrides to single-tone, that call is documented there and applied). Keep role="img" + <title>, tight viewBox, data-variant="LK-B" data-integrated="true".
2. `logo-primary-light.svg` — same lockup geometry, light colorway. Letters Basalt-950 #11100F. Mark-'o' Scoria-600 #B94F31 if two-tone (match the primary's single-vs-two-tone decision from Task 2).
3. `logo-mark.svg` — TV-1 mark alone (the standalone TV-1-mark.svg path). Fill Ember-500 #E65A32 as the default brand colorway. Keep role="img", <title> "Scoria mark", <desc> optional — match Threadline mark conventions (title/desc, role="img").
4. `logo-monochrome.svg` — LK-B fused lockup, BOTH paths a single `fill="currentColor"`, fill-rule="evenodd" preserved so the mark holes stay punched. One color only (no two-tone).
5. `logo-lockup-subtitle.svg` — LK-B lockup + "AI ops for Phoenix apps." set BENEATH in IBM Plex Sans. Outline the tagline to paths via the existing opentype.js wordmark lib (wordmark.mjs / lib pattern) OR, if outlining the tagline is out of budget, emit a <text> with an explicit system-font fallback stack and a comment declaring the fallback — but PREFER outlined paths (this is the ONLY variant permitted to carry the tagline; smaller size, open tracking). This is the only root SVG with subtitle/tagline text.
6. `logotype-integrated.svg` — the fused wordmark in single-color form (currentColor). Since LK-B IS the integrated logotype, this is the canonical "typemark" deliverable. Document in variant-spec (Task 2) that primary and integrated share lineage; per Claude's-discretion it MAY be byte-identical to logo-monochrome or carry its own tuning — make the call and note it.
7. `favicon.svg` — the TV-1 `holes16` 3-hole simplified path (from TV-1-fav.svg). Pixel-grid snap: round outer-silhouette and hole coordinates to even integers on a 16-unit grid (re-emit the path from TV_1.holes16 via geometry, or snap the existing TV-1-fav path's hole centers/radii to even values), verify each hole ≥1.5px radius at 16px render. Fill Ember-500 #E65A32. MUST be ≤1KB on disk — strip the <title>/<desc> to a minimal aria-label if needed to hit the budget. Exactly 3 hole subpaths (LOGO-06).
8. `social-card.svg` — 1280×640. Basalt-950 #11100F ground via a single full-canvas `<rect>` (this is the ONLY permitted rect in the root set — the card IS a bounded artwork, documented exemption). Compose: LK-B lockup (light/dark-on-dark colorway, letters #FFF9F3) + tagline "AI ops for Phoenix apps." + a hex/GitHub hint line (calm, evidence-not-hype voice). Text outlined to paths OR system-font fallback declared in a comment.

Add a `"converge": "node converge.mjs"` script to package.json. Run `node brandbook/tools/converge.mjs` and confirm it writes all 8 files.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/scoria && node brandbook/tools/converge.mjs && ls brandbook/logo-primary.svg brandbook/logo-primary-light.svg brandbook/logo-mark.svg brandbook/logo-monochrome.svg brandbook/logo-lockup-subtitle.svg brandbook/logotype-integrated.svg brandbook/favicon.svg brandbook/social-card.svg && test "$(du -k brandbook/favicon.svg | cut -f1)" -le 1 && echo FAVICON_LE_1KB</automated>
  </verify>
  <done>converge.mjs writes all 8 census-named SVGs to brandbook/ root; favicon.svg is ≤1KB; each file is well-formed SVG with the locked geometry (no coordinate changes vs source candidates).</done>
</task>

<task type="auto">
  <name>Task 2: Optical-correction pass + variant-spec.md (clear-space, min-size, documented adjustments)</name>
  <files>brandbook/tools/variant-spec.md, brandbook/logo-primary.svg, brandbook/logo-primary-light.svg, brandbook/logotype-integrated.svg, brandbook/favicon.svg</files>
  <read_first>brandbook/logo-primary.svg + the 7 sibling root SVGs from Task 1, .planning/phases/19-logo-divergence-user-choice/19-03-SUMMARY.md (LK-B craft numbers: mark 56.7u wide vs 56.3u 'o' advance; ink density 0.61 vs 0.57; root hole r13→14.6 micro-tune), .planning/phases/20-logo-convergence-variant-set/20-CONTEXT.md §Craft/optical pass requirements + §Specific Ideas, brandbook/pressure-test.md §SECTION 8 (clear space = largest vesicle unit; min sizes)</read_first>
  <action>
Run the manual optical-correction review of every root variant and DOCUMENT it in `brandbook/tools/variant-spec.md`. This is a craft pass, not a redraw — only adjustments justified by the numeric review are applied to the SVGs (fills, the single-vs-two-tone primary call, favicon hole snap), never silhouette geometry.

variant-spec.md MUST contain:
1. **Single-vs-two-tone primary decision** (Claude's discretion, documented): state the call (audit leans two-tone Ember 'o'). Give the ink-density proxy numbers: mark ink density 0.61 vs the round letters' 0.57 (from 19-03 SUMMARY) — confirm the two-tone weight balance reads as a deliberate accent, not a heavier glyph. If you flip to single-tone, justify it here and apply to logo-primary.svg + logo-primary-light.svg.
2. **Overshoot / baseline note**: the mark-'o' must not look clipped or floating vs Plex's humanist 'o' overshoot. Record that the LK-B mark already occupies x 111.22–167.87 (o-advance 111.4–167.7), y −53.38–1.18 (x-height band) with no collision with neighbouring 'c'/'r' (from 19-03 geometry validation) — confirm the substituted glyph sits in the x-height band, not clipped/floating.
3. **logotype-integrated lineage call**: state whether it is byte-identical to logo-monochrome or independently tuned, and why.
4. **Favicon legibility**: confirm each of the 3 holes ≥1.5px radius at 16px and the holes snapped to even 16-grid coordinates; record the snap technique used.
5. **Clear space**: cap-height/2 around the lockup (reconcile with §8's "largest vesicle = unit v, 1.5v around lockup" — state the rule you ship and note the §8 alternate). Give the numeric clearspace for the primary lockup.
6. **Min sizes**: primary lockup ≥120px wide, mark ≥20px, favicon exact at 16/32px (per 20-CONTEXT; §8 says lockup ≥112px — ship ≥120 and note it).
7. **LOGO-01..07 binding confirmation**: a one-line statement that all root files (social-card rect exempted) obey no-rect / evenodd / no-stroke / tight-viewBox / ≤2-decimal rules.

Apply any adjustment the review justifies (e.g. snap favicon holes, flip primary tone) back into the SVGs from Task 1. Keep all coordinate changes ≤2 decimals.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/scoria && test -f brandbook/tools/variant-spec.md && grep -qi "clear space" brandbook/tools/variant-spec.md && grep -qi "min" brandbook/tools/variant-spec.md && grep -Eqi "two-tone|single-tone|ink density|0\.6" brandbook/tools/variant-spec.md && grep -q "120" brandbook/tools/variant-spec.md && echo SPEC_OK</automated>
  </verify>
  <done>variant-spec.md documents the single-vs-two-tone call with ink-density numbers, overshoot/baseline note, logotype lineage call, favicon legibility, clear-space (cap-height/2) and min-size rules (lockup ≥120px, mark ≥20px, favicon 16/32px), and the LOGO-01..07 binding statement; any justified adjustment is applied back into the SVGs.</done>
</task>

<task type="auto">
  <name>Task 3: Extend verify-logos.mjs with ROOT-* checks; prune losing candidates + galleries; size re-check</name>
  <files>brandbook/tools/verify-logos.mjs, brandbook/tools/options-gallery.html, brandbook/tools/options-gallery-round2.html, brandbook/tools/candidates/</files>
  <read_first>brandbook/tools/verify-logos.mjs (existing helpers pass/fail/check + BUDGET walkSize), brandbook/logo-primary.svg + the 8 root SVGs from Task 1, .planning/phases/20-logo-convergence-variant-set/20-CONTEXT.md §Pruning + §decisions (social-card rect exemption), brandbook/tools/candidates/ listing (keep only TV-1 lineage + LK-B)</read_first>
  <action>
Extend `verify-logos.mjs` to gate the 8 ROOT variants and then prune.

ROOT checks to add (read brandbook/*.svg, the 8 census files; reuse the existing pass/fail/check helpers):
- **ROOT-EXISTS**: all 8 exact filenames present at brandbook/ root (logo-primary, logo-primary-light, logo-mark, logo-monochrome, logo-lockup-subtitle, logotype-integrated, favicon, social-card .svg).
- **ROOT-NORECT**: no `<rect` in any root SVG EXCEPT social-card.svg, which is allowed exactly one card-ground rect (documented exemption — add an inline comment citing 20-CONTEXT "the card IS a bounded artwork, not a logo background"). Assert social-card has its rect; assert the other 7 have zero.
- **ROOT-EVENODD**: every root SVG with a logo path uses `fill-rule="evenodd"` (mark holes punched).
- **ROOT-STROKE**: no active stroke attributes in any root SVG (reuse the existing stroke detection logic).
- **ROOT-VIEWBOX**: every root SVG has a viewBox; logo viewBoxes tight near origin (social-card is 0 0 1280 640 — exempt the social-card from the tight-origin bound).
- **ROOT-DECIMALS**: all coordinates ≤2 decimal places across the 8 files.
- **ROOT-CURRENTCOLOR**: logo-monochrome.svg and logotype-integrated.svg use currentColor.
- **ROOT-FAVICON**: favicon.svg ≤1024 bytes AND has exactly 3 hole subpaths (M-count − 1 === 3, reuse the LOGO-06 counting approach).
- **ROOT-SUBTITLE**: logo-lockup-subtitle.svg contains the tagline "AI ops for Phoenix apps."; the OTHER 7 root SVGs contain no tagline/subtitle text (social-card MAY carry the tagline — exempt social-card from the no-tagline assertion).

Because the gallery files are deleted in this same task, the existing GALLERY-* checks would fail. Guard every GALLERY-* check (and the candidate-count checks that reference pruned files) so they SKIP gracefully when options-gallery.html is absent (e.g. `if (!existsSync(GALLERY_FILE)) { pass('GALLERY-*', 'gallery pruned in Phase 20 — skipped'); }`), OR remove the gallery checks entirely and keep only the candidate structural checks for the retained TV-1/LK-B files. Update the BUDGET check threshold back to the final target: brandbook/ text+SVG (excluding node_modules) must be < 500 KB now that the galleries are gone.

Then PRUNE (20-CONTEXT §Pruning):
- Delete BOTH gallery HTML files: options-gallery.html and options-gallery-round2.html.
- Delete losing candidates from candidates/: remove everything that is NOT TV-1 lineage or LK-B. KEEP: TV-1-mark.svg, TV-1-mono.svg, TV-1-fav.svg, TV-1-lockup.svg, LK-B-lockup.svg. DELETE: TV-2*, TV-3*, CM-1*, CM-2*, AP-1*, LK-A/C/D/E/F-lockup.svg, TYPE-1.svg, TYPE-2.svg. (Generator toolchain lib/, presets.mjs, smoke.mjs, contrast-check.mjs, generate*.mjs, gallery*.mjs, package.json/lock stay — they are small source for reproducibility.)

After pruning, run the verifier (exit 0) and `du -sk brandbook/` (report it; must be well under 500 KB excluding node_modules — node_modules is gitignored).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/scoria && node brandbook/tools/verify-logos.mjs && test ! -f brandbook/tools/options-gallery.html && test ! -f brandbook/tools/options-gallery-round2.html && test -z "$(ls brandbook/tools/candidates/ | grep -Ev '^(TV-1-(mark|mono|fav|lockup)|LK-B-lockup)\.svg$')" && echo CANDIDATES_PRUNED && du -sk --exclude=node_modules brandbook/ 2>/dev/null || (echo "manual du:" && expr $(du -sk brandbook | cut -f1) - $(du -sk brandbook/tools/node_modules | cut -f1))</automated>
  </verify>
  <done>verify-logos.mjs exits 0 with all ROOT-* checks passing and gallery checks skipped/removed; both gallery HTML files deleted; candidates/ contains only TV-1-mark/mono/fav/lockup + LK-B-lockup; BUDGET threshold back to <500KB and brandbook/ (sans node_modules) is well under it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated SVG → consuming surface (README, dashboard, HexDocs in Phase 22) | Logo SVGs are inert artwork rendered as `<img>`/inline; no script, no external refs |
| converge.mjs / verify-logos.mjs → filesystem | Node scripts write brandbook/ root + delete pruned files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | favicon.svg / root SVGs (silent geometry drift from locked gate-#2 artwork) | mitigate | ROOT-* verifier asserts evenodd, no-rect, tight viewBox, ≤2-decimal, 3-hole favicon; geometry is copied verbatim from candidates, not regenerated |
| T-20-02 | Information disclosure | social-card.svg / lockup-subtitle.svg text | accept | Text is public brand tagline + repo hint; no secrets. Rationale: brand collateral is intentionally public |
| T-20-03 | Tampering | embedded `<script>`/`xlink:href` external refs in any root SVG | mitigate | Root SVGs are fills-only paths/rect; no script and no http refs introduced (Phase 22 quality gate re-verifies offline-safety) |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — toolchain (opentype.js, wawoff2) already present from Phase 19; no new dependencies, legitimacy gate N/A |
</threat_model>

<verification>
- `node brandbook/tools/converge.mjs` writes all 8 census files.
- `node brandbook/tools/verify-logos.mjs` exits 0 (ROOT-* pass, galleries skipped, BUDGET <500KB).
- `ls brandbook/*.svg` shows exactly the 8 census filenames.
- `du -k brandbook/favicon.svg` ≤ 1KB.
- `ls brandbook/tools/candidates/` shows only TV-1-mark/mono/fav/lockup + LK-B-lockup.
- Both `options-gallery*.html` files absent.
- `brandbook/tools/variant-spec.md` documents optical pass + clear-space + min-size.
</verification>

<success_criteria>
- All 8 root variant SVGs committed with locked geometry and census colorways.
- Optical-correction pass documented (single-vs-two-tone, ink-density, overshoot, favicon legibility) in variant-spec.md.
- Clear-space (cap-height/2) + min-size rules written.
- Root variants gated by verify-logos.mjs (exit 0); social-card rect exemption documented.
- Losing candidates + both galleries pruned; brandbook/ (sans node_modules) well under 500KB.
- BRAND-04 structural half satisfied (confirm checkpoint is 20-02).
</success_criteria>

<output>
Create `.planning/phases/20-logo-convergence-variant-set/20-01-SUMMARY.md` when done.
</output>
