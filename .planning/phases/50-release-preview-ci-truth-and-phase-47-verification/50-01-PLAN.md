---
phase: 50-release-preview-ci-truth-and-phase-47-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - docs/operator_verification.md
  - test/scoria/adoption_surface_test.exs
autonomous: true
requirements:
  - ADPT-03
  - ADPT-04
must_haves:
  truths:
    - "The canonical maintainer proof command remains plain `mix scoria.release_preview`."
    - "CI runs the release-preview lane in the env where `mix docs` is actually supported, instead of inheriting the global `MIX_ENV=test` job env."
    - "Source assertions fail if the repo drifts back to presenting `MIX_ENV=test mix scoria.release_preview` as the closeout contract."
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "Truthful release-preview env contract in CI"
    - path: docs/operator_verification.md
      provides: "Maintainer-facing explanation of the canonical release-preview lane"
    - path: test/scoria/adoption_surface_test.exs
      provides: "Source assertions that pin the closeout command contract"
  key_links:
    - from: .github/workflows/ci.yml
      to: docs/operator_verification.md
      via: "release-preview lane command"
      pattern: "mix scoria\\.release_preview"
    - from: test/scoria/adoption_surface_test.exs
      to: docs/operator_verification.md
      via: "closeout command wording assertions"
      pattern: "mix scoria\\.release_preview"
---

<objective>
Repair the release-preview lane's CI env contract without widening the public closeout story.

Purpose: The current CI job exports `MIX_ENV=test`, but `mix scoria.release_preview` depends on `mix docs`, which only exists when `:ex_doc` is loaded in `:dev`. This plan makes the merge gate truthful again while preserving Phase 49's canonical closeout command.
Output: A CI release-preview step that runs in `:dev`, maintainer docs that keep the command unprefixed, and drift-prevention assertions that reject `MIX_ENV=test mix scoria.release_preview` as the support contract.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v2.2-MILESTONE-AUDIT.md
@.planning/phases/50-release-preview-ci-truth-and-phase-47-verification/50-RESEARCH.md
@.planning/phases/50-release-preview-ci-truth-and-phase-47-verification/50-VALIDATION.md
@.planning/phases/49-support-truth-and-adoption-closeout/49-CONTEXT.md
@mix.exs
@.github/workflows/ci.yml
@docs/operator_verification.md
@test/scoria/adoption_surface_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-scope the CI release-preview step to the supported env</name>
  <read_first>
    - .github/workflows/ci.yml
    - mix.exs
    - .planning/v2.2-MILESTONE-AUDIT.md
    - docs/operator_verification.md
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>Update the `Run release preview lane` step in `.github/workflows/ci.yml` so it no longer inherits the job-wide `MIX_ENV=test` setting. Set the step to run `MIX_ENV=dev mix scoria.release_preview`, or equivalently give the step an explicit `env` override with `MIX_ENV: dev`, while leaving the later database, adoption, and broad test steps on `MIX_ENV=test`. Keep the release-preview step before `Prepare database` and before the broad `mix test` step so publish-surface drift still fails fast.</action>
  <verify>
    <automated>rg -n "Run release preview lane|MIX_ENV=dev mix scoria.release_preview" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a `Run release preview lane` step that explicitly runs `mix scoria.release_preview` with `MIX_ENV=dev`.
    - The same file still leaves the adoption and broad test steps under the job's `MIX_ENV=test` contract.
    - The release-preview step appears before `Prepare database` and before the broad `Run tests` step.
  </acceptance_criteria>
  <done>The merge gate now exercises the bounded release-preview lane in the env where `mix docs` exists.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the maintainer command contract in docs and source assertions</name>
  <read_first>
    - docs/operator_verification.md
    - test/scoria/adoption_surface_test.exs
    - .planning/phases/49-support-truth-and-adoption-closeout/49-CONTEXT.md
    - .planning/phases/50-release-preview-ci-truth-and-phase-47-verification/50-RESEARCH.md
  </read_first>
  <files>docs/operator_verification.md, test/scoria/adoption_surface_test.exs</files>
  <action>Keep `docs/operator_verification.md` aligned with the repaired contract: the canonical maintainer command must remain plain `mix scoria.release_preview`, and if the guide mentions CI behavior it should say the lane runs in `MIX_ENV=dev` because ExDoc remains a dev-only tool. Update `test/scoria/adoption_surface_test.exs` so the operator-guide assertions still require `mix scoria.release_preview` and also reject the string `MIX_ENV=test mix scoria.release_preview`. Do not change the closeout chain away from `mix scoria.release_preview` then `mix test.adoption`.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/adoption_surface_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
    - `docs/operator_verification.md` contains `mix scoria.release_preview` as the maintainer lane command.
    - `test/scoria/adoption_surface_test.exs` asserts the operator guide contains `mix scoria.release_preview`.
    - `test/scoria/adoption_surface_test.exs` refutes `MIX_ENV=test mix scoria.release_preview`.
    - Neither file rewrites the closeout chain away from `mix scoria.release_preview` followed by `mix test.adoption`.
  </acceptance_criteria>
  <done>Support-truth and source assertions now agree on the repaired release-preview env contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI env -> maintainer proof | A job-wide env export can invalidate the release-preview lane before it checks any real docs/package regression |
| public docs -> source assertions | If docs and tests diverge, the repo can silently drift back to an unsupported closeout command |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | T | `.github/workflows/ci.yml` | mitigate | Override the release-preview step to `MIX_ENV=dev mix scoria.release_preview` so CI stops failing on env wiring drift |
| T-50-02 | I | `docs/operator_verification.md`, `test/scoria/adoption_surface_test.exs` | mitigate | Keep the canonical maintainer command unprefixed and add assertions rejecting `MIX_ENV=test mix scoria.release_preview` |
</threat_model>

<verification>
- `rg -n "Run release preview lane|MIX_ENV=dev mix scoria.release_preview" .github/workflows/ci.yml`
- `MIX_ENV=test mix test test/scoria/adoption_surface_test.exs --trace`
</verification>

<success_criteria>
- CI runs the release-preview lane in a supported env.
- Public support docs still name plain `mix scoria.release_preview` as the bounded maintainer proof.
- Source assertions fail if the repo drifts back to a `MIX_ENV=test` release-preview contract.
</success_criteria>

<output>
After completion, create `.planning/phases/50-release-preview-ci-truth-and-phase-47-verification/50-01-SUMMARY.md`
</output>
