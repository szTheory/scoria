---
phase: 43.1-fix-full-suite-failures-blocking-clean-relay-closeout
plan: 2
type: execute
wave: 2
depends_on:
  - 43.1-01
files_modified:
  - lib/scoria_web/components/approval_inbox_component.ex
  - lib/scoria_web/live/orchestrator_live.ex
  - test/scoria_web/live/orchestrator_live_test.exs
autonomous: true
requirements:
  - Relay closeout hygiene
nyquist_compliant: true
must_haves:
  truths:
    - "Pending approvals with sparse metadata render safely in the approval inbox instead of crashing the operator dashboard."
    - "HITL approve/reject behavior, durable audit outbox emission, and run/step state transitions remain intact after the nil-safe rendering fix."
    - "The approval inbox fix stays conservative and does not redesign the dashboard surface."
  artifacts:
    - path: "lib/scoria_web/components/approval_inbox_component.ex"
      provides: "Nil-safe rendering over sparse approval maps"
    - path: "test/scoria_web/live/orchestrator_live_test.exs"
      provides: "Executable proof that approve/reject flows survive sparse metadata"
  key_links:
    - from: "lib/scoria_web/components/approval_inbox_component.ex"
      to: "test/scoria_web/live/orchestrator_live_test.exs"
      via: "approvals missing connector metadata must still render and support approve/reject actions"
      pattern: "connector_label|Approval Required|Approve Decision|Reject Decision"
---

<objective>
Stop the operator dashboard from crashing on sparse pending approvals while preserving the existing HITL audit flow.

Purpose: remove the LiveView regressions blocking `mix test` green without turning this closeout slice into a dashboard redesign.
Output: the approval inbox handles generic/local approvals safely and the HITL tests pass again.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/43.1-fix-full-suite-failures-blocking-clean-relay-closeout/43.1-CONTEXT.md
@.planning/phases/43.1-fix-full-suite-failures-blocking-clean-relay-closeout/43.1-RESEARCH.md
@lib/scoria_web/components/approval_inbox_component.ex
@lib/scoria_web/live/orchestrator_live.ex
@test/scoria_web/live/orchestrator_live_test.exs

<interfaces>
From `lib/scoria_web/components/approval_inbox_component.ex`:
```elixir
<span :if={approval.connector_label}>· <%= approval.connector_label %></span>
```

From `test/scoria_web/live/orchestrator_live_test.exs`:
```elixir
send(view.pid, {:hitl_request, approval})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make approval rendering nil-safe for sparse pending approvals</name>
  <files>lib/scoria_web/components/approval_inbox_component.ex, lib/scoria_web/live/orchestrator_live.ex, test/scoria_web/live/orchestrator_live_test.exs</files>
  <read_first>.planning/phases/43.1-fix-full-suite-failures-blocking-clean-relay-closeout/43.1-RESEARCH.md, lib/scoria_web/components/approval_inbox_component.ex, test/scoria_web/live/orchestrator_live_test.exs</read_first>
  <behavior>
    - Test 1: rendering a pending approval without `connector_label` no longer raises `KeyError`.
    - Test 2: approvals with connector metadata still render that label when present.
    - Test 3: the inbox continues to show stable operator truth such as tool name and status even when connector fields are absent.
  </behavior>
  <action>Repair the approval projection/rendering path so sparse approval maps are safe by default. The likely fix is optional access in the component and, if needed, normalizing approval assigns in `OrchestratorLive` before render. Keep the surface conservative: preserve the current card layout and operator wording, but make connector metadata an optional decoration rather than a required key.</action>
  <acceptance_criteria>
    - `rg -n 'connector_label|Map\\.get|get_in|\\[:connector_label\\]' lib/scoria_web/components/approval_inbox_component.ex lib/scoria_web/live/orchestrator_live.ex` returns matches showing nil-safe access.
    - `mix test test/scoria_web/live/orchestrator_live_test.exs --trace` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/orchestrator_live_test.exs --trace</automated>
  </verify>
  <done>Sparse approvals no longer crash the operator dashboard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve HITL workflow truth after the rendering fix</name>
  <files>test/scoria_web/live/orchestrator_live_test.exs, lib/scoria_web/live/orchestrator_live.ex</files>
  <read_first>test/scoria_web/live/orchestrator_live_test.exs, lib/scoria_web/live/orchestrator_live.ex</read_first>
  <behavior>
    - Test 1: approve keeps the run and step transitions plus `approval.approved` audit outbox event.
    - Test 2: reject keeps the waiting states plus `approval.rejected` audit outbox event.
    - Test 3: the modal and approval inbox continue to render the same user-facing action affordances after the nil-safe fix.
  </behavior>
  <action>Re-run and tighten the HITL approve/reject tests around the fixed rendering path so the phase does not accidentally trade a render crash for a broken action flow. If the LiveView now needs a tiny projection helper to keep sparse approvals and modal state aligned, keep that helper local to the LiveView and avoid changing the durable approval model.</action>
  <acceptance_criteria>
    - `rg -n 'approval\\.approved|approval\\.rejected|Approval Required|Approve Decision|Reject Decision' test/scoria_web/live/orchestrator_live_test.exs` returns matches.
    - `mix test test/scoria_web/live/orchestrator_live_test.exs --trace` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/orchestrator_live_test.exs --trace</automated>
  </verify>
  <done>The HITL flow remains fully auditable and operator-visible after the sparse-render fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sparse approval truth -> LiveView rendering | assuming optional metadata exists can crash the UI and hide real pending approval work |
| render fix -> action flow | a superficial UI fix could accidentally break approve/reject transitions or audit evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43.1-03 | D | approval inbox render path | mitigate | make connector metadata optional and keep stable fields visible |
| T-43.1-04 | R | HITL approve/reject flow | mitigate | re-run the existing audit-backed tests after the render fix |
</threat_model>

<verification>
- `mix test test/scoria_web/live/orchestrator_live_test.exs --trace`
</verification>

<success_criteria>
- Sparse approvals render safely.
- Approve/reject flows still update run/step state and emit audit events.
- The operator dashboard fix stays narrow and non-disruptive.
</success_criteria>

<output>
After completion, create `.planning/phases/43.1-fix-full-suite-failures-blocking-clean-relay-closeout/43.1-fix-full-suite-failures-blocking-clean-relay-closeout-02-SUMMARY.md`
</output>
