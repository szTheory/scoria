---
phase: 43.1-fix-full-suite-failures-blocking-clean-relay-closeout
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/eval.ex
  - lib/scoria/eval/runner.ex
  - test/scoria/eval/offline_runner_test.exs
  - test/scoria/eval/eval_run_persistence_test.exs
autonomous: true
requirements:
  - Relay closeout hygiene
nyquist_compliant: true
must_haves:
  truths:
    - "The eval lane exposes one supported offline replay truth instead of leaving dead runner tests beside a different persistence contract."
    - "Eval specs persist only durable immutable facts; mutable convenience aliases/defaults are rejected explicitly if the tests continue to call them invalid."
    - "Repairing the red eval tests must preserve campaign lineage, dataset/version snapshots, and legacy eval-run compatibility."
  artifacts:
    - path: "lib/scoria/eval.ex"
      provides: "Canonical durable eval persistence contract"
    - path: "lib/scoria/eval/runner.ex"
      provides: "Supported offline replay API or compatibility wrapper for the checked eval tests"
    - path: "test/scoria/eval/eval_run_persistence_test.exs"
      provides: "Durable contract proof for immutable eval specs and persisted run lineage"
    - path: "test/scoria/eval/offline_runner_test.exs"
      provides: "Executable proof for the offline replay entrypoint"
  key_links:
    - from: "lib/scoria/eval.ex"
      to: "test/scoria/eval/eval_run_persistence_test.exs"
      via: "create_eval_spec/1 must reject mutable alias/default inputs while preserving current durable snapshots"
      pattern: "create_eval_spec|dataset_alias|default_judge_model|mutable aliases are not durable eval truth"
    - from: "lib/scoria/eval/runner.ex"
      to: "test/scoria/eval/offline_runner_test.exs"
      via: "offline replay entrypoint proves the committed cassette and dataset assertions through a supported API"
      pattern: "run_offline|assert_dataset|fixture_key|threshold_verdict"
---

<objective>
Repair the red eval suite by restoring one supported offline replay contract and re-locking immutable eval-spec truth.

Purpose: remove the current eval regressions blocking a clean Relay closeout without weakening proof or widening eval product scope.
Output: targeted eval tests are green, and the repo has one boring supported story for offline replay and durable eval spec persistence.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43.1-fix-full-suite-failures-blocking-clean-relay-closeout/43.1-CONTEXT.md
@.planning/phases/43.1-fix-full-suite-failures-blocking-clean-relay-closeout/43.1-RESEARCH.md
@lib/scoria/eval.ex
@test/scoria/eval/eval_run_persistence_test.exs
@test/scoria/eval/offline_runner_test.exs

<interfaces>
From `lib/scoria/eval.ex`:
```elixir
def create_eval_spec(attrs \\ %{}) do
```

From `test/scoria/eval/offline_runner_test.exs`:
```elixir
assert {:ok, result} =
         Runner.run_offline(%{
```

From `test/scoria/eval/eval_run_persistence_test.exs`:
```elixir
assert {:error, changeset} =
         Eval.create_eval_spec(
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore one supported offline replay runner seam</name>
  <files>lib/scoria/eval/runner.ex, lib/scoria/eval.ex, test/scoria/eval/offline_runner_test.exs</files>
  <read_first>.planning/phases/43.1-fix-full-suite-failures-blocking-clean-relay-closeout/43.1-RESEARCH.md, lib/scoria/eval.ex, test/scoria/eval/offline_runner_test.exs</read_first>
  <behavior>
    - Test 1: the repo exposes a supported offline replay entrypoint that satisfies `Runner.run_offline/1` and `Runner.assert_dataset/1`, or the tests are moved to a clearly supported seam with equivalent proof strength in the same change.
    - Test 2: the offline replay result still includes fixture key, threshold verdict, completed eval run, and persisted score evidence.
    - Test 3: the dataset assertion path still validates a sealed dataset/eval-spec pair instead of becoming a no-op.
  </behavior>
  <action>Implement the missing offline replay runner surface in the narrowest boring way that fits the current Eval model. If `Scoria.Eval.Runner` is still the right public seam, add `lib/scoria/eval/runner.ex` and wire it to the existing dataset/spec/run/score primitives without inventing a parallel persistence layer. If the repo has intentionally consolidated onto another Eval entrypoint, migrate the checked proof to that supported seam in the same change, preserving the same observable guarantees around fixture key, verdict, completed run state, and persisted scores.</action>
  <acceptance_criteria>
    - `rg -n 'defmodule Scoria\\.Eval\\.Runner|def run_offline|def assert_dataset' lib/scoria/eval/runner.ex test/scoria/eval/offline_runner_test.exs` returns matches.
    - `mix test test/scoria/eval/offline_runner_test.exs --trace` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/eval/offline_runner_test.exs --trace</automated>
  </verify>
  <done>The offline replay proof lane no longer points at a missing module or unsupported contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-lock immutable eval-spec inputs without regressing persistence coverage</name>
  <files>lib/scoria/eval.ex, test/scoria/eval/eval_run_persistence_test.exs</files>
  <read_first>.planning/phases/43.1-fix-full-suite-failures-blocking-clean-relay-closeout/43.1-CONTEXT.md, lib/scoria/eval.ex, test/scoria/eval/eval_run_persistence_test.exs</read_first>
  <behavior>
    - Test 1: `Eval.create_eval_spec/1` rejects mutable convenience attrs like `dataset_alias` and `default_judge_model` with the existing durable-truth error semantics if that remains the supported contract.
    - Test 2: campaign lineage, tenant identity, dataset/version snapshots, and legacy eval runs remain green.
    - Test 3: no fix reintroduces mutable convenience state into persisted eval-spec rows just to satisfy the suite.
  </behavior>
  <action>Make `create_eval_spec/1` explicit about what may and may not enter durable eval truth. Add a guard or validation path that rejects mutable alias/default attrs before insert if the checked contract still forbids them. Preserve the rest of the persistence behavior already proven in `eval_run_persistence_test.exs`: immutable spec versioning, dataset snapshots, campaign lineage, and legacy compatibility. If any test wording must change, keep the durable-truth rule explicit and equally strong.</action>
  <acceptance_criteria>
    - `rg -n 'dataset_alias|default_judge_model|mutable aliases are not durable eval truth' lib/scoria/eval.ex test/scoria/eval/eval_run_persistence_test.exs` returns matches.
    - `mix test test/scoria/eval/eval_run_persistence_test.exs --trace` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/eval/eval_run_persistence_test.exs --trace</automated>
  </verify>
  <done>The eval persistence contract is explicit again and the red eval lane is restored without weakening durable truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked eval proof -> supported API | dead or drifting APIs can make the suite fail for reasons that are neither intentional nor documented |
| convenience inputs -> durable persisted truth | mutable aliases/defaults can silently weaken auditability if they are allowed into eval-spec rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43.1-01 | T | `Scoria.Eval.Runner` surface | mitigate | restore or replace the seam explicitly so the repo has one supported offline replay contract |
| T-43.1-02 | I | `Eval.create_eval_spec/1` | mitigate | reject mutable alias/default attrs before durable persistence |
</threat_model>

<verification>
- `mix test test/scoria/eval/offline_runner_test.exs --trace`
- `mix test test/scoria/eval/eval_run_persistence_test.exs --trace`
</verification>

<success_criteria>
- The offline replay proof no longer references a missing module.
- Immutable eval-spec truth is enforced explicitly again.
- All targeted eval regression tests pass without sacrificing lineage or snapshot coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/43.1-fix-full-suite-failures-blocking-clean-relay-closeout/43.1-fix-full-suite-failures-blocking-clean-relay-closeout-01-SUMMARY.md`
</output>
