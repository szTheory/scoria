---
phase: 39-replay-operator-ux-draft-dataset-promotion
plan: 3
type: execute
wave: 3
depends_on:
  - 39-01
  - 39-02
files_modified:
  - lib/scoria/eval.ex
  - lib/scoria/eval/dataset_promotion.ex
  - lib/scoria/workflows/dataset_promotion.ex
  - lib/scoria_web/live/dataset_live/promote_component.ex
  - test/scoria/eval_test.exs
  - test/scoria/workflows_test.exs
  - test/scoria/workflows/remote_approval_projection_test.exs
  - test/scoria_web/live/dataset_live/promote_component_test.exs
autonomous: true
requirements:
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - "Operators can promote either the original trace or replay trace into an open draft dataset."
    - "Inserted dataset items carry a frozen evidence snapshot that preserves workflow and replay provenance."
    - "Sealed baseline datasets stay visible but immutable, and baseline promotion routes through explicit approval semantics."
  artifacts:
    - path: "lib/scoria/eval/dataset_promotion.ex"
      provides: "Frozen workflow-source snapshot builder and draft promotion transaction."
    - path: "lib/scoria/eval.ex"
      provides: "Public eval boundary for workflow-source promotion and target classification."
    - path: "lib/scoria/workflows/dataset_promotion.ex"
      provides: "Workflow-owned baseline approval request service."
    - path: "lib/scoria_web/live/dataset_live/promote_component.ex"
      provides: "Promotion UI with open/selectable drafts and visible/disabled sealed baselines."
    - path: "test/scoria_web/live/dataset_live/promote_component_test.exs"
      provides: "Promotion modal regression coverage for open and sealed dataset states."
  key_links:
    - from: "lib/scoria_web/live/dataset_live/promote_component.ex"
      to: "lib/scoria/eval.ex"
      via: "open dataset submit promotes the selected comparison source into a frozen dataset item"
      pattern: "promote_workflow_source|preview_workflow_source_promotion"
    - from: "lib/scoria_web/live/dataset_live/promote_component.ex"
      to: "lib/scoria/workflows/dataset_promotion.ex"
      via: "sealed baseline action requests approval instead of mutating the dataset"
      pattern: "request_baseline_promotion"
    - from: "lib/scoria/eval/dataset_promotion.ex"
      to: "lib/scoria/eval/dataset_item.ex"
      via: "dataset item metadata stores source_variant and replay provenance in one immutable insert"
      pattern: "source_variant|source_run_id|source_checkpoint_id|recorded_outcome"
---

<objective>
Freeze workflow-source dataset promotion and gate sealed baseline targets through workflow-owned approval semantics.

Purpose: Phase 39 must let operators promote original or replay evidence into draft datasets without ever mutating sealed baselines directly.
Output: A frozen dataset-promotion service, a promotion modal that distinguishes open versus sealed targets, and a baseline approval request path.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-RESEARCH.md
@.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-PATTERNS.md
@.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-UI-SPEC.md
@.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-02-PLAN.md
@lib/scoria/eval.ex
@lib/scoria/eval/dataset.ex
@lib/scoria/eval/dataset_item.ex
@lib/scoria_web/live/dataset_live/promote_component.ex
@lib/scoria/workflows/prompt_release.ex
@lib/scoria/workflows.ex
@test/scoria/eval_test.exs
@test/scoria/workflows/prompt_release_test.exs
@test/scoria/workflows_test.exs
@test/scoria/workflows/remote_approval_projection_test.exs
@test/scoria_web/live/dataset_live/promote_component_test.exs

<interfaces>
From lib/scoria/eval.ex:
```elixir
def list_datasets do
def get_dataset!(id), do: Repo.get!(Dataset, id)
def add_dataset_item(dataset_id, attrs) do
```

From lib/scoria/eval/dataset_item.ex:
```elixir
field :source_trace_id, :string
field :input, :map
field :expected_output, :map
field :metadata, :map, default: %{}
```

From lib/scoria_web/live/dataset_live/promote_component.ex:
```elixir
def update(assigns, socket) do
def handle_event("save", %{"item" => params}, socket) do
```

From lib/scoria/workflows/prompt_release.ex:
```elixir
def request_remote_approval(run_id, step_id, attrs) do
def approve(approval_id, status, attrs \\ %{}) when status in ["approved", "rejected"] do
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build frozen workflow-source draft promotion at the eval boundary</name>
  <files>lib/scoria/eval.ex, lib/scoria/eval/dataset_promotion.ex, test/scoria/eval_test.exs</files>
  <read_first>.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-RESEARCH.md, .planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-PATTERNS.md, .planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-UI-SPEC.md, lib/scoria/eval.ex, lib/scoria/eval/dataset.ex, lib/scoria/eval/dataset_item.ex, test/scoria/eval_test.exs</read_first>
  <behavior>
    - Test 1: promoting an `original` workflow source into an open dataset inserts exactly one dataset item with frozen provenance metadata.
    - Test 2: promoting a `replay` workflow source preserves `source_run_id`, `source_checkpoint_id`, `replay_reason_code`, and the recorded outcome in dataset item metadata.
    - Test 3: attempting direct draft promotion into a sealed dataset returns the existing immutable-state error and inserts nothing.
  </behavior>
  <action>Create `Scoria.Eval.DatasetPromotion` and expose concrete wrappers from `Scoria.Eval` named `preview_workflow_source_promotion/1` and `promote_workflow_source/1`, following the exact central-service snapshot style assigned in `39-PATTERNS.md` for `lib/scoria/eval.ex` and `test/scoria/eval_test.exs`. The input contract must include the exact keys `dataset_id`, `workflow_run_id`, `workflow_step_id`, `source_variant`, `provenance`, `checkpoint_output`, `safety`, `promotion_snapshot`, optional `notes`, and optional `expected_output`. Build one immutable dataset item insert where `input` stores the frozen workflow evidence snapshot, `expected_output` stores the operator-supplied expectation map, and `metadata` stores the exact keys `promoted_from_workflow`, `source_variant`, `workflow_run_id`, `workflow_step_id`, `source_run_id`, `source_checkpoint_id`, `execution_mode`, `replay_disposition`, `replay_reason_code`, and `recorded_outcome`. Reuse `DatasetItem.changeset/3` for sealed-dataset enforcement and keep everything inside one `Ecto.Multi`. Do not add schema fields or mutate the dataset row directly.</action>
  <acceptance_criteria>
    - `rg -n "def preview_workflow_source_promotion|def promote_workflow_source" lib/scoria/eval.ex lib/scoria/eval/dataset_promotion.ex` returns matches.
    - `rg -n "promoted_from_workflow|source_variant|workflow_run_id|workflow_step_id|source_run_id|source_checkpoint_id|execution_mode|replay_disposition|replay_reason_code|recorded_outcome" lib/scoria/eval/dataset_promotion.ex test/scoria/eval_test.exs` returns matches.
    - `rg -n "alter table|migration" lib/scoria/eval/dataset_promotion.ex test/scoria/eval_test.exs` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/eval_test.exs</automated>
  </verify>
  <done>Open draft promotion writes one immutable dataset item snapshot per selected source variant and sealed datasets still reject direct mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Show open versus sealed targets and route baseline requests through workflow approvals</name>
  <files>lib/scoria/workflows/dataset_promotion.ex, lib/scoria_web/live/dataset_live/promote_component.ex, test/scoria/workflows_test.exs, test/scoria/workflows/remote_approval_projection_test.exs, test/scoria_web/live/dataset_live/promote_component_test.exs</files>
  <read_first>.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-UI-SPEC.md, .planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-PATTERNS.md, lib/scoria_web/live/dataset_live/promote_component.ex, lib/scoria/workflows/prompt_release.ex, lib/scoria/workflows.ex, lib/scoria/eval.ex, test/scoria/workflows/prompt_release_test.exs, test/scoria/workflows_test.exs, test/scoria/workflows/remote_approval_projection_test.exs, test/scoria_web/live/dataset_live/promote_component_test.exs</read_first>
  <action>Create `Scoria.Workflows.DatasetPromotion` as the workflow-owned sealed-baseline approval wrapper, following the exact service shell and approval-request transaction posture assigned in `39-PATTERNS.md` for `lib/scoria/workflows/dataset_promotion.ex`, `lib/scoria/workflows.ex`, and `test/scoria/workflows_test.exs`, and use the exact `tool_name` value `dataset_baseline_promotion`. Update `ScoriaWeb.DatasetLive.PromoteComponent` to keep the current LiveComponent event loop from `39-PATTERNS.md`, but accept the `promotion_context` contract from Plan 39-02, load all datasets, split them into `open_datasets` and `sealed_datasets`, and render open targets as selectable rows plus a separate disabled `Sealed baseline` section labeled `Approval required`. The primary submit path must call `Eval.promote_workflow_source/1` for open targets and send `{:promote_successful, %{source_variant: ..., dataset_name: ..., dataset_version: ...}}` to the parent. Add a sealed-target action labeled `Request baseline approval` that calls `Scoria.Workflows.DatasetPromotion.request_baseline_promotion/1` and sends `{:baseline_promotion_requested, %{dataset_name: ..., dataset_version: ...}}` without inserting a dataset item. Extend `test/scoria/workflows_test.exs` to prove the approval request persists replay-safe workflow evidence, and extend `test/scoria/workflows/remote_approval_projection_test.exs` to prove the pending approval for `dataset_baseline_promotion` surfaces through the existing projection boundary. If a dataset becomes sealed between render and submit, keep the modal open, refresh the target list, and surface `cannot add or modify items in a sealed dataset` inline. Preserve operator-entered notes and expected output on failure.</action>
  <acceptance_criteria>
    - `rg -n "dataset_baseline_promotion|request_baseline_promotion|Approval required|Sealed baseline|Request baseline approval" lib/scoria/workflows/dataset_promotion.ex lib/scoria_web/live/dataset_live/promote_component.ex test/scoria/workflows_test.exs test/scoria/workflows/remote_approval_projection_test.exs test/scoria_web/live/dataset_live/promote_component_test.exs` returns matches.
    - `rg -n "promote_successful|baseline_promotion_requested|cannot add or modify items in a sealed dataset" lib/scoria_web/live/dataset_live/promote_component.ex test/scoria_web/live/dataset_live/promote_component_test.exs` returns matches.
    - `rg -n "Enum.filter\\(&\\(&1.state == :open\\)\\)" lib/scoria_web/live/dataset_live/promote_component.ex` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/dataset_live/promote_component_test.exs test/scoria/workflows_test.exs test/scoria/workflows/remote_approval_projection_test.exs</automated>
  </verify>
  <done>The modal promotes into open datasets with frozen snapshots, keeps sealed baselines visible but non-mutable, and requests explicit workflow approval for baseline lanes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow comparison source -> dataset item insert | incorrect payload shaping could freeze the wrong source variant or lose provenance |
| operator modal action -> sealed baseline target | direct mutation of sealed datasets would violate immutable release truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-09 | T | `Scoria.Eval.DatasetPromotion` | mitigate | Build the dataset item snapshot from the exact `promotion_context` fields and persist `source_variant`, `workflow_run_id`, and replay provenance in one transaction. |
| T-39-10 | E | `ScoriaWeb.DatasetLive.PromoteComponent` | mitigate | Route sealed-target actions to `Scoria.Workflows.DatasetPromotion.request_baseline_promotion/1` and never call `add_dataset_item/2` for sealed datasets. |
| T-39-11 | R | baseline approval path | mitigate | Use workflow-owned approval requests with `tool_name: "dataset_baseline_promotion"` so approval lineage and audit events remain durable and reviewable. |
| T-39-12 | I | frozen promotion snapshot | mitigate | Keep advanced raw editing collapsed by default and persist only the selected structured source contract plus operator notes/expected output. |
</threat_model>

<verification>
- `mix test test/scoria/eval_test.exs`
- `mix test test/scoria_web/live/dataset_live/promote_component_test.exs test/scoria/workflows/dataset_promotion_test.exs`
</verification>

<success_criteria>
- Open datasets accept frozen original or replay workflow-source promotions through the eval boundary.
- Sealed datasets remain visible in the modal but cannot be mutated directly.
- Baseline promotion requests reuse workflow approval semantics rather than direct dataset mutation.
</success_criteria>

<output>
After completion, create `.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-03-SUMMARY.md`
</output>
