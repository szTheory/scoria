---
phase: 39-replay-operator-ux-draft-dataset-promotion
plan: 2
type: execute
wave: 2
depends_on:
  - 39-01
files_modified:
  - lib/scoria_web/live/workflow_live/show.ex
  - lib/scoria_web/components/workflow_detail_panel_component.ex
  - lib/scoria_web/components/replay_evidence_notebook_component.ex
  - test/scoria_web/live/workflow_live_test.exs
autonomous: true
requirements:
  - RPLY-03
must_haves:
  truths:
    - "Replay runs show a provenance strip directly on the workflow page."
    - "Operators can switch the selected step between original and replay evidence without leaving `/scoria/workflows/:id`."
    - "The right rail renders grouped comparison evidence and durable promotion feedback instead of raw `inspect/1` dumps."
  artifacts:
    - path: "lib/scoria_web/live/workflow_live/show.ex"
      provides: "Workflow-page state for selected comparison source, replay provenance strip, and promotion notices."
    - path: "lib/scoria_web/components/workflow_detail_panel_component.ex"
      provides: "Step-detail shell that preserves the existing CTA and delegates notebook rendering."
    - path: "lib/scoria_web/components/replay_evidence_notebook_component.ex"
      provides: "Replay evidence notebook with grouped cards and typed empty/error states."
    - path: "test/scoria_web/live/workflow_live_test.exs"
      provides: "LiveView regression coverage for replay provenance and comparison UX."
  key_links:
    - from: "lib/scoria_web/live/workflow_live/show.ex"
      to: "lib/scoria/runtime.ex"
      via: "page reads runtime detail DTOs rather than raw workflow structs for the right rail"
      pattern: "Runtime\\.get_run_detail!"
    - from: "lib/scoria_web/live/workflow_live/show.ex"
      to: "lib/scoria_web/live/dataset_live/promote_component.ex"
      via: "promotion_context passes selected source variant and comparison entry into the modal"
      pattern: "promotion_context|selected_source_variant"
    - from: "lib/scoria_web/components/workflow_detail_panel_component.ex"
      to: "lib/scoria_web/components/replay_evidence_notebook_component.ex"
      via: "detail panel preserves its shell and CTA while delegating notebook rendering"
      pattern: "ReplayEvidenceNotebookComponent"
    - from: "lib/scoria_web/components/replay_evidence_notebook_component.ex"
      to: "lib/scoria/runtime/replay_comparison.ex"
      via: "notebook renders provenance/overrides/checkpoint_output/safety/promotion_snapshot groups"
      pattern: "provenance|overrides|checkpoint_output|safety|promotion_snapshot"
---

<objective>
Render replay provenance and original-versus-replay comparison UX on the existing workflow page.

Purpose: Operators must inspect replay provenance, compare outcomes, and stay inside the current workflow route with clear, structured evidence.
Output: Workflow LiveView state for comparison selection, a replay provenance strip, and a right-rail evidence notebook replacing raw dumps.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-RESEARCH.md
@.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-PATTERNS.md
@.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-UI-SPEC.md
@.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-01-PLAN.md
@lib/scoria_web/live/workflow_live/show.ex
@lib/scoria_web/components/workflow_detail_panel_component.ex
@lib/scoria_web/components/incident_evidence_component.ex
@lib/scoria/runtime.ex
@lib/scoria/runtime/run_detail.ex
@test/scoria_web/live/workflow_live_test.exs

<interfaces>
From lib/scoria/runtime/run_detail.ex after 39-01:
```elixir
defstruct [
  :summary,
  :steps,
  :checkpoints,
  :events,
  :approvals,
  :handoffs,
  :comparison_by_step,
  :replay_provenance_strip
]
```

From lib/scoria_web/live/workflow_live/show.ex:
```elixir
def handle_event("select_step", %{"id" => step_id}, socket) do
def handle_event("open_promote_modal", %{"step-id" => step_id}, socket) do
```

From lib/scoria_web/components/workflow_detail_panel_component.ex:
```elixir
attr :step, :map, default: nil
attr :checkpoint, :map, default: nil
```

From lib/scoria_web/components/incident_evidence_component.ex:
```elixir
attr(:evidence, :map, required: true)
defp badge_class(value, kind) do
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workflow-page state for replay provenance, source toggles, and promotion feedback</name>
  <files>lib/scoria_web/live/workflow_live/show.ex, test/scoria_web/live/workflow_live_test.exs</files>
  <read_first>.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-UI-SPEC.md, .planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-PATTERNS.md, lib/scoria_web/live/workflow_live/show.ex, lib/scoria/runtime.ex, lib/scoria/runtime/run_detail.ex, test/scoria_web/live/workflow_live_test.exs</read_first>
  <action>Update `ScoriaWeb.WorkflowLive.Show` using the exact mount/reload/event composition pattern excerpted in `39-PATTERNS.md` for this file: keep `load_run/2`, `assign_selection/2`, and modal state as helper-driven assigns, and keep the HEEx template declarative. Load both `Workflows.get_run_tree!/1` for the tree/timeline and `Runtime.get_run_detail!/1` for the comparison notebook. Add the assign `selected_source_variant` with exact values `"original"` and `"replay"`, defaulting replay runs to `"replay"` and live runs to `"original"`. Add a replay provenance strip directly below the page header when `@run.execution_mode == "replay"` showing, in this order, `Replay branch`, `source_run_id`, `source_checkpoint_id`, `execution_mode`, override summary, and latest replay disposition summary. Add `handle_event("select_comparison_source", %{"source" => source}, socket)` and pass a concrete `promotion_context` map into the modal with `workflow_run_id`, `workflow_step_id`, `selected_source_variant`, and the selected comparison entry. Add `handle_info` handlers for `{:promote_successful, payload}` and `{:baseline_promotion_requested, payload}` so the page can show durable inline notices after Plan 39-03 without further parent edits.</action>
  <acceptance_criteria>
    - `rg -n "selected_source_variant|select_comparison_source|promotion_context|promote_successful|baseline_promotion_requested" lib/scoria_web/live/workflow_live/show.ex` returns matches.
    - `rg -n "Replay branch|source checkpoint|execution mode" lib/scoria_web/live/workflow_live/show.ex test/scoria_web/live/workflow_live_test.exs` returns matches.
    - `rg -n "Runtime.get_run_detail!" lib/scoria_web/live/workflow_live/show.ex` returns a match.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/workflow_live_test.exs</automated>
  </verify>
  <done>The workflow page owns comparison-source selection and provenance strip rendering without creating a new replay route.</done>
</task>

<task type="auto">
  <name>Task 2: Replace raw detail dumps with a replay evidence notebook and typed empty states</name>
  <files>lib/scoria_web/components/workflow_detail_panel_component.ex, lib/scoria_web/components/replay_evidence_notebook_component.ex, lib/scoria_web/live/workflow_live/show.ex, test/scoria_web/live/workflow_live_test.exs</files>
  <read_first>.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-UI-SPEC.md, .planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-PATTERNS.md, lib/scoria_web/components/workflow_detail_panel_component.ex, lib/scoria_web/components/incident_evidence_component.ex, lib/scoria_web/live/workflow_live/show.ex, lib/scoria/runtime/run_detail.ex, test/scoria_web/live/workflow_live_test.exs</read_first>
  <action>Keep `WorkflowDetailPanelComponent` as the shell and CTA host described in `39-PATTERNS.md`, but move the comparison body into a new `ScoriaWeb.ReplayEvidenceNotebookComponent` built from the exact notebook header, two-column body, and badge-helper posture assigned from `IncidentEvidenceComponent`. The new notebook component must render the five structured groups named exactly `Provenance`, `Overrides`, `Checkpoint / Output`, `Safety Evidence`, and `Promotion Snapshot Summary`, plus a segmented toggle labeled `Original trace` and `Replay trace`. Render the empty-state heading `No Replay Comparison Available` with the approved body copy when the selected step has no durable comparison source, and keep raw JSON only inside collapsed advanced sections. Change the CTA label to `Promote Trace to Draft Dataset`, keep it disabled when `promotion_snapshot` is missing, and render helper copy that names the active source variant. Do not introduce a new top-level replay console, tab bar, or dataset-management page.</action>
  <acceptance_criteria>
    - `rg -n "defmodule ScoriaWeb.ReplayEvidenceNotebookComponent|defp badge_class" lib/scoria_web/components/replay_evidence_notebook_component.ex` returns matches.
    - `rg -n "No Replay Comparison Available|Promote Trace to Draft Dataset|Original trace|Replay trace|Safety Evidence|Promotion Snapshot Summary" lib/scoria_web/components/workflow_detail_panel_component.ex lib/scoria_web/components/replay_evidence_notebook_component.ex test/scoria_web/live/workflow_live_test.exs` returns matches.
    - `rg -n "inspect\\(@step\\.projected_context\\)|inspect\\(@checkpoint\\.snapshot\\)|inspect\\(@step\\.error_envelope\\)" lib/scoria_web/components/workflow_detail_panel_component.ex lib/scoria_web/components/replay_evidence_notebook_component.ex` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/workflow_live_test.exs test/scoria_web/components/workflow_tree_component_test.exs</automated>
  </verify>
  <done>The right rail shows replay comparison evidence in structured cards with typed state handling and promotion context anchored to the selected source variant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime DTOs -> LiveView assigns | UI must not reinterpret or fabricate replay/source truth |
| operator interaction -> promotion context | wrong source selection could promote original evidence when replay was intended, or vice versa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-05 | T | `ScoriaWeb.WorkflowLive.Show` | mitigate | Store the exact `selected_source_variant` value and the selected comparison entry in assigns so modal submits use durable page state instead of template inference. |
| T-39-06 | I | `ScoriaWeb.WorkflowDetailPanelComponent` | mitigate | Render only structured notebook groups by default and hide raw JSON in advanced sections to reduce accidental overexposure of internal payloads. |
| T-39-07 | R | workflow promotion notices | mitigate | Inline notices include source variant, dataset name, and dataset version so operators can verify what was promoted or approval-requested. |
| T-39-08 | S | replay provenance strip | mitigate | Show typed `execution_mode`, `source_run_id`, and `source_checkpoint_id` directly from runtime DTOs so a live run cannot spoof replay identity through UI-only copy. |
</threat_model>

<verification>
- `mix test test/scoria_web/live/workflow_live_test.exs`
- `mix test test/scoria_web/live/workflow_live_test.exs test/scoria_web/components/workflow_tree_component_test.exs`
</verification>

<success_criteria>
- Replay runs render a provenance strip and comparison toggle on `/scoria/workflows/:id`.
- The detail panel uses grouped evidence cards and typed empty states instead of raw inspection dumps.
- The workflow page is already prepared to receive promotion success and baseline-approval notices from the dataset component.
</success_criteria>

<output>
After completion, create `.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-02-SUMMARY.md`
</output>
