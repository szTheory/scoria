---
phase: 39-replay-operator-ux-draft-dataset-promotion
plan: 5
type: execute
wave: 5
depends_on:
  - 39-02
  - 39-03
files_modified:
  - lib/scoria/runtime/replay_comparison.ex
  - lib/scoria_web/live/workflow_live/show.ex
  - lib/scoria/eval/dataset_promotion.ex
  - test/scoria/runtime_view_test.exs
  - test/scoria/eval_test.exs
  - test/scoria_web/live/dataset_live/promote_component_test.exs
autonomous: true
gap_closure: true
requirements:
  - RPLY-03
  - DATA-01
must_haves:
  truths:
    - "Replay promotion carries the real source checkpoint lineage from the replay source metadata instead of the replay checkpoint row id."
    - "Draft dataset items promoted from replay persist replay disposition and replay reason metadata from the runtime-selected replay evidence."
    - "Regression coverage proves the workflow/runtime promotion contract survives end-to-end into inserted dataset item metadata."
  artifacts:
    - path: "lib/scoria/runtime/replay_comparison.ex"
      provides: "Replay comparison contract with correct source-lineage provenance and replay safety metadata."
    - path: "lib/scoria_web/live/workflow_live/show.ex"
      provides: "Workflow-page promotion context that forwards the corrected runtime contract without reshaping keys."
    - path: "lib/scoria/eval/dataset_promotion.ex"
      provides: "Frozen dataset promotion persistence that reads replay metadata from the runtime/LiveView contract and writes it into dataset item metadata."
    - path: "test/scoria/runtime_view_test.exs"
      provides: "Regression coverage for replay provenance contract fields."
    - path: "test/scoria/eval_test.exs"
      provides: "Regression coverage for replay dataset metadata persistence."
    - path: "test/scoria_web/live/dataset_live/promote_component_test.exs"
      provides: "LiveView-driven regression coverage for the workflow-page promotion payload."
  key_links:
    - from: "lib/scoria/runtime/replay_comparison.ex"
      to: "lib/scoria_web/live/workflow_live/show.ex"
      via: "selected comparison entry becomes workflow-page promotion_context"
      pattern: "source_checkpoint_id|replay_disposition|replay_reason_code"
    - from: "lib/scoria_web/live/workflow_live/show.ex"
      to: "lib/scoria/eval/dataset_promotion.ex"
      via: "promotion_context passes runtime-selected replay evidence into promote_workflow_source/1"
      pattern: "promotion_context|source_variant|safety|provenance"
    - from: "lib/scoria/eval/dataset_promotion.ex"
      to: "test/scoria/eval_test.exs"
      via: "dataset item metadata assertions prove replay lineage and reason persistence"
      pattern: "source_checkpoint_id|replay_disposition|replay_reason_code"
---

<objective>
Close the goal-critical replay promotion regression in Phase 39.

Purpose: The shipped draft-promotion lane currently misstates replay source checkpoint lineage, drops replay disposition/reason metadata at persistence time, and is protected only by synthetic tests that miss the real runtime-to-LiveView contract.
Output: Corrected replay promotion contract fields, corrected dataset metadata persistence, and end-to-end regression coverage from runtime/LiveView payload to inserted dataset item.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-VERIFICATION.md
@.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-03-PLAN.md
@lib/scoria/runtime/replay_comparison.ex
@lib/scoria_web/live/workflow_live/show.ex
@lib/scoria/eval/dataset_promotion.ex
@test/scoria/runtime_view_test.exs
@test/scoria/eval_test.exs
@test/scoria_web/live/dataset_live/promote_component_test.exs

<interfaces>
From lib/scoria/runtime/replay_comparison.ex:
```elixir
def build(replay_run, source_run \\ nil) do
```

From lib/scoria_web/live/workflow_live/show.ex:
```elixir
defp promotion_context(socket) do
```

From lib/scoria/eval/dataset_promotion.ex:
```elixir
def promote(attrs) do
def preview(attrs) do
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Correct the replay promotion contract at the runtime and workflow-page seams</name>
  <files>lib/scoria/runtime/replay_comparison.ex, lib/scoria_web/live/workflow_live/show.ex, test/scoria/runtime_view_test.exs</files>
  <read_first>.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-VERIFICATION.md, .planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-03-PLAN.md, lib/scoria/runtime/replay_comparison.ex, lib/scoria_web/live/workflow_live/show.ex, test/scoria/runtime_view_test.exs</read_first>
  <behavior>
    - Test 1: replay comparison provenance uses the durable replay source lineage field for `source_checkpoint_id`, not the replay checkpoint row id.
    - Test 2: the replay comparison entry exposes `replay_disposition` and `replay_reason_code` in the runtime-selected contract that `promotion_context` forwards.
    - Test 3: `promotion_context` keeps the grouped runtime keys `provenance`, `checkpoint_output`, `safety`, and `promotion_snapshot` intact for the replay variant.
  </behavior>
  <action>Update `Scoria.Runtime.ReplayComparison` so the replay-side `provenance` group sets `source_checkpoint_id` from the replay source metadata already resolved for lineage, specifically the durable source checkpoint identifier that points back to the original run, not `checkpoint.id` from the replay checkpoint itself. Include `replay_disposition` and `replay_reason_code` in the replay promotion contract under both the runtime-selected `safety` group and the replay `provenance` group so downstream consumers can persist them without inference. Update `ScoriaWeb.WorkflowLive.Show.promotion_context/1` to forward the selected replay entry exactly as `{provenance, checkpoint_output, safety, promotion_snapshot}` from runtime detail, preserving the keys `source_checkpoint_id`, `replay_disposition`, and `replay_reason_code` instead of recomputing or renaming them. Replace or extend `test/scoria/runtime_view_test.exs` so the assertions inspect the replay entry produced by `Runtime.get_run_detail!/1` or the direct comparison builder and prove the corrected lineage and metadata fields are present in the runtime contract used by the workflow page.</action>
  <acceptance_criteria>
    - `rg -n "source_checkpoint_id" lib/scoria/runtime/replay_comparison.ex lib/scoria_web/live/workflow_live/show.ex test/scoria/runtime_view_test.exs` returns matches tied to replay provenance assertions.
    - `rg -n "replay_disposition|replay_reason_code" lib/scoria/runtime/replay_comparison.ex lib/scoria_web/live/workflow_live/show.ex test/scoria/runtime_view_test.exs` returns matches.
    - `rg -n "checkpoint.id" lib/scoria/runtime/replay_comparison.ex` does not show the replay provenance assignment using the replay checkpoint row id.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/runtime_view_test.exs</automated>
  </verify>
  <done>The runtime comparison DTO and workflow-page promotion context both carry the real replay source checkpoint lineage plus replay disposition/reason metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist replay metadata end-to-end and replace synthetic promotion coverage with runtime-driven regressions</name>
  <files>lib/scoria/eval/dataset_promotion.ex, test/scoria/eval_test.exs, test/scoria_web/live/dataset_live/promote_component_test.exs</files>
  <read_first>.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-VERIFICATION.md, .planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-03-PLAN.md, lib/scoria/eval/dataset_promotion.ex, test/scoria/eval_test.exs, test/scoria_web/live/dataset_live/promote_component_test.exs</read_first>
  <behavior>
    - Test 1: `promote_workflow_source/1` persists `source_checkpoint_id`, `replay_disposition`, and `replay_reason_code` into dataset item metadata when the input is the replay contract emitted by the workflow/runtime seam.
    - Test 2: the component regression builds or captures replay `promotion_context` from the runtime/LiveView contract rather than hand-written synthetic params.
    - Test 3: the inserted dataset item metadata still preserves the existing replay fields `source_run_id`, `workflow_run_id`, `workflow_step_id`, and `recorded_outcome`.
  </behavior>
  <action>Update `Scoria.Eval.DatasetPromotion` so frozen dataset metadata reads replay metadata from the exact runtime/LiveView contract shipped by Phase 39: `source_checkpoint_id` from `provenance.source_checkpoint_id`, `replay_disposition` from `safety.replay_disposition` with a fallback to `provenance.replay_disposition`, and `replay_reason_code` from `safety.replay_reason_code` with a fallback to `provenance.replay_reason_code`. Keep the dataset item metadata keys named exactly `source_checkpoint_id`, `replay_disposition`, and `replay_reason_code`. Replace the synthetic replay test setup in `test/scoria/eval_test.exs` and `test/scoria_web/live/dataset_live/promote_component_test.exs` with a runtime-driven or workflow-page-driven replay contract: either build the replay comparison entry through the real runtime helper or derive `promotion_context` from the LiveView seam before submitting the modal. The regression must assert the inserted dataset item metadata contains `source_run_id`, the corrected `source_checkpoint_id`, `replay_disposition`, `replay_reason_code`, `workflow_run_id`, `workflow_step_id`, and `recorded_outcome` after a replay promotion succeeds. Do not keep a synthetic replay contract that bypasses the runtime/LiveView shape.</action>
  <acceptance_criteria>
    - `rg -n "replay_disposition|replay_reason_code|source_checkpoint_id" lib/scoria/eval/dataset_promotion.ex test/scoria/eval_test.exs test/scoria_web/live/dataset_live/promote_component_test.exs` returns matches for dataset metadata persistence assertions.
    - `rg -n "promotion_context|Runtime.get_run_detail!|ReplayComparison" test/scoria/eval_test.exs test/scoria_web/live/dataset_live/promote_component_test.exs` returns at least one match showing runtime-driven replay contract setup.
    - `rg -n "synthetic" test/scoria/eval_test.exs test/scoria_web/live/dataset_live/promote_component_test.exs` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/eval_test.exs</automated>
  </verify>
  <done>Replay promotions persist the corrected metadata into dataset items, and regression coverage now exercises the real runtime-to-LiveView promotion contract instead of a fabricated payload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime replay DTO -> workflow promotion context | wrong lineage or missing replay metadata here corrupts the persisted dataset snapshot |
| workflow promotion context -> eval dataset insert | missing or reshaped replay keys here causes silent evidence loss in immutable dataset items |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-13 | T | `Scoria.Runtime.ReplayComparison` | mitigate | Bind replay provenance `source_checkpoint_id` to the durable replay source lineage field and lock it with a runtime regression test. |
| T-39-14 | R | `ScoriaWeb.WorkflowLive.Show` | mitigate | Forward the runtime-selected replay groups without recomputing field names so the page can prove exactly what evidence was promoted. |
| T-39-15 | I | `Scoria.Eval.DatasetPromotion` | mitigate | Persist `replay_disposition` and `replay_reason_code` from the contract fields already exposed by runtime/UI so immutable dataset metadata does not lose replay safety context. |
| T-39-16 | E | replay promotion test seam | mitigate | Replace synthetic params with runtime-driven promotion contracts so the regression suite exercises the same evidence path operators use in production. |
</threat_model>

<verification>
- `mix test test/scoria/runtime_view_test.exs`
- `mix test test/scoria/eval_test.exs test/scoria_web/live/dataset_live/promote_component_test.exs`
</verification>

<success_criteria>
- Replay promotion uses the durable source checkpoint lineage instead of the replay checkpoint id.
- Replay disposition and replay reason metadata are persisted into dataset item metadata on successful replay promotion.
- Tests fail if the runtime/LiveView replay contract drifts from the persisted dataset metadata contract again.
</success_criteria>

<output>
After completion, create `.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-05-SUMMARY.md`
</output>
