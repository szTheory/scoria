---
phase: 39-replay-operator-ux-draft-dataset-promotion
plan: 4
type: execute
wave: 4
depends_on:
  - 39-02
  - 39-03
files_modified:
  - lib/scoria/workflows.ex
  - lib/scoria/workflows/dataset_promotion.ex
  - lib/scoria/workflows/remote_approval_projection.ex
  - lib/scoria_web/live/dataset_live/promote_component.ex
  - test/scoria/workflows_test.exs
  - test/scoria/workflows/dataset_promotion_test.exs
  - test/scoria/workflows/remote_approval_projection_test.exs
  - test/scoria_web/live/dataset_live/promote_component_test.exs
autonomous: true
requirements:
  - DATA-02
must_haves:
  truths:
    - "Sealed baseline datasets stay visible but immutable from the promotion modal."
    - "Baseline promotion requests route through explicit workflow approval semantics instead of direct dataset-item insertion."
    - "Operators can inspect pending baseline-promotion approvals through the existing workflow projection boundary."
  artifacts:
    - path: "lib/scoria/workflows/dataset_promotion.ex"
      provides: "Workflow-owned baseline approval request service."
    - path: "lib/scoria/workflows.ex"
      provides: "Public workflow boundary exposing baseline-promotion approval helpers."
    - path: "lib/scoria/workflows/remote_approval_projection.ex"
      provides: "Operator-facing projection of baseline-promotion approval lineage."
    - path: "lib/scoria_web/live/dataset_live/promote_component.ex"
      provides: "Promotion UI with visible sealed baselines, approval-required copy, and confirmation flow."
    - path: "test/scoria_web/live/dataset_live/promote_component_test.exs"
      provides: "Promotion modal regression coverage for sealed-baseline approval states."
  key_links:
    - from: "lib/scoria_web/live/dataset_live/promote_component.ex"
      to: "lib/scoria/workflows/dataset_promotion.ex"
      via: "sealed baseline action requests approval instead of mutating the dataset"
      pattern: "request_baseline_promotion|baseline_promotion_requested"
    - from: "lib/scoria/workflows/dataset_promotion.ex"
      to: "lib/scoria/workflows.ex"
      via: "workflow boundary delegates baseline-promotion approval requests and inspection reads"
      pattern: "request_baseline_promotion|list_pending_approvals|get_approval_lineage!"
    - from: "lib/scoria/workflows/remote_approval_projection.ex"
      to: "test/scoria/workflows/remote_approval_projection_test.exs"
      via: "baseline-promotion approvals project explicit lineage and tool_name"
      pattern: "dataset_baseline_promotion|tool_name|replay_disposition"
---

<objective>
Gate sealed baseline promotion through workflow-owned approvals.

Purpose: Phase 39 must keep sealed release-driving datasets visible to operators while preventing direct mutation and recording every baseline-promotion request as durable workflow approval truth.
Output: A workflow-owned baseline approval service, projection coverage for approval lineage, and modal copy/confirmation for sealed targets.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-RESEARCH.md
@.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-PATTERNS.md
@.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-UI-SPEC.md
@.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-02-PLAN.md
@.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-03-PLAN.md
@lib/scoria_web/live/dataset_live/promote_component.ex
@lib/scoria/workflows.ex
@lib/scoria/workflows/remote_approval_projection.ex
@lib/scoria/workflows/prompt_release.ex
@test/scoria/workflows/prompt_release_test.exs
@test/scoria/workflows_test.exs
@test/scoria/workflows/dataset_promotion_test.exs
@test/scoria/workflows/remote_approval_projection_test.exs
@test/scoria_web/live/dataset_live/promote_component_test.exs

<interfaces>
From lib/scoria_web/live/dataset_live/promote_component.ex:
```elixir
def update(assigns, socket) do
def handle_event("save", %{"item" => params}, socket) do
```

From lib/scoria/workflows/prompt_release.ex:
```elixir
def request_remote_approval(run_id, step_id, attrs) do
def approve(approval_id, status, attrs \\ %{}) when status in ["approved", "rejected"] do
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workflow-owned baseline promotion wrappers and approval projection coverage</name>
  <files>lib/scoria/workflows.ex, lib/scoria/workflows/dataset_promotion.ex, lib/scoria/workflows/remote_approval_projection.ex, test/scoria/workflows_test.exs, test/scoria/workflows/dataset_promotion_test.exs, test/scoria/workflows/remote_approval_projection_test.exs</files>
  <read_first>.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-RESEARCH.md, .planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-PATTERNS.md, lib/scoria/workflows.ex, lib/scoria/workflows/remote_approval_projection.ex, lib/scoria/workflows/prompt_release.ex, test/scoria/workflows/prompt_release_test.exs, test/scoria/workflows_test.exs, test/scoria/workflows/dataset_promotion_test.exs, test/scoria/workflows/remote_approval_projection_test.exs</read_first>
  <action>Create `Scoria.Workflows.DatasetPromotion` as the workflow-owned sealed-baseline approval wrapper, following the exact service shell and approval-request transaction posture assigned in `39-PATTERNS.md` for `lib/scoria/workflows/dataset_promotion.ex`, `lib/scoria/workflows.ex`, and `lib/scoria/workflows/remote_approval_projection.ex`, and use the exact `tool_name` value `dataset_baseline_promotion`. Export the wrapper through `Scoria.Workflows` so later operator surfaces call the standard workflow boundary, and extend `Scoria.Workflows.RemoteApprovalProjection` so baseline-promotion approvals can be inspected through the existing projection path with explicit `tool_name`, lineage, and replay fields. Extend `test/scoria/workflows_test.exs` to prove the approval request persists replay-safe workflow evidence, extend `test/scoria/workflows/dataset_promotion_test.exs` to cover the wrapper contract, and extend `test/scoria/workflows/remote_approval_projection_test.exs` to prove the pending approval for `dataset_baseline_promotion` surfaces through the existing projection boundary.</action>
  <acceptance_criteria>
    - `rg -n "dataset_baseline_promotion|request_baseline_promotion" lib/scoria/workflows.ex lib/scoria/workflows/dataset_promotion.ex test/scoria/workflows_test.exs test/scoria/workflows/dataset_promotion_test.exs` returns matches.
    - `rg -n "dataset_baseline_promotion|tool_name|replay_disposition" lib/scoria/workflows/remote_approval_projection.ex test/scoria/workflows/remote_approval_projection_test.exs` returns matches.
    - `rg -n "add_dataset_item|DatasetItem" lib/scoria/workflows/dataset_promotion.ex` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/workflows/dataset_promotion_test.exs test/scoria/workflows/remote_approval_projection_test.exs</automated>
  </verify>
  <done>Sealed-baseline promotion requests persist as workflow approvals with inspectable lineage and without direct dataset-item mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Add sealed-baseline visibility, approval-required copy, and confirmation flow to the promotion modal</name>
  <files>lib/scoria_web/live/dataset_live/promote_component.ex, test/scoria_web/live/dataset_live/promote_component_test.exs</files>
  <read_first>.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-UI-SPEC.md, .planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-PATTERNS.md, lib/scoria_web/live/dataset_live/promote_component.ex, lib/scoria/workflows.ex, test/scoria_web/live/dataset_live/promote_component_test.exs</read_first>
  <action>Update `ScoriaWeb.DatasetLive.PromoteComponent` to keep the current LiveComponent event loop from `39-PATTERNS.md`, but extend the Plan 39-03 open-target flow with a sealed-baseline lane. Load all datasets, split them into `open_datasets` and `sealed_datasets`, and render open targets as selectable rows plus a separate disabled `Sealed baseline` section labeled `Approval required`. Add a dedicated confirmation step headed `Baseline Promotion Approval` that repeats the immutability semantics from the approved UI copy before any request is sent, and require the operator to confirm that step before the sealed-target action may call `Scoria.Workflows.request_baseline_promotion/1`. The sealed-target path must send `{:baseline_promotion_requested, %{dataset_name: ..., dataset_version: ...}}` only after the confirmation step succeeds, without inserting a dataset item. Preserve operator-entered notes and expected output on confirmation failures or approval-request errors.</action>
  <acceptance_criteria>
    - `rg -n "Approval required|Sealed baseline|Request baseline approval|Baseline Promotion Approval|baseline_promotion_requested" lib/scoria_web/live/dataset_live/promote_component.ex test/scoria_web/live/dataset_live/promote_component_test.exs` returns matches.
    - `rg -n "immutable until an explicit approval workflow records the decision" lib/scoria_web/live/dataset_live/promote_component.ex test/scoria_web/live/dataset_live/promote_component_test.exs` returns matches.
    - `rg -n "promote_workflow_source\\(" lib/scoria_web/live/dataset_live/promote_component.ex` does not show the sealed-target action path calling the eval insert boundary.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/dataset_live/promote_component_test.exs</automated>
  </verify>
  <done>Sealed baselines are visible, clearly approval-gated, and require explicit operator confirmation before a workflow approval request is sent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator modal action -> sealed baseline target | direct mutation of sealed datasets would violate immutable release truth |
| workflow approval request -> operator read model | missing tool identity or lineage would make baseline approvals unauditable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-11 | R | baseline approval path | mitigate | Use workflow-owned approval requests with `tool_name: "dataset_baseline_promotion"` so approval lineage and audit events remain durable and reviewable. |
| T-39-17 | E | `ScoriaWeb.DatasetLive.PromoteComponent` | mitigate | Require an explicit baseline confirmation step and route sealed-target actions exclusively through `Scoria.Workflows.request_baseline_promotion/1`. |
| T-39-18 | T | `Scoria.Workflows.RemoteApprovalProjection` | mitigate | Project `tool_name`, lineage, and replay fields for pending approvals so operator inbox views cannot confuse baseline approvals with other remote actions. |
</threat_model>

<verification>
- `mix test test/scoria/workflows/dataset_promotion_test.exs test/scoria/workflows/remote_approval_projection_test.exs`
- `mix test test/scoria_web/live/dataset_live/promote_component_test.exs test/scoria/workflows_test.exs`
</verification>

<success_criteria>
- Sealed datasets remain visible in the modal but cannot be mutated directly.
- Baseline promotion requests reuse workflow approval semantics rather than direct dataset mutation.
- Pending baseline-promotion approvals are inspectable through the existing workflow projection boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/39-replay-operator-ux-draft-dataset-promotion/39-04-SUMMARY.md`
</output>
