---
phase: 31-dockerfile-caching-audit-doc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile.dev
  - docs/docker_dev_dx.md
  - test/scoria/ci_policy_contract_test.exs
autonomous: false
requirements: [CACHE-01]
nyquist_compliant: true

must_haves:
  truths:
    - "Dockerfile.dev contains an invariant comment above COPY lib lib carrying the marker INVARIANT: volatile source"
    - "docs/docker_dev_dx.md contains a 4-row layer-invalidation table at the end of the No rebuild section"
    - "ci_policy_contract_test.exs asserts COPY mix.exs mix.lock precedes COPY config precedes COPY lib"
    - "ci_policy_contract_test.exs asserts the Dockerfile.dev boundary marker is present via @layer_invariant_marker"
    - "mix test --no-start test/scoria/ci_policy_contract_test.exs passes with the new COPY-order test"
    - "A cold docker compose build --progress=plain after a CSS/HEEx touch shows mix deps.get only as CACHED, recorded in SUMMARY"
  artifacts:
    - path: "Dockerfile.dev"
      provides: "Boundary invariant comment above COPY lib lib"
      contains: "INVARIANT: volatile source"
    - path: "docs/docker_dev_dx.md"
      provides: "Layer-invalidation table + bind-mount-vs-cold-build framing"
      contains: "Layer-cache invalidation"
    - path: "test/scoria/ci_policy_contract_test.exs"
      provides: "Static COPY-order + marker contract test and @layer_invariant_marker attribute"
      contains: "@layer_invariant_marker"
  key_links:
    - from: "test/scoria/ci_policy_contract_test.exs"
      to: "Dockerfile.dev"
      via: "File.read! + index_of/2 substring-order assertions and df =~ @layer_invariant_marker"
      pattern: "@layer_invariant_marker"
  prohibitions:
    - "Do NOT reorder Dockerfile.dev COPY/RUN lines — the order is already correct (D-01)"
    - "Do NOT create a make cache-audit target (D-04 rejected)"
    - "Do NOT edit .github/workflows/ci-verify.yml (CI-topology touch)"
    - "Do NOT create any new test file — append to existing ci_policy_contract_test.exs (D-08)"
    - "Do NOT use index_of! (does not exist) — use existing index_of/2"
    - "Do NOT remove the existing # 3) step comment — the docs table cites it as step 3"
    - "Do NOT build Phase 34's doc-string contract test (D-11 is record-only)"
---

<objective>
Empirically prove and permanently guard that `Dockerfile.dev`'s layer order prevents a Mix dependency refetch and full app recompile on a CSS/HEEx-only edit. The order is already correct (D-01) — this phase does NOT change it. It locks the guarantee with three coherent, non-overlapping guards: a boundary invariant comment in `Dockerfile.dev` (humans, D-10), a layer-invalidation table in `docs/docker_dev_dx.md` (mental model, D-06/D-07), and a static COPY-order test folded into the existing policy lane (machines, D-08/D-09) — plus a one-time empirical proof recorded in the SUMMARY (D-04/D-05).

Purpose: Satisfy CACHE-01 — a CSS/HEEx-only source edit triggers no dep refetch and no full app recompile, empirically verified and documented as a table + a layer-order invariant comment, so future contributors cannot regress it silently.

Output: Edits to `Dockerfile.dev`, `docs/docker_dev_dx.md`, and `test/scoria/ci_policy_contract_test.exs` (append only); a phase SUMMARY recording the empirical proof verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-dockerfile-caching-audit-doc/31-CONTEXT.md
@.planning/phases/31-dockerfile-caching-audit-doc/31-RESEARCH.md
@.planning/phases/31-dockerfile-caching-audit-doc/31-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols/strings this phase creates (the drift-verification pass excludes these as newly-created):

- **New `test` block:** `"Dockerfile.dev keeps cache-optimal COPY layer order (deps -> config -> source)"` in `test/scoria/ci_policy_contract_test.exs`
- **New module attribute:** `@layer_invariant_marker "INVARIANT: volatile source"` in `test/scoria/ci_policy_contract_test.exs` (added to the `@attr` block at lines 6–17)
- **New Dockerfile marker string:** `INVARIANT: volatile source` — the load-bearing substring of the boundary invariant comment in `Dockerfile.dev` (above `COPY lib lib`)
- **New doc section heading:** `### Layer-cache invalidation (cold `docker compose up --build` only)` plus its 4-row table in `docs/docker_dev_dx.md` (appended to §"No rebuild on source/style edits")
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the boundary invariant comment to Dockerfile.dev and the layer-invalidation table to docs/docker_dev_dx.md</name>
  <files>Dockerfile.dev, docs/docker_dev_dx.md</files>
  <read_first>
    - Dockerfile.dev (read the WHOLE file; the boundary is L50–52: blank line, then `# 3) Volatile source last...` at L51, then `COPY lib lib` at L52; header rationale is L6–11 with the phrase "Layer order is deliberate" at L7)
    - docs/docker_dev_dx.md (read §"No rebuild on source/style edits" L68–89; the "Cold builds" bullet ends with `don't add them.` ~L87, followed by a blank line ~L88, then `## Adopting this in another repo` ~L89)
  </read_first>
  <action>
    **Dockerfile.dev (D-10):** Insert a 4-line boundary invariant comment block ABOVE the existing `# 3) Volatile source last...` comment line (which sits immediately above `COPY lib lib`). Do NOT reorder any COPY/RUN line (D-01) and do NOT remove the existing `# 3)` comment — the docs table cites it as "step 3" (D-06). The inserted block (verbatim) is:
    `# INVARIANT: volatile source (lib/dev/priv) MUST stay below deps.get + deps.compile.`
    `# Reordering re-fetches/recompiles deps on every source edit. Layer order is deliberate`
    `# — see docs/docker_dev_dx.md (layer-invalidation table). Guarded by`
    `# test/scoria/ci_policy_contract_test.exs.`
    The load-bearing marker substring `INVARIANT: volatile source` MUST appear on the first line and MUST NOT already exist anywhere else in the file (it does not — verified). The phrase "Layer order is deliberate" already exists at L7; that is fine here, but Task 2's marker deliberately does NOT use that phrase.

    **docs/docker_dev_dx.md (D-06/D-07):** Append the bind-mount-vs-cold-build framing paragraph + the 4-row table to the END of §"No rebuild on source/style edits", AFTER the "Cold builds" bullet (`don't add them.`) and BEFORE the `## Adopting this in another repo` heading. Use the RESEARCH §"Code Examples" markdown verbatim (heading `### Layer-cache invalidation (cold `docker compose up --build` only)`, the framing paragraph, then the 4 rows). The load-bearing strings that MUST appear in the table cells, verbatim (D-08): `mix deps.get`, `mix deps.compile`, `mix compile`, `app compile only`, `assets/`, `mix.exs`, `mix.lock`. The CSS row MUST say nothing rebuilds (NOT "app compile only"); only the HEEx/`lib/` row says `app compile only` (per D-02 — the table must tell this honestly, not restate the roadmap's conflated row). Do NOT add a standalone apt/system row — the apt/base layer is folded into the framing paragraph (D-07).
  </action>
  <verify>
    <automated>grep -q 'INVARIANT: volatile source' Dockerfile.dev && grep -q '# 3) Volatile source last' Dockerfile.dev && grep -q 'Layer-cache invalidation' docs/docker_dev_dx.md && grep -q 'app compile only' docs/docker_dev_dx.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `Dockerfile.dev` contains the exact substring `INVARIANT: volatile source` on a comment line directly above the existing `# 3) Volatile source last...` comment, which sits directly above `COPY lib lib`.
    - The existing `# 3) Volatile source last` comment is STILL present (not replaced).
    - No COPY/RUN line in `Dockerfile.dev` changed order vs the pre-edit file (diff shows only insertion of the comment block).
    - `docs/docker_dev_dx.md` contains the heading `### Layer-cache invalidation (cold `docker compose up --build` only)` appended at the end of §"No rebuild on source/style edits", before `## Adopting this in another repo`.
    - The table contains all of these verbatim strings: `mix deps.get`, `mix deps.compile`, `mix compile`, `app compile only`, `assets/`, `mix.exs`, `mix.lock`.
    - The CSS row states nothing rebuilds (does NOT use `app compile only`); only the HEEx/`lib/` row uses `app compile only`.
    - No standalone apt/system layer row exists in the table.
  </acceptance_criteria>
  <done>The Dockerfile boundary marker and the docs layer-invalidation table both exist with all load-bearing strings; the COPY order is unchanged; the `# 3)` step comment is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Append the static COPY-order + marker contract test to ci_policy_contract_test.exs</name>
  <files>test/scoria/ci_policy_contract_test.exs</files>
  <read_first>
    - test/scoria/ci_policy_contract_test.exs (read the module-attribute block at lines 6–17 for the `@attr "string"` idiom; read the existing private helper `defp index_of/2` at ~line 704 — it does `:binary.match` and `flunk`s on `:nomatch`; read lines 626–639 to see the last `test` block ends ~L637 and the first `defp lane_contract_step` begins at L639 — append the new test block between them)
    - Dockerfile.dev (confirm the marker `INVARIANT: volatile source` added in Task 1 is present, since the new test asserts `df =~ @layer_invariant_marker`)
  </read_first>
  <action>
    Add a module attribute `@layer_invariant_marker "INVARIANT: volatile source"` to the existing `@attr` block at lines 6–17 (two-space indented, matching the surrounding `@ci_verify`/`@ci_entry` idiom). The marker value MUST be `INVARIANT: volatile source` — NOT `"Layer order is deliberate"` (that phrase pre-exists in the Dockerfile header at L7 and would make the assertion pass vacuously before the boundary comment exists).

    Append ONE new flat `test` block (no `describe` — this module has none) AFTER the last `test` block (`"planning ledgers..."`, ends ~L637) and BEFORE the first `defp lane_contract_step` (L639). Use the RESEARCH §"Code Examples" block verbatim:
    - `df = File.read!("Dockerfile.dev")`
    - `i_lock = index_of(df, "COPY mix.exs mix.lock")` — use the EXISTING `index_of/2` helper (NO bang; `index_of!` does NOT exist and will not compile). The substring `"COPY mix.exs mix.lock"` is a valid prefix match of the actual `COPY mix.exs mix.lock ./` line.
    - `i_config = index_of(df, "COPY config")`
    - `i_lib = index_of(df, "COPY lib")`
    - `assert i_lock < i_config` (with message "COPY mix.exs mix.lock must precede COPY config")
    - `assert i_config < i_lib` (with message "COPY config must precede COPY lib")
    - `assert df =~ @layer_invariant_marker` (with a message naming the marker) — this couples the human boundary comment to the machine guard.

    Do NOT create a new test file. Do NOT touch `.github/workflows/ci-verify.yml` — the policy-lane step at L56 already runs this file with `--no-start`, so the new test is covered automatically. Do NOT modify `VerificationLanes.closeout_order/0`. This adds test CONTENT only, staying in the `--no-start` policy lane (no DB, no app boot).
  </action>
  <verify>
    <automated>mix test --no-start test/scoria/ci_policy_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/scoria/ci_policy_contract_test.exs` contains `@layer_invariant_marker "INVARIANT: volatile source"` in the module-attribute block.
    - A new flat `test "Dockerfile.dev keeps cache-optimal COPY layer order (deps -> config -> source)"` block exists between the last `test` and the first `defp`.
    - The test uses `index_of(df, ...)` (no bang) three times and asserts `i_lock < i_config` and `i_config < i_lib`.
    - The test asserts `df =~ @layer_invariant_marker`.
    - `mix test --no-start test/scoria/ci_policy_contract_test.exs` passes (exit 0, the new test green). The test would have FAILED on the marker assertion before Task 1 added the boundary comment — confirming the marker is distinct from the pre-existing header phrase.
    - No new file created; `.github/workflows/ci-verify.yml` unchanged; `VerificationLanes.closeout_order/0` unchanged.
  </acceptance_criteria>
  <done>The policy-lane contract test pins both the COPY layer order and the boundary invariant marker; `mix test --no-start test/scoria/ci_policy_contract_test.exs` is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Record the one-time empirical Docker cache proof in SUMMARY</name>
  <files>.planning/phases/31-dockerfile-caching-audit-doc/31-01-SUMMARY.md</files>
  <action>Run the empirical Docker cache proof locally (a human must drive a live Docker daemon — this cannot run in the daemon-free policy lane and is non-deterministic in CI, so it is a one-time recorded snapshot per D-04, NOT a standing build harness and NOT a `make cache-audit` target). Follow the steps in `<how-to-verify>` exactly: warm the cache, touch a real CSS file + a HEEx file, rebuild with `--progress=plain`, grep for `CACHED`/`mix deps.get`. Paste the grep output into the phase SUMMARY as the permanent evidence for criterion 1, and record the Phase 34 hand-off note (D-11, RECORD-ONLY). Do NOT build Phase 34's doc-string contract test.</action>
  <verify>
    <human-check>The phase SUMMARY contains the pasted `grep -nE 'CACHED|mix deps\.get' /tmp/scoria-cacheproof.log` output showing `mix deps.get` as a `CACHED` step (no executing download output), the D-11 hand-off note, and confirmation the COPY order was unchanged.</human-check>
  </verify>
  <done>The empirical proof verdict (deps.get shown as CACHED on a CSS/HEEx-only touch) is recorded in `31-01-SUMMARY.md`, satisfying success criterion 1; the D-11 Phase 34 hand-off note is recorded.</done>
  <what-built>Tasks 1–2 added the boundary invariant comment, the docs layer-invalidation table, and the static COPY-order + marker contract test. This checkpoint records the one-time empirical Docker proof (D-04/D-05) for success criterion 1 — it requires a live Docker daemon + real BuildKit cache and is non-deterministic in CI, so it is run once locally and the verdict is pasted into the phase SUMMARY (no standing build harness, D-04).</what-built>
  <how-to-verify>
    Run these commands from the repo root (Docker 29.5.2 / Compose v5.1.3 / buildx v0.34.0 confirmed available; the compose service is `web`):

    1. Warm the cache first (REQUIRED — skipping this makes the proof vacuously wrong, every step would run uncached):
       `docker compose build web`
    2. Touch a REAL CSS file and a HEEx file (note: `assets/css/app.css` does NOT exist — use `06-utilities.css`, per D-03):
       `touch assets/css/06-utilities.css lib/scoria_web/components/layouts/root.html.heex`
    3. Rebuild with plain progress and capture the log:
       `docker compose build --progress=plain web 2>&1 | tee /tmp/scoria-cacheproof.log`
    4. Confirm the deps step is CACHED, not executing:
       `grep -nE 'CACHED|mix deps\.get' /tmp/scoria-cacheproof.log`

    PASS = the `mix deps.get` step appears only as a `CACHED` step (a `CACHED` marker on/before the `mix deps.get` step line; no download/fetch output follows it). The same holds for `mix deps.compile`. The HEEx touch invalidates `COPY lib lib` → `mix compile` runs (app compile only); the CSS touch invalidates nothing (`assets/` is never COPY'd).

    Optional (Claude's Discretion, D-05 / Open Q2): note that `docker compose up --build web` (the criterion's literal user-facing command) shows no deps activity — its `--progress=auto` collapses cached steps. Cite the `--progress=plain` grep as the authoritative evidence.

    Then RECORD in the phase SUMMARY: paste the grepped line(s) + the `CACHED` markers for the `mix deps.get` / `mix deps.compile` steps as the permanent empirical evidence for criterion 1 (D-04/D-05). Also record the Phase 34 hand-off note (D-11, RECORD-ONLY): Phase 34's `docker_dx_doc_contract_test.exs` should assert the table's load-bearing strings (`mix deps.get`, `mix deps.compile`, `app compile only`) survive in `docs/docker_dev_dx.md`; and Dockerfile layer-order was absent from every phase's stated criteria until D-08 closed it — if D-08 were ever dropped, Phase 34 must absorb a Dockerfile-order assertion. Do NOT build Phase 34's test here.
  </how-to-verify>
  <resume-signal>Type "approved" once the CACHED proof is recorded in SUMMARY (paste the grep output), or describe what the build output showed instead.</resume-signal>
</task>

</tasks>

<verification>
- `grep -q 'INVARIANT: volatile source' Dockerfile.dev` → present (boundary comment, criterion 2).
- `grep -q '# 3) Volatile source last' Dockerfile.dev` → present (step-3 comment preserved).
- `docker compose config` is unaffected; `Dockerfile.dev` COPY/RUN order unchanged (D-01).
- `docs/docker_dev_dx.md` contains `### Layer-cache invalidation` heading + 4-row table with `mix deps.get`, `mix deps.compile`, `mix compile`, `app compile only`, `assets/`, `mix.exs`, `mix.lock` (criterion 3).
- `mix test --no-start test/scoria/ci_policy_contract_test.exs` → green, including the new COPY-order + marker test.
- Per-wave merge gate: `mix test --no-start --warnings-as-errors test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs test/scoria/adoption_surface_test.exs` → green.
- Empirical proof (`docker compose build --progress=plain web` after CSS/HEEx touch) shows `mix deps.get` only as `CACHED` — recorded in SUMMARY (criterion 1).
</verification>

<success_criteria>
1. Editing a real CSS file (`assets/css/06-utilities.css`) or a `lib/**/*.heex` then `docker compose build --progress=plain web` produces no executing `mix deps.get` (only `CACHED`) — recorded in the phase SUMMARY.
2. `Dockerfile.dev` contains an invariant comment at the COPY/RUN boundary (above `COPY lib lib`) carrying the marker `INVARIANT: volatile source`, explaining the order is deliberate and must not be violated.
3. `docs/docker_dev_dx.md` contains a layer-invalidation table covering at minimum: CSS/HEEx-only edit → app compile only (CSS = nothing rebuilds, honestly); `config/` edit → `mix deps.compile` + `mix compile`; `mix.exs`/`mix.lock` edit → full dep rebuild.
4. The static COPY-order + marker test passes in the policy lane on every `mix test --no-start`, mechanically preventing silent regression.
</success_criteria>

<output>
Create `.planning/phases/31-dockerfile-caching-audit-doc/31-01-SUMMARY.md` when done. The SUMMARY MUST include:
- The empirical proof block: the `grep -nE 'CACHED|mix deps\.get' /tmp/scoria-cacheproof.log` output showing `mix deps.get` as `CACHED` (D-04/D-05).
- The Phase 34 hand-off note (D-11, RECORD-ONLY).
- Confirmation the COPY order was NOT changed (documents + guards only, D-01).
</output>
