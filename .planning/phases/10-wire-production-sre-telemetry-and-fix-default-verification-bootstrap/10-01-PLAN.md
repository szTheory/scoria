---
phase: 10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/sre/telemetry_identity.ex
  - lib/scoria/sre/telemetry.ex
  - lib/scoria/workflows/runtime.ex
  - lib/scoria/mcp/executor.ex
  - test/scoria/sre/telemetry_test.exs
  - test/scoria/workflows/runtime_telemetry_test.exs
  - test/scoria/mcp/executor_telemetry_test.exs
autonomous: true
requirements:
  - SRE-04
must_haves:
  truths:
    - "Workflow runtime emits live reason-coded SRE telemetry from the real execution seam."
    - "MCP execution emits the same low-cardinality SRE identity contract instead of inventing a second label shape."
    - "Raw runtime and MCP telemetry groups by canonical operational identity and keeps trace or run refs out of grouping labels."
  artifacts:
    - path: "lib/scoria/sre/telemetry_identity.ex"
      provides: "Shared canonical identity and identity_key builder for live SRE telemetry"
    - path: "lib/scoria/workflows/runtime.ex"
      provides: "Workflow runtime telemetry producers on real execution outcomes"
    - path: "lib/scoria/mcp/executor.ex"
      provides: "MCP execution telemetry producers on real tool outcomes"
  key_links:
    - from: "lib/scoria/workflows/runtime.ex"
      to: "lib/scoria/sre/telemetry.ex"
      via: "live runtime outcome emission"
      pattern: "Scoria\\.SRE\\.Telemetry"
    - from: "lib/scoria/mcp/executor.ex"
      to: "lib/scoria/sre/telemetry_identity.ex"
      via: "shared identity_key derivation"
      pattern: "identity_key|TelemetryIdentity"
---

<objective>
Make `Scoria.SRE.Telemetry` a real production seam at workflow and MCP execution boundaries.

Purpose: close the live-path half of `SRE-04` by emitting reason-coded runtime telemetry from the two actual execution seams, using one canonical low-cardinality identity contract.
Output: shared telemetry identity module, runtime and MCP telemetry wiring, and focused execution-seam regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/milestones/v1.3-REQUIREMENTS.md
@.planning/v1.3-MILESTONE-AUDIT.md
@.planning/phases/10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap/10-CONTEXT.md
@.planning/phases/10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap/10-RESEARCH.md
@.planning/phases/10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap/10-VALIDATION.md
@.planning/phases/07-seismograph/07-03-PLAN.md
@.planning/phases/08-reconcile-budget-reservations-on-breaker-open-paths/08-01-PLAN.md
@lib/scoria/sre/telemetry.ex
@lib/scoria/workflows/runtime.ex
@lib/scoria/mcp/executor.ex

<interfaces>
From lib/scoria/sre/telemetry.ex:
```elixir
def emit_latency(attrs)
def emit_cost(attrs)
def emit_quality(attrs)
def emit_budget_burn(attrs)
def emit_breaker_state(attrs)
def emit_tool_reliability(attrs)
```

From lib/scoria/workflows/runtime.ex:
```elixir
def execute_step(step_id, opts \\ [])
```

From lib/scoria/mcp/executor.ex:
```elixir
def execute(tool_module, args, context, timeout \\ 5000)
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Define the Canonical Runtime Telemetry Identity Contract</name>
  <files>lib/scoria/sre/telemetry_identity.ex, lib/scoria/sre/telemetry.ex, test/scoria/sre/telemetry_test.exs</files>
  <action>Create `Scoria.SRE.TelemetryIdentity` as the single builder for low-cardinality operational identity per D-05, D-06, D-07, D-08, D-09, and D-10. The contract must derive shared fields from stable dimensions such as `tenant_id`, `subject_kind`, `policy_key`, `reason_code`, and `window_bucket`, plus a deterministic `identity_key`; keep `incident_key` optional at raw runtime time per D-08 and keep `trace_id` and `run_id` in correlation refs only per D-09. Update `Scoria.SRE.Telemetry` to consume that contract instead of hand-selecting mixed metadata keys, and extend helper tests so runtime labels, refs, and `identity_key` are asserted explicitly.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/sre/telemetry_test.exs -x</automated>
  </verify>
  <done>`Scoria.SRE.Telemetry` emits a shared canonical identity shape with `identity_key`, stable grouping dimensions, and ref-only correlation fields.</done>
</task>

<task type="tdd">
  <name>Task 2: Emit Live Runtime and MCP Telemetry from Real Execution Outcomes</name>
  <files>lib/scoria/workflows/runtime.ex, lib/scoria/mcp/executor.ex, lib/scoria/sre/telemetry.ex, test/scoria/workflows/runtime_telemetry_test.exs, test/scoria/mcp/executor_telemetry_test.exs</files>
  <action>Wire `Scoria.Workflows.Runtime` and `Scoria.MCP.Executor` into `Scoria.SRE.Telemetry` at the true execution seams per D-01 and D-06. Use the shared identity builder from Task 1 so both seams classify outcomes the same way, emit reason-coded latency, cost, budget-burn, breaker-state, and tool-reliability signals from completed, timeout, execution-failed, handler-error, access-denied, and breaker-open paths, and keep this slice in the runtime namespace only per D-04. Do not emit incident lifecycle events here, and do not group by run-specific ids, tool args, or other high-cardinality refs per D-04 and D-09.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/workflows/runtime_telemetry_test.exs test/scoria/mcp/executor_telemetry_test.exs -x</automated>
  </verify>
  <done>Workflow and MCP execution both publish live SRE telemetry with one shared identity contract and no incident double-counting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime/MCP outcome classification -> telemetry bus | Internal execution results become externally consumable SRE signals |
| Correlation refs -> grouping labels | High-cardinality execution refs risk leaking into operator grouping keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Information Disclosure | `Scoria.SRE.Telemetry` metadata | mitigate | Block completion until `trace_id`, `run_id`, tool args, and other refs stay out of grouping labels by centralizing label/ref derivation in `Scoria.SRE.TelemetryIdentity` and asserting it in focused tests. |
| T-10-01-02 | Tampering | runtime/MCP outcome semantics | mitigate | Block completion until runtime and MCP share one identity and outcome-classification contract so one seam cannot drift into incompatible reason codes or keys. |
| T-10-01-03 | Repudiation | live runtime telemetry evidence | mitigate | Emit only from `Scoria.Workflows.Runtime` and `Scoria.MCP.Executor` per D-01 so every signal can be traced back to a real execution boundary instead of caller-side best effort instrumentation. |
</threat_model>

<verification>
- `test/scoria/sre/telemetry_test.exs` proves canonical labels, refs, and `identity_key`.
- `test/scoria/workflows/runtime_telemetry_test.exs` proves runtime outcomes emit live SRE telemetry.
- `test/scoria/mcp/executor_telemetry_test.exs` proves MCP outcomes emit the same shared telemetry contract.
</verification>

<success_criteria>
- `SRE-04` is materially closed at the workflow and MCP execution seams.
- Runtime telemetry uses one low-cardinality identity contract shared across both live producers.
- Runtime telemetry remains semantically distinct from later incident lifecycle telemetry.
</success_criteria>

<output>
After completion, create `.planning/phases/10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap/10-01-SUMMARY.md`
</output>
