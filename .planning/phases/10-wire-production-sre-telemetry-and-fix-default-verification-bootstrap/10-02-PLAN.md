---
phase: 10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - lib/scoria/sre/telemetry.ex
  - lib/scoria/sre/adapters/parapet.ex
  - lib/scoria/sre/incident_manager.ex
  - lib/scoria/sre.ex
  - test/scoria/sre/incident_telemetry_test.exs
  - test/scoria/sre/parapet_translation_test.exs
autonomous: true
requirements:
  - SRE-04
must_haves:
  truths:
    - "Durable incident creation emits a separate post-commit telemetry namespace instead of reusing runtime SLI events."
    - "Incident lifecycle telemetry reuses the same canonical operational identity and identity_key that runtime telemetry already uses."
    - "Parapet-facing translation groups on canonical labels without pretending every runtime event already has an incident_key."
  artifacts:
    - path: "lib/scoria/sre/incident_manager.ex"
      provides: "Post-commit incident lifecycle telemetry at the durable incident seam"
    - path: "lib/scoria/sre/adapters/parapet.ex"
      provides: "Canonical translation of runtime and incident telemetry into Parapet-facing envelopes"
    - path: "lib/scoria/sre.ex"
      provides: "Public SRE seam that emits incident telemetry only after committed incident writes"
  key_links:
    - from: "lib/scoria/sre/incident_manager.ex"
      to: "lib/scoria/sre/telemetry.ex"
      via: "post-commit incident lifecycle emission"
      pattern: "incident"
    - from: "lib/scoria/sre/adapters/parapet.ex"
      to: "lib/scoria/sre/telemetry.ex"
      via: "canonical label/ref translation"
      pattern: "identity_key|incident_key"
---

<objective>
Add the second half of the telemetry contract: post-commit incident lifecycle signals and Parapet alignment.

Purpose: finish `SRE-04` without double-counting runtime failures by emitting separate incident lifecycle telemetry only after durable incident state commits, while keeping Parapet translation aligned to one canonical identity model.
Output: incident lifecycle telemetry emitters, post-commit SRE wiring, Parapet translation updates, and focused incident telemetry regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/milestones/v1.3-REQUIREMENTS.md
@.planning/v1.3-MILESTONE-AUDIT.md
@.planning/phases/10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap/10-CONTEXT.md
@.planning/phases/10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap/10-RESEARCH.md
@.planning/phases/10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap/10-VALIDATION.md
@.planning/phases/07-seismograph/07-04-SUMMARY.md
@.planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-02-SUMMARY.md
@.planning/phases/09-restore-audited-approval-and-incident-delivery-wiring/09-03-SUMMARY.md
@lib/scoria/sre/incident_manager.ex
@lib/scoria/sre/adapters/parapet.ex
@lib/scoria/sre.ex

<interfaces>
From lib/scoria/sre/incident_manager.ex:
```elixir
def record_alert_event(envelope) when is_map(envelope)
def open_incident(attrs) when is_map(attrs)
def append_incident_event(%Incident{} = incident, attrs) when is_map(attrs)
```

From lib/scoria/sre/adapters/parapet.ex:
```elixir
def translate([:scoria, :sre, :sli, category], measurements, metadata)
```

From lib/scoria/sre.ex:
```elixir
def record_alert_event(envelope) when is_map(envelope)
def append_incident_event(incident, attrs)
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Emit Separate Post-Commit Incident Lifecycle Telemetry</name>
  <files>lib/scoria/sre/telemetry.ex, lib/scoria/sre/incident_manager.ex, lib/scoria/sre.ex, test/scoria/sre/incident_telemetry_test.exs</files>
  <action>Extend `Scoria.SRE.Telemetry` with a distinct incident lifecycle namespace and wire it from the durable incident seam per D-02, D-04, D-06, D-07, and D-08. Emit only after `Scoria.SRE.IncidentManager` successfully commits incident, alert-event, incident-event, and delivery-intent writes so telemetry reflects durable truth; the runtime namespace from Plan 10-01 must remain untouched. Reuse the canonical operational identity and `identity_key` from D-06 and D-07, allow `incident_key` to become a derived projection only after incident materialization per D-08, and keep delivery or relay transport truth DB-first per D-03.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/sre/incident_telemetry_test.exs -x</automated>
  </verify>
  <done>Incident creation and escalation emit separate post-commit telemetry with shared identity and no runtime double-counting.</done>
</task>

<task type="tdd">
  <name>Task 2: Align Parapet Translation with Canonical Identity and Namespace Separation</name>
  <files>lib/scoria/sre/adapters/parapet.ex, lib/scoria/sre/telemetry.ex, test/scoria/sre/parapet_translation_test.exs</files>
  <action>Update `Scoria.SRE.Adapters.Parapet` so runtime and incident telemetry translate through one canonical label/ref split per D-10 and D-18. Runtime events must group on `identity_key` plus canonical low-cardinality labels even when `incident_key` is absent per D-08; incident lifecycle events may add the materialized `incident_key`, but they must stay in their own namespace per D-04. Keep relay and delivery semantics DB-first per D-03 without taking ownership of `test/scoria/sre/relay_test.exs`; that suite is intentionally reserved for the bootstrap cleanup plan that removes default-lane `ensure_*` helpers.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/sre/parapet_translation_test.exs -x</automated>
  </verify>
  <done>Parapet-facing helpers consume one canonical identity model across runtime and incident telemetry without collapsing the two namespaces into one burn stream.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Durable incident transaction -> telemetry bus | Committed incident lifecycle state becomes externally consumable telemetry |
| Canonical telemetry metadata -> Parapet translation | Public helper output must preserve low-cardinality grouping and ref discipline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Tampering | runtime vs incident telemetry semantics | mitigate | Block completion until incident lifecycle events use a separate namespace and emit only after commit per D-02 and D-04, preventing one failure from burning both runtime and incident budgets. |
| T-10-02-02 | Repudiation | incident lifecycle evidence | mitigate | Emit from `Scoria.SRE.IncidentManager` only after the transaction succeeds so every incident telemetry event corresponds to durable incident rows and delivery intent. |
| T-10-02-03 | Information Disclosure | Parapet translation | mitigate | Preserve the canonical label/ref split from Plan 10-01 so Parapet grouping stays low-cardinality and correlation refs remain refs-only per D-10. |
</threat_model>

<verification>
- `test/scoria/sre/incident_telemetry_test.exs` proves incident telemetry emits only after durable writes succeed.
- `test/scoria/sre/incident_test.exs` remains owned by the bootstrap-cleanup plan so the default-lane incident suite and helper removal stay in one place.
- `test/scoria/sre/parapet_translation_test.exs` proves canonical labels and refs translate consistently across runtime and incident namespaces.
- Relay and delivery DB-truth verification remains covered in the dependent bootstrap-cleanup plan so `test/scoria/sre/relay_test.exs` has one owner.
</verification>

<success_criteria>
- `SRE-04` is closed end-to-end across runtime, MCP, incident, and Parapet translation seams.
- Runtime telemetry and incident telemetry remain distinct public contracts.
- Canonical `identity_key` and low-cardinality dimensions are shared across all SRE telemetry consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap/10-02-SUMMARY.md`
</output>
