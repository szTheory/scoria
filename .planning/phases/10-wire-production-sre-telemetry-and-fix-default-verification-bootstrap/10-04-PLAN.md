---
phase: 10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap
plan: 04
type: execute
wave: 2
depends_on:
  - 03
files_modified:
  - test/scoria/mcp/executor_test.exs
  - test/scoria/workflows/integration_test.exs
  - test/scoria/workflows/runtime_test.exs
  - test/scoria/sre/audit_outbox_test.exs
  - test/scoria/sre/incident_test.exs
  - test/scoria/sre/relay_test.exs
  - test/scoria_web/live/orchestrator_live_sre_test.exs
  - test/scoria_web/live/orchestrator_live_test.exs
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - SRE-08
must_haves:
  truths:
    - "Default-lane backend and LiveView suites stop creating SRE tables with local `ensure_*` helpers and instead rely on the repaired core bootstrap."
    - "The remaining helper-bearing suites called out in D-14 all move onto the boring default lane or are explicitly kept on the knowledge lane only when they truly require pgvector."
    - "CI continuously proves both the boring default core lane and the explicit knowledge/full lane."
  artifacts:
    - path: "test/scoria/mcp/executor_test.exs"
      provides: "Default-lane MCP coverage without `ensure_*` audit-table patching"
    - path: "test/scoria/sre/incident_test.exs"
      provides: "Incident lifecycle coverage without local incident-table creation helpers"
    - path: ".github/workflows/ci.yml"
      provides: "Two-lane CI contract for core/SRE and knowledge/full verification"
  key_links:
    - from: "test/scoria/workflows/runtime_test.exs"
      to: "test/test_helper.exs"
      via: "default migrated core/SRE schema"
      pattern: "Approval|audit|Repo"
    - from: ".github/workflows/ci.yml"
      to: "mix test and mix test.knowledge"
      via: "two-lane CI contract"
      pattern: "test\\.knowledge|mix test"
---

<objective>
Remove the remaining default-lane `ensure_*` schema patchwork and lock in two-lane CI.

Purpose: finish the D-14, D-15, and D-17 half of `SRE-08` by moving every remaining helper-bearing default-lane suite onto the repaired core bootstrap, while keeping the knowledge/full lane explicit and continuously verified.
Output: cleaned backend and LiveView test suites, no remaining default-lane `ensure_*` schema helpers in the named files, and CI that runs both lane entrypoints.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/milestones/v1.3-REQUIREMENTS.md
@.planning/v1.3-MILESTONE-AUDIT.md
@.planning/phases/10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap/10-CONTEXT.md
@.planning/phases/10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap/10-RESEARCH.md
@.planning/phases/10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap/10-VALIDATION.md
@.planning/phases/10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap/10-03-PLAN.md
@test/scoria/mcp/executor_test.exs
@test/scoria/workflows/integration_test.exs
@test/scoria/workflows/runtime_test.exs
@test/scoria/sre/audit_outbox_test.exs
@test/scoria/sre/incident_test.exs
@test/scoria/sre/relay_test.exs
@test/scoria_web/live/orchestrator_live_sre_test.exs
@test/scoria_web/live/orchestrator_live_test.exs
@.github/workflows/ci.yml
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Remove Default-Lane `ensure_*` Helpers from Backend SRE and Workflow Suites</name>
  <files>test/scoria/mcp/executor_test.exs, test/scoria/workflows/integration_test.exs, test/scoria/workflows/runtime_test.exs, test/scoria/sre/audit_outbox_test.exs, test/scoria/sre/incident_test.exs, test/scoria/sre/relay_test.exs</files>
  <action>Eliminate the architectural need for test-local schema patching per D-14 in every remaining backend suite that still provisions core/SRE tables manually. This task must remove the helper-bearing default-lane setup from `test/scoria/mcp/executor_test.exs`, `test/scoria/workflows/integration_test.exs`, `test/scoria/workflows/runtime_test.exs`, `test/scoria/sre/audit_outbox_test.exs`, `test/scoria/sre/incident_test.exs`, and `test/scoria/sre/relay_test.exs`. After the cleanup, these suites should rely on the repaired default bootstrap from Plan 10-03 for audit outbox, incident, notification delivery, and related SRE tables instead of `ensure_audit_outbox_table!`, `ensure_notification_delivery_table!`, or `ensure_incident_tables!`. Keep DB rows as truth per D-03; this is schema bootstrap cleanup, not a transport-semantics change.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/mcp/executor_test.exs test/scoria/workflows/integration_test.exs test/scoria/workflows/runtime_test.exs test/scoria/sre/audit_outbox_test.exs test/scoria/sre/incident_test.exs test/scoria/sre/relay_test.exs -x</automated>
  </verify>
  <done>The named backend suites pass on the default lane without local `ensure_*` schema helpers.</done>
</task>

<task type="tdd">
  <name>Task 2: Remove Default-Lane LiveView Helpers and Enforce Two-Lane CI</name>
  <files>test/scoria_web/live/orchestrator_live_sre_test.exs, test/scoria_web/live/orchestrator_live_test.exs, .github/workflows/ci.yml</files>
  <action>Finish D-14 for the remaining UI-facing default-lane suites by removing schema setup helpers from `test/scoria_web/live/orchestrator_live_sre_test.exs` and `test/scoria_web/live/orchestrator_live_test.exs`, including the current `ensure_sre_tables!` and audit-outbox helper paths. Then update `.github/workflows/ci.yml` per D-15 so CI runs both explicit lanes: the boring default core/SRE path via ordinary `mix test`, and the explicit knowledge/full path via `mix test.knowledge` from Plan 10-03. Make the common path the shortest path per D-17 by keeping the default lane repo-native, batteries-included, and free of manual pgvector ceremony, while reserving the knowledge/full lane for the explicit opt-in command only. Do not add a third ad hoc bootstrap lane or reintroduce manual pgvector ceremony into the default path.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_sre_test.exs test/scoria_web/live/orchestrator_live_test.exs -x && MIX_ENV=test mix test.knowledge</automated>
  </verify>
  <done>The remaining LiveView suites stop using local schema helpers, and CI continuously proves both verification lanes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Default migrated schema -> focused test suites | Suites trust the shared bootstrap instead of suite-local DDL |
| CI lane selection -> repo verification contract | Continuous verification must reflect the same default and opt-in paths contributors use locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Tampering | focused test schema truth | mitigate | Remove suite-local `ensure_*` schema helpers from every remaining default-lane file named in D-14 so tests cannot drift away from migration-driven schema truth. |
| T-10-04-02 | Denial of Service | local verification ergonomics | mitigate | Keep the ordinary core/SRE lane as the default path and prove the named suites pass there without pgvector-specific setup. |
| T-10-04-03 | Repudiation | CI verification contract | mitigate | Update `.github/workflows/ci.yml` to run exactly the same two explicit lanes promised to contributors: default `mix test` and explicit `mix test.knowledge`, and verify the knowledge lane command itself instead of only inspecting CI text. |
</threat_model>

<verification>
- Backend helper cleanup is proven by the focused backend suite batch running successfully on the default lane.
- LiveView helper cleanup is proven by the focused LiveView suite batch running successfully on the default lane.
- Two-lane CI is proven by the focused LiveView suite batch plus a real `mix test.knowledge` execution, matching the explicit lane contract contributors will use.
</verification>

<success_criteria>
- All remaining default-lane helper-bearing suites named by the checker are accounted for and moved off `ensure_*` schema patching.
- `SRE-08` no longer depends on suite-local DDL in core/SRE or operator-surface tests.
- CI continuously enforces the boring default lane plus the explicit knowledge/full lane, with the default path remaining the least-surprise entrypoint per D-17.
</success_criteria>

<output>
After completion, create `.planning/phases/10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap/10-04-SUMMARY.md`
</output>
