---
phase: 10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - test/test_helper.exs
  - test/support/knowledge_case.exs
  - lib/mix/tasks/scoria.pgvector.bootstrap.ex
  - lib/mix/tasks/scoria.test.knowledge.ex
  - priv/repo/migrations/20260511000300_create_knowledge_tables.exs
  - priv/repo/knowledge_migrations/20260511000300_create_knowledge_tables.exs
  - test/scoria/bootstrap/migration_lane_compatibility_test.exs
autonomous: true
requirements:
  - SRE-08
must_haves:
  truths:
    - "Ordinary `mix test` runs core and SRE verification without pgvector bootstrap or Docker steps."
    - "Knowledge and pgvector verification remain explicit, first-class, and runnable through a separate blessed lane that keeps migration version `20260511000300` compatible for already-migrated environments."
    - "Fresh core-only environments and already-migrated knowledge environments can both run the new lane routing safely."
  artifacts:
    - path: "mix.exs"
      provides: "Default core test alias plus explicit knowledge/full lane entrypoint"
    - path: "lib/mix/tasks/scoria.test.knowledge.ex"
      provides: "Blessed explicit knowledge/full verification lane"
    - path: "priv/repo/knowledge_migrations/20260511000300_create_knowledge_tables.exs"
      provides: "Knowledge migration kept explicit behind the pgvector-aware path"
    - path: "test/scoria/bootstrap/migration_lane_compatibility_test.exs"
      provides: "Executable proof that fresh core-only and already-migrated knowledge environments both behave safely"
  key_links:
    - from: "mix.exs"
      to: "priv/repo/knowledge_migrations/20260511000300_create_knowledge_tables.exs"
      via: "explicit knowledge/full lane only"
      pattern: "knowledge|migrations-path"
    - from: "test/test_helper.exs"
      to: "mix.exs"
      via: "boring default core bootstrap path"
      pattern: "ecto|migrate|test"
---

<objective>
Fix the default verification bootstrap so core and SRE work use a boring Postgres lane while pgvector-backed knowledge remains explicit and first-class.

Purpose: close the migration-routing half of `SRE-08` by moving the split to the migration/bootstrap boundary and preserving compatibility for both fresh core-only and already-migrated knowledge environments.
Output: default core-lane test/bootstrap wiring, explicit knowledge/full lane task or alias, knowledge migration split, and compatibility-focused regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/milestones/v1.3-REQUIREMENTS.md
@.planning/v1.3-MILESTONE-AUDIT.md
@.planning/phases/10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap/10-CONTEXT.md
@.planning/phases/10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap/10-RESEARCH.md
@.planning/phases/10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap/10-VALIDATION.md
@.planning/phases/07-seismograph/07-04-SUMMARY.md
@mix.exs
@test/test_helper.exs
@test/support/knowledge_case.exs
@lib/mix/tasks/scoria.pgvector.bootstrap.ex
@priv/repo/migrations/20260511000300_create_knowledge_tables.exs
@priv/repo/migrations/20260511171000_create_sre_incident_and_audit_tables.exs

<interfaces>
From lib/mix/tasks/scoria.pgvector.bootstrap.ex:
```elixir
def run(args)
def configure_runtime_env
def ensure_pgvector!
```

From test/support/knowledge_case.exs:
```elixir
setup tags do
  Mix.Tasks.Scoria.Pgvector.Bootstrap.ensure_pgvector!()
end
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Split the Core and Knowledge Bootstrap Paths at the Migration Boundary</name>
  <files>mix.exs, lib/mix/tasks/scoria.pgvector.bootstrap.ex, lib/mix/tasks/scoria.test.knowledge.ex, priv/repo/migrations/20260511000300_create_knowledge_tables.exs, priv/repo/knowledge_migrations/20260511000300_create_knowledge_tables.exs, test/scoria/bootstrap/migration_lane_compatibility_test.exs</files>
  <action>Move the knowledge migration behind an explicit pgvector-aware path per D-11, D-12, and D-13 without rewriting its version semantics. Implement the locked compatibility mechanism exactly: the default core lane uses migration paths that omit knowledge migrations entirely, while the explicit knowledge/full lane includes the existing knowledge migration version `20260511000300` in its own path plus any later knowledge follow-up migrations. Do not renumber, replace, or mutate the historical meaning of `20260511000300`; already-migrated environments keep that recorded version and safely skip it when the explicit knowledge lane reruns, while fresh core-only environments never encounter that version until they opt into the knowledge lane. Add a focused compatibility regression in `test/scoria/bootstrap/migration_lane_compatibility_test.exs` that proves both cases: a fresh core-only bootstrap reaches the later SRE tables without pgvector, and an already-migrated knowledge environment can rerun the new explicit knowledge lane idempotently.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/bootstrap/migration_lane_compatibility_test.exs -x</automated>
  </verify>
  <done>Lane-specific migration routing is in place, `20260511000300` stays compatible for already-migrated environments, and the explicit knowledge/full lane is a repo-native command.</done>
</task>

<task type="tdd">
  <name>Task 2: Make the Default Test Bootstrap Boring and Keep Knowledge Opt-In</name>
  <files>mix.exs, test/test_helper.exs, test/support/knowledge_case.exs, lib/mix/tasks/scoria.pgvector.bootstrap.ex, lib/mix/tasks/scoria.test.knowledge.ex</files>
  <action>Wire the repaired migration split into the ordinary repo entrypoints so `mix test` becomes the boring default per D-11 and D-16, while `KnowledgeCase` and the explicit knowledge/full command remain the opt-in pgvector seam per D-12. `test/test_helper.exs` should migrate only the core/SRE path on plain Postgres, and `test/support/knowledge_case.exs` should keep knowledge bootstrap explicit instead of leaking it into unrelated suites. Do not remove focused-suite `ensure_*` helpers or change CI in this plan; that cleanup is intentionally split into the dependent follow-on plan so file ownership stays narrow and execution order remains unambiguous.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/sre_test.exs -x</automated>
  </verify>
  <done>Ordinary `mix test` uses the core-only bootstrap path by default, and the explicit knowledge/full lane remains separate and first-class.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo bootstrap -> test database schema | Default verification commands create persistent schema state for developers and CI |
| Explicit knowledge lane -> pgvector-aware database | Optional vector infrastructure must remain isolated from the core default lane |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Denial of Service | default test bootstrap | mitigate | Block completion until ordinary `mix test` reaches core and SRE tables without pgvector so contributors are not hard-blocked by unrelated optional infra. |
| T-10-03-02 | Tampering | migration history compatibility | mitigate | Preserve version `20260511000300` as-is and prove with a focused compatibility regression that already-migrated knowledge environments safely skip it when the explicit lane reruns. |
| T-10-03-03 | Repudiation | bootstrap contract | mitigate | Keep the default core lane and explicit knowledge lane as separate repo-native entrypoints so operator and contributor expectations match what the code actually migrates. |
</threat_model>

<verification>
- `test/scoria/bootstrap/migration_lane_compatibility_test.exs` proves both a fresh core-only bootstrap and an already-migrated knowledge environment behave safely under the new lane routing.
- `mix test` proves core/SRE verification works on the default Postgres lane without pgvector bootstrap.
- The explicit knowledge/full lane is introduced in repo code here, then exercised as part of the dependent CI-oriented cleanup plan and wave-close validation.
</verification>

<success_criteria>
- `SRE-08` is closed at the migration/bootstrap boundary rather than by documentation or test tags alone.
- Default verification is boring for core and SRE contributors.
- Knowledge and pgvector remain explicit, first-class, and compatible with existing migration history.
</success_criteria>

<output>
After completion, create `.planning/phases/10-wire-production-sre-telemetry-and-fix-default-verification-bootstrap/10-03-SUMMARY.md`
</output>
