---
phase: 63-manifest-check-fingerprint-hardening
plan: 03
type: execute
wave: 2
depends_on: [63-02]
files_modified:
  - docs/operator_verification.md
  - docs/adoption_lanes.md
  - test/mix/tasks/scoria.install_check_test.exs
  - test/scoria/adoption_surface_test.exs
  - test/scoria/install/mode_equivalence_test.exs
autonomous: true
requirements: [INST-07]
must_haves:
  truths:
    - "Operators have Check vs apply drift detection subsection in operator_verification.md (D-11)."
    - "Tampered manifest fingerprints do not change check classification or exit (D-18)."
    - "Compliant host without manifest file exits 0 — manifest absence informational only (D-07, D-09)."
    - "Stale apply fingerprint test and mode equivalence remain green (D-19)."
    - "adoption_surface_test pins new operator phrases (D-13)."
  artifacts:
    - path: "docs/operator_verification.md"
      provides: "Operator-facing check vs apply fingerprint semantics"
      contains: "Check vs apply drift detection"
    - path: "test/mix/tasks/scoria.install_check_test.exs"
      provides: "Contract tests for manifest tamper and absent manifest"
      contains: "manifest"
  key_links:
    - from: "docs/operator_verification.md"
      to: "test/scoria/adoption_surface_test.exs"
      via: "phrase pins prevent doc drift"
      pattern: "Check vs apply"
---

<objective>
Document the check vs apply fingerprint contract for operators and lock it with integration tests plus adoption doc pins.

Purpose: Close Phase 63 success criteria 1 and 3 — documented semantics with targeted non-regression tests.
Output: Operator guide subsection, adoption cross-link, and install check contract tests.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/63-manifest-check-fingerprint-hardening/63-CONTEXT.md
@.planning/phases/61-proof-and-stability-closeout/61-CONTEXT.md
@docs/operator_verification.md
@test/mix/tasks/scoria.install_check_test.exs
@test/scoria/adoption_surface_test.exs
@test/support/scoria/host_install_fixtures.ex

Do not add fingerprint semantics to README. Do not create new top-level doc files (D-12).
</context>

<tasks>

<task type="auto">
  <name>Task 63-03-01: Add operator Check vs apply drift detection subsection</name>
  <requirement_ids>INST-07</requirement_ids>
  <files>docs/operator_verification.md, docs/adoption_lanes.md</files>
  <read_first>
    docs/operator_verification.md
    docs/adoption_lanes.md
    .planning/phases/63-manifest-check-fingerprint-hardening/63-CONTEXT.md
    .planning/phases/61-proof-and-stability-closeout/61-CONTEXT.md
  </read_first>
  <action>In `docs/operator_verification.md`, after `## Installer verification modes (upgrade-safe)` block, add `### Check vs apply drift detection` with a table: check/dry-run = live surfaces only, manifest informational; apply preflight = plan fingerprint vs live disk; post-apply = manifest written. Include calm copy: do not edit `.scoria/install/manifest.json` by hand; absent manifest does not fail compliant check. In `docs/adoption_lanes.md`, add one sentence cross-link to the new subsection anchor under the existing installer verification link area.</action>
  <acceptance_criteria>
    - `rg '### Check vs apply drift detection' docs/operator_verification.md` returns one match.
    - `rg 'do not edit' docs/operator_verification.md` returns manifest hand-edit warning (case insensitive ok).
    - `rg 'informational' docs/operator_verification.md` returns check-time manifest role mention.
    - `rg 'Check vs apply' docs/adoption_lanes.md` returns cross-link.
  </acceptance_criteria>
  <verify>
    <automated>rg '### Check vs apply drift detection' docs/operator_verification.md</automated>
  </verify>
  <done>Operators have a single subsection explaining fingerprint roles without README churn.</done>
</task>

<task type="auto">
  <name>Task 63-03-02: Pin operator doc phrases in adoption_surface_test</name>
  <requirement_ids>INST-07</requirement_ids>
  <files>test/scoria/adoption_surface_test.exs</files>
  <read_first>
    test/scoria/adoption_surface_test.exs
    docs/operator_verification.md
    .planning/phases/61-proof-and-stability-closeout/61-CONTEXT.md
  </read_first>
  <action>Extend `test/scoria/adoption_surface_test.exs` with pins (same style as existing operator guide assertions): file `docs/operator_verification.md` must contain substring `Check vs apply drift detection` and `live host surfaces` (or exact absent-manifest line phrase from Report). Follow D-29 pattern from phase 61 — one assertion per pinned phrase.</action>
  <acceptance_criteria>
    - `rg 'Check vs apply drift detection' test/scoria/adoption_surface_test.exs` returns pin assertion.
    - `MIX_ENV=test mix test test/scoria/adoption_surface_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/adoption_surface_test.exs</automated>
  </verify>
  <done>CI guards against operator doc drift for manifest fingerprint semantics.</done>
</task>

<task type="auto">
  <name>Task 63-03-03: Add install check tests for tampered manifest and absent manifest</name>
  <requirement_ids>INST-07</requirement_ids>
  <files>test/mix/tasks/scoria.install_check_test.exs, test/scoria/install/mode_equivalence_test.exs</files>
  <read_first>
    test/mix/tasks/scoria.install_check_test.exs
    test/support/scoria/host_install_fixtures.ex
    lib/scoria/install/planner.ex
    lib/scoria/install/report.ex
    .planning/phases/63-manifest-check-fingerprint-hardening/63-CONTEXT.md
  </read_first>
  <action>Add integration tests in `install_check_test.exs` using `HostInstallFixtures`: (1) **Tampered manifest** — build compliant fixture, run apply or write manifest via fixture helper, rewrite all manifest entry fingerprints to `"deadbeef"`, run `mix scoria.install --check`, assert same `SCORIA_CHECK_RESULT` status/exit as pre-tamper baseline and human/JSON still show live `fingerprint` not equal to tampered manifest-only values for classification purposes. (2) **Absent manifest compliant** — compliant fixture without manifest file (delete `.scoria/install/manifest.json` if present), run `--check`, assert `SCORIA_CHECK_RESULT status=compliant exit_code=0` and output includes absent manifest human line. Update `mode_equivalence_test.exs` `normalize_plan/1` to strip or compare `manifest_path`/`manifest_state` consistently so dry-run ≡ check still holds. Do not modify stale-apply test in `install_test.exs` except to confirm it still passes.</action>
  <acceptance_criteria>
    - `rg 'tampered|deadbeef|manifest' test/mix/tasks/scoria.install_check_test.exs` returns new test cases.
    - `MIX_ENV=test mix test test/mix/tasks/scoria.install_check_test.exs` exits 0.
    - `MIX_ENV=test mix test test/scoria/install/mode_equivalence_test.exs` exits 0.
    - `MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs --only stale` or full install_test exits 0 with stale fingerprint test passing.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/mix/tasks/scoria.install_check_test.exs test/scoria/install/mode_equivalence_test.exs test/mix/tasks/scoria.install_test.exs</automated>
  </verify>
  <done>Automated proof that manifest tampering and absence do not change check tri-state contract.</done>
</task>

</tasks>

<threat_model>
## Security Gate

- Enforcement: enabled
- ASVS level: 1
- Block threshold: high

| ID | Threat | Severity | Mitigation in plan |
|----|--------|----------|-------------------|
| T-63-05 | Doc tells operators to disable checks | low | Copy reinforces --check workflow |

No high-severity threats. Plan may proceed.
</threat_model>

<verification>
## Plan Verification

- [ ] operator_verification.md subsection present
- [ ] adoption_surface_test pins green
- [ ] install_check tamper + absent tests green
- [ ] install_test stale gate green
- [ ] mode_equivalence_test.exs green
</verification>
