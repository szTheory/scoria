---
phase: "28"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/YYYYMMDDHHMMSS_create_ai_compacted_memories.exs
  - priv/repo/migrations/YYYYMMDDHHMMSS_add_compacted_at_to_events.exs
  - lib/scoria/runtime/compacted_memory.ex
  - lib/scoria/workflows/event.ex
  - test/scoria/runtime/compacted_memory_test.exs
  - test/scoria/workflows/event_test.exs
autonomous: true
requirements:
  - OUTRIDER-04
must_haves:
  truths:
    - Raw session events are gracefully retired without permanent deletion.
    - Summarized session histories are persisted and retrievable.
  artifacts:
    - path: lib/scoria/runtime/compacted_memory.ex
      provides: Ecto Schema CompactedMemory
      min_lines: 20
    - path: lib/scoria/workflows/event.ex
      provides: Updated schema with compacted_at field
  key_links:
    - from: lib/scoria/runtime/compacted_memory.ex
      to: ai_compacted_memories
      via: Ecto schema mapping
      pattern: schema "ai_compacted_memories"
---

<objective>
Introduce the database foundation for async session compaction by adding the `ai_compacted_memories` table with `pgvector` embedding support, and introducing soft-delete semantics (`compacted_at`) to `ai_workflow_events`.

Purpose: Prepares the Ecto schemas to store LLM-generated event summaries while safely tracking which raw events have been compacted without hard-deleting them.
Output: Working migrations and updated Ecto schemas.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-async-session-compaction-engine/DECISIONS.md
@.planning/phases/28-async-session-compaction-engine/28-PATTERNS.md
@lib/scoria/workflows/event.ex
</context>

<interfaces>
From `lib/scoria/workflows/event.ex`:
```elixir
  schema "ai_workflow_events" do
    field :sequence, :integer
    field :event_type, :string
    field :payload, :map, default: %{}
    belongs_to :run, Scoria.Workflows.Run
    #...
  end
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add compacted_at to ai_workflow_events</name>
  <files>priv/repo/migrations/YYYYMMDDHHMMSS_add_compacted_at_to_events.exs, lib/scoria/workflows/event.ex, test/scoria/workflows/event_test.exs</files>
  <behavior>
    - Test 1: Event schema correctly casts `compacted_at` timestamp.
  </behavior>
  <action>
    Create a new Ecto migration to `alter table(:ai_workflow_events)` and add `add :compacted_at, :utc_datetime_usec`.
    Update the `Scoria.Workflows.Event` schema to include `field :compacted_at, :utc_datetime_usec`. Add it to the permitted cast fields in `changeset/2`.
  </action>
  <verify>
    <automated>mix ecto.migrate && mix test test/scoria/workflows/event_test.exs</automated>
  </verify>
  <done>Migration runs successfully and Event changeset permits compacted_at assignment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ai_compacted_memories schema and migration</name>
  <files>priv/repo/migrations/YYYYMMDDHHMMSS_create_ai_compacted_memories.exs, lib/scoria/runtime/compacted_memory.ex, test/scoria/runtime/compacted_memory_test.exs</files>
  <behavior>
    - Test 1: Can insert a valid CompactedMemory record with a run_id and vector embedding.
    - Test 2: Fails validation if required fields (like summary_text) are missing.
  </behavior>
  <action>
    Create a migration for `ai_compacted_memories`. Ensure `CREATE EXTENSION IF NOT EXISTS vector` runs.
    Reference `priv/repo/knowledge_migrations/20260511000300_create_knowledge_tables.exs` and `lib/scoria/knowledge/chunk.ex` for Ecto vector schema analogs per `28-PATTERNS.md`.
    Columns needed: 
    - `id` (binary_id, primary key)
    - `run_id` (references ai_workflow_runs, null: false)
    - `session_id` (string, indexed)
    - `start_sequence` (integer)
    - `end_sequence` (integer)
    - `summary_text` (text)
    - `embedding` (:vector, size: 3 for now or matching existing knowledge tables, typically 1536 but check `Pgvector.Ecto.Vector` patterns in `.planning/phases/28-async-session-compaction-engine/28-PATTERNS.md` which uses 3 in test vectors, but 1536 is standard for OpenAI. Use size 1536 if unstated). Let's omit exact size if unknown, but `size: 1536` is typical for production AI models unless instructed otherwise. Looking at 28-PATTERNS, `size: 3` was used in a test analogy, so just omit size constraint or use `size: 1536` if you must, or simply define `add :embedding, :vector`.
    - `token_count` (integer)
    - timestamps

    Create `Scoria.Runtime.CompactedMemory` using standard Ecto module structure. Add validations for required fields.
  </action>
  <verify>
    <automated>mix ecto.migrate && mix test test/scoria/runtime/compacted_memory_test.exs</automated>
  </verify>
  <done>Schema and migration created, tests pass validating successful Ecto insertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB -> Code | Reading compacted memories from database assumes the summary and embeddings haven't been poisoned by external db manipulation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | `CompactedMemory` Schema | mitigate | Use Ecto changesets to strictly validate data types (integers for sequences, text for summaries) preventing injection. |
</threat_model>

<verification>
mix format --check-formatted
mix ecto.migrate
</verification>

<success_criteria>
- The database contains the `ai_compacted_memories` table with correct schema.
- `ai_workflow_events` has a nullable `compacted_at` timestamp.
- Ecto schemas are fully wired and covered by initial ExUnit test assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/28-async-session-compaction-engine/28-01-SUMMARY.md`
</output>
