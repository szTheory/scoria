---
phase: "28"
plan: "02"
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - lib/scoria/compaction/summarize_worker.ex
  - lib/scoria/compaction/tokenizer.ex
  - lib/scoria/workflows/event_compactor.ex
  - lib/scoria/workflows.ex
  - test/scoria/compaction/summarize_worker_test.exs
  - test/scoria/workflows/event_compactor_test.exs
autonomous: true
requirements:
  - OUTRIDER-03
  - OUTRIDER-05
must_haves:
  truths:
    - An active session that breaches a configured token/time limit automatically enqueues an Oban compaction job.
    - The Oban worker successfully calls the LLM, summarizes the raw events, and stores the result in a new Ecto schema.
  artifacts:
    - path: lib/scoria/compaction/summarize_worker.ex
      provides: Async Oban worker for summarization
      min_lines: 30
    - path: lib/scoria/workflows/event_compactor.ex
      provides: Token evaluator and enqueueing trigger
  key_links:
    - from: lib/scoria/workflows.ex
      to: lib/scoria/workflows/event_compactor.ex
      via: insert_event/4 calling maybe_enqueue_compaction
      pattern: Scoria.Workflows.EventCompactor.maybe_enqueue_compaction
    - from: lib/scoria/compaction/summarize_worker.ex
      to: ReqLlm
      via: Sync summarization request within perform/1
---

<objective>
Implement the asynchronous memory compaction logic by evaluating session sizes and dispatching Oban jobs that summarize older events via LLM.

Purpose: Protect context windows without blocking the synchronous event ingestion pipeline.
Output: An Oban worker calling `ReqLlm` and a trigger hook inside `Scoria.Workflows.insert_event/4`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-async-session-compaction-engine/DECISIONS.md
@.planning/phases/28-async-session-compaction-engine/28-PATTERNS.md
@lib/scoria/workflows.ex
@lib/scoria/workflows/event.ex
</context>

<interfaces>
From `lib/scoria/workflows.ex`:
```elixir
  defp insert_event(repo, run_id, step_id, attrs) do
    sequence = next_sequence(repo, Event, run_id)

    event = %Event{}
    |> Event.changeset(
      attrs
      |> Map.put(:run_id, run_id)
      |> Map.put(:step_id, step_id)
      |> Map.put(:sequence, sequence)
    )
    |> repo.insert!()
    
    # We will inject the compaction trigger here
    
    event
  end
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Tokenizer and Event Compactor Trigger</name>
  <files>lib/scoria/compaction/tokenizer.ex, lib/scoria/workflows/event_compactor.ex, lib/scoria/workflows.ex, test/scoria/workflows/event_compactor_test.exs</files>
  <behavior>
    - Test 1: `maybe_enqueue_compaction/2` enqueues an Oban job if uncompacted tokens exceed a mock threshold.
    - Test 2: Duplicate calls within the debounce period do not enqueue duplicate jobs.
  </behavior>
  <action>
    Create `Scoria.Compaction.Tokenizer` that wraps `Tiktoken.encode/1` to estimate tokens for a string payload safely.
    Create `Scoria.Workflows.EventCompactor` module. It should have a `count_uncompacted_tokens(repo, run_id)` function that queries `ai_workflow_events` where `compacted_at` is nil and estimates total token size (using the Tokenizer or a fast SQL `length/1` heuristic as proposed in PATTERNS).
    Implement `maybe_enqueue_compaction(repo, run_id)` that enqueues `Scoria.Compaction.SummarizeWorker` if tokens > e.g. 4000. It MUST use Oban's `unique: [period: 60, keys: [:run_id]]` constraint.
    Modify `insert_event/4` in `lib/scoria/workflows.ex` to call this function asynchronously (or non-blocking via `Task.start` or just as an Oban insert without a transaction wrap if safe) after `repo.insert!()`.
  </action>
  <verify>
    <automated>mix test test/scoria/workflows/event_compactor_test.exs</automated>
  </verify>
  <done>Event insertions trigger Oban worker enqueue operations debounced by unique constraints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SummarizeWorker with ReqLlm</name>
  <files>lib/scoria/compaction/summarize_worker.ex, test/scoria/compaction/summarize_worker_test.exs</files>
  <behavior>
    - Test 1: Worker fetches oldest uncompacted events and calls ReqLlm to summarize.
    - Test 2: Worker creates a `CompactedMemory` record and updates `compacted_at` on raw events.
  </behavior>
  <action>
    Create `Scoria.Compaction.SummarizeWorker` using `Oban.Worker`. Reference `lib/scoria/connectors/discovery_job.ex` for standard `Oban.Worker` implementation patterns as specified in `28-PATTERNS.md`.
    In `perform/1`, fetch the target `run_id`'s oldest `ai_workflow_events` (where `compacted_at` is null). 
    Query the associated `Scoria.Workflows.Run` (using the `run_id`) to fetch the `session_id` to populate this field in `CompactedMemory`.
    Format the event payloads into a prompt string. Use `ReqLlm` to call the LLM synchronously.
    On success:
    1. Insert the summary into `ai_compacted_memories` using `Scoria.Runtime.CompactedMemory` (ensure `session_id` is included).
    2. Update the `compacted_at` timestamp on the processed `ai_workflow_events`.
    Use `Repo.transaction` for the DB updates to ensure atomicity.
  </action>
  <verify>
    <automated>mix test test/scoria/compaction/summarize_worker_test.exs</automated>
  </verify>
  <done>Worker successfully summarizes events, persists the memory, and marks the source events as compacted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal -> LLM | The worker sends event payloads to an external LLM. PII/Sensitive data must be handled per global prompt policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02 | Denial of Service | `SummarizeWorker` | mitigate | Use Oban `unique` job constraints to prevent compounding job queues from burst event streams. |
| T-28-03 | Information Disclosure | `SummarizeWorker` | mitigate | Enforce system prompt boundaries in `ReqLlm` to prevent prompt injection or leaking system context to external models. |
</threat_model>

<verification>
mix format --check-formatted
mix test test/scoria/workflows/event_compactor_test.exs test/scoria/compaction/summarize_worker_test.exs
</verification>

<success_criteria>
- The Oban queue is successfully populated when an event stream breaches the token limit.
- The `SummarizeWorker` calls the LLM, inserts a `CompactedMemory`, and updates `compacted_at`.
- End-to-end event insertion pipeline is not blocked by synchronous LLM calls.
</success_criteria>

<output>
After completion, create `.planning/phases/28-async-session-compaction-engine/28-02-SUMMARY.md`
</output>
