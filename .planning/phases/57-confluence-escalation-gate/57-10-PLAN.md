---
phase: 57-confluence-escalation-gate
plan: 10
type: execute
wave: 7
depends_on: ["57-08", "57-09"]
files_modified:
  - test/scoria/confluence_concurrency_test.exs
  - lib/scoria/confluence.ex
  - lib/scoria/adopter_doc_contract.ex
  - guides/scoria-vs-external-llm-ops.md
  - test/scoria/adoption_surface_test.exs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03, GATE-04]

must_haves:
  truths:
    - "A concurrent multi-sibling-step integration suite exercises the interaction class RESEARCH.md names as the phase's highest risk, and it is green (D-25, D-26, D-28)."
    - "The full test suite passes with warnings treated as errors against a migrated database."
    - "The published comparison guide no longer denies the confluence gate as a current capability, and a POSITIVE contract assertion fails when that edit is absent — so a green suite can no longer hide a published lie (D-53)."
    - "The doc-contract lists, the guide and the adoption-surface test are edited together; the repair cannot land without the feature and the feature cannot land without the repair (D-53, CONTEXT.md group H)."
    - "GATE-02 and the roadmap's second success criterion are amended to the step-scoped wording that names where the decision is made and where the transition happens (D-18, D-54)."
    - "GATE-04 and the roadmap's fourth success criterion are amended to name the grades, because no design satisfies both original sentences exactly as written (D-31, D-54)."
    - "The residual that RAG-retrieved content does not light the untrusted-content leg this milestone is stated in the shipped documentation rather than implied (D-02)."
    - "The five-rung adoption ladder is documented with rung three gated on a counter that has read zero, and with no preflight task and no refuse-to-boot check, because there is no tool registry to enumerate (D-35)."
    - "The step-scoped pause limitation selected at the plan-01 checkpoint is recorded in the accepted-limitation register voice the repo already uses (D-25)."
    - statement: "A reader of the shipped adopter documentation can determine, without reading the source, which of the three legs are declaration-sourced and which can be scanner-observed."
      verification: backstop
  artifacts:
    - path: "test/scoria/confluence_concurrency_test.exs"
      provides: "The concurrent / multi-sibling-step interaction suite"
      contains: "concurrent"
    - path: "guides/scoria-vs-external-llm-ops.md"
      provides: "The repaired current-claims section naming the confluence escalation gate"
      contains: "confluence"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Amended GATE-02 and GATE-04 wording"
      contains: "GATE-02"
  key_links:
    - from: "test/scoria/adoption_surface_test.exs"
      to: "lib/scoria/adopter_doc_contract.ex"
      via: "the positive required-claim assertion, so the ABSENCE of the guide edit is what fails"
      pattern: "adopter_doc_contract|AdopterDocContract"
  prohibitions:
    - statement: "MUST NOT publish a capability claim the code does not deliver, and MUST NOT leave a shipped denial of a capability the code now does deliver — an adopter-facing comparison that is wrong in either direction is a lie the test suite was structured not to catch."
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT imply that retrieval-sourced content feeds the confluence gate this milestone — the untrusted-content leg is declaration- and scanner-sourced only, and stating otherwise would sell a boundary the code does not enforce."
      category: transparency
      status: resolved
      verification: test
---

<objective>
Close the phase: exercise the interaction class most likely to be wrong, repair the shipped denial atomically with the feature, and amend the two requirement statements no implementation could satisfy as originally written.

Purpose: three separate hazards converge here. First, RESEARCH.md names the concurrent multi-sibling-step class as the single highest-risk untested interaction in the phase, and it is invisible to any single-step fixture. Second, the doc contract currently FORCES the published comparison guide to deny this milestone's flagship capability, and the contract test only goes red if the guide is edited — so a green suite hides a published lie until someone touches it. Third, CONTEXT.md flags that there is no design satisfying both GATE-02's and GATE-04's original sentences exactly as written, and both must be amended deliberately rather than silently.

Output: a concurrency suite, the three-file doc repair with a positive assertion, the requirement and roadmap amendments, and the residuals and adoption ladder written down where an adopter will find them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/57-confluence-escalation-gate/57-CONTEXT.md
@.planning/phases/57-confluence-escalation-gate/57-RESEARCH.md
@.planning/phases/57-confluence-escalation-gate/57-01-SUMMARY.md
@.planning/phases/57-confluence-escalation-gate/57-08-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Concurrent and multi-sibling-step interaction suite</name>
  <precondition>Plans 01 through 09 are merged and `mix ecto.migrate` has been run, so every confluence column and code path this suite exercises exists.</precondition>
  <files>test/scoria/confluence_concurrency_test.exs</files>
  <behavior>
    - Two steps of one run escalate concurrently; both approvals exist, both are independently approvable, and both runs resume to completion.
    - A sibling step completes while an escalation is in flight; the escalating task does not crash and its step is not left in the running state.
    - A sibling completing after an escalation rewrites the run status back to running, and the escalation is still resumable.
    - After a resume, the identical tool call passes through on the consumed approval and does not create a second approval.
    - Two concurrent tool calls in one run lighting different legs produce an accumulator containing both, with strongest-wins per leg.
    - A run that halts on a rail limit while an escalation is pending leaves no approval in the pending state.
    - A retry attempted against an escalated step is refused and changes nothing.
  </behavior>
  <read_first>
    - `.planning/phases/57-confluence-escalation-gate/57-CONTEXT.md` — D-25, D-26, D-27, D-28, D-52
    - `.planning/phases/57-confluence-escalation-gate/57-RESEARCH.md` — Pitfalls 3, 4 and 5, which name this class as the highest risk and describe each failure's warning signs
    - `.planning/phases/57-confluence-escalation-gate/57-08-SUMMARY.md` — which D-25 option was implemented, which determines the expected sibling-dispatch behavior
    - `test/scoria/workflows_test.exs` — the multi-step run fixtures and the concurrency conventions this project already uses
    - `test/scoria/mcp/executor_confluence_test.exs` — the confluence fixtures built across plans 01, 05 and 06
    - `lib/scoria/workflows.ex` — `list_runnable_steps/0`, `complete_step/3` and `resume_run/1`, the three functions whose interaction this suite pins
  </read_first>
  <action>
Create `test/scoria/confluence_concurrency_test.exs` as a DB-backed integration suite dedicated to the multi-sibling-step and concurrent-escalation interaction class. Every fixture in it must be genuinely multi-step with at least one sibling — a suite of single-step-per-run fixtures is exactly the shape that makes this bug class invisible, and RESEARCH.md names that as the warning sign.

Cover every behavior listed above. Assert the expected sibling-dispatch behavior according to which D-25 option the plan-08 summary records: under the step-scoped option, assert that remaining queued siblings DO become dispatchable after an in-flight sibling completes, and that this is the documented, intended behavior — pinning it so it can never change silently; under the run-clamp option, assert the opposite. Comment the assertion with the decision id and the reason, so a future reader knows which contract they are looking at.

For the two-concurrent-escalations case, drive the two steps from separate processes and assert both approvals exist with distinct ids, both are approvable in either order, and both resumes succeed — this is what the plan-08 three-axis widening exists to make true, and it is the case that fails when only the finder is widened.

For the accumulator concurrency case, drive two tool calls against one run from separate processes, each lighting a different leg with a different witness strength, and assert both legs are present with the strongest witness per leg.

Tag the suite so it can be selected on its own, and note in a module comment that this suite is the phase gate RESEARCH.md's sampling-rate section requires before verification.
  </action>
  <verify>
    <automated>mix test test/scoria/confluence_concurrency_test.exs</automated>
    <automated>mix test --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test/scoria/confluence_concurrency_test.exs` exists and every run fixture in it creates at least two steps.
    - A test asserts two concurrent escalations in one run produce two approvals with distinct ids and that both resume successfully, exercised in both approval orders.
    - A test asserts a sibling completing mid-escalation leaves the escalating step not in the `running` status and does not crash the escalating process.
    - A test asserts a resume followed by the identical tool call creates no second approval.
    - A test asserts two concurrent calls lighting different legs produce an accumulator with both legs and the strongest witness per leg.
    - A test asserts a rail halt with a pending escalation leaves zero approvals in the `pending` status for that run.
    - A test asserts a retry against an escalated step is refused and leaves the step, approval and run unchanged.
    - `mix test test/scoria/confluence_concurrency_test.exs` exits 0 and `mix test --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The phase's highest-risk interaction class is exercised by a genuinely multi-step suite and the full suite is green with warnings as errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair the shipped denial atomically, with a positive assertion</name>
  <files>lib/scoria/adopter_doc_contract.ex, guides/scoria-vs-external-llm-ops.md, test/scoria/adoption_surface_test.exs</files>
  <behavior>
    - The published comparison guide's current-claims section names the confluence escalation gate as a shipped capability.
    - The guide's deferred-work section no longer denies that capability.
    - A contract assertion requires the guide to contain the confluence claim, so removing the guide edit turns the suite red.
    - The forbidden-current-claims list no longer forbids the phrase describing this capability, while every other forbidden phrase is unchanged.
    - The guide states that the untrusted-content leg is declaration- and scanner-sourced only this milestone.
    - The remaining deferred-work denials are unchanged.
  </behavior>
  <read_first>
    - `.planning/phases/57-confluence-escalation-gate/57-CONTEXT.md` — D-53, D-02, D-07
    - `lib/scoria/adopter_doc_contract.ex` — the deferred-not-current-claims list, the forbidden-current-claims list, and the surrounding list-naming conventions
    - `guides/scoria-vs-external-llm-ops.md` — the current-claims section and the deferred-work section that currently carries the denial
    - `test/scoria/adoption_surface_test.exs` — the block that asserts the deferred and forbidden lists against the guide, and the assertion helpers to reuse
    - `lib/scoria/ai_doc_contract.ex` — the separate contract that machine-forbids the hyphenated coinage forms, which the new positive claim's wording must not violate
  </read_first>
  <action>
Repair the shipped denial across all three files in ONE commit. This cannot land separately from the feature: the contract currently forces the published guide to deny this milestone's flagship capability, and the contract test only goes red if the guide is edited, so the absence of the repair is invisible until someone touches the file.

In `lib/scoria/adopter_doc_contract.ex`: remove the entry in the deferred-not-current-claims list that denies rule-of-two style enforcement as a current capability, and remove from the forbidden-current-claims list the phrase that names that enforcement. Leave every other entry in both lists exactly as it is — this is a two-entry edit, not a list rewrite.

Add a NEW positive list — a required-current-claims list — containing a claim naming the confluence escalation gate, and assert it in the adoption-surface test so the ABSENCE of the guide edit is what fails. Without this, a green suite hides a published lie. Word the claim using the phase's own vocabulary — the confluence escalation gate, private data, untrusted content, external egress — and do NOT use the hyphenated coinage forms that the separate AI doc contract machine-forbids; the popularized coinage may appear only as prose attribution elsewhere, never as the claim itself.

In `guides/scoria-vs-external-llm-ops.md`: remove the denial line from the deferred-work section and add the corresponding positive claim to the current-claims section. In the same edit, state the residual honestly: the untrusted-content leg is sourced from tool declarations and from a real content scanner's verdict, and retrieval-sourced content is NOT linked to a run this milestone, so it does not light that leg. That residual is real — nothing in the workflows layer calls the retrieval entrypoint and the retrieval-run record persists no run or step id, so there is no path from a retrieved chunk's tier into a run-scoped accumulator, and one cannot be built in this phase.

In `test/scoria/adoption_surface_test.exs`: add the positive assertion over the new required-current-claims list, and update the existing deferred and forbidden assertions to the two-entry-shorter lists. Keep the assertions exact-match rather than weakening them to substring or subset checks.
  </action>
  <verify>
    <automated>mix test test/scoria/adoption_surface_test.exs</automated>
    <automated>mix compile --warnings-as-errors</automated>
    <automated>grep -ci 'confluence' guides/scoria-vs-external-llm-ops.md</automated>
  </verify>
  <acceptance_criteria>
    - `lib/scoria/adopter_doc_contract.ex` defines a required-current-claims list containing at least one entry naming the confluence escalation gate.
    - `test/scoria/adoption_surface_test.exs` asserts every entry of that required list is present in `guides/scoria-vs-external-llm-ops.md`, and that assertion fails when the guide entry is removed (verify by temporarily removing it, observing the red, and restoring).
    - `guides/scoria-vs-external-llm-ops.md` contains the word `confluence` at least twice and contains a sentence stating that retrieval-sourced content does not light the untrusted-content leg this milestone.
    - The deferred-not-current-claims and forbidden-current-claims lists each lost exactly one entry and are otherwise unchanged, verifiable by diffing against the pre-change file.
    - The new positive claim's wording contains neither hyphenated coinage form that `lib/scoria/ai_doc_contract.ex` forbids.
    - `mix test test/scoria/adoption_surface_test.exs` exits 0.
  </acceptance_criteria>
  <done>The published guide names the confluence escalation gate as shipped, states the retrieval residual honestly, and a positive contract assertion makes the absence of that edit a red suite.</done>
</task>

<task type="auto">
  <name>Task 3: Requirement and roadmap amendments, adoption ladder, and the accepted-limitation register</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, lib/scoria/confluence.ex</files>
  <reversibility rating="costly">GATE-02 and GATE-04 plus the two roadmap success criteria are the statements verification and any downstream audit read; changing them again later means re-reconciling every artifact in this milestone that cites them. Flagged for weight; execution proceeds.</reversibility>
  <read_first>
    - `.planning/phases/57-confluence-escalation-gate/57-CONTEXT.md` — D-18 (the proposed GATE-02 replacement wording), D-25, D-31 (why GATE-04 must name the grades), D-35 (the corrected five-rung ladder), D-54, and D-02
    - `.planning/phases/57-confluence-escalation-gate/57-01-SUMMARY.md` and `57-08-SUMMARY.md` — the developer's D-25 selection and how it was implemented
    - `.planning/REQUIREMENTS.md` — the GATE block and the surrounding requirement voice
    - `.planning/ROADMAP.md` — the Phase 57 section's goal and four success criteria, and the Phase 58 section, which is a constraint rather than background
    - `guides/capabilities/per-run-rails.md` — the adopter-doc voice and the accepted-limitation register style to mirror
    - `lib/scoria/confluence.ex` — the moduledoc, which is the adopter-facing home for the ladder and the residuals this phase, since the dedicated capability guide is deferred to Phase 57.1
  </read_first>
  <action>
Amend GATE-02 in `.planning/REQUIREMENTS.md` and the roadmap's second Phase 57 success criterion to the wording D-18 fixes: the confluence gate DECIDES and REFUSES at the executor, before the tool's execution task is started, and the run's STEP is transitioned to the waiting-for-approval status through the existing pause function, so the escalation is resumable; tool calls that reach the executor with no runtime step attribution cannot be paused, and that gap is telemetried and documented. Keep the amendment faithful to what was actually built per the plan-08 summary, including the D-25 scoping selection.

Amend GATE-04 and the roadmap's fourth success criterion to NAME THE GRADES, because there is no design that satisfies both original sentences exactly as written: ungated means the three weak grades — unclassified, scanner infrastructure failure, and default tier — which emit telemetry and never block; positive declared evidence enforces from the shipped default; and strict mode extends enforcement to the three ungated grades. Mark both amendments in the requirements file in whatever way that file already marks a revised requirement, and record the reason in one line each.

Update the roadmap's Phase 57 plan list and count to the ten plans this phase produced, replacing the placeholder.

Document the five-rung adoption ladder in `Scoria.Confluence`'s moduledoc, since the dedicated capability guide is deferred to Phase 57.1: rung one is the upgrade itself with zero configuration, where every event records the unclassified grade and the allow decision and nothing changes; rung two is declaring every tool while watching the already-shipped unclassified-classification counter fall to zero; rung three closes the ratchet with the require-tool-classification setting, ONLY after that counter has read zero for a week; rung four installs a real content scanner, and requires re-sizing rails AND draining in-flight runs first, because each escalation round trip costs two against the step budget and rails freeze at run creation so an in-flight run cannot be re-sized; rung five optionally enables strict mode, only after the infrastructure-failure rate is provably near zero. State explicitly that there is NO preflight task and NO refuse-to-boot check, because there is no tool registry to enumerate — the router takes tools as a plug option and direct executor callers bypass it — so any static scan would be structurally partial and would give false assurance.

Add an accepted-limitation register to the same moduledoc, in the voice the per-run-rails guide already uses, recording: the pause scoping selected at the plan-01 checkpoint and what it means for sibling steps; the second exclusive row lock per tool call; the calls that cannot be paused at all (calls outside a runtime step, and a raw spawn with no callers chain); a host catching the exit signal between the executor and the runtime; and the retrieval residual from D-02. Each entry is one or two sentences naming the limitation and why it is accepted rather than closed.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
    <automated>grep -c 'GATE-02' .planning/REQUIREMENTS.md</automated>
    <automated>grep -ci 'accepted limitation' lib/scoria/confluence.ex</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md`'s GATE-02 entry states that the gate decides and refuses at the executor before the tool's execution task starts, that the step is transitioned through the existing pause function, and that unattributed calls cannot be paused.
    - `.planning/REQUIREMENTS.md`'s GATE-04 entry names the three weak grades and the declared grade explicitly.
    - `.planning/ROADMAP.md`'s Phase 57 second and fourth success criteria match the amended requirement wording.
    - `.planning/ROADMAP.md`'s Phase 57 plans line lists ten plans rather than the placeholder.
    - `lib/scoria/confluence.ex`'s moduledoc documents five numbered adoption rungs and states that there is no preflight task and no refuse-to-boot check, with the reason.
    - `lib/scoria/confluence.ex`'s moduledoc contains an accepted-limitation register with at least five entries covering pause scoping, the second row lock, unpausable call sites, the exit-catching residual, and the retrieval residual.
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The two requirements no implementation could satisfy as written are amended deliberately, the roadmap matches, and an adopter can read the ladder and every accepted limitation from the module's own documentation.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Complete phase-wide list of symbols Phase 57 creates. The plan-review source-grounding pass reads this section to exclude newly-created symbols from drift verification.

**Modules**
- `Scoria.Confluence`
- `Scoria.Confluence.Evidence`

**Public functions**
- `Scoria.Confluence.classify/1`, `combinations/0`, `normalize_combination/1`, `reason_codes/0`, `normalize_reason_code/1`, `grades/0`, `grade/1`, `decide/2`, `resolve_config/1`, `validate_app_env/0`, `audit_metadata/1`
- `Scoria.Observe.Semconv.confluence_keys/0`, `confluence_attributes/1`, `confluence_grades/0`
- `ScoriaWeb.ApprovalCopy` confluence row builders plus the combination label and tone mappings (exact names executor discretion)

**Private functions (new)**
- `Scoria.MCP.Executor.confluence_gate/3` plus its consume, accumulator-fold, attribution, containment and audit helpers

**Struct fields**
- `%Scoria.Confluence.Evidence{combination, grade, decision, reason_code, private_data_source, untrusted_content_source, exfil_source, action_class, confluence_idempotency_key, run_id, step_id, tool_ref}`
- `%Scoria.Trust.Verdict{scanner_tier}` (added)
- `Scoria.Workflows.Run.confluence_legs`
- `Scoria.Observe.Approval.consumed_at`, `consumed_by_step_id`, `confluence_scope`

**Database**
- Columns: `ai_workflow_runs.confluence_legs` (jsonb, NOT NULL, default `{}`), `ai_approvals.consumed_at` (utc_datetime_usec), `ai_approvals.consumed_by_step_id` (binary_id), `ai_approvals.confluence_scope` (string)
- Index: `ai_audit_outbox_events (tenant_id, event_type, inserted_at DESC)`
- Migration file: `priv/repo/migrations/20260728140000_add_confluence_columns.exs`

**Enum values**
- Combinations (strings): `none`, `private_data`, `untrusted_content`, `exfil_capable`, `private_data_and_untrusted_content`, `private_data_to_egress`, `untrusted_content_to_egress`, `exfiltration_path`; plus the `:unevaluable` atom sentinel
- Grades (strings): `unclassified`, `scanner_infra`, `default_tier`, `declared`
- Reason codes (atoms): `:unclassified_default`, `:approval_pending`, `:approval_granted`, `:approval_denied`, `:confluence_rejected`, `:scanner_malformed`, `:unknown`, `:confluence_resolver_fallthrough`
- `Approval.blocker_kind` value `"confluence"`; `Approval.confluence_scope` values `"call"` and `"run_tool"`

**Config keys** (`config :scoria, Scoria.Confluence`)
- `enforcement`, `declared`, `unclassified`, `scanner_infra`, `default_tier`, `strict`, `unattributed`

**Telemetry events**
- `[:scoria, :gate, :confluence, :observed]`, `[:scoria, :gate, :confluence, :skipped]`, `[:scoria, :gate, :confluence, :fallback]`

**Semantic-convention attribute keys**
- `scoria.confluence.combination`, `scoria.confluence.decision`, `scoria.confluence.grade`, `scoria.confluence.reason_code`, `scoria.confluence.approval_ref`

**Audit outbox values**
- `event_type` `"tool.confluence.escalated"`, `policy_class` `"confluence_gate"`, `actor_ref` `"system:scoria.confluence"`

**Signals and markers**
- `exit({:shutdown, {:scoria_confluence_escalation, attrs}})` and the matching `Scoria.Workflows.Runtime.execute_handler/6` clause
- The process-dictionary containment marker resolved through `self()` and `Process.get(:"$callers", [])`

**Operator string**
- `Private data + untrusted content + external egress → exfiltration path`

**New files**
- `lib/scoria/confluence.ex`, `lib/scoria/confluence/evidence.ex`, `priv/repo/migrations/20260728140000_add_confluence_columns.exs`, `test/scoria/confluence_test.exs`, `test/scoria/mcp/executor_confluence_test.exs`, `test/scoria/confluence_audit_test.exs`, `test/scoria/confluence_concurrency_test.exs`, and (if absent) `test/scoria_web/approval_copy_test.exs` and `test/scoria/workflows/remote_approval_projection_test.exs`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped documentation → adopter security expectations | An adopter decides what to build themselves based on what Scoria claims to enforce |
| concurrent step processes → the same run row | The interaction class this suite exercises |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-51 | Repudiation | published capability claims | high | mitigate | A POSITIVE required-current-claims assertion makes the absence of the guide edit a red suite, so a green suite can no longer hide a published denial of a shipped capability (D-53). Verified by temporarily removing the claim and observing the red. |
| T-57-52 | Spoofing | implied retrieval coverage | high | mitigate | The guide states explicitly that retrieval-sourced content does not light the untrusted-content leg this milestone, so an adopter cannot infer a boundary the code does not enforce (D-02). |
| T-57-53 | Denial of Service | concurrent escalation handling | high | mitigate | A dedicated multi-step suite pins the two-concurrent-escalations, sibling-completion, resume, accumulator-race, halt and retry interactions that single-step fixtures cannot reach (D-25, D-26, D-28). |
| T-57-54 | Repudiation | requirement wording versus behavior | medium | mitigate | GATE-02 and GATE-04 are amended to what the code actually does, including the unpausable-call gap and the named grades, rather than leaving a requirement no implementation satisfies (D-18, D-31, D-54). |
| T-57-55 | Information Disclosure | undisclosed residual gaps | medium | mitigate | The accepted-limitation register names every gap the phase cannot close — pause scoping, the extra row lock, unpausable call sites, the exit-catching residual and the retrieval residual — so Phase 58's boundary document inherits a complete list rather than rediscovering it. |
| T-57-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — zero new external dependencies this phase; no install task exists. |
</threat_model>

<verification>
- `mix test --warnings-as-errors` exits 0 against a migrated database.
- `mix test test/scoria/confluence_concurrency_test.exs` exits 0.
- `mix test test/scoria/adoption_surface_test.exs` exits 0, and removing the required claim from the guide turns it red.
- `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` carry the amended GATE-02 and GATE-04 wording.
</verification>

<success_criteria>
- The phase's highest-risk interaction class is exercised and green.
- The published comparison guide names the confluence escalation gate, states the retrieval residual, and its absence would fail the suite.
- GATE-02 and GATE-04 and both roadmap success criteria are amended deliberately to what shipped.
- The adoption ladder and every accepted limitation are documented where an adopter reads them.
</success_criteria>

<output>
Create `.planning/phases/57-confluence-escalation-gate/57-10-SUMMARY.md` when done. Carry forward, for Phase 58's security-boundary document, the complete accepted-limitation register written in Task 3.
</output>
