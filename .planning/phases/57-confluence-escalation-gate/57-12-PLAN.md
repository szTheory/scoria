---
phase: 57-confluence-escalation-gate
plan: 12
type: execute
wave: 10
depends_on: ["57-11"]
gap_closure: true
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GATE-01, GATE-03]

must_haves:
  truths:
    - "REQUIREMENTS.md's GATE-01 and GATE-03 checkboxes and their traceability rows read Complete, matching the code and the tests that already satisfy them, so the tracking document stops understating shipped work (D-54)."
    - "GATE-02's and GATE-04's amended wording, their amendment rationale notes, and the coverage totals are left exactly as plan 57-10 wrote them — this is a two-requirement status correction, not a rewrite."
    - "The broken-window entry tracking the unmet reviewer-evidence truth is marked fixed — not waived — and only because plan 57-11 proved that truth end-to-end against a real escalation."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "GATE-01 and GATE-03 recorded as Complete in both the checkbox list and the traceability table"
      contains: "GATE-03"
    - path: ".planning/WINDOWS.md"
      provides: "The unmet-truth entry for the confluence evidence rows, closed"
      contains: "unmet-truth"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/57-confluence-escalation-gate/57-VERIFICATION.md"
      via: "the verification report's requirements-coverage table is the evidence for each status flip"
      pattern: "GATE-01|GATE-03"
  prohibitions:
    - statement: "MUST NOT mark a requirement complete or close a broken-window entry whose underlying truth is still unmet. A ticked checkbox that outruns the code converts the tracking system into a laundering device — the exact failure this phase's own verification caught. Every flip in this plan must be backed by a passing test named in the verification report or in plan 57-11's summary."
      category: transparency
      status: flagged
---

<objective>
Close the phase's documentation-only gap: two requirement statuses that understate shipped, test-covered work, and one broken-window entry that plan 57-11 has now genuinely resolved.

Purpose: the verification report found GATE-01 and GATE-03 functionally complete and test-covered (92 classifier tests and 18 audit tests respectively) while `.planning/REQUIREMENTS.md` still shows both unchecked and "Pending". Plan 57-10 explicitly left this for a later pass because its own scope was limited to the GATE-02/GATE-04 wording amendment. The report also conditions the fix on the evidence-rows gap being resolved first, which is why this plan runs after 57-11 and not before.

Output: two corrected checkboxes, two corrected traceability rows, and one closed broken-window entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-confluence-escalation-gate/57-VERIFICATION.md
@.planning/phases/57-confluence-escalation-gate/57-11-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record GATE-01 and GATE-03 as Complete in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <precondition>`.planning/phases/57-confluence-escalation-gate/57-11-SUMMARY.md` exists and reports its tasks complete with green tests — the verification report conditions this bookkeeping on the evidence-rows gap being resolved first. If that summary is absent or reports failure, HALT and report rather than flipping any status.</precondition>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the GATE block (the four `- [ ]` / `- [x]` checkbox lines) and the Traceability table's `| GATE-01 | Phase 57 | Pending |` and `| GATE-03 | Phase 57 | Pending |` rows
    - `.planning/phases/57-confluence-escalation-gate/57-VERIFICATION.md` — the Requirements Coverage table naming the evidence for each of the four GATE IDs, and the "Secondary, non-blocking finding" paragraph that specifies exactly this edit and its precondition
    - `.planning/phases/57-confluence-escalation-gate/57-11-SUMMARY.md` — confirmation the evidence-rows gap is closed
    - `.planning/phases/57-confluence-escalation-gate/57-10-SUMMARY.md` — what plan 57-10 already amended (GATE-02 and GATE-04 wording only), so this plan does not touch it
  </read_first>
  <action>
Make exactly four scoped edits in `.planning/REQUIREMENTS.md`, and nothing else.

1. Change the GATE-01 checkbox from unchecked to checked. Leave its requirement sentence byte-identical — the classifier's totality wording was never amended and must not be.
2. Change the GATE-03 checkbox from unchecked to checked. Leave its requirement sentence byte-identical.
3. In the Traceability table, change the GATE-01 row's Status cell from `Pending` to `Complete`.
4. In the Traceability table, change the GATE-03 row's Status cell from `Pending` to `Complete`.

Do NOT touch GATE-02 or GATE-04 — their checkboxes, their amended requirement sentences, their italicised amendment-rationale lines and their traceability rows were written deliberately by plan 57-10 and are already correct.

Do NOT touch any TAINT, CLASS, RAIL, HOOK, BOUND or GOVERN line, the v2 Requirements section, the Out of Scope table, or the Coverage totals — the totals count mapped requirements, not completed ones, and are unaffected by a status flip.

Update the trailing `*Last updated: ...*` line to record this pass, keeping the existing line's format.

Use scoped replacements (`Edit`), never a whole-file rewrite — a full-file write would risk dropping requirement text outside the diff window.

The evidence for each flip is already recorded in `57-VERIFICATION.md`'s Requirements Coverage table: GATE-01 is satisfied by `lib/scoria/confluence.ex`'s total eight-vector classifier plus `test/scoria/confluence_test.exs`; GATE-03 is satisfied by `record_confluence_audit/5` plus `test/scoria/confluence_audit_test.exs`. Cite both in the plan summary so the flip is auditable rather than asserted.
  </action>
  <verify>
    <automated>grep -c '^- \[x\] \*\*GATE-01\*\*' .planning/REQUIREMENTS.md</automated>
    <automated>grep -c '^- \[x\] \*\*GATE-03\*\*' .planning/REQUIREMENTS.md</automated>
    <automated>grep -c '^| GATE-0[13] | Phase 57 | Complete |' .planning/REQUIREMENTS.md</automated>
    <automated>grep -c '^| GATE-0[24] | Phase 57 | Complete |' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*GATE-01\*\*' .planning/REQUIREMENTS.md` outputs `1`.
    - `grep -c '^- \[x\] \*\*GATE-03\*\*' .planning/REQUIREMENTS.md` outputs `1`.
    - `grep -c '^| GATE-0[13] | Phase 57 | Complete |' .planning/REQUIREMENTS.md` outputs `2`.
    - `grep -c '^| GATE-0[24] | Phase 57 | Complete |' .planning/REQUIREMENTS.md` outputs `2` (GATE-02 and GATE-04 rows are unchanged).
    - `grep -c 'Pending' .planning/REQUIREMENTS.md` outputs `4` (down from `6`) — only the four Phase 58 traceability rows (HOOK-01, HOOK-02, BOUND-01, GOVERN-01) still read Pending; no GATE row does.
    - `git diff --stat .planning/REQUIREMENTS.md` shows changes confined to the GATE-01 line, the GATE-03 line, two traceability rows and the last-updated line.
    - The GATE-02 and GATE-04 requirement sentences and their italicised amendment-rationale lines are byte-identical to their pre-task content.
  </acceptance_criteria>
  <done>The requirements document states what the code and the test suite actually deliver for all four GATE requirements, with the evidence for each status recorded in the summary.</done>
</task>

<task type="auto">
  <name>Task 2: Close the unmet-truth broken-window entry</name>
  <files>.planning/WINDOWS.md</files>
  <precondition>Plan 57-11's tests must be green — the confluence reviewer-evidence test, the projection test and the approvals LiveView test. Re-run them before closing the entry; a broken window is closed by proof, never by assertion. If any is red, HALT and leave the entry open.</precondition>
  <read_first>
    - `.planning/WINDOWS.md` — the frontmatter counters (`open_count`, `waived_count`, `fixed_count`, `total_count`, `last_updated`), the markdown table, and the fenced JSON block that mirrors it; note that entry 5 is the `unmet-truth` row for `lib/scoria_web/approval_copy.ex`
    - `.planning/phases/57-confluence-escalation-gate/57-11-SUMMARY.md` — the evidence that entry 5's truth is now proven end-to-end
    - `.planning/phases/57-confluence-escalation-gate/57-VERIFICATION.md` — the gap description entry 5 tracks
  </read_first>
  <action>
Mark broken-window entry 5 — the confluence evidence rows unmet-truth — as fixed, because plan 57-11 proved the truth against a real executor escalation.

Prefer the GSD tooling verb so the ledger's three representations stay consistent automatically: invoke the `windows fixed` subcommand with entry id `5` through the project's GSD tools launcher (`node <resolved gsd-tools.cjs path> windows fixed 5`; resolve the path the same way the execute-plan workflow does).

If and only if that verb is unavailable in this installation, edit `.planning/WINDOWS.md` by hand and keep ALL THREE representations in agreement: the markdown table row's `status` cell becomes `fixed` and its `resolved_at` cell gets the current UTC timestamp; the matching object in the fenced JSON block gets `"status": "fixed"` and the same `"resolved_at"` value; and the frontmatter counters become `open_count: 4` and `fixed_count: 1`, with `total_count` staying `5` and `last_updated` refreshed. Do not renumber any entry.

Do NOT touch entries 1 through 4. Entry 1 is the pre-existing `capture_parity_test.exs` full-suite ordering flake, independently reproduced at the pre-phase-57 baseline commit `5a9d0f8f`; entries 2, 3 and 4 are Phase 56.1 deviations. None of them is this phase's to close, and none may be waived here.

Do NOT waive entry 5. A waiver records "we decided to live with it"; this entry is genuinely resolved and must read that way.
  </action>
  <verify>
    <automated>mix test test/scoria/confluence_reviewer_evidence_test.exs test/scoria/workflows/remote_approval_projection_test.exs test/scoria_web/live/approvals_live_test.exs</automated>
    <automated>grep -c '"status": "fixed"' .planning/WINDOWS.md</automated>
    <automated>grep -c 'open_count: 4' .planning/WINDOWS.md</automated>
    <automated>grep -c 'fixed_count: 1' .planning/WINDOWS.md</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scoria/confluence_reviewer_evidence_test.exs test/scoria/workflows/remote_approval_projection_test.exs test/scoria_web/live/approvals_live_test.exs` exits 0.
    - `grep -c '"status": "fixed"' .planning/WINDOWS.md` outputs `1`.
    - `grep -c 'open_count: 4' .planning/WINDOWS.md` outputs `1` and `grep -c 'fixed_count: 1' .planning/WINDOWS.md` outputs `1`.
    - `grep -c 'total_count: 5' .planning/WINDOWS.md` outputs `1` — no entry was deleted or renumbered.
    - `grep -c '"status": "waived"' .planning/WINDOWS.md` outputs `0`.
    - The entry-5 markdown table row and its JSON object both carry a non-empty `resolved_at` value and both read `fixed`.
    - Entries 1 through 4 still read `open` in both the table and the JSON block.
  </acceptance_criteria>
  <done>The ledger records the reviewer-evidence truth as resolved by proof, and the three remaining open windows are untouched and still visible to the ship gate.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols created by THIS PLAN (new — exclude from drift verification):

- No code symbols. This plan produces only documentation state changes:
  - `.planning/REQUIREMENTS.md`: GATE-01 and GATE-03 checkbox values flipped to checked; two Traceability Status cells changed from `Pending` to `Complete`; the last-updated line refreshed.
  - `.planning/WINDOWS.md`: entry 5's status changed from `open` to `fixed` with a `resolved_at` timestamp, in both the markdown table and the mirrored JSON block; frontmatter `open_count` and `fixed_count` adjusted.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification evidence → requirement status | A tracking document's claim of completeness is derived from test evidence |
| broken-window ledger → ship gate | `/gsd-ship` is blocked by the ledger's open count, so closing an entry removes a release gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-57 | Repudiation | requirement status flip | high | mitigate | Each flip is gated on a precondition that 57-11's summary exists and reports success, and each cites the specific test file named in the verification report's Requirements Coverage table, so the status change is auditable rather than asserted (D-54). |
| T-57-58 | Elevation of Privilege | broken-window closure removing a ship gate | high | mitigate | Entry 5 is closed only after re-running 57-11's three test files green in this plan's own verify step; the entry is marked fixed, never waived, and entries 1 through 4 stay open so the ship gate remains armed for the genuinely unresolved windows. |
| T-57-59 | Tampering | whole-file rewrite of a tracking document | medium | mitigate | Both tasks use scoped replacements only; a full-file write is explicitly prohibited, and the acceptance criteria bound the diff to the named lines and assert that untouched requirement statuses and ledger entries are unchanged. |
| T-57-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — documentation-only plan; zero dependencies added and no package-manager install task exists. |
</threat_model>

<verification>
- `grep -c '^| GATE-0[1-4] | Phase 57 | Complete |' .planning/REQUIREMENTS.md` outputs `4`.
- `.planning/WINDOWS.md` frontmatter reads `open_count: 4`, `fixed_count: 1`, `total_count: 5`, with entries 1 through 4 still open.
- `mix test test/scoria/confluence_reviewer_evidence_test.exs test/scoria/workflows/remote_approval_projection_test.exs test/scoria_web/live/approvals_live_test.exs` exits 0.
- No file outside `.planning/REQUIREMENTS.md` and `.planning/WINDOWS.md` is modified by this plan.
</verification>

<success_criteria>
- All four GATE requirements read Complete in both the checkbox list and the traceability table, each backed by a named passing test.
- The reviewer-evidence broken window is closed as fixed, by proof, with the three unrelated open windows untouched.
- No requirement wording, coverage total, or ledger entry outside the four intended edits changed.
</success_criteria>

<output>
Create `.planning/phases/57-confluence-escalation-gate/57-12-SUMMARY.md` when done.
</output>
