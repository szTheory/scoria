---
phase: 56.1-per-run-rails-split-from-phase-56
plan: 02
type: execute
wave: 2
depends_on: ["56.1-01"]
files_modified:
  - lib/scoria/runtime/rails.ex
  - lib/scoria/runtime/params.ex
  - test/scoria/runtime/rails_test.exs
autonomous: true
requirements: [RAIL-01]

must_haves:
  truths:
    - "App default max_steps 10 plus per-run max_steps 100 resolves to 100 — per-run wins outright in BOTH directions, deliberately not a tighten-only join (per D-11)"
    - "A run created with no :rails option and no app config has rail_max_steps, rail_max_tool_calls and rail_max_active_ms all nil (per D-11 L5, SC#3)"
    - "max_steps: 0, -5, 3.5 or \"ten\" returns {:error, {:invalid_rail, :max_steps, value}} from Scoria.start_run/2 and creates NO run row (per D-12)"
    - "An unknown key inside :rails returns {:error, {:unknown_rail, key}} rather than silently becoming unlimited (per D-12)"
    - "Junk in config :scoria, Scoria.Runtime.Rails refuses the run and logs once per boot — it never raises out of Application.start/2 (per D-12, T-53-08)"
    - "Scoria.start_handoff_run/3 with rails: [max_steps: 1] halts on its second step, proving Params.start_handoff/3's separate with chain threads Rails.resolve/1 (per D-11)"
    - "Params.resume/2 structurally cannot change rails — :rails is not in @dispatch_keys (per D-11)"
  artifacts:
    - path: "lib/scoria/runtime/rails.ex"
      provides: "Scoria.Runtime.Rails.resolve/1 two-rung config ladder and validate_app_env/0"
      contains: "def resolve"
    - path: "test/scoria/runtime/rails_test.exs"
      provides: "L1-L5 directional ladder tests and D-12 validation refusal tests"
      min_lines: 60
  key_links:
    - from: "lib/scoria/runtime/params.ex"
      to: "lib/scoria/runtime/rails.ex"
      via: "both start/2 and start_handoff/3 with chains call Rails.resolve/1"
      pattern: "Rails\\.resolve\\("
    - from: "lib/scoria/runtime/params.ex"
      to: "lib/scoria/workflows/run.ex"
      via: "rail_max_steps / rail_max_tool_calls / rail_max_active_ms ride as top-level workflow_attrs keys into Run.changeset/2's cast list"
      pattern: "rail_max_steps"
  prohibitions:
    - statement: "MUST NOT let malformed rail configuration crash or halt the host application's boot — a runaway-prevention feature must never itself become the adopter's outage; refuse the run, which is recoverable, never the boot, which is not"
      status: resolved
      verification: judgment
---

<objective>
Give the rails an adopter-facing config surface: a two-rung ladder where a per-run value wins outright
over the app default in either direction, malformed values are refused loudly at the run rather than
silently coerced, and a typo'd key is an error instead of an accidental "unlimited".

Purpose: RAIL-01's adopter on-ramp, plus success criterion #3 (configure nothing, see unchanged
behaviour).
Output: `Scoria.Runtime.Rails` and rail threading through both `Params` entry points.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56.1-per-run-rails-split-from-phase-56/56.1-CONTEXT.md
@.planning/phases/56.1-per-run-rails-split-from-phase-56/56.1-PATTERNS.md
@.planning/phases/56.1-per-run-rails-split-from-phase-56/56.1-01-SUMMARY.md
</context>

## Artifacts this phase produces

Newly-created symbols — these do not exist in the codebase before this phase and must not be treated
as drift by any source-grounding pass.

**Modules:** `Scoria.Workflows.Rails` (`lib/scoria/workflows/rails.ex`), `Scoria.Runtime.Rails`
(`lib/scoria/runtime/rails.ex`).

**Public functions:** `Scoria.Workflows.halt_run/3`, `Scoria.Workflows.Run.halted?/1`,
`Scoria.Workflows.Rails.admit_step/2`, `Scoria.Workflows.Rails.admit_tool_call/2`,
`Scoria.Workflows.Rails.deny_reason/1`, `Scoria.Runtime.Rails.resolve/1`,
`Scoria.Runtime.Rails.validate_app_env/0`, `Scoria.Observe.Semconv.rail_attributes/1`.

**Private functions:** `Scoria.Workflows.maybe_emit_rail_observed/1`,
`Scoria.Workflows.Run.derive_rail_pause_accounting/1`.

**Columns on `ai_workflow_runs`:** `rail_max_steps`, `rail_max_tool_calls`, `rail_max_active_ms`,
`rail_steps`, `rail_tool_calls`, `rail_paused_ms`, `rail_paused_at`.

**Migration file:** `priv/repo/migrations/20260728120000_add_rail_columns_to_ai_workflow_runs.exs`.

**New status value:** `"halted"`. **Checkpoint transition / event type:** `"run_halted"`.

**Audit-outbox values:** `event_type: "run.rail.tripped"`, `policy_class: "run_rail"`,
`actor_ref: "system:scoria.rails"`, `dedupe_key: "run.rail.tripped:" <> run.id`.

**Telemetry events:** `[:scoria, :run, :rail, :tripped]`, `[:scoria, :run, :rail, :observed]`,
`[:scoria, :run, :rail, :skipped]`.

**Trace attribute keys:** `scoria.rail.rail`, `scoria.rail.limit`, `scoria.rail.observed`.

**Config keys:** `config :scoria, Scoria.Runtime.Rails` with `:max_steps`, `:max_tool_calls`,
`:max_active_ms`; the per-run option container `:rails`.

**Error tuples:** `{:error, :already_halted}`, `{:error, :run_halted}`,
`{:error, :run_not_retryable}`, `{:error, {:invalid_rail, key, value}}`,
`{:error, {:unknown_rail, key}}`.

**Reason codes:** `"max_steps_exceeded"`, `"max_tool_calls_exceeded"`, `"max_active_ms_exceeded"`.

**Error-envelope status:** `"run_halted"`. **Context marker:** `:rail_admission`.

**New files:** `guides/capabilities/per-run-rails.md`, `test/scoria/workflows/rails_test.exs`,
`test/scoria/runtime/rails_test.exs`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scoria.Runtime.Rails — the two-rung ladder and its refusals</name>
  <reversibility rating="costly">The three public key names and the app-env module key become a published Hex contract the moment they ship; renaming them later breaks every adopter's config.</reversibility>
  <files>lib/scoria/runtime/rails.ex, test/scoria/runtime/rails_test.exs</files>
  <read_first>
    - `.planning/phases/56.1-per-run-rails-split-from-phase-56/56.1-CONTEXT.md` — D-11, D-12, D-13 (locked)
    - `.planning/phases/56.1-per-run-rails-split-from-phase-56/56.1-PATTERNS.md` — the `lib/scoria/runtime/rails.ex` section and the `bounds.ex` log_once section
    - `.planning/phases/56.1-per-run-rails-split-from-phase-56/56.1-VALIDATION.md` — the config-ladder L1-L5 table and the "Validation refusals (D-12)" section
    - `lib/scoria/runtime/defaults.ex` lines 15-50 and 70-90 — `app_defaults/0`, `resolve/2`'s `with` shape, `runtime_config/0`'s `Application.get_env(:scoria, Scoria.Runtime, [])`
    - `lib/scoria/runtime/defaults.ex` lines 135-165 — `validate_runtime_override/2`, the `Enum.reduce_while` + `{:halt, {:error, {tag, field}}}` tuple contract
    - `lib/scoria/observe/bounds.ex` lines 165-180 and 369-395 — the module-namespaced `Application.get_env(:scoria, __MODULE__, [])` idiom and the ETS `log_once` idiom
    - `lib/scoria/application.ex` lines 50-60 — the T-53-08 never-raise-at-boot doctrine, verbatim
    - `test/scoria/runtime/defaults_test.exs` — the test-module shape to mirror for a `Scoria.Runtime.*` config module
  </read_first>
  <behavior>
    - L1 (the one that catches a copy-pasted most-restrictive join): app `max_steps: 10` plus per-run `max_steps: 100` resolves to `100`.
    - L2: app `100`, per-run `10` resolves to `10`.
    - L3: app `10`, per-run absent resolves to `10`.
    - L4: app absent, per-run `10` resolves to `10`.
    - L5: app absent, per-run absent resolves to `nil`.
    - `max_steps: 0` returns `{:error, {:invalid_rail, :max_steps, 0}}`; the same shape for `-5`, `3.5`, and `"ten"`.
    - A typo'd singular key inside `:rails` returns `{:error, {:unknown_rail, :max_tool_call}}`.
    - `Application.put_env(:scoria, Scoria.Runtime.Rails, max_steps: "")` makes `resolve/1` return `{:error, {:invalid_rail, :max_steps, ""}}` and logs at `error` level once, and no exception escapes.
  </behavior>
  <action>
    Create `lib/scoria/runtime/rails.ex`, module `Scoria.Runtime.Rails`, sitting next to
    `Scoria.Runtime.Defaults` and mirroring its shape.

    Public `resolve(opts)` returning `{:ok, %{rail_max_steps: integer() | nil, rail_max_tool_calls:
    integer() | nil, rail_max_active_ms: integer() | nil}}` or
    `{:error, {:invalid_rail, key, value}}` or `{:error, {:unknown_rail, key}}`. The three recognised
    public keys are `:max_steps` (a count), `:max_tool_calls` (a count), and `:max_active_ms` (integer
    milliseconds, a duration, never an absolute deadline). Their container in the per-run opts is
    `:rails`. Any other key inside `:rails` is `{:error, {:unknown_rail, key}}` — this is the
    highest-value validation in the module, because a typo'd `max_tool_call` silently becoming
    "unlimited" is the most likely real-world adopter mistake, and it deliberately diverges from
    `Bounds.config/0`'s silent `Keyword.merge`.

    Public `validate_app_env/0` reading `Application.get_env(:scoria, Scoria.Runtime.Rails, [])` and
    returning `:ok` or `{:error, {:invalid_rail, key, value}}`. `resolve/1` calls it as the first step
    of its `with` chain so a misconfigured app env refuses the run. It must NEVER be called from, or
    raise out of, `Scoria.Application.start/2` — `application.ex` states the doctrine verbatim: a boot
    crash takes the entire host app down (T-53-08). Fail closed on the run, which is recoverable;
    never on the boot, which is not. Log the app-env problem once per boot at `Logger.error` using the
    ETS `log_once` idiom from `bounds.ex` (a named public ETS table, `:ets.insert_new/2` as the
    first-time test, created lazily via `:ets.whereis/1`).

    Precedence is `effective = first_present([per_run_value, app_default_value])` — last-writer-wins,
    per key, independently. This is deliberately NOT the tighten-only join Phase 56's D-04 uses, and
    NOT `min/2`: D-04 justifies tighten-only because its host operand is request-derived, whereas both
    rail operands are host source code, so D-04's own predicate returns false here. Put that sentence
    in the moduledoc so nobody later "fixes" it into a min-join. A `min/2` or `max/2` copied from
    `Trust.Scan.most_restrictive/2` or from Phase 56 passes L2 through L5 and only L1 goes red — which
    is exactly why L1 must be its own named test rather than a table row.

    Validation table, applied per key at both rungs: absent or `nil` inherits the next rung and is
    unlimited if neither is present; a positive integer is accepted; `0`, a negative integer, a float,
    a binary, or anything else is `{:error, {:invalid_rail, key, value}}`. `0` is an error and NOT
    "halt immediately" — this is the deliberate inversion of Phase 56's D-04 fail-closed-to-most-
    restrictive rule, because a rail's junk operand is a typo in the host's own source, where "most
    restrictive" would mean every run halts on step zero and the adopter is bricked. Loud refusal at a
    host-controlled call site IS the fail-closed behaviour here. Resolution happens exactly once, at
    run creation — a later config change must never retroactively halt a live run.

    Do not add NimbleOptions. It is already transitively in the tree, but it would be Scoria's first
    direct validation dependency for three integer keys and its error strings do not produce the
    `{:error, {tag, key, value}}` tuples the `Params` `with` chain already speaks. Do not implement
    `:infinity` as a rail value and do not implement a `validate_rails_tighten_only/1` — both are cut
    in CONTEXT.md `<deferred>`.

    Add a moduledoc line disambiguating `:rails` from Ruby on Rails, and stating that `:limits` and
    `:budget` were rejected because `budget_context` is an active dispatch key feeding tenant
    `BudgetEngine` and RAIL-01 says rails are distinct from tenant budgets.

    Write `test/scoria/runtime/rails_test.exs` as a new module covering all the `<behavior>` cases.
    L1 gets its own named test. The unknown-key typo case gets its own named test. The junk-app-env
    case must assert both the run refusal and that no exception propagates out of the boot path.
    Restore any `Application.put_env` changes in an `on_exit` callback.
  </action>
  <verify>
    <automated>mix test test/scoria/runtime/rails_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scoria/runtime/rails_test.exs` exits 0.
    - `lib/scoria/runtime/rails.ex` contains `Application.get_env(:scoria, Scoria.Runtime.Rails, [])`.
    - `lib/scoria/runtime/rails.ex` contains `def validate_app_env`.
    - `test/scoria/runtime/rails_test.exs` contains a test whose name references the app-10 / per-run-100 case and asserts the effective value is `100`.
    - `test/scoria/runtime/rails_test.exs` contains a test asserting `{:error, {:unknown_rail, :max_tool_call}}`.
    - Behaviour: `Scoria.Runtime.Rails.resolve(rails: [max_steps: 0])` returns `{:error, {:invalid_rail, :max_steps, 0}}`.
    - Behaviour: `Scoria.Runtime.Rails.resolve([])` with no app env returns `{:ok, %{rail_max_steps: nil, rail_max_tool_calls: nil, rail_max_active_ms: nil}}`.
    - `! grep -q 'nimble_options' lib/scoria/runtime/rails.ex` succeeds.
    - `grep -c 'use Application' lib/scoria/runtime/rails.ex` outputs `0` — the module is not part of the supervision tree and cannot participate in boot.
    - Behaviour: with deliberately-junk app config in place, calling `Scoria.Runtime.Rails.validate_app_env/0` returns an error tuple and raises nothing, asserted by a test.
  </acceptance_criteria>
  <done>`Scoria.Runtime.Rails.resolve/1` implements last-writer-wins over the two rungs, refuses every malformed input shape with a tagged tuple, and cannot take down a host at boot. L1-L5 and every refusal case are green. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread Rails.resolve/1 into BOTH Params entry points</name>
  <files>lib/scoria/runtime/params.ex, test/scoria/runtime/rails_test.exs</files>
  <read_first>
    - `.planning/phases/56.1-per-run-rails-split-from-phase-56/56.1-CONTEXT.md` — D-11 (the "start_handoff does NOT get rails for free" paragraph), D-13
    - `.planning/phases/56.1-per-run-rails-split-from-phase-56/56.1-PATTERNS.md` — the `lib/scoria/runtime/params.ex` section with both `with` chains quoted
    - `lib/scoria/runtime/params.ex` lines 1-100 — `@dispatch_keys` at :9, `start/2`'s `with` at :16 and its `workflow_attrs` literal, `start_handoff/3` at :36-75 with its own `with` at :44-55 and its own `workflow_attrs` literal at :56-62, `resume/2` at :80-87
    - `lib/scoria/runtime.ex` lines 170-195 — `start_handoff_run/3`, which calls `Runtime.execute_step/2` directly at :185 and therefore bypasses the Reconciler entirely
    - `lib/scoria/workflows.ex` lines 110-125 — `create_run/1` merging `run_attrs` straight into `Run.changeset/2`
    - `lib/scoria/workflows/run.ex` — the `cast/3` list as modified by plan `56.1-01`
  </read_first>
  <behavior>
    - `Scoria.start_run(identity, rails: [max_steps: 5])` creates a run whose `rail_max_steps` is `5`.
    - `Scoria.start_run(identity, rails: [max_steps: 0])` returns `{:error, {:invalid_rail, :max_steps, 0}}` and the `ai_workflow_runs` row count is unchanged.
    - `Scoria.start_handoff_run(identity, role, rails: [max_steps: 1])` produces a run that halts on its second step — proving the handoff chain threads the resolution rather than silently leaving every handoff run unlimited.
    - `Scoria.resume_run/2` cannot change rails: `:rails` is absent from `@dispatch_keys`, so a `rails:` option passed to resume is not forwarded and the run's persisted limits are unchanged.
  </behavior>
  <action>
    Add `{:ok, rails} <- Rails.resolve(opts)` as a step in the `with` chain of BOTH `Params.start/2`
    and `Params.start_handoff/3`, aliasing the module as `Scoria.Runtime.Rails`. Merge the three
    resolved keys — `rail_max_steps:`, `rail_max_tool_calls:`, `rail_max_active_ms:` — into BOTH
    `workflow_attrs` literals as TOP-LEVEL keys, never nested under `metadata:`. `create_run/1` merges
    `run_attrs` straight into `Run.changeset/2`, so top-level keys flow through now that the cast list
    accepts them.

    `start_handoff/3` is a genuinely separate function with its own `with` chain and its own
    `workflow_attrs` literal; it shares only `start_metadata/5`, which is irrelevant because rails ride
    as top-level keys rather than metadata. Wiring only `start/2` would leave every
    `Scoria.start_handoff_run/3` run unlimited forever — and that is precisely the path that bypasses
    the Reconciler by calling `Runtime.execute_step/2` directly. Both chains, or the feature is half
    shipped.

    Do NOT add `:rails` to `@dispatch_keys`. `Params.resume/2` returns only `dispatch_opts/1` over
    `@dispatch_keys`, which is what structurally prevents a resume from changing a live run's rails.
    That is a "do nothing" confirmation, not an edit — and it gets a test.

    Because `Rails.resolve/1` runs inside the `with` chain, its `{:error, {:invalid_rail, ...}}` and
    `{:error, {:unknown_rail, ...}}` tuples propagate out of `Scoria.start_run/2` and
    `Scoria.start_handoff_run/3` before any run row exists, exactly as `{:error,
    {:unsafe_runtime_override, field}}` and `:invalid_root_role_id` already do from the same chains.
    `Scoria.start_run/2` is already typed `{:ok, run} | {:error, term}`, so no public contract widens.

    Add the four `<behavior>` tests to `test/scoria/runtime/rails_test.exs`. The handoff test is named
    explicitly for the `start_handoff_run` path. The no-row-created assertion compares the
    `ai_workflow_runs` count before and after the refused call.
  </action>
  <verify>
    <automated>mix test test/scoria/runtime/rails_test.exs test/scoria/runtime/defaults_test.exs</automated>
    <automated>mix test test/scoria/workflows/runtime_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scoria/runtime/rails_test.exs test/scoria/runtime/defaults_test.exs test/scoria/workflows/runtime_test.exs` exits 0.
    - `grep -c 'Rails.resolve(' lib/scoria/runtime/params.ex` outputs at least `2`.
    - `grep -c 'rail_max_steps:' lib/scoria/runtime/params.ex` outputs at least `2` (one per `workflow_attrs` literal).
    - `lib/scoria/runtime/params.ex` line 9's `@dispatch_keys` does not list `rails`.
    - Behaviour: `Scoria.start_run(identity, rails: [max_steps: 5])` returns `{:ok, run}` with `run.rail_max_steps == 5`.
    - Behaviour: `Scoria.start_run(identity, rails: [max_steps: 0])` returns `{:error, {:invalid_rail, :max_steps, 0}}` and `Repo.aggregate(Run, :count)` is unchanged across the call.
    - Behaviour: a run started via `Scoria.start_handoff_run(identity, role, rails: [max_steps: 1])` reaches `status == "halted"` after its second step is dispatched.
  </acceptance_criteria>
  <done>Both `Params` entry points resolve and persist rails; a handoff run honours `max_steps` exactly as a normal run does; a resume cannot change them. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adopter application config → `Scoria.Runtime.Rails` | `config :scoria, Scoria.Runtime.Rails` values may be `System.get_env/1` results that differ per environment; a set-but-empty env var yields `""` |
| adopter call site → `Scoria.start_run/2` / `Scoria.start_handoff_run/3` | The `:rails` keyword list is host source code, arriving unvalidated at the run boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56.1-03 | Denial of Service (self-inflicted) | `Scoria.Runtime.Rails.resolve/1` — a misconfigured rail bricks every run | high | mitigate | `0`/negative/float/binary refuse with `{:error, {:invalid_rail, key, value}}` at the run call site rather than coercing to most-restrictive; the adopter sees an explicit error, never a silently bricked application |
| T-56.1-08 | Denial of Service (boot) | `Scoria.Runtime.Rails.validate_app_env/0` | high | mitigate | Never called from and never raising out of `Application.start/2` (T-53-08 doctrine); returns a tuple and logs once per boot via the ETS `log_once` idiom |
| T-56.1-09 | Tampering / silent bypass | unknown key inside `:rails` | high | mitigate | Unknown keys are `{:error, {:unknown_rail, key}}`, deliberately diverging from a permissive `Keyword.merge`; a typo cannot become "unlimited" |
| T-56.1-10 | Elevation of Privilege | a per-run value raising an app default | low | accept | Last-writer-wins is the locked precedence (D-11): both operands are host source code, not request-derived, so a per-run raise is the host overriding its own default. The un-loosenable economic ceiling remains `BudgetEngine.reserve_step/1`'s DB-loaded policy, which rails do not touch |
| T-56.1-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no packages (RESEARCH § Package Legitimacy Audit: not applicable); no legitimacy checkpoint is required |
</threat_model>

<verification>
- `mix test test/scoria/runtime/rails_test.exs` green, including all five ladder cases as distinct tests.
- `mix test --warnings-as-errors` green.
- A refused run leaves the `ai_workflow_runs` table untouched.
</verification>

<success_criteria>
- The ladder is last-writer-wins per key, proven by L1 in both argument orders.
- Every malformed input shape and every unknown key is refused with a tagged tuple before a run row exists.
- Both `start/2` and `start_handoff/3` persist the resolved limits.
- Nothing in this plan can raise out of `Application.start/2`.
</success_criteria>

<output>
Create `.planning/phases/56.1-per-run-rails-split-from-phase-56/56.1-02-SUMMARY.md` when done.
</output>
