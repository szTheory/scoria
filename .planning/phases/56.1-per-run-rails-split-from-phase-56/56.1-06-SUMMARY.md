---
phase: 56.1-per-run-rails-split-from-phase-56
plan: 06
subsystem: docs
tags: [guides, exdoc, changelog, migration-disclosure, rails]

requires:
  - phase: 56.1-per-run-rails-split-from-phase-56
    plan: "01"
    provides: "The rail_* columns, migration file, Scoria.Workflows.Rails, the terminal \"halted\" status, and Workflows.halt_run/3 that this plan documents"
  - phase: 56.1-per-run-rails-split-from-phase-56
    plan: "02"
    provides: "Scoria.Runtime.Rails.resolve/1's two-rung config ladder and the :rails per-run option this guide's precedence section documents"
  - phase: 56.1-per-run-rails-split-from-phase-56
    plan: "03"
    provides: "The run_id forward obligation, the MCP.Router no-coverage gap, and [:scoria, :run, :rail, :skipped] telemetry this guide and its three mirrors document"
  - phase: 56.1-per-run-rails-split-from-phase-56
    plan: "04"
    provides: "The max_active_ms active-time semantics, the Phase 57 waiting_for_approval invariant, and the accepted-limitation prose this guide restates"
  - phase: 56.1-per-run-rails-split-from-phase-56
    plan: "05"
    provides: "The [:scoria, :run, :rail, :observed] telemetry payload shape (measurements/metadata) this guide's telemetry snippet and cardinality warning are grounded in"
provides:
  - "guides/capabilities/per-run-rails.md -- the adopter-facing RAIL-01 contract, registered in mix.exs docs_extras/0, the Capabilities docs_extra_groups group, mix.exs package/0's files: list, and test/scoria/package_surface_test.exs's @canonical_guides"
  - "The run-attribution footgun mirrored into guides/capabilities/default-runtime.md, guides/ownership-boundary.md, and guides/capabilities/connectors-and-mcp.md"
  - "CHANGELOG.md Unreleased migration disclosure (Migration Required (per-run rails, RAIL-01)) coexisting deliberately with the pre-existing pinned terminology-migration sentence, contract-tested in test/scoria/changelog_contract_test.exs"
  - "guides/troubleshooting.md's explicit run-the-migration / 42703 undefined_column instruction"
affects: []

tech-stack:
  added: []
  patterns:
    - "docs_code_autolink_skips/0 allowlist entries for internal (non-public-surface) modules named in guide prose -- the established repo pattern for keeping mix docs --warnings-as-errors clean when a guide must name an implementation-detail module without ExDoc attempting (and failing) to autolink it"

key-files:
  created:
    - guides/capabilities/per-run-rails.md
  modified:
    - mix.exs
    - test/scoria/package_surface_test.exs
    - guides/capabilities/default-runtime.md
    - guides/ownership-boundary.md
    - guides/capabilities/connectors-and-mcp.md
    - CHANGELOG.md
    - guides/troubleshooting.md
    - test/scoria/changelog_contract_test.exs
    - .planning/phases/56.1-per-run-rails-split-from-phase-56/deferred-items.md
    - .planning/WINDOWS.md

key-decisions:
  - "Registered per-run-rails.md in FOUR places, not the two the plan's read_first named: mix.exs docs_extras/0 + the Capabilities docs_extra_groups group (as instructed) PLUS mix.exs package/0's files: list, which package_surface_test.exs's hex-preview and package-files tests also require -- otherwise the guide is a registered ExDoc extra that never actually ships in the packaged Hex artifact."
  - "The new CHANGELOG subsection is titled \"Migration Required (per-run rails, RAIL-01)\" and placed directly after the existing breaking-changes block (before ### Added), opening with the plan's literal coexistence framing sentence; the pre-existing pinned \"no database migration\" sentence in the terminology-migration subsection is untouched, and a new pinned-coexistence test was added asserting both subsections' required substrings and their relative order."
  - "Added 9 internal-module terms this plan's own new guide content introduced (Scoria.Runtime.Rails, Scoria.Runtime.Rails.resolve/1, Scoria.Runtime.Rails.validate_app_env/0, Scoria.MCP.Executor.execute/4, Scoria.MCP.Router, Scoria.SRE.BudgetEngine, Scoria.Runtime.Params, Scoria.Workflows.Run, Scoria.Application.start/2) to mix.exs's pre-existing docs_code_autolink_skips/0 allowlist, since these are deliberately non-public modules (filtered out of docs_public_modules/0) that ExDoc otherwise fails to autolink under `mix docs --warnings-as-errors` -- the mechanism behind mix scoria.release_preview."

requirements-completed: [RAIL-01]

coverage:
  - id: D1
    description: "guides/capabilities/per-run-rails.md exists, is >= 90 lines, and is registered in mix.exs docs_extras/0 + Capabilities group + package files, and in test/scoria/package_surface_test.exs's @canonical_guides"
    requirement: "RAIL-01"
    verification:
      - kind: unit
        ref: "test/scoria/package_surface_test.exs (10/10 green, including hex-preview and package-files assertions)"
        status: pass
      - kind: manual
        ref: "wc -l guides/capabilities/per-run-rails.md -> 204 lines; grep -c 'per-run-rails.md' mix.exs -> 2 (docs_extras + Capabilities group); grep -c ... test/scoria/package_surface_test.exs -> 1"
        status: pass
    human_judgment: false
  - id: D2
    description: "The guide names all three rail keys with units and the :rails container, states the app default is a default not a ceiling, and its SQL recipe selects max(rail_steps)/max(rail_tool_calls)/max(rail_paused_ms) over a 30-day window"
    requirement: "RAIL-01"
    verification:
      - kind: manual
        ref: "grep for max_steps/max_tool_calls/max_active_ms/\"not a ceiling\"/max(rail_steps)/interval '30 days' in guides/capabilities/per-run-rails.md -- all present"
        status: pass
    human_judgment: false
  - id: D3
    description: "The guide and all three mirrored guides state rails are enforced only where a run_id is present, that MCP.Router has no max_tool_calls coverage, and name [:scoria, :run, :rail, :skipped] as the measurement"
    requirement: "RAIL-01"
    verification:
      - kind: manual
        ref: "grep '## Per-run rails'/'rail'/'per-run-rails.md' in default-runtime.md, ownership-boundary.md, connectors-and-mcp.md -- all present; all three link to the new guide"
        status: pass
    human_judgment: false
  - id: D4
    description: "CHANGELOG Unreleased discloses the migration without removing the pinned no-database-migration sentence; changelog_contract_test.exs asserts both coexist; guides/troubleshooting.md names 42703 within 20 lines of the install --check block"
    requirement: "RAIL-01"
    verification:
      - kind: unit
        ref: "test/scoria/changelog_contract_test.exs (5/5 green, including the new coexistence test)"
        status: pass
      - kind: manual
        ref: "git diff --name-only for the task-3 commit does not list README.md; guides/troubleshooting.md line 174 (42703) is 20 lines after line 154 (mix scoria.install --check)"
        status: pass
    human_judgment: false

duration: 61min
completed: 2026-07-28
status: complete
---

# Phase 56.1 Plan 06: Adopter Docs (Guide, Footgun Mirrors, Migration Disclosure) Summary

**One new `guides/capabilities/per-run-rails.md` (204 lines) wired into all four places a package/docs contract requires it, the run-attribution footgun mirrored into three boundary guides, and a CHANGELOG/troubleshooting migration disclosure that coexists deliberately (and contract-tested) with the pre-existing pinned terminology-migration sentence.**

## Performance

- **Duration:** ~61 min
- **Started:** 2026-07-28T21:29:00Z (approx, first file read)
- **Completed:** 2026-07-28T22:30:00Z
- **Tasks:** 3
- **Files modified:** 10 (1 created, 9 modified)

## Accomplishments

- New `guides/capabilities/per-run-rails.md`: the two-rung config ladder (app default vs. per-run `:rails`, last-writer-wins and explicitly why — both operands are host source code, unlike the tighten-only join elsewhere in Scoria), the three keys with units (`:max_steps`, `:max_tool_calls`, `:max_active_ms`) and why `:timeout` cannot be a rail name, the validation contract, the run_id forward obligation stated as the guide's most important sentence (Scoria populates `run_id`/`step_id` into `run.metadata["runtime"]` at dispatch but the host must forward them; `Scoria.MCP.Router` carries none today and therefore has zero `max_tool_calls` coverage; every unattributed call is measured via `[:scoria, :run, :rail, :skipped]`), the attempts-not-successes/READ COMMITTED contract, the accepted-limitation paragraph (three residual gaps), the sizing recipe (observed max over 30 days × 2, explicitly NOT p99, with the `max(rail_tool_calls)` reads `0` warning), a `:telemetry.attach/4` snippet on `[:scoria, :run, :rail, :observed]` with the cardinality warning (never tag `run_id`/`step_id`/`trace_id`/`audit_outbox_event_id`), and the Phase 57 re-sizing warning verbatim (a round trip costs 2 against `max_steps`).
- Registered the guide in **four** places (the plan's read_first named two — `docs_extras/0` and the Capabilities `docs_extra_groups` group — but `test/scoria/package_surface_test.exs`'s hex-preview and package-files tests also require the guide in `mix.exs package/0`'s `files:` list, or it is a registered ExDoc extra that never actually ships in the Hex tarball): `mix.exs docs_extras/0`, the Capabilities `docs_extra_groups` group, `mix.exs package/0 files:`, and `test/scoria/package_surface_test.exs`'s `@canonical_guides`.
- Mirrored the run-attribution footgun into the three guides an adopter consults about boundaries: a new `## Per-run rails` section in `guides/capabilities/default-runtime.md` (next to the pre-existing `session_id` is not `run_id` section), a new Common-Footguns bullet in `guides/ownership-boundary.md`, and a new Embedded-boundary table row in `guides/capabilities/connectors-and-mcp.md`. All three name `[:scoria, :run, :rail, :skipped]` and link to the new guide; none implies `MCP.Router` is the only gap (phrased as "wherever a `run_id` is absent," with the router as the shipped example).
- `CHANGELOG.md` Unreleased gains a new `### Migration Required (per-run rails, RAIL-01)` subsection placed directly after the existing breaking-changes block, opening with the plan's literal coexistence framing (the terminology migration needs no schema change; this feature adds one). Names the migration file, the seven `rail_*` columns, the catalog-only/no-backfill contract, the new `"halted"` status, and the new `"run.rail.tripped"` audit-outbox `event_type` (called out explicitly for adopter `AuditSink` implementations that pattern-match exhaustively). The pre-existing pinned "no database migration" sentence is byte-identical and untouched; `README.md` was not touched.
- `test/scoria/changelog_contract_test.exs` gains a new test pinning the coexistence: both subsections' required substrings present, and the new subsection ordered before the pre-existing terminology one — all 4 pre-existing assertions in the file remain green and unmodified.
- `guides/troubleshooting.md` gains an explicit run-the-migration instruction (`mix ecto.migrate`) 20 lines after the existing `mix scoria.install --check` block, naming the exact failure symptom an adopter who skips it will see: Postgres `42703 undefined_column` across the entire run lifecycle (not just rail code), since `Scoria.Workflows.Run` is a plain Ecto schema and every generated `SELECT` names the seven new columns.
- `mix.exs`'s `docs_code_autolink_skips/0` allowlist gained 9 new entries for internal modules this plan's own guide content names in prose (`Scoria.Runtime.Rails` and two of its functions, `Scoria.MCP.Executor.execute/4`, `Scoria.MCP.Router`, `Scoria.SRE.BudgetEngine`, `Scoria.Runtime.Params`, `Scoria.Workflows.Run`, `Scoria.Application.start/2`) — see Deviations.

## Task Commits

1. **Task 1: guides/capabilities/per-run-rails.md and its two contract registrations** — `c9d5e84f` (feat)
2. **Task 2: Mirror the run-attribution footgun into three guides** — `b792c24f` (docs)
3. **Task 3: Disclose the migration in CHANGELOG and troubleshooting** — `9738307e` (docs)
4. **Follow-up: keep the docs warnings-as-errors gate clean (Rule 1)** — `9a232696` (fix)

**Plan metadata:** (this SUMMARY's own commit, made by the worktree executor)

## Files Created/Modified

- `guides/capabilities/per-run-rails.md` — new adopter guide (204 lines).
- `mix.exs` — the new guide added to `docs_extras/0`, the Capabilities `docs_extra_groups` group, `package/0`'s `files:` list, and 9 new `docs_code_autolink_skips/0` entries.
- `test/scoria/package_surface_test.exs` — the new guide added to `@canonical_guides`.
- `guides/capabilities/default-runtime.md` — new `## Per-run rails` section.
- `guides/ownership-boundary.md` — new Common-Footguns bullet.
- `guides/capabilities/connectors-and-mcp.md` — new Embedded-boundary table row.
- `CHANGELOG.md` — new `### Migration Required (per-run rails, RAIL-01)` Unreleased subsection.
- `guides/troubleshooting.md` — new run-the-migration / `42703` instruction next to the install-check block.
- `test/scoria/changelog_contract_test.exs` — new coexistence-pinning test.
- `.planning/phases/56.1-per-run-rails-split-from-phase-56/deferred-items.md` / `.planning/WINDOWS.md` — logged 1 pre-existing, out-of-scope `mix docs --warnings-as-errors` regression (4 warnings, none from files this plan touched).

## Decisions Made

See `key-decisions` in frontmatter. Notably: the guide is registered in four places, not the two the plan's `read_first` pointed at, because `package_surface_test.exs` independently requires the guide in `mix.exs package/0`'s `files:` list for it to actually ship in the Hex tarball; and the docs-autolink-skips fix keeps the pre-existing `mix scoria.release_preview` gate from gaining new noise from this plan's own content, even though the plan's own `<verification>` block only required plain `mix docs` to build (which it already did).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added `per-run-rails.md` to `mix.exs package/0`'s `files:` list**
- **Found during:** Task 1 verification (`mix test test/scoria/package_surface_test.exs`)
- **Issue:** The plan's `read_first` pointed only at `docs_extras/0` and the Capabilities `docs_extra_groups` group (lines 155-200). Registering the guide there alone left it absent from the package's `files:` list, failing `package_surface_test.exs`'s "hex preview includes the required release surface" and "package files include guides..." tests — the guide would have been a valid ExDoc extra that never actually ships in the packaged Hex artifact.
- **Fix:** Added `"guides/capabilities/per-run-rails.md"` to `package/0`'s `files:` list, alongside the other capability guides already there.
- **Files modified:** `mix.exs`
- **Committed in:** `c9d5e84f` (Task 1 commit)

**2. [Rule 1 - Bug] Added 9 internal-module references to `docs_code_autolink_skips/0`**
- **Found during:** Post-Task-3 verification, running `MIX_ENV=dev mix docs --warnings-as-errors` (the mechanism behind the pre-existing maintainer gate `mix scoria.release_preview`, referenced by this plan's own new `guides/troubleshooting.md` content)
- **Issue:** This plan's own new guide prose names several deliberately non-public internal modules (`Scoria.Runtime.Rails`, `Scoria.MCP.Router`, `Scoria.MCP.Executor.execute/4`, `Scoria.SRE.BudgetEngine`, `Scoria.Runtime.Params`, `Scoria.Workflows.Run`, `Scoria.Application.start/2`) inside backtick code spans. ExDoc tries to autolink every such span and fails on modules filtered out of `docs_public_modules/0`, producing 9 new "reference to a filtered module" / "hidden function" warnings that `--warnings-as-errors` turns into a build failure.
- **Fix:** Added the 9 literal terms to `mix.exs`'s pre-existing `docs_code_autolink_skips/0` allowlist — the established repo pattern for exactly this situation (6 pre-existing entries already there for the same reason, e.g. `ScoriaWeb.UI`, `Scoria.SupportJourney`).
- **Files modified:** `mix.exs`
- **Committed in:** `9a232696` (follow-up commit)

### Pre-existing issue found and logged (not fixed, out of scope)

**`MIX_ENV=dev mix docs --warnings-as-errors` was already RED before this plan touched any file**, from 4 warnings across files this plan never modified: `lib/scoria/knowledge.ex:161` (`Scoria.Knowledge.set_source_trust/3`, reported twice), `lib/scoria/mcp/tool.ex:28` (`Scoria.MCP.Tool` module doc), and both `README.md` and `guides/reference/glossary.md` referencing a `guides/capabilities/trace-observability.md` file that does not exist. Verified pre-existing via `git log --oneline` on all four files — most recent touches are from phases 55/56, not 56.1. Left untouched per the SCOPE BOUNDARY rule; logged to `deferred-items.md` and `.planning/WINDOWS.md` (entry 4). This plan's own `<verification>` block only requires plain `mix docs` to build without a missing-extra error, which it does — `MIX_ENV=dev mix docs` (without `--warnings-as-errors`) exits 0 with no missing-extra error.

---

**Total deviations:** 2 auto-fixed (1 Rule 3 — a genuine blocking gap in the plan's own read_first scope; 1 Rule 1 — a docs-quality regression directly caused by this plan's own new content) plus 1 pre-existing issue logged but not fixed (out of scope, unrelated files).
**Impact on plan:** Both fixes were necessary for the plan's own stated acceptance criteria (package_surface_test.exs) and for keeping the pre-existing release-doc gate from gaining new, avoidable noise. No scope creep — the pre-existing 4-warning regression was left alone and logged rather than fixed.

## Issues Encountered

Full-suite `mix test --warnings-as-errors` (required by this plan's `<verification>`) surfaced exactly 1 pre-existing failure, already acknowledged in this plan's own `<test_environment>` instructions and logged as WINDOWS entry 1:

1. `test/scoria/warning_inventory/capture_parity_test.exs:53` — the already-acknowledged SEED-004-class full-suite-ordering flake; verified passing in isolation (`mix test test/scoria/warning_inventory/capture_parity_test.exs` → 2 tests, 0 failures) immediately after the full-suite run failed it. Not a plan-06 regression — this plan touched no test-warning-inventory files.

The scoped verification lane (`mix test test/scoria/package_surface_test.exs test/scoria/changelog_contract_test.exs`) is green (15/15). The full suite (`mix test --warnings-as-errors`) is green apart from the one acknowledged flake (3 doctests, 1569 tests, 1 failure — that flake). `MIX_ENV=dev mix docs` builds with no missing-extra error (0 exit).

## User Setup Required

None — no external service configuration required. `mix deps.get` was run at worktree start per this plan's `<test_environment>` instructions; `mix ecto.migrate` reported "already up" (wave 1's rail-column migration is already applied to the shared test DB).

## Next Phase Readiness

- RAIL-01's adopter-facing surface is complete: one guide covering every locked-decision requirement, registered in all four package/docs contracts; the run-attribution gap is impossible to miss across the three boundary guides an adopter would consult; and the migration/new-status/new-audit-event-type are disclosed without weakening the pinned terminology-migration contract string.
- This is the final plan of Phase 56.1 (6 of 6, wave 5). Phase 57 (confluence escalation gate) inherits `guides/capabilities/per-run-rails.md`'s documented re-sizing warning (a round trip costs 2 against `max_steps`) as a stated obligation for whoever enables strict mode.
- No blockers.

---
*Phase: 56.1-per-run-rails-split-from-phase-56*
*Completed: 2026-07-28*

## Self-Check: PASSED

All created/modified files verified present on disk; all four task commits (`c9d5e84f`, `b792c24f`, `9738307e`, `9a232696`) verified present in `git log --oneline --all`.
