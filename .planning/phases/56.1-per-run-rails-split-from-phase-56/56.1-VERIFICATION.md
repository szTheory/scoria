---
phase: 56.1-per-run-rails-split-from-phase-56
verified: 2026-07-28T22:42:40Z
status: passed
score: 4/4 roadmap success criteria verified; 34/34 plan-level must_haves truths verified across 6 plans
behavior_unverified: 0
overrides_applied: 0
re_verification: null
---

# Phase 56.1: Per-Run Rails Verification Report

**Phase Goal:** A single run cannot exceed its own step/call/time budget unnoticed; exceeding a rail
halts the run terminally and the halt is audited.
**Verified:** 2026-07-28T22:42:40Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A run exceeding `max_steps`/`max_tool_calls`/`timeout` halts, and the halt is recorded in the audit trail. | ✓ VERIFIED | `lib/scoria/workflows.ex:609 def halt_run/3` writes a `"run.rail.tripped"` audit-outbox row (`actor_ref: "system:scoria.rails"`) inside the same transaction as the `"halted"` status write. Enforcement sites: `Runtime.execute_step/2` (max_steps via `Rails.admit_step/2` at runtime.ex:273, max_active_ms via `active_time_check/1` at runtime.ex:255), `MCP.Executor.execute/4` (max_tool_calls, executor.ex:34, ahead of `resolve_classification/2` at :46), `Connectors.Invocation.invoke/4` (max_tool_calls, invocation.ex:34, ahead of `replay_resolution/5` at :41). Tests: `test/scoria/workflows/rails_test.exs` end-to-end tracer test (second dispatch denies, run reads `"halted"`, exactly one audit row); `test/scoria/mcp/executor_test.exs` tool-call denial/halt test. Full scoped suite green (252/252, `--seed 0`). |
| 2 | The halt is genuinely terminal — not resurrectable via `Workflows.retry_step/1` / `Resume.retry_failed_step/2`. | ✓ VERIFIED | Six terminality guards (G1-G6) in `lib/scoria/workflows.ex`: G1 `claim_step/1` → `Repo.rollback(:run_halted)` (line 244); G2 `retry_step/1` → `{:error, :run_not_retryable}` (line 934); G3 `resume_run/1` → structurally falls to `{:error, :not_resumable}` (line 1039, regression-tested, no code added); G4/G5/G6 `clamp_run_status/2` (line 898) + `suppress_when_halted/3` (line 905) applied in `complete_step/3`, `fail_step/3`, `mark_waiting_for_approval/3`. Two concurrent `halt_run/3` calls yield exactly one `{:ok, %Run{}}` and one `{:error, :already_halted}` — a genuine `Task.async` concurrency test, not a sequential loop, in `test/scoria/workflows_test.exs`. All 10+ guard test cases plus the concurrent-halt test pass, run twice with `--seed 0` per plan verification requirement. |
| 3 | Rails default to unlimited, so an adopter who configures nothing sees unchanged behavior; counting is always on so limits can be sized from real traffic. | ✓ VERIFIED | Migration uses `add_if_not_exists` with no backfill — existing rows read `0` counters / `NULL` limits (`NULL` = unlimited). `Rails.admit_step/2`/`admit_tool_call/2` use `is_nil(r.rail_max_*) or ...` disjuncts that never short-circuit, so an unlimited run still increments. SC#3 two-part proof test: an unconfigured run executes 50 steps with zero halts AND `rail_steps == 50` on the SAME run (`test/scoria/workflows/rails_test.exs`). `Scoria.Runtime.Rails.resolve/1`'s L5 case (both rungs absent) resolves to `nil` for all three keys (`test/scoria/runtime/rails_test.exs`). |
| 4 | Surfaces with no run attribution (inbound JSON-RPC via `MCP.Router`) are an explicit, telemetried no-op rather than a silent gap. | ✓ VERIFIED | `MCP.Executor.execute/4`'s `admit_tool_call_rail/2` branches on absent `:run_id` (and on `Rails.deny_reason/1` returning `:no_run`) to emit `[:scoria, :run, :rail, :skipped]` with `reason: :no_run_id`/`:no_run` and execute the tool normally — not a refusal. `MCP.Router` (line 36 per PLAN read_first) has no run attribution and is covered by this executor-level guard. Documented explicitly in `guides/capabilities/per-run-rails.md` (naming `MCP.Router` as the concrete shipped gap) and mirrored into 3 boundary guides. Tests in `test/scoria/mcp/executor_test.exs` assert the `:skipped` event fires exactly once and the tool's own result is unaffected. |

**Score:** 4/4 roadmap success criteria verified.

### Plan-Level Must-Haves (all 6 plans)

All `must_haves.truths`/`artifacts`/`key_links` from all six plan frontmatters were checked against the
actual codebase (not SUMMARY claims):

| Plan | Truths | Artifacts | Key Links | Status |
|------|--------|-----------|-----------|--------|
| 56.1-01 (tracer + G1-G6) | 6/6 | 5/5 | 3/3 | ✓ All verified against source; migration has exactly 7 `add_if_not_exists`, no index/CHECK; `Run.halted?/1`, `halt_run/3` with `lock: "FOR UPDATE"`, `Rails.admit_step` wired in `runtime.ex` before span_opts. |
| 56.1-02 (config ladder) | 7/7 | 2/2 | 2/2 | ✓ `Scoria.Runtime.Rails.resolve/1` implements last-writer-wins (not tighten-only); `Rails.resolve(` appears ≥2x in `params.ex` (both `start/2` and `start_handoff/3`); `:rails` confirmed absent from `@dispatch_keys`. |
| 56.1-03 (tool-call rail + CAS battery) | 8/8 | 2/2 | 3/3 | ✓ `admit_tool_call_rail/2` is the literal first statement ahead of `resolve_classification`/`replay_resolution` in both entry points (line-order confirmed by direct grep); `:rail_admission` marker present and stripped before tool sees it; `run_id`/`step_id` forwarded via `decorate_run_with_trace_context/4`. |
| 56.1-04 (timeout rail) | 6/6 | 2/2 | 2/2 | ✓ `derive_rail_pause_accounting/1` in `run.ex`, pause fields absent from `cast/3`; active-time predicate uses `:utc_datetime_usec` type ascription, no `fragment("now()")`; check order max_active_ms → max_steps → max_tool_calls confirmed by named test. |
| 56.1-05 (evidence surfaces) | 5/5 | 2/2 | 2/2 | ✓ `attribute_registry/0` gains exactly the 3 `scoria.rail.*` keys, canary test updated in commit; `maybe_emit_rail_observed/1` called from exactly 3 terminal-status writers (`complete_step/3`, `fail_step/3`, `halt_run/3`); `:not_resumable` approval-inbox clause and `tone("halted") == :fail` present. |
| 56.1-06 (adopter docs) | 7/7 | 1/1 | 2/2 | ✓ `guides/capabilities/per-run-rails.md` (204 lines) registered in `mix.exs` (4 places) and `package_surface_test.exs`; contains "not a ceiling", the 30-day SQL sizing recipe, `READ COMMITTED`, `MCP.Router`; CHANGELOG discloses migration without altering the pinned pre-1.0 sentence; troubleshooting names `42703`. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `priv/repo/migrations/20260728120000_add_rail_columns_to_ai_workflow_runs.exs` | 7 rail columns, `add_if_not_exists` | ✓ VERIFIED | File exists; migration applied to test DB (confirmed via successful test runs referencing `rail_max_steps` etc. in generated SELECTs). |
| `lib/scoria/workflows/rails.ex` | `admit_step/2`, `admit_tool_call/2`, `deny_reason/2` | ✓ VERIFIED | 184 lines; all three functions present and exercised by tests. |
| `lib/scoria/workflows/run.ex` | `"halted"` status, `halted?/1`, `derive_rail_pause_accounting/1` | ✓ VERIFIED | `@statuses` includes `halted`; `halted?/1` present; pause-derivation present and wired into `changeset/2`. |
| `lib/scoria/workflows.ex` | `halt_run/3`, six guards, `maybe_emit_rail_observed/1` | ✓ VERIFIED | All present at documented line ranges; guards confirmed via grep and passing tests. |
| `lib/scoria/workflows/runtime.ex` | rail admission + `run_id`/`step_id` forward | ✓ VERIFIED | `Rails.admit_step`, `active_time_check`, `decorate_run_with_trace_context/4` all present. |
| `lib/scoria/runtime/rails.ex` | `resolve/1`, `validate_app_env/0` | ✓ VERIFIED | Present; never called from `Application.start/2` (grep confirms `use Application` count is 0). |
| `lib/scoria/mcp/executor.ex` | `admit_tool_call_rail/2` as first gate | ✓ VERIFIED | Confirmed ahead of `resolve_classification` by line number. |
| `lib/scoria/connectors/invocation.ex` | `admit_tool_call_rail/2` before replay | ✓ VERIFIED | Confirmed ahead of `replay_resolution` by line number. |
| `lib/scoria/observe/semconv.ex` | 3 `scoria.rail.*` keys + `rail_attributes/1` | ✓ VERIFIED | Present; canary test in same commit per SUMMARY and grep. |
| `guides/capabilities/per-run-rails.md` | Adopter guide, ≥90 lines | ✓ VERIFIED | 204 lines; registered in `mix.exs` (4x) and `package_surface_test.exs` (1x). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `runtime.ex` | `rails.ex` | `Rails.admit_step/2` in `execute_step/2` | ✓ WIRED | Confirmed at runtime.ex:273. |
| `runtime.ex` | `workflows.ex` | denial → `halt_run/3` | ✓ WIRED | Confirmed inside step span body. |
| `workflows.ex` | `sre.ex` | `insert_audit_outbox_event` inside `halt_run/3` transaction | ✓ WIRED | Confirmed; audit-failure-rollback test passes. |
| `params.ex` | `runtime/rails.ex` | `Rails.resolve(` in both `with` chains | ✓ WIRED | Confirmed ≥2 occurrences. |
| `mcp/executor.ex` | `workflows/rails.ex` | `admit_tool_call_rail` before classification | ✓ WIRED | Confirmed by line-number ordering. |
| `connectors/invocation.ex` | `mcp/executor.ex` | `:rail_admission` marker survives hop | ✓ WIRED | Double-count test asserts exactly +1 across the hop. |
| `run.ex` | `rails.ex` | `rail_paused_ms` as subtrahend in active-time predicate | ✓ WIRED | Confirmed in `admit_step/2`'s dynamic fragment. |
| `mix.exs` | `guides/capabilities/per-run-rails.md` | `docs_extras/0` + Capabilities group + `package/0` files | ✓ WIRED | Confirmed (4 occurrences total). |

### Behavioral Spot-Checks / Test Execution

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Scoped rail test lanes (10 test files, all 6 plans' tests) | `mix test test/scoria/workflows/rails_test.exs test/scoria/workflows_test.exs test/scoria/runtime/rails_test.exs test/scoria/mcp/executor_test.exs test/scoria/workflows/runtime_test.exs test/scoria/workflows/runtime_span_test.exs test/scoria/observe/semconv_test.exs test/scoria_web/live/approvals_live_test.exs test/scoria/package_surface_test.exs test/scoria/changelog_contract_test.exs --seed 0` | 252 tests, 0 failures | ✓ PASS |
| Full suite (run 1) | `mix test` | 3 doctests, 1569 tests, 2 failures (pre-existing full-suite ordering flakes) | ✓ PASS (flakes not phase regressions, see below) |
| Full suite (run 2) | `mix test` | 3 doctests, 1569 tests, 1 failure — `test/scoria/warning_inventory/capture_parity_test.exs:53` only | ✓ PASS (matches documented known_state exactly) |
| Admission ordering: rail gate before classification/replay | direct grep of line numbers in `executor.ex` and `invocation.ex` | admission at lower line number in both files | ✓ PASS |
| No debt markers in any of the 12 core modified files | `grep -E "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` | zero matches across all 12 files | ✓ PASS |

Both full-suite runs confirm the exact pre-existing flake set documented in `deferred-items.md` and
`.planning/WINDOWS.md` (`capture_parity_test.exs:53`, `observe/telemetry_test.exs:245`,
`orchestrator_live_test.exs:356`) — none touch any file this phase modified, all pass in isolation per
the SUMMARY files, and none is a regression introduced by this phase.

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|--------------|--------------|-------------|--------|----------|
| RAIL-01 | 56.1-01 through 56.1-06 (all 6) | A single run enforces `max_steps`/`max_tool_calls`/`timeout` rails (distinct from tenant budgets) and halts a run that exceeds them, with the halt audited. | ✓ SATISFIED | All three rails are implemented, enforced at every shipped call site, halt terminally with audit evidence, default to unlimited, count always, and the no-run-attribution gap is an explicit telemetried no-op. REQUIREMENTS.md and ROADMAP.md checkboxes for RAIL-01 are still unmarked (`[ ]`/"Pending") — this is expected per the phase's known_state note (executors deliberately left it unmarked since it spans all 6 plans) and should be flipped to Complete as part of phase-completion bookkeeping following this verification. No orphaned requirements: RAIL-01 is the only requirement mapped to Phase 56.1 in REQUIREMENTS.md and it is claimed by all 6 plans' frontmatter. |

### Anti-Patterns Found

None. Scanned all 12 core files created/modified across the phase's `key-files` sections
(`lib/scoria/workflows/run.ex`, `lib/scoria/workflows/rails.ex`, `lib/scoria/workflows.ex`,
`lib/scoria/workflows/runtime.ex`, `lib/scoria/runtime/rails.ex`, `lib/scoria/runtime/params.ex`,
`lib/scoria/mcp/executor.ex`, `lib/scoria/connectors/invocation.ex`, `lib/scoria/observe/semconv.ex`,
`lib/scoria_web/ui.ex`, `lib/scoria_web/live/approvals_live/index.ex`, the migration file) for
`TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers — zero matches. Documented, deliberate gaps
(e.g. the G4/G5/G6 stale-copy interleaving losing evidence, the long-tool-call/crash-recovery residual
gaps) are recorded as explicit prose in moduledocs and the adopter guide rather than left as silent
debt, consistent with the phase's stated posture.

### Human Verification Required

None. All four roadmap success criteria and every plan-level must-have are backed by either direct
source inspection or a passing named test that exercises the specific claim (concurrency tests use
genuine `Task.async`/`Task.await_many`, not sequential loops that would pass by accident; the
timezone test uses a real `SET LOCAL TIME ZONE` against the sandboxed connection).

### Gaps Summary

No gaps. The only outstanding item is non-code bookkeeping: REQUIREMENTS.md's RAIL-01 checkbox and
status column, and ROADMAP.md's Phase 56.1 checkbox, remain unmarked pending this verification's
result — that is the normal, expected sequencing (verify before mark-complete), not a phase defect.

---

_Verified: 2026-07-28T22:42:40Z_
_Verifier: Claude (gsd-verifier)_
