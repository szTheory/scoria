---
phase: 56.1-per-run-rails-split-from-phase-56
plan: 06
type: execute
wave: 5
depends_on: ["56.1-01", "56.1-02", "56.1-03", "56.1-04", "56.1-05"]
files_modified:
  - guides/capabilities/per-run-rails.md
  - mix.exs
  - test/scoria/package_surface_test.exs
  - guides/capabilities/default-runtime.md
  - guides/ownership-boundary.md
  - guides/capabilities/connectors-and-mcp.md
  - CHANGELOG.md
  - guides/troubleshooting.md
  - test/scoria/changelog_contract_test.exs
autonomous: true
requirements: [RAIL-01]

must_haves:
  truths:
    - "guides/capabilities/per-run-rails.md exists and is listed in BOTH mix.exs docs_extras/0 and test/scoria/package_surface_test.exs's @canonical_guides (per D-20)"
    - "The guide states that the app default is a default, not a ceiling — a per-run value may raise it (per D-11, D-20)"
    - "The guide states that rails are enforced only where a run_id is present in the tool context, that inbound JSON-RPC via MCP.Router has no max_tool_calls coverage at all, and that counting [:scoria, :run, :rail, :skipped] measures exactly how much traffic is unrailed (per D-19, SC#4)"
    - "The guide's sizing recipe is observed max over 30 days times 2, rounded up — explicitly not p99 (per D-20)"
    - "The guide carries the Phase 57 re-sizing warning: the confluence gate converts undeclared tool calls into approval round trips, each costing 2 against max_steps (per D-20)"
    - "CHANGELOG Unreleased discloses the new migration WITHOUT removing the existing pinned \"no database migration\" sentence, and changelog_contract_test.exs asserts both statements coexist deliberately (per D-21)"
    - "guides/troubleshooting.md carries an explicit run-the-migration line next to its existing mix scoria.install --check block (per D-21)"
  artifacts:
    - path: "guides/capabilities/per-run-rails.md"
      provides: "The adopter guide: two rungs, three keys and units, the run_id forward obligation, the attempts-not-successes contract, the READ COMMITTED dependency, the SQL sizing recipe and a telemetry snippet"
      min_lines: 90
  key_links:
    - from: "mix.exs"
      to: "guides/capabilities/per-run-rails.md"
      via: "docs_extras/0 and the Capabilities group in docs_extra_groups/0 both list the new guide"
      pattern: "per-run-rails.md"
    - from: "test/scoria/package_surface_test.exs"
      to: "mix.exs"
      via: "@canonical_guides is hardcoded and cross-checked against docs_extras/0"
      pattern: "per-run-rails.md"
---

<objective>
Ship the adopter-facing half: one guide, three mirrored footgun notes, and an honest migration
disclosure.

Every gap this phase does not close is named in adopter-facing docs rather than left silent — that is
the milestone's stated posture. The most important sentence in the whole guide is the one saying
rails only fire where a `run_id` is present in the tool context and that inbound JSON-RPC has no
`max_tool_calls` coverage at all, because that is the difference between a documented limitation and
a decorative hook.

Purpose: RAIL-01's adopter surface; success criteria #3 (sizing from real traffic) and #4 (the gap is
explicit).
Output: `guides/capabilities/per-run-rails.md` wired into the docs contract, three mirrored notes,
and the CHANGELOG/troubleshooting disclosure.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56.1-per-run-rails-split-from-phase-56/56.1-CONTEXT.md
@.planning/phases/56.1-per-run-rails-split-from-phase-56/56.1-02-SUMMARY.md
@.planning/phases/56.1-per-run-rails-split-from-phase-56/56.1-03-SUMMARY.md
@.planning/phases/56.1-per-run-rails-split-from-phase-56/56.1-04-SUMMARY.md
@.planning/phases/56.1-per-run-rails-split-from-phase-56/56.1-05-SUMMARY.md
</context>

## Artifacts this phase produces

Newly-created symbols — these do not exist in the codebase before this phase and must not be treated
as drift by any source-grounding pass.

**Modules:** `Scoria.Workflows.Rails` (`lib/scoria/workflows/rails.ex`), `Scoria.Runtime.Rails`
(`lib/scoria/runtime/rails.ex`).

**Public functions:** `Scoria.Workflows.halt_run/3`, `Scoria.Workflows.Run.halted?/1`,
`Scoria.Workflows.Rails.admit_step/2`, `Scoria.Workflows.Rails.admit_tool_call/2`,
`Scoria.Workflows.Rails.deny_reason/1`, `Scoria.Runtime.Rails.resolve/1`,
`Scoria.Runtime.Rails.validate_app_env/0`, `Scoria.Observe.Semconv.rail_attributes/1`.

**Private functions:** `Scoria.Workflows.maybe_emit_rail_observed/1`,
`Scoria.Workflows.Run.derive_rail_pause_accounting/1`.

**Columns on `ai_workflow_runs`:** `rail_max_steps`, `rail_max_tool_calls`, `rail_max_active_ms`,
`rail_steps`, `rail_tool_calls`, `rail_paused_ms`, `rail_paused_at`.

**Migration file:** `priv/repo/migrations/20260728120000_add_rail_columns_to_ai_workflow_runs.exs`.

**New status value:** `"halted"`. **Checkpoint transition / event type:** `"run_halted"`.

**Audit-outbox values:** `event_type: "run.rail.tripped"`, `policy_class: "run_rail"`,
`actor_ref: "system:scoria.rails"`, `dedupe_key: "run.rail.tripped:" <> run.id`.

**Telemetry events:** `[:scoria, :run, :rail, :tripped]`, `[:scoria, :run, :rail, :observed]`,
`[:scoria, :run, :rail, :skipped]`.

**Trace attribute keys:** `scoria.rail.rail`, `scoria.rail.limit`, `scoria.rail.observed`.

**Config keys:** `config :scoria, Scoria.Runtime.Rails` with `:max_steps`, `:max_tool_calls`,
`:max_active_ms`; the per-run option container `:rails`.

**Error tuples:** `{:error, :already_halted}`, `{:error, :run_halted}`,
`{:error, :run_not_retryable}`, `{:error, {:invalid_rail, key, value}}`,
`{:error, {:unknown_rail, key}}`.

**Reason codes:** `"max_steps_exceeded"`, `"max_tool_calls_exceeded"`, `"max_active_ms_exceeded"`.

**Error-envelope status:** `"run_halted"`. **Context marker:** `:rail_admission`.

**New files:** `guides/capabilities/per-run-rails.md`, `test/scoria/workflows/rails_test.exs`,
`test/scoria/runtime/rails_test.exs`.

<tasks>

<task type="auto">
  <name>Task 1: guides/capabilities/per-run-rails.md and its two contract registrations</name>
  <files>guides/capabilities/per-run-rails.md, mix.exs, test/scoria/package_surface_test.exs</files>
  <read_first>
    - `.planning/phases/56.1-per-run-rails-split-from-phase-56/56.1-CONTEXT.md` — D-20 in full (the required contents list, the sizing recipe, the Phase 57 warning, the `max(rail_tool_calls)` reads zero caveat), plus D-09's honest contract prose and the `<deferred>` accepted-limitation prose
    - `guides/capabilities/default-runtime.md` — the house voice and structure for a capabilities guide
    - `guides/capabilities/connectors-and-mcp.md` — a second capabilities-guide sample
    - `mix.exs` lines 155-200 — `docs_extras/0` and the `Capabilities:` group inside `docs_extra_groups/0`
    - `test/scoria/package_surface_test.exs` lines 1-30 — `@canonical_guides`, hardcoded and cross-checked against `mix.exs`
    - `.planning/phases/56.1-per-run-rails-split-from-phase-56/56.1-02-SUMMARY.md` and `56.1-05-SUMMARY.md` — the shipped config-key and telemetry-payload shapes the guide documents
  </read_first>
  <behavior>
    - `mix test test/scoria/package_surface_test.exs` passes with the new guide present in both lists.
    - The guide names all three keys with their units and the `:rails` container.
    - The guide's precedence section states the app default is a default, not a ceiling.
    - The guide's SQL recipe selects `max(rail_steps)`, `max(rail_tool_calls)` and `max(rail_paused_ms)` from `ai_workflow_runs` over a 30-day window filtered to terminated statuses.
  </behavior>
  <action>
    Create `guides/capabilities/per-run-rails.md` in the existing `guides/capabilities/` family. It
    must cover, at minimum:

    The two rungs — `config :scoria, Scoria.Runtime.Rails` for the app default and the `:rails`
    keyword container on `Scoria.start_run/2` and `Scoria.start_handoff_run/3` for the per-run value —
    and the precedence sentence stated plainly: the app default is a default, not a ceiling; a per-run
    value may raise it. Say why, so nobody later "fixes" it into a most-restrictive join: both
    operands are host source code, unlike the request-derived operand that justifies tighten-only
    elsewhere in Scoria. Note that the un-loosenable economic ceiling is the tenant `BudgetEngine`
    policy, which rails do not touch.

    The three key names and units: `:max_steps` (a count), `:max_tool_calls` (a count),
    `:max_active_ms` (integer milliseconds, a duration, not an absolute deadline). Note that
    `:timeout` is unavailable as a rail name because it is an active dispatch key that changes the
    per-step handler bound, so a `timeout:` meant as a run rail would be silently swallowed.

    The validation contract: `0`, negatives, floats, binaries and unknown keys inside `:rails` are
    refused at `Scoria.start_run/2` with a tagged error tuple before any run row exists, and a
    malformed app-env value refuses the run rather than crashing the host at boot.

    The required host `run_id` forward, stated so it cannot be missed: rails are enforced only where a
    `run_id` is present in the tool context; Scoria populates `run_id` and `step_id` into
    `run.metadata["runtime"]` at step dispatch, but the host handler must forward them into whatever
    context it passes to `Scoria.MCP.Executor.execute/4` — Scoria will not infer one. Inbound JSON-RPC
    MCP requests handled by `Scoria.MCP.Router` carry only `actor_id`, `tenant_id` and `session_id`
    and therefore have no `max_tool_calls` coverage at all. Every unattributed call emits
    `[:scoria, :run, :rail, :skipped]` with `reason: :no_run_id`; counting that event measures exactly
    how much of the adopter's tool traffic is unrailed.

    The attempts-not-successes contract and the honest no-overshoot statement from the locked spec:
    the counter rails are enforced by a single atomic compare-and-swap, exactly `limit` executions are
    admitted and there is no overshoot, a call that crashes, times out, is blocked by the replay gate
    or is served as a historical stub has already consumed its budget, and the no-overshoot guarantee
    depends on the host running Postgres READ COMMITTED. State that `max_active_ms` is a ceiling on
    admission, not a wall-clock kill — work already in flight is allowed to finish — and that it
    measures active time with paused intervals subtracted, so a long human approval wait does not
    consume it.

    The accepted-limitation paragraph: rails are enforced at admission, and three cases are not
    caught — a run wedged inside one long tool call, a run whose steps are all orphaned in `"running"`
    after a node death (crash recovery, not this feature; the real residual gap), and a run in
    `waiting_for_approval` (by design, not a gap).

    The sizing recipe: a four-line SQL query selecting `max(rail_steps)`, `max(rail_tool_calls)` and
    `max(rail_paused_ms)` from `ai_workflow_runs` where `inserted_at` is inside the last 30 days and
    `status` is in the terminated set including `'halted'`. The locked guidance is: set the rail at
    observed max over 30 days times 2, rounded up — explicitly NOT p99, because a rail's job is to
    stop a runaway loop rather than trim the tail, and p99 sizing produces false halts on legitimate
    long runs. Then confirm `[:scoria, :run, :rail, :tripped]` stays at zero for a week before relying
    on it. Warn that `max(rail_tool_calls)` will read `0` until the adopter wires the `run_id`
    forward, and that `0` is an illegal rail value — so do not configure what the query returns in
    that state.

    A short `:telemetry.attach/4` snippet on `[:scoria, :run, :rail, :observed]`, around a dozen lines,
    plus the cardinality warning: `run_id`, `step_id`, `trace_id` and `audit_outbox_event_id` are
    correlation keys for log and trace joins and must never be attached as metric tags; the
    safe-to-tag dimensions are `rail`, `reason_code`, `unit`, `terminal_status`, `tripped`, `reason`
    and `site`.

    The Phase 57 re-sizing warning, verbatim in substance: re-size every rail after enabling the
    Phase 57 confluence gate, because the gate converts undeclared tool calls into approval round
    trips and each round trip consumes 2 against `max_steps`. A `max_steps` sized on pre-Phase-57
    traffic will start halting legitimate runs the day strict mode is enabled.

    Do not add a `mix scoria.rails.observe` task and do not add a dashboard screen — both are out of
    scope; the dashboard is Phase 58 / GOVERN-01.

    Register the guide in BOTH places or the contract test fails: add
    `"guides/capabilities/per-run-rails.md"` to `docs_extras/0` in `mix.exs`, add it to the
    `Capabilities:` group inside `docs_extra_groups/0` for ExDoc sidebar consistency, and add it to
    `@canonical_guides` in `test/scoria/package_surface_test.exs`, which hardcodes the list and
    cross-checks it against `mix.exs`.
  </action>
  <verify>
    <automated>mix test test/scoria/package_surface_test.exs</automated>
    <automated>grep -c 'per-run-rails.md' mix.exs test/scoria/package_surface_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scoria/package_surface_test.exs` exits 0.
    - `grep -c 'per-run-rails.md' mix.exs` outputs `2` (docs_extras plus the Capabilities group).
    - `grep -c 'per-run-rails.md' test/scoria/package_surface_test.exs` outputs `1`.
    - `guides/capabilities/per-run-rails.md` contains all of: `max_steps`, `max_tool_calls`, `max_active_ms`, `Scoria.Runtime.Rails`, `rail_steps`, `[:scoria, :run, :rail, :observed]`, `[:scoria, :run, :rail, :skipped]`, `READ COMMITTED`, `MCP.Router`.
    - `guides/capabilities/per-run-rails.md` contains the phrase "not a ceiling".
    - `guides/capabilities/per-run-rails.md` contains a SQL block selecting `max(rail_steps)` with a 30-day interval filter.
    - `guides/capabilities/per-run-rails.md` names Phase 57's round-trip cost of 2 against `max_steps`.
    - `guides/capabilities/per-run-rails.md` is at least 90 lines.
  </acceptance_criteria>
  <done>The guide exists, covers every item the locked decision requires, and both docs contracts list it. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the run-attribution footgun into the three guides that already own it</name>
  <files>guides/capabilities/default-runtime.md, guides/ownership-boundary.md, guides/capabilities/connectors-and-mcp.md</files>
  <read_first>
    - `.planning/phases/56.1-per-run-rails-split-from-phase-56/56.1-CONTEXT.md` — D-19's documentation paragraph and its mirroring instruction
    - `guides/capabilities/default-runtime.md` lines 95-125 — the section that already owns the `session_id` is not `run_id` distinction, where the new `## Per-run rails` section belongs
    - `guides/ownership-boundary.md` lines 45-60 — the `## Common Footguns` section
    - `guides/capabilities/connectors-and-mcp.md` lines 15-35 — the `## Embedded boundary` table
    - `guides/capabilities/per-run-rails.md` — as written in Task 1; these three notes link to it rather than restating it
  </read_first>
  <behavior>
    - `guides/capabilities/default-runtime.md` gains a `## Per-run rails` section stating the run_id forward obligation and linking to the new guide.
    - `guides/ownership-boundary.md`'s Common Footguns list gains an entry for the unattributed-tool-call gap.
    - `guides/capabilities/connectors-and-mcp.md`'s Embedded boundary table gains a row for rail coverage.
    - All three notes name `[:scoria, :run, :rail, :skipped]` as the way to measure the gap.
  </behavior>
  <action>
    Add a new `## Per-run rails` section to `guides/capabilities/default-runtime.md`, placed near the
    existing text that owns the `session_id` is not `run_id` distinction, stating: rails are enforced
    only where a `run_id` is present in the tool context; Scoria populates `run_id` and `step_id` into
    `run.metadata["runtime"]` at step dispatch, but the handler must forward them into whatever
    context it passes to `Scoria.MCP.Executor.execute/4`; inbound JSON-RPC MCP requests handled by
    `Scoria.MCP.Router` carry only `actor_id`, `tenant_id` and `session_id` and therefore have no
    `max_tool_calls` coverage at all; every unattributed call emits `[:scoria, :run, :rail, :skipped]`
    with `reason: :no_run_id`, and counting that event measures exactly how much tool traffic is
    unrailed. Link to `guides/capabilities/per-run-rails.md` for the full contract.

    Mirror the same point, compressed to the surrounding format, into `guides/ownership-boundary.md`'s
    `## Common Footguns` section and into `guides/capabilities/connectors-and-mcp.md`'s
    `## Embedded boundary` table. These are the two places an adopter looks when reasoning about what
    Scoria owns versus what they own, and the run-attribution gap is exactly that kind of boundary
    fact.

    Do not phrase any of the three notes as though `MCP.Router` were the only gap — `MCPController`
    and direct host `Executor.execute/4` calls have the identical shape, which is why the guard lives
    at the executor rather than the router. Phrase it as "wherever a `run_id` is absent", naming the
    router as the concrete shipped example.
  </action>
  <verify>
    <automated>mix test test/scoria/package_surface_test.exs</automated>
    <automated>grep -c 'rail' guides/capabilities/default-runtime.md guides/ownership-boundary.md guides/capabilities/connectors-and-mcp.md</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scoria/package_surface_test.exs` exits 0.
    - `guides/capabilities/default-runtime.md` contains `## Per-run rails` and `[:scoria, :run, :rail, :skipped]`.
    - `guides/ownership-boundary.md` contains `rail` inside its `## Common Footguns` section.
    - `guides/capabilities/connectors-and-mcp.md` contains `rail` inside its `## Embedded boundary` table.
    - All three files contain a relative link to `per-run-rails.md`.
  </acceptance_criteria>
  <done>The run-attribution gap is stated in all three guides an adopter would consult about boundaries, each pointing at the full contract. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: Disclose the migration in CHANGELOG and troubleshooting, reconciled with the pinned contract test</name>
  <files>CHANGELOG.md, guides/troubleshooting.md, test/scoria/changelog_contract_test.exs</files>
  <read_first>
    - `.planning/phases/56.1-per-run-rails-split-from-phase-56/56.1-CONTEXT.md` — D-21 in full, plus Residual Open Question 11 on whether a new audit `event_type` warrants a callout
    - `CHANGELOG.md` lines 145-200 — the `## [Unreleased]` section and its existing `### ⚠ BREAKING CHANGES (0.1.4 cut)` subsection
    - `test/scoria/changelog_contract_test.exs` lines 25-80 — both pinned occurrences of the migration sentence and the README upgrade-note assertions
    - `README.md` — the `## Install` section's upgrade note, which carries the same pinned sentence and must NOT be changed
    - `guides/troubleshooting.md` lines 145-175 — the existing `mix scoria.install --check` block where the run-the-migration line belongs
    - `priv/repo/migrations/20260728120000_add_rail_columns_to_ai_workflow_runs.exs` — as created in plan `56.1-01`
  </read_first>
  <behavior>
    - `mix test test/scoria/changelog_contract_test.exs` passes.
    - The existing sentence containing the pinned migration phrase remains present and unmodified in both `CHANGELOG.md` and `README.md`.
    - A new Unreleased subsection discloses that the per-run rails feature adds one migration and that hosts must run it.
    - `guides/troubleshooting.md` carries an explicit run-the-migration instruction adjacent to the install-check block.
  </behavior>
  <action>
    **The reconciliation, decided:** keep the existing pinned sentence intact. It is scoped to the
    pre-1.0 terminology migration, and the contract test asserts its presence in both `CHANGELOG.md`
    and `README.md`. Do NOT edit or reword it, and do NOT touch `README.md` at all. Instead add a
    NEW, separately-titled subsection under `## [Unreleased]` — placed after the existing breaking-
    changes block — that explicitly scopes itself, opening with a sentence of the form: the pre-1.0
    terminology migration itself requires no schema change, and the per-run rails feature described
    below adds one. That framing lets both statements coexist deliberately rather than by accident,
    which is exactly what D-21 asks the plan to state.

    The new subsection must name: the migration file
    `20260728120000_add_rail_columns_to_ai_workflow_runs.exs`; the seven columns it adds to
    `ai_workflow_runs`; that it is catalog-only with no backfill, so existing rows read zero counters
    and null limits and every pre-existing run behaves exactly as before; that hosts must run their
    migrations after upgrading; the new `"halted"` run status; and the new audit-outbox
    `event_type` `"run.rail.tripped"` — call the event type out explicitly, because `SRE.Relay` is
    event-type-agnostic and every in-repo UI filter is positive, but an adopter's own `AuditSink` may
    pattern-match exhaustively and would crash on an unrecognised type.

    Add an assertion to `test/scoria/changelog_contract_test.exs` pinning the new subsection's
    presence alongside the existing pinned sentence, so the deliberate coexistence is itself
    contract-tested and a future edit cannot silently collapse one into the other. Do not remove or
    weaken any existing assertion in that file.

    Add an explicit run-the-migration line to `guides/troubleshooting.md` next to the existing
    `mix scoria.install --check` block, naming the symptom an adopter will actually see if they skip
    it: Postgres `42703 undefined_column` on the entire run lifecycle, not merely on rail code —
    because `Scoria.Workflows.Run` is a plain Ecto schema and the generated SELECT names the rail
    columns on every run read.

    Do not build a schema probe or any in-library escape hatch. It cannot work: adding the fields to
    the schema means the generated SELECT names them on every `repo.get!(Run, ...)`, so a runtime
    probe inside rail code cannot gate a compile-time field list. Disclosure is the whole mitigation.
  </action>
  <verify>
    <automated>mix test test/scoria/changelog_contract_test.exs test/scoria/package_surface_test.exs</automated>
    <automated>! git diff --name-only HEAD~1 HEAD | grep -q '^README.md$'</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scoria/changelog_contract_test.exs test/scoria/package_surface_test.exs` exits 0.
    - `README.md` is unchanged by this task (`git diff --name-only` for the task's commit does not list it).
    - `CHANGELOG.md`'s `## [Unreleased]` section still contains the pre-existing pinned migration sentence.
    - `CHANGELOG.md` contains `20260728120000_add_rail_columns_to_ai_workflow_runs.exs`, `rail_max_steps`, `"halted"` and `run.rail.tripped`.
    - `test/scoria/changelog_contract_test.exs` gained at least one new assertion referencing the rails migration, and every pre-existing assertion in the file is still present.
    - `guides/troubleshooting.md` contains `42703` and a run-the-migration instruction within 20 lines of the `mix scoria.install --check` block.
  </acceptance_criteria>
  <done>The migration, the new status and the new audit event type are disclosed where adopters read, the pinned contract string still holds, and the coexistence is itself tested. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scoria release → adopter host repo | Migrations are copied into host repos; an adopter who upgrades without migrating hits a hard failure on the entire run lifecycle |
| Scoria audit outbox → adopter `AuditSink` implementation | Adopter sink code may pattern-match `event_type` exhaustively and crash on an unrecognised value |
| Scoria docs → adopter configuration decisions | A guide that mis-states precedence or sizing leads adopters to configure a rail that halts legitimate runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56.1-21 | Denial of Service (self-inflicted) | an adopter upgrading without running the migration | high | mitigate | Explicit CHANGELOG subsection naming the migration file and the seven columns, plus a troubleshooting entry naming the `42703 undefined_column` symptom; no in-library probe, which provably cannot work |
| T-56.1-22 | Denial of Service (self-inflicted) | an adopter sizing a rail from p99 or from a `max(rail_tool_calls)` that still reads zero | high | mitigate | The guide locks the sizing recipe to observed-max-over-30-days times 2, warns that `0` is an illegal rail value, and instructs confirming zero `:tripped` events for a week before relying on the rail |
| T-56.1-23 | Denial of Service | an adopter `AuditSink` crashing on the new `"run.rail.tripped"` event type | medium | mitigate | The event type is called out explicitly in the CHANGELOG so exhaustive-matching sinks are updated before upgrade |
| T-56.1-24 | Information Disclosure (false assurance) | a guide implying rails cover all tool traffic | high | mitigate | The run-attribution gap is stated in the new guide and mirrored into `default-runtime.md`, `ownership-boundary.md` and `connectors-and-mcp.md`, with `[:scoria, :run, :rail, :skipped]` named as the measurement |
| T-56.1-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no packages (RESEARCH § Package Legitimacy Audit: not applicable); no legitimacy checkpoint is required |
</threat_model>

<verification>
- `mix test test/scoria/package_surface_test.exs test/scoria/changelog_contract_test.exs` green.
- `mix test --warnings-as-errors` green (full phase suite).
- `mix docs` builds without a missing-extra error.
</verification>

<success_criteria>
- One new guide, registered in both docs contracts, covering every item the locked decision names.
- The run-attribution gap is impossible to miss across the three boundary guides.
- The migration and the new audit event type are disclosed without breaking the pinned contract string.
</success_criteria>

<output>
Create `.planning/phases/56.1-per-run-rails-split-from-phase-56/56.1-06-SUMMARY.md` when done.
</output>
