---
phase: 60-drift-classification-and-safe-apply
plan: 02
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - lib/mix/tasks/scoria.install.ex
  - lib/scoria/install/planner.ex
  - lib/scoria/install/report.ex
  - lib/scoria/install/manifest.ex
  - lib/scoria/install/apply_executor.ex
  - test/mix/tasks/scoria.install_test.exs
  - test/mix/tasks/scoria.install_check_test.exs
autonomous: true
requirements: [INST-06, INST-07]
must_haves:
  truths:
    - "Apply mode executes planner entries directly through typed operations in deterministic planner order with no ad-hoc mutator branch (D-08, D-09)."
    - "Apply preflight enforces strict atomic safety: any manual_review or stale plan fingerprint mismatch blocks all writes with exit 1 (D-05, D-10)."
    - "Exit semantics remain stable and explicit across installer modes: 0 success/compliant, 1 blocked-by-drift/manual-review, 2 execution error (D-06, D-07)."
    - "SCORIA_CHECK_RESULT trailer format remains unchanged and check/apply reporting derives from canonical planner/remediation payload (D-11, D-12, D-13)."
  artifacts:
    - path: "lib/scoria/install/apply_executor.ex"
      provides: "Planner-led apply dispatcher with preflight safety gates"
      contains: "def run(plan, opts \\\\ [])"
    - path: "lib/mix/tasks/scoria.install.ex"
      provides: "CLI routing that builds planner artifact for apply and exits with stable 0/1/2 semantics"
      contains: "Planner.build(router_path, tailwind_path, config_path, mode: :apply)"
    - path: "lib/scoria/install/manifest.ex"
      provides: "Plan freshness verification and persisted ownership baseline updates after successful apply"
      contains: "def validate_freshness(plan, project_root)"
    - path: "test/mix/tasks/scoria.install_test.exs"
      provides: "Blocked apply zero-write and planner/apply equivalence contract tests"
      contains: "mix scoria.install blocks apply with zero writes when manual review exists"
  key_links:
    - from: "lib/mix/tasks/scoria.install.ex"
      to: "lib/scoria/install/apply_executor.ex"
      via: "apply mode delegates execution to planner-driven executor"
      pattern: "ApplyExecutor\\.run"
    - from: "lib/scoria/install/apply_executor.ex"
      to: "lib/scoria/install/manifest.ex"
      via: "preflight plan freshness and post-apply manifest persistence"
      pattern: "validate_freshness|write!"
    - from: "lib/scoria/install/report.ex"
      to: "test/mix/tasks/scoria.install_check_test.exs"
      via: "stable trailer and exit-code contract regression checks"
      pattern: "SCORIA_CHECK_RESULT"
---

<objective>
Implement Wave 2 planner-led safe apply behavior so preview/check/apply share one mutation truth and blocked drift states never write host files.

Purpose: Deliver INST-06 directly while enforcing strict safe defaults and remediation-first drift handling required by INST-07.
Output: `mix scoria.install` apply path builds planner artifact in apply mode, runs preflight safety gates, executes typed planner operations only when safe, and exits with stable 0/1/2 semantics.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-drift-classification-and-safe-apply/60-CONTEXT.md
@.planning/phases/60-drift-classification-and-safe-apply/60-RESEARCH.md
@.planning/phases/60-drift-classification-and-safe-apply/60-PATTERNS.md
@.planning/phases/60-drift-classification-and-safe-apply/60-VALIDATION.md
@.planning/phases/60-drift-classification-and-safe-apply/60-01-PLAN.md
@lib/mix/tasks/scoria.install.ex
@lib/scoria/install/planner.ex
@lib/scoria/install/report.ex
@lib/scoria/install/manifest.ex
@test/mix/tasks/scoria.install_test.exs
@test/mix/tasks/scoria.install_check_test.exs

Decisions D-05..D-10 define Wave 2 gating and execution behavior; D-11..D-13 remain mandatory for remediation/trailer compatibility.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 60-02-01: Replace ad-hoc apply mutators with planner-led apply executor and strict preflight safety gates</name>
  <requirement_ids>INST-06, INST-07</requirement_ids>
  <files>lib/scoria/install/apply_executor.ex, lib/mix/tasks/scoria.install.ex, lib/scoria/install/planner.ex, lib/scoria/install/manifest.ex, lib/scoria/install/report.ex</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/60-drift-classification-and-safe-apply/60-CONTEXT.md
    .planning/phases/60-drift-classification-and-safe-apply/60-01-PLAN.md
    lib/mix/tasks/scoria.install.ex
    lib/scoria/install/planner.ex
    lib/scoria/install/report.ex
    lib/scoria/install/manifest.ex
  </read_first>
  <action>Create `Scoria.Install.ApplyExecutor` with `run(plan, opts \\ [])` that performs `validate_no_blockers!/1`, `validate_freshness!/2`, and deterministic operation dispatch ordered by `{order, id}`. In `Mix.Tasks.Scoria.Install.run/1`, replace direct `do_run/3` apply branch with planner-led flow: build plan via `Planner.build(..., mode: :apply)`, render plan/report output, call `ApplyExecutor.run/2`, and terminate with explicit `System.halt(exit_code)` semantics (`0` success/compliant, `1` blocked by manual_review or stale plan mismatch, `2` execution failure). Preserve existing `--check` trailer output contract exactly (`SCORIA_CHECK_RESULT status=<status> exit_code=<exit_code>`) and do not alter status vocabulary. Ensure blocked apply performs zero writes and does not partially mutate any surface.</action>
  <acceptance_criteria>
    - `rg -n "defmodule Scoria\\.Install\\.ApplyExecutor" lib/scoria/install/apply_executor.ex` returns one apply executor module declaration.
    - `rg -n "def run\\(plan, opts \\\\ \\[\\]\\)" lib/scoria/install/apply_executor.ex` returns one run function signature.
    - `rg -n "validate_(no_blockers|freshness)!\\(" lib/scoria/install/apply_executor.ex` returns preflight gate functions.
    - `rg -n "Enum\\.sort_by\\(&\\{&1\\.order, &1\\.id\\}\\)" lib/scoria/install/apply_executor.ex lib/scoria/install/planner.ex` returns deterministic execution ordering.
    - `rg -n "Planner\\.build\\(router_path, tailwind_path, config_path,\\s*mode:\\s*:apply\\)" lib/mix/tasks/scoria.install.ex` returns planner-led apply branch.
    - `rg -n "ApplyExecutor\\.run" lib/mix/tasks/scoria.install.ex` returns apply executor delegation.
    - `rg -n "System\\.halt\\(exit_code\\)" lib/mix/tasks/scoria.install.ex` returns explicit apply/check exit behavior.
    - `rg -n "SCORIA_CHECK_RESULT status=\\#\\{status\\} exit_code=\\#\\{exit_code\\}" lib/scoria/install/report.ex` returns unchanged trailer format.
    - `MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.install_check_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.install_check_test.exs</automated>
  </verify>
  <done>Apply path is planner-driven, freshness-gated, and strictly zero-write when drift/manual_review blockers are present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 60-02-02: Add integration coverage for blocked-apply zero-write guarantees, planner/apply equivalence, and stable exit/trailer semantics</name>
  <requirement_ids>INST-06, INST-07</requirement_ids>
  <files>test/mix/tasks/scoria.install_test.exs, test/mix/tasks/scoria.install_check_test.exs, lib/mix/tasks/scoria.install.ex, lib/scoria/install/report.ex</files>
  <read_first>
    .planning/phases/60-drift-classification-and-safe-apply/60-VALIDATION.md
    test/mix/tasks/scoria.install_test.exs
    test/mix/tasks/scoria.install_check_test.exs
    lib/mix/tasks/scoria.install.ex
    lib/scoria/install/report.ex
  </read_first>
  <action>Extend `test/mix/tasks/scoria.install_test.exs` with explicit tests named `mix scoria.install blocks apply with zero writes when manual review exists`, `mix scoria.install apply executes only planner-classified actionable operations`, and `mix scoria.install blocks stale planner fingerprints before writes`. Reuse before/after snapshot helpers to assert router/tailwind/runtime/migration byte equality for blocked runs. Extend `test/mix/tasks/scoria.install_check_test.exs` subprocess matrix to assert exit-code semantics remain stable after apply refactor (`0`, `1`, `2`) and trailer line remains exactly parseable with `SCORIA_CHECK_RESULT status=<status> exit_code=<exit_code>`. Add one regression assertion that check output still includes remediation reason codes from planner payload and one command-level verification instruction.</action>
  <acceptance_criteria>
    - `rg -n "mix scoria.install blocks apply with zero writes when manual review exists" test/mix/tasks/scoria.install_test.exs` returns blocked-apply zero-write test name.
    - `rg -n "apply executes only planner-classified actionable operations" test/mix/tasks/scoria.install_test.exs` returns planner/apply equivalence test name.
    - `rg -n "blocks stale planner fingerprints before writes" test/mix/tasks/scoria.install_test.exs` returns freshness block test name.
    - `rg -n "status=compliant exit_code=0|status=drift exit_code=1|status=manual_review exit_code=1|status=error exit_code=2" test/mix/tasks/scoria.install_check_test.exs` returns stable trailer semantics assertions.
    - `rg -n "SCORIA_CHECK_RESULT status=" test/mix/tasks/scoria.install_check_test.exs test/mix/tasks/scoria.install_test.exs` returns machine trailer assertions.
    - `rg -n "reason_code|mix scoria\\.install --check" test/mix/tasks/scoria.install_check_test.exs` returns remediation parity assertions.
    - `MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.install_check_test.exs` passes.
    - `MIX_ENV=test mix test` passes.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.install_check_test.exs</automated>
    <automated>MIX_ENV=test mix test</automated>
  </verify>
  <done>Apply-mode safety and equivalence guarantees are proven end-to-end while check trailer and exit semantics remain stable for CI consumers.</done>
</task>

</tasks>

<threat_model>
## Security Gate

- Enforcement: enabled
- ASVS level: 1
- Block threshold: high severity threats

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner artifact -> apply filesystem writes | Apply must not mutate outside planner-classified operations. |
| Preflight blocker scan -> mutation dispatcher | Any manual_review or stale fingerprint state must short-circuit to zero writes. |
| Check/apply output -> CI gate automation | Exit code and SCORIA_CHECK_RESULT trailer are relied on by automation and must remain stable. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-60-04 | Tampering | high | apply execution path in `Mix.Tasks.Scoria.Install` and `Scoria.Install.ApplyExecutor` | block | Block if apply branch can call legacy ad-hoc mutators or bypass planner operations. | `Task 60-02-01` grep assertions for `Planner.build(... mode: :apply)` and `ApplyExecutor.run`, plus installer integration tests. |
| T-60-05 | Tampering | high | preflight safety gate and mutation ordering | block | Block if manual_review or freshness mismatch can still perform partial writes. | `Task 60-02-02` zero-write snapshot tests and stale-fingerprint block tests. |
| T-60-06 | Repudiation | high | exit semantics and trailer contract | block | Block if status/exit mapping or `SCORIA_CHECK_RESULT` trailer format changes. | Subprocess trailer assertions in `test/mix/tasks/scoria.install_check_test.exs` plus full-suite verification. |

High-severity threats T-60-04, T-60-05, and T-60-06 are hard blockers for Plan 60-02 completion.
</threat_model>

<verification>
Run task-level checks after each task, then execute:

```bash
MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.install_check_test.exs
MIX_ENV=test mix test
```
</verification>

<success_criteria>
- `mix scoria.install` apply path executes planner entries only, in deterministic order, with no fallback ad-hoc mutation pipeline.
- Any manual_review or stale-fingerprint condition blocks apply with zero writes and exit code `1`.
- Execution/tooling failures return exit code `2`; successful/compliant apply returns `0`.
- `mix scoria.install --check` trailer format and status semantics stay parse-stable as `SCORIA_CHECK_RESULT status=<status> exit_code=<exit_code>`.
</success_criteria>

<output>
After completion, create `.planning/phases/60-drift-classification-and-safe-apply/60-02-SUMMARY.md`.
</output>
