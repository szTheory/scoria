---
phase: 60-drift-classification-and-safe-apply
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/install/planner.ex
  - lib/scoria/install/report.ex
  - lib/scoria/install/manifest.ex
  - lib/scoria/install/surface/router.ex
  - lib/scoria/install/surface/runtime_config.ex
  - lib/scoria/install/surface/migrations.ex
  - lib/scoria/install/surface/tailwind.ex
  - test/scoria/install/planner_test.exs
  - test/mix/tasks/scoria.install_check_test.exs
autonomous: true
requirements: [INST-07, INST-06]
must_haves:
  truths:
    - "Planner entries carry explicit ownership mode, drift evidence, operation type, and remediation payload for every managed surface (D-01, D-02, D-04, D-11)."
    - "Missing snippet ownership markers never auto-adopt and always classify as manual_review with actionable remediation (D-03)."
    - "Human and JSON report renderers project the same remediation payload while preserving SCORIA_CHECK_RESULT trailer compatibility (D-12, D-13)."
    - "Planner ordering remains deterministic by order then stable id so Wave 2 apply execution can consume the same artifact safely (D-08, D-09)."
  artifacts:
    - path: "lib/scoria/install/planner.ex"
      provides: "Canonical phase-60 planner artifact with ownership/drift/remediation fields"
      contains: "def build(router_path, tailwind_path, config_path, opts \\\\ [])"
    - path: "lib/scoria/install/manifest.ex"
      provides: "Canonical install manifest schema and load/persist API for planner entry ownership baselines"
      contains: "def load(project_root)"
    - path: "lib/scoria/install/report.ex"
      provides: "Shared human/json remediation rendering with stable trailer contract"
      contains: "def trailer_line(%{status: status, exit_code: exit_code})"
    - path: "test/scoria/install/planner_test.exs"
      provides: "Deterministic planner-entry metadata tests for ownership/drift/remediation payload shape"
      contains: "ownership_mode"
  key_links:
    - from: "lib/scoria/install/surface/router.ex"
      to: "lib/scoria/install/planner.ex"
      via: "surface analyzer output is normalized into executable planner entries"
      pattern: "ownership_mode|reason_code|operation"
    - from: "lib/scoria/install/planner.ex"
      to: "lib/scoria/install/report.ex"
      via: "remediation payload is rendered from one canonical planner artifact"
      pattern: "remediation"
    - from: "lib/scoria/install/report.ex"
      to: "test/mix/tasks/scoria.install_check_test.exs"
      via: "stable trailer and remediation parity checks"
      pattern: "SCORIA_CHECK_RESULT"
---

<objective>
Implement the Wave 1 contract for manifest-aware drift classification so Phase 60 apply safety can execute from planner truth without ambiguous ownership.

Purpose: Deliver ownership/drift/remediation data model and reporting parity required by INST-07 while preparing planner artifact completeness for INST-06.
Output: Planner entries include ownership mode, drift reason code, operation type, remediation payload, and manifest key/fingerprint fields for router/runtime/tailwind/migrations.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-drift-classification-and-safe-apply/60-CONTEXT.md
@.planning/phases/60-drift-classification-and-safe-apply/60-RESEARCH.md
@.planning/phases/60-drift-classification-and-safe-apply/60-PATTERNS.md
@.planning/phases/60-drift-classification-and-safe-apply/60-VALIDATION.md
@lib/scoria/install/planner.ex
@lib/scoria/install/report.ex
@lib/scoria/install/surface/router.ex
@lib/scoria/install/surface/runtime_config.ex
@lib/scoria/install/surface/migrations.ex
@lib/scoria/install/surface/tailwind.ex
@test/scoria/install/planner_test.exs
@test/mix/tasks/scoria.install_check_test.exs

Locked decisions D-01 through D-13 in `60-CONTEXT.md` are mandatory. Wave 1 focuses on D-01..D-04 and D-11..D-13 while preserving deterministic planner contract used by D-08..D-10 in Wave 2.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 60-01-01: Add manifest schema and ownership-aware drift classification fields to all planner surfaces</name>
  <requirement_ids>INST-07</requirement_ids>
  <files>lib/scoria/install/manifest.ex, lib/scoria/install/surface/router.ex, lib/scoria/install/surface/runtime_config.ex, lib/scoria/install/surface/migrations.ex, lib/scoria/install/surface/tailwind.ex, lib/scoria/install/planner.ex</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/60-drift-classification-and-safe-apply/60-CONTEXT.md
    .planning/phases/60-drift-classification-and-safe-apply/60-RESEARCH.md
    .planning/phases/60-drift-classification-and-safe-apply/60-PATTERNS.md
    lib/scoria/install/planner.ex
    lib/scoria/install/surface/router.ex
    lib/scoria/install/surface/runtime_config.ex
    lib/scoria/install/surface/migrations.ex
    lib/scoria/install/surface/tailwind.ex
  </read_first>
  <action>Create `Scoria.Install.Manifest` in `lib/scoria/install/manifest.ex` with explicit schema version and APIs `path/1`, `load/1`, `write!/2`, and `entry_for/2`, keyed by planner entry id. Extend each `Scoria.Install.Surface.*.analyze/2` return map with concrete fields: `operation`, `ownership_mode`, `manifest_key`, `fingerprint`, `drift`, and `remediation`. For `router`, `runtime_config`, and `tailwind`, set `ownership_mode: :marker_region` and classify missing/ambiguous markers as `classification: :manual_review` with stable `drift.reason_code` values and remediation steps that include one exact `mix scoria.install --check` verify command. For `migrations`, set `ownership_mode: :structural_set` and store required/observed basename evidence in `drift`. In `Scoria.Install.Planner.annotate_entries/1`, preserve stable id generation and ensure every entry carries these new fields without deriving them in any other module. Add planner-level contract tests that explicitly assert missing marker ownership falls back to `manual_review` with remediation metadata.</action>
  <acceptance_criteria>
    - `rg -n "defmodule Scoria\\.Install\\.Manifest" lib/scoria/install/manifest.ex` returns one manifest module declaration.
    - `rg -n "def (path|load|write!|entry_for)\\(" lib/scoria/install/manifest.ex` returns all required manifest API functions.
    - `rg -n "ownership_mode: :marker_region" lib/scoria/install/surface/router.ex lib/scoria/install/surface/runtime_config.ex lib/scoria/install/surface/tailwind.ex` returns one or more marker-region ownership assignments.
    - `rg -n "ownership_mode: :structural_set" lib/scoria/install/surface/migrations.ex` returns one structural ownership assignment.
    - `rg -n "reason_code" lib/scoria/install/surface/router.ex lib/scoria/install/surface/runtime_config.ex lib/scoria/install/surface/migrations.ex lib/scoria/install/surface/tailwind.ex` returns explicit reason-code fields.
    - `rg -n "verify_command: \"mix scoria\\.install --check\"" lib/scoria/install/surface/router.ex lib/scoria/install/surface/runtime_config.ex lib/scoria/install/surface/migrations.ex lib/scoria/install/surface/tailwind.ex` returns remediation verify commands.
    - `rg -n "missing marker ownership falls back to manual_review" test/scoria/install/planner_test.exs` returns explicit fallback test coverage for T-60-01.
    - `rg -n "operation|manifest_key|fingerprint|drift|remediation" lib/scoria/install/planner.ex` returns planner entry field normalization logic.
    - `MIX_ENV=test mix test test/scoria/install/planner_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/install/planner_test.exs</automated>
  </verify>
  <done>All managed surfaces expose manifest-aware ownership/drift/remediation metadata with no silent takeover path for missing ownership markers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 60-01-02: Render canonical remediation payload consistently in human/json output while preserving check trailer contract</name>
  <requirement_ids>INST-07, INST-06</requirement_ids>
  <files>lib/scoria/install/planner.ex, lib/scoria/install/report.ex, test/scoria/install/planner_test.exs, test/mix/tasks/scoria.install_check_test.exs</files>
  <read_first>
    .planning/phases/60-drift-classification-and-safe-apply/60-CONTEXT.md
    .planning/phases/60-drift-classification-and-safe-apply/60-VALIDATION.md
    lib/scoria/install/planner.ex
    lib/scoria/install/report.ex
    test/scoria/install/planner_test.exs
    test/mix/tasks/scoria.install_check_test.exs
  </read_first>
  <action>Update planner entry ordering to `Enum.sort_by(&{&1.order, &1.id})` so downstream apply execution remains deterministic across equal-order entries. In `Scoria.Install.Report.render_human/2` and `render_json/2`, render the same `remediation` payload fields (`reason_code`, `summary`, `steps`, `verify_command`) from planner entries, not duplicated report-local logic. Keep `Scoria.Install.Report.trailer_line/1` string format unchanged as `SCORIA_CHECK_RESULT status=<status> exit_code=<exit_code>`, and keep `check_result/1` exit semantics stable (`0`, `1`, `2`). Add planner/report tests that assert remediation payload appears in both formats and that trailer output remains parse-stable.</action>
  <acceptance_criteria>
    - `rg -n "Enum\\.sort_by\\(&\\{&1\\.order, &1\\.id\\}\\)" lib/scoria/install/planner.ex` returns deterministic two-key sort logic.
    - `rg -n "reason_code|verify_command|steps" lib/scoria/install/report.ex` returns remediation rendering in both human and json code paths.
    - `rg -n "SCORIA_CHECK_RESULT status=\\#\\{status\\} exit_code=\\#\\{exit_code\\}" lib/scoria/install/report.ex` returns the unchanged trailer contract string.
    - `rg -n "exit_code: 0|exit_code: 1|exit_code: 2" lib/scoria/install/report.ex` returns tri-state exit mapping logic.
    - `rg -n "status=compliant exit_code=0|status=drift exit_code=1|status=manual_review exit_code=1|status=error exit_code=2" test/mix/tasks/scoria.install_check_test.exs` returns stable trailer assertions.
    - `MIX_ENV=test mix test test/scoria/install/planner_test.exs test/mix/tasks/scoria.install_check_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/install/planner_test.exs test/mix/tasks/scoria.install_check_test.exs</automated>
  </verify>
  <done>Planner artifact and report outputs share one remediation truth model and preserve existing SCORIA_CHECK_RESULT plus 0/1/2 semantics.</done>
</task>

</tasks>

<threat_model>
## Security Gate

- Enforcement: enabled
- ASVS level: 1
- Block threshold: high severity threats

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Managed-surface ownership detection -> host filesystem mutation policy | Ownership ambiguity must never downgrade to automatic writes. |
| Planner artifact -> CI/operator decisions | Drift reason and remediation truth must stay consistent across human and JSON channels. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-60-01 | Tampering | high | ownership classification in `Scoria.Install.Surface.*` | block | Block if missing marker ownership can produce `create`/`update` instead of `manual_review` with remediation. | `Task 60-01-01` grep checks for marker ownership + reason_code + verify command, plus planner tests. |
| T-60-02 | Repudiation | high | report rendering parity in `Scoria.Install.Report` | block | Block if human and JSON remediation payload diverge or omit reason code/steps/verify command. | `Task 60-01-02` grep checks and `MIX_ENV=test mix test test/scoria/install/planner_test.exs test/mix/tasks/scoria.install_check_test.exs`. |
| T-60-03 | Denial of Service | medium | manifest ingestion in planner build | mitigate | Guard manifest schema/version parsing and degrade to explicit error/manual review evidence instead of crashing planner pipeline. | Planner tests for manifest load fallback paths and check trailer `status=error exit_code=2`. |

High-severity threats T-60-01 and T-60-02 are hard blockers for Plan 60-01 completion.
</threat_model>

<verification>
Run task-level checks after each task, then execute:

```bash
MIX_ENV=test mix test test/scoria/install/planner_test.exs
MIX_ENV=test mix test test/scoria/install/planner_test.exs test/mix/tasks/scoria.install_check_test.exs
```
</verification>

<success_criteria>
- Surface analyzers encode D-01..D-04 ownership/drift rules with explicit reason codes and remediation payloads.
- Planner artifact includes deterministic operation metadata needed for apply parity without introducing ad-hoc write logic.
- Human and JSON report channels stay aligned on remediation truth and preserve SCORIA_CHECK_RESULT plus 0/1/2 semantics.
- Wave 2 can implement strict apply gate against the completed planner/manifest contract without schema redesign.
</success_criteria>

<output>
After completion, create `.planning/phases/60-drift-classification-and-safe-apply/60-01-SUMMARY.md`.
</output>
