---
phase: 14-least-iterated-screens-polish
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria_web/live/incidents_live/index.ex
  - test/scoria_web/live/incidents_live_test.exs
  - lib/scoria_web/components/incident_evidence_component.ex
  - test/scoria_web/components/incident_evidence_component_test.exs
  - test/support/ds06_baseline.txt
autonomous: true
requirements:
  - SCREEN-01
user_setup: []
must_haves:
  truths:
    - "Incidents renders through shared panels, badges, metrics, fields, and table/list treatment."
    - "IncidentEvidenceComponent is the only evidence adapter converted in Phase 14."
    - "Incident evidence keeps health rollup, budget, incident notebook, breaker/relay, and delivery outcomes."
  artifacts:
    - path: "lib/scoria_web/live/incidents_live/index.ex"
      provides: "Shared-component incidents screen"
      contains: "ScoriaWeb.IncidentsLive.Index"
    - path: "lib/scoria_web/components/incident_evidence_component.ex"
      provides: "Thin notebook/panel incident evidence adapter"
      contains: "incident-evidence-notebook"
    - path: "test/scoria_web/components/incident_evidence_component_test.exs"
      provides: "Incident evidence section preservation coverage"
      contains: "IncidentEvidenceComponent"
  key_links:
    - from: "lib/scoria_web/live/incidents_live/index.ex"
      to: "lib/scoria_web/components/incident_evidence_component.ex"
      via: "selected incident evidence render"
      pattern: "IncidentEvidenceComponent"
    - from: "lib/scoria_web/live/incidents_live/index.ex"
      to: "/workflows"
      via: "origin-preserving incident run link"
      pattern: "from=incident"
    - from: "lib/scoria_web/components/incident_evidence_component.ex"
      to: "ScoriaWeb.UI.notebook"
      via: "single allowed evidence adapter conversion"
      pattern: "<\\.notebook"
---

<objective>
Convert the Incidents screen and its first-order evidence component to the shared component layer.

Purpose: This completes the D-18 through D-21 Incidents slice without pulling in Phase 15's broad evidence-adapter sweep.
Output: Tokenized Incidents LiveView, one converted IncidentEvidenceComponent adapter, tests, and DS-06 baseline reductions.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-least-iterated-screens-polish/14-CONTEXT.md
@.planning/phases/14-least-iterated-screens-polish/14-RESEARCH.md
@.planning/phases/14-least-iterated-screens-polish/14-VALIDATION.md
@.planning/phases/14-least-iterated-screens-polish/14-PATTERNS.md
@.planning/phases/14-least-iterated-screens-polish/14-UI-SPEC.md
@.planning/phases/13-orientation-spine-ia/13-07-SUMMARY.md
@.planning/phases/12-design-system-component-layer/12-04-SUMMARY.md
@.planning/phases/12-design-system-component-layer/12-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert Incidents screen shell to shared components</name>
  <files>lib/scoria_web/live/incidents_live/index.ex, test/scoria_web/live/incidents_live_test.exs, test/support/ds06_baseline.txt</files>
  <read_first>
    - lib/scoria_web/live/incidents_live/index.ex
    - test/scoria_web/live/incidents_live_test.exs
    - test/support/ds06_baseline.txt
    - test/scoria_web/ds06_drift_guard_test.exs
    - lib/scoria_web/ui.ex
    - assets/css/04-components.css
    - .planning/phases/14-least-iterated-screens-polish/14-CONTEXT.md
    - .planning/phases/14-least-iterated-screens-polish/14-PATTERNS.md
    - .planning/phases/14-least-iterated-screens-polish/14-UI-SPEC.md
    - .planning/phases/13-orientation-spine-ia/13-07-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: Incidents renders exact empty heading `No open incidents` and body `Runtime failures, breaker trips, and delivery issues will appear here with links back to the affected run.`
    - Test 2: incident selection through `select_incident` still updates selected incident and evidence.
    - Test 3: incident run and trace links continue to include decoded `from=incident:<id>` context.
    - Test 4: rendered status badges include visible text, not color-only state.
  </behavior>
  <action>Per D-18 and D-23 through D-25, convert `ScoriaWeb.IncidentsLive.Index` to shared components while preserving `mount/3`, `select_incident`, `incident_run_path/2`, `incident_trace_path/2`, and tenant selection behavior. Use `<.metric>` for summary counts, `<.panel>` for the incident list and selected incident evidence region, `<.field>` for filters if filters exist, `<.table>` or a tokenized list for incidents, `<.badge>` with visible text for severity/status, and `<.empty_state title="No open incidents">` with the exact UI-SPEC empty body. Keep origin-context links to workflow run and home trace stream. Update `test/support/ds06_baseline.txt` by lowering or removing only the `lib/scoria_web/live/incidents_live/index.ex` row according to the scanner count.</action>
  <acceptance_criteria>
    - Source assertion: `lib/scoria_web/live/incidents_live/index.ex` contains `<.panel`, `<.metric`, and `<.badge`.
    - Source assertion: the raw-palette scanner count for `lib/scoria_web/live/incidents_live/index.ex` is less than the previous baseline value 10, and `test/support/ds06_baseline.txt` reflects the new count or removes the row at zero.
    - Behavior assertion: incidents tests verify exact empty-state copy, selected incident switching, and origin-preserving links.
    - Behavior assertion: incidents tests verify visible status/severity text appears in the badge content.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/incidents_live_test.exs test/scoria_web/ds06_drift_guard_test.exs</automated>
  </verify>
  <done>Incidents screen shell is tokenized through shared components, preserves selection and origin links, and reduces its DS-06 baseline row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert IncidentEvidenceComponent as the single evidence-adapter exception</name>
  <files>lib/scoria_web/components/incident_evidence_component.ex, test/scoria_web/components/incident_evidence_component_test.exs, test/support/ds06_baseline.txt</files>
  <read_first>
    - lib/scoria_web/components/incident_evidence_component.ex
    - test/scoria_web/components/incident_evidence_component_test.exs
    - lib/scoria_web/components/remote_invocation_evidence_component.ex
    - lib/scoria_web/ui.ex
    - assets/css/04-components.css
    - test/support/ds06_baseline.txt
    - test/scoria_web/ds06_drift_guard_test.exs
    - .planning/phases/14-least-iterated-screens-polish/14-CONTEXT.md
    - .planning/phases/14-least-iterated-screens-polish/14-PATTERNS.md
    - .planning/phases/14-least-iterated-screens-polish/14-UI-SPEC.md
    - .planning/phases/12-design-system-component-layer/12-04-SUMMARY.md
    - .planning/phases/12-design-system-component-layer/12-05-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: component renders a notebook or panel adapter with id `incident-evidence-notebook`.
    - Test 2: component output includes section text for health rollup, budget, incident notebook, breaker/relay, and delivery outcomes.
    - Test 3: raw JSON/evidence values are HEEx-escaped and not rendered through `raw/1`.
    - Test 4: no other evidence adapter files are modified in this plan.
  </behavior>
  <action>Per D-19 through D-21, convert only `ScoriaWeb.IncidentEvidenceComponent` among evidence adapters. Import the minimum needed `ScoriaWeb.UI` functions, including `notebook`, `panel`, `badge`, and `raw_evidence` as needed. Keep the existing evidence assign contract and preserve current sections: health rollup, budget, incident notebook, breaker/relay, and delivery outcomes. Wrap those sections in a thin local notebook or panel adapter using `id="incident-evidence-notebook"`. Do not edit `delegated_evidence_component.ex`, `semantic_evidence_notebook_component.ex`, `replay_evidence_notebook_component.ex`, `trace_tree_component.ex`, workflow detail panels, or any other evidence adapter. Update `test/support/ds06_baseline.txt` by lowering or removing only the `lib/scoria_web/components/incident_evidence_component.ex` row according to the scanner count.</action>
  <acceptance_criteria>
    - Source assertion: `lib/scoria_web/components/incident_evidence_component.ex` contains `<.notebook` or `<.panel` and `incident-evidence-notebook`.
    - Source assertion: `lib/scoria_web/components/incident_evidence_component.ex` does not contain `raw(`.
    - Source assertion: the raw-palette scanner count for `lib/scoria_web/components/incident_evidence_component.ex` is less than the previous baseline value 69, and `test/support/ds06_baseline.txt` reflects the new count or removes the row at zero.
    - Behavior assertion: component tests verify all five required evidence sections render with current evidence data.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/incidents_live_test.exs test/scoria_web/components/incident_evidence_component_test.exs test/scoria_web/ds06_drift_guard_test.exs</automated>
  </verify>
  <done>IncidentEvidenceComponent is the only converted evidence adapter in Phase 14 and preserves its evidence contract while reducing DS-06 baseline debt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser-to-Incidents events | Incident selection IDs and tenant query/session values influence selected evidence. |
| Incidents-to-evidence component | Incident evidence maps are rendered into HTML. |
| Incident links-to-destinations | Incident IDs and trace IDs are passed into workflow/home links with origin context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-01 | Tampering | `select_incident` event | mitigate | Preserve existing lookup from the already-loaded incident list; ignore unknown IDs. |
| T-14-04-02 | Information Disclosure | incident evidence rendering | mitigate | Render evidence through HEEx and shared UI components; do not use `raw/1`. |
| T-14-04-03 | Information Disclosure | incident run/trace links | mitigate | Preserve existing base-aware origin link helpers and URL-encode `from=incident:<id>`. |
| T-14-04-04 | Tampering | evidence adapter scope | mitigate | Tests and summary must confirm only `incident_evidence_component.ex` was converted; other evidence adapters remain untouched for Phase 15. |
| T-14-04-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks exist in this plan; RESEARCH.md package audit says no external packages are required. |
</threat_model>

<verification>
Run `mix test test/scoria_web/live/incidents_live_test.exs test/scoria_web/components/incident_evidence_component_test.exs test/scoria_web/ds06_drift_guard_test.exs`.
</verification>

<success_criteria>
Plan 14-04 succeeds when Incidents and IncidentEvidenceComponent render through shared components, all existing evidence sections and origin links still work, no out-of-scope evidence adapters are touched, and DS-06 passes with Incidents evidence baseline rows lowered.
</success_criteria>

<output>
Create `.planning/phases/14-least-iterated-screens-polish/14-04-SUMMARY.md` when done.
</output>
