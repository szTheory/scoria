---
phase: 14-least-iterated-screens-polish
plan: "03"
type: execute
wave: 3
depends_on:
  - "14-02"
files_modified:
  - lib/scoria_web/live/review_queue_live.ex
  - test/scoria_web/live/review_queue_live_test.exs
  - lib/scoria_web/live/workflow_live/show.ex
  - test/scoria_web/live/workflow_live_test.exs
  - test/support/ds06_baseline.txt
autonomous: true
requirements:
  - SCREEN-01
  - SCREEN-02
user_setup: []
must_haves:
  truths:
    - "Review Queue remains a single fast master-detail triage LiveView."
    - "Review Queue renders through shared components with no direct dataset-management form."
    - "Review Queue and Workflow Show navigate promotion affordances into Dataset Builder."
    - 'D-08: Review Queue uses index title only, no object breadcrumb, the exact subtitle `Review flagged traces before they become datasets, baselines, or dismissed noise.`, compact metrics from `@summary`, `<.field>` filters, `<.table id="review-queue">`, and a desktop detail rail.'
    - "D-09: Table columns are Candidate (rationale plus short trace/run IDs), Severity, Score, Sample, Promotion, and Action; selected state uses aria-current and/or visible Selected text/badge, never color only."
    - "D-11: Mobile detail may reuse the same content in <.drawer> if needed; filters keep form-level phx-change, and LiveView streams are not introduced without a real scaling need."
    - "D-24: Bind polish to the existing runtime token system; do not modify assets/css/02-tokens.css for cosmetic preference."
  artifacts:
    - path: "lib/scoria_web/live/review_queue_live.ex"
      provides: "Shared-component Review Queue ingress surface"
      contains: "review-queue"
    - path: "lib/scoria_web/live/workflow_live/show.ex"
      provides: "Minimum Dataset Builder promote navigation from workflow source evidence"
      contains: "Promote in Dataset Builder"
    - path: "test/support/ds06_baseline.txt"
      provides: "Removed Review Queue raw-palette baseline row after zero leakage"
      contains: "review_queue_live"
  key_links:
    - from: "lib/scoria_web/live/review_queue_live.ex"
      to: "/datasets"
      via: "review candidate promotion link"
      pattern: "promote=review"
    - from: "lib/scoria_web/live/workflow_live/show.ex"
      to: "/datasets"
      via: "workflow source promotion link"
      pattern: "promote=workflow"
    - from: "lib/scoria_web/live/review_queue_live.ex"
      to: "Scoria.Eval"
      via: "existing review queue events"
      pattern: "Eval[.](list_review_queue|summarize_review_queue|dismiss_review_candidate)"
---

<objective>
Convert Review Queue into a shared-component ingress surface and converge its promotion affordances into Dataset Builder.

Purpose: This implements D-06 through D-12 while preserving the current master-detail triage speed.
Output: Tokenized Review Queue table/filter/detail rail, Dataset Builder links, minimum Workflow Show promotion link, tests, and DS-06 baseline reduction.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-least-iterated-screens-polish/14-CONTEXT.md
@.planning/phases/14-least-iterated-screens-polish/14-RESEARCH.md
@.planning/phases/14-least-iterated-screens-polish/14-VALIDATION.md
@.planning/phases/14-least-iterated-screens-polish/14-PATTERNS.md
@.planning/phases/14-least-iterated-screens-polish/14-UI-SPEC.md
@.planning/phases/13-orientation-spine-ia/13-07-SUMMARY.md
@.planning/phases/13-orientation-spine-ia/13-08-SUMMARY.md
@.planning/phases/12-design-system-component-layer/12-02-SUMMARY.md
@.planning/phases/12-design-system-component-layer/12-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route Review Queue and Workflow promotion affordances to Dataset Builder</name>
  <files>lib/scoria_web/live/review_queue_live.ex, test/scoria_web/live/review_queue_live_test.exs, lib/scoria_web/live/workflow_live/show.ex, test/scoria_web/live/workflow_live_test.exs</files>
  <read_first>
    - lib/scoria_web/live/review_queue_live.ex
    - test/scoria_web/live/review_queue_live_test.exs
    - lib/scoria_web/live/workflow_live/show.ex
    - test/scoria_web/live/workflow_live_test.exs
    - lib/scoria_web/live/dataset_live/index.ex
    - test/scoria_web/live/dataset_live/index_test.exs
    - .planning/phases/14-least-iterated-screens-polish/14-CONTEXT.md
    - .planning/phases/14-least-iterated-screens-polish/14-PATTERNS.md
    - .planning/phases/14-least-iterated-screens-polish/14-UI-SPEC.md
    - .planning/phases/13-orientation-spine-ia/13-07-SUMMARY.md
    - .planning/phases/13-orientation-spine-ia/13-08-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: Review Queue detail renders `Promote in Dataset Builder` with href containing `promote=review`, `review_candidate_id=<id>`, and decoded `from=review:<id>`.
    - Test 2: Review Queue no longer renders direct dataset target selection, expected-output editing, `Promote candidate`, or direct sealed-baseline target picking.
    - Test 3: Workflow Show renders `Promote in Dataset Builder` or `Request baseline approval in Dataset Builder` with href containing `promote=workflow`, `run_id`, `step_id`, `source_variant`, and decoded `from=run:<id>`.
    - Test 4: Workflow Show change is limited to promotion navigation; broader Workflow Show raw-palette row remains non-increasing for Phase 15 ownership.
  </behavior>
  <action>Per D-06 and D-10, replace source-local promotion ownership with Dataset Builder navigation. In Review Queue, keep `Open run`, `View runtime context`, and dismiss behavior, then replace direct promotion and baseline controls with base-aware links labeled `Promote in Dataset Builder` and `Request baseline approval in Dataset Builder` where the current candidate state supports them. Use `URI.encode_query/1` to build `/datasets?promote=review&review_candidate_id=<candidate.id>&from=review:<candidate.id>`. In Workflow Show, replace the source-local promote button/modal entry point with a base-aware Dataset Builder link using `/datasets?promote=workflow&run_id=<run.id>&step_id=<selected_step_id>&source_variant=<selected_source_variant>&from=run:<run.id>`. Preserve `promote_span_disabled?/2`, selected step behavior, and existing quality-loop links. Do not broaden Workflow Show polish beyond these affordance lines.</action>
  <acceptance_criteria>
    - Source assertion: `lib/scoria_web/live/review_queue_live.ex` contains `Promote in Dataset Builder` and `promote=review`.
    - Source assertion: `lib/scoria_web/live/workflow_live/show.ex` contains `promote=workflow`, `run_id`, `step_id`, and `source_variant`.
    - Source assertion: Review Queue render no longer contains direct expected-output JSON editing controls for promotion.
    - Behavior assertion: tests parse hrefs and verify decoded `from=review:<id>` and `from=run:<id>` query values.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/review_queue_live_test.exs test/scoria_web/live/workflow_live_test.exs</automated>
  </verify>
  <done>Review Queue and Workflow Show point promotion actions to Dataset Builder with stable IDs and origin context while preserving non-promotion triage actions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert Review Queue to shared components and update DS-06</name>
  <files>lib/scoria_web/live/review_queue_live.ex, test/scoria_web/live/review_queue_live_test.exs, test/support/ds06_baseline.txt</files>
  <read_first>
    - lib/scoria_web/live/review_queue_live.ex
    - test/scoria_web/live/review_queue_live_test.exs
    - test/support/ds06_baseline.txt
    - test/scoria_web/ds06_drift_guard_test.exs
    - lib/scoria_web/ui.ex
    - assets/css/04-components.css
    - .planning/phases/14-least-iterated-screens-polish/14-CONTEXT.md
    - .planning/phases/14-least-iterated-screens-polish/14-PATTERNS.md
    - .planning/phases/14-least-iterated-screens-polish/14-UI-SPEC.md
    - .planning/phases/12-design-system-component-layer/12-02-SUMMARY.md
    - .planning/phases/12-design-system-component-layer/12-03-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: Review Queue renders title `Review Queue` and subtitle `Review flagged traces before they become datasets, baselines, or dismissed noise.`
    - Test 2: Review Queue renders `<.table id="review-queue">` with visible columns `Candidate`, `Severity`, `Score`, `Sample`, `Promotion`, and `Action`.
    - Test 3: selected row state includes visible `Selected` text or badge and an `aria-current` or equivalent semantic selected-state attribute.
    - Test 4: empty state uses heading `No flagged traces for this filter set` and body `Production traces that fail scoring, trigger policy, or look promotion-ready will appear here.`
  </behavior>
  <action>Per D-07 through D-12 and D-22, keep Review Queue as a single LiveView and preserve form-level `phx-change="change_filters"`, `select_candidate`, and `dismiss_candidate` behavior. Convert markup to shared components: compact metric strip from current `@summary` via `<.metric>`, filter panel fields via `<.field>`, primary queue via `<.table id="review-queue" rows={@queue_rows} density={:compact}>`, status via text-labeled `<.badge>`, and selected-candidate detail via `<.panel>` or tokenized rail. Use the exact subtitle and empty-state copy from the UI-SPEC. The table column order must be `Candidate`, `Severity`, `Score`, `Sample`, `Promotion`, `Action`. Add visible `Selected` text or badge and `aria-current` or equivalent semantic state on the selected row. Remove every DS-06 raw-palette match from `lib/scoria_web/live/review_queue_live.ex`, then remove the corresponding `test/support/ds06_baseline.txt` row instead of lowering it.</action>
  <acceptance_criteria>
    - Source assertion: `lib/scoria_web/live/review_queue_live.ex` contains `<.table`, `id="review-queue"`, `<.metric`, and `<.field`.
    - Source assertion: the DS-06 raw-palette scanner count for `lib/scoria_web/live/review_queue_live.ex` is zero.
    - Source assertion: `test/support/ds06_baseline.txt` has no line beginning `lib/scoria_web/live/review_queue_live.ex:`.
    - Behavior assertion: tests verify exact subtitle, exact empty-state copy, the six required table column labels, and selected-state semantics.
    - Behavior assertion: tests prove `dismiss_candidate` still removes a candidate from the active queue.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/review_queue_live_test.exs test/scoria_web/live/workflow_live_test.exs test/scoria_web/ds06_drift_guard_test.exs && ! grep -E '^lib/scoria_web/live/review_queue_live\.ex:' test/support/ds06_baseline.txt</automated>
  </verify>
  <done>Review Queue is a shared-component ingress screen with fast master-detail triage, Dataset Builder promotion links, zero DS-06 raw-palette leakage, and no Review Queue baseline row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser-to-ReviewQueue events | Filter, select, dismiss, and link params are controlled by the browser. |
| ReviewQueue-to-DatasetBuilder URL | Review candidate IDs and origin context cross screens through query params. |
| WorkflowShow-to-DatasetBuilder URL | Workflow run ID, step ID, and source variant cross screens through query params. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Tampering | Review Queue Dataset Builder links | mitigate | Build links with `URI.encode_query/1`; pass stable IDs and `from=review:<id>` only. |
| T-14-03-02 | Tampering | Workflow Show Dataset Builder links | mitigate | Keep URL params to `promote`, `run_id`, `step_id`, `source_variant`, and `from`; Dataset Builder from Plan 14-02 validates and reconstructs. |
| T-14-03-03 | Information Disclosure | Review candidate table/detail rendering | mitigate | Render all candidate text through HEEx escaping and avoid `raw/1`; preserve existing `Eval.list_review_queue/1` and `Eval.get_review_candidate/1` data boundaries. |
| T-14-03-04 | Denial of Service | Review Queue filters | accept | Existing form-level filter event remains; no streams, polling, or unbounded query surface is added in this UI conversion. |
| T-14-03-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks exist in this plan; RESEARCH.md package audit says no external packages are required. |
</threat_model>

<verification>
Run `mix test test/scoria_web/live/review_queue_live_test.exs test/scoria_web/live/workflow_live_test.exs test/scoria_web/ds06_drift_guard_test.exs` and confirm `grep -E '^lib/scoria_web/live/review_queue_live\.ex:' test/support/ds06_baseline.txt` returns no rows.
</verification>

<success_criteria>
Plan 14-03 succeeds when Review Queue is a shared-component master-detail screen, its promotion actions route to Dataset Builder, direct dataset-management affordances are absent, Workflow Show has only the minimum Dataset Builder promotion link change, Review Queue has zero DS-06 raw-palette matches, and its `test/support/ds06_baseline.txt` row is absent.
</success_criteria>

<output>
Create `.planning/phases/14-least-iterated-screens-polish/14-03-SUMMARY.md` when done.
</output>
