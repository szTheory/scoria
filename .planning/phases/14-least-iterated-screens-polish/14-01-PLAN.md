---
phase: 14-least-iterated-screens-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria_web/live/dataset_live/index.ex
  - test/scoria_web/live/dataset_live/index_test.exs
  - lib/scoria_web/dashboard_nav.ex
  - test/scoria_web/dashboard_nav_test.exs
  - lib/scoria_web/router.ex
  - test/scoria_web/router_test.exs
autonomous: true
requirements:
  - SCREEN-02
user_setup: []
must_haves:
  truths:
    - "Dataset Builder is a real Improve screen at /datasets, not a coming-soon capability."
    - "Operators can reach Dataset Builder through sidebar navigation, command palette navigation, and the g d shortcut."
    - "Dataset Builder lists real open and sealed datasets and renders an honest empty state when none exist."
    - "D-24: Bind polish to the existing runtime token system; do not modify assets/css/02-tokens.css for cosmetic preference."
  artifacts:
    - path: "lib/scoria_web/live/dataset_live/index.ex"
      provides: "Dataset Builder index LiveView"
      contains: "defmodule ScoriaWeb.DatasetLive.Index"
    - path: "test/scoria_web/live/dataset_live/index_test.exs"
      provides: "Dataset Builder route and index behavior coverage"
      contains: "Dataset Builder"
    - path: "lib/scoria_web/dashboard_nav.ex"
      provides: "Dataset Builder nav, active view, command row, and shortcut metadata"
      contains: "Dataset Builder"
  key_links:
    - from: "lib/scoria_web/router.ex"
      to: "lib/scoria_web/live/dataset_live/index.ex"
      via: "live /datasets route"
      pattern: "live\\(\"/datasets\", ScoriaWeb\\.DatasetLive\\.Index, :index\\)"
    - from: "lib/scoria_web/dashboard_nav.ex"
      to: "/datasets"
      via: "Improve nav item and command row"
      pattern: "datasets.*g d|g d.*datasets"
    - from: "lib/scoria_web/live/dataset_live/index.ex"
      to: "Scoria.Eval"
      via: "real dataset loading"
      pattern: "Eval\\.list_datasets"
---

<objective>
Create the canonical Dataset Builder entry point required by D-01 and SCREEN-02.

Purpose: This gives promotion flows a real destination before source screens begin routing their promote affordances there.
Output: A tested `/datasets` LiveView, dashboard route, nav item, command-palette row, active-state mapping, and shortcut.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-least-iterated-screens-polish/14-CONTEXT.md
@.planning/phases/14-least-iterated-screens-polish/14-RESEARCH.md
@.planning/phases/14-least-iterated-screens-polish/14-VALIDATION.md
@.planning/phases/14-least-iterated-screens-polish/14-PATTERNS.md
@.planning/phases/14-least-iterated-screens-polish/14-UI-SPEC.md
@.planning/phases/13-orientation-spine-ia/13-02-SUMMARY.md
@.planning/phases/12-design-system-component-layer/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Dataset Builder route and navigation contracts</name>
  <files>lib/scoria_web/dashboard_nav.ex, test/scoria_web/dashboard_nav_test.exs, lib/scoria_web/router.ex, test/scoria_web/router_test.exs</files>
  <read_first>
    - lib/scoria_web/dashboard_nav.ex
    - test/scoria_web/dashboard_nav_test.exs
    - lib/scoria_web/router.ex
    - test/scoria_web/router_test.exs
    - .planning/phases/14-least-iterated-screens-polish/14-CONTEXT.md
    - .planning/phases/14-least-iterated-screens-polish/14-PATTERNS.md
    - .planning/phases/14-least-iterated-screens-polish/14-UI-SPEC.md
    - .planning/phases/13-orientation-spine-ia/13-02-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: `DashboardNav.groups/0` places `Dataset Builder` in Improve after `Review Queue` and before `Eval Workbench`.
    - Test 2: `DashboardNav.command_sections("/scoria")` contains `Dataset Builder` with path `/scoria/datasets`, aliases including `dataset`, `datasets`, and `builder`, and kbd `g d`.
    - Test 3: `DashboardNav.active_key(ScoriaWeb.DatasetLive.Index)` returns `:datasets` and `DashboardNav.stub_screen("dataset-builder")` returns nil.
    - Test 4: the dashboard router recognizes `/scoria/datasets` through `scoria_dashboard("/scoria")`.
  </behavior>
  <action>Per D-01, add a real Dataset Builder nav item with `key: :datasets`, `label: "Dataset Builder"`, `path: "/datasets"`, `icon: :grid`, and aliases `["dataset", "datasets", "builder"]`. Add `ScoriaWeb.DatasetLive.Index => :datasets` to `@views`, `datasets: "g d"` to `@g_chords`, and `datasets` to the regex inside `strip_known_prefixes/1`. Add `live("/datasets", ScoriaWeb.DatasetLive.Index, :index)` inside the existing `:scoria_dashboard` live session. Update tests before implementation and keep route construction base-aware under arbitrary `scoria_dashboard("/path")` mounts.</action>
  <acceptance_criteria>
    - Source assertion: `lib/scoria_web/dashboard_nav.ex` contains `key: :datasets`, `label: "Dataset Builder"`, `path: "/datasets"`, and `datasets: "g d"`.
    - Source assertion: `lib/scoria_web/router.ex` contains `live("/datasets", ScoriaWeb.DatasetLive.Index, :index)`.
    - Behavior assertion: dashboard nav tests prove Dataset Builder is not returned by `stub_screen/1`.
    - Behavior assertion: route coverage proves `/scoria/datasets` renders through the dashboard route harness.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/dashboard_nav_test.exs test/scoria_web/router_test.exs</automated>
  </verify>
  <done>Dataset Builder is routable and discoverable through nav, command palette metadata, active-state lookup, and `g d` without adding raw palette classes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create real Dataset Builder index</name>
  <files>lib/scoria_web/live/dataset_live/index.ex, test/scoria_web/live/dataset_live/index_test.exs</files>
  <read_first>
    - lib/scoria_web/live/dataset_live/index.ex
    - test/scoria_web/live/dataset_live/index_test.exs
    - lib/scoria_web/live/workflow_live/index.ex
    - lib/scoria_web/live/prompt_live/release_workbench_live.ex
    - lib/scoria/eval.ex
    - lib/scoria/eval/dataset.ex
    - lib/scoria/eval/dataset_item.ex
    - lib/scoria_web/ui.ex
    - .planning/phases/14-least-iterated-screens-polish/14-CONTEXT.md
    - .planning/phases/14-least-iterated-screens-polish/14-PATTERNS.md
    - .planning/phases/14-least-iterated-screens-polish/14-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: `/scoria/datasets` renders index title `Dataset Builder` and subtitle `Curate production traces into eval datasets and baseline approval requests.`
    - Test 2: when no datasets exist, the page renders empty heading `No datasets yet` and body `Promote a flagged trace or workflow source to start a regression dataset.`
    - Test 3: when open and sealed datasets exist, `<.table id="datasets">` renders columns `Dataset`, `State`, `Items`, `Last promoted`, `Source`, and `Action` from real records.
    - Test 4: `lib/scoria_web/live/dataset_live/index.ex` introduces zero raw-palette matches under the DS-06 scanner.
  </behavior>
  <action>Per D-01 and D-02, create `ScoriaWeb.DatasetLive.Index` with `use Phoenix.LiveView, layout: {ScoriaWeb.Layouts, :app}`, `import ScoriaWeb.UI`, and `alias Scoria.Eval`. On mount, assign `:page_title` to `Dataset Builder`, load real datasets through `Eval.list_datasets/0`, compute open and sealed dataset counts from real dataset state/version data, and never fabricate rows or counts. Render a page header with the exact title and subtitle from the UI-SPEC, a compact metric strip, and `<.table id="datasets" rows={@datasets} density={:compact}>` with the required columns. Use `<.empty_state title="No datasets yet">` with the exact empty body from the UI-SPEC when the dataset list is empty. Do not implement promotion drawer state in this plan; leave URL promotion handling for Plan 14-02.</action>
  <acceptance_criteria>
    - Source assertion: `lib/scoria_web/live/dataset_live/index.ex` contains `defmodule ScoriaWeb.DatasetLive.Index`, `Eval.list_datasets`, and `id="datasets"`.
    - Source assertion: the new LiveView contains no `stone-`, `rose-`, `sky-`, `emerald-`, `amber-`, `slate-`, `zinc-`, or `gray-` raw palette class strings.
    - Behavior assertion: Dataset Builder index test renders exact title, subtitle, empty heading, and empty body.
    - Behavior assertion: Dataset Builder index test creates real dataset records and sees them in the `datasets` table.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/dataset_live/index_test.exs test/scoria_web/dashboard_nav_test.exs test/scoria_web/router_test.exs test/scoria_web/ds06_drift_guard_test.exs</automated>
  </verify>
  <done>`/datasets` is a real, tested, data-backed Dataset Builder index with no fake rows and no raw-palette leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser-to-LiveView route | User navigates to `/datasets`; route params and session data are untrusted inputs. |
| LiveView-to-Eval context | Dataset Builder reads persisted dataset records through existing Eval APIs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Spoofing | `ScoriaWeb.DatasetLive.Index` | accept | This plan adds no authentication or session model; it stays inside the existing `scoria_dashboard/2` live session. |
| T-14-01-02 | Information Disclosure | dataset index rendering | mitigate | Read datasets only through existing `Scoria.Eval` APIs and render values through HEEx interpolation; do not use `raw/1`. |
| T-14-01-03 | Tampering | nav and route metadata | mitigate | Add deterministic tests for nav order, active key, command row, shortcut, and route existence so Dataset Builder cannot silently degrade into a coming-soon entry. |
| T-14-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks exist in this plan; RESEARCH.md package audit says no external packages are required. |
</threat_model>

<verification>
Run `mix test test/scoria_web/live/dataset_live/index_test.exs test/scoria_web/dashboard_nav_test.exs test/scoria_web/router_test.exs test/scoria_web/ds06_drift_guard_test.exs`.
</verification>

<success_criteria>
Plan 14-01 succeeds when `/scoria/datasets` renders a real Dataset Builder index, Dataset Builder is listed in Improve navigation and command metadata with `g d`, and DS-06 reports no new raw-palette leakage.
</success_criteria>

<output>
Create `.planning/phases/14-least-iterated-screens-polish/14-01-SUMMARY.md` when done.
</output>
