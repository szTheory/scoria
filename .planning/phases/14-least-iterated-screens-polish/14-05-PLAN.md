---
phase: 14-least-iterated-screens-polish
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria_web/live/eval_spec_live/index.ex
  - test/scoria_web/live/eval_spec_live/index_test.exs
autonomous: true
requirements:
  - SCREEN-01
user_setup: []
must_haves:
  truths:
    - "Eval Workbench answers what was evaluated, against which dataset/spec, what regressed, and where the operator goes next."
    - "Eval Workbench uses shared tables, panels, fields, badges, and empty states without adding fake experiment controls."
    - "Existing eval edit/results behavior and quality-loop links are preserved."
    - "D-24: Bind polish to the existing runtime token system; do not modify assets/css/02-tokens.css for cosmetic preference."
  artifacts:
    - path: "lib/scoria_web/live/eval_spec_live/index.ex"
      provides: "Shared-component Eval Workbench"
      contains: "Eval Workbench"
    - path: "test/scoria_web/live/eval_spec_live/index_test.exs"
      provides: "Eval Workbench behavior and link coverage"
      contains: "Open prompt release"
  key_links:
    - from: "lib/scoria_web/live/eval_spec_live/index.ex"
      to: "/prompts/:id/release"
      via: "prompt release link"
      pattern: "prompt_release_path"
    - from: "lib/scoria_web/live/eval_spec_live/index.ex"
      to: "/workflows/:id"
      via: "regressed run link"
      pattern: "regressed_runs_path"
    - from: "lib/scoria_web/live/eval_spec_live/index.ex"
      to: "Scoria.Eval"
      via: "existing backed eval spec/run behavior"
      pattern: "Eval\\.(list_eval_specs|update_eval_spec)"
---

<objective>
Convert Eval Workbench to the shared component layer while preserving backed eval behavior.

Purpose: This completes the Eval Workbench portion of SCREEN-01 and keeps the quality-loop links created in Phase 13 intact.
Output: Tokenized Eval Workbench tables/forms/panels and focused tests proving behavior and links remain.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-least-iterated-screens-polish/14-CONTEXT.md
@.planning/phases/14-least-iterated-screens-polish/14-RESEARCH.md
@.planning/phases/14-least-iterated-screens-polish/14-VALIDATION.md
@.planning/phases/14-least-iterated-screens-polish/14-PATTERNS.md
@.planning/phases/14-least-iterated-screens-polish/14-UI-SPEC.md
@.planning/phases/13-orientation-spine-ia/13-08-SUMMARY.md
@.planning/phases/12-design-system-component-layer/12-02-SUMMARY.md
@.planning/phases/12-design-system-component-layer/12-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert Eval Workbench list and results surfaces</name>
  <files>lib/scoria_web/live/eval_spec_live/index.ex, test/scoria_web/live/eval_spec_live/index_test.exs</files>
  <read_first>
    - lib/scoria_web/live/eval_spec_live/index.ex
    - test/scoria_web/live/eval_spec_live/index_test.exs
    - lib/scoria_web/ui.ex
    - assets/css/04-components.css
    - .planning/phases/14-least-iterated-screens-polish/14-CONTEXT.md
    - .planning/phases/14-least-iterated-screens-polish/14-PATTERNS.md
    - .planning/phases/14-least-iterated-screens-polish/14-UI-SPEC.md
    - .planning/phases/13-orientation-spine-ia/13-08-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: Eval Workbench renders exact empty heading `No eval runs yet` and body `Promote a production trace to a dataset, then run an eval to compare prompt behavior against a baseline.`
    - Test 2: eval spec and eval run result surfaces use `scoria-table` through `<.table>` and visible text-labeled badges.
    - Test 3: existing `Open prompt release`, `Open regressed runs`, and decoded `from=eval:<eval-run-id>` links still render when backed data exists.
    - Test 4: rendered markup does not add experiment controls, fake charts, playground affordances, or wizard/stepper language.
  </behavior>
  <action>Per D-13, D-14, D-17, D-23, and D-25, convert Eval Workbench list/results markup to shared components. Use `<.panel>` for screen regions, `<.table>` for eval specs and eval result rows, `<.badge>` with visible tone text for status/outcome, and `<.empty_state title="No eval runs yet">` with the exact UI-SPEC body when no eval runs exist. Preserve `prompt_release_path/2`, `regressed_runs_path/2`, `origin_query/1`, `Eval.list_eval_specs/0`, and current eval result row behavior. Do not add experiment controls, fake charts, playground controls, steppers, or capability-implying UI.</action>
  <acceptance_criteria>
    - Source assertion: `lib/scoria_web/live/eval_spec_live/index.ex` contains `<.table`, `<.panel`, and `<.badge`.
    - Source assertion: `lib/scoria_web/live/eval_spec_live/index.ex` does not contain `wizard`, `stepper`, `playground`, or fake chart terms introduced by this plan.
    - Behavior assertion: tests verify exact eval empty-state heading/body and quality-loop hrefs to prompt release and regressed workflow runs.
    - Behavior assertion: DS-06 guard passes while `eval_spec_live/index.ex` remains at zero raw-palette baseline.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/eval_spec_live/index_test.exs test/scoria_web/ds06_drift_guard_test.exs</automated>
  </verify>
  <done>Eval Workbench list/results surfaces render through shared components and preserve Phase 13 quality-loop links without raw-palette leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert Eval Workbench edit form controls</name>
  <files>lib/scoria_web/live/eval_spec_live/index.ex, test/scoria_web/live/eval_spec_live/index_test.exs</files>
  <read_first>
    - lib/scoria_web/live/eval_spec_live/index.ex
    - test/scoria_web/live/eval_spec_live/index_test.exs
    - lib/scoria_web/ui.ex
    - assets/css/04-components.css
    - lib/scoria/eval.ex
    - .planning/phases/14-least-iterated-screens-polish/14-CONTEXT.md
    - .planning/phases/14-least-iterated-screens-polish/14-PATTERNS.md
    - .planning/phases/14-least-iterated-screens-polish/14-UI-SPEC.md
    - .planning/phases/12-design-system-component-layer/12-03-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: existing edit, save, cancel, and JSON rubric validation behavior still passes.
    - Test 2: edit form uses shared `<.field>` and `<.form_section>` wrappers.
    - Test 3: changeset errors render as visible text, not color-only indicators.
  </behavior>
  <action>Convert Eval Workbench edit form markup to `<.form_section>` groups and `<.field>` wrappers for every backed input already present in the screen. Preserve existing `handle_event("edit")`, `handle_event("cancel_edit")`, `handle_event("save")`, JSON rubric parsing, and changeset error behavior. Keep current input names and event payload shapes so existing tests and `Eval.update_eval_spec/2` continue to work. Error text must remain visible next to the associated field.</action>
  <acceptance_criteria>
    - Source assertion: edit form area in `lib/scoria_web/live/eval_spec_live/index.ex` contains `<.form_section` and `<.field`.
    - Behavior assertion: existing save/cancel/edit tests pass without changing event names.
    - Behavior assertion: invalid rubric JSON still renders a visible validation error.
    - Behavior assertion: `mix test test/scoria_web/ds06_drift_guard_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/eval_spec_live/index_test.exs test/scoria_web/ds06_drift_guard_test.exs</automated>
  </verify>
  <done>Eval Workbench forms use shared field/section components while backed edit and validation behavior remains unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser-to-EvalLive events | Eval spec edit/save params and JSON rubric payloads are user-controlled. |
| EvalLive-to-Eval context | Eval spec and eval run records are loaded/mutated through existing Eval APIs. |
| EvalLive-to-linked screens | Prompt release and regressed run links pass origin context through query params. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05-01 | Tampering | eval spec save form | mitigate | Preserve existing changeset and JSON rubric validation; do not alter event payload names or mutation APIs. |
| T-14-05-02 | Information Disclosure | eval result rendering | mitigate | Render dataset/spec/run output through HEEx and shared components; do not introduce `raw/1`. |
| T-14-05-03 | Tampering | quality-loop hrefs | mitigate | Preserve `URI.encode_query/1` origin query pattern from Phase 13 for `from=eval:<id>`. |
| T-14-05-04 | Spoofing | fake experiment controls | mitigate | Do not add controls that imply experiments can run unless backed by existing code; tests assert prohibited UI terms are absent. |
| T-14-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks exist in this plan; RESEARCH.md package audit says no external packages are required. |
</threat_model>

<verification>
Run `mix test test/scoria_web/live/eval_spec_live/index_test.exs test/scoria_web/ds06_drift_guard_test.exs`.
</verification>

<success_criteria>
Plan 14-05 succeeds when Eval Workbench list/results/forms render through shared components, backed edit and quality-loop behavior remains intact, no fake experiment UI is added, and DS-06 remains green with eval at zero raw palette.
</success_criteria>

<output>
Create `.planning/phases/14-least-iterated-screens-polish/14-05-SUMMARY.md` when done.
</output>
