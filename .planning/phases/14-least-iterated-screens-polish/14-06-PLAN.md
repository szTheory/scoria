---
phase: 14-least-iterated-screens-polish
plan: "06"
type: execute
wave: 4
depends_on:
  - "14-03"
files_modified:
  - lib/scoria_web/live/prompt_live/index.ex
  - test/scoria_web/live/prompt_live_test.exs
  - lib/scoria_web/live/prompt_live/release_workbench_live.ex
  - test/scoria_web/live/prompt_live/release_workbench_live_test.exs
  - test/support/ds06_baseline.txt
autonomous: true
requirements:
  - SCREEN-01
user_setup: []
must_haves:
  truths:
    - "Prompt Registry shows prompt versions, state, and release eligibility through shared components."
    - "Release Workbench remains a focused object page with object header, flat next-step verbs, comparison panels, and shared approve/reject modals."
    - "Prompt Registry and Release Workbench keep backed edit, token-estimate, approval, and release behavior."
    - "D-24: Bind polish to the existing runtime token system; do not modify assets/css/02-tokens.css for cosmetic preference."
  artifacts:
    - path: "lib/scoria_web/live/prompt_live/index.ex"
      provides: "Shared-component Prompt Registry"
      contains: "Prompt Registry"
    - path: "lib/scoria_web/live/prompt_live/release_workbench_live.ex"
      provides: "Shared-component Release Workbench"
      contains: "Release Workbench"
    - path: "test/support/ds06_baseline.txt"
      provides: "Removed Release Workbench raw-palette baseline row after zero leakage"
      contains: "release_workbench_live"
  key_links:
    - from: "lib/scoria_web/live/prompt_live/release_workbench_live.ex"
      to: "/eval_specs"
      via: "View eval results next-step link"
      pattern: "View eval results"
    - from: "lib/scoria_web/live/prompt_live/release_workbench_live.ex"
      to: "/workflows"
      via: "View baseline runs next-step link"
      pattern: "View baseline runs"
    - from: "lib/scoria_web/live/prompt_live/index.ex"
      to: "Scoria.Prompts"
      via: "existing prompt edit/version behavior"
      pattern: "PromptTemplate\\.changeset|PromptRelease"
---

<objective>
Convert Prompt Registry and Release Workbench to shared components while preserving backed prompt/release workflows.

Purpose: This completes the Prompt Registry / Release Workbench portion of SCREEN-01 without merging it into Eval Workbench or implying unbacked experiment capability.
Output: Tokenized Prompt Registry, tokenized Release Workbench comparison/approval UI, tests, and DS-06 baseline reduction for Release Workbench.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-least-iterated-screens-polish/14-CONTEXT.md
@.planning/phases/14-least-iterated-screens-polish/14-RESEARCH.md
@.planning/phases/14-least-iterated-screens-polish/14-VALIDATION.md
@.planning/phases/14-least-iterated-screens-polish/14-PATTERNS.md
@.planning/phases/14-least-iterated-screens-polish/14-UI-SPEC.md
@.planning/phases/13-orientation-spine-ia/13-08-SUMMARY.md
@.planning/phases/12-design-system-component-layer/12-02-SUMMARY.md
@.planning/phases/12-design-system-component-layer/12-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert Prompt Registry table and edit form</name>
  <files>lib/scoria_web/live/prompt_live/index.ex, test/scoria_web/live/prompt_live_test.exs</files>
  <read_first>
    - lib/scoria_web/live/prompt_live/index.ex
    - test/scoria_web/live/prompt_live_test.exs
    - lib/scoria_web/ui.ex
    - assets/css/04-components.css
    - .planning/phases/14-least-iterated-screens-polish/14-CONTEXT.md
    - .planning/phases/14-least-iterated-screens-polish/14-PATTERNS.md
    - .planning/phases/14-least-iterated-screens-polish/14-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: Prompt Registry renders exact empty heading `No prompt versions yet` and body `Prompt versions appear after backed prompt edits are recorded.`
    - Test 2: prompt versions table uses shared table markup and visible text-labeled state badges.
    - Test 3: existing edit, validate, token estimate, save, and draft update behavior still passes.
    - Test 4: Prompt Registry introduces no raw-palette baseline row and stays at zero raw-palette matches.
  </behavior>
  <action>Per D-13, D-15, D-17, D-23, and D-25, convert Prompt Registry markup to shared components while preserving backed prompt edit behavior. Use `<.panel>` for screen regions, `<.table>` for prompt versions, `<.badge>` for visible state labels, `<.form_section>` and `<.field>` for edit form controls, and `<.empty_state title="No prompt versions yet">` with the exact UI-SPEC body when no versions exist. Preserve `handle_event("validate")`, token estimation with `Tokenizer.estimate_tokens/1`, save/draft behavior, current form input names, and existing tests. Do not add generated variants, model playground controls, hidden client-side experiment state, or fake experiment controls.</action>
  <acceptance_criteria>
    - Source assertion: `lib/scoria_web/live/prompt_live/index.ex` contains `<.table`, `<.field`, and `<.form_section`.
    - Source assertion: `lib/scoria_web/live/prompt_live/index.ex` does not contain `stone-`, `rose-`, `sky-`, `emerald-`, `amber-`, `slate-`, `zinc-`, or `gray-` raw palette class strings.
    - Behavior assertion: prompt live tests verify exact empty-state copy and existing validate/token-estimate/save behavior.
    - Behavior assertion: tests verify Prompt Registry remains separate from Eval Workbench and does not render a merged Improve Workbench tab shell.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/prompt_live_test.exs test/scoria_web/ds06_drift_guard_test.exs</automated>
  </verify>
  <done>Prompt Registry uses shared table/form components, preserves backed prompt editing, and remains at zero raw-palette leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert Release Workbench comparison and approval UI</name>
  <files>lib/scoria_web/live/prompt_live/release_workbench_live.ex, test/scoria_web/live/prompt_live/release_workbench_live_test.exs, test/support/ds06_baseline.txt</files>
  <read_first>
    - lib/scoria_web/live/prompt_live/release_workbench_live.ex
    - test/scoria_web/live/prompt_live/release_workbench_live_test.exs
    - test/support/ds06_baseline.txt
    - test/scoria_web/ds06_drift_guard_test.exs
    - lib/scoria_web/ui.ex
    - assets/css/04-components.css
    - .planning/phases/14-least-iterated-screens-polish/14-CONTEXT.md
    - .planning/phases/14-least-iterated-screens-polish/14-PATTERNS.md
    - .planning/phases/14-least-iterated-screens-polish/14-UI-SPEC.md
    - .planning/phases/13-orientation-spine-ia/13-08-SUMMARY.md
    - .planning/phases/12-design-system-component-layer/12-03-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: Release Workbench still renders `<.object_header>` with Prompt Registry parent path.
    - Test 2: next-step row renders exact verbs `View eval results` and `View baseline runs`.
    - Test 3: comparison regions render visible headings `Draft Candidate` and `Active Baseline`.
    - Test 4: approve and reject flows still use backed events and shared modal markup with text-labeled notices.
  </behavior>
  <action>Per D-16, D-17, and D-22, keep Release Workbench as a focused object page and preserve existing `object_header`, `origin_context`, `request_release`, `approve_release`, and reject behavior. Convert the comparison deck, notices, approval rail, and approve/reject modal bodies to shared components: use `<.panel>` for `Draft Candidate` and `Active Baseline`, `<.badge>` for text-labeled status, `<.button>` for actions, and `<.modal>` for approve/reject confirmations. Keep next-step verbs exactly `View eval results` and `View baseline runs`; do not add steppers, wizard language, generated variants, model playground controls, or hidden client-side experiment state. Remove every DS-06 raw-palette match from `lib/scoria_web/live/prompt_live/release_workbench_live.ex`, then remove the corresponding `test/support/ds06_baseline.txt` row instead of lowering it.</action>
  <acceptance_criteria>
    - Source assertion: `lib/scoria_web/live/prompt_live/release_workbench_live.ex` contains `<.object_header`, `<.panel`, `<.modal`, and the exact strings `Draft Candidate` and `Active Baseline`.
    - Source assertion: the DS-06 raw-palette scanner count for `lib/scoria_web/live/prompt_live/release_workbench_live.ex` is zero.
    - Source assertion: `test/support/ds06_baseline.txt` has no line beginning `lib/scoria_web/live/prompt_live/release_workbench_live.ex:`.
    - Behavior assertion: release workbench tests verify `View eval results`, `View baseline runs`, approve flow, and reject flow still pass.
    - Behavior assertion: release workbench tests verify no stepper, wizard, or experiment-control copy appears.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/prompt_live_test.exs test/scoria_web/live/prompt_live/release_workbench_live_test.exs test/scoria_web/ds06_drift_guard_test.exs && ! grep -E '^lib/scoria_web/live/prompt_live/release_workbench_live\.ex:' test/support/ds06_baseline.txt</automated>
  </verify>
  <done>Release Workbench uses shared comparison panels and modal shells, preserves backed approval behavior, reaches zero DS-06 raw-palette leakage, and has no Release Workbench baseline row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser-to-PromptLive events | Prompt edit, validate, save, approve, and reject events carry user-controlled params. |
| PromptLive-to-prompt contexts | Prompt versions and release approvals are loaded/mutated through existing contexts. |
| ReleaseWorkbench-to-linked screens | Eval result and baseline run links pass origin query params to other LiveViews. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06-01 | Tampering | prompt edit form | mitigate | Preserve existing changeset, input names, token estimation, and save/draft update behavior. |
| T-14-06-02 | Tampering | release approval events | mitigate | Preserve existing `PromptRelease.start_release_workflow`, `PromptRelease.approve`, and reject event flows; only convert visual shells. |
| T-14-06-03 | Information Disclosure | prompt body/comparison rendering | mitigate | Render prompt text through HEEx and shared components; do not introduce `raw/1`. |
| T-14-06-04 | Spoofing | fake experiment controls | mitigate | Do not add generated variants, playground controls, or wizard/stepper copy; tests assert prohibited UI terms are absent. |
| T-14-06-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks exist in this plan; RESEARCH.md package audit says no external packages are required. |
</threat_model>

<verification>
Run `mix test test/scoria_web/live/prompt_live_test.exs test/scoria_web/live/prompt_live/release_workbench_live_test.exs test/scoria_web/ds06_drift_guard_test.exs` and confirm `grep -E '^lib/scoria_web/live/prompt_live/release_workbench_live\.ex:' test/support/ds06_baseline.txt` returns no rows.

End-of-phase visual/rubric verification: after Plans 14-01 through 14-06 are implemented, start the dashboard and render or capture Dataset Builder, Review Queue, Incidents, Eval Workbench, Prompt Registry, and Release Workbench. Compare each screen against `.planning/phases/14-least-iterated-screens-polish/14-UI-SPEC.md` and the manual-only rubric checks in `.planning/phases/14-least-iterated-screens-polish/14-VALIDATION.md`; the phase is not complete until the target screens meet the Phase 14 rubric bar, not merely the LiveView markup and DS-06 tests.
</verification>

<success_criteria>
Plan 14-06 succeeds when Prompt Registry and Release Workbench render through shared components, backed prompt/release behavior and Phase 13 next-step links still pass, no unbacked experiment UI appears, Release Workbench has zero DS-06 raw-palette matches with no baseline row, and the end-of-phase UI-SPEC/rubric review has been performed for all Phase 14 target screens.
</success_criteria>

<output>
Create `.planning/phases/14-least-iterated-screens-polish/14-06-SUMMARY.md` when done.
</output>
