---
phase: 14-least-iterated-screens-polish
plan: "02"
type: execute
wave: 2
depends_on:
  - "14-01"
  - "14-04"
files_modified:
  - lib/scoria_web/live/dataset_live/index.ex
  - test/scoria_web/live/dataset_live/index_test.exs
  - lib/scoria_web/live/dataset_live/promote_component.ex
  - test/scoria_web/live/dataset_live/promote_component_test.exs
  - test/support/ds06_baseline.txt
autonomous: true
requirements:
  - SCREEN-02
user_setup: []
must_haves:
  truths:
    - "Dataset Builder opens promotion state from URL params that carry stable IDs and intent only."
    - "Promotion context is reconstructed server-side from existing review or workflow records."
    - "The reused DatasetLive.PromoteComponent renders inside a shared drawer without raw-palette leakage."
  artifacts:
    - path: "lib/scoria_web/live/dataset_live/index.ex"
      provides: "URL-driven promotion state and Dataset Builder drawer host"
      contains: "handle_params"
    - path: "lib/scoria_web/live/dataset_live/promote_component.ex"
      provides: "Converted promotion form behavior owner"
      contains: "ScoriaWeb.DatasetLive.PromoteComponent"
    - path: "test/support/ds06_baseline.txt"
      provides: "Lowered or removed promote_component raw-palette baseline"
      contains: "dataset_live/promote_component"
  key_links:
    - from: "lib/scoria_web/live/dataset_live/index.ex"
      to: "Scoria.Eval.get_review_candidate"
      via: "promote=review reconstruction"
      pattern: "Eval\\.get_review_candidate"
    - from: "lib/scoria_web/live/dataset_live/index.ex"
      to: "ScoriaWeb.DatasetLive.PromoteComponent"
      via: "drawer live_component"
      pattern: "module=\\{ScoriaWeb\\.DatasetLive\\.PromoteComponent\\}"
    - from: "lib/scoria_web/live/dataset_live/promote_component.ex"
      to: "Scoria.Eval.DatasetPromotion"
      via: "existing promotion attrs builder"
      pattern: "DatasetPromotion\\.build_promotion_attrs"
---

<objective>
Move the promotion surface into Dataset Builder without changing backend promotion rules.

Purpose: This implements D-02 through D-05 so later source screens can navigate into Dataset Builder instead of owning duplicate promotion UX.
Output: URL-driven promotion reconstruction in Dataset Builder, a shared drawer host, and a tokenized PromoteComponent with preserved draft and sealed-baseline behavior.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-least-iterated-screens-polish/14-CONTEXT.md
@.planning/phases/14-least-iterated-screens-polish/14-RESEARCH.md
@.planning/phases/14-least-iterated-screens-polish/14-VALIDATION.md
@.planning/phases/14-least-iterated-screens-polish/14-PATTERNS.md
@.planning/phases/14-least-iterated-screens-polish/14-UI-SPEC.md
@.planning/phases/12-design-system-component-layer/12-03-SUMMARY.md
@.planning/phases/13-orientation-spine-ia/13-08-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add URL-driven promotion reconstruction</name>
  <files>lib/scoria_web/live/dataset_live/index.ex, test/scoria_web/live/dataset_live/index_test.exs</files>
  <read_first>
    - lib/scoria_web/live/dataset_live/index.ex
    - test/scoria_web/live/dataset_live/index_test.exs
    - lib/scoria_web/live/dataset_live/promote_component.ex
    - test/scoria_web/live/dataset_live/promote_component_test.exs
    - lib/scoria/eval.ex
    - lib/scoria/eval/review_queue.ex
    - lib/scoria_web/live/workflow_live/show.ex
    - lib/scoria_web/ui.ex
    - .planning/phases/14-least-iterated-screens-polish/14-CONTEXT.md
    - .planning/phases/14-least-iterated-screens-polish/14-PATTERNS.md
    - .planning/phases/14-least-iterated-screens-polish/14-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: `/scoria/datasets?promote=review&review_candidate_id=...` calls existing review-candidate data and opens a promotion drawer titled for Dataset Builder.
    - Test 2: `/scoria/datasets?promote=workflow&run_id=...&step_id=...&source_variant=original` reconstructs promotion context from existing workflow/run evidence without encoded JSON params.
    - Test 3: invalid or stale promotion params render heading `Promotion source not found` and body `The source ID no longer resolves. Return to the originating run or review item and open Dataset Builder again.`
    - Test 4: same-LiveView close or dataset selection state changes use patch semantics rather than full navigate away from `/datasets`.
  </behavior>
  <action>Per D-03 and D-04, extend `ScoriaWeb.DatasetLive.Index` with `handle_params/3` that allowlists `promote` values `review` and `workflow`. For `promote=review`, load canonical context through `Eval.get_review_candidate(review_candidate_id)` and use the returned `promotion_context`. For `promote=workflow`, reconstruct context from existing run, step, and selected source variant records using the same stable fields as `WorkflowLive.Show`: `workflow_run_id`, `workflow_step_id`, `source_variant`, `provenance`, `checkpoint_output`, `safety`, `promotion_snapshot`, `notes`, and `expected_output`. Do not embed raw promotion snapshots, expected-output JSON, or arbitrary maps in query params. Render the promotion UI in `<.drawer id="dataset-promote-drawer" show={@promotion_context != nil} on_dismiss="close_promote">` with title `Promote traced evidence`. Invalid or stale IDs assign a calm error state using the exact UI-SPEC heading and body.</action>
  <acceptance_criteria>
    - Source assertion: `lib/scoria_web/live/dataset_live/index.ex` contains `handle_params`, `promote`, `review_candidate_id`, `run_id`, `step_id`, and `source_variant`.
    - Source assertion: Dataset Builder index code does not contain `Jason.encode` or `Base.encode` for promotion params.
    - Behavior assertion: index tests prove `promote=review` and `promote=workflow` open `dataset-promote-drawer`.
    - Behavior assertion: index tests prove invalid promotion IDs render exact `Promotion source not found` copy and stay on Dataset Builder.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/dataset_live/index_test.exs</automated>
  </verify>
  <done>Dataset Builder owns URL-driven promotion state and reconstructs promotion context from existing records with stable-ID query params only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert PromoteComponent to shared UI inside Dataset Builder</name>
  <files>lib/scoria_web/live/dataset_live/promote_component.ex, test/scoria_web/live/dataset_live/promote_component_test.exs, test/support/ds06_baseline.txt</files>
  <read_first>
    - lib/scoria_web/live/dataset_live/promote_component.ex
    - test/scoria_web/live/dataset_live/promote_component_test.exs
    - test/support/ds06_baseline.txt
    - test/scoria_web/ds06_drift_guard_test.exs
    - lib/scoria_web/ui.ex
    - assets/css/04-components.css
    - lib/scoria/eval/dataset_promotion.ex
    - lib/scoria/eval.ex
    - .planning/phases/14-least-iterated-screens-polish/14-CONTEXT.md
    - .planning/phases/14-least-iterated-screens-polish/14-PATTERNS.md
    - .planning/phases/14-least-iterated-screens-polish/14-UI-SPEC.md
    - .planning/phases/12-design-system-component-layer/12-03-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: existing draft promotion submit still sends `{:promote_successful, %{dataset_name: ...}}`.
    - Test 2: existing sealed-baseline request still sends `{:baseline_promotion_requested, %{dataset_name: ..., dataset_version: ...}}`.
    - Test 3: invalid expected-output JSON still shows `must be valid JSON`.
    - Test 4: converted component uses shared `panel`, `field`, `button`, `badge`, and `empty_state` surfaces and the DS-06 baseline row for `dataset_live/promote_component.ex` is lowered or removed.
  </behavior>
  <action>Per D-05, keep `ScoriaWeb.DatasetLive.PromoteComponent` as the stateful promotion behavior owner. Preserve `update/2`, `save_promotion`, `request_baseline_promotion`, `Eval.DatasetPromotion.build_promotion_attrs/4`, `Eval.promote_workflow_source/1`, and `Workflows.request_baseline_promotion/1` behavior. Convert its markup from raw palette classes to `ScoriaWeb.UI` components and semantic Scoria classes: use `<.form_section>` for promotion groups, `<.field>` for dataset target, notes, and expected-output JSON controls, `<.button>` for submit actions, `<.badge>` for open/sealed dataset state, `<.empty_state>` for no available datasets, and `<.raw_evidence>` for advanced source evidence if raw JSON is shown. Update `test/support/ds06_baseline.txt` by lowering or removing only the `lib/scoria_web/live/dataset_live/promote_component.ex` row according to the scanner count.</action>
  <acceptance_criteria>
    - Source assertion: `lib/scoria_web/live/dataset_live/promote_component.ex` contains `<.field`, `<.form_section`, and `<.button`.
    - Source assertion: the raw-palette scanner count for `lib/scoria_web/live/dataset_live/promote_component.ex` is less than the previous baseline value 68, and `test/support/ds06_baseline.txt` reflects the new count or removes the row at zero.
    - Behavior assertion: PromoteComponent tests for draft promotion, sealed baseline approval request, and invalid JSON continue to pass.
    - Behavior assertion: DS-06 drift guard passes after the baseline edit.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/live/dataset_live/index_test.exs test/scoria_web/live/dataset_live/promote_component_test.exs test/scoria_web/ds06_drift_guard_test.exs</automated>
  </verify>
  <done>Dataset Builder embeds a shared-component promotion form that keeps existing promotion semantics and reduces the DS-06 baseline for the promotion component.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser-query-params-to-DatasetLive | `promote`, `review_candidate_id`, `run_id`, `step_id`, and `source_variant` are user-controlled. |
| DatasetLive-to-persistence | Promotion context is reconstructed from persisted records before existing promotion APIs are called. |
| PromoteComponent-form-submit | Dataset IDs, notes, and expected output JSON cross from browser form data to mutation functions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Tampering | `DatasetLive.Index.handle_params/3` | mitigate | Allowlist `promote` values, validate required IDs, reconstruct context server-side, and render exact not-found state for stale or invalid IDs. |
| T-14-02-02 | Information Disclosure | promotion URL shape | mitigate | Keep URLs to stable IDs and intent only; tests must reject encoded JSON or raw snapshot params. |
| T-14-02-03 | Tampering | sealed baseline promotion | mitigate | Preserve `Workflows.request_baseline_promotion/1`; do not insert directly into sealed datasets. |
| T-14-02-04 | Information Disclosure | raw evidence display | mitigate | Render raw evidence through HEEx-escaped component slots or `<.raw_evidence>`; do not use `raw/1` for promotion snapshots. |
| T-14-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks exist in this plan; RESEARCH.md package audit says no external packages are required. |
</threat_model>

<verification>
Run `mix test test/scoria_web/live/dataset_live/index_test.exs test/scoria_web/live/dataset_live/promote_component_test.exs test/scoria_web/ds06_drift_guard_test.exs`.
</verification>

<success_criteria>
Plan 14-02 succeeds when Dataset Builder opens a tested promotion drawer from stable-ID URL params, invalid IDs show exact recoverable copy, the existing promotion component keeps draft and sealed-baseline behavior, and DS-06 baseline debt for the promotion component is reduced.
</success_criteria>

<output>
Create `.planning/phases/14-least-iterated-screens-polish/14-02-SUMMARY.md` when done.
</output>
