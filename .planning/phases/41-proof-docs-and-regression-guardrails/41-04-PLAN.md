---
phase: 41-proof-docs-and-regression-guardrails
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/dev/shots.mjs
  - priv/dev/contact_sheet.mjs
  - priv/shots/contact_sheet_index.md
  - priv/dev/e2e/drawer_focus.spec.mjs
autonomous: true
requirements: [PROOF-01]
must_haves:
  truths:
    - "The screenshot matrix captures the two missing surfaces — component-lab states and toast legibility — by adding /_lab/overlays (the real static toast fixture) to the SCREENS set in both shots.mjs and contact_sheet.mjs."
    - "The toast is actually present in every toast-legibility capture: the harness re-navigates before each theme×viewport shot so each capture lands inside the fresh 4000ms phx-mounted auto-hide window."
    - "The committed proof-of-record — priv/shots/contact_sheet_index.md — enumerates the new lab_overlays screen; PNGs/JSON/HTML stay gitignored (evidence, never a gate)."
  artifacts:
    - "priv/dev/shots.mjs (SCREENS += /_lab/overlays with freshMountPerCapture; toast-timing-safe capture branch)"
    - "priv/dev/contact_sheet.mjs (mirrored SCREENS entry)"
    - "priv/shots/contact_sheet_index.md (regenerated manifest listing lab_overlays)"
  key_links:
    - "The real static toast lives at dev/lab/sections/overlays.ex:91-94 (RISK-TOAST-LEGIBILITY) — NOT states.ex (badges only, GA-3's fix rejected)."
  prohibitions:
    - "Do NOT change ui.ex's toast/1 auto-hide behavior — fix the CAPTURE timing, never the product (D-01)."
    - "Do NOT add a pixel diff or any CI screenshot assertion (VISUAL-CI-01 deferred; screenshots stay human evidence)."
    - "Do NOT commit PNG/JSON/HTML outputs — only the markdown manifest is the committed proof-of-record."
    - "Do NOT assert exact toast-timing determinism — re-navigate to reset the clock, do not race it (D-15)."
---

<objective>
Close the two real screenshot-matrix gaps (D-14): component-lab states and toast legibility. The
current shots.mjs matrix covers theme switching, overlays, mobile shell, static copy affordances, and
the approvals flow, but misses the lab's component-state gallery and the static toast. Add /_lab/overlays
(the real RISK-TOAST-LEGIBILITY toast fixture) to the SCREENS set, and beat the toast's 4000ms
auto-hide by re-navigating before each capture (D-15) so every "toast legibility" shot actually
contains a toast.

Purpose: The dated, human-reviewable contact-sheet baseline (PROOF-01, criterion 2) now covers toast
legibility and component-lab states — evidence a maintainer eyeballs, never a gate (D-13).
Output: SCREENS additions in both scripts + a regenerated committed manifest.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-proof-docs-and-regression-guardrails/41-CONTEXT.md
@.planning/phases/41-proof-docs-and-regression-guardrails/41-RESEARCH.md
@.planning/phases/41-proof-docs-and-regression-guardrails/41-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /_lab/overlays to SCREENS with a toast-timing-safe capture (D-14/D-15)</name>
  <files>priv/dev/shots.mjs, priv/dev/contact_sheet.mjs</files>
  <read_first>
    - priv/dev/shots.mjs:90-189 (SCREENS array, THEMES, VIEWPORTS) and :213-313 (captureScreen's single-navigation-then-loop shape + the existing `prompt_release` re-navigate special-case — the precedent for a screen-specific navigation branch)
    - priv/dev/contact_sheet.mjs (its duplicate SCREENS array — mirror the name-only entry)
    - dev/lab/sections/overlays.ex:75-96 (the RISK-TOAST-LEGIBILITY static toast fixture, `<.toast tone={:warn}/>`+`{:fail}` — the real capture target) and dev/dev_router.ex:51-57 (the /scoria/_lab/:section route shape)
    - lib/scoria_web/ui.ex:936-957 (toast/1's `phx-mounted={JS.hide(time: @duration_ms)}`, default 4000ms — the race)
    - 41-RESEARCH.md Pattern 5 + Pitfall 4 + Assumption A3; 41-PATTERNS.md "shots.mjs" + "contact_sheet.mjs" sections
  </read_first>
  <action>
    Add a SCREENS entry to shots.mjs for the lab overlays screen: `{ name: 'lab_overlays', path:
    '/_lab/overlays', tenantScoped: false, overlays: [], freshMountPerCapture: true }` (the path must
    resolve under the dev lab mount — confirm the exact prefix the shots harness uses to reach
    /scoria/_lab/overlays vs /_lab/overlays). Optionally also add /_lab/primitives and/or /_lab/states
    for the broader "component lab states" criterion, but /_lab/overlays is the one that satisfies toast
    legibility (states.ex is badges only). In captureScreen's base-state theme×viewport loop, branch on
    the new `freshMountPerCapture` flag: when set, `await page.goto(url); await waitForReady(page)`
    before each capture so the toast's phx-mounted timer resets to "now" and every one of the shots lands
    inside the 4000ms budget (D-15 — do NOT assert exact timing; re-navigate to reset the clock). Add a
    lightweight sanity check per Pitfall 4: `await page.locator('.scoria-toast').count()` before the
    toast-screen screenshot, logging a warning if zero (catches the silent-empty-shot flake during
    authoring). Then mirror the name-only entry into contact_sheet.mjs's SCREENS array:
    `{ name: 'lab_overlays', tenantScoped: false }`. Note Assumption A3: if a CI runner's goto+waitForReady
    round-trip approaches 4s, note the possible flake in the SUMMARY and consider the "reduce matrix to
    1-2 representative shots" fallback.
  </action>
  <verify>
    <automated>node --check priv/dev/shots.mjs && node --check priv/dev/contact_sheet.mjs && grep -c "lab_overlays" priv/dev/shots.mjs priv/dev/contact_sheet.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Both shots.mjs and contact_sheet.mjs SCREENS arrays contain the lab_overlays entry.
    - shots.mjs re-navigates before each capture for the freshMountPerCapture screen and includes the toast-count sanity warning.
    - Both files pass `node --check` (syntax valid).
    - ui.ex's toast/1 markup is unchanged (only the capture script adapts).
  </acceptance_criteria>
  <done>The screenshot matrix covers /_lab/overlays with a capture strategy that reliably includes the auto-hiding toast.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the dated contact sheet + update the committed manifest (env-gated)</name>
  <files>priv/shots/contact_sheet_index.md</files>
  <read_first>
    - priv/shots/.gitignore and priv/shots/contact_sheet_index.md (the committed manifest; PNGs/JSON/HTML gitignored)
    - The mix task entry points: `mix scoria.ui.shots` and `priv/dev/contact_sheet.mjs`
    - 41-RESEARCH.md "Environment Availability" (Node/Playwright/DB/server needed) + D-04 defer-not-blind fallback; 41-CONTEXT.md D-13
  </read_first>
  <action>
    Run the screenshot harness to produce a fresh dated capture with the corrected SCREENS, then render
    and update the committed markdown manifest priv/shots/contact_sheet_index.md so it enumerates the new
    lab_overlays screen. This needs Node/Playwright + a running dev server (`make dev` in a second shell)
    + DB. FIRST verify the environment is available (`npm --prefix priv/dev ci` + `npx --prefix priv/dev
    playwright install --with-deps chromium`, and a reachable dev server). IF available: run the capture,
    eyeball that the lab_overlays shots actually contain a visible toast (the Task 1 sanity warning must
    not have fired), and commit ONLY the updated manifest markdown (PNGs/JSON/HTML stay gitignored — do
    NOT commit images). IF the environment is NOT available in this executor: do NOT flip blind — record
    in this plan's SUMMARY that the capture must be run manually before milestone close, and hand that
    note to Plan 05's evidence manifest (D-19/D-21 honesty: record what could/couldn't be run). The
    SCREENS wiring from Task 1 is the durable committed change either way.
  </action>
  <verify>
    <automated>grep -c "lab_overlays\|lab-overlays\|/_lab/overlays" priv/shots/contact_sheet_index.md || echo "MANUAL-CAPTURE-PENDING recorded in SUMMARY"</automated>
  </verify>
  <human-check>
    Open the regenerated contact sheet and confirm the /_lab/overlays warn+fail toasts are legible in
    both light and dark themes across the 6 viewport widths. This is advisory human evidence (D-13),
    NOT a blocking gate — record the observation in the SUMMARY.
  </human-check>
  <acceptance_criteria>
    - If the harness ran: contact_sheet_index.md lists the lab_overlays screen and only the manifest markdown is committed (no images).
    - If the harness could not run: the SUMMARY explicitly records MANUAL-CAPTURE-PENDING for Plan 05's evidence manifest to cite honestly.
    - No pixel-diff/CI screenshot assertion was added; toast/1 product code unchanged.
  </acceptance_criteria>
  <done>The committed contact-sheet manifest reflects the new toast/lab coverage — or the inability to run the harness is honestly recorded for the evidence manifest.</done>
</task>

<task type="auto">
  <name>Task 3: D-04 VERIFY-THEN-DEFER — decide the drawer_focus.spec.mjs D-13 collector's disposition</name>
  <files>priv/dev/e2e/drawer_focus.spec.mjs</files>
  <read_first>
    - priv/dev/e2e/drawer_focus.spec.mjs:280-329 (the D-13 live-PubSub focus-survival check — the SOLE report-only collector on a real behavior: `console.warn` + `testInfo.attach`, no `expect()`)
    - 41-CONTEXT.md D-04 (the locked VERIFY-THEN-DEFER rule + its fallback) and 41-RESEARCH.md D-04 + Environment Availability
  </read_first>
  <action>
    Resolve D-04's single genuine flip candidate. FIRST verify the e2e environment is available (same
    check as Task 2: `npm --prefix priv/dev ci` + Playwright chromium + a reachable dev server). IF the
    e2e lane can run: run `mix scoria.ui.e2e` and observe drawer_focus.spec.mjs's D-13 collector — if it
    NEVER warns/flakes across the run, flip it from the report-only `console.warn`+`testInfo.attach`
    pattern to a throwing `expect()` (a free lock, zero product code); if it DOES warn/flake, leave it
    report-only (the only fix would be a new restore hook = out of budget) and hand a gap-register
    Section B row to Plan 05. IF the executor CANNOT run `mix scoria.ui.e2e` (locked D-04 fallback):
    do NOT flip blind — leave drawer_focus.spec.mjs unchanged and record the defer/register disposition
    for Plan 05 Section B. Record the outcome (flipped / deferred-warned / deferred-env-unavailable) in
    the SUMMARY either way.
  </action>
  <verify>
    <automated>node --check priv/dev/e2e/drawer_focus.spec.mjs</automated>
  </verify>
  <acceptance_criteria>
    - drawer_focus.spec.mjs still passes `node --check` (syntax valid) whether flipped or unchanged.
    - The D-13 disposition (flipped to expect() / deferred-because-warned / deferred-because-env-unavailable) is recorded in the SUMMARY for Plan 05 Section B.
    - No blind flip: if the e2e lane could not run, the collector is left report-only (D-04 locked fallback).
  </acceptance_criteria>
  <done>The D-04 single flip candidate is resolved: either locked as a throwing assertion, or deferred/registered with a recorded reason — never flipped blind.</done>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- Edited: `priv/dev/shots.mjs` — `lab_overlays` SCREENS entry with `freshMountPerCapture` flag + toast-timing-safe capture branch + toast-count sanity warning.
- Edited: `priv/dev/contact_sheet.mjs` — mirrored `lab_overlays` SCREENS entry.
- Updated: `priv/shots/contact_sheet_index.md` — committed manifest enumerating `lab_overlays` (or a recorded MANUAL-CAPTURE-PENDING note if the harness could not run).
- Conditionally edited: `priv/dev/e2e/drawer_focus.spec.mjs` — D-04 D-13 collector flipped to a throwing `expect()` IF it never warns and the e2e lane ran; otherwise left report-only with a recorded defer reason.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Capture harness ↔ evidence integrity | A screenshot script can silently capture the wrong (empty) state, producing misleading "proof". |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-07 | Repudiation / evidence integrity | toast-legibility captures silently missing the auto-hidden toast | medium | mitigate | Re-navigate per capture (fresh 4000ms window) + a `.scoria-toast` count sanity warning during authoring, so "toast legibility" evidence actually contains a toast (Pitfall 4). |
| T-41-08 | Tampering (Hex footprint) | accidentally committing screenshot binaries or shipping dev-only capture code | low | accept | `priv/shots`/`priv/dev` are excluded from `package.files` and PNG/JSON/HTML are gitignored; only the markdown manifest is committed — zero Hex footprint by existing design. |

No packages are installed beyond the already-pinned `priv/dev` devDeps (playwright 1.60.0,
@axe-core/playwright 4.12.1) — no new supply-chain surface.
</threat_model>

<verification>
- `node --check` passes for both scripts; `grep lab_overlays` finds the entry in both.
- If runnable: `mix scoria.ui.shots` produces captures whose toast-count sanity check did not warn, and the manifest lists lab_overlays.
- If not runnable: MANUAL-CAPTURE-PENDING is recorded in the SUMMARY for Plan 05.
- No images committed; toast/1 unchanged; no pixel gate added.
</verification>

<success_criteria>
The contact-sheet evidence covers toast legibility and component-lab states with a capture strategy that
reliably beats the 4s auto-hide — human-reviewable evidence, never a gate (PROOF-01 criterion 2).
</success_criteria>

<output>
Create `.planning/phases/41-proof-docs-and-regression-guardrails/41-04-SUMMARY.md` when done.
Record: whether the harness ran (and the toast-sanity result) or MANUAL-CAPTURE-PENDING, plus any A3
flake note — Plan 05's evidence manifest cites the contact_sheet_index.md and this run status.
</output>
