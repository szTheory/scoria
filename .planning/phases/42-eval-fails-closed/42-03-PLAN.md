---
phase: 42-eval-fails-closed
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/eval/scorers/exact_match.ex
  - test/scoria/eval/scorers/exact_match_test.exs
autonomous: true
requirements: [EVAL-02]
must_haves:
  truths:
    - "ExactMatch.score/3 is a pure, key-free, LLM-free deterministic scorer returning %{status, score, details} on a real comparison or {:not_scored, reason} when it cannot run."
    - "A normalized equal comparison yields status \"passed\"/score 1.0; a clean mismatch yields status \"failed\"/score 0.0 (binary — no warning band)."
    - "A missing/nil expected field, incomparable types, or absent actual yields {:not_scored, reason} — never a fake pass."
  artifacts:
    - lib/scoria/eval/scorers/exact_match.ex
  key_links:
    - "ExactMatch is the scorer the offline runner (plan 04) dispatches to for scorer_kind \"exact_match\"; it writes the existing ai_scores sink unchanged (no migration)."
  prohibitions:
    - "Must NOT route through Grounding.status/1's 0.4/0.75 warning bands — equality is binary."
    - "Must NOT implement fuzzy/semantic/Levenshtein/embedding matching (that is SEED-008, the fake-sophistication EVAL-02 exists to kill)."
    - "ran-and-mismatched (\"failed\") must never be conflated with couldn't-run ({:not_scored})."
---

<objective>
Build `Scoria.Eval.Scorers.ExactMatch` — the first real deterministic scorer (D-03) — mirroring the `Scoria.Knowledge.Grounding` pure-scorer style with a binary equality verdict and an explicit not-scored outcome.

Purpose: EVAL-02 requires at least one real scorer that compares actual vs expected. Exact match is the honest floor: it proves a real negative (mismatch → failed) and refuses to run when it cannot (missing field/incomparable → not_scored) rather than fake-passing.
Output: `Scoria.Eval.Scorers.ExactMatch.score/3`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-eval-fails-closed/42-CONTEXT.md
@.planning/phases/42-eval-fails-closed/42-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ExactMatch.score/3 pure deterministic scorer</name>
  <files>lib/scoria/eval/scorers/exact_match.ex, test/scoria/eval/scorers/exact_match_test.exs</files>
  <read_first>
    - lib/scoria/knowledge/grounding.ex:4-10 (binary %{status, score, details} shape to clone) and :78-80 (status/1 warning bands — deliberately NOT used here)
    - lib/scoria/eval/runner.ex:116-123 (scorer_kind coercion + to_string) and :161-163 (fetch/2 atom-or-string key idiom)
  </read_first>
  <behavior>
    - score("Scoria", %{"answer" => "Scoria"}, []) → %{status: "passed", score: 1.0, details: ...}
    - score("scoria", %{"answer" => "Scoria"}, []) → %{status: "failed", score: 0.0, ...} (case-sensitive default)
    - score("scoria", %{"answer" => "Scoria"}, [case_insensitive: true]) → passed
    - score("  Scoria  is\tgreat ", %{"answer" => "Scoria is great"}, []) → passed (NFC → trim → collapse internal whitespace)
    - score(nil, %{"answer" => "Scoria"}, []) → {:not_scored, reason} (no actual output)
    - score("x", %{}, []) → {:not_scored, reason} (missing/nil expected field)
    - score(%{"a" => 1}, %{"a" => 1}, [match: "map"]) → passed (canonical whole-map compare)
    - score(123, %{"answer" => "123"}, []) → {:not_scored, reason} (incomparable types, not a fake pass)
  </behavior>
  <action>
    Create `Scoria.Eval.Scorers.ExactMatch` with `score/3` (arg order mirroring Grounding: actual, expected/labels, opts). Fields for the persisted score: `scorer_kind: "exact_match"`, `scorer_version: "exact-match@1"`. Default extracts `expected_output["answer"]` for comparison; per-scorer `field` opt overrides the expected key and `match: "map"` opt does a canonical whole-map compare. Coerce string↔atom keys before comparing (reuse the normalize idiom style from online_scoring.ex normalize_map/1 or runner.ex to_string). Normalization for string compares: Unicode NFC → trim → collapse internal whitespace runs to a single space; case-SENSITIVE by default with a `case_insensitive` opt. Binary verdict: normalized-equal → %{status: "passed", score: 1.0, details: %{...}}, clean mismatch → %{status: "failed", score: 0.0, details: %{...}}. Return `{:not_scored, reason}` (reason atom) for: nil/absent actual, missing/nil expected field, or incomparable types. Do NOT call Grounding.status/1 (its warning band is meaningless for equality). Do NOT implement any fuzzy/semantic distance — that is SEED-008. `details` map shape (e.g. a normalized-values diff) is Claude's discretion.
  </action>
  <verify>
    <automated>mix test test/scoria/eval/scorers/exact_match_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Every case in <behavior> is asserted and passes.
    - The mismatch case returns "failed"/0.0 and the missing-field case returns {:not_scored, _} — asserted as distinct outcomes (the sharp line).
    - No dependency on Grounding.status/1 and no fuzzy matching.
  </acceptance_criteria>
  <done>ExactMatch.score/3 exists as a pure binary scorer with an explicit not_scored outcome; test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| actual/expected → score | Untrusted/ill-typed inputs cross into a pass/fail measurement; a couldn't-run masquerading as passed is fake-green. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-09 | Tampering (fake-green) | ExactMatch couldn't-run path | high | mitigate | Missing field / incomparable types / absent actual return {:not_scored, reason}, never passed; test pins the ran-mismatch vs couldn't-run distinction. |
| T-42-10 | Repudiation (fake sophistication) | scorer scope | low | accept | Scope is exact-match only; fuzzy/semantic scoring is deferred to SEED-008 (documented), so the scorer cannot over-claim. |
</threat_model>

<verification>
- `mix test test/scoria/eval/scorers/exact_match_test.exs --warnings-as-errors` passes; pure, no DB, no LLM.
</verification>

<success_criteria>
A real deterministic scorer compares actual vs expected with a binary verdict and refuses to fake-pass when it cannot run.
</success_criteria>

<artifacts_produced>
- `Scoria.Eval.Scorers.ExactMatch.score/3` (scorer_kind "exact_match", scorer_version "exact-match@1")
</artifacts_produced>

<output>
Create `.planning/phases/42-eval-fails-closed/42-03-SUMMARY.md` when done.
</output>
