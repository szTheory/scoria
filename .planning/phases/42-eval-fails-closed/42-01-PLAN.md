---
phase: 42-eval-fails-closed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/eval/verdict.ex
  - lib/scoria/eval/score.ex
  - lib/scoria_web/ui.ex
  - lib/scoria_web/copy.ex
  - test/scoria/eval/verdict_test.exs
  - test/scoria/eval/score_changeset_test.exs
  - test/scoria_web/eval_vocabulary_test.exs
autonomous: true
requirements: [EVAL-03]
must_haves:
  truths:
    - "Verdict.compute/2 returns :inconclusive (never :passed) for an empty score list — the online_scoring Enum.all?([]) fail-OPEN bug is fixed once, centrally."
    - "Verdict.compute/2 returns :inconclusive when any item is not_scored and no not_scored_tolerance is set (strict coverage default)."
    - "Verdict.compute/2 computes pass_rate/mean_score/max_latency over the scored-only subset with no crash when some scores have score: nil."
    - "Score.changeset accepts status: \"not_scored\" with score: nil, and still requires :score for every other status."
    - "The dashboard renders not_scored and inconclusive as amber (:warn), not gray (:neutral) and not red (:fail)."
  artifacts:
    - lib/scoria/eval/verdict.ex
    - "Copy curated labels: \"Not scored\", \"Inconclusive\""
  key_links:
    - "Verdict.blocks_release?/1 is the single passing-set authority consumed by ReleaseGate (plan 07) and every runner (plans 04-06)."
    - "item_scored?/1 is the single nil/not_scored filter reused by all mean/sum math."
  prohibitions:
    - "compute/2 must never return :passed for an empty or all-not_scored score set."
    - "No new DB enum / validate_inclusion for threshold_verdict — \"inconclusive\" is additive on the free-form string column."
    - "0.0 must never be substituted for a missing measurement — not_scored means score: nil."
---

<objective>
Build `Scoria.Eval.Verdict` — the single fail-closed decision point that unifies the three duplicated `threshold_verdict` functions (D-01) — and land the tri-state honest vocabulary at the schema and dashboard edges (D-02). This is the spine every other plan in the phase consumes.

Purpose: One shared, nil-safe, coverage-guarded verdict function makes "fail closed" a single testable property instead of three drifting copies (runner/judge fail-closed on empty; online fail-OPEN on empty). It also fixes the live `Enum.all?([]) == "passed"` bug.
Output: `Scoria.Eval.Verdict` (compute/2, blocks_release?/1, item_scored?/1), `Score.changeset` conditional-required `:score`, and amber dashboard rendering + curated labels for the two new states.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-eval-fails-closed/42-CONTEXT.md
@.planning/phases/42-eval-fails-closed/42-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Scoria.Eval.Verdict (the fail-closed spine)</name>
  <files>lib/scoria/eval/verdict.ex, test/scoria/eval/verdict_test.exs</files>
  <read_first>
    - lib/scoria/eval/runner.ex:125-152 (the fail-CLOSED "good copy" of threshold_verdict — preserve its pass_rate/mean_score/max_latency policy math verbatim, just make it nil-safe + coverage-guarded)
    - lib/scoria/eval/online_scoring.ex:354-356 (the fail-OPEN Enum.all?([]) bug this module replaces) and 351-352 (average_score/1 nil-guard idiom to reuse)
    - lib/scoria/semantic_cache/lookup.ex:83-92 (Map.fetch!/raise-on-missing fail-closed posture to mirror)
    - lib/scoria/eval/eval_run.ex:28,80 (threshold_verdict is free-form :string with NO validate_inclusion; status inclusion list stays ["pending","running","completed","failed"])
  </read_first>
  <behavior>
    - compute([], _policy) → :inconclusive (empty score list; fixes the Enum.all?([]) fail-open)
    - compute(all_not_scored, _policy) → :inconclusive (zero real measurements)
    - compute(mix_with_one_not_scored, policy_without_tolerance) → :inconclusive (strict coverage default)
    - compute(mix_with_one_not_scored, %{"not_scored_tolerance" => value}) → verdict computed over scored subset (escape hatch)
    - compute(all_scored_clearing_policy, policy) → :passed
    - compute(all_scored_below_threshold, policy) → :failed
    - compute(scores_with_score_nil_present, policy) → does not raise (mean/sum filter to scored subset first)
    - blocks_release?("passed") → false; blocks_release?/1 for every other value → true
    - item_scored?(score) → false when status == "not_scored" OR score is nil; true otherwise
  </behavior>
  <action>
    Create module `Scoria.Eval.Verdict` per D-01/D-02. Public API: `compute(scores, threshold_policy) :: :passed | :failed | :inconclusive`, `blocks_release?(verdict) :: boolean` (only the canonical passing string "passed" → false; accept the persisted string form since ReleaseGate reads it off the DB), `item_scored?(score) :: boolean`. Define a module constant for the ONLY passing verdict string ("passed") and expose it so callers never enumerate a block-list. compute/2 must: filter to the scored-only subset via `item_scored?/1` BEFORE any Enum.sum/Enum.count; return :inconclusive when the raw score list is empty, when the scored subset is empty (no real scorer ran), or when any item is not_scored and `threshold_policy["not_scored_tolerance"]` (string OR atom key) is absent; otherwise re-express today's runner.ex:135-141 policy check (pass_rate >= pass_rate_gte AND mean_score >= mean_score_gte AND avg_latency <= max_latency_ms) over the scored subset → :passed / :failed. Preserve the latency extraction shape from runner.ex:144-152 (integer or stringified integer in metadata "latency_ms", default 0). Read policy keys tolerantly (atom or string) as the existing `fetch/2` helpers do. Persisted string form is `Atom.to_string(verdict)`; callers do the to_string when writing threshold_verdict. Do NOT route through any 0.0-default — a missing measurement is score: nil, filtered out, never counted as a 0.
  </action>
  <verify>
    <automated>mix test test/scoria/eval/verdict_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - verdict_test.exs covers every case in <behavior> and all pass.
    - An empty list and an all-not_scored list both yield :inconclusive (explicit assertions, the anti-fake-green property).
    - A scores list containing a score: nil item does not raise.
    - blocks_release?("passed") is false and blocks_release?("inconclusive")/blocks_release?("failed") are true.
  </acceptance_criteria>
  <done>Scoria.Eval.Verdict exists with compute/2, blocks_release?/1, item_scored?/1; verdict_test.exs is green; no live-LLM, no DB (pure module test).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Score.changeset conditional-required :score (not_scored ⇒ score nil)</name>
  <files>lib/scoria/eval/score.ex, test/scoria/eval/score_changeset_test.exs</files>
  <read_first>
    - lib/scoria/eval/score.ex:46 (validate_required list including :score) and :51-63 (normalize_attrs conditional-shaping idiom to mirror)
    - lib/scoria/eval.ex:653-675 (aggregate_score_attrs — passed_items/failed_items counts; a not_scored item counts toward neither, so the not_scored count is derivable as total_items - passed_items - failed_items; do NOT add a column per D-02)
  </read_first>
  <behavior>
    - changeset with status: "not_scored" and no :score → valid (score persists as nil)
    - changeset with status: "passed" and no :score → invalid (:score required)
    - changeset with status: "failed" and score: 0.0 → valid
  </behavior>
  <action>
    Split the `validate_required/2` at score.ex:46 so `:score` is required ONLY when `status != "not_scored"`. Keep `:status`, `:scorer_kind`, `:eval_run_id`, `:dataset_item_id` unconditionally required. Apply the conditional `validate_required([:score])` after casting, reading the cast status (via `get_field/2`). `ai_scores.score` already accepts nil (no migration). Do not alter normalize_attrs behavior for explanation/details/metadata. Note in a code comment that not_scored count is derived (total - passed - failed), not stored, per D-02.
  </action>
  <verify>
    <automated>mix test test/scoria/eval/score_changeset_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - not_scored + nil score is accepted; passed/failed without a score is rejected on :score.
    - Existing score persistence for passed/failed rows is unchanged (regression assertion).
  </acceptance_criteria>
  <done>Score.changeset requires :score only when status != "not_scored"; score_changeset_test.exs green.</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard renders not_scored / inconclusive as amber</name>
  <files>lib/scoria_web/ui.ex, lib/scoria_web/copy.ex, test/scoria_web/eval_vocabulary_test.exs</files>
  <read_first>
    - lib/scoria_web/ui.ex:24-50 (tone/1 — the :warn bucket at line 35 is the amber bucket; the _ → :neutral fallback at 45-46 is gray "benign")
    - lib/scoria_web/copy.ex:42-56 (@status_labels curated list) and :87-98 (status_label/1 with humanize fallback)
  </read_first>
  <action>
    Per D-02 dashboard revision: add `not_scored` and `inconclusive` to the `:warn` (amber) bucket in `ScoriaWeb.UI.tone/1` at ui.ex:35 so they render amber (not the :neutral gray fallback, which reads as benign, and not :fail red, which reads as a false regression). Add curated entries to `ScoriaWeb.Copy` `@status_labels` (copy.ex:42): `{"not_scored", "Not scored"}` and `{"inconclusive", "Inconclusive"}`. The humanize fallback already prevents a crash; these are the honesty-accurate operator labels. Do NOT change the status inclusion list or any other tone bucket.
  </action>
  <verify>
    <automated>mix test test/scoria_web/eval_vocabulary_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - tone("not_scored") == :warn and tone("inconclusive") == :warn.
    - Copy.status_label("not_scored") == "Not scored" and Copy.status_label("inconclusive") == "Inconclusive".
    - tone("passed") is still :pass and tone("failed") is still :fail (no bucket regression).
  </acceptance_criteria>
  <done>Both new states map to :warn and to curated labels; eval_vocabulary_test.exs green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval scores → run verdict | Untrusted/incomplete score sets cross into the pass/fail decision that gates releases. |
| eval state → dashboard | State strings cross into operator-facing color/label rendering; a benign color on an unmeasured run is a trust failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01 | Elevation of Privilege (fake-green) | Verdict.compute/2 empty/zero-coverage path | high | mitigate | compute/2 returns :inconclusive for empty and all-not_scored inputs; verdict_test.exs pins both as explicit anti-fake-green properties (fixes online_scoring Enum.all?([]) fail-open). |
| T-42-02 | Tampering (gaming surface) | Verdict.compute/2 coverage rule | high | mitigate | Strict coverage default: any not_scored item ⇒ :inconclusive unless not_scored_tolerance is explicitly set; test asserts a 1-passed/N-not_scored set is :inconclusive. |
| T-42-06 | Information Disclosure (misleading UI) | UI.tone/1 fallback | medium | mitigate | not_scored/inconclusive routed to :warn amber so an unmeasured run never renders as benign gray or false-regression red. |
</threat_model>

<verification>
- `mix test test/scoria/eval/verdict_test.exs test/scoria/eval/score_changeset_test.exs test/scoria_web/eval_vocabulary_test.exs --warnings-as-errors` passes.
- No live-LLM, no API keys used by any test in this plan.
</verification>

<success_criteria>
Verdict is the single fail-closed authority (empty ⇒ inconclusive), Score persists not_scored honestly (nil score), and the dashboard renders the two new states as amber with curated labels.
</success_criteria>

<artifacts_produced>
- `Scoria.Eval.Verdict` — `compute/2`, `blocks_release?/1`, `item_scored?/1`, passing-verdict constant
- `Score.status "not_scored"` accepted with `score: nil`
- `EvalRun.threshold_verdict "inconclusive"` (additive on free-form column; produced by callers via Verdict)
- Dashboard: `not_scored`/`inconclusive` → `:warn` amber; Copy labels "Not scored" / "Inconclusive"
</artifacts_produced>

<output>
Create `.planning/phases/42-eval-fails-closed/42-01-SUMMARY.md` when done.
</output>
