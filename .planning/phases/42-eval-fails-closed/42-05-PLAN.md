---
phase: 42-eval-fails-closed
plan: 05
type: execute
wave: 2
depends_on: ["42-01", "42-02"]
files_modified:
  - lib/scoria/eval/judge_runner.ex
  - test/scoria/eval/judge_runner_test.exs
autonomous: true
requirements: [EVAL-01, EVAL-03]
must_haves:
  truths:
    - "The judge prompt's \"Actual\" comes from the resolved subject output (frozen captured_output), not from expected_output[\"answer\"] self-graded against itself."
    - "An item with empty/absent capture is scored :not_scored (its judge call is skipped), never fake-passed."
    - "The judge run verdict is produced by Verdict.compute/2, not a local threshold_verdict copy."
  artifacts:
    - "judge_runner.ex using SubjectOutput.resolve + Verdict (build_subject_output self-grade deleted)"
  key_links:
    - "judge_runner.ex consumes Scoria.Eval.SubjectOutput (plan 02) and Scoria.Eval.Verdict (plan 01); the injected orchestrator_module/req_llm_module seam is preserved for zero-live-LLM tests."
  prohibitions:
    - "build_subject_output must NOT return expected_output[\"answer\"] as the Actual (the self-grade)."
    - "No local threshold_verdict duplicate may remain in judge_runner.ex."
    - "Tests must drive the judge via the injected orchestrator stub — never a live model call."
---

<objective>
Kill the judge runner self-grade (D-04) and route its verdict through the shared `Verdict` (D-01). Today `build_subject_output/1` injects `expected_output["answer"]` as the "Actual", so the judge grades the sealed answer against itself — a guaranteed fake-green.

Purpose: The judge must evaluate a real subject output. After this plan the "Actual" is the resolved frozen capture; when capture is empty the item is `:not_scored` and its judge call is skipped; the run verdict comes from `Verdict.compute/2`. The injected orchestrator seam keeps every test key-free.
Output: `judge_runner.ex` wired to `SubjectOutput` + `Verdict`, self-grade removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-eval-fails-closed/42-CONTEXT.md
@.planning/phases/42-eval-fails-closed/42-PATTERNS.md
@lib/scoria/eval/verdict.ex
@lib/scoria/eval/subject_output.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace self-grade with SubjectOutput.resolve + Verdict</name>
  <files>lib/scoria/eval/judge_runner.ex</files>
  <read_first>
    - lib/scoria/eval/judge_runner.ex:138-140 (build_subject_output self-grade to delete), :106-107 (call site + build_judge_prompt), :142-150 (Actual: injection), :165-182 (local threshold_verdict to DELETE), :62-69 (injected orchestrator_module/req_llm_module seam to PRESERVE), :105-136 (build_score_attrs reduce loop)
    - lib/scoria/eval/subject_output.ex (resolve/2 — plan 02), lib/scoria/eval/verdict.ex (compute/2 — plan 01)
  </read_first>
  <action>
    In `build_score_attrs/7`, replace `build_subject_output(dataset_item)` (judge_runner.ex:106) with `SubjectOutput.resolve(dataset_item, :live_judge)` (or :offline for the frozen capture per the resolver's contract). On `{:ok, actual}`, feed `actual` into `build_judge_prompt/2` as the "Actual" and call the orchestrator via the existing seam as today. On `{:not_scored, reason}`, SKIP the orchestrator call for that item and emit a not_scored score attrs (status "not_scored", score nil, reason in details) — never fabricate an actual and never call the judge on an absent subject. Delete `build_subject_output/1` (138-140). Delete the local `threshold_verdict/2` (165-182) and its private helpers now owned by Verdict; change both `run_live/1` (judge_runner.ex:35) and `run_existing/2` (:55) to persist `Verdict.compute(...) |> Atom.to_string()`. Keep the `List.wrap(base_score_attrs) ++ score_attrs` concat (84) intact — plan 06 removes the fabricated base rows upstream. Preserve the orchestrator_module/req_llm_module seam untouched.
  </action>
  <verify>
    <automated>mix test test/scoria/eval/judge_runner_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - build_subject_output is gone; the Actual is sourced from SubjectOutput.resolve.
    - An empty-capture item yields a not_scored score and no orchestrator call for that item.
    - The run verdict is Verdict.compute-derived; no local threshold_verdict remains.
  </acceptance_criteria>
  <done>Judge runner grades a real (or not_scored) subject output and verdicts via Verdict; self-grade deleted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Judge runner tests prove no self-grade, no live LLM</name>
  <files>test/scoria/eval/judge_runner_test.exs</files>
  <read_first>
    - test/scoria/eval/judge_runner_test.exs:1-45 (existing ReqLLMStub/orchestrator stub + run_live test, single-orchestrator seam)
    - test/support/eval_case.ex
  </read_first>
  <behavior>
    - With a captured_output present, the stub orchestrator receives that captured value as the Actual in the prompt (NOT expected_output["answer"]).
    - With an empty/absent capture, the item is scored not_scored and the stub orchestrator is NOT called for it.
    - The run verdict matches Verdict.compute over the resulting scores (e.g. all-not_scored → inconclusive).
  </behavior>
  <action>
    Extend the judge stub test so the seeded contract carries a real captured_output and assert (via the `{:req_llm_called, model_spec, prompt}` message the stub sends) that the prompt's Actual is the captured value, not the sealed expected answer. Add a case with no capture asserting the item is not_scored and the stub was not invoked for it (assert_receive count / refute_receive). Assert the completed run's threshold_verdict equals the Verdict-derived value for the produced score set. All judge interaction goes through the injected stub — no live model call, no API key.
  </action>
  <verify>
    <automated>mix test test/scoria/eval/judge_runner_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts the Actual sent to the judge is the captured output, not the expected answer (self-grade regression guard).
    - A no-capture test asserts not_scored + no judge call for that item.
    - Suite runs with no API key.
  </acceptance_criteria>
  <done>judge_runner_test.exs proves the self-grade is gone and the path stays zero-live-LLM.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dataset item → judge prompt | The "Actual" fed to the judge determines the verdict; self-grading the expectation guarantees a pass. |
| eval → external judge model | The judge path must be provable without a live/keyed call (injected seam). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-13 | Elevation (self-grade fake-green) | judge_runner.build_subject_output | high | mitigate | Actual sourced from SubjectOutput.resolve; test asserts the captured value (not expected_output["answer"]) reaches the judge prompt. |
| T-42-14 | Tampering (judge on absent subject) | build_score_attrs not_scored path | high | mitigate | Empty capture → item scored not_scored and the judge call is skipped; test refutes the orchestrator invocation for that item. |
| T-42-15 | Denial (live-call test dependency) | orchestrator seam | low | accept | Injected orchestrator_module/req_llm_module preserved; all tests stub it — no API key ever required. |
</threat_model>

<verification>
- `mix test test/scoria/eval/judge_runner_test.exs --warnings-as-errors` passes via the injected stub only.
</verification>

<success_criteria>
The judge evaluates a real subject output (or scores not_scored), never grades the expectation against itself, and verdicts through Verdict — all without a live LLM.
</success_criteria>

<artifacts_produced>
- judge_runner.ex: `SubjectOutput.resolve` for the Actual, not_scored-skips-judge path, `Verdict.compute` wiring; `build_subject_output/1` and local `threshold_verdict/2` removed
</artifacts_produced>

<output>
Create `.planning/phases/42-eval-fails-closed/42-05-SUMMARY.md` when done.
</output>
