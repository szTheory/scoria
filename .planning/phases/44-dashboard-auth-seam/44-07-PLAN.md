---
phase: 44-dashboard-auth-seam
plan: 07
type: execute
wave: 3
depends_on:
  - 44-02
  - 44-03
  - 44-04
  - 44-05
  - 44-06
files_modified:
  - docs/adoption_lanes.md
  - docs/operator_verification.md
  - docs/MAINTAINERS.md
  - test/scoria/adoption_surface_test.exs
  - test/scoria_web/dashboard_auth_source_scan_test.exs
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "AUTH-01: adopter docs show the Phoenix-native host on_mount seam while bare dashboard mount remains supported."
    - "AUTH-02: docs teach host-authenticated scope_resolver as the canonical tenant authority and explicitly avoid in-lib RBAC."
    - "AUTH-03: source guard blocks reintroduction of dashboard tenant authority from public tenant params or hardcoded default tenant fallbacks."
    - "AUTH-03: phase gate runs focused router, scope, source scan, and dashboard LiveView auth tests."
  artifacts:
    - path: "docs/adoption_lanes.md"
      provides: "adopter-facing dashboard auth seam guidance"
    - path: "docs/operator_verification.md"
      provides: "operator verification proof for host-asserted dashboard scope"
    - path: "docs/MAINTAINERS.md"
      provides: "maintainer dashboard scope proof update where existing docs claim global dashboard pages"
    - path: "test/scoria/adoption_surface_test.exs"
      provides: "documentation contract assertions"
    - path: "test/scoria_web/dashboard_auth_source_scan_test.exs"
      provides: "source-scan regression guard for dashboard tenant authority"
  key_links:
    - from: "docs/adoption_lanes.md"
      to: "lib/scoria_web/router.ex"
      via: "documents `on_mount:` and `scope_resolver:` options"
      pattern: "scope_resolver"
    - from: "test/scoria_web/dashboard_auth_source_scan_test.exs"
      to: "lib/scoria_web/live/**/*.ex"
      via: "source scan for forbidden tenant authority patterns"
      pattern: "dashboard tenant authority"
  prohibitions:
    - statement: "Docs do not present query params as dashboard tenant authority."
      status: "resolved"
      verification: "Doc contract asserts host-owned scope wording and source guard passes."
    - statement: "Docs do not add Scoria-owned roles, tenant membership, or authorization policy values."
      status: "resolved"
      verification: "Doc contract asserts authz remains delegated to the host."
---

<objective>
Lock Phase 44 with docs, source-scan regression coverage, and a focused phase proof for AUTH-01 through AUTH-03.

Purpose: prevent the old spoof path from returning through future page edits and teach adopters the host-owned auth/scope seam without implying Scoria owns authorization policy.
Output: docs, doc contracts, source guard, and final verification commands.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/44-dashboard-auth-seam/44-CONTEXT.md
@.planning/phases/44-dashboard-auth-seam/44-RESEARCH.md
@.planning/phases/44-dashboard-auth-seam/44-PATTERNS.md
@.planning/phases/44-dashboard-auth-seam/44-UI-SPEC.md
@docs/adoption_lanes.md
@docs/operator_verification.md
@docs/MAINTAINERS.md
@test/scoria/adoption_surface_test.exs
</context>

## Artifacts this phase produces

- Test module: `ScoriaWeb.DashboardAuthSourceScanTest`.
- Documentation contract assertions in `Scoria.AdoptionSurfaceTest`.
- Documentation sections describing `on_mount:` plus `scope_resolver:` as the dashboard seam.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dashboard tenant-authority source scan</name>
  <files>test/scoria_web/dashboard_auth_source_scan_test.exs</files>
  <read_first>
    <file>test/scoria_web/dashboard_auth_source_scan_test.exs</file>
    <file>test/scoria/workflows/approval_write_invariant_guard_test.exs</file>
    <file>lib/scoria_web/live/orchestrator_live.ex</file>
    <file>lib/scoria_web/live/approvals_live/index.ex</file>
    <file>lib/scoria_web/live/connectors_live/index.ex</file>
    <file>lib/scoria_web/live/incidents_live/index.ex</file>
    <file>lib/scoria_web/live/incidents_live/show.ex</file>
    <file>lib/scoria_web/live/workflow_live/index.ex</file>
    <file>lib/scoria_web/live/workflow_live/show.ex</file>
    <file>lib/scoria_web/live/review_queue_live.ex</file>
    <file>lib/scoria_web/live/eval_spec_live/index.ex</file>
    <file>lib/scoria_web/live/dataset_live/index.ex</file>
    <file>lib/scoria_web/live/prompt_live/index.ex</file>
    <file>lib/scoria_web/live/prompt_live/release_workbench_live.ex</file>
  </read_first>
  <behavior>
    - Source scan fails when dashboard LiveViews derive tenant authority from the old query-param expression.
    - Source scan fails when dashboard LiveViews combine session tenant with a hardcoded default tenant.
    - Source scan fails when a hardcoded default fallback appears near dashboard tenant assignment.
    - Comments are stripped before matching so explanatory prose does not count as code.
  </behavior>
  <action>Create `ScoriaWeb.DashboardAuthSourceScanTest` using the approval write-invariant guard's file scan and comment-stripping style. Scan `lib/scoria_web/live/**/*.ex` and scope matches to code lines. Use exact D-04 forbidden pattern literals in the test data, but keep failure messages framed as dashboard tenant authority violations. Add narrow allow-list support only for non-authority prose or tests if the implementation truly needs it; production LiveView authority paths should have no allow-list entries.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria_web/dashboard_auth_source_scan_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the scan includes `lib/scoria_web/live/**/*.ex`.
    - Negative source assertion: `grep -v '^#' lib/scoria_web/live/**/*.ex | grep -c 'params["tenant"]'` returns zero or only source-scan-approved non-authority matches.
    - Negative source assertion: `grep -v '^#' lib/scoria_web/live/**/*.ex | grep -c 'session["tenant_id"] || "default"'` returns zero.
    - Negative source assertion: the test detects hardcoded default tenant fallback near tenant assignment without blocking unrelated UI copy.
    - CLI assertion: the source-scan test passes after Plans 02-06.
  </acceptance_criteria>
  <done>Static guard prevents the spoof/default authority path from returning to dashboard LiveViews.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update adopter and operator verification docs</name>
  <files>docs/adoption_lanes.md, docs/operator_verification.md, docs/MAINTAINERS.md, test/scoria/adoption_surface_test.exs</files>
  <read_first>
    <file>docs/adoption_lanes.md</file>
    <file>docs/operator_verification.md</file>
    <file>docs/MAINTAINERS.md</file>
    <file>test/scoria/adoption_surface_test.exs</file>
    <file>lib/scoria_web/router.ex</file>
    <file>lib/scoria_web/dashboard_scope.ex</file>
    <file>.planning/phases/44-dashboard-auth-seam/44-CONTEXT.md</file>
    <file>.planning/phases/44-dashboard-auth-seam/44-UI-SPEC.md</file>
  </read_first>
  <behavior>
    - Docs say the host authenticates the operator and asserts dashboard tenant scope.
    - Docs show `scoria_dashboard "/scoria", on_mount: ..., scope_resolver: ...` as the canonical authenticated mount and still note that bare mount compiles with the session-backed default resolver.
    - Docs state query params do not choose dashboard tenants.
    - Docs state authorization policy and membership checks belong to the host, not Scoria.
    - Browser-facing failure copy remains generic.
  </behavior>
  <action>Update docs per D-05. In `docs/adoption_lanes.md`, replace session-only dashboard identity guidance with host auth hook plus `scope_resolver:` as the recommended seam, while documenting session keys as the default resolver compatibility input. In `docs/operator_verification.md`, add proof steps for mounting with host scope and verifying a tenant query hint does not change authority. In `docs/MAINTAINERS.md`, update any dashboard proof note that still describes Review Queue, Eval Workbench, Prompt Registry, or Workflow Index as globally unscoped. Add assertions to `test/scoria/adoption_surface_test.exs` for the new canonical phrases. Do not add tenant switching, roles, membership tables, or policy-value guidance.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/adoption_surface_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: docs include `scope_resolver:` and `on_mount:` dashboard examples.
    - Source assertion: docs contain the sentence `The host app authenticates the operator and asserts dashboard tenant scope.`
    - Source assertion: docs say query params do not choose tenants for the dashboard.
    - Source assertion: docs state authorization remains delegated to the host and no Scoria role model is introduced.
    - CLI assertion: adoption surface doc contract tests pass.
  </acceptance_criteria>
  <done>Adopter/operator docs teach the Phase 44 seam and no longer imply query-param or session-only authority.</done>
</task>

<task type="auto">
  <name>Task 3: Run focused phase proof and record remaining gaps</name>
  <files>test/scoria_web/dashboard_auth_source_scan_test.exs, test/scoria/adoption_surface_test.exs</files>
  <read_first>
    <file>test/scoria_web/dashboard_auth_source_scan_test.exs</file>
    <file>test/scoria/adoption_surface_test.exs</file>
    <file>test/scoria_web/router_test.exs</file>
    <file>test/scoria_web/dashboard_scope_test.exs</file>
    <file>test/scoria_web/live/dashboard_auth_home_connectors_incidents_test.exs</file>
    <file>test/scoria_web/live/dashboard_auth_approvals_test.exs</file>
    <file>test/scoria_web/live/dashboard_auth_workflows_test.exs</file>
    <file>test/scoria_web/live/dashboard_auth_quality_data_test.exs</file>
    <file>test/scoria_web/live/dashboard_auth_prompts_test.exs</file>
  </read_first>
  <action>Run the focused Phase 44 proof command after all implementation plans have landed. If a failure is caused by a real uncovered auth seam, fix it in the owning implementation file and rerun the focused proof. If an unrelated pre-existing full-suite issue appears outside Phase 44 files, document it in the summary with command output and leave Phase 44 proof focused on AUTH-01..03.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria_web/router_test.exs test/scoria_web/dashboard_scope_test.exs test/scoria_web/dashboard_auth_source_scan_test.exs test/scoria/adoption_surface_test.exs test/scoria_web/live/dashboard_auth_home_connectors_incidents_test.exs test/scoria_web/live/dashboard_auth_approvals_test.exs test/scoria_web/live/dashboard_auth_workflows_test.exs test/scoria_web/live/dashboard_auth_quality_data_test.exs test/scoria_web/live/dashboard_auth_prompts_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: focused Phase 44 proof command passes.
    - Source assertion: every AUTH-01, AUTH-02, and AUTH-03 proof file named in this plan exists.
    - Summary assertion: any unrelated residual failures are named with exact command and file, not collapsed into a vague note.
  </acceptance_criteria>
  <done>Phase 44 has focused proof for router seam, resolver gate, LiveView spoof closure, source guard, and docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future code edits -> dashboard LiveViews | Later edits can reintroduce public tenant authority or hardcoded defaults. |
| docs -> adopter implementation | Documentation shapes host integration behavior and must not teach unsafe authority. |
| test runner -> phase proof | Focused proof must catch Phase 44 regressions without hiding real auth failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-07-R | Repudiation | docs/proof trail | mitigate | Doc contracts and summary cite exact proof commands and seam behavior. |
| T-44-07-I | Information Disclosure | future LiveView edits | mitigate | Source-scan guard blocks tenant-param/default authority patterns. |
| T-44-07-E | Elevation of Privilege | adopter docs | mitigate | Docs teach host-owned auth hook and resolver, not Scoria-owned RBAC. |
| T-44-07-T | Tampering | source-scan allow-list | mitigate | Keep allow-list narrow and production LiveView authority paths unallowlisted. |
</threat_model>

<verification>
Plan-level proof:
- `MIX_ENV=test mix test test/scoria_web/dashboard_auth_source_scan_test.exs test/scoria/adoption_surface_test.exs --warnings-as-errors`
- `MIX_ENV=test mix test test/scoria_web/router_test.exs test/scoria_web/dashboard_scope_test.exs test/scoria_web/dashboard_auth_source_scan_test.exs test/scoria/adoption_surface_test.exs test/scoria_web/live/dashboard_auth_home_connectors_incidents_test.exs test/scoria_web/live/dashboard_auth_approvals_test.exs test/scoria_web/live/dashboard_auth_workflows_test.exs test/scoria_web/live/dashboard_auth_quality_data_test.exs test/scoria_web/live/dashboard_auth_prompts_test.exs --warnings-as-errors`
</verification>

<success_criteria>
- Static and behavioral proof covers AUTH-01, AUTH-02, and AUTH-03.
- Docs teach the host-owned auth/scope seam without expanding Scoria into authorization policy.
</success_criteria>

<output>
Create `.planning/phases/44-dashboard-auth-seam/44-07-SUMMARY.md` when done.
</output>
