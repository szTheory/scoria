---
phase: 44-dashboard-auth-seam
plan: 02
type: execute
wave: 2
depends_on:
  - 44-01
files_modified:
  - lib/scoria_web/live/orchestrator_live.ex
  - lib/scoria_web/live/connectors_live/index.ex
  - lib/scoria_web/live/incidents_live/index.ex
  - lib/scoria_web/live/incidents_live/show.ex
  - test/scoria_web/live/dashboard_auth_home_connectors_incidents_test.exs
  - test/scoria_web/live/orchestrator_live_test.exs
  - test/scoria_web/live/connectors_live_test.exs
  - test/scoria_web/live/incidents_live_test.exs
autonomous: true
requirements:
  - AUTH-03
must_haves:
  truths:
    - "AUTH-03: Home, Connectors, and Incidents pages use `socket.assigns.tenant_id` from ScoriaWeb.DashboardScope instead of public tenant params."
    - "AUTH-03: a tenant A session visiting with a tenant B query hint never subscribes to tenant B PubSub topics or renders tenant B incidents/runtimes/traces."
    - "AUTH-03: missing dashboard scope stops before Home/Connectors/Incidents data reads."
  artifacts:
    - path: "lib/scoria_web/live/orchestrator_live.ex"
      provides: "Home dashboard mounted from assigned tenant scope"
    - path: "lib/scoria_web/live/connectors_live/index.ex"
      provides: "Connectors dashboard mounted from assigned tenant scope"
    - path: "lib/scoria_web/live/incidents_live/index.ex"
      provides: "Incident index mounted from assigned tenant scope"
    - path: "lib/scoria_web/live/incidents_live/show.ex"
      provides: "Tenant-scoped incident detail"
    - path: "test/scoria_web/live/dashboard_auth_home_connectors_incidents_test.exs"
      provides: "cross-tenant spoof closure tests for Home, Connectors, and Incidents"
  key_links:
    - from: "lib/scoria_web/dashboard_scope.ex"
      to: "lib/scoria_web/live/orchestrator_live.ex"
      via: "mount reads `socket.assigns.tenant_id`"
      pattern: "socket.assigns.tenant_id"
    - from: "lib/scoria_web/live/connectors_live/index.ex"
      to: "lib/scoria_web/operator_surface.ex"
      via: "tenant-scoped OperatorSurface.load_runtimes/1 and connector_fleet/1"
      pattern: "OperatorSurface.load_runtimes"
  prohibitions:
    - statement: "Home, Connectors, and Incidents do not use public query params as tenant authority."
      status: "resolved"
      verification: "Source scan and LiveView tests prove tenant B query hints do not alter tenant A scope."
---

<objective>
Close the tenant-source spoof path in Home, Connectors, and Incidents for AUTH-03.

Purpose: replace the highest-signal dashboard tenant derivations with the shared scope assign before any PubSub subscribe, OperatorSurface call, or incident detail lookup.
Output: three page groups read only validated scope and cross-tenant tests prove query spoofing does not change tenant authority.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/44-dashboard-auth-seam/44-CONTEXT.md
@.planning/phases/44-dashboard-auth-seam/44-PATTERNS.md
@lib/scoria_web/dashboard_scope.ex
@lib/scoria_web/live/orchestrator_live.ex
@lib/scoria_web/live/connectors_live/index.ex
@lib/scoria_web/live/incidents_live/index.ex
@lib/scoria_web/live/incidents_live/show.ex
@lib/scoria_web/operator_surface.ex
@test/scoria_web/live/orchestrator_live_test.exs
@test/scoria_web/live/connectors_live_test.exs
@test/scoria_web/live/incidents_live_test.exs
</context>

## Artifacts this phase produces

- Test module: `ScoriaWeb.DashboardAuthHomeConnectorsIncidentsTest`.
- LiveView behavior: Home, Connectors, and Incidents mounts consume `socket.assigns.tenant_id`.
- Incident detail behavior: foreign incident IDs render the existing unavailable/not-found state under the asserted tenant.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cross-tenant spoof tests for Home, Connectors, and Incidents</name>
  <files>test/scoria_web/live/dashboard_auth_home_connectors_incidents_test.exs</files>
  <read_first>
    <file>test/scoria_web/live/dashboard_auth_home_connectors_incidents_test.exs</file>
    <file>test/scoria_web/live/orchestrator_live_integration_test.exs</file>
    <file>test/scoria_web/live/connectors_live_test.exs</file>
    <file>test/scoria_web/live/incidents_live_test.exs</file>
    <file>lib/scoria_web/live/orchestrator_live.ex</file>
    <file>lib/scoria_web/live/connectors_live/index.ex</file>
    <file>lib/scoria_web/live/incidents_live/index.ex</file>
    <file>lib/scoria_web/live/incidents_live/show.ex</file>
  </read_first>
  <behavior>
    - Session/resolver tenant A plus a public tenant B hint renders tenant A data or empty state only.
    - Home subscribes/hydrates traces using tenant A.
    - Connectors subscribes to the runtime presence topic for tenant A.
    - Incident index and detail refuse tenant B rows while tenant A is asserted.
  </behavior>
  <action>Create a focused LiveView test module with an endpoint/router using `scoria_dashboard("/scoria")` after Plan 01. Seed tenant A and tenant B runtime, trace, connector, and incident records using existing test fixture helpers and direct schema inserts where current tests do so. Assert tenant B marker strings are absent from rendered HTML when the request includes a tenant B query hint and tenant A session keys. Include a missing-scope case that proves the dashboard unavailable path returns before tenant-owned rows render per D-03 and D-04.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria_web/live/dashboard_auth_home_connectors_incidents_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: tests fail against the pre-phase tenant-source code.
    - Behavior assertion: tenant B runtime, trace, connector, and incident marker strings do not render under tenant A scope.
    - Source assertion: the test router uses the real `scoria_dashboard("/scoria")` macro rather than isolated LiveView mounts.
  </acceptance_criteria>
  <done>Exploit-shaped tests exist for the page group before production cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace local tenant derivation with assigned scope</name>
  <files>lib/scoria_web/live/orchestrator_live.ex, lib/scoria_web/live/connectors_live/index.ex, lib/scoria_web/live/incidents_live/index.ex, lib/scoria_web/live/incidents_live/show.ex</files>
  <read_first>
    <file>lib/scoria_web/live/orchestrator_live.ex</file>
    <file>lib/scoria_web/live/connectors_live/index.ex</file>
    <file>lib/scoria_web/live/incidents_live/index.ex</file>
    <file>lib/scoria_web/live/incidents_live/show.ex</file>
    <file>lib/scoria_web/dashboard_scope.ex</file>
    <file>lib/scoria_web/operator_surface.ex</file>
    <file>.planning/phases/44-dashboard-auth-seam/44-CONTEXT.md</file>
  </read_first>
  <behavior>
    - `mount/3` in each page reads the tenant from `socket.assigns.tenant_id`.
    - PubSub subscriptions are built only after scope assignment and use the asserted tenant.
    - Query params remain allowed for non-authority UI state such as runtime, from, or object IDs after tenant-scoped lookup.
    - Incident detail continues to use `OperatorSurface.fetch_tenant_incident/2` and renders not found for foreign IDs.
  </behavior>
  <action>Refactor the four LiveViews per D-03. Remove local tenant extraction from params/session/default expressions. Keep ordinary UI filters from params, but route every tenant-owned read and subscription through `socket.assigns.tenant_id`. In OrchestratorLive, ensure review candidate deep-link loading cannot render a candidate unless it matches the asserted tenant or is handled by a later plan-owned helper. In IncidentsLive.Show, remove the session/default mount fallback and rely on the shared gate.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria_web/live/dashboard_auth_home_connectors_incidents_test.exs test/scoria_web/live/orchestrator_live_test.exs test/scoria_web/live/connectors_live_test.exs test/scoria_web/live/incidents_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the four LiveView files no longer contain the old query-param tenant authority expression or session-plus-hardcoded-default tenant fallback.
    - Behavior assertion: focused cross-tenant tests pass.
    - Behavior assertion: existing Home, Connectors, and Incidents tests pass after their setup uses host session/resolver scope.
    - Security assertion: connected PubSub subscription topics are derived from tenant A in tenant A sessions, even when the request URL carries a tenant B hint.
  </acceptance_criteria>
  <done>Home, Connectors, and Incidents consume only ScoriaWeb.DashboardScope tenant assigns.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh existing page harnesses for host-asserted scope</name>
  <files>test/scoria_web/live/orchestrator_live_test.exs, test/scoria_web/live/connectors_live_test.exs, test/scoria_web/live/incidents_live_test.exs</files>
  <read_first>
    <file>test/scoria_web/live/orchestrator_live_test.exs</file>
    <file>test/scoria_web/live/connectors_live_test.exs</file>
    <file>test/scoria_web/live/incidents_live_test.exs</file>
    <file>test/scoria_web/live/dashboard_auth_home_connectors_incidents_test.exs</file>
    <file>lib/scoria_web/dashboard_scope.ex</file>
  </read_first>
  <action>Update existing tests in this page group so their router sessions provide a host-asserted tenant and actor where the page needs one. Replace expectations that assume a hardcoded default tenant with explicit tenant fixture values. Preserve existing behavior assertions unrelated to dashboard auth.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_test.exs test/scoria_web/live/connectors_live_test.exs test/scoria_web/live/incidents_live_test.exs test/scoria_web/live/dashboard_auth_home_connectors_incidents_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: the existing page test files pass with the new scope gate.
    - Source assertion: tests construct sessions with explicit tenant IDs instead of relying on an implicit default tenant.
    - Behavior assertion: no non-auth behavior covered by these test files is loosened or removed.
  </acceptance_criteria>
  <done>Existing page tests exercise the host-session default resolver rather than the removed fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser URL -> LiveView mount | Public params enter page mounts but cannot define tenant authority. |
| LiveView mount -> PubSub | Tenant-specific subscriptions are created from asserted scope only. |
| LiveView mount -> OperatorSurface | Dashboard data reads receive a validated tenant string. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-02-S | Spoofing | Home/Connectors/Incidents tenant source | mitigate | Remove page-local tenant derivation and read `socket.assigns.tenant_id` only. |
| T-44-02-I | Information Disclosure | incident detail and lists | mitigate | Use existing tenant-qualified OperatorSurface incident functions and assert foreign rows do not render. |
| T-44-02-E | Elevation of Privilege | PubSub topic selection | mitigate | Subscribe only to topics interpolated from asserted tenant scope. |
| T-44-02-D | Denial of Service | missing dashboard scope | accept | Missing scope halts the dashboard surface; this is desired fail-closed behavior. |
</threat_model>

<verification>
Plan-level proof:
- `MIX_ENV=test mix test test/scoria_web/live/dashboard_auth_home_connectors_incidents_test.exs test/scoria_web/live/orchestrator_live_test.exs test/scoria_web/live/connectors_live_test.exs test/scoria_web/live/incidents_live_test.exs --warnings-as-errors`
- `rg -n "socket.assigns.tenant_id|Dashboard unavailable" lib/scoria_web/live/orchestrator_live.ex lib/scoria_web/live/connectors_live/index.ex lib/scoria_web/live/incidents_live/index.ex lib/scoria_web/live/incidents_live/show.ex`
</verification>

<success_criteria>
- A request scoped to tenant A cannot use URL tenant hints to see tenant B Home, Connectors, or Incidents data.
- Missing scope blocks the page group before data reads.
</success_criteria>

<output>
Create `.planning/phases/44-dashboard-auth-seam/44-02-SUMMARY.md` when done.
</output>
