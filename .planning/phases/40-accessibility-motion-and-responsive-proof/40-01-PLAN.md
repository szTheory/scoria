---
phase: 40-accessibility-motion-and-responsive-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/dev/package.json
  - priv/dev/package-lock.json
  - priv/dev/e2e/lib/boxes_intersect.mjs
  - priv/dev/e2e/lib/axe.mjs
  - .planning/phases/40-accessibility-motion-and-responsive-proof/40-GAP-REGISTER.md
autonomous: true
requirements: [A11Y-01, A11Y-02, RESP-01]
must_haves:
  truths:
    - "`@axe-core/playwright` is a dev-only devDependency, exact-pinned, with transitive `axe-core` pinned via `overrides` (D-05); no new Hex runtime dep."
    - "A single shared `boxesIntersect(a,b)` helper exists that both the drawer focus spec (dynamic occlusion, D-11) and responsive scan (static occlusion, D-16(5)) import — one intersection primitive, not two (D-17)."
    - "A single shared axe-run helper sets tags `['wcag2a','wcag2aa','wcag21a','wcag21aa','wcag22aa']` (never best-practice) AND explicitly `rules: { 'target-size': { enabled: true } }` (report-only) so target-size actually runs (research delta #2, D-06)."
    - "A working gap register exists recording out-of-scope-boundary defects with viewport/AT/repro + the exact boundary crossed (D-01/D-02/D-03); `prefers-contrast`/`forced-colors` is pre-recorded as considered-and-deferred non-goal (D-20)."
  artifacts:
    - priv/dev/package.json
    - priv/dev/package-lock.json
    - priv/dev/e2e/lib/boxes_intersect.mjs
    - priv/dev/e2e/lib/axe.mjs
    - .planning/phases/40-accessibility-motion-and-responsive-proof/40-GAP-REGISTER.md
  key_links:
    - "axe.mjs `rules` override is set ONCE and imported by every axe scan — the target-size enable and tag list cannot drift between full-lab and curated-page scans."
    - "boxes_intersect.mjs is the sole geometry primitive; drawer and responsive specs import it rather than re-deriving intersection math."
---

<objective>
Stand up the shared, dependency-level infrastructure that every later Phase 40 spec imports, and open the running gap register. This is the Wave 1 prerequisite: the axe package pin + shared axe-run helper, the shared bounding-box intersection helper, and the gap-register working artifact.

Purpose: Later specs (axe scan, drawer/modal focus, responsive scan) must NOT each re-pin axe, re-thread the axe rules override, or re-implement box intersection. Centralizing here prevents the two documented drift risks (target-size override diverging between scans; intersection math diverging between the two occlusion checks) and gives the whole phase one place to accumulate out-of-scope defects.
Output: `@axe-core/playwright` pinned + lockfile committed; `priv/dev/e2e/lib/axe.mjs`; `priv/dev/e2e/lib/boxes_intersect.mjs`; `40-GAP-REGISTER.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-accessibility-motion-and-responsive-proof/40-CONTEXT.md
@.planning/phases/40-accessibility-motion-and-responsive-proof/40-RESEARCH.md
@.planning/phases/40-accessibility-motion-and-responsive-proof/40-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin @axe-core/playwright dev-only with transitive axe-core override</name>
  <read_first>
    - priv/dev/package.json (current: dependencies.playwright=1.60.0, devDependencies.@playwright/test=1.60.0; @axe-core/playwright absent)
    - priv/dev/package-lock.json (git-tracked — confirm before edit)
    - mix.exs (~line 160: `priv/dev` excluded from Hex `package.files` — the reason this dep never ships)
    - 40-RESEARCH.md § Dependency-Pin Guidance (verified-legitimate verdict: Deque org, 5.1M weekly downloads, no postinstall; the exact `devDependencies` + `overrides` block to apply)
  </read_first>
  <action>
    Add `@axe-core/playwright` at exact version `4.12.1` to `devDependencies` in `priv/dev/package.json` (NOT `dependencies` — it is dev/test-only, per D-05). Add an `overrides` block pinning transitive `axe-core` to exact `4.12.1` — pinning only the parent leaves `axe-core` on its own `~4.12.1` tilde range and a floating patch could flip CI (research Dependency-Pin Guidance). Then run `npm --prefix priv/dev install` so the git-tracked `package-lock.json` reflects both pins, matching the existing `playwright: 1.60.0` lockfile discipline. Do NOT add any Hex dependency and do NOT touch root-level deps. The package legitimacy audit is already complete in 40-RESEARCH.md (verdict OK, official Deque package) — no human legitimacy checkpoint required.
  </action>
  <verify>
    <automated>node -e "const p=require('/Users/jon/projects/scoria/priv/dev/package.json'); const l=require('/Users/jon/projects/scoria/priv/dev/package-lock.json'); if(p.dependencies&&p.dependencies['@axe-core/playwright'])throw new Error('axe must be devDependency not dependency'); if(p.devDependencies['@axe-core/playwright']!=='4.12.1')throw new Error('axe not exact-pinned 4.12.1'); if(!p.overrides||p.overrides['axe-core']!=='4.12.1')throw new Error('missing axe-core override pin'); console.log('axe pin + override OK')"</automated>
  </verify>
  <acceptance_criteria>
    `priv/dev/package.json` lists `@axe-core/playwright@4.12.1` under `devDependencies` (never `dependencies`), has an `overrides.axe-core` of `4.12.1`, and the committed `package-lock.json` resolves both to exactly 4.12.1. No Hex runtime dependency added.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create shared axe-run helper and boxesIntersect geometry helper</name>
  <read_first>
    - priv/dev/e2e/lib/ready.mjs (existing lib helper — file placement + module style to mirror)
    - priv/dev/e2e/command_palette.spec.mjs (import style for e2e lib modules)
    - 40-RESEARCH.md § Common Pitfalls Pitfall 1 (target-size enabled:false by default — the `rules` override is mandatory or the rule is a silent no-op) and § Open Questions #2 (thread the override once in a shared helper)
    - 40-CONTEXT.md D-06 (tag list, report-only stance) and D-17 (single shared intersection helper)
  </read_first>
  <action>
    Create `priv/dev/e2e/lib/axe.mjs` exporting a helper that builds and runs an AxeBuilder scan with a fixed configuration used by ALL axe scans this phase: `withTags` set to exactly `['wcag2a','wcag2aa','wcag21a','wcag21aa','wcag22aa']` (never `best-practice` — it pulls non-AA opinions + the classic lab false-positives), and an explicit rules override enabling `target-size` (kept report-only by the caller, not asserted). Enabling target-size is required because axe-core 4.12.1 ships it `enabled: false` by default (research delta #2) — without the override D-06's target-size clause silently produces zero signal. Expose the run so callers can read violation results for both report-only collection and curated assert-zero. Create `priv/dev/e2e/lib/boxes_intersect.mjs` exporting a single pure `boxesIntersect(a, b)` predicate over DOMRect-like `{top,right,bottom,left}` objects (axis-aligned overlap test with a small tolerance consistent with the 1px idiom used elsewhere). This is the ONLY intersection primitive — the drawer focus spec (dynamic SC 2.4.11 occlusion) and the responsive scan (static toast-over-nav occlusion) both import it (D-17). Keep both modules dependency-free beyond `@axe-core/playwright`.
  </action>
  <verify>
    <automated>node --input-type=module -e "import {boxesIntersect} from '/Users/jon/projects/scoria/priv/dev/e2e/lib/boxes_intersect.mjs'; const A={top:0,left:0,right:10,bottom:10}; const B={top:5,left:5,right:15,bottom:15}; const C={top:20,left:20,right:30,bottom:30}; if(!boxesIntersect(A,B))throw new Error('overlap not detected'); if(boxesIntersect(A,C))throw new Error('false overlap'); console.log('boxesIntersect OK')"</automated>
  </verify>
  <acceptance_criteria>
    `axe.mjs` exports a scan helper whose config fixes the five WCAG tags (no best-practice) and enables `target-size` via a `rules` override; `boxes_intersect.mjs` exports `boxesIntersect(a,b)` returning true for overlapping and false for disjoint rects. Both are importable ES modules under `priv/dev/e2e/lib/`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Open the running gap register working artifact</name>
  <read_first>
    - 40-CONTEXT.md D-01/D-02/D-03 (prove-and-fix with a scope-boundary escape valve; defer by scope never size; start the register in 40, Phase 41 finalizes) and D-20 (prefers-contrast/forced-colors explicit non-goal)
    - .planning/REQUIREMENTS.md (the out-of-scope list = the D-02 defer-line discriminator)
  </read_first>
  <action>
    Create `.planning/phases/40-accessibility-motion-and-responsive-proof/40-GAP-REGISTER.md` as a working artifact (NOT the final polished register — that is Phase 41 / PROOF). Give it a table schema with columns: defect id, requirement (A11Y/MOTION/RESP), surface/page, viewport or AT, repro steps, the exact locked boundary the only-fix would cross (new/changed primitive vocabulary · public macro/`.scoria-root` · new runtime dep · approval-semantics rewrite), and status (registered / fixed-in-40). Pre-seed ONE row: `prefers-contrast` / `forced-colors` (Windows High Contrast) recorded as considered-and-deferred explicit non-goal per D-20 (not a defect). Add a header note stating the register accumulates entries as later Phase 40 plans surface out-of-scope defects, and that a FLOOD of defects needing primitive changes is an escalate/replan signal (D-02), not a workload to absorb. Do NOT front-run Phase 41's final register that separates fixed-vs-deferred.
  </action>
  <verify>
    <automated>test -f /Users/jon/projects/scoria/.planning/phases/40-accessibility-motion-and-responsive-proof/40-GAP-REGISTER.md && grep -qi 'forced-colors\|prefers-contrast' /Users/jon/projects/scoria/.planning/phases/40-accessibility-motion-and-responsive-proof/40-GAP-REGISTER.md && grep -qi 'boundary' /Users/jon/projects/scoria/.planning/phases/40-accessibility-motion-and-responsive-proof/40-GAP-REGISTER.md && echo "gap register scaffolded"</automated>
  </verify>
  <acceptance_criteria>
    `40-GAP-REGISTER.md` exists with a per-defect schema capturing viewport/AT/repro + the exact crossed boundary, pre-seeds the `prefers-contrast`/`forced-colors` non-goal (D-20), and notes the D-02 flood-is-a-signal rule.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dev toolchain | `@axe-core/playwright` + transitive `axe-core` enter the dev/test toolchain (never the shipped Hex package — `priv/dev` excluded from `package.files`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-SC | Tampering | `@axe-core/playwright` / transitive `axe-core` install | high | mitigate | Exact pin `4.12.1` + `overrides` pin on transitive `axe-core` + committed lockfile so a floating patch cannot flip CI; legitimacy pre-verified in 40-RESEARCH.md (official Deque org, no postinstall). Dev-only: excluded from Hex `package.files` (mix.exs ~160), never reaches an adopter runtime. |
| T-40-01 | Information disclosure | gap register file | low | accept | Register lives in `.planning/` (not shipped); records defect repros only, no secrets. |
</threat_model>

<verification>
- `node -e` package assertion passes (axe pinned dev-only + override).
- `boxesIntersect` smoke test passes.
- Gap register file exists with required schema + D-20 seed.
- `npm --prefix priv/dev ci` (or `install`) resolves `axe-core` to exactly 4.12.1 in the lockfile.
</verification>

<success_criteria>
The three shared artifacts (axe pin+lockfile, `axe.mjs`, `boxes_intersect.mjs`) and the gap register exist; downstream Wave 2 specs can import the helpers and pin without re-deriving them. No Hex runtime dependency added.
</success_criteria>

<output>
Create `.planning/phases/40-accessibility-motion-and-responsive-proof/40-01-SUMMARY.md` when done.
</output>
