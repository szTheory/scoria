---
phase: 40-accessibility-motion-and-responsive-proof
plan: 04
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - priv/dev/e2e/a11y_axe.spec.mjs
  - brandbook/tokens.json
  - brandbook/tokens.css
  - assets/css/02-tokens.css
autonomous: false
requirements: [A11Y-02]
must_haves:
  truths:
    - "axe scans run with tags `['wcag2a','wcag2aa','wcag21a','wcag21aa','wcag22aa']` (never best-practice) via the shared `axe.mjs` helper, exercising BOTH `data-theme=dark` AND `data-theme=light` (D-09) — scanning one theme leaves half the contrast surface unproven."
    - "First run = report-only baseline capture (violation count + rule breakdown attached to the playwright-report), NOT assert-zero — the dev lab is a specimen gallery where muted/disabled specimens fire color-contrast by design (D-06)."
    - "assert-zero applies ONLY to a curated allow-list of seeded real pages confirmed clean (or fixed first); the full-lab scan stays report-only through Phase 40; `target-size` stays report-only (D-06)."
    - "Any genuine contrast violation is fixed via the brand token SSOT (`brandbook/tokens.json` → `tokens.css` / `assets/css/02-tokens.css`, re-greening `token_contrast_guard_test.exs`), NEVER page-local color (that would reintroduce the DS-06 raw-value drift) (D-01/Spine #5)."
  artifacts:
    - priv/dev/e2e/a11y_axe.spec.mjs
    - .planning/phases/40-accessibility-motion-and-responsive-proof/40-GAP-REGISTER.md
  key_links:
    - "a11y_axe.spec.mjs imports the shared `axe.mjs` (Plan 01) so the tag list + target-size override cannot drift between the report-only full-lab scan and the curated assert-zero scan."
    - "Contrast fixes flow token SSOT → tokens.css → 02-tokens.css and must keep `token_contrast_guard_test.exs` (dual-theme) green."
---

<objective>
Author the axe-core WCAG 2.2 AA e2e scan (A11Y-02): a report-only baseline across the full dev lab in both themes first, then a curated assert-zero on seeded real pages after human baseline inspection. This is axe's slice of the D-07 coverage map — computed contrast/name/role/ARIA that neither the source-scan nor keyboard-e2e can prove.

Purpose: Close A11Y-02's computed-truth requirement without red-walling the required gate. Per D-06 the first run captures a baseline (not assert-zero) because the lab intentionally renders muted/ghost/disabled specimens that fire color-contrast; only a curated allow-list of real pages ratchets to assert-zero. Genuine contrast hits are fixed through the token SSOT, never page-local.
Output: `a11y_axe.spec.mjs` (report-only full-lab both themes + curated assert-zero); any token-SSOT contrast fixes; gap-register entries for out-of-scope findings.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-accessibility-motion-and-responsive-proof/40-CONTEXT.md
@.planning/phases/40-accessibility-motion-and-responsive-proof/40-RESEARCH.md
@.planning/phases/40-accessibility-motion-and-responsive-proof/40-VALIDATION.md
@priv/dev/e2e/lab.spec.mjs
@test/scoria_web/token_contrast_guard_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: a11y_axe.spec.mjs — report-only baseline, full lab, both themes</name>
  <read_first>
    - priv/dev/e2e/lib/axe.mjs (Plan 01 shared helper: fixed WCAG tag list + `rules: { 'target-size': { enabled: true } }` report-only override — import it, do NOT re-declare tags/rules inline)
    - priv/dev/e2e/lab.spec.mjs (the specimen-gallery lab route + `VIEWPORT_WIDTHS`/`waitForReady` usage), priv/dev/e2e/lib/ready.mjs
    - assets/js/scoria.js (`data-theme` cycling ~113-114 — the mechanism to toggle dark/light for D-09)
    - 40-RESEARCH.md § Common Pitfalls (Pitfall 1 target-size default-disabled; Pitfall 5 best-practice exclusion rationale), § Dependency-Pin Guidance
    - 40-CONTEXT.md D-06, D-09
  </read_first>
  <action>
    Create `priv/dev/e2e/a11y_axe.spec.mjs` importing the shared `axe.mjs` run helper (never re-declaring the tag list or the target-size override inline — single source per Plan 01). Scan the full dev lab route in BOTH themes: toggle `data-theme=dark` then `data-theme=light` via the existing theme mechanism and run the scan under each (D-09 — color-contrast differs per theme). This first run is REPORT-ONLY: collect the violation count + per-rule breakdown and attach it to the playwright-report via `testInfo.attach()` (+ `console.warn` summary); do NOT `expect(violations).toEqual([])` on the full lab (muted/ghost/disabled specimens fire color-contrast by design — a bare assert-zero here red-walls the required gate, violating D-04). Keep `target-size` (2.5.8) results in the report-only breakdown, never asserted. Use `waitForReady` before each scan.
  </action>
  <verify>
    <automated>npx --prefix priv/dev playwright test a11y_axe.spec.mjs  # scoped per-task run against a running make dev; inspect the attached report-only breakdown in playwright-report; full mix scoria.ui.e2e reserved for the per-wave sampling tier</automated>
  </verify>
  <acceptance_criteria>
    `a11y_axe.spec.mjs` produces a report-only baseline (violation count + per-rule breakdown attached, both themes) with no throwing assert-zero on the full lab; imports the shared `axe.mjs`; target-size appears in the breakdown (proves the override works) but is not asserted. Run does not fail the gate.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Curate the axe assert-zero real-page allow-list from the baseline</name>
  <action>Present the report-only axe baseline (both themes, per-rule breakdown) to the developer; do not proceed to the curated assert-zero scan until the developer confirms which seeded real pages ratchet to assert-zero and which findings (if any) need a token-SSOT fix or a gap-register entry.</action>
  <what-built>The report-only axe baseline across the full dev lab in both dark and light themes (Task 1), with per-rule violation breakdown attached to the playwright-report. This checkpoint curates which seeded REAL pages are clean enough to ratchet to assert-zero, and flags any genuine contrast defect for a token-SSOT fix.</what-built>
  <how-to-verify>
    1. Open the attached axe baseline breakdown in the latest `playwright-report` (or the `console.warn` summary from the e2e run).
    2. Separate expected specimen-gallery noise (muted/ghost/disabled/danger tone variants firing `color-contrast`) from GENUINE violations on seeded real operator pages (approvals, review queue, incidents, a workflow detail).
    3. Confirm the proposed curated assert-zero allow-list = only real pages with zero genuine violations in BOTH themes.
    4. For any genuine contrast hit on a real page: confirm it is fixed via the brand token SSOT (`brandbook/tokens.json` → `tokens.css` / `assets/css/02-tokens.css`), NOT page-local color, keeping `token_contrast_guard_test.exs` green. For any violation whose only fix crosses a locked boundary (vocabulary/macro/`.scoria-root`/runtime dep), confirm it is registered in `40-GAP-REGISTER.md`, not forced into scope.
  </how-to-verify>
  <resume-signal>Reply with the confirmed curated assert-zero page allow-list (or "approved: {pages}"), or describe genuine violations to fix first.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Curated assert-zero scan on the confirmed real-page allow-list (+ token-SSOT fixes if needed)</name>
  <read_first>
    - priv/dev/e2e/a11y_axe.spec.mjs (Task 1 — extend with the curated describe block)
    - brandbook/tokens.json, brandbook/tokens.css, assets/css/02-tokens.css (the ONLY sanctioned contrast-fix path — token SSOT)
    - test/scoria_web/token_contrast_guard_test.exs (dual-theme luminance guard that must stay green after any token edit)
    - 40-CONTEXT.md D-06 (curated allow-list ratchet), D-01/Spine #5 (token-SSOT fixes only)
  </read_first>
  <action>
    Add a curated `test.describe` block to `a11y_axe.spec.mjs` that runs the shared axe scan against ONLY the human-confirmed allow-list of seeded real pages (from the checkpoint), in BOTH themes, and asserts zero violations (throwing `expect`). This is fix-and-assert atomic: if any confirmed page still has a genuine contrast violation, fix it FIRST via the token SSOT (`brandbook/tokens.json` → regenerate `tokens.css` → `assets/css/02-tokens.css`), re-greening `token_contrast_guard_test.exs`, then let the assert-zero pass — never introduce page-local color (would reintroduce the DS-06 raw-value drift). Keep the full-lab scan (Task 1) report-only and `target-size` report-only through Phase 40. Register any out-of-scope-boundary finding in `40-GAP-REGISTER.md`.
  </action>
  <verify>
    <automated>mix test test/scoria_web/token_contrast_guard_test.exs test/scoria_web/ds06_drift_guard_test.exs --color && echo "token guards green — e2e curated assert-zero runs in the e2e lane (mix scoria.ui.e2e)"</automated>
  </verify>
  <acceptance_criteria>
    The curated assert-zero describe block passes zero-violations on the confirmed real-page allow-list in both themes; any contrast fix went through the token SSOT and `token_contrast_guard_test.exs` + `ds06_drift_guard_test.exs` stay green; the full-lab scan and target-size remain report-only.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered UI → axe engine | Real Chromium renders both themes; axe computes contrast/name/role/ARIA that Floki cannot. |
| contrast fix → token layer | Contrast remediation must cross only the token SSOT boundary, never page-local color. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-07 | Tampering (premature gate) | full-lab axe assert-zero | high | mitigate | First run report-only (attach breakdown, no throwing expect); assert-zero only on human-curated clean real pages — no bare `expect()` merges ahead of a fix (D-04). |
| T-40-08 | Tampering (token drift) | contrast fix path | medium | mitigate | Fixes routed through token SSOT; `ds06_drift_guard_test.exs` (raw-palette zero) + `token_contrast_guard_test.exs` (dual-theme luminance) stay green — page-local color is structurally blocked. |
| T-40-SC | Tampering | axe-core engine version | high | mitigate | Consumed via Plan 01's exact pin + override + lockfile; report reproducible across runs. |
</threat_model>

<verification>
- Report-only baseline attaches a both-themes per-rule breakdown; full lab does not assert-zero.
- Human checkpoint confirms the curated allow-list + genuine-vs-noise split.
- Curated assert-zero passes zero-violations on confirmed real pages, both themes.
- `token_contrast_guard_test.exs` + `ds06_drift_guard_test.exs` green after any token fix.
</verification>

<success_criteria>
A11Y-02 computed contrast/name/role/ARIA is proven: report-only baseline (both themes) captured, curated assert-zero green on confirmed real pages, target-size report-only, all contrast fixes token-SSOT-routed with drift guards green.
</success_criteria>

<output>
Create `.planning/phases/40-accessibility-motion-and-responsive-proof/40-04-SUMMARY.md` when done.
</output>
