---
phase: 40-accessibility-motion-and-responsive-proof
plan: 03
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - lib/scoria_web/ui.ex
  - lib/scoria_web/components/approval_inbox_component.ex
  - lib/scoria_web/live/approvals_live/index.ex
  - lib/scoria_web/live/connectors_live/index.ex
  - lib/scoria_web/live/dataset_live/index.ex
  - lib/scoria_web/live/workflow_live/show.ex
  - lib/scoria_web/live/prompt_live/release_workbench_live.ex
  - priv/dev/e2e/drawer_focus.spec.mjs
  - priv/dev/e2e/modal_focus.spec.mjs
autonomous: true
requirements: [A11Y-01]
must_haves:
  truths:
    - "`drawer/1` and `modal/1` trap focus (Tab/Shift+Tab wrap, never lands on background), move focus inside on open, close on Esc, and restore focus to the opening trigger on close — implemented with ZERO new attrs/slots (D-10, in-scope internal a11y wiring, not a vocabulary change)."
    - "Focus restoration is captured at the OPENER (`JS.push_focus()` at each ~7-8 opener call sites, not just inside ui.ex) and released via `pop_focus` from the component's internal `on_dismiss` wiring (research delta #4 / Pitfall 2)."
    - "`focus_wrap` is nested with a DISTINCT id `#{@id}-focus` inside the existing `role=dialog` element — it does NOT reuse the overlay shell's `@id` (research delta #5 / Pitfall 3)."
    - "The drawer spec proves SC 2.4.11: the focused primary action is not covered by the `position:sticky;bottom:0` approval footer (rect non-overlap via shared `boxesIntersect`, D-11 owns dynamic occlusion per D-17)."
    - "D-13 live-patch focus survival is a WARNING-GRADE collector (`console.warn` + `testInfo.attach()`, no throwing `expect()`); everything else in these specs is fix-and-assert ATOMIC in this same plan/commit (D-04) — no new bare `expect()` merges ahead of the fix."
  artifacts:
    - lib/scoria_web/ui.ex
    - priv/dev/e2e/drawer_focus.spec.mjs
    - priv/dev/e2e/modal_focus.spec.mjs
  key_links:
    - "`push_focus` at opener → `focus_wrap`+`focus_first` on mount → `pop_focus` on `on_dismiss`. Break any link and restore lands on `<body>` instead of the trigger."
    - "`Phoenix.FocusWrap` client hook ships in the phoenix_live_view JS bundle — auto-registered regardless of Scoria's custom `hooks:` object; no scoria.js change, no new Hex dep."
    - "drawer_focus.spec.mjs imports `boxesIntersect` from Plan 01's `lib/boxes_intersect.mjs` (D-17 shared primitive)."
---

<objective>
Ship the single most load-bearing Phase 40 fix: give `drawer/1` and `modal/1` real focus trap + restore (the drawer is the $10k-refund approval decision surface — the highest-stakes keyboard surface in the product, and today it keeps focus on the trigger behind the scrim). Land it fix-and-assert ATOMIC with the drawer + modal keyboard-driving specs, per the D-04 two-bucket rule.

Purpose: Close the verified A11Y-01 gap (D-10). The command palette + mobile nav already trap/restore correctly; this brings the server-rendered overlays up to that standard using core Phoenix (`focus_wrap` + `JS.push_focus/focus_first/pop_focus`) — no new attr, no new slot, no new Hex dep. Because the e2e lane is a hard-fail required gate, the fix and its `expect()` assertions ship in the SAME plan/commit; the D-13 live-patch survival check is a non-throwing collector.
Output: focus-trap+restore wiring in `ui.ex` + all opener call sites; `drawer_focus.spec.mjs`; `modal_focus.spec.mjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-accessibility-motion-and-responsive-proof/40-CONTEXT.md
@.planning/phases/40-accessibility-motion-and-responsive-proof/40-RESEARCH.md
@.planning/phases/40-accessibility-motion-and-responsive-proof/40-VALIDATION.md
@lib/scoria_web/ui.ex
@priv/dev/e2e/command_palette.spec.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focus trap + restore to drawer/1 and modal/1 (ui.ex + all opener call sites)</name>
  <read_first>
    - lib/scoria_web/ui.ex (`modal/1` def line 708: only `autofocus` on the X + `phx-key="Escape"`; `drawer/1` def line 761: `role="dialog" aria-modal="true"` at 773-774, NO autofocus/trap/restore — the fix target)
    - assets/js/scoria.js (CommandPalette/MobileNav `trapFocus`/`focusableElements`/`restoreFocus` — the proven pattern; confirm the `Phoenix.FocusWrap` client hook ships in the LiveView bundle and needs no scoria.js edit)
    - lib/scoria_web/live/approvals_live/index.ex (drawer render ~238, modal render ~356), lib/scoria_web/components/approval_inbox_component.ex (the row-click opener `phx-click={@select_event}` at 110/134 — opener is HERE, not in the LiveView), lib/scoria_web/live/connectors_live/index.ex (drawers ~190/196; openers `open_runtime_drawer` ~114, `open_connector_drawer` ~151/172), lib/scoria_web/live/dataset_live/index.ex (drawer ~151), lib/scoria_web/live/workflow_live/show.ex (modal ~271), lib/scoria_web/live/prompt_live/release_workbench_live.ex (modals ~282/298; openers `open_approve_modal`/`open_reject_modal`)
    - 40-RESEARCH.md § Common Pitfalls (Pitfall 2 opener fan-out, Pitfall 3 id collision, Pitfall 4 conditional-mount lifecycle favors focus_wrap), § Already-Green item 7 (focus_wrap/JS ops present in pinned phoenix_live_view 1.1.30)
    - 40-CONTEXT.md D-10, D-13
  </read_first>
  <action>
    Implement D-10 as internal a11y wiring adding ZERO attrs and ZERO slots. In `ui.ex`, wrap the panel body of BOTH `modal/1` and `drawer/1` in `Phoenix.Component.focus_wrap/1` nested INSIDE the existing `role="dialog"` element, with a DISTINCT id `"#{@id}-focus"` — do NOT reuse the shell's `@id` (focus_wrap renders its own `id={@id}` div; reusing collides — research Pitfall 3). Prefer `focus_wrap` over porting the scoria.js hook because the overlays mount conditionally via `:if={@show}` and `Phoenix.FocusWrap` is designed for that re-insertion lifecycle (Pitfall 4). Move focus inside on open (autofocus already on modal's X; ensure the drawer also moves focus in — via `focus_first`/an autofocus target inside the wrap). Centralize `pop_focus` in the component's internal `on_dismiss`/close wiring so focus restores to the trigger on Esc/close. Then, at EVERY opener call site, compose the existing open trigger into a `JS` command chain that ALSO issues `JS.push_focus()` so the trigger element is remembered before the overlay mounts (a server-side `handle_event` opener alone cannot capture focus — the `phx-click` must run `JS.push_focus()` client-side first). Re-grep `'<\.drawer\b\|<\.modal\b'` and the opener events to confirm the full enumeration before editing (research flagged this Medium-confidence: expected ~7-8 sites across approval_inbox_component.ex, approvals_live/index.ex, connectors_live/index.ex ×2, dataset_live/index.ex, workflow_live/show.ex, release_workbench_live.ex ×2) — cover ALL of them; a missed opener means restore lands on `<body>`. Do not add a new Hex dep, do not change any public attr/slot, do not touch `.scoria-root` or the public macro.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5; mix test test/scoria_web/a11y_structural_guard_test.exs test/scoria_web/ui_component_test.exs --color</automated>
  </verify>
  <acceptance_criteria>
    `ui.ex` `modal/1` and `drawer/1` nest `focus_wrap` at id `#{@id}-focus` inside `role=dialog`; `pop_focus` fires on dismiss; every enumerated opener call site issues `JS.push_focus()`. Zero new attrs/slots, zero new Hex deps, compiles clean, structural guard + component tests stay green (role/aria/label associations unchanged).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: drawer_focus.spec.mjs — tab-in / trap / Esc / restore / SC 2.4.11 + live-patch collector</name>
  <read_first>
    - priv/dev/e2e/command_palette.spec.mjs (proven open/trap/Esc/restore pattern: `await expect(opener).toBeFocused()` after Esc; Tab-from-last-wraps assertion)
    - priv/dev/e2e/lib/ready.mjs (`waitForReady` / `data-scoria-ready`), priv/dev/e2e/lib/boxes_intersect.mjs (Plan 01 shared helper — import for SC 2.4.11)
    - lib/mix/tasks/scoria.ui.e2e.ex (seeds approvals + exports seeded demo ids as env to Playwright — use them to open the real approval drawer deterministically)
    - assets/css/04-components.css (`.scoria-approval-actions { position: sticky; bottom: 0 }` ~854 — the footer whose rect must NOT cover the focused primary action)
    - 40-CONTEXT.md D-11 (drawer assertion set), D-13 (live-patch survival = warning-grade), D-17 (D-11 owns dynamic occlusion), D-04 (two-bucket rule)
  </read_first>
  <action>
    Create `priv/dev/e2e/drawer_focus.spec.mjs` extending the `command_palette.spec.mjs` pattern against the seeded approval decision drawer. Assert (THROWING `expect()`, fix-and-assert atomic since the fix lands in Task 1 this same plan): tab-in (focus is inside the drawer on open), trap (Tab from the last focusable wraps to the first and Shift+Tab from the first wraps to the last, focus never lands on a background element behind the scrim), Esc closes the drawer, restore-to-trigger (`activeElement === opener`). Add the SC 2.4.11 assertion: after tabbing to the primary action, its bounding rect does NOT intersect the `position:sticky;bottom:0` approval footer rect — use the imported `boxesIntersect` helper (D-11 owns this dynamic occlusion, D-17). Add the D-13 live-patch survival check as a WARNING-GRADE NON-THROWING collector: focus inside the drawer → trigger a simulated live patch (an unrelated broadcast / `phx-update`) → re-check focus is still inside; if not, emit `console.warn` + `testInfo.attach()` into the playwright-report — NO throwing `expect()`, NO `test.fail()`, NO `expect.soft` (D-04 bans them). Use `waitForReady` before interacting.
  </action>
  <verify>
    <automated>mix scoria.ui.e2e --base-url http://localhost:4799/scoria 2>&1 | tail -20  # runs the e2e lane incl. drawer_focus.spec.mjs (requires make dev running); OR push and let the required CI e2e job run it</automated>
  </verify>
  <acceptance_criteria>
    `drawer_focus.spec.mjs` passes: tab-in, trap (both directions wrap, never background), Esc-closes, restore-to-opener, and SC 2.4.11 rect non-overlap all green against the Task 1 fix. The D-13 live-patch check is a non-throwing collector (warns + attaches, never fails the run). No `test.fail()`/`expect.soft`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: modal_focus.spec.mjs — tab-in / trap / Esc / restore</name>
  <read_first>
    - priv/dev/e2e/command_palette.spec.mjs (pattern), priv/dev/e2e/lib/ready.mjs (`waitForReady`)
    - lib/scoria_web/live/workflow_live/show.ex (promote modal ~271, `on_dismiss="close_modal"`) OR lib/scoria_web/live/prompt_live/release_workbench_live.ex (approve/reject modals) — pick one reachable seeded modal surface
    - 40-CONTEXT.md D-11 (modal assertion set: tab-in · trap · Esc · restore), D-04
  </read_first>
  <action>
    Create `priv/dev/e2e/modal_focus.spec.mjs` extending the same keyboard-driving pattern against a reachable modal (the workflow promote modal or a prompt release modal — choose the most deterministically openable seeded surface). Assert (THROWING `expect()`, atomic with Task 1's fix): tab-in (focus inside on open), trap (Tab/Shift+Tab wrap, never background), Esc closes, restore-to-trigger (`activeElement === opener`). Use `waitForReady`. No live-patch collector needed here (that risk is drawer-specific, D-13). Keep it a pure hard-gate spec — it only ships because its fix landed in Task 1.
  </action>
  <verify>
    <automated>mix scoria.ui.e2e --base-url http://localhost:4799/scoria 2>&1 | tail -20  # runs modal_focus.spec.mjs in the e2e lane (requires make dev); OR CI e2e job</automated>
  </verify>
  <acceptance_criteria>
    `modal_focus.spec.mjs` passes tab-in / trap / Esc / restore-to-trigger against the Task 1 fix. All assertions throwing (fix already landed same plan). No warning-grade collector, no banned mechanisms.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard user → overlay | Focus must stay inside a modal overlay (`aria-modal`) and restore to the trigger — a focus escape behind the scrim is the operability defect being closed. |
| live PubSub broadcast → open drawer DOM | An unrelated broadcast can `phx-update` the open approval drawer mid-review (D-13). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-04 | Denial of service (operability) | drawer/modal focus | high | mitigate | `focus_wrap` traps + `push_focus`/`pop_focus` restore; drawer/modal specs prove tab-in/trap/Esc/restore fix-and-assert atomic so regressions red the required gate. |
| T-40-05 | Repudiation (lost focus mid-decision) | live-patch on approval drawer | medium | mitigate | D-13 warning-grade collector surfaces focus-drop on live patch into the playwright-report; if `focus_wrap` alone does not hold it, the private restore hook keyed on the existing id + stable per-approval DOM ids (Phase-39 D-14) handle it; Phase 41 hardens to blocking. |
| T-40-06 | Tampering (premature gate) | new e2e assertions | high | mitigate | Two-bucket rule: throwing `expect()` only on surfaces fixed in this same plan (Tasks 1→2/3 atomic); the not-yet-hardened live-patch survival is a non-throwing collector — no bare `expect()` merges ahead of its fix. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean after the fix.
- `mix test test/scoria_web/a11y_structural_guard_test.exs test/scoria_web/ui_component_test.exs` stay green (no attr/slot/role/aria regression).
- e2e lane (`mix scoria.ui.e2e`) green incl. `drawer_focus.spec.mjs` + `modal_focus.spec.mjs`; live-patch collector warns without failing.
- Every enumerated opener issues `JS.push_focus()` (re-grep confirms no missed site).
</verification>

<success_criteria>
`drawer/1` and `modal/1` trap + restore focus with zero vocabulary change; the drawer + modal keyboard specs pass fix-and-assert atomic; SC 2.4.11 rect non-overlap proven; D-13 live-patch survival is a non-throwing collector. A11Y-01 keyboard-operability on the highest-stakes surfaces is literally true at plan close.
</success_criteria>

<output>
Create `.planning/phases/40-accessibility-motion-and-responsive-proof/40-03-SUMMARY.md` when done.
</output>
