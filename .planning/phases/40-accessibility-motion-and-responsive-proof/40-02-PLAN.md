---
phase: 40-accessibility-motion-and-responsive-proof
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/scoria_web/motion_drift_guard_test.exs
  - test/scoria_web/a11y_structural_guard_test.exs
autonomous: true
requirements: [MOTION-01, A11Y-01, A11Y-02]
must_haves:
  truths:
    - "A browserless (async, sub-second) motion source-scan guard proves: no `transition: all`; every animation uses tokenized dur+ease EXCEPT the two allow-listed keyframes; keyframes animate only transform/opacity/border-color; no new @keyframes outside 05-motion.css (D-19)."
    - "The motion guard allow-lists BOTH `scoria-skeleton-pulse` AND `scoria-approval-pulse` BY ANIMATION-NAME (research delta #3 / addendum) — allow-listing only the skeleton is a false-RED on landing day."
    - "A browserless a11y structural guard proves: every icon-only button has an accessible name; no color-only status (every badge carries text/icon — primary owner of brand §7/§9); native-semantics presence (`<details>`, `role=`, `<dialog>`); and the A11Y-01 calm-surface structural contract (native `<details>/<summary>`, real `<button>`/`<a>` sort/filter, copy control `aria-label`+`aria-live`, form `<label for>`) (D-08/D-11/D-12)."
    - "Both guards are WARNING-GRADE this phase (collect offenders, this-phase assertion proves only the already-green baseline); they do NOT front-run Phase 41's blocking hardening; `ds06_drift_guard_test.exs` and `token_contrast_guard_test.exs` stay green (no vocabulary/token change)."
  artifacts:
    - test/scoria_web/motion_drift_guard_test.exs
    - test/scoria_web/a11y_structural_guard_test.exs
  key_links:
    - "Allow-list keyed on animation NAME (not literal duration string) so it survives a future duration edit on either exception (research Open Question #1)."
    - "Guards scan `assets/css/**` and `lib/scoria_web/**` respectively via `Path.wildcard` + `Regex.match?` + collect-offenders (the `ui_drift_guard_test.exs` idiom) — no browser, gate every PR."
---

<objective>
Author the two browserless ExUnit source-scan guards (Lane A) that gate every PR sub-second without a browser: the MOTION-01 tokenization/keyframe guard and the A11Y-02/A11Y-01 structural-presence guard. These are the "structural presence" owners in the D-07 coverage map — complementary to axe (computed) and keyboard-e2e (behavior).

Purpose: Prove MOTION-01's tokenization discipline and A11Y-02's structural invariants (accessible names, no color-only status, native semantics) as fast pure-Elixir scans, modeled on the existing `ui_drift_guard_test.exs` idiom. Both are warning-grade this phase — they lock the already-green baseline and collect offenders, but do not become the blocking hardening (that is Phase 41).
Output: `test/scoria_web/motion_drift_guard_test.exs`, `test/scoria_web/a11y_structural_guard_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-accessibility-motion-and-responsive-proof/40-CONTEXT.md
@.planning/phases/40-accessibility-motion-and-responsive-proof/40-RESEARCH.md
@.planning/phases/40-accessibility-motion-and-responsive-proof/40-VALIDATION.md
@test/scoria_web/ui_drift_guard_test.exs
@test/scoria_web/ds06_drift_guard_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: MOTION-01 tokenization + keyframe source-scan guard</name>
  <read_first>
    - test/scoria_web/ui_drift_guard_test.exs (the clean single-assertion idiom: `Path.wildcard` + per-file `Regex.match?` + collect offenders + one assert — reuse this structure)
    - assets/css/05-motion.css (tokenized keyframes; reduced-motion kill switch ~71; `scoria-approval-pulse` border-color exception ~35; `.scoria-attention { animation: scoria-approval-pulse 600ms var(--scoria-ease-in-out) 2 }` ~46 — the SECOND non-tokenized decl, duration only)
    - assets/css/04-components.css (`animation: scoria-skeleton-pulse 1.5s ease-in-out infinite` at line 1610 — the FIRST non-tokenized decl; tokenized paint-prop transitions at 1012/1277 that are legitimate and must NOT be flagged)
    - 40-RESEARCH.md § Anchors (600ms addendum), § Already-Green Confirmations (items 1-5), § Common Pitfalls (keyframe-vs-transition split)
    - 40-CONTEXT.md D-19, D-20, D-21
  </read_first>
  <action>
    Create `test/scoria_web/motion_drift_guard_test.exs` modeling `ui_drift_guard_test.exs`. Assert four warning-grade invariants over `assets/css/**` (glob via `Path.wildcard`, strip comment lines before counting per grep-gate hygiene): (i) zero `transition` shorthand or `transition-property` whose value is the all-properties keyword (already green — confirms it stays zero); (ii) every `animation:` declaration uses `--scoria-dur-*` AND `--scoria-ease-*` EXCEPT declarations whose animation-NAME is in a two-entry allow-list — allow-list `scoria-skeleton-pulse` (D-20 opacity-only skeleton, non-tokenized 1.5s at 04-components.css:1610) AND `scoria-approval-pulse` (D-21, non-tokenized 600ms at 05-motion.css:46). Key the allow-list on the animation name, not the literal duration string, so a future duration edit does not silently defeat it (research Open Question #1). Missing the second entry produces a false-RED the moment the guard lands; both MUST be present. (iii) every `@keyframes` body animates ONLY `transform`, `opacity`, or `border-color` (border-color is the documented D-21 exception) — this is a KEYFRAME rule only; transitions legitimately animate paint props like `background-color`/`box-shadow`/`filter` (04-components.css:1012/1277) and must NOT be flagged. (iv) no `@keyframes` at-rule appears in any file other than `assets/css/05-motion.css`. Each assertion collects offenders and asserts the offender list equals the empty (already-green) baseline. Keep it warning-grade: prove the current green state, do not add a blocking ratchet beyond baseline (Phase 41 hardens).
  </action>
  <verify>
    <automated>mix test test/scoria_web/motion_drift_guard_test.exs --color</automated>
  </verify>
  <acceptance_criteria>
    `motion_drift_guard_test.exs` passes green on the current tree; both `scoria-skeleton-pulse` and `scoria-approval-pulse` are allow-listed by name; keyframe restriction is transform/opacity/border-color only; the new-@keyframes-outside-05-motion.css check passes. `ds06_drift_guard_test.exs` and `token_contrast_guard_test.exs` remain green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: A11Y structural-presence guard (icon-button names, no color-only status, native semantics, calm-surface contract)</name>
  <read_first>
    - test/scoria_web/ui_drift_guard_test.exs (scan idiom to reuse)
    - lib/scoria_web/ui.ex (`badge/1`, `field/1`, `raw_evidence/1` native `<details>`, copy controls, `modal/1` ~708, `drawer/1` ~761 role/aria, `table/1` ~1256 with `.scoria-table__viewport tabindex="0"` ~1273)
    - 40-CONTEXT.md D-07 (coverage map — source-scan is PRIMARY OWNER of status-not-color-only), D-08, D-11 (calm-surface browserless contract list), D-12 (browserless-vs-browser line)
    - brandbook/brand-book.md §7/§9 (status not color-only)
  </read_first>
  <action>
    Create `test/scoria_web/a11y_structural_guard_test.exs` scanning `lib/scoria_web/**/*.ex` (+ `.heex` if present) with the collect-offenders idiom. Assert warning-grade structural presence: (a) every icon-only button (icon_button / button rendering only an icon with no visible text) carries an accessible name — an `aria-label` or a visually-hidden text node; (b) no color-only status — every status/badge surface carries a text or icon affordance alongside color (this guard is the PRIMARY OWNER of the brand §7/§9 status-not-color-only invariant, D-08); (c) native-semantics presence — disclosures use native `<details>/<summary>` (never a JS-toggled div), overlays expose `role="dialog"` + `aria-modal` + a label association + Escape key binding, tables keep the scroll viewport `tabindex` reachable and real `<button>`/`<a>` sort/filter controls; (d) copy controls expose an accessible name + a live-region node; forms associate labels via `for`, mark required via sr-only text, and render icon+text errors (not color-alone). Follow the browserless-vs-browser line (D-12): this guard asserts STRUCTURE/semantics presence only — behavior/geometry/focus/tab-order are Playwright's job (Plan 03), so do not attempt to assert actual focus movement here. Collect offenders per assertion; assert against the current (already-accessible-by-construction) baseline. Keep warning-grade — do not add a blocking ratchet beyond baseline (Phase 41 hardens). If a genuine structural gap surfaces whose only fix crosses a locked-vocabulary boundary, register it in `40-GAP-REGISTER.md` rather than changing an `attr`/`slot`.
  </action>
  <verify>
    <automated>mix test test/scoria_web/a11y_structural_guard_test.exs --color</automated>
  </verify>
  <acceptance_criteria>
    `a11y_structural_guard_test.exs` passes green on the current tree; it owns the no-color-only-status assertion; it asserts icon-button accessible names, native-semantics presence, and the A11Y-01 calm-surface structural contract (tables/disclosures/copy/forms) as structure-only checks. Any newly-surfaced out-of-scope gap is recorded in the gap register, not fixed by vocabulary change.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → source tree | Guards read (never write) `assets/css/**` and `lib/scoria_web/**`; no runtime surface, no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-02 | Tampering (false-green) | motion/a11y guards | medium | mitigate | Guards assert against an explicit already-green baseline with collect-offenders; the motion allow-list is keyed on animation name (not a wildcard) so it cannot silently swallow a new non-tokenized animation; strip comment lines before counting (grep-gate hygiene) so header prose cannot self-invalidate a count gate. |
| T-40-03 | Denial of service (CI flake) | Lane A async scans | low | accept | Pure file reads, deterministic regex, sub-second; no network/browser — no flake surface. |
</threat_model>

<verification>
- `mix test test/scoria_web/motion_drift_guard_test.exs` green.
- `mix test test/scoria_web/a11y_structural_guard_test.exs` green.
- `mix test test/scoria_web/ds06_drift_guard_test.exs test/scoria_web/token_contrast_guard_test.exs` stay green (no regression).
</verification>

<success_criteria>
Both browserless guards exist, pass green on the current tree, are warning-grade (baseline-locking, not blocking-hardening), and honor the two-entry motion allow-list + the status-not-color-only ownership. No locked-vocabulary or token change introduced.
</success_criteria>

<output>
Create `.planning/phases/40-accessibility-motion-and-responsive-proof/40-02-SUMMARY.md` when done.
</output>
