---
phase: 56-tool-declared-trifecta-classification-per-run-rails
plan: 02
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - lib/scoria/mcp/executor.ex
  - lib/scoria/observe/semconv.ex
  - test/scoria/mcp/classification_test.exs
  - test/scoria/mcp/executor_test.exs
  - test/scoria/mcp/executor_telemetry_test.exs
  - test/scoria/observe/semconv_test.exs
autonomous: true
requirements: [CLASS-02]
must_haves:
  truths:
    - "`config :scoria, :require_tool_classification` defaults to `false`; with the flag absent or `false`, an undeclared tool still runs and only telemetry fires — behavior is byte-identical to before this phase (D-03)."
    - "With `config :scoria, :require_tool_classification, true`, `MCP.Executor.execute/4` refuses an undeclared tool with `{:error, %{status: :unclassified_tool, ...}}` in the same envelope shape as the existing `:access_denied` path at `executor.ex:519-527`, and the tool is never invoked (D-03)."
    - "The strict refusal path emits NO `[:scoria, :class, :unclassified]` event — telemetry fires only on the ungated path, mirroring `Scoria.Runtime.ReleaseGate.handle_missing_verdict/1` at `release_gate.ex:81-93` (D-03)."
    - "Every resolved classification is persisted to `step.result_envelope[\"scoria.classification\"]` through the existing jsonb `fragment(\"? || ?\")` choke point, carrying `source` so Phase 57 can give `:unclassified_default` a disposition separate from `:tool_declared` (D-03, D-06)."
    - "Persistence is best-effort in the same discipline as `persist_taint_to_step/3`: a context with no `:step_id`, or no matching step row, is `:ok` and never an error (D-03)."
    - "`Scoria.Observe.Semconv.classification_attributes/1` is a fixed-key projector over exactly five `scoria.classification.*` keys and never spreads its input map, so a free-text or score-shaped field is structurally impossible to emit (SEC-01 discipline, mirrors `trust_attributes/1` at `semconv.ex:556-563`)."
    - "The five new keys are registered in `attribute_registry/0` — `action_class` and `source` as `:enum`, the three trifecta legs as `:flag` — and `test/scoria/observe/semconv_test.exs:274`'s canary list is edited in the same commit (RESEARCH.md Pitfall 4)."
    - "Classification attributes reach telemetry by merging into the EXISTING `[:scoria, :tool, :completed]` metadata alongside `trust_attributes/1` — no second span is created (Phase 55 D-21 discipline)."
  prohibitions:
    - "The strict-mode refusal MUST NOT be reachable by default. Any change that makes `require_tool_classification` default to `true`, or that couples it to another flag, brings down every adopter whose tools predate this phase."
---

<objective>
Complete CLASS-02: give the fail-closed-but-inspectable default its enforcement path (the opt-in `require_tool_classification` refusal), make the resolved classification durable on the step's `result_envelope` so Phase 57's escalation decisions are replayable, and project it onto the closed trace-attribute registry.

Purpose: without the flag, CLASS-02 ships pure telemetry with no enforcement path at all; without persistence, `source` exists only in memory and D-06's Phase-57 cascade mitigation is impossible. Both are named in 56-CONTEXT.md D-03 as "do not defer".

Output: the `require_tool_classification` refusal branch, `persist_classification_to_step/3`, the `scoria.classification.*` fixed-key projector plus registry entries, and the deliberate `attribute_registry/0` canary edit.
</objective>

<artifacts_this_phase_produces>
Symbols created by THIS PHASE (full list in `56-01-PLAN.md` `<artifacts_this_phase_produces>`; the subset created by THIS PLAN):
- `config :scoria, :require_tool_classification` (config key, default `false`)
- `{:error, %{status: :unclassified_tool, ...}}` refusal envelope from `Scoria.MCP.Executor.execute/4`
- `Scoria.MCP.Executor.persist_classification_to_step/3` (private)
- `step.result_envelope["scoria.classification"]` persisted jsonb key
- `Scoria.Observe.Semconv.classification_keys/0`, `Scoria.Observe.Semconv.classification_attributes/1`, `@classification_keys`
- Attribute keys `scoria.classification.action_class`, `scoria.classification.source`, `scoria.classification.reads_private_data`, `scoria.classification.sees_untrusted_content`, `scoria.classification.can_exfiltrate`

Consumed from plan 56-01 (already minted there, not new here): `Scoria.MCP.Classification` and its struct fields, `action_classes/0`, `default_action_class/0`, `normalize_action_class/1`, `resolve/2`, `tool_declaration/1`, `unclassified_default/0`, the `classification/0` callback, the `use Scoria.MCP.Tool` macro, `[:scoria, :class, :unclassified]`, `[:scoria, :class, :precedence_conflict]`, the `:tool_classification` context/seam key.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-tool-declared-trifecta-classification-per-run-rails/56-CONTEXT.md
@.planning/phases/56-tool-declared-trifecta-classification-per-run-rails/56-RESEARCH.md
@.planning/phases/56-tool-declared-trifecta-classification-per-run-rails/56-PATTERNS.md
@.planning/phases/56-tool-declared-trifecta-classification-per-run-rails/56-VALIDATION.md
@.planning/phases/56-tool-declared-trifecta-classification-per-run-rails/56-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `require_tool_classification` strict refusal path</name>
  <precondition>Plan 56-01 has landed: `Scoria.MCP.Executor.execute/4` already routes through `resolve_classification/2` shaped as a `case` with an `{:ok, context}` branch and an `{:error, envelope}` branch, and `Scoria.MCP.Classification.resolve/2` returns a struct whose `source` is one of `:tool_declared`, `:host_tightened`, `:unclassified_default`.</precondition>
  <reversibility rating="costly">`require_tool_classification` is a published config key and `:unclassified_tool` is a published error `status` an adopter will pattern-match on; renaming either later is a breaking change for anyone who opted in. Locked in 56-CONTEXT.md D-03.</reversibility>
  <files>lib/scoria/mcp/executor.ex, test/scoria/mcp/classification_test.exs, test/scoria/mcp/executor_test.exs</files>
  <read_first>
    - `lib/scoria/runtime/release_gate.ex:81-93` (`handle_missing_verdict/1`) — the exact precedent shape: `Application.get_env(:scoria, :require_eval_verdict, false)` gating a structured `{:error, _}` return, with `:telemetry.execute/3` firing ONLY on the else (ungated) branch. Mirror this literally, including which branch is silent.
    - `lib/scoria/mcp/executor.ex:494-536` (`maybe_capture_sensitive_mcp_access/3`) and specifically the `:access_denied` envelope at `:519-527` — the envelope SHAPE the refusal must mirror: a map with `status:`, a reason field, `trace_id:`, and `policy_key:`.
    - `lib/scoria/mcp/executor.ex:26-36` and `:95-99` — where `{:error, envelope}` returns already flow, and `emit_access_denied_telemetry/3` at `:620-631` which pattern-matches `%{status: :access_denied}` and falls through for other statuses (so the new status will not accidentally emit SRE latency/reliability telemetry).
    - `lib/scoria/mcp/classification.ex` (plan 56-01) — `resolve/2` and the `source` values.
    - `config/test.exs` — how this repo sets `config :scoria, ...` in the test env, so the flag can be toggled per-test via `Application.put_env/3` + an `on_exit` restore rather than a permanent config change.
  </read_first>
  <behavior>
    - Flag absent: `Executor.execute(UndeclaredTool, args, %{})` runs the tool and returns its normal result; one `[:scoria, :class, :unclassified]` event fires.
    - Flag explicitly `false`: identical to absent.
    - Flag `true` and the tool declares `classification/0`: unchanged — the tool runs, no refusal, no unclassified event.
    - Flag `true` and the tool does NOT declare: `execute/4` returns `{:error, %{status: :unclassified_tool}}` carrying a reason code, `tool_ref`, `trace_id`, and `policy_key`; `tool_module.execute/2` is never invoked (proven by a fixture that sends a message to the test pid on invocation); and NO `[:scoria, :class, :unclassified]` event fires.
    - Flag `true` and the classification resolves from a host declaration only (`source: :host_tightened`): the call proceeds — the flag refuses only genuinely unclassified calls, i.e. `source == :unclassified_default`.
  </behavior>
  <action>
    In `lib/scoria/mcp/executor.ex`, populate the `{:error, envelope}` branch of `resolve_classification/2` that plan 56-01 shaped. Read the flag with `Application.get_env(:scoria, :require_tool_classification, false)` — default-off, exactly like `release_gate.ex:82`'s `require_eval_verdict`. When the flag is truthy AND the resolved classification's `source` is `:unclassified_default`, return `{:error, %{status: :unclassified_tool, reason_code: "tool_classification_required", tool_ref: inspect(tool_module), trace_id: Map.get(context, :trace_id), policy_key: Map.get(context, :policy_key)}}` and do NOT emit `[:scoria, :class, :unclassified]` — the telemetry belongs to the ungated branch only, mirroring the precedent. On every other path return `{:ok, context}` unchanged.

    Gate on `source == :unclassified_default` specifically, not on "was there a tool declaration" — a host-only declaration is a real classification, so the flag must not refuse it. Refusal happens before `replay_gate/3` and therefore before any budget reservation, audit insert, or tool Task, so a refused call has zero side effects.

    Verify the new status does not leak into the SRE telemetry paths: `emit_access_denied_telemetry/3` at `executor.ex:620-631` matches `%{status: :access_denied}` with a catch-all fallback returning `:ok`, and the refusal returns directly from `execute/4` rather than through `execute_live/4`'s `else` clause, so no latency/reliability/budget event is emitted for a refused call. Confirm this by reading, and assert it in a test rather than assuming it.

    Extend `test/scoria/mcp/classification_test.exs` and `test/scoria/mcp/executor_test.exs` with each `<behavior>` row. Toggle the flag with `Application.put_env(:scoria, :require_tool_classification, true)` inside the test plus an `on_exit` that restores the prior value (or deletes the key when it was absent) — never a permanent `config/test.exs` change, which would flip the default for the whole suite.
  </action>
  <verify>
    <automated>mix test test/scoria/mcp/classification_test.exs test/scoria/mcp/executor_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "require_tool_classification" lib/scoria/mcp/executor.ex` matches and the same line contains the literal default `false`.
    - `grep -n "unclassified_tool" lib/scoria/mcp/executor.ex` matches.
    - `grep -rn "require_tool_classification" config/` returns nothing (the flag ships default-off with no config-file entry, so no adopter inherits it).
    - A test proves the flag-`true` + undeclared-tool case returns `{:error, %{status: :unclassified_tool}}` AND that the tool's `execute/2` was never called (assert the fixture's message was not received).
    - A test proves the flag-`true` refusal emits zero `[:scoria, :class, :unclassified]` events, while the flag-`false` path emits exactly one.
    - A test proves a host-only declaration (`source: :host_tightened`) is NOT refused under the flag.
    - `mix test test/scoria/mcp/classification_test.exs test/scoria/mcp/executor_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>CLASS-02 has a real enforcement path: default-off telemetry-only, opt-in hard refusal with zero side effects, mirroring the shipped `require_eval_verdict` precedent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist the resolved classification to `step.result_envelope`</name>
  <precondition>Postgres is reachable for the DB-backed executor tests (`SCORIA_DB_PORT`, pgvector; this worktree's convention is port 55432) — the persistence assertion reads a real `Scoria.Workflows.Step` row back through `Scoria.Repo`.</precondition>
  <reversibility rating="one-way">`step.result_envelope["scoria.classification"]` is a durable jsonb key Phase 57 reads to replay escalation decisions; renaming it later orphans every row already written. Locked in 56-CONTEXT.md D-03/D-06.</reversibility>
  <files>lib/scoria/mcp/executor.ex, test/scoria/mcp/executor_test.exs</files>
  <read_first>
    - `lib/scoria/mcp/executor.ex:302-341` (`persist_taint_to_step/3`) — the exact choke point to mirror: the `case Map.get(context, :step_id)` nil-is-not-an-error guard, the `from(step in Step, where: ..., update: [set: [result_envelope: fragment("? || ?", step.result_envelope, type(^%{...}, :map))]])` jsonb merge, `Repo.update_all([])`, and the surrounding `try/rescue _ -> :ok` best-effort discipline.
    - `lib/scoria/mcp/executor.ex:271-300` (`persist_taint/3` and `emit_taint_telemetry/3`) — how the taint pair is invoked and why it is best-effort.
    - `lib/scoria/mcp/executor.ex:26-36` — `execute/4`'s current shape after plan 56-01, to place the persistence call.
    - `lib/scoria/workflows/step.ex` — the `result_envelope` field type, confirming it is a jsonb map column.
    - `test/scoria/mcp/executor_test.exs` — the existing `scoria.taint` persistence test (if present) as the assertion template: create a run + step, call `execute/4` with `step_id` in context, reload the step, read the jsonb key.
  </read_first>
  <behavior>
    - `execute/4` with a `:step_id` in context persists `result_envelope["scoria.classification"]` as a map with string keys carrying `action_class`, `source`, `reads_private_data`, `sees_untrusted_content`, `can_exfiltrate`, and `tool_ref`.
    - `source` round-trips as a string (`"unclassified_default"` / `"tool_declared"` / `"host_tightened"`), never as an Elixir atom literal, so Phase 57 can branch on it after a jsonb read.
    - Persistence is a jsonb MERGE: a step whose `result_envelope` already carries `"scoria.taint"` retains it after the classification write.
    - `execute/4` with NO `:step_id` in context returns normally and writes nothing — not an error.
    - A `:step_id` that matches no row returns normally — not an error.
    - Classification is persisted on every resolved call, including calls that `replay_gate/3` later stubs or blocks, because persistence happens at resolution time and not on the success leg.
  </behavior>
  <action>
    Add a private `persist_classification_to_step/3` to `lib/scoria/mcp/executor.ex` as a near-identical sibling of `persist_taint_to_step/3` (`:302-341`): same `case Map.get(context, :step_id)` guard with `nil -> :ok`, same `from(step in Step, where: step.id == ^step_id, update: [set: [result_envelope: fragment("? || ?", step.result_envelope, type(^%{"scoria.classification" => data}, :map))]]) |> Repo.update_all([])`, same enclosing `try/rescue _ -> :ok`, same final `:ok`. Build `data` as a string-keyed map from the resolved struct's five fields plus `"tool_ref" => inspect(tool_module)`, converting `source` with `to_string/1` so it survives the jsonb round trip as a string.

    Call it from `resolve_classification/2`'s success path — i.e. at resolution time, immediately after the classification is resolved and before `replay_gate/3`. Do NOT hang it off `finalize_tool_result/5` like `persist_taint/3` is: taint is only meaningful for a completed `{:ok, value}` result, whereas Phase 57 needs the classification of blocked and stubbed calls too (D-06). Do not call it on the strict-refusal branch from Task 1 — a refused call never runs and has no step evidence to attach.

    Note for the SUMMARY, not a code change: this adds a second `Repo.update_all/2` per tool call alongside the existing taint write. Both are best-effort and both target the same row; merging them into one write would require moving taint persistence earlier than D-07's load-bearing ordering allows, so they stay separate.

    Extend `test/scoria/mcp/executor_test.exs` with each `<behavior>` row, including the merge-preserves-`scoria.taint` case and the no-`step_id` / unmatched-`step_id` best-effort cases.
  </action>
  <verify>
    <automated>mix test test/scoria/mcp/executor_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "scoria.classification" lib/scoria/mcp/executor.ex` matches.
    - `grep -c 'fragment("? || ?"' lib/scoria/mcp/executor.ex` returns 2 (the pre-existing taint write plus the new classification write; both use the same choke-point shape).
    - A test reloads a persisted `Scoria.Workflows.Step` and asserts `step.result_envelope["scoria.classification"]["source"] == "unclassified_default"` for an undeclared tool and `"tool_declared"` for a declaring tool.
    - A test asserts a step that already carried `result_envelope["scoria.taint"]` still carries it after the classification write (jsonb merge, not replace).
    - A test asserts `execute/4` with no `:step_id` in context returns the tool's normal result and raises nothing.
    - `mix test test/scoria/mcp/executor_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Every resolved classification — declared, host-tightened, or unclassified-default — is durable on the step's `result_envelope` with a string `source`, satisfying D-06's obligation to make Phase 57's separate disposition for `:unclassified_default` possible without implementing Phase 57's branching here.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `scoria.classification.*` fixed-key projector, registry entries, and the deliberate canary edit</name>
  <reversibility rating="one-way">`attribute_registry/0` is the closed, single-origin registry of every attribute key Scoria may persist (SEC-01). The five dotted key strings become part of adopters' trace vocabulary; renaming them later breaks any dashboard or exporter reading them. Naming follows the shipped `scoria.trust.*` / `scoria.spotlight.*` convention — rating recorded, no gate.</reversibility>
  <files>lib/scoria/observe/semconv.ex, lib/scoria/mcp/executor.ex, test/scoria/observe/semconv_test.exs, test/scoria/mcp/executor_telemetry_test.exs</files>
  <read_first>
    - `lib/scoria/observe/semconv.ex:276-291` (`@trust_keys` + `trust_keys/0`) and `:553-565` (`trust_attributes/1`) — the exact keyword-list-plus-`Enum.reduce` projector shape to mirror; note it drops `nil` values and never spreads the input map.
    - `lib/scoria/observe/semconv.ex:240-251` (`@attribute_classes ~w(id count enum flag timestamp structured)a`) — the closed six-value class vocabulary. `action_class` and `source` are `:enum`; the three boolean legs are `:flag`.
    - `lib/scoria/observe/semconv.ex:362-398` (`@attribute_registry`) — the `Keyword.fetch!(@trust_keys, :tier) => :enum` entry style the new rows must follow, and the exact position in the merged literal.
    - `test/scoria/observe/semconv_test.exs:274-315` — the `"attribute_registry/0 registry canary (SEC-01 Test 1)"` test asserting the full sorted key list. This canary is DESIGNED to go RED on any new key; the correct response is to edit the expected list in the same commit, never to revert the registry addition (RESEARCH.md Pitfall 4).
    - `test/scoria/observe/semconv_test.exs:317-333` — the `attribute_classes/0` exhaustiveness tests that will also read the new entries, confirming `:enum` and `:flag` are legal values.
    - `lib/scoria/mcp/executor.ex:56-75` — the `[:scoria, :tool, :completed]` emission where `trust_attrs` is already merged into `metadata`; the classification attributes join the SAME merge (no second span, Phase 55 D-21).
    - `test/scoria/mcp/executor_telemetry_test.exs:1-40` — the telemetry-handler attach/assert harness to extend.
  </read_first>
  <behavior>
    - `Semconv.classification_keys()` returns a keyword list mapping `:action_class`, `:source`, `:reads_private_data`, `:sees_untrusted_content`, `:can_exfiltrate` to `"scoria.classification.action_class"`, `"scoria.classification.source"`, `"scoria.classification.reads_private_data"`, `"scoria.classification.sees_untrusted_content"`, `"scoria.classification.can_exfiltrate"`.
    - `Semconv.classification_attributes(%{action_class: "admin", source: "unclassified_default", reads_private_data: true, sees_untrusted_content: true, can_exfiltrate: true})` returns exactly those five dotted keys and nothing else.
    - `classification_attributes/1` drops `nil`-valued fields and IGNORES any unlisted key in its input — passing a map with an extra `score:` or `reason:` field emits no additional attribute.
    - `Semconv.attribute_registry()` contains all five keys, with `"scoria.classification.action_class"` and `"scoria.classification.source"` classed `:enum` and the three legs classed `:flag`.
    - The `[:scoria, :tool, :completed]` telemetry event's metadata carries the five `scoria.classification.*` keys alongside the existing `scoria.trust.*` keys, and no new span or event name is introduced.
    - `false`-valued legs are still emitted (the projector drops `nil`, not `false`) — an explicit `false` is a real declaration and must be distinguishable from absence.
  </behavior>
  <action>
    In `lib/scoria/observe/semconv.ex`, add `@classification_keys` as a keyword list beside `@trust_keys` (`:276-281`) with the five field-to-dotted-key pairs named in `<behavior>`, plus a `@doc`'d `classification_keys/0` accessor matching `trust_keys/0`'s shape. Add `classification_attributes/1` beside `trust_attributes/1` (`:553-565`) using the identical `Enum.reduce(@classification_keys, %{}, fn {field, key}, acc -> case Map.get(input, field) ...)` shape — the fixed-key discipline is what makes a free-text or score-shaped field structurally impossible to emit, so do NOT spread the input map and do NOT use `Map.take/2` at call sites. Keep the `nil`-drop semantics but ensure `false` is NOT dropped: the existing `trust_attributes/1` matches only `nil` in its skip clause, so mirror it exactly rather than testing truthiness.

    Register the five keys in `@attribute_registry` (`:362-398`) using the `Keyword.fetch!(@classification_keys, :field) => :class` style already used for `@trust_keys`, placing them immediately after the `@spotlight_keys` / `@trust_keys` block. Classes: `action_class` and `source` are `:enum`; `reads_private_data`, `sees_untrusted_content`, and `can_exfiltrate` are `:flag`. All five classes are existing members of `attribute_classes/0` — do not add a seventh class.

    Edit `test/scoria/observe/semconv_test.exs:274`'s canary list, adding the five keys in sorted position. In the pinned sorted list they fall between `"scoria.attributes.truncated_keys"` and `"scoria.guardrail.decision"`, in this order: `"scoria.classification.action_class"`, `"scoria.classification.can_exfiltrate"`, `"scoria.classification.reads_private_data"`, `"scoria.classification.sees_untrusted_content"`, `"scoria.classification.source"`. This edit is the intended, deliberate response to the SEC-01 closed-registry guarantee — a RED canary here is the guard working, not a regression to work around.

    In `lib/scoria/mcp/executor.ex`, merge `Semconv.classification_attributes/1` into the SAME `[:scoria, :tool, :completed]` metadata map that already merges `trust_attrs` (`:69-73`). Source the input from the resolved struct on `context[:tool_classification]` via `Map.from_struct/1`, converting `source` with `to_string/1` so the emitted value is a string enum. Do not create a new span, a new event name, or a second emission — Phase 55's D-21 no-second-span discipline applies unchanged.

    Extend `test/scoria/observe/semconv_test.exs` with direct projector tests (the five-key projection, the unlisted-key-ignored case, the `false`-is-emitted case) and `test/scoria/mcp/executor_telemetry_test.exs` with an assertion that a completed tool call's `[:scoria, :tool, :completed]` metadata carries the five classification keys.

    Record in the SUMMARY, not as a code change: `Scoria.Observe.Adapters.MCP.emit_tool_span/4` builds span attributes from a fixed 8-key literal plus `merge_host_declared/2` and never spreads metadata, so these attributes reach live telemetry handlers but are dropped from the PERSISTED span — the same known gap `scoria.trust.*` already has. That is the phase-55 follow-up recorded in 56-CONTEXT.md `<deferred>`, explicitly out of scope here.
  </action>
  <verify>
    <automated>mix test test/scoria/observe/semconv_test.exs test/scoria/mcp/executor_telemetry_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "@classification_keys" lib/scoria/observe/semconv.ex` matches and `grep -n "def classification_attributes" lib/scoria/observe/semconv.ex` matches.
    - `grep -c "scoria.classification." lib/scoria/observe/semconv.ex` returns at least 5.
    - `grep -c "scoria.classification." test/scoria/observe/semconv_test.exs` returns at least 5 (the canary list was edited, not the registry reverted).
    - `mix test test/scoria/observe/semconv_test.exs --warnings-as-errors` exits 0 — this proves both the edited canary list and the `attribute_classes/0` exhaustiveness check pass with the new `:enum`/`:flag` entries.
    - `grep -n "classification_attributes" lib/scoria/mcp/executor.ex` matches, and the call appears inside the existing `[:scoria, :tool, :completed]` emission rather than in a new `:telemetry.execute/3` call — verified by `grep -c ':telemetry.execute(' lib/scoria/mcp/executor.ex` being unchanged from its pre-task value except for any event added by Task 1/2 (record the before and after counts in the SUMMARY).
    - A projector test asserts a map containing an extra unlisted field emits exactly five keys.
    - A projector test asserts a `false`-valued leg IS emitted (only `nil` is dropped).
    - `mix test test/scoria/mcp/executor_telemetry_test.exs --warnings-as-errors` exits 0 and includes the `[:scoria, :tool, :completed]` classification-attribute assertion.
  </acceptance_criteria>
  <done>The five `scoria.classification.*` keys are registered in the closed registry with legal classes, projected by a fixed-key function that cannot leak prose, merged into the existing tool-completed event with no second span, and the SEC-01 canary was deliberately edited rather than worked around.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolved classification → persisted jsonb / trace attributes | anything crossing here becomes durable adopter-visible data and must be closed-vocabulary |
| adopter deploy config → executor enforcement | a default-on strict flag would refuse production traffic for every pre-phase-56 adopter |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-01 | Elevation of Privilege | `require_tool_classification` refusal branch | high | mitigate | The enforcement path for silent under-classification: with the flag on, an unclassified tool cannot execute at all, and the refusal happens before any budget reservation, audit insert, or tool Task, so a refused call has zero side effects. Default-off preserves every existing adopter. Task 1 tests. ASVS V4. |
| T-56-03 | Denial of Service (of the security signal) | `source` persistence to `step.result_envelope` | medium | mitigate | If every legacy call resolved all-three-legs-true with no way to tell it apart from a real declaration, Phase 57's confluence gate would fire on 100% of legacy traffic and adopters would permanently filter the signal. `source` is `@enforce_keys` (56-01) and now durable as a string on the step, so Phase 57 can give `:unclassified_default` an operator-selectable disposition separate from `:tool_declared`. Task 2 tests. This phase makes the distinction possible and does NOT implement Phase 57's branching (D-06). |
| T-56-04 | Information Disclosure | `classification_attributes/1` projector + `attribute_registry/0` | medium | mitigate | Fixed-key projection over exactly five closed-vocabulary keys, never spreading the input map, so a score-shaped or free-text field is structurally impossible to emit; the closed registry plus the SEC-01 canary means a Scoria developer cannot add a key without a deliberate, reviewed test edit. Task 3 tests. |
</threat_model>

<verification>
- `mix test test/scoria/mcp/classification_test.exs test/scoria/mcp/executor_test.exs test/scoria/mcp/executor_telemetry_test.exs test/scoria/observe/semconv_test.exs --warnings-as-errors` exits 0.
- `mix compile --warnings-as-errors` exits 0.
- `mix test --warnings-as-errors` (full suite) green before this wave is considered merged.
- `grep -rn "require_tool_classification" config/` returns nothing.
</verification>

<success_criteria>
- ROADMAP Phase 56 Success Criterion 2 satisfied: an unclassified tool no longer silently resolves to `approval_sensitive: false`; it fails closed to an inspectable default, emits telemetry, is durable on the step for replay, and can be made to hard-refuse via an opt-in flag.
- CLASS-02 complete.
- D-06's cross-phase obligation discharged: `source` is enforced and persisted, so Phase 57 can distinguish `:unclassified_default` from `:tool_declared` without any phase-56 branching.
</success_criteria>

<output>
Create `.planning/phases/56-tool-declared-trifecta-classification-per-run-rails/56-02-SUMMARY.md` when done. Record the before/after `:telemetry.execute/3` call counts in `executor.ex` and the accepted phase-55 span-projection limitation.
</output>
