---
phase: 56-tool-declared-trifecta-classification-per-run-rails
plan: 03
type: execute
wave: 3
depends_on: ["56-01", "56-02"]
files_modified:
  - lib/scoria/mcp/classification.ex
  - lib/scoria/mcp/executor.ex
  - lib/scoria/connectors/invocation.ex
  - lib/scoria/workflows/runtime.ex
  - test/scoria/mcp/classification_test.exs
  - test/scoria/mcp/executor_test.exs
  - test/scoria/connectors/invocation_test.exs
  - test/scoria/workflows/replay_disposition_test.exs
autonomous: true
requirements: [CLASS-03]
must_haves:
  truths:
    - "All FIVE fail-open sites named in D-05 consume the resolved classification: `build_replay_seam/2` (site 1, plan 56-01), `policy_sensitive_invocation?/1` at `executor.ex:552-554` (site 2), `budget_required?/1` at `executor.ex:442-447` (site 3), `Connectors.Invocation.build_seam/2` at `invocation.ex:59-76` (site 4), and `Workflows.Runtime`'s `%{local_classification: :pure}` default at `runtime.ex:474` (site 5). Site 5 is NOT scoped out."
    - "Consumption is read-side and declared-only: no resolved value is ever written into `:policy_sensitive`, `:sensitive_tool`, `:approval_sensitive`, `:local_classification`, `:action_class`, or `:risk_level` at any site, and a classification whose `source` is `:unclassified_default` is never an operand of any widened predicate (D-03, D-04, planner decision D-A2)."
    - "Behavior for traffic that exists today is byte-identical: with no tool declaring `classification/0`, `policy_sensitive_invocation?/1` and `budget_required?/1` return exactly what they returned before this phase, and every replay disposition is unchanged."
    - "A tool that DOES declare `can_exfiltrate: true` or `action_class` in `[\"exec\", \"admin\"]` makes `policy_sensitive_invocation?/1` and `budget_required?/1` true even when the host passed neither `:policy_sensitive` nor `:sensitive_tool` — this is the fail-open seam actually closing for adopters who opt in."
    - "`Connectors.Invocation.invoke/4` resolves and injects the classification into `context` BEFORE `build_seam/2` and therefore before `replay_resolution/5` at `invocation.ex:26`, which runs before `Executor.execute/4` at `invocation.ex:45` — so connector-routed tools are not left uncovered."
    - "Resolution is single-shot across the connector path: `Executor.execute/4` reuses an already-present `:tool_classification` rather than re-resolving, so exactly one `[:scoria, :class, :unclassified]` event fires per connector-routed call, not two."
    - "`Workflows.Runtime.replay_execution/8`'s anonymous inline default seam is replaced by a single named, documented default that carries `Scoria.MCP.Classification.unclassified_default/0` under `:tool_classification` and emits one `[:scoria, :class, :unclassified]` event with `site: :workflow_runtime_step` when the caller supplied no `:replay_seam` — the bypass is now inspectable rather than silent."
    - "The site-5 default seam still resolves `{:execute_live, %{replay_reason_code: \"local_safe_to_rerun\"}}` — its `local_classification: :pure` entry is unchanged, so `ReplayDisposition`'s clause-3 short-circuit is preserved and no currently-live replay step becomes `:blocked`."
    - "No Ecto migration and no schema change exists anywhere in this phase."
  prohibitions:
    - "The `require_tool_classification` refusal MUST NOT be extended to sites 4 or 5 in this phase. D-03 scopes the refusal to `MCP.Executor` resolution; site 5 has no tool module to classify, and inventing a step-level refusal here would halt workflow replay for reasons no adopter opted into."
---

<objective>
Complete CLASS-03: make all five of D-05's fail-open sites consume the resolved classification, including the two the requirement text missed (`Connectors.Invocation.build_seam/2`, which decides replay BEFORE the executor is ever reached, and `Workflows.Runtime`'s `%{local_classification: :pure}` step-granularity bypass that D-05 calls "the worst").

Consumption is read-side and declared-only per planner decision D-A2, because D-05's "must consume" and D-03's hard write-prohibition reconcile exactly one way. The consequence is stated plainly rather than hidden: for the traffic that exists today nothing changes behaviorally, and the seam genuinely closes the moment an adopter declares a classification on their tool.

Output: `Classification.declared_sensitive?/1`, sites 2 and 3 widened declared-only, `Connectors.Invocation` resolving before its own replay decision, and a named, telemetried, classification-carrying default seam in `Workflows.Runtime`.
</objective>

<artifacts_this_phase_produces>
Symbols created by THIS PHASE (full list in `56-01-PLAN.md` `<artifacts_this_phase_produces>`; the subset created by THIS PLAN):
- `Scoria.MCP.Classification.declared_sensitive?/1`
- `Scoria.Connectors.Invocation`'s classification injection into `context` under `:tool_classification`, and the `:tool_classification` entry in its `build_seam/2` output
- `Scoria.Workflows.Runtime`'s named default replay-seam helper (replacing the anonymous `%{local_classification: :pure}` literal at `runtime.ex:474`) and its `site: :workflow_runtime_step` telemetry metadata value

Consumed from plans 56-01 / 56-02 (already minted there, not new here): `Scoria.MCP.Classification` and its struct fields, `action_classes/0`, `normalize_action_class/1`, `resolve/2`, `tool_declaration/1`, `unclassified_default/0`, the `classification/0` callback, the `use Scoria.MCP.Tool` macro, `config :scoria, :require_tool_classification`, `[:scoria, :class, :unclassified]`, `[:scoria, :class, :precedence_conflict]`, the `:tool_classification` context/seam key, `step.result_envelope["scoria.classification"]`, the `scoria.classification.*` attribute keys.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-tool-declared-trifecta-classification-per-run-rails/56-CONTEXT.md
@.planning/phases/56-tool-declared-trifecta-classification-per-run-rails/56-RESEARCH.md
@.planning/phases/56-tool-declared-trifecta-classification-per-run-rails/56-PATTERNS.md
@.planning/phases/56-tool-declared-trifecta-classification-per-run-rails/56-VALIDATION.md
@.planning/phases/56-tool-declared-trifecta-classification-per-run-rails/56-01-SUMMARY.md
@.planning/phases/56-tool-declared-trifecta-classification-per-run-rails/56-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Site inventory re-verification + sites 2 and 3 consume the classification declared-only</name>
  <precondition>Postgres is reachable for the DB-backed executor tests (`SCORIA_DB_PORT`, pgvector; this worktree's convention is port 55432) — `test/scoria/mcp/executor_test.exs` is `async: false` and checks out an Ecto sandbox, and the budget-reservation assertions read real `Scoria.SRE.BudgetReservation` rows.</precondition>
  <reversibility rating="reversible">`declared_sensitive?/1`'s predicate (which declared shapes count as sensitive) is an internal, single-origin helper behind a stable name; tuning which legs trip it later is a local change with no persisted or published surface.</reversibility>
  <files>lib/scoria/mcp/classification.ex, lib/scoria/mcp/executor.ex, test/scoria/mcp/classification_test.exs, test/scoria/mcp/executor_test.exs</files>
  <read_first>
    - `lib/scoria/mcp/executor.ex:552-554` (`policy_sensitive_invocation?/1`) — currently `Map.get(context, :policy_sensitive) || Map.get(context, :sensitive_tool)`, nil-falsy with no explicit default. Also read `:538-550` (`ensure_policy_sensitive_invocation/4`) and `:556-589` (`policy_sensitive_audit_envelope/3`), both of which call the same predicate, and `:40-42` where an `{:error, _}` from the audit insert aborts the whole call.
    - `lib/scoria/mcp/executor.ex:442-447` (`budget_required?/1`) and its two call sites: `reserve_budget/3` at `:398-422` and the read-only `maybe_emit_budget/4` at `:681-696`.
    - `lib/scoria/mcp/classification.ex` (plans 56-01/56-02) — the struct, `sources/0`, `action_classes/0`.
    - 56-CONTEXT.md D-03's prohibition table (why `:policy_sensitive` / `:sensitive_tool` must not be written) and D-04 (why `:unclassified_default` is never a join operand).
    - `56-01-PLAN.md` `<planner_decisions>` D-A2 — the read-side, declared-only reconciliation this task implements.
    - `.planning/phases/56-tool-declared-trifecta-classification-per-run-rails/56-01-SUMMARY.md` — the Step-0 pre-flight grep inventory recorded there.
  </read_first>
  <behavior>
    - `Classification.declared_sensitive?(nil)` is `false`.
    - `Classification.declared_sensitive?(Classification.unclassified_default())` is `false` — `:unclassified_default` is never an operand.
    - `declared_sensitive?/1` is `true` for a struct with `source: :tool_declared` and `can_exfiltrate: true`, and for one with `action_class` in `["exec", "admin"]`; `false` for `source: :tool_declared` with all legs `false` and `action_class: "read"`.
    - `declared_sensitive?/1` behaves identically for `source: :host_tightened` as for `:tool_declared` — a host-tightened classification is a real declaration.
    - `Executor.execute/4` with an UNDECLARED tool and a context carrying neither `:policy_sensitive` nor `:sensitive_tool`: no `tool.invocation` audit-outbox row is written and no budget reservation is made — byte-identical to pre-phase behavior.
    - `Executor.execute/4` with a tool declaring `can_exfiltrate: true` and a context carrying neither `:policy_sensitive` nor `:sensitive_tool`: a `tool.invocation` audit-outbox row IS written and a budget reservation IS made.
    - A host that already passes `:policy_sensitive` or `:sensitive_tool` sees no change in either predicate's result.
  </behavior>
  <action>
    **Step 0 — closing re-verification of the site inventory (RESEARCH.md A2).** Re-run the pre-flight grep from plan 56-01 Task 1 (`grep -rn "local_classification\|approval_sensitive\|action_class" lib/scoria/workflows/ lib/scoria/connectors/ lib/scoria/mcp/`) and diff the result against the inventory recorded in `56-01-SUMMARY.md`. The expected inventory is four seam builders (`executor.ex:181`, `invocation.ex:59`, `runtime.ex:474`, `workflows.ex:994`) plus the two executor predicates at `:442` and `:552`, where `workflows.ex:994-1007` is a hardcoded MAXIMAL, fail-CLOSED seam and therefore out of scope. If anything new appears, STOP and report rather than widening this plan.

    **Add `declared_sensitive?/1` to `lib/scoria/mcp/classification.ex`.** It takes `nil` or a `%Classification{}` and returns a strict boolean. It returns `false` for `nil` and for any struct whose `source` is `:unclassified_default` — that guard is the whole reason legacy traffic is unaffected and is the direct expression of D-04's "the fallback is never an operand" law, so write it as an explicit clause and cover it with its own test rather than letting it fall out of the leg values. For a struct whose `source` is `:tool_declared` or `:host_tightened`, return true when `can_exfiltrate` is true or when `action_class` is a member of `Enum.drop(action_classes(), 2)` — i.e. `["exec", "admin"]`. Derive that subset from `action_classes/0` rather than writing a second literal list, and document in the function's `@doc` that the offset encodes "exec and above", so it moves correctly if the enum ever grows.

    **Widen site 2, `policy_sensitive_invocation?/1` (`executor.ex:552-554`), declared-only.** Add a third OR term reading `Classification.declared_sensitive?(Map.get(context, :tool_classification))`. Do NOT write into `:policy_sensitive` or `:sensitive_tool` — the first two terms stay byte-identical, so a host value still wins and a host-`false` is still falsy exactly as today. The predicate may now return a strict boolean rather than the raw truthy value; confirm both remaining call sites (`ensure_policy_sensitive_invocation/4` at `:538-550` and `policy_sensitive_audit_envelope/3` at `:556-589`) only branch on truthiness before changing the return type.

    **Widen site 3, `budget_required?/1` (`executor.ex:442-447`), declared-only.** Add the same `Classification.declared_sensitive?/1` term as a fifth OR operand alongside the four existing `Map.get/2` reads. Use the SAME helper, not a second inline predicate — one origin for "does this declaration count as sensitive" is what keeps sites 2 and 3 from drifting apart. Note that `budget_required?/1` also gates the read-only `maybe_emit_budget/4` at `:681-696`, so a declaring tool now emits budget telemetry too; that is intended and consistent.

    Extend `test/scoria/mcp/classification_test.exs` with the `declared_sensitive?/1` matrix (including the explicit `:unclassified_default` false case) and `test/scoria/mcp/executor_test.exs` with the four end-to-end rows: undeclared tool writes no audit row and reserves no budget; a `can_exfiltrate: true` declaring tool writes the audit row and reserves budget; an `action_class: "admin"` declaring tool does the same; and a host already passing `:policy_sensitive` is unaffected. Assert the audit row by querying `Scoria.SRE.AuditOutboxEvent` for `event_type: "tool.invocation"`.
  </action>
  <verify>
    <automated>mix test test/scoria/mcp/classification_test.exs test/scoria/mcp/executor_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def declared_sensitive?" lib/scoria/mcp/classification.ex` matches, and `grep -n "unclassified_default" lib/scoria/mcp/classification.ex` shows an explicit clause or guard inside `declared_sensitive?/1`'s body.
    - `grep -n "declared_sensitive?" lib/scoria/mcp/executor.ex` matches exactly twice — once in `policy_sensitive_invocation?/1` and once in `budget_required?/1`.
    - `grep -nE "Map\.put\((seam|context), :(approval_sensitive|local_classification|action_class|risk_level|policy_sensitive|sensitive_tool)" lib/scoria/mcp/executor.ex` returns nothing (D-03 hard prohibition, unchanged from plan 56-01).
    - `git diff lib/scoria/mcp/executor.ex` shows the pre-existing operands of both predicates unchanged — only an added OR term in each.
    - A test asserts `Classification.declared_sensitive?(Classification.unclassified_default()) == false`.
    - A test asserts an undeclared tool with a bare context writes zero `"tool.invocation"` audit-outbox rows and creates zero budget reservations.
    - A test asserts a `can_exfiltrate: true` declaring tool with the same bare context writes one `"tool.invocation"` row and creates one budget reservation.
    - `mix test test/scoria/mcp/classification_test.exs test/scoria/mcp/executor_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Sites 2 and 3 consume the resolved classification through one shared, declared-only predicate; today's traffic is provably unchanged and a declaring tool's audit and budget gates now fire on its own declaration instead of a host-passed default.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Site 4 — `Connectors.Invocation` resolves before its own replay decision</name>
  <precondition>Postgres is reachable (`SCORIA_DB_PORT`, this worktree's convention is port 55432) — `test/scoria/connectors/invocation_test.exs` is `async: false`, checks out an Ecto sandbox, and creates real `Scoria.Workflows` run and step rows in `setup`.</precondition>
  <reversibility rating="reversible">Injecting the classification into `context` before `build_seam/2` is an internal ordering change inside `invoke/4`; the observable contract (the `:tool_classification` key) was already fixed by plan 56-01.</reversibility>
  <files>lib/scoria/connectors/invocation.ex, lib/scoria/mcp/executor.ex, test/scoria/connectors/invocation_test.exs</files>
  <read_first>
    - `lib/scoria/connectors/invocation.ex:18-48` (`invoke/4`) — note the ordering that makes this site real: `build_seam/2` at line 21, `replay_resolution/5` at line 26, and `Executor.execute/4` only at line 45. A tool routed through here gets its replay decision made from THIS seam, before the executor's resolution ever runs.
    - `lib/scoria/connectors/invocation.ex:59-76` (`build_seam/2`) — the 18-line `Map.put_new/3` pipeline whose `action_class` defaults to `"read"`, `approval_sensitive` to `false`, and `local_classification` to `:read`. All three of those `put_new` lines must stay byte-identical.
    - `lib/scoria/connectors/invocation.ex:78-94` (`attach_replay_context/3`) — how context is enriched before `Executor.execute/4`, i.e. where the injected classification must survive to.
    - `lib/scoria/mcp/executor.ex` `resolve_classification/2` (plan 56-01) — specifically its idempotence guard: an already-present `%Classification{}` under `:tool_classification` is reused and re-emits no telemetry.
    - `test/scoria/connectors/invocation_test.exs:1-40` — the `ReplayTool` fixture (which messages `context.test_pid` when invoked) and the `setup` block creating a replay-mode run plus a step. That fixture is the ready-made proof that context reaches the executor intact.
  </read_first>
  <behavior>
    - `Invocation.invoke/4` with a declaring tool: the seam built at line 21 carries `:tool_classification` holding the resolved struct, and the four pre-existing keys (`:action_class`, `:risk_level`, `:approval_sensitive`, `:local_classification`) hold exactly the values they held before this phase.
    - `Invocation.invoke/4` with an undeclared tool: exactly ONE `[:scoria, :class, :unclassified]` event fires for the whole call — the connector resolves it and `Executor.execute/4` reuses it rather than resolving a second time.
    - The live path forwards the resolved classification: the `context` that reaches `Executor.execute/4` at line 45 (and therefore the tool's own `execute/2`) carries `:tool_classification`.
    - Every existing assertion in `test/scoria/connectors/invocation_test.exs` passes unedited — historical-stub, blocked, and execute-live dispositions are all unchanged.
  </behavior>
  <action>
    In `lib/scoria/connectors/invocation.ex`, resolve the classification inside `invoke/4` immediately after `normalize_map(context)` on line 19 and BEFORE `build_seam/2` on line 21, injecting the resolved `%Scoria.MCP.Classification{}` into `context` under `:tool_classification`. Route through the same `Scoria.MCP.Classification.resolve/2` the executor uses — do not duplicate resolution logic here; the whole point of D-05's single choke point is one implementation, reached from two entry points. Emit `[:scoria, :class, :unclassified]` on the undeclared path with metadata `site: :connector_invocation`, wrapped in `try/rescue -> :ok`.

    Add ONE new entry to `build_seam/2` (`:59-76`): a `Map.put_new(:tool_classification, Map.get(context, :tool_classification))` step in the existing pipeline. Its `Map.put_new(:action_class, ...)`, `Map.put_new(:risk_level, ...)`, `Map.put_new(:approval_sensitive, ...)`, and `Map.put_new(:local_classification, ...)` lines must be byte-identical afterward — those four are read directly by `ReplayDisposition.resolve/5` and overwriting any of them flips currently-`:execute_live` connector calls to `:blocked` (D-03, RESEARCH.md Pitfalls 1 and 2).

    Confirm the injected key survives into the executor: `attach_replay_context/3` (`:78-94`) pipes `context` through `Map.put/3` calls and preserves unknown keys, and its non-replay head returns `context` untouched, so no change is needed there — read it and verify rather than assuming, then assert it in a test.

    In `lib/scoria/mcp/executor.ex`, verify (and test) that `resolve_classification/2`'s idempotence guard from plan 56-01 actually holds for this path: a context arriving with a `%Classification{}` already under `:tool_classification` must be returned unchanged with no second telemetry emission. If plan 56-01's guard checks only key presence rather than struct shape, tighten it to match on the struct so a host passing an arbitrary value under that key cannot suppress resolution.

    Extend `test/scoria/connectors/invocation_test.exs` with each `<behavior>` row, including the single-emission assertion (attach a telemetry handler, invoke through `Invocation.invoke/4`, assert exactly one event) and an assertion that the tool's received `context` carries `:tool_classification`, using the existing `ReplayTool` fixture's `send(context.test_pid, {:tool_executed, args, context})` message.
  </action>
  <verify>
    <automated>mix test test/scoria/connectors/invocation_test.exs test/scoria/mcp/executor_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "tool_classification" lib/scoria/connectors/invocation.ex` matches in both `invoke/4` and `build_seam/2`.
    - `grep -nE "Map\.put_new\(:(action_class|risk_level|approval_sensitive|local_classification)" lib/scoria/connectors/invocation.ex` returns the same four lines with the same default values as before the task (`"read"`, `"low"`, `false`, `:read`).
    - `git diff lib/scoria/connectors/invocation.ex` shows `build_seam/2` gained exactly one pipeline step and shows no modification to those four lines.
    - A test asserts exactly ONE `[:scoria, :class, :unclassified]` event per `Invocation.invoke/4` call with an undeclared tool (the double-emission regression this task exists to prevent).
    - A test asserts the tool's received `context` (via the `ReplayTool` fixture message) carries a `%Scoria.MCP.Classification{}` under `:tool_classification`.
    - Every pre-existing test in `test/scoria/connectors/invocation_test.exs` passes with zero edits to its assertions.
    - `mix test test/scoria/connectors/invocation_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Connector-routed tools no longer bypass CLASS-03: the classification is resolved before the connector's own `replay_resolution/5` call, carried on the seam, forwarded to the executor, and resolved exactly once per call.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Site 5 — replace `Workflows.Runtime`'s anonymous `:pure` bypass with a named, telemetried, classification-carrying default</name>
  <reversibility rating="costly">`Workflows.Runtime.replay_execution/8`'s default seam governs step-granularity replay for every workflow handler that passes no `:replay_seam`. Changing its `local_classification` value — as opposed to what it carries alongside — would flip live replay steps to `:blocked` across every adopter, which is exactly why this task changes what the default CARRIES and not what it SAYS.</reversibility>
  <files>lib/scoria/workflows/runtime.ex, test/scoria/workflows/replay_disposition_test.exs</files>
  <read_first>
    - `lib/scoria/workflows/runtime.ex:464-500` (`replay_execution/8`) — line 474's `seam = Keyword.get(opts, :replay_seam, %{local_classification: :pure})` and the three disposition branches it feeds. Read enough of the surrounding function to see that `step` and `run` are in scope for telemetry metadata but that there is NO tool module here — this site guards a workflow STEP (a handler function), not a tool call, which is why it needs its own default rather than the executor's per-tool resolution.
    - `lib/scoria/workflows/replay_disposition.ex:28` (`pure_local?/1`, clause 3) and `:65-67` — why `:pure` short-circuits to `:execute_live` BEFORE clause 7's `effectful_or_remote?/1` is ever evaluated. Changing that value is the single fastest way to brick every replaying adopter (RESEARCH.md Pitfall 1).
    - `lib/scoria/runtime/release_gate.ex:81-93` — the "defaulted/ungated path emits telemetry, gated path is silent" precedent this site mirrors.
    - `lib/scoria/mcp/classification.ex` — `unclassified_default/0`.
    - `test/scoria/workflows/replay_disposition_test.exs` — the site-5 non-bricking regression added in plan 56-01 Task 2, which this task must keep green.
  </read_first>
  <behavior>
    - A caller that DOES pass `opts[:replay_seam]`: behavior is completely unchanged, and no `[:scoria, :class, :unclassified]` event fires (the caller made an explicit statement).
    - A caller that passes NO `:replay_seam`: the named default is used, it still carries `local_classification: :pure`, it additionally carries `:tool_classification` holding `Classification.unclassified_default/0`, and exactly one `[:scoria, :class, :unclassified]` event fires carrying `site: :workflow_runtime_step` plus the step and run identifiers.
    - The resulting disposition for the default seam is unchanged: `{:execute_live, %{replay_reason_code: "local_safe_to_rerun"}}`.
    - `config :scoria, :require_tool_classification, true` does NOT cause a refusal at this site — the flag is scoped to `MCP.Executor` resolution per D-03, and there is no tool module here to classify.
  </behavior>
  <action>
    In `lib/scoria/workflows/runtime.ex`, replace line 474's anonymous inline literal with a call to a new private helper — `seam = Keyword.get(opts, :replay_seam) || default_replay_seam(run, step)` — so the default has exactly one named, documented origin instead of being an unlabelled map literal buried in an 8-arity function. Note the operator change from `Keyword.get/3` to `Keyword.get/2` plus `||`: this is deliberate so the telemetry only fires when the caller genuinely supplied nothing, and it is safe here because a caller passing an explicit `nil` seam is indistinguishable from passing none.

    `default_replay_seam/2` returns a map that keeps `local_classification: :pure` EXACTLY as today — this is load-bearing and must not change. `:pure` hits `ReplayDisposition`'s clause 3 (`pure_local?/1`) and short-circuits to `:execute_live` before clause 7 is evaluated; substituting any other value flips every workflow step that replays today to `:blocked`. The helper ADDS `tool_classification: Scoria.MCP.Classification.unclassified_default()` alongside it, and emits one `[:scoria, :class, :unclassified]` event with metadata `%{site: :workflow_runtime_step, run_id: run.id, step_id: step.id}` wrapped in `try/rescue -> :ok`. Write a moduledoc-level or function-level comment stating in plain terms what this site is and is not: it is a step-granularity replay default, it is now explicit and telemetried rather than silent, and it deliberately does NOT change its own disposition — D-05 says that if site 5 were scoped out it must be said in writing, and it is not scoped out.

    Do NOT extend the `require_tool_classification` refusal to this site. D-03 scopes the flag to `MCP.Executor` resolution; there is no tool module here, and a step-level refusal would halt workflow replay for a reason no adopter opted into. State that boundary in the same comment.

    Extend `test/scoria/workflows/replay_disposition_test.exs` with a direct test of the default seam's shape and disposition — that it still resolves `{:execute_live, %{replay_reason_code: "local_safe_to_rerun"}}` with `local_classification: :pure` intact and `:tool_classification` present. Keep this in the existing `async: true`, no-DB, hand-built-seam-map style; do not add a DB-backed runtime integration test for a pure-map assertion.
  </action>
  <verify>
    <automated>mix test test/scoria/workflows/replay_disposition_test.exs --warnings-as-errors && mix test --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "default_replay_seam" lib/scoria/workflows/runtime.ex` matches at both the call site and the definition.
    - `grep -n "local_classification: :pure" lib/scoria/workflows/runtime.ex` still matches — the value is unchanged, only relocated into the named helper.
    - `grep -n "workflow_runtime_step" lib/scoria/workflows/runtime.ex` matches (the `site` discriminator).
    - `grep -n "require_tool_classification" lib/scoria/workflows/runtime.ex` returns nothing (the refusal is not extended to this site).
    - A test asserts the default seam resolves `{:execute_live, _}` with reason `"local_safe_to_rerun"` and that its `:local_classification` is `:pure` and its `:tool_classification` is a `%Scoria.MCP.Classification{source: :unclassified_default}`.
    - `mix test --warnings-as-errors` (full suite) exits 0 — the phase gate.
    - `git status --porcelain priv/repo/migrations/ | wc -l` returns 0 (no Ecto migration anywhere in this phase; migrations are Phase 56.1 scope).
  </acceptance_criteria>
  <done>All five D-05 fail-open sites now consume the resolved classification. Site 5 — the one D-05 calls "the worst" — is explicitly in scope, its total replay bypass is now a named, documented, telemetried default carrying a real classification, and its disposition is provably unchanged so no replaying adopter is bricked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| connector-routed call → replay decision | `Connectors.Invocation` decides replay at `invocation.ex:26`, before the executor's choke point is ever reached |
| workflow step with no declared seam → replay decision | `runtime.ex:474`'s default is a total, silent, step-granularity replay bypass |
| resolved classification → the four `ReplayDisposition` input keys | writing into any of them inverts the replay-safety invariant in either direction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-01 | Elevation of Privilege | `policy_sensitive_invocation?/1` (site 2), `budget_required?/1` (site 3) | high | mitigate | A declared sensitive tool can no longer skip its `tool.invocation` audit row or its budget reservation just because the host forgot to pass `:policy_sensitive` / `:sensitive_tool`. One shared `declared_sensitive?/1` predicate, declared-only so legacy traffic is unchanged. Task 1 tests. ASVS V4. |
| T-56-02 | Tampering / Elevation of Privilege | `Connectors.Invocation.invoke/4` (site 4) | high | mitigate | Without this task a connector-routed tool's replay decision is made from a seam built entirely from host context, before any classification exists — the fail-open path a forgetful or malicious connector would take. Resolution is moved ahead of `replay_resolution/5` and reuses the single shared `Classification.resolve/2`, so a connector cannot present a different classification than the executor would. Task 2 tests. ASVS V4. |
| T-56-04 | Tampering | `build_seam/2` (site 4), `default_replay_seam/2` (site 5) | high | mitigate | Both sites gain the classification as a parallel key only; `:local_classification`, `:action_class`, `:risk_level`, and `:approval_sensitive` are byte-unchanged at both, and site 5 keeps `:pure` verbatim so `ReplayDisposition`'s clause-3 short-circuit is preserved. Enforced by the negative `Map.put`/`Map.put_new` greps, the `local_classification: :pure` positive grep, the one-step `git diff` criterion, and the disposition regression tests. ASVS V4. |
</threat_model>

<verification>
- `mix test test/scoria/mcp/classification_test.exs test/scoria/mcp/executor_test.exs test/scoria/workflows/replay_disposition_test.exs test/scoria/connectors/invocation_test.exs --warnings-as-errors` exits 0 (the phase's pinned quick run).
- `mix compile --warnings-as-errors` exits 0.
- `mix test --warnings-as-errors` (full suite) exits 0 — the phase gate before `/gsd-verify-work`.
- Negative grep: no `Map.put` / `Map.put_new` of a resolved value into any of the six prohibited keys across `lib/scoria/mcp/executor.ex`, `lib/scoria/connectors/invocation.ex`, or `lib/scoria/workflows/runtime.ex`.
- `git status --porcelain priv/repo/migrations/` is empty (Phase 56.1 scope fence).
</verification>

<success_criteria>
- ROADMAP Phase 56 Success Criterion 3 satisfied end-to-end: at `MCP.Executor` enforcement, and at every path that reaches or precedes it, a tool call's classification derives from the tool's own declaration rather than a host-passed default.
- ROADMAP Phase 56 Success Criterion 4 satisfied: resolution covers ALL five fail-open seams — the replay seam, `policy_sensitive_invocation?/1`, `budget_required?/1`, `Connectors.Invocation.build_seam/2`, and `Workflows.Runtime`'s `%{local_classification: :pure}` default. None is scoped out; site 5's treatment is documented in code.
- CLASS-03 complete; CLASS-01, CLASS-02, and CLASS-03 all closed for Phase 56.
- No Ecto migration, no `runs`-table change, and no per-run rail exists anywhere in the phase (Phase 56.1 scope fence held).
</success_criteria>

<output>
Create `.planning/phases/56-tool-declared-trifecta-classification-per-run-rails/56-03-SUMMARY.md` when done. Record the Step-0 re-verified site inventory and an explicit statement that site 5 was covered rather than scoped out (D-05 requires this in writing).
</output>
