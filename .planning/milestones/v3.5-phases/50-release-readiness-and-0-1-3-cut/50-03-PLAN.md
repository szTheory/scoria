---
phase: 50-release-readiness-and-0-1-3-cut
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/scoria.post_publish_smoke.ex
  - mix.exs
  - .github/workflows/release-please.yml
  - .github/workflows/hex-publish.yml
  - .github/workflows/ci.yml
  - .github/workflows/ci-verify.yml
autonomous: true
requirements: [REL-03]
must_haves:
  truths:
    - "No stale 0.1.1 release guidance remains in release-facing helper copy (only historical CHANGELOG entries keep 0.1.1)"
    - "Workflow header comments point at guides/maintainers.md, not the retired docs/*.md compatibility-stub paths"
    - "mix.exs docs URL uses the current per-package HexDocs subdomain form (old path form still 301-redirects, non-blocking)"
    - "test/scoria/adoption_surface_test.exs stale-version refute test stays green; main @version remains 0.1.2 (Release Please owns the 0.1.3 bump)"
  artifacts:
    - lib/mix/tasks/scoria.post_publish_smoke.ex
    - mix.exs
    - .github/workflows/release-please.yml
    - .github/workflows/hex-publish.yml
    - .github/workflows/ci.yml
    - .github/workflows/ci-verify.yml
  key_links:
    - "workflow header comments -> guides/maintainers.md (canonical, replacing docs/*.md stubs)"
    - "mix.exs @hexdocs_url -> https://scoria.hexdocs.pm (2026 Hex subdomain change, D-14)"
---

<objective>
Complete the residual REL-03 version/docs-truth polish. Per RESEARCH.md, README, `mix.exs` `@version`, and `.release-please-manifest.json` are ALREADY correct — do NOT manufacture work there. Only three small, enumerated residual items remain, all non-CI-blocking D-13/D-14 correctness polish:
(a) four workflow header comments still point at retired `docs/MAINTAINERS.md` / `docs/operator_verification.md` stub paths;
(b) `scoria.post_publish_smoke.ex` moduledoc shows a stale `SCORIA_REGISTRY_VERSION=0.1.1` example;
(c) `mix.exs` `@hexdocs_url` uses the old path-based HexDocs URL form instead of the 2026 per-package subdomain form.

Per D-12/D-13/D-14/D-15: keep `main` at live baseline `0.1.2` (Release Please owns the `0.1.3` bump on the release branch); remove stale `0.1.1` guidance except in historical CHANGELOG; treat the HexDocs URL as release-metadata correctness. Do NOT edit generated `doc/` output. Do NOT edit README/manifest/@version (already correct).

Purpose: Version/docs truth is consistent across README, maintainer docs, release automation, and package metadata (ROADMAP Success Criterion 3).
Output: Four repointed comments, one refreshed example version, one updated docs URL; adoption-surface test stays green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-release-readiness-and-0-1-3-cut/50-RESEARCH.md
@.planning/phases/50-release-readiness-and-0-1-3-cut/50-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint stale workflow header comments to guides/maintainers.md and refresh the post-publish-smoke example version</name>
  <files>.github/workflows/release-please.yml, .github/workflows/hex-publish.yml, .github/workflows/ci.yml, .github/workflows/ci-verify.yml, lib/mix/tasks/scoria.post_publish_smoke.ex</files>
  <read_first>
    - .github/workflows/release-please.yml line 3 and .github/workflows/hex-publish.yml line 3 (both point at docs/operator_verification.md hex-release recovery anchor)
    - .github/workflows/ci.yml line 18 and .github/workflows/ci-verify.yml line 11 (both point at docs/MAINTAINERS.md CI gate map narrative)
    - lib/mix/tasks/scoria.post_publish_smoke.ex lines 20-21 (moduledoc example block) and ~line 61 (required-env error message, which correctly uses 0.1.0 and must stay)
    - RESEARCH.md REL-03 row + Code Example 4; 50-PATTERNS.md workflow-header-comments and post_publish_smoke sections
  </read_first>
  <action>
    In release-please.yml line 3 and hex-publish.yml line 3, change the header-comment path from the retired docs/operator_verification.md hex-release-recovery reference to guides/maintainers.md#hex-release--recovery-maintainers. That anchor id is the one plan 01 (REL-01) establishes on the restored "## Hex release & recovery" heading — this is an informational cross-reference only, not a wave dependency, since header comments do not gate CI.
    In ci.yml line 18 and ci-verify.yml line 11, change the retired docs/MAINTAINERS.md CI-gate-map narrative reference to point at guides/maintainers.md instead, keeping the trailing "CI gate map + flake policy." descriptive text.
    In lib/mix/tasks/scoria.post_publish_smoke.ex line 21, change the moduledoc example SCORIA_REGISTRY_VERSION=0.1.1 to use 0.1.3 instead. Keep the adjacent 0.1.0 example line (line 20) intact, and leave the required-env error message near line 61 (which uses 0.1.0) unchanged — it is not stale.
    Change only these comment/example lines. Do NOT alter any workflow job logic, step, needs:, matrix, or trigger (D-03: no CI topology change). Do NOT touch generated doc/ output.
  </action>
  <verify>
    <automated>test -z "$(grep -rl 'docs/MAINTAINERS.md\|docs/operator_verification.md' .github/workflows/)" && echo CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'docs/MAINTAINERS.md\|docs/operator_verification.md' .github/workflows/` returns no matches (all four comments repointed to guides/maintainers.md).
    - `grep -n 'SCORIA_REGISTRY_VERSION=0.1.1' lib/mix/tasks/scoria.post_publish_smoke.ex` returns no matches; the 0.1.0 example and the required-env error message remain present.
    - No workflow job/step/needs/trigger lines changed (only comment lines differ).
  </acceptance_criteria>
  <done>Four workflow header comments point at guides/maintainers.md; the post-publish-smoke moduledoc example uses 0.1.3; no CI topology or generated-doc changes.</done>
</task>

<task type="auto">
  <name>Task 2: Update mix.exs HexDocs URL to the per-package subdomain form (D-14)</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs lines 4-7 (@version "0.1.2", @hexdocs_url, @release_docs_url) and lines 22, 143, 399-400 (consumers: homepage_url, package :links Documentation/Release Docs)
    - RESEARCH.md State of the Art table + Code Example 5 (Hex 2026-06-01 per-package subdomain change; old form 301-redirects, so this is correctness polish, not a blocker)
    - 50-PATTERNS.md mix.exs section
  </read_first>
  <action>
    Change @hexdocs_url at mix.exs line 6 from the old path-based form "https://hexdocs.pm/scoria" to the per-package subdomain form "https://scoria.hexdocs.pm". Leave @release_docs_url (line 7) as the derived "#{@hexdocs_url}/#{@version}" expression — it recomposes correctly against the new base. Do NOT change @version (stays 0.1.2; Release Please owns the 0.1.3 bump per D-12). Do NOT edit README or the release manifest (already correct). Confirm the package/docs preview still builds cleanly so no downstream metadata consumer breaks.
  </action>
  <verify>
    <automated>MIX_ENV=dev mix scoria.release_preview</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '@hexdocs_url' mix.exs` shows "https://scoria.hexdocs.pm".
    - `grep -n '@version' mix.exs` still shows "0.1.2".
    - `MIX_ENV=dev mix scoria.release_preview` exits 0 (docs + package preview build clean).
  </acceptance_criteria>
  <done>@hexdocs_url uses the subdomain form; @version unchanged at 0.1.2; release preview builds clean.</done>
</task>

</tasks>

<verification>
Confirm no release-blocking regression and version truth holds:
`MIX_ENV=test mix test test/scoria/adoption_surface_test.exs` must be green (the "README does not contain stale 0.1.1 release or GitHub fallback guidance" test stays passing).
`MIX_ENV=dev mix scoria.release_preview` must be green (docs + package preview clean after the URL change).
No stale `0.1.1` outside historical CHANGELOG: `grep -rn '0\.1\.1' lib/mix/tasks/ .github/workflows/ mix.exs` returns nothing (CHANGELOG excluded from this plan's scope).
</verification>

<success_criteria>
- README, maintainer docs, CHANGELOG, release automation, and package metadata agree on live 0.1.2 baseline and 0.1.3 target (ROADMAP Success Criterion 3).
- No stale 0.1.1 guidance in release-facing helper copy; historical CHANGELOG untouched (D-13).
- HexDocs URL uses current subdomain form (D-14); main @version stays 0.1.2 (D-12); no generated doc/ edits (D-15).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release tooling / package metadata -> Hex registry + docs host | Static metadata + workflow comments; no runtime, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-03-01 | Tampering | Accidental @version bump on main defeating the release-please-owned 0.1.3 bump | low | mitigate | Plan forbids @version change; acceptance criterion asserts main @version stays 0.1.2 (D-12) |
| T-50-03-02 | Spoofing | Docs URL pointing at a wrong/typo subdomain misdirecting adopters | low | accept | Old path form 301-redirects and the new subdomain is the Hex-canonical per-package form; release_preview build confirms metadata validity |

No net-new high-severity threats. This plan edits static metadata and comments only.
</threat_model>

## Artifacts this phase produces (plan 03)

- `.github/workflows/release-please.yml`, `hex-publish.yml`, `ci.yml`, `ci-verify.yml`: four header comments repointed from `docs/*.md` stub paths to `guides/maintainers.md` (comment-only; no job logic changed).
- `lib/mix/tasks/scoria.post_publish_smoke.ex`: moduledoc example version `0.1.1` -> `0.1.3` (line 21).
- `mix.exs`: `@hexdocs_url` changed from `https://hexdocs.pm/scoria` to `https://scoria.hexdocs.pm`; `@version` unchanged at `0.1.2`.

<output>
Create `.planning/phases/50-release-readiness-and-0-1-3-cut/50-03-SUMMARY.md` when done
</output>
