---
phase: 50-release-readiness-and-0-1-3-cut
plan: 11
type: execute
wave: 2
depends_on: ["50-05", "50-06", "50-07", "50-08", "50-09", "50-10"]
files_modified: []
autonomous: true
gap_closure: true
requirements: [REL-04]
must_haves:
  truths:
    - "Full local mix test (all partitions) and the connector lane pass on the tip of the fixes — 0 failures across the Bucket A-G surface"
    - "The fixes are pushed to origin; Release Please refreshes PR #12; CI / ci-gate is GREEN on PR #12's latest head"
    - "The release train can proceed: hand off to the EXISTING 50-04 plan at its maintainer checkpoint (do NOT duplicate merge/publish here)"
  artifacts: []
  key_links:
    - "gap fixes (50-05..50-10) -> full mix test + connector green -> push origin/main -> Release Please refresh PR #12 -> ci-gate green -> 50-04 maintainer checkpoint"
---

<objective>
Release-train re-entry gate. After all Bucket A-G fixes (50-05..50-10) are green individually, prove
the FULL suite is green, push to origin, let Release Please refresh PR #12, and confirm `ci-gate` is
green — then hand off to the EXISTING `50-04-PLAN.md` at its maintainer checkpoint (Task 2/3).

This plan does NOT merge, tag, or publish, and does NOT duplicate 50-04's maintainer/publish steps
(D-01/D-02/D-04). It is the autonomous integration gate that turns `ci-gate` from RED to GREEN so the
release train that stalled at 50-04 can finish. Honor D-03: keep `ci-gate` required; never bypass
branch protection to force green.

Purpose: convert the individual bucket fixes into a green PR #12 head and route back into the release
cut.
Output: green full local suite, green `ci-gate` on PR #12, and an explicit handoff to 50-04.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-release-readiness-and-0-1-3-cut/50-CI-GAP-INVENTORY.md
@.planning/phases/50-release-readiness-and-0-1-3-cut/50-CONTEXT.md
@.planning/phases/50-release-readiness-and-0-1-3-cut/50-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove the full suite green locally across all partitions plus connector</name>
  <files>(no source edits — full-suite verification only)</files>
  <read_first>
    - .planning/phases/50-release-readiness-and-0-1-3-cut/50-CI-GAP-INVENTORY.md (the per-lane failure counts and full-suite/connector partitioning of run 29137880790 — the target is 0 failures across all of these)
    - .github/workflows/*.yml (the verify lanes: `test`, `full-suite (1-4)`, `connector` — mirror their invocation locally as closely as the harness allows)
    - the 50-05..50-10 SUMMARY.md files (confirm each bucket landed before running the aggregate)
  </read_first>
  <action>
    With all bucket fixes merged into the working tree, run the FULL suite the way CI partitions it,
    not just the per-bucket files:
    - `mix test` (full default lane) — expect 0 failures.
    - the connector lane / tag as CI invokes it (`mix test` with the connector tag, or the dedicated
      connector task per the workflow) — expect 0 failures (its 5-7 were re-runs of Bucket A/E; they
      should now pass).
    - the SupportCopilot gallery driver `mix test test/scoria/support_copilot_gallery_test.exs`
      (nested project) — expect the proof to complete.
    If any residual failure surfaces that was not in Buckets A-G, STOP and report it (do not push a
    still-red tree). Do not weaken any assertion to reach green (CONTEXT/verification decision).
  </action>
  <verify>
    <automated>mix test</automated>
  </verify>
  <acceptance_criteria>
    - `mix test` exits 0 (full default lane).
    - The connector lane and the nested gallery proof pass (0 failures).
    - No assertion was weakened to reach green (git diff across 50-05..50-10 shows repoints/root-cause fixes only).
  </acceptance_criteria>
  <done>The full suite (all partitions + connector + nested gallery) is green locally on the tip of the fixes.</done>
</task>

<task type="auto">
  <name>Task 2: Push the fixes, refresh PR #12, and confirm ci-gate is green</name>
  <files>(no source edits — push + CI confirmation)</files>
  <read_first>
    - .planning/phases/50-release-readiness-and-0-1-3-cut/50-CONTEXT.md D-01 (normal Release Please path — forward-fix main, let Release Please refresh PR #12), D-03 (keep ci-gate required; do not bypass branch protection)
    - .planning/phases/50-release-readiness-and-0-1-3-cut/50-04-PLAN.md (the downstream consumer — its Task 1 confirms preview/docs/ci-gate, Task 2/3 are the maintainer merge + publish + post-publish smoke)
    - .github/workflows/release-please*.yml and the ci-gate aggregate (how PR #12 refreshes and what rolls up into ci-gate)
  </read_first>
  <action>
    Push the committed bucket fixes to `origin/main` via the normal path (D-01). Release Please
    refreshes the release PR #12 from main. Then confirm the gate WITHOUT bypassing it (D-03):
    - `gh pr view 12 --json state,mergeable` — expect OPEN + MERGEABLE.
    - `gh pr checks 12` — confirm the verify lanes (`test`, `full-suite 1-4`, `connector`,
      `verify-summary`) and `CI / ci-gate` are green on PR #12's latest head. Poll until the run
      completes; if a lane is still red, capture which test failed and STOP/route back to the owning
      bucket plan (do not force the gate green).
    Do NOT merge, tag, or publish here — that is 50-04's maintainer checkpoint. On green ci-gate,
    hand off: resume the EXISTING `50-04-PLAN.md` at Task 2 (maintainer merges PR #12 through Release
    Please, publishes 0.1.3) and Task 3 (post-publish smoke + D-04 closeout). Record the PR #12 run
    URL/ID in the SUMMARY as the green-gate evidence.
  </action>
  <verify>
    <automated>gh pr checks 12</automated>
  </verify>
  <acceptance_criteria>
    - `gh pr view 12 --json state,mergeable` shows OPEN + MERGEABLE.
    - `gh pr checks 12` shows all verify lanes and `CI / ci-gate` green on PR #12's latest head.
    - No branch-protection/topology change was made to force green (D-03).
    - SUMMARY records the green PR #12 run URL/ID and an explicit handoff to 50-04 Task 2/3.
  </acceptance_criteria>
  <done>Fixes pushed; PR #12 refreshed; ci-gate GREEN; release train handed off to 50-04's maintainer checkpoint.</done>
</task>

</tasks>

<verification>
- `mix test` exits 0 locally (full lane) and the connector + nested gallery suites pass.
- `gh pr checks 12` shows `CI / ci-gate` green on PR #12's latest head.
- No maintainer merge/publish performed here (that stays in 50-04); handoff recorded.
</verification>

<success_criteria>
- ci-gate flips RED -> GREEN on PR #12 via the normal Release Please refresh (D-01), gate never bypassed (D-03).
- The release train that stalled at 50-04 can proceed to merge/tag/publish 0.1.3 and post-publish smoke (50-04 Task 2/3).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local fixes -> origin/main -> Release Please PR #12 -> ci-gate (required aggregate gate) | The release gate must go green on real passing CI, not a bypass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-11-01 | Elevation of Privilege | ci-gate branch protection | high | mitigate | Never change branch-protection topology or bypass ci-gate to force green (D-03); only push real fixes and confirm the gate passes on its own |
| T-50-11-02 | Repudiation | Green-gate evidence | low | mitigate | Record the PR #12 run URL/ID in the SUMMARY as inspectable evidence before handing to 50-04 |
| T-50-11-SC | Tampering | package installs | low | accept | No new packages; verification + push only |
</threat_model>

## Artifacts this phase produces (plan 11)

- No source or planning-file edits. This plan verifies the full suite, pushes the already-committed bucket fixes, and records the green PR #12 ci-gate run URL/ID in its SUMMARY.
- Explicit handoff to the existing `50-04-PLAN.md` maintainer checkpoint (Task 2 merge/publish, Task 3 post-publish smoke + D-04 closeout).

<output>
Create `.planning/phases/50-release-readiness-and-0-1-3-cut/50-11-SUMMARY.md` when done
</output>
</content>
