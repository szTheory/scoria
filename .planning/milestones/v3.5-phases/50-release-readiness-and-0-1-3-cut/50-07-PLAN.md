---
phase: 50-release-readiness-and-0-1-3-cut
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - test/scoria/runtime_integration_test.exs
  - test/scoria/workflows/integration_test.exs
  - test/scoria_web/components/memory_notebook_component_test.exs
  - test/scoria_web/live/incidents_live_test.exs
autonomous: true
gap_closure: true
requirements: [REL-04]
must_haves:
  truths:
    - "mix test test/scoria/runtime_integration_test.exs test/scoria/workflows/integration_test.exs exits 0 (seeded run renders instead of 'Workflow run not found')"
    - "mix test test/scoria_web/components/memory_notebook_component_test.exs test/scoria_web/live/incidents_live_test.exs exits 0"
    - "Rendered contracts are repaired by fixing the seed/scope mismatch or repointing to relocated copy — no assertion is deleted or loosened"
  artifacts:
    - test/scoria/runtime_integration_test.exs
    - test/scoria/workflows/integration_test.exs
    - test/scoria_web/components/memory_notebook_component_test.exs
    - test/scoria_web/live/incidents_live_test.exs
  key_links:
    - "tenant-scoped seeded run -> operator workflow LiveView lookup -> rendered run title (vs 'Workflow run not found')"
    - "incident detail evidence copy SSOT -> incidents_live rendered assertion"
---

<objective>
Close Bucket C — runtime/LiveView seeded-run and rendered contracts (4 failures, ~2 root causes).
Two integration tests render the mobile-topbar title "Workflow run not found" instead of the seeded
run — a tenant-scoped seeded-run lookup mismatch (adjacent to REL-02's arity-3 tenant seeding, but
in the test fixtures rather than dev_seed.exs). Plus two rendered-contract drifts: shared notebook
primitives and a stale/relocated incident-evidence copy string.

Do NOT weaken assertions: repair the seed/scope so the run is found and renders, or repoint an
assertion to the relocated canonical copy after confirming the new string. Never delete or loosen a
rendered contract to force green.

Purpose: turn the 4 Bucket-C failures green.
Output: seeded-run pages render the seeded run; notebook + incident rendered contracts pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-release-readiness-and-0-1-3-cut/50-CI-GAP-INVENTORY.md
@.planning/phases/50-release-readiness-and-0-1-3-cut/50-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the tenant-scoped seeded-run lookup so operator workflow pages render the seeded run</name>
  <files>
    test/scoria/runtime_integration_test.exs
    test/scoria/workflows/integration_test.exs
  </files>
  <read_first>
    - test/scoria/runtime_integration_test.exs:159 ("operator-visible workflow page stays aligned with the public runtime contract"; renders "Workflow run not found" vs the expected seeded run id) and its setup/seed block above :159
    - test/scoria/workflows/integration_test.exs:148 ("operator-visible LiveView state matches the durable recovery path"; `assert render(view) =~ "waiting_for_approval"`) and its seed block
    - .planning/phases/50-release-readiness-and-0-1-3-cut/50-02-SUMMARY.md (REL-02 arity-3 tenant-scoped `start_release_workflow(..., tenant_id: tenant_id)` fix in dev_seed.exs — same tenant-scoping shape, applied to test fixtures here)
    - lib/scoria_web/dashboard_scope.ex:51 (`@scope_keys [:tenant_id, ...]`) — the tenant_id the LiveView mounts under must match the seeded run's tenant_id
  </read_first>
  <action>
    Both tests render "Workflow run not found" because the run is created under one tenant_id but the
    LiveView is mounted/queried under a different (or absent) tenant scope — the same arity-3
    tenant-scoping mismatch REL-02 fixed in dev_seed.exs, now present in these test fixtures.
    Investigate the seed-vs-mount tenant_id in each test and make them consistent: seed the run with
    the SAME tenant_id the test conn/scope mounts under (create the run tenant-scoped, and/or seed the
    mounting session/scope with that tenant_id), so the operator workflow lookup finds the seeded run.
    Prefer fixing the seed/scope wiring over changing the asserted run id or the rendered strings.
    Keep the assertions (`"waiting_for_approval"`, the run-title/run-id contract) intact.
    If the two tests share a fixture helper, fix it once. Do NOT loosen the assertion to accept
    "Workflow run not found".
  </action>
  <verify>
    <automated>mix test test/scoria/runtime_integration_test.exs test/scoria/workflows/integration_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Both files exit 0; neither renders "Workflow run not found" at the asserted line.
    - `test/scoria/workflows/integration_test.exs:148` `render(view) =~ "waiting_for_approval"` passes.
    - git diff shows seed/scope tenant_id wiring changes, not weakened assertions.
  </acceptance_criteria>
  <done>Seeded-run operator pages render the seeded run under a consistent tenant scope; both integration tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Repair the shared notebook primitives and incident-evidence rendered contracts</name>
  <files>
    test/scoria_web/components/memory_notebook_component_test.exs
    test/scoria_web/live/incidents_live_test.exs
  </files>
  <read_first>
    - test/scoria_web/components/memory_notebook_component_test.exs:10 ("retrieval, delegated, and memory adapters use shared notebook primitives") and the component/source it asserts against
    - test/scoria_web/live/incidents_live_test.exs:219 ("incident detail route renders evidence"; `assert html =~ "Trace-first incident evidence"` — flagged as stale/relocated copy)
    - The rendered source that owns each contract (the memory notebook component module; the incident detail LiveView/template) to locate whether the primitive/copy moved or the render regressed
  </read_first>
  <action>
    For memory_notebook_component_test:10: determine whether the shared-notebook-primitive contract
    regressed in the component (fix the component) or the test asserts a primitive that was renamed
    (repoint the assertion to the canonical primitive AFTER confirming it in the source). Prefer
    fixing the source if the shared-primitive contract genuinely regressed.

    For incidents_live_test:219: the inventory flags `"Trace-first incident evidence"` as
    stale/relocated copy. Grep the incident detail LiveView/template for the current evidence heading.
    If the copy was intentionally reworded during the v3.5 docs/vocabulary migration (D-19/D-20), the
    rendered source is the SSOT — repoint the assertion to the exact current string. If instead the
    render regressed (copy missing when it should be present), fix the template. Confirm which before
    editing; do not invent a new string or delete the assertion.
  </action>
  <verify>
    <automated>mix test test/scoria_web/components/memory_notebook_component_test.exs test/scoria_web/live/incidents_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Both files exit 0.
    - Any assertion change is a repoint to a grep-confirmed current string in the rendered source, not a deletion or loosening.
  </acceptance_criteria>
  <done>Notebook-primitive and incident-evidence rendered contracts pass against the canonical source.</done>
</task>

</tasks>

<verification>
- `mix test test/scoria/runtime_integration_test.exs test/scoria/workflows/integration_test.exs test/scoria_web/components/memory_notebook_component_test.exs test/scoria_web/live/incidents_live_test.exs` exits 0.
</verification>

<success_criteria>
- All 4 Bucket-C failures green.
- Seeded-run pages render under a consistent tenant scope; no rendered assertion weakened.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeded run tenant_id -> dashboard scope tenant_id -> operator LiveView lookup | Tenant-scoped run lookup must match the mounted scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-07-01 | Tampering | Rendered-contract assertions | medium | mitigate | Fix seed/scope wiring or repoint to grep-confirmed current copy; never accept "Workflow run not found" or delete an assertion (acceptance criteria + git diff) |
| T-50-07-SC | Tampering | package installs | low | accept | No new packages; test/fixture edits only |
</threat_model>

## Artifacts this phase produces (plan 07)

- `test/scoria/runtime_integration_test.exs` + `test/scoria/workflows/integration_test.exs`: seed/scope tenant_id wiring so the seeded run is found.
- `test/scoria_web/components/memory_notebook_component_test.exs`: shared-primitive contract aligned to canonical source.
- `test/scoria_web/live/incidents_live_test.exs`: incident-evidence assertion aligned to current SSOT copy (or template fixed).

<output>
Create `.planning/phases/50-release-readiness-and-0-1-3-cut/50-07-SUMMARY.md` when done
</output>
</content>
