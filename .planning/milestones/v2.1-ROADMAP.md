# Scoria v2.1 Tenant-scoped Semantic Fast Path Roadmap

**Status:** ✅ SHIPPED 2026-05-25
**Canonical proof chain:** `.planning/phases/44-semantic-cache-contract-and-persistence/44-VERIFICATION.md` through `.planning/phases/46-operator-evidence-and-verification/46-VERIFICATION.md`
**Archive note:** Closed on 2026-05-25 after `.planning/v2.1-MILESTONE-AUDIT.md` returned `passed` with all requirements, phases, integrations, and flows satisfied. The milestone shipped with one named semantic proof lane on `SCORIA_DB_PORT=55432` and operator-visible evidence for hit, miss, reject, stale, invalidated, and writeback-rejected outcomes.

## Phases

- [x] **Phase 44: Semantic cache contract and persistence** - Establish the semantic cache as a Scoria-owned core surface with explicit eligibility rules, tenant partitioning, and persistence primitives.
- [x] **Phase 45: Compatibility and invalidation engine** - Make semantic reuse conservative and explainable by combining similarity checks with prompt, policy, source, and freshness compatibility.
- [x] **Phase 46: Operator evidence and verification** - Project semantic cache behavior into Scoria's operator surfaces and close the milestone with checked proof.

## Phase Details

### Phase 44: Semantic cache contract and persistence
**Goal**: Establish the semantic cache as a Scoria-owned core surface with explicit eligibility rules, tenant partitioning, and persistence primitives.
**Depends on**: None
**Requirements**: FAST-01, SAFE-01, FAST-02
**Success Criteria**:
  1. Developers can route only explicitly safe read-only lanes through semantic fast-path evaluation.
  2. Unsafe write-side, approval-sensitive, or personalized-tool-backed responses are rejected from cache admission by default.
  3. Semantic cache entries persist durable tenant-partitioned lookup state and compatibility metadata.
  4. Miss or ineligible requests continue through the normal runtime path without mutating workflow truth.
**Plans**: 3 plans
- [x] `44-01-PLAN.md` — Add durable semantic-cache entry and event persistence with explicit hit, miss, and bypass outcomes.
- [x] `44-02-PLAN.md` — Define the public semantic-lane contract and conservative eligibility engine.
- [x] `44-03-PLAN.md` — Wire semantic fast-path lookup, miss fallback, and writeback lineage through the runtime.

### Phase 45: Compatibility and invalidation engine
**Goal**: Make semantic reuse conservative and explainable by combining similarity checks with prompt, policy, source, and freshness compatibility.
**Depends on**: Phase 44
**Requirements**: LOOK-01, LOOK-02, INVD-01, INVD-02
**Success Criteria**:
  1. Cache hits require semantic similarity plus prompt, policy, and source compatibility.
  2. Stale, incompatible, or rejected candidates fall through cleanly to the normal execution path.
  3. Prompt-version, source-fingerprint, and policy changes invalidate affected entries explicitly.
  4. Active, stale, and invalidated entry states remain distinguishable in persisted truth.
**Plans**: 4 plans
- [x] `45-00-PLAN.md` — Align Phase 45 verification to the trusted pgvector proof lane and document the unusable local `5432` path.
- [x] `45-01-PLAN.md` — Upgrade semantic-cache compatibility truth and implement exact-first filtered lookup.
- [x] `45-02-PLAN.md` — Persist stage-separated runtime outcomes and compatibility-rich writeback/fallthrough behavior.
- [x] `45-03-PLAN.md` — Add transactional stale/invalidation transitions and proof coverage.

### Phase 46: Operator evidence and verification
**Goal**: Project semantic cache behavior into Scoria's operator surfaces and close the milestone with checked proof.
**Depends on**: Phase 45
**Requirements**: EVID-01, PROOF-01
**Success Criteria**:
  1. Operators can inspect semantic cache hit, miss, stale, and rejection outcomes with provenance context.
  2. Runtime or workflow detail surfaces expose the partitioning and compatibility reasons behind each semantic outcome.
  3. Scoria ships a checked verification lane that proves partitioning, fallback, and invalidation semantics.
  4. Milestone docs align the fast-path setup, constraints, and troubleshooting story to the shipped behavior.
**Plans**: 5 plans
- [x] `46-00-PLAN.md` — Add the canonical semantic fast-path proof lane and its bounded file inventory.
- [x] `46-01-PLAN.md` — Project one curated semantic-evidence contract from runtime metadata plus semantic entry/event truth.
- [x] `46-02-PLAN.md` — Extend the runtime presence drawer with summary-first semantic evidence and deep links.
- [x] `46-03-PLAN.md` — Add the workflow semantic evidence notebook as the canonical deep-inspection surface.
- [x] `46-04-PLAN.md` — Finalize the semantic proof lane, docs, and discoverability checks around shipped operator evidence.

## Progress

| Phase | Plans Complete | Status | Completed |
|-------|----------------|--------|-----------|
| 44. Semantic cache contract and persistence | 3/3 | Complete | 2026-05-25 |
| 45. Compatibility and invalidation engine | 4/4 | Complete | 2026-05-25 |
| 46. Operator evidence and verification | 5/5 | Complete | 2026-05-25 |

## Milestone Summary

### Key Decisions

- Semantic caching remains a Scoria-owned durable truth surface rather than provider prompt caching or invisible middleware.
- Reuse stays opt-in through explicit semantic lanes and safe read-only admission rules.
- Compatibility checks gate semantic hits on prompt, policy, source, tenant, and scope truth before any answer reuse is allowed.
- Operator trust requires runtime and workflow surfaces to project semantic evidence directly from durable runtime metadata and semantic entry/event history.

### Issues Resolved

- Added durable semantic cache entry/event persistence instead of burying reuse truth in runtime metadata alone.
- Replaced ad hoc reuse with exact-first plus compatibility-filtered semantic fallback and explicit reject reasons.
- Distinguished `active`, `stale`, `invalidated`, and `writeback_rejected` entry states as durable lifecycle truth.
- Closed the operator support story with one named semantic proof lane and aligned docs/source assertions.

### Issues Deferred

- None recorded at milestone close.

### Technical Debt Incurred

- None recorded in the passing closeout audit.
