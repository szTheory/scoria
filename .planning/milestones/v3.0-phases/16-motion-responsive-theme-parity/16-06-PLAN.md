---
phase: 16-motion-responsive-theme-parity
plan: 06
type: execute
wave: 6
depends_on: ["16-01", "16-02", "16-03", "16-04", "16-05"]
files_modified:
  - priv/dev/e2e/phase16_parity.spec.mjs
  - test/scoria_web/token_contrast_guard_test.exs
autonomous: true
requirements: [MOTION-01, MOTION-02, MOTION-03, MOTION-04]
must_haves:
  truths:
    - "A Playwright spec proves no page-level horizontal overflow at 375px on the shell and a representative table screen"
    - "A Playwright spec proves visible focus outlines on representative link/button/input/table-action/command-item/mobile-nav-item in light and dark"
    - "A Playwright spec proves reduced-motion collapses one drawer/modal path and one skeleton/attention cue"
    - "A Playwright spec proves theme toggle works on Home/shell, one table, one evidence screen, and one overlay path"
    - "The optional token contrast guard reads only assets/css/02-tokens.css and asserts AA/non-text floors (or is documented skipped per D-31)"
  artifacts:
    - path: "priv/dev/e2e/phase16_parity.spec.mjs"
      provides: "Targeted browser-truth checks for MOTION-01..04"
      contains: "reducedMotion"
    - path: "test/scoria_web/token_contrast_guard_test.exs"
      provides: "Deterministic AA contrast floor guard over the single token SSOT (optional per D-31)"
  key_links:
    - from: "priv/dev/e2e/phase16_parity.spec.mjs"
      to: "running dev server via waitForReady"
      via: "import { waitForReady } from './lib/ready.mjs'"
      pattern: "waitForReady"
    - from: "test/scoria_web/token_contrast_guard_test.exs"
      to: "assets/css/02-tokens.css"
      via: "single token SSOT parse"
      pattern: "02-tokens.css"
---

<objective>
Add the targeted Phase 16 browser-truth proof under the existing `mix scoria.ui.e2e` Playwright lane (D-28/D-29/D-30) so MOTION-01..04 are mechanically verified before the Phase 17 proof pass — without building visual-regression or full axe infrastructure (D-32). Create `priv/dev/e2e/phase16_parity.spec.mjs` covering 375px overflow, focus visibility (light + dark), reduced-motion collapse, and theme-toggle smoke. Optionally add a deterministic token contrast guard that reuses ONLY `assets/css/02-tokens.css` (D-31); skip it if it would create a parallel token source.

Purpose: This is the Wave 0 validation gap from the validation strategy — it closes the "no automated browser proof exists yet" hole and prevents late Phase 17 surprises. Implements the Verification Contract section of the UI design contract.
Output: One Playwright spec auto-discovered by the e2e lane, plus an optional ExUnit contrast guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-motion-responsive-theme-parity/16-CONTEXT.md
@.planning/phases/16-motion-responsive-theme-parity/16-UI-SPEC.md
@.planning/phases/16-motion-responsive-theme-parity/16-RESEARCH.md
@.planning/phases/16-motion-responsive-theme-parity/16-PATTERNS.md
@.planning/phases/16-motion-responsive-theme-parity/16-01-SUMMARY.md
@.planning/phases/16-motion-responsive-theme-parity/16-05-SUMMARY.md

<interfaces>
<!-- Spec scaffolding to copy (priv/dev/e2e/command_palette.spec.mjs:1-18): -->
<!--   import { test, expect } from '@playwright/test'; -->
<!--   import { waitForReady } from './lib/ready.mjs'; -->
<!--   const BASE = process.env.PLAYWRIGHT_BASE_URL || 'http://localhost:4000/scoria'; -->
<!--   async function openHome(page) { await page.goto(BASE); await waitForReady(page); } -->
<!-- ready.mjs waitForReady(page) gates on data-scoria-ready="true" on <html> (15s default). -->
<!-- playwright.config.mjs: testMatch '**/*.spec.mjs' auto-includes the new file; single chromium project; -->
<!--   no fixed sleeps — rely on waitForReady + expect auto-wait; retries:2 only in CI. -->
<!-- Per-test overrides (RESEARCH-supported): -->
<!--   test.use({ viewport: { width: 375, height: 812 }, colorScheme: 'dark' }); -->
<!--   await page.emulateMedia({ reducedMotion: 'reduce' }); -->
<!--   await page.emulateMedia({ colorScheme: 'light' | 'dark' }); -->
<!-- Stable selectors that exist: [data-command-open], #scoria-command-palette, #scoria-mobile-nav, -->
<!--   [data-mobile-nav-open], .scoria-table__viewport, .scoria-skeleton, .scoria-attention, -->
<!--   #scoria-theme-toggle (and the mobile theme toggle id from 16-01). -->
<!--   READ 16-01-SUMMARY.md for the final mobile topbar/drawer selector ids. -->
<!-- RESEARCH code examples (16-RESEARCH.md): overflow check, focus-visible check, reduced-motion check shapes. -->
<!-- Contrast-guard analogs: test/scoria_web/ui_drift_guard_test.exs and ds06_drift_guard_test.exs -->
<!--   (deterministic source/token parsers). Contrast guard MUST parse only 02-tokens.css. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase16_parity.spec.mjs targeted browser-truth checks</name>
  <files>priv/dev/e2e/phase16_parity.spec.mjs</files>
  <read_first>
    - priv/dev/e2e/command_palette.spec.mjs (scaffold lines 1-25; selector/keyboard/focus patterns)
    - priv/dev/e2e/uat.spec.mjs (overlay + seeded-fixture + serial/destructive conventions)
    - priv/dev/e2e/lib/ready.mjs (waitForReady contract)
    - priv/dev/e2e/playwright.config.mjs (testMatch, retries, no-sleep posture)
    - .planning/phases/16-motion-responsive-theme-parity/16-RESEARCH.md (Code Examples: overflow, focus-visible, reduced-motion)
    - .planning/phases/16-motion-responsive-theme-parity/16-01-SUMMARY.md (final mobile topbar/drawer selector ids)
    - .planning/phases/16-motion-responsive-theme-parity/16-UI-SPEC.md (Verification Contract — required checks list)
  </read_first>
  <action>
    Create `priv/dev/e2e/phase16_parity.spec.mjs` using the command_palette.spec.mjs scaffold (`@playwright/test`, `waitForReady`, `BASE`). The file is auto-discovered by the lane (`testMatch '**/*.spec.mjs'`); no mix task change needed. Keep selectors stable and light, rely on `waitForReady` + expect auto-wait (no fixed sleeps), and add NO screenshot comparisons (D-30/D-32). Cover exactly the UI-SPEC Verification Contract required checks, grouped into focused tests:

    - No page-level horizontal overflow at 375px on the shell (Home) AND one representative table screen (e.g. `/workflows`): `test.use({ viewport: { width: 375, height: 812 } })`; assert `document.documentElement.scrollWidth <= window.innerWidth` (overflow false) per the RESEARCH overflow example.
    - Table overflow is contained in the table viewport and keyboard reachable at 375px: assert `.scoria-table__viewport` exists, is scrollable/`tabindex="0"`, and that the page itself does not overflow even when the table does.
    - Visible focus outline on representative controls in BOTH themes: for a link, a button (`[data-command-open]`), an input, a table action, a command palette item, and a mobile nav item — focus each and assert `getComputedStyle(el).outlineStyle !== 'none'` and `outlineWidth >= 2px` (per the RESEARCH focus example). Run the focus checks once under `colorScheme: 'light'` and once under `colorScheme: 'dark'` via `page.emulateMedia`.
    - Reduced-motion collapse: `await page.emulateMedia({ reducedMotion: 'reduce' })`; open one drawer/modal path (mobile nav drawer or a modal) and probe one skeleton/attention cue (`.scoria-skeleton, .scoria-attention`); assert `animationDuration`/`transitionDuration` is `0s` or `0.001ms` (accept both serializations per RESEARCH assumption A2).
    - Theme-toggle smoke: on Home/shell, one shared-table scan screen, one object/evidence screen, and one overlay path — toggle theme via `#scoria-theme-toggle` (or the mobile id from 16-01-SUMMARY) and assert the `data-theme` (or equivalent) attribute flips and the page stays rendered/ready (`waitForReady` still passes, key landmark visible).

    Mobile-nav-item and mobile-topbar checks should use the 375px viewport so those elements are present. If a destructive/seeded fixture is needed for a screen, copy the serial/seed conventions from uat.spec.mjs; otherwise keep tests independent.
  </action>
  <verify>
    <automated>node --check priv/dev/e2e/phase16_parity.spec.mjs && grep -q "waitForReady" priv/dev/e2e/phase16_parity.spec.mjs && grep -q "reducedMotion" priv/dev/e2e/phase16_parity.spec.mjs && grep -q "375" priv/dev/e2e/phase16_parity.spec.mjs && ! grep -qE "toMatchSnapshot|toHaveScreenshot" priv/dev/e2e/phase16_parity.spec.mjs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `priv/dev/e2e/phase16_parity.spec.mjs` passes `node --check` (valid JS) and imports `waitForReady` from `./lib/ready.mjs`
    - Spec contains a 375px-viewport test asserting no page-level horizontal overflow on shell + one table screen
    - Spec contains focus-outline assertions run under both `colorScheme: 'light'` and `colorScheme: 'dark'` for ≥6 representative control types
    - Spec contains a `reducedMotion: 'reduce'` test asserting drawer/modal + skeleton/attention motion collapses to `0s`/`0.001ms`
    - Spec contains a theme-toggle test covering shell + table + evidence + overlay
    - Spec contains NO screenshot comparison (`toMatchSnapshot`/`toHaveScreenshot` absent)
    - With a seeded dev server running, `mix scoria.ui.e2e` runs the new spec green (run if a dev server is available; otherwise `node --check` + the grep gates are the automated proof and the human checkpoint in 16-05 covers the live run)
  </acceptance_criteria>
  <done>A selector-light, screenshot-free Playwright spec proves 375px overflow-free shell+table, both-theme focus visibility, reduced-motion collapse, and theme-toggle smoke, auto-discovered by the existing e2e lane.</done>
</task>

<task type="auto">
  <name>Task 2: Optional token contrast guard (D-31) — single-SSOT AA floor</name>
  <files>test/scoria_web/token_contrast_guard_test.exs</files>
  <read_first>
    - test/scoria_web/ui_drift_guard_test.exs (deterministic source-parsing guard pattern)
    - test/scoria_web/ds06_drift_guard_test.exs (token/file parsing + assertion style)
    - assets/css/02-tokens.css (the single token SSOT: light values + dark re-pointing; read the text/surface/focus-ring token names and resolved hex values)
    - .planning/phases/16-motion-responsive-theme-parity/16-RESEARCH.md (Token Contrast Guard Shape example + Open Question 1)
    - .planning/phases/16-motion-responsive-theme-parity/16-CONTEXT.md (D-31 — only build if it reuses 02-tokens.css with no parallel source)
  </read_first>
  <action>
    Decision gate per D-31: build this guard ONLY if it can read its color pairs and values exclusively from `assets/css/02-tokens.css` (and brandbook-approved pairings) without creating a second token source. If implementing it would require hardcoding a parallel color table that could drift from the SSOT, SKIP this task and document the skip + rationale in the SUMMARY (D-31 explicitly permits skipping).

    If implementing: create `test/scoria_web/token_contrast_guard_test.exs` that parses `assets/css/02-tokens.css` for the resolved light and dark hex values of the relevant semantic token pairs, computes WCAG contrast ratios, and asserts the floors: body text pairs (`--scoria-text` on `--scoria-surface-app`, `--scoria-text-muted` on `--scoria-surface-panel`) ≥ 4.5:1 in both themes; non-text/focus pairs (`--scoria-focus-ring` on `--scoria-surface-panel`/raised) ≥ 3:1 in both themes. Resolve token-to-token references (e.g. `--scoria-focus-ring: var(--scoria-molten-400)`) by reading the referenced primitive's value from the same file — do not hardcode the hex outside the parsed file. Implement the relative-luminance/contrast math inline (small, deterministic). Follow the existing drift-guard test module/describe/assert style. The guard is a floor, not a replacement for visual review (D-31).
  </action>
  <verify>
    <automated>if [ -f test/scoria_web/token_contrast_guard_test.exs ]; then mix test test/scoria_web/token_contrast_guard_test.exs 2>&1 | tail -10; else echo "SKIPPED per D-31 — verify SUMMARY documents rationale"; fi</automated>
  </verify>
  <acceptance_criteria>
    - EITHER `test/scoria_web/token_contrast_guard_test.exs` exists, reads color values ONLY from `assets/css/02-tokens.css` (no hardcoded parallel color table), asserts ≥4.5:1 body and ≥3:1 non-text/focus floors in both themes, and passes `mix test test/scoria_web/token_contrast_guard_test.exs`
    - OR the task is skipped with the SUMMARY documenting why a single-SSOT guard was not feasible (D-31)
    - If implemented, the guard does not introduce any second/parallel token definition source
  </acceptance_criteria>
  <done>A single-SSOT AA contrast floor guard exists and passes, OR the guard is skipped per D-31 with documented rationale; no parallel token source is created either way.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev tooling→running dev server | Playwright drives an already-running dev server; the contrast guard parses a static asset file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Denial of Service | CI e2e lane | mitigate | Keep Phase 16 checks targeted and selector-light under the existing `mix scoria.ui.e2e` lane; add no screenshot baselines or full axe/all-screen matrix (D-32) that would balloon CI. |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | No new packages — reuse the locked dev-only `@playwright/test` 1.60.0 from `priv/dev` (RESEARCH Package Legitimacy Audit). |

Assessment: No new attack surface in the shipped library. These are dev-only test artifacts (excluded from the Hex package) that read the running dashboard and a static token file.
</threat_model>

<verification>
- `node --check priv/dev/e2e/phase16_parity.spec.mjs` passes; spec contains 375px/focus/reduced-motion/theme checks and no screenshot comparisons.
- `mix scoria.ui.e2e` green against a seeded dev server (live run, or covered by 16-05 human checkpoint if no server available at execution time).
- Contrast guard passes OR is documented-skipped per D-31.
- `mix test` overall remains green.
</verification>

<success_criteria>
- The e2e lane gains targeted, selector-light, screenshot-free proof for MOTION-01..04.
- 375px overflow, both-theme focus visibility, reduced-motion collapse, and theme-toggle smoke are mechanically asserted.
- An optional single-SSOT contrast floor guard exists or is documented-skipped per D-31.
- No visual-regression/full-axe infrastructure is added (Phase 17 scope preserved).
</success_criteria>

<output>
Create `.planning/phases/16-motion-responsive-theme-parity/16-06-SUMMARY.md` when done.
</output>
