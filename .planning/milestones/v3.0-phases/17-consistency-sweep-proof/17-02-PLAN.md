---
phase: 17-consistency-sweep-proof
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - priv/dev/contact_sheet.mjs
  - priv/shots/.gitignore
  - priv/shots/contact_sheet_index.md
  - docs/MAINTAINERS.md
autonomous: true
requirements: [PROOF-02]
must_haves:
  truths:
    - "A maintainer can run a committed generator to produce a before/after contact sheet from two dated shot dirs"
    - "The generator is re-runnable for future milestones by pointing --before/--after at new dated dirs with no code change"
    - "The generated HTML grid is gitignored; only the committed index records the dir pair + per-screen delta notes"
    - "The contact-sheet command is documented in the existing MAINTAINERS harness section"
  artifacts:
    - path: "priv/dev/contact_sheet.mjs"
      provides: "Before/after contact-sheet generator (--before/--after/--out), plain Node fs/path, no Playwright"
      min_lines: 40
    - path: "priv/shots/contact_sheet_index.md"
      provides: "Committed index recording baseline dir, final dir, per-screen delta notes, regenerate command"
    - path: "priv/shots/.gitignore"
      provides: "*.html rule so generated contact_sheet.html stays gitignored"
      contains: "*.html"
  key_links:
    - from: "priv/dev/contact_sheet.mjs"
      to: "priv/shots/{before,after}/{screen}/*.png"
      via: "fs.readdir + screen/filename pairing"
      pattern: "readdir"
    - from: "priv/shots/contact_sheet_index.md"
      to: "priv/dev/contact_sheet.mjs"
      via: "documented regenerate command"
      pattern: "contact_sheet\\.mjs"
---

<objective>
Close PROOF-02: ship a COMMITTED contact-sheet GENERATOR (`priv/dev/contact_sheet.mjs`) plus a committed `priv/shots/contact_sheet_index.md`, while the rendered HTML grid stays gitignored and regenerable (D-05). The generator accepts `--before <dir> --after <dir>` so future milestones re-run by pointing at new dated dirs (D-06). It reads the baseline `priv/shots/2026-06-04/` and the fresh final dir produced by Plan 01, pairs PNGs by `screen/filename`, and emits an HTML before/after grid. Add `*.html` to `priv/shots/.gitignore` (RESEARCH finding #3 — existing rules miss a root-level HTML file) and document the new command in the existing MAINTAINERS harness section (D-11).

Purpose: PROOF-02 documents the iteration as a re-runnable basis for future passes — a committed generator + index (not a montage binary, not dead links to gitignored PNGs).
Output: `priv/dev/contact_sheet.mjs` (committed), `priv/shots/contact_sheet_index.md` (committed), `priv/shots/.gitignore` (+`*.html`), `docs/MAINTAINERS.md` (harness-section contact-sheet subsection).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-consistency-sweep-proof/17-CONTEXT.md
@.planning/phases/17-consistency-sweep-proof/17-RESEARCH.md
@.planning/phases/17-consistency-sweep-proof/17-PATTERNS.md

# Generator analog (arg-parsing shape, SCREENS manifest, PNG naming, main() guard):
@priv/dev/shots.mjs

# Gitignore to extend (self-analog):
@priv/shots/.gitignore

# Index analog + harness-section pattern to extend:
@priv/shots/gap_register.md
@docs/MAINTAINERS.md

# Plan 01 output — the {final-date} dir name to pass to --after:
@.planning/phases/17-consistency-sweep-proof/17-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write priv/dev/contact_sheet.mjs generator + extend priv/shots/.gitignore</name>
  <files>priv/dev/contact_sheet.mjs, priv/shots/.gitignore</files>
  <read_first>
    - priv/dev/shots.mjs (copy the JSDoc header shape, the parseArgs(argv) function shape, the SCREENS manifest with tenantScoped flags, the PNG naming `{presence}_{theme}_{viewport}.png`, and the `main().catch(...)` entry-point guard — see 17-PATTERNS.md §contact_sheet.mjs for exact line cites)
    - priv/shots/.gitignore (current rules `*/`, `*.png`, `*.json`, `!gap_register.md` — and the comment block to update)
    - .planning/phases/17-consistency-sweep-proof/17-PATTERNS.md §contact_sheet.mjs + §.gitignore (the exact import block, parseArgs result shape with before/after/out, and the one-line `*.html` addition)
    - .planning/phases/17-consistency-sweep-proof/17-RESEARCH.md §Contact-Sheet Generator (Pairing Logic, Known Empty-State Limitation, Assumption A2 relative img src paths)
  </read_first>
  <action>
    Create `priv/dev/contact_sheet.mjs` (D-06) modeled on `priv/dev/shots.mjs` but with NO Playwright dependency — plain Node `fs/promises`, `fs`, `path` only. Import block per 17-PATTERNS.md (readdir/readFile/writeFile, existsSync, join/relative/dirname, fileURLToPath). Mirror the `parseArgs(argv)` shape from shots.mjs but with result `{ before, after, out }`, default `out` = `priv/shots/contact_sheet.html`, and a guard that exits 1 if `--before` or `--after` is missing. Core logic: `readdir` both `--before` and `--after` dirs, build the set of `{screen}/{filename}.png` paths from each, and pair files by matching `screen/filename` across the two dirs. Emit an HTML file (to `--out`) with a CSS grid showing before/after image pairs per screen; use `<img src>` with RELATIVE paths from the output file's location (Assumption A2 — relative, NOT absolute, so the HTML renders on any machine) computed via `path.relative`. Handle the empty-state asymmetry gracefully (RESEARCH §Known Empty-State Limitation): non-tenant-scoped screens (reviews, eval_specs, prompts, prompt_release) only have `populated_*` pairs — skip-and-note missing `empty_*` pairs rather than erroring; log screens present in one dir but not the other. Include the JSDoc header documenting the `--before/--after/--out` usage. End with the `async function main()` + `main().catch((err) => { console.error(err); process.exit(1); })` guard. Then extend `priv/shots/.gitignore`: add a single `*.html` line after `*.json` (before the `!gap_register.md` negation) plus a matching comment line `#   *.html    — ignores generated contact-sheet HTML (contact_sheet.html)` (RESEARCH finding #3 / Pitfall 2 — a root-level `.html` is NOT caught by `*/`, `*.png`, or `*.json`). Do NOT touch `mix.exs` — `priv/dev/` and `priv/shots/` are already excluded from `package.files`; the generator inherits Hex exclusion (RESEARCH §Gitignore and Hex Hygiene).
  </action>
  <verify>
    <automated>node --check priv/dev/contact_sheet.mjs && node priv/dev/contact_sheet.mjs 2>&1 | grep -qi "before" && grep -q "\*.html" priv/shots/.gitignore && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `priv/dev/contact_sheet.mjs` exists and passes `node --check priv/dev/contact_sheet.mjs` (valid syntax)
    - Running `node priv/dev/contact_sheet.mjs` with no args exits non-zero and prints an error mentioning `--before`/`--after` required
    - The script imports no Playwright/chromium (grep for `playwright` returns nothing)
    - `priv/shots/.gitignore` contains a `*.html` rule
    - `mix.exs` is unchanged (`git diff --quiet mix.exs`)
  </acceptance_criteria>
  <done>Committed generator with `--before/--after/--out`, relative img paths, graceful empty-state handling; `*.html` gitignored; mix.exs untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the contact sheet, author contact_sheet_index.md, document command in MAINTAINERS harness section</name>
  <files>priv/shots/contact_sheet_index.md, docs/MAINTAINERS.md</files>
  <read_first>
    - priv/shots/gap_register.md (the committed-.md analog style for contact_sheet_index.md)
    - .planning/phases/17-consistency-sweep-proof/17-PATTERNS.md §contact_sheet_index.md (header/metadata block, per-screen delta-notes table, How-to-Regenerate section) and §MAINTAINERS.md (the existing "Screenshot + Critique Harness" section pattern + the Contact-sheet generation subsection to append)
    - docs/MAINTAINERS.md (the existing "Screenshot + Critique Harness (dev-only)" section ~line 161–243 — append the contact-sheet subsection AFTER the dev-only posture summary; do NOT rewrite the section, D-11)
    - .planning/phases/17-consistency-sweep-proof/17-01-SUMMARY.md (the actual {final-date} dir name from Plan 01)
    - brandbook/brand-book.md (calm/exact/useful voice)
  </read_first>
  <action>
    Run the generator against the real dirs: `node priv/dev/contact_sheet.mjs --before priv/shots/2026-06-04 --after priv/shots/{final-date} --out priv/shots/contact_sheet.html` (use the `{final-date}` recorded in Plan 01's SUMMARY). Confirm the generated `contact_sheet.html` is gitignored (`git status` must not show it as a tracked/untracked-to-commit file). Then hand-author `priv/shots/contact_sheet_index.md` (D-07) mirroring `gap_register.md`'s committed-.md style: header `# Contact Sheet Index — v3.0 Control Room Milestone Close`, metadata `**Baseline dir:** priv/shots/2026-06-04`, `**Final dir:** priv/shots/{final-date}`, `**Generated:** {final-date}`; a `## Per-Screen Delta Notes` table (Screen | State | Baseline File | Final File | Notes) covering all 9 screens — non-tenant-scoped screens (reviews, eval_specs, prompts, prompt_release) list only `populated_*` rows and the index states this explicitly so missing empty-state shots are not misread as a gap; Notes cite the Phase 14/15/16 improvements per screen (mobile nav, focus indicators, responsive shell, table density). End with a `## How to Regenerate` fenced block giving the exact generator command and a line that future passes substitute new baseline/final dirs with no code change. Finally, append a `### Contact-sheet generation` subsection to the EXISTING harness section of `docs/MAINTAINERS.md` (D-11 — verify the section stays accurate; do NOT rewrite it) per 17-PATTERNS.md: the `node priv/dev/contact_sheet.mjs --before … --after … --out …` command, a note the HTML is gitignored (`*.html`), and that `contact_sheet_index.md` (committed) records the dir pair. Voice: calm, exact, useful. Ensure no API key / tenant PII / secret leaks into the index or the doc.
  </action>
  <verify>
    <automated>test -f priv/shots/contact_sheet_index.md && grep -q "contact_sheet.mjs" priv/shots/contact_sheet_index.md && grep -q "Contact-sheet generation" docs/MAINTAINERS.md && git check-ignore priv/shots/contact_sheet.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `priv/shots/contact_sheet_index.md` exists, records `priv/shots/2026-06-04` (baseline dir) and the `{final-date}` final dir, and has a per-screen delta-notes table covering all 9 screens
    - The index's `## How to Regenerate` block contains the `contact_sheet.mjs --before … --after …` command
    - `git check-ignore priv/shots/contact_sheet.html` exits 0 (the generated HTML is gitignored)
    - `docs/MAINTAINERS.md` contains a `Contact-sheet generation` subsection inside the existing harness section (grep finds the heading)
    - No API key / secret / tenant PII string appears in `contact_sheet_index.md` or the new MAINTAINERS subsection
  </acceptance_criteria>
  <done>Contact sheet generated (gitignored); committed index records the dir pair + per-screen notes + regenerate command; MAINTAINERS harness section documents the new command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev tooling → committed repo | Committed `.mjs` generator + committed `.md` index; generated HTML gitignored; no runtime, no network, no user input |
| Hex package boundary | `priv/dev/` + `priv/shots/` already excluded from `package.files` — generator and index never ship to adopters |

Dev-only proof plan: no new routes, no backend capability, no auth surface, no committed binaries, no new dependency (plain Node fs/path, no new npm install). Minimal threat surface; the real hygiene items are (a) keeping the generated HTML gitignored so a binary-ish artifact is never committed, and (b) no secret/PII in the committed index.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Tampering | committed repo (binary posture) | mitigate | Task 1 adds `*.html` to `priv/shots/.gitignore`; Task 2 verify asserts `git check-ignore priv/shots/contact_sheet.html` exits 0 — generated grid never enters git |
| T-17-04 | Information Disclosure | `priv/shots/contact_sheet_index.md`, MAINTAINERS subsection | mitigate | Acceptance criteria assert no API key / secret / tenant PII string in the committed index or doc |
| T-17-05 | Tampering | Hex package contents | accept | `priv/dev/` + `priv/shots/` already excluded from `package.files`; no `mix.exs` change — generator inherits exclusion; Task 1 verify asserts `mix.exs` unchanged |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No package installs — generator uses only Node stdlib; nothing to audit |
</threat_model>

<verification>
- `node --check priv/dev/contact_sheet.mjs` exits 0
- `node priv/dev/contact_sheet.mjs` (no args) exits non-zero with a `--before/--after` error
- `git check-ignore priv/shots/contact_sheet.html` exits 0
- `priv/shots/contact_sheet_index.md` and the MAINTAINERS contact-sheet subsection exist
- `git diff --quiet mix.exs` (package.files untouched)
</verification>

<success_criteria>
PROOF-02 closed: a committed, re-runnable `--before/--after` generator + committed `contact_sheet_index.md` document the before/after iteration; the rendered HTML stays gitignored; the command is reachable from `docs/MAINTAINERS.md`.
</success_criteria>

<output>
Create `.planning/phases/17-consistency-sweep-proof/17-02-SUMMARY.md` when done.
</output>
