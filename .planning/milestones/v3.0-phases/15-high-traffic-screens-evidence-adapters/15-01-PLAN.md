---
phase: 15-high-traffic-screens-evidence-adapters
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria_web/ui.ex
  - assets/css/04-components.css
  - test/scoria_web/ui_component_test.exs
autonomous: true
requirements:
  - SCREEN-04
user_setup: []
must_haves:
  truths:
    - "D-18/D-19/D-21: ScoriaWeb.UI owns evidence section chrome, evidence rows, action rows, empty state framing, raw disclosure reuse, tone mapping, and accessibility semantics."
    - "The phase adds small evidence primitives only; it does not introduce a descriptor renderer, plugin registry, adapter behaviour, or UI schema language."
    - "D-26: New CSS is token-bound in assets/css/04-components.css and does not modify assets/css/02-tokens.css for cosmetic preference; token changes are reserved for accessibility defects or genuine coherence breaks."
    - "The primitives support the later adapter contract: adapters own projection/copy/events; UI primitives own repeated layout."
  artifacts:
    - path: "lib/scoria_web/ui.ex"
      provides: "Shared evidence primitives for notebook adapters"
      contains: "def evidence_section"
    - path: "assets/css/04-components.css"
      provides: "Token-bound evidence primitive classes"
      contains: ".scoria-evidence-section"
    - path: "test/scoria_web/ui_component_test.exs"
      provides: "Component tests for evidence primitives"
      contains: "evidence_section"
  key_links:
    - from: "lib/scoria_web/ui.ex"
      to: "lib/scoria_web/components/*_evidence*_component.ex"
      via: "evidence primitive imports"
      pattern: "evidence_section|evidence_rows|evidence_action_row|evidence_empty"
---

<objective>
Add the small shared evidence primitives that Phase 15 adapters need before any screen or adapter conversion begins.

Purpose: This prevents five later plans from duplicating section chrome, key-value rows, action rows, empty states, and raw disclosure markup.
Output: `ScoriaWeb.UI` evidence primitives, token-bound component CSS, and focused component tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-high-traffic-screens-evidence-adapters/15-CONTEXT.md
@.planning/phases/15-high-traffic-screens-evidence-adapters/15-RESEARCH.md
@.planning/phases/15-high-traffic-screens-evidence-adapters/15-VALIDATION.md
@.planning/phases/15-high-traffic-screens-evidence-adapters/15-PATTERNS.md
@.planning/phases/15-high-traffic-screens-evidence-adapters/15-UI-SPEC.md
@.planning/phases/12-design-system-component-layer/12-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared evidence primitives to ScoriaWeb.UI</name>
  <files>lib/scoria_web/ui.ex, test/scoria_web/ui_component_test.exs</files>
  <read_first>
    - lib/scoria_web/ui.ex
    - test/scoria_web/ui_component_test.exs
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-CONTEXT.md
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-RESEARCH.md
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-UI-SPEC.md
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: `evidence_section/1` renders a titled section with optional description and a slot for actions/content.
    - Test 2: `evidence_rows/1` renders stable label/value rows and preserves list order.
    - Test 3: `evidence_action_row/1` renders compact action/link content without inventing a new button grammar.
    - Test 4: `evidence_empty/1` renders notebook-scoped empty copy with an exact title/body and no raw palette classes.
    - Test 5: `raw_evidence/1` remains the single advanced raw disclosure primitive; do not duplicate it.
  </behavior>
  <action>Add four small function components to `ScoriaWeb.UI`: `evidence_section/1`, `evidence_rows/1`, `evidence_action_row/1`, and `evidence_empty/1`. `evidence_section/1` must accept `title`, optional `description`, optional `tone`/`badge`, optional `class`, an `:actions` slot, and required inner content. `evidence_rows/1` must accept `rows` as a list of `{label, value}` tuples or maps with `:label` and `:value`, render labels with label/meta typography, and render values in HEEx without `raw/1`. `evidence_action_row/1` must be a compact flex row for caller-provided links/buttons. `evidence_empty/1` must accept `title` and render slot body text in notebook context. Keep adapters responsible for data projection and event/link wiring; do not add a typed renderer, registry, behaviour, or schema language. If later plans need `<.button>` with `phx-value-step-id`, `phx-value-source`, or similar event values, extend the `button/1` global attr include list here deliberately and cover it with a source/component assertion.</action>
  <acceptance_criteria>
    - Source assertion: `lib/scoria_web/ui.ex` contains `def evidence_section(assigns)`, `def evidence_rows(assigns)`, `def evidence_action_row(assigns)`, and `def evidence_empty(assigns)`.
    - Source assertion: evidence primitives emit only `scoria-evidence-*`, existing `scoria-panel`, existing `scoria-badge`, or existing button/table/notebook classes; they do not emit raw palette classes.
    - Source assertion: no new `raw(` call appears in `lib/scoria_web/ui.ex`.
    - Behavior assertion: `test/scoria_web/ui_component_test.exs` verifies section title/body, row labels/values, action row content, and empty-state title/body.
    - Behavior assertion: component tests prove values containing `<script>` are escaped in rendered output.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/ui_component_test.exs test/scoria_web/ds06_drift_guard_test.exs</automated>
  </verify>
  <done>Shared evidence primitives exist, are tested, and preserve the thin-adapter contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add token-bound CSS for evidence primitives</name>
  <files>assets/css/04-components.css, test/scoria_web/ui_component_test.exs</files>
  <read_first>
    - assets/css/04-components.css
    - assets/css/02-tokens.css
    - test/scoria_web/ui_component_test.exs
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-UI-SPEC.md
    - .planning/phases/15-high-traffic-screens-evidence-adapters/15-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: evidence primitive tests render the expected class names.
    - Test 2: CSS source contains the evidence class families.
    - Test 3: no raw hex colors are added to the new CSS block.
  </behavior>
  <action>Add token-bound CSS to `assets/css/04-components.css` for `.scoria-evidence-section`, `.scoria-evidence-section__header`, `.scoria-evidence-section__description`, `.scoria-evidence-section__actions`, `.scoria-evidence-rows`, `.scoria-evidence-row`, `.scoria-evidence-row__label`, `.scoria-evidence-row__value`, `.scoria-evidence-action-row`, and `.scoria-evidence-empty`. Use only `var(--scoria-*)`, existing typography tokens, existing border/surface tokens, and existing spacing tokens. Do not edit `assets/css/02-tokens.css`. Keep border radius at existing Scoria component values and keep dense operator-dashboard spacing.</action>
  <acceptance_criteria>
    - Source assertion: `assets/css/04-components.css` contains `.scoria-evidence-section`, `.scoria-evidence-rows`, `.scoria-evidence-action-row`, and `.scoria-evidence-empty`.
    - Source assertion: the new CSS block contains no raw hex color literal and no Tailwind palette class string.
    - Behavior assertion: component tests assert rendered HTML includes `scoria-evidence-section`, `scoria-evidence-row`, `scoria-evidence-action-row`, and `scoria-evidence-empty`.
    - Source assertion: `assets/css/02-tokens.css` is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria_web/ui_component_test.exs test/scoria_web/ds06_drift_guard_test.exs && grep -q '.scoria-evidence-section' assets/css/04-components.css && grep -q '.scoria-evidence-rows' assets/css/04-components.css</automated>
  </verify>
  <done>Evidence primitive CSS exists, is token-bound, and is covered by UI component tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component caller to HEEx output | Evidence values passed from adapters may contain user/application data and must be escaped by HEEx. |
| CSS author to rendered UI | Static CSS classes must stay token-bound and not bypass the semantic token system. |
| package boundary | No package manager operation is needed for this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Information Disclosure | evidence rows/raw values | mitigate | Render evidence row values through HEEx, avoid `raw/1`, and keep advanced payloads inside the existing `raw_evidence/1` disclosure. |
| T-15-01-02 | Tampering | design-system gateway | mitigate | Add primitives only to `ScoriaWeb.UI` and CSS only to `04-components.css`; tests assert class names and escaped values. |
| T-15-01-SC | Tampering | package installs | accept | No dependency changes or package-manager commands are in this plan. |
</threat_model>

<verification>
Run `mix test test/scoria_web/ui_component_test.exs test/scoria_web/ds06_drift_guard_test.exs`. Confirm `lib/scoria_web/ui.ex` exposes the four evidence primitives, `assets/css/04-components.css` contains the `.scoria-evidence-*` classes, and `assets/css/02-tokens.css` has no diff.
</verification>

<success_criteria>
Plan 15-01 succeeds when later Phase 15 screens and adapters can use shared evidence sections, rows, action rows, and empty states without duplicating chrome, raw evidence disclosure, tone mapping, or tokenized CSS.
</success_criteria>

<output>
Create `.planning/phases/15-high-traffic-screens-evidence-adapters/15-01-SUMMARY.md` when done.
</output>
