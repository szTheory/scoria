---
phase: 54-executable-proof-and-closeout-truth
plan: 03
type: execute
wave: 2
depends_on: [54-01, 54-02]
files_modified:
  - .github/workflows/ci.yml
  - docs/operator_verification.md
  - .planning/phases/54-executable-proof-and-closeout-truth/54-VERIFICATION.md
autonomous: true
requirements: [DOCS-02, PROOF-01, PROOF-02]
must_haves:
  truths:
    - "Milestone closeout chain runs in this exact order: mix scoria.release_preview, mix test.adoption, mix test.runtime_to_handoff."
    - "CI and operator closeout documentation publish the same closeout sequence."
    - "Closeout evidence ledger records command result truth or a time-bounded exception protocol entry."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Canonical proof-lane sequence in CI"
      contains: "Run runtime-to-handoff proof lane"
    - path: "docs/operator_verification.md"
      provides: "Operator closeout command chain documentation"
      contains: "mix test.runtime_to_handoff"
    - path: ".planning/phases/54-executable-proof-and-closeout-truth/54-VERIFICATION.md"
      provides: "Auditable closeout evidence ledger"
      contains: "blocked_command"
  key_links:
    - from: "docs/operator_verification.md"
      to: ".github/workflows/ci.yml"
      via: "identical command ordering for closeout truth"
      pattern: "mix scoria\\.release_preview[\\s\\S]*mix test\\.adoption[\\s\\S]*mix test\\.runtime_to_handoff"
    - from: ".planning/phases/54-executable-proof-and-closeout-truth/54-VERIFICATION.md"
      to: "docs/operator_verification.md"
      via: "ledger evidence references canonical closeout chain"
      pattern: "closeout chain"
---

<objective>
Finalize closeout truth for Phase 54 with executable evidence and auditable records.

Purpose: Complete DOCS-02, PROOF-01, and PROOF-02 by aligning CI and operator closeout guidance to the shipped runtime-to-handoff lane and capturing reproducible command evidence in the phase ledger.
Output: CI runs the canonical closeout chain in order, operator verification docs match that chain, and `54-VERIFICATION.md` records either passing evidence or explicit temporary exception fields with owner and expiry.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/54-executable-proof-and-closeout-truth/54-CONTEXT.md
@.planning/phases/54-executable-proof-and-closeout-truth/54-RESEARCH.md
@.planning/phases/54-executable-proof-and-closeout-truth/54-PATTERNS.md
@.planning/phases/54-executable-proof-and-closeout-truth/54-VALIDATION.md
@.github/workflows/ci.yml
@docs/operator_verification.md
@.planning/phases/53-operator-evidence-and-lane-guidance/53-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 54-03-01: Align CI and operator closeout chain to canonical runtime-to-handoff proof</name>
  <requirement_ids>DOCS-02, PROOF-01</requirement_ids>
  <files>.github/workflows/ci.yml, docs/operator_verification.md</files>
  <read_first>
    .planning/phases/54-executable-proof-and-closeout-truth/54-CONTEXT.md
    .planning/phases/54-executable-proof-and-closeout-truth/54-RESEARCH.md
    .planning/phases/54-executable-proof-and-closeout-truth/54-PATTERNS.md
    .github/workflows/ci.yml
    docs/operator_verification.md
  </read_first>
  <action>Update `.github/workflows/ci.yml` by inserting a dedicated step named `Run runtime-to-handoff proof lane` with command `mix test.runtime_to_handoff` immediately after `Run adoption closure lane` and before broad `Run tests`. Update `docs/operator_verification.md` Maintainer closeout section so the canonical proof chain code block lists exactly three lines in this order: `mix scoria.release_preview`, `mix test.adoption`, `mix test.runtime_to_handoff`. Keep the guidance that CI should execute release preview with `MIX_ENV=dev` while preserving plain user-facing command strings in docs.</action>
  <acceptance_criteria>
    - `grep -n "Run adoption closure lane\\|Run runtime-to-handoff proof lane\\|Run tests" .github/workflows/ci.yml` returns all three step names.
    - `grep -n "mix test.runtime_to_handoff" .github/workflows/ci.yml docs/operator_verification.md` returns matches in both files.
    - `python - <<'PY'\nfrom pathlib import Path\nci = Path('.github/workflows/ci.yml').read_text()\nassert ci.index('mix test.adoption') < ci.index('mix test.runtime_to_handoff') < ci.index('\\n      - name: Run tests')\ndoc = Path('docs/operator_verification.md').read_text()\nassert doc.index('mix scoria.release_preview') < doc.index('mix test.adoption') < doc.index('mix test.runtime_to_handoff')\nprint('order-ok')\nPY` prints `order-ok`.
    - `grep -n "MIX_ENV=dev mix scoria.release_preview" .github/workflows/ci.yml` returns one CI execution entry.
    - `grep -n "mix test.handoff\\|mix scoria.test.handoff" docs/operator_verification.md` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/adoption_surface_test.exs</automated>
  </verify>
  <done>CI and operator closeout docs publish the same canonical sequence with runtime-to-handoff proof inserted before broad test execution.</done>
</task>

<task type="auto">
  <name>Task 54-03-02: Record closeout execution evidence in the phase verification ledger</name>
  <requirement_ids>DOCS-02, PROOF-01, PROOF-02</requirement_ids>
  <files>.planning/phases/54-executable-proof-and-closeout-truth/54-VERIFICATION.md</files>
  <read_first>
    .planning/phases/54-executable-proof-and-closeout-truth/54-CONTEXT.md
    .planning/phases/54-executable-proof-and-closeout-truth/54-VALIDATION.md
    .planning/phases/53-operator-evidence-and-lane-guidance/53-VERIFICATION.md
    .planning/phases/54-executable-proof-and-closeout-truth/54-VERIFICATION.md
  </read_first>
  <action>Create or update `.planning/phases/54-executable-proof-and-closeout-truth/54-VERIFICATION.md` with frontmatter fields `phase`, `verified`, `status`, and `score`. Add a `## Verification Commands` section listing the exact closeout commands with execution status: `MIX_ENV=dev mix scoria.release_preview`, `MIX_ENV=test mix test.adoption`, `MIX_ENV=test mix test.runtime_to_handoff`. Add a `## Exception Protocol` table with columns `blocked_command`, `blocker_evidence`, `compensating_checks`, `owner`, `expiry`, `rerun_due`; if no exception exists, record `none`/`n/a` values explicitly. Add a `## Requirement Coverage` section mapping each requirement ID (`DOCS-02`, `PROOF-01`, `PROOF-02`) to command/test evidence and final pass/fail result.</action>
  <acceptance_criteria>
    - `grep -n "^phase:\\|^verified:\\|^status:\\|^score:" .planning/phases/54-executable-proof-and-closeout-truth/54-VERIFICATION.md` returns all four frontmatter keys.
    - `grep -n "MIX_ENV=dev mix scoria.release_preview\\|MIX_ENV=test mix test.adoption\\|MIX_ENV=test mix test.runtime_to_handoff" .planning/phases/54-executable-proof-and-closeout-truth/54-VERIFICATION.md` returns all three commands.
    - `grep -n "blocked_command\\|blocker_evidence\\|compensating_checks\\|owner\\|expiry\\|rerun_due" .planning/phases/54-executable-proof-and-closeout-truth/54-VERIFICATION.md` returns all exception protocol fields.
    - `grep -n "DOCS-02\\|PROOF-01\\|PROOF-02" .planning/phases/54-executable-proof-and-closeout-truth/54-VERIFICATION.md` returns all three requirement IDs.
    - `MIX_ENV=dev mix scoria.release_preview && MIX_ENV=test mix test.adoption && MIX_ENV=test mix test.runtime_to_handoff` completes successfully, or a filled exception row records blocking evidence with owner and expiry.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=dev mix scoria.release_preview && MIX_ENV=test mix test.adoption && MIX_ENV=test mix test.runtime_to_handoff</automated>
  </verify>
  <done>Phase 54 closeout ledger contains reproducible command evidence and explicit requirement coverage mapping for DOCS-02, PROOF-01, and PROOF-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI pipeline order -> release truth | CI sequence determines whether command contract regressions fail before broad suite noise. |
| Verification ledger -> milestone closeout claims | Ledger evidence is the authoritative source for completion claims and exception handling. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-54-07 | Repudiation | high | `54-VERIFICATION.md` closeout evidence | block | Block closeout unless the ledger records the full three-command chain outcomes or a fully populated exception row with owner and expiry. | Grep acceptance criteria plus full closeout command execution. |
| T-54-08 | Tampering | high | `.github/workflows/ci.yml` command order | block | Block merge if runtime-to-handoff lane is missing or ordered after broad `mix test`. | Python order check and CI config grep acceptance criteria. |
| T-54-09 | Denial of Service | medium | closeout command guidance | mitigate | Keep canonical closeout chain bounded and lane-specific to avoid forcing optional-lane setup during default closeout. | Operator guide grep checks and closeout chain run command. |

High-severity threats T-54-07 and T-54-08 are hard blockers: Phase 54 cannot be marked complete until both are closed.
</threat_model>

<verification>
Run task-level checks after each task, then execute:

```bash
MIX_ENV=test mix test test/scoria/adoption_surface_test.exs
MIX_ENV=dev mix scoria.release_preview && MIX_ENV=test mix test.adoption && MIX_ENV=test mix test.runtime_to_handoff
```
</verification>

<success_criteria>
- CI includes `mix test.runtime_to_handoff` in canonical closeout order after adoption lane and before broad suite.
- Operator closeout docs publish the same three-command chain without alias drift.
- `54-VERIFICATION.md` records command evidence and explicit requirement coverage for DOCS-02, PROOF-01, and PROOF-02.
- Any blocked command path is documented with owner, expiry, and rerun commitment before closeout claims are accepted.
</success_criteria>

<output>
After completion, create `.planning/phases/54-executable-proof-and-closeout-truth/54-03-SUMMARY.md`.
</output>
