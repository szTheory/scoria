---
phase: 54-executable-proof-and-closeout-truth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/operator_verification.md
  - docs/adoption_lanes.md
  - docs/phoenix_runtime_example.md
  - docs/bounded_handoffs.md
  - test/scoria/adoption_surface_test.exs
  - test/support/scoria/adoption_example.ex
  - test/scoria/phoenix_example_source_test.exs
  - test/scoria/handoff_example_source_test.exs
autonomous: true
requirements: [DOCS-02, PROOF-02]
must_haves:
  truths:
    - "README and all support guides name the same canonical runtime-to-handoff verifier command: mix test.runtime_to_handoff."
    - "Support surfaces keep mix test.adoption as the default-lane verifier and runtime-to-handoff as bounded escalation proof."
    - "Docs and drift tests explicitly state semantic fast-path, knowledge, retrieval, and hosted onboarding are not prerequisites for the runtime-to-handoff lane."
  artifacts:
    - path: "README.md"
      provides: "Top-level lane command contract and lane hierarchy wording"
      contains: "mix test.runtime_to_handoff"
    - path: "docs/operator_verification.md"
      provides: "Operator-facing runtime-to-handoff verification guidance"
      contains: "mix test.runtime_to_handoff"
    - path: "test/scoria/adoption_surface_test.exs"
      provides: "Canonical command assert/refute drift matrix"
      contains: "refute content =~ \"mix test.handoff\""
    - path: "test/support/scoria/adoption_example.ex"
      provides: "Shared source fragment literals for command consistency"
      contains: "mix test.runtime_to_handoff"
  key_links:
    - from: "test/support/scoria/adoption_example.ex"
      to: "test/scoria/phoenix_example_source_test.exs"
      via: "shared command fragment assertions"
      pattern: "AdoptionExample"
    - from: "test/scoria/adoption_surface_test.exs"
      to: "README.md docs/adoption_lanes.md docs/operator_verification.md docs/phoenix_runtime_example.md docs/bounded_handoffs.md"
      via: "string-level support-surface command contract checks"
      pattern: "mix test\\.runtime_to_handoff"
---

<objective>
Align all public support surfaces to the canonical runtime-to-handoff proof command.

Purpose: Satisfy DOCS-02 and the documentation facet of PROOF-02 by making one command contract explicit across README, operator docs, lane guide, runtime example, bounded handoff guide, and drift tests.
Output: Every required support surface uses `mix test.runtime_to_handoff`, preserves `mix test.adoption` as default-lane verifier, and documents runtime-to-handoff prerequisite boundaries truthfully.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-executable-proof-and-closeout-truth/54-CONTEXT.md
@.planning/phases/54-executable-proof-and-closeout-truth/54-RESEARCH.md
@.planning/phases/54-executable-proof-and-closeout-truth/54-PATTERNS.md
@.planning/phases/54-executable-proof-and-closeout-truth/54-UI-SPEC.md
@README.md
@docs/operator_verification.md
@docs/adoption_lanes.md
@docs/phoenix_runtime_example.md
@docs/bounded_handoffs.md
@test/scoria/adoption_surface_test.exs
@test/support/scoria/adoption_example.ex
@test/scoria/phoenix_example_source_test.exs
@test/scoria/handoff_example_source_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 54-02-01: Update support docs to a single runtime-to-handoff command contract</name>
  <requirement_ids>DOCS-02</requirement_ids>
  <files>README.md, docs/operator_verification.md, docs/adoption_lanes.md, docs/phoenix_runtime_example.md, docs/bounded_handoffs.md</files>
  <read_first>
    .planning/phases/54-executable-proof-and-closeout-truth/54-CONTEXT.md
    .planning/phases/54-executable-proof-and-closeout-truth/54-RESEARCH.md
    .planning/phases/54-executable-proof-and-closeout-truth/54-UI-SPEC.md
    README.md
    docs/operator_verification.md
    docs/adoption_lanes.md
    docs/phoenix_runtime_example.md
    docs/bounded_handoffs.md
  </read_first>
  <action>Update all five support surfaces so runtime-to-handoff verification uses the exact command string `mix test.runtime_to_handoff`. Keep `mix test.adoption` explicitly labeled as default-lane verifier and position runtime-to-handoff as bounded escalation proof. Insert one explicit non-prerequisite sentence in each of `README.md`, `docs/operator_verification.md`, and `docs/adoption_lanes.md` with this target wording: `This lane does not require semantic fast-path setup, knowledge/pgvector bootstrap, retrieval setup, or hosted onboarding setup.` In `docs/phoenix_runtime_example.md` and `docs/bounded_handoffs.md`, tie the command to the same run-detail path by naming `Scoria.get_run_detail/1` and `delegated_handoffs` near the runtime-to-handoff verifier text.</action>
  <acceptance_criteria>
    - `grep -n "mix test.runtime_to_handoff" README.md docs/operator_verification.md docs/adoption_lanes.md docs/phoenix_runtime_example.md docs/bounded_handoffs.md` returns at least one match in each file.
    - `grep -n "mix test.adoption" README.md docs/operator_verification.md docs/adoption_lanes.md` returns matches in all three files.
    - `grep -n "does not require semantic fast-path setup, knowledge/pgvector bootstrap, retrieval setup, or hosted onboarding setup" README.md docs/operator_verification.md docs/adoption_lanes.md` returns at least one match.
    - `grep -n "Scoria.get_run_detail/1\\|delegated_handoffs" docs/phoenix_runtime_example.md docs/bounded_handoffs.md` returns matches in both docs.
    - `grep -n "mix test.handoff\\|mix scoria.test.handoff\\|runtime_to_handoff --trace" README.md docs/operator_verification.md docs/adoption_lanes.md docs/phoenix_runtime_example.md docs/bounded_handoffs.md` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/adoption_surface_test.exs</automated>
  </verify>
  <done>Required support surfaces publish one canonical runtime-to-handoff command while preserving default-lane-first adoption and explicit non-prerequisite boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 54-02-02: Migrate docs/source drift guards to Phase 54 command truth</name>
  <requirement_ids>DOCS-02, PROOF-02</requirement_ids>
  <files>test/scoria/adoption_surface_test.exs, test/support/scoria/adoption_example.ex, test/scoria/phoenix_example_source_test.exs, test/scoria/handoff_example_source_test.exs</files>
  <read_first>
    .planning/phases/54-executable-proof-and-closeout-truth/54-CONTEXT.md
    .planning/phases/54-executable-proof-and-closeout-truth/54-PATTERNS.md
    test/scoria/adoption_surface_test.exs
    test/support/scoria/adoption_example.ex
    test/scoria/phoenix_example_source_test.exs
    test/scoria/handoff_example_source_test.exs
  </read_first>
  <action>Replace Phase 53 runtime-to-handoff command refutations in `test/scoria/adoption_surface_test.exs` with positive assertions for `mix test.runtime_to_handoff` across README, lane guide, operator guide, runtime example, and bounded handoff guide. Keep explicit refutations for non-canonical synonyms `mix test.handoff` and `mix scoria.test.handoff`. Extend `test/support/scoria/adoption_example.ex` fragment lists to include literal command fragment `mix test.runtime_to_handoff` plus the explicit lane distinction fragment `mix test.adoption`. Update `test/scoria/phoenix_example_source_test.exs` and `test/scoria/handoff_example_source_test.exs` only if needed so both consume the new shared fragments and fail on command drift.</action>
  <acceptance_criteria>
    - `grep -n "assert content =~ \"mix test.runtime_to_handoff\"" test/scoria/adoption_surface_test.exs` returns assertion lines.
    - `grep -n "refute content =~ \"mix test.handoff\"\\|refute content =~ \"mix scoria.test.handoff\"" test/scoria/adoption_surface_test.exs` returns refutation lines.
    - `grep -n "mix test.runtime_to_handoff\\|mix test.adoption" test/support/scoria/adoption_example.ex` returns fragment literals.
    - `grep -n "AdoptionExample" test/scoria/phoenix_example_source_test.exs test/scoria/handoff_example_source_test.exs` returns matches in both files.
    - `MIX_ENV=test mix test test/scoria/adoption_surface_test.exs test/scoria/phoenix_example_source_test.exs test/scoria/handoff_example_source_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/adoption_surface_test.exs test/scoria/phoenix_example_source_test.exs test/scoria/handoff_example_source_test.exs</automated>
  </verify>
  <done>Phase 54 command-contract drift tests assert the canonical runtime-to-handoff command and reject ambiguous synonyms across support surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Support docs -> adopter execution behavior | Docs directly determine which commands operators run and trust. |
| Drift tests -> support truth enforcement | ExUnit file assertions are the gate that blocks command-contract divergence. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-54-04 | Repudiation | high | README/docs command naming | block | Block merge if any required support surface omits `mix test.runtime_to_handoff` or promotes a synonym. | Grep acceptance criteria plus `MIX_ENV=test mix test test/scoria/adoption_surface_test.exs`. |
| T-54-05 | Spoofing | high | docs/source drift tests | block | Block merge until drift tests assert canonical command and refute `mix test.handoff` / `mix scoria.test.handoff`. | `MIX_ENV=test mix test test/scoria/adoption_surface_test.exs test/scoria/phoenix_example_source_test.exs test/scoria/handoff_example_source_test.exs`. |
| T-54-06 | Information Disclosure | medium | prerequisite guidance wording | mitigate | Require explicit non-prerequisite statements so operators do not over-provision optional semantic/knowledge/retrieval setup for canonical proof. | Grep acceptance criteria on non-prerequisite sentence in docs. |

High-severity threats T-54-04 and T-54-05 are hard blockers: no closeout ledger entry is valid while either remains unresolved.
</threat_model>

<verification>
Run task-level checks after each task, then execute:

```bash
MIX_ENV=test mix test test/scoria/adoption_surface_test.exs test/scoria/phoenix_example_source_test.exs test/scoria/handoff_example_source_test.exs
```
</verification>

<success_criteria>
- DOCS-02 is satisfied with one canonical runtime-to-handoff command across all required support surfaces.
- PROOF-02 docs contract explicitly states optional semantic/knowledge/retrieval/hosted setup are not required.
- Drift tests fail fast on command alias drift or reintroduced placeholder phrasing.
- Default lane (`mix test.adoption`) remains clearly distinct from bounded runtime-to-handoff proof.
</success_criteria>

<output>
After completion, create `.planning/phases/54-executable-proof-and-closeout-truth/54-02-SUMMARY.md`.
</output>
