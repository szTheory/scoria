---
phase: 54-executable-proof-and-closeout-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/scoria.test.runtime_to_handoff.ex
  - lib/mix/tasks/test.runtime_to_handoff.ex
  - mix.exs
  - test/mix/tasks/test.runtime_to_handoff_test.exs
  - test/scoria/runtime_test.exs
  - test/scoria/runtime_integration_test.exs
autonomous: true
requirements: [PROOF-01, PROOF-02]
must_haves:
  truths:
    - "Scoria ships one bounded runtime-to-handoff proof lane command: mix test.runtime_to_handoff."
    - "The runtime-to-handoff lane executes a narrow public-contract test subset and is not a broad mix test alias."
    - "The lane proves optional semantic, knowledge, retrieval, and hosted onboarding setup are not prerequisites."
  artifacts:
    - path: "lib/mix/tasks/scoria.test.runtime_to_handoff.ex"
      provides: "Canonical bounded runtime-to-handoff lane implementation"
      contains: "@runtime_to_handoff_test_files"
    - path: "lib/mix/tasks/test.runtime_to_handoff.ex"
      provides: "Adopter-facing wrapper command"
      contains: "Mix.Tasks.Test.RuntimeToHandoff"
    - path: "mix.exs"
      provides: "Preferred env mapping for both task names"
      contains: "test.runtime_to_handoff"
    - path: "test/mix/tasks/test.runtime_to_handoff_test.exs"
      provides: "Lane discoverability and non-prerequisite contract assertions"
      contains: "refute Enum.any?"
  key_links:
    - from: "mix.exs"
      to: "lib/mix/tasks/test.runtime_to_handoff.ex"
      via: "preferred_envs routes command to test env"
      pattern: "test\\.runtime_to_handoff"
    - from: "lib/mix/tasks/scoria.test.runtime_to_handoff.ex"
      to: "test/mix/tasks/test.runtime_to_handoff_test.exs"
      via: "bounded test-file list contract"
      pattern: "runtime_to_handoff_test_files"
---

<objective>
Add the bounded executable runtime-to-handoff proof lane.

Purpose: Satisfy PROOF-01 and the executable portion of PROOF-02 by introducing a dedicated command lane that validates the runtime-to-handoff public contract without optional-lane setup.
Output: `mix test.runtime_to_handoff` runs a bounded set of runtime-to-handoff proof files, is discoverable through Mix task registry, and explicitly excludes optional semantic/knowledge/hosted-onboarding setup paths.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-executable-proof-and-closeout-truth/54-CONTEXT.md
@.planning/phases/54-executable-proof-and-closeout-truth/54-RESEARCH.md
@.planning/phases/54-executable-proof-and-closeout-truth/54-PATTERNS.md
@.planning/phases/54-executable-proof-and-closeout-truth/54-VALIDATION.md
@mix.exs
@lib/mix/tasks/test.adoption.ex
@lib/mix/tasks/scoria.test.semantic_fast_path.ex
@lib/mix/tasks/scoria.test.knowledge.ex
@test/mix/tasks/test.adoption_test.exs
@test/scoria/runtime_test.exs
@test/scoria/runtime_integration_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 54-01-01: Add canonical runtime-to-handoff lane command and discoverability test</name>
  <requirement_ids>PROOF-01</requirement_ids>
  <files>lib/mix/tasks/scoria.test.runtime_to_handoff.ex, lib/mix/tasks/test.runtime_to_handoff.ex, mix.exs, test/mix/tasks/test.runtime_to_handoff_test.exs</files>
  <read_first>
    .planning/phases/54-executable-proof-and-closeout-truth/54-CONTEXT.md
    .planning/phases/54-executable-proof-and-closeout-truth/54-RESEARCH.md
    .planning/phases/54-executable-proof-and-closeout-truth/54-PATTERNS.md
    mix.exs
    lib/mix/tasks/test.adoption.ex
    test/mix/tasks/test.adoption_test.exs
  </read_first>
  <action>Create `lib/mix/tasks/scoria.test.runtime_to_handoff.ex` with module `Mix.Tasks.Scoria.Test.RuntimeToHandoff`, shortdoc `Runs the bounded runtime-to-handoff verification lane`, and a function `runtime_to_handoff_test_files/0`. Set `@runtime_to_handoff_test_files` exactly to `test/scoria/runtime_test.exs`, `test/scoria/runtime_integration_test.exs`, `test/scoria/adoption_surface_test.exs`, `test/scoria/phoenix_example_source_test.exs`, `test/scoria/handoff_example_source_test.exs`, and `test/mix/tasks/test.runtime_to_handoff_test.exs`. Implement `run/1` with `Mix.Task.run("loadpaths")`, `Mix.Task.reenable("test")`, and `Mix.Task.run("test", args ++ @runtime_to_handoff_test_files)`. Add `lib/mix/tasks/test.runtime_to_handoff.ex` wrapper module `Mix.Tasks.Test.RuntimeToHandoff` that delegates to `Mix.Tasks.Scoria.Test.RuntimeToHandoff.run/1`. Update `mix.exs` `preferred_envs` with both `"scoria.test.runtime_to_handoff": :test` and `"test.runtime_to_handoff": :test`. Add `test/mix/tasks/test.runtime_to_handoff_test.exs` mirroring the adoption discoverability pattern and asserting both tasks are registered.</action>
  <acceptance_criteria>
    - `grep -n "defmodule Mix.Tasks.Scoria.Test.RuntimeToHandoff" lib/mix/tasks/scoria.test.runtime_to_handoff.ex` returns a module declaration line.
    - `grep -n "runtime_to_handoff_test_files" lib/mix/tasks/scoria.test.runtime_to_handoff.ex test/mix/tasks/test.runtime_to_handoff_test.exs` returns matches in both files.
    - `grep -n "\"scoria.test.runtime_to_handoff\": :test\\|\"test.runtime_to_handoff\": :test" mix.exs` returns two preferred-env entries.
    - `grep -n "Mix.Task.get(\"scoria.test.runtime_to_handoff\")\\|Mix.Task.get(\"test.runtime_to_handoff\")" test/mix/tasks/test.runtime_to_handoff_test.exs` returns two discoverability assertions.
    - `MIX_ENV=test mix test test/mix/tasks/test.runtime_to_handoff_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/mix/tasks/test.runtime_to_handoff_test.exs</automated>
  </verify>
  <done>`mix test.runtime_to_handoff` resolves to a bounded lane task, runs under `MIX_ENV=test`, and is enforced by a discoverability contract test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 54-01-02: Prove optional-lane prerequisites are excluded from the canonical lane</name>
  <requirement_ids>PROOF-02</requirement_ids>
  <files>lib/mix/tasks/scoria.test.runtime_to_handoff.ex, test/mix/tasks/test.runtime_to_handoff_test.exs, test/scoria/runtime_test.exs, test/scoria/runtime_integration_test.exs</files>
  <read_first>
    .planning/phases/54-executable-proof-and-closeout-truth/54-CONTEXT.md
    .planning/phases/54-executable-proof-and-closeout-truth/54-RESEARCH.md
    lib/mix/tasks/scoria.test.semantic_fast_path.ex
    lib/mix/tasks/scoria.test.knowledge.ex
    test/scoria/runtime_test.exs
    test/scoria/runtime_integration_test.exs
    test/test_helper.exs
  </read_first>
  <action>Extend `test/mix/tasks/test.runtime_to_handoff_test.exs` with explicit negative-contract assertions that `runtime_to_handoff_test_files/0` contains no semantic-cache, semantic-fast-path, knowledge, install, or generated-host onboarding proof files. Keep this lane implementation free of `Mix.Task.run("app.start")`, `Mix.Task.run("scoria.pgvector.bootstrap")`, `Migrations.migrate_knowledge!`, and `System.put_env("SCORIA_TEST_INCLUDE_KNOWLEDGE", "true")`. In runtime contract tests, ensure at least one runtime-to-handoff path assertion remains in `test/scoria/runtime_test.exs` and one end-to-end public facade assertion remains in `test/scoria/runtime_integration_test.exs` so lane pass/fail reflects real runtime behavior, not docs-only checks.</action>
  <acceptance_criteria>
    - `grep -n "refute Enum.any?(.*semantic\\|refute Enum.any?(.*knowledge\\|refute Enum.any?(.*install\\|refute Enum.any?(.*host_app_consumer" test/mix/tasks/test.runtime_to_handoff_test.exs` returns at least three negative-contract assertion lines.
    - `grep -n "Mix.Task.run(\"app.start\")\\|scoria.pgvector.bootstrap\\|migrate_knowledge!\\|SCORIA_TEST_INCLUDE_KNOWLEDGE" lib/mix/tasks/scoria.test.runtime_to_handoff.ex` returns no matches.
    - `grep -n "Scoria.start_handoff_run\\|unsafe_projected_context\\|delegated_handoffs" test/scoria/runtime_test.exs` returns runtime handoff contract assertions.
    - `grep -n "Scoria.start_run\\|Scoria.get_run_detail\\|Scoria.get_run" test/scoria/runtime_integration_test.exs` returns runtime facade assertions.
    - `env -u SCORIA_TEST_INCLUDE_KNOWLEDGE MIX_ENV=test mix test.runtime_to_handoff` passes.
  </acceptance_criteria>
  <verify>
    <automated>env -u SCORIA_TEST_INCLUDE_KNOWLEDGE MIX_ENV=test mix test.runtime_to_handoff</automated>
  </verify>
  <done>The canonical runtime-to-handoff proof lane executes without optional semantic/knowledge/bootstrap prerequisites and still proves runtime public-contract behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mix command contract -> maintainer/adopter workflow | The published command string determines what operators trust as executable proof. |
| Lane file list -> optional-lane isolation | Included files define whether optional semantic/knowledge setup leaks into canonical proof. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-54-01 | Spoofing | high | `mix test.runtime_to_handoff` task wiring | block | Block merge unless wrapper module, namespaced task, and both preferred-env mappings are present and discoverable. | `MIX_ENV=test mix test test/mix/tasks/test.runtime_to_handoff_test.exs` plus grep acceptance criteria. |
| T-54-02 | Denial of Service | medium | runtime lane execution path | mitigate | Prevent optional-lane setup calls (`app.start`, knowledge migrations, pgvector bootstrap) from entering the canonical lane implementation. | Grep refute checks in task acceptance criteria and lane execution command. |
| T-54-03 | Repudiation | medium | bounded file-list contract | mitigate | Assert exact bounded file list and explicit exclusion checks to prevent silent broad-suite drift. | `env -u SCORIA_TEST_INCLUDE_KNOWLEDGE MIX_ENV=test mix test.runtime_to_handoff`. |

High-severity threat T-54-01 is a hard blocker: do not proceed to closeout planning or release verification while it is open.
</threat_model>

<verification>
Run task-level checks after each task, then execute:

```bash
MIX_ENV=test mix test test/mix/tasks/test.runtime_to_handoff_test.exs
env -u SCORIA_TEST_INCLUDE_KNOWLEDGE MIX_ENV=test mix test.runtime_to_handoff
```
</verification>

<success_criteria>
- PROOF-01 is met with a discoverable bounded `mix test.runtime_to_handoff` lane.
- PROOF-02 executable guards confirm no semantic/knowledge/retrieval/hosted-onboarding setup is required to run the lane.
- `mix.exs` preferred env mappings keep both runtime-to-handoff task names pinned to `:test`.
- Lane regressions fail fast via dedicated task contract tests.
</success_criteria>

<output>
After completion, create `.planning/phases/54-executable-proof-and-closeout-truth/54-01-SUMMARY.md`.
</output>
