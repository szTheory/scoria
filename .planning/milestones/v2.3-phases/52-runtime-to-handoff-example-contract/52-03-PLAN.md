---
phase: 52-runtime-to-handoff-example-contract
plan: 03
type: execute
wave: 3
depends_on:
  - 52-02
files_modified:
  - docs/bounded_handoffs.md
  - test/support/scoria/adoption_example.ex
  - test/scoria/adoption_surface_test.exs
  - test/scoria/handoff_example_source_test.exs
autonomous: true
requirements:
  - EXMP-01
  - EXMP-02
must_haves:
  truths:
    - "Projected context is documented as a bounded, explicit handoff payload rather than hidden runtime state."
    - "Rejected projected-context inputs name truthful runtime behavior and the `:unsafe_projected_context` error."
    - "The host app owns identity, escalation policy, prompt/draft selection, and projected-context selection."
    - "Scoria owns durable run creation, unsafe projected-context rejection, queued delegated child creation, and curated readback through Scoria.get_run_detail/1."
  artifacts:
    - path: "docs/bounded_handoffs.md"
      provides: "Projected-context safety and host/Scoria ownership boundary documentation"
      contains: ":unsafe_projected_context"
    - path: "test/scoria/adoption_surface_test.exs"
      provides: "Public docs safety assertions"
      contains: "host app owns identity, escalation policy, prompt or draft selection, and projected-context selection"
    - path: "test/support/scoria/adoption_example.ex"
      provides: "Pinned handoff guide fragments"
      contains: "Scoria rejects the call with `{:error, :unsafe_projected_context}`"
  key_links:
    - from: "docs/bounded_handoffs.md"
      to: "lib/scoria/runtime/params.ex"
      via: "unsafe projected_context rejection wording"
      pattern: ":unsafe_projected_context"
    - from: "docs/bounded_handoffs.md"
      to: "Scoria.get_run_detail/1"
      via: "curated readback boundary"
      pattern: "Scoria\\.get_run_detail\\(started\\.run_id\\)"
---

<objective>
Document projected-context safety, rejection behavior, and the host-app ownership boundary for the runtime-to-handoff example.

Purpose: Satisfy EXMP-02 without hiding unsafe inputs, leaking raw workflow state, or implying Scoria owns host escalation policy.
Output: Hardened bounded handoff documentation and source-alignment tests that pin safety and ownership language.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md
@.planning/phases/52-runtime-to-handoff-example-contract/52-RESEARCH.md
@.planning/phases/52-runtime-to-handoff-example-contract/52-VALIDATION.md
@.planning/phases/52-runtime-to-handoff-example-contract/52-PATTERNS.md
@.planning/phases/52-runtime-to-handoff-example-contract/52-02-SUMMARY.md
@docs/bounded_handoffs.md
@test/support/scoria/adoption_example.ex
@test/scoria/adoption_surface_test.exs
@test/scoria/handoff_example_source_test.exs
@lib/scoria/runtime/params.ex
@lib/scoria/runtime/run_detail.ex

<interfaces>
Runtime rejection behavior to document:
```elixir
{:error, :unsafe_projected_context}
```

Curated readback behavior to document:
```elixir
{:ok, detail} = Scoria.get_run_detail(started.run_id)
delegated = detail.delegated_handoffs
```

Do not document raw `Scoria.Workflows` table reads as the adopter-facing inspection path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden bounded handoff safety and ownership docs</name>
  <files>docs/bounded_handoffs.md, test/support/scoria/adoption_example.ex</files>
  <read_first>
    docs/bounded_handoffs.md
    test/support/scoria/adoption_example.ex
    .planning/phases/52-runtime-to-handoff-example-contract/52-EXAMPLE-SHAPE.md
    .planning/phases/52-runtime-to-handoff-example-contract/52-02-SUMMARY.md
    .planning/phases/52-runtime-to-handoff-example-contract/52-PATTERNS.md
    lib/scoria/runtime/params.ex
    lib/scoria/runtime/run_detail.ex
  </read_first>
  <action>In `docs/bounded_handoffs.md`, add or update a section titled `## Host and Scoria ownership boundary` immediately after `## Core contract`. The section must include these exact sentences: `The host app owns identity, escalation policy, prompt or draft selection, and projected-context selection.` `Scoria owns durable run creation, projected-context validation, queued delegated child creation, and curated readback through \`Scoria.get_run_detail/1\`.` `Scoria does not copy hidden transcript, provider session, socket assigns, cookies, headers, or secrets into the handoff.` In `## Safety rule: projected context must stay narrow`, add a short rejected example code block that calls `Scoria.start_handoff_run(identity, "critic", root_role_id: "planner", delegated_kind: "review", handoff_input: %{"brief" => "Review the draft answer for policy and accuracy"}, projected_context: %{"request_headers" => %{"authorization" => "secret"}})` and asserts `{:error, :unsafe_projected_context}`. Include the exact sentence `Scoria rejects the call with \`{:error, :unsafe_projected_context}\` before creating a durable delegated run.` Update `test/support/scoria/adoption_example.ex` `handoff_doc_fragments/0` to include these exact fragments: `Host and Scoria ownership boundary`, `The host app owns identity, escalation policy, prompt or draft selection, and projected-context selection.`, `Scoria owns durable run creation, projected-context validation, queued delegated child creation, and curated readback through \`Scoria.get_run_detail/1\`.`, `Scoria does not copy hidden transcript, provider session, socket assigns, cookies, headers, or secrets into the handoff.`, `{:error, :unsafe_projected_context}`, and `before creating a durable delegated run.`</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/handoff_example_source_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "## Host and Scoria ownership boundary" docs/bounded_handoffs.md` returns one match.
    - `grep -F "The host app owns identity, escalation policy, prompt or draft selection, and projected-context selection." docs/bounded_handoffs.md test/support/scoria/adoption_example.ex` returns matches in both files.
    - `grep -F "Scoria owns durable run creation, projected-context validation, queued delegated child creation, and curated readback through \`Scoria.get_run_detail/1\`." docs/bounded_handoffs.md test/support/scoria/adoption_example.ex` returns matches in both files.
    - `grep -F "Scoria rejects the call with \`{:error, :unsafe_projected_context}\` before creating a durable delegated run." docs/bounded_handoffs.md` returns one match.
    - `MIX_ENV=test mix test test/scoria/handoff_example_source_test.exs` passes.
  </acceptance_criteria>
  <done>The bounded handoff guide explicitly documents host ownership, Scoria ownership, unsafe projected-context rejection, and curated readback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin public docs safety assertions</name>
  <files>test/scoria/adoption_surface_test.exs, test/scoria/handoff_example_source_test.exs</files>
  <read_first>
    test/scoria/adoption_surface_test.exs
    test/scoria/handoff_example_source_test.exs
    test/support/scoria/adoption_example.ex
    docs/bounded_handoffs.md
    .planning/phases/52-runtime-to-handoff-example-contract/52-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `Scoria.AdoptionSurfaceTest` asserts the bounded handoff guide contains the ownership boundary, `{:error, :unsafe_projected_context}`, and `before creating a durable delegated run`.
    - Test 2: `Scoria.AdoptionSurfaceTest` refutes raw internals and hidden context transfer strings in the bounded handoff guide.
  </behavior>
  <action>Update the existing `test "bounded handoff guide documents the narrow public delegation lane"` in `test/scoria/adoption_surface_test.exs`. Add assertions for `Host and Scoria ownership boundary`, `The host app owns identity, escalation policy, prompt or draft selection, and projected-context selection.`, `Scoria owns durable run creation, projected-context validation, queued delegated child creation, and curated readback through \`Scoria.get_run_detail/1\`.`, `{:error, :unsafe_projected_context}`, and `before creating a durable delegated run`. Add refutations for `Scoria.Workflows.create_run`, `Repo.all`, `workflow_steps`, `workflow_handoffs`, `copy hidden transcript`, and `provider_session token`. Do not remove the existing assertions for `mix test.adoption`, `separate verifier lane`, broad runtime-state keys, or `/scoria/workflows/:run_id`. Leave `test/scoria/handoff_example_source_test.exs` on the existing fragment-loop pattern unless the test needs a module name or fixture path fix.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/adoption_surface_test.exs test/scoria/handoff_example_source_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "{:error, :unsafe_projected_context}" test/scoria/adoption_surface_test.exs` returns one match.
    - `grep -F "Scoria.Workflows.create_run" test/scoria/adoption_surface_test.exs` returns one match inside a refutation.
    - `grep -F "workflow_handoffs" test/scoria/adoption_surface_test.exs` returns one match inside a refutation.
    - `MIX_ENV=test mix test test/scoria/adoption_surface_test.exs test/scoria/handoff_example_source_test.exs` passes.
  </acceptance_criteria>
  <done>Public docs tests fail if bounded handoff documentation loses projected-context safety, ownership boundary, curated readback, or raw-table leakage protections.</done>
</task>

<task type="auto">
  <name>Task 3: Run Phase 52 docs/source and full validation lanes</name>
  <files>docs/bounded_handoffs.md, test/support/scoria/adoption_example.ex, test/scoria/adoption_surface_test.exs, test/scoria/handoff_example_source_test.exs</files>
  <read_first>
    .planning/phases/52-runtime-to-handoff-example-contract/52-VALIDATION.md
    docs/bounded_handoffs.md
    test/support/scoria/adoption_example.ex
    test/scoria/adoption_surface_test.exs
    test/scoria/handoff_example_source_test.exs
  </read_first>
  <action>Run `MIX_ENV=test mix test test/scoria/adoption_surface_test.exs test/scoria/handoff_example_source_test.exs`. Then run `MIX_ENV=test mix test test/scoria/runtime_test.exs test/scoria/adoption_surface_test.exs test/scoria/handoff_example_source_test.exs test/scoria/phoenix_example_source_test.exs`. If PostgreSQL is unreachable, record the DB failure and the docs/source command result in `52-03-SUMMARY.md`; do not edit tests to skip DB-backed runtime rejection behavior. Verify that docs do not contain `Scoria.Workflows.create_run`, `workflow_handoffs`, or `provider_session token`.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/adoption_surface_test.exs test/scoria/handoff_example_source_test.exs</automated>
    <automated>MIX_ENV=test mix test test/scoria/runtime_test.exs test/scoria/adoption_surface_test.exs test/scoria/handoff_example_source_test.exs test/scoria/phoenix_example_source_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The docs/source command passes.
    - The full Phase 52 command passes when PostgreSQL is reachable.
    - If PostgreSQL is unreachable, `52-03-SUMMARY.md` records the exact connection failure and does not claim DB-backed tests passed.
    - `grep -F "Scoria.Workflows.create_run" docs/bounded_handoffs.md` returns no matches.
    - `grep -F "workflow_handoffs" docs/bounded_handoffs.md` returns no matches.
    - `grep -F "provider_session token" docs/bounded_handoffs.md` returns no matches.
  </acceptance_criteria>
  <done>Projected-context and ownership docs are pinned by source tests and verified through the Phase 52 validation contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host app docs -> adopter implementation | Documentation controls what adopters copy into Phoenix controllers and support flows. |
| Projected context -> durable handoff run | Host-provided maps cross into runtime validation and persistence. |
| Runtime detail -> support/operator evidence | Scoria exposes curated detail through `Scoria.get_run_detail/1`, not raw workflow internals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Information Disclosure | `docs/bounded_handoffs.md` projected-context section | mitigate | Document rejected `request_headers` example and exact `{:error, :unsafe_projected_context}` behavior before durable delegated run creation. |
| T-52-02 | Elevation of Privilege | host/Scoria ownership boundary | mitigate | Require docs and tests to pin `The host app owns identity, escalation policy, prompt or draft selection, and projected-context selection.` |
| T-52-03 | Spoofing / Repudiation | session/run wording | mitigate | Preserve existing `session_id` and `run_id` assertions in adoption docs tests and keep bounded handoff language tied to `/scoria/workflows/:run_id`. |
| T-52-04 | Information Disclosure | raw internal readback leakage | mitigate | Add tests refuting `Scoria.Workflows.create_run`, `Repo.all`, `workflow_steps`, and `workflow_handoffs`; docs must point to `Scoria.get_run_detail/1`. |
</threat_model>

<verification>
Docs/source quick command:
```bash
MIX_ENV=test mix test test/scoria/adoption_surface_test.exs test/scoria/handoff_example_source_test.exs
```

Full Phase 52 command when PostgreSQL is reachable:
```bash
MIX_ENV=test mix test test/scoria/runtime_test.exs test/scoria/adoption_surface_test.exs test/scoria/handoff_example_source_test.exs test/scoria/phoenix_example_source_test.exs
```
</verification>

<success_criteria>
- `docs/bounded_handoffs.md` contains the host/Scoria ownership boundary section.
- The guide demonstrates `{:error, :unsafe_projected_context}` for rejected projected context and states rejection happens before durable delegated run creation.
- Public docs tests pin the ownership boundary and refute raw workflow readback strings.
- Phase 52 verification is truthful about PostgreSQL availability.
</success_criteria>

<output>
After completion, create `.planning/phases/52-runtime-to-handoff-example-contract/52-03-SUMMARY.md`
</output>
