---
phase: 53-operator-evidence-and-lane-guidance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/runtime/run_detail.ex
  - lib/scoria_web/components/delegated_evidence_component.ex
  - test/scoria/runtime_test.exs
  - test/scoria_web/live/workflow_live_test.exs
autonomous: true
requirements: [EVID-01]
must_haves:
  truths:
    - "Operator can inspect a default runtime run with no delegated handoffs without reading it as failed adoption."
    - "Operator can inspect same-run delegated lineage, projected-context summary, and delegated outcome/status together on /scoria/workflows/:run_id."
    - "Pending delegated child readback remains visible as child step pending."
  artifacts:
    - path: "lib/scoria/runtime/run_detail.ex"
      provides: "Curated detail.delegated_handoffs DTO projection"
      contains: "child_step_pending"
    - path: "lib/scoria_web/components/delegated_evidence_component.ex"
      provides: "One Delegated Evidence section with id=\"delegated-evidence\""
      contains: "Inspect Delegated Evidence"
    - path: "test/scoria/runtime_test.exs"
      provides: "Runtime DTO coverage for delegated_handoffs"
      contains: "delegated_handoffs"
    - path: "test/scoria_web/live/workflow_live_test.exs"
      provides: "LiveView evidence state coverage"
      contains: "No Delegated Handoffs Recorded"
  key_links:
    - from: "lib/scoria_web/live/workflow_live/show.ex"
      to: "lib/scoria/runtime/run_detail.ex"
      via: "detail.delegated_handoffs assigned to @delegated_handoffs"
      pattern: "assign\\(:delegated_handoffs, detail\\.delegated_handoffs\\)"
    - from: "lib/scoria_web/components/delegated_evidence_component.ex"
      to: "Scoria.get_run_detail/1 DTO"
      via: "renders only curated delegated_handoffs"
      pattern: "attr :delegated_handoffs, :list, required: true"
---

<objective>
Verify and, if needed, tighten the operator evidence projection for the runtime-to-handoff example.

Purpose: Satisfy EVID-01 by keeping delegated lineage, projected context, and delegated status inspectable through the existing curated operator surface, per D-01 through D-04 and the approved UI contract.
Output: Runtime DTO and LiveView evidence tests pass with one stable `id="delegated-evidence"` section, valid default-lane empty copy, pending-state copy, and populated delegated evidence.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-operator-evidence-and-lane-guidance/53-CONTEXT.md
@.planning/phases/53-operator-evidence-and-lane-guidance/53-RESEARCH.md
@.planning/phases/53-operator-evidence-and-lane-guidance/53-PATTERNS.md
@.planning/phases/53-operator-evidence-and-lane-guidance/53-VALIDATION.md
@.planning/phases/53-operator-evidence-and-lane-guidance/53-UI-SPEC.md
@lib/scoria/runtime/run_detail.ex
@lib/scoria_web/components/delegated_evidence_component.ex
@lib/scoria_web/live/workflow_live/show.ex
@test/scoria/runtime_test.exs
@test/scoria_web/live/workflow_live_test.exs

<interfaces>
Runtime facade and DTO contract:
```elixir
def get_run_detail(run_id), do: Runtime.get_run_detail(run_id)
def get_run_detail!(run_id), do: Runtime.get_run_detail!(run_id)

%{
  handoff_id: handoff.id,
  parent_step_id: handoff.step_id,
  parent_step_sequence: parent_step && parent_step.sequence,
  delegated_role_id: handoff.delegated_role_id,
  delegated_kind: handoff.delegated_kind,
  handoff_input: handoff.handoff_input,
  child_step_id: child_step && child_step.id,
  child_status: child_step_status(child_step),
  status: child_step_status(child_step),
  projected_context: child_projected_context(child_step)
}

defp child_step_status(nil), do: "child_step_pending"
defp child_projected_context(nil), do: %{}
```

LiveView wiring contract:
```elixir
<DelegatedEvidenceComponent.render delegated_handoffs={@delegated_handoffs} />
assign(:delegated_handoffs, detail.delegated_handoffs)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 53-01-01: Pin curated delegated handoff DTO evidence</name>
  <files>lib/scoria/runtime/run_detail.ex, test/scoria/runtime_test.exs</files>
  <read_first>
    .planning/phases/53-operator-evidence-and-lane-guidance/53-CONTEXT.md
    .planning/phases/53-operator-evidence-and-lane-guidance/53-RESEARCH.md
    .planning/phases/53-operator-evidence-and-lane-guidance/53-PATTERNS.md
    lib/scoria/runtime/run_detail.ex
    test/scoria/runtime_test.exs
  </read_first>
  <behavior>
    - Default runtime runs return `detail.delegated_handoffs == []`.
    - Handoff rows without child readback return `child_status: "child_step_pending"`, `status: "child_step_pending"`, and `projected_context: %{}`.
    - Completed delegated child readback includes parent step id, child step id, delegated role/kind, handoff input, projected context, and child status from curated DTO data.
  </behavior>
  <action>Review existing DTO coverage before editing. If `test/scoria/runtime_test.exs` does not already assert all three behaviors above, add assertions to the existing delegated-handoff tests using the current `detail.delegated_handoffs` shape. Only edit `lib/scoria/runtime/run_detail.ex` if those assertions reveal a real missing curated field. Preserve `Scoria.get_run_detail/1` as the public readback path per D-04. Do not introduce raw `workflow_steps`, `workflow_handoffs`, `Repo.all`, or `Scoria.Workflows.create_run` readback in the runtime DTO or tests.</action>
  <acceptance_criteria>
    - `grep -n "child_step_pending" test/scoria/runtime_test.exs` returns at least one assertion line.
    - `grep -n "delegated_handoffs" test/scoria/runtime_test.exs` returns assertions for non-empty and empty delegated collections.
    - `grep -n "projected_context" test/scoria/runtime_test.exs` returns an assertion in delegated-handoff runtime coverage.
    - `grep -n "workflow_handoffs\\|workflow_steps\\|Repo.all\\|Scoria.Workflows.create_run" test/scoria/runtime_test.exs` does not show new public readback assertions for this task.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/runtime_test.exs</automated>
  </verify>
  <done>Runtime DTO coverage proves delegated_handoffs is curated, empty default-lane state is valid, pending child readback stays explicit, and projected context remains read through the DTO.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 53-01-02: Tighten Delegated Evidence UI states</name>
  <files>lib/scoria_web/components/delegated_evidence_component.ex, test/scoria_web/live/workflow_live_test.exs</files>
  <read_first>
    .planning/phases/53-operator-evidence-and-lane-guidance/53-CONTEXT.md
    .planning/phases/53-operator-evidence-and-lane-guidance/53-UI-SPEC.md
    .planning/phases/53-operator-evidence-and-lane-guidance/53-PATTERNS.md
    lib/scoria_web/components/delegated_evidence_component.ex
    lib/scoria_web/live/workflow_live/show.ex
    test/scoria_web/live/workflow_live_test.exs
  </read_first>
  <behavior>
    - The workflow page contains exactly one section with `id="delegated-evidence"`.
    - The section includes the link text `Inspect Delegated Evidence` pointing to `#delegated-evidence`.
    - Empty state renders `No Delegated Handoffs Recorded` and the exact sentence `This run stayed on the default runtime lane. No bounded handoff is required for first adoption; use Scoria.start_handoff_run/3 only when a same-run delegation needs narrow projected context.`
    - Pending state renders `child step pending`.
    - Populated state renders lineage, projected context preview/full context, handoff input, and child status together from `@delegated_handoffs`.
  </behavior>
  <action>Update `DelegatedEvidenceComponent` copy and tests to satisfy the UI contract. Keep the only section anchor as `id="delegated-evidence"`, keep the CTA text exactly `Inspect Delegated Evidence`, and keep the empty-state heading exactly `No Delegated Handoffs Recorded`. Replace any empty-state body that implies handoff is needed for adoption with: `This run stayed on the default runtime lane. No bounded handoff is required for first adoption; use Scoria.start_handoff_run/3 only when a same-run delegation needs narrow projected context.` Keep `View full context` as a native `<details>` disclosure. Render only data supplied by the `delegated_handoffs` assign; do not add raw workflow table names or a new route.</action>
  <acceptance_criteria>
    - `grep -n "id=\"delegated-evidence\"" lib/scoria_web/components/delegated_evidence_component.ex | wc -l` outputs `1`.
    - `grep -n "Inspect Delegated Evidence" lib/scoria_web/components/delegated_evidence_component.ex test/scoria_web/live/workflow_live_test.exs` returns matches in both files.
    - `grep -n "This run stayed on the default runtime lane" lib/scoria_web/components/delegated_evidence_component.ex test/scoria_web/live/workflow_live_test.exs` returns matches in both files.
    - `grep -n "child step pending" lib/scoria_web/components/delegated_evidence_component.ex test/scoria_web/live/workflow_live_test.exs` returns matches in both files.
    - `grep -n "workflow_handoffs\\|workflow_steps\\|Repo.all" lib/scoria_web/components/delegated_evidence_component.ex` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria_web/live/workflow_live_test.exs</automated>
  </verify>
  <done>The operator workflow page exposes one delegated evidence anchor with default, pending, and populated evidence states that satisfy D-01 through D-04 and the UI copy contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime DTO -> LiveView component | Curated runtime data crosses into operator-visible HTML. |
| persisted projected context -> operator readback | Host-supplied projected context becomes visible evidence through `detail.delegated_handoffs`. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-53-01 | Information Disclosure | medium | `RunDetail.delegated_handoffs` | mitigate | Keep evidence readback on `Scoria.get_run_detail/1` and curated `detail.delegated_handoffs`; do not add raw workflow table inspection or new public API. | `MIX_ENV=test mix test test/scoria/runtime_test.exs` and grep acceptance criteria refuting raw internals. |
| T-53-02 | Repudiation | low | `DelegatedEvidenceComponent` empty/pending states | mitigate | Keep `No Delegated Handoffs Recorded` as valid default-lane copy and keep `child step pending` visible when handoff is recorded before child readback. | `MIX_ENV=test mix test test/scoria_web/live/workflow_live_test.exs`. |
| T-53-03 | Tampering | low | `/scoria/workflows/:run_id` evidence layout | accept | Phase 53 adds no mutation controls and no destructive operator actions; existing route behavior remains read-only for this surface. | No new form, event, or API mutation added in files_modified. |
</threat_model>

<verification>
Run the plan-local commands after each task, then run:

```bash
MIX_ENV=test mix test test/scoria/runtime_test.exs test/scoria_web/live/workflow_live_test.exs
```
</verification>

<success_criteria>
- EVID-01 is satisfied for default empty, pending delegated, and populated delegated evidence states.
- `DelegatedEvidenceComponent` keeps one `id="delegated-evidence"` section and the `Inspect Delegated Evidence` anchor.
- Operator evidence uses curated `detail.delegated_handoffs` data only.
- No Phase 54 proof command, prerequisite-denial proof, or support-surface command naming is added.
</success_criteria>

<output>
After completion, create `.planning/phases/53-operator-evidence-and-lane-guidance/53-01-SUMMARY.md`.
</output>
