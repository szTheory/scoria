---
phase: 04-evaluation-flywheel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/99999999999999_create_eval_tables.exs
  - lib/scoria/eval/dataset.ex
  - lib/scoria/eval/dataset_item.ex
  - lib/scoria/eval/eval_spec.ex
  - lib/scoria/eval/eval_run.ex
  - lib/scoria/eval/score.ex
  - lib/scoria/eval.ex
  - test/scoria/eval_test.exs
autonomous: true
requirements:
  - EVAL-01
must_haves:
  truths:
    - "Dataset modifications create new immutable versions."
    - "Evaluation specs (rubrics) can be stored and versioned."
  artifacts:
    - path: "lib/scoria/eval/dataset.ex"
      provides: "Dataset schema"
    - path: "lib/scoria/eval.ex"
      provides: "Context API for immutable versioning"
  key_links:
    - from: "lib/scoria/eval.ex"
      to: "Scoria.Repo"
      via: "Ecto.Multi transaction"
      pattern: "Ecto\\.Multi"
---

<objective>
Build the core Ecto schemas and context logic for the evaluation flywheel, ensuring datasets and rubrics follow an immutable snapshot versioning pattern.

Purpose: To guarantee absolute determinism in CI pipelines by making sure evaluation data is never mutated in place.
Output: Migrations, schemas, and the `Scoria.Eval` context API with tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database Migrations for Evaluation Tables</name>
  <files>priv/repo/migrations/99999999999999_create_eval_tables.exs</files>
  <action>Generate a single migration file containing `ai_datasets`, `ai_dataset_items`, `ai_eval_specs`, `ai_eval_runs`, and `ai_scores`. The `ai_datasets` and `ai_eval_specs` tables must include `entity_id` (uuid) and `version` (integer) to support immutable snapshots, alongside an `is_current` (boolean, default: true) flag. Use `binary_id` for primary keys.</action>
  <verify>
    <automated>mix ecto.migrate</automated>
  </verify>
  <done>All tables created successfully in the test and dev databases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Evaluation Ecto Schemas</name>
  <files>lib/scoria/eval/dataset.ex, lib/scoria/eval/dataset_item.ex, lib/scoria/eval/eval_spec.ex, lib/scoria/eval/eval_run.ex, lib/scoria/eval/score.ex</files>
  <behavior>
    - Dataset and EvalSpec define `entity_id` and `version` fields.
    - Dataset has many DatasetItems.
    - EvalRun belongs to Dataset and EvalSpec.
  </behavior>
  <action>Create the 5 Ecto schemas mapping to the respective tables. Define the foreign key types as `:binary_id` and primary keys as `:binary_id`. Implement standard Ecto `changeset/2` functions for validation.</action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Schemas compile cleanly with associations established.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scoria.Eval Context with Immutable Versioning</name>
  <files>lib/scoria/eval.ex, test/scoria/eval_test.exs</files>
  <behavior>
    - Test 1: update_dataset/2 creates a new row with incremented version and sets old row is_current = false.
    - Test 2: update_dataset/2 clones associated dataset_items to the new dataset version.
  </behavior>
  <action>Implement the `Scoria.Eval` context module. Provide `create_dataset/1` and `update_dataset/2`. `update_dataset/2` must use `Ecto.Multi` to deprecate the current version (`is_current: false`), insert the new version row, and insert clones of the existing dataset_items pointing to the new `dataset_id`. Implement the same for `update_eval_spec/2`.</action>
  <verify>
    <automated>mix test test/scoria/eval_test.exs</automated>
  </verify>
  <done>Tests pass proving dataset updates create immutable snapshots and clone associations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB -> Code | Loading dataset items into memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | `update_dataset` | mitigate | Immutable snapshots prevent historical data tampering; data cannot be updated in place via standard operations. |
</threat_model>

<verification>
- Tables exist.
- Ecto schemas compile and relate.
- Multi transaction logic successfully versions datasets.
</verification>

<success_criteria>
- Operator can insert and version datasets immutably.
- Ecto schemas fully represent the evaluation flywheel data model.
</success_criteria>

<output>
After completion, create `.planning/phases/04-evaluation-flywheel/04-01-SUMMARY.md`
</output>