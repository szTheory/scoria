---
phase: 04-evaluation-flywheel
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/scoria/eval.ex
  - test/scoria/eval_test.exs
  - lib/scoria_web/live/dataset_live/promote_component.ex
  - test/scoria_web/live/dataset_live/promote_component_test.exs
  - lib/scoria_web/live/eval_spec_live/index.ex
  - test/scoria_web/live/eval_spec_live/index_test.exs
autonomous: true
requirements:
  - EVAL-02
must_haves:
  truths:
    - "Operator can promote a failed trace into a versioned dataset via the UI."
    - "Operator can view and edit evaluation rubrics via LiveView."
  artifacts:
    - path: "lib/scoria_web/live/dataset_live/promote_component.ex"
      provides: "LiveComponent for dataset promotion"
    - path: "lib/scoria_web/live/eval_spec_live/index.ex"
      provides: "LiveView for EvalSpecs"
  key_links:
    - from: "lib/scoria_web/live/dataset_live/promote_component.ex"
      to: "Scoria.Eval"
      via: "context call"
      pattern: "Scoria\\.Eval\\.promote"
    - from: "lib/scoria_web/live/eval_spec_live/index.ex"
      to: "Scoria.Eval"
      via: "context call"
      pattern: "Scoria\\.Eval\\.update_eval_spec"
---

<objective>
Build the LiveView UI integration allowing operators to promote a failed production trace into a versioned dataset snapshot directly, and manage versioned rubrics.

Purpose: Complete the production-to-CI feedback loop directly from the UI, embracing the "SaaS in a Box" philosophy.
Output: Context promotion logic, Promote component, and EvalSpec LiveView index.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promotion Context Logic</name>
  <files>lib/scoria/eval.ex, test/scoria/eval_test.exs</files>
  <behavior>
    - Test 1: promote_trace_to_dataset/2 creates dataset and item from a given trace struct.
  </behavior>
  <action>Extend `Scoria.Eval` with `promote_trace_to_dataset(trace, dataset_attrs)`. This function takes an existing Trace and its Spans (loaded from `Scoria.Observe` or `Scoria.Repo.Trace`), transforms them into `ai_dataset_items` attributes, and inserts a new immutable `ai_dataset` snapshot with these items. Ensure tests assert the trace conversion is correct.</action>
  <verify>
    <automated>mix test test/scoria/eval_test.exs</automated>
  </verify>
  <done>Traces can be seamlessly mapped and saved as Dataset items.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promote Trace LiveComponent</name>
  <files>lib/scoria_web/live/dataset_live/promote_component.ex, test/scoria_web/live/dataset_live/promote_component_test.exs</files>
  <behavior>
    - Test 1: Component renders promotion button and triggers promote logic on submit.
  </behavior>
  <action>Build `ScoriaWeb.DatasetLive.PromoteComponent` (a `Phoenix.LiveComponent`). It accepts a `trace` assign. Provide a form to specify the target dataset name. On submit, invoke `Scoria.Eval.promote_trace_to_dataset/2`. Include basic LiveView tests to verify rendering and event handling.</action>
  <verify>
    <automated>mix test test/scoria_web/live/dataset_live/promote_component_test.exs</automated>
  </verify>
  <done>Component accurately handles promotion form lifecycle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: EvalSpec Rubric Editor UI</name>
  <files>lib/scoria_web/live/eval_spec_live/index.ex, test/scoria_web/live/eval_spec_live/index_test.exs</files>
  <behavior>
    - Test 1: EvalSpec index LiveView mounts and allows creating new rubric versions.
  </behavior>
  <action>Create `ScoriaWeb.EvalSpecLive.Index`, a LiveView dashboard to view and edit evaluation rubrics (`EvalSpec`). Upon editing a rubric, it must utilize `Scoria.Eval.update_eval_spec/2` to ensure a new immutable version is stored instead of mutating the current row. Focus on rendering the list and basic edit form logic. Write ExUnit tests to verify LiveView mount and form submission behavior.</action>
  <verify>
    <automated>mix test test/scoria_web/live/eval_spec_live/index_test.exs</automated>
  </verify>
  <done>LiveView correctly renders and handles form submissions, verified by ExUnit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> UI -> DB | Operator editing rubrics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Elevation of Privilege | `EvalSpecLive.Index` | mitigate | Standard Phoenix session auth checks (inherited from parent live session) protect the route. |
</threat_model>

<verification>
- Promotion logic maps Traces to Dataset Items.
- LiveComponent renders form and triggers context functions.
- EvalSpec UI allows creating new rubric versions.
</verification>

<success_criteria>
- Operators can convert traces to datasets via the LiveComponent.
- Domain experts can tweak "Tone/Helpfulness" rubrics directly in the dashboard, storing them immutably.
</success_criteria>

<output>
After completion, create `.planning/phases/04-evaluation-flywheel/04-03-SUMMARY.md`
</output>