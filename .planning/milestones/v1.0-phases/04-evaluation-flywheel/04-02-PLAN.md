---
phase: 04-evaluation-flywheel
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - test/support/eval_case.ex
  - test/support/eval_case_test.exs
  - lib/mix/tasks/scoria.eval.ex
  - test/mix/tasks/scoria.eval_test.exs
autonomous: true
requirements:
  - EVAL-03
  - EVAL-04
must_haves:
  truths:
    - "Operator can execute ExUnit tests utilizing Tribunal for evaluations."
    - "Operator can run a mix task to execute LLM-as-judge benchmarks."
  artifacts:
    - path: "test/support/eval_case.ex"
      provides: "ExUnit macro for Tribunal"
    - path: "lib/mix/tasks/scoria.eval.ex"
      provides: "Mix task for running evals"
  key_links:
    - from: "test/support/eval_case.ex"
      to: "ExUnit.CaseTemplate"
      via: "use ExUnit.CaseTemplate"
      pattern: "use ExUnit\\.CaseTemplate"
    - from: "lib/mix/tasks/scoria.eval.ex"
      to: "Scoria.Eval"
      via: "context call"
      pattern: "Scoria\\.Eval\\."
---

<objective>
Integrate Tribunal into Scoria to act as the execution engine for evaluations, enabling deterministic ExUnit unit testing and a dedicated Mix task for LLM-as-judge runs.

Purpose: Segregate fast deterministic checks from slow API-bound LLM evaluations while preserving idiomatic Elixir developer ergonomics.
Output: ExUnit case template macro and `mix scoria.eval` task.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ExUnit Evaluation Macro (EvalCase)</name>
  <files>test/support/eval_case.ex, test/support/eval_case_test.exs</files>
  <behavior>
    - Test 1: Module using EvalCase successfully imports Tribunal functions and sets up Sandbox.
  </behavior>
  <action>Create `test/support/eval_case.ex` using `ExUnit.CaseTemplate`. The `using` macro should import `Scoria.EvalCase`, import `Tribunal`, and configure `Scoria.Repo` for SQL sandbox isolation. Write a simple dummy test in `test/support/eval_case_test.exs` using the macro to assert it loads and compiles correctly.</action>
  <verify>
    <automated>mix test test/support/eval_case_test.exs</automated>
  </verify>
  <done>Macro compiles and test runs with access to Tribunal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LLM-as-Judge Mix Task</name>
  <files>lib/mix/tasks/scoria.eval.ex, test/mix/tasks/scoria.eval_test.exs</files>
  <behavior>
    - Test 1: Task executes safely without crashing, starting Repo.
  </behavior>
  <action>Create `lib/mix/tasks/scoria.eval.ex` (`Mix.Tasks.Scoria.Eval`). The `run/1` function should start the Ecto Repo (`Mix.Task.run("app.start")`), fetch a target dataset via `Scoria.Eval` context (using CLI args to select dataset), and invoke `Tribunal` evaluation over the items. For now, implement basic CLI parsing and the runner shell. Add a unit test verifying the task parses args and runs.</action>
  <verify>
    <automated>mix test test/mix/tasks/scoria.eval_test.exs</automated>
  </verify>
  <done>Task executes and handles arguments correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI -> Eval Task | CLI arguments parsing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Denial of Service | `mix scoria.eval` | mitigate | Slow LLM tasks are segregated from ExUnit main suite, preventing CI timeouts across standard test suites. |
</threat_model>

<verification>
- `use Scoria.EvalCase` injects Tribunal and Sandbox.
- `mix scoria.eval` parses args and runs.
</verification>

<success_criteria>
- Developers can write Tribunal evaluations inside standard ExUnit files.
- Operators can trigger long-running evaluations via Mix task.
</success_criteria>

<output>
After completion, create `.planning/phases/04-evaluation-flywheel/04-02-SUMMARY.md`
</output>