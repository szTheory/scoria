---
phase: 01-core-observability
plan: 04
type: execute
wave: 3
depends_on:
  - 02
  - 03
files_modified:
  - lib/scoria/observe/telemetry.ex
  - lib/scoria/observe/adapters/req_llm.ex
  - lib/scoria/observe/adapters/jido.ex
  - test/scoria/observe/telemetry_test.exs
  - test/scoria/observe/adapters/req_llm_test.exs
  - test/scoria/observe/adapters/jido_test.exs
autonomous: true
requirements:
  - OBS-02
  - OBS-05
must_haves:
  truths:
    - Erlang telemetry events are accurately translated to OpenInference spans.
  artifacts:
    - path: lib/scoria/observe/telemetry.ex
      provides: Telemetry handlers
  key_links:
    - from: lib/scoria/observe/telemetry.ex
      to: Scoria.Observe.Redactor
      via: redacting payload before buffering
    - from: lib/scoria/observe/telemetry.ex
      to: Scoria.Observe.Buffer
      via: async cast to buffer
---

<objective>
Implement Telemetry event handlers and third-party library adapters.
Purpose: Connect raw application events (from standard Erlang telemetry, ReqLLM, Jido) into the standardized observation pipeline.
Output: Telemetry handler modules.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/RESEARCH.md
@.planning/PATTERNS.md

<interfaces>
From lib/scoria/observe/buffer.ex:
```elixir
defmodule Scoria.Observe.Buffer do
  def cast_span(span_data)
end
```
From lib/scoria/observe/redactor.ex:
```elixir
defmodule Scoria.Observe.Redactor do
  def redact(payload)
end
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Core Telemetry Handlers</name>
  <files>lib/scoria/observe/telemetry.ex, test/scoria/observe/telemetry_test.exs</files>
  <behavior>
    - Attaches to Erlang `:telemetry` events for standard OpenInference execution.
    - Applies `Scoria.Observe.Redactor.redact/1`.
    - Pushes formatted span to `Scoria.Observe.Buffer.cast_span/1`.
  </behavior>
  <action>Implement `Scoria.Observe.Telemetry` to expose `attach/0`. Create handlers that take telemetry measurements/metadata, construct the hybrid map, redact it, and cast it to the buffer. Ensure there is an end-to-end integration test validating the full path from telemetry emission -> buffer -> database insertion.</action>
  <verify>
    <automated>mix test test/scoria/observe/telemetry_test.exs # including e2e integration test for telemetry -> buffer -> db</automated>
  </verify>
  <done>Telemetry correctly intercepts, redacts, and buffers spans, and the end-to-end flow is tested.</done>
</task>

<task type="auto">
  <name>Task 2: ReqLLM and Jido Adapters</name>
  <files>lib/scoria/observe/adapters/req_llm.ex, lib/scoria/observe/adapters/jido.ex, test/scoria/observe/adapters/req_llm_test.exs, test/scoria/observe/adapters/jido_test.exs</files>
  <action>Implement adapters that attach to `ReqLLM` or `Jido` specific telemetry namespaces, transforming their proprietary metadata shapes into standard OpenInference map representations before routing them to the core Scoria telemetry ingestion.</action>
  <verify>
    <automated>mix test test/scoria/observe/adapters/req_llm_test.exs test/scoria/observe/adapters/jido_test.exs</automated>
  </verify>
  <done>Adapters successfully translate third-party events into standard formats.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Telemetry API | Untrusted third party event data |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Spoofing | Telemetry | accept | Telemetry is internal to the application process boundary. |
</threat_model>

<success_criteria>
Events are consistently processed, transformed, and routed to the buffer.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-observability/01-04-SUMMARY.md`
</output>
