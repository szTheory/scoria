---
phase: 01-core-observability
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/scoria/observe/redactor.ex
  - test/scoria/observe/redactor_test.exs
autonomous: true
requirements:
  - OBS-04
must_haves:
  truths:
    - PII and secrets are completely scrubbed from data before database insertion.
  artifacts:
    - path: lib/scoria/observe/redactor.ex
      provides: Data scrubbing utility
  key_links:
    - from: lib/scoria/observe/redactor.ex
      to: Application Config
      via: reads deny-list and MFA override from config
---

<objective>
Implement the Telemetry Redaction Engine.
Purpose: Ensure strict telemetry redaction boundaries to scrub PII, secrets, and API keys.
Output: Working, tested redactor utility.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/RESEARCH.md
@.planning/PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Redactor Utility</name>
  <files>lib/scoria/observe/redactor.ex, test/scoria/observe/redactor_test.exs</files>
  <behavior>
    - Replaces values for keys matching the default deny-list with "[REDACTED]"
    - Traverses nested maps and lists recursively
    - Merges user-configured deny-list from Application config
    - Defers to MFA override if configured
  </behavior>
  <action>Implement `Scoria.Observe.Redactor.redact/1`. Use pattern matching to walk arbitrary data structures. Provide sensible default deny-list (password, api_key, token). Emphasize exact key matching for the deny-list to avoid accidentally redacting OpenInference keys like `llm.token_count`.</action>
  <verify>
    <automated>mix test test/scoria/observe/redactor_test.exs</automated>
  </verify>
  <done>Redactor passes tests recursively scrubbing maps and lists based on deny-list, ensuring partial key matches (e.g., token_count) are unaffected.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Telemetry | Scrubbing raw application telemetry |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Info Disclosure | Redactor | mitigate | Deep recursive redaction of sensitive keys before any storage. |
</threat_model>

<success_criteria>
PII and secrets are completely scrubbed from data before database insertion.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-observability/01-02-SUMMARY.md`
</output>
