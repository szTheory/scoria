---
phase: "02-mcp-gateway"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/mcp/protocol.ex
  - lib/scoria/mcp/router.ex
  - test/scoria/mcp/protocol_test.exs
  - test/scoria/mcp/router_test.exs
autonomous: true
requirements:
  - MCP-01
  - MCP-02
must_haves:
  truths:
    - JSON-RPC requests are correctly parsed and responses are properly formatted.
    - POST requests to the MCP endpoint are handled by the Plug router.
    - The actor context is extracted from connection assigns.
  artifacts:
    - path: lib/scoria/mcp/protocol.ex
      provides: JSON-RPC 2.0 parsing and formatting
    - path: lib/scoria/mcp/router.ex
      provides: Plug router for MCP endpoints
  key_links:
    - from: lib/scoria/mcp/router.ex
      to: lib/scoria/mcp/protocol.ex
      via: JSON-RPC parsing
---

<objective>
Build the fundamental Plug-based HTTP transport and JSON-RPC 2.0 protocol handlers for the MCP Gateway.

Purpose: To establish a secure boundary for MCP interactions using standard HTTP and Plug pipelines, avoiding external opinionated MCP dependencies.
Output: A functional Plug router that parses JSON-RPC requests, extracts the actor context, and returns JSON-RPC formatted responses.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/phase_2_decisions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement JSON-RPC 2.0 Protocol Handler</name>
  <files>lib/scoria/mcp/protocol.ex, test/scoria/mcp/protocol_test.exs</files>
  <action>
    Create `Scoria.MCP.Protocol`.
    Implement functions to parse incoming maps into structured JSON-RPC requests (validating `jsonrpc: "2.0"`, `method`, `params`, `id`).
    Implement functions to format successful responses (`result`, `id`) and error responses (`error`, `id`) according to the JSON-RPC 2.0 specification.
  </action>
  <verify>
    <automated>mix test test/scoria/mcp/protocol_test.exs</automated>
  </verify>
  <done>JSON-RPC parsing and formatting functions are fully unit tested and compliant with standard shapes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement MCP Plug Router</name>
  <files>lib/scoria/mcp/router.ex, test/scoria/mcp/router_test.exs</files>
  <action>
    Create `Scoria.MCP.Router` using `Plug.Router`.
    Add a POST route (e.g., `/`) that reads the body, parses the JSON-RPC request via `Scoria.MCP.Protocol`, and delegates execution (for now, simply echoing back the parsed method/params as a placeholder result).
    Critically, extract `conn.assigns.current_actor` and pass it into the context for the placeholder execution. If the actor is missing, the request should still proceed but with a nil or default unauthenticated context.
    Use `Plug.Parsers` to parse JSON bodies before your route match.
  </action>
  <verify>
    <automated>mix test test/scoria/mcp/router_test.exs</automated>
  </verify>
  <done>The router successfully processes a simulated POST request containing JSON-RPC, extracts the actor assign, and returns a JSON-RPC response.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> MCP Router | Untrusted external MCP requests enter the system here via HTTP POST. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Protocol Parser | mitigate | Ensure strict parsing of JSON-RPC 2.0 structure. Reject malformed requests immediately before processing. |
| T-02-02 | Spoofing | Plug Router Assigns | mitigate | Rely on the host application's Plug pipeline to securely set `conn.assigns.current_actor` prior to hitting `Scoria.MCP.Router`. |
</threat_model>

<verification>
- `mix test test/scoria/mcp/` passes cleanly.
- `Scoria.MCP.Router` can be mounted in a standard Phoenix app.
</verification>

<success_criteria>
- The system can accept and parse standard JSON-RPC 2.0 requests over HTTP POST.
</success_criteria>

<output>
After completion, create `.planning/phases/02-mcp-gateway/02-01-SUMMARY.md`
</output>