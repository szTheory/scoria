---
phase: "02-mcp-gateway"
plan: "03"
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - lib/scoria/mcp/executor.ex
  - lib/scoria/mcp/router.ex
  - test/scoria/mcp/executor_test.exs
  - test/scoria/mcp/router_test.exs
autonomous: true
requirements:
  - MCP-04
  - MCP-05
must_haves:
  truths:
    - Tools are executed in isolated OTP Tasks with strict timeouts.
    - A crashing tool does not crash the caller (the HTTP request).
    - Audit logs are emitted via `:telemetry` events containing actor context.
    - The MCP Router wires the protocol, validation, and execution together.
  artifacts:
    - path: lib/scoria/mcp/executor.ex
      provides: Isolated Task execution and timeout enforcement
    - path: lib/scoria/mcp/router.ex
      provides: End-to-end MCP request processing
  key_links:
    - from: lib/scoria/mcp/router.ex
      to: lib/scoria/mcp/executor.ex
      via: Invoking tools after validation
    - from: lib/scoria/mcp/executor.ex
      to: :telemetry
      via: Emitting audit events
---

<objective>
Implement OTP-isolated tool execution with strict timeouts and integrate the full end-to-end MCP pipeline, including telemetry-based audit logging.

Purpose: To protect the host application from rogue or hanging LLM tool calls and to emit comprehensive audit trails (for Threadline) without hard coupling.
Output: A robust Executor module and a fully wired Router that handles JSON-RPC -> Validation -> Isolated Execution -> JSON-RPC Response.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/phase_2_decisions.md
</context>

<interfaces>
From lib/scoria/mcp/validator.ex:
```elixir
def validate_args(tool_module, raw_args) :: {:ok, map()} | {:error, Ecto.Changeset.t()}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Isolated Executor with Telemetry</name>
  <files>lib/scoria/mcp/executor.ex, test/scoria/mcp/executor_test.exs</files>
  <behavior>
    - Test 1: Successful tool execution returns the result and emits `[:scoria, :tool, :started]` and `[:scoria, :tool, :completed]` telemetry events.
    - Test 2: Tool that exceeds the timeout is terminated, returns a timeout error, and emits a `[:scoria, :tool, :timeout]` event.
    - Test 3: Tool that crashes returns an execution error, isolating the crash, and emits a `[:scoria, :tool, :failed]` event.
  </behavior>
  <action>
    Create `Scoria.MCP.Executor`.
    Implement `execute(tool_module, args, context, timeout \\ 5000)`.
    Wrap `tool_module.execute(args, context)` inside `Task.async/1`.
    Use `Task.yield(task, timeout)` to wait. 
    If `{:ok, result}`: emit `:completed` telemetry.
    If `nil` (timeout): `Task.shutdown(task, :brutal_kill)`, emit `:timeout` telemetry, return error.
    If `{:exit, reason}`: emit `:failed` telemetry, return error.
    All telemetry events must include `context.actor_id` (or the whole context) in their metadata for audit logging.
  </action>
  <verify>
    <automated>mix test test/scoria/mcp/executor_test.exs</automated>
  </verify>
  <done>Tools execute safely in isolation, enforcing timeouts and emitting detailed audit telemetry.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the Router Pipeline</name>
  <files>lib/scoria/mcp/router.ex, test/scoria/mcp/router_test.exs</files>
  <action>
    Update `Scoria.MCP.Router`.
    Replace the placeholder logic with the full pipeline:
    1. Parse JSON-RPC request (`Scoria.MCP.Protocol`).
    2. Map the request `method` to a registered tool module (you can pass a registry map into the router's init options or use Application env).
    3. Validate arguments using `Scoria.MCP.Validator`.
    4. On valid args, execute the tool using `Scoria.MCP.Executor`, passing the extracted `conn.assigns.current_actor` as context.
    5. Format the result or any errors (validation, timeout, execution failure) back into JSON-RPC 2.0 format via `Scoria.MCP.Protocol` and respond.
  </action>
  <verify>
    <automated>mix test test/scoria/mcp/router_test.exs</automated>
  </verify>
  <done>The router handles end-to-end execution of valid and invalid requests, mapping them accurately to JSON-RPC responses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Execution Isolation | Tool code runs in a separate OTP process to prevent host crash. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Denial of Service | Executor | mitigate | Strict `Task.yield` timeouts and `Task.shutdown` prevent malicious or hanging tools from exhausting connection pools or blocking the main process. |
| T-02-05 | Repudiation | Telemetry | mitigate | Emitting deterministic telemetry events with actor context ensures robust audit logging (Threadline integration). |
</threat_model>

<verification>
- `mix test test/scoria/mcp/executor_test.exs` asserts timeouts and telemetry emissions.
- `mix test test/scoria/mcp/router_test.exs` tests the full e2e flow.
</verification>

<success_criteria>
- Tool execution is fully isolated and timeout-bound.
- The Plug router successfully orchestrates JSON-RPC parsing, Ecto validation, isolated execution, and JSON-RPC formatting.
</success_criteria>

<output>
After completion, create `.planning/phases/02-mcp-gateway/02-03-SUMMARY.md`
</output>