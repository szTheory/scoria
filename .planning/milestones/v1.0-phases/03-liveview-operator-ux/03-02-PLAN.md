---
phase: 03-liveview-operator-ux
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: ["lib/scoria_web/components/trace_tree_component.ex", "lib/scoria_web/live/orchestrator_live.ex", "test/scoria_web/components/trace_tree_component_test.exs", "test/scoria_web/live/orchestrator_live_test.exs"]
autonomous: true
requirements: [UI-02, UI-05]

must_haves:
  truths:
    - "User can view nested trace structures without browser lag or deep DOM nesting"
    - "The orchestrator automatically updates passively via PubSub subscriptions"
    - "Traces are lazily loaded or streamed into the UI"
  artifacts:
    - path: "lib/scoria_web/components/trace_tree_component.ex"
      provides: "CSS grid-based trace visualizer"
    - path: "lib/scoria_web/live/orchestrator_live.ex"
      provides: "PubSub subscriber and stream assigner"
  key_links:
    - from: "Phoenix PubSub"
      to: "OrchestratorLive"
      via: "Phoenix.PubSub.subscribe in mount/3"
    - from: "OrchestratorLive"
      to: "TraceTreeComponent"
      via: "Rendering component and passing stream assigns"
---

<objective>
Build the Visual Trace Explorer using CSS Grid and wire up real-time passive PubSub updates.

Purpose: Operators need to understand the "Shape of AI" execution (e.g., deeply nested LLM calls). Deep DOM nesting breaks browsers; we use a flat list mapped to CSS Grid to present the hierarchy. PubSub is used to passively populate the trace view as the backend generates observability events.
Output: Trace tree UI component and active PubSub subscription in the root LiveView.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build CSS Grid Trace Tree LiveComponent</name>
  <files>lib/scoria_web/components/trace_tree_component.ex, test/scoria_web/components/trace_tree_component_test.exs</files>
  <behavior>
    - Test 1: Renders trace span data using a flat DOM structure.
    - Test 2: Applies appropriate indentation style via dynamic CSS variable (`--indent-level`).
  </behavior>
  <action>
    Create a `Phoenix.LiveComponent` module `ScoriaWeb.TraceTreeComponent`.
    Implement `render/1` that renders a list of spans and uses `assign_async` for lazy rendering of deep span metadata.
    CRITICAL: Avoid deep `div` nesting for child spans. Instead, iterate over a flattened representation of the tree where each node knows its depth.
    Use inline styles like `style={"--indent-level: #{@depth}"}` and Tailwind utility classes utilizing CSS grid to indent rows without physical HTML nesting.
  </action>
  <verify>
    <automated>mix test test/scoria_web/components/trace_tree_component_test.exs</automated>
  </verify>
  <done>LiveComponent compiles, tests pass, and supports lazy rendering with a flat DOM structure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire PubSub and Trace Rendering in LiveView</name>
  <files>lib/scoria_web/live/orchestrator_live.ex, test/scoria_web/live/orchestrator_live_test.exs</files>
  <behavior>
    - Test 1: Asserts rendering of nested dummy elements on LiveView mount.
  </behavior>
  <action>
    Modify `mount/3`. If connected (using `connected?(socket)`), subscribe to a generic tenant channel (e.g., `Phoenix.PubSub.subscribe(Scoria.PubSub, "scoria:runs:all")`).
    Initialize a LiveView `stream` for `:traces` in the mount assigns.
    Implement `handle_info({:new_trace, trace}, socket)` to handle incoming trace events and insert them into the `:traces` stream.
    Update `render/1` to iterate over the `@streams.traces` using `phx-update="stream"` and explicitly render `<.live_component module={ScoriaWeb.TraceTreeComponent} id={trace.id} span={trace} />`.
    Implement `assign_async` to fetch deep span metadata lazily on node expansion using a LiveComponent, as explicitly decided in phase_3_decisions.md.
  </action>
  <verify>
    <automated>mix test test/scoria_web/live/orchestrator_live_test.exs</automated>
  </verify>
  <done>LiveView handles PubSub messages and actively renders traces using the TraceTreeComponent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend -> Frontend | Raw trace data is injected into the HTML UI via LiveView updates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Spoofing (XSS) | TraceTreeComponent | mitigate | Rely on standard LiveView HEEx HTML escaping. Do not use `raw/1` on trace attributes. |
| T-03-04 | Denial of Service | PubSub Handler | mitigate | LiveView Streams handle infinite lists. The flat DOM prevents browser freezing on very deep traces. |
</threat_model>

<verification>
mix compile --warnings-as-errors
mix test test/scoria_web/components/trace_tree_component_test.exs
mix test test/scoria_web/live/orchestrator_live_test.exs
</verification>

<success_criteria>
- CSS-grid-based trace component correctly renders without deep DOM nodes.
- OrchestratorLive successfully connects to PubSub.
- LiveView streams are used to push new traces asynchronously to the client.
</success_criteria>

<output>
After completion, create `.planning/phases/03-liveview-operator-ux/03-02-SUMMARY.md`
</output>