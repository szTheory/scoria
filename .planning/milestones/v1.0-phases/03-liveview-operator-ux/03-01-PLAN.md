---
phase: 03-liveview-operator-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["lib/scoria_web/router.ex", "lib/scoria_web/live/orchestrator_live.ex", "lib/mix/tasks/scoria.install.ex", "test/scoria_web/router_test.exs", "test/scoria_web/live/orchestrator_live_test.exs", "test/mix/tasks/scoria.install_test.exs"]
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "Developer can install the dashboard via mix scoria.install without manual configuration"
    - "Host app router can embed the Scoria LiveView dashboard via a macro"
    - "User can view the basic orchestrator LiveView UI at the mounted path"
  artifacts:
    - path: "lib/scoria_web/router.ex"
      provides: "scoria_dashboard macro"
    - path: "lib/scoria_web/live/orchestrator_live.ex"
      provides: "Root LiveView module"
    - path: "lib/mix/tasks/scoria.install.ex"
      provides: "Installation utility"
  key_links:
    - from: "Host Router"
      to: "ScoriaWeb.Router"
      via: "Macro invocation"
      pattern: "scoria_dashboard \"/scoria\""
---

<objective>
Build the core Phoenix LiveView integration infrastructure for Scoria.

Purpose: The Scoria dashboard must be seamlessly embeddable into host Phoenix applications. A router macro provides the API for developers to mount it, and an installation task automates the setup, matching the developer experience of standard tools like Phoenix LiveDashboard.
Output: Router macro, root LiveView controller, and mix installation task.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/RESEARCH.md
@.planning/phases/03-liveview-operator-ux/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ScoriaWeb Router Macro</name>
  <files>lib/scoria_web/router.ex, test/scoria_web/router_test.exs</files>
  <behavior>
    - Test 1: scoria_dashboard macro correctly expands to a live_session and live route.
  </behavior>
  <action>
    Create `ScoriaWeb.Router` with a `defmacro scoria_dashboard(path, opts \\ [])`.
    Use `live_session :scoria_dashboard` inside the macro to isolate the LiveView and mount `ScoriaWeb.OrchestratorLive` at the given path.
    Follow the standard Phoenix dashboard embedding pattern. Do not build custom session handling; rely on the host's existing `pipe_through :browser`. Reference analog `lib/scoria/mcp/router.ex`.
  </action>
  <verify>
    <automated>mix test test/scoria_web/router_test.exs</automated>
  </verify>
  <done>Macro compiles and successfully delegates to OrchestratorLive route in tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Root Orchestrator LiveView</name>
  <files>lib/scoria_web/live/orchestrator_live.ex, test/scoria_web/live/orchestrator_live_test.exs</files>
  <behavior>
    - Test 1: OrchestratorLive mounts successfully and renders dummy wrapper.
  </behavior>
  <action>
    Create `ScoriaWeb.OrchestratorLive` module (`use Phoenix.LiveView`).
    Implement a basic `mount/3` that assigns standard defaults.
    Implement a basic `render/1` returning a simple placeholder `~H` wrapper with the `scoria-dashboard` namespace for CSS scoping.
    Ensure styling strictly relies on semantic Tailwind classes without relying on a pre-compiled CSS bundle.
  </action>
  <verify>
    <automated>mix test test/scoria_web/live/orchestrator_live_test.exs</automated>
  </verify>
  <done>OrchestratorLive compiles and passes mount test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build Scoria Installer Mix Task</name>
  <files>lib/mix/tasks/scoria.install.ex, test/mix/tasks/scoria.install_test.exs</files>
  <behavior>
    - Test 1: mix scoria.install automatically injects the router macro and tailwind config into dummy host app files.
  </behavior>
  <action>
    Create `Mix.Tasks.Scoria.Install` module.
    Implement the `run/1` callback to parse the host application's `router.ex` and `tailwind.config.js` and automatically inject `scoria_dashboard "/scoria"` and the Scoria Tailwind paths (`deps/scoria/lib/**/*.*ex`). Do not just print instructions; implement actual file injection for a zero-configuration setup.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/scoria.install_test.exs</automated>
  </verify>
  <done>Running the task successfully modifies host files automatically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web Browser -> LiveView Socket | End-users interact with the dashboard over WebSockets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | Router Macro | mitigate | Rely on host application's authentication pipelines (e.g., `pipe_through :admin_browser`) when invoking the macro. |
| T-03-02 | Spoofing | LiveView Mount | accept | Scoria does not manage actor sessions itself; it trusts the connection passed by the host app via the router macro. |
</threat_model>

<verification>
mix compile --warnings-as-errors
mix test test/scoria_web/router_test.exs
mix test test/scoria_web/live/orchestrator_live_test.exs
mix test test/mix/tasks/scoria.install_test.exs
</verification>

<success_criteria>
- Router macro is fully implemented and tested.
- `OrchestratorLive` compiles successfully and passes tests.
- `mix scoria.install` successfully executes file injections.
</success_criteria>

<output>
After completion, create `.planning/phases/03-liveview-operator-ux/03-01-SUMMARY.md`
</output>