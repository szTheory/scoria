# Milestone v2.16: ReqLLM Peer Bump

**Status:** ✅ SHIPPED 2026-05-30
**Phases:** 08–10 + 10.1 (inserted)
**Total Plans:** 1 formal (+ 3 retroactive implementation phases)

## Overview

Keep Scoria's LLM orchestration peer current on Hex without widening product scope. Pins `req_llm` to `~> 1.13` (1.13.0 locked), verifies orchestrator/judge/compaction/observe integration paths, and proves CI trust bundle stays green.

## Phases

| # | Phase | Goal | Requirements | Status |
|---|-------|------|--------------|--------|
| 08 | Bump ReqLLM constraint and lockfile | Pin Scoria to ReqLLM 1.13.x on Hex | DEPS-01 | complete |
| 09 | Compatibility fixes and test verification | Prove all ReqLLM integration paths still work | DEPS-02, DEPS-03 | complete |
| 10 | CI lane verification and closeout | Maintainer trust — bump does not regress adoption/CI contracts | DEPS-04 | complete |
| 10.1 | VERIFICATION gap closeout (INSERTED) | Retroactive VERIFICATION ledgers + audit re-run | process traceability | complete |

### Phase 08: Bump ReqLLM constraint and lockfile

**Goal:** Pin Scoria to ReqLLM 1.13.x on Hex.

**Requirements:** DEPS-01

**Success criteria:**

1. `mix.exs` declares `{:req_llm, "~> 1.13"}`
2. `mix.lock` resolves req_llm to 1.13.x
3. `mix compile --warnings-as-errors` succeeds

### Phase 09: Compatibility fixes and test verification

**Goal:** Prove all ReqLLM integration paths still work after the bump.

**Requirements:** DEPS-02, DEPS-03

**Success criteria:**

1. Orchestrator, judge runner, and compaction worker tests pass under WAE
2. `Scoria.Observe.Adapters.ReqLLM` tests pass
3. Orchestrator live integration test passes
4. Full `mix test --warnings-as-errors` is green

### Phase 10: CI lane verification and closeout

**Goal:** Maintainer trust — bump does not regress adoption/CI contracts.

**Requirements:** DEPS-04

**Success criteria:**

1. `mix scoria.test.ci_trust` passes
2. CHANGELOG notes ReqLLM 1.13 peer bump under `[Unreleased]`
3. No new warning debt introduced

### Phase 10.1: Close gap — VERIFICATION artifacts for v2.16 phases 08–10 (INSERTED)

**Goal:** Close the v2.16 process gate — write retroactive `08/09/10-VERIFICATION.md` + `10-SUMMARY.md`, re-run milestone audit to `status: passed`.

**Requirements:** DEPS-01 through DEPS-04 (process traceability only)

**Depends on:** Phase 10

**Plans:** 1/1 plans complete

Plans:

- [x] 10.1-01-PLAN.md — Coordinated retro VERIFICATION + 10-SUMMARY + milestone re-audit (5 tasks)

---

## Milestone Summary

**Decimal Phases:**

- Phase 10.1: VERIFICATION gap closeout (inserted after Phase 10 for process gate — retroactive ledgers for phases shipped without GSD execute artifacts)

**Key Decisions:**

- Minimal ReqLLM-only scope — no optional szTheory Hex deps (parapet, threadline, chimeway)
- Tribunal stays at 1.3.6 — no bump needed
- ReqLLM streaming → `emit_span_delta/1` adapter deferred (ECOS-02, from v2.11)
- Hex publish `0.1.1` stays in release queue, not v2.16 scope

**Issues Resolved:**

- ReqLLM peer bumped from `~> 1.11` (1.12.0) to `~> 1.13` (1.13.0)
- Integration slice (17 tests) + CI trust bundle (43 tests) verified green
- Milestone audit re-run to `passed`, `requirements_process: 4/4`

**Issues Deferred:**

- SummarizeWorker dedicated unit test (compile + orchestrator path coverage only)
- No `*-VALIDATION.md` for phases 08–10 (thin milestone; audit-time gates in VERIFICATION)
- ReqLLM streaming adapter (ECOS-02)
- Publish `0.1.1` via release-please

**Technical Debt Incurred:**

- Phases 08–10 shipped without GSD execute artifacts; retroactive VERIFICATION written by Phase 10.1
- Nyquist VALIDATION missing for 08–10 (non-blocking for thin dependency bump)

---

_For current project status, see `.planning/ROADMAP.md`_
