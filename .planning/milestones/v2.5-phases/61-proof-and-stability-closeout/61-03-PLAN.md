---
phase: 61-proof-and-stability-closeout
plan: 03
type: execute
wave: 3
depends_on: [61-01, 61-02]
files_modified:
  - docs/operator_verification.md
  - docs/adoption_lanes.md
  - test/scoria/adoption_surface_test.exs
  - .planning/phases/61-proof-and-stability-closeout/61-VERIFICATION.md
autonomous: true
requirements: [INST-08]
must_haves:
  truths:
    - "Operator docs describe upgrade-safe dry-run → check → remediate → apply flow; check never writes (D-27)."
    - "adoption_lanes.md cross-references installer verification modes without doc sweep (D-28)."
    - "adoption_surface_test.exs pins new operator subsection markers and --check mention (D-29)."
    - "v2.4 closeout chain passes unchanged; no fourth lane added (D-22, D-16, D-24)."
  artifacts:
    - path: "docs/operator_verification.md"
      provides: "Installer verification modes subsection"
      contains: "Installer verification modes (upgrade-safe)"
    - path: "docs/adoption_lanes.md"
      provides: "Cross-reference to installer modes"
      contains: "operator_verification.md"
    - path: ".planning/phases/61-proof-and-stability-closeout/61-VERIFICATION.md"
      provides: "Phase 61 guardrail and optional smoke evidence"
      contains: "closeout"
  key_links:
    - from: "lib/scoria/install/contract.ex"
      to: "docs/operator_verification.md"
      via: "doc prose matches Contract verify_command and trailer format"
      pattern: "mix scoria.install --check"
    - from: "test/scoria/adoption_surface_test.exs"
      to: "docs/operator_verification.md"
      via: "substring pins enforce doc truth"
      pattern: "Installer verification modes"
---

<objective>
Close INST-08 with minimal operator docs, adoption surface pins, and v2.4 guardrail regression proof.

Purpose: Align docs/support with Install.Contract SSOT and prove installer-safety work did not erode lane contract or CI closeout order.
Output: Operator modes subsection, adoption cross-ref, adoption_surface pins, 61-VERIFICATION.md with closeout evidence.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/61-proof-and-stability-closeout/61-CONTEXT.md
@.planning/phases/61-proof-and-stability-closeout/61-RESEARCH.md
@.planning/phases/61-proof-and-stability-closeout/61-VALIDATION.md
@lib/scoria/install/contract.ex
@lib/scoria/verification_lanes.ex
@test/scoria/adoption_surface_test.exs
@test/scoria/verification_lanes_test.exs
@docs/operator_verification.md
@docs/adoption_lanes.md
@.planning/WARNING-BASELINE.md

Locked D-22 through D-29. Do not modify README, phoenix example, or handoffs unless pins fail.
</context>

<tasks>

<task type="auto">
  <name>Task 61-03-01: Add minimal installer verification docs and adoption cross-reference</name>
  <requirement_ids>INST-08</requirement_ids>
  <files>docs/operator_verification.md, docs/adoption_lanes.md</files>
  <read_first>
    docs/operator_verification.md
    docs/adoption_lanes.md
    lib/scoria/install/contract.ex
    lib/mix/tasks/scoria.install.ex
    .planning/phases/61-proof-and-stability-closeout/61-CONTEXT.md
  </read_first>
  <action>Add subsection `## Installer verification modes (upgrade-safe)` to `docs/operator_verification.md` with exactly this workflow: (1) `mix scoria.install --dry-run` to preview changes without writes; (2) `mix scoria.install --check` to verify state without writes; (3) remediate any `manual_review` entries using printed steps; (4) `mix scoria.install` to apply. State explicitly: `--check` never writes; `manual_review` never silent overwrite; automation should parse `SCORIA_CHECK_RESULT status=&lt;compliant|drift|manual_review|error&gt; exit_code=&lt;0|1|2&gt;`. Under "Default runtime lane" section in `docs/adoption_lanes.md`, add 2–3 sentences linking to the new subsection and noting `--check` as post-upgrade verification. Do not edit other doc files.</action>
  <acceptance_criteria>
    - `rg -n "Installer verification modes \\(upgrade-safe\\)" docs/operator_verification.md` returns subsection heading.
    - `rg -n "mix scoria.install --dry-run" docs/operator_verification.md` returns dry-run step.
    - `rg -n "never writes" docs/operator_verification.md` returns check no-write guarantee.
    - `rg -n "SCORIA_CHECK_RESULT" docs/operator_verification.md` returns trailer mention.
    - `rg -n "operator_verification.md" docs/adoption_lanes.md` returns cross-reference link.
    - `rg -n "--check" docs/adoption_lanes.md` returns check mention in runtime lane section.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Installer verification modes|SCORIA_CHECK_RESULT" docs/operator_verification.md docs/adoption_lanes.md</automated>
  </verify>
  <done>Minimal operator docs describe upgrade-safe installer modes with automation trailer guidance.</done>
</task>

<task type="auto">
  <name>Task 61-03-02: Extend adoption_surface_test pins for installer modes</name>
  <requirement_ids>INST-08</requirement_ids>
  <files>test/scoria/adoption_surface_test.exs</files>
  <read_first>
    test/scoria/adoption_surface_test.exs
    docs/operator_verification.md
    docs/adoption_lanes.md
    lib/scoria/install/contract.ex
  </read_first>
  <action>Add one test to `adoption_surface_test.exs`: `"operator verification guide documents upgrade-safe installer modes"`. Assert `@operator_guide` contains `"Installer verification modes (upgrade-safe)"`, `"mix scoria.install --dry-run"`, `"mix scoria.install --check"`, and `"SCORIA_CHECK_RESULT"`. Assert `@lane_guide` contains link or path to `operator_verification.md` and `"--check"`. Do not add pins to README or other guides.</action>
  <acceptance_criteria>
    - `rg -n "Installer verification modes \\(upgrade-safe\\)" test/scoria/adoption_surface_test.exs` returns pin test reference.
    - `rg -n "mix scoria.install --check" test/scoria/adoption_surface_test.exs` returns check pin.
    - `MIX_ENV=test mix test test/scoria/adoption_surface_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/adoption_surface_test.exs</automated>
  </verify>
  <done>Adoption surface test enforces installer modes doc truth without broad doc sweep.</done>
</task>

<task type="auto">
  <name>Task 61-03-03: Record guardrail closeout evidence in 61-VERIFICATION.md</name>
  <requirement_ids>INST-08</requirement_ids>
  <files>.planning/phases/61-proof-and-stability-closeout/61-VERIFICATION.md</files>
  <read_first>
    .planning/phases/61-proof-and-stability-closeout/61-CONTEXT.md
    .planning/WARNING-BASELINE.md
    lib/scoria/verification_lanes.ex
    test/scoria/verification_lanes_test.exs
    .github/workflows/ci.yml
  </read_first>
  <action>Create `.planning/phases/61-proof-and-stability-closeout/61-VERIFICATION.md` documenting Phase 61 verification evidence. Include section "v2.4 closeout chain (required gate)" listing D-22 commands in order: (1) `MIX_ENV=test mix compile --warnings-as-errors`; (2) `mix test --warnings-as-errors test/scoria/verification_lanes_test.exs test/scoria/adoption_surface_test.exs`; (3) `MIX_ENV=dev mix scoria.release_preview`; (4) `mix ecto.create && mix ecto.migrate`; (5) `mix test.adoption`; (6) `mix test.runtime_to_handoff`. Run all six commands and record pass/fail with timestamp. Add "Optional smoke (non-blocking per D-23)" for `mix test` and `mix test.knowledge` with note to classify failures via WARNING-BASELINE.md. Assert `VerificationLanes.closeout_order/0` still returns `[:release_preview, :adoption, :runtime_to_handoff]` — grep test must pass. Confirm no new lane in closeout order and no `mix test.install_contract` in CI.</action>
  <acceptance_criteria>
    - `test -f .planning/phases/61-proof-and-stability-closeout/61-VERIFICATION.md` succeeds.
    - `rg -n "mix test.adoption" .planning/phases/61-proof-and-stability-closeout/61-VERIFICATION.md` returns closeout step.
    - `rg -n "Optional smoke" .planning/phases/61-proof-and-stability-closeout/61-VERIFICATION.md` returns non-blocking section.
    - `MIX_ENV=test mix test test/scoria/verification_lanes_test.exs` exits 0.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
    - `MIX_ENV=test mix test --warnings-as-errors test/scoria/verification_lanes_test.exs test/scoria/adoption_surface_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors && MIX_ENV=test mix test --warnings-as-errors test/scoria/verification_lanes_test.exs test/scoria/adoption_surface_test.exs && MIX_ENV=test mix test.adoption</automated>
  </verify>
  <done>61-VERIFICATION.md records v2.4 closeout chain evidence; lane order unchanged; scoped guardrails green.</done>
</task>

</tasks>

<threat_model>
## Security Gate

- Enforcement: enabled
- ASVS level: 1
- Block threshold: high severity threats

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc prose -> operator actions | Docs must not imply check mode writes or silent manual_review overwrite. |
| Closeout order -> CI trust | VerificationLanes closeout_order must remain frozen at three lanes. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-61-07 | Tampering | medium | operator docs | mitigate | Explicit "never writes" and manual_review zero-write statements. | adoption_surface_test pins. |
| T-61-08 | Repudiation | low | 61-VERIFICATION.md | mitigate | Record command output evidence for closeout chain. | File exists with timestamped results. |

No high-severity threats block Plan 61-03 completion.
</threat_model>

<verification>
After all tasks:

```bash
MIX_ENV=test mix test test/scoria/adoption_surface_test.exs test/scoria/verification_lanes_test.exs
MIX_ENV=test mix compile --warnings-as-errors
MIX_ENV=test mix test.adoption
MIX_ENV=test mix test.runtime_to_handoff
```
</verification>

## PLANNING COMPLETE
