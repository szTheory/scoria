---
phase: 59-planner-contract-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/scoria.install.ex
  - lib/scoria/install/planner.ex
  - lib/scoria/install/surface/router.ex
  - lib/scoria/install/surface/tailwind.ex
  - lib/scoria/install/surface/migrations.ex
  - lib/scoria/install/surface/runtime_config.ex
  - test/scoria/install/planner_test.exs
  - test/mix/tasks/scoria.install_test.exs
autonomous: true
requirements: [INST-03, INST-05]
must_haves:
  truths:
    - "mix scoria.install --dry-run uses a single pure planner path that performs zero host writes."
    - "Planner entries are emitted in deterministic surface order with stable IDs and explicit classification rationale."
    - "Every managed surface is classified as create/update/no-op/manual-review before any apply-path mutation work."
  artifacts:
    - path: "lib/scoria/install/planner.ex"
      provides: "Canonical planner artifact builder for dry-run/check"
      contains: "def build(router_path, tailwind_path, config_path, opts \\\\ [])"
    - path: "lib/mix/tasks/scoria.install.ex"
      provides: "Mode routing for --dry-run and --check without mutator calls"
      contains: "@switches [dry_run: :boolean, check: :boolean, format: :string]"
    - path: "test/scoria/install/planner_test.exs"
      provides: "Deterministic ordering and stable ID contract tests"
      contains: "assert Enum.map(plan.entries, & &1.surface)"
    - path: "test/mix/tasks/scoria.install_test.exs"
      provides: "No-write and rationale-visible dry-run contract tests"
      contains: "mix scoria.install --dry-run does not mutate host files"
  key_links:
    - from: "lib/mix/tasks/scoria.install.ex"
      to: "lib/scoria/install/planner.ex"
      via: "dry-run/check branches call planner build path"
      pattern: "Scoria\\.Install\\.Planner\\.build"
    - from: "lib/scoria/install/planner.ex"
      to: "test/scoria/install/planner_test.exs"
      via: "deterministic order and stable id assertions"
      pattern: "@surface_order|stable id"
---

<objective>
Establish the Phase 59 planner contract foundation for preview semantics before any apply-path rewrite.

Purpose: Deliver INST-03 and INST-05 directly, and lay the concrete contract consumed by INST-04 check semantics in Wave 2.
Output: `mix scoria.install --dry-run` produces a deterministic, no-write planner artifact with per-surface classification, rationale, and target paths.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-planner-contract-foundation/59-CONTEXT.md
@.planning/phases/59-planner-contract-foundation/59-RESEARCH.md
@.planning/phases/59-planner-contract-foundation/59-PATTERNS.md
@.planning/phases/59-planner-contract-foundation/59-VALIDATION.md
@lib/mix/tasks/scoria.install.ex
@test/mix/tasks/scoria.install_test.exs
@lib/scoria/verification_lanes.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 59-01-01: Build deterministic pure planner artifact and wire dry-run mode</name>
  <requirement_ids>INST-03, INST-05</requirement_ids>
  <files>lib/mix/tasks/scoria.install.ex, lib/scoria/install/planner.ex, lib/scoria/install/surface/router.ex, lib/scoria/install/surface/tailwind.ex, lib/scoria/install/surface/migrations.ex, lib/scoria/install/surface/runtime_config.ex</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/59-planner-contract-foundation/59-CONTEXT.md
    .planning/phases/59-planner-contract-foundation/59-PATTERNS.md
    lib/mix/tasks/scoria.install.ex
    test/mix/tasks/scoria.install_test.exs
  </read_first>
  <action>Create `lib/scoria/install/planner.ex` with module `Scoria.Install.Planner` and function `build(router_path, tailwind_path, config_path, opts \\ [])` returning `%{schema_version: 1, mode: :dry_run | :check, entries: [...], summary: %{create: n, update: n, no_op: n, manual_review: n}}`. Define `@surface_order [:router, :tailwind, :migrations, :runtime_config]` and sort entries strictly by this order. Each entry must include `id`, `surface`, `target_path`, `classification`, `rationale`, `evidence`, and `order`; `classification` must be one of `:create`, `:update`, `:no_op`, `:manual_review`. Add pure analyzer modules under `lib/scoria/install/surface/` with explicit `analyze/2` signatures for router/tailwind/migrations/runtime config, reusing read-only probes from installer logic and falling back to `:manual_review` on ambiguity. In `lib/mix/tasks/scoria.install.ex`, add `@switches [dry_run: :boolean, check: :boolean, format: :string]`, parse args, reject `--dry-run` + `--check` together, reject unsupported `--format` values outside `human|json`, and route `--dry-run` to planner/report rendering only. Ensure dry-run/check branches never call `do_run/3`, `inject_router/1`, `maybe_inject_tailwind/1`, `copy_core_migrations/1`, or `inject_runtime_config/1`.</action>
  <acceptance_criteria>
    - `rg -n "@switches \\[dry_run: :boolean, check: :boolean, format: :string\\]" lib/mix/tasks/scoria.install.ex` returns one switch declaration.
    - `rg -n "def build\\(router_path, tailwind_path, config_path, opts \\\\ \\[\\]\\)" lib/scoria/install/planner.ex` returns one function signature match.
    - `rg -n "@surface_order \\[:router, :tailwind, :migrations, :runtime_config\\]" lib/scoria/install/planner.ex` returns one deterministic ordering declaration.
    - `rg -n "classification: :create" lib/scoria/install/planner.ex lib/scoria/install/surface` returns the create classification variant.
    - `rg -n "classification: :update" lib/scoria/install/planner.ex lib/scoria/install/surface` returns the update classification variant.
    - `rg -n "classification: :no_op" lib/scoria/install/planner.ex lib/scoria/install/surface` returns the no-op classification variant.
    - `rg -n "classification: :manual_review" lib/scoria/install/planner.ex lib/scoria/install/surface` returns the manual-review classification variant.
    - `rg -n "def analyze\\(" lib/scoria/install/surface/router.ex lib/scoria/install/surface/tailwind.ex lib/scoria/install/surface/migrations.ex lib/scoria/install/surface/runtime_config.ex` returns one `analyze/` signature per module.
    - `MIX_ENV=test mix test test/scoria/install/planner_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/install/planner_test.exs</automated>
  </verify>
  <done>Planner contract exists as a deterministic, pure artifact builder and dry-run is routed through planner-only execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 59-01-02: Add no-write dry-run, deterministic ordering, and rationale contract tests</name>
  <requirement_ids>INST-03, INST-05</requirement_ids>
  <files>test/mix/tasks/scoria.install_test.exs, test/scoria/install/planner_test.exs</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/59-planner-contract-foundation/59-VALIDATION.md
    lib/mix/tasks/scoria.install.ex
    lib/scoria/install/planner.ex
    test/mix/tasks/scoria.install_test.exs
  </read_first>
  <action>Extend `test/mix/tasks/scoria.install_test.exs` with explicit dry-run contract tests named `mix scoria.install --dry-run does not mutate host files`, `mix scoria.install --dry-run output is deterministic across repeated runs`, and `mix scoria.install --dry-run prints classification, target path, and rationale for every surface`. Each no-write assertion must snapshot router/tailwind/runtime config/migration directory before and after running `Mix.Tasks.Scoria.Install.run(["--dry-run"])` and assert byte-equality and unchanged migration file list. Add `test/scoria/install/planner_test.exs` assertions that `Scoria.Install.Planner.build/4` returns entries in exactly `[:router, :tailwind, :migrations, :runtime_config]` order and stable `id` values across two identical invocations.</action>
  <acceptance_criteria>
    - `rg -n "mix scoria.install --dry-run does not mutate host files" test/mix/tasks/scoria.install_test.exs` returns the test name.
    - `rg -n "mix scoria.install --dry-run output is deterministic across repeated runs" test/mix/tasks/scoria.install_test.exs` returns the test name.
    - `rg -n "classification, target path, and rationale" test/mix/tasks/scoria.install_test.exs` returns the rationale visibility assertion test.
    - `rg -n "assert Enum.map\\(plan.entries, & &1.surface\\) == \\[:router, :tailwind, :migrations, :runtime_config\\]" test/scoria/install/planner_test.exs` returns one order assertion.
    - `rg -n "assert Enum.map\\(plan.entries, & &1.id\\) == Enum.map\\(plan_again.entries, & &1.id\\)" test/scoria/install/planner_test.exs` returns one stable-ID assertion.
    - `MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs test/scoria/install/planner_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs test/scoria/install/planner_test.exs</automated>
  </verify>
  <done>Dry-run planner behavior is proven no-write, deterministic, and classification-rationale complete across managed surfaces.</done>
</task>

</tasks>

<threat_model>
## Security Gate

- Enforcement: enabled
- ASVS level: 1
- Block threshold: high severity threats

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mix task CLI -> host app filesystem | Preview/check flows must not mutate host files before apply-path safety work lands. |
| Planner artifact -> CI/operator decisions | Deterministic output and stable classification are required for trustworthy automation. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-59-01 | Tampering | high | `--dry-run` execution path | block | Block if dry-run/check path calls mutator functions or writes files. | No-write tests plus grep checks for planner routing. |
| T-59-02 | Repudiation | high | planner ordering/identity | block | Block if entry order or IDs vary across identical runs. | Deterministic order and stable-ID tests. |
| T-59-03 | Elevation of Privilege | medium | classifier fallback behavior | mitigate | Force `manual_review` on ambiguous anchors or unknown ownership. | Analyzer unit tests asserting fallback paths. |

High-severity threats T-59-01 and T-59-02 are hard blockers for Phase 59 completion.
</threat_model>

<verification>
Run task-level checks after each task, then execute:

```bash
MIX_ENV=test mix test test/scoria/install/planner_test.exs
MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs test/scoria/install/planner_test.exs
```
</verification>

<success_criteria>
- `mix scoria.install --dry-run` emits deterministic planner output without host mutations.
- Planner entries include surface, target path, classification, and rationale with stable IDs.
- Classification contract (`create|update|no-op|manual-review`) is test-backed for all managed surfaces.
- Phase 60 apply rewrite is untouched; this wave delivers planner/check foundation only.
</success_criteria>

<output>
After completion, create `.planning/phases/59-planner-contract-foundation/59-01-SUMMARY.md`.
</output>
