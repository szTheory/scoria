---
phase: 59-planner-contract-foundation
plan: 02
type: execute
wave: 2
depends_on: [59-01]
files_modified:
  - lib/mix/tasks/scoria.install.ex
  - lib/scoria/install/report.ex
  - test/mix/tasks/scoria.install_check_test.exs
  - test/mix/tasks/scoria.install_test.exs
  - test/scoria/verification_lanes_test.exs
  - test/mix/tasks/test.adoption_test.exs
autonomous: true
requirements: [INST-03, INST-04, INST-05]
must_haves:
  truths:
    - "mix scoria.install --check uses the same planner artifact as --dry-run and remains no-write."
    - "--check exit semantics are deterministic and stable: 0=compliant, 1=unsafe/manual-review, 2=execution error."
    - "Every --check run ends with one stable machine trailer line: SCORIA_CHECK_RESULT status=<...> exit_code=<...>."
    - "v2.4 reliability guardrails and verification lane contracts remain green after planner/check integration."
  artifacts:
    - path: "lib/scoria/install/report.ex"
      provides: "Check result aggregation and trailer formatter"
      contains: "def trailer_line(%{status: status, exit_code: exit_code})"
    - path: "lib/mix/tasks/scoria.install.ex"
      provides: "Check mode branch with explicit System.halt exit semantics"
      contains: "System.halt(exit_code)"
    - path: "test/mix/tasks/scoria.install_check_test.exs"
      provides: "Subprocess assertions for 0/1/2 exits and stable trailer line"
      contains: "System.cmd(\"mix\", [\"scoria.install\", \"--check\""
    - path: "test/scoria/verification_lanes_test.exs"
      provides: "Guardrail contract for closeout lane order unchanged"
      contains: "closeout chain stays pinned"
  key_links:
    - from: "lib/scoria/install/planner.ex"
      to: "lib/scoria/install/report.ex"
      via: "report/check result derived from planner entry classifications"
      pattern: "entries"
    - from: "lib/scoria/install/report.ex"
      to: "test/mix/tasks/scoria.install_check_test.exs"
      via: "trailer and exit code contract assertions"
      pattern: "SCORIA_CHECK_RESULT"
---

<objective>
Finish Phase 59 by implementing check semantics and enforcing stable machine-readable outcomes.

Purpose: Satisfy INST-04 directly while preserving INST-03 and INST-05 contracts from Wave 1.
Output: `mix scoria.install --check` reports deterministic compatibility/drift state, exits with `0/1/2` semantics, and emits one stable trailer line for CI parsers.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-planner-contract-foundation/59-CONTEXT.md
@.planning/phases/59-planner-contract-foundation/59-RESEARCH.md
@.planning/phases/59-planner-contract-foundation/59-PATTERNS.md
@.planning/phases/59-planner-contract-foundation/59-VALIDATION.md
@lib/mix/tasks/scoria.install.ex
@lib/scoria/install/planner.ex
@lib/scoria/verification_lanes.ex
@test/mix/tasks/scoria.install_test.exs
@test/scoria/verification_lanes_test.exs
@test/mix/tasks/test.adoption_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 59-02-01: Implement check result aggregation, stable trailer line, and tri-state exits</name>
  <requirement_ids>INST-04, INST-03, INST-05</requirement_ids>
  <files>lib/mix/tasks/scoria.install.ex, lib/scoria/install/report.ex</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/59-planner-contract-foundation/59-CONTEXT.md
    .planning/phases/59-planner-contract-foundation/59-PATTERNS.md
    lib/mix/tasks/scoria.install.ex
    lib/scoria/install/planner.ex
    lib/scoria/verification_lanes.ex
  </read_first>
  <action>Create `lib/scoria/install/report.ex` with module `Scoria.Install.Report` that exposes `check_result(plan)`, `render_human(plan, mode)`, `render_json(plan, mode)`, and `trailer_line(result)`. Implement exact exit mapping in `check_result/1`: return `%{status: :compliant, exit_code: 0}` when all planner entries are `:no_op` (including explicit intentional-skip evidence), `%{status: :drift, exit_code: 1}` when any entry is `:create` or `:update`, `%{status: :manual_review, exit_code: 1}` when any entry is `:manual_review`, and `%{status: :error, exit_code: 2}` for planner/runtime failures. In `Mix.Tasks.Scoria.Install.run/1`, add a dedicated check-mode branch that calls `Scoria.Install.Planner.build/4`, renders output through `Scoria.Install.Report`, prints trailer exactly as `SCORIA_CHECK_RESULT status=<status> exit_code=<exit_code>`, and exits via `System.halt(exit_code)`. Keep this branch no-write by invoking planner/report modules only.</action>
  <acceptance_criteria>
    - `rg -n "defmodule Scoria.Install.Report" lib/scoria/install/report.ex` returns one module declaration.
    - `rg -n "def check_result\\(plan\\)" lib/scoria/install/report.ex` returns one check-result function signature.
    - `rg -n "SCORIA_CHECK_RESULT status=\\#\\{status\\} exit_code=\\#\\{exit_code\\}" lib/scoria/install/report.ex` returns one trailer format string.
    - `rg -n "System.halt\\(exit_code\\)" lib/mix/tasks/scoria.install.ex` returns one explicit check exit call.
    - `rg -n "exit_code: 0" lib/scoria/install/report.ex` returns the compliant exit code outcome.
    - `rg -n "exit_code: 1" lib/scoria/install/report.ex` returns the unsafe/manual-review exit code outcome.
    - `rg -n "exit_code: 2" lib/scoria/install/report.ex` returns the error exit code outcome.
    - `MIX_ENV=test mix test test/mix/tasks/scoria.install_check_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/mix/tasks/scoria.install_check_test.exs</automated>
  </verify>
  <done>Check mode emits deterministic status/trailer output and returns stable 0/1/2 semantics from planner classifications.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 59-02-02: Add subprocess check-semantic tests and reliability-guardrail regression checks</name>
  <requirement_ids>INST-04, INST-03, INST-05</requirement_ids>
  <files>test/mix/tasks/scoria.install_check_test.exs, test/mix/tasks/scoria.install_test.exs, test/scoria/verification_lanes_test.exs, test/mix/tasks/test.adoption_test.exs</files>
  <read_first>
    .planning/STATE.md
    .planning/phases/59-planner-contract-foundation/59-VALIDATION.md
    lib/mix/tasks/scoria.install.ex
    lib/scoria/install/report.ex
    test/mix/tasks/scoria.install_test.exs
    test/scoria/verification_lanes_test.exs
    test/mix/tasks/test.adoption_test.exs
  </read_first>
  <action>Create `test/mix/tasks/scoria.install_check_test.exs` using subprocess execution with `System.cmd("mix", ["scoria.install", "--check"], cd: fixture_root, stderr_to_stdout: true)` and assert four concrete scenarios: compliant fixture returns exit `0` plus trailer `SCORIA_CHECK_RESULT status=compliant exit_code=0`; drift fixture returns exit `1` plus trailer `status=drift exit_code=1`; ambiguous fixture returns exit `1` plus trailer `status=manual_review exit_code=1`; unreadable/invalid fixture returns exit `2` plus trailer `status=error exit_code=2`. Add one test in `test/mix/tasks/scoria.install_test.exs` confirming `--check` performs no writes (same before/after file snapshot strategy as dry-run). Re-run and preserve lane contracts by keeping `test/scoria/verification_lanes_test.exs` and `test/mix/tasks/test.adoption_test.exs` green without command-contract changes.</action>
  <acceptance_criteria>
    - `rg -n "status=compliant exit_code=0" test/mix/tasks/scoria.install_check_test.exs` returns the compliant trailer assertion.
    - `rg -n "status=drift exit_code=1" test/mix/tasks/scoria.install_check_test.exs` returns the drift trailer assertion.
    - `rg -n "status=manual_review exit_code=1" test/mix/tasks/scoria.install_check_test.exs` returns the manual-review trailer assertion.
    - `rg -n "status=error exit_code=2" test/mix/tasks/scoria.install_check_test.exs` returns the error trailer assertion.
    - `rg -n "System.cmd\\(\"mix\", \\[\"scoria.install\", \"--check\"" test/mix/tasks/scoria.install_check_test.exs` returns subprocess invocation lines.
    - `rg -n "mix scoria.install --check does not mutate host files" test/mix/tasks/scoria.install_test.exs` returns the no-write check test name.
    - `MIX_ENV=test mix test test/mix/tasks/scoria.install_check_test.exs test/mix/tasks/scoria.install_test.exs` passes.
    - `MIX_ENV=test mix test test/scoria/verification_lanes_test.exs test/mix/tasks/test.adoption_test.exs` passes.
    - `MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.install_check_test.exs test/scoria/verification_lanes_test.exs test/mix/tasks/test.adoption_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.install_check_test.exs test/scoria/verification_lanes_test.exs test/mix/tasks/test.adoption_test.exs</automated>
  </verify>
  <done>Check mode exit semantics and trailer stability are proven in subprocess tests while v2.4 reliability and lane contracts remain intact.</done>
</task>

</tasks>

<threat_model>
## Security Gate

- Enforcement: enabled
- ASVS level: 1
- Block threshold: high severity threats

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Check result -> CI release gate | Exit code and trailer semantics determine pass/fail automation behavior. |
| Planner/check integration -> reliability lanes | New check logic must not break existing adoption/closeout lane contracts. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-59-04 | Spoofing | high | `--check` exit semantics | block | Block if unsafe states can return exit `0` or error states collapse into exit `1`. | Subprocess tests assert exact 0/1/2 mappings for compliant, drift/manual-review, and error fixtures. |
| T-59-05 | Repudiation | high | machine trailer contract | block | Block if trailer format or status vocabulary changes from stable contract. | Trailer regex assertions for all four status scenarios. |
| T-59-06 | Tampering | medium | reliability guardrails | mitigate | Re-run verification lane tests to ensure check integration does not alter v2.4 lane contracts. | `verification_lanes_test` and `test.adoption_test` regression commands. |

High-severity threats T-59-04 and T-59-05 are hard blockers for Phase 59 closeout.
</threat_model>

<verification>
Run task-level checks after each task, then execute:

```bash
MIX_ENV=test mix test test/mix/tasks/scoria.install_check_test.exs
MIX_ENV=test mix test test/mix/tasks/scoria.install_test.exs test/mix/tasks/scoria.install_check_test.exs
MIX_ENV=test mix test test/scoria/verification_lanes_test.exs test/mix/tasks/test.adoption_test.exs
```
</verification>

<success_criteria>
- `mix scoria.install --check` emits deterministic diagnostics with stable 0/1/2 exits and one trailer line contract.
- Check mode behavior is no-write and sourced from the same planner artifact as dry-run.
- Classification rationale remains visible in check output for create/update/no-op/manual-review outcomes.
- Existing v2.4 lane and reliability guardrails stay green after planner/check integration.
</success_criteria>

<output>
After completion, create `.planning/phases/59-planner-contract-foundation/59-02-SUMMARY.md`.
</output>
