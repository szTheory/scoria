---
phase: 63-manifest-check-fingerprint-hardening
plan: 02
type: execute
wave: 1
depends_on: [63-01]
files_modified:
  - lib/scoria/install/contract.ex
  - lib/scoria/install/report.ex
  - lib/mix/tasks/scoria.install.ex
  - test/scoria/install/report_test.exs
autonomous: true
requirements: [INST-07]
must_haves:
  truths:
    - "JSON adds optional top-level manifest map with check_role informational and apply_role freshness_baseline (D-15)."
    - "Human output prints one calm manifest context line for present and absent states (D-16)."
    - "schema_version stays 1.0 — additive fields only (D-15 discretion)."
    - "mix help documents apply freshness with pointer to operator guide (D-11)."
  artifacts:
    - path: "lib/scoria/install/report.ex"
      provides: "Additive manifest metadata in human and JSON check output"
      contains: "check_role"
    - path: "lib/scoria/install/contract.ex"
      provides: "Manifest metadata key SSOT"
      contains: "manifest_check_role"
    - path: "lib/mix/tasks/scoria.install.ex"
      provides: "Apply freshness moduledoc slice"
      contains: "Apply freshness"
  key_links:
    - from: "lib/scoria/install/planner.ex"
      to: "lib/scoria/install/report.ex"
      via: "plan.manifest_state and manifest_path consumed in render"
      pattern: "manifest_state"
---

<objective>
Expose manifest check vs apply roles in operator-facing output (human + JSON) and command help without changing tri-state trailer or remediation payloads.

Purpose: Make the live-host-only check contract discoverable at the CLI and automation boundary.
Output: Extended `Report`, `Contract` constants, and `Mix.Tasks.Scoria.Install` moduledoc.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/63-manifest-check-fingerprint-hardening/63-CONTEXT.md
@.planning/phases/63-manifest-check-fingerprint-hardening/63-RESEARCH.md
@lib/scoria/install/report.ex
@lib/scoria/install/contract.ex
@lib/mix/tasks/scoria.install.ex
@docs/operator_verification.md

Depends on plan 63-01 for `manifest_state` and `manifest_path` on plan map.
</context>

<tasks>

<task type="auto">
  <name>Task 63-02-01: Add Contract manifest metadata constants</name>
  <requirement_ids>INST-07</requirement_ids>
  <files>lib/scoria/install/contract.ex</files>
  <read_first>
    lib/scoria/install/contract.ex
    .planning/phases/63-manifest-check-fingerprint-hardening/63-CONTEXT.md
  </read_first>
  <action>Extend `Scoria.Install.Contract` with functions: `manifest_check_role/0` returning `"informational"`, `manifest_apply_role/0` returning `"freshness_baseline"`, and `manifest_json_keys/0` returning `[:present, :path, :schema_version, :check_role, :apply_role]`. Keep `schema_version/0` as `"1.0"`. Document in module `@moduledoc` that manifest JSON is additive and does not affect `check_result/1`.</action>
  <acceptance_criteria>
    - `rg 'def manifest_check_role' lib/scoria/install/contract.ex` returns `"informational"`.
    - `rg 'def manifest_apply_role' lib/scoria/install/contract.ex` returns `"freshness_baseline"`.
    - `Contract.schema_version()` remains `"1.0"` per `rg 'def schema_version' lib/scoria/install/contract.ex`.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <done>Contract is SSOT for manifest metadata strings used in JSON output.</done>
</task>

<task type="auto">
  <name>Task 63-02-02: Render additive manifest metadata in Report human and JSON</name>
  <requirement_ids>INST-07</requirement_ids>
  <files>lib/scoria/install/report.ex, test/scoria/install/report_test.exs</files>
  <read_first>
    lib/scoria/install/report.ex
    lib/scoria/install/contract.ex
    lib/scoria/install/planner.ex
    test/scoria/install/report_test.exs
    .planning/phases/63-manifest-check-fingerprint-hardening/63-CONTEXT.md
  </read_first>
  <action>Update `Scoria.Install.Report`: (1) In `normalize_plan_for_json/2`, add top-level `"manifest"` map when plan has `manifest_state`: keys `"present"` (boolean), `"path"` (string from plan), `"schema_version"` (integer 1 from manifest module or 1 default), `"check_role"` from `Contract.manifest_check_role/0`, `"apply_role"` from `Contract.manifest_apply_role/0`. (2) In `normalize_entry_for_json/1`, include `"manifest_fingerprint"` key only when entry has `:manifest_fingerprint` — do not rename existing `"fingerprint"` (live observed). (3) In `render_human/2`, after the mode header line, insert exactly one manifest context line: if `manifest_state == :absent`, print `Install manifest not found — check inspected live host surfaces only. First successful apply writes .scoria/install/manifest.json.`; if `:present`, print `Install manifest present at {path} — informational snapshot only; check used live surface fingerprints.` (use actual path from plan). (4) Extend `report_test.exs` with tests for JSON manifest map keys and both human lines. Do not change `check_result/1` or `trailer_line/1`.</action>
  <acceptance_criteria>
    - `rg 'check_role' lib/scoria/install/report.ex` returns match in JSON builder.
    - `rg 'Install manifest not found' lib/scoria/install/report.ex` returns absent-state human line.
    - `rg 'manifest_fingerprint' lib/scoria/install/report.ex` returns optional per-entry key handling.
    - `MIX_ENV=test mix test test/scoria/install/report_test.exs` exits 0.
    - `MIX_ENV=test mix test test/mix/tasks/scoria.install_check_test.exs` exits 0 (trailer unchanged).
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/install/report_test.exs test/mix/tasks/scoria.install_check_test.exs</automated>
  </verify>
  <done>Human and JSON outputs document manifest role additively; existing fingerprint field remains live-observed.</done>
</task>

<task type="auto">
  <name>Task 63-02-03: Add Apply freshness section to mix scoria.install moduledoc</name>
  <requirement_ids>INST-07</requirement_ids>
  <files>lib/mix/tasks/scoria.install.ex</files>
  <read_first>
    lib/mix/tasks/scoria.install.ex
    docs/operator_verification.md
    lib/scoria/install/manifest.ex
  </read_first>
  <action>In `Mix.Tasks.Scoria.Install` `@moduledoc`, add section `## Apply freshness` after existing mode documentation. State: apply compares plan entry fingerprints captured at plan build to live disk before writes (`Manifest.validate_freshness/2`); stored `.scoria/install/manifest.json` is not the apply gate input; if apply blocks, re-run `--dry-run` and `--check` without editing managed files between check and apply. Link to `docs/operator_verification.md` for the `Check vs apply drift detection` subsection (use relative path in backticks). Do not duplicate full operator workflow in moduledoc.</action>
  <acceptance_criteria>
    - `rg '## Apply freshness' lib/mix/tasks/scoria.install.ex` returns section header.
    - `rg 'validate_freshness|stale' lib/mix/tasks/scoria.install.ex` returns apply gate mention.
    - `mix help scoria.install` output includes `Apply freshness`.
    - `mix help scoria.install` output includes `operator_verification`.
  </acceptance_criteria>
  <verify>
    <automated>mix help scoria.install | rg -n 'Apply freshness|operator_verification'</automated>
  </verify>
  <done>mix help surfaces apply-time fingerprint gate and points operators to the guide.</done>
</task>

</tasks>

<threat_model>
## Security Gate

- Enforcement: enabled
- ASVS level: 1
- Block threshold: high

| ID | Threat | Severity | Mitigation in plan |
|----|--------|----------|-------------------|
| T-63-03 | JSON manifest block leaks sensitive paths | low | path is install-relative `.scoria/install/manifest.json` only |
| T-63-04 | Information disclosure via manifest_fingerprint in JSON | low | Same data already on disk in manifest file |

No high-severity threats. Plan may proceed.
</threat_model>

<verification>
## Plan Verification

- [ ] Contract manifest role functions exist
- [ ] Report JSON manifest map and human lines
- [ ] report_test.exs and install_check_test.exs green
- [ ] mix help includes Apply freshness
</verification>
