---
phase: 63-manifest-check-fingerprint-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/install/planner.ex
  - lib/scoria/install/manifest.ex
autonomous: true
requirements: [INST-07]
must_haves:
  truths:
    - "Surface analyzer fingerprints remain authoritative for check/apply plan entries (D-01, D-04)."
    - "Stored manifest never overrides live :fingerprint via put_new (D-02, D-04)."
    - "Plan exposes manifest_state :absent | :present for reporting without affecting classification (D-07, D-10)."
    - "Optional per-entry manifest_fingerprint attached when stored manifest entry exists (D-05, D-15)."
  artifacts:
    - path: "lib/scoria/install/planner.ex"
      provides: "Honest planner contract without dead manifest fingerprint merge"
      contains: "manifest_state"
    - path: "lib/scoria/install/manifest.ex"
      provides: "Documented check vs apply manifest roles"
      contains: "@moduledoc"
  key_links:
    - from: "lib/scoria/install/planner.ex"
      to: "lib/scoria/install/manifest.ex"
      via: "Manifest.load/1 for informational metadata only"
      pattern: "Manifest.load"
---

<objective>
Remove the misleading manifest fingerprint merge path and attach honest manifest metadata on the planner artifact so check/dry-run/apply share a single truthful contract.

Purpose: Close audit gap `manifest-check-fingerprint` at the code layer without changing tri-state semantics or apply freshness gates.
Output: Updated `Planner` and `Manifest` with maintainer-facing documentation of check vs apply roles.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-manifest-check-fingerprint-hardening/63-CONTEXT.md
@.planning/phases/63-manifest-check-fingerprint-hardening/63-RESEARCH.md
@.planning/phases/63-manifest-check-fingerprint-hardening/63-PATTERNS.md
@.planning/phases/63-manifest-check-fingerprint-hardening/63-VALIDATION.md
@lib/scoria/install/planner.ex
@lib/scoria/install/manifest.ex
@lib/scoria/install/surface/router.ex

Locked decisions D-01 through D-10 and D-18 are mandatory. Do not change `Report.check_result/1`, `Manifest.validate_freshness/2`, or `ApplyExecutor` behavior in this plan.
</context>

<tasks>

<task type="auto">
  <name>Task 63-01-01: Remove dead manifest fingerprint merge from planner</name>
  <requirement_ids>INST-07</requirement_ids>
  <files>lib/scoria/install/planner.ex</files>
  <read_first>
    .planning/phases/63-manifest-check-fingerprint-hardening/63-CONTEXT.md
    lib/scoria/install/planner.ex
    lib/scoria/install/surface/router.ex
    lib/scoria/install/manifest.ex
  </read_first>
  <action>In `normalize_contract_fields/4`, delete the line `|> Map.put_new(:fingerprint, manifest_entry[:fingerprint] || "unavailable")`. Do not replace with `Map.put/3`. Live `:fingerprint` from surface analyzers must remain the only fingerprint on plan entries. When `manifest_entry` has a `:fingerprint` key (atom or string), set optional field `manifest_fingerprint` on the entry via a private helper (e.g. `maybe_put_manifest_fingerprint/2`) — only when stored value is present and not `"unavailable"`. Never overwrite `:fingerprint` from manifest data.</action>
  <acceptance_criteria>
    - `rg 'put_new\\(:fingerprint, manifest_entry' lib/scoria/install/planner.ex` returns no matches.
    - `rg 'manifest_fingerprint' lib/scoria/install/planner.ex` returns at least one match.
    - `rg 'Map\\.put\\(:fingerprint' lib/scoria/install/planner.ex` returns no matches (no manifest override via put).
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rg 'put_new\(:fingerprint, manifest_entry' lib/scoria/install/planner.ex; test $? -ne 0</automated>
  </verify>
  <done>Planner no longer suggests manifest merge into live fingerprint; optional manifest_fingerprint is informational only.</done>
</task>

<task type="auto">
  <name>Task 63-01-02: Add plan-level manifest_state and build metadata</name>
  <requirement_ids>INST-07</requirement_ids>
  <files>lib/scoria/install/planner.ex</files>
  <read_first>
    lib/scoria/install/planner.ex
    lib/scoria/install/manifest.ex
    .planning/phases/63-manifest-check-fingerprint-hardening/63-CONTEXT.md
  </read_first>
  <action>In `Planner.build/4`, after `Manifest.load(project_root)`, compute `manifest_state` as `:present` when manifest file exists at `Manifest.path(project_root)` and decoded entries map is non-empty OR file exists with schema_version 1 (treat empty entries file as `:present` if file exists); otherwise `:absent`. Add top-level plan key `manifest_state` with that atom. Add top-level `manifest_path` string via `Manifest.path(project_root)`. Do not add manifest_state to individual entry classifications or drift reason codes.</action>
  <acceptance_criteria>
    - `rg 'manifest_state' lib/scoria/install/planner.ex` returns matches in build return map.
    - `rg 'manifest_path' lib/scoria/install/planner.ex` returns at least one match.
    - `MIX_ENV=test mix test test/scoria/install/mode_equivalence_test.exs` exits 0 (update `normalize_plan/1` in test to drop or include new keys consistently in both modes).
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/install/mode_equivalence_test.exs</automated>
  </verify>
  <done>Plan artifact carries manifest_state and path for downstream report rendering without affecting check_result.</done>
</task>

<task type="auto">
  <name>Task 63-01-03: Document check vs apply roles on Manifest and Planner</name>
  <requirement_ids>INST-07</requirement_ids>
  <files>lib/scoria/install/manifest.ex, lib/scoria/install/planner.ex</files>
  <read_first>
    lib/scoria/install/manifest.ex
    lib/scoria/install/planner.ex
    lib/scoria/install/apply_executor.ex
    .planning/phases/63-manifest-check-fingerprint-hardening/63-CONTEXT.md
  </read_first>
  <action>Add `@moduledoc` to `Scoria.Install.Manifest` stating: (1) `.scoria/install/manifest.json` is written post-apply as last-applied snapshot; (2) `load/1` is informational for planner/report only — not a check-time drift baseline; (3) `validate_freshness/2` compares plan entry fingerprint at build time to live disk at apply time and does not read stored manifest for the gate. Add `@moduledoc` to `Scoria.Install.Planner` stating: `--check` and `--dry-run` classify from live surface analyzers only; manifest fingerprints never override entry `:fingerprint`; `manifest_state` is reporting-only.</action>
  <acceptance_criteria>
    - `rg '@moduledoc' lib/scoria/install/manifest.ex` returns a module doc block containing `check` and `apply`.
    - `rg '@moduledoc' lib/scoria/install/planner.ex` returns a module doc block containing `live` or `surface`.
    - `rg 'validate_freshness' lib/scoria/install/manifest.ex` is referenced in moduledoc text (grep moduledoc section for "freshness" or "apply").
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rg '@moduledoc' lib/scoria/install/manifest.ex lib/scoria/install/planner.ex</automated>
  </verify>
  <done>Maintainers can read module docs for check vs apply fingerprint contracts without reading planning artifacts.</done>
</task>

</tasks>

<threat_model>
## Security Gate

- Enforcement: enabled
- ASVS level: 1
- Block threshold: high

| ID | Threat | Severity | Mitigation in plan |
|----|--------|----------|-------------------|
| T-63-01 | Attacker tampers manifest to force wrong check exit | low | Check exit unchanged; manifest not used for classification |
| T-63-02 | Removing fingerprint merge breaks apply gate | low | Out of scope — validate_freshness untouched |

No high-severity threats. Plan may proceed.
</threat_model>

<verification>
## Plan Verification

- [ ] `put_new(:fingerprint, manifest_entry` removed from planner.ex
- [ ] `manifest_state` on plan map
- [ ] Module docs on Manifest and Planner
- [ ] mode_equivalence_test.exs green
</verification>
