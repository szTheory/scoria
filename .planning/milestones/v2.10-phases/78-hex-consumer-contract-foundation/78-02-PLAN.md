---
phase: 78-hex-consumer-contract-foundation
plan: 02
type: execute
wave: 2
depends_on: [78-01]
files_modified:
  - lib/scoria/hex_consumer_contract.ex
  - test/scoria/package_surface_test.exs
autonomous: true
requirements: [DOCS-HEX-01]
must_haves:
  truths:
    - "ensure_current_unpack_root!/0 resolves unpack dir containing packaged mix.exs with three-layer cache: env short-circuit, fingerprint dir, exclusive lock build (D-10, D-12)."
    - "package_fingerprint/0 hashes package[:files] file contents plus version — not list-only (D-10 discretion)."
    - "SCORIA_HEX_UNPACK_ROOT env reuses release preview output when set and valid (D-12)."
    - "Shared cache under tmp/scoria-hex-consumer/ is never deleted in test on_exit (D-10)."
    - "package_surface_test hex preview uses ensure_current_unpack_root!/0 instead of per-test hex.build (D-13)."
  artifacts:
    - path: "lib/scoria/hex_consumer_contract.ex"
      provides: "Fingerprinted unpack cache with lock and env override"
      contains: "ensure_current_unpack_root!"
    - path: "test/scoria/package_surface_test.exs"
      provides: "Hex preview integration via contract cache"
      contains: "ensure_current_unpack_root!"
  key_links:
    - from: "lib/scoria/hex_consumer_contract.ex"
      to: "lib/mix/tasks/scoria.release_preview.ex"
      via: "unpack_root!/1 discovery matches nested hex.build --unpack layout"
      pattern: "unpack_root!"
    - from: "test/scoria/package_surface_test.exs"
      to: "lib/scoria/hex_consumer_contract.ex"
      via: "hex preview test calls ensure_current_unpack_root!/0"
      pattern: "ensure_current_unpack_root!"
---

<objective>
Replace the wave-1 unpack stub with a fingerprinted, lock-protected `ensure_current_unpack_root!/0` cache and refactor `package_surface_test` hex preview to consume it.

Purpose: Eliminate redundant per-test `hex.build` runs and align local/CI unpack reuse with `tmp/scoria-release-preview` via `SCORIA_HEX_UNPACK_ROOT`.
Output: Full unpack resolver in `HexConsumerContract`; `package_surface_test` hex preview integration test uses cache.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/78-hex-consumer-contract-foundation/78-CONTEXT.md
@.planning/phases/78-hex-consumer-contract-foundation/78-RESEARCH.md
@.planning/phases/78-hex-consumer-contract-foundation/78-VALIDATION.md
@lib/scoria/hex_consumer_contract.ex
@lib/mix/tasks/scoria.release_preview.ex
@test/scoria/package_surface_test.exs

Depends on plan 78-01 for HexConsumerContract API shell. Do not modify Generator, Runner, consumer proof, or ci-verify.yml in this plan. Do not touch ci_policy_contract_test or adoption_surface_test (D-23).
</context>

<tasks>

<task type="auto">
  <name>Task 78-02-01: Implement package_fingerprint/0, unpack_root!/1, and ensure_current_unpack_root!/0</name>
  <requirement_ids>DOCS-HEX-01</requirement_ids>
  <files>lib/scoria/hex_consumer_contract.ex</files>
  <read_first>
    lib/scoria/hex_consumer_contract.ex
    lib/mix/tasks/scoria.release_preview.ex
    mix.exs
    .planning/phases/78-hex-consumer-contract-foundation/78-CONTEXT.md
    .planning/phases/78-hex-consumer-contract-foundation/78-RESEARCH.md
  </read_first>
  <action>
Replace stub `ensure_current_unpack_root!/0` in `Scoria.HexConsumerContract` with full resolver:

1. **`unpack_root!/1`** — copy nested-dir discovery from `Mix.Tasks.Scoria.ReleasePreview.unpack_root!/1`: if `mix.exs` at output root return it; else find first child dir containing `mix.exs`; raise if missing.

2. **`package_fingerprint/0`** — read `Mix.Project.config()[:package][:files]` and `published_version/0`; SHA256 each listed path's file contents (skip missing with raise); concatenate sorted `relative_path:hash` lines; truncate hex digest to 12 chars for dir suffix. Export publicly for tests/debug.

3. **`ensure_current_unpack_root!/0` resolution order:**
   - If `System.get_env("SCORIA_HEX_UNPACK_ROOT")` set: expand path, call `unpack_root!/1`, verify `mix.exs` exists, return absolute path (D-12).
   - Else compute `version = published_version()`, `fp = package_fingerprint()`, cache dir `tmp/scoria-hex-consumer/#{version}-#{fp}/`.
   - Cache hit: dir exists, stamp file `.scoria-hex-consumer.stamp` content equals `fp`, and `unpack_root!/1` valid → return unpack root.
   - Cache miss: acquire exclusive lock at `tmp/scoria-hex-consumer/.build.lock` via `:file.open` + `:file.lock(fd, :exclusive)`; double-check cache after lock; run `System.cmd("mix", ["hex.build", "--unpack", "--output", cache_dir], cd: File.cwd!(), stderr_to_stdout: true)` asserting exit 0; write stamp; unlock in `after`; return `unpack_root!(cache_dir)`.

4. Never `File.rm_rf!` shared cache parent in this module. Do not use compile-time `@unpack_root` (D-15).

Add `@cache_parent "tmp/scoria-hex-consumer"` module attribute or equivalent constant.
  </action>
  <acceptance_criteria>
    - `rg -n 'def ensure_current_unpack_root!' lib/scoria/hex_consumer_contract.ex` returns implementation (not Mix.raise stub).
    - `rg -n 'def package_fingerprint' lib/scoria/hex_consumer_contract.ex` returns function.
    - `rg -n 'def unpack_root!' lib/scoria/hex_consumer_contract.ex` returns function.
    - `rg -n 'SCORIA_HEX_UNPACK_ROOT' lib/scoria/hex_consumer_contract.ex` returns env short-circuit.
    - `rg -n ':file\\.lock' lib/scoria/hex_consumer_contract.ex` returns exclusive lock usage.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <done>ensure_current_unpack_root!/0 builds or reuses fingerprinted unpack dirs with env override and lock safety.</done>
</task>

<task type="auto">
  <name>Task 78-02-02: Refactor package_surface_test hex preview to use contract cache</name>
  <requirement_ids>DOCS-HEX-01</requirement_ids>
  <files>test/scoria/package_surface_test.exs</files>
  <read_first>
    test/scoria/package_surface_test.exs
    lib/scoria/hex_consumer_contract.ex
    lib/mix/tasks/scoria.release_preview.ex
    .planning/phases/78-hex-consumer-contract-foundation/78-RESEARCH.md
  </read_first>
  <action>
In `test/scoria/package_surface_test.exs` test `"hex preview includes the required release surface"`: remove `System.tmp_dir!()` unique output dir, inline `System.cmd("mix", ["hex.build", ...])`, `on_exit` rm of output dir, and local `find_unpack_root!/1` helper. Replace with `unpack_root = Scoria.HexConsumerContract.ensure_current_unpack_root!()` then iterate `@required_package_paths` asserting `File.exists?(Path.join(unpack_root, relative_path))`. Keep `@required_package_paths` list unchanged (broader than ReleasePreview — includes `support_copilot_gallery.md`). Do not add `on_exit` that deletes `tmp/scoria-hex-consumer/` (D-10).
  </action>
  <acceptance_criteria>
    - `rg -n 'ensure_current_unpack_root!' test/scoria/package_surface_test.exs` returns match in hex preview test.
    - `rg -n 'hex\\.build' test/scoria/package_surface_test.exs` returns zero matches (no inline hex.build in test file).
    - `rg -n 'find_unpack_root!' test/scoria/package_surface_test.exs` returns zero matches.
    - `MIX_ENV=test mix test test/scoria/package_surface_test.exs` exits 0.
    - Second consecutive run `MIX_ENV=test mix test test/scoria/package_surface_test.exs` exits 0 (cache hit, no duplicate build failure).
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/package_surface_test.exs</automated>
  </verify>
  <done>Hex preview integration test proves unpack artifact surface via shared contract cache.</done>
</task>

</tasks>

<verification>
After all tasks:

```bash
MIX_ENV=test mix test test/scoria/hex_consumer_contract_test.exs test/scoria/package_surface_test.exs
# Optional CI parity probe (manual per 78-VALIDATION.md):
MIX_ENV=dev mix scoria.release_preview && SCORIA_HEX_UNPACK_ROOT=tmp/scoria-release-preview MIX_ENV=test mix test test/scoria/package_surface_test.exs
```
</verification>

<success_criteria>
- First local run creates `tmp/scoria-hex-consumer/<version>-<fingerprint>/` with valid unpack root.
- Setting `SCORIA_HEX_UNPACK_ROOT=tmp/scoria-release-preview` after `mix scoria.release_preview` skips cache miss build.
- package_surface_test no longer spawns per-test hex.build.
</success_criteria>

<must_haves>
- Unpack root always contains packaged `mix.exs`, not monorepo root.
- Fingerprint invalidates cache when packaged file contents change.
- Shared cache directories survive test runs (no on_exit rm_rf of cache parent).
</must_haves>
