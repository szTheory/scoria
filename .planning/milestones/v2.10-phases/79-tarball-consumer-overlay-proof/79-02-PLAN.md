---
phase: 79-tarball-consumer-overlay-proof
plan: 02
type: execute
wave: 2
depends_on: [79-01]
files_modified:
  - test/support/scoria/host_app_proof/runner.ex
  - test/support/scoria/host_app_proof/generator.ex
autonomous: true
requirements: [HEX-CONSUMER-01]
must_haves:
  truths:
    - "Every failed run_mix!/1 step raises triage block with step, command, host paths, unpack_root, tarball snippet, overlay list, SCORIA_DB_* (no password), replay command, preserve hint (D-39)."
    - "overlay_smoke failures surface first FAIL or ** ( line from nested subprocess output (D-39)."
    - "SCORIA_PRESERVE_HOST in ~w(1 true yes) skips on_exit File.rm_rf! and IO.warns preserved working_root path (D-40)."
    - "Default cleanup-on-exit preserved when SCORIA_PRESERVE_HOST unset — no always-preserve (D-44)."
    - "On proof failure, snapshot copies host.working_root to tmp/scoria-host-proof-last-failure/host/ with MANIFEST.txt before re-raise (D-41)."
    - "Snapshot destination is workspace-relative tmp/scoria-host-proof-last-failure/ — replaces prior on each failure."
    - "No per-step ExUnit timeouts added to Runner (D-35)."
  artifacts:
    - path: "test/support/scoria/host_app_proof/runner.ex"
      provides: "triage_message/4, extract_nested_failure_line/1, maybe_snapshot_failure!/2"
      contains: "SCORIA_PRESERVE_HOST"
    - path: "test/support/scoria/host_app_proof/generator.ex"
      provides: "preserve_host?/0 opt-in cleanup skip"
      contains: "SCORIA_PRESERVE_HOST"
    - path: "tmp/scoria-host-proof-last-failure/MANIFEST.txt"
      provides: "Failure snapshot manifest (created on induced failure only)"
      contains: "failed_step:"
  key_links:
    - from: "test/support/scoria/host_app_proof/runner.ex"
      to: "test/support/scoria/host_app_proof/generator.ex"
      via: "host.working_root and host.unpack_root on enriched map from 79-01"
      pattern: "host.working_root"
    - from: "test/support/scoria/host_app_proof/runner.ex"
      to: "lib/scoria/hex_consumer_contract.ex"
      via: "tarball_dep_snippet, package_fingerprint, published_version in MANIFEST"
      pattern: "HexConsumerContract"
---

<objective>
Layer operator-first failure diagnostics on the host proof harness: rich raises, opt-in host preservation, and workspace failure snapshots.

Purpose: Make tarball overlay failures in CI and locally triageable without re-running full `phx.new` — critical for HOST-01 sandbox and HOST-02 LiveView smokes on packaged artifact.
Output: Enhanced `Runner.run_mix!/1` failure path, `SCORIA_PRESERVE_HOST` in Generator cleanup, snapshot under `tmp/scoria-host-proof-last-failure/`.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/79-tarball-consumer-overlay-proof/79-CONTEXT.md
@.planning/phases/79-tarball-consumer-overlay-proof/79-RESEARCH.md
@.planning/phases/79-tarball-consumer-overlay-proof/79-VALIDATION.md
@test/support/scoria/host_app_proof/runner.ex
@test/support/scoria/host_app_proof/generator.ex
@lib/scoria/hex_consumer_contract.ex

Depends on 79-01 for `run_full_proof!/1` consumer switch, `expected_steps/1`, and host map fields `working_root`, `unpack_root`, `dep_mode`. Do not upload CI artifacts in this plan — 79-03. Do not modify consumer test except if compile requires alias changes (should not).
</context>

<tasks>

<task type="auto">
  <name>Task 79-02-01: Implement enhanced Runner triage raise on subprocess failure</name>
  <requirement_ids>HEX-CONSUMER-01</requirement_ids>
  <files>test/support/scoria/host_app_proof/runner.ex</files>
  <read_first>
    test/support/scoria/host_app_proof/runner.ex
    lib/scoria/hex_consumer_contract.ex
    .planning/phases/79-tarball-consumer-overlay-proof/79-CONTEXT.md
    .planning/phases/79-tarball-consumer-overlay-proof/79-RESEARCH.md
  </read_first>
  <action>
Refactor `Scoria.TestSupport.HostAppProof.Runner` failure path in `run_mix!/1` (lines 63–70):

1. Extract private `defp triage_message(host, step, args, output)` returning plain string raise body (no custom exception module).
2. Required fields in triage block (D-39):
   - `step`, `command: mix #{Enum.join(args, " ")}`
   - `host.root`, `host.app_name`, `host.db_name`
   - `unpack_root` from `Map.get(host, :unpack_root)` and `SCORIA_HEX_UNPACK_ROOT=#{System.get_env("SCORIA_HEX_UNPACK_ROOT") || "(unset)"}`
   - Tarball dep snippet: when `host.dep_mode == :hex_tarball` and unpack_root present, include `Scoria.HexConsumerContract.tarball_dep_snippet(unpack_root)`; else note path dep mode
   - Overlay file list: `inspect(host.overlay_tests)`
   - `SCORIA_DB_HOST`, `SCORIA_DB_PORT`, `SCORIA_DB_USERNAME` from env — **omit password**
   - Replay command:
     - Install steps: `"cd #{host.root} && MIX_ENV=test mix #{Enum.join(args, " ")}"`
     - Overlay (`step == :overlay_smoke`): `"cd #{host.root} && MIX_ENV=test mix test #{Enum.join(Enum.map(host.overlay_tests, &("test/" <> &1)), " ")} --trace"`
   - Preserve hint: `"SCORIA_PRESERVE_HOST=1 MIX_ENV=test mix test --only host_proof --trace"`
3. Add `extract_nested_failure_line/1`: when `step == :overlay_smoke`, scan `output` lines for first matching `~r/^\\s*\\d+\\) test/` with `FAIL` or any line containing `** (`; prepend extracted line to triage block before full output.
4. Replace minimal raise with `raise triage_message(host, step, args, output)`.

Do not add per-step timeouts (D-35). Keep `IO.puts("HOST STEP …")` logging unchanged.
  </action>
  <acceptance_criteria>
    - `rg -n 'defp triage_message' test/support/scoria/host_app_proof/runner.ex` returns match.
    - `rg -n 'SCORIA_PRESERVE_HOST' test/support/scoria/host_app_proof/runner.ex` returns preserve hint in triage block.
    - `awk '/defp triage_message/,/^  end$/' test/support/scoria/host_app_proof/runner.ex | rg 'SCORIA_DB_PASSWORD' returns exit code 1 (password omitted from triage body; `host_env/0` may still set password separately).
    - `rg -n 'extract_nested_failure_line' test/support/scoria/host_app_proof/runner.ex` returns match.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
    - Manual: temporarily break an overlay assertion in a local branch; run consumer test; raised message includes replay command and overlay file list (document in commit or skip if reverted).
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <done>Subprocess failures emit structured triage block with replay and preserve hints.</done>
</task>

<task type="auto">
  <name>Task 79-02-02: Add SCORIA_PRESERVE_HOST opt-in to Generator cleanup</name>
  <requirement_ids>HEX-CONSUMER-01</requirement_ids>
  <files>test/support/scoria/host_app_proof/generator.ex</files>
  <read_first>
    test/support/scoria/host_app_proof/generator.ex
    .planning/phases/79-tarball-consumer-overlay-proof/79-CONTEXT.md
    .planning/phases/79-tarball-consumer-overlay-proof/79-PATTERNS.md
  </read_first>
  <action>
In `Scoria.TestSupport.HostAppProof.Generator`:

1. Add private `preserve_host?/0`:

```elixir
defp preserve_host? do
  System.get_env("SCORIA_PRESERVE_HOST") in ~w(1 true yes)
end
```

2. Update `register_cleanup/2` (lines 210–214): when `preserve_host?()` is true, **do not** register `File.rm_rf!` callback; instead `IO.warn("SCORIA_PRESERVE_HOST: preserved host at #{path}")`. When false, keep existing `register.(fn -> File.rm_rf!(path) end)` behavior.

`path` argument is `working_root` (parent of generated app) — same value passed today at line 47. Default remains cleanup-on-exit (D-40, D-44).
  </action>
  <acceptance_criteria>
    - `rg -n 'preserve_host\\?' test/support/scoria/host_app_proof/generator.ex` returns function definition.
    - `rg -n 'SCORIA_PRESERVE_HOST' test/support/scoria/host_app_proof/generator.ex` returns match in preserve_host? and IO.warn.
    - `SCORIA_PRESERVE_HOST=1 MIX_ENV=test mix test --only host_proof` exits 0 and stderr/stdout contains `preserved host at` with path under system tmp.
    - Default path: `MIX_ENV=test mix test --only host_proof` exits 0 without `preserved host at` in output (cleanup-on-exit default).
  </acceptance_criteria>
  <verify>
    <automated>SCORIA_PRESERVE_HOST=1 MIX_ENV=test mix test --only host_proof 2>&1 | rg 'preserved host at'</automated>
  </verify>
  <done>Maintainers can opt in to inspect generated host tree after proof run.</done>
</task>

<task type="auto">
  <name>Task 79-02-03: Implement failure snapshot to tmp/scoria-host-proof-last-failure/</name>
  <requirement_ids>HEX-CONSUMER-01</requirement_ids>
  <files>test/support/scoria/host_app_proof/runner.ex</files>
  <read_first>
    test/support/scoria/host_app_proof/runner.ex
    test/support/scoria/host_app_proof/generator.ex
    lib/scoria/hex_consumer_contract.ex
    .planning/phases/79-tarball-consumer-overlay-proof/79-CONTEXT.md
  </read_first>
  <action>
In `Scoria.TestSupport.HostAppProof.Runner`:

1. Add `@snapshot_root "tmp/scoria-host-proof-last-failure"` module attribute (workspace-relative).
2. Implement `maybe_snapshot_failure!(host, step)` called from `run_mix!/1` **before** `raise triage_message(...)` when `status != 0`.
3. Snapshot semantics (D-41):
   - Resolve destination: `System.get_env("SCORIA_HOST_PROOF_ROOT") || @snapshot_root` (optional override).
   - `File.rm_rf!(destination)` if exists, then `File.mkdir_p!(destination)`.
   - Copy `host.working_root` → `Path.join(destination, "host")` using `File.cp_r!/3` with `dereference_symlinks: true` if available, else `cp -R` via `System.cmd` fallback.
   - Write `Path.join(destination, "MANIFEST.txt")` with minimum fields:
     - `timestamp:` ISO8601 UTC
     - `failed_step:`, `host_root:`, `app_name:`, `db_name:`
     - `unpack_root:`, `scoria_version:` from `HexConsumerContract.published_version/0`
     - `package_fingerprint:` from `HexConsumerContract.package_fingerprint/0`
     - `SCORIA_HEX_UNPACK_ROOT:` env value or `(unset)`
     - `replay_full:` overlay replay command (same as triage block)
     - `replay_preserve: SCORIA_PRESERVE_HOST=1 MIX_ENV=test mix test --only host_proof`
     - `tarball_dep:` snippet when `:hex_tarball`
4. Guard: if `host.working_root` missing or not a directory, skip copy but still write MANIFEST with note.

Do not snapshot on success. Do not copy `_build` or hex cache separately — only working_root tree.
  </action>
  <acceptance_criteria>
    - `rg -n 'maybe_snapshot_failure!' test/support/scoria/host_app_proof/runner.ex` returns definition and call before raise.
    - `rg -n 'scoria-host-proof-last-failure' test/support/scoria/host_app_proof/runner.ex` returns @snapshot_root.
    - Manual induced failure: after broken overlay run, `test -f tmp/scoria-host-proof-last-failure/MANIFEST.txt` succeeds and `test -d tmp/scoria-host-proof-last-failure/host` succeeds.
    - MANIFEST contains `failed_step:` and `package_fingerprint:`.
    - `MIX_ENV=test mix test test/scoria/host_app_consumer_proof_test.exs` exits 0 on clean tree (no snapshot written on green run).
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/host_app_consumer_proof_test.exs</automated>
  </verify>
  <done>Proof failures leave workspace-relative snapshot for agents and CI artifact upload in 79-03.</done>
</task>

</tasks>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-79-06 | Leak SCORIA_DB_PASSWORD in failure output | High | Triage block includes host/port/username only; acceptance criteria grep excludes password in triage_message |
| T-79-07 | Always-preserve hosts exhaust CI disk | Medium | Opt-in env only; default cleanup unchanged (D-44) |
| T-79-08 | Snapshot copies wrong tree without working_root on host map | High | Depends on 79-01 host map enrichment; guard + MANIFEST note if missing |
| T-79-09 | Combined overlay subprocess obscures failing smoke | Medium | extract_nested_failure_line/1 surfaces first FAIL/** (` line |
| T-79-10 | Snapshot writes sensitive Cloak key from generated host test.exs | Low | Snapshot is test-only generated host under maintainer tmp; CI artifact scoped 7-day retention in 79-03 |

**Blockers:** T-79-06 and T-79-08 must be mitigated before plan complete.
</threat_model>

<verification>
After all tasks:

```bash
MIX_ENV=test mix compile --warnings-as-errors
MIX_ENV=test mix test test/scoria/host_app_consumer_proof_test.exs
SCORIA_PRESERVE_HOST=1 MIX_ENV=test mix test --only host_proof
# Manual (induced failure on branch, revert after):
# break priv overlay assertion → confirm tmp/scoria-host-proof-last-failure/MANIFEST.txt
MIX_ENV=test mix test.adoption
```

Green adoption lane confirms diagnostics do not break happy path.
</verification>

<success_criteria>
- Failed proof steps raise actionable triage with replay and preserve commands.
- `SCORIA_PRESERVE_HOST=1` preserves generated host for local inspection.
- Failed runs write replaceable snapshot under `tmp/scoria-host-proof-last-failure/`.
- Happy-path consumer and adoption tests remain green.
</success_criteria>
