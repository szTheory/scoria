---
phase: 80-upgrade-smoke-in-adoption-lane
plan: 03
type: execute
wave: 3
depends_on: [80-01, 80-02]
files_modified:
  - test/scoria/host_app_upgrade_proof_test.exs
  - lib/mix/tasks/test.adoption.ex
  - test/mix/tasks/test.adoption_test.exs
  - .planning/phases/80-upgrade-smoke-in-adoption-lane/80-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [HEX-UPGRADE-01]
must_haves:
  truths:
    - "host_app_upgrade_proof_test uses baseline_unpack_root! then run_upgrade_proof! with ensure_current_unpack_root! (D-53, D-54)."
    - "Test asserts HexConsumerContract.same_semver_content_upgrade?/0 — fails if baseline_fp == current_fp (D-54)."
    - "proof.steps == Runner.expected_upgrade_steps(host) exactly (D-58)."
    - "@moduletag :host_upgrade and timeout 180_000; consumer module unchanged (D-61, D-62, D-63)."
    - "mix test.adoption includes upgrade test file; test.adoption_test.exs guard updated (D-61)."
    - "80-VERIFICATION.md documents latent migration criterion #4; HEX-UPGRADE-01 Complete only with green adoption (D-65, D-68)."
    - "D-56: post_baseline_version/0 deferred — no semver bump assertions until release-please passes 0.1.0."
    - "D-66: knowledge lane not in default host upgrade chain — core ecto.migrate only; matches migration_lane_compatibility_test precedent."
  artifacts:
    - path: "test/scoria/host_app_upgrade_proof_test.exs"
      provides: "Merge-blocking upgrade smoke"
      contains: "run_upgrade_proof!"
    - path: "lib/mix/tasks/test.adoption.ex"
      provides: "Adoption lane includes upgrade proof"
      contains: "host_app_upgrade_proof_test.exs"
---

<objective>
Wire upgrade proof into adoption lane, assert content-revision upgrade invariants, and complete HEX-UPGRADE-01 closeout ceremony.

Purpose: Make upgrade smoke merge-blocking and flip requirement traceability when verification passes.
Output: Green `mix test.adoption`, passed `80-VERIFICATION.md`, ROADMAP/STATE/REQUIREMENTS updated.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/80-upgrade-smoke-in-adoption-lane/80-CONTEXT.md
@.planning/phases/80-upgrade-smoke-in-adoption-lane/80-RESEARCH.md
@.planning/phases/80-upgrade-smoke-in-adoption-lane/80-VALIDATION.md
@test/scoria/host_app_consumer_proof_test.exs
@lib/mix/tasks/test.adoption.ex
@test/mix/tasks/test.adoption_test.exs
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-80-05 | Requirement marked Complete before adoption green | high | D-68 same-PR tail ceremony |
| T-80-06 | Adoption list drift | medium | test.adoption_test.exs guard |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 80-03-01: Create host_app_upgrade_proof_test.exs</name>
  <requirement_ids>HEX-UPGRADE-01</requirement_ids>
  <files>test/scoria/host_app_upgrade_proof_test.exs</files>
  <read_first>
    test/scoria/host_app_consumer_proof_test.exs
    test/support/scoria/host_app_proof/runner.ex
    lib/scoria/hex_consumer_contract.ex
    .planning/phases/80-upgrade-smoke-in-adoption-lane/80-CONTEXT.md
  </read_first>
  <action>
Create `Scoria.HostAppUpgradeProofTest`:

1. `use ExUnit.Case, async: false`
2. `@moduletag timeout: 180_000`
3. `@moduletag :host_upgrade`

4. `setup_all`:
   - `baseline = HexConsumerContract.baseline_unpack_root!()`
   - `current = HexConsumerContract.ensure_current_unpack_root!()`
   - `{:ok, baseline_unpack_root: baseline, current_unpack_root: current}`

5. Single test `"generated Phoenix host proves content-revision upgrade from 0.1.0 baseline"`:
   - `assert HexConsumerContract.same_semver_content_upgrade?()` with message explaining fixture must differ from current
   - `assert HexConsumerContract.baseline_package_fingerprint() != HexConsumerContract.package_fingerprint()`
   - `host = Generator.create_host!(dep_mode: :hex_tarball, unpack_root: baseline, cleanup: &on_exit/1)`
   - Assert mix.exs contains `tarball_dep_snippet(baseline)` not repo root path
   - `proof = Runner.run_upgrade_proof!(host, current_unpack_root: current)`
   - `assert proof.steps == Runner.expected_upgrade_steps(host)`

Do not modify `host_app_consumer_proof_test.exs`.
  </action>
  <acceptance_criteria>
    - `test -f test/scoria/host_app_upgrade_proof_test.exs` exits 0.
    - `rg -n ':host_upgrade' test/scoria/host_app_upgrade_proof_test.exs` returns match.
    - `rg -n 'run_upgrade_proof!' test/scoria/host_app_upgrade_proof_test.exs` returns match.
    - `MIX_ENV=test mix test --only host_upgrade` exits 0 (may take up to 180s).
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test --only host_upgrade</automated>
  </verify>
  <done>Upgrade proof passes in isolation.</done>
</task>

<task type="auto">
  <name>Task 80-03-02: Register upgrade test in adoption lane</name>
  <requirement_ids>HEX-UPGRADE-01</requirement_ids>
  <files>lib/mix/tasks/test.adoption.ex, test/mix/tasks/test.adoption_test.exs</files>
  <read_first>
    lib/mix/tasks/test.adoption.ex
    test/mix/tasks/test.adoption_test.exs
    .planning/phases/79-tarball-consumer-overlay-proof/79-03-PLAN.md
  </read_first>
  <action>
1. In `Mix.Tasks.Scoria.Test.Adoption`, append `"test/scoria/host_app_upgrade_proof_test.exs"` to `@adoption_test_files` immediately after `"test/scoria/host_app_consumer_proof_test.exs"`.

2. Update `test/mix/tasks/test.adoption_test.exs` expected file list to include the same path (mirror Phase 79-03 pattern).

Do not change `VerificationLanes.closeout_order/0`.
  </action>
  <acceptance_criteria>
    - `rg -n 'host_app_upgrade_proof_test' lib/mix/tasks/test.adoption.ex` returns match.
    - `rg -n 'host_app_upgrade_proof_test' test/mix/tasks/test.adoption_test.exs` returns match.
    - `MIX_ENV=test mix test test/mix/tasks/test.adoption_test.exs` exits 0.
    - `MIX_ENV=test mix test.adoption` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/mix/tasks/test.adoption_test.exs && MIX_ENV=test mix test.adoption</automated>
  </verify>
  <done>Upgrade smoke is merge-blocking in adoption lane.</done>
</task>

<task type="auto">
  <name>Task 80-03-03: Phase 80 verification and closeout ceremony</name>
  <requirement_ids>HEX-UPGRADE-01</requirement_ids>
  <files>.planning/phases/80-upgrade-smoke-in-adoption-lane/80-VERIFICATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    .planning/phases/79-tarball-consumer-overlay-proof/79-VERIFICATION.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
    .planning/phases/80-upgrade-smoke-in-adoption-lane/80-CONTEXT.md
  </read_first>
  <action>
1. Create `.planning/phases/80-upgrade-smoke-in-adoption-lane/80-VERIFICATION.md` with `status: passed` only after `mix test.adoption` green in this plan execution. Include:
   - Criteria 1–3 PASS with command evidence
   - Criterion #4 **latent** — document identical 24 migrations at v0.1.0 and HEAD; contract assertion for future `pending_core_migrations/1` when semver or migration basenames diverge
   - Note Phase 80 = content-revision (path tarball), Phase 81 = registry semver (D-55)
   - Timing observation for D-37 escalation decision

2. Update `.planning/REQUIREMENTS.md`: HEX-UPGRADE-01 → Complete

3. Update `.planning/ROADMAP.md` Phase 80: Status Complete with date; Plans: 3/3

4. Update `.planning/STATE.md`: completed phase 80; current focus → Phase 81

Mirror 79-VERIFICATION.md structure for scorer tables.
  </action>
  <acceptance_criteria>
    - `rg -n 'status: passed' .planning/phases/80-upgrade-smoke-in-adoption-lane/80-VERIFICATION.md` returns match.
    - `rg -n 'latent' .planning/phases/80-upgrade-smoke-in-adoption-lane/80-VERIFICATION.md` returns match for criterion #4.
    - `rg -n 'HEX-UPGRADE-01.*Complete' .planning/REQUIREMENTS.md` returns match.
    - `rg -n 'Phase 80.*Complete' .planning/ROADMAP.md` returns match.
    - `rg -n 'Phase 81' .planning/STATE.md` returns current focus.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'status: passed' .planning/phases/80-upgrade-smoke-in-adoption-lane/80-VERIFICATION.md && rg -n 'HEX-UPGRADE-01' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>HEX-UPGRADE-01 traceability and phase closeout recorded.</done>
</task>

</tasks>

<verification>
- `MIX_ENV=test mix test --only host_upgrade`
- `MIX_ENV=test mix test.adoption`
- `MIX_ENV=test mix test test/mix/tasks/test.adoption_test.exs`
</verification>
