---
phase: 82-docs-truth-milestone-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/hex_consumer_contract.ex
  - lib/scoria/adopter_doc_contract.ex
  - README.md
  - docs/adoption_lanes.md
  - docs/operator_verification.md
  - test/scoria/adoption_surface_test.exs
  - test/scoria/ci_policy_contract_test.exs
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DOCS-HEX-01]
must_haves:
  truths:
    - "HexConsumerContract.adopter_doc_surfaces/0 maps README + adoption_lanes only — not operator gate map (D-96, D-95)."
    - "AdopterDocContract.readme_maintainer_command_refutes/0 includes mix scoria.post_publish_smoke (D-100)."
    - "README Verification stays adopter-scoped: mix test.adoption, tarball vocabulary, HexConsumerContract, operator link — no PR-vs-release table or workflow filenames (D-91)."
    - "adoption_lanes.md notes packaged tarball closeout under default runtime proof block with operator guide link — no SCORIA_HEX_UNPACK_ROOT env vars (D-92)."
    - "operator_verification.md gate map expands upgrade sequence substrings for content-revision vs registry semver (D-93, D-104 #5)."
    - "adoption_surface_test pins five adopter anchors via adopter_doc_surfaces/0 macro + refute loop (D-101)."
    - "ci_policy_contract_test pins six maintainer anchors via section_after/2 on gate map (D-104, D-105)."
    - "REQUIREMENTS.md DOCS-HEX-01 checkbox unchecked and traceability Partial until 82-02 — fixes premature Complete (D-112)."
    - "VerificationLanes.closeout_order/0 unchanged — no new closeout lane."
    - "Layered trust narrative (PR tarball vs release registry) lives in operator_verification.md only — README stays install+verify, not release runbook (D-113)."
    - "Adopters see one verification path mix test.adoption; lane commands sourced from VerificationLanes.command/1 in pins (D-115)."
    - "README prose stays evidence-based Field Engineer voice — no milestone banner noise; AdopterDocContract capability nouns unchanged (D-116)."
  artifacts:
    - path: "lib/scoria/hex_consumer_contract.ex"
      provides: "adopter_doc_surfaces/0 per-surface fragment map"
      contains: "def adopter_doc_surfaces"
    - path: "lib/scoria/adopter_doc_contract.ex"
      provides: "post_publish_smoke README refute"
      contains: "mix scoria.post_publish_smoke"
    - path: "test/scoria/adoption_surface_test.exs"
      provides: "adopter hex consumer drift guards"
      contains: "HexConsumerContract.adopter_doc_surfaces"
    - path: "test/scoria/ci_policy_contract_test.exs"
      provides: "maintainer gate map topology pins"
      contains: "PR vs release proof depth"
    - path: ".planning/REQUIREMENTS.md"
      provides: "honest DOCS-HEX-01 Partial ledger"
      contains: "DOCS-HEX-01"
  key_links:
    - from: "lib/scoria/hex_consumer_contract.ex"
      to: "test/scoria/adoption_surface_test.exs"
      via: "adopter_doc_surfaces/0 compile-time macro iteration"
      pattern: "adopter_doc_surfaces"
    - from: "docs/operator_verification.md"
      to: "test/scoria/ci_policy_contract_test.exs"
      via: "section_after gate map subsection extraction"
      pattern: "PR vs release proof depth"
    - from: "lib/scoria/adopter_doc_contract.ex"
      to: "test/scoria/adoption_surface_test.exs"
      via: "readme_maintainer_command_refutes refute loop"
      pattern: "post_publish_smoke"
---

<objective>
Ship DOCS-HEX-01 executable drift guards: contract surface map, adopter/maintainer prose alignment, and test pins — with honest REQUIREMENTS ledger.

Purpose: Close the deferred pin gap from Phases 78–81 (D-114); adopter docs prove tarball consumer story without maintainer runbook leakage (D-90–D-93).
Output: Green combined drift-guard test run; DOCS-HEX-01 remains Partial in REQUIREMENTS until 82-02 ceremony.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/82-docs-truth-milestone-closeout/82-CONTEXT.md
@.planning/phases/82-docs-truth-milestone-closeout/82-RESEARCH.md
@.planning/phases/82-docs-truth-milestone-closeout/82-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@lib/scoria/hex_consumer_contract.ex
@lib/scoria/adopter_doc_contract.ex
@lib/scoria/support_journey.ex
@test/scoria/adoption_surface_test.exs
@test/scoria/ci_policy_contract_test.exs
@test/scoria/support_journey_source_test.exs
@README.md
@docs/adoption_lanes.md
@docs/operator_verification.md

Do not merge HexConsumerContract into AdopterDocContract (D-95). Do not mark DOCS-HEX-01 Complete in this plan (D-112). Do not pin full PR-vs-release table rows in adoption_surface_test (D-102). Do not widen VerificationLanes.closeout_order/0.
</context>

<tasks>

<task type="auto">
  <name>Task 82-01-01: Add adopter_doc_surfaces/0 and extend maintainer refutes</name>
  <requirement_ids>DOCS-HEX-01</requirement_ids>
  <files>lib/scoria/hex_consumer_contract.ex, lib/scoria/adopter_doc_contract.ex</files>
  <read_first>
    lib/scoria/hex_consumer_contract.ex
    lib/scoria/support_journey.ex
    lib/scoria/adopter_doc_contract.ex
    lib/scoria/verification_lanes.ex
    .planning/phases/82-docs-truth-milestone-closeout/82-CONTEXT.md
  </read_first>
  <action>
1. In `lib/scoria/hex_consumer_contract.ex`, add `@doc` and public function `adopter_doc_surfaces/0` mirroring `SupportJourney.adopter_doc_surfaces/0` pattern (D-96). Scope **adopter surfaces only** — README and `docs/adoption_lanes.md`; do **not** include `docs/operator_verification.md` (maintainer pins live in `ci_policy_contract_test` per D-98).

2. Derive fragments from contract helpers where possible:

```elixir
def adopter_doc_surfaces do
  adoption_cmd = Scoria.VerificationLanes.command(:adoption)

  %{
    "README.md" => [
      adoption_cmd,
      "mix hex.build --unpack",
      "{:scoria, path: unpack_root}",
      "Scoria.HexConsumerContract",
      "docs/operator_verification.md"
    ],
    "docs/adoption_lanes.md" => [
      adoption_cmd,
      "mix hex.build --unpack",
      "packaged tarball",
      "operator_verification.md"
    ]
  }
end
```

3. In `lib/scoria/adopter_doc_contract.ex`, append `"mix scoria.post_publish_smoke"` to `@readme_maintainer_command_refutes` list (D-100). Keep alphabetical-ish grouping with existing maintainer commands.

4. Do **not** merge modules or move workflow YAML strings into HexConsumerContract (D-95, D-98).
  </action>
  <acceptance_criteria>
    - `rg -n 'def adopter_doc_surfaces' lib/scoria/hex_consumer_contract.ex` returns match.
    - `rg -n 'VerificationLanes.command\\(:adoption\\)' lib/scoria/hex_consumer_contract.ex` returns match inside `adopter_doc_surfaces/0`.
    - `rg -n 'operator_verification.md' lib/scoria/hex_consumer_contract.ex` returns match in README surface list only — not as third map key.
    - `rg -n 'mix scoria.post_publish_smoke' lib/scoria/adopter_doc_contract.ex` returns match in refutes list.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <done>Contract modules export adopter surface map and post-publish smoke README refute.</done>
</task>

<task type="auto">
  <name>Task 82-01-02: Adopter and maintainer prose sweep</name>
  <requirement_ids>DOCS-HEX-01</requirement_ids>
  <files>README.md, docs/adoption_lanes.md, docs/operator_verification.md</files>
  <read_first>
    README.md
    docs/adoption_lanes.md
    docs/operator_verification.md
    lib/scoria/hex_consumer_contract.ex
    .planning/phases/82-docs-truth-milestone-closeout/82-CONTEXT.md
    .planning/ROADMAP.md
  </read_first>
  <action>
1. **README.md** (`## Verification`, ~L191–203) — tighten only if gaps vs D-91; preserve existing tarball sentence. Must contain: `mix test.adoption`, `mix hex.build --unpack`, `{:scoria, path: unpack_root}`, `Scoria.HexConsumerContract`, link to `docs/operator_verification.md`. Must **not** add: `mix scoria.post_publish_smoke`, `post-publish-smoke.yml`, PR-vs-release table, or workflow filenames.

2. **`docs/adoption_lanes.md`** — under `### 1. Default runtime lane` proof block (after the `mix test.adoption` code fence, ~L34–40), add one sentence (D-92):

> Adoption closeout exercises a packaged tarball from `mix hex.build --unpack` (not a monorepo root `path:` dep). Maintainer CI topology — PR tarball vs release registry attest — lives in [`docs/operator_verification.md`](operator_verification.md).

Do **not** add `SCORIA_HEX_UNPACK_ROOT`, `SCORIA_PRESERVE_HOST`, or other maintainer env vars.

3. **`docs/operator_verification.md`** — expand **PR vs release proof depth (v2.10 gate map)** subsection (~L319–328) without duplicating into README (D-93). Add upgrade-sequence clarity bullets **immediately after** the gate map table with these stable substrings (required for D-104 #5 pins):

   - Content-revision upgrade: `scoria-0.1.0-unpack` fixture → HEAD tarball (same semver content-revision path in PR CI)
   - Registry semver upgrade: `baseline exact previous → target just-published` when `published_version > 0.1.0`

Preserve existing version namespace bullet (~L328): planning `v2.x` vs Hex semver (D-94).
  </action>
  <acceptance_criteria>
    - `rg -n 'mix hex.build --unpack' README.md docs/adoption_lanes.md` returns match in both files.
    - `rg -n 'packaged tarball|mix hex.build' docs/adoption_lanes.md` returns match under Default runtime lane section.
    - `rg -n 'SCORIA_HEX_UNPACK_ROOT|SCORIA_PRESERVE_HOST' docs/adoption_lanes.md` returns no matches.
    - `rg -n 'scoria-0.1.0-unpack.*HEAD|HEAD tarball' docs/operator_verification.md` returns match in gate map area.
    - `rg -n 'baseline exact previous.*target just-published' docs/operator_verification.md` returns match.
    - `rg -n 'mix scoria.post_publish_smoke' README.md` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'packaged tarball|operator_verification' docs/adoption_lanes.md && rg -n 'scoria-0.1.0-unpack|baseline exact previous' docs/operator_verification.md</automated>
  </verify>
  <done>Adopter docs state tarball closeout; operator guide documents three-layer trust and upgrade sequence.</done>
</task>

<task type="auto">
  <name>Task 82-01-03: Wire drift guard tests for adopter and maintainer surfaces</name>
  <requirement_ids>DOCS-HEX-01</requirement_ids>
  <files>test/scoria/adoption_surface_test.exs, test/scoria/ci_policy_contract_test.exs</files>
  <read_first>
    test/scoria/adoption_surface_test.exs
    test/scoria/ci_policy_contract_test.exs
    test/scoria/support_journey_source_test.exs
    lib/scoria/hex_consumer_contract.ex
    lib/scoria/adopter_doc_contract.ex
    .planning/phases/82-docs-truth-milestone-closeout/82-CONTEXT.md
  </read_first>
  <action>
1. **`test/scoria/adoption_surface_test.exs`** (D-97, D-101):
   - Add `alias Scoria.HexConsumerContract`.
   - Add compile-time macro loop copied from `SupportJourneySourceTest` pattern:

```elixir
for {path, fragments} <- HexConsumerContract.adopter_doc_surfaces() do
  test "adopter doc #{path} stays aligned with HexConsumerContract surface SSOT" do
    content = File.read!(unquote(path))
    for fragment <- unquote(Macro.escape(fragments)) do
      assert content =~ fragment, ...
    end
  end
end
```

   - Do **not** duplicate README active dep byte-match — that stays in `hex_consumer_contract_test.exs` only (D-99, D-101 #1).
   - Existing `AdopterDocContract` refute loop in `"README shipped truth is capability-based"` automatically covers new `mix scoria.post_publish_smoke` refute once D-100 lands (D-101 #5).
   - Do **not** add positive asserts for `mix scoria.post_publish_smoke` or PR-vs-release table rows (D-102, D-103).

2. **`test/scoria/ci_policy_contract_test.exs`** (D-98, D-104, D-105) — add test `"operator gate map pins v2.10 PR vs release proof depth"`:

```elixir
operator_docs = File.read!("docs/operator_verification.md")
gate_map = section_after(operator_docs, "### CI gate map (maintainers)")
pr_release = section_after(gate_map, "**PR vs release proof depth (v2.10 gate map):**")
```

Assert within `pr_release` subsection:
   - PR row: `mix test.adoption`, `content-revision upgrade`, tarball/full overlay vocabulary (`tarball consumer full overlay` or `mix hex.build`)
   - Release row: `mix scoria.post_publish_smoke`, `post-publish-smoke.yml`, `{:scoria, "<version>", hex: :scoria}` or `exact-pinned`
   - Upgrade sequence: `scoria-0.1.0-unpack` and `HEAD tarball` (or equivalent prose from 82-01-02)
   - Upgrade sequence: `baseline exact previous` and `target just-published`
   - Version namespace: `v2.x` planning milestones vs Hex semver

Keep existing partial workflow pins in `"release-please.yml includes preflight and publish hardening"` — assert `post-publish-attest` and `post-publish-smoke.yml` (D-104 #1–#2). Optionally strengthen `post-publish-attest` needs `publish-hex` if not already asserted elsewhere.

Do **not** pin full overlay step lists or Runner internals (D-106).
  </action>
  <acceptance_criteria>
    - `rg -n 'HexConsumerContract.adopter_doc_surfaces' test/scoria/adoption_surface_test.exs` returns macro loop match.
    - `rg -n 'PR vs release proof depth' test/scoria/ci_policy_contract_test.exs` returns gate map test match.
    - `rg -n 'mix scoria.post_publish_smoke' test/scoria/adoption_surface_test.exs` returns no positive assert (refute-only via AdopterDocContract loop).
    - `MIX_ENV=test mix test --warnings-as-errors test/scoria/adoption_surface_test.exs test/scoria/ci_policy_contract_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/scoria/adoption_surface_test.exs test/scoria/ci_policy_contract_test.exs</automated>
  </verify>
  <done>Executable drift guards pin adopter surfaces and maintainer gate map topology.</done>
</task>

<task type="auto">
  <name>Task 82-01-04: Fix REQUIREMENTS honesty and run 82-01 gate</name>
  <requirement_ids>DOCS-HEX-01</requirement_ids>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/STATE.md
    .planning/phases/82-docs-truth-milestone-closeout/82-CONTEXT.md
    .planning/phases/82-docs-truth-milestone-closeout/82-VALIDATION.md
  </read_first>
  <action>
1. **`.planning/REQUIREMENTS.md`** — fix premature Complete (D-112):
   - Change DOCS-HEX-01 checkbox from `[x]` to `[ ]` in v2.10 Requirements section (~L17).
   - Update traceability row (~L47): `DOCS-HEX-01 | 78, 82 | Partial` — prose note: drift guards land in 82-01; milestone Complete deferred to 82-02 after verification + audit.
   - Do **not** mark Complete in this plan.

2. Run **82-01 combined gate** (must exit 0 before task done):

```bash
MIX_ENV=test mix test --warnings-as-errors \
  test/scoria/hex_consumer_contract_test.exs \
  test/scoria/adoption_surface_test.exs \
  test/scoria/ci_policy_contract_test.exs \
  test/scoria/support_journey_source_test.exs
```

3. Confirm `VerificationLanes.closeout_order/0` unchanged — no edits to `lib/scoria/verification_lanes.ex` in this plan.
  </action>
  <acceptance_criteria>
    - `rg -n '^- \\[ \\] \\*\\*DOCS-HEX-01' .planning/REQUIREMENTS.md` returns unchecked checkbox match.
    - `rg -n 'DOCS-HEX-01.*Partial' .planning/REQUIREMENTS.md` returns traceability match.
    - `rg -n 'DOCS-HEX-01.*Complete' .planning/REQUIREMENTS.md` traceability row does **not** read Complete-only without Partial note.
    - Combined gate command exits 0 with 0 failures.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/scoria/hex_consumer_contract_test.exs test/scoria/adoption_surface_test.exs test/scoria/ci_policy_contract_test.exs test/scoria/support_journey_source_test.exs</automated>
  </verify>
  <done>Drift guards green; REQUIREMENTS ledger honestly Partial until 82-02 ceremony.</done>
</task>

</tasks>

<threat_model>
security_enforcement: enabled

| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-82-01 | README becomes maintainer release runbook — steers adopters to wrong install path | High | D-91/D-100 refutes; no post_publish_smoke or workflow names in README; ci_policy owns maintainer topology |
| T-82-02 | Doc drift without executable pins — prose-only closeout | High | adopter_doc_surfaces/0 + macro tests + gate map section_after pins (D-114) |
| T-82-03 | Premature DOCS-HEX-01 Complete before pins green | High | Uncheck REQUIREMENTS in 82-01; Complete only in 82-02 (D-112) |
| T-82-04 | Module merge breaks 78 contract split | Medium | Reject HexConsumer → AdopterDoc merge (D-95); separate test homes |
| T-82-05 | Env vars leak into adopter lane guide | Medium | D-92 forbids SCORIA_* in adoption_lanes; grep acceptance |
| T-82-06 | closeout_order/0 widened with hex consumer lane | Low | No VerificationLanes edits; verification_lanes_test unchanged |

**Blockers:** T-82-01, T-82-02, T-82-03 must be mitigated before 82-01 complete.
</threat_model>

<verification>
82-01 gate (all exit 0):

```bash
MIX_ENV=test mix test --warnings-as-errors \
  test/scoria/hex_consumer_contract_test.exs \
  test/scoria/adoption_surface_test.exs \
  test/scoria/ci_policy_contract_test.exs \
  test/scoria/support_journey_source_test.exs
```

Ledger checks:

```bash
rg -n 'def adopter_doc_surfaces' lib/scoria/hex_consumer_contract.ex
rg -n 'mix scoria.post_publish_smoke' lib/scoria/adopter_doc_contract.ex
rg -n '^- \[ \] \*\*DOCS-HEX-01' .planning/REQUIREMENTS.md
rg -n 'SCORIA_HEX_UNPACK_ROOT' docs/adoption_lanes.md && exit 1 || true
```
</verification>

<success_criteria>
- `HexConsumerContract.adopter_doc_surfaces/0` ships with README + adoption_lanes fragment maps.
- Adopter prose aligned (D-91–D-93); operator gate map includes upgrade sequence substrings.
- `adoption_surface_test` and `ci_policy_contract_test` guard new anchors (D-101, D-104).
- Combined drift-guard test run green.
- DOCS-HEX-01 unchecked/Partial in REQUIREMENTS — **not** milestone Complete.
</success_criteria>
