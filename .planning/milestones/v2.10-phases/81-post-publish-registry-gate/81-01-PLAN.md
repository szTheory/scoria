---
phase: 81-post-publish-registry-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/hex_consumer_contract.ex
  - test/support/scoria/host_app_proof/generator.ex
  - test/scoria/hex_consumer_contract_test.exs
autonomous: true
requirements: [HEX-REGISTRY-01]
must_haves:
  truths:
    - "HexConsumerContract exposes exact-pinned registry dep tuple/snippet helpers separate from ~> 0.1 policy (D-73, D-86)."
    - "semver_upgrade_eligible?/1, registry_upgrade_pair/1, registry_upgrade_from_version/1 resolve conditional upgrade legs (D-83, D-86)."
    - "Generator accepts dep_mode :hex_registry with hex_version opt and patches mix.exs with exact pin (D-73)."
    - "overlay_from_dep!/1 copies route+runtime overlays from deps/scoria/priv/host_app_proof/overlay/test after deps.get (D-69, D-70)."
    - "bump_registry_dep!/2 rewrites {:scoria, _, hex: :scoria} line for semver upgrade baseline/target (D-85, D-86)."
  artifacts:
    - path: "lib/scoria/hex_consumer_contract.ex"
      provides: "registry_dep_tuple_pinned/1 and semver helpers"
      contains: "def registry_dep_tuple_pinned"
    - path: "test/support/scoria/host_app_proof/generator.ex"
      provides: ":hex_registry dep mode"
      contains: ":hex_registry"
---

<objective>
Extend HexConsumerContract and HostAppProof.Generator for live Hex registry consumer proof — exact version pins, overlay-from-deps, and registry dep bump helpers.

Purpose: Foundation for registry proof without checkout overlay footgun (D-70–D-74).
Output: Compiling contract + generator APIs with unit tests green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/81-post-publish-registry-gate/81-CONTEXT.md
@.planning/phases/81-post-publish-registry-gate/81-RESEARCH.md
@lib/scoria/hex_consumer_contract.ex
@test/support/scoria/host_app_proof/generator.ex
@test/scoria/hex_consumer_contract_test.exs
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-81-01 | Loose `~> 0.1` resolves wrong artifact during index lag | high | `registry_dep_tuple_pinned/1` for attest paths |
| T-81-02 | Overlays copied from git HEAD not Hex package | high | `overlay_from_dep!/1` reads `deps/scoria/priv/...` |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 81-01-01: Add HexConsumerContract registry and semver APIs</name>
  <requirement_ids>HEX-REGISTRY-01</requirement_ids>
  <files>lib/scoria/hex_consumer_contract.ex, test/scoria/hex_consumer_contract_test.exs</files>
  <read_first>
    lib/scoria/hex_consumer_contract.ex
    test/scoria/hex_consumer_contract_test.exs
    .planning/phases/81-post-publish-registry-gate/81-CONTEXT.md
  </read_first>
  <action>
In `Scoria.HexConsumerContract` add:

1. `registry_dep_tuple_pinned(version)` → `{:scoria, version, hex: :scoria}` when version is binary semver string.

2. `registry_dep_snippet_pinned(version)` → string `{:scoria, "#{version}", hex: :scoria}` for mix.exs patching.

3. `semver_upgrade_eligible?(version)` → `Version.compare(version, "0.1.0") == :gt`.

4. `registry_upgrade_from_version(version)` → previous patch string; floor at `"0.1.0"` when bumping from `"0.1.1"` (first patch increment returns `"0.1.0"`). Implementation may use simple patch decrement on third segment with floor — document in `@doc`.

5. `registry_upgrade_pair(current_version)` → `%{from: registry_upgrade_from_version(current_version), to: current_version}`.

Keep existing `hex_dep_tuple/0` and `hex_dep_snippet/0` (`~> 0.1`) unchanged for adopter docs — pinned helpers are attest-only.

Add contract tests asserting pinned tuple shape, `semver_upgrade_eligible?("0.1.0") == false`, `semver_upgrade_eligible?("0.1.1") == true`, and pair `%{from: "0.1.0", to: "0.1.1"}`.
  </action>
  <acceptance_criteria>
    - `rg -n 'def registry_dep_tuple_pinned' lib/scoria/hex_consumer_contract.ex` returns match.
    - `rg -n 'def semver_upgrade_eligible' lib/scoria/hex_consumer_contract.ex` returns match.
    - `MIX_ENV=test mix test test/scoria/hex_consumer_contract_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/hex_consumer_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Registry semver SSOT helpers exist and unit-tested.</done>
</task>

<task type="auto">
  <name>Task 81-01-02: Extend Generator for :hex_registry dep mode and overlay_from_dep!</name>
  <requirement_ids>HEX-REGISTRY-01</requirement_ids>
  <files>test/support/scoria/host_app_proof/generator.ex</files>
  <read_first>
    test/support/scoria/host_app_proof/generator.ex
    lib/scoria/hex_consumer_contract.ex
    priv/host_app_proof/overlay/test/host_route_smoke_test.exs
    .planning/phases/81-post-publish-registry-gate/81-CONTEXT.md
  </read_first>
  <action>
1. Allow `dep_mode` in `[:hex_tarball, :path, :hex_registry]` in `create_host!/1`.

2. For `:hex_registry`, require `hex_version:` opt in `create_host!/1`. In `patch_mix_exs!/1`, insert `HexConsumerContract.registry_dep_snippet_pinned(hex_version)` — not `hex_dep_snippet/0`.

3. Do NOT copy overlays from repo root in `:hex_registry` create path initially — set `overlay_tests: []` until `overlay_from_dep!/1` runs.

4. Add `overlay_from_dep!(host)`:
   - Resolve `deps_root = Path.join(host.root, "deps/scoria")`.
   - Validate directory exists (raise clear error if deps.get not run).
   - Copy only `host_route_smoke_test.exs` and `host_runtime_smoke_test.exs` from `Path.join(deps_root, "priv/host_app_proof/overlay/test")` into host `test/`.
   - Return host with `overlay_tests` set to those two basenames sorted.

5. Add `bump_registry_dep!(host, to_version)` — regex-replace `{:scoria, "...", hex: :scoria}` with pinned snippet for `to_version`; raise if no match.

6. Store `hex_version` and `dep_mode: :hex_registry` on host map for Runner MANIFEST/triage.
  </action>
  <acceptance_criteria>
    - `rg -n ':hex_registry' test/support/scoria/host_app_proof/generator.ex` returns matches.
    - `rg -n 'def overlay_from_dep!' test/support/scoria/host_app_proof/generator.ex` returns match.
    - `rg -n 'def bump_registry_dep!' test/support/scoria/host_app_proof/generator.ex` returns match.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <done>Generator supports registry hosts with dep-tree overlays and registry bump.</done>
</task>

</tasks>

<verification>
- Compile clean with new dep_mode branch.
- Contract tests cover pinned registry helpers and semver eligibility.
</verification>
