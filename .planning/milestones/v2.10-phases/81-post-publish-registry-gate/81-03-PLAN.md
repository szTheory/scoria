---
phase: 81-post-publish-registry-gate
plan: 03
type: execute
wave: 3
depends_on: [81-02]
files_modified:
  - .github/workflows/post-publish-smoke.yml
  - .github/workflows/release-please.yml
  - .github/workflows/hex-publish.yml
  - docs/operator_verification.md
  - .planning/phases/81-post-publish-registry-gate/81-VERIFICATION.md
autonomous: true
requirements: [HEX-REGISTRY-01]
must_haves:
  truths:
    - "post-publish-smoke.yml is workflow_call SSOT with version and skip_index_wait inputs (D-77, D-80)."
    - "release-please.yml post-publish-attest needs publish-hex and calls reusable workflow with skip_index_wait true (D-78, D-82)."
    - "hex-publish.yml recovery path includes same attest job (D-79)."
    - "Post-publish job uses Postgres pgvector on 55432 like ci-verify test job (D-81)."
    - "Operator gate map row states PR tarball vs release registry responsibilities (D-89)."
    - "81-VERIFICATION.md documents latent semver upgrade at 0.1.0-only releases (D-83)."
  artifacts:
    - path: ".github/workflows/post-publish-smoke.yml"
      provides: "reusable registry attest workflow"
      contains: "workflow_call"
    - path: ".github/workflows/release-please.yml"
      provides: "blocking post-publish-attest job"
      contains: "post-publish-attest"
    - path: ".planning/phases/81-post-publish-registry-gate/81-VERIFICATION.md"
      provides: "phase verification ledger"
      contains: "HEX-REGISTRY-01"
---

<objective>
Wire post-publish registry proof into release workflows as blocking attestation and document operator topology.

Purpose: HEX-REGISTRY-01 release-blocking criterion and gate map stub (D-77–D-89).
Output: Green release-please implies registry consumer proof passed; VERIFICATION ledger ready.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/81-post-publish-registry-gate/81-CONTEXT.md
@.planning/phases/81-post-publish-registry-gate/81-RESEARCH.md
@.github/workflows/post-publish-smoke.yml
@.github/workflows/release-please.yml
@.github/workflows/hex-publish.yml
@.github/workflows/ci-verify.yml
@docs/operator_verification.md
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-81-05 | release:published races publish-hex | high | attest job needs publish-hex; remove primary blocking on release event |
| T-81-06 | Attest without Postgres | high | services.postgres + SCORIA_DB_PORT=55432 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 81-03-01: Refactor post-publish-smoke.yml to workflow_call SSOT</name>
  <requirement_ids>HEX-REGISTRY-01</requirement_ids>
  <files>.github/workflows/post-publish-smoke.yml</files>
  <read_first>
    .github/workflows/post-publish-smoke.yml
    .github/workflows/ci-verify.yml
    .github/workflows/release-please.yml
    lib/mix/tasks/scoria.post_publish_smoke.ex
    .planning/phases/81-post-publish-registry-gate/81-CONTEXT.md
  </read_first>
  <action>
Refactor `.github/workflows/post-publish-smoke.yml`:

1. Add `on.workflow_call` inputs:
   - `version` (required string) — semver without v prefix
   - `skip_index_wait` (boolean, default false)

2. Keep `workflow_dispatch` with `tag`/`version` inputs for maintainer debug (D-80).

3. Remove or demote `release: published` trigger — add comment that blocking attest is via release-please `workflow_call` (D-77). Optional: keep `release: published` as non-blocking informational only OR remove entirely per CONTEXT.

4. Replace inline `mix new` consumer smoke with:
   - checkout (for Mix task + test support from repo)
   - setup-beam from `.tool-versions`
   - Postgres service matching ci-verify (pgvector:pg16, port 55432, healthcheck)
   - env: `SCORIA_REGISTRY_VERSION: ${{ inputs.version }}`, `SCORIA_DB_PORT: 55432`, username/password postgres
   - Conditional index wait (36×10s) only when `skip_index_wait != true`
   - Run `mix deps.get`, `mix compile --warnings-as-errors` in repo root
   - Run `mix scoria.post_publish_smoke`

5. Update job summary to list install/migrate/overlay subset steps (not deps.get-only).

6. Set job timeout-minutes to 20.
  </action>
  <acceptance_criteria>
    - `rg -n 'workflow_call' .github/workflows/post-publish-smoke.yml` returns match.
    - `rg -n 'scoria.post_publish_smoke' .github/workflows/post-publish-smoke.yml` returns match.
    - `rg -n '55432' .github/workflows/post-publish-smoke.yml` returns match.
    - `rg -n 'skip_index_wait' .github/workflows/post-publish-smoke.yml` returns match.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'workflow_call|scoria.post_publish_smoke|55432' .github/workflows/post-publish-smoke.yml</automated>
  </verify>
  <done>Reusable post-publish workflow runs full registry attest with Postgres.</done>
</task>

<task type="auto">
  <name>Task 81-03-02: Wire blocking attest jobs in release-please and hex-publish</name>
  <requirement_ids>HEX-REGISTRY-01</requirement_ids>
  <files>.github/workflows/release-please.yml, .github/workflows/hex-publish.yml</files>
  <read_first>
    .github/workflows/release-please.yml
    .github/workflows/hex-publish.yml
    .github/workflows/post-publish-smoke.yml
    .planning/phases/81-post-publish-registry-gate/81-CONTEXT.md
  </read_first>
  <action>
1. In `release-please.yml` add job `post-publish-attest`:
   - `needs: [release-please, publish-hex]` (include verify/gate-ci-green only if publish-hex already depends on them — match existing graph)
   - `if: needs.release-please.outputs.release_created == 'true'`
   - `uses: ./.github/workflows/post-publish-smoke.yml`
   - `with: version: ${{ needs.release-please.outputs.version }}, skip_index_wait: true`

2. In `hex-publish.yml` add parallel attest job after successful publish step:
   - `needs` publish job
   - Same `workflow_call` with `version: ${{ github.event.inputs.release_version }}`, `skip_index_wait: true` when publish just ran index wait, else false for dry-run-only paths

3. Ensure attest failure fails the parent workflow (default `workflow_call` behavior).
  </action>
  <acceptance_criteria>
    - `rg -n 'post-publish-attest' .github/workflows/release-please.yml` returns match.
    - `rg -n 'post-publish-smoke.yml' .github/workflows/hex-publish.yml` returns match.
    - `rg -n 'skip_index_wait: true' .github/workflows/release-please.yml` returns match.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'post-publish-attest|post-publish-smoke' .github/workflows/release-please.yml .github/workflows/hex-publish.yml</automated>
  </verify>
  <done>Release and recovery publish paths block on registry attest.</done>
</task>

<task type="auto">
  <name>Task 81-03-03: Operator gate map stub and 81-VERIFICATION.md</name>
  <requirement_ids>HEX-REGISTRY-01</requirement_ids>
  <files>docs/operator_verification.md, .planning/phases/81-post-publish-registry-gate/81-VERIFICATION.md</files>
  <read_first>
    docs/operator_verification.md
    .planning/phases/80-upgrade-smoke-in-adoption-lane/80-VERIFICATION.md
    .planning/phases/81-post-publish-registry-gate/81-CONTEXT.md
    .planning/ROADMAP.md
  </read_first>
  <action>
1. In `docs/operator_verification.md` CI gate map section, add table row or bullet:
   - **PR CI:** tarball consumer full overlay + content-revision upgrade (`mix test.adoption`)
   - **Release:** post-publish registry subset + conditional semver upgrade (`mix scoria.post_publish_smoke` via `post-publish-smoke.yml` after `publish-hex`)
   - Reference `.github/workflows/post-publish-smoke.yml` and `mix scoria.post_publish_smoke`

2. Create `81-VERIFICATION.md` with:
   - status: pending (updated to passed at execute closeout)
   - Criteria mapped to ROADMAP success criteria 1–4
   - Criterion 3 latent note: at `0.1.0`-only releases, semver upgrade leg skipped; documents activation when `0.1.1+` publishes
   - Criterion 4 migration delta latent (D-87 continuity)
   - Commands for manual verification

3. Optionally extend `test/scoria/ci_policy_contract_test.exs` with minimal assertion that `release-please.yml` contains `post-publish-attest` — only if pattern exists in file; else skip per D-89 discretion.
  </action>
  <acceptance_criteria>
    - `rg -n 'post-publish|registry' docs/operator_verification.md` returns matches in gate map area.
    - `test -f .planning/phases/81-post-publish-registry-gate/81-VERIFICATION.md` exits 0.
    - `rg -n 'HEX-REGISTRY-01' .planning/phases/81-post-publish-registry-gate/81-VERIFICATION.md` returns match.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/81-post-publish-registry-gate/81-VERIFICATION.md && rg -n 'post-publish' docs/operator_verification.md</automated>
  </verify>
  <done>Operator docs and verification ledger reflect v2.10 release gate topology.</done>
</task>

</tasks>

<verification>
- Workflow graph shows attest after publish-hex.
- Gate map distinguishes PR vs release proof depth.
</verification>
