---
phase: 81-post-publish-registry-gate
plan: 02
type: execute
wave: 2
depends_on: [81-01]
files_modified:
  - test/support/scoria/host_app_proof/runner.ex
  - test/scoria/host_app_registry_proof_test.exs
  - test/scoria/host_app_registry_upgrade_proof_test.exs
  - lib/mix/tasks/scoria.post_publish_smoke.ex
autonomous: true
requirements: [HEX-REGISTRY-01]
must_haves:
  truths:
    - "run_registry_proof!/1 executes 6 steps: deps_get, scoria_install, ecto_create, ecto_migrate, host_route_smoke_test, host_runtime_smoke_test (D-69, D-71)."
    - "expected_registry_steps/1 returns exact atom list for assertions (D-71)."
    - "run_upgrade_proof!/2 accepts bump {:registry, from: prev, to: current} reusing Phase 80 orchestration (D-84)."
    - "Failure MANIFEST includes dep_mode registry and registry_version (D-76)."
    - "mix scoria.post_publish_smoke runs registry proof; not registered in mix test.adoption (D-75)."
  artifacts:
    - path: "test/support/scoria/host_app_proof/runner.ex"
      provides: "run_registry_proof!/1"
      contains: "def run_registry_proof!"
    - path: "lib/mix/tasks/scoria.post_publish_smoke.ex"
      provides: "release attest Mix entry"
      contains: "Scoria.PostPublishSmoke"
    - path: "test/scoria/host_app_registry_proof_test.exs"
      provides: "fresh-install registry ExUnit proof"
      contains: "HostAppRegistryProofTest"
---

<objective>
Implement Runner registry proof paths, ExUnit proof modules, and the maintainer Mix task entrypoint.

Purpose: Executable proof layer consumed by CI workflow in 81-03 (D-72, D-75–D-76).
Output: Local `SCORIA_REGISTRY_VERSION=X mix scoria.post_publish_smoke` runs fresh-install proof; upgrade module env-gated.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/81-post-publish-registry-gate/81-CONTEXT.md
@.planning/phases/81-post-publish-registry-gate/81-RESEARCH.md
@test/support/scoria/host_app_proof/runner.ex
@test/scoria/host_app_upgrade_proof_test.exs
@lib/mix/tasks/test.adoption.ex
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-81-03 | Wrong step order or subset drift | medium | `expected_registry_steps/1` exact `==` assertion |
| T-81-04 | Registry upgrade uses path bump | high | `bump_dep!` dispatches on `{:registry, _, _}` |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 81-02-01: Add Runner registry proof and upgrade bump dispatch</name>
  <requirement_ids>HEX-REGISTRY-01</requirement_ids>
  <files>test/support/scoria/host_app_proof/runner.ex</files>
  <read_first>
    test/support/scoria/host_app_proof/runner.ex
    test/support/scoria/host_app_proof/generator.ex
    .planning/phases/81-post-publish-registry-gate/81-CONTEXT.md
  </read_first>
  <action>
1. Add `expected_registry_steps/1` returning `[:deps_get, :scoria_install, :ecto_create, :ecto_migrate, :host_route_smoke_test, :host_runtime_smoke_test]` (ignore host overlay list — fixed subset per D-69).

2. Add `run_registry_proof!/1`:
   - Assert host has `dep_mode: :hex_registry`.
   - `deps_get!(host)` then `Generator.overlay_from_dep!(host)`.
   - Run steps: deps_get (already done — skip duplicate OR structure as: deps_get once, overlay_from_dep, then install chain): final order must match expected_registry_steps — implement as single deps_get at start, overlay refresh, then scoria_install, ecto_create, ecto_migrate, smoke_pair with route+runtime only.

3. Extend `run_upgrade_proof!/2` opts: accept `bump: {:registry, from: from_ver, to: to_ver}`.
   - In `bump_dep!/2`, when bump is registry tuple, call `Generator.bump_registry_dep!(host, to_ver)` and `deps.clean scoria` (mirror tarball bump).
   - Baseline phase uses host created with `hex_version: from_ver` and registry overlays from deps.

4. Extend `triage_message/5` and failure MANIFEST writer to include `registry_version: host.hex_version` and `dep_mode: :hex_registry` when applicable.

5. Extend `expected_upgrade_steps/1` for registry hosts: baseline uses registry overlay atoms; upgrade phase unchanged installer chain from Phase 80.
  </action>
  <acceptance_criteria>
    - `rg -n 'def run_registry_proof!' test/support/scoria/host_app_proof/runner.ex` returns match.
    - `rg -n 'def expected_registry_steps' test/support/scoria/host_app_proof/runner.ex` returns match.
    - `rg -n 'registry' test/support/scoria/host_app_proof/runner.ex` shows bump dispatch branch.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <done>Runner can execute registry fresh-install and registry semver upgrade paths.</done>
</task>

<task type="auto">
  <name>Task 81-02-02: Add registry ExUnit modules and post_publish_smoke Mix task</name>
  <requirement_ids>HEX-REGISTRY-01</requirement_ids>
  <files>test/scoria/host_app_registry_proof_test.exs, test/scoria/host_app_registry_upgrade_proof_test.exs, lib/mix/tasks/scoria.post_publish_smoke.ex</files>
  <read_first>
    test/scoria/host_app_consumer_proof_test.exs
    test/scoria/host_app_upgrade_proof_test.exs
    lib/mix/tasks/test.adoption.ex
    .planning/phases/81-post-publish-registry-gate/81-CONTEXT.md
  </read_first>
  <action>
1. Create `test/scoria/host_app_registry_proof_test.exs`:
   - `@moduletag timeout: 180_000`, `@moduletag :registry_proof`
   - Read version from `System.get_env("SCORIA_REGISTRY_VERSION")` — raise with helpful message if unset.
   - `Generator.create_host!(dep_mode: :hex_registry, hex_version: version, cleanup: &on_exit/1)`
   - `Runner.run_registry_proof!(host)`; assert `proof.steps == Runner.expected_registry_steps(host)`.

2. Create `test/scoria/host_app_registry_upgrade_proof_test.exs`:
   - `@moduletag :registry_upgrade`
   - Skip unless `HexConsumerContract.semver_upgrade_eligible?(version)` for `SCORIA_REGISTRY_VERSION`.
   - Baseline host with `from` version; `run_upgrade_proof!(host, bump: {:registry, from: from, to: to}, ...)` per Phase 80 pattern.
   - Assert `proof.steps == Runner.expected_upgrade_steps(host)`.

3. Create `lib/mix/tasks/scoria.post_publish_smoke.ex`:
   - Requires `SCORIA_REGISTRY_VERSION`.
   - Runs `mix test test/scoria/host_app_registry_proof_test.exs --warnings-as-errors`.
   - When `semver_upgrade_eligible?`, also run registry upgrade test file.
   - Exit non-zero on test failure.
   - Document env vars in moduledoc: `SCORIA_REGISTRY_VERSION`, `SCORIA_DB_*`, `SCORIA_PRESERVE_HOST`.

4. Confirm `lib/mix/tasks/test.adoption.ex` does NOT list registry proof files.
  </action>
  <acceptance_criteria>
    - `test -f test/scoria/host_app_registry_proof_test.exs` exits 0.
    - `test -f lib/mix/tasks/scoria.post_publish_smoke.ex` exits 0.
    - `mix help scoria.post_publish_smoke` exits 0.
    - `rg -n 'host_app_registry_proof' lib/mix/tasks/test.adoption.ex` returns no match.
  </acceptance_criteria>
  <verify>
    <automated>mix help scoria.post_publish_smoke && MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <done>Registry proof runnable via Mix task; isolated from adoption lane.</done>
</task>

</tasks>

<verification>
- `mix help scoria.post_publish_smoke` documents task.
- Full smoke requires live Hex + Postgres (manual/CI in 81-03).
</verification>
