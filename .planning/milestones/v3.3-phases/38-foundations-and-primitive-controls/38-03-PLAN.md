---
phase: 38-foundations-and-primitive-controls
plan: 03
type: execute
wave: 3
depends_on: ["38-02"]
files_modified:
  - test/scoria_web/ui_component_test.exs
  - lib/scoria_web/ui.ex
autonomous: true
requirements: [DS-02, DS-03]
nyquist_compliant: true

must_haves:
  truths:
    - "Interactive/labelable primitives expose accessible names: modal + drawer render role=dialog with aria-labelledby and an aria-labelled close button; field renders a <label for>; table exposes <th> column headers and an aria-labelled pagination nav; badge always renders a text label (never color-alone) (DS-02, D-12, Criterion 2)."
    - "Timestamps (time/1) render a machine-readable <time datetime> with an exact-time title; metadata rows (evidence_rows/1) render a <dl> dt/dd structure — both coherent with the design-system language (DS-03, Criterion 2)."
    - "Links resolve through the semantic --scoria-link / --scoria-link-hover tokens in both themes via .scoria-link (no per-component link-color override) (DS-01/DS-03)."
    - "The full Criterion-2 primitive set (links, badges, timestamps, metadata rows, panels, drawers, modals, forms, tables, lists) carries no ad-hoc pixel inline style in lib/scoria_web/ui.ex and introduces no variant/tone atom beyond the locked Phase 36/37 vocabulary (DS-02, D-13, D-14)."
  artifacts:
    - test/scoria_web/ui_component_test.exs
    - lib/scoria_web/ui.ex
  key_links:
    - ".scoria-link { color } -> var(--scoria-link) -> ui_component_test link-token guard asserts token presence + consumption"
    - "modal/drawer role=dialog + aria-labelledby + close aria-label -> ui_component_test accessible-name coverage guard"
---

<objective>
Extend primitive coherence coverage to the rest of ROADMAP Criterion 2's enumerated primitive list —
links, badges, timestamps, metadata rows, panels, drawers, modals, forms, tables, lists — which the
earlier plans did not touch. This is an AUDIT-AND-LOCK plan: verify each primitive is already coherent
in code (RESEARCH confirmed the codebase is mostly already-correct), then lock that coherence with
regression guards so Phase 39+ cannot silently drift it.

Purpose: 38-01 and 38-02 covered toasts, stats, copy controls, and the button size/focus scale.
Criterion 2 also names links/badges/timestamps/metadata-rows/panels/drawers/modals/forms/tables/lists.
Grounding reads show these are already coherent (modal/drawer expose `role=dialog` + `aria-labelledby`
+ aria-labelled close buttons; field renders `<label for>`; links use `--scoria-link` tokens; there
are zero ad-hoc pixel inline styles in `ui.ex`). The gap is the absence of a REGRESSION GUARD, not a
missing fix — mirroring the prospective-guard methodology already used for the copy-icon ceiling.

Output: two new describe blocks in `ui_component_test.exs` (accessible-name coverage; spacing/variant/
link-token coherence). No component redesign (Phase 39 owns page flows; Phase 40 owns focus-order /
WCAG sweep / motion / responsive). Any minimal accessible-name fix that surfaces during the audit is
applied in `lib/scoria_web/ui.ex`; if the audit finds everything already coherent (expected), `ui.ex`
is unchanged and this is a guards-only plan.

Depends on 38-02 because both add describe blocks to `test/scoria_web/ui_component_test.exs` (and may
both edit `lib/scoria_web/ui.ex`) — they must not share a wave.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-foundations-and-primitive-controls/38-CONTEXT.md
@.planning/phases/38-foundations-and-primitive-controls/38-RESEARCH.md
@.planning/phases/38-foundations-and-primitive-controls/38-VALIDATION.md

# Files this plan reads/guards:
@lib/scoria_web/ui.ex
@assets/css/02-tokens.css
@assets/css/04-components.css
@test/scoria_web/ui_component_test.exs
@test/scoria_web/ds06_drift_guard_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Accessible-name + semantic-structure coverage guards for the untouched primitives</name>
  <read_first>
    - lib/scoria_web/ui.ex `modal/1` (681-733): renders `role="dialog"`, `aria-labelledby={"#{@id}-title"}` when titled, and a close `icon_button` with `aria-label="Close dialog"`.
    - lib/scoria_web/ui.ex `drawer/1` (734-794): renders `role="dialog"`, `aria-labelledby`, and a close control with `aria-label="Close drawer"`.
    - lib/scoria_web/ui.ex `field/1` (795-820): renders `<label for={@id}>` + `sr-only` "(required)"; `form_section/1` (822-841) renders an `<h3>` title.
    - lib/scoria_web/ui.ex `table/1` (1229-1330): `<table class="scoria-table" id=>` with `<th class="scoria-table__th">` column headers, `aria-sort` on sortable headers, and a pagination `<nav aria-label="Pagination">` when `total_pages > 1` (which requires `on_page_change`).
    - lib/scoria_web/ui.ex `badge/1` (68-84): always renders a text `@label`/slot alongside tone (never color-alone); `time/1` (334-353): `<time datetime title>`; `evidence_rows/1` (1145-1157): `<dl class="scoria-evidence-rows">` dt/dd; `panel/1` (155-192).
    - test/scoria_web/ui_component_test.exs — existing `render_component/2` idiom + the existing `modal/1` and `drawer/1` describe blocks (extend coverage, don't duplicate their exact assertions).
    - SCOPE FENCE: assert accessible NAMES and semantic structure only. Do NOT add/assert focus-order, focus-trap, keyboard operability, or a full WCAG sweep — those are Phase 40 (A11Y-01/A11Y-02).
  </read_first>
  <files>test/scoria_web/ui_component_test.exs, lib/scoria_web/ui.ex</files>
  <action>
    Add a `describe "primitive accessible-name + structure coverage (DS-02/DS-03/Criterion 2)"` block that render-asserts, for the untouched primitives: (a) modal — `render_component(&ScoriaWeb.UI.modal/1, id: "m", title: "Confirm", show: true, inner_block: slot_block("body"))` contains `role="dialog"`, `aria-labelledby="m-title"`, and a close control carrying `aria-label="Close dialog"`; (b) drawer — same shape asserting `role="dialog"`, `aria-labelledby`, and `aria-label="Close drawer"`; (c) field — `render_component(&ScoriaWeb.UI.field/1, id: "f", label: "Name", inner_block: slot_block("<input/>"))` contains `<label for="f"`; (d) table — a `render_component(&ScoriaWeb.UI.table/1, ...)` with a keyed `:col` slot and rows asserts a `scoria-table__th` header renders (structural column names) and, with `total_pages: 2, page: 1, on_page_change: "page"`, asserts the `<nav aria-label="Pagination">` renders; (e) badge — `render_component(&ScoriaWeb.UI.badge/1, tone: :warn, label: "Blocked")` contains the visible text "Blocked" (never color-alone); (f) time — `render_component(&ScoriaWeb.UI.time/1, at: ~U[2026-01-01 00:00:00Z])` contains `<time` with a `datetime=` attribute and a `title=`; (g) evidence_rows (metadata rows) — `render_component(&ScoriaWeb.UI.evidence_rows/1, rows: [%{label: "Actor", value: "svc-1"}])` contains a `<dl class="scoria-evidence-rows"` with matching `<dt>`/`<dd>`. Where the audit surfaces a genuinely missing accessible name on an interactive/labelable primitive, apply the minimal fix in lib/scoria_web/ui.ex (matching the existing `aria-label`/`<label for>` pattern) and let the guard assert it; if everything is already coherent (expected per grounding), make no ui.ex change and keep this a guards-only task.
  </action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 mix test test/scoria_web/ui_component_test.exs</automated>
  </verify>
  <done>Coverage describe block asserts accessible names/semantic structure for modal, drawer, field, table (headers + pagination nav), badge, time, and evidence_rows; all green. Any ui.ex fix (if needed) is minimal and pattern-consistent.</done>
</task>

<task type="auto">
  <name>Task 2: Spacing / variant-vocabulary / link-token coherence lock (source-scan guards)</name>
  <read_first>
    - lib/scoria_web/ui.ex — a repo grep confirms exactly one inline `style=` (the required-asterisk color on `field/1`, which uses a token `var(--scoria-danger-action)`, NOT a pixel value) and ZERO pixel-valued inline styles. The guard locks this clean state (D-14: no page-local pixel values).
    - lib/scoria_web/ui.ex `button/1` `attr(:variant, ...)` is `values: [:primary, :ghost, :danger]`; `badge/1` tone draws from the locked `[:neutral, :pass, :info, :warn, :fail, :trace, :brand]` vocabulary. These are the Phase 36/37-locked sets — the guard forbids introducing a NEW variant/tone atom (no scope creep).
    - assets/css/02-tokens.css lines 112-113 (dark) + 182-183 (light): `--scoria-link` / `--scoria-link-hover`. assets/css/04-components.css `.scoria-link` (792-797) consumes them.
    - test/scoria_web/ds06_drift_guard_test.exs — mirror its `File.read!` + `Regex` source-scan style; this task adds NO production code (guards only).
    - COMMENT-TEXT NOTE: this guard reads lib/scoria_web/ui.ex and the CSS files; its own regex strings live only in the test file, which no other guard scans — no self-invalidation risk.
  </read_first>
  <files>test/scoria_web/ui_component_test.exs</files>
  <action>
    Add a `describe "primitive spacing / variant / link-token coherence (DS-02/DS-03/D-13/D-14)"` block with source-scan assertions (guards only — no production change): (a) no-ad-hoc-pixel guard — assert a regex for a pixel-valued inline style attribute (an inline `style` attribute whose value contains digits immediately followed by the `px` unit) finds NO match in `File.read!("lib/scoria_web/ui.ex")`, locking D-14's "no page-local pixel values" across all primitives' markup; (b) variant-vocabulary lock — assert via regex over `File.read!("lib/scoria_web/ui.ex")` that `button/1`'s `attr(:variant, ...)` `values:` list is exactly `[:primary, :ghost, :danger]` and that no tone/variant atom outside the locked Phase 36/37 vocabulary set (`:neutral, :pass, :info, :warn, :fail, :trace, :brand` for tones; `:md, :sm` for sizes) has been added (assert the size scale stays `[:md, :sm]` — reinforcing 38-02's guard is acceptable, or reference it and focus this assertion on the tone/variant sets); (c) link-token guard — assert `--scoria-link` and `--scoria-link-hover` are declared in BOTH theme blocks of `File.read!("assets/css/02-tokens.css")` and that `.scoria-link` in `File.read!("assets/css/04-components.css")` consumes `var(--scoria-link)` (links resolve through semantic tokens, DS-01/DS-03); (d) spacing-tokenization spot-check — assert that the panel, drawer, modal, form-section, table, evidence-rows/evidence-row, and list (`.scoria-selectable-list`, `.scoria-command__list`) component rules in `File.read!("assets/css/04-components.css")` each reference a `var(--scoria-space` token (spacing comes from tokens, not literals — D-14). Including the list selectors here closes "lists" from Criterion 2's primitive enumeration explicitly (the only Criterion-2 primitive without a standalone component — list markup lives inside `command_palette/1` and `selectable_card/1`). Keep every assertion boring and source-scan style; do not build a lint framework.
  </action>
  <verify>
    <automated>SCORIA_DB_PORT=55432 mix test test/scoria_web/ui_component_test.exs test/scoria_web/ds06_drift_guard_test.exs</automated>
  </verify>
  <done>Spacing/variant/link-token guards are green: no pixel-valued inline style in ui.ex, variant/tone/size vocabulary unchanged from the locked sets, links resolve through `--scoria-link*` tokens, and the named component rules (including list selectors) use `--scoria-space-*` spacing; ds06 stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/AT user → dashboard markup | Coverage guards + (at most) minimal accessible-name additions; no data/input path changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-05 | Tampering | primitive coherence drift | low | mitigate | Source-scan/render guards lock the already-coherent primitive set against future variant/spacing/accessible-name drift; ds06 stays green. No package install → no supply-chain threat this phase. |
| T-38-06 | Information Disclosure | accessible-name additions | low | accept | Any added `aria-label` exposes only already-visible operator labels to AT; no new data surface. |
</threat_model>

<verification>
- `SCORIA_DB_PORT=55432 mix test test/scoria_web/ui_component_test.exs test/scoria_web/ds06_drift_guard_test.exs` all green.
- `SCORIA_DB_PORT=55432 MIX_ENV=test mix test --warnings-as-errors` full suite green before `/gsd-verify-work`.
- `mix scoria.ui.e2e --base-url http://localhost:4799/scoria` green (unaffected; no markup redesign).
</verification>

<success_criteria>
- Links, badges, timestamps, metadata rows, panels, drawers, modals, forms, tables, and lists all carry a coherence guard (accessible name / structure / spacing / variant vocabulary) locking Criterion 2 coverage (DS-02, DS-03).
- No component redesign, no new variant/size tier, no focus-order/WCAG/motion/responsive work (fences held for Phase 39/40).
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces
- NEW describe blocks in `test/scoria_web/ui_component_test.exs`: "primitive accessible-name + structure coverage" (modal, drawer, field, table, badge, time, evidence_rows) and "primitive spacing / variant / link-token coherence".
- (Conditional) minimal accessible-name additions in `lib/scoria_web/ui.ex` only if the audit surfaces a genuine gap (none expected per grounding).
</artifacts_produced>

<output>
Create `.planning/phases/38-foundations-and-primitive-controls/38-03-SUMMARY.md` when done.
</output>
