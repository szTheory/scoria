---
phase: 39-component-groups-and-operator-flows
plan: 06
type: execute
wave: 2
depends_on: [39-01, 39-03]
files_modified:
  - lib/scoria_web/live/approvals_live/index.ex
  - assets/css/04-components.css
  - test/scoria_web/live/approvals_live_test.exs
autonomous: true
requirements: [FLOW-03, COPY-01]
must_haves:
  truths:
    - "The pending drawer is decision-first, top→bottom: header eyebrow+title (ApprovalCopy) → single status badge → plain-language consequence (impact/1) → actions Deny·Approve in reading flow directly under the consequence → always-visible facts → collapsed disclosures → view-run link (D-12)."
    - "The uppercase warn banner .scoria-approval-summary__label is DELETED (markup at index.ex:147 and CSS ~841-857); status is stated ONCE via a badge; salience lives in the exact-magnitude consequence line, not red chrome (D-13)."
    - "Progressive disclosure uses TWO native <details> disclosures: (i) \"Identifiers\" via <.id>+<.time> rows, (ii) \"Request payload\" via raw_evidence open={false}; the bespoke .scoria-approval-details tech-grid is DELETED (D-14)."
    - "The payload <details> carries a stable per-approval DOM id so a different approval mounts a fresh collapsed node; a LiveViewTest asserts an unrelated {:hitl_request} broadcast leaves an open <details> intact (D-14)."
    - "Decision copy is deduped to a single ApprovalCopy.status_line/1 badge in the drawer header; the drawer audit line (index.ex:170-172) is removed; the eyebrow uses ApprovalCopy.eyebrow/1 (D-16)."
    - "The two-step confirm modal is kept for BOTH approve and deny with no-auto-focus on the action; its copy uses the concrete irreversible effect + magnitude (impact_lead/1), not a title restate; Deny is neutral/secondary, not --danger (D-15)."
    - "The action <section> + confirm modal are emitted ONLY when pending (positive predicate decided?/1); the decided receipt asserts the recorded decision only (D-27), links to the run for execution truth (D-19)."
  artifacts:
    - lib/scoria_web/live/approvals_live/index.ex
  key_links:
    - "The drawer is restructured in place — no drawer/1 primitive change; the sticky-top .scoria-approval-actions is stripped and optionally re-added as position:sticky;bottom:0 (Claude's discretion: sticky-bottom footer chosen)."
    - "The approvals page header is migrated to page_header/1 in the same edit."
    - "decided?/1 is a positive whitelist s in ~w(approved rejected expired), mirroring the server guard index.ex:354-360, so buttons are structurally not emitted when decided."
---

<objective>
Redesign the approval drawer to be decision-first (Area 3, FLOW-03/D-12..D-16) and encode the
⚠ SAFETY honest-receipt structure (D-27), restructuring the existing `render/1` drawer subtree in place
with NO `drawer/1` primitive change. De-alarm the surface, dedup the ~6-place decision copy down to one
`ApprovalCopy.status_line/1` badge, split the wall-of-JSON into two native `<details>` disclosures, and
make the confirm modal earn its friction with concrete magnitude copy.

Purpose: This breaks the red-team failure chain (calm consequence → reflexive confirm → silent resume
failure → clean "Approved"): D-13 magnitude salience + D-15 confirm-earns-friction + D-27 honest
receipt + D-12 decision-first placement. It owns approvals_live/index.ex and the approval CSS chrome;
Plan 07 (decision history, Wave 3) builds on this same file afterward.

Output: a decision-first pending drawer, deleted alarm chrome, and the positive-predicate gate that
makes the decided read-only state (wired fully in Plan 07) structurally unable to imply reversal.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md
@.planning/phases/39-component-groups-and-operator-flows/39-PATTERNS.md
@lib/scoria_web/ui.ex
@lib/scoria_web/approval_copy.ex
@lib/scoria_web/live/approvals_live/index.ex
@assets/css/04-components.css
@brandbook/brand-book.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-sequence the pending drawer decision-first + de-alarm + dedup copy</name>
  <files>lib/scoria_web/live/approvals_live/index.ex</files>
  <read_first>
    - lib/scoria_web/live/approvals_live/index.ex (~121 approvals page header; ~141-219 the current
      drawer subtree to re-sequence; ~147 the warn banner to delete; ~148 impact/1 consequence;
      ~154-166 the action buttons to move up; ~170-172 the drawer audit line to remove; ~175/177
      evidence_rows; ~188 the .scoria-approval-details tech-grid; ~211 raw_evidence).
    - lib/scoria_web/approval_copy.ex (Plan 03 — status_line/1, eyebrow/1, impact/1, impact_lead/1).
    - lib/scoria_web/ui.ex badge/1, page_header/1, evidence_section/1 (~1083), raw_evidence/1 (~1016).
    - .planning/phases/39-component-groups-and-operator-flows/39-PATTERNS.md lines 231-282 (drawer
      redesign map, D-12..D-16 sequence, D-27 hazard).
    - .planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md D-12, D-13, D-16, D-27;
      brandbook §6.5 "approval is part of the workflow, not an alarm".
  </read_first>
  <action>
    Restructure the drawer render subtree in place (no drawer/1 change) into the D-12 decision-first
    order: (1) header eyebrow (ApprovalCopy.eyebrow/1 — tool context, replacing the generic "Approval
    request") + title; (2) a SINGLE status badge from ApprovalCopy.status_line/1; (3) the plain-language
    consequence via ApprovalCopy.impact/1; (4) the actions Deny · Approve directly under the consequence
    in reading flow (moved up from ~154-166); (5) always-visible "What you're approving" facts via
    evidence_section + evidence_rows; (6) the two collapsed disclosures (Task 2); (7) the view-run link.
    De-alarm (D-13): DELETE the uppercase warn banner markup (`.scoria-approval-summary__label`, ~147) —
    state status once via the badge; salience lives in the exact-magnitude consequence line, not red
    chrome. Dedup (D-16): collapse the ~6 decision-status emissions to the one status_line/1 badge;
    REMOVE the drawer audit line (~170-172) — keep audit reassurance only in the confirm modal. Migrate
    the approvals page header (~121) to `<.page_header>`. Grep offenders by symbol, not line number.
  </action>
  <verify>
    <automated>mix test test/scoria_web/live/approvals_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - The drawer renders in the D-12 order with the actions directly under the consequence.
    - The uppercase warn banner and the drawer audit line are gone; status appears once via a badge.
    - The eyebrow uses ApprovalCopy.eyebrow/1; the approvals page header uses page_header/1.
    - approvals_live_test.exs passes.
  </acceptance_criteria>
  <done>The pending drawer is decision-first and de-alarmed, with decision copy deduped to one status_line badge.</done>
</task>

<task type="auto">
  <name>Task 2: Two native &lt;details&gt; disclosures + stable DOM id + open-state regression</name>
  <files>lib/scoria_web/live/approvals_live/index.ex, test/scoria_web/live/approvals_live_test.exs</files>
  <read_first>
    - lib/scoria_web/live/approvals_live/index.ex (~188 .scoria-approval-details tech-grid to delete;
      ~211 raw_evidence to set open={false}; ~69-83 the {:hitl_request} broadcast path;
      ~273-281 seed_focused_active_approval reassignment that can collapse an open details).
    - lib/scoria_web/ui.ex id/1 (~276, the copyable log-grep affordance), time/1 (~304),
      raw_evidence/1 (~1005-1016, native <details>).
    - .planning/phases/39-component-groups-and-operator-flows/39-PATTERNS.md lines 248-258 (D-14 two
      disclosures + open-state mitigation).
    - .planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md D-14.
  </read_first>
  <action>
    Replace the single wall-of-JSON disclosure with TWO collapsed native `<details>` (D-14, no bespoke
    CSS): (i) "Identifiers" — `<.id>` rows for approval/run/session/trace id (each with its "Copy
    <thing>" affordance) + `<.time>` for requested-at (this is why IDs cannot fold into the JSON `<pre>`
    — raw_evidence renders a single `<pre>` that can't host copy controls); (ii) "Request payload" —
    `<.raw_evidence open={false} value={ApprovalCopy.raw_arguments(@active_approval)} />`. DELETE the
    bespoke `.scoria-approval-details` tech-grid markup (~188). Give the payload details a STABLE
    per-approval DOM id (id derived from `@active_approval.id`) so a different approval mounts a fresh
    correctly-collapsed node. Add a LiveViewTest regression asserting an unrelated `{:hitl_request}`
    broadcast (which reloads the inbox but not @active_approval) leaves an already-open `<details>`
    intact. (`phx-update="ignore"` is NOT a reliable fix and must not be used for this.)
  </action>
  <verify>
    <automated>mix test test/scoria_web/live/approvals_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Two collapsed <details> render (Identifiers via <.id>+<.time>, Request payload via raw_evidence
      open={false}); the .scoria-approval-details tech-grid markup is gone.
    - The payload <details> has a stable per-approval DOM id.
    - A LiveViewTest asserts an unrelated {:hitl_request} broadcast preserves an open <details>.
  </acceptance_criteria>
  <done>Payload/identifiers are split into two keyboard/SR-safe disclosures with a stable DOM id and an open-state regression test.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm modal earns friction + Deny neutral + decided gate + CSS chrome removal</name>
  <files>lib/scoria_web/live/approvals_live/index.ex, assets/css/04-components.css</files>
  <read_first>
    - lib/scoria_web/live/approvals_live/index.ex (~108-115 open_decision_modal; ~154-166 Deny/Approve
      buttons; ~220 confirm modal; ~283-314 record_approval_decision decision path; ~316-328
      maybe_resume_approval; ~354-360 the :not_pending/StaleEntryError server guard to mirror).
    - lib/scoria_web/approval_copy.ex (impact_lead/1, decision_outcome/1 from Plan 03).
    - assets/css/04-components.css (~841-857 .scoria-approval-summary__label DELETE; ~865-877
      .scoria-approval-actions sticky-top STRIP; ~886-943 .scoria-approval-details DELETE).
    - .planning/phases/39-component-groups-and-operator-flows/39-PATTERNS.md lines 264-282 (D-27 hazard
      + D-19 positive predicate) and 373-379 (CSS chrome removal).
    - .planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md D-15, D-19, D-27, D-12.
  </read_first>
  <action>
    Keep the two-step confirm `modal` for BOTH approve and deny and keep no-auto-focus on the action
    (the modal X carries autofocus, so initial focus lands on dismiss — the safe default) (D-15). Make
    the modal earn its friction: replace the title-restate confirm copy with the concrete irreversible
    effect + magnitude via `ApprovalCopy.impact_lead/1` (e.g. "This issues a $10,000.00 refund to
    cust_889. Scoria records the decision, then continues the run."). Render Deny as neutral/secondary,
    NOT `--danger` — denying is the safe reversible hold; if the locked tone vocabulary can't express a
    middle tone, at minimum add a code comment documenting the risk-gradient rationale so red-on-deny is
    not mistaken for a considered signal (D-15). Add a positive-whitelist predicate
    `decided?(%{status: s}), do: s in ~w(approved rejected expired)` and emit the action `<section>` +
    confirm modal ONLY when NOT decided (pending) — buttons structurally not emitted when decided,
    mirroring the server guard ~354-360 (D-19). ⚠ D-27: ensure the decided-state receipt asserts the
    recorded decision only and links to the run (workflow_run_id) for execution truth — never claim
    resume/side-effect success. In 04-components.css: DELETE `.scoria-approval-summary__label`
    (~841-857), STRIP `position:sticky;top:0`+gradient from `.scoria-approval-actions` (~865-877) and
    optionally re-add as `position:sticky;bottom:0` sticky-footer (chosen), and DELETE
    `.scoria-approval-details` (~886-943). Keep ds06/token-contrast green.
  </action>
  <verify>
    <automated>mix test test/scoria_web/live/approvals_live_test.exs test/scoria_web/ds06_drift_guard_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - The confirm modal keeps two-step + no-auto-focus for both actions and uses impact_lead/1 magnitude
      copy; Deny is not --danger.
    - decided?/1 gates the action section + confirm modal so they render only when pending.
    - The three approval-alarm CSS blocks are deleted/stripped; ds06 guard stays green.
  </acceptance_criteria>
  <done>The confirm modal carries honest magnitude friction, Deny is neutral, the decided gate prevents reversal affordances, and alarm CSS chrome is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → refund-approval decision | An operator approving a high-value refund is the highest-value action on this surface |
| resume failure → receipt rendering | A decided-but-unresumed run must not render as clean success |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-06-Sp | Spoofing | decided receipt | high | mitigate | D-27 receipt asserts the recorded decision only + links to the run for execution truth; a failed maybe_resume cannot present as success |
| T-39-06-E | Elevation of Privilege | reflexive rubber-stamp confirm | high | mitigate | D-15 confirm modal uses concrete irreversible magnitude copy (impact_lead/1), two-step for both actions, no-auto-focus on the action; D-13 magnitude salience in the consequence line |
| T-39-06-T | Tampering | reversal implication | high | mitigate | D-19 positive-whitelist decided?/1 emits action section only when pending — reversal affordances structurally absent when decided |
| T-39-06-DoS | Denial of Service | open-state loss / status render | medium | mitigate | Stable per-approval DOM id + open-state regression test (D-14); status via status_line/1 (safe fallback) |
| T-39-06-SC | Tampering | dependency installs | low | accept | No package installs this phase |
</threat_model>

<verification>
- `mix test test/scoria_web/live/approvals_live_test.exs test/scoria_web/ds06_drift_guard_test.exs test/scoria_web/token_contrast_guard_test.exs --warnings-as-errors` passes.
- `mix compile --warnings-as-errors` succeeds.
- Grep confirms the warn banner, drawer audit line, and tech-grid markup are gone; the three CSS blocks are deleted/stripped; a single status badge remains in the drawer header.
</verification>

<success_criteria>
- The pending drawer is decision-first, de-alarmed, deduped to one status badge, with two native disclosures and a stable DOM id.
- The confirm modal earns friction with magnitude copy; Deny is neutral; the decided gate blocks reversal affordances; the receipt is honest (D-27).
- Approval-alarm CSS chrome is removed; DS-06/contrast guards stay green.
</success_criteria>

<artifacts_this_phase_produces>
- Decision-first pending drawer + `decided?/1` predicate in approvals_live/index.ex
- Removed approval-alarm CSS chrome in assets/css/04-components.css
- Open-state LiveViewTest regression in approvals_live_test.exs
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/39-component-groups-and-operator-flows/39-06-SUMMARY.md` when done.
</output>
