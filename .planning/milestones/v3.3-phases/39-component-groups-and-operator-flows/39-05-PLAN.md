---
phase: 39-component-groups-and-operator-flows
plan: 05
type: execute
wave: 2
depends_on: [39-01, 39-02]
files_modified:
  - lib/scoria_web/live/dataset_live/index.ex
  - lib/scoria_web/live/connectors_live/index.ex
  - lib/scoria_web/live/review_queue_live.ex
  - lib/scoria_web/live/incidents_live/index.ex
autonomous: true
requirements: [FLOW-01, FLOW-02, COPY-01]
must_haves:
  truths:
    - "dataset/connectors/review_queue/incidents each render their single page-outline <h1> through page_header/1; dataset drops the semantically-redundant region <:title> \"Datasets\" under its \"Dataset Builder\" <h1> (D-04)."
    - "review_queue's filter state moves from socket assigns to URL params via push_patch + handle_params (shareable/reconnect-safe); ephemeral UI state stays in assigns (D-09/D-11)."
    - "Sort policy (B) is chosen: sort stays in socket assigns as a per-session view pref and is EXPLICITLY EXEMPT from the D-11 guard; dataset_live is not red-flagged (D-09 option B)."
    - "The incidents <ul> uses phx-update=\"stream\" + per-<li> id with zero table/1 change; the pending approvals inbox is NOT streamed (D-10)."
    - "Microcopy offenders fixed: connectors runtime.status routes through status_label/1 (or ConnectorCopy) + badge (not a raw string); connectors run_id/session_id demote to <.id> evidence; review row raw status routes through status_label/1 or ReviewCopy; dataset version uses a plain version label, not <.id> (D-23)."
  artifacts:
    - lib/scoria_web/live/incidents_live/index.ex
  key_links:
    - "review_queue reuses the dataset_live push_patch + handle_params URL-encoding idiom; free-text filters debounce before push_patch; no record payloads in the URL (D-09)."
    - "incidents stream converts @incidents to stream(:incidents) and iterates @streams.incidents with id={id} on each <li> (D-10)."
    - "connectors runtime.status label uses status_label/1 or ConnectorCopy from Plans 01/02; review status label uses status_label/1 or ReviewCopy."
---

<objective>
Normalize the cross-page scan conventions (Area 2, FLOW-02/D-06..D-11) and fix the remaining
microcopy offenders (Area 5, COPY-01/D-23) on the four scan pages: dataset, connectors, review_queue,
and incidents. Each ends with a single page_header, URL-held shareable state, the correct table-vs-list
idiom, and operator-first columns with IDs demoted to evidence.

Purpose: The second of two parallel Wave-2 page-migration plans (disjoint files from Plan 04). These
four pages carry the real IA inconsistencies (review_queue's socket-held filter, ad-hoc statuses,
un-streamed incidents list) that FLOW-02 targets, plus the connectors/review/dataset microcopy
offenders.

Locked discretion choices for this plan:
- **D-09 sort policy = option (B):** sort stays in socket assigns as a per-session view preference and
  is EXEMPT from the D-11 guard (smaller blast radius; sort is ephemeral; Phase 41 can harden). Only
  review_queue's FILTER migrates to the URL. The D-11 guard (Plan 08) must exclude sort and must not
  red-flag dataset_live.
- **D-10 incidents = stream; decided-history = capped + load-more (Plan 07); pending inbox = never
  streamed.**
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md
@.planning/phases/39-component-groups-and-operator-flows/39-PATTERNS.md
@lib/scoria_web/ui.ex
@lib/scoria_web/live/dataset_live/index.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: dataset + connectors — header, redundant-title, and microcopy</name>
  <files>lib/scoria_web/live/dataset_live/index.ex, lib/scoria_web/live/connectors_live/index.ex</files>
  <read_first>
    - lib/scoria_web/live/dataset_live/index.ex (~62-67 "Dataset Builder" pagehead <h1>; ~82 region
      <:title> "Datasets" — semantic redundancy per D-04; ~98 `<.id value={"v#{...version}"}>` misuse;
      ~102/~330 state_label/1 which legitimately uses its own label fn — do NOT break it).
    - lib/scoria_web/live/connectors_live/index.ex (~78 `label={runtime.status}` raw string on a badge;
      ~83 current_run_id as a primary cell; ~86 host_session_id as a primary cell).
    - lib/scoria_web/ui.ex page_header/1, status_label/1, id/1 (~276), badge/1 (~68).
    - lib/scoria_web/connector_copy.ex (Plan 02 — the operator label for a connector runtime status).
    - .planning/phases/39-component-groups-and-operator-flows/39-PATTERNS.md lines 319-333 (offender
      table) and 331 (D-04 dataset redundancy).
    - .planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md D-04, D-23.
  </read_first>
  <action>
    dataset_live: route the "Dataset Builder" header through `<.page_header>`, and DROP the
    semantically-redundant region `<:title>Datasets</:title>` (~82) — render the region flush/untitled
    since the page `<h1>` already implies it (D-04). Fix the version cell (~98): render a plain version
    label instead of `<.id>` (the `<.id>` copyable-ID primitive is misused for a non-ID version, D-23).
    Leave `state_label/1` (dataset's own label fn) intact. connectors_live: replace the raw
    `label={runtime.status}` badge (~78) with `status_label(runtime.status)` (or ConnectorCopy's
    operator label) so status is never a raw string (D-23/D-26 — this is the allow-list catch); lead
    the run/session cells (~83/~86) with status and demote `current_run_id`/`host_session_id` to `<.id>`
    evidence. Route connectors' page header through `page_header/1`. Grep offenders by symbol. Add no CSS.
    Ensure each page's data region carries the canonical D-08 states: empty renders via `empty_state/1`
    with a next action, error renders via inline `scoria-flash--fail` + retry (plain language); no loading
    spinner (mount queries synchronously, D-08).
  </action>
  <verify>
    <automated>mix test test/scoria_web/live/dataset_live test/scoria_web/live/connectors_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - dataset renders one page_header <h1>, has no redundant region "Datasets" title, and shows the
      version as a plain label (not <.id>).
    - connectors status renders via status_label/1 or ConnectorCopy + badge (no raw string label);
      run_id/session_id render as <.id> evidence.
    - dataset and connectors render their empty state via empty_state/1 with a next action and their
      error state via inline scoria-flash--fail + retry (per D-08); no loading spinner.
    - dataset/connectors LiveView tests pass.
  </acceptance_criteria>
  <done>dataset and connectors orient with page_header, drop redundant/duplicate copy, route status through a label fn, and demote IDs to evidence.</done>
</task>

<task type="auto">
  <name>Task 2: review_queue — header, filter socket→URL, raw status fix</name>
  <files>lib/scoria_web/live/review_queue_live.ex</files>
  <read_first>
    - lib/scoria_web/live/review_queue_live.ex (~52-56 the richest existing pagehead with --with-actions
      + ghost "Back to dashboard" — the page_header :actions exemplar; ~31-33/~95-119 the socket-held
      filter to migrate; ~118 the table/1 :filter slot usage; ~130 `row.sample_reason || row.status`
      raw status atom).
    - lib/scoria_web/live/dataset_live/index.ex (the push_patch + handle_params URL-encoding idiom to
      mirror for the filter).
    - lib/scoria_web/ui.ex page_header/1, status_label/1; lib/scoria_web/review_copy.ex (Plan 02).
    - .planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md D-09, D-11, D-23.
    - .planning/phases/39-component-groups-and-operator-flows/39-PATTERNS.md lines 400-404 (URL-state
      vs assigns split).
  </read_first>
  <action>
    Migrate review_queue's page header to `<.page_header>` using the existing `--with-actions` + ghost
    "Back to dashboard" link as the single `<:actions>` entry. Move the filter facet state from socket
    assigns (~31-33/~95-119) to the URL via `push_patch` + `handle_params` (mirror the dataset_live
    idiom) so it is shareable and reconnect-safe (D-09/D-11); enum facets only in the URL, debounce
    free-text before push_patch, never put record payloads in the URL. Keep ephemeral UI state (toggles,
    toasts) in assigns. Fix the raw status atom at ~130: route `row.status` through `status_label/1`
    (or ReviewCopy) so no raw atom renders (D-23). Sort stays in assigns (policy B). Add no CSS.
    Render the review_queue data region's empty state via `empty_state/1` with a next action and its
    error state via inline `scoria-flash--fail` + retry (per D-08); no loading spinner (synchronous mount).
  </action>
  <verify>
    <automated>mix test test/scoria_web/live/review_queue_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - review_queue renders page_header with the ghost action; the filter is read from and written to
      URL params via handle_params/push_patch, not held only in socket assigns.
    - The review row status renders via status_label/1 or ReviewCopy (no raw atom).
    - review_queue renders its empty state via empty_state/1 with a next action and its error state via
      inline scoria-flash--fail + retry (per D-08); no loading spinner.
    - review_queue_live_test.exs passes (adjust filter-related assertions for the URL round-trip).
  </acceptance_criteria>
  <done>review_queue orients via page_header, holds its filter in the URL, and renders status through a label fn.</done>
</task>

<task type="auto">
  <name>Task 3: incidents — page_header + stream the list (D-10)</name>
  <files>lib/scoria_web/live/incidents_live/index.ex</files>
  <read_first>
    - lib/scoria_web/live/incidents_live/index.ex (~56 pagehead header; ~105-121 the caller-owned
      `<ul class="scoria-selectable-list">` with `<li :for={incident <- @incidents}>` and
      `<.selectable_card>` — the ONE safe stream target: mount-only load, no PubSub).
    - lib/scoria_web/ui.ex page_header/1.
    - .planning/phases/39-component-groups-and-operator-flows/39-PATTERNS.md lines 303-315 (stream the
      incidents <ul>) and 133/331 (incidents-as-list is correct per JTBD — do NOT convert to a table).
    - .planning/phases/39-component-groups-and-operator-flows/39-CONTEXT.md D-07 (incidents = list),
      D-10 (stream the ul, zero table/1 change), D-08 (empty state at the region).
  </read_first>
  <action>
    Route the incidents page header through `<.page_header>`. Convert `@incidents` to
    `stream(socket, :incidents, ...)`, iterate `:for={{id, incident} <- @streams.incidents}`, put
    `id={id}` on each `<li>`, and add `phx-update="stream"` to the `<ul>` (zero `table/1` change — this
    is the caller-owned list, D-10). Keep incidents as a LIST (not a table) — document briefly in a code
    comment WHY (heterogeneous "open the pressing one" JTBD, D-07) so it does not read as drift. Ensure
    the empty state renders at the region via `empty_state/1` with a next action (D-08). Do NOT stream
    any pending-approvals inbox. Add no CSS.
  </action>
  <verify>
    <automated>mix test test/scoria_web/live/incidents_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - incidents renders page_header; the <ul> uses phx-update="stream" with a per-<li> id and
      @streams.incidents iteration; table/1 is unchanged.
    - The list (not table) idiom is retained with a rationale comment; empty state renders at the region
      via empty_state/1 with a next action, and the error state via inline scoria-flash--fail + retry
      (per D-08); no loading spinner.
    - incidents_live_test.exs passes (adjust for stream assigns if needed).
  </acceptance_criteria>
  <done>incidents orients via page_header and renders a streamed selectable list with an empty state, keeping the list idiom.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params → LiveView filter state | Operator-controlled query params drive filter/scope; must be enum-validated |
| record fields → columns / <h1> | Opaque IDs / raw status atoms can leak into orientation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-05-T | Tampering | review_queue URL filter | medium | mitigate | Only enum filter facets are URL-encoded and validated via handle_params; free-text debounced; no record payloads in the URL (D-09) |
| T-39-05-I | Information Disclosure | connectors/review columns | medium | mitigate | Raw status atoms routed through status_label/1 (never color/atom-only); run_id/session_id demoted to <.id> evidence (D-23) |
| T-39-05-DoS | Denial of Service | status rendering | medium | mitigate | status_label/1 retains its generic fallback (Plan 01) so an unseen connector/review status cannot 500 the render |
| T-39-05-SC | Tampering | dependency installs | low | accept | No package installs this phase |
</threat_model>

<verification>
- `mix test test/scoria_web/live/dataset_live test/scoria_web/live/connectors_live_test.exs test/scoria_web/live/review_queue_live_test.exs test/scoria_web/live/incidents_live_test.exs --warnings-as-errors` passes.
- `mix compile --warnings-as-errors` succeeds.
- Grep confirms: one `<h1>` per module; no raw `label={runtime.status}`; no `row.status` rendered without a label fn; `phx-update="stream"` present on the incidents `<ul>`; review_queue filter read via `handle_params`.
- Each migrated scan page (dataset, connectors, review_queue, incidents) renders the canonical D-08 data-region convention: empty via `empty_state/1` with a next action, error via inline `scoria-flash--fail` + retry; no loading spinner (synchronous mount, D-08).
</verification>

<success_criteria>
- All four scan pages orient via page_header, follow the canonical section conventions, and demote IDs/atoms to evidence.
- review_queue filter is URL-held; incidents list is streamed; sort remains in assigns (policy B, exempt from the D-11 guard).
</success_criteria>

<artifacts_this_phase_produces>
- Migrated + convention-normalized dataset_live/index, connectors_live/index, review_queue_live, incidents_live/index (consumers of page_header/1, status_label/1, ConnectorCopy, ReviewCopy)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/39-component-groups-and-operator-flows/39-05-SUMMARY.md` when done.
</output>
