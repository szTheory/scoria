---
phase: 41.1-wire-orphaned-scoriaweb-copy-datasetcopy-into-dataset-page-c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria_web/ui.ex
  - lib/scoria_web/live/dataset_live/index.ex
  - test/scoria_web/copy_test.exs
  - test/scoria_web/copy_guard_test.exs
autonomous: true
requirements: [COPY-01]

must_haves:
  truths:
    - "Operator sees byte-identical dataset-page copy after the refactor: empty-state title, filtered-empty CTA, state labels (:open/:sealed), version tokens, and curated status labels are unchanged (COPY-01 byte-stable)."
    - "ScoriaWeb.Copy (base) has >=1 real caller in lib/ (UI.status_label/1 delegation + Copy.empty_title(:datasets) in dataset_live) — no longer orphaned."
    - "ScoriaWeb.DatasetCopy has >=1 real caller in lib/ (state_label/1 + version_label/1 in dataset_live) — no longer orphaned."
    - "UI.status_label/1 and Copy.status_label/1 return identical labels across the 13 curated statuses + nil + an unseen key; a future re-divergence fails a test (D4a)."
    - "A page-scoped regression guard fails if dataset_live/index.ex reverts its empty-state title to an inline literal instead of Copy.empty_title(:datasets) (D4b)."
  artifacts:
    - "lib/scoria_web/ui.ex — status_label/1 delegates to Copy.status_label/1 (inline status map deleted)."
    - "lib/scoria_web/live/dataset_live/index.ex — wired to Copy.empty_title/1, DatasetCopy.state_label/1, DatasetCopy.version_label/1; local state_label/1 removed; state_tone/1 kept."
    - "test/scoria_web/copy_test.exs — UI/Copy status_label parity test (D4a)."
    - "test/scoria_web/copy_guard_test.exs — dataset empty-state-title caller-presence / literal-absence guard (D4b)."
  key_links:
    - "UI.status_label/1 -> Copy.status_label/1 (one-directional new edge; Copy adds NO ScoriaWeb.UI reference — stays a dependency-free leaf per Phase 39-02 / D-24b)."
    - "dataset_live/index.ex -> Copy.empty_title(:datasets), DatasetCopy.state_label/1, DatasetCopy.version_label/1 (import ScoriaWeb.UI already present; add alias ScoriaWeb.Copy + alias ScoriaWeb.DatasetCopy)."
  prohibitions:
    - "NO operator-visible copy change to any string that passed COPY-01 (bytes must stay stable): empty-state title, filtered-empty CTA wording, state labels for :open/:sealed, v{version} tokens, and the 13 curated status labels."
    - "ScoriaWeb.Copy MUST NOT gain any ScoriaWeb.UI reference — it stays a dependency-free leaf (Phase 39-02 locked; D-24b). Delegation direction is UI -> Copy only."
    - "Do NOT swap the dataset empty-state CTA (filtered-empty semantics) to Copy.empty_cta(:datasets) ('Promote a production trace…') or the generic Copy.empty_cta(_) ('…once new data arrives.') — neither is byte-identical to today's visible CTA (D2)."
    - "Do NOT remove or relocate dataset_live's local defp state_tone/1 — badge tone is a rendering concern and stays local (D3); Copy/DatasetCopy expose no tone."
---

<objective>
Close the single COPY-01 SSOT tech-debt item the v3.3 milestone audit surfaced: three orphaned/duplicated operator-copy surfaces get wired to one source of truth WITHOUT changing any operator-visible copy that already passed COPY-01 verification.

- D1: `ScoriaWeb.UI.status_label/1` delegates to `ScoriaWeb.Copy.status_label/1` (deletes UI's duplicate 13-entry status map); `Copy` stays a dependency-free leaf.
- D2: dataset empty-state title sourced from `Copy.empty_title(:datasets)` (byte-identical); CTA wording preserved exactly (no byte-identical Copy home exists — kept inline + recorded).
- D3: dataset state label + version routed through `DatasetCopy`; badge tone stays local; `orientation/1` documented-not-silently-orphaned.
- D4: a regression guard proves UI/Copy status-label parity and that the empty-state title cannot silently revert to an inline literal.

Purpose: Remove dead-code / SSOT-drift debt so the built-and-tested `ScoriaWeb.Copy` and `ScoriaWeb.DatasetCopy` modules are actually the single source of truth for the dataset page. Maintainability, zero behavior change.
Output: 2 wired source files (`ui.ex`, `dataset_live/index.ex`) + 2 extended guard/test files (`copy_test.exs`, `copy_guard_test.exs`).

Artifacts this phase produces:
- Changed public behavior: `ScoriaWeb.UI.status_label/1` now delegates (same return values for the curated vocabulary; single convergence noted in Task 1 for `nil` input).
- New callers wired: `Copy.empty_title/1`, `Copy.status_label/1`, `DatasetCopy.state_label/1`, `DatasetCopy.version_label/1`.
- New tests: a UI/Copy status_label parity test (in `copy_test.exs`) and a dataset empty-state-title guard test (in `copy_guard_test.exs`).
- No new public symbols; no new modules; no CSS/asset changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/41.1-wire-orphaned-scoriaweb-copy-datasetcopy-into-dataset-page-c/41.1-CONTEXT.md

@lib/scoria_web/copy.ex
@lib/scoria_web/dataset_copy.ex
@lib/scoria_web/ui.ex
@lib/scoria_web/live/dataset_live/index.ex
@test/scoria_web/copy_test.exs
@test/scoria_web/copy_guard_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delegate UI.status_label/1 to Copy.status_label/1 + parity guard (D1, D4a)</name>
  <read_first>
    - lib/scoria_web/ui.ex (status_label/1 clauses ~lines 62-83; the @doc above them explains the additive-fallback intent — preserve that intent)
    - lib/scoria_web/copy.ex (status_label/1 ~lines 87-98; note explicit status_label(nil) -> "Unknown")
    - test/scoria_web/copy_test.exs (describe "status_label/1" block, existing assertion style; no UI alias yet)
  </read_first>
  <files>lib/scoria_web/ui.ex, test/scoria_web/copy_test.exs</files>
  <action>
    In lib/scoria_web/ui.ex, replace UI's three inline status_label/1 clauses (the atom clause, the binary case-map of 13 statuses + humanize fallback, and the final catch-all returning "Unknown") with a single implementation that forwards every input to Copy.status_label/1 (add `alias ScoriaWeb.Copy` to the module or call it fully-qualified). Keep the existing @doc. This creates a one-directional UI -> Copy edge; do NOT add any Copy -> UI reference (Copy must remain a dependency-free leaf per the Phase 39-02 locked decision and D-24b — verified today: copy.ex has no reference to the UI module).

    Record the single behavioral convergence in the SUMMARY: pre-delegation `UI.status_label(nil)` humanized the atom to "Nil" (nil is an atom; the old atom clause did Atom.to_string(nil) -> "nil" -> "Nil"), whereas Copy.status_label(nil) returns "Unknown". Post-delegation UI(nil) becomes "Unknown". This nil input is not reachable at any render call site — every caller passes a required `.status`/`.severity`/`.state` field or an `x || y`-guarded value (e.g. review_queue_live status_label(row.score_status || row.status)) — so it is a curated-SSOT convergence, not a visible regression. Do NOT change any of the 13 curated labels or the humanize fallback for non-curated strings; those stay byte-identical.

    In test/scoria_web/copy_test.exs, add `alias ScoriaWeb.UI` and a parity test (D4a) asserting `UI.status_label(x) == Copy.status_label(x)` for: each of the 13 curated statuses in string form ("pending","approved","expired","passed","failed","regressed","running","promoted","draft","published","connected","disconnected","idle"), plus `nil`, plus one deliberately-uncurated key (e.g. "archived_forever"). Match the file's existing plain-assert style. This test fails if a future edit re-introduces a divergent inline UI status map.
  </action>
  <verify>
    <automated>mix test test/scoria_web/copy_test.exs test/scoria_web/ui_component_test.exs</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - ui.ex status_label/1 delegates to Copy.status_label/1; no inline 13-entry status map remains in ui.ex.
    - lib/scoria_web/copy.ex still contains no reference to the ScoriaWeb.UI module (leaf preserved).
    - Parity test in copy_test.exs is green across the 13 curated statuses + nil + one unseen key.
    - `mix compile --warnings-as-errors` is clean (no unused-clause / unused-alias warnings).
  </acceptance_criteria>
  <done>UI.status_label/1 delegates to Copy.status_label/1 (inline map gone), Copy stays a leaf, and the parity test passes over 13 statuses + nil + unseen key with a clean warnings-as-errors compile.</done>
</task>

<task type="auto">
  <name>Task 2: Wire dataset_live/index.ex to Copy + DatasetCopy, byte-stable (D2, D3)</name>
  <read_first>
    - lib/scoria_web/live/dataset_live/index.ex (module header ~lines 1-14: `import ScoriaWeb.UI`, `alias Scoria.Eval`; template ~lines 87-142 for empty_state / State col / version tokens; defps ~lines 350-357 for state_tone/1 and state_label/1)
    - lib/scoria_web/copy.ex (empty_title/1 line 102 returns "No datasets match this view"; empty_cta/1 lines 114/125 — confirm neither is byte-identical to the page CTA)
    - lib/scoria_web/dataset_copy.ex (state_label/1, version_label/1, orientation/1, field/2)
    - test/scoria_web/live/dataset_live/index_test.exs (rendered-output expectations to keep green)
  </read_first>
  <files>lib/scoria_web/live/dataset_live/index.ex</files>
  <action>
    Add `alias ScoriaWeb.Copy` and `alias ScoriaWeb.DatasetCopy` to the module header (alongside the existing `import ScoriaWeb.UI` and `alias Scoria.Eval` / `alias Scoria.Runtime`).

    (a) Empty-state title (D2): change the `<.empty_state title="...">` binding in the table `<:empty>` slot (~line 96) to `title={Copy.empty_title(:datasets)}`. The returned string is byte-identical to the current literal, so the rendered page does not change.

    (b) Empty-state CTA (D2 — PRESERVE, do not swap): the empty_state body currently reads the filtered-empty CTA "Adjust your filters or check back when data is available." `Copy.empty_cta(:datasets)` ("Promote a production trace to start a regression suite.") and the generic `Copy.empty_cta(_)` ("Adjust your filters or check back once new data arrives.") are BOTH byte-different from today's visible text, so there is no byte-identical Copy home. Keep the CTA text exactly as-is inline and add a one-line code comment marking it a deliberate non-SSOT string (filtered-empty semantics; no byte-identical Copy accessor — any wording change is an owner decision). Do NOT swap it.

    (c) State label (D3): replace both `label={state_label(dataset.state)}` call sites — the State `<:col>` (~line 107) and the `<:mobile_summary>` status (~line 130) — with `label={DatasetCopy.state_label(dataset.state)}`. Then delete the now-unused local `defp state_label/1` (~lines 355-357). KEEP `defp state_tone/1` (~lines 351-353) exactly as-is — badge tone is a rendering concern, not copy (D3). Known states `:open`/`:sealed` return byte-identical "Open"/"Sealed"; DatasetCopy's unknown fallback ("State not recorded") is unreachable with current seed data (state is always :open/:sealed) — confirm no fixture/test asserts the old "Unknown" state string.

    (d) Version token (D3): replace `v{dataset.version}` at the table Dataset cell (~line 103) and the mobile summary (~line 127) with `{DatasetCopy.version_label(dataset)}`. The row map carries the required `:version` string field (schema: `field :version, :string`, validate_required), so version_label returns "v" <> version — byte-identical to the old `v` + interpolation, and the surrounding literal spacing (e.g. the leading space in the mobile `<span> v...`) is preserved because the "v" moves inside the function output. This gives version_label/1 a real caller.

    (e) orientation/1 (D3): the dataset index is a sortable table with no per-row orientation-prose surface (orientation lines belong to dataset-detail / promote surfaces, which are out of scope this phase). Leave orientation/1 unwired and add a short comment (module-level or near the DatasetCopy alias) stating it is intentionally unused on the index page — documented, not silently orphaned.
  </action>
  <verify>
    <automated>mix test test/scoria_web/live/dataset_live/index_test.exs</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - index.ex aliases ScoriaWeb.Copy and ScoriaWeb.DatasetCopy and calls Copy.empty_title(:datasets), DatasetCopy.state_label/1 (both call sites), and DatasetCopy.version_label/1 (both call sites).
    - Local `defp state_label/1` is removed; `defp state_tone/1` is retained unchanged.
    - Empty-state CTA text is unchanged from today's bytes and carries an explanatory non-SSOT comment; orientation/1 has a documented-unused comment.
    - Rendered dataset page output is byte-unchanged (index_test.exs green); `mix compile --warnings-as-errors` clean (no unused-function warning for the removed state_label/1).
  </acceptance_criteria>
  <done>dataset_live/index.ex sources title/state-label/version from Copy+DatasetCopy with byte-identical output, local state_label/1 removed and state_tone/1 kept, CTA + orientation dispositions recorded in comments, index_test.exs green.</done>
</task>

<task type="auto">
  <name>Task 3: Regression guard — dataset empty-state title must stay Copy-sourced (D4b)</name>
  <read_first>
    - test/scoria_web/copy_guard_test.exs (full file — match its @page_glob source-scan, offender-list, `assert offenders == []` descriptive-failure style; note @excluded dialog-scoped promote_component.ex)
    - lib/scoria_web/live/dataset_live/index.ex (the Task 2 wired `<:empty>` slot)
  </read_first>
  <files>test/scoria_web/copy_guard_test.exs</files>
  <action>
    Extend the existing D-26 page-scoped source-scan guard with one new test asserting the dataset index empty-state title is sourced from Copy, not an inline literal. The test reads lib/scoria_web/live/dataset_live/index.ex and asserts BOTH: (i) it references `Copy.empty_title(:datasets)` (caller-presence), and (ii) the raw empty-state title literal is absent from the file (literal-absence). Build it in the file's established style — an evidence/offender list plus `assert present`/`assert offenders == []` with a multi-line failure message that names the fix ("source the title from Copy.empty_title(:datasets)"). Do NOT introduce a new test framework, dependency, or LiveViewTest DOM harness — reuse File.read! + Regex/String.contains? exactly as the sibling assertions do.

    <!-- planner-discipline-allow: No datasets match this view -->
    The exact literal the test must assert-absent from index.ex is: No datasets match this view
  </action>
  <verify>
    <automated>mix test test/scoria_web/copy_guard_test.exs test/scoria_web/copy_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - New guard test is green against the Task 2 wiring.
    - Reverting index.ex to the inline title literal (removing the Copy.empty_title(:datasets) call) makes the guard fail — confirming it protects against silent regression (D4b).
    - The guard matches the existing copy_guard_test.exs conventions (page-scoped source scan, descriptive failure message); no new dependency added.
  </acceptance_criteria>
  <done>copy_guard_test.exs gains a green guard asserting the dataset empty-state title is Copy-sourced and the raw literal is absent from index.ex; reverting the wiring makes it fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This phase is an internal Elixir copy-module refactor. It adds no new external input path, no route, no parameter parsing, no package install, and no asset/CSS change. All edits move existing operator-copy strings behind existing pure functions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41.1-01 | Tampering | SSOT drift: dataset empty-state title / status-label map silently reverting to inline duplicates | low | mitigate | D4 guards — copy_guard_test.exs literal-absence for the title + copy_test.exs UI/Copy status_label parity fail on re-divergence. |
| T-41.1-02 | Denial of Service | Unseen status/state atom raising FunctionClauseError inside render/1 (page-500) | low | accept | Both Copy.status_label/1 and DatasetCopy.state_label/1 retain safe generic `_ ->` fallbacks (never raise); delegation preserves this — no closed allow-list introduced. |
</threat_model>

<verification>
Phase-level checks (run after all three tasks):

- `mix test test/scoria_web/copy_test.exs test/scoria_web/copy_guard_test.exs test/scoria_web/ui_component_test.exs test/scoria_web/live/dataset_live/index_test.exs` — all green.
- `mix compile --warnings-as-errors` — clean (no unused local function / unused alias warnings).
- Manual byte-diff sanity: the rendered dataset page (empty-state title + CTA, State badges, version tokens) is unchanged from before this phase — every wired string is byte-identical to the prior literal (COPY-01 stays satisfied).
- `ScoriaWeb.Copy` and `ScoriaWeb.DatasetCopy` each now have >=1 real caller in lib/ (audit finding closed).
</verification>

<success_criteria>
1. `ScoriaWeb.Copy` (via UI.status_label delegation + Copy.empty_title(:datasets)) and `ScoriaWeb.DatasetCopy` (state_label/1 + version_label/1) each have >=1 real caller in lib/.
2. dataset_live/index.ex sources its state label + empty-state title from DatasetCopy/Copy and routes v{version} through DatasetCopy.version_label/1; no local duplicated clauses remain.
3. UI.status_label/1 delegates to Copy.status_label/1 (single status map); Copy remains a dependency-free leaf (no ScoriaWeb.UI reference).
4. No operator-visible copy changed without a recorded decision — the empty-state title, CTA, state labels, version tokens, and curated status labels stay byte-stable; the CTA and orientation/1 dispositions are recorded in code comments and the SUMMARY.
5. A regression guard (copy_guard_test.exs literal-absence + copy_test.exs parity) prevents the wired call sites from silently reverting to inline literals or a duplicated status map.
</success_criteria>

<output>
Create `.planning/phases/41.1-wire-orphaned-scoriaweb-copy-datasetcopy-into-dataset-page-c/41.1-01-SUMMARY.md` when done. Record the single behavioral convergence (UI.status_label(nil): "Nil" -> "Unknown", unreachable at render sites) and the CTA/orientation dispositions.
</output>
