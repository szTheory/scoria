---
phase: 40-accessibility-motion-and-responsive-proof
plan: 05
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - priv/dev/e2e/responsive_scan.spec.mjs
  - priv/dev/e2e/lib/instant_duration.mjs
  - priv/dev/e2e/reduced_motion.spec.mjs
  - priv/dev/shots.mjs
autonomous: true
requirements: [RESP-01, MOTION-01]
must_haves:
  truths:
    - "`responsive_scan.spec.mjs` reuses `VIEWPORT_WIDTHS = [320,375,768,1024,1440,1920]` + `waitForReady` and generalizes the proven `phase16_parity.spec.mjs` 375 no-h-overflow assertion to ~4 representative primary pages (D-14)."
    - "Tiered per D-15: cheap doc-overflow (D-16(1)) at all 6 widths on ~4 pages; expensive per-element checks (D-16(2)/(6)) only at 320/375/768."
    - "The full D-16 assertion catalog (1)-(7) is implemented, including `:mobile_summary` swap correctness (4), table-overflow-contained-not-leaked (3), no fixed toast region covering nav (5, via shared `boxesIntersect`, EXCLUDING the sticky approval footer/command palette/mobile-nav/scrims per D-17), 24px min target (6), no trapped scroll (7)."
    - "Reduced-motion e2e proves computed `animationDuration`/`transitionDuration` collapse under `emulateMedia({reducedMotion:'reduce'})` — including the `infinite` skeleton (line 1610) tamed by the kill switch (D-19)."
    - "Responsive collectors on not-yet-fixed layout surfaces are WARNING-GRADE non-throwing (`console.warn`+`testInfo.attach()`, D-04); in-scope layout defects are fixed, out-of-scope-boundary defects registered in the gap register (D-01/D-02); screenshots NEVER gate (D-14)."
  artifacts:
    - priv/dev/e2e/responsive_scan.spec.mjs
    - priv/dev/e2e/reduced_motion.spec.mjs
    - priv/dev/shots.mjs
  key_links:
    - "responsive_scan.spec.mjs imports `boxesIntersect` from Plan 01 for D-16(5) static occlusion (D-17 shared primitive)."
    - "`isInstantDuration` factored into `lib/instant_duration.mjs` (from phase16_parity.spec.mjs) — accepts '0s'/'0.001ms'/'1e-06s' comma-split; reused by reduced_motion.spec.mjs (research Don't-Hand-Roll)."
    - "`shots.mjs` widened 2→6 viewports feeds `contact_sheet.mjs` as human evidence only (assertions gate, screenshots inform)."
---

<objective>
Author the RESP-01 responsive proof harness and the MOTION-01 reduced-motion e2e proof, plus widen the screenshot capture to the 6-width matrix for human evidence. This is the responsive-scan slice of the D-07 coverage map — per-viewport geometry that only a real browser can measure.

Purpose: Prove primary pages stay usable at 320/375/768/1024/1440/1920 (no clipped content, trapped scroll, squished essential columns, or floating controls over nav) and that motion collapses under reduced-motion. Reuses the proven `phase16_parity.spec.mjs` overflow + reduced-motion patterns and the shared `boxesIntersect` helper. Layout defects surfaced on not-yet-fixed surfaces are warning-grade collectors; in-scope defects are fixed, out-of-scope registered.
Output: `responsive_scan.spec.mjs`, `reduced_motion.spec.mjs`, factored `lib/instant_duration.mjs`, widened `shots.mjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-accessibility-motion-and-responsive-proof/40-CONTEXT.md
@.planning/phases/40-accessibility-motion-and-responsive-proof/40-RESEARCH.md
@.planning/phases/40-accessibility-motion-and-responsive-proof/40-VALIDATION.md
@priv/dev/e2e/phase16_parity.spec.mjs
@priv/dev/e2e/lab.spec.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: responsive_scan.spec.mjs — tiered D-16 assertion catalog across ~4 primary pages</name>
  <read_first>
    - priv/dev/e2e/phase16_parity.spec.mjs (proven 375 no-h-overflow on Home + /workflows; the table-viewport-may-overflow-while-page-doesn't pairing = D-16(3) already proven on one page — the exact seed to generalize)
    - priv/dev/e2e/lab.spec.mjs (`VIEWPORT_WIDTHS = [320,375,768,1024,1440,1920]` at line 36 — reuse), priv/dev/e2e/lib/ready.mjs (`waitForReady`), priv/dev/e2e/lib/boxes_intersect.mjs (Plan 01 — import for D-16(5))
    - lib/scoria_web/live/ (enumerate the ~4 representative primary pages: approvals/review-queue = dense table + sticky action bar + `:mobile_summary`; incidents = page-split grid + list; a workflow detail = page-split + evidence + drawer; the lab shell already wired)
    - assets/css/04-components.css (`:mobile_summary` swap `max-width:767px`/`min-width:768px` ~1747/1757; `.scoria-table__viewport overflow-x:auto tabindex=0` ~1684/1273; `.scoria-toast-region position:fixed` ~1641; sticky approval footer ~854 — EXCLUDED from D-16(5))
    - lib/mix/tasks/scoria.ui.e2e.ex (seeded demo ids env for deterministic page state)
    - 40-CONTEXT.md D-14, D-15, D-16 (assertion catalog (1)-(7)), D-17 (D-16(5) static occlusion, exclusions), D-18 (table strategy), D-04
  </read_first>
  <action>
    Create `priv/dev/e2e/responsive_scan.spec.mjs` reusing the imported `VIEWPORT_WIDTHS` and `waitForReady`. Enumerate ~4 representative primary pages (per D-15, ones that between them exercise every locked primitive at worst case). Implement the tiered D-16 catalog: (1) no document h-overflow (`document.documentElement.scrollWidth <= innerWidth + 1`) at ALL 6 widths on the ~4 pages (cheap); (2) no essential element clipped off-viewport over a CURATED selector set (interactive controls, primary headings, action bars — NOT `*`, which trips on the off-canvas drawer `translateX(-100%)` and intentional ellipsis) at 320/375/768 only (expensive); (3) table overflow CONTAINED not leaked — at 320/375 the `.scoria-table__viewport` MAY have inner `scrollWidth>clientWidth` while assertion (1) still passes (that pairing is the "scroll trapped in container not document" proof); (4) `:mobile_summary` swap correctness — below 768 mobile summaries visible + viewport hidden for `--has-summary` tables, inverse ≥768; (5) no non-modal fixed/floating region covering nav — `boxesIntersect(.scoria-toast-region, nav)` false (nav = `.scoria-mobile-topbar` <768 / `.scoria-sidebar` ≥768), EXCLUDING the command palette, mobile-nav drawer, scrims, AND the sticky approval footer (that is D-11's SC 2.4.11, D-17); (6) min 24px target (WCAG 2.5.8) at ≤375 as a regression floor; (7) no trapped scroll — any `overflow:auto|scroll` container with `scrollWidth>clientWidth` must be keyboard-focusable (the table viewport `tabindex=0` already satisfies this — confirm). Apply the two-bucket rule (D-04): assertions on the already-clean surfaces (the generalized `phase16_parity` baseline) may throw; assertions on not-yet-verified surfaces that could surface new layout defects are WARNING-GRADE non-throwing collectors (`console.warn`+`testInfo.attach()`, no `test.fail()`/`expect.soft`) until any in-scope fix lands. Fix in-scope layout defects (CSS media-query/layout, no vocabulary change); register out-of-scope-boundary defects in `40-GAP-REGISTER.md` with viewport/repro/boundary.
  </action>
  <verify>
    <automated>npx --prefix priv/dev playwright test responsive_scan.spec.mjs  # scoped per-task run across 6 widths against a running make dev; collectors warn+attach, hard assertions gate; full mix scoria.ui.e2e reserved for the per-wave sampling tier</automated>
  </verify>
  <acceptance_criteria>
    `responsive_scan.spec.mjs` implements D-16 (1)-(7) tiered per D-15 across ~4 primary pages, imports the shared `boxesIntersect`, excludes the sticky footer + by-design overlays from (5), and honors the two-bucket rule (throwing only on already-clean surfaces; warning-grade collectors elsewhere). In-scope defects fixed without vocabulary change; out-of-scope registered.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: reduced_motion.spec.mjs — computed duration collapse (+ factor isInstantDuration helper)</name>
  <read_first>
    - priv/dev/e2e/phase16_parity.spec.mjs (`isInstantDuration` at line 23 — accepts '0s'/'0.001ms'/'1e-06s' comma-split; `emulateMedia({reducedMotion:'reduce'})` pattern at 428/451/472 asserting collapsed `transitionDuration`/`animationDuration`)
    - assets/css/05-motion.css (reduced-motion kill switch ~71: forces `animation-duration:0.001ms !important`, `animation-iteration-count:1`, `transition-duration:0.001ms !important`), assets/css/04-components.css (`scoria-skeleton-pulse ... infinite` at 1610 — the infinite animation the kill switch must tame)
    - 40-RESEARCH.md § Don't Hand-Roll (reuse `isInstantDuration`, do not hardcode '0s'), § Anchors
    - 40-CONTEXT.md D-19 (e2e reduced-motion proof)
  </read_first>
  <action>
    Factor the proven `isInstantDuration` predicate out of `phase16_parity.spec.mjs` into a shared `priv/dev/e2e/lib/instant_duration.mjs` module (keep it accepting '0s', '0.001ms', and Chromium's '1e-06s' scientific-notation serialization, comma-split for shorthand — do NOT hardcode a '0s' string equality, research Don't-Hand-Roll). Have `phase16_parity.spec.mjs` import it (no behavior change) and create `priv/dev/e2e/reduced_motion.spec.mjs` that: sets `emulateMedia({ reducedMotion: 'reduce' })`, then asserts computed `animationDuration`/`transitionDuration` collapse (via `isInstantDuration`) on representative motion surfaces INCLUDING a skeleton element (proves even the `infinite` skeleton at line 1610 is tamed to `0.001ms`/iteration-count 1 by the kill switch). Use `waitForReady`. This is a genuinely-provable hard assertion (the kill switch is authoritative CSS) — throwing `expect()` is correct here.
  </action>
  <verify>
    <automated>npx --prefix priv/dev playwright test reduced_motion.spec.mjs phase16_parity.spec.mjs  # scoped per-task run against a running make dev (phase16_parity confirms the isInstantDuration extraction is behavior-preserving); full mix scoria.ui.e2e reserved for the per-wave sampling tier</automated>
  </verify>
  <acceptance_criteria>
    `lib/instant_duration.mjs` exports the tolerant duration predicate; `phase16_parity.spec.mjs` imports it with no behavior change; `reduced_motion.spec.mjs` proves `animationDuration`/`transitionDuration` collapse under reduced-motion including the infinite skeleton. All green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Widen shots.mjs to the 6-width matrix for contact-sheet human evidence</name>
  <read_first>
    - priv/dev/shots.mjs (current `VIEWPORTS = [{desktop 1280×900},{mobile 375×812}]` at lines 176-178 — widen to the 6-width matrix)
    - priv/dev/contact_sheet.mjs (renders dated PNG capture dirs into an HTML before/after grid — the human-review surface; plain Node fs/path, no Playwright dep)
    - 40-CONTEXT.md D-14 (screenshots never gate — human evidence + Phase-41 baseline seed)
  </read_first>
  <action>
    Widen `priv/dev/shots.mjs`'s `VIEWPORTS` from the current 2 entries to the 6-width matrix (320, 375, 768, 1024, 1440, 1920 — heights sensible per width) so the capture covers the same widths the responsive scan asserts, and confirm `contact_sheet.mjs` renders them as human-reviewable evidence. Screenshots are evidence ONLY — do NOT wire any pixel-diff assertion or gate (D-14; that is a deferred Phase-41/VISUAL-CI concern). This produces the visual artifact a human eyeballs for clipping/squish and seeds the Phase-41 screenshot baseline.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('/Users/jon/projects/scoria/priv/dev/shots.mjs','utf8'); const w=[320,375,768,1024,1440,1920].filter(x=>s.includes(String(x))); if(w.length<6)throw new Error('shots.mjs missing widths: '+w.join(',')); console.log('shots.mjs 6-width matrix OK')"</automated>
  </verify>
  <acceptance_criteria>
    `shots.mjs` captures all 6 viewport widths; `contact_sheet.mjs` renders them as human evidence; no pixel-diff gate introduced (screenshots inform, never gate).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewport width → layout | Real-browser geometry (`scrollWidth`/`getBoundingClientRect`) is the only truth for overflow/clipping/occlusion. |
| reduced-motion media → computed style | The CSS kill switch must win (unlayered `!important`); proof requires `getComputedStyle` in a real browser. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-09 | Tampering (premature gate) | responsive collectors | high | mitigate | Two-bucket rule: warning-grade non-throwing collectors on surfaces that could surface not-yet-fixed layout defects; hard `expect()` only on the already-clean generalized baseline — no bare `expect()` red-walls the gate ahead of a fix (D-04). |
| T-40-10 | Spoofing (false-pass duration) | reduced-motion assertion | medium | mitigate | Reuse `isInstantDuration` (accepts 0s/0.001ms/1e-06s) instead of naive '0s' equality — Chromium's scientific-notation serialization cannot silently false-pass. |
| T-40-11 | Tampering | screenshot capture | low | accept | Screenshots are human-evidence only, never a gate (D-14) — no CI decision depends on pixel output. |
</threat_model>

<verification>
- `responsive_scan.spec.mjs` runs D-16 (1)-(7) tiered across ~4 pages; collectors warn+attach, hard assertions gate; `boxesIntersect` imported.
- `reduced_motion.spec.mjs` proves duration collapse incl. the infinite skeleton; `phase16_parity.spec.mjs` still green after the `isInstantDuration` extraction.
- `shots.mjs` node assertion confirms all 6 widths; contact sheet renders.
- Any in-scope layout fix introduces no vocabulary change; out-of-scope defects appear in `40-GAP-REGISTER.md`.
</verification>

<success_criteria>
RESP-01 is proven across 6 widths on ~4 primary pages (D-16 catalog, tiered, with the correct exclusions) and MOTION-01's reduced-motion collapse is proven; screenshots provide human evidence without gating; layout defects are fixed in-scope or registered out-of-scope.
</success_criteria>

<output>
Create `.planning/phases/40-accessibility-motion-and-responsive-proof/40-05-SUMMARY.md` when done.
</output>
