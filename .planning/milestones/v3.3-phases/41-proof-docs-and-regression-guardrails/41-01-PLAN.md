---
phase: 41-proof-docs-and-regression-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria_web/live/review_queue_live.ex
  - lib/scoria_web/live/prompt_live/release_workbench_live.ex
  - lib/scoria_web/ui.ex
  - test/scoria_web/live/review_queue_live_test.exs
  - test/scoria_web/live/prompt_live/release_workbench_live_test.exs
  - test/scoria_web/a11y_structural_guard_test.exs
autonomous: true
requirements: [PROOF-01, PROOF-03]
must_haves:
  truths:
    - "Sending 'dismiss_candidate' to the review queue with no candidate selected leaves the LiveView alive and shows a graceful notice instead of crashing."
    - "ReleaseWorkbenchLive.mount/2 assigns a default :origin_context so render/1 never depends on handle_params/3 having run first."
    - "The .scoria-table__viewport scroll region carries an aria-label so screen-reader users know the region is scrollable."
  artifacts:
    - "lib/scoria_web/live/review_queue_live.ex (dismiss_candidate handle_event has an exhaustive else)"
    - "lib/scoria_web/live/prompt_live/release_workbench_live.ex (mount/2 defensively assigns :origin_context)"
    - "lib/scoria_web/ui.ex (.scoria-table__viewport carries aria-label)"
    - "test/scoria_web/live/review_queue_live_test.exs (CR-01 regression test)"
    - "test/scoria_web/live/prompt_live/release_workbench_live_test.exs (WR-04 regression test)"
    - "test/scoria_web/a11y_structural_guard_test.exs (tightened to assert the aria-label)"
  key_links:
    - "Each fix is locked by a regression test that fails on today's source and passes only after the fix."
  prohibitions:
    - "Do NOT touch WR-01 (approvals_live/index.ex:663-689), WR-02 (:250), or any IN-* item — those stay deferred to the gap register Section B2 (D-16b scope fence)."
    - "Do NOT change any public attr/slot API, the tone/size/state vocabulary, or the scoria_dashboard macro."
    - "Do NOT add a new dependency, primitive, or CSS class."
---

<objective>
The owner-approved D-16b bounded fix lane. Fix exactly the two CRASH-class LiveView bugs that
Phases 39/40 surfaced but never fixed, plus the D-18 table-scroll aria-label that rides this lane —
each locked by a regression test (or tightened guard) that is red on today's source and green after.
A "proof" milestone must not ship known crashes on the very operator flows it claims to prove.

Purpose: Move CR-01(39-review), WR-04, and D-18 from "surfaced-but-unfixed" into gap-register
Section A (fixed-in-v3.3). This is the ONLY remediation Phase 41 performs (D-01 / D-16b).
Output: Two crash fixes + one aria-label, each with a locked regression test; no scope beyond these.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-proof-docs-and-regression-guardrails/41-CONTEXT.md
@.planning/phases/41-proof-docs-and-regression-guardrails/41-RESEARCH.md
@.planning/phases/41-proof-docs-and-regression-guardrails/41-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix CR-01(39-review) dismiss_candidate crash + regression test</name>
  <files>lib/scoria_web/live/review_queue_live.ex, test/scoria_web/live/review_queue_live_test.exs</files>
  <read_first>
    - lib/scoria_web/live/review_queue_live.ex:53-64 (the buggy `with` in handle_event("dismiss_candidate", ...) — no `else`)
    - test/scoria_web/live/review_queue_live_test.exs:1-40 (Router/Endpoint/import boilerplate) and the `test_conn/0` helper defined near :252 (grep it — do NOT redefine it)
    - 41-RESEARCH.md Pattern 1 + Code Examples (CR-01 fix); 41-PATTERNS.md "review_queue_live.ex" section (verbatim else clause)
  </read_first>
  <behavior>
    - RED first: add the regression test and confirm it crashes the LiveView on today's source (the `with` returns a non-`{:noreply, socket}` value when @selected_candidate is nil).
    - GREEN: after adding the `else` clause, `render_click(view, "dismiss_candidate", %{})` returns HTML containing the graceful notice and the process stays alive.
  </behavior>
  <action>
    Add an `else` clause to the `with` inside handle_event("dismiss_candidate", _params, socket) that
    returns a valid `{:noreply, socket}` assigning a graceful notice (e.g. "Could not dismiss this
    candidate. Refresh and try again."). Copy the else shape verbatim from 41-PATTERNS.md — it already
    matches 39-REVIEW.md's own suggested fix. This is a private handle_event/3 body change only; it
    crosses no locked public boundary (per D-16b). Then add the regression test to
    review_queue_live_test.exs using RESEARCH Pattern 1: `render_click(view, "dismiss_candidate", %{})`
    in view-form (pushes the event directly, bypassing the DOM since the dismiss button is not rendered
    without a selection). Reuse the file's existing `test_conn/0` helper and `live(test_conn(),
    "/scoria/reviews")` (no candidate fixture needed — @selected_candidate defaults to nil). Assert the
    returned HTML matches the exact notice literal you chose in the fix.
  </action>
  <verify>
    <automated>mix test test/scoria_web/live/review_queue_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Regression test fails on the pre-fix source (LiveView crashes / bad return) and passes after the else clause.
    - `render_click(view, "dismiss_candidate", %{})` returns HTML containing the graceful notice; the LiveView process is not killed.
    - No other handle_event clause changed; no new dependency/primitive/CSS added.
  </acceptance_criteria>
  <done>CR-01(39-review) is fixed and locked by a red→green regression test in review_queue_live_test.exs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix WR-04 release_workbench mount/2 :origin_context KeyError + regression test</name>
  <files>lib/scoria_web/live/prompt_live/release_workbench_live.ex, test/scoria_web/live/prompt_live/release_workbench_live_test.exs</files>
  <read_first>
    - lib/scoria_web/live/prompt_live/release_workbench_live.ex:16-58 (mount/2, never assigns :origin_context) and :178 (render/1 reads @origin_context unconditionally)
    - test/scoria_web/live/prompt_live/release_workbench_live_test.exs (existing `setup` block that provisions draft/active/dataset/spec fixtures — reuse them)
    - 41-RESEARCH.md Pattern 2 + Assumption A1 + Open Question 1 (bare-socket spike + source-scan fallback); 41-PATTERNS.md "release_workbench_live.ex" section
  </read_first>
  <behavior>
    - The regression test MUST distinguish "mount assigns a safe default" from "mount relies on handle_params". A full `live/2` navigation test would falsely pass both before AND after the fix (handle_params/3 always runs before first render under the real router) — so it is NOT a valid regression test here (RESEARCH Pitfall 1).
    - Preferred: call mount/2 + render/1 directly on a bare `%Phoenix.LiveView.Socket{}` (Pattern 2). Before fix: render raises KeyError :origin_context. After fix: render returns a `%Phoenix.LiveView.Rendered{}`.
  </behavior>
  <action>
    In mount/2, defensively `assign(socket, :origin_context, nil)` before returning `{:ok, socket}`;
    handle_params/3 (unchanged) still overrides it later. Then add the regression test per RESEARCH
    Pattern 2 — call `ReleaseWorkbenchLive.mount(%{"id" => draft.id}, session, %Phoenix.LiveView.Socket{})`
    then `ReleaseWorkbenchLive.render(socket.assigns)`, reusing the existing setup block's `draft`
    fixture for a valid id (mount/2 calls PromptRegistry.get_prompt_template!/1 which needs a real
    record — Open Question 1). Honor Assumption A1: SPIKE the bare-socket + Phoenix.Component.assign/3
    compatibility against the pinned phoenix_live_view 1.1.30 FIRST. If the bare struct proves brittle
    (fails to compile/run for reasons unrelated to the bug), fall back to the source-scan guard the
    researcher provided (File.read! + regex asserting mount/2's body contains the origin_context default
    assign), mirroring the guard-suite idiom — weaker but zero-risk. Record which technique you used in
    the plan SUMMARY.
  </action>
  <verify>
    <automated>mix test test/scoria_web/live/prompt_live/release_workbench_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Regression test fails on pre-fix source (direct-callback: KeyError; or source-scan: missing assign) and passes after the fix.
    - mount/2 assigns :origin_context; handle_params/3 unchanged.
    - The chosen technique (direct-callback vs A1 source-scan fallback) is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>WR-04 is fixed and locked by a regression test that actually distinguishes fixed from unfixed (not a false-pass live/2 nav test).</done>
</task>

<task type="auto">
  <name>Task 3: D-18 — add .scoria-table__viewport aria-label + tighten the a11y guard to assert it</name>
  <files>lib/scoria_web/ui.ex, test/scoria_web/a11y_structural_guard_test.exs</files>
  <read_first>
    - lib/scoria_web/ui.ex:~1320 (`<div class="scoria-table__viewport" tabindex="0">` — no aria-label today)
    - test/scoria_web/a11y_structural_guard_test.exs:116-129 ("table scroll viewport stays keyboard-reachable" test) and its `window_after/2` helper (binary_part/3, UTF-8-safe — RESEARCH Pitfall 2)
    - 41-RESEARCH.md D-18 Code Examples; 41-PATTERNS.md "ui.ex" + "a11y_structural_guard_test.exs" sections
  </read_first>
  <action>
    Add an internal `aria-label` to the `.scoria-table__viewport` div in ui.ex (exact copy is your
    discretion per D-11, e.g. "Scrollable table content"). This is static HEEx markup — NOT a public
    attr/slot change (D-18). Then tighten the existing tabindex test in a11y_structural_guard_test.exs
    to ALSO assert the aria-label is present, attribute-order-agnostic (two `Regex.match?` alternations
    like the existing tabindex check). If any regex-index windowing is needed, reuse the file's own
    `window_after/2` helper (binary_part/3, not String.slice/3) to stay UTF-8-safe.
  </action>
  <verify>
    <automated>mix test test/scoria_web/a11y_structural_guard_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The tightened guard fails on pre-fix ui.ex (no aria-label) and passes after the aria-label is added.
    - .scoria-table__viewport retains its tabindex="0" and gains an aria-label.
    - No public attr/slot, tone/size/state vocabulary, or CSS class changed.
  </acceptance_criteria>
  <done>D-18 aria-label is applied and the a11y structural guard now asserts it (genuine PROOF-03 hardening).</done>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- Fixed: `lib/scoria_web/live/review_queue_live.ex` — exhaustive `else` on the dismiss_candidate `with`.
- Fixed: `lib/scoria_web/live/prompt_live/release_workbench_live.ex` — mount/2 defensively assigns `:origin_context`.
- Fixed: `lib/scoria_web/ui.ex` — `.scoria-table__viewport` gains an `aria-label`.
- New test: `test/scoria_web/live/review_queue_live_test.exs` — "dismiss_candidate with no selected candidate does not crash the LiveView".
- New test: `test/scoria_web/live/prompt_live/release_workbench_live_test.exs` — "mount/2 assigns a default :origin_context …" (or A1 source-scan fallback).
- Tightened guard: `test/scoria_web/a11y_structural_guard_test.exs` — asserts `.scoria-table__viewport` carries an `aria-label`.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LiveView client → server callback | A client (or stale/replayed DOM, concurrent tab) can push any registered event/params to `handle_event/3`; the server must always return a valid callback shape. |
| LiveView mount/render lifecycle | `render/1` reads assigns that must exist regardless of callback ordering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Denial of Service | `review_queue_live.ex` handle_event("dismiss_candidate") non-exhaustive `with` | high | mitigate | Task 1 adds an exhaustive `else` returning a valid `{:noreply, socket}`; a client can no longer crash the shared LiveView by pushing the event with no selection. Locked by red→green regression test. |
| T-41-02 | Denial of Service | `release_workbench_live.ex` render/1 reading unassigned `@origin_context` | high | mitigate | Task 2 assigns a safe default in mount/2 so render never KeyErrors on a callback-order change. Locked by a direct-callback regression test (or A1 source-scan guard). |
| T-41-03 | Information Disclosure (a11y) | `.scoria-table__viewport` scroll region without an accessible name | low | mitigate | Task 3 adds an aria-label; guard asserts it. Not a security threat proper — recorded honestly as a low a11y-integrity item. |

No new packages are installed this phase, so there is no supply-chain (T-*-SC) surface.
</threat_model>

<verification>
- `mix test test/scoria_web/live/review_queue_live_test.exs test/scoria_web/live/prompt_live/release_workbench_live_test.exs test/scoria_web/a11y_structural_guard_test.exs` is green.
- Each of the three regression checks demonstrably fails on the pre-fix source (verified during RED) and passes after.
- No file outside `files_modified` is touched; WR-01/WR-02/IN-* remain untouched (deferred to Plan 05 Section B2).
</verification>

<success_criteria>
CR-01(39-review) and WR-04 no longer crash their operator flows, D-18's aria-label is applied, and all
three are locked by regression tests/guards — making them honestly eligible for gap-register Section A.
</success_criteria>

<output>
Create `.planning/phases/41-proof-docs-and-regression-guardrails/41-01-SUMMARY.md` when done.
Record: the exact notice literal chosen for CR-01, and which WR-04 technique (direct-callback vs A1
source-scan fallback) was used — Plan 05's gap register Section A cites both.
</output>
