---
phase: 41-proof-docs-and-regression-guardrails
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/scoria_web/single_header_rendered_guard_test.exs
autonomous: true
requirements: [PROOF-03]
must_haves:
  truths:
    - "A rendered-DOM guard proves that region titles never restate the page title in the actual rendered output of routed pages — closing the gap the source-scan guard self-declares it cannot see."
  artifacts:
    - "test/scoria_web/single_header_rendered_guard_test.exs (net-new rendered-DOM Floki assertion, async: false)"
  key_links:
    - "The new guard renders each routed page for real (live/2) and Floki-compares the sanctioned header's rendered title text against every region :title slot's rendered text — the dynamic/interpolated case single_header_guard_test.exs:28-30 defers to Phase-41 PROOF-03."
  prohibitions:
    - "Do NOT introduce a shared ConnCase/test helper — every existing *_live_test.exs duplicates its own Router+Endpoint; mirror that (RESEARCH anti-pattern)."
    - "Do NOT invent CSS selectors — verify the real class names against ui.ex before landing (Assumption A2)."
    - "Do NOT modify ui.ex or any page LiveView — this is a proof-only guard, no remediation (D-01)."
---

<objective>
The single required net-new drift guard (D-06 / GAP-A). `single_header_guard_test.exs:28-30`
self-declares that its rendered-DOM semantic-restatement check is deferred to Phase-41 PROOF-03 —
today it is a pure `File.read!`+Regex source scan that cannot see dynamic/interpolated titles.
This plan adds the rendered-DOM LiveViewTest+Floki assertion that closes the 8th of PROOF-03's eight
named regressions (the other 7 already have live blocking guards per D-05).

Purpose: Make PROOF-03 fully guarded — the redundant single-region-header regression now has a
rendered-DOM proof, not just a static-literal scan.
Output: One net-new `async: false` guard module that renders each routed page and asserts no region
title restates the page title.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-proof-docs-and-regression-guardrails/41-CONTEXT.md
@.planning/phases/41-proof-docs-and-regression-guardrails/41-RESEARCH.md
@.planning/phases/41-proof-docs-and-regression-guardrails/41-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the D-06 rendered-DOM region-title-restatement guard</name>
  <files>test/scoria_web/single_header_rendered_guard_test.exs</files>
  <read_first>
    - test/scoria_web/single_header_guard_test.exs (full — especially the moduledoc :1-31 self-declared deferral at :28-30, and the `page_files/0`/`@excluded`/`@dialog_scoped_fragments` conventions)
    - test/scoria_web/live/review_queue_live_test.exs:1-40 (Router+Endpoint+ErrorView boilerplate to mirror; the `test_conn/0` helper shape)
    - lib/scoria_web/ui.ex around :260 (`scoria-pagehead__title` confirmed) and page_header/1, object_header/1, stub_page/1, panel/1 `:title` slot markup — VERIFY the exact rendered class names before finalizing selectors (Assumption A2)
    - lib/scoria_web/router.ex:37-49 (the 12 live routes; index/static routes vs param routes :id/:screen)
    - 41-RESEARCH.md Pattern 3 + Open Question 2; 41-PATTERNS.md "single_header_guard_test.exs or sibling" section
  </read_first>
  <action>
    Create a NEW sibling file `test/scoria_web/single_header_rendered_guard_test.exs` (own module,
    `use ExUnit.Case, async: false` — it needs a real LiveView render and the existing
    single_header_guard module is `async: true`/no-DB, so keep them separate rather than mixing async
    settings in one module). Mirror the per-file Router+Endpoint+ErrorView boilerplate from
    review_queue_live_test.exs (do NOT introduce a shared ConnCase — RESEARCH anti-pattern). For each
    routed page: `live(test_conn(), path)`, `Floki.parse_document!(html)`, extract the sanctioned page
    title text (page_header/object_header/stub_page title class), extract every region `:title` slot's
    rendered text, and `refute` any region title case-insensitively equals the page title. FIRST verify
    the exact CSS class selectors against ui.ex's real rendered markup (Assumption A2) — the RESEARCH
    example is shape-only, not final selector strings. Cover the routes that render without heavy
    fixtures (the index/static live routes: /, /approvals, /reviews, /datasets, /workflows, /connectors,
    /incidents, /eval_specs, /prompts); document any param-route skips (/workflows/:id, /incidents/:id,
    /prompts/:id/release, /coming/:screen) explicitly in the moduledoc, mirroring dev_lab_boundary_test.exs's
    "honesty caveat" precedent (Open Question 2). The moduledoc must state this guard closes the
    single_header_guard_test.exs:28-30 deferral.
  </action>
  <verify>
    <automated>mix test test/scoria_web/single_header_rendered_guard_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - New guard renders each covered routed page and asserts (via Floki over rendered DOM) that no region title restates the page title.
    - Selectors were verified against ui.ex's actual markup (A2), not copied blind from the research sketch.
    - Any skipped param-routes are documented in the moduledoc with rationale.
    - The guard is green against current source (Phase 40 already remediated real defects — this locks the invariant), and would go red if a page reintroduced a restating region title.
  </acceptance_criteria>
  <done>PROOF-03's 8th regression (redundant single-region header) has a rendered-DOM blocking guard; the source-scan guard's self-declared deferral is closed.</done>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- New guard module: `ScoriaWeb.SingleHeaderRenderedGuardTest` in `test/scoria_web/single_header_rendered_guard_test.exs` (rendered-DOM region-title-restatement assertion; `async: false`; per-route parameterized tests; documented param-route skips).
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a test-only proof artifact. It executes no production code path and exposes no new runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-04 | Tampering (regression integrity) | PROOF-03 redundant-header invariant previously guarded by source-scan only | low | mitigate | The net-new rendered-DOM guard closes the dynamic/interpolated blind spot, so a future page can no longer silently reintroduce a restating region title past the source-scan. Test-only; no product code changes. |

No new packages are installed this phase — no supply-chain (T-*-SC) surface.
</threat_model>

<verification>
- `mix test test/scoria_web/single_header_rendered_guard_test.exs` is green.
- The guard uses real class selectors verified against ui.ex (A2), not the shape-only sketch.
- No production code (ui.ex, page LiveViews) was modified; no shared ConnCase introduced.
</verification>

<success_criteria>
All eight PROOF-03 regressions now have a live blocking guard; the one gap (GAP-A) is closed by a
rendered-DOM assertion that a source scan structurally cannot provide.
</success_criteria>

<output>
Create `.planning/phases/41-proof-docs-and-regression-guardrails/41-02-SUMMARY.md` when done.
Record: the final CSS selectors used (post-A2 verification) and any param-routes skipped with rationale
— Plan 03's design_system.md accessibility/headers sections and Plan 05's evidence manifest reference this guard.
</output>
