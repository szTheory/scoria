---
phase: 41-proof-docs-and-regression-guardrails
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/design_system.md
  - docs/MAINTAINERS.md
  - test/scoria_web/design_system_doc_contract_test.exs
  - .github/workflows/ci-verify.yml
  - test/scoria/ci_policy_contract_test.exs
autonomous: true
requirements: [PROOF-02]
must_haves:
  truths:
    - "A maintainer can read one docs/design_system.md that defines every named design-system convention (BEM, tokens, page headers, stats, overlays, evidence/code, copy controls, fixtures, motion, accessibility, screenshot-proof + drift-guard roster) and, for each, names the real drift guard that enforces it."
    - "The doc cannot silently drift from the guards: a contract test fails if a named guard path disappears, a cited token vanishes from 02-tokens.css, or a required section heading is dropped."
    - "The doc-contract test runs in the CI policy lane-contract step and is asserted present by ci_policy_contract_test.exs, mirroring the docker-DX precedent."
  artifacts:
    - "docs/design_system.md (11 sections, Rule → SSOT → Guard → Example)"
    - "docs/MAINTAINERS.md (one cross-link line in the catalog section)"
    - "test/scoria_web/design_system_doc_contract_test.exs (3 checks, modeled 1:1 on docker_dx_doc_contract_test.exs)"
    - ".github/workflows/ci-verify.yml (design_system_doc_contract added to the policy lane-contract step)"
    - "test/scoria/ci_policy_contract_test.exs (@design_system_doc_contract module attr + lane-step assertion)"
  key_links:
    - "Every doc section names a REAL existing guard → PROOF-02 and PROOF-03 become a matched pair, kept honest by the doc-contract test."
  prohibitions:
    - "Do NOT add docs/design_system.md to ExDoc `extras` or to `package.files` (mirror docker_dev_dx.md / uat_automation.md; it documents dev-only dev/lab/**, test/**, assets/css/** that never ship)."
    - "Do NOT extend MAINTAINERS.md with the content, and do NOT fan out to multiple doc files (D-08)."
    - "The doc documents EXISTING conventions only — it invents NO new rule and names no guard that does not exist (D-10)."
    - "Do NOT go heavier than the 3 checks (no token-by-token or DOM diff) (D-12)."
---

<objective>
The maintainer design-system conventions doc (PROOF-02) plus its anti-drift contract. One new
`docs/design_system.md` with 11 sections, each documenting an existing SSOT and naming the real drift
guard that enforces it — the "matched pair" that ties PROOF-02 to PROOF-03. A doc-contract test
(1:1 on the proven docker-DX precedent) keeps the doc and the guards honest, wired into the CI policy
lane exactly like `@docker_dx_doc_contract`.

Purpose: A durable, drift-proof maintainer reference for the design-system conventions Phases 36-40
established — documenting existing rules, inventing none.
Output: The doc, its cross-link, the contract test, and its CI wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-proof-docs-and-regression-guardrails/41-CONTEXT.md
@.planning/phases/41-proof-docs-and-regression-guardrails/41-RESEARCH.md
@.planning/phases/41-proof-docs-and-regression-guardrails/41-PATTERNS.md
@docs/docker_dev_dx.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/design_system.md (11 sections) + MAINTAINERS.md cross-link</name>
  <files>docs/design_system.md, docs/MAINTAINERS.md</files>
  <read_first>
    - docs/docker_dev_dx.md and docs/uat_automation.md (sibling one-topic-per-file idiom)
    - docs/MAINTAINERS.md:1-3 (cross-link precedent at :3) and :255-336 (catalog section — where the new cross-link line goes)
    - mix.exs:123-179 (confirm docker_dev_dx.md / uat_automation.md are absent from BOTH `extras` and `package.files` — mirror that exclusion; add NOTHING to mix.exs)
    - The SSOT→guard mapping in 41-CONTEXT.md D-05/D-10 and 41-RESEARCH.md D-10/D-11: tokens → brandbook/tokens.json + assets/css/02-tokens.css → token_contrast_guard_test.exs/toast_opacity_guard_test.exs; headers → ui.ex object_header/1/page_header/1 → single_header_guard_test.exs (+ the new single_header_rendered_guard_test.exs from Plan 02); motion → assets/css/05-motion.css → motion_drift_guard_test.exs + reduced_motion.spec.mjs; fixtures → dev/lab/fixtures.ex + priv/repo/dev_seed.exs → dev_lab_boundary_test.exs; stats → ui.ex overview_stats/1 (:357) → ui_component_test.exs; copy controls/oversized copy → ui_component_test.exs:1632/1645/1657; density → ui_component_test.exs:1273-1300; palette → ds06_drift_guard_test.exs; icon buttons/table viewport → a11y_structural_guard_test.exs; toasts → toast_opacity_guard_test.exs; screenshot-proof → priv/dev/shots.mjs + priv/shots/contact_sheet_index.md
    - 41-PATTERNS.md "docs/design_system.md" + "docs/MAINTAINERS.md" sections
  </read_first>
  <action>
    Write docs/design_system.md with exactly 11 sections (BEM/selectors, tokens, page headers, stats,
    overlays, evidence/code, copy controls, fixtures, motion, accessibility, screenshot-proof +
    drift-guard roster). Each section is a tight ~6-12-line block in the D-11 fixed 4-part shape:
    Rule (one sentence) → SSOT (the file[s], file:line where useful) → Guard (the enforcing test + its
    exact `mix test <path>` command) → Example (one REAL snippet or class name, never invented). Every
    section MUST name a real, existing guard (verify each path exists before citing it) and must
    document existing convention only — for a convention with no enforcing guard (e.g. pure BEM naming),
    say "convention, guarded only for palette leakage" rather than imply a new enforced rule (D-10).
    Keep section headings stable and greppable (the contract test in Task 2 pins them). Then add ONE
    cross-link line to MAINTAINERS.md's catalog section (:255-336), mirroring the :3 docker-DX
    cross-link's phrasing/format. Do NOT add design_system.md to mix.exs extras or package.files.

    Comment-text discipline: the contract test (Task 2) will negative-check nothing here, but keep any
    "what NOT to do" phrasing conceptual — do not paste a literal that a guard elsewhere greps for.
  </action>
  <verify>
    <automated>test -f docs/design_system.md && grep -c '^## ' docs/design_system.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/design_system.md exists with 11 `## ` sections, each in Rule → SSOT → Guard → Example shape.
    - Every named guard path actually exists on disk; every cited token actually appears in 02-tokens.css; every example is a real snippet/class from the repo.
    - MAINTAINERS.md has exactly one new cross-link line in its catalog section.
    - design_system.md is NOT in mix.exs `extras` or `package.files`.
  </acceptance_criteria>
  <done>The maintainer conventions doc exists, documents 11 existing conventions each paired to a real guard, and is cross-linked from MAINTAINERS.md.</done>
</task>

<task type="auto">
  <name>Task 2: Author design_system_doc_contract_test.exs (3 checks, 1:1 on docker-DX precedent)</name>
  <files>test/scoria_web/design_system_doc_contract_test.exs</files>
  <read_first>
    - test/scoria/docker_dx_doc_contract_test.exs (full — the 1:1 model: async: true, File.read! only, no DB, String.contains?/regex)
    - docs/design_system.md (authored in Task 1 — the file this contract reads)
    - 41-RESEARCH.md Pattern 4; 41-PATTERNS.md "design_system_doc_contract_test.exs" section
  </read_first>
  <action>
    Create test/scoria_web/design_system_doc_contract_test.exs modeled 1:1 on
    docker_dx_doc_contract_test.exs (`use ExUnit.Case, async: true`, only `File.read!`, no DB, no
    compile-path risk). Exactly three checks (D-12, no heavier): (1) every guard path the doc names
    (`Regex.scan` for `` `test/…_test.exs` `` backtick-fenced paths) resolves via `File.exists?` — the
    matched-pair enforcer; (2) a small sample of token names the doc cites (e.g. --scoria-text-subtle)
    still appear in assets/css/02-tokens.css; (3) the 11 section headings are pinned present via
    `String.contains?`. Failure messages must instruct "update the doc + this contract together."
  </action>
  <verify>
    <automated>mix test test/scoria_web/design_system_doc_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Contract test is `async: true`, DB-free, reads only files via `File.read!`.
    - All three checks pass against the Task 1 doc; the test goes red if a named guard path, a cited token, or a section heading disappears.
    - No fourth/heavier check (no token-by-token, no DOM diff).
  </acceptance_criteria>
  <done>The doc-contract test keeps design_system.md and the guard roster a matched, drift-proof pair.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the doc-contract test into the CI policy lane-contract step</name>
  <files>.github/workflows/ci-verify.yml, test/scoria/ci_policy_contract_test.exs</files>
  <read_first>
    - .github/workflows/ci-verify.yml:56 (the `mix test --no-start --warnings-as-errors …` policy lane-contract line to extend)
    - test/scoria/ci_policy_contract_test.exs:19-21 (module attrs incl. @docker_dx_doc_contract) and :654-663 (the lane-contract-step assertion test, incl. ordering)
    - 41-RESEARCH.md Pattern 4 (exact wiring points); 41-PATTERNS.md "ci_policy_contract_test.exs" + "ci-verify.yml" sections
  </read_first>
  <action>
    Add `test/scoria_web/design_system_doc_contract_test.exs` to the space-separated `mix test
    --no-start --warnings-as-errors` file list in ci-verify.yml's policy lane-contract step, adjacent to
    docker_dx_doc_contract_test.exs. Then in ci_policy_contract_test.exs add a
    `@design_system_doc_contract "test/scoria_web/design_system_doc_contract_test.exs"` module attr and
    add `assert lane_step =~ @design_system_doc_contract` to the existing lane-contract-step test (:654-663),
    mirroring the @docker_dx_doc_contract assertion and any existing index_of/2 ordering assertions.
    Keep `CI / ci-gate`, workflow topology, job names, `needs`, and matrices byte-stable — this is a
    single path addition to an existing step, not a topology change.
  </action>
  <verify>
    <automated>mix test --no-start --warnings-as-errors test/scoria/ci_policy_contract_test.exs test/scoria_web/design_system_doc_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - ci-verify.yml's lane-contract step now lists the design-system doc contract test path.
    - ci_policy_contract_test.exs asserts that path is present (and ordered, if the file asserts ordering) in the step.
    - No CI job name / topology / required-check name changed.
  </acceptance_criteria>
  <done>The design-system doc contract runs in the same CI-gated policy lane as the docker-DX contract and is asserted present.</done>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- New doc: `docs/design_system.md` (11 sections; Rule → SSOT → Guard → Example; out of ExDoc extras + package.files).
- Edited doc: `docs/MAINTAINERS.md` (one catalog cross-link line).
- New test module: `ScoriaWeb.DesignSystemDocContractTest` in `test/scoria_web/design_system_doc_contract_test.exs` (3 checks: guard-path existence, token-name sample, section-heading pins).
- New module attr: `@design_system_doc_contract` in `test/scoria/ci_policy_contract_test.exs` + a lane-step assertion.
- Edited CI config: `.github/workflows/ci-verify.yml` policy lane-contract step (one path added).
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs ↔ enforcing guards | Maintainer docs can silently drift from the code/guards they describe, giving false assurance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-05 | Tampering (documentation integrity) | docs/design_system.md drifting from the real guards/tokens/sections | medium | mitigate | The doc-contract test (Task 2) fails in the CI-gated policy lane if a named guard path, cited token, or section heading drifts — the doc and guards stay a matched pair. |
| T-41-06 | Tampering (CI wiring) | new contract test not actually running in CI | low | mitigate | ci_policy_contract_test.exs asserts the path is present in the lane-contract step, so the wiring itself is guarded (Task 3). |

The doc documents dev-only surfaces (`dev/lab/**`, `test/**`, `assets/css/**`) and is excluded from
`package.files` — it adds zero Hex/runtime footprint. No packages installed; no supply-chain surface.
</threat_model>

<verification>
- `mix test test/scoria_web/design_system_doc_contract_test.exs test/scoria/ci_policy_contract_test.exs` is green.
- `grep -c '^## ' docs/design_system.md` returns 11; each named guard path exists; each cited token is in 02-tokens.css.
- mix.exs is unchanged (doc excluded from extras + package.files by omission).
</verification>

<success_criteria>
PROOF-02 is satisfied: one maintainer doc defines all named conventions, each paired to a real guard,
kept drift-proof by a CI-gated contract test modeled on the proven docker-DX precedent.
</success_criteria>

<output>
Create `.planning/phases/41-proof-docs-and-regression-guardrails/41-03-SUMMARY.md` when done.
Record: the 11 section headings (verbatim) and the guard/token list the contract test pins —
Plan 05's evidence manifest references docs/design_system.md as the PROOF-02 artifact.
</output>
