# Milestone v2.8: CI Hardening & Post-Ship Hygiene

**Status:** ✅ SHIPPED 2026-05-29
**Phases:** 73
**Total Plans:** 0 (commit-driven thin closeout)

## Overview

Close deferred CI trust items from v2.6/v2.7 without reordering default closeout lanes. Land semantic fast-path WAE, knowledge WAE, and warning-inventory baseline policy gate in PR CI; fix post-publish smoke workflow; update operator gate map.

## Phases

### Phase 73: CI Hardening Closeout

**Goal:** Land SEM-CI-01, CI-KNOW-01, and CI-INV-01 in `ci-verify.yml` without widening `VerificationLanes.closeout_order/0`.

**Depends on:** v2.7 Hex publish on main (`0.1.0` / `v0.1.0`)

**Plans:** 0 formal plans — shipped via commit `d9f5fc7` with follow-up test stabilization (`6cd6118`, `0e95f52`, `4450cd1`)

**Requirements:** SEM-CI-01, CI-KNOW-01, CI-INV-01

**Success criteria:**

1. Semantic fast-path WAE runs in PR CI test job after closeout lanes; `closeout_order/0` unchanged.
2. `mix test.knowledge --warnings-as-errors` runs in CI after full-suite WAE.
3. `mix scoria.warning_inventory.check_baseline` runs in policy job; committed inventory JSON keeps `clusters` empty.
4. Post-publish smoke workflow checks out before setup-beam; v2.7 smoke attestation closed.
5. Operator gate map and `docs/connector_adoption.md` reflect v2.8 CI topology.

**Non-goals:** New runtime features, widening closeout order, semver `2.8.0`.

---

## Milestone Summary

**Key Decisions:**

- Semantic and knowledge lanes stay after closeout/full WAE — not in `closeout_order/0`.
- Inventory baseline gate lives in policy job (cheap failure left) alongside baseline expiry.
- Thin commit closeout acceptable for single-phase hygiene milestone; planning artifacts optional.

**Issues Resolved:**

- Deferred SEM-CI-01, CI-KNOW-01, CI-INV-01 from v2.6/v2.7 planning.
- Post-publish smoke checkout ordering fixed after v0.1.0 flip.

**Issues Deferred:**

- Phase 73 planning directory (VERIFICATION.md, SUMMARY.md) — evidence lives in CI contracts and operator docs.

**Technical Debt Incurred:**

- No formal `.planning/phases/73-*` directory; optional retroactive `/gsd-validate-phase 73`.

---

_For current project status, see `.planning/ROADMAP.md`_
