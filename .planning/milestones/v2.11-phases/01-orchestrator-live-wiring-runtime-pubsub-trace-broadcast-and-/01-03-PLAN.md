---
phase: 01-orchestrator-live-wiring-runtime-pubsub-trace-broadcast-and-
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - lib/scoria_web/live/orchestrator_live.ex
  - config/config.exs
  - test/scoria_web/live/orchestrator_live_integration_test.exs
  - lib/mix/tasks/scoria.test.semantic_fast_path.ex
  - test/mix/tasks/test.semantic_fast_path_test.exs
  - docs/adoption_lanes.md
autonomous: true
requirements: [ORCH-LIVE-01]
must_haves:
  truths:
    - "On connected?(socket), OrchestratorLive hydrates recent traces for tenant_id from ai_traces/ai_spans (default limit 25), seeds :traces stream, then merges PubSub deltas (D-119)."
    - "orchestrator_live_integration_test.exs proves Runtime.start_run → LiveView DOM without send/2 — required producer path, not optional telemetry.execute (D-129)."
    - "Reconnect trace hydrate test: render_disconnect → span persisted via Buffer flush → render_reconnect → trace visible from DB hydrate (D-119)."
    - "Integration test pinned in mix scoria.test.semantic_fast_path file list + contract test (D-129)."
    - "Reconnect test: render_disconnect → real approval → render_reconnect → modal from DB pending list on mount (D-131)."
    - "Adoption doc fragment documents session tenant_id and actor_id host contract; mix scoria.install unchanged (D-130)."
    - "VerificationLanes.closeout_order/0 unchanged — integration test in semantic lane only, not closeout (D-129)."
    - "Existing orchestrator_live_test.exs send/2 tests remain UI unit tier."
  artifacts:
    - path: "lib/scoria_web/live/orchestrator_live.ex"
      provides: "DB hydrate on connect"
      contains: "hydrate_traces"
    - path: "test/scoria_web/live/orchestrator_live_integration_test.exs"
      provides: "ORCH-LIVE-01 producer-path proof"
      contains: "Runtime.start_run"
    - path: "lib/mix/tasks/scoria.test.semantic_fast_path.ex"
      provides: "semantic lane pin for integration test"
      contains: "orchestrator_live_integration_test"
    - path: "docs/adoption_lanes.md"
      provides: "session tenant_id/actor_id host contract"
      contains: "session[\"tenant_id\"]"
  key_links:
    - from: "test/scoria_web/live/orchestrator_live_integration_test.exs"
      to: "lib/scoria/observe/telemetry.ex"
      via: "real span stop → PubSub → LiveView"
      pattern: "eventually"
    - from: "lib/scoria_web/live/orchestrator_live.ex"
      to: "lib/scoria/repo/trace.ex"
      via: "hydrate query on connected?"
      pattern: "tenant_id"
    - from: "lib/mix/tasks/scoria.test.semantic_fast_path.ex"
      to: "test/mix/tasks/test.semantic_fast_path_test.exs"
      via: "semantic_fast_path_test_files/0 contract"
      pattern: "orchestrator_live_integration_test"
---

<objective>
Close ORCH-LIVE-01 with DB hydrate on reconnect, integration tests proving production producer paths, semantic lane pins, and adoption doc session contract.

Purpose: PubSub alone loses messages during LiveView disconnect; integration tests must prove real runtime wiring without send/2 hollow props (D-119, D-129–D-131).
Output: Green semantic fast-path lane; phase gate ready for /gsd-verify-work.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-orchestrator-live-wiring-runtime-pubsub-trace-broadcast-and-/01-CONTEXT.md
@.planning/phases/01-orchestrator-live-wiring-runtime-pubsub-trace-broadcast-and-/01-RESEARCH.md
@.planning/phases/01-orchestrator-live-wiring-runtime-pubsub-trace-broadcast-and-/01-VALIDATION.md
@test/scoria/runtime_integration_test.exs
@test/scoria/observe/telemetry_test.exs
@test/support/scoria/integration_case.ex
@test/support/scoria/eventually.ex
@lib/mix/tasks/scoria.test.semantic_fast_path.ex
@lib/scoria/verification_lanes.ex
@docs/adoption_lanes.md

Session tenant_id in LiveView tests MUST match runtime tenant_id for PubSub alignment. Do NOT widen VerificationLanes.closeout_order/0. Do NOT add gallery /scoria landing smoke to closeout (deferred). mix scoria.install unchanged (D-130).
</context>

<tasks>

<task type="auto">
  <name>Task 01-03-01: DB hydrate recent traces on connected mount</name>
  <requirement_ids>ORCH-LIVE-01</requirement_ids>
  <files>lib/scoria_web/live/orchestrator_live.ex, config/config.exs</files>
  <read_first>
    lib/scoria_web/live/orchestrator_live.ex
    lib/scoria/repo/trace.ex
    lib/scoria/repo/span.ex
    lib/scoria/observe/trace_projection.ex
    .planning/phases/01-orchestrator-live-wiring-runtime-pubsub-trace-broadcast-and-/01-RESEARCH.md
  </read_first>
  <action>
1. Add private `hydrate_traces/2` called from `mount/3` when `connected?(socket)` **after** PubSub subscribe (D-119):
   - Limit: `Application.get_env(:scoria, :orchestrator_hydrate_trace_limit, 25)`
   - Query strategy (per RESEARCH): join recent spans where `attributes->>'tenant_id' = ^tenant_id`, group by trace_id, order by latest span end_time desc, limit N traces
   - Load spans per trace; build trace maps compatible with existing `trace_records` / `:traces` stream shape
   - Apply `TraceProjection.with_depths/1` to span lists
   - Seed `trace_records` map and `stream(:traces, traces, reset: true)` or equivalent stream seed

2. Pending approvals: ensure `load_operator_surface/1` on mount seeds inbox; for reconnect modal (D-131 prep), add helper `maybe_seed_active_approval/1` that sets `@active_approval` from first pending approval matching hybrid focus rules when socket reconnects with nil active_approval.

3. Idempotent merge: after hydrate, PubSub deltas continue upserting — do not duplicate traces if same id arrives via PubSub.

4. Add `config :scoria, :orchestrator_hydrate_trace_limit, 25` to `config/config.exs`.
  </action>
  <acceptance_criteria>
    - `rg -n 'hydrate_traces' lib/scoria_web/live/orchestrator_live.ex` returns match.
    - `rg -n 'orchestrator_hydrate_trace_limit' config/config.exs` returns match.
    - `rg -n 'orchestrator_hydrate_trace_limit' lib/scoria_web/live/orchestrator_live.ex` returns match (Application.get_env read).
    - `rg -n "attributes->>'tenant_id'" lib/scoria_web/live/orchestrator_live.ex` returns match OR equivalent tenant filter documented in function @doc.
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <done>OrchestratorLive hydrates recent tenant traces from DB on connect; pending approval seed helper ready for reconnect test.</done>
</task>

<task type="auto">
  <name>Task 01-03-02: Create orchestrator_live_integration_test.exs — live trace path</name>
  <requirement_ids>ORCH-LIVE-01</requirement_ids>
  <files>test/scoria_web/live/orchestrator_live_integration_test.exs</files>
  <read_first>
    test/scoria/runtime_integration_test.exs
    test/scoria/observe/telemetry_test.exs
    test/support/scoria/integration_case.ex
    test/support/scoria/eventually.ex
    lib/scoria_web/live/orchestrator_live.ex
    .planning/phases/01-orchestrator-live-wiring-runtime-pubsub-trace-broadcast-and-/01-CONTEXT.md
  </read_first>
  <action>
1. Create `test/scoria_web/live/orchestrator_live_integration_test.exs` using `Scoria.IntegrationCase` (`async: false`).

2. Setup (mirror telemetry_test + runtime_integration_test):
   - Sandbox checkout shared
   - Start Buffer supervised process
   - Attach `Scoria.Observe.Telemetry`
   - Dedicated Router/Endpoint with `scoria_dashboard("/scoria")` and session plug

3. Test **"live trace appears in orchestrator without send/2"** (D-129) — **must use `Runtime.start_run/2` producer path** (not raw `:telemetry.execute`):
   - `tenant_id = "orch-int-tenant-" <> unique`
   - Init session: `%{"tenant_id" => tenant_id, "actor_id" => "operator-int"}`
   - `live(conn, "/scoria")` mount OrchestratorLive
   - `Scoria.Runtime.start_run(%{tenant_id: tenant_id, actor_id: "operator-int", session_id: session_id}, initial_step: ..., handlers: ...)` with handler that completes a span whose name is assertable in DOM (mirror `runtime_integration_test.exs` patterns; span metadata **must** include matching `tenant_id`)
   - **Do NOT** `send(view.pid, ...)` and **do NOT** call raw `:telemetry.execute` as the primary assertion path
   - `eventually(fn -> assert render(view) =~ expected_span_name end)` where `expected_span_name` matches handler output (e.g. `"req_llm_request"`)

4. Assert trace arrives via PubSub merge (live path), not DB hydrate alone — test runs before Buffer flush completes or uses distinctive span name from runtime handler.
  </action>
  <acceptance_criteria>
    - `rg -n 'Runtime.start_run' test/scoria_web/live/orchestrator_live_integration_test.exs` returns match.
    - `rg -n ':telemetry.execute' test/scoria_web/live/orchestrator_live_integration_test.exs` returns no matches in trace test (runtime path only).
    - `rg -n 'eventually' test/scoria_web/live/orchestrator_live_integration_test.exs` returns match.
    - `rg -n 'send(view.pid' test/scoria_web/live/orchestrator_live_integration_test.exs` returns no matches.
    - `rg -n 'Scoria.IntegrationCase' test/scoria_web/live/orchestrator_live_integration_test.exs` returns match.
    - `MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_integration_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_integration_test.exs</automated>
  </verify>
  <done>Integration test proves Runtime.start_run → telemetry → PubSub → OrchestratorLive trace DOM without send/2.</done>
</task>

<task type="auto">
  <name>Task 01-03-02b: Integration test — trace DB hydrate on reconnect</name>
  <requirement_ids>ORCH-LIVE-01</requirement_ids>
  <files>test/scoria_web/live/orchestrator_live_integration_test.exs</files>
  <read_first>
    test/scoria_web/live/orchestrator_live_integration_test.exs
    lib/scoria_web/live/orchestrator_live.ex
    lib/scoria/observe/buffer.ex
    .planning/phases/01-orchestrator-live-wiring-runtime-pubsub-trace-broadcast-and-/01-CONTEXT.md
  </read_first>
  <action>
1. Add test **"reconnect hydrates traces from DB after missed PubSub"** (D-119):
   - Mount `/scoria` with matching session `tenant_id`
   - `render_disconnect(view)`
   - While disconnected, trigger `Runtime.start_run/2` (or `:telemetry.execute` span stop) so spans persist via Buffer flush — **force Buffer flush** (`Buffer.flush/1` or `:sys.get_state` + explicit flush helper from test setup) so `ai_traces`/`ai_spans` rows exist before reconnect
   - **Do NOT** rely on PubSub delivery during disconnect window
   - `render_reconnect(view)`
   - `eventually(fn -> assert render(view) =~ expected_span_name end)` — trace visible from `hydrate_traces/2` on connected mount, not from live PubSub

2. Share Endpoint module and setup helper with other integration tests in same file.
  </action>
  <acceptance_criteria>
    - `rg -n 'reconnect hydrates traces' test/scoria_web/live/orchestrator_live_integration_test.exs` returns match (test name).
    - `rg -n 'render_disconnect' test/scoria_web/live/orchestrator_live_integration_test.exs` returns match in trace hydrate test.
    - `rg -n 'render_reconnect' test/scoria_web/live/orchestrator_live_integration_test.exs` returns match in trace hydrate test.
    - `rg -n 'Buffer' test/scoria_web/live/orchestrator_live_integration_test.exs` returns match (flush before reconnect).
    - `rg -n 'send(view.pid' test/scoria_web/live/orchestrator_live_integration_test.exs` returns no matches in trace hydrate test.
    - `MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_integration_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_integration_test.exs</automated>
  </verify>
  <done>Integration test proves DB hydrate seeds trace tree on reconnect when PubSub messages were missed.</done>
</task>

<task type="auto">
  <name>Task 01-03-03: Integration test — real HITL modal and reconnect DB catch-up</name>
  <requirement_ids>ORCH-LIVE-01</requirement_ids>
  <files>test/scoria_web/live/orchestrator_live_integration_test.exs</files>
  <read_first>
    test/scoria/runtime_integration_test.exs
    test/scoria_web/live/orchestrator_live_integration_test.exs
    lib/scoria/workflows.ex
    .planning/phases/01-orchestrator-live-wiring-runtime-pubsub-trace-broadcast-and-/01-CONTEXT.md
  </read_first>
  <action>
1. Add test **"real approval surfaces blocking modal without send/2"** (D-129):
   - `Scoria.start_run(%{tenant_id: tenant_id, actor_id: ..., session_id: ...}, initial_step: approval kind, handlers: ...)`
   - Session tenant_id **must equal** runtime tenant_id
   - Mount `/scoria`
   - Drive workflow to approval pause via existing test handlers (see runtime_integration_test patterns)
   - `eventually(fn -> assert render(view) =~ "Approval Required" end)`
   - Assert modal shows tool_name and does not contain raw secret from approval arguments
   - No `send(view.pid, {:hitl_request, _})`

2. Add test **"reconnect shows modal from DB pending approval"** (D-131):
   - Mount live view; `render_disconnect(view)`
   - While disconnected, trigger real approval (Runtime/mark_waiting_for_approval path)
   - `render_reconnect(view)` 
   - Assert HTML contains "Approval Required" from **mount hydrate** of pending approvals (not relying on PubSub during disconnect)
   - Optionally assert `arguments_preview` substring in modal

3. Keep tests in same file; share Endpoint module and setup helper.
  </action>
  <acceptance_criteria>
    - `rg -n 'render_disconnect' test/scoria_web/live/orchestrator_live_integration_test.exs` returns match.
    - `rg -n 'render_reconnect' test/scoria_web/live/orchestrator_live_integration_test.exs` returns match.
    - `rg -n 'Approval Required' test/scoria_web/live/orchestrator_live_integration_test.exs` returns match.
    - `rg -n 'send(view.pid' test/scoria_web/live/orchestrator_live_integration_test.exs` returns no matches.
    - `MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_integration_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_integration_test.exs</automated>
  </verify>
  <done>Integration tests prove real HITL modal and reconnect DB catch-up for ORCH-LIVE-01.</done>
</task>

<task type="auto">
  <name>Task 01-03-04: Pin integration test in semantic fast-path lane</name>
  <requirement_ids>ORCH-LIVE-01</requirement_ids>
  <files>lib/mix/tasks/scoria.test.semantic_fast_path.ex, test/mix/tasks/test.semantic_fast_path_test.exs</files>
  <read_first>
    lib/mix/tasks/scoria.test.semantic_fast_path.ex
    test/mix/tasks/test.semantic_fast_path_test.exs
    lib/scoria/verification_lanes.ex
    .planning/phases/01-orchestrator-live-wiring-runtime-pubsub-trace-broadcast-and-/01-VALIDATION.md
  </read_first>
  <action>
1. Append `"test/scoria_web/live/orchestrator_live_integration_test.exs"` to `@semantic_fast_path_test_files` in `lib/mix/tasks/scoria.test.semantic_fast_path.ex` (D-129).

2. Update `expected_files` (or equivalent assertion list) in `test/mix/tasks/test.semantic_fast_path_test.exs` to include the integration test path.

3. Verify `VerificationLanes.closeout_order/0` is **NOT** modified — run existing verification_lanes_test.

4. Keep existing `orchestrator_live_test.exs` in semantic list as UI unit tier (D-129).
  </action>
  <acceptance_criteria>
    - `rg -n 'orchestrator_live_integration_test' lib/mix/tasks/scoria.test.semantic_fast_path.ex` returns match.
    - `rg -n 'orchestrator_live_integration_test' test/mix/tasks/test.semantic_fast_path_test.exs` returns match.
    - `MIX_ENV=test mix test test/mix/tasks/test.semantic_fast_path_test.exs test/scoria/verification_lanes_test.exs` exits 0.
    - `rg -n 'orchestrator_live_integration' lib/scoria/verification_lanes.ex` returns no matches (not in closeout_order).
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/mix/tasks/test.semantic_fast_path_test.exs test/scoria/verification_lanes_test.exs</automated>
  </verify>
  <done>Semantic fast-path lane includes integration test; closeout_order unchanged.</done>
</task>

<task type="auto">
  <name>Task 01-03-05: Adoption doc session contract fragment</name>
  <requirement_ids>ORCH-LIVE-01</requirement_ids>
  <files>docs/adoption_lanes.md</files>
  <read_first>
    docs/adoption_lanes.md
    test/support/scoria/adoption_example.ex
    lib/scoria_web/live/orchestrator_live.ex
    .planning/phases/01-orchestrator-live-wiring-runtime-pubsub-trace-broadcast-and-/01-CONTEXT.md
  </read_first>
  <action>
1. Under operator surfaces / default runtime lane section in `docs/adoption_lanes.md`, add subsection **"Host session identity"** (D-130):

   - Host apps MUST set `session["tenant_id"]` and `session["actor_id"]` before mounting `/scoria` (or any `scoria_dashboard/2` route)
   - Explain: PubSub topic `scoria:runs:{tenant_id}` and audit refs on approve/3 require matching runtime identity tenant
   - Example snippet referencing conn/session assign pattern (mirror `adoption_example.ex` doc_fragments style)
   - Explicit note: `mix scoria.install` does **not** inject auth — host-owned

2. Optional one-liner: Observe Buffer/Telemetry may need explicit host startup if operator live broadcast enabled (reference existing telemetry_test setup pattern — no installer change).

3. Do NOT add installer code changes.
  </action>
  <acceptance_criteria>
    - `rg -n 'session\\["tenant_id"\\]|session\\[\"tenant_id\"\\]' docs/adoption_lanes.md` returns match.
    - `rg -n 'session\\["actor_id"\\]|session\\[\"actor_id\"\\]' docs/adoption_lanes.md` returns match.
    - `rg -n 'mix scoria.install' docs/adoption_lanes.md` returns match noting auth-agnostic behavior.
    - `rg -n 'scoria:runs:' docs/adoption_lanes.md` returns match explaining PubSub scoping.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'tenant_id|actor_id' docs/adoption_lanes.md</automated>
  </verify>
  <done>Adoption docs document host session contract for operator live wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host app → Session | Host must inject correct tenant_id before /scoria mount |
| DB → LiveView hydrate | Historical traces loaded for tenant filter only |
| CI semantic lane | Integration test proves producer path without hollow send/2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Elevation of privilege | DB hydrate query | **HIGH** | **block** | Filter traces/spans by tenant_id in attributes — never hydrate all traces (D-119) |
| T-01-13 | Spoofing | Session tenant mismatch | **HIGH** | **block** | Document host contract; integration test uses matching session + runtime tenant_id (D-130, D-129) |
| T-01-14 | Repudiation | Hollow test props | MEDIUM | mitigate | Integration test forbids send/2; semantic lane pin (D-129) |
| T-01-15 | Tampering | closeout lane widening | LOW | accept | Explicit guard: closeout_order/0 unchanged; integration in semantic lane only |

**Gate:** Plans MUST NOT proceed if T-01-12 or T-01-13 mitigations are not implemented (tenant-scoped hydrate query; session/runtime tenant alignment in integration test).
</threat_model>

<verification>
MIX_ENV=test mix compile --warnings-as-errors
MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_integration_test.exs
MIX_ENV=test mix test test/mix/tasks/test.semantic_fast_path_test.exs test/scoria/verification_lanes_test.exs
SCORIA_DB_PORT=55432 SCORIA_DB_PASSWORD=postgres MIX_ENV=test mix test.semantic_fast_path --warnings-as-errors
</verification>

<success_criteria>
- DB hydrate seeds traces on reconnect.
- Integration tests pass without send/2.
- Runtime.start_run is the required trace integration test producer path (D-129).
- Trace DB hydrate on reconnect proven by behavioral integration test.
- Reconnect test proves DB catch-up modal.
- Semantic fast-path lane green.
- Adoption doc lists session tenant_id/actor_id contract.
- closeout_order/0 unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/01-orchestrator-live-wiring-runtime-pubsub-trace-broadcast-and-/01-03-SUMMARY.md`
</output>
