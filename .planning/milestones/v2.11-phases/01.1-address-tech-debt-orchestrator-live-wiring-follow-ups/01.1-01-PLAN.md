---
phase: 01.1-address-tech-debt-orchestrator-live-wiring-follow-ups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/observe/telemetry.ex
  - lib/scoria_web/live/orchestrator_live.ex
  - test/scoria/observe/telemetry_test.exs
  - test/scoria_web/live/orchestrator_live_integration_test.exs
autonomous: true
requirements: [ORCH-LIVE-01]
must_haves:
  truths:
    - "Phase 01.1 is surgical hardening only — three waves redaction → approval E2E → token delta; no UAT, UI stubs, ReqLLM streaming, or closeout widening (D-132)."
    - "Span-delta telemetry applies Redactor.redact/1 before OperatorBroadcast.span_delta/1 — same ordering as span stop (D-133, D-118)."
    - "Redacted deny-list keys in delta chunk metadata never appear verbatim on PubSub {:trace_delta, _} messages."
    - "DB hydrate path re-redacts span.attributes before TraceProjection.span_view/1 (D-134)."
    - "Legacy or manually inserted spans with secrets in DB cannot leak deny-list keys into attributes_preview after reconnect hydrate."
    - "VerificationLanes.closeout_order/0 unchanged."
  artifacts:
    - path: "lib/scoria/observe/telemetry.ex"
      provides: "redacted delta handler"
      contains: "Redactor.redact"
    - path: "lib/scoria_web/live/orchestrator_live.ex"
      provides: "hydrate defense in depth"
      contains: "span_view_from_record"
    - path: "test/scoria/observe/telemetry_test.exs"
      provides: "delta redaction unit proof"
      contains: "api_key"
    - path: "test/scoria_web/live/orchestrator_live_integration_test.exs"
      provides: "hydrate re-redact integration proof"
      contains: "reconnect hydrates"
  key_links:
    - from: "lib/scoria/observe/telemetry.ex"
      to: "lib/scoria/observe/operator_broadcast.ex"
      via: "span_delta/1 after Redactor.redact/1"
      pattern: "OperatorBroadcast.span_delta"
    - from: "lib/scoria_web/live/orchestrator_live.ex"
      to: "lib/scoria/observe/redactor.ex"
      via: "span_view_from_record/1"
      pattern: "Redactor.redact"
---

<objective>
Close 01-REVIEW redaction warnings #1–2: span-delta path and DB hydrate defense-in-depth.

Purpose: Secrets/PII must never reach PubSub or operator UI via delta chunks or stale DB attributes (D-133, D-134).
Output: Green telemetry + integration tests; redact → broadcast ordering on delta matches stop path.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01.1-address-tech-debt-orchestrator-live-wiring-follow-ups/01.1-CONTEXT.md
@.planning/phases/01.1-address-tech-debt-orchestrator-live-wiring-follow-ups/01.1-RESEARCH.md
@.planning/phases/01-orchestrator-live-wiring-runtime-pubsub-trace-broadcast-and-/01-REVIEW.md
@lib/scoria/observe/telemetry.ex
@lib/scoria/observe/redactor.ex
@lib/scoria/observe/operator_broadcast.ex
@lib/scoria/observe/trace_projection.ex
@lib/scoria_web/live/orchestrator_live.ex
@test/scoria/observe/telemetry_test.exs

Do NOT widen VerificationLanes.closeout_order/0. Do NOT change ReqLLM adapter streaming behavior.
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-01.1-01 | Info disclosure via unredacted delta chunk on PubSub | high | Redactor.redact before span_delta broadcast (D-133) |
| T-01.1-02 | Info disclosure via legacy DB span attributes on hydrate | medium | Re-redact in span_view_from_record (D-134) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 01.1-01-01: Redact span-delta telemetry before broadcast</name>
  <files>lib/scoria/observe/telemetry.ex, test/scoria/observe/telemetry_test.exs</files>
  <read_first>
    lib/scoria/observe/telemetry.ex
    lib/scoria/observe/redactor.ex
    lib/scoria/observe/operator_broadcast.ex
    test/scoria/observe/telemetry_test.exs
    .planning/phases/01-orchestrator-live-wiring-runtime-pubsub-trace-broadcast-and-/01-REVIEW.md
  </read_first>
  <action>
1. In `handle_event([:scoria, :observe, :span, :delta], ...)`, assign `redacted = Redactor.redact(metadata)` then call `OperatorBroadcast.span_delta(redacted)` — mirror the `:stop` handler ordering (D-133).

2. Add test `"redacts span delta chunk before broadcast"` in `telemetry_test.exs`:
   - Subscribe to `OperatorBroadcast.tenant_topic(tenant_id)` (reuse setup pattern)
   - Execute delta with metadata including `chunk: "leak api_key=super-secret-key"` and `attributes: %{"api_key" => "secret"}`
   - `assert_receive {:trace_delta, delta}`
   - Assert `delta.chunk` does not contain `"super-secret-key"` (either `[REDACTED]` substitution or key scrub per Redactor behavior)
   - Assert delta map has no raw secret values in nested attributes if present
  </action>
  <acceptance_criteria>
    - `rg -n 'Redactor.redact' lib/scoria/observe/telemetry.ex` matches both `:stop` and `:delta` handlers.
    - `rg -n 'redacts span delta' test/scoria/observe/telemetry_test.exs` returns match.
    - `MIX_ENV=test mix test test/scoria/observe/telemetry_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/observe/telemetry_test.exs</automated>
  </verify>
  <done>Delta path matches D-118 redact-before-broadcast; unit test proves scrubbed PubSub payload.</done>
</task>

<task type="auto">
  <name>Task 01.1-01-02: Re-redact attributes on DB hydrate</name>
  <files>lib/scoria_web/live/orchestrator_live.ex, test/scoria_web/live/orchestrator_live_integration_test.exs</files>
  <read_first>
    lib/scoria_web/live/orchestrator_live.ex
    lib/scoria/observe/redactor.ex
    lib/scoria/observe/trace_projection.ex
    lib/scoria/repo/span.ex
    test/scoria_web/live/orchestrator_live_integration_test.exs
  </read_first>
  <action>
1. Update `span_view_from_record/1` in `orchestrator_live.ex` (D-134):
   - Build input map from `%Span{}` fields as today
   - Before `TraceProjection.span_view/1`, pass attributes through `Redactor.redact(%{attributes: span.attributes || %{}})` and use returned `:attributes` (or redact full map if simpler — keep field names `id`, `name`, `span_kind`, etc.)

2. Add integration test `"reconnect hydrates redacted span attributes from DB"`:
   - Unique tenant/session; `live(conn, "/scoria")` then `render_disconnect(view)`
   - Insert trace + span directly via Repo with `attributes: %{"api_key" => "db-secret-value", "public" => "ok"}` and `attributes->>'tenant_id'` matching tenant (mirror hydrate query filters from `hydrate_traces/2`)
   - `force_buffer_flush` not required if span inserted directly
   - `render_reconnect(conn, view, "/scoria")`
   - `eventually` assert `render(view)` contains `"ok"` and `refute render(view) =~ "db-secret-value"`
   - Assert rendered HTML does not contain `[REDACTED]` in a way that exposes raw key names if deny-list strips keys entirely from preview — match TraceProjection behavior
  </action>
  <acceptance_criteria>
    - `rg -n 'Redactor.redact' lib/scoria_web/live/orchestrator_live.ex` returns match inside `span_view_from_record`.
    - `rg -n 'reconnect hydrates redacted' test/scoria_web/live/orchestrator_live_integration_test.exs` returns match.
    - `rg -n 'db-secret-value' test/scoria_web/live/orchestrator_live_integration_test.exs` returns match (fixture secret).
    - `MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_integration_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_integration_test.exs</automated>
  </verify>
  <done>Hydrate path defense-in-depth; integration test proves DB rows with secrets do not leak to DOM.</done>
</task>

</tasks>

<verification>
```bash
MIX_ENV=test mix test test/scoria/observe/telemetry_test.exs
MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_integration_test.exs
mix compile --warnings-as-errors
```
</verification>
