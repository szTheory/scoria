---
phase: 01.1-address-tech-debt-orchestrator-live-wiring-follow-ups
plan: 01
subsystem: observability
tags: [redactor, telemetry, orchestrator-live, pubsub, security]

requires:
  - phase: 01-orchestrator-live-wiring-runtime-pubsub-trace-broadcast-and-
    provides: OperatorBroadcast, TraceProjection, OrchestratorLive hydrate, D-118 redact-before-broadcast on span stop

provides:
  - Delta telemetry path with redact → scrub_text → span_delta ordering (D-133)
  - Hydrate path with Redactor.redact before TraceProjection.span_view (D-134)
  - Redactor.scrub_text/1 for deny-list key=value patterns in streaming chunks

affects:
  - 01.1-02 (approval E2E — independent)
  - 01.1-03 (emit_span_delta — builds on redacted delta handler)

tech-stack:
  added: []
  patterns:
    - "Redact → scrub chunk text → broadcast on [:scoria, :observe, :span, :delta]"
    - "Re-redact DB attributes in span_view_from_record before UI projection"

key-files:
  created: []
  modified:
    - lib/scoria/observe/telemetry.ex
    - lib/scoria/observe/redactor.ex
    - lib/scoria_web/live/orchestrator_live.ex
    - test/scoria/observe/telemetry_test.exs
    - test/scoria/observe/redactor_test.exs
    - test/scoria_web/live/orchestrator_live_integration_test.exs

key-decisions:
  - "D-133: Redactor.redact before OperatorBroadcast.span_delta; scrub_text on chunk for embedded key=value secrets"
  - "D-134: span_view_from_record re-redacts attributes before TraceProjection for legacy DB rows"

patterns-established:
  - "Delta egress matches stop-path redact-before-broadcast ordering"
  - "Hydrate defense-in-depth even when Buffer persisted redacted attributes"

requirements-completed: [ORCH-LIVE-01]

duration: 12min
completed: 2026-05-30
---

# Phase 01.1 Plan 01: Redaction Defense-in-Depth Summary

**Span-delta PubSub and DB hydrate paths now redact before broadcast/projection, with scrub_text for embedded secrets in streaming chunks**

## Performance

- **Duration:** 12 min
- **Started:** 2026-05-30T11:17:00Z
- **Completed:** 2026-05-30T11:29:00Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- `Telemetry.handle_event/4` for `:delta` applies `Redactor.redact/1` then `scrub_delta_chunk/1` before `OperatorBroadcast.span_delta/1`
- Added `Redactor.scrub_text/1` to replace deny-list `key=value` patterns in free-form chunk strings
- `OrchestratorLive.span_view_from_record/1` re-redacts `span.attributes` before `TraceProjection.span_view/1`
- Unit test proves scrubbed `{:trace_delta, _}` payload; integration test proves reconnect hydrate hides `db-secret-value`

## Task Commits

Each task was committed atomically:

1. **Task 01.1-01-01: Redact span-delta telemetry before broadcast** - `40b9436` (feat)
2. **Task 01.1-01-02: Re-redact attributes on DB hydrate** - `f667cd1` (feat)

**Plan metadata:** pending (docs commit with STATE/ROADMAP after this summary)

## Files Created/Modified

- `lib/scoria/observe/telemetry.ex` — redact + scrub chunk before span_delta
- `lib/scoria/observe/redactor.ex` — `scrub_text/1` for chunk key=value patterns
- `lib/scoria_web/live/orchestrator_live.ex` — hydrate re-redact in `span_view_from_record/1`
- `test/scoria/observe/telemetry_test.exs` — delta redaction unit proof
- `test/scoria/observe/redactor_test.exs` — scrub_text unit proof
- `test/scoria_web/live/orchestrator_live_integration_test.exs` — reconnect hydrate integration proof

## Decisions Made

- Extended Redactor with `scrub_text/1` because map-key redaction alone leaves secrets embedded in chunk strings (required by D-133 and plan acceptance tests)
- Integration test grants `Sandbox.allow` to LiveView pid so hydrate queries see inserted spans on reconnect

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Added Redactor.scrub_text/1 for chunk string secrets**
- **Found during:** Task 01.1-01-01 (delta redaction unit test)
- **Issue:** `Redactor.redact/1` only redacts map keys; `chunk: "leak api_key=super-secret-key"` passed through verbatim
- **Fix:** Added `scrub_text/1` and `scrub_delta_chunk/1` in telemetry handler after redact
- **Files modified:** `lib/scoria/observe/redactor.ex`, `lib/scoria/observe/telemetry.ex`, `test/scoria/observe/redactor_test.exs`
- **Verification:** `MIX_ENV=test mix test test/scoria/observe/telemetry_test.exs` — 4 tests, 0 failures
- **Committed in:** `40b9436`

**2. [Rule 3 - Blocking] Sandbox allow for LiveView on hydrate integration test**
- **Found during:** Task 01.1-01-02 (reconnect hydrate test)
- **Issue:** `eventually` timed out because reconnect LiveView could not see span insert without `Sandbox.allow`
- **Fix:** `Ecto.Adapters.SQL.Sandbox.allow(Scoria.Repo, self(), view.pid)` after live/reconnect; direct assert on reconnect html
- **Files modified:** `test/scoria_web/live/orchestrator_live_integration_test.exs`
- **Verification:** `MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_integration_test.exs` — 5 tests, 0 failures
- **Committed in:** `f667cd1`

---

**Total deviations:** 2 auto-fixed (1 bug, 1 blocking)
**Impact on plan:** Both required for D-133/D-134 truth and green verification. No scope creep; `closeout_order/0` unchanged.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Verification Results

```
MIX_ENV=test mix test test/scoria/observe/telemetry_test.exs          → 4 tests, 0 failures
MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_integration_test.exs → 5 tests, 0 failures
mix compile --warnings-as-errors                                    → OK
```

## Self-Check: PASSED

- [x] `Redactor.redact` in telemetry.ex for both `:stop` and `:delta` handlers
- [x] `redacts span delta` test present
- [x] `Redactor.redact` in `span_view_from_record`
- [x] `reconnect hydrates redacted` integration test present
- [x] `db-secret-value` fixture in integration test
- [x] `VerificationLanes.closeout_order/0` not modified

## Next Phase Readiness

Ready for **01.1-02** (approval approve/reject integration tests). Delta handler redaction is in place for **01.1-03** `emit_span_delta/1`.

---
*Phase: 01.1-address-tech-debt-orchestrator-live-wiring-follow-ups*
*Completed: 2026-05-30*
