# Phase 01.1 Research — Orchestrator Live Wiring Follow-ups

**Researched:** 2026-05-30  
**Phase:** 01.1 — Address tech debt: orchestrator live wiring follow-ups  
**Source:** Phase 01 CONTEXT/REVIEW/VERIFICATION + v2.11 milestone audit + codebase grep  
**Confidence:** HIGH

---

## Summary

Phase 01 closed ORCH-LIVE-01 with green verification. Milestone audit and `01-REVIEW.md` identified **three surgical gaps**: span-delta redaction bypass, hydrate re-redact bypass, and missing integration proof for approval decisions + token coalesce. Phase 01.1 is a hardening slice — no new features, no `closeout_order` changes.

## Codebase findings (verified)

| Gap | Current state | Fix |
|-----|---------------|-----|
| Delta redaction | `telemetry.ex:20-21` calls `OperatorBroadcast.span_delta(metadata)` without `Redactor.redact/1` | Redact metadata (including `chunk`) before broadcast (D-133) |
| Hydrate re-redact | `orchestrator_live.ex:1046-1056` passes raw `span.attributes` to `TraceProjection.span_view/1` | `Redactor.redact(%{attributes: ...})` before projection (D-134) |
| Approval E2E | Integration tests cover modal **open** only; approve/reject use `send/2` in unit tier | `render_click(view, "approve")` / `"reject"` after `Runtime.start_run` (D-135–D-136) |
| Token delta proof | `telemetry_test.exs` proves PubSub tuple; no LiveView coalesce proof | `Telemetry.emit_span_delta/1` + handler emitting deltas during `start_run` (D-139–D-140) |
| Stale timer | `maybe_clear_token_preview/2` deletes preview but not `token_timers` ref | `Process.cancel_timer/1` on span complete (D-141) |

## Patterns to preserve

- D-118: redact → broadcast → buffer (stop path already correct)
- D-129: integration tests use `Runtime.start_run/2` producer path — no `send(view.pid, ...)`, no raw `:telemetry.execute` in integration file for proof tests
- Semantic fast-path pin: extend integration file only; do not add to `VerificationLanes.closeout_order/0`

## Deferred (unchanged)

- ReqLLM streaming adapter (v2.12+)
- UAT `/gsd-verify-work`
- UI stubs, legacy `{:new_trace}` shim, host Observe auto-attach
