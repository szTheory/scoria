---
phase: 01.1-address-tech-debt-orchestrator-live-wiring-follow-ups
plan: 03
subsystem: observability
tags: [telemetry, orchestrator-live, token-coalesce, integration-test, pubsub]

requires:
  - phase: 01.1-address-tech-debt-orchestrator-live-wiring-follow-ups
    plan: 01
    provides: Redacted delta handler path (D-133)
  - phase: 01.1-address-tech-debt-orchestrator-live-wiring-follow-ups
    plan: 02
    provides: Integration test harness patterns on producer path

provides:
  - Public Scoria.Observe.Telemetry.emit_span_delta/1 (D-139)
  - Token timer cancel on span complete in maybe_clear_token_preview/2 (D-141)
  - E2E proof Runtime.start_run → emit_span_delta → coalesced token_previews in DOM (D-140)

affects: []

tech-stack:
  added: []
  patterns:
    - "emit_span_delta/1 is the only supported delta emit API for adapters and integration tests"
    - "Cancel token_timers + clear token_buffers on span end_time in maybe_clear_token_preview"

key-files:
  created: []
  modified:
    - lib/scoria/observe/telemetry.ex
    - lib/scoria_web/live/orchestrator_live.ex
    - test/scoria_web/live/orchestrator_live_integration_test.exs

key-decisions:
  - "D-139: Public emit_span_delta/1 wraps :telemetry.execute for [:scoria, :observe, :span, :delta]"
  - "D-140: Integration test uses active LLM span (no end_time) + handler emit_span_delta only"
  - "D-141: Process.cancel_timer on span complete clears stale coalesce flush risk"

patterns-established:
  - "Delta proof path: Telemetry.emit_span_delta → redact → OperatorBroadcast → {:trace_delta} → 75ms coalesce"
  - "Span complete clears token_timers, token_buffers, and token_previews together"

requirements-completed: [ORCH-LIVE-01]

duration: 18min
completed: 2026-05-30
---

# Phase 01.1 Plan 03: Token Delta Coalesce E2E Summary

**Public emit_span_delta/1, timer cancel on span stop, and integration proof that coalesced token previews render on active LLM spans via the producer path**

## Performance

- **Duration:** 18 min
- **Started:** 2026-05-30T11:08:00Z
- **Completed:** 2026-05-30T11:26:41Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments

- Added `Telemetry.emit_span_delta/1` with `@doc` directing adapters and tests away from raw `:telemetry.execute`
- `maybe_clear_token_preview/2` cancels pending `token_timers`, deletes `token_buffers` and `token_previews` when `end_time` is set
- Integration test `"token delta coalesces into span preview via producer path"` proves `Hello world` coalesce on active LLM span via `Runtime.start_run` handler — no raw delta telemetry in test file

## Task Commits

Each task was committed atomically:

1. **Task 01.1-03-01: Add Telemetry.emit_span_delta/1 public API** - `2d329cb` (feat)
2. **Task 01.1-03-02: Cancel token timers when span completes** - `a5d0b29` (fix)
3. **Task 01.1-03-03: Integration test token delta coalesce on trace tree** - `223d7dc` (test)

**Plan metadata:** pending (docs commit with STATE/ROADMAP after this summary)

## Files Created/Modified

- `lib/scoria/observe/telemetry.ex` — `emit_span_delta/1` public API
- `lib/scoria_web/live/orchestrator_live.ex` — `Process.cancel_timer` + buffer cleanup on span complete
- `test/scoria_web/live/orchestrator_live_integration_test.exs` — `emit_token_deltas/2` handler + coalesce integration test

## Decisions Made

- Handler opens active LLM span via `:stop` telemetry without `end_time` so `TraceTreeComponent` shows preview (`llm_token_preview?` requires nil `end_time`)
- Delta chunks emitted only through `Telemetry.emit_span_delta/1` in handler — satisfies D-129 spirit for integration proof

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Verification Results

```
rg -n 'def emit_span_delta' lib/scoria/observe/telemetry.ex          → match
rg -n 'cancel_timer' lib/scoria_web/live/orchestrator_live.ex      → match in maybe_clear_token_preview
rg -n 'emit_span_delta' test/.../orchestrator_live_integration_test.exs → Handlers only
rg ':telemetry.execute([:scoria, :observe, :span, :delta]' test/...  → no matches
MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_integration_test.exs → 8 tests, 0 failures
SCORIA_DB_PORT=55432 SCORIA_DB_PASSWORD=postgres MIX_ENV=test mix test.semantic_fast_path --warnings-as-errors → OK
mix compile --warnings-as-errors → OK
```

## Self-Check: PASSED

- [x] `def emit_span_delta` in telemetry.ex
- [x] `:telemetry.execute` for delta only inside emit_span_delta in telemetry.ex
- [x] `cancel_timer` inside `maybe_clear_token_preview`
- [x] `token_timers` delete on span complete path
- [x] `emit_span_delta` in integration Handlers module
- [x] `token delta coalesces` test present
- [x] No raw `:telemetry.execute` for delta in integration test
- [x] `VerificationLanes.closeout_order/0` not modified

## Next Phase Readiness

Phase 01.1 complete — all three plans landed. Ready for `/gsd-verify-work` conversational UAT on ORCH-LIVE-01.

---
*Phase: 01.1-address-tech-debt-orchestrator-live-wiring-follow-ups*
*Completed: 2026-05-30*
