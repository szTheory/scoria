---
phase: 01.1-address-tech-debt-orchestrator-live-wiring-follow-ups
plan: 02
type: execute
wave: 2
depends_on: [01.1-01]
files_modified:
  - test/scoria_web/live/orchestrator_live_integration_test.exs
autonomous: true
requirements: [ORCH-LIVE-01]
must_haves:
  truths:
    - "Approve happy path proven via Runtime.start_run producer → render_click approve → modal clears without send/2 (D-135)."
    - "Reject path proven via render_click reject → modal clears, run stays waiting_for_approval, no Resume.resume_run (D-136)."
    - "DOM never shows raw api_key from wait_for_approval handler arguments after decision (D-121)."
    - "Existing orchestrator_live_test.exs send/2 UI unit tests preserved (D-137)."
    - "VerificationLanes.closeout_order/0 unchanged."
  artifacts:
    - path: "test/scoria_web/live/orchestrator_live_integration_test.exs"
      provides: "approval decision integration proof"
      contains: "render_click"
  key_links:
    - from: "test/scoria_web/live/orchestrator_live_integration_test.exs"
      to: "lib/scoria/workflows.ex"
      via: "Runtime.start_run → Workflows.approve"
      pattern: "render_click(view, \"approve\""
---

<objective>
Close milestone-audit gap: approval decision E2E on the producer path.

Purpose: Prove approve/reject → Workflows.approve/3 → approval_decided PubSub → modal/inbox UX without test send/2 (D-135–D-137).
Output: Two new integration tests green; semantic fast-path file list unchanged (same file, more tests).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01.1-address-tech-debt-orchestrator-live-wiring-follow-ups/01.1-CONTEXT.md
@.planning/phases/01.1-address-tech-debt-orchestrator-live-wiring-follow-ups/01.1-RESEARCH.md
@test/scoria_web/live/orchestrator_live_integration_test.exs
@test/scoria_web/live/orchestrator_live_test.exs
@lib/scoria/workflows.ex
@lib/scoria_web/live/orchestrator_live.ex

Reuse Handlers.wait_for_approval/2 from integration test module. Do NOT remove unit-tier send/2 tests. Do NOT add orchestrator_live_integration to closeout_order.
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-01.1-03 | Repudiation — approval path untested in integration | medium | Producer-path render_click tests (D-135–D-136) |
| T-01.1-04 | Info disclosure — raw arguments in DOM after decision | high | refute html =~ secret key assertions |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 01.1-02-01: Integration test approve clears modal via producer path</name>
  <files>test/scoria_web/live/orchestrator_live_integration_test.exs</files>
  <read_first>
    test/scoria_web/live/orchestrator_live_integration_test.exs
    test/scoria_web/live/orchestrator_live_test.exs
    lib/scoria/workflows.ex
    lib/scoria_web/live/orchestrator_live.ex
    lib/scoria/runtime.ex
  </read_first>
  <action>
Add test `"approve decision clears modal via producer path"`:

1. Setup: unique tenant/session, `live(conn, "/scoria")` with session tenant_id + actor_id (mirror existing HITL test).

2. `Runtime.start_run/2` with `kind: "approval"`, handler `{Handlers, :wait_for_approval}`.

3. `eventually`: run status `waiting_for_approval`, render contains `"Approval Required"` and `"publish"`, `refute render =~ "super-secret-key"`.

4. `render_click(view, "approve", %{})` — **not** `send(view.pid, ...)`.

5. `eventually`:
   - `refute render(view) =~ "Approval Required"` (modal cleared)
   - Approval record status `"approved"` via `Repo.get!(Scoria.Observe.Approval, ...)` — obtain id from run/approval query pattern used in unit test or `Workflows.list_pending_remote_approvals` before click
   - `refute render(view) =~ "super-secret-key"`

6. Optionally assert run progressed (approved path may resume) — at minimum modal cleared and approval status updated.
  </action>
  <acceptance_criteria>
    - `rg -n 'approve decision clears modal' test/scoria_web/live/orchestrator_live_integration_test.exs` returns match.
    - `rg -n 'render_click\\(view, \"approve\"' test/scoria_web/live/orchestrator_live_integration_test.exs` returns match in approve test.
    - `rg -n 'send\\(view\\.pid' test/scoria_web/live/orchestrator_live_integration_test.exs` returns no matches in approve test body.
    - `MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_integration_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_integration_test.exs</automated>
  </verify>
  <done>Approve path proven on producer trust model.</done>
</task>

<task type="auto">
  <name>Task 01.1-02-02: Integration test reject keeps run paused</name>
  <files>test/scoria_web/live/orchestrator_live_integration_test.exs</files>
  <read_first>
    test/scoria_web/live/orchestrator_live_integration_test.exs
    test/scoria_web/live/orchestrator_live_test.exs
    lib/scoria/runtime/resume.ex
    lib/scoria_web/live/orchestrator_live.ex
  </read_first>
  <action>
Add test `"reject decision clears modal and keeps run paused"`:

1. Same setup as approve test through modal visible.

2. `render_click(view, "reject", %{})`.

3. `eventually`:
   - `refute render(view) =~ "Approval Required"`
   - Approval status `"rejected"` in DB
   - `{:ok, %{status: "waiting_for_approval"}} = Runtime.get_run(run_id)` — run does not auto-resume (D-136, D-124)
   - `refute render(view) =~ "super-secret-key"`
   - Render contains rejection footer copy substring `"Reject records a durable rejection"` OR inbox shows rejected state (planner picks observable assertion)

Do not call `Resume.resume_run/1` in test. Preserve all existing tests in file.
  </action>
  <acceptance_criteria>
    - `rg -n 'reject decision clears modal' test/scoria_web/live/orchestrator_live_integration_test.exs` returns match.
    - `rg -n 'render_click\\(view, \"reject\"' test/scoria_web/live/orchestrator_live_integration_test.exs` returns match.
    - `rg -n 'waiting_for_approval' test/scoria_web/live/orchestrator_live_integration_test.exs` returns match in reject test.
    - `MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_integration_test.exs` exits 0.
    - `MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_test.exs` exits 0 (unit tier preserved).
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_integration_test.exs test/scoria_web/live/orchestrator_live_test.exs</automated>
  </verify>
  <done>Reject path proven; durable rejection without resume.</done>
</task>

</tasks>

<verification>
```bash
MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_integration_test.exs
MIX_ENV=test mix test test/scoria_web/live/orchestrator_live_test.exs
```
</verification>
