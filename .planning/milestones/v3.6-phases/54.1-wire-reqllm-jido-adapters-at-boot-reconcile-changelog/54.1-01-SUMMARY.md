---
phase: 54.1-wire-reqllm-jido-adapters-at-boot-reconcile-changelog
plan: 01
subsystem: observability
tags: [telemetry, req_llm, jido, boot-attach, otel-genai]

# Dependency graph
requires:
  - phase: 53
    provides: "Scoria.Observe.Adapters.ReqLLM / Scoria.Observe.Adapters.Jido attach/handle_event modules and safe_attach_observe_mcp/0 boot-seam precedent in Scoria.Application"
provides:
  - "Scoria.Application.safe_attach_observe_req_llm/0 and safe_attach_observe_jido/0 private boot-attach helpers"
  - "observe_children/0 boot-attaches ReqLLM + Jido adapters unconditionally under the single enabled != false gate"
  - "Additive boot-path test proving a real [:req_llm, :request, :stop] event with no manual attach persists a span"
  - "Corrected runtime_span_test.exs moduledoc (ReqLLM IS boot-attached, matching MCP)"
affects: [54.1-02, changelog, docs-accuracy]

# Tech tracking
tech-stack:
  added: []
  patterns: ["boot-attach helper mirrors safe_attach_observe_mcp/0's match-and-ignore {:error, :already_exists} shape"]

key-files:
  created:
    - test/scoria/observe/adapters/boot_attach_test.exs
  modified:
    - lib/scoria/application.ex
    - test/scoria/workflows/runtime_span_test.exs

key-decisions:
  - "Jido attaches unconditionally with no Code.ensure_loaded?(Jido) guard (D-02) since the adapter references zero Jido.* modules; jido stays absent from mix.exs deps."
  - "Both new helpers are called inside the existing enabled != false branch (D-04) -- one switch, no per-adapter knob."
  - "Reworded the comment describing the Jido helper's no-guard rationale to avoid the literal string 'Code.ensure_loaded?' appearing in application.ex, since the Task-1 acceptance grep asserts that literal is absent (count 0) -- the earlier draft's own explanatory comment was tripping its own gate."

requirements-completed: [SPAN-01, SPAN-02, COMPAT-01, EVENT-01]

coverage:
  - id: D1
    description: "observe_children/0 boot-attaches both the ReqLLM and Jido telemetry adapters inside the existing enabled != false branch"
    requirement: "SPAN-01"
    verification:
      - kind: unit
        ref: "test/scoria/application_test.exs (4 tests, incl. enabled:false removes Buffer/skips attach)"
        status: pass
      - kind: unit
        ref: "test/scoria/observe/adapters/boot_attach_test.exs#boot handler is already registered for [:req_llm, :request, :stop]"
        status: pass
      - kind: unit
        ref: "test/scoria/observe/adapters/boot_attach_test.exs#boot handler is already registered for [:jido, :action, :stop]"
        status: pass
    human_judgment: false
  - id: D2
    description: "A [:req_llm, :request, :stop] event with no attach/0 call in the test body persists a span to Postgres (SC#2, D-07, the real boot-attach proof)"
    requirement: "EVENT-01"
    verification:
      - kind: unit
        ref: "test/scoria/observe/adapters/boot_attach_test.exs#a [:req_llm, :request, :stop] event with no attach/0 in this test persists a span"
        status: pass
    human_judgment: false
  - id: D3
    description: "Jido leg proves handler registration + pipeline wiring via a hand-synthesized [:jido, :action, :stop] event (jido is not a dep, no real in-suite producer -- explicitly weaker than the ReqLLM leg)"
    requirement: "EVENT-01"
    verification:
      - kind: unit
        ref: "test/scoria/observe/adapters/boot_attach_test.exs#a hand-synthesized [:jido, :action, :stop] event persists a span (no real jido producer exists)"
        status: pass
    human_judgment: false
  - id: D4
    description: "Both new helpers match-and-ignore {:error, :already_exists} and never raise in start/2 (T-53-08)"
    requirement: "COMPAT-01"
    verification:
      - kind: unit
        ref: "test/scoria/application_test.exs#Telemetry.attach/1 is idempotent at boot: a duplicate handler id does not raise (pattern shared by all boot helpers)"
        status: pass
      - kind: other
        ref: "grep -c already_exists lib/scoria/application.ex -> 5 (telemetry + mcp + reqllm + jido share the clause)"
        status: pass
    human_judgment: false
  - id: D5
    description: "runtime_span_test.exs moduledoc no longer claims ReqLLM is NOT boot-attached; zero functional edits; blast-radius tests stay green"
    requirement: "SPAN-02"
    verification:
      - kind: unit
        ref: "test/scoria/workflows/runtime_span_test.exs (full file, green)"
        status: pass
      - kind: unit
        ref: "test/scoria_web/live/orchestrator_live_integration_test.exs (full file, green)"
        status: pass
      - kind: other
        ref: "git diff --stat test/scoria/workflows/runtime_span_test.exs -> moduledoc-only, 6 insertions/2 deletions"
        status: pass
    human_judgment: false

duration: 15min
completed: 2026-07-18
status: complete
---

# Phase 54.1 Plan 01: Boot-attach ReqLLM + Jido adapters + reconcile moduledoc Summary

**Closed the runtime integration gap: `Scoria.Application.observe_children/0` now boot-attaches both the ReqLLM and Jido telemetry adapters (previously test-only), proven by an additive boot-path test with zero manual attach, and reconciled the one test moduledoc that had gone stale.**

## Performance

- **Duration:** 15 min
- **Started:** 2026-07-18T23:07:00Z (approx, first read)
- **Completed:** 2026-07-18T23:11:02Z
- **Tasks:** 3
- **Files modified:** 3 (1 created, 2 modified)

## Accomplishments
- `Scoria.Application.observe_children/0` now boot-attaches ReqLLM and Jido adapters (previously only `Scoria.Observe.Telemetry` and MCP were boot-attached), so real host apps get LLM/TOOL spans with zero hand-wiring.
- New `test/scoria/observe/adapters/boot_attach_test.exs` proves a `[:req_llm, :request, :stop]` event persists a span with no manual `attach()` call in the proving test body — the literal SC#2 boot-path claim.
- `test/scoria/workflows/runtime_span_test.exs`'s moduledoc no longer contradicts the code; the false "ReqLLM is NOT boot-attached" claim is replaced with an accurate description matching the MCP precedent.

## Task Commits

Each task was committed atomically:

1. **Task 1: Boot-attach ReqLLM + Jido adapters through observe_children/0 (D-01, D-02, D-04, T-53-08)** - `dfaf0339` (feat)
2. **Task 2: Additive SC#2 boot-path test — boot-attached ReqLLM persists a span with no manual attach/0 (D-07)** - `69649a8e` (test)
3. **Task 3: Reconcile runtime_span_test.exs moduledoc — ReqLLM IS boot-attached now (SC#3, D-06)** - `37083bb3` (docs)

_Note: this plan's tasks were `tdd="true"`/plain `auto` but each was a single atomic commit — behavior + assertion were verified together before each commit, not split into separate RED/GREEN commits, since the plan's `<verify>` blocks are acceptance-grep + `mix test` gates rather than a literal red-green cycle._

## Files Created/Modified
- `lib/scoria/application.ex` - Added `safe_attach_observe_req_llm/0` and `safe_attach_observe_jido/0`, called from `observe_children/0` inside the existing `enabled != false` branch
- `test/scoria/observe/adapters/boot_attach_test.exs` - New: SC#2 boot-path proof (ReqLLM real-producer leg + Jido handler-registration leg)
- `test/scoria/workflows/runtime_span_test.exs` - Moduledoc-only correction (L11-18 region)

## Decisions Made
- Jido helper attaches unconditionally, no `Code.ensure_loaded?` guard, per D-02 — the adapter references zero `Jido.*` modules so a guard would guard nothing.
- Both new helpers live inside the single `enabled != false` gate (D-04) — no new per-adapter config knob introduced.
- Reworded the Jido helper's explanatory comment to avoid literally containing the string the Task-1 acceptance grep checks is absent (`Code.ensure_loaded?`) — the first draft's own comment text was self-tripping the `grep -c 'Code.ensure_loaded?' lib/scoria/application.ex` == 0 acceptance check.

## Deviations from Plan

None - plan executed exactly as written (one self-corrected authoring slip during Task 1: the first comment draft literally contained the string `Code.ensure_loaded?` inside a negative statement, which the acceptance grep counted as a hit; reworded before committing, no functional change).

## Issues Encountered
- The worktree had no `deps/`/`_build/` (fresh checkout) — ran `mix deps.get` before `mix compile --warnings-as-errors` could succeed. No plan/task impact; this is expected worktree-isolation behavior, not a deviation.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Plan 02 (guides/capabilities/llm-and-tool-adapters.md) can proceed independently — Wave 1, no dependency on this plan's file changes beyond the now-accurate boot-attach behavior it documents.
- `lib/scoria/application.ex`'s `observe_children/0` is now the complete boot-attach seam for all four first-party adapters (Telemetry, MCP, ReqLLM, Jido) — any future adapter should mirror this same match-and-ignore shape.

---
*Phase: 54.1-wire-reqllm-jido-adapters-at-boot-reconcile-changelog*
*Completed: 2026-07-18*

## Self-Check: PASSED

- FOUND: lib/scoria/application.ex
- FOUND: test/scoria/observe/adapters/boot_attach_test.exs
- FOUND: test/scoria/workflows/runtime_span_test.exs
- FOUND: .planning/phases/54.1-wire-reqllm-jido-adapters-at-boot-reconcile-changelog/54.1-01-SUMMARY.md
- FOUND commit: dfaf0339
- FOUND commit: 69649a8e
- FOUND commit: 37083bb3
- FOUND commit: a899ead1
