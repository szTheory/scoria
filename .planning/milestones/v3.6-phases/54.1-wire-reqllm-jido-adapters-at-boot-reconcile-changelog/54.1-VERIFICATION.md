---
phase: 54.1-wire-reqllm-jido-adapters-at-boot-reconcile-changelog
verified: 2026-07-18T23:43:31Z
status: passed
score: 7/7 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 54.1: Wire ReqLLM/Jido adapters at boot + reconcile CHANGELOG Verification Report

**Phase Goal:** A real `req_llm` chat call or Jido action in a host app produces a persisted LLM/TOOL span with zero host hand-wiring — closing the v3.6 milestone-audit integration gap where `Scoria.Observe.Adapters.ReqLLM.attach/0` and `Adapters.Jido.attach/0` had no `lib/` caller (only tests attached them), so those spans fired into a void in production while the CHANGELOG claimed otherwise.
**Verified:** 2026-07-18T23:43:31Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | (ROADMAP SC#1 / D-01,D-04) `Scoria.Application` boot-attaches both ReqLLM and Jido adapters inside the single `enabled != false` gate, mirroring `safe_attach_observe_mcp/0` | ✓ VERIFIED | `lib/scoria/application.ex` L42-52: `observe_children/0` calls `safe_attach_observe_req_llm()` and `safe_attach_observe_jido()` inside the existing gate, before the `[Buffer]` return. `test/scoria/application_test.exs` test `"removes Buffer from the boot children list and skips the boot attach"` passes (ran directly, 0 failures) confirming `enabled: false` → `observe_children() == []`. |
| 2 | (ROADMAP SC#2 / D-07, EVENT-01) A `[:req_llm, :request, :stop]` event with NO manual `attach/0` in the test body persists a span row to Postgres — the boot path did the wiring | ✓ VERIFIED | `test/scoria/observe/adapters/boot_attach_test.exs` test `"a [:req_llm, :request, :stop] event with no attach/0 in this test persists a span"` — ran directly against real Postgres via the sandbox: `Repo.get_by!(Span, id: span_id)` returns a row with `name == "req_llm_request"` and correct `trace_id`. Ran twice with different `--seed` values, both green. Companion registration assertions (`assert {:error, :already_exists} = ReqLLM.attach()`) also pass, proving the handler was already live before the test body ran. |
| 3 | Jido leg proves handler registration + pipeline wiring via a hand-synthesized `[:jido, :action, :stop]` event (explicitly weaker — jido is not a dep) | ✓ VERIFIED | Same test file, `"a hand-synthesized [:jido, :action, :stop] event persists a span"` — passes, persists `name == "jido_action"`. Moduledoc explicitly states this leg's weaker evidentiary status (D-07), matching CHANGELOG/guide framing. |
| 4 | (T-53-08) Both new helpers match-and-ignore `{:error, :already_exists}` and never raise/halt in `start/2` | ✓ VERIFIED | `lib/scoria/application.ex` L83-103: both `safe_attach_observe_req_llm/0` and `safe_attach_observe_jido/0` use the identical `case ... do :ok -> :ok; {:error, :already_exists} -> :ok end` shape as `safe_attach_observe_mcp/0`. `grep -c already_exists` = 5 (telemetry + mcp + reqllm + jido helpers + boot_attach_test.exs assertions). Idempotency directly exercised by the boot_attach_test.exs "already registered" assertions (real double-attach). |
| 5 | (D-02) Jido helper attaches UNCONDITIONALLY — no `Code.ensure_loaded?(Jido)` guard, `jido` NOT added to mix.exs | ✓ VERIFIED | `grep -c 'Code.ensure_loaded?' lib/scoria/application.ex` = 0. `grep -n jido mix.exs` = no matches (jido absent from deps). |
| 6 | (ROADMAP SC#3 / D-06) `runtime_span_test.exs` moduledoc no longer claims ReqLLM is NOT boot-attached; blast-radius tests still pass with zero functional edits | ✓ VERIFIED | `test/scoria/workflows/runtime_span_test.exs` L13-19 moduledoc now reads "ReqLLM IS ALSO boot-attached... exactly like MCP (Phase 54.1)". `git show 37083bb3` confirms the diff is confined to the moduledoc (6 insertions/2 deletions, no test-body changes). `mix test test/scoria/workflows/runtime_span_test.exs test/scoria_web/live/orchestrator_live_integration_test.exs` — ran directly, all pass. |
| 7 | (COMPAT-01 / D-03) CHANGELOG `[Unreleased]` Added block distinguishes span PERSIST (zero host wiring) from trace JOIN (requires forwarded `trace_id`/`parent_id`/`tenant_id`), links the new guide, no ALL-CAPS "ORPHAN"; separate default-pipeline persistence claim left untouched | ✓ VERIFIED | `CHANGELOG.md` L194-201: dedicated adapter paragraph states "persist to Postgres with zero host wiring" and the join caveat, links `guides/capabilities/llm-and-tool-adapters.md`. `grep -c ORPHAN CHANGELOG.md` = 0. `grep -ic 'trace_id\|join'` = 3. Default-pipeline paragraph (L182-190) unchanged in substance, still byte-consistent with `application_test.exs:6-7`'s claim (that test passes). |

**Score:** 7/7 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/scoria/application.ex` | `safe_attach_observe_req_llm/0` + `safe_attach_observe_jido/0`, each called from `observe_children/0` | ✓ VERIFIED | Both private helpers defined (L83-103), both called (L46-47) inside the `enabled != false` branch. |
| `test/scoria/observe/adapters/boot_attach_test.exs` | Additive SC#2 boot-path test (only new test file) | ✓ VERIFIED | Created, 4 tests, all pass against real Postgres via sandbox. |
| `test/scoria/workflows/runtime_span_test.exs` | Corrected moduledoc | ✓ VERIFIED | Moduledoc-only diff confirmed via `git show`. |
| `CHANGELOG.md` | Amended `[Unreleased]` Added block | ✓ VERIFIED | Persist-vs-join paragraph present, guide linked, no ORPHAN caps. |
| `guides/capabilities/llm-and-tool-adapters.md` | New adopter guide (5-point D-05 outline) | ✓ VERIFIED | File exists (2.9KB), covers: boot-attach behavior, persist-vs-join, metadata forwarding (points at moduledocs as SSOT), ownership-boundary tie-in, jido asymmetry. No autolinking backtick references to filtered modules. |
| `guides/troubleshooting.md` | New orphan-span entry (edit only) | ✓ VERIFIED | "Why is my LLM/tool call a standalone single-span trace?" entry present, links the new guide. |
| `mix.exs` + `lib/mix/tasks/scoria.release_preview.ex` + `test/scoria/package_surface_test.exs` + `test/mix/tasks/scoria.release_preview_test.exs` | Guide registered across all six mirrored inventory sites | ✓ VERIFIED | Counts match exactly: mix.exs=3, release_preview.ex=1, package_surface_test.exs=2, release_preview_test.exs=1. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `observe_children/0` | `safe_attach_observe_req_llm/0` / `safe_attach_observe_jido/0` | direct call inside `enabled != false` branch | ✓ WIRED | Confirmed by source read + passing `application_test.exs`. |
| `[:req_llm, :request, :stop]` | `Scoria.Observe.Buffer` → Postgres (`ai_spans`) | "scoria-observe-reqllm" handler → `[:scoria, :observe, :span, :stop]` → Buffer | ✓ WIRED | Live-DB test proof: `boot_attach_test.exs` executes the real telemetry event with no attach/0 call and reads the persisted row back via `Repo.get_by!/2`. |
| `[:jido, :action, :stop]` | Postgres (`ai_spans`) | "scoria-observe-jido" handler → same span pipeline | ✓ WIRED (dormant in prod without a Jido producer, as documented) | Same test file, hand-synthesized event persists correctly. |
| `guides/capabilities/llm-and-tool-adapters.md` | mix.exs `docs_extras` / `docs_extra_groups` Capabilities / package `files:` | registration | ✓ WIRED | grep counts match (3 sites in mix.exs). |
| same guide path | `release_preview.ex` `@required_package_paths` + both surface tests' `@canonical_guides` | mirrored inventory | ✓ WIRED | `mix test test/scoria/package_surface_test.exs test/mix/tasks/scoria.release_preview_test.exs test/scoria/hex_consumer_contract_test.exs` — ran directly, 28 tests, 0 failures. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Boot-attach compiles clean | `mix compile --warnings-as-errors` | exit 0, no output | ✓ PASS |
| SC#1/SC#2/SC#3 blast-radius suite | `mix test test/scoria/application_test.exs test/scoria/observe/adapters/boot_attach_test.exs test/scoria/observe/adapters/req_llm_test.exs test/scoria/observe/adapters/jido_test.exs test/scoria/workflows/runtime_span_test.exs test/scoria_web/live/orchestrator_live_integration_test.exs` | 32 tests, 0 failures | ✓ PASS |
| boot_attach_test.exs robust across suite orderings | `mix test test/scoria/observe/adapters/boot_attach_test.exs --seed 0` then `--seed 12345` | 4 tests, 0 failures (both runs) | ✓ PASS |
| Docs-source inventory + package surface | `mix test test/scoria/package_surface_test.exs test/mix/tasks/scoria.release_preview_test.exs test/scoria/hex_consumer_contract_test.exs` | 28 tests, 0 failures | ✓ PASS |
| Docs build clean under warnings-as-errors | `MIX_ENV=dev mix docs --warnings-as-errors` | exit 0, generated cleanly | ✓ PASS |
| Full suite (verifier-run, once) | `mix test` | 3 doctests, 1319 tests, 1 failure (60 excluded) | ✓ PASS (see note) |

**Note on the one full-suite failure:** `test/scoria/warning_inventory/capture_parity_test.exs` — this is a pre-existing, well-documented order-dependent flake (referenced in `.planning/v3.6-MILESTONE-AUDIT.md` as "SEED-004 class, last touched Phase 28... passes standalone; unrelated to v3.6. Carried across all four phases' verifications" and freshly re-filed as tracked debt at commit `273fc77b`, dated the same day as this phase). Verifier confirmed independently: `mix test test/scoria/warning_inventory/capture_parity_test.exs` in isolation passes (2 tests, 0 failures). This is not caused by, or related to, any file this phase touched.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| SPAN-01 | 54.1-01, 54.1-02 | LLM span OTel-GenAI attributes (originally satisfied Phase 51) — this phase closes the runtime-reachability gap so the attribute-bearing adapter actually fires in production | ✓ SATISFIED | Boot-attach wiring + live persistence proof (Truth #2). |
| SPAN-02 | 54.1-01 | Correct `span_kind` taxonomy (originally satisfied Phase 51) — reachability closed by boot-attach | ✓ SATISFIED | Adapter internals untouched (confirmed via read — `span_kind` logic unchanged), now reachable via boot attach. |
| COMPAT-01 | 54.1-01, 54.1-02 | CHANGELOG-documented legacy-key decision (originally satisfied Phase 51) — this phase's scope is the persist-vs-join CHANGELOG reconciliation | ✓ SATISFIED | Truth #7. |
| EVENT-01 | 54.1-01, 54.1-02 | Tool/prompt/retrieval/guardrail as real child spans (originally satisfied Phase 53) — reachability of the TOOL-kind Jido leg closed here | ✓ SATISFIED | Truth #3 (Jido leg) + Truth #2 (ReqLLM leg). |

**Note:** These four requirement IDs are mapped in `.planning/REQUIREMENTS.md`'s traceability table to Phase 51 (SPAN-01/SPAN-02/COMPAT-01) and Phase 53 (EVENT-01), not to Phase 54.1 — expected, since Phase 54.1 is an inserted milestone-audit gap-closure phase, not part of the original 16-requirement/4-phase mapping. No orphaned requirements: the traceability table's "16 mapped / 0 unmapped" count is unaffected by this insertion, and ROADMAP.md explicitly documents Phase 54.1 as "(INSERTED)" addressing the same four IDs' runtime-reachability gap. This is a legitimate extension of already-"Complete" requirements' actual production availability, not a re-opening or contradiction of their Phase 51/53 completion status.

### Anti-Patterns Found

None. Scanned all phase-touched files (`lib/scoria/application.ex`, `test/scoria/observe/adapters/boot_attach_test.exs`, `test/scoria/workflows/runtime_span_test.exs`, `CHANGELOG.md`, `guides/capabilities/llm-and-tool-adapters.md`, `guides/troubleshooting.md`, adapter modules) for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` and "placeholder/coming soon/not yet implemented" phrasing — zero matches.

### Human Verification Required

None. All must-haves are mechanically verifiable (code presence, wiring, live-Postgres persistence via ExUnit against the sandbox, grep-based doc/inventory consistency, and a full docs build). No visual, UX, or external-service-dependent claims in this phase's scope.

### Gaps Summary

No gaps. Every ROADMAP success criterion (SC#1, SC#2, SC#3) and every PLAN-frontmatter must-have from both 54.1-01 and 54.1-02 is verified directly against the live codebase — not merely asserted by SUMMARY.md. The boot-attach wiring was read from source and independently exercised: `Scoria.Application.observe_children/0` now calls both new helpers inside the single `enabled != false` gate; the SC#2 boot-path test was re-run twice under different seeds against a real Postgres instance and both passed; the `runtime_span_test.exs` moduledoc correction was diff-verified to be confined to the moduledoc; the CHANGELOG amendment and new guide/troubleshooting entry/inventory registration were grep- and build-verified (`mix docs --warnings-as-errors` exits 0); and the one full-suite test failure (`capture_parity_test.exs`) was independently confirmed to be a pre-existing, isolation-passing, project-tracked flake unrelated to any file this phase touched.

One process-hygiene note (not a phase-goal gap): `.planning/ROADMAP.md` line 24 still shows Phase 54.1's roadmap-level checkbox as `[ ]` even though both of its plans are checked `[x]` and `.planning/STATE.md` still says "EXECUTING" / "Phase 54.1 execution started" — this is bookkeeping that the workflow updates after verification passes, not a code or documentation gap in the deliverable itself.

---

*Verified: 2026-07-18T23:43:31Z*
*Verifier: Claude (gsd-verifier)*
