---
phase: 52-retriever-span-host-declared-attributes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/observe/semconv.ex
  - test/scoria/observe/semconv_test.exs
autonomous: true
requirements: [RETR-02, ATTR-01, ATTR-02]
must_haves:
  truths:
    - "Semconv owns every new key string; no other module inlines a reserved key literal (D-00c)."
    - "retrieval_config_attributes/1 always emits all three keys with a \"none\" sentinel, never nil (D-RETR02-4)."
    - "merge_host_declared/2 skips absent host keys entirely — empty metadata yields no reserved keys (D-ATTR01-2/6)."
    - "prompt_context/1 emits IDs and counts only, never raw chunk/memory text (D-ATTR02-4)."
  artifacts:
    - lib/scoria/observe/semconv.ex
    - test/scoria/observe/semconv_test.exs
  key_links:
    - "retrieval_config_keys/0 keyword list is the single origin the RETR-02 span↔table guard reads on both sinks."
    - "host_declared_keys/0 atom list is the single origin the RETRIEVER, prompt, and adapter spans all reduce over."
---

<objective>
Extend `Scoria.Observe.Semconv` with the three sibling projection families that the entire phase composes through — the one shared, Semconv-owned metadata→attributes seam (D-00/D-00c, RESEARCH Recommendation 4). Add the drift-guard unit tests (canary + shape + never-default + never-text + anti-inline grep) that structurally prove single-origin key ownership.

Purpose: Every RETR-02 config key, ATTR-01 host-declared key, and ATTR-02 context-pack key string lives in exactly one module. Both emitters (52-03) and both adapters (52-05) and `retrieve/2` (52-04) call these functions instead of inlining literals, so a rename is a one-file diff and the guard tests catch any divergence.
Output: Six new public functions on `Semconv`; three new `describe` blocks in `semconv_test.exs`.

## Artifacts This Phase Produces (full phase roll-up)
This plan delivers the six Semconv functions marked ★ below. The remaining symbols are delivered by sibling plans (referenced for executor context):
- ★ `Scoria.Observe.Semconv.retrieval_config_keys/0`
- ★ `Scoria.Observe.Semconv.retrieval_config_attributes/1`
- ★ `Scoria.Observe.Semconv.host_declared_keys/0`
- ★ `Scoria.Observe.Semconv.merge_host_declared/2`
- ★ `Scoria.Observe.Semconv.prompt_context_key/0`
- ★ `Scoria.Observe.Semconv.prompt_context/1`
- `Scoria.Observe.emit_retriever_span/1` (52-03)
- `Scoria.Observe.emit_prompt_span/1` (52-03)
- `Scoria.Knowledge.Embedder.model_name/0` optional callback + `Deterministic.model_name/0` (52-02)
- `Scoria.Knowledge.retrieve/2` additive return fields `trace_id`/`span_id` + new `:embedder` opt (52-04)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-retriever-span-host-declared-attributes/52-CONTEXT.md
@.planning/phases/52-retriever-span-host-declared-attributes/52-RESEARCH.md
@.planning/phases/52-retriever-span-host-declared-attributes/52-PATTERNS.md
</context>

<!-- planner-discipline-allow: scoria.retrieval. -->
<!-- planner-discipline-allow: scoria.prompt.context -->

<tasks>

<task type="auto">
  <name>Task 1: Add retrieval-config projection (RETR-02 keys + sentinel)</name>
  <files>lib/scoria/observe/semconv.ex, test/scoria/observe/semconv_test.exs</files>
  <read_first>
    - lib/scoria/observe/semconv.ex (the existing `openinference_span_kind_key/0` + `merge_req_llm_attributes/2` sibling shape to mirror — module-attribute constant + @doc + @spec + thin function)
    - .planning/phases/52-retriever-span-host-declared-attributes/52-PATTERNS.md § "lib/scoria/observe/semconv.ex (extend)" (the sibling-function delta list)
    - .planning/phases/52-retriever-span-host-declared-attributes/52-RESEARCH.md § "The RETR-02 consistency guard" code example (the exact canary keyword list is defined there — copy the dotted strings verbatim from that anchor)
  </read_first>
  <action>
    Add `retrieval_config_keys/0` returning a keyword list mapping `:embedding_model`, `:index_version`, and `:reranker` to their dotted attribute-key strings under the Scoria retrieval-config namespace (the exact three strings are in the RESEARCH canary anchor — take them verbatim; do not paraphrase the dotted key). Back it with a module-attribute constant `@retrieval_config_keys` per the existing `@openinference_span_kind_key` shape (D-RETR02-3, D-00c).
    Add `retrieval_config_attributes/1` taking one canonical map with keys `:embedding_model`, `:index_version`, `:reranker`; for each of the three, look up the value and normalise a nil/absent value to the literal sentinel `"none"` (never nil, never omit — D-RETR02-4); project onto the dotted string keys from `retrieval_config_keys/0` so the produced map always has exactly three string-keyed entries. Do NOT reject nils; the `"none"` sentinel replaces the reject-nils step (per PATTERNS delta).
    Update the module moduledoc to state these keys are now implemented (not "reserved").
  </action>
  <verify>
    <automated>mix test test/scoria/observe/semconv_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `Semconv.retrieval_config_keys/0` returns a keyword list of exactly `[:embedding_model, :index_version, :reranker]` mapped to their dotted string keys, byte-identical to the RESEARCH canary anchor.
    - `Semconv.retrieval_config_attributes(%{})` returns a map of `map_size == 3` where every value is the binary `"none"` (proves sentinel-never-nil / never-omit).
    - `Semconv.retrieval_config_attributes(%{embedding_model: "m", index_version: "v1", reranker: "r"})` returns the three dotted-string keys mapped to `"m"`/`"v1"`/`"r"`.
    - A canary test asserts the exact `retrieval_config_keys/0` keyword list; it fails if any key string is renamed on one side.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add host-declared seam (ATTR-01 bare keys + merge)</name>
  <files>lib/scoria/observe/semconv.ex, test/scoria/observe/semconv_test.exs</files>
  <read_first>
    - lib/scoria/observe/semconv.ex (the moduledoc already reserves feature/route/archetype/intent — line 13; and the sibling-function shape)
    - .planning/phases/52-retriever-span-host-declared-attributes/52-CONTEXT.md § D-ATTR01-1/2/3 (bare keys, Enum.reduce skip-nil, atom-keyed map input)
    - .planning/phases/52-retriever-span-host-declared-attributes/52-PATTERNS.md § semconv.ex delta (`host_declared_keys/0`, `merge_host_declared/2` verbatim reduce shape)
  </read_first>
  <action>
    Add `host_declared_keys/0` returning the atom list of the four reserved host dimensions in the order feature, route, archetype, intent (use the `~w(...)a` sigil form; these are the bare keys per D-00b — no prefix). Back it with a module-attribute constant.
    Add `merge_host_declared/2` with spec `(map(), map()) :: map()`. It takes an accumulator attributes map and an atom-keyed source metadata map; `Enum.reduce` over `host_declared_keys/0`; for each key read `Map.get(metadata, key)`; when the value is nil, skip (do not default, do not put); otherwise `Map.put(acc, Atom.to_string(key), value)`. This is the single seam reused by the RETRIEVER span, the prompt span, and both adapters (D-ATTR01-2/5). Do NOT add any length bound, truncation, or enum validation — value hygiene stays the existing Redactor (D-ATTR01-4).
  </action>
  <verify>
    <automated>mix test test/scoria/observe/semconv_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `Semconv.host_declared_keys/0` returns exactly `[:feature, :route, :archetype, :intent]` (canary).
    - `Semconv.merge_host_declared(%{}, %{})` returns `%{}` and `refute Map.has_key?` holds for every reserved key — the never-default proof (D-ATTR01-6b).
    - `Semconv.merge_host_declared(%{}, %{feature: "support-copilot"})` returns `%{"feature" => "support-copilot"}` byte-for-byte (string key, unmodified value) and contains no route/archetype/intent key.
    - A non-nil value passes through byte-identical (no truncation, no coercion).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add prompt-context builder (ATTR-02 never-text) + anti-inline grep guards</name>
  <files>lib/scoria/observe/semconv.ex, test/scoria/observe/semconv_test.exs</files>
  <read_first>
    - lib/scoria/observe/semconv.ex
    - test/scoria/observe/semconv_test.exs (the existing anti-inline grep test at lines 81-89 that refutes a `~r/"gen_ai\./` literal — mirror this shape, inverted target)
    - .planning/phases/52-retriever-span-host-declared-attributes/52-CONTEXT.md § D-ATTR02-3/4/6/7 (nested shape, never-text structural map, ≤100 cap + truncated marker, omit-when-empty)
    - .planning/phases/52-retriever-span-host-declared-attributes/52-PATTERNS.md § semconv.ex delta (`prompt_context_key/0`, `prompt_context/1`)
  </read_first>
  <action>
    Add `prompt_context_key/0` returning the single Scoria prompt-context namespace key string (exact string in the CONTEXT D-ATTR02-3 anchor — take verbatim), backed by a module-attribute constant.
    Add `prompt_context/1` taking a map with `:chunks`, `:memories` (each a list of item maps carrying at least `:id` and `:tokens`) and `:token_budget` (a map with `:total`, `:chunks`, `:memories`, `:overhead`). Build a nested string-keyed map: `"chunks"` and `"memories"` are lists where each item is mapped to ONLY `%{"id" => item id, "tokens" => item token count}` — never pass through the host's raw item map, so a `text`/`content`/`body` field on an over-sharing host item cannot reach the span (D-ATTR02-4, the structural never-text guarantee). `"token_budget"` maps the four integer sub-fields. Cap each of chunks/memories at ≤100 items; when a list is truncated add `"truncated" => true` at the top level (D-ATTR02-6).
    Do NOT build the omit-when-empty behaviour here — `prompt_context/1` builds the value; the emitter (52-03) decides whether to attach it (D-ATTR02-7). Document that contract in the @doc.
    Add three new `describe` blocks to `test/scoria/observe/semconv_test.exs`, one per new family (retrieval-config, host-declared, prompt-context), covering: canary key assertions; the sentinel/never-default/pass-through behaviours from Tasks 1-2; the prompt-context never-text structural guard (over a fully-built value: assert every map key is in the allowed set `chunks/memories/token_budget/id/tokens/total/overhead/truncated`, assert no key matches `~r/text|content|body|message|prompt|raw/i`, assert every leaf is a binary ≤64 bytes or a non-negative integer, assert `Jason.encode!/1` of the value is ≤ 8192 bytes); the ≤100-item cap producing `"truncated" => true`.
    Add anti-inline grep guards mirroring the existing `~r/"gen_ai\./` test: for each reserved family, read each of the consumer lib files that exist — `lib/scoria/knowledge.ex`, `lib/scoria/observe.ex`, `lib/scoria/observe/adapters/req_llm.ex`, `lib/scoria/observe/adapters/jido.ex` — filtered with `File.exists?/1` (observe.ex is created by 52-03 in a later wave), and `refute` the reserved key literal appears in any of them. Scope the grep to lib consumer files only (not `semconv.ex`, not the test file) so the legitimate definition and the canary assertion are not flagged.
  </action>
  <verify>
    <automated>mix test test/scoria/observe/semconv_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `Semconv.prompt_context_key/0` returns the exact prompt-context key string (canary).
    - Building `prompt_context/1` from an item that also carries a `:text`/`:body` field produces a value whose chunk/memory items contain ONLY `"id"` and `"tokens"` keys (the smuggled field is absent).
    - No key in the built value matches `~r/text|content|body|message|prompt|raw/i`; every leaf is a binary ≤64B or a non-negative integer; `Jason.encode!/1` ≤ 8192 bytes.
    - A 101-item chunk list is capped to 100 items with `"truncated" => true` present.
    - Anti-inline grep tests are green: no existing lib consumer file contains a reserved key literal (File.exists?-filtered so the missing `observe.ex` does not error).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → Scoria (attribute values) | Host-supplied `context_pack` item maps and host-declared dimension values cross into span attributes here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-01 | Information Disclosure | `prompt_context/1` | high | mitigate | Structural map-to-`%{"id","tokens"}` drops any host `text`/`content`/`body` field; regression test asserts no `~r/text\|content\|body\|message\|prompt\|raw/i` key on the built value (D-ATTR02-4). |
| T-52-02 | Denial of Service | `prompt_context/1` (jsonb/GIN bloat) | medium | mitigate | ≤100-item cap per list + `"truncated"` marker + `Jason.encode!` ≤8KB guard now; uniform write-time bound deferred to SEC-01/Phase 53 (D-ATTR02-6). |
| T-52-03 | Information Disclosure | deny-listed key named as a host dimension | low | accept | Existing `Redactor` key-based recursion redacts `token`/`secret`/`password`/`api_key` values downstream; documented behaviour (Pitfall 3), no new hygiene added (D-ATTR01-4). |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase — pure application-layer additions over modules already in tree (RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `mix test test/scoria/observe/semconv_test.exs` green (canary + sentinel + never-default + pass-through + never-text + cap + anti-inline grep).
- `mix test test/scoria/observe/` green (no regression to existing Semconv/adapter tests).
</verification>

<success_criteria>
- Six new Semconv functions exist with @spec + @doc, each key string owned by exactly one module.
- Every drift-guard test class from the VALIDATION Per-Task Verification Map that targets `semconv_test.exs` is present and green.
</success_criteria>

<output>
Create `.planning/phases/52-retriever-span-host-declared-attributes/52-01-SUMMARY.md` when done.
</output>
