---
phase: 52-retriever-span-host-declared-attributes
plan: 06
type: execute
wave: 3
depends_on: ["52-03"]
files_modified:
  - test/scoria/observe/prompt_span_test.exs
autonomous: true
requirements: [ATTR-02, ATTR-01]
must_haves:
  truths:
    - "A populated context_pack (≥1 chunk AND ≥1 memory with token counts) emitted via emit_prompt_span/1 persists a span whose attributes carry the nested scoria.prompt.context map coexisting with gen_ai.usage.input_tokens — IDs and counts only, never raw text (SC#4, reinterpreted per D-ATTR02-1)."
    - "input_tokens absence is tolerated — the usage key is simply absent, no unconditional-presence assertion (D-ATTR02-5)."
    - "Host-declared keys ride the prompt span on a REAL emit_prompt_span/1 emission, not a hand-synthesized event (D-ATTR01-6)."
    - "The context_pack is HOST-SUPPLIED (Scoria never assembles the RAG prompt — the only build_prompt/2 is compaction-internal — so it cannot infer which chunks/memories entered the budget); chunk IDs are the same UUIDs the RETRIEVER span records, so operators join retriever→prompt on the ID value (D-ATTR02-2)."
  artifacts:
    - test/scoria/observe/prompt_span_test.exs
  key_links:
    - "The persisted prompt span joins to the RETRIEVER span on the shared chunk/memory ID values operators can query."
---

<objective>
Deliver the SC#4 acceptance test: a real `Scoria.Observe.emit_prompt_span/1` emission with a populated context-pack, flushed through `Buffer.flush_now/1`, persists an `ai_spans` row whose attributes carry the nested `scoria.prompt.context` composition map (which chunk IDs, which memory IDs, per-source token split) coexisting with `gen_ai.usage.input_tokens` — IDs and counts only, never raw text (D-ATTR02-7). Also prove ATTR-01 host-key pass-through on the prompt span on a real emission, `input_tokens`-absence tolerance, and the never-text structural guarantee end-to-end.

Purpose: This is the headline ATTR-02 acceptance criterion (ROADMAP SC#4). The plan records the SC#4 reinterpretation: the attributes ride the Scoria-emitted composition span now; Phase 53 relocates the identical keys onto a real PROMPT child span with zero contract change (D-ATTR02-1).
Output: `test/scoria/observe/prompt_span_test.exs` — a real-Postgres integration test over `emit_prompt_span/1`.

## Artifacts This Plan Produces
- `test/scoria/observe/prompt_span_test.exs`
(Full phase artifact roll-up is in 52-01-PLAN.md. The emit_prompt_span/1 function itself is delivered by 52-03.)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/52-retriever-span-host-declared-attributes/52-CONTEXT.md
@.planning/phases/52-retriever-span-host-declared-attributes/52-RESEARCH.md
@lib/scoria/observe/buffer.ex
@lib/scoria/observe/semconv.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC#4 prompt-span acceptance (real emit + flush_now + persisted-span assertions)</name>
  <files>test/scoria/observe/prompt_span_test.exs</files>
  <read_first>
    - lib/scoria/observe.ex (the emit_prompt_span/1 contract from 52-03: opts keys trace_id/span_id/parent_id/context_pack/input_tokens + host-declared keys)
    - lib/scoria/observe/buffer.ex (flush_now/1 at :23 — call before querying ai_spans)
    - lib/scoria/observe/semconv.ex (prompt_context_key/0 and the usage input-tokens key source)
    - .planning/phases/52-retriever-span-host-declared-attributes/52-CONTEXT.md § D-ATTR02-3/4/5/7, D-ATTR01-6
    - test/scoria/observe/telemetry_test.exs or buffer_test.exs (an existing observe test that exercises the Buffer + Repo persistence path — mirror its setup/DB case + flush_now usage)
  </read_first>
  <action>
    Create `test/scoria/observe/prompt_span_test.exs` using the observe DB-backed test case (mirror the setup an existing Buffer/telemetry test uses so `Buffer.flush_now/1` + `Repo.get` work against real Postgres).
    Write the SC#4 acceptance test: build a populated `context_pack` with at least one chunk (`%{id: <uuid>, tokens: <int>}`) AND at least one memory (`%{id: <uuid>, tokens: <int>}`) plus a `token_budget` map; call `emit_prompt_span/1` with that pack, a fresh `trace_id`/`span_id`, and a host `input_tokens` value; call `Scoria.Observe.Buffer.flush_now()`; load the persisted span via `Repo.get_by!(Scoria.Repo.Span, id: <the span id>)`; assert its attributes contain `Semconv.prompt_context_key()` mapped to the nested map (with `"chunks"`/`"memories"`/`"token_budget"`, each item `%{"id" => ..., "tokens" => ...}`) AND the `gen_ai.usage.input_tokens` key mapped to the host value — the two coexist on the same persisted span (D-ATTR02-7).
    Add a never-text end-to-end assertion over the persisted `scoria.prompt.context` value: no key matches `~r/text|content|body|message|prompt|raw/i`; every chunk/memory item has only `"id"` and `"tokens"` keys; every leaf is an ID binary or a non-negative integer.
    Add an ATTR-01-on-the-prompt-span test: pass a non-deny-listed host `feature` value through `emit_prompt_span/1`; after flush, assert the persisted span attributes carry `"feature" => <value>` byte-for-byte, and an omitted host key is absent (`refute Map.has_key?`) — a REAL emission, not hand-synthesized (D-ATTR01-6).
    Add an `input_tokens`-absence test: call `emit_prompt_span/1` with the populated pack but no `input_tokens`; after flush, assert `scoria.prompt.context` is present AND the usage input-tokens key is ABSENT — tolerate absence, no unconditional-presence (D-ATTR02-5).
    Add an empty-pack omit test: call `emit_prompt_span/1` with no context_pack; after flush, assert `Semconv.prompt_context_key()` is ABSENT from the persisted span attributes (D-ATTR02-7).
  </action>
  <verify>
    <automated>mix test test/scoria/observe/prompt_span_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - With a populated pack (≥1 chunk AND ≥1 memory with tokens) + host input_tokens, the persisted span attributes carry BOTH the nested `scoria.prompt.context` map and `gen_ai.usage.input_tokens` (coexistence, SC#4) after `Buffer.flush_now/1`.
    - The persisted composition value contains only IDs and counts — no key matches `~r/text|content|body|message|prompt|raw/i`; items have only `"id"`/`"tokens"`.
    - A non-deny-listed host `feature` value passes byte-for-byte onto the prompt span on a real emission; an omitted host key is absent.
    - With `input_tokens` absent, `scoria.prompt.context` persists and the usage key is absent.
    - With no context_pack, the `scoria.prompt.context` key is absent from the persisted span.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host prompt-assembly → emit_prompt_span/1 → persisted span | Host-supplied context_pack + host keys cross into persisted jsonb attributes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-06a | Information Disclosure | raw chunk/memory text leaking onto the persisted span | high | mitigate | End-to-end never-text assertion over the persisted `scoria.prompt.context` value proves items carry only `"id"`/`"tokens"` and no `~r/text\|content\|body\|message\|prompt\|raw/i` key (D-ATTR02-4, ASVS V8). |
| T-52-06b | Denial of Service | oversized/high-cardinality composition value | medium | mitigate | The ≤100-item cap + `Jason.encode!` ≤8KB guard (built in `prompt_context/1`, 52-01) bounds the persisted value; uniform write-time bound deferred to SEC-01/Phase 53 (D-ATTR02-6). |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `mix test test/scoria/observe/prompt_span_test.exs` green (SC#4 coexistence, never-text, ATTR-01 pass-through, input_tokens absence, empty-pack omit — real Postgres after flush_now/1).
- `mix test` full suite green before `/gsd-verify-work` (phase gate).
</verification>

<success_criteria>
- SC#4 is proven: a real emit_prompt_span/1 populated-pack emission persists a span carrying nested scoria.prompt.context coexisting with gen_ai.usage.input_tokens, IDs and counts only.
</success_criteria>

<output>
Create `.planning/phases/52-retriever-span-host-declared-attributes/52-06-SUMMARY.md` when done.
</output>
