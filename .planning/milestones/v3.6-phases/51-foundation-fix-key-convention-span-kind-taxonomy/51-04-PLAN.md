---
phase: 51-foundation-fix-key-convention-span-kind-taxonomy
plan: 04
type: execute
wave: 2
depends_on: ["51-01", "51-02", "51-03"]
files_modified:
  - lib/scoria/observe/adapters/req_llm.ex
  - test/scoria/observe/adapters/req_llm_test.exs
  - CHANGELOG.md
autonomous: true
requirements: [SPAN-01, SPAN-02, COMPAT-01]

must_haves:
  truths:
    - "a persisted LLM span carries gen_ai.request.model, .temperature, .top_p, .max_tokens, .seed AND gen_ai.usage.* together (all four model-config params on the same span, never a partial subset), sourced via ReqLLM.OpenTelemetry.Attributes through Semconv — SC#2"
    - "the ReqLLM adapter sets a native-lowercase span_kind via SpanKind.normalize/2 and a mirrored openinference.span.kind attribute keyed by Semconv.openinference_span_kind_key/0 — SC#3"
    - "the legacy keys llm.model_name, llm.token_count, req.url are absent from new span attributes (clean replacement, no dual-emit) — COMPAT-01/D-01"
    - "every gen_ai.*/openinference.* key the adapter writes comes from Semconv, never an inline string literal in req_llm.ex — SC#4"
  artifacts:
    - "lib/scoria/observe/adapters/req_llm.ex (rewritten attribute + span_kind construction)"
    - "test/scoria/observe/adapters/req_llm_test.exs (realistic %LLMDB.Model{} fixture, gen_ai.* completeness, legacy-key absence, openinference mirror)"
    - "CHANGELOG.md (0.1.4 breaking-change entry: old->new key mapping table + upgrade sentence + flush_error/on_flush_error announcement)"
  key_links:
    - "req_llm.ex calls Semconv.merge_req_llm_attributes/2 (sole gen_ai.* source) + Semconv.openinference_span_kind_key/0 + SpanKind.normalize/2 + SpanKind.to_openinference/1 — no gen_ai.*/openinference.* literal in the adapter"
    - "the FK fix (plan 03) + this key rename land together in the 0.1.4 cut as one atomic change — D-04"
---

<objective>
Rewrite the ReqLLM telemetry adapter to (a) merge the full `gen_ai.*` attribute set via `Semconv.merge_req_llm_attributes/2` (SPAN-01), (b) set a correct native-lowercase `span_kind` + mirrored `openinference.span.kind` through `SpanKind`/`Semconv` (SPAN-02), (c) DROP the legacy keys `llm.model_name`/`llm.token_count`/`req.url` entirely (COMPAT-01 clean replacement), and ship the `0.1.4` CHANGELOG breaking-change entry documenting the old->new mapping plus the FOUND-01 flush-error surface.

Purpose: Deliver the adopter-visible key convention and fix the second (masked) bug where `metadata[:model]` is a raw `%LLMDB.Model{}` struct being stuffed into jsonb. Per D-04 this key rename is atomic with the plan-03 FK fix — both land in the same 0.1.4 cut.

Output: rewritten adapter + realistic-fixture test + CHANGELOG entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-CONTEXT.md
@.planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-RESEARCH.md
@.planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-PATTERNS.md
@lib/scoria/observe/adapters/req_llm.ex
@deps/req_llm/lib/req_llm/open_telemetry/attributes.ex
</context>

## Artifacts this phase produces (this plan)

- Rewritten `Scoria.Observe.Adapters.ReqLLM.handle_event/4` (Semconv + SpanKind wiring, legacy keys dropped)
- Updated `test/scoria/observe/adapters/req_llm_test.exs`
- CHANGELOG.md `0.1.4` breaking-change entry (COMPAT-01 mapping table + FOUND-01 announcement)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite ReqLLM adapter — Semconv gen_ai.* merge, SpanKind wiring, drop legacy keys</name>
  <files>lib/scoria/observe/adapters/req_llm.ex, test/scoria/observe/adapters/req_llm_test.exs</files>
  <read_first>
    - lib/scoria/observe/adapters/req_llm.ex (current attribute + span-map construction :15-40 — the exact block to replace)
    - .planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-PATTERNS.md (§req_llm.ex — target shape, the incidental %LLMDB.Model{} struct bug to fix)
    - .planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-RESEARCH.md (§Code Examples exact gen_ai.* key set; Pitfall 1 struct-in-jsonb; Pitfall 2 casing; Anti-Patterns)
    - .planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-CONTEXT.md (D-01 drop legacy keys; D-10/D-11 casing + kinds; D-16 keys from Semconv)
    - lib/scoria/observe/semconv.ex (produced by plan 02 — merge_req_llm_attributes/2, openinference_span_kind_key/0)
    - lib/scoria/observe/span_kind.ex (produced by plan 01 — normalize/2, to_openinference/1)
  </read_first>
  <behavior>
    - Test: a [:req_llm,:request,:stop] event with a realistic %LLMDB.Model{} metadata[:model], request_options (temperature/top_p/max_tokens/seed) and usage produces a span whose attributes contain gen_ai.request.model + .temperature + .top_p + .max_tokens + .seed + a gen_ai.usage.* key together
    - Test: the produced span has span_kind == "llm" (lowercase-native) and attributes["openinference.span.kind"] == "LLM"
    - Test: the produced span attributes do NOT contain "llm.model_name", "llm.token_count", or "req.url"
    - Test: tenant_id / workflow_run_id from metadata still appear in the attributes map when present
  </behavior>
  <action>
    Replace the `attributes` construction: start from a base map of Scoria-domain keys only (`"tenant_id"`/`"workflow_run_id"` when non-nil), then pass it through `Scoria.Observe.Semconv.merge_req_llm_attributes(base, metadata)` to fold in the req_llm-owned `gen_ai.*` set. DROP `"llm.model_name"`, `"llm.token_count"`, `"req.url"` entirely (D-01 — no dual-emit) and never read raw `metadata[:model]`/`metadata[:url]`/`measurements[:total_tokens]` into an attribute literal (Pitfall 1: metadata[:model] is a `%LLMDB.Model{}` struct — the builder's `request_model/1` extracts `.id` for free). Set `span_kind: Scoria.Observe.SpanKind.normalize(metadata[:operation] || "llm")` (replaces the `"LLM"` literal, Pitfall 2 casing fix). Add the mirror by putting `Scoria.Observe.SpanKind.to_openinference(span_kind)` into the attributes map under the key `Scoria.Observe.Semconv.openinference_span_kind_key()` — NEVER hardcode the `"openinference.span.kind"` string or the UPPERCASE value in the adapter. Preserve the surrounding span-map shape (name, start_time/end_time, trace_id fallback, parent_id, session_id) and the final `:telemetry.execute([:scoria,:observe,:span,:stop], %{}, span)`. Add module aliases for `Semconv` and `SpanKind`.
  </action>
  <verify>
    <automated>mix test test/scoria/observe/adapters/req_llm_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -Ec '"(llm\.model_name|llm\.token_count|req\.url)"' lib/scoria/observe/adapters/req_llm.ex` == 0
    - Source assertion: `grep -Ec '"(gen_ai\.|openinference\.span\.kind)"' lib/scoria/observe/adapters/req_llm.ex` == 0 (all such keys come from Semconv, none inline)
    - Source assertion: `grep -c 'span_kind: "LLM"' lib/scoria/observe/adapters/req_llm.ex` == 0 and `grep -c 'SpanKind.normalize' lib/scoria/observe/adapters/req_llm.ex` >= 1
    - Behavior: the SC#2 completeness test (all four model-config params + a usage key present together) passes
  </acceptance_criteria>
  <done>Adapter merges gen_ai.* via Semconv, sets lowercase span_kind + mirrored openinference key via SpanKind/Semconv, legacy keys gone, struct-in-jsonb bug fixed; no inline convention-key literals.</done>
</task>

<task type="auto">
  <name>Task 2: Update req_llm_test.exs — realistic %LLMDB.Model{} fixture + SPAN-01/SPAN-02/COMPAT-01 assertions</name>
  <files>test/scoria/observe/adapters/req_llm_test.exs</files>
  <read_first>
    - test/scoria/observe/adapters/req_llm_test.exs (current fixture uses model: "gpt-4" string + asserts old llm.model_name/llm.token_count — the exact unrealistic path to replace)
    - .planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-RESEARCH.md (Pitfall 1 — the realistic %LLMDB.Model{} shape and why the old fixture masked the bug; §Code Examples for the exact keys to assert)
    - deps/llm_db/lib/llm_db/model.ex (%LLMDB.Model{} struct fields, @derive Jason.Encoder)
    - .planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-CONTEXT.md (D-11 mcp->TOOL context; SC#2 never-a-partial-subset)
  </read_first>
  <action>
    Extend `test/scoria/observe/adapters/req_llm_test.exs` (already begun in Task 1) — replace the `metadata` fixture: use a real `%LLMDB.Model{}` struct (or a faithful struct-shaped value with `:id`, `:provider`) for `model:` (NOT `"gpt-4"`), add a `request_options` map with `temperature`, `top_p`, `max_tokens`, `seed`, add a `usage` map with input/output tokens, plus `provider`/`operation` as the builder expects. Update assertions: (a) SPAN-01 completeness — assert all of `"gen_ai.request.model"`, `"gen_ai.request.temperature"`, `"gen_ai.request.top_p"`, `"gen_ai.request.max_tokens"`, `"gen_ai.request.seed"` are present TOGETHER plus a `"gen_ai.usage.*"` key (never a partial subset); (b) SPAN-02 — assert `span.span_kind == "llm"` and `span.attributes["openinference.span.kind"] == "LLM"`; (c) COMPAT-01 — `refute Map.has_key?(span.attributes, "llm.model_name")` and likewise for `"llm.token_count"`, `"req.url"`. Keep the existing `attach`/`send(parent, {:span, metadata})` harness shape (`async: false`). Run `mix hex.outdated req_llm` first as a cheap pre-flight (RESEARCH Assumption A2) — if the locked version is no longer 1.13.0, re-confirm the exact key names before asserting.
  </action>
  <verify>
    <automated>mix test test/scoria/observe/adapters/req_llm_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'model: "gpt-4"' test/scoria/observe/adapters/req_llm_test.exs` == 0 (unrealistic fixture replaced)
    - Test exits 0 with the SPAN-01 completeness, SPAN-02 mirror, and COMPAT-01 legacy-absence assertions all present
  </acceptance_criteria>
  <done>Test uses a realistic %LLMDB.Model{} fixture and proves gen_ai.* completeness, span_kind/openinference mirror, and legacy-key absence.</done>
</task>

<task type="auto">
  <name>Task 3: CHANGELOG 0.1.4 breaking-change entry (COMPAT-01 mapping + FOUND-01 announcement)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (existing Keep-a-Changelog format + the "Planning milestones vs Hex releases" note + latest 0.1.3 heading style)
    - .planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-RESEARCH.md (§Code Examples — CHANGELOG Old->New Key Mapping Table, verbatim; note the req.url->server.address+port lossiness and the request-vs-response model split)
    - .planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-CONTEXT.md (D-03 mapping table + one upgrade-guide sentence; specifics — pair COMPAT-01 note with the FOUND-01 flush_error/on_flush_error announcement)
  </read_first>
  <action>
    Add an Unreleased/`0.1.4` breaking-change section following the existing Keep-a-Changelog heading style. Include the literal old->new key mapping table from RESEARCH §Code Examples: `llm.model_name` -> `gen_ai.request.model` (requested) / `gen_ai.response.model` (actual, call out the split); `llm.token_count` -> `gen_ai.usage.input_tokens` + `gen_ai.usage.output_tokens` (old total = sum); `req.url` -> `server.address` + `server.port` (call out that path is LOSSY, combine with `gen_ai.operation.name`). Add ONE upgrade-guide sentence for adopters who attached a custom `:telemetry` handler reading the old keys in-memory (D-03). Also announce the FOUND-01 persistence-failure surface: the new `[:scoria, :observe, :buffer, :flush_error]` telemetry event and the `:on_flush_error` (`:log` default | `:raise`) Buffer option (specifics — operators learn both "your queries may change" and "here's how persistence failures now surface"). Do NOT add a dual-emit/shim note (D-01: clean replacement, and D-02: no adopter Postgres ever persisted the legacy keys due to the FK bug, so there is zero legacy data to protect).
  </action>
  <verify>
    <automated>grep -Eq 'gen_ai.request.model' CHANGELOG.md && grep -Eq 'server.address' CHANGELOG.md && grep -Eq 'flush_error' CHANGELOG.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - CLI: the verify grep prints OK (mapping table + flush_error announcement present)
    - Source assertion: the entry names all three legacy keys (llm.model_name, llm.token_count, req.url) and their replacements, plus one upgrade-guide sentence and the :on_flush_error option
    - No dual-emit/back-compat-shim language present (clean replacement per D-01)
  </acceptance_criteria>
  <done>0.1.4 breaking-change entry present with the mapping table, upgrade sentence, and FOUND-01 announcement; no shim language.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| req_llm telemetry metadata -> ReqLLM adapter -> stored span attributes | Adapter transforms host/req_llm-supplied metadata into persisted jsonb keys/values |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-06 | Information Disclosure | new gen_ai.*/openinference.* span attributes | low | mitigate | Keys produced are scalar/categorical (model id, token counts, temperatures, host+port) — no raw prompt/completion text; the existing Redactor deny-list (exact-key-match) does not collide with any new dotted key (RESEARCH Security Domain VERIFIED). Fixing the metadata[:model] struct-in-jsonb bug also STOPS a large nested %LLMDB.Model{} object (pricing/capabilities) from being serialized into attributes |
| T-51-07 | Tampering (input validation) | span_kind from metadata[:operation] | low | mitigate | Routed through SpanKind.normalize/2 which fails closed to a listed kind; never persists an unlisted value (ASVS V5) |
| T-51-SC | Tampering | package installs | n/a | accept | No package installs (mix.exs unchanged); consumes locked req_llm 1.13.0 only |
</threat_model>

<verification>
- `mix test test/scoria/observe/adapters/req_llm_test.exs` green
- `mix test test/scoria/observe/` (regression) green
- CHANGELOG grep gate prints OK
- Full-suite `mix test` green before phase gate
</verification>

<success_criteria>
- Ties to ROADMAP SC#2 (all four model-config params + usage together), SC#3 (correct span_kind + mirrored openinference.span.kind), SC#4 (keys from Semconv), SC#5 (legacy keys -> documented clean replacement).
- SPAN-01, SPAN-02 (ReqLLM write-side), COMPAT-01 complete.
</success_criteria>

<output>
Create `.planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-04-SUMMARY.md` when done
</output>
