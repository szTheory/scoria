---
phase: 51-foundation-fix-key-convention-span-kind-taxonomy
plan: 05
type: execute
wave: 2
depends_on: ["51-01", "51-02"]
files_modified:
  - lib/scoria/observe/adapters/jido.ex
  - test/scoria/observe/adapters/jido_test.exs
autonomous: true
requirements: [SPAN-02]

must_haves:
  truths:
    - "the Jido adapter sets span_kind = SpanKind.normalize(metadata[:span_kind] || \"tool\") — host-declared override only, no action-name inference; replaces the INTERNAL category error — D-13"
    - "the Jido span carries a mirrored openinference.span.kind attribute keyed by Semconv.openinference_span_kind_key/0, derived via SpanKind.to_openinference/1 — SC#3"
    - "span_kind is drawn from the shared SpanKind whitelist (same module the UI components and the ReqLLM adapter use) — no inline literal — SC#3/SC#4"
  artifacts:
    - "lib/scoria/observe/adapters/jido.ex (span_kind via SpanKind.normalize + openinference mirror)"
    - "test/scoria/observe/adapters/jido_test.exs (tool default, host-override, openinference mirror)"
  key_links:
    - "jido.ex delegates to Scoria.Observe.SpanKind (shared whitelist) and Scoria.Observe.Semconv.openinference_span_kind_key/0 — the same seams the ReqLLM adapter and UI components use"
---

<objective>
Fix the Jido telemetry adapter's `span_kind`: replace the `"INTERNAL"` literal (an OTel `SpanKind` category error, not an OpenInference kind) with `SpanKind.normalize(metadata[:span_kind] || "tool")` (D-13 — host-declared override only, no action-name inference), and add the mirrored `openinference.span.kind` attribute via `SpanKind`/`Semconv` — matching the ReqLLM adapter's mirror pattern.

Purpose: A generic Jido action is discrete function execution = `TOOL` semantics; `agent` is reserved for the orchestrating span. This makes Jido spans render correctly and portably instead of collapsing to generic `agent` styling.

Output: fixed Jido adapter + test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-CONTEXT.md
@.planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-RESEARCH.md
@.planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-PATTERNS.md
@lib/scoria/observe/adapters/jido.ex
</context>

## Artifacts this phase produces (this plan)

- Modified `Scoria.Observe.Adapters.Jido.handle_event/4` (span_kind via SpanKind.normalize + openinference mirror)
- Updated `test/scoria/observe/adapters/jido_test.exs`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Jido adapter — span_kind via SpanKind.normalize (host-declared, default tool) + openinference mirror</name>
  <files>lib/scoria/observe/adapters/jido.ex, test/scoria/observe/adapters/jido_test.exs</files>
  <read_first>
    - lib/scoria/observe/adapters/jido.ex (current span-map with span_kind: "INTERNAL" at :28 — the only line whose semantics change)
    - .planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-PATTERNS.md (§jido.ex — the exact target line; sibling req_llm.ex mirror pattern)
    - .planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-CONTEXT.md (D-13 — tool default, host-declared override only, no inference)
    - lib/scoria/observe/span_kind.ex (plan 01 — normalize/2, to_openinference/1), lib/scoria/observe/semconv.ex (plan 02 — openinference_span_kind_key/0)
  </read_first>
  <behavior>
    - Test: a [:jido,:action,:stop] event with NO :span_kind in metadata produces span.span_kind == "tool"
    - Test: an event with metadata[:span_kind] == "agent" produces span.span_kind == "agent" (host override honored)
    - Test: an event with metadata[:span_kind] == "mcp" produces span.span_kind == "mcp" and attributes["openinference.span.kind"] == "TOOL"
    - Test: the produced span attributes contain openinference.span.kind mirroring the native kind
  </behavior>
  <action>
    Replace `span_kind: "INTERNAL"` with `span_kind: Scoria.Observe.SpanKind.normalize(metadata[:span_kind] || "tool")` (D-13 — no action-name classifier; host declares or defaults to tool). Compute the kind into a local binding and add the mirror to the `attributes` map under `Scoria.Observe.Semconv.openinference_span_kind_key()` with value `Scoria.Observe.SpanKind.to_openinference(span_kind)` — mirroring the ReqLLM adapter (never hardcode the `"openinference.span.kind"` string or the UPPERCASE value). Preserve everything else (the jido.* attributes, duration_ms, tenant_id/workflow_run_id, trace_id fallback, the final `:telemetry.execute([:scoria,:observe,:span,:stop], %{}, span)`). Add aliases for `SpanKind` and `Semconv`. Do NOT merge any gen_ai.* set here — Jido spans are not LLM-model spans.
  </action>
  <verify>
    <automated>mix test test/scoria/observe/adapters/jido_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'span_kind: "INTERNAL"' lib/scoria/observe/adapters/jido.ex` == 0 and `grep -c 'SpanKind.normalize' lib/scoria/observe/adapters/jido.ex` >= 1
    - Source assertion: `grep -c '"openinference.span.kind"' lib/scoria/observe/adapters/jido.ex` == 0 (key comes from Semconv, not inline)
    - Behavior: default span_kind is "tool"; host override honored; mcp mirrors to "TOOL"
  </acceptance_criteria>
  <done>Jido span_kind is host-declared (default tool) via SpanKind.normalize with an openinference mirror via Semconv; no INTERNAL literal, no inline convention-key string.</done>
</task>

<task type="auto">
  <name>Task 2: Update jido_test.exs — tool default, host override, openinference mirror</name>
  <files>test/scoria/observe/adapters/jido_test.exs</files>
  <read_first>
    - test/scoria/observe/adapters/jido_test.exs (current fixture asserting span_kind: "INTERNAL" — the assertions to update)
    - .planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-CONTEXT.md (D-13; D-11 mcp->TOOL)
    - test/scoria/observe/adapters/req_llm_test.exs (sibling harness shape to mirror)
  </read_first>
  <action>
    Extend `test/scoria/observe/adapters/jido_test.exs` (already begun in Task 1) — update the existing test and add cases mirroring the Task 1 behavior list: (1) NO metadata[:span_kind] -> assert `span.span_kind == "tool"` (replaces the old `"INTERNAL"` assertion); (2) metadata[:span_kind] == "agent" -> assert `span.span_kind == "agent"` (host override); (3) metadata[:span_kind] == "mcp" -> assert `span.span_kind == "mcp"` AND `span.attributes["openinference.span.kind"] == "TOOL"`; (4) assert every produced span carries an `"openinference.span.kind"` attribute. Keep the existing `attach`/`send(parent, {:span, metadata})` harness (`async: false`).
  </action>
  <verify>
    <automated>mix test test/scoria/observe/adapters/jido_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'span_kind == "INTERNAL"' test/scoria/observe/adapters/jido_test.exs` == 0
    - Test exits 0 with the tool-default, host-override, and mcp->TOOL mirror cases present
  </acceptance_criteria>
  <done>Jido test proves tool default, host override, and openinference mirror; no INTERNAL assertion remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jido/host telemetry metadata (incl. metadata[:span_kind]) -> Jido adapter -> stored span attributes | Host-declared span_kind and jido.* metadata are transformed into persisted jsonb; span_kind crosses the validation boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-07 | Tampering (input validation) | Jido span_kind from metadata[:span_kind] | low | mitigate | Routed through SpanKind.normalize/2 — a host-supplied unlisted value fails closed to the default and emits the observable fallback event; never inferred from action names (host-declares doctrine, D-13) |
| T-51-06 | Information Disclosure | Jido span attributes (jido.action_name, jido.status) | low | accept | Values are categorical/identifier data, not free-text prompt content; existing Redactor deny-list unaffected; no new key collides |
| T-51-SC | Tampering | package installs | n/a | accept | No package installs (mix.exs unchanged) |
</threat_model>

<verification>
- `mix test test/scoria/observe/adapters/jido_test.exs` green
- `mix test test/scoria/observe/` (regression) green
- Full-suite `mix test` green before phase gate
</verification>

<success_criteria>
- Ties to ROADMAP SC#3: Jido span_kind drawn from the shared SpanKind whitelist with a mirrored openinference.span.kind; INTERNAL category error removed; default tool.
- SPAN-02 (Jido write-side) complete.
</success_criteria>

<output>
Create `.planning/phases/51-foundation-fix-key-convention-span-kind-taxonomy/51-05-SUMMARY.md` when done
</output>
