---
phase: 25-lane-parallelization-topology-docs
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - docs/MAINTAINERS.md
  - docs/operator_verification.md
  - README.md
  - test/scoria/ci_policy_contract_test.exs
autonomous: true
requirements: [DX-02]
must_haves:
  truths:
    - "docs/MAINTAINERS.md CI gate map names the parallel topology line policy → build → { test, ratchet, knowledge, connector } → verify-summary"
    - "docs/MAINTAINERS.md gives a maintainer a job→local-reproduction-command mapping"
    - "docs/operator_verification.md describes the parallel CI topology (it had none before)"
    - "README CI-topology line reflects the parallel structure"
    - "The docs-topology contract test asserts the gate map names each parallel lane and verify-summary, so docs cannot drift from the YAML"
  artifacts:
    - path: "docs/MAINTAINERS.md"
      provides: "Updated CI gate map: topology line, job→command table, renamed parallel-jobs heading"
      contains: "verify-summary"
    - path: "docs/operator_verification.md"
      provides: "Brief parallel CI topology note"
      contains: "verify-summary"
    - path: "test/scoria/ci_policy_contract_test.exs"
      provides: "Updated docs-topology assertions (new heading + lane names + verify-summary)"
      contains: "verify-summary"
  key_links:
    - from: "ci_policy_contract_test.exs docs-topology test"
      to: "docs/MAINTAINERS.md CI gate map"
      via: "=~ asserts on new heading, ratchet, knowledge, connector, verify-summary"
      pattern: "verify-summary"
---

<objective>
Update the three docs (`docs/MAINTAINERS.md`, `docs/operator_verification.md`, `README.md`) to
describe the new parallel CI topology, and update the docs-as-contract assertions in
`ci_policy_contract_test.exs` so they pin the new gate-map shape — committed in lockstep with the
YAML/test change from plan 25-01.

Purpose: realize DX-02 (SC#5) — docs describe the parallel topology and the "docs describe
topology" contract test stays green, so a maintainer who hits a failed *parallel job* can map it
to the exact local mix command.

Output: docs in lockstep with the topology + a green docs-topology contract test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-lane-parallelization-topology-docs/25-CONTEXT.md
@.planning/phases/25-lane-parallelization-topology-docs/25-RESEARCH.md

# The files this plan edits
@docs/MAINTAINERS.md
@docs/operator_verification.md
@README.md
@test/scoria/ci_policy_contract_test.exs

# The YAML this plan's docs must faithfully describe (produced by 25-01)
@.github/workflows/ci-verify.yml

<interfaces>
<!-- Docs-topology contract test currently at ci_policy_contract_test.exs L351 ("maintainer CI gate
     map documents topology, parity, ratchet, and failure diagnosis"). It currently asserts:
       gate_map =~ "Policy job", =~ "Test job closeout", =~ "Local parity",
       =~ "Ratchet is maintainer-only", =~ "When CI fails, run the matching maintainer command next",
       =~ "mix compile --warnings-as-errors"
     section_after/2 slices a "## CI gate map" section. Reuse it; do NOT delete it. -->
<!-- DO NOT break these other still-true tests touching MAINTAINERS.md / README:
       "CI-03 documents CI gate map for maintainers" (needs: "CI gate map", "policy",
         "mix test.semantic_fast_path", "mix test.connector",
         "mix scoria.warning_inventory.check_baseline", "Version namespaces",
         "mix scoria.test.install_contract")
       "maintainer gate map pins v2.10 PR vs release proof depth" (the **PR vs release proof depth**
         table — leave it intact)
       "README links maintainers to maintainer guide near status section" (needs README =~
         "For maintainers", "docs/MAINTAINERS.md", "CI topology"; refute duplicate "Maintainer guide" lines) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the MAINTAINERS.md CI gate map for parallel topology (topology line + job→command table + renamed heading)</name>
  <files>docs/MAINTAINERS.md</files>
  <read_first>
    - docs/MAINTAINERS.md (the "## CI gate map {#ci-gate-map-maintainers}" section — the intro sentence, the `**Test job closeout (Postgres on 55432):**` heading + its 9-step list, the "Verification lanes in PR CI" table, "When CI fails, run the matching maintainer command next" block; leave "**PR vs release proof depth**" and "## Hex release & recovery" untouched)
    - .planning/phases/25-lane-parallelization-topology-docs/25-RESEARCH.md (§"docs/MAINTAINERS.md `## CI gate map` section" — exact required changes + the job→command table)
    - .github/workflows/ci-verify.yml (the AFTER-state topology the docs must match)
  </read_first>
  <action>
    Rewrite the CI gate map intro sentence: it no longer "runs two jobs in order" — describe
    `policy → build → { test, ratchet, knowledge, connector } → verify-summary`, with each of
    test/ratchet/knowledge/connector `needs: build` and gated by the `verify-summary` fan-in (the
    reusable-workflow result that `ci-gate` consumes; `CI / ci-gate` remains the branch-protection check).
    Add the literal topology line `policy → build → { test, ratchet, knowledge, connector } → verify-summary`.

    Rename the `**Test job closeout (Postgres on 55432):**` heading to an accurate parallel-jobs heading
    `**Parallel verify jobs (each needs: build):**`. Under it, describe the four lanes; keep the
    `test:` job's internal step order (release_preview → DB-prep → adoption → runtime_to_handoff →
    semantic WAE → full-suite WAE) and note ratchet/knowledge/connector are now siblings, with the
    gallery as the connector tail.

    Add a job→local-reproduction-command table (the highest-value debugging artifact now that "which job
    failed" ≠ "which step"):
    | Job | Local command |
    |-----|---------------|
    | `test` | `SCORIA_DB_PORT=55432 mix test --warnings-as-errors` |
    | `ratchet` | `MIX_ENV=test mix test --warnings-as-errors test/scoria/warning_inventory/tmp_preflight_test.exs` |
    | `knowledge` | `SCORIA_DB_PORT=55432 mix test.knowledge --warnings-as-errors` |
    | `connector` | `SCORIA_DB_PORT=55432 mix test.connector --warnings-as-errors` |

    KEEP intact: the "**Local parity:**" line, the "**Ratchet is maintainer-only:**" line, the "When CI
    fails, run the matching maintainer command next:" block, the "**PR vs release proof depth**" table,
    and all anchors/headings the other contract tests assert (`CI gate map`, `Policy job`,
    `mix test.semantic_fast_path`, `mix test.connector`, `mix scoria.warning_inventory.check_baseline`,
    `Version namespaces`, `mix scoria.test.install_contract`, `mix compile --warnings-as-errors`).
  </action>
  <verify>
    <automated>grep -c 'policy → build → { test, ratchet, knowledge, connector } → verify-summary' docs/MAINTAINERS.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/MAINTAINERS.md` contains the literal line `policy → build → { test, ratchet, knowledge, connector } → verify-summary` (grep returns ≥ 1).
    - `docs/MAINTAINERS.md` contains the heading `Parallel verify jobs` and NO longer contains `Test job closeout` (`grep -c 'Test job closeout' docs/MAINTAINERS.md` returns 0).
    - `docs/MAINTAINERS.md` contains all four job→command rows (greps for `tmp_preflight_test.exs`, `mix test.knowledge --warnings-as-errors`, `mix test.connector --warnings-as-errors` each ≥ 1).
    - `docs/MAINTAINERS.md` still contains `Policy job`, `Local parity`, `Ratchet is maintainer-only`, `When CI fails, run the matching maintainer command next`, `mix compile --warnings-as-errors`, `mix scoria.test.install_contract`, `Version namespaces` (the other contract tests stay green).
  </acceptance_criteria>
  <done>The CI gate map describes the parallel topology with a topology line and a job→command table; the stale "Test job closeout" heading is gone; all still-true headings/commands preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add parallel CI topology note to operator_verification.md + README; update the docs-topology contract asserts</name>
  <files>docs/operator_verification.md, README.md, test/scoria/ci_policy_contract_test.exs</files>
  <read_first>
    - docs/operator_verification.md ("## Maintainers" section near L288 — currently zero CI topology content per RESEARCH grep)
    - README.md ("## For maintainers" at L279-283 — the "CI topology" line at L281)
    - test/scoria/ci_policy_contract_test.exs (the "maintainer CI gate map documents topology..." test at L351; the "README links maintainers..." test at L364; section_after/2 helper at L444)
    - .planning/phases/25-lane-parallelization-topology-docs/25-RESEARCH.md (§"docs/operator_verification.md", §"README.md")
  </read_first>
  <action>
    docs/operator_verification.md: in the "## Maintainers" section add a brief (1-2 sentence) CI topology
    note plus the topology line `policy → build → { test, ratchet, knowledge, connector } → verify-summary`,
    naming the `verify-summary` fan-in. Keep it minimal (adopter-facing doc) — enough to satisfy DX-02
    and the contract assert without over-documenting CI internals.

    README.md: update the "## For maintainers" CI-topology line so it mentions the parallel structure /
    `verify-summary` fan-in. Keep the literal phrase `CI topology` present (the README contract test
    asserts `readme =~ "CI topology"`). Do NOT introduce a duplicate "Maintainer guide" line (the test
    refutes two consecutive "Maintainer guide" lines).

    Update `ci_policy_contract_test.exs` `"maintainer CI gate map documents topology, parity, ratchet,
    and failure diagnosis"` (L351): change `assert gate_map =~ "Test job closeout"` to `assert gate_map
    =~ "Parallel verify jobs"`; ADD `assert gate_map =~ "ratchet"`, `assert gate_map =~ "knowledge"`,
    `assert gate_map =~ "connector"`, `assert gate_map =~ "verify-summary"`. Keep the existing asserts
    for "Policy job", "Local parity", "Ratchet is maintainer-only", "When CI fails, run the matching
    maintainer command next", "mix compile --warnings-as-errors", and the `refute gate_map =~ "Policy:
    compile WAE failed → ..."` guard.
  </action>
  <verify>
    <automated>mix test --no-start --warnings-as-errors test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test --no-start --warnings-as-errors test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs` exits 0 (the exact policy-job lane-contract step, both files green together).
    - `docs/operator_verification.md` contains `verify-summary` and the topology line (`grep -c 'verify-summary' docs/operator_verification.md` ≥ 1).
    - `README.md` still contains `CI topology` and now references the parallel structure / `verify-summary`; no duplicate "Maintainer guide" lines.
    - The docs-topology test asserts `Parallel verify jobs`, `ratchet`, `knowledge`, `connector`, and `verify-summary` (`grep -c 'verify-summary' test/scoria/ci_policy_contract_test.exs` ≥ 1).
    - `mix test --warnings-as-errors` (full suite) is green at the close of this wave.
  </acceptance_criteria>
  <done>operator_verification.md and README describe the parallel topology, the docs-topology contract asserts the new gate-map shape, and the full suite is green — docs and YAML are in lockstep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs (maintainer mental model) → CI topology (actual YAML) | Stale docs let a maintainer mis-diagnose a failed lane or trust a topology that no longer exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05 | Information disclosure / Drift | Docs describing CI topology drifting from the real YAML | mitigate | Docs-as-contract: the updated `"maintainer CI gate map documents topology..."` test asserts the gate map names each parallel lane and `verify-summary`, so docs that drift from the YAML turn the suite red. |
| T-25-06 | Repudiation | A maintainer cannot map a failed parallel job to a reproduction command (false sense the bar is met) | mitigate | The job→local-reproduction-command table makes "which job failed?" → "which exact mix command?" explicit and contract-checked. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase (RESEARCH §"Package Legitimacy Audit": N/A); docs-only + test-assert changes. |
</threat_model>

<verification>
- `mix test --no-start --warnings-as-errors test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs` exits 0.
- `mix test --warnings-as-errors` (full suite) is green — the phase gate before `/gsd:verify-work`.
- Docs grep checks above all pass; no stale `Test job closeout` heading remains.
</verification>

<success_criteria>
- DX-02 / SC#5: MAINTAINERS.md, operator_verification.md, and README describe the parallel topology, and the docs-topology contract test asserts it (docs in lockstep with the YAML).
</success_criteria>

<output>
Create `.planning/phases/25-lane-parallelization-topology-docs/25-02-SUMMARY.md` when done.
</output>
