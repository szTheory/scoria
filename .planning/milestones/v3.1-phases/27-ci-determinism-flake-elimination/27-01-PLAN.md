---
phase: 27-ci-determinism-flake-elimination
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/ci-verify.yml
  - docs/MAINTAINERS.md
  - test/scoria/ci_policy_contract_test.exs
autonomous: true
requirements: [FLAKE-01, FLAKE-02, FLAKE-03]

must_haves:
  truths:
    - "No CI Postgres job (across ci.yml + ci-verify.yml) binds a host port in the ephemeral range (>= 32768); the fixed 55432 bind is gone from both files (FLAKE-01 / SC1)."
    - "The TEMP diagnostic step is absent from the e2e job in ci.yml (FLAKE-02 / SC2)."
    - "A retry-vs-fix policy is documented in docs/MAINTAINERS.md and a contract test guards against continue-on-error / retry-action on the test workflows (FLAKE-03 / SC3)."
    - "Local SCORIA_DB_PORT=55432 references in MAINTAINERS.md / verification_lanes.ex / config remain unchanged (D-05); CI=5432 and local=55432 coexist."
    - "Existing contract tests + verification lanes stay green; no lane removed or demoted (SC4)."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "e2e job Postgres on host port 5432, TEMP step removed"
      contains: "- 5432:5432"
    - path: ".github/workflows/ci-verify.yml"
      provides: "4 Postgres lanes (test, knowledge, connector, full-suite) on host port 5432"
      contains: "- 5432:5432"
    - path: "docs/MAINTAINERS.md"
      provides: "### Flake policy: retry vs fix subsection inside ## CI gate map"
      contains: "Flake policy: retry vs fix"
    - path: "test/scoria/ci_policy_contract_test.exs"
      provides: "Durable guards: ephemeral-port ban, no-TEMP-step, no-retry-on-test-workflows"
      contains: "ephemeral range"
  key_links:
    - from: "test/scoria/ci_policy_contract_test.exs"
      to: ".github/workflows/ci.yml + ci-verify.yml"
      via: "job_blocks/1 + body =~ \"postgres:\" filter, asserting host port < 32768"
      pattern: "host_port < 32_?768"
    - from: "docs/MAINTAINERS.md ### Flake policy"
      to: "ci.yml + ci-verify.yml zero-retry posture"
      via: "documented banned patterns mirrored by the D-07 contract assertion"
      pattern: "continue-on-error"
---

<objective>
Eliminate the two known CI flakes at the root and codify a retry-vs-fix policy guarded by a
durable contract test, without weakening the verification bar.

1. FLAKE-01: Replace the fixed `55432:5432` Postgres host-port bind (in the GitHub-runner
   ephemeral range 32768–60999) with the conventional `5432:5432` (below the range), in all 5
   Postgres job blocks across `ci.yml` (1) and `ci-verify.yml` (4), and set `SCORIA_DB_PORT: 5432`
   in each. Structurally eliminates the "port already allocated" failure class (run 27508317719).
2. FLAKE-02: Delete the leftover `TEMP diagnose runs visibility` step from the e2e job in `ci.yml`.
3. FLAKE-03: Document a zero-retry-default policy in `docs/MAINTAINERS.md`, and add durable
   contract assertions (ephemeral-port ban, no-TEMP guard, no-retry-on-test-workflows).

Purpose: Known flakes recur and erode trust in the gate; fixing them at the root (not masking
with retries) and pinning the fix with a contract test prevents regression.
Output: 2 edited workflow files, 1 new docs subsection, 3 new contract-test assertions, all
existing tests + lanes still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-ci-determinism-flake-elimination/27-CONTEXT.md
@.planning/phases/27-ci-determinism-flake-elimination/27-RESEARCH.md
@.planning/phases/27-ci-determinism-flake-elimination/27-PATTERNS.md

<interfaces>
<!-- Contract-test helper the executor must reuse for Task 3 (do not rewrite it). -->
<!-- Extracted from test/scoria/ci_policy_contract_test.exs. -->

Module attributes already present (test/scoria/ci_policy_contract_test.exs lines 6–9):
```elixir
@ci_verify ".github/workflows/ci-verify.yml"
@ci_entry ".github/workflows/ci.yml"
@maintainer_docs "docs/MAINTAINERS.md"
@operator_docs "docs/operator_verification.md"
```

File-agnostic helper (lines 586–616) — call once per file, then filter by content:
```elixir
defp job_blocks(content)
# Returns %{job_name => job_body_string}. For @ci_entry the keys include
# "verify", "e2e", "ci-gate"; only "e2e" matches body =~ "postgres:".
# For @ci_verify the postgres jobs are test, knowledge, connector, full-suite.
```

Existing assertion that the new tests live adjacent to (line 179):
```elixir
test "postgres service is configured only for test, knowledge, and connector jobs"
```

Note: the gate-map doc test at line 488 reads `section_after(maintainer_docs, "## CI gate map")`
up to the next `## ` heading. The new `### Flake policy` subsection (a `###`, not `##`) inserted
before `## Hex release & recovery` stays INSIDE that section — additive, all existing asserts pass.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FLAKE-01 + FLAKE-02 — fix Postgres host port in all 5 blocks and delete the TEMP e2e step</name>
  <read_first>
    - .github/workflows/ci.yml (the file being modified)
    - .github/workflows/ci-verify.yml (the file being modified)
    - .planning/phases/27-ci-determinism-flake-elimination/27-RESEARCH.md (Edit-Site Inventory — exact line numbers)
    - .planning/phases/27-ci-determinism-flake-elimination/27-PATTERNS.md (block shapes, full-suite exception)
    - .planning/phases/27-ci-determinism-flake-elimination/27-CONTEXT.md (D-01, D-02, D-05, D-06, D-11)
  </read_first>
  <files>.github/workflows/ci.yml, .github/workflows/ci-verify.yml</files>
  <action>
    In `.github/workflows/ci.yml`: change the e2e job's Postgres `ports:` entry `- 55432:5432`
    (line 42) to `- 5432:5432`, and the job-level `SCORIA_DB_PORT: 55432` (line 52) to
    `SCORIA_DB_PORT: 5432` (D-01/D-02). Then delete the entire `TEMP diagnose runs visibility`
    step including its 3-line leading comment block — lines 115–126 inclusive, from the
    `# TEMP DIAGNOSTIC (remove after skeleton e2e root-caused): separates` comment through the
    `echo "SCORIA_DB_NAME=$SCORIA_DB_NAME SCORIA_DB_PORT=$SCORIA_DB_PORT"` run line and its
    trailing blank line, leaving `- name: Run dashboard e2e lane` (was line 127) immediately
    after the `Boot dev dashboard` readiness step with no gap (D-06).

    In `.github/workflows/ci-verify.yml`: in each of the 4 Postgres job blocks change
    `- 55432:5432` to `- 5432:5432` and `SCORIA_DB_PORT: 55432` to `SCORIA_DB_PORT: 5432`.
    The 8 lines are: test job ports 115 / env 125; knowledge ports 226 / env 236; connector
    ports 286 / env 296; full-suite ports 355 / env 365. Change ONLY the two port values per
    block. Do NOT add `SCORIA_DB_NAME` to the full-suite env block (it intentionally omits it for
    per-shard DB isolation via MIX_TEST_PARTITION — adding it collides all 4 shards).

    Do NOT touch any `SCORIA_DB_PORT=55432` reference in docs/MAINTAINERS.md,
    lib/scoria/verification_lanes.ex, lib/mix/tasks/scoria.pgvector.bootstrap.ex, or config/* —
    those are intentional local-only values (D-05). The change is CI YAML port values only.

    Discretionary (D-11, RESEARCH recommends including now): retarget the maintainer-narrative
    header comment in both files from `docs/operator_verification.md` to `docs/MAINTAINERS.md`
    (ci.yml line 18, ci-verify.yml line 11). Keep the rest of each comment line intact. No
    contract test asserts the old target, so this is safe.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/scoria && test "$(grep -rc '55432:5432' .github/workflows/ci.yml .github/workflows/ci-verify.yml | awk -F: '{s+=$2} END{print s}')" = "0" && test "$(grep -c -- '- 5432:5432' .github/workflows/ci-verify.yml)" = "4" && test "$(grep -c -- '- 5432:5432' .github/workflows/ci.yml)" = "1" && test "$(grep -c 'SCORIA_DB_PORT: 55432' .github/workflows/ci.yml .github/workflows/ci-verify.yml | awk -F: '{s+=$2} END{print s}')" = "0" && ! grep -q 'TEMP diagnose' .github/workflows/ci.yml && ! grep -q 'SCORIA_DB_NAME' <(awk '/^  full-suite:/{f=1} f&&/^    env:/{e=1} e&&/^      [A-Z]/{print} f&&/^  [a-z].*:/&&!/^  full-suite:/{f=0}' .github/workflows/ci-verify.yml) && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '55432:5432'` across ci.yml + ci-verify.yml == 0; `grep -c 'SCORIA_DB_PORT: 55432'` across both == 0.
    - `grep -c -- '- 5432:5432' .github/workflows/ci-verify.yml` == 4; same in ci.yml == 1 (5 total).
    - `grep -q 'TEMP diagnose' .github/workflows/ci.yml` returns nothing (step + comment removed).
    - The full-suite env block still has NO `SCORIA_DB_NAME` line.
    - MAINTAINERS.md / verification_lanes.ex / config still contain their local `55432` references (untouched).
    - YAML remains valid (downstream contract test parses it in Task 3).
  </acceptance_criteria>
  <done>All 5 Postgres blocks bind host port 5432 with SCORIA_DB_PORT 5432; the TEMP e2e step is gone; full-suite still omits SCORIA_DB_NAME; local 55432 references untouched; header comments point at MAINTAINERS.md.</done>
</task>

<task type="auto">
  <name>Task 2: FLAKE-03 — document the retry-vs-fix flake policy in MAINTAINERS.md</name>
  <read_first>
    - docs/MAINTAINERS.md (the file being modified — lines 84–92 around the insertion point)
    - .planning/phases/27-ci-determinism-flake-elimination/27-PATTERNS.md (MAINTAINERS.md subsection — exact content to insert)
    - .planning/phases/27-ci-determinism-flake-elimination/27-CONTEXT.md (D-07, D-08, D-09, D-10, D-11)
    - .planning/phases/27-ci-determinism-flake-elimination/27-RESEARCH.md (MAINTAINERS.md section — 12 local 55432 refs must stay)
  </read_first>
  <files>docs/MAINTAINERS.md</files>
  <action>
    Insert a new `### Flake policy: retry vs fix {#flake-policy}` subsection into
    `docs/MAINTAINERS.md` between the last line of the `## CI gate map` content (the
    `- Full-suite (k/4): WAE failed → ...` bullet, line 88) and the `## Hex release & recovery`
    heading (line 90), so the new `###` subsection nests inside the existing `## CI gate map`
    section (D-11). Use the exact prose drafted in 27-PATTERNS.md "New subsection content to
    insert", which must cover: the zero-retry default stance (D-07); the banned patterns on
    ci.yml / ci-verify.yml test steps — `continue-on-error: true`, job-level `retry:`, and
    retry-action `uses:` wrappers (`nick-fields/retry`, `Wandalen/wretry.action`) on
    assertion steps; the carve-out that release-please.yml / hex-publish.yml /
    release-pr-automerge.yml `attempt` polling loops are control-flow waits, NOT test retries,
    and are out of scope (D-08); the one allowed exception class for infra-transient steps with
    its 5 conditions — distinct name, inline justification, `RETRY <step> attempt N/M: <reason>`
    log line, max 3 attempts, added under review (D-09); fix-don't-retry — root-cause-and-fix or
    quarantine with `@tag :flaky` + tracking issue, and that `mix test --repeat-until-failure`
    is for reproducing not masking (D-10); and a durable-enforcement note that
    ci_policy_contract_test.exs bans Postgres host ports in the ephemeral range (>= 32768),
    citing run 27508317719 as the FLAKE-01 root cause, with CI now on 5432 while local stays
    55432.

    Do NOT modify any of the 12 existing `SCORIA_DB_PORT=55432` local-parity references (lines
    24, 26, 27, 28, 30, 41, 45, 50, 54, 77, 87, 88) — they are intentional (D-05). This is an
    additive insertion, not a search/replace.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/scoria && grep -q 'Flake policy: retry vs fix' docs/MAINTAINERS.md && grep -q 'Zero-retry default' docs/MAINTAINERS.md && grep -q 'continue-on-error' docs/MAINTAINERS.md && grep -q '@tag :flaky' docs/MAINTAINERS.md && grep -q '27508317719' docs/MAINTAINERS.md && test "$(grep -c 'SCORIA_DB_PORT=55432' docs/MAINTAINERS.md)" -ge "12" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Flake policy: retry vs fix' docs/MAINTAINERS.md` matches (new subsection present).
    - Subsection mentions: `Zero-retry default`, `continue-on-error`, the retry-action slugs, `@tag :flaky`, `--repeat-until-failure`, the carve-out files, and run `27508317719`.
    - `grep -c 'SCORIA_DB_PORT=55432' docs/MAINTAINERS.md` >= 12 (existing local refs untouched).
    - The new subsection is a `###` under `## CI gate map`, placed before `## Hex release & recovery`.
    - Existing gate-map doc contract test (line 488, asserts `section_after("## CI gate map")` contents) still passes — verified in Task 3.
  </acceptance_criteria>
  <done>MAINTAINERS.md has a complete `### Flake policy: retry vs fix` subsection inside the CI gate map section covering D-07..D-11; all 12 local 55432 references remain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Durable contract guards — ephemeral-port ban, no-TEMP-step, no-retry-on-test-workflows</name>
  <read_first>
    - test/scoria/ci_policy_contract_test.exs (the file being modified — attrs lines 6–16, postgres test line 179, job_blocks/1 line 586)
    - .planning/phases/27-ci-determinism-flake-elimination/27-PATTERNS.md (test — exact new test bodies, non-empty guard idiom, insertion point)
    - .planning/phases/27-ci-determinism-flake-elimination/27-RESEARCH.md (Contract Test Extension — job_blocks/1 reuse, Pitfall 3 vacuous-pass guard)
    - .planning/phases/27-ci-determinism-flake-elimination/27-CONTEXT.md (D-12; D-07/D-08 scope; Claude's Discretion on regex + retry-slug ban)
  </read_first>
  <files>test/scoria/ci_policy_contract_test.exs</files>
  <behavior>
    - Test "no CI Postgres job binds a host port in the ephemeral range (>= 32768)": reads
      @ci_verify and @ci_entry, derives postgres job blocks via job_blocks/1 filtered by
      `body =~ "postgres:"`, asserts `map_size(postgres_blocks) >= 5` (vacuous-pass guard), then
      for every `- NNNN:5432` / `- NNNN/tcp` host-port binding asserts the host port < 32768.
      Passes on the Task-1 state (all 5432); would fail if any block reintroduced 55432.
    - Test "e2e job in ci.yml has no TEMP diagnostic step": `refute File.read!(@ci_entry) =~ "TEMP diagnose"`.
    - Test "no test workflow step uses continue-on-error or a retry-action": refutes
      `continue-on-error`, `nick-fields/retry`, `Wandalen/wretry` in BOTH @ci_verify and @ci_entry.
      Carve-out files (release-please.yml, hex-publish.yml, release-pr-automerge.yml) are NOT asserted (D-08).
    - All 42 pre-existing tests in the file remain green (no edits to existing assertions).
  </behavior>
  <action>
    Extend `test/scoria/ci_policy_contract_test.exs` by adding three new `test` blocks
    immediately after the existing `"postgres service is configured only for test, knowledge,
    and connector jobs"` test (ends line 194), matching the file's existing conventions exactly:
    `File.read!` + `=~` / `Regex.scan` / `refute`, string/regex only — NO YAML parser dependency.
    Reuse the existing file-agnostic `job_blocks/1` helper (do not duplicate or rewrite it);
    call it once per file and filter by `body =~ "postgres:"` to derive the Postgres-job set.
    Optionally add a `@ephemeral_range_min 32_768` module attribute near the existing attrs
    (lines 6–16) for readability (Claude's Discretion).

    Use the three test bodies drafted in 27-PATTERNS.md "New tests to add": (1) the D-12
    ephemeral-port ban with the `map_size(postgres_blocks) >= 5` non-empty guard before the
    iteration and a per-binding `host_port < 32_768` assertion with a descriptive failure
    message naming the offending job and port; (2) the FLAKE-02 `refute ... =~ "TEMP diagnose"`
    guard; (3) the D-07 enforcement refuting `continue-on-error` and the two retry-action slugs
    in @ci_verify and @ci_entry only. Do NOT assert against the D-08 carve-out workflow files.

    The host-port regex (Claude's Discretion, D-12) must extract the host port from
    `- NNNN:5432` and `- NNNN/tcp` shapes; root-cause-faithful — it bans ANY ephemeral-range
    fixed port, not literally 55432.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/scoria && mix test --no-start --warnings-as-errors test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `mix test --no-start --warnings-as-errors test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs` exits 0 with `0 failures`.
    - Test count in ci_policy_contract_test.exs increases from 42 to 45 (three new tests).
    - `grep -c 'ephemeral range' test/scoria/ci_policy_contract_test.exs` >= 1 and `grep -q 'TEMP diagnose' test/scoria/ci_policy_contract_test.exs` (refute assertion present).
    - The new ephemeral test contains a `>= 5` non-empty guard (Pitfall 3 — no vacuous pass).
    - `job_blocks/1` is reused (not redefined): `grep -c 'defp job_blocks' test/scoria/ci_policy_contract_test.exs` == 1.
    - No YAML parser added (`grep -c 'yaml_elixir\|YamlElixir' test/scoria/ci_policy_contract_test.exs` == 0).
    - All 42 pre-existing tests still pass (no existing assertion edited).
  </acceptance_criteria>
  <done>Three durable contract guards added and green; full contract + lane suite passes with no failures; job_blocks/1 reused; no YAML parser introduced; the YAML and docs from Tasks 1–2 are now pinned against regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner OS ↔ Docker service container | Postgres host-port publish on the ephemeral CI runner (host-side bind only) |
| Maintainer ↔ CI workflow YAML | Maintainer edits to gating workflows, guarded by the contract test |

This phase introduces NO new runtime attack surface: no new application inputs, no new network
exposure, no new dependencies, no new external services. The Postgres port change is host-side
CI service binding only, on ephemeral, trusted, single-tenant GitHub runners; the container port
(5432) and credentials are unchanged. The contract test reads local files only. There are no
HIGH-severity threats to block on (honest assessment per the security gate).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | Future edit reintroduces an ephemeral-range fixed host port (e.g. 55432) in a Postgres block | mitigate | D-12 contract test bans any host port >= 32768 across ci.yml + ci-verify.yml with a `>= 5` non-empty guard so a broken regex fails loud, not vacuously |
| T-27-02 | Tampering | Future edit adds `continue-on-error` / retry-action to a gating test step, masking a real flake | mitigate | D-07 contract assertion refutes `continue-on-error` and known retry-action slugs in both test workflows; carve-out (D-08) files intentionally excluded |
| T-27-03 | Repudiation | A re-run masks a non-deterministic failure with no audit trail | accept (policy) | Documented zero-retry default + fix-don't-retry policy (D-10) in MAINTAINERS.md; allowed infra-transient retries must log `RETRY <step> attempt N/M: <reason>` (D-09) |
| T-27-04 | Information disclosure | TEMP diagnostic step dumps DB row counts / server render into public CI logs | mitigate | FLAKE-02 deletes the TEMP step entirely; durable `refute "TEMP diagnose"` guard prevents reintroduction (low severity — dev seed data only, no secrets) |
| T-27-SC | Tampering | npm/pip/cargo supply-chain | n/a | No package installs in this phase — zero new dependencies (RESEARCH: no Package Legitimacy Audit required) |

No HIGH-severity threats; nothing to block on. ASVS L1: not applicable beyond CI hygiene
(no auth, session, input-handling, or crypto surface touched).
</threat_model>

<verification>
- After each task: `mix test --no-start --warnings-as-errors test/scoria/ci_policy_contract_test.exs test/scoria/verification_lanes_test.exs` (≈3s, must stay green throughout).
- FLAKE-01: zero `55432:5432` and zero `SCORIA_DB_PORT: 55432` across both workflow files; exactly 5 `- 5432:5432` bindings (1 ci.yml + 4 ci-verify.yml); D-12 ephemeral-port test green.
- FLAKE-02: `grep -q 'TEMP diagnose' .github/workflows/ci.yml` empty; no-TEMP contract guard green.
- FLAKE-03: MAINTAINERS.md flake-policy subsection present; no-retry-on-test-workflows contract guard green.
- SC4 (bar preserved): full contract suite + verification_lanes_test green; no lane removed or demoted; full-suite job still omits SCORIA_DB_NAME; all 12 local 55432 refs intact.
- D-13 (manual, recorded in VERIFICATION, not part of this plan's gate): one-time ~10× workflow_dispatch sweep; paste run URLs; frame non-recurrence as structural (D-14), sweep as weak corroboration only.
- Phase gate: full suite `mix test --warnings-as-errors` green before `/gsd:verify-work`.
</verification>

<success_criteria>
- All 5 CI Postgres blocks bind host port 5432 (below the ephemeral range); fixed 55432 bind eliminated from both files (SC1 / FLAKE-01).
- TEMP diagnostic step removed from the e2e job in ci.yml (SC2 / FLAKE-02).
- Retry-vs-fix policy documented in MAINTAINERS.md and enforced by a contract test; no blanket auto-retries (SC3 / FLAKE-03).
- ci_policy_contract_test.exs grows to 45 tests, all green; verification_lanes_test green; no lane removed/demoted (SC4).
- Local 55432 references and the full-suite SCORIA_DB_NAME omission preserved (D-05; no shard collision).
</success_criteria>

<output>
Create `.planning/phases/27-ci-determinism-flake-elimination/27-01-SUMMARY.md` when done.
</output>
