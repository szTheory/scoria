---
phase: 18-pressure-test-audit-decision-lock
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - brandbook/pressure-test.md
autonomous: false
requirements: [BRAND-01, BRAND-02]
must_haves:
  truths:
    - "A maintainer can read brandbook/pressure-test.md SECTIONS 8-14 of the canonical pressure-test, completing all 14 sections."
    - "Section 8 (logo + mark system) ranks the brand book's 4 logo directions (Cinder Mark, Trace Vesicle Mark, Cutaway Cone Mark, Aperture/Vesicle Mark) against 16px favicon scalability, monochrome survival, and distinctiveness — and encodes the user's logo constraints (no rectangular backgrounds, evenodd-punched negative space, logotype optically tight, no subtitle in main lockup, integrated typemark wanted) so Phase 19 inherits them."
    - "The audit ends with an explicit machine-readable propagation verdict: `propagation: required` (listing the specific failing shipped pairs from 18-01's table) or `propagation: not-required`."
    - "A 'Decisions Locked' section records: final tagline (pick/confirm from the brand book's candidates), one-liner, naming confirmation (Scoria, never 'Scoria AI'; feature names unchanged), palette deltas (before/after hex or 'none'), typography confirmation (IBM Plex Sans + JetBrains Mono unless material reason), ranked logo-direction guidance for Phase 19, and the propagation verdict."
    - "The plan's final task is a checkpoint:decision that lays out the lockable choices so the ORCHESTRATOR can run gate #1 (AskUserQuestion) before Phase 19."
  artifacts:
    - path: "brandbook/pressure-test.md"
      provides: "Completed Sections 8-14 + Decisions Locked section + propagation verdict"
      contains: "Decisions Locked"
      min_lines: 600
  key_links:
    - from: "brandbook/pressure-test.md Decisions Locked"
      to: "brandbook/pressure-test.md Section 5 / 18-01 contrast table"
      via: "propagation verdict cites the shipped FAIL pairs (or states none)"
      pattern: "propagation: (required|not-required)"
    - from: "brandbook/pressure-test.md Section 8"
      to: "Phase 19 logo divergence"
      via: "ranked logo directions + encoded no-rect/evenodd/tight-lockup constraints"
      pattern: "evenodd|favicon|Cinder Mark|Vesicle"
---

<objective>
Complete the pressure-test audit (Sections 8-14), distill it into a user-approvable "Decisions Locked" section with an explicit propagation verdict, and prepare the gate #1 decision summary the orchestrator will present.

Purpose: This closes BRAND-01 (all 14 sections done) and produces BRAND-02 (the locked decisions). Section 8 is load-bearing for Phase 19 — it must rank the four logo directions and bake in the user's hard logo constraints so the next phase does not re-litigate them. The propagation verdict gates conditional BRAND-09 in Phase 22. The final checkpoint hands a clean set of choices to the orchestrator, which runs the AskUserQuestion gate (the executor does NOT ask the user directly).

Output: `brandbook/pressure-test.md` extended with Sections 8-14 and a Decisions Locked section ending in `propagation: required|not-required`; a checkpoint:decision summarizing the lockable choices for gate #1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-pressure-test-audit-decision-lock/18-CONTEXT.md
@prompts/brand-book-pressure-test-prompt.md
@prompts/scoria-brand-book-deep-research.md
@assets/css/02-tokens.css
@prompts/sztheory-elixir-dna.md
@.planning/phases/18-pressure-test-audit-decision-lock/18-01-SUMMARY.md

<reference_data>
Logo directions to rank in Section 8 (from brand book §3/§4): Cinder Mark, Trace Vesicle Mark, Cutaway Cone Mark, Aperture/Vesicle Mark. Rank on: 16px favicon scalability, monochrome survival, distinctiveness vs adjacent OSS/devtools marks, distinctiveness vs Threadline's mark.

Hard logo constraints to encode in Section 8 (from 18-CONTEXT, feed Phase 19):
- NO rectangular background/container shapes behind logomarks — marks break boundaries; negative space via fill-rule="evenodd" punched holes.
- Logotype optically TIGHT to the mark; main lockup has NO subtitle/slogan (separate subtitle variant allowed).
- Integrated logotype-only treatments (motif worked INTO letterforms) wanted as first-class options, not just icon-left-of-text.

Tagline candidates (brand book §1.5):
- Primary: "Trace the run. Prove the change. Ship the agent."
- Alternate: "AI ops for Phoenix apps."
- Technical: "A Phoenix-native control plane for LLM traces, evals, prompts, tools, and MCP."
- Poetic: "Make the fire inspectable."
- Do-not-use: "The AI platform for everything." / "Autonomous agents for Phoenix." / "LangSmith for Elixir." / "Phoenix AI." / "The future of AI apps."

Naming truth (brand book §15): "Scoria" (never "Scoria AI"); feature names unchanged (Trace Explorer, Eval Workbench, Prompt Registry, etc.).
Typography truth (brand book §6 / shipped tokens): IBM Plex Sans + JetBrains Mono.
</reference_data>

<interfaces>
This plan APPENDS to the existing brandbook/pressure-test.md created by 18-01 (Sections 1-7). Use Edit/append, not Write — do not overwrite Sections 1-7. The propagation verdict's input is 18-01's contrast table (Section 7) and the Section 5 gap list: if any SHIPPED pairing is FAIL → propagation: required (list pairs + ratios); else propagation: not-required.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write pressure-test.md Sections 8-14 (logo system ranks directions + encodes constraints)</name>
  <files>brandbook/pressure-test.md</files>
  <read_first>
    - brandbook/pressure-test.md (the existing Sections 1-7 from 18-01 — append after Section 7, do NOT overwrite)
    - prompts/brand-book-pressure-test-prompt.md (verbatim Section 8-14 structure)
    - prompts/scoria-brand-book-deep-research.md (§3/§4 logo directions, §9/§10 voice+microcopy, §11 landing/docs, §12 repo artifacts, §15 naming)
    - .planning/phases/18-pressure-test-audit-decision-lock/18-CONTEXT.md (the Section-8 logo-constraint decision block — MUST be encoded so Phase 19 inherits them)
    - /Users/jon/projects/threadline/brandbook/pressure-test.md (sibling format precedent for these sections)
  </read_first>
  <action>
    Append to `brandbook/pressure-test.md` (after Section 7) Sections 8 through 14, using H2 headings of the exact form `## SECTION 8 — Logo and mark system`, `## SECTION 9 — Visual examples and screenshot guidance`, `## SECTION 10 — Brand voice and microcopy`, `## SECTION 11 — Landing page and docs blueprint`, `## SECTION 12 — Repo-ready artifact plan`, `## SECTION 13 — Prioritized action plan`, `## SECTION 14 — Final quality gate`.

    SECTION 8 is the highest-stakes part of this plan. It must: (1) recommend the logo TYPE (combination lockup vs integrated typemark vs mark-only); (2) present a ranked TABLE of the four directions (Cinder Mark, Trace Vesicle Mark, Cutaway Cone Mark, Aperture/Vesicle Mark) scored on 16px favicon scalability, monochrome survival, distinctiveness vs adjacent OSS marks, and distinctiveness vs Threadline's mark, with a recommended #1 and rationale; (3) encode the hard constraints from reference_data VERBATIM as named rules Phase 19 must obey — no rectangular background shapes, fill-rule="evenodd" punched negative space, logotype optically tight to the mark, no subtitle in the main lockup (subtitle variant allowed), integrated typemark studies as first-class options. State explicitly that no SVG is generated yet (Phase 19 owns generation) and what decision must be locked first (the chosen direction at gate #2).

    SECTION 9: visual-example guidance (palette specimen, typography specimen, component examples, code block, README header mock, landing hero mock, docs page mock, social card, terminal style) — purpose/layout/dimensions/path/when-worth-it each; reproducible SVG/text only, no fake product UI screenshots.

    SECTION 10: voice system + concrete copy blocks (one-liner, 140-char, GitHub description, Hex.pm description, README opener, hero headline+subhead, CTAs, 3 feature blurbs, 3 "why this exists", example error/empty/success, release announcement). Tag KEEP/TIGHTEN/REWORK on the brand book's existing voice where relevant. Confirm naming = "Scoria" (never "Scoria AI").

    SECTION 11: landing + docs/README page blueprint (hero, problem, solution, install, minimal example, benefits, how-it-works, use cases, why-not, doc/GitHub/community CTAs, footer; README: promise, install, quickstart, example, concepts, API, recipes, troubleshooting, contribution, license). Tie each back to the voice + visual system.

    SECTION 12: repo artifact plan for the self-contained brandbook/ — which files committed/generated/not-committed/need-review, SVG/text-only, <500KB budget, naming conventions, CI/lint checks (no-rect grep, evenodd grep, du-size, hex-consistency). Confirm prompts/scoria-brand-book-deep-research.md stays untouched as historical input.

    SECTION 13: prioritized actions grouped Do now / Do next / Defer / Do not do — concrete, value-tied, mapped to phases 19-22.

    SECTION 14: final quality-gate checklist answering the prompt's eight questions (designer can build? engineer can implement? maintainer can keep consistent? contributor understands? supports marketing without cheese? survives dark/small/docs/social? specific to this library? avoids thrash?).

    Throughout: KEEP/TIGHTEN/REWORK/ADD/REMOVE vocabulary, all-killer-no-filler, suite coherence vs Threadline + szTheory DNA without cloning. Do NOT write the Decisions Locked section here (next task).
  </action>
  <verify>
    <automated>F=brandbook/pressure-test.md; for s in "## SECTION 8 — Logo and mark system" "## SECTION 9 — Visual examples and screenshot guidance" "## SECTION 10 — Brand voice and microcopy" "## SECTION 11 — Landing page and docs blueprint" "## SECTION 12 — Repo-ready artifact plan" "## SECTION 13 — Prioritized action plan" "## SECTION 14 — Final quality gate"; do grep -qF "$s" "$F" || echo "MISSING: $s"; done; echo "sec1_still_present=$(grep -cF '## SECTION 1 — Executive judgment' "$F")"; echo "logo_dirs=$(grep -cE 'Cinder Mark|Trace Vesicle|Cutaway Cone|Aperture' "$F")"; echo "constraints=$(grep -ciE 'evenodd|rectangular|favicon|optically tight|subtitle' "$F")"</automated>
  </verify>
  <acceptance_criteria>
    - All seven `## SECTION N — …` headings (8-14) exist verbatim; verify loop prints no `MISSING:` line.
    - Sections 1-7 from 18-01 are still present (sec1_still_present == 1) — append did not overwrite.
    - Section 8 names all four logo directions (logo_dirs >= 4) in a ranked table with a recommended #1.
    - Section 8 encodes the no-rect / evenodd / optically-tight / no-subtitle / integrated-typemark constraints (constraints count >= 5) and states no SVG is generated this phase.
    - Section 13 maps actions across phases 19-22; Section 14 answers all eight quality-gate questions.
    - No "Decisions Locked" heading yet (owned by Task 2).
  </acceptance_criteria>
  <done>Sections 8-14 appended, completing the 14-section audit; Section 8 ranks logo directions and encodes Phase-19 constraints; Sections 1-7 intact.</done>
</task>

<task type="auto">
  <name>Task 2: Append Decisions Locked section with explicit propagation verdict</name>
  <files>brandbook/pressure-test.md</files>
  <read_first>
    - brandbook/pressure-test.md (the now-complete Sections 1-14; read Section 5 gaps + Section 7 contrast table to derive the propagation verdict, and Section 8 for the ranked logo guidance)
    - .planning/phases/18-pressure-test-audit-decision-lock/18-CONTEXT.md (the "Decisions Locked + propagation verdict" decision blocks — the exact fields required)
    - prompts/scoria-brand-book-deep-research.md (§1.5 tagline candidates, §6 typography, §15 naming — the source-of-truth values to confirm or tweak)
  </read_first>
  <action>
    Append a final `## Decisions Locked` section to `brandbook/pressure-test.md` capturing every required field from 18-CONTEXT:
    - Final tagline: pick/confirm ONE from the brand book candidates (primary is "Trace the run. Prove the change. Ship the agent.") with a one-paragraph rationale; if recommending a change, justify it as material (the book is a seed).
    - One-liner / short description (confirm or TIGHTEN).
    - Naming confirmation: "Scoria" (never "Scoria AI"); feature names unchanged — state explicitly.
    - Palette deltas: list any material change as before/after hex, or state "Palette deltas: none (cosmetic preferences stay brandbook-only)".
    - Typography confirmation: IBM Plex Sans + JetBrains Mono, or a justified material change.
    - Logo-direction guidance for Phase 19: the ranked recommendation from Section 8 (which of the 4 directions are strongest, in order), restated here for the lock.
    - Propagation verdict: a clearly delimited machine-readable line `propagation: required` or `propagation: not-required`. If required, list each failing SHIPPED pairing with token names + measured ratio (sourced from Section 5 / the Section 7 contrast table). If not-required, state the rule satisfied ("no shipped fg/bg pairing falls below AA").

    Keep this section self-contained and skimmable — it is what the user approves at gate #1 and what Phases 19/21/22 read back. Do not restate the whole audit; reference section numbers.
  </action>
  <verify>
    <automated>F=brandbook/pressure-test.md; grep -qF "## Decisions Locked" "$F" || echo "MISSING Decisions Locked"; echo "verdict=$(grep -cE 'propagation: (required|not-required)' "$F")"; echo "fields=$(grep -ciE 'tagline|one-liner|naming|palette delta|typograph|logo direction|propagation' "$F")"; grep -qiE 'IBM Plex Sans|JetBrains Mono' "$F" || echo "MISSING typography confirm"; grep -qiE "never.{0,40}Scoria AI|Scoria AI.{0,40}never" "$F" || echo "MISSING naming rejection"</automated>
  </verify>
  <acceptance_criteria>
    - `## Decisions Locked` heading exists.
    - Exactly one machine-readable `propagation: required` OR `propagation: not-required` line exists (verdict count == 1).
    - If `propagation: required`, every failing shipped pair is listed with token names and a measured ratio; if `not-required`, the satisfied rule is stated.
    - All seven lock fields are covered (tagline, one-liner, naming, palette deltas, typography, logo guidance, propagation) — fields count >= 7.
    - Typography line confirms IBM Plex Sans + JetBrains Mono (or justifies a material change).
    - Naming line confirms "Scoria" and rejects "Scoria AI".
  </acceptance_criteria>
  <done>Decisions Locked section complete with all lock fields and a single explicit propagation verdict that cites the contrast evidence.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Gate #1 — present locked decisions for user approval (orchestrator runs AskUserQuestion)</name>
  <read_first>
    - brandbook/pressure-test.md (the Decisions Locked section + propagation verdict from Task 2)
  </read_first>
  <decision>Approve the Decisions Locked block (tagline, one-liner, naming, palette/typography deltas, ranked logo direction, propagation verdict) before Phase 19 logo generation begins.</decision>
  <context>
    BRAND-02 requires explicit user approval of the locked decisions before any asset generation. The executor has written the Decisions Locked section in brandbook/pressure-test.md; the ORCHESTRATOR presents it via AskUserQuestion (the executor does not prompt the user). The propagation verdict here gates whether conditional BRAND-09 fires in Phase 22.
    Summarize for the orchestrator to present: (a) the recommended final tagline + the alternates, (b) one-liner, (c) naming confirmation, (d) palette deltas (or none) with before/after hex, (e) typography confirmation, (f) the ranked logo directions for Phase 19, (g) propagation verdict (required + failing pairs, or not-required).
  </context>
  <options>
    <option id="approve-as-locked">
      <name>Approve as written</name>
      <pros>Unblocks Phase 19 immediately; all decisions trace to the audit evidence.</pros>
      <cons>None if the user agrees with the recommendations.</cons>
    </option>
    <option id="adjust">
      <name>Adjust specific decisions</name>
      <pros>User can override the tagline pick, palette deltas, logo ranking, or propagation call.</pros>
      <cons>Requires a follow-up edit to the Decisions Locked section before proceeding.</cons>
    </option>
  </options>
  <resume-signal>User approves the locked decisions (or specifies adjustments), after which Phase 19 may begin.</resume-signal>
</task>

</tasks>

<verification>
- All 14 sections present (1-7 from 18-01, 8-14 from this plan), headings verbatim.
- Section 8 ranks the four logo directions and encodes the Phase-19 logo constraints (no-rect, evenodd, tight lockup, no-subtitle, integrated typemark).
- Decisions Locked section present with all seven fields and exactly one `propagation:` verdict line citing the contrast evidence.
- Final task is a blocking checkpoint:decision; plan is `autonomous: false`.
</verification>

<success_criteria>
- BRAND-01 satisfied: complete 14-section pressure-test with scorecard, gaps, contrast verdict, suite-coherence, and propagation verdict.
- BRAND-02 ready for approval: Decisions Locked section is self-contained and presentable at gate #1.
- Phase 19 inherits ranked logo directions + hard constraints; Phase 22 inherits the propagation verdict.
</success_criteria>

<output>
Create `.planning/phases/18-pressure-test-audit-decision-lock/18-02-SUMMARY.md` when done.
</output>
