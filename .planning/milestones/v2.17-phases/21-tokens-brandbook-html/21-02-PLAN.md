---
phase: 21-tokens-brandbook-html
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/brand-book.md
  - brandbook/examples/palette.svg
  - brandbook/examples/typography.svg
  - brandbook/examples/components.svg
  - brandbook/examples/terminal.svg
  - brandbook/examples/readme-header.svg
  - brandbook/examples/landing-hero.svg
  - brandbook/examples/docs-page.svg
autonomous: true
requirements: [BRAND-05]

must_haves:
  truths:
    - "brandbook/brand-book.md is a ~400–700 line post-audit rewrite, all KEEP/TIGHTEN/REWORK verdicts applied, with final ready-to-paste copy blocks"
    - "brand-book.md contains the verbatim final copy blocks (Hex/GitHub description, README opener, hero headline/subhead, CTAs, 3 feature blurbs, 3 why-this-exists bullets, error/empty/success microcopy, release-note)"
    - "brandbook/examples/ contains exactly 7 SVGs: palette, typography, components, terminal, readme-header, landing-hero, docs-page"
    - "each example SVG ≤12KB; total examples/ ≤60KB; text + system-fallback font stacks; logo path data reused verbatim where logos appear"
  artifacts:
    - path: "brandbook/brand-book.md"
      provides: "Post-audit canonical brand guide"
      min_lines: 400
    - path: "brandbook/examples/palette.svg"
      provides: "Swatch grid with hex labels, both surface groups"
    - path: "brandbook/examples/landing-hero.svg"
      provides: "Hero verb-triplet + tagline subhead + CTAs treatment"
    - path: "brandbook/examples/terminal.svg"
      provides: "Dark terminal mock with mix scoria.install + evidence-voice output"
  key_links:
    - from: "brandbook/brand-book.md"
      to: "brandbook/pressure-test.md §10 copy blocks"
      via: "verbatim copy of final ready-to-paste blocks"
      pattern: "Trace the run"
    - from: "brandbook/examples/readme-header.svg"
      to: "brandbook/logo-primary.svg path data"
      via: "verbatim reuse of logo d strings"
      pattern: "path"
---

<objective>
Author the post-audit `brandbook/brand-book.md` rewrite (dense, buildable, no filler, ~400–700 lines, all audit verdicts applied, final ready-to-paste copy blocks) and the 7 `brandbook/examples/*.svg` specimens (hand-authored SVG, ≤12KB each / ≤60KB total, text + system-fallback stacks, reusing shipped logo path data verbatim where logos appear).

Purpose: brand-book.md REPLACES the 1,618-line deep-research doc as the canonical guide; Phase 22 consumes its copy blocks verbatim. The examples are the visual specimens the index.html (21-03) embeds. Covers BRAND-05's brand-book + examples slice.
Output: brand-book.md, 7 example SVGs. This plan runs in WAVE 1 parallel with 21-01 (zero files_modified overlap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-tokens-brandbook-html/21-CONTEXT.md

<read_first>
- brandbook/pressure-test.md — the audit. Drive brand-book.md content from: §1 (executive judgment), §2 (brand DNA), §6 (recommended upgrades — apply every KEEP/TIGHTEN/REWORK verdict), §7 (token spec + contrast table — abbreviate to the constraints that matter; the text-subtle PASS-LARGE warning + Scoria-600 negative-control warning are load-bearing), §9 (visual examples guidance — specs for each of the 7 SVGs), §10 (voice system + FINAL copy blocks at lines ~791–905 — copy verbatim), §11 (landing/docs blueprints), §Decisions Locked (~1197+: tagline hierarchy, one-liner, naming, palette/type confirmation).
- brandbook/tools/variant-spec.md — logo usage rules for the logo-system section: two-tone primary (dark: White-Hot letters + Ember-500 'o'; light: Basalt-950 letters + Scoria-600 'o'), clear-space = cap-height/2 (≈38.4u), min sizes (primary ≥120px, mark ≥20px, favicon exact 16/32px), 8-variant set, LK-B Mark-as-o is primary, mark-left classic lockups NOT in the system.
- assets/css/02-tokens.css — hex values for the palette.svg swatches and the abbreviated color section (basalt-950 #11100f, ember-500 #e65a32, etc.). Use these literal hexes in palette.svg labels.
- brandbook/logo-primary.svg, brandbook/logo-primary-light.svg, brandbook/logo-mark.svg — copy `d` path-data verbatim into readme-header.svg / landing-hero.svg where the logo appears. Do NOT redraw.
- /Users/jon/projects/threadline/brandbook/brand-book.md — length/density precedent (~506 lines); Scoria target is ~400–700. Rhyme structure, do not clone content.
- /Users/jon/projects/threadline/brandbook/examples/*.svg — the 7-specimen census + hand-authored <text>/system-fallback-stack precedent (illustrations, not pixel-perfect UI). Match this approach.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write brandbook/brand-book.md (post-audit rewrite, final copy blocks)</name>
  <files>brandbook/brand-book.md</files>
  <action>Write the canonical guide, dense and buildable, ~400–700 lines, applying every KEEP/TIGHTEN/REWORK verdict from pressure-test.md §6. Sections (ordering at Claude's discretion): (1) Identity & positioning — essence, audience, field-engineer archetype, naming rule ("Scoria", never "Scoria AI"), tagline hierarchy (primary "AI ops for Phoenix apps."; hero-headline treatment "Trace the run. Prove the change. Ship the agent."), one-liner ("Scoria is the Phoenix-native AI ops layer for LLM traces, evals, prompt versions, and tool governance."), plus the FINAL ready-to-paste copy blocks from §10 lines ~791–905 verbatim (140-char Hex description, GitHub description, README opener, hero headline + subhead, primary/secondary CTA, 3 feature blurbs [Trace Explorer / Eval Workbench / Tool Governance], 3 why-this-exists bullets, error/empty/success microcopy, release-note). (2) Logo system — the 8 variants, two-tone primary rule, usage + misuse, clear-space cap-height/2 + min-sizes from variant-spec.md. (3) Color — primitives + semantic roles, the ABBREVIATED contrast constraints (the text-subtle PASS-LARGE "muted labels only, never running text" warning + the Scoria-600-on-Basalt-950 negative-control warning; full table lives in index.html), highlight the tone + span-kind families as the domain differentiator. (4) Typography — IBM Plex Sans + JetBrains Mono, weights, the fs scale. (5) Voice & microcopy — calm/exact/useful, say-this-not-this tables, error/success/empty styles using real Scoria nouns (runs, spans, evals, prompts, approvals, connectors). (6) UI guidance — states, components pointer to ui.ex. (7) Landing/docs blueprints from §11. (8) Accessibility rules. (9) Do/don't list. Killer no filler — every line buildable. Do NOT reduce any copy block to a "v1" or placeholder; ship the final verbatim text.</action>
  <verify>
    <automated>L=$(wc -l < brandbook/brand-book.md); test "$L" -ge 400 && test "$L" -le 750 && grep -q 'Trace the run' brandbook/brand-book.md && grep -q 'AI ops for Phoenix apps' brandbook/brand-book.md && grep -qi 'never .*Scoria AI\|Scoria AI' brandbook/brand-book.md && grep -qi 'text-subtle\|Pumice' brandbook/brand-book.md && grep -qi 'Trace Explorer' brandbook/brand-book.md && grep -qi 'Eval Workbench' brandbook/brand-book.md</automated>
  </verify>
  <done>brand-book.md is 400–750 lines, contains the verbatim hero triplet, primary tagline, naming rule, the text-subtle/Pumice constraint, and the three feature names; all required copy blocks present.</done>
</task>

<task type="auto">
  <name>Task 2: Author palette / typography / components / terminal example SVGs (4 of 7)</name>
  <files>brandbook/examples/palette.svg, brandbook/examples/typography.svg, brandbook/examples/components.svg, brandbook/examples/terminal.svg</files>
  <action>Hand-author 4 SVGs, each ≤12KB, using <rect>/<text> with system-fallback font stacks (e.g. font-family="'IBM Plex Sans', ui-sans-serif, system-ui, sans-serif" for sans; "'JetBrains Mono', ui-monospace, monospace" for mono — illustration, not embedded fonts). Dark-first art direction (Basalt-950 #11100f ground, warm Ash text). palette.svg: swatch grid with hex labels for BOTH surface groups (dark neutrals + warm brand scale + functional accent pairs) — each swatch a colored rect + its hex string label, values from 02-tokens.css. typography.svg: Plex Sans + JetBrains Mono specimen rendering real brand text (the tagline + a code line) across the fs scale (display/title/panel/body/label). components.svg: button + badge + callout + card mock in brand tokens (ember action, tone-pass/fail/warn badges, a callout box). terminal.svg: dark terminal snippet mock showing `mix scoria.install` and an evidence-voice output line (e.g. "Run failed during lookup_order. The tool returned 403 for actor usr_184."). No external/network refs.</action>
  <verify>
    <automated>for f in palette typography components terminal; do test -f brandbook/examples/$f.svg && test $(wc -c < brandbook/examples/$f.svg) -le 12288 || exit 1; done; grep -qi 'mix scoria.install' brandbook/examples/terminal.svg && grep -qi '#e65a32\|#E65A32' brandbook/examples/palette.svg && ! grep -Eq 'https?://[^"]*\.(woff|ttf|otf|css|png|jpg)' brandbook/examples/*.svg</automated>
  </verify>
  <done>palette/typography/components/terminal SVGs exist, each ≤12KB, palette carries the ember hex, terminal carries the install command, no network asset refs.</done>
</task>

<task type="auto">
  <name>Task 3: Author readme-header / landing-hero / docs-page example SVGs (3 of 7)</name>
  <files>brandbook/examples/readme-header.svg, brandbook/examples/landing-hero.svg, brandbook/examples/docs-page.svg</files>
  <action>Hand-author the final 3 SVGs, each ≤12KB, same dark-first art direction + system-fallback font stacks. readme-header.svg: the README hero band — reuse logo-primary.svg path `d` data VERBATIM (copy the d strings; do not redraw) on a Basalt ground, with the tagline. landing-hero.svg: hero treatment demonstrating the locked hierarchy — the three-line verb-triplet ("Trace the run." / "Prove the change." / "Ship the agent.") as the display treatment, the "AI ops for Phoenix apps." tagline as subhead, and the two CTAs ("Get started" / "View on GitHub"). docs-page.svg: a HexDocs-ish page mock (sidebar + content column + a code block in brand tokens). After authoring, confirm total examples/ directory ≤60KB across all 7 files. No external/network refs.</action>
  <verify>
    <automated>for f in readme-header landing-hero docs-page; do test -f brandbook/examples/$f.svg && test $(wc -c < brandbook/examples/$f.svg) -le 12288 || exit 1; done; test $(ls brandbook/examples/*.svg | wc -l) -eq 7 && test $(cat brandbook/examples/*.svg | wc -c) -le 61440 && grep -q 'Trace the run' brandbook/examples/landing-hero.svg && grep -q 'Get started' brandbook/examples/landing-hero.svg && grep -q '<path' brandbook/examples/readme-header.svg</automated>
  </verify>
  <done>All 3 SVGs exist ≤12KB each; examples/ has exactly 7 SVGs totaling ≤60KB; landing-hero carries the verb-triplet + CTA; readme-header reuses logo path data.</done>
</task>

</tasks>

<verification>
- wc -l brand-book.md in [400,750]; all required copy blocks grep-present.
- ls brandbook/examples/*.svg count = 7; each ≤12KB; total ≤60KB.
- No network asset refs in any example SVG (font/image URLs).
- readme-header/landing-hero reuse shipped logo path data (no redraw).
- No edits to repo-root README.md, layouts.ex, mix.exs (Phase 22 scope).
</verification>

<success_criteria>
BRAND-05 brand-book + examples slice satisfied: post-audit brand-book.md with verbatim final copy blocks, and 7 specimen SVGs within budget covering the full census.
</success_criteria>

<output>
Create `.planning/phases/21-tokens-brandbook-html/21-02-SUMMARY.md` when done.
</output>
