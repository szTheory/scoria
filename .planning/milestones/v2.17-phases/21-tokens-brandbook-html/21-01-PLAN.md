---
phase: 21-tokens-brandbook-html
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/tokens.json
  - brandbook/tokens.css
  - brandbook/README.md
  - brandbook/tools/check-consistency.mjs
autonomous: true
requirements: [BRAND-05]

must_haves:
  truths:
    - "brandbook/tokens.css exists, :root-scoped, --scoria- prefixed, two-tier (primitives + semantic incl. all state tokens), hex-identical to assets/css/02-tokens.css"
    - "brandbook/tokens.json exists, Threadline-compatible nested shape, same hex values as tokens.css"
    - "brandbook/README.md documents maintenance rules and the two-SSOT distinction (02-tokens.css runtime vs brandbook/tokens.css docs)"
    - "node brandbook/tools/check-consistency.mjs exits 0, asserting tokens.json ↔ tokens.css ↔ 02-tokens.css hex equality"
  artifacts:
    - path: "brandbook/tokens.css"
      provides: ":root-scoped --scoria- token layer (primitives + semantic + state)"
      contains: ":root"
    - path: "brandbook/tokens.json"
      provides: "Structured nested token object mirroring Threadline shape"
      contains: "semantic"
    - path: "brandbook/README.md"
      provides: "Brandbook directory maintenance rules"
      min_lines: 30
    - path: "brandbook/tools/check-consistency.mjs"
      provides: "Hex-consistency assertion script across the three token sources"
      contains: "process.exit"
  key_links:
    - from: "brandbook/tools/check-consistency.mjs"
      to: "assets/css/02-tokens.css"
      via: "file read + hex extraction"
      pattern: "02-tokens\\.css"
    - from: "brandbook/tools/check-consistency.mjs"
      to: "brandbook/tokens.json"
      via: "file read + hex compare"
      pattern: "tokens\\.json"
---

<objective>
Author the canonical brandbook token layer: `brandbook/tokens.json` (structured, Threadline-shaped) and `brandbook/tokens.css` (`:root`-scoped, `--scoria-`-prefixed, two-tier). Both are hex-identical to `assets/css/02-tokens.css`. Add `brandbook/README.md` (directory maintenance rules) and `brandbook/tools/check-consistency.mjs` (a hex-equality assertion across the three token sources, run at plan-execution time).

Purpose: Establish the docs/marketing token SSOT so Phase 22 propagation is mechanical, and the consistency check protects against drift. This plan covers BRAND-05's token + README + tooling slice.
Output: tokens.json, tokens.css, README.md, tools/check-consistency.mjs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-tokens-brandbook-html/21-CONTEXT.md

<read_first>
- assets/css/02-tokens.css — HEX SSOT. Every primitive + semantic token here is the source of truth; brandbook/tokens.css MUST emit the identical hex values. Note: 02-tokens.css scopes under `.scoria-root` and `.scoria-root[data-theme="light"]`; brandbook/tokens.css scopes under `:root` and `:root[data-theme="light"]` instead — same names, same hexes, different selector.
- brandbook/pressure-test.md §7 (lines ~457–584) — "Semantic token inventory (reconciliation with shipped)" lists the exact token families to carry forward; "Naming reconciliation with brand book §5.7" confirms KEEP --scoria-muted-warm and lowercase hex are functionally identical; the contrast table (lines 510–584) is the verified pairing set.
- /Users/jon/projects/threadline/brandbook/tokens.json — nested shape to mirror (raw.color, semantic.dark, semantic.light, typography, spacing, radius, border, shadow, focus, code, callout, state). Rhyme, do not clone values.
- /Users/jon/projects/threadline/brandbook/tokens.css — plain :root custom-properties precedent (Threadline used --tl- prefix; Scoria uses --scoria-).
- /Users/jon/projects/threadline/brandbook/README.md — README shape: "what this directory is", open index.html, source-of-truth files, Maintenance Rules, Best Current Defaults.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author brandbook/tokens.css (two-tier, :root-scoped, --scoria- prefixed)</name>
  <files>brandbook/tokens.css</files>
  <action>Port every token from assets/css/02-tokens.css into brandbook/tokens.css with IDENTICAL hex values and IDENTICAL `--scoria-` names. Two changes only vs the runtime file: (1) selector becomes `:root` for the dark/default block and `:root[data-theme="light"]` for the light block (NOT `.scoria-root`), so values are copy-pasteable into any host page; (2) drop the `@layer scoria.tokens {}` wrapper (docs context, no cascade-layer need). Preserve ALL tiers: PRIMITIVE neutrals (basalt/char/graphite/pumice/tuff/ash/white-hot/muted-warm), PRIMITIVE warm scale (scoria-900..50, ember/molten/cinder), PRIMITIVE functional accent pairs (success/info/warning/danger/trace light+dark), space, radius, type (sans/mono + fs/lh/tracking), z-index, motion (ease/dur); and ALL SEMANTIC roles: surface/text/link/border/focus/action/shadow + the full tone family (neutral/pass/info/warn/fail/trace/brand × fg/bg/border) + span-kind tokens. Include both the dark default block AND the light re-point block. Keep --scoria-muted-warm (audit KEEP). Header comment: note this is the :root docs/marketing SSOT, while assets/css/02-tokens.css (.scoria-root) is the dashboard runtime SSOT, hexes kept identical. Do NOT add, rename, or recolor any token — this is a faithful re-scope only.</action>
  <verify>
    <automated>test $(grep -c -- '--scoria-' brandbook/tokens.css) -ge 90 && grep -q '^:root {' brandbook/tokens.css && grep -q 'data-theme="light"' brandbook/tokens.css && grep -qi '#11100f' brandbook/tokens.css && grep -qi '#e65a32' brandbook/tokens.css && ! grep -q 'scoria-root' brandbook/tokens.css</automated>
  </verify>
  <done>tokens.css exists, :root + :root[data-theme="light"] scoped, ≥90 --scoria- declarations, contains the basalt-950 and ember-500 hexes, no .scoria-root selector, no @layer wrapper.</done>
</task>

<task type="auto">
  <name>Task 2: Author brandbook/tokens.json (Threadline-shaped, same hexes)</name>
  <files>brandbook/tokens.json</files>
  <action>Create a structured nested token object mirroring Threadline's tokens.json top-level keys: name ("Scoria brand tokens"), version, source, raw.color (all primitive hexes from 02-tokens.css: neutrals, warm scale, functional accent pairs), semantic.dark + semantic.light (surface/text/link/border/focus/action and the resolved hex for each — resolve var() references to their literal hex from the primitive layer; for color-mix()/rgba translucent tokens, record the literal CSS string), typography (family.sans/mono, size map from fs-* tokens, lineHeight, tracking), spacing (space-1..9), radius (sm/md/lg/xl/pill), motion (ease + dur), and state (default/hover/active/focus/disabled/selected/success/warning/error/info/subtle/muted — Threadline-parity state block, pointing at the scoria semantic tokens). Every hex MUST match tokens.css exactly (lowercase ok — the consistency check normalizes case). Prefer nesting over flat (Claude's discretion per 21-CONTEXT, Threadline-compatible for suite tooling).</action>
  <verify>
    <automated>node -e "const t=require('./brandbook/tokens.json'); if(!t.semantic||!t.semantic.dark||!t.semantic.light||!t.raw||!t.state||!t.typography){process.exit(1)}; console.log('ok')"</automated>
  </verify>
  <done>tokens.json is valid JSON with raw.color, semantic.dark, semantic.light, typography, spacing, radius, state keys; hexes mirror tokens.css.</done>
</task>

<task type="auto">
  <name>Task 3: README.md maintenance rules + check-consistency.mjs hex-equality script</name>
  <files>brandbook/README.md, brandbook/tools/check-consistency.mjs</files>
  <action>README.md (mirror Threadline's README shape): (1) what this directory is (repo-canonical Scoria brand system, SVG/text/md/json/css/html only, self-contained); (2) "open index.html to review"; (3) source-of-truth list (brand-book.md = canonical guide, pressure-test.md = QA record, tokens.json/tokens.css = docs/marketing tokens, logo-*.svg/favicon.svg/social-card.svg/examples/*.svg = vector assets — use logo-primary.svg on dark, logo-primary-light.svg on light/README); (4) Maintenance Rules: SVG/text only, no binaries, no font files (dashboard self-hosts under priv/fonts/), tools/ regenerates logos, CRITICAL distinction — assets/css/02-tokens.css (.scoria-root) is the dashboard RUNTIME token SSOT and brandbook/tokens.css (:root) is the docs/marketing SSOT; keep their hexes in sync via node tools/check-consistency.mjs; candidates/ are historical; (5) Best Current Defaults: positioning ("AI ops for Phoenix apps."), one-liner, logo system (LK-B Mark-as-o fused primary + 8-variant set), palette (volcanic dark-first, ember accent), voice one-liner (calm/exact/useful). Do NOT edit the repo-root README.md (Phase 22). check-consistency.mjs (Node ESM, .mjs, no deps): read the three files; extract every `--scoria-NAME: #hex` pair from brandbook/tokens.css AND assets/css/02-tokens.css; extract hex values from tokens.json; normalize hex to lowercase; assert that for every primitive token name present in both CSS files the hexes are equal, and that the primitive hex set in tokens.json is a subset of (equal to the corresponding) tokens.css hexes; print a PASS line per source and a summary; process.exit(0) on full agreement, process.exit(1) with a diff listing on any mismatch. (Note: brand-book.md is added to this check in plan 21-03 — leave a clearly-commented extension point / TODO for the brand-book.md hex source so 21-03 wires it in without restructuring.)</action>
  <verify>
    <automated>node brandbook/tools/check-consistency.mjs; test $? -eq 0 && test $(grep -cv '^[[:space:]]*#' brandbook/README.md | head -1) -ge 30</automated>
  </verify>
  <done>check-consistency.mjs exits 0 (tokens.json ↔ tokens.css ↔ 02-tokens.css hexes agree); README.md ≥30 content lines covering the two-SSOT rule and maintenance rules; brand-book.md extension point present as a TODO.</done>
</task>

</tasks>

<verification>
- node brandbook/tools/check-consistency.mjs exits 0.
- brandbook/tokens.css is :root-scoped, --scoria- prefixed, hex-identical to 02-tokens.css (spot-check basalt-950 #11100f, ember-500 #e65a32).
- brandbook/tokens.json is valid JSON with the Threadline-shaped key set.
- brandbook/README.md documents the runtime-vs-docs SSOT distinction.
- No edits to repo-root README.md, layouts.ex, or mix.exs (Phase 22 scope).
</verification>

<success_criteria>
BRAND-05 token slice satisfied: tokens.json + tokens.css present, naming reconciled with 02-tokens.css (mechanical future propagation), README maintenance rules in place, consistency check green.
</success_criteria>

<output>
Create `.planning/phases/21-tokens-brandbook-html/21-01-SUMMARY.md` when done.
</output>
