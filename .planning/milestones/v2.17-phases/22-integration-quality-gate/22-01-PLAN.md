---
phase: 22-integration-quality-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - lib/scoria_web/components/layouts.ex
  - lib/scoria_web/assets.ex
  - lib/scoria_web/components/layouts/root.html.heex
  - priv/static/favicon.svg
  - mix.exs
autonomous: true
requirements: [BRAND-07, BRAND-08]

must_haves:
  truths:
    - "README header shows the brand lockup via a GitHub dark/light-aware <picture> block with the # Scoria H1 retained and the existing badge row kept"
    - "The README opening paragraph is the brand-book.md verbatim README-opener copy block"
    - "The dashboard sidebar renders the real TV-1 (Span rail) mark at 24x24 (single evenodd path, CSS-var fill, no placeholder circles/stroke)"
    - "The dashboard <head> declares a favicon (Scoria's real mark) served through the existing self-contained inline asset pipeline"
    - "mix.exs description/0 returns the verbatim Hex.pm 140-char copy block"
    - "GitHub repo description equals the verbatim brand-book.md GitHub repo description block"
    - "mix test (full default suite, incl. DS-06 ratchet) is green"
    - "test/support/ds06_baseline.txt is byte-identical (no git diff)"
  artifacts:
    - path: "priv/static/favicon.svg"
      provides: "Scoria SVG favicon (copy of brandbook/favicon.svg)"
      contains: "fill-rule=\"evenodd\""
    - path: "lib/scoria_web/components/layouts.ex"
      provides: "brand_mark/1 rendering the real TV-1 mark (24x24 evenodd path)"
      contains: "fill-rule=\"evenodd\""
    - path: "lib/scoria_web/assets.ex"
      provides: "favicon_data_uri/0 compile-time-inlined favicon data-URI (mirrors css/0, js/0)"
      contains: "def favicon_data_uri"
    - path: "README.md"
      provides: "Brand header with <picture> dark/light + retained H1 + verbatim opener"
      contains: "<picture>"
    - path: "mix.exs"
      provides: "Verbatim Hex.pm description copy block"
      contains: "Phoenix-native AI ops"
  key_links:
    - from: "lib/scoria_web/components/layouts/root.html.heex"
      to: "ScoriaWeb.Assets.favicon_data_uri/0"
      via: "inlined <link rel=icon> in <head>, mirroring the <style>/<script> inline pattern"
      pattern: "rel=\"icon\""
    - from: "lib/scoria_web/components/layouts/app.html.heex"
      to: "brand_mark/1"
      via: "sidebar brand slot already calls <.brand_mark />"
      pattern: "<\\.brand_mark"
---

<objective>
Wire the finalized Scoria brand onto every real surface: the GitHub/HexDocs README header, the dashboard sidebar mark and favicon, and the Hex.pm + GitHub repo copy. All copy is pasted verbatim from `brandbook/brand-book.md` §2 copy blocks (BRAND-07: README + dashboard surfaces; BRAND-08: Hex/GitHub/HexDocs copy). After the layout change, `mix test` (full default suite incl. the DS-06 raw-color ratchet) must be green and `test/support/ds06_baseline.txt` must be byte-identical.

Purpose: This is the integration that makes the brand system real — the final phase of milestone v2.17.
Output: Updated README.md, layouts.ex, assets.ex, root.html.heex, new priv/static/favicon.svg, updated mix.exs, and a mutated GitHub repo description.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-integration-quality-gate/22-CONTEXT.md

<read_first>
Read these BEFORE writing any task code. The exact bytes are load-bearing.

- `brandbook/brand-book.md` §2 "Ready-to-paste copy blocks" (lines ~97-120). Three blocks are used verbatim:
  - Hex.pm `description:` (140-char block): `Phoenix-native AI ops: LLM traces, evals, prompt versions, replay, tool governance, and MCP workflows. Ecto-backed, LiveView-included.`
  - GitHub repo description: `Phoenix-native AI ops: trace, eval, replay, govern. LLM runs, tool approvals, prompt versions, and MCP workflows wired into Phoenix + Ecto + LiveView.`
  - README opening paragraph: `Scoria is a batteries-included Phoenix library for production AI features. It records every run — prompt renders, model calls, tool calls, retrieval events, approvals, and eval scores — as structured, queryable traces. You get a LiveView operator UI, an eval flywheel, a prompt version registry, and a tool/MCP governance layer, all wired into Phoenix, Ecto, and OTP without a black-box dependency.`
  - Tagline (for README, §1 lines ~76): `AI ops for Phoenix apps.`
- `brandbook/favicon.svg` — the exact SVG to copy into priv/static/favicon.svg. viewBox `-53.32 -52.98 99.66 96.07`, single `fill="#E65A32" fill-rule="evenodd"` path with 3 evenodd holes (favicon-simplified geometry, legible at 16px).
- `brandbook/logo-mark.svg` — the full TV-1 "Span rail" mark (4-hole). For the 24x24 dashboard mark, adapt the favicon's 3-hole simplified geometry (per CONTEXT §specifics: judge holes ≥1.5px at 24px; the favicon path is already pixel-snapped and proven legible at 16px, so it is the correct source for the 24px slot).
- `lib/scoria_web/components/layouts.ex` lines 16-34 — current `brand_mark/1` (placeholder cinder blob + circles to be replaced).
- `lib/scoria_web/components/layouts/root.html.heex` — the dashboard `<head>`. Note the self-contained inline model: CSS via `<style>...ScoriaWeb.Assets.css()...</style>` and JS via `<script>...ScoriaWeb.Assets.js()...</script>`, both nonce-guarded. There is NO Plug.Static route for the dashboard — assets are inlined at compile time. The favicon MUST follow this same inline pattern (a data-URI `<link rel="icon">`), NOT a static file URL the dashboard does not serve.
- `lib/scoria_web/assets.ex` — the compile-time asset embedder (`@external_resource` + `File.read!` at compile time, exposing `css/0` and `js/0`). Add a `favicon/0` reading `priv/static/favicon.svg` the same way.
- `lib/scoria_web/components/layouts/app.html.heex` line 4 — the sidebar brand slot already calls `<.brand_mark /> Scoria`; do not change the call site.
- `mix.exs` lines 118-120 — `description/0` (must stay a plain string; no @version bump).
- README.md lines 1-21 — current `# Scoria` H1, badge row (lines 3-8, KEEP verbatim), and opening paragraph (line 10, to be replaced).
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README brand header + verbatim opener</name>
  <files>README.md</files>
  <action>
    Replace the README header region (current lines 1-10) so it reads, top to bottom:
    1. A centered `<picture>` brand block: `<picture><source media="(prefers-color-scheme: dark)" srcset="brandbook/logo-primary.svg"><img src="brandbook/logo-primary-light.svg" alt="Scoria" width="360"></picture>` wrapped in a `<p align="center">` (or `<div align="center">`). Use RELATIVE paths (these SVGs already ship in the repo; do NOT add absolute raw URLs, do NOT add brandbook assets to the Hex package files list — that is a deferred idea).
    2. RETAIN the `# Scoria` H1 immediately below the picture block — this is the graceful-degradation anchor: HexDocs strips/relativizes the picture, so the H1 must always carry the accessible title. (CONTEXT decision: keep the H1 so every renderer degrades gracefully.)
    3. The tagline line `AI ops for Phoenix apps.` as plain text beneath the H1 (verbatim from brand-book.md §1 primary tagline).
    4. KEEP the existing badge row (CI, Hex.pm, Hex Docs, License, Elixir, Phoenix — current README lines 3-8) verbatim and in the same order (audit verdict KEEP — no badge restyling).
    5. Replace the current opening paragraph (current line 10) with the brand-book.md "README opening paragraph" copy block VERBATIM (see read_first). Do NOT restructure or touch any content below the opening paragraph.
    Order within the header is Claude's discretion (picture → H1 → tagline → badges → opener is the recommended order). Do not change anything from "## Who This Is For" onward.
  </action>
  <verify>
    <automated>grep -q "<picture>" README.md && grep -q 'srcset="brandbook/logo-primary.svg"' README.md && grep -q "^# Scoria" README.md && grep -q "AI ops for Phoenix apps." README.md && grep -q "batteries-included Phoenix library for production AI features" README.md && grep -qF "![CI]" README.md && grep -qF "Phoenix-1.7" README.md && echo OK</automated>
  </verify>
  <done>README has a dark/light <picture> block, retained `# Scoria` H1, the tagline, all six original badges, and the verbatim opener paragraph; content below the opener is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard TV-1 mark swap + favicon wiring</name>
  <files>priv/static/favicon.svg, lib/scoria_web/components/layouts.ex, lib/scoria_web/assets.ex, lib/scoria_web/components/layouts/root.html.heex</files>
  <action>
    Three coordinated changes for the self-contained dashboard:
    1. Copy `brandbook/favicon.svg` to a NEW file `priv/static/favicon.svg` byte-for-byte (it is the pixel-snapped, 3-hole, evenodd, 16px-legible mark). priv/static already ships in the Hex package files list, so no mix.exs files change is needed.
    2. In `lib/scoria_web/components/layouts.ex`, rewrite `brand_mark/1` to render the REAL TV-1 mark. Replace the placeholder (cinder blob path with opacity+stroke, plus the three `<circle>` elements) with a SINGLE `<path>` using `fill-rule="evenodd"`, `fill="var(--scoria-ember-500)"`, NO stroke, NO opacity layers. Adapt the favicon's 3-hole simplified geometry to a `viewBox="0 0 24 24"`: take the favicon path (viewBox `-53.32 -52.98 99.66 96.07`) and either (a) keep its native viewBox on the `<svg>` while preserving the `scoria-brand__mark` class + `@class` attr API + `aria-hidden="true"`, OR (b) translate/scale the coordinates into a 0 0 24 24 box. Either is acceptable as long as the rendered mark is the real Span-rail vesicle with intact evenodd holes and no holes shrink below ~1.5px at 24px. Keep the `attr(:class, :string, default: nil)` API and the `class={["scoria-brand__mark", @class]}` binding UNCHANGED. Update the `@doc` to reference the TV-1 "Span rail" mark instead of the placeholder description.
    3. Wire the favicon into the dashboard `<head>` via the existing compile-time inline model (NOT a static URL — the dashboard serves no Plug.Static route):
       - In `lib/scoria_web/assets.ex`, add a `favicon/0` function mirroring `css/0`/`js/0`: add a `favicon_path` pointing at `priv/static/favicon.svg`, `@external_resource favicon_path`, read it at compile time (`@favicon (if File.exists?(...), do: File.read!(...), else: "")`), and expose `def favicon, do: @favicon`. Build a data-URI: percent-encode is unnecessary for a `data:image/svg+xml;base64,` form — base64-encode the SVG at compile time so the value is safe inside an attribute. Prefer `def favicon_data_uri, do: "data:image/svg+xml;base64," <> Base.encode64(@favicon)` (returns "" gracefully if the file is absent at compile time).
       - In `lib/scoria_web/components/layouts/root.html.heex`, add inside `<head>` (after the `<.live_title>` line): `<link rel="icon" type="image/svg+xml" href={ScoriaWeb.Assets.favicon_data_uri()} />`. This keeps the dashboard fully self-contained and path-independent, consistent with the inlined CSS/JS.
    DS-06 guard: the mark uses ONLY the CSS var `var(--scoria-ember-500)` — no raw palette classes, no hex literals in layouts.ex.
  </action>
  <verify>
    <automated>test -f priv/static/favicon.svg && grep -q 'fill-rule="evenodd"' priv/static/favicon.svg && grep -q 'fill-rule="evenodd"' lib/scoria_web/components/layouts.ex && ! grep -q "<circle" lib/scoria_web/components/layouts.ex && grep -q "var(--scoria-ember-500)" lib/scoria_web/components/layouts.ex && grep -q "def favicon_data_uri" lib/scoria_web/assets.ex && grep -q 'rel="icon"' lib/scoria_web/components/layouts/root.html.heex && grep -q "favicon_data_uri()" lib/scoria_web/components/layouts/root.html.heex && echo OK</automated>
  </verify>
  <done>priv/static/favicon.svg exists (evenodd); brand_mark/1 renders a single evenodd path with CSS-var fill and no `<circle>` placeholders; assets.ex exposes a compile-time favicon data-URI; root.html.heex declares `<link rel="icon" ...>` in `<head>` via the inline model.</done>
</task>

<task type="auto">
  <name>Task 3: Hex/GitHub copy swap + full mix test gate</name>
  <files>mix.exs</files>
  <action>
    1. In `mix.exs`, replace the `description/0` return string with the verbatim Hex.pm 140-char copy block (see read_first): `Phoenix-native AI ops: LLM traces, evals, prompt versions, replay, tool governance, and MCP workflows. Ecto-backed, LiveView-included.` Keep `description/0` a plain string literal. Do NOT bump `@version`, do NOT touch deps, package files, CHANGELOG, or docs extras.
    2. Update the GitHub repo description via the gh CLI (external mutation): run `gh repo edit szTheory/scoria --description "Phoenix-native AI ops: trace, eval, replay, govern. LLM runs, tool approvals, prompt versions, and MCP workflows wired into Phoenix + Ecto + LiveView."` (verbatim brand-book.md GitHub repo description block). If gh returns an auth error, surface it as a blocking auth gate (do not silently skip) — the user re-auths then you retry.
    3. Run the full default test suite AFTER the Task 2 layout/asset change: `mix test`. It must exit 0, including the DS-06 raw-color drift guard. Confirm the DS-06 baseline was not modified: `git diff --quiet -- test/support/ds06_baseline.txt` (must show no change).
  </action>
  <verify>
    <automated>grep -qF "Phoenix-native AI ops: LLM traces, evals, prompt versions, replay, tool governance, and MCP workflows. Ecto-backed, LiveView-included." mix.exs && git diff --quiet -- test/support/ds06_baseline.txt && mix test && [ "$(gh repo view szTheory/scoria --json description -q .description)" = "Phoenix-native AI ops: trace, eval, replay, govern. LLM runs, tool approvals, prompt versions, and MCP workflows wired into Phoenix + Ecto + LiveView." ] && echo OK</automated>
  </verify>
  <done>mix.exs description/0 is the verbatim Hex block; GitHub repo description equals the verbatim block; `mix test` exits 0; ds06_baseline.txt is byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard browser ← inlined `<head>` favicon | A data-URI SVG is inlined into the dashboard document head |
| maintainer → GitHub repo metadata | `gh repo edit` mutates external repo description |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | brand_mark/1 SVG markup swap | mitigate | Markup-only change; `mix test` (incl. LiveViewTest + DS-06 ratchet) gates it; only `app.html.heex` consumes brand_mark and no test asserts SVG internals (grep-confirmed) |
| T-22-02 | Information disclosure | favicon data-URI in `<head>` | accept | Static brand SVG, no PII; same nonce-guarded inline model as existing CSS/JS |
| T-22-03 | Tampering | `gh repo edit` external mutation | mitigate | Verify step asserts the live description equals the exact expected string via `gh repo view --json description` |
| T-22-04 | Tampering | DS-06 baseline (test/support/ds06_baseline.txt) | mitigate | `git diff --quiet` gate asserts byte-identical baseline; no token/CSS change in this plan |
</threat_model>

<verification>
- README: `grep "<picture>" README.md`, `grep "^# Scoria" README.md`, opener paragraph present, all six badges intact.
- Dashboard: `priv/static/favicon.svg` exists; `brand_mark/1` is a single evenodd path with no `<circle>`; `assets.ex` exposes favicon data-URI; `root.html.heex` has `<link rel="icon">`.
- Copy: mix.exs description verbatim; GitHub description verbatim (via `gh repo view`).
- `mix test` exits 0; `test/support/ds06_baseline.txt` byte-identical (`git diff --quiet`).
</verification>

<success_criteria>
README header (picture + H1 + tagline + badges + verbatim opener), dashboard TV-1 mark + inlined favicon, mix.exs + GitHub copy all landed; `mix test` green; DS-06 baseline untouched. BRAND-07 and BRAND-08 satisfied.
</success_criteria>

<output>
Create `.planning/phases/22-integration-quality-gate/22-01-SUMMARY.md` when done.
</output>
