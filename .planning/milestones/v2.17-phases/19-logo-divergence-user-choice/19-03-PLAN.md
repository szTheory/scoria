---
phase: 19-logo-divergence-user-choice
plan: 03
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - brandbook/tools/verify-logos.mjs
  - brandbook/tools/options-gallery.html
autonomous: false
requirements: [BRAND-03]

must_haves:
  truths:
    - "A scripted verifier checks every candidate SVG against LOGO-01..07 and exits non-zero if any candidate fails a structural rule"
    - "The verifier confirms the gallery is standalone (no external network refs), has both grounds, the full size ramp, monochrome row, favicon/sidebar/README mocks, and all stable option IDs"
    - "The ranked recommendation block in options-gallery.html is finalized (TV #1 primary, CM #2 fallback, AP study, with rationale)"
    - "Gate #2 is presented to the user as a checkpoint:decision: open the gallery, pick an option ID or choose 'none of these → second round'"
  artifacts:
    - path: "brandbook/tools/verify-logos.mjs"
      provides: "scripted LOGO-01..07 + gallery-completeness verification, exits non-zero on any failure"
      min_lines: 60
  key_links:
    - from: "brandbook/tools/verify-logos.mjs"
      to: "brandbook/tools/candidates/"
      via: "reads each candidate SVG, asserts structural rules"
      pattern: "candidates"
    - from: "brandbook/tools/verify-logos.mjs"
      to: "brandbook/tools/options-gallery.html"
      via: "asserts gallery completeness (grounds, sizes, mocks, IDs, no network)"
      pattern: "options-gallery"
---

<objective>
Verify every candidate against the LOGO-01..07 hard rules with a scripted sweep, confirm the gallery is complete and standalone, finalize the ranked recommendation, and present gate #2 to the user as a checkpoint:decision (orchestrator-presented): the user opens `brandbook/tools/options-gallery.html`, then picks a logo direction by option ID — or selects "none of these → second round."

Purpose: This is the quality gate before Phase 20 commits to a single direction. Structural compliance is scripted (grep/AST-free string checks); 16px legibility and craft divergence are explicitly deferred to human judgment at the gate. The user's recorded choice is the BRAND-03 deliverable.

Output: verify-logos.mjs (passing), a finalized recommendation block in the gallery, and a checkpoint:decision capturing the user's chosen direction. NO new candidates, NO Phase 20 variant set, NO brandbook/ root files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-logo-divergence-user-choice/19-CONTEXT.md
@.planning/phases/19-logo-divergence-user-choice/19-02-SUMMARY.md

# Logo rules + grep checks + direction ranking (source of truth)
@brandbook/pressure-test.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scripted LOGO-01..07 + gallery-completeness verifier</name>
  <files>brandbook/tools/verify-logos.mjs</files>
  <read_first>
    - brandbook/pressure-test.md §SECTION 8 RULE LOGO-01..07 + §"CI/lint checks" (the grep idioms: reject <rect>, count evenodd, budget <512KB)
    - .planning/phases/19-logo-divergence-user-choice/19-CONTEXT.md (§Gallery evidence list; structural checks are grep-able)
    - brandbook/tools/candidates/ (the SVGs to verify) and brandbook/tools/options-gallery.html (from 19-02-SUMMARY)
  </read_first>
  <action>
    Implement `verify-logos.mjs` (Node ESM) that reads all `brandbook/tools/candidates/*.svg` and the gallery, and asserts, printing a per-check PASS/FAIL line and `process.exit(1)` on any failure:
    - LOGO-01: zero `<rect` (and no full-bleed background container) across all candidate SVGs.
    - LOGO-02: every `*-mark.svg` contains `fill-rule="evenodd"` and exactly one `<path>`.
    - LOGO-03: each lockup SVG root carries the generator-emitted `data-gap-ratio` attribute (contract from 19-01 composeLockup) with a numeric value in [0.35, 0.5]. Missing attribute = FAIL (do not fall back to coordinate re-derivation).
    - LOGO-04: no lockup contains subtitle/tagline text ("AI ops"/"Phoenix").
    - LOGO-05: >=2 typemark studies present (TYPE-* files).
    - LOGO-06: Trace Vesicle favicon SVGs exist and declare exactly 3 holes (count hole subpaths in the 16px variant).
    - LOGO-07: monochrome variants exist using currentColor, holes intact.
    - No strokes anywhere; coordinates <=2 decimals; viewBox present and tight (origin near 0).
    - Gallery completeness: standalone (zero external http(s) refs, zero `<img`), both grounds (#11100F + #FAF5EF), size ramp (256/64/32/16), monochrome + favicon + sidebar + README mock keywords, all stable IDs (>=6 marks + 2 typemarks), ranked-recommendation block present, "second round" escape note present.
    - brandbook/ budget under 512KB (the §8 CI budget check).
    Add a `"verify": "node verify-logos.mjs"` script to package.json. Do not modify candidates; this script only reads + reports.
  </action>
  <verify>
    <automated>cd brandbook/tools && node verify-logos.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node brandbook/tools/verify-logos.mjs` exits 0 with all LOGO-01..07 checks printing PASS
    - The script genuinely fails when fed a bad input (sanity: temporarily run against an SVG with a `<rect>` shows FAIL + exit 1 — verify by a throwaway check, do not commit the bad SVG)
    - Independent spot-checks corroborate the script:
      - `grep -rc '<rect' brandbook/tools/candidates/*.svg | grep -v ':0' | wc -l` returns 0
      - `grep -L 'fill-rule="evenodd"' brandbook/tools/candidates/*-mark.svg` returns empty
      - `ls brandbook/tools/candidates/TYPE-*.svg | wc -l` returns >=2
      - `du -sk brandbook | cut -f1` is < 512
    - Gallery completeness asserted (no external network refs; both grounds; size ramp; mock keywords; all IDs; recommendation + escape present)
  </acceptance_criteria>
  <done>verify-logos.mjs passes all LOGO-01..07 + gallery-completeness checks, exits 0, and genuinely fails on bad input.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize ranked recommendation in the gallery</name>
  <files>brandbook/tools/options-gallery.html</files>
  <read_first>
    - brandbook/pressure-test.md §SECTION 8 direction ranking (TV #1 why, CM #2 why, AP #3 why, CC #4 why — the rationale source)
    - .planning/phases/19-logo-divergence-user-choice/19-CONTEXT.md (§Gallery ranked recommendation block + stable IDs + escape path)
    - brandbook/tools/options-gallery.html (the recommendation block to finalize, from 19-02)
  </read_first>
  <action>
    Edit the recommendation block at the top of `options-gallery.html` to name the recommended option by its specific stable ID (e.g. "Recommended: TV-1 — Trace Vesicle"), give a 2-3 sentence rationale grounded in the §8 ranking (trace-tree internal logic, 16px survival, monochrome survival, distinctiveness vs Threadline + devtools peers), list the runner-up (a Cinder option as fallback) and the Aperture as a study, and state the explicit escape: "None of these → request a second round." Keep it concise and utilitarian. Do not add new candidates or alter the SVGs.
  </action>
  <verify>
    <automated>cd brandbook/tools && grep -qi 'recommend' options-gallery.html && grep -qiE 'second round|none of these' options-gallery.html && grep -oE 'TV-[0-9]|CM-[0-9]' options-gallery.html | head -1 | grep -q . && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Recommendation block names a specific recommended option ID: `grep -oE 'Recommended[^<]*(TV-[0-9]|CM-[0-9])' options-gallery.html` returns a match (or the recommended ID appears within a "Recommended" heading region)
    - Runner-up + study mentioned: `grep -ci 'fallback\|runner-up\|study' brandbook/tools/options-gallery.html` returns >=1
    - Escape path present: `grep -ciE 'second round|none of these' brandbook/tools/options-gallery.html` returns >=1
    - `node brandbook/tools/verify-logos.mjs` still exits 0 after the edit
  </acceptance_criteria>
  <done>The gallery's recommendation block names a specific recommended option ID with rationale, runner-up, study, and the second-round escape; verifier still passes.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Gate #2 — user chooses a logo direction</name>
  <what-built>
    6+ hand-tuned mark options (with combination lockups) and 2+ integrated typemark studies, all passing LOGO-01..07 structural verification, presented in a standalone `brandbook/tools/options-gallery.html` with dark+light grounds, a 256/64/32/16px size ramp, monochrome row, browser-tab favicon strip, 24px sidebar mock, README header band mock, and a ranked recommendation.
  </what-built>
  <how-to-verify>
    1. Open `brandbook/tools/options-gallery.html` in a browser (file:// — no server needed).
    2. Review each option at all sizes on BOTH the dark and light grounds. Pay special attention to the 16px favicon strip and the monochrome row (LOGO-06/07 are pass/fail by eye here).
    3. Compare the Trace Vesicle options (TV-*), Cinder options (CM-*), the Aperture study (AP-1), and the integrated typemark studies (TYPE-1, TYPE-2).
    4. Read the ranked recommendation block at the top.
  </how-to-verify>
  <decision>Which logo direction advances to Phase 20 (full variant set)?</decision>
  <context>
    Gate #2 of the v2.17 Vesicle milestone. The chosen direction is locked and becomes the single direction Phase 20 builds the full 8-variant set from. "None of these" triggers a second round of curated options (back into 19-02 territory) rather than advancing.
  </context>
  <options>
    <option id="pick-option-id">
      <name>Choose a specific option by ID (e.g. TV-1, TV-2, CM-1, CM-2, AP-1, TYPE-1, TYPE-2, or the 6th mark ID)</name>
      <pros>Direction locked; Phase 20 can begin the full variant set + optical-correction pass.</pros>
      <cons>None — this is the intended happy path.</cons>
    </option>
    <option id="second-round">
      <name>None of these → request a second round</name>
      <pros>Honest escape if no option is right; preserves quality bar over shipping a mediocre mark.</pros>
      <cons>Re-enters 19-02 to hand-tune a fresh curated set before re-presenting.</cons>
    </option>
  </options>
  <resume-signal>State the chosen option ID (e.g. "TV-1"), or "second round". The orchestrator records the choice in STATE.md before Phase 20.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate SVGs -> verifier | structural-rule enforcement before human review |
| human judgment -> recorded direction | the gate-#2 decision that locks Phase 20 scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Repudiation | recorded logo choice | mitigate | gate #2 is an explicit checkpoint:decision; the chosen option ID is recorded in STATE.md by the orchestrator before Phase 20 |
| T-19-06 | Tampering | a non-compliant candidate slips to the gate | mitigate | verify-logos.mjs gates LOGO-01..07 + gallery completeness and exits non-zero on any failure before the human decision |
</threat_model>

<verification>
- `node brandbook/tools/verify-logos.mjs` exits 0 (all LOGO-01..07 + gallery checks PASS)
- options-gallery.html names a recommended option ID and includes the second-round escape
- Gate #2 checkpoint:decision presented; user's chosen direction recorded before Phase 20
- No Phase 20 leak: `ls brandbook/*.svg brandbook/tools/favicon.svg brandbook/tools/social-card.svg 2>/dev/null | wc -l` returns 0
</verification>

<success_criteria>
- Every candidate passes scripted LOGO-01..07 verification
- Gallery is standalone + complete; recommendation finalized with a named option ID
- User has chosen a logo direction (or requested a second round) at gate #2 — BRAND-03 satisfied
</success_criteria>

<output>
Create `.planning/phases/19-logo-divergence-user-choice/19-03-SUMMARY.md` when done
</output>
