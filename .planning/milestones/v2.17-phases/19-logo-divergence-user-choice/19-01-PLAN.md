---
phase: 19-logo-divergence-user-choice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/tools/package.json
  - brandbook/tools/.gitignore
  - brandbook/tools/lib/geometry.mjs
  - brandbook/tools/lib/wordmark.mjs
  - brandbook/tools/lib/lockup.mjs
  - brandbook/tools/lib/svg.mjs
  - brandbook/tools/smoke.mjs
autonomous: true
requirements: [BRAND-03]

must_haves:
  truths:
    - "Running the smoke script produces one throwaway mark SVG and one lockup SVG that pass all structural checks"
    - "The geometry library emits a single evenodd compound path string (outer faceted polygon + punched vesicle holes), never a <rect> or stroke"
    - "The wordmark module decompresses the repo woff2 at runtime and outlines 'Scoria' in IBM Plex Sans SemiBold to a path, with an integrated-glyph 'o'-replacement capability"
    - "The lockup composer places mark + wordmark with a gap of 0.35-0.5x cap height (LOGO-03)"
  artifacts:
    - path: "brandbook/tools/package.json"
      provides: "opentype.js + wawoff2 deps, type:module, Node>=18"
      contains: "opentype.js"
    - path: "brandbook/tools/.gitignore"
      provides: "node_modules ignore so no deps are committed"
      contains: "node_modules"
    - path: "brandbook/tools/lib/geometry.mjs"
      provides: "faceted-polygon + vesicle-hole preset -> single evenodd path string"
      min_lines: 60
    - path: "brandbook/tools/lib/wordmark.mjs"
      provides: "woff2 decompress -> opentype.js outline of Scoria + glyph-replacement hook"
      min_lines: 50
    - path: "brandbook/tools/lib/lockup.mjs"
      provides: "mark + wordmark composer with LOGO-03 spacing + tight viewBox"
      min_lines: 30
    - path: "brandbook/tools/lib/svg.mjs"
      provides: "SVG document builder (viewBox-hugging, title/role=img, currentColor support)"
      min_lines: 30
    - path: "brandbook/tools/smoke.mjs"
      provides: "renders one mark + one lockup, asserts structural validity, exits non-zero on failure"
      min_lines: 40
  key_links:
    - from: "brandbook/tools/lib/wordmark.mjs"
      to: "assets/fonts/ibm-plex-sans_latest_latin-600-normal.woff2"
      via: "wawoff2.decompress(readFileSync) -> opentype.parse(buffer)"
      pattern: "wawoff2|decompress"
    - from: "brandbook/tools/lib/lockup.mjs"
      to: "brandbook/tools/lib/geometry.mjs"
      via: "import mark path + wordmark path, compose with spacing"
      pattern: "import.*geometry"
---

<objective>
Build the reproducible generation toolchain for Phase 19 logo options: a `package.json` with the font/SVG dependencies, a faceted-polygon + evenodd-hole geometry library, a wordmark module that outlines "Scoria" from the repo's IBM Plex Sans SemiBold woff2 (with an integrated-glyph replacement hook), a lockup composer enforcing LOGO-03 tight spacing, and a smoke test that renders one throwaway mark + lockup and proves they are structurally valid (single evenodd path, no rect, no stroke, tight viewBox).

Purpose: This is the scaffolding every option in 19-02 is built on. Getting the structural invariants (LOGO-01/02/03) enforced in code here means the curated presets in 19-02 inherit compliance by construction rather than re-litigating it per option.

Output: `brandbook/tools/` toolchain modules + a passing smoke test. NO candidate options yet (that is 19-02). NO files under `brandbook/` root (that is Phase 20).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-logo-divergence-user-choice/19-CONTEXT.md

# Logo system rules + grep checks (source of truth)
@brandbook/pressure-test.md

# SVG conventions precedent (read-only sibling)
@/Users/jon/projects/threadline/brandbook/logo-mark.svg
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tooling scaffold (package.json + .gitignore)</name>
  <files>brandbook/tools/package.json, brandbook/tools/.gitignore</files>
  <read_first>
    - .planning/phases/19-logo-divergence-user-choice/19-CONTEXT.md (§Tooling — deps, gitignore, Node>=18 ESM)
    - brandbook/pressure-test.md §SECTION 8 "Do not do" #15 (SVG-first, no rasters) and CI checks (budget <512KB)
  </read_first>
  <action>
    Create `brandbook/tools/package.json` with `"type": "module"`, `"private": true`, an engines field requiring Node >=18, and dependencies pinned explicitly: `"opentype.js": "^1.3.5"` (pin to the 1.x line — npm `latest` is 2.0.0, a major bump; 1.3.5 ships the `parse(buffer)` + `Font.getPath` API this plan uses) and `"wawoff2": "^2.0.1"`. Add npm scripts: `"smoke": "node smoke.mjs"` and `"generate": "node generate.mjs"` (generate.mjs lands in 19-02 — the script entry can reference it now). Create `brandbook/tools/.gitignore` ignoring `node_modules/` and any transient `*.ttf` buffers. Do NOT run `npm install` of un-audited packages without checking the legitimacy table; opentype.js and wawoff2 are both long-established, high-download packages (see acceptance criteria for verification). Do NOT commit node_modules or any font binary under brandbook/.
  </action>
  <verify>
    <automated>cd brandbook/tools && grep -q '"type": "module"' package.json && grep -q 'opentype.js' package.json && grep -q 'wawoff2' package.json && grep -qx 'node_modules/' .gitignore && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'opentype.js' brandbook/tools/package.json` returns >=1
    - `grep -c 'wawoff2' brandbook/tools/package.json` returns >=1
    - `grep -c 'node_modules' brandbook/tools/.gitignore` returns >=1
    - package.json has `"type": "module"`
    - opentype.js verified legitimate at npmjs.com/package/opentype.js (>1M weekly downloads, est. 2015); wawoff2 verified at npmjs.com/package/wawoff2 (official Google woff2 wasm port). Both PASS legitimacy.
  </acceptance_criteria>
  <done>package.json and .gitignore exist with correct deps and ignores; both packages are legitimacy-verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Geometry + SVG document libraries</name>
  <files>brandbook/tools/lib/geometry.mjs, brandbook/tools/lib/svg.mjs</files>
  <read_first>
    - .planning/phases/19-logo-divergence-user-choice/19-CONTEXT.md (§Geometry language — faceted polygon 6-8 anchors, ±12-18% radius variance, 1-2 flat facets, holes 5-7 circular vesicles with size hierarchy, evenodd; §Specifics — edge-bleeding notch idea)
    - brandbook/pressure-test.md §SECTION 8 RULE LOGO-01 and LOGO-02 (no rect, evenodd holes, no white shapes)
    - /Users/jon/projects/threadline/brandbook/logo-mark.svg (viewBox + title/role conventions)
  </read_first>
  <behavior>
    - `facetedPolygon({anchors, radiusVariance, flatFacets, rounding, seed})` returns an SVG path `d` string of a closed rounded faceted polygon (quadratic/arc joins), all coordinates rounded to <=2 decimals.
    - `vesicleHoles([{cx,cy,r}, ...])` returns hole sub-paths; combined with the outer polygon into ONE `d` string the holes punch via fill-rule="evenodd" (page shows through).
    - `markPath(preset)` returns a single compound `d` string (outer + holes) — NEVER multiple paths, NEVER a rect, NEVER a stroke attribute.
    - `svgDocument({pathD, fillRule, fill, viewBox, title})` (svg.mjs) returns an SVG string with a viewBox hugging the artwork bbox, `role="img"`, a `<title>`, a single `<path fill-rule="evenodd">`, and supports `fill="currentColor"` for monochrome.
    - An optional edge-notch flag lets one hole bleed through the silhouette edge (CONTEXT §Specifics "breaking the boundaries").
  </behavior>
  <action>
    Implement `geometry.mjs`: faceted-polygon generator (low-frequency radius perturbation only, deterministic given a seed so diffs are stable), a vesicle-hole layout helper, and `markPath()` that concatenates outer-polygon + reversed-or-evenodd hole subpaths into a single `d`. Round all numbers to <=2 decimals. Implement `svg.mjs`: a `svgDocument()` builder that computes a tight viewBox from the path bbox (no padding container), emits `role="img"`, `<title>`, and exactly one `<path>` with `fill-rule="evenodd"`. NO `<rect>`, NO `stroke`. Use `currentColor` as the default fill so monochrome rendering is a fill override. Do not inline full preset parameter sets here — presets are 19-02's craft work; this library just provides the primitives.
  </action>
  <verify>
    <automated>cd brandbook/tools && node -e "import('./lib/geometry.mjs').then(g=>{const d=g.markPath({anchors:7,radiusVariance:0.15,flatFacets:1,holes:[{cx:0,cy:-8,r:4},{cx:3,cy:2,r:2}],seed:1}); if(typeof d!=='string'||d.length<20) process.exit(1); import('./lib/svg.mjs').then(s=>{const svg=s.svgDocument({pathD:d,fillRule:'evenodd',fill:'currentColor',title:'t'}); if(/<rect/.test(svg)||/stroke/.test(svg)) process.exit(1); if(!/fill-rule=\"evenodd\"/.test(svg)) process.exit(1); if((svg.match(/<path/g)||[]).length!==1) process.exit(1); console.log('OK')})})"</automated>
  </verify>
  <acceptance_criteria>
    - `markPath()` returns a single string `d` >= 20 chars
    - `svgDocument()` output contains `fill-rule="evenodd"`, contains `role="img"`, contains exactly one `<path`
    - `svgDocument()` output contains zero `<rect` and zero `stroke`
    - All numeric coordinates in the path are rounded to <=2 decimals (no `\d+\.\d{3,}` matches)
  </acceptance_criteria>
  <done>Geometry + SVG libraries produce a single evenodd compound path with a tight viewBox, no rect, no stroke; coordinates are 2-decimal clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wordmark + lockup modules + smoke test</name>
  <files>brandbook/tools/lib/wordmark.mjs, brandbook/tools/lib/lockup.mjs, brandbook/tools/smoke.mjs</files>
  <read_first>
    - .planning/phases/19-logo-divergence-user-choice/19-CONTEXT.md (§Tooling — woff2 source path + opentype.js getPath; §Specifics — Plex 'o' near-circular, integrated 'o' keeps advance width + x-height, ring weight matches 'S' stem; LOGO-03 spacing; §decisions wordmark "Scoria" SemiBold tracking -1% to 0%)
    - brandbook/pressure-test.md §SECTION 8 RULE LOGO-03 (gap 0.35-0.5x cap height) and RULE LOGO-05 (integrated typemark motif INTO letterforms)
    - assets/fonts/ibm-plex-sans_latest_latin-600-normal.woff2 (the glyph source — must decompress at runtime, never commit)
  </read_first>
  <behavior>
    - `loadFont()` reads the repo woff2, decompresses via wawoff2 to a ttf buffer, parses with opentype.js — returns a Font. Throws a clear error if the font path is missing.
    - `wordmarkPath("Scoria", {tracking})` returns the outlined `d` for the word plus its measured cap height and total width, tracking applied (-1% to 0%).
    - `integratedTypemark("Scoria", {oReplacement})` returns a wordmark path where the 'o' glyph is replaced by a vesicle-ring form whose advance width and x-height alignment match the original 'o' (so kerning is undisturbed) and whose ring weight ≈ the 'S' stem width.
    - `composeLockup({markD, markBox, wordmarkD, wordmarkBox})` (lockup.mjs) returns an SVG string placing the mark left of the wordmark with gap = 0.35-0.5x cap height and optical baseline alignment, tight viewBox over both.
    - `smoke.mjs` renders one throwaway mark SVG and one throwaway lockup SVG to a temp dir and asserts: single `<path>` per mark, `fill-rule="evenodd"` present, zero `<rect>`, zero `stroke`, a viewBox present and tight (origin near 0, no oversized padding). Exits non-zero on any failure.
  </behavior>
  <action>
    Implement `wordmark.mjs` using wawoff2 + opentype.js `Font.getPath`/`getPaths` to outline "Scoria". IMPORTANT — wawoff2 is CJS-only: use the default import form `import wawoff2 from 'wawoff2'` then call `wawoff2.decompress(...)`; do NOT write `import { decompress } from 'wawoff2'` (fails in Node ESM: "does not provide an export named 'decompress'"). Expose cap-height and width measurements; implement the integrated 'o'-replacement hook (swap the 'o' contour for a vesicle ring while preserving advance + x-height — the actual ring geometry can reuse geometry.mjs's vesicle helper). Implement `lockup.mjs` `composeLockup()` enforcing the LOGO-03 gap formula (compute gap as a fraction of measured cap height, clamp to 0.35-0.5x) and baseline alignment; `composeLockup` MUST emit a `data-gap-ratio="<gap/capHeight, 2 decimals>"` attribute on the lockup SVG root — this is the machine-checkable LOGO-03 contract that 19-03's verifier asserts numerically. Implement `smoke.mjs` to render one mark + one lockup to `os.tmpdir()` (NOT into candidates/ — these are throwaway), run the structural assertions, print a PASS line, and `process.exit(1)` on failure. Do not produce gallery or candidate files here.
  </action>
  <verify>
    <automated>cd brandbook/tools && npm install --no-audit --no-fund >/dev/null 2>&1; node smoke.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node brandbook/tools/smoke.mjs` exits 0 and prints a PASS line
    - The smoke-rendered mark SVG (inspected by the script) has exactly one `<path>`, contains `fill-rule="evenodd"`, contains zero `<rect>`, contains zero `stroke`
    - The smoke-rendered lockup SVG has a viewBox attribute and renders both mark and wordmark paths
    - `wordmark.mjs` reads `assets/fonts/ibm-plex-sans_latest_latin-600-normal.woff2` via a wawoff2 decompress call (grep: `grep -c 'decompress' brandbook/tools/lib/wordmark.mjs` >= 1)
    - `wordmark.mjs` uses the CJS-safe default import (grep: `grep -c "import wawoff2 from" brandbook/tools/lib/wordmark.mjs` >= 1; `grep -c "import {.*} from 'wawoff2'" brandbook/tools/lib/wordmark.mjs` returns 0)
    - The smoke-rendered lockup SVG root carries `data-gap-ratio` with a value in [0.35, 0.5] (smoke.mjs asserts this)
    - No font binary is written under brandbook/ (grep: `find brandbook -name '*.woff2' -o -name '*.ttf' | wc -l` returns 0)
  </acceptance_criteria>
  <done>Wordmark outlines from the repo woff2, integrated-glyph hook exists, lockup enforces LOGO-03 spacing, and `smoke.mjs` exits 0 proving the toolchain renders structurally valid throwaway artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> tools/node_modules | third-party package code executed at generation time |
| repo woff2 -> opentype.js parser | binary font input parsed by JS library |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | npm installs (opentype.js, wawoff2) | mitigate | both verified legitimate (opentype.js >1M wk downloads est.2015; wawoff2 official Google woff2 wasm port) in Task 1 acceptance; node_modules gitignored so no vendored code enters repo |
| T-19-02 | Denial of Service | font parse in wordmark.mjs | accept | input is a repo-controlled latin-subset woff2, not untrusted; parse runs offline at dev time only |
| T-19-SC | Tampering | npm install step | mitigate | only two long-established packages; legitimacy asserted before install in Task 1; generation tooling never ships in the published Hex package or dashboard runtime |
</threat_model>

<verification>
- `node brandbook/tools/smoke.mjs` exits 0
- `find brandbook -name '*.woff2' -o -name '*.ttf' | wc -l` returns 0 (no committed font binaries)
- `ls brandbook/tools/lib/` shows geometry.mjs, svg.mjs, wordmark.mjs, lockup.mjs
- No files created under `brandbook/` root or `brandbook/tools/candidates/` yet
</verification>

<success_criteria>
- The toolchain renders a structurally valid mark + lockup on demand (smoke passes)
- LOGO-01 (no rect), LOGO-02 (evenodd single path), LOGO-03 (tight spacing) are enforced in code
- Wordmark outlines "Scoria" from the repo woff2 at runtime; integrated 'o'-replacement hook present
- node_modules and font binaries are gitignored / never committed
</success_criteria>

<output>
Create `.planning/phases/19-logo-divergence-user-choice/19-01-SUMMARY.md` when done
</output>
