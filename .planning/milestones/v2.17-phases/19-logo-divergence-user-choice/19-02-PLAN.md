---
phase: 19-logo-divergence-user-choice
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - brandbook/tools/presets.mjs
  - brandbook/tools/generate.mjs
  - brandbook/tools/gallery.mjs
  - brandbook/tools/candidates/ (generated SVGs, ~14+ files, e.g. TV-1-mark.svg / TV-1-lockup.svg / TYPE-1.svg)
  - brandbook/tools/options-gallery.html
autonomous: true
requirements: [BRAND-03]

must_haves:
  truths:
    - "There are at least 6 distinct mark presets (TV-1, TV-2, CM-1, CM-2, AP-1, and a 6th = CC-1 or a 3rd Trace Vesicle) each with a combination lockup, plus at least 2 integrated typemark studies (TYPE-1, TYPE-2)"
    - "Each preset carries a hand-authored design-intent comment block in presets.mjs explaining what distinguishes it (TV-1 vs TV-2, etc.) — not loop-generated variations"
    - "Running generate.mjs writes all candidate SVGs into brandbook/tools/candidates/ and exits 0"
    - "options-gallery.html is a standalone file:// page: inline SVG, dark + light grounds, 256/64/32/16px rows, monochrome row, favicon strip, 24px sidebar mock, README band mock, ranked recommendation block, stable option IDs"
  artifacts:
    - path: "brandbook/tools/presets.mjs"
      provides: "6 mark presets + 2 typemark presets, each with a design-intent comment block"
      min_lines: 120
    - path: "brandbook/tools/generate.mjs"
      provides: "orchestrator: presets -> candidate SVGs in candidates/"
      min_lines: 30
    - path: "brandbook/tools/gallery.mjs"
      provides: "builds options-gallery.html with all evidence rows from the generated candidates"
      min_lines: 80
    - path: "brandbook/tools/options-gallery.html"
      provides: "standalone gate-#2 evidence page"
      min_lines: 100
  key_links:
    - from: "brandbook/tools/generate.mjs"
      to: "brandbook/tools/presets.mjs"
      via: "import presets, render each via lib/lockup + lib/svg"
      pattern: "import.*presets"
    - from: "brandbook/tools/gallery.mjs"
      to: "brandbook/tools/candidates/"
      via: "inline each candidate SVG into HTML at multiple sizes"
      pattern: "candidates"
---

<objective>
Produce the curated logo options — THE CRAFT CORE of Phase 19. Hand-tune 6 distinct mark presets (Trace Vesicle x2, Cinder x2, Aperture x1, and a 6th slot) each with a combination lockup, plus 2 integrated typemark studies, each preset carrying a documented design-intent comment block. Generate the candidate SVGs into `brandbook/tools/candidates/` and assemble `brandbook/tools/options-gallery.html` with every gate-#2 evidence row: dark + light grounds, 256/64/32/16px, monochrome, favicon strip, 24px sidebar mock, README band mock, ranked recommendation, and stable option IDs.

Purpose: This is where genuine divergence and craft happen. The presets must be hand-authored parameter sets with explicit design intent (what makes TV-1 different from TV-2), NOT loop-generated variations. The gallery is the evidence the user judges at gate #2.

Output: presets.mjs, generate.mjs, gallery.mjs, candidate SVGs under candidates/, and options-gallery.html. NO files under `brandbook/` root, NO favicon.svg / social-card.svg (Phase 20/22). NO subtitle in any primary lockup (LOGO-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-logo-divergence-user-choice/19-CONTEXT.md
@.planning/phases/19-logo-divergence-user-choice/19-01-SUMMARY.md

# Logo rules, direction ranking, palette (source of truth)
@brandbook/pressure-test.md

<interfaces>
<!-- From 19-01 toolchain (brandbook/tools/lib/). Use these directly. -->
<!-- geometry.mjs:  markPath(preset) -> single evenodd `d`; facetedPolygon(); vesicleHoles(); edge-notch flag -->
<!-- svg.mjs:       svgDocument({pathD, fillRule, fill, viewBox, title}) -> SVG string (role=img, <title>, single <path>, no rect/stroke) -->
<!-- wordmark.mjs:  loadFont(); wordmarkPath("Scoria",{tracking}) -> {d, capHeight, width}; integratedTypemark("Scoria",{oReplacement}) -->
<!-- lockup.mjs:    composeLockup({markD, markBox, wordmarkD, wordmarkBox}) -> SVG string, LOGO-03 gap = 0.35-0.5x capHeight -->
</interfaces>

# Palette (from CONTEXT §decisions / pressure-test §Section 4):
#   Dark ground Basalt-950 #11100F; accents Ember-500 #E65A32, Molten-400 #FF7A4D
#   Light ground Ash-50 #FAF5EF; mark Scoria-600 #B94F31
#   Monochrome via currentColor
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hand-tuned presets with design-intent comments</name>
  <files>brandbook/tools/presets.mjs</files>
  <read_first>
    - .planning/phases/19-logo-divergence-user-choice/19-CONTEXT.md (§decisions direction ranking + option mix TV×2 CM×2 AP×1 CC×1 + ≥2 typemarks; §Geometry language; §Specifics trace-tree hole layout echoes span rail, integrated 'o' constraints; §Claude's Discretion — may swap CC-1 for a 3rd TV)
    - brandbook/pressure-test.md §SECTION 8 direction ranking (why TV #1, CM #2, AP #3, CC #4) + RULE LOGO-05 + RULE LOGO-06 (16px = exactly 3 holes vertical/diagonal for Trace Vesicle)
    - brandbook/tools/lib/geometry.mjs and lib/wordmark.mjs (the preset shape that markPath/integratedTypemark consume — from 19-01-SUMMARY)
  </read_first>
  <action>
    Author a curated preset set as exported objects, each preceded by a design-intent comment block (3-5 lines) stating what the option is and what distinguishes it from its siblings:
    - TV-1, TV-2 (Trace Vesicle): faceted silhouette + holes encoding a downward-branching trace tree (root large near top, 2-3 children down-right, one leaf offset — echoing the dashboard span rail). TV-1 vs TV-2 must differ meaningfully (e.g. TV-1 tighter/more vertical tree, TV-2 wider branching or an edge-bleeding notch). Each must declare a 16px-simplified variant of exactly 3 holes in a vertical/diagonal arrangement (LOGO-06).
    - CM-1, CM-2 (Cinder): irregular faceted cinder, 5-7 scattered holes, no trace-tree logic; CM-1 vs CM-2 differ in facet count/hole density/one flat facet placement.
    - AP-1 (Aperture, study only): central cavity + surrounding smaller cavities — flagged as a study (risks reading as camera/eye).
    - Sixth slot: CC-1 (Cutaway Cone study) OR a third Trace Vesicle (TV-3) per discretion — keep ≥6 marks total and ≥2 directions beyond Trace Vesicle. Document the choice in its comment block.
    - TYPE-1, TYPE-2 (integrated typemarks): the 'o' of "Scoria" replaced by a vesicle-ring (advance width + x-height preserved, ring weight ≈ 'S' stem); TYPE-1 vs TYPE-2 differ in technique (e.g. TYPE-1 = 'o'-only replacement, TYPE-2 = 'o' replacement + 1-2 tiny punched vesicles in the 'a' bowl). Glyph replacement only — no full boolean letterform surgery.
    Use the brand palette hexes for color presets. Do NOT generate via random seeds or a parameter loop; these are hand-authored. Do NOT inline full path implementations here — presets are parameter objects consumed by the 19-01 libraries.
  </action>
  <verify>
    <automated>cd brandbook/tools && node -e "import('./presets.mjs').then(p=>{const marks=p.markPresets||p.default?.markPresets; const types=p.typemarkPresets||p.default?.typemarkPresets; if(!marks||marks.length<6) process.exit(1); if(!types||types.length<2) process.exit(1); console.log('marks',marks.length,'types',types.length)})"</automated>
  </verify>
  <acceptance_criteria>
    - `presets.mjs` exports >=6 mark presets and >=2 typemark presets (asserted by the node check above)
    - Each preset has a preceding design-intent comment block tagged with the literal JSDoc-style tag `@design-intent` (the standard for this file): `grep -c '@design-intent' brandbook/tools/presets.mjs` returns >=8 (one per option)
    - Stable IDs present: `grep -o "'TV-1'\|'TV-2'\|'CM-1'\|'CM-2'\|'AP-1'" brandbook/tools/presets.mjs | sort -u | wc -l` returns 5 (the 6th + the two TYPE ids also present)
    - Trace Vesicle presets declare a 16px 3-hole simplification (grep for a `favicon`/`simplified`/`holes16` key)
    - No random-seed loop generating the option list (no `for` loop producing presets; grep the file shows discrete named exports)
  </acceptance_criteria>
  <done>presets.mjs holds >=6 hand-authored mark presets + >=2 typemark presets, each with a documented design intent and stable ID, honoring the locked direction ranking and geometry language.</done>
</task>

<task type="auto">
  <name>Task 2: Generate candidate SVGs</name>
  <files>brandbook/tools/generate.mjs, brandbook/tools/candidates/</files>
  <read_first>
    - .planning/phases/19-logo-divergence-user-choice/19-CONTEXT.md (§Tooling — gallery builder writes candidates into brandbook/tools/candidates/; §decisions viewBox hugs artwork, numbers <=2 decimals)
    - brandbook/tools/lib/svg.mjs, lib/lockup.mjs, lib/wordmark.mjs (render APIs from 19-01-SUMMARY)
    - brandbook/pressure-test.md §SECTION 8 RULE LOGO-01/02/04 (no rect, evenodd, no subtitle in primary lockup)
  </read_first>
  <action>
    Implement `generate.mjs` to import presets.mjs + the 19-01 libraries and render, for each mark preset, into `brandbook/tools/candidates/`: a mark-only SVG (e.g. `TV-1-mark.svg`), a combination lockup SVG (`TV-1-lockup.svg`, NO subtitle — LOGO-04), a monochrome mark SVG (`TV-1-mono.svg`, currentColor fill), and the 16px-simplified favicon mark for Trace Vesicle presets (`TV-1-fav.svg`). For each typemark preset render a full-width typemark SVG (`TYPE-1.svg`). Ensure every emitted SVG uses currentColor or a brand hex (no hardcoded backgrounds — grounds belong to the gallery page). Mark exits 0 and reports a count of files written. Coordinates rounded <=2 decimals (delegated to the libraries). Do NOT write into `brandbook/` root.
  </action>
  <verify>
    <automated>cd brandbook/tools && node generate.mjs && test "$(ls candidates/*.svg 2>/dev/null | wc -l | tr -d ' ')" -ge 14 && test "$(ls candidates/*-mark.svg 2>/dev/null | wc -l | tr -d ' ')" -ge 6 && grep -L 'fill-rule="evenodd"' candidates/*-mark.svg | wc -l | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node brandbook/tools/generate.mjs` exits 0
    - `ls brandbook/tools/candidates/*.svg | wc -l` returns >=14 (6 marks + 6 lockups + 2 typemarks at minimum; mono/fav add more)
    - `grep -c '<rect' brandbook/tools/candidates/*.svg` returns 0 matches across all files (LOGO-01)
    - `grep -L 'fill-rule="evenodd"' brandbook/tools/candidates/*-mark.svg` returns empty (every mark uses evenodd — LOGO-02)
    - `grep -c 'stroke' brandbook/tools/candidates/*-mark.svg` returns 0 (no strokes)
    - Each `*-mark.svg` contains exactly one `<path` (per-file: `grep -c '<path' file` == 1)
    - No candidate lockup contains subtitle/tagline text (`grep -ci 'AI ops\|Phoenix' brandbook/tools/candidates/*-lockup.svg` returns 0 — LOGO-04)
    - No files written under `brandbook/` root: `ls brandbook/*.svg 2>/dev/null | wc -l` returns 0
  </acceptance_criteria>
  <done>generate.mjs renders >=14 candidate SVGs into candidates/, all structurally compliant (no rect, evenodd, no stroke, single path per mark, no subtitle).</done>
</task>

<task type="auto">
  <name>Task 3: Build options-gallery.html with all evidence rows</name>
  <files>brandbook/tools/gallery.mjs, brandbook/tools/options-gallery.html</files>
  <read_first>
    - .planning/phases/19-logo-divergence-user-choice/19-CONTEXT.md (§Gallery — standalone file://, inline SVG no network, dark+light grounds, per-option 256/64/32/16px + monochrome row + favicon strip + 24px sidebar mock + README band mock, ranked recommendation block, stable IDs; typemarks at full width + small)
    - brandbook/pressure-test.md §SECTION 8 RULE LOGO-06/07 (16px + monochrome are pass/fail evidence) + direction ranking (the recommendation rationale text)
    - the generated SVGs in brandbook/tools/candidates/ (inputs to inline)
  </read_first>
  <action>
    Implement `gallery.mjs` to read every candidate SVG from candidates/ and emit a single self-contained `brandbook/tools/options-gallery.html`. Inline all SVG markup (no `<img src>`, no http/https references, no external fonts/CSS). Page structure: two ground sections (Basalt-950 #11100F dark, Ash-50 #FAF5EF light); per mark option a card with its stable ID + design-intent caption showing lockup + mark at 256/64/32/16px, a monochrome row (currentColor inheriting a flipped text color on each ground), a simulated browser-tab favicon strip, a 24px dashboard-sidebar mock row, and a README header band mock. Typemark studies shown full width + at small sizes. A ranked recommendation block at top (Trace Vesicle #1, Cinder #2 fallback, Aperture study, with the why per the §8 ranking) and a note documenting the "none of these → second round" escape. Keep styling utilitarian using inline `<style>` with brand hexes. Run gallery.mjs as the final step (or have generate.mjs call it). Do NOT reference any network resource.
  </action>
  <verify>
    <automated>cd brandbook/tools && node gallery.mjs && test -f options-gallery.html && ! grep -qE 'https?://[^"]*\.(css|js|woff|png|jpg|svg)' options-gallery.html && grep -q '256' options-gallery.html && grep -qi 'recommend' options-gallery.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `brandbook/tools/options-gallery.html` exists and is >=100 lines
    - No external network references: `grep -cE 'https?://[^"]*\.(css|js|woff|woff2|png|jpg|jpeg|svg)' brandbook/tools/options-gallery.html` returns 0, and `grep -c '<img' brandbook/tools/options-gallery.html` returns 0 (SVG is inlined)
    - All stable option IDs appear in the HTML: `grep -o 'TV-1\|TV-2\|CM-1\|CM-2\|AP-1\|TYPE-1\|TYPE-2' brandbook/tools/options-gallery.html | sort -u | wc -l` returns >=7
    - Size evidence present: HTML references widths/sizes 256, 64, 32, and 16 (grep each returns >=1)
    - Mock rows present: `grep -ci 'favicon\|sidebar\|readme\|recommend\|monochrome' brandbook/tools/options-gallery.html` returns >=5
    - Both grounds present: HTML contains both `#11100F` and `#FAF5EF`
    - VISUAL QUALITY (16px legibility, true craft divergence between options, gallery aesthetics) is NOT auto-verifiable and is DEFERRED to the human gate #2 in 19-03 — do not assert it here.
  </acceptance_criteria>
  <done>options-gallery.html is a standalone file:// page inlining all candidates with every required evidence row (size ramp, monochrome, favicon strip, sidebar mock, README band, ranked recommendation, escape note) and stable IDs — ready for 19-03 verification + the human gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated SVG -> file:// HTML | inline SVG rendered by the user's browser at gate #2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Information Disclosure | options-gallery.html network calls | mitigate | acceptance asserts zero external http(s) references and zero <img>; fully self-contained file:// page |
| T-19-04 | Tampering | candidate SVGs with hidden rect/white shapes | mitigate | per-file grep gates (zero <rect>, evenodd present, single path) enforce LOGO-01/02 by construction |
</threat_model>

<verification>
- `node brandbook/tools/generate.mjs` exits 0; `node brandbook/tools/gallery.mjs` exits 0
- `grep -c '<rect' brandbook/tools/candidates/*.svg` returns 0
- `ls brandbook/*.svg 2>/dev/null | wc -l` returns 0 (no Phase 20 root leak)
- `ls brandbook/tools/favicon.svg brandbook/tools/social-card.svg 2>/dev/null | wc -l` returns 0 (no Phase 20/22 scope leak)
- options-gallery.html has zero external network references
</verification>

<success_criteria>
- >=6 distinct hand-tuned mark presets (each with lockup) + >=2 integrated typemark studies, each with documented design intent
- All candidate SVGs satisfy LOGO-01 (no rect), LOGO-02 (evenodd single path), LOGO-04 (no subtitle)
- options-gallery.html is standalone, dual-ground, with full size ramp + monochrome + favicon/sidebar/README mocks + ranked recommendation + stable IDs
- Genuine divergence: ≥2 directions beyond Trace Vesicle present
</success_criteria>

<output>
Create `.planning/phases/19-logo-divergence-user-choice/19-02-SUMMARY.md` when done
</output>
