---
phase: 20-logo-convergence-variant-set
plan: "02"
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - brandbook/tools/final-variants.html
  - brandbook/tools/final-strip.mjs
autonomous: false
requirements: [BRAND-04]

must_haves:
  truths:
    - "A small standalone final-variants.html renders all 8 root variants on both dark (#11100F) and light (#FAF5EF) grounds at realistic sizes"
    - "final-variants.html is fully standalone (zero external network refs, zero <img>) and small"
    - "node brandbook/tools/verify-logos.mjs exits 0 against the committed root set"
    - "A confirm-checkpoint summary is prepared for the orchestrator to present a single ship-it/adjust question"
    - "No Phase 21/22 scope leaked (no tokens, brand-book.md, examples, README/dashboard edits)"
  artifacts:
    - path: "brandbook/tools/final-variants.html"
      provides: "Lightweight confirm strip — 8 variants on both grounds at realistic sizes"
      contains: "#11100F"
    - path: "brandbook/tools/final-strip.mjs"
      provides: "Generator that inlines the 8 root SVGs into the standalone strip"
      min_lines: 20
  key_links:
    - from: "brandbook/tools/final-strip.mjs"
      to: "brandbook/*.svg"
      via: "reads + inlines the 8 root SVGs"
      pattern: "logo-primary|readFileSync"
    - from: "brandbook/tools/final-variants.html"
      to: "the 8 root variants"
      via: "inline SVG on both grounds"
      pattern: "logo-primary|<svg"
---

<objective>
Build the lightweight confirm strip and run final verification so the orchestrator can present a single "ship it / adjust" checkpoint. This is NOT another multi-option gallery — it is a small standalone `final-variants.html` showing the 8 committed root variants on both grounds at realistic sizes, plus a confirm-checkpoint summary.

Purpose: BRAND-04's final success criterion — a lightweight confirm checkpoint reviewed and accepted before Phase 21/22 integration proceeds. The decision belongs to the user; this plan prepares the visual + verification evidence and stops at the checkpoint.

Output: `final-variants.html` (small, standalone), `final-strip.mjs` (its generator), a clean verifier run, and a confirm-checkpoint summary for orchestrator presentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-logo-convergence-variant-set/20-CONTEXT.md
@.planning/phases/20-logo-convergence-variant-set/20-01-PLAN.md

# The 8 committed root variants from 20-01 (inline these):
@brandbook/logo-primary.svg
@brandbook/logo-primary-light.svg
@brandbook/logo-mark.svg
@brandbook/favicon.svg

# Standalone-HTML inlining pattern + grounds + verifier:
@brandbook/tools/verify-logos.mjs

<interfaces>
<!-- Contracts the executor uses directly. -->
The 8 root SVGs live at brandbook/ root with census filenames (from 20-01):
  logo-primary.svg, logo-primary-light.svg, logo-mark.svg, logo-monochrome.svg,
  logo-lockup-subtitle.svg, logotype-integrated.svg, favicon.svg, social-card.svg

Grounds (must match the verifier's GALLERY-GROUNDS convention used in Phase 19):
  dark  = #11100F   light = #FAF5EF

Standalone rule (verifier enforced): zero external http(s) refs (xmlns excepted),
zero <img> tags — inline the SVG markup directly via readFileSync.

verify-logos.mjs exits 0 when the committed root set passes all ROOT-* checks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: final-strip.mjs → standalone final-variants.html confirm strip</name>
  <files>brandbook/tools/final-strip.mjs, brandbook/tools/final-variants.html</files>
  <read_first>brandbook/logo-primary.svg + the 8 root SVGs, brandbook/tools/verify-logos.mjs (GALLERY-STANDALONE / GALLERY-GROUNDS conventions: #11100F dark, #FAF5EF light, no external refs, no <img>), .planning/phases/20-logo-convergence-variant-set/20-CONTEXT.md §Confirm checkpoint</read_first>
  <action>
Create `final-strip.mjs` that reads the 8 root SVGs from brandbook/ root via readFileSync and inlines them (no <img>, no external refs) into a SMALL standalone `final-variants.html`. This is a confirm strip, not a gallery: one compact page, each of the 8 variants shown at a realistic display size, on BOTH a dark band (#11100F) and a light band (#FAF5EF). Recommended layout: two columns/sections (dark ground / light ground); within each, the lockup variants at ~240px wide, the mark at ~48px, favicon at 16px AND 32px, social-card scaled to fit. Label each tile with its filename. Add one short heading line naming this the "Phase 20 final variant confirm strip — ship it or adjust?".

Keep it small (target well under the old gallery sizes — these are 8 inline SVGs, not 23+ on a size ramp). No size-ramp grid, no recommendation block, no escape note (those were gate-#2 gallery features — this is a confirm strip). Inline only; the verifier's standalone rule (zero http refs except xmlns, zero <img>) must hold.

Run `node brandbook/tools/final-strip.mjs` and confirm final-variants.html is written and opens.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/scoria && node brandbook/tools/final-strip.mjs && test -f brandbook/tools/final-variants.html && grep -q "#11100F" brandbook/tools/final-variants.html && grep -q "#FAF5EF" brandbook/tools/final-variants.html && test -z "$(grep -oE 'https?://(?!www\.w3\.org)[^\" ]+' brandbook/tools/final-variants.html)" && test -z "$(grep -o '<img' brandbook/tools/final-variants.html)" && echo STRIP_STANDALONE_OK</automated>
  </verify>
  <done>final-variants.html exists, is standalone (no external http refs, no <img>), shows all 8 root variants on both #11100F and #FAF5EF grounds at realistic sizes, and is small.</done>
</task>

<task type="auto">
  <name>Task 2: Full verification + confirm-checkpoint summary preparation</name>
  <files>brandbook/tools/final-variants.html</files>
  <read_first>brandbook/tools/verify-logos.mjs, brandbook/tools/variant-spec.md (from 20-01), .planning/phases/20-logo-convergence-variant-set/20-CONTEXT.md §Confirm checkpoint, .planning/REQUIREMENTS.md (BRAND-04 acceptance)</read_first>
  <action>
Run the full structural verification and assemble the evidence the orchestrator needs to present the confirm checkpoint. No new artwork — this is the verify + package step.

Run and capture for the SUMMARY:
- `node brandbook/tools/verify-logos.mjs` → must exit 0 (root set passes).
- `ls brandbook/*.svg` → exactly the 8 census filenames.
- `du -k brandbook/favicon.svg` → ≤1KB.
- `du -sk brandbook/` minus node_modules → well under 500KB.
- `ls brandbook/tools/candidates/` → only TV-1-mark/mono/fav/lockup + LK-B-lockup.
- Confirm zero Phase 21/22 leak: no tokens.json/tokens.css, no brand-book.md, no examples/, no edits to README/lib/scoria_web/layouts.ex.

In the 20-02-SUMMARY.md, write a "Confirm checkpoint — for orchestrator" block that states: what was built (8 root variants + spec), how to view it (open brandbook/tools/final-variants.html in a browser), the single question to ask the user ("Ship this variant set as the committed Scoria logo system, or note adjustments?"), and the verification evidence (verifier exit 0, 8 files, favicon ≤1KB, size under budget, candidates pruned). This block is what the orchestrator presents — do NOT block on the user here; the executor finishes and the orchestrator owns the checkpoint:confirm presentation.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/scoria && node brandbook/tools/verify-logos.mjs && test "$(ls brandbook/*.svg | wc -l | tr -d ' ')" = "8" && test ! -f brandbook/tokens.json && test ! -f brandbook/brand-book.md && test ! -d brandbook/examples && echo NO_PHASE21_LEAK</automated>
  </verify>
  <done>verify-logos.mjs exits 0; exactly 8 root SVGs present; no Phase 21/22 artifacts created; the SUMMARY contains a confirm-checkpoint block with view instructions, the single ship-it/adjust question, and verification evidence for the orchestrator to present.</done>
</task>

<task type="checkpoint:confirm" gate="blocking">
  <name>Task 3: Confirm checkpoint — orchestrator presents ship-it / adjust</name>
  <what-built>The complete 8-variant Scoria logo system at brandbook/ root (logo-primary, logo-primary-light, logo-mark, logo-monochrome, logo-lockup-subtitle, logotype-integrated, favicon, social-card), an optical-correction pass documented in variant-spec.md, and a lightweight standalone confirm strip.</what-built>
  <how-to-verify>
    1. Open `brandbook/tools/final-variants.html` in a browser — review all 8 variants on the dark (#11100F) and light (#FAF5EF) grounds at realistic sizes.
    2. Spot-check the favicon at 16px/32px and the mark-as-'o' fusion in logo-primary (sits in the x-height band, holes read as the counter, two-tone Ember 'o' if shipped two-tone).
    3. Read `brandbook/tools/variant-spec.md` for the clear-space / min-size rules and the optical-pass rationale.
    4. Verifier evidence: `node brandbook/tools/verify-logos.mjs` exits 0; 8 census files present; favicon ≤1KB; brandbook/ under 500KB; losing candidates + galleries pruned.
  </how-to-verify>
  <resume-signal>Type "ship it" to accept the variant set and proceed to Phase 21, or describe specific adjustments (colorway, optical tuning, favicon snap) to revise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| final-variants.html → reviewer browser | Standalone confirm page; inline SVG only, opened from file:// |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Tampering | final-variants.html external refs (would break offline-safe review + Phase 22 gate) | mitigate | final-strip.mjs inlines SVGs via readFileSync; verify step asserts zero http refs (xmlns excepted) and zero <img> |
| T-20-05 | Elevation of privilege | scope creep into Phase 21/22 (tokens, brand-book.md, dashboard edits) | mitigate | Task 2 asserts no tokens/brand-book/examples/layouts edits; checkpoint scopes the question to ship-it/adjust on the 8 variants only |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package installs — reuses Phase 19 toolchain; no new dependencies, legitimacy gate N/A |
</threat_model>

<verification>
- `node brandbook/tools/final-strip.mjs` writes final-variants.html.
- `node brandbook/tools/verify-logos.mjs` exits 0.
- final-variants.html is standalone (no http refs, no <img>), both grounds present.
- Exactly 8 root SVGs; no Phase 21/22 artifacts.
- SUMMARY contains the confirm-checkpoint block for the orchestrator.
</verification>

<success_criteria>
- Lightweight standalone confirm strip renders all 8 variants on both grounds.
- Full verification passes (verifier exit 0, 8 files, favicon ≤1KB, under budget, pruned).
- No Phase 21/22 scope leak.
- Confirm-checkpoint summary prepared; checkpoint:confirm presented by the orchestrator as a single ship-it/adjust question.
- BRAND-04 fully satisfied on user confirmation.
</success_criteria>

<output>
Create `.planning/phases/20-logo-convergence-variant-set/20-02-SUMMARY.md` when done.
</output>
