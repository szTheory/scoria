---
phase: 43-knowledge-tenant-isolation
plan: 02
type: execute
wave: 2
depends_on:
  - 43-01
files_modified:
  - priv/repo/knowledge_migrations/20260705010000_add_knowledge_tenant_scope.exs
  - lib/scoria/knowledge/source.ex
  - lib/scoria/knowledge/chunk.ex
  - lib/scoria/knowledge/retrieval_run.ex
  - lib/scoria/knowledge/retrieval_result.ex
  - lib/scoria/knowledge/citation.ex
  - test/scoria/knowledge/tenant_isolation_test.exs
autonomous: true
requirements:
  - KNOW-01
  - KNOW-02
  - KNOW-03
user_setup: []
must_haves:
  truths:
    - "Knowledge source and chunk rows have first-class tenant, actor, and scope_kind fields."
    - "Retrieval run, result, and citation rows have first-class tenant/actor audit evidence fields."
    - "The additive migration lives only under `priv/repo/knowledge_migrations/` and is applied by the knowledge migration path."
    - "Legacy null-tenant rows remain possible for upgrade compatibility but are not treated as valid new writes."
  artifacts:
    - path: "priv/repo/knowledge_migrations/20260705010000_add_knowledge_tenant_scope.exs"
      provides: "Additive tenant-scope migration for optional knowledge lane"
    - path: "lib/scoria/knowledge/source.ex"
      provides: "Source schema tenant fields and new-write validation"
    - path: "lib/scoria/knowledge/chunk.ex"
      provides: "Chunk schema tenant fields and new-write validation"
    - path: "lib/scoria/knowledge/retrieval_run.ex"
      provides: "Retrieval run audit tenant fields"
    - path: "lib/scoria/knowledge/retrieval_result.ex"
      provides: "Retrieval result audit tenant fields"
    - path: "lib/scoria/knowledge/citation.ex"
      provides: "Citation tenant, actor, and scope_kind fields"
  key_links:
    - from: "priv/repo/knowledge_migrations/20260705010000_add_knowledge_tenant_scope.exs"
      to: "lib/scoria/test_support/migrations.ex"
      via: "existing KnowledgeMigrationRepo runner discovers files under priv/repo/knowledge_migrations"
      pattern: "knowledge_migrations"
    - from: "lib/scoria/knowledge/source.ex"
      to: "lib/scoria/knowledge/chunk.ex"
      via: "source/chunk schemas share tenant_id, actor_id, and scope_kind fields"
      pattern: "tenant_id.*scope_kind"
---

<objective>
Add tenant-scope storage and schema proof for the optional knowledge lane.

Purpose: KNOW-01 and KNOW-02 require first-class tenant/actor evidence before API and backend enforcement can be complete. D-02 requires an additive knowledge migration under the separate knowledge migration path, not a mutation of the historical create migration or the main no-op compatibility shell.
Output: A new knowledge migration, schema fields and changeset validation for new writes, and migration/schema/index assertions in the tenant isolation test file.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/43-knowledge-tenant-isolation/43-CONTEXT.md
@.planning/phases/43-knowledge-tenant-isolation/43-RESEARCH.md
@.planning/phases/43-knowledge-tenant-isolation/43-PATTERNS.md
@.planning/phases/43-knowledge-tenant-isolation/43-VALIDATION.md
@.planning/phases/43-knowledge-tenant-isolation/43-01-SUMMARY.md
@priv/repo/knowledge_migrations/20260511000300_create_knowledge_tables.exs
@priv/repo/migrations/20260525070000_create_semantic_cache_tables.exs
@lib/scoria/test_support/migrations.ex
@lib/scoria/semantic_cache/entry.ex
@lib/scoria/knowledge/source.ex
@lib/scoria/knowledge/chunk.ex
@lib/scoria/knowledge/retrieval_run.ex
@lib/scoria/knowledge/retrieval_result.ex
@lib/scoria/knowledge/citation.ex
</context>

## Dependency Graph

| Plan | Needs | Creates | Wave |
|------|-------|---------|------|
| 43-01 | Existing SemanticCache scope precedent and knowledge lane contract | `Scoria.Knowledge.Scope`, tenant isolation test spine | 1 |
| 43-02 | 43-01 scope helper | Knowledge tenant migration and schema fields | 2 |
| 43-03 | 43-02 schema fields | Public Knowledge API write/list/run/result enforcement | 3 |
| 43-04 | 43-03 public retrieve/run shape | Pgvector and Scrypath tenant-qualified retrieval | 4 |
| 43-05 | 43-04 retrieval enforcement | Citation/grounding scope enforcement, docs, final proof | 5 |

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | - | Tenant-isolated retrieval end to end with nil tenant raising | 43-01,43-02,43-03,43-04,43-05 | COVERED | Storage evidence lands here. |
| REQ | KNOW-01 | Tenant columns and tenant indexes via knowledge migration path | 43-02 | COVERED | This plan owns the migration and source/chunk schema fields. |
| REQ | KNOW-02 | Retrieval runs/results/citations carry tenant/actor audit evidence | 43-02,43-03,43-05 | COVERED | This plan adds the fields; later plans populate and validate them. |
| REQ | KNOW-03 | Mandatory fail-closed tenant filter across public APIs and leaves | 43-01,43-03,43-04,43-05 | COVERED | This plan makes schema validation fail closed for new rows. |
| REQ | KNOW-04 | Cross-tenant isolation test proves tenant A cannot retrieve tenant B | 43-01,43-04 | COVERED | Retrieval behavior lands later. |
| RESEARCH | R-01 | Explicit scope normalization before side effects | 43-01 | COVERED | Scope helper already created. |
| RESEARCH | R-02 | Tenant predicate before optional source filters | 43-04 | COVERED | Later backend plan. |
| RESEARCH | R-03 | Validate backend results before insert | 43-03 | COVERED | Later public API plan. |
| RESEARCH | R-04 | Additive migration with legacy null-tenant quarantine | 43-02,43-03,43-04,43-05 | COVERED | Nullable migration plus new-write validation starts here. |
| CONTEXT | D-01 | Explicit `scope:` plus shorthand normalization | 43-01 | COVERED | Already implemented by dependency. |
| CONTEXT | D-02 | Additive knowledge migration and quarantined legacy rows | 43-02 | COVERED | This plan implements D-02. |
| CONTEXT | D-03 | Public boundary plus backend/result/citation leaves | 43-03,43-04,43-05 | COVERED | Uses fields created here. |
| CONTEXT | D-04 | Duplicate audit evidence onto run/result/citation rows | 43-02,43-03,43-05 | COVERED | This plan adds audit fields. |
| CONTEXT | D-05 | Focused tests and optional knowledge docs | 43-01,43-05 | COVERED | Migration tests extend the tenant isolation file. |

## Artifacts This Phase Produces

| Artifact | Symbol / Path | Produced By |
|----------|----------------|-------------|
| Module | `Scoria.Knowledge.Scope` | 43-01 |
| Function | `Scoria.Knowledge.Scope.new!/1` | 43-01 |
| Function | `Scoria.Knowledge.Scope.from_opts!/1` | 43-01 |
| Function | `Scoria.Knowledge.Scope.for_write!/1` | 43-01 |
| Function | `Scoria.Knowledge.Scope.put_source_attrs/2` | 43-01 |
| Function | `Scoria.Knowledge.Scope.put_audit_attrs/2` | 43-01 |
| Function | `Scoria.Knowledge.Scope.visible_to/2` | 43-01 |
| Migration | `priv/repo/knowledge_migrations/20260705010000_add_knowledge_tenant_scope.exs` | 43-02 |
| Schema fields | `tenant_id`, `actor_id`, `scope_kind` on `ai_knowledge_sources` and `ai_knowledge_chunks` | 43-02 |
| Schema fields | `tenant_id`, `actor_id` on `ai_retrieval_runs` and `ai_retrieval_results` | 43-02 |
| Schema fields | `tenant_id`, `actor_id`, `scope_kind` on `ai_knowledge_citations` | 43-02 |
| Index | `ai_knowledge_sources_tenant_id_index` | 43-02 |
| Index | `ai_knowledge_sources_tenant_entity_version_index` | 43-02 |
| Index | `ai_knowledge_chunks_tenant_id_index` | 43-02 |
| Index | `ai_knowledge_chunks_tenant_source_id_index` | 43-02 |
| Index | `ai_retrieval_runs_tenant_status_inserted_at_index` | 43-02 |
| Index | `ai_retrieval_results_tenant_run_rank_index` | 43-02 |
| Index | `ai_knowledge_citations_tenant_source_id_index` | 43-02 |
| Index | `ai_knowledge_citations_tenant_chunk_id_index` | 43-02 |
| API behavior | `Scoria.Knowledge.create_source/2`, `ingest_source/2`, `reembed_source/2`, `reindex_source/2`, `list_source_chunks/2`, `create_retrieval_run/1`, `append_retrieval_results/2` require tenant scope | 43-03 |
| Backend behavior | `Scoria.Knowledge.retrieve/2`, `Scoria.Knowledge.Backends.Pgvector.similar_chunks/2`, `Scoria.Knowledge.Retrievers.Scrypath.retrieve/2`, `normalize_results/2` require tenant scope | 43-04 |
| Citation behavior | `Scoria.Knowledge.create_citation/1`, `Scoria.Knowledge.CitationFormatter.validate_anchor/2`, `Scoria.Knowledge.Grounding.score_citation_validity/2` validate anchors under tenant scope | 43-05 |
| Test file | `test/scoria/knowledge/tenant_isolation_test.exs` | 43-01 through 43-05 |
| Contract update | `test/scoria/knowledge_lane_contract_test.exs` expected file list includes tenant isolation proof | 43-01 |
| Docs section | README optional knowledge lane tenant-scope sentence | 43-05 |
| Docs section | `docs/adoption_lanes.md` optional knowledge identity and metadata-filter warning | 43-05 |
| Docs section | `docs/operator_verification.md` `mix test.knowledge` tenant-isolation proof wording | 43-05 |
| CLI/doc reference | `mix test.knowledge --warnings-as-errors` proves missing-tenant raise, cross-tenant exclusion, actor narrowing, citation evidence | 43-05 |
| CLI/doc reference | `KnowledgeMigrationRepo`, `schema_migrations_knowledge`, and `priv/repo/knowledge_migrations/` documented as the production knowledge migration path | 43-05 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the knowledge tenant-scope migration and index proof</name>
  <files>priv/repo/knowledge_migrations/20260705010000_add_knowledge_tenant_scope.exs, test/scoria/knowledge/tenant_isolation_test.exs</files>
  <read_first>
    - `priv/repo/knowledge_migrations/20260511000300_create_knowledge_tables.exs`
    - `priv/repo/migrations/20260525070000_create_semantic_cache_tables.exs`
    - `lib/scoria/test_support/migrations.ex`
    - `test/scoria/knowledge/tenant_isolation_test.exs`
    - `.planning/phases/43-knowledge-tenant-isolation/43-CONTEXT.md`
    - `.planning/phases/43-knowledge-tenant-isolation/43-PATTERNS.md`
  </read_first>
  <behavior>
    - `ai_knowledge_sources` gains nullable `tenant_id`, `actor_id`, and `scope_kind` columns for upgrade compatibility per D-02.
    - `ai_knowledge_chunks` gains nullable `tenant_id`, `actor_id`, and `scope_kind` columns.
    - `ai_retrieval_runs` and `ai_retrieval_results` gain nullable `tenant_id` and `actor_id` columns.
    - `ai_knowledge_citations` gains nullable `tenant_id`, `actor_id`, and `scope_kind` columns.
    - The migration creates tenant indexes named in this plan and does not edit `20260511000300_create_knowledge_tables.exs`.
    - The historical global `ai_knowledge_sources` entity/version uniqueness is replaced only if necessary to permit the same logical source version under different non-null tenants; rollback recreates the historical index name.
  </behavior>
  <action>Add `priv/repo/knowledge_migrations/20260705010000_add_knowledge_tenant_scope.exs` under the real knowledge migration directory per D-02. Implement reversible `up/0` and `down/0` callbacks with `alter table` additions for the fields listed in the behavior block. Create indexes `ai_knowledge_sources_tenant_id_index`, `ai_knowledge_sources_tenant_entity_version_index`, `ai_knowledge_chunks_tenant_id_index`, `ai_knowledge_chunks_tenant_source_id_index`, `ai_retrieval_runs_tenant_status_inserted_at_index`, `ai_retrieval_results_tenant_run_rank_index`, `ai_knowledge_citations_tenant_source_id_index`, and `ai_knowledge_citations_tenant_chunk_id_index`. If the existing global source uniqueness blocks same entity/version across tenants, replace it in this migration with the partial unique tenant index where `tenant_id IS NOT NULL` and make `down/0` restore the previous global index. Extend `tenant_isolation_test.exs` with migration/schema/index assertions that query `information_schema.columns`, `pg_indexes`, and `schema_migrations_knowledge` through the existing test repo after the knowledge lane migrator runs.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/knowledge/tenant_isolation_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the new migration file is under `priv/repo/knowledge_migrations/` and no historical knowledge create migration or main compatibility shell is modified.
    - Source assertion: the migration contains all table/column/index names listed in the behavior block.
    - Behavior assertion: tenant isolation tests prove the columns and indexes exist after the existing knowledge migration runner applies the lane.
    - Test command passes: `MIX_ENV=test mix test test/scoria/knowledge/tenant_isolation_test.exs --warnings-as-errors`.
  </acceptance_criteria>
  <done>The optional knowledge lane has an additive tenant-scope migration and executable proof that the knowledge migration path applies it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tenant fields to knowledge schemas and new-write validation</name>
  <files>lib/scoria/knowledge/source.ex, lib/scoria/knowledge/chunk.ex, lib/scoria/knowledge/retrieval_run.ex, lib/scoria/knowledge/retrieval_result.ex, lib/scoria/knowledge/citation.ex, test/scoria/knowledge/tenant_isolation_test.exs</files>
  <read_first>
    - `lib/scoria/semantic_cache/entry.ex`
    - `lib/scoria/knowledge/source.ex`
    - `lib/scoria/knowledge/chunk.ex`
    - `lib/scoria/knowledge/retrieval_run.ex`
    - `lib/scoria/knowledge/retrieval_result.ex`
    - `lib/scoria/knowledge/citation.ex`
    - `test/scoria/knowledge/tenant_isolation_test.exs`
    - `.planning/phases/43-knowledge-tenant-isolation/43-PATTERNS.md`
  </read_first>
  <behavior>
    - `Source.changeset/2`, `Chunk.changeset/2`, and `Citation.changeset/2` cast and require `tenant_id` plus `scope_kind`, validate `scope_kind` in `~w(tenant_shared actor_scoped)`, and cast nullable `actor_id`.
    - `RetrievalRun.changeset/2` and `RetrievalResult.changeset/2` cast and require `tenant_id`, and cast nullable `actor_id`.
    - New writes fail closed at changeset level even though migration columns remain nullable for legacy adopter rows per D-02.
    - Schema structs expose the new fields so later plans can populate first-class audit evidence per D-04.
  </behavior>
  <action>Update the five knowledge schema modules with the exact fields and changeset validations from the behavior block per D-02 and D-04. Use the `SemanticCache.Entry` scope-field pattern for `@scope_kinds`, `cast`, `validate_required`, and `validate_inclusion`. Keep actor requirement for actor-scoped writes in `Scoria.Knowledge.Scope.for_write!/1`; schema changesets should require `tenant_id` and `scope_kind` where those columns exist and not invent in-lib authorization. Extend `tenant_isolation_test.exs` with changeset assertions for missing tenant, valid tenant-shared rows, valid actor-scoped rows with actor, and invalid scope_kind on source/chunk/citation schemas.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/scoria/knowledge/tenant_isolation_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `Source`, `Chunk`, and `Citation` schemas each define `field :tenant_id`, `field :actor_id`, and `field :scope_kind`.
    - Source assertion: `RetrievalRun` and `RetrievalResult` schemas each define `field :tenant_id` and `field :actor_id`.
    - Behavior assertion: direct changeset tests reject missing tenant for every tenant-owned schema changed in this task.
    - Behavior assertion: migration remains nullable while changesets enforce new-write tenant requirements.
    - Test command passes: `MIX_ENV=test mix test test/scoria/knowledge/tenant_isolation_test.exs --warnings-as-errors`.
  </acceptance_criteria>
  <done>Knowledge schemas can persist tenant/actor/scope evidence and reject new unscoped writes while still allowing the migration to upgrade legacy rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration to adopter database | Existing optional knowledge installations receive nullable upgrade columns without synthetic backfill. |
| Schema changesets to Repo inserts | Tenant-owned rows become invalid when new writes omit tenant scope. |
| Audit row schemas to operator evidence | Runs, results, and citations gain first-class tenant/actor fields instead of relying on metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-02-I | Information Disclosure | `ai_knowledge_sources` / `ai_knowledge_chunks` | mitigate | Add tenant/scope fields and indexes so every later read path can filter before returning raw chunk bodies. |
| T-43-02-R | Repudiation | `ai_retrieval_runs`, `ai_retrieval_results`, `ai_knowledge_citations` | mitigate | Add tenant/actor audit columns per D-04 so retrieved-under scope is visible without replaying joins. |
| T-43-02-T | Tampering | Legacy null-tenant rows | mitigate | Keep nullable upgrade columns but enforce tenant-required new writes in schemas; later read paths quarantine null tenant rows. |
| T-43-02-D | Denial of Service | Source uniqueness migration | mitigate | Use reversible index changes and named indexes; do not rewrite knowledge table data or force reingest. |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install is planned; RESEARCH.md Package Legitimacy Audit lists no new packages. |
</threat_model>

<verification>
Plan-level verification:
- `MIX_ENV=test mix test test/scoria/knowledge/tenant_isolation_test.exs --warnings-as-errors`
- `MIX_ENV=test mix test.knowledge --warnings-as-errors` if local feedback stays under the expected knowledge-lane runtime.
- Confirm the new migration path is `priv/repo/knowledge_migrations/` and not `priv/repo/migrations/`.
</verification>

<success_criteria>
- New knowledge migration adds required tenant/actor/scope columns and tenant indexes through the separate knowledge migration path.
- Source/chunk/citation changesets require tenant and scope_kind for new writes.
- Retrieval run/result changesets require tenant for new writes.
- Tests prove migration/index/schema presence and new-write validation.
</success_criteria>

<output>
Create `.planning/phases/43-knowledge-tenant-isolation/43-02-SUMMARY.md` when done.
</output>
