---
phase: 42-eval-fails-closed
plan: 07
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - lib/scoria/runtime/release_gate.ex
  - config/config.exs
  - priv/repo/migrations/*_add_eval_runs_verdict_index.exs
  - test/scoria/runtime/release_gate_test.exs
autonomous: true
requirements: [EVAL-04]
must_haves:
  truths:
    - "ReleaseGate.check/1 consults the latest completed EvalRun verdict for the prompt_template_id: only threshold_verdict \"passed\" passes; any other completed verdict → {:error, {:eval_not_passing, verdict}}."
    - "The draft check stays first and unchanged ({:error, :unapproved_draft})."
    - "No completed verdict → :ok by default AND emits [:scoria, :release_gate, :ungated] telemetry; with config :scoria, :require_eval_verdict true → {:error, :eval_required}."
    - "Online-scoring campaign runs (campaign.metadata[\"source\"] == \"online_scoring\") are excluded from the lookup; legit offline campaign runs still count."
    - "DB errors propagate — the gate never rescues a lookup failure into a fake-allow."
  artifacts:
    - "release_gate.ex verdict consult; composite index ai_eval_runs (prompt_template_id, status, inserted_at DESC); config :scoria, :require_eval_verdict (default false); [:scoria, :release_gate, :ungated] telemetry"
  key_links:
    - "release_gate.ex consumes Scoria.Eval.Verdict.blocks_release?/1 (plan 01) — it does not re-derive the passing set. \"passed\" is the ONLY passing verdict string."
  prohibitions:
    - "Must NOT enumerate a block-list — use the allowlist via Verdict.blocks_release?/1 (only \"passed\" passes)."
    - "Must NOT add prompt_version to the WHERE (runtime String vs EvalRun integer type mismatch)."
    - "Must NOT rescue a DB/lookup error into :ok — errors propagate (no fail-closed self-outage bypass)."
    - "Must NOT exclude online runs by runner_mode (there is no :online runner_mode) — exclude by campaign.metadata source."
---

<objective>
Make `Runtime.ReleaseGate` consult the eval verdict, not only the draft flag (D-05). Add an allowlist verdict check (only `"passed"` passes), an opt-in strict mode, `:ungated` telemetry, and the composite index the lookup needs.

Purpose: EVAL-04 — a release must be blocked when the eval verdict is not passing. The gate uses `Verdict.blocks_release?/1` as the single passing authority, defaults to open when no eval governs a prompt (least blast radius, but emits telemetry so it is never silently green), and hardens against the fail-closed self-outage footgun (DB errors propagate, never rescue-to-allow).
Output: `release_gate.ex` verdict consult + migration + config + telemetry, proven deterministically.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-eval-fails-closed/42-CONTEXT.md
@.planning/phases/42-eval-fails-closed/42-PATTERNS.md
@lib/scoria/eval/verdict.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReleaseGate verdict consult (allowlist + ungated telemetry + strict opt-in)</name>
  <files>lib/scoria/runtime/release_gate.ex, config/config.exs</files>
  <read_first>
    - lib/scoria/runtime/release_gate.ex:15-16 (draft clause stays first; %PromptTemplate{} clause is where the consult inserts), :29-33 (UUID.cast + Repo.get pattern to reuse)
    - lib/scoria/semantic_cache/lookup.ex:63-92 (latest-row join: where + order_by desc + limit + fetch! fail-closed keys — the query shape to mirror)
    - lib/scoria/eval/eval_run.ex:12 (prompt_template_id :binary_id), :28 (threshold_verdict), :33 (belongs_to :campaign)
    - lib/scoria/eval/verdict.ex (blocks_release?/1 — plan 01)
    - lib/scoria/eval/online_scoring.ex:119 (campaign metadata "source" => "online_scoring")
  </read_first>
  <action>
    Insert the verdict consult into the `check(%PromptTemplate{})` clause (release_gate.ex:16), keeping the `check(%PromptTemplate{status: "draft"})` clause first and unchanged. Look up the latest `status == "completed"` EvalRun WHERE `prompt_template_id == template.id` (that row-id space matches how the gate resolves prompt_ref via Repo.get(PromptTemplate, uuid)), joined to its campaign, EXCLUDING runs whose `campaign.metadata["source"] == "online_scoring"` (online runs carry a runner_mode of :offline_replay/:live_judge, so a runner_mode filter cannot remove them — use the metadata source; legit offline campaign evals must still count), ordered `inserted_at DESC`, limit 1. Do NOT add prompt_version to the WHERE (runtime String vs EvalRun :integer type mismatch). If a completed verdict exists: pass it to `Verdict.blocks_release?/1` — false → :ok; true → `{:error, {:eval_not_passing, verdict}}` (tagged tuple carrying the specific verdict string). If NO completed verdict: read `Application.get_env(:scoria, :require_eval_verdict, false)` — false → emit `[:scoria, :release_gate, :ungated]` telemetry (with prompt_template_id metadata) then `:ok`; true → `{:error, :eval_required}`. DB/query errors MUST propagate — do NOT wrap the lookup in a rescue that returns :ok (no fail-closed self-outage bypass). Add a documented `config :scoria, require_eval_verdict: false` line to config/config.exs so the toggle is discoverable.
  </action>
  <verify>
    <automated>mix test test/scoria/runtime/release_gate_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Draft clause still returns {:error, :unapproved_draft} first.
    - A latest completed "passed" verdict → :ok; a "failed"/"inconclusive" → {:error, {:eval_not_passing, verdict}}.
    - No completed verdict → :ok + :ungated telemetry (default); with require_eval_verdict true → {:error, :eval_required}.
    - An online-scoring campaign run does not satisfy the gate (excluded); a legit offline campaign run does.
  </acceptance_criteria>
  <done>check/1 consults the verdict via the allowlist, excludes online runs, and emits :ungated telemetry when no eval governs the prompt.</done>
</task>

<task type="auto">
  <name>Task 2: Composite index migration for the gate lookup</name>
  <files>priv/repo/migrations/*_add_eval_runs_verdict_index.exs</files>
  <read_first>
    - priv/repo/migrations/20260523000300_expand_ai_scores_and_create_online_score_candidates.exs:44-46,96-103 (create_if_not_exists index idiom), :106-129 (down with drop_if_exists)
    - lib/scoria/eval/eval_run.ex (ai_eval_runs table; prompt_template_id, status, inserted_at)
  </read_first>
  <action>
    Generate via the standard path: `mix ecto.gen.migration add_eval_runs_verdict_index` (timestamp must sort after 20260525090000 and after plan 02's capture migration). up/0: `create_if_not_exists index(:ai_eval_runs, [:prompt_template_id, :status, "inserted_at DESC"])` (the DESC ordering supports the latest-completed lookup). down/0: `drop_if_exists` the same index. One indexed query per gated run is acceptable per D-05; do NOT denormalize a verdict column onto PromptTemplate (out of scope).
  </action>
  <verify>
    <automated>mix ecto.migrate && mix ecto.rollback && mix ecto.migrate</automated>
  </verify>
  <acceptance_criteria>
    - The composite index is created on (prompt_template_id, status, inserted_at DESC) and drops cleanly.
    - Migration is idempotent (create_if_not_exists / drop_if_exists).
  </acceptance_criteria>
  <done>The gate's latest-completed lookup is backed by a composite index migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ReleaseGate tests (allowlist, exclusion, telemetry, DB-error propagation)</name>
  <files>test/scoria/runtime/release_gate_test.exs</files>
  <read_first>
    - lib/scoria/runtime/release_gate.ex (post-Task-1 behavior), test/support (case template / DataCase), :telemetry test attach patterns already in the repo if any
  </read_first>
  <behavior>
    - draft template → {:error, :unapproved_draft} (first, unchanged)
    - latest completed EvalRun verdict "passed" for the prompt_template_id → :ok
    - latest completed verdict "inconclusive"/"failed" → {:error, {:eval_not_passing, verdict}}
    - no completed EvalRun → :ok AND a [:scoria, :release_gate, :ungated] event fires (attach a telemetry handler)
    - no completed EvalRun with require_eval_verdict true → {:error, :eval_required}
    - an online-scoring campaign run (metadata source "online_scoring") is excluded → treated as no verdict
    - a legit offline campaign run's completed verdict IS consulted
  </behavior>
  <action>
    Add tests seeding PromptTemplate + EvalRun rows (via the eval context / factories) for each <behavior> case. Attach a :telemetry handler to assert the :ungated event with prompt_template_id metadata. Toggle `require_eval_verdict` via Application.put_env in a setup/on_exit (restore original). Seed one online-scoring campaign run (campaign metadata source "online_scoring") and assert it does NOT satisfy the gate, plus a legit offline campaign run that does. Assert the tagged tuple carries the specific verdict string. Keep everything deterministic DB fixtures — no live LLM.
  </action>
  <verify>
    <automated>mix test test/scoria/runtime/release_gate_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - All <behavior> cases assert and pass, including the :ungated telemetry event and the online-exclusion vs offline-inclusion pair.
    - The allowlist is proven (only "passed" → :ok; unknown/other completed verdict → error).
  </acceptance_criteria>
  <done>release_gate_test.exs proves the verdict consult, allowlist, online exclusion, telemetry, and strict opt-in deterministically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime request → release gate | The gate is the only place a verdict blocks execution (runtime.ex:30); a wrong allow ships an ungoverned prompt. |
| eval verdict lookup → gate decision | Online/fabricated runs or a rescued DB error can pollute or bypass the decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-04 | Denial of Service (self-outage bypass) | ReleaseGate lookup error handling | high | mitigate | DB/query errors propagate; no rescue-to-allow. Test asserts a lookup failure does not resolve to :ok. |
| T-42-17 | Elevation (block-list bypass) | verdict consult | high | mitigate | Allowlist via Verdict.blocks_release?/1 — only "passed" passes; any other completed verdict → {:error, {:eval_not_passing, verdict}}; test proves an unexpected verdict string is blocked. |
| T-42-18 | Tampering (online run pollution) | latest-run lookup filter | high | mitigate | Exclude campaign.metadata source "online_scoring"; test seeds an online run and asserts it does not satisfy the gate while an offline campaign run does. |
| T-42-19 | Repudiation (silent ungoverned pass) | no-verdict default | medium | mitigate | :ok default emits [:scoria, :release_gate, :ungated] telemetry (inspectable) + opt-in :require_eval_verdict for a hard block. |
</threat_model>

<verification>
- `mix test test/scoria/runtime/release_gate_test.exs --warnings-as-errors` passes; `mix ecto.migrate`/`rollback` clean.
- No live-LLM/API keys.
</verification>

<success_criteria>
The release gate blocks on a non-passing verdict via an allowlist, excludes online runs, propagates DB errors (no fake-allow), and makes ungoverned prompts inspectable via telemetry with an opt-in hard block.
</success_criteria>

<artifacts_produced>
- release_gate.ex verdict consult (allowlist, tagged `{:eval_not_passing, verdict}`, `:eval_required`)
- `config :scoria, :require_eval_verdict` (default false)
- `[:scoria, :release_gate, :ungated]` telemetry event
- composite index `ai_eval_runs (prompt_template_id, status, inserted_at DESC)` migration
</artifacts_produced>

<output>
Create `.planning/phases/42-eval-fails-closed/42-07-SUMMARY.md` when done.
</output>
