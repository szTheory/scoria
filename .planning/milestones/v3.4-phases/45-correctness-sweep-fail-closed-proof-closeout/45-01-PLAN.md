---
phase: 45-correctness-sweep-fail-closed-proof-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/knowledge/backends/pgvector.ex
  - test/scoria/knowledge/pgvector_test.exs
  - test/scoria/knowledge/retrieval_test.exs
autonomous: true
requirements:
  - FIX-01
user_setup: []
must_haves:
  truths:
    - "FIX-01: Pgvector retrieval results persist raw cosine similarity as score = 1 - cosine_distance."
    - "FIX-01: Tenant visibility remains applied before source filters, vector ordering, and result limiting."
    - "FIX-01: Nil-embedding chunks are not returned with fabricated score evidence."
    - "FIX-01: Knowledge.retrieve/2 persists the backend-projected cosine score unchanged."
  artifacts:
    - path: "lib/scoria/knowledge/backends/pgvector.ex"
      provides: "Tenant-filtered pgvector retrieval with DB-projected raw cosine score"
      exports: ["similar_chunks/2"]
    - path: "test/scoria/knowledge/pgvector_test.exs"
      provides: "Direct backend proof for exact/orthogonal/nil/dimension retrieval score behavior"
    - path: "test/scoria/knowledge/retrieval_test.exs"
      provides: "Knowledge.retrieve/2 persisted score proof"
  key_links:
    - from: "lib/scoria/knowledge/backends/pgvector.ex"
      to: "lib/scoria/knowledge/scope.ex"
      via: "Scope.visible_to(scope) before source filtering and cosine ordering"
      pattern: "Scope.visible_to"
    - from: "lib/scoria/knowledge/backends/pgvector.ex"
      to: "lib/scoria/knowledge/retrieval_result.ex"
      via: "similar_chunks/2 returns score consumed by Knowledge.append_retrieval_results/2"
      pattern: "score:"
---

<objective>
Repair FIX-01 by making Pgvector retrieval score evidence use the same cosine metric as ranking.

Purpose: Retrieval evidence must stop reporting fabricated component-sum numbers while preserving the Phase 43 tenant-isolation query boundary.
Output: Pgvector backend score projection plus focused backend and persisted-result tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-CONTEXT.md
@.planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-RESEARCH.md
@.planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-PATTERNS.md
@.planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-VALIDATION.md
@.planning/phases/43-knowledge-tenant-isolation/43-04-SUMMARY.md
@.planning/phases/43-knowledge-tenant-isolation/43-05-SUMMARY.md
@lib/scoria/knowledge/backends/pgvector.ex
@lib/scoria/knowledge.ex
@lib/scoria/knowledge/scope.ex
@lib/scoria/knowledge/retrieval_result.ex
@test/scoria/knowledge/pgvector_test.exs
@test/scoria/knowledge/retrieval_test.exs
</context>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

Symbols created by this plan:
- No new public modules, functions, CLI flags, schema fields, or file paths.

Existing symbols changed:
- `Scoria.Knowledge.Backends.Pgvector.similar_chunks/2` returns backend results whose `:score` is raw DB-projected cosine similarity.

Symbols removed from active code:
- The private component-sum retrieval score helper in `lib/scoria/knowledge/backends/pgvector.ex`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project Pgvector cosine scores from the DB ranking metric</name>
  <files>lib/scoria/knowledge/backends/pgvector.ex, test/scoria/knowledge/pgvector_test.exs</files>
  <read_first>
    - .planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-CONTEXT.md
    - .planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-PATTERNS.md
    - lib/scoria/knowledge/backends/pgvector.ex
    - lib/scoria/knowledge/scope.ex
    - test/scoria/knowledge/pgvector_test.exs
  </read_first>
  <behavior>
    - Exact vector match: direct `Pgvector.similar_chunks/2` returns score `1.0` within a small float delta.
    - Orthogonal vectors: direct backend results persist raw cosine similarity near `0.0`, not a component-sum surrogate.
    - Nil embeddings: rows with `embedding` nil are excluded before ranking and never returned with score `0.0`.
    - Tenant isolation: a closer foreign-tenant chunk remains excluded because `Scope.visible_to(scope)` runs before ordering and limit.
    - Invalid vector/dimension mismatch: backend call fails loudly and does not fabricate result rows.
  </behavior>
  <action>Add failing backend tests first for FIX-01. Then update `Scoria.Knowledge.Backends.Pgvector.similar_chunks/2` so it builds one `query_vector = Pgvector.new(query_embedding)`, applies `Scope.visible_to(scope)`, applies `maybe_filter_source(source_id)`, filters `not is_nil(chunk.embedding)`, orders by `cosine_distance(chunk.embedding, ^query_vector)`, and selects each row with a DB-derived score equal to `1.0 - cosine_distance(chunk.embedding, ^query_vector)`. Map the selected row into the existing backend result shape with `chunk_id`, `source_id`, `body`, `metadata`, `rank`, and `score`. Do not clamp, round, normalize, or relabel the score as confidence. Preserve direct-backend missing-scope raises from Phase 43.</action>
  <acceptance_criteria>
    - `test/scoria/knowledge/pgvector_test.exs` contains assertions for exact-match score, orthogonal score, nil-embedding exclusion, dimension/invalid-vector loud failure, and closer foreign-tenant exclusion.
    - `lib/scoria/knowledge/backends/pgvector.ex` contains `Scope.visible_to(scope)` before `order_by`.
    - `lib/scoria/knowledge/backends/pgvector.ex` contains `not is_nil(chunk.embedding)` in the active retrieval query.
    - `lib/scoria/knowledge/backends/pgvector.ex` no longer contains the component-sum score formula, `Enum.sum` score math, or a nil-embedding fake score clause.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test --include knowledge test/scoria/knowledge/pgvector_test.exs --warnings-as-errors</automated>
  </verify>
  <done>FIX-01 direct Pgvector backend proof passes and backend scores come from the same cosine-distance metric used for ranking.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove Knowledge.retrieve persists the projected cosine score</name>
  <files>test/scoria/knowledge/retrieval_test.exs, lib/scoria/knowledge/backends/pgvector.ex</files>
  <read_first>
    - .planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-CONTEXT.md
    - .planning/phases/43-knowledge-tenant-isolation/43-03-SUMMARY.md
    - .planning/phases/43-knowledge-tenant-isolation/43-04-SUMMARY.md
    - lib/scoria/knowledge.ex
    - lib/scoria/knowledge/backends/pgvector.ex
    - lib/scoria/knowledge/retrieval_result.ex
    - test/scoria/knowledge/retrieval_test.exs
  </read_first>
  <behavior>
    - `Knowledge.retrieve/2` with Pgvector backend persists `RetrievalResult.score` equal to the backend result score for the returned chunk.
    - Persisted result rank remains consistent with cosine-distance ordering.
    - Retrieval run and retrieval result tenant/actor evidence from Phase 43 remain populated from normalized scope.
  </behavior>
  <action>Add a focused FIX-01 integration test in `test/scoria/knowledge/retrieval_test.exs` that inserts scoped source/chunk fixtures with known embeddings, calls `Knowledge.retrieve/2` with the real `Scoria.Knowledge.Backends.Pgvector` backend and explicit tenant scope, and reloads persisted `Scoria.Knowledge.RetrievalResult` rows. Assert the persisted score equals the expected raw cosine similarity within a small delta and that tenant_id/actor_id audit fields still match the normalized scope. Keep custom backend-spy tests intact.</action>
  <acceptance_criteria>
    - `test/scoria/knowledge/retrieval_test.exs` proves persisted `RetrievalResult.score` equals raw cosine similarity from the Pgvector path.
    - Existing retrieval scope propagation and audit tests still pass.
    - No test requires a live LLM, external API key, or a non-local provider.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test --include knowledge test/scoria/knowledge/pgvector_test.exs test/scoria/knowledge/retrieval_test.exs --warnings-as-errors</automated>
  </verify>
  <done>FIX-01 is proven through both direct backend results and persisted `Knowledge.retrieve/2` retrieval result evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> Pgvector backend | Query vectors, filters, and scope options enter the retrieval backend. |
| backend -> database | Pgvector query ranks tenant-visible chunks and projects persisted score evidence. |
| backend result -> RetrievalResult | Backend score evidence crosses into persisted retrieval audit rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Information Disclosure | `Pgvector.similar_chunks/2` | mitigate | Keep `Scope.visible_to(scope)` before source filters, nil-embedding filtering, cosine ordering, and limit; prove closer foreign-tenant rows are excluded. |
| T-45-02 | Tampering / Repudiation | Retrieval score evidence | mitigate | Project `score = 1 - cosine_distance` from the DB query and test persisted values instead of recomputing unrelated score math. |
| T-45-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install tasks exist in this plan; use existing locked dependencies only. |
</threat_model>

<verification>
Run the task-level focused knowledge commands, then include this plan in the final phase gate:

<automated>MIX_ENV=test mix test --include knowledge test/scoria/knowledge/pgvector_test.exs test/scoria/knowledge/retrieval_test.exs --warnings-as-errors</automated>
</verification>

<success_criteria>
- FIX-01 backend and persisted retrieval tests pass.
- Active Pgvector retrieval no longer contains component-sum scoring or nil-embedding fake score behavior.
- Phase 43 tenant isolation remains intact in the Pgvector score path.
</success_criteria>

<output>
Create `.planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-01-SUMMARY.md` when done.
</output>
