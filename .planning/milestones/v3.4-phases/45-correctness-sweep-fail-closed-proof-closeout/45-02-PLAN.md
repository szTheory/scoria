---
phase: 45-correctness-sweep-fail-closed-proof-closeout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/knowledge/grounding.ex
  - lib/scoria/knowledge/chunker.ex
  - test/scoria/knowledge/grounding_test.exs
  - test/scoria/knowledge_test.exs
autonomous: true
requirements:
  - FIX-02
  - FIX-03
user_setup: []
must_haves:
  truths:
    - "FIX-02: Citation presence scoring respects explicit expected_answerable labels."
    - "FIX-02: Missing answerability labels preserve legacy empty-citation failure behavior."
    - "FIX-03: Chunker.Default is documented and implemented as section/paragraph based and non-overlapping."
    - "FIX-03: Repeat ingest preserves deterministic offsets and chunk digests."
  artifacts:
    - path: "lib/scoria/knowledge/grounding.ex"
      provides: "Label-aware deterministic citation-presence scoring"
      exports: ["score_citation_presence/1"]
    - path: "lib/scoria/knowledge/chunker.ex"
      provides: "Default non-overlapping chunk offset generation"
    - path: "test/scoria/knowledge/grounding_test.exs"
      provides: "Answerability matrix proof"
    - path: "test/scoria/knowledge_test.exs"
      provides: "Default chunker non-overlap and repeat-ingest stability proof"
  key_links:
    - from: "lib/scoria/knowledge/grounding.ex"
      to: "lib/scoria/knowledge/grounding_score.ex"
      via: "score_citation_presence/1 details persist through existing GroundingScore sink"
      pattern: "expected_answerable"
    - from: "lib/scoria/knowledge/chunker.ex"
      to: "lib/scoria/knowledge.ex"
      via: "Knowledge.ingest_source/2 consumes Chunker.Default offsets and chunk_digest values"
      pattern: "end_offset"
---

<objective>
Repair FIX-02 and FIX-03 without widening retrieval-eval scope.

Purpose: Citation-presence checks must stop penalizing explicit correct abstention, and the default chunker must stop advertising a fake overlap option while preserving existing offset and digest behavior.
Output: Grounding answerability-label logic, non-overlap chunker cleanup, and focused tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-CONTEXT.md
@.planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-RESEARCH.md
@.planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-PATTERNS.md
@.planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-VALIDATION.md
@.planning/phases/43-knowledge-tenant-isolation/43-05-SUMMARY.md
@lib/scoria/knowledge/grounding.ex
@lib/scoria/knowledge/chunker.ex
@lib/scoria/knowledge.ex
@test/scoria/knowledge/grounding_test.exs
@test/scoria/knowledge_test.exs
</context>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

Symbols created by this plan:
- No new public modules, functions, CLI flags, schema fields, or file paths.

Existing symbols changed:
- `Scoria.Knowledge.Grounding.score_citation_presence/1` accepts `expected_answerable` and `answerable` labels with atom or string keys.
- `Scoria.Knowledge.Chunker.Default.chunk/2` advances each chunk start to the previous `end_offset`.

Symbols removed from active code:
- The dead default-chunker overlap option read and no-op next-offset expression.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make citation presence aware of explicit answerability labels</name>
  <files>lib/scoria/knowledge/grounding.ex, test/scoria/knowledge/grounding_test.exs</files>
  <read_first>
    - .planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-CONTEXT.md
    - .planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-PATTERNS.md
    - lib/scoria/knowledge/grounding.ex
    - lib/scoria/knowledge/grounding_score.ex
    - test/scoria/knowledge/grounding_test.exs
  </read_first>
  <behavior>
    - `%{expected_answerable: true, citations: [_]}` returns `%{status: "passed", score: 1.0}`.
    - `%{expected_answerable: true, citations: []}` returns `%{status: "failed", score: 0.0}`.
    - `%{expected_answerable: false, citations: []}` returns `%{status: "passed", score: 1.0}`.
    - `%{expected_answerable: false, citations: [_]}` returns `%{status: "failed", score: 0.0}`.
    - String-key `"expected_answerable"` and compatibility alias `"answerable"` behave the same as atom keys for boolean labels.
    - Missing label with empty citations preserves legacy failure.
  </behavior>
  <action>Add failing tests for the complete FIX-02 answerability matrix, string-key support, alias support, and missing-label legacy behavior. Then update `Scoria.Knowledge.Grounding.score_citation_presence/1` to normalize only strict boolean `expected_answerable` and `answerable` labels from atom or string keys. Include `details.count` for every result and include `details.expected_answerable` only when a boolean label is present. Do not parse natural-language refusal text, introduce semantic answer scoring, or emit `not_scored` from citation-presence scoring.</action>
  <acceptance_criteria>
    - `test/scoria/knowledge/grounding_test.exs` contains the six required matrix/compatibility cases.
    - Explicit unanswerable with zero citations passes with score `1.0`.
    - Explicit unanswerable with citations fails with score `0.0`.
    - Missing answerability label with zero citations still fails.
    - Result details contain citation count and canonical expected answerability when present.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test --include knowledge test/scoria/knowledge/grounding_test.exs --warnings-as-errors</automated>
  </verify>
  <done>FIX-02 label-aware citation-presence behavior is implemented and proven without schema changes or live providers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove Chunker.Default fake overlap and lock non-overlap proof</name>
  <files>lib/scoria/knowledge/chunker.ex, test/scoria/knowledge_test.exs</files>
  <read_first>
    - .planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-CONTEXT.md
    - .planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-PATTERNS.md
    - lib/scoria/knowledge/chunker.ex
    - lib/scoria/knowledge.ex
    - test/scoria/knowledge_test.exs
  </read_first>
  <behavior>
    - Adjacent default chunks are monotonic and non-overlapping: each `next.start_offset >= previous.end_offset`.
    - Passing `overlap:` to `Chunker.Default.chunk/2` does not create overlapping spans or change chunk digests.
    - Repeat ingest produces stable `chunk_digest`, `start_offset`, and `end_offset` values.
  </behavior>
  <action>Add failing tests in `test/scoria/knowledge_test.exs` for non-overlapping offsets and repeat-ingest digest/offset stability when callers pass an `overlap:` option. Then update `Scoria.Knowledge.Chunker.Default` documentation to state that default chunking is section/paragraph based and non-overlapping. Remove the dead overlap option read and set each next offset directly to `chunk.end_offset`. Preserve existing deterministic digest generation and existing callers that pass extra opts.</action>
  <acceptance_criteria>
    - `lib/scoria/knowledge/chunker.ex` documentation contains the phrase `non-overlapping`.
    - Active chunker code no longer reads `:overlap`.
    - Active chunker code no longer computes a max between reduced end offset and end offset.
    - `test/scoria/knowledge_test.exs` proves monotonic non-overlap and repeat-ingest digest/offset stability.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test --include knowledge test/scoria/knowledge_test.exs test/scoria/knowledge/grounding_test.exs --warnings-as-errors</automated>
  </verify>
  <done>FIX-03 removes the fake overlap implementation path while preserving default chunker determinism.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scorer payload -> Grounding | Caller-supplied citations and labels enter deterministic scoring. |
| source body -> Chunker.Default | Host/source text becomes persisted chunk offsets and digests. |
| chunk offsets -> citations | Chunk offsets feed future citation anchors and proof evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-03 | Tampering | `score_citation_presence/1` | mitigate | Accept only explicit boolean answerability labels; missing labels keep legacy empty-citation failure. |
| T-45-04 | Repudiation | Grounding score details | mitigate | Persist operator-readable `count` and canonical `expected_answerable` evidence in score details. |
| T-45-05 | Tampering | `Chunker.Default.chunk/2` | mitigate | Remove the fake overlap read and prove offsets/digests remain deterministic and non-overlapping. |
| T-45-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install tasks exist in this plan; use existing locked dependencies only. |
</threat_model>

<verification>
Run the task-level focused commands, then include this plan in the final phase gate:

<automated>MIX_ENV=test mix test --include knowledge test/scoria/knowledge/grounding_test.exs test/scoria/knowledge_test.exs --warnings-as-errors</automated>
</verification>

<success_criteria>
- FIX-02 answerability matrix passes and missing-label behavior remains fail-closed.
- FIX-03 non-overlap cleanup passes and repeat-ingest digest/offset stability is preserved.
- No overlap behavior, warning path, or new chunker module is introduced by this plan.
</success_criteria>

<output>
Create `.planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-02-SUMMARY.md` when done.
</output>
