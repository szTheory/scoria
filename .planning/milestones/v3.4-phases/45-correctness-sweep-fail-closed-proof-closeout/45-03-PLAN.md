---
phase: 45-correctness-sweep-fail-closed-proof-closeout
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scoria/eval/timing.ex
  - lib/scoria/eval/verdict.ex
  - lib/scoria/eval/runner.ex
  - lib/scoria/eval/judge_runner.ex
  - test/scoria/eval/timing_test.exs
  - test/scoria/eval/verdict_test.exs
  - test/scoria/eval/offline_runner_test.exs
  - test/scoria/eval/judge_runner_test.exs
autonomous: true
requirements:
  - FIX-04
user_setup: []
must_haves:
  truths:
    - "FIX-04: max_latency_ms is a score-level gate over recorded score metadata, not whole-run duration."
    - "FIX-04: Missing or unparseable scored-item latency under configured max_latency_ms returns :inconclusive."
    - "FIX-04: Offline runner scores record measured latency metadata instead of hardcoded zero."
    - "FIX-04: Judge runner scores record measured latency metadata through the injected no-live-LLM seam."
    - "FIX-04: EvalRun.duration_ms is measured from whole-run monotonic duration for offline and judge paths."
  artifacts:
    - path: "lib/scoria/eval/timing.ex"
      provides: "Internal monotonic timing helper"
      exports: ["Scoria.Eval.Timing.mark/0", "Scoria.Eval.Timing.elapsed_ms/1", "Scoria.Eval.Timing.measure/1"]
    - path: "lib/scoria/eval/verdict.ex"
      provides: "Fail-closed latency gate semantics"
      exports: ["compute/2"]
    - path: "lib/scoria/eval/runner.ex"
      provides: "Offline score latency and run duration measurement"
    - path: "lib/scoria/eval/judge_runner.ex"
      provides: "Judge score latency and run duration measurement"
  key_links:
    - from: "lib/scoria/eval/runner.ex"
      to: "lib/scoria/eval/timing.ex"
      via: "offline score and run duration measurement"
      pattern: "Scoria.Eval.Timing"
    - from: "lib/scoria/eval/judge_runner.ex"
      to: "lib/scoria/eval/timing.ex"
      via: "judge seam latency and run duration measurement"
      pattern: "Timing.measure"
    - from: "lib/scoria/eval/verdict.ex"
      to: "lib/scoria/eval/score.ex"
      via: "metadata[latency_ms] parsed from scored items"
      pattern: "max_latency_ms"
---

<objective>
Repair the shared FIX-04 latency policy and the offline/judge score writers.

Purpose: Phase 42 made eval verdicts fail closed, but the latency gate still reads fabricated zeroes. This plan creates the monotonic timing helper, makes configured missing latency inconclusive, and records real latency for offline and judge scores.
Output: Internal timing helper, verdict latency policy repair, offline runner timing, judge runner timing, and focused tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-CONTEXT.md
@.planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-RESEARCH.md
@.planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-PATTERNS.md
@.planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-VALIDATION.md
@.planning/phases/42-eval-fails-closed/42-01-SUMMARY.md
@.planning/phases/42-eval-fails-closed/42-04-SUMMARY.md
@.planning/phases/42-eval-fails-closed/42-05-SUMMARY.md
@lib/scoria/eval/verdict.ex
@lib/scoria/eval/runner.ex
@lib/scoria/eval/judge_runner.ex
@lib/scoria/eval/scorers/exact_match.ex
@test/scoria/eval/verdict_test.exs
@test/scoria/eval/offline_runner_test.exs
@test/scoria/eval/judge_runner_test.exs
</context>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

Symbols created by this plan:
- `Scoria.Eval.Timing` in `lib/scoria/eval/timing.ex`.
- `Scoria.Eval.Timing.mark/0`.
- `Scoria.Eval.Timing.elapsed_ms/1`.
- `Scoria.Eval.Timing.measure/1`.
- `Scoria.Eval.TimingTest` in `test/scoria/eval/timing_test.exs`.

Existing symbols changed:
- `Scoria.Eval.Verdict.compute/2` treats configured missing or invalid score latency as `:inconclusive`.
- `Scoria.Eval.Runner.run/2` and score-recording internals persist measured latency and run duration.
- `Scoria.Eval.JudgeRunner.run_live/1` and `Scoria.Eval.JudgeRunner.run_existing/2` persist measured latency and run duration.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add monotonic timing helper and fail-closed latency verdicts</name>
  <files>lib/scoria/eval/timing.ex, lib/scoria/eval/verdict.ex, test/scoria/eval/timing_test.exs, test/scoria/eval/verdict_test.exs</files>
  <read_first>
    - .planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-CONTEXT.md
    - .planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-PATTERNS.md
    - .planning/phases/42-eval-fails-closed/42-01-SUMMARY.md
    - lib/scoria/eval/verdict.ex
    - lib/scoria/mcp/executor.ex
    - test/scoria/eval/verdict_test.exs
  </read_first>
  <behavior>
    - `Scoria.Eval.Timing.measure(fn -> value end)` returns `{value, latency_ms}` where latency is a non-negative integer from monotonic native time.
    - `Scoria.Eval.Timing.mark/0` and `elapsed_ms/1` support whole-run duration measurement.
    - `Verdict.compute/2` returns `:failed` when any scored item has parseable latency greater than configured `max_latency_ms`.
    - `Verdict.compute/2` returns `:inconclusive` when `max_latency_ms` is configured and any scored item lacks parseable latency metadata.
    - `Verdict.compute/2` preserves existing pass behavior when no latency policy is configured.
  </behavior>
  <action>Add failing tests for the `Scoria.Eval.Timing` helper and for verdict over-threshold, missing-latency, invalid-latency, and no-latency-policy cases. Create `lib/scoria/eval/timing.ex` with `mark/0`, `elapsed_ms/1`, and `measure/1`, using `System.monotonic_time()` in native units plus `System.convert_time_unit(duration, :native, :millisecond)`. Update `Scoria.Eval.Verdict.compute/2` so policy maps containing `max_latency_ms` require every scored item to have an integer or parseable integer-string latency value. Missing or invalid latency under that configured policy returns `:inconclusive`; parseable latency above the threshold returns `:failed`; absent latency policy does not change existing pass-rate and score behavior.</action>
  <acceptance_criteria>
    - `lib/scoria/eval/timing.ex` defines `Scoria.Eval.Timing.mark/0`, `elapsed_ms/1`, and `measure/1`.
    - `lib/scoria/eval/timing.ex` uses `System.monotonic_time` and `System.convert_time_unit`.
    - `test/scoria/eval/verdict_test.exs` covers over-threshold latency, missing latency, invalid latency, and no-latency-policy behavior.
    - `Verdict.compute/2` does not coerce configured missing latency to zero.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/eval/timing_test.exs test/scoria/eval/verdict_test.exs --warnings-as-errors</automated>
  </verify>
  <done>FIX-04 verdict semantics fail closed for missing configured latency and the shared timing helper exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Record measured latency in the offline runner</name>
  <files>lib/scoria/eval/runner.ex, test/scoria/eval/offline_runner_test.exs</files>
  <read_first>
    - .planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-CONTEXT.md
    - .planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-PATTERNS.md
    - .planning/phases/42-eval-fails-closed/42-04-SUMMARY.md
    - lib/scoria/eval/timing.ex
    - lib/scoria/eval/runner.ex
    - lib/scoria/eval/subject_output.ex
    - lib/scoria/eval/scorers/exact_match.ex
    - test/scoria/eval/offline_runner_test.exs
  </read_first>
  <behavior>
    - Offline exact-match scored rows persist `metadata["latency_ms"]` as a measured non-negative integer.
    - Offline exact-match scored rows keep `metadata["cost_usd"] == "0.0"`.
    - Completed offline eval runs persist `duration_ms` from whole-run monotonic duration.
    - `threshold_verdict` still comes from `Scoria.Eval.Verdict.compute/2`.
  </behavior>
  <action>Add failing offline-runner assertions that score metadata contains parseable measured latency and completed eval runs no longer persist a literal zero duration by construction. Update `Scoria.Eval.Runner` to alias or fully qualify `Scoria.Eval.Timing`. Mark run start before work begins, wrap each actual scorer dispatch with `Timing.measure/1`, write the measured value to `metadata["latency_ms"]`, and complete the run with `duration_ms: Timing.elapsed_ms(run_started_at)`. Preserve Phase 42 `SubjectOutput.resolve/2`, ExactMatch scorer dispatch, `not_scored` handling, and `Verdict.compute/2` threshold verdict flow.</action>
  <acceptance_criteria>
    - Offline scored rows have integer `metadata["latency_ms"] >= 0` produced through `Timing.measure/1`.
    - `lib/scoria/eval/runner.ex` no longer contains a hardcoded score latency metadata value.
    - `lib/scoria/eval/runner.ex` no longer completes successful eval runs with literal `duration_ms: 0`.
    - Existing offline captured-output and exact-match fail-closed tests still pass.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/eval/offline_runner_test.exs test/scoria/eval/verdict_test.exs test/scoria/eval/timing_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Offline eval score latency and run duration are measured instead of fabricated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Record measured latency in the judge runner</name>
  <files>lib/scoria/eval/judge_runner.ex, test/scoria/eval/judge_runner_test.exs</files>
  <read_first>
    - .planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-CONTEXT.md
    - .planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-PATTERNS.md
    - .planning/phases/42-eval-fails-closed/42-05-SUMMARY.md
    - lib/scoria/eval/timing.ex
    - lib/scoria/eval/judge_runner.ex
    - lib/scoria/eval/subject_output.ex
    - test/scoria/eval/judge_runner_test.exs
  </read_first>
  <behavior>
    - Judge scored rows persist `metadata["latency_ms"]` as measured non-negative integer evidence.
    - Judge scored rows keep no-live-LLM test behavior through injected stub modules.
    - Empty-capture judge rows remain `not_scored` and do not call the judge seam.
    - Completed judge eval runs persist measured `duration_ms`.
  </behavior>
  <action>Add failing judge-runner assertions for score latency metadata and run duration using the existing stubbed judge seam. Update `Scoria.Eval.JudgeRunner` to use `Scoria.Eval.Timing` around `generate_object/4` calls and whole-run completion in both live and existing-run paths. Persist the measured latency in the passed/failed judge score metadata. Keep empty-capture skip behavior unchanged: unscoreable rows remain `not_scored`, skip the injected judge seam, and continue to flow through `Verdict.compute/2`.</action>
  <acceptance_criteria>
    - `test/scoria/eval/judge_runner_test.exs` proves latency metadata on judge-scored rows without live provider calls.
    - `lib/scoria/eval/judge_runner.ex` no longer writes hardcoded score latency metadata for judge-scored rows.
    - `lib/scoria/eval/judge_runner.ex` no longer completes successful judge eval runs with literal `duration_ms: 0`.
    - Existing no-self-grade, empty-capture, and no-live-LLM tests still pass.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/eval/judge_runner_test.exs test/scoria/eval/verdict_test.exs test/scoria/eval/timing_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Judge eval score latency and run duration are measured through the existing deterministic test seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| score metadata -> Verdict | Persisted score metadata controls `max_latency_ms` verdict behavior. |
| runner/judge seam -> Eval.Score | Scorer execution writes latency evidence into persisted score rows. |
| injected judge stub -> JudgeRunner | Test and host-injected judge modules return deterministic objects without live provider credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-06 | Tampering / Repudiation | `Scoria.Eval.Verdict` latency gate | mitigate | Parse latency explicitly and return `:inconclusive` for missing/invalid scored-item latency when `max_latency_ms` is configured. |
| T-45-07 | Tampering / Repudiation | `Scoria.Eval.Runner` score metadata | mitigate | Use monotonic timing around scorer dispatch and persist measured latency in score metadata. |
| T-45-08 | Spoofing / Tampering | `Scoria.Eval.JudgeRunner` injected seam | mitigate | Preserve no-live-LLM injected stubs in tests and measure only the actual seam call made by the runner. |
| T-45-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install tasks exist in this plan; use existing locked dependencies only. |
</threat_model>

<verification>
Run task-level focused tests and then the FIX-04 offline/judge subset:

<automated>mix test test/scoria/eval/timing_test.exs test/scoria/eval/verdict_test.exs test/scoria/eval/offline_runner_test.exs test/scoria/eval/judge_runner_test.exs --warnings-as-errors</automated>
</verification>

<success_criteria>
- FIX-04 verdict missing-latency semantics pass.
- Offline runner and judge runner persist measured latency metadata and measured run duration.
- No live LLM calls, sleeps, external providers, or broad eval feature work are introduced.
</success_criteria>

<output>
Create `.planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-03-SUMMARY.md` when done.
</output>
