---
phase: 45-correctness-sweep-fail-closed-proof-closeout
plan: 04
type: execute
wave: 2
depends_on:
  - 45-03
files_modified:
  - lib/scoria/eval/online_scoring.ex
  - test/scoria/eval/online_scoring_test.exs
autonomous: true
requirements:
  - FIX-04
user_setup: []
must_haves:
  truths:
    - "FIX-04: Online deterministic score rows persist measured latency metadata."
    - "FIX-04: Online scoring completion uses measured EvalRun.duration_ms instead of fabricated zero."
    - "FIX-04: Clean online traces still do not produce fabricated deterministic pass rows."
    - "FIX-04: Judge-backed online scoring relies on the measured judge path from 45-03."
  artifacts:
    - path: "lib/scoria/eval/online_scoring.ex"
      provides: "Measured online deterministic score latency and run duration"
    - path: "test/scoria/eval/online_scoring_test.exs"
      provides: "Online latency and no-fabricated-pass regression proof"
  key_links:
    - from: "lib/scoria/eval/online_scoring.ex"
      to: "lib/scoria/eval/timing.ex"
      via: "online scoring latency and duration measurement"
      pattern: "Scoria.Eval.Timing"
    - from: "lib/scoria/eval/online_scoring.ex"
      to: "lib/scoria/eval/judge_runner.ex"
      via: "clean online traces use judge path rather than deterministic pass evidence"
      pattern: "JudgeRunner"
---

<objective>
Complete FIX-04 for online scoring after the shared eval timing helper exists.

Purpose: Online scoring was part of the hardcoded-latency bug. This plan updates online deterministic evidence and completion duration while preserving the Phase 42 rule that clean traces do not create fabricated pass rows.
Output: Online scoring measured latency/duration repair and focused tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-CONTEXT.md
@.planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-RESEARCH.md
@.planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-PATTERNS.md
@.planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-VALIDATION.md
@.planning/phases/42-eval-fails-closed/42-06-SUMMARY.md
@.planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-03-SUMMARY.md
@lib/scoria/eval/timing.ex
@lib/scoria/eval/online_scoring.ex
@lib/scoria/eval/judge_runner.ex
@test/scoria/eval/online_scoring_test.exs
</context>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

Symbols created by this plan:
- No new public modules, functions, CLI flags, schema fields, or file paths.

Existing symbols changed:
- `Scoria.Eval.OnlineScoring` deterministic score and completion paths consume `Scoria.Eval.Timing`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Measure online scoring latency and duration</name>
  <files>lib/scoria/eval/online_scoring.ex, test/scoria/eval/online_scoring_test.exs</files>
  <read_first>
    - .planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-CONTEXT.md
    - .planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-PATTERNS.md
    - .planning/phases/42-eval-fails-closed/42-06-SUMMARY.md
    - .planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-03-SUMMARY.md
    - lib/scoria/eval/timing.ex
    - lib/scoria/eval/online_scoring.ex
    - lib/scoria/eval/judge_runner.ex
    - test/scoria/eval/online_scoring_test.exs
  </read_first>
  <behavior>
    - Negative-signal deterministic online score rows persist integer `metadata["latency_ms"] >= 0`.
    - Online completion paths persist `EvalRun.duration_ms` from monotonic duration.
    - Clean online traces without judge capture still produce no deterministic pass rows.
    - Clean online traces with judge capture still use `JudgeRunner` and therefore measured judge latency from Plan 45-03.
  </behavior>
  <action>Add failing online-scoring tests for deterministic negative-signal latency metadata and measured completion duration. Update `Scoria.Eval.OnlineScoring` to use `Scoria.Eval.Timing` for online run start/end duration and deterministic-rule score metadata. Preserve Phase 42 behavior: deterministic scoring remains a negative-signal detector only, clean traces do not persist deterministic passed rows, promotion candidates still require non-empty passing real score evidence, and judge-backed clean traces continue through `Scoria.Eval.JudgeRunner`.</action>
  <acceptance_criteria>
    - `test/scoria/eval/online_scoring_test.exs` proves deterministic negative-signal score metadata includes integer latency.
    - `test/scoria/eval/online_scoring_test.exs` proves completed online eval runs have measured duration metadata.
    - `lib/scoria/eval/online_scoring.ex` no longer writes hardcoded deterministic score latency metadata.
    - `lib/scoria/eval/online_scoring.ex` no longer completes online eval runs with literal `duration_ms: 0`.
    - Existing clean-trace no-deterministic-pass tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scoria/eval/online_scoring_test.exs test/scoria/eval/judge_runner_test.exs test/scoria/eval/verdict_test.exs test/scoria/eval/timing_test.exs --warnings-as-errors</automated>
  </verify>
  <done>FIX-04 online scoring latency and duration use measured evidence while preserving Phase 42 no-fabricated-pass semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime trace -> OnlineScoring | Trace/candidate evidence enters online scoring. |
| online scorer -> Eval.Score | Deterministic negative-signal score metadata is persisted. |
| online scoring -> JudgeRunner | Clean samples may delegate to the measured judge path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-09 | Tampering / Repudiation | `Scoria.Eval.OnlineScoring` score metadata | mitigate | Use `Scoria.Eval.Timing` for deterministic online score latency and test persisted metadata. |
| T-45-10 | Tampering | Online deterministic scoring | mitigate | Preserve Phase 42 negative-signal-only behavior; clean traces cannot create deterministic pass rows. |
| T-45-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install tasks exist in this plan; use existing locked dependencies only. |
</threat_model>

<verification>
Run the focused online command and include the full FIX-04 eval subset:

<automated>mix test test/scoria/eval/timing_test.exs test/scoria/eval/verdict_test.exs test/scoria/eval/offline_runner_test.exs test/scoria/eval/judge_runner_test.exs test/scoria/eval/online_scoring_test.exs --warnings-as-errors</automated>
</verification>

<success_criteria>
- FIX-04 online scoring tests pass.
- Online code consumes the shared timing helper from 45-03.
- No hardcoded online latency or duration zero remains in active online completion/scoring paths.
</success_criteria>

<output>
Create `.planning/phases/45-correctness-sweep-fail-closed-proof-closeout/45-04-SUMMARY.md` when done.
</output>
