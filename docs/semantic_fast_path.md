# Semantic Cache

Compatibility note: this old source path remains so copied GitHub links to
`docs/semantic_fast_path.md` keep working. The canonical guide is
`guides/capabilities/semantic-cache.md`.

Read the current guide: [Semantic Cache](../guides/capabilities/semantic-cache.md).

This page is compatibility material only. It is not the canonical ExDoc source
and is intentionally excluded from ExDoc extras so the public guide ladder stays
focused on the current `guides/` structure.

The old `semantic_fast_path` and semantic fast-path names are retained only as
compatibility context for 0.1.x-era links. New docs should use Semantic Cache
and `guides/capabilities/semantic-cache.md`.
