# Bounded Handoffs

Compatibility note: this old source path remains so copied GitHub links to
`docs/bounded_handoffs.md` keep working. The canonical guide is
`guides/capabilities/bounded-handoffs.md`.

Read the current guide: [Bounded Handoffs](../guides/capabilities/bounded-handoffs.md).

This page is compatibility material only. It is not the canonical ExDoc source
and is intentionally excluded from ExDoc extras so the public guide ladder stays
focused on the current `guides/` structure.

The old `bounded_handoffs` source name is retained only as compatibility
context for 0.1.x-era links. New docs should use the Bounded Handoffs guide
under `guides/capabilities/`.
