# Connectors and MCP

Compatibility note: this old source path remains so copied GitHub links to
`docs/connector_adoption.md` keep working. The canonical guide is
`guides/capabilities/connectors-and-mcp.md`.

Read the current guide: [Connectors and MCP](../guides/capabilities/connectors-and-mcp.md).

This page is compatibility material only. It is not the canonical ExDoc source
and is intentionally excluded from ExDoc extras so the public guide ladder stays
focused on the current `guides/` structure.

The old connector adoption source name is retained only as compatibility
context for 0.1.x-era links. New docs should use Connectors and MCP under
`guides/capabilities/`.
