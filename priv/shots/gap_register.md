# Scoria UI Gap Register

_Generated by `mix scoria.ui.shots --critique` — run at phase-milestone boundaries._

This file is updated by the critique pass (`mix scoria.ui.shots --critique`) and committed
as the baseline audit output (EVAL-03 / PROOF-03). Phase 05 populates the initial baseline.

## Status

**Baseline audit:** not yet run. Run `mix scoria.ui.shots` then `mix scoria.ui.shots --critique`
after applying `mix run priv/repo/dev_seed.exs` and starting `mix phx.server`.

## Known Issues (pre-audit)

| Screen | Dimension | Severity | Finding |
|--------|-----------|----------|---------|
| All screens with flash | consistency | high | `flash_tone_class/1` at `lib/scoria_web/ui.ex` uses raw palette classes (`border-rose-200 bg-rose-50 text-rose-900`) instead of semantic design-system tokens — DS-05 gap. Fix in Phase 12. |

---

_For harness prerequisites and usage, see `docs/MAINTAINERS.md` § Screenshot + Critique Harness._
